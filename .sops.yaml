# GENERATED by `kbb -M:sops:yaml` (etzhayyim.sops-age) — DO NOT hand-edit.
# SoT = the per-actor :rad/age-recipient on each kotoba-rad identity
# journal (80-data/kotoba-rad/<actor>.identity.journal.edn).
# No actor has onboarded a recipient yet. Run `kbb -M:sops:keygen <actor> --apply`
# then `kbb -M:actor:evolve <actor> --apply` to record :rad/age-recipient and
# regenerate this file. (encrypt always also passes --age explicitly.)
creation_rules: []
