CREATE TABLE IF NOT EXISTS vertex_gcal_account (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      account_did       VARCHAR,
      email             VARCHAR,
      display_name      VARCHAR,
      status            VARCHAR,
      scope             VARCHAR,
      sync_token        VARCHAR,
      last_sync_at      VARCHAR,
      connected_at      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gcal_calendar (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      calendar_id       VARCHAR,
      account_did       VARCHAR,
      email             VARCHAR,
      summary           VARCHAR,
      description       VARCHAR,
      timezone          VARCHAR,
      access_role       VARCHAR,
      primary_flag      VARCHAR,
      selected_flag     VARCHAR,
      color_id          VARCHAR,
      etag              VARCHAR,
      sync_token        VARCHAR,
      last_sync_at      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gcal_event (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      event_id          VARCHAR,
      ical_uid          VARCHAR,
      calendar_id       VARCHAR,
      account_did       VARCHAR,
      summary           VARCHAR,
      description       VARCHAR,
      location          VARCHAR,
      start_time        VARCHAR,
      end_time          VARCHAR,
      start_date        VARCHAR,
      end_date          VARCHAR,
      timezone          VARCHAR,
      all_day           VARCHAR,
      status            VARCHAR,
      visibility        VARCHAR,
      transparency      VARCHAR,
      recurrence        VARCHAR,
      recurring_event_id VARCHAR,
      organizer_email   VARCHAR,
      organizer_name    VARCHAR,
      creator_email     VARCHAR,
      hangout_link      VARCHAR,
      meet_uri          VARCHAR,
      conference_id     VARCHAR,
      attendees_json    VARCHAR,
      attachments_json  VARCHAR,
      reminders_json    VARCHAR,
      etag              VARCHAR,
      sequence          BIGINT,
      html_link         VARCHAR,
      created_time      VARCHAR,
      updated_time      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gcal_attendee (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      attendee_id       VARCHAR,
      event_id          VARCHAR,
      calendar_id       VARCHAR,
      email             VARCHAR,
      display_name      VARCHAR,
      response_status   VARCHAR,
      is_organizer      VARCHAR,
      is_optional       VARCHAR,
      is_resource       VARCHAR,
      comment           VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gcal_watch_channel (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      channel_id        VARCHAR,
      resource_id       VARCHAR,
      resource_uri      VARCHAR,
      calendar_id       VARCHAR,
      account_did       VARCHAR,
      kind              VARCHAR,
      token             VARCHAR,
      expiration        BIGINT,
      renew_at          VARCHAR,
      status            VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS edge_gcal_event_in_calendar (
      edge_id           VARCHAR PRIMARY KEY,
      src_vid           VARCHAR,
      dst_vid           VARCHAR,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      event_id          VARCHAR,
      calendar_id       VARCHAR,
      created_at        VARCHAR
    );

CREATE TABLE IF NOT EXISTS edge_gcal_event_attendee (
      edge_id           VARCHAR PRIMARY KEY,
      src_vid           VARCHAR,
      dst_vid           VARCHAR,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      event_id          VARCHAR,
      email             VARCHAR,
      response_status   VARCHAR,
      created_at        VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gdrive_account (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      account_did       VARCHAR,
      email             VARCHAR,
      display_name      VARCHAR,
      status            VARCHAR,
      scope             VARCHAR,
      quota_bytes_used  BIGINT,
      quota_bytes_limit BIGINT,
      start_page_token  VARCHAR,
      last_sync_at      VARCHAR,
      connected_at      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gdrive_file (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      file_id           VARCHAR,
      account_did       VARCHAR,
      name              VARCHAR,
      mime_type         VARCHAR,
      kind              VARCHAR,
      size_bytes        BIGINT,
      md5_checksum      VARCHAR,
      sha256_checksum   VARCHAR,
      description       VARCHAR,
      starred           VARCHAR,
      trashed           VARCHAR,
      explicitly_trashed VARCHAR,
      shared            VARCHAR,
      owners_json       VARCHAR,
      parents_json      VARCHAR,
      spaces_json       VARCHAR,
      web_view_link     VARCHAR,
      web_content_link  VARCHAR,
      icon_link         VARCHAR,
      thumbnail_link    VARCHAR,
      original_filename VARCHAR,
      file_extension    VARCHAR,
      full_file_extension VARCHAR,
      head_revision_id  VARCHAR,
      version_num       BIGINT,
      view_count        BIGINT,
      capabilities_json VARCHAR,
      export_links_json VARCHAR,
      drive_id          VARCHAR,
      team_drive_id     VARCHAR,
      created_time      VARCHAR,
      modified_time     VARCHAR,
      viewed_by_me_time VARCHAR,
      shared_with_me_time VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gdrive_permission (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      permission_id     VARCHAR,
      file_id           VARCHAR,
      account_did       VARCHAR,
      kind              VARCHAR,
      type              VARCHAR,
      role              VARCHAR,
      email             VARCHAR,
      display_name      VARCHAR,
      domain            VARCHAR,
      allow_file_discovery VARCHAR,
      deleted           VARCHAR,
      expiration_time   VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gdrive_revision (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      revision_id       VARCHAR,
      file_id           VARCHAR,
      account_did       VARCHAR,
      mime_type         VARCHAR,
      size_bytes        BIGINT,
      md5_checksum      VARCHAR,
      original_filename VARCHAR,
      keep_forever      VARCHAR,
      published         VARCHAR,
      publish_auto      VARCHAR,
      last_modifying_user VARCHAR,
      modified_time     VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gdrive_watch_channel (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      channel_id        VARCHAR,
      resource_id       VARCHAR,
      resource_uri      VARCHAR,
      account_did       VARCHAR,
      kind              VARCHAR,
      token             VARCHAR,
      expiration        BIGINT,
      renew_at          VARCHAR,
      status            VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS edge_gdrive_file_parent (
      edge_id           VARCHAR PRIMARY KEY,
      src_vid           VARCHAR,
      dst_vid           VARCHAR,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      file_id           VARCHAR,
      parent_file_id    VARCHAR,
      created_at        VARCHAR
    );

CREATE TABLE IF NOT EXISTS edge_gdrive_file_permission (
      edge_id           VARCHAR PRIMARY KEY,
      src_vid           VARCHAR,
      dst_vid           VARCHAR,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      file_id           VARCHAR,
      permission_id     VARCHAR,
      role              VARCHAR,
      created_at        VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gcontacts_account (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      account_did       VARCHAR,
      email             VARCHAR,
      display_name      VARCHAR,
      status            VARCHAR,
      scope             VARCHAR,
      sync_token        VARCHAR,
      last_sync_at      VARCHAR,
      connected_at      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gcontacts_contact (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      resource_name     VARCHAR,
      account_did       VARCHAR,
      etag              VARCHAR,
      display_name      VARCHAR,
      given_name        VARCHAR,
      family_name       VARCHAR,
      middle_name       VARCHAR,
      honorific_prefix  VARCHAR,
      honorific_suffix  VARCHAR,
      nickname          VARCHAR,
      emails_json       VARCHAR,
      phones_json       VARCHAR,
      addresses_json    VARCHAR,
      organizations_json VARCHAR,
      biographies_json  VARCHAR,
      birthdays_json    VARCHAR,
      urls_json         VARCHAR,
      photos_json       VARCHAR,
      memberships_json  VARCHAR,
      user_defined_json VARCHAR,
      metadata_json     VARCHAR,
      updated_time      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gcontacts_group (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      resource_name     VARCHAR,
      account_did       VARCHAR,
      etag              VARCHAR,
      name              VARCHAR,
      group_type        VARCHAR,
      formatted_name    VARCHAR,
      member_count      BIGINT,
      updated_time      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS edge_gcontacts_contact_group (
      edge_id           VARCHAR PRIMARY KEY,
      src_vid           VARCHAR,
      dst_vid           VARCHAR,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      contact_resource_name VARCHAR,
      group_resource_name VARCHAR,
      created_at        VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gtasks_account (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      account_did       VARCHAR,
      email             VARCHAR,
      display_name      VARCHAR,
      status            VARCHAR,
      scope             VARCHAR,
      last_sync_at      VARCHAR,
      connected_at      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gtasks_list (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      list_id           VARCHAR,
      account_did       VARCHAR,
      title             VARCHAR,
      etag              VARCHAR,
      self_link         VARCHAR,
      updated_time      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gtasks_task (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      task_id           VARCHAR,
      list_id           VARCHAR,
      account_did       VARCHAR,
      title             VARCHAR,
      notes             VARCHAR,
      status            VARCHAR,
      due_time          VARCHAR,
      completed_time    VARCHAR,
      parent_task_id    VARCHAR,
      position          VARCHAR,
      etag              VARCHAR,
      self_link         VARCHAR,
      web_view_link     VARCHAR,
      hidden            VARCHAR,
      deleted           VARCHAR,
      links_json        VARCHAR,
      updated_time      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS edge_gtasks_task_in_list (
      edge_id           VARCHAR PRIMARY KEY,
      src_vid           VARCHAR,
      dst_vid           VARCHAR,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      task_id           VARCHAR,
      list_id           VARCHAR,
      created_at        VARCHAR
    );

CREATE TABLE IF NOT EXISTS edge_gtasks_task_parent (
      edge_id           VARCHAR PRIMARY KEY,
      src_vid           VARCHAR,
      dst_vid           VARCHAR,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      task_id           VARCHAR,
      parent_task_id    VARCHAR,
      created_at        VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gdocs_account (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      account_did       VARCHAR,
      email             VARCHAR,
      display_name      VARCHAR,
      status            VARCHAR,
      scope             VARCHAR,
      last_sync_at      VARCHAR,
      connected_at      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gdocs_document (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      document_id       VARCHAR,
      account_did       VARCHAR,
      file_id           VARCHAR,
      title             VARCHAR,
      body_preview      VARCHAR,
      body_content_json VARCHAR,
      revision_id       VARCHAR,
      suggestions_view_mode VARCHAR,
      named_ranges_json VARCHAR,
      inline_objects_json VARCHAR,
      positioned_objects_json VARCHAR,
      lists_json        VARCHAR,
      headers_json      VARCHAR,
      footers_json      VARCHAR,
      footnotes_json    VARCHAR,
      doc_style_json    VARCHAR,
      word_count        BIGINT,
      char_count        BIGINT,
      page_count        BIGINT,
      updated_time      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gdocs_revision (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      revision_id       VARCHAR,
      document_id       VARCHAR,
      account_did       VARCHAR,
      last_modifying_user VARCHAR,
      mime_type         VARCHAR,
      modified_time     VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gsheets_account (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      account_did       VARCHAR,
      email             VARCHAR,
      display_name      VARCHAR,
      status            VARCHAR,
      scope             VARCHAR,
      last_sync_at      VARCHAR,
      connected_at      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gsheets_spreadsheet (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      spreadsheet_id    VARCHAR,
      account_did       VARCHAR,
      file_id           VARCHAR,
      title             VARCHAR,
      locale            VARCHAR,
      time_zone         VARCHAR,
      auto_recalc       VARCHAR,
      sheet_count       BIGINT,
      named_ranges_json VARCHAR,
      developer_metadata_json VARCHAR,
      spreadsheet_url   VARCHAR,
      updated_time      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gsheets_sheet (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      sheet_id          VARCHAR,
      spreadsheet_id    VARCHAR,
      account_did       VARCHAR,
      title             VARCHAR,
      sheet_type        VARCHAR,
      sheet_index       BIGINT,
      row_count         BIGINT,
      column_count      BIGINT,
      frozen_row_count  BIGINT,
      frozen_column_count BIGINT,
      hidden            VARCHAR,
      tab_color         VARCHAR,
      grid_values_json  VARCHAR,
      grid_values_preview VARCHAR,
      cell_count        BIGINT,
      data_bytes        BIGINT,
      charts_json       VARCHAR,
      protected_ranges_json VARCHAR,
      updated_time      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS edge_gsheets_sheet_in_spreadsheet (
      edge_id           VARCHAR PRIMARY KEY,
      src_vid           VARCHAR,
      dst_vid           VARCHAR,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      sheet_id          VARCHAR,
      spreadsheet_id    VARCHAR,
      sheet_index       BIGINT,
      created_at        VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gslides_account (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      account_did       VARCHAR,
      email             VARCHAR,
      display_name      VARCHAR,
      status            VARCHAR,
      scope             VARCHAR,
      last_sync_at      VARCHAR,
      connected_at      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gslides_presentation (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      presentation_id   VARCHAR,
      account_did       VARCHAR,
      file_id           VARCHAR,
      title             VARCHAR,
      locale            VARCHAR,
      revision_id       VARCHAR,
      slide_count       BIGINT,
      master_count      BIGINT,
      layout_count      BIGINT,
      page_width_emu    BIGINT,
      page_height_emu   BIGINT,
      notes_master_json VARCHAR,
      updated_time      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gslides_slide (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      slide_object_id   VARCHAR,
      presentation_id   VARCHAR,
      account_did       VARCHAR,
      slide_index       BIGINT,
      layout_object_id  VARCHAR,
      master_object_id  VARCHAR,
      notes_text        VARCHAR,
      page_elements_json VARCHAR,
      page_elements_preview VARCHAR,
      element_count     BIGINT,
      page_properties_json VARCHAR,
      updated_time      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS edge_gslides_slide_in_presentation (
      edge_id           VARCHAR PRIMARY KEY,
      src_vid           VARCHAR,
      dst_vid           VARCHAR,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      slide_object_id   VARCHAR,
      presentation_id   VARCHAR,
      slide_index       BIGINT,
      created_at        VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gmeet_account (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      account_did       VARCHAR,
      email             VARCHAR,
      display_name      VARCHAR,
      status            VARCHAR,
      scope             VARCHAR,
      last_sync_at      VARCHAR,
      connected_at      VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gmeet_conference (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      conference_id     VARCHAR,
      conference_name   VARCHAR,
      space_name        VARCHAR,
      meeting_code      VARCHAR,
      account_did       VARCHAR,
      organizer_email   VARCHAR,
      start_time        VARCHAR,
      end_time          VARCHAR,
      duration_seconds  BIGINT,
      calendar_event_id VARCHAR,
      entry_points_json VARCHAR,
      meet_uri          VARCHAR,
      recording_file_id VARCHAR,
      transcript_file_id VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gmeet_participant (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      participant_id    VARCHAR,
      conference_id     VARCHAR,
      account_did       VARCHAR,
      email             VARCHAR,
      display_name      VARCHAR,
      role              VARCHAR,
      join_time         VARCHAR,
      leave_time        VARCHAR,
      duration_seconds  BIGINT,
      device_type       VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS vertex_gmeet_recording (
      vertex_id         VARCHAR PRIMARY KEY,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      rkey              VARCHAR,
      repo              VARCHAR,
      recording_id      VARCHAR,
      conference_id     VARCHAR,
      account_did       VARCHAR,
      drive_file_id     VARCHAR,
      mime_type         VARCHAR,
      size_bytes        BIGINT,
      start_time        VARCHAR,
      end_time          VARCHAR,
      web_view_link     VARCHAR,
      export_uri        VARCHAR,
      transcript_file_id VARCHAR,
      transcript_body_preview VARCHAR,
      created_at        VARCHAR,
      org_id            VARCHAR,
      user_id           VARCHAR,
      actor_id          VARCHAR
    );

CREATE TABLE IF NOT EXISTS edge_gmeet_conference_participant (
      edge_id           VARCHAR PRIMARY KEY,
      src_vid           VARCHAR,
      dst_vid           VARCHAR,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      conference_id     VARCHAR,
      participant_id    VARCHAR,
      email             VARCHAR,
      created_at        VARCHAR
    );

CREATE TABLE IF NOT EXISTS edge_gmeet_conference_calendar_event (
      edge_id           VARCHAR PRIMARY KEY,
      src_vid           VARCHAR,
      dst_vid           VARCHAR,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      conference_id     VARCHAR,
      calendar_event_id VARCHAR,
      created_at        VARCHAR
    );

CREATE TABLE IF NOT EXISTS edge_gmeet_recording_drive_file (
      edge_id           VARCHAR PRIMARY KEY,
      src_vid           VARCHAR,
      dst_vid           VARCHAR,
      _seq              BIGINT,
      created_date      DATE,
      sensitivity_ord   BIGINT,
      owner_did         VARCHAR,
      recording_id      VARCHAR,
      drive_file_id     VARCHAR,
      created_at        VARCHAR
    );

CREATE MATERIALIZED VIEW IF NOT EXISTS mv_gworkspace_account_health AS
    SELECT
      'gmail' AS service,  account_did, email, status, last_sync_at
      FROM vertex_gmail_account
    UNION ALL SELECT 'calendar',  account_did, email, status, last_sync_at FROM vertex_gcal_account
    UNION ALL SELECT 'drive',     account_did, email, status, last_sync_at FROM vertex_gdrive_account
    UNION ALL SELECT 'contacts',  account_did, email, status, last_sync_at FROM vertex_gcontacts_account
    UNION ALL SELECT 'tasks',     account_did, email, status, last_sync_at FROM vertex_gtasks_account
    UNION ALL SELECT 'docs',      account_did, email, status, last_sync_at FROM vertex_gdocs_account
    UNION ALL SELECT 'sheets',    account_did, email, status, last_sync_at FROM vertex_gsheets_account
    UNION ALL SELECT 'slides',    account_did, email, status, last_sync_at FROM vertex_gslides_account
    UNION ALL SELECT 'meet',      account_did, email, status, last_sync_at FROM vertex_gmeet_account;
