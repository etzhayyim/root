{
  "name": "etzhayyim-signal-test-island",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Isolated test island for @etzhayyim/signal field-level encryption (fake-indexeddb + WebCrypto). Outside the pnpm workspace glob so it carries its own lock; does not touch the root pnpm-lock.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "vitest": "^4.1.8"
  }
}
