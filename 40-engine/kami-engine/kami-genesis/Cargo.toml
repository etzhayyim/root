[package]
name = "kami-genesis"
description = "Genesis (Apache-2.0, Genesis-Embodied-AI/Genesis) physics backend bind — Taichi → wgpu (WebGPU). 5 solvers: rigid / MPM / SPH / FEM / PBD. Isaac Sim articulation API-compat target. R1.0 path reservation per ADR-2605261800."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
log = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
