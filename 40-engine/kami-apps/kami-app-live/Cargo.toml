[package]
name = "kami-app-live"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

# kami-app-live — Reference WASM entry for live.etzhayyim.com.
#
# Composes:
#   kami-pipelines::{SkyAdapter, TerrainAdapter, WaterAdapter,
#                    AtlasVisAdapter, ParticleAdapter}
# with the pure-Rust `kami-live::LiveShow` orchestrator. No new GPU
# pipelines are introduced — the visual response (lasers, sparkles,
# crowd cheers, confetti) routes through the existing N1-N6 atlas /
# particle systems shared with isekai/quarry-walk.

[dependencies]
kami-app = { git = "https://github.com/etzhayyim/kami-engine.git", rev = "c1cff42cee8b9087bfad67931a916c5bfe087638" }
kami-pipelines = { git = "https://github.com/etzhayyim/kami-engine.git", rev = "c1cff42cee8b9087bfad67931a916c5bfe087638" }
kami-terrain = { git = "https://github.com/etzhayyim/kami-engine.git", rev = "c1cff42cee8b9087bfad67931a916c5bfe087638" }
kami-render = { git = "https://github.com/etzhayyim/kami-engine.git", rev = "c1cff42cee8b9087bfad67931a916c5bfe087638" }
kami-live = { git = "https://github.com/etzhayyim/kami-engine.git", rev = "c1cff42cee8b9087bfad67931a916c5bfe087638" }
glam = { workspace = true }
log = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[target.'cfg(target_family = "wasm")'.dependencies]
wasm-bindgen = { workspace = true }
wasm-bindgen-futures = "0.4"
web-sys = { version = "0.3", features = ["console", "Window", "Document"] }
console_error_panic_hook = "0.1"
console_log = "1"
js-sys = "0.3"
