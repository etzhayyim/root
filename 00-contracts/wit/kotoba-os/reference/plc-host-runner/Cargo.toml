# Native wasmtime host that runs the real plc-control component end-to-end
# (ADR-2606031600 §D2/§D3). Standalone ([workspace]). Builds for the host arch
# (plain cargo). Demonstrates: scan cycle = Datom transaction + N3 fault-atomicity
# through the actual WASM Component-Model component.
[package]
name = "plc-host-runner"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "wasmtime host driving the kotoba:os plc-control component over a Datom log (ADR-2606031600)"

[workspace]

[dependencies]
wasmtime = "47"
anyhow = "1"
