{
  "lexicon": 1,
  "id": "com.etzhayyim.tsukuroi.patchValidationResult",
  "description": "Result of building + testing a candidate patch inside the egress-restricted tsukuroi-validate namespace. ranAgainstLiveTarget is a structural const false (G9): validation NEVER executes against the live target — only a sandboxed checkout. R0 = schema skeleton; R1 = additionalProperties=false + required-field enforcement.",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "record": {
        "type": "object",
        "required": [
          "createdAt",
          "patchProposalCid",
          "ranAgainstLiveTarget",
          "buildPass",
          "testPass"
        ],
        "properties": {
          "createdAt": {"type": "string", "format": "datetime", "description": "ISO 8601 UTC timestamp of validation run."},
          "patchProposalCid": {"type": "string", "description": "CID of the com.etzhayyim.tsukuroi.patchProposal validated."},
          "ranAgainstLiveTarget": {"type": "boolean", "const": false, "description": "G9: structurally false. Build/test runs only in the sandboxed tsukuroi-validate namespace (NetworkPolicy reconciled from active mandates), never the live runtime target."},
          "buildPass": {"type": "boolean", "description": "Sandbox build succeeded."},
          "testPass": {"type": "boolean", "description": "Sandbox test suite passed."},
          "sandboxNamespace": {"type": "string", "const": "tsukuroi-validate", "description": "Egress-restricted namespace the validation ran in."},
          "testLogCid": {"type": "string", "description": "CID of the build/test log (ciphertext in vault)."},
          "regressionNotes": {"type": "string", "description": "Optional notes on regressions / side effects observed in the sandbox."}
        }
      }
    }
  }
}
