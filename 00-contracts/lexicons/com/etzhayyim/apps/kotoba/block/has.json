{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.kotoba.block.has",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Given a list of content-addressed block CIDs, return the subset the server does NOT already hold, so a publisher sends only the delta (immutable blocks never need resending). Completeness is preserved because the server retains the blocks it already had plus the delta.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["cids"],
          "properties": {
            "cids": {
              "type": "array",
              "items": { "type": "string" },
              "maxLength": 4096,
              "description": "Candidate block CIDs."
            }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["missing"],
          "properties": {
            "missing": {
              "type": "array",
              "items": { "type": "string" },
              "description": "CIDs not present server-side; the publisher should include these in block.put."
            }
          }
        }
      }
    }
  }
}
