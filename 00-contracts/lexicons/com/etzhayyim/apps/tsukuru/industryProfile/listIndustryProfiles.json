{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.tsukuru.industryProfile.listIndustryProfiles",
  "defs": {
    "main": {
      "type": "query",
      "description": "List tsukuru manufacturing industry profiles.",
      "parameters": {
        "type": "params",
        "properties": {
          "category": { "type": "string" },
          "process": { "type": "string" },
          "compliance_flag": { "type": "string" },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100 }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "items": { "type": "array", "items": { "type": "unknown" } },
            "total": { "type": "integer" },
            "limit": { "type": "integer" }
          },
          "required": ["items", "total", "limit"]
        }
      }
    }
  }
}
