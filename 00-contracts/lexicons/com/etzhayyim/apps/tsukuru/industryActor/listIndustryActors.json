{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.tsukuru.industryActor.listIndustryActors",
  "defs": {
    "main": {
      "type": "query",
      "description": "List published ISIC industry actors for tsukuru.",
      "parameters": {
        "type": "params",
        "properties": {
          "section_code": { "type": "string" },
          "industry_code": { "type": "string" },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100 }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "items": { "type": "array", "items": { "type": "unknown" } },
            "total": { "type": "integer" },
            "limit": { "type": "integer" }
          },
          "required": ["items", "total", "limit"]
        }
      }
    }
  }
}
