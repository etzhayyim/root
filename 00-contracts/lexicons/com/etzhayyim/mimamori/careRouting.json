{
  "lexicon": 1,
  "id": "com.etzhayyim.mimamori.careRouting",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "description": "A CONSENTED routing of a concern to a care actor (G1). The kept consents to THIS routing, each time (G3) — routing is an offer of care, never a report about a person. The target enum is the whole representable surface: there is no denunciation rail.",
      "record": {
        "type": "object",
        "required": ["bondRef", "to", "keptConsents", "cycle"],
        "properties": {
          "bondRef": {
            "type": "string",
            "description": "→ :mishmeret.route/bond"
          },
          "to": {
            "type": "string",
            "enum": ["kokoro", "wakai", "iyashi"],
            "description": "→ :mishmeret.route/to. G1 INVARIANT — care actors ONLY. police / authority / council / employer / family are NOT enum members (五人組→Stasi degeneration structurally blocked)."
          },
          "keptConsents": {
            "type": "boolean",
            "description": "→ :mishmeret.route/consent. G3 INVARIANT — must be true; per-act, never standing."
          },
          "cycle": {
            "type": "integer",
            "description": "→ :mishmeret.route/cycle"
          }
        }
      }
    }
  }
}
