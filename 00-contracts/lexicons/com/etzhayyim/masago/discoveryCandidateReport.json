{
  "lexicon": 1,
  "id": "com.etzhayyim.masago.discoveryCandidateReport",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["matId", "application", "confidence", "route", "advisory"],
        "properties": {
          "matId": {
            "type": "string",
            "description": "→ open-materials-ontology :mat/id (a :material node)"
          },
          "application": {
            "type": "string",
            "enum": ["battery-cathode", "battery-anode", "solid-electrolyte", "semiconductor", "photovoltaic", "catalyst", "thermoelectric", "superconductor", "dielectric", "structural", "magnet", "hydrogen-storage"],
            "description": "G1 INVARIANT — open-use application classes only; weapon / energetic / explosive / propellant / warhead / fissile / enrichment are NOT enum members (unrepresentable)"
          },
          "discoveryPriority": {
            "type": "number",
            "description": "G2 — the edge-primary integral (Σ incident evidence × disclosed confidence), DERIVED on read; informational only, not a stored material score"
          },
          "confidence": {
            "type": "string",
            "enum": ["experimental", "dft", "mlip-predicted", "mlip-screened", "estimated"],
            "description": "G3 — DISCLOSED provenance of the underlying evidence, never a masago verdict"
          },
          "route": {
            "type": "string",
            "enum": ["research"],
            "description": "G3 INVARIANT — the only route is RESEARCH; make / buy / trade / synthesize are NOT enum members (non-adjudicating; masago never trades)"
          },
          "advisory": {
            "type": "boolean",
            "const": true,
            "description": "G3 — a candidate shortlist surfaced to human scientists; not a binding decision"
          }
        }
      }
    }
  }
}
