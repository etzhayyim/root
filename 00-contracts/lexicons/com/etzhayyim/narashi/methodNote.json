{
  "lexicon": 1,
  "id": "com.etzhayyim.narashi.methodNote",
  "description": "An open, versioned normalization/aggregation heuristic used by any narashi cell (G6). Mirrors com.etzhayyim.kanae.methodNote's discipline: the public can audit the aggregation itself, not just its output.",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["createdAt", "version", "appliesToCell", "description", "attestingDid"],
        "properties": {
          "createdAt": {"type": "string", "format": "datetime"},
          "version": {"type": "string", "description": "semver, e.g. v1"},
          "appliesToCell": {
            "type": "string",
            "knownValues": ["narashi_metric_ingest", "narashi_cross_reference", "narashi_narrative"]
          },
          "description": {
            "type": "string",
            "maxLength": 4096,
            "description": "Human-readable description of the heuristic (e.g. PPP-adjustment basis, missing-year interpolation rule, normalization to a canonical indicator set)"
          },
          "sourceCid": {"type": "string", "description": "Optional CID of the open-source implementation of this heuristic"},
          "attestingDid": {"type": "string", "format": "did"}
        }
      }
    }
  }
}
