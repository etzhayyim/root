{
  "lexicon": 1,
  "id": "com.etzhayyim.liberation.adherentExitNotice",
  "description": "Adherent voluntary stage-exit notice. Right-of-exit per ADR-2605261000 §2.5: SBT remains, benefits suspend, no public record of exit-cause. Only 'exitHappened: true' published; cause stays encrypted (adherent-only).",
  "defs": {
    "main": {
      "type": "record",
      "record": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "adherentDid": {"type": "string"},
          "exitTimestamp": {"type": "string"},
          "exitedFromStage": {"type": "string", "knownValues": ["L1", "L2", "L3", "L4", "L5", "L6"]},
          "exitHappened": {"type": "boolean", "description": "Only public field; always true if record exists"},
          "encryptedReasonCid": {"type": "string", "description": "Optional adherent-side encrypted reason; private; not Council-reviewed"}
        },
        "required": ["adherentDid", "exitTimestamp", "exitedFromStage", "exitHappened"]
      }
    }
  }
}
