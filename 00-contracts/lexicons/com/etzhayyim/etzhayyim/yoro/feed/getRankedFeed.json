{
  "lexicon": 1,
  "id": "com.etzhayyim.yoro.feed.getRankedFeed",
  "defs": {
    "main": {
      "type": "query",
      "description": "5-theory ranked feed (Shannon + Bayes + Graph PPR + TDA + Joucho emotion). Well-becoming guardrails (PII Tier 3, cohort k>=50, stress/doom-scroll) are enforced on the hot path. Falls back to com.etzhayyim.yoro.feed.getDiscoverFeed on MV unavailability. See /root/.claude/plans/yoro-etzhayyim-ai-facebook-zazzy-teapot.md.",
      "parameters": {
        "type": "params",
        "properties": {
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 30 },
          "cursor": { "type": "string", "description": "ISO timestamp cursor for pagination." },
          "debug": { "type": "boolean", "default": false, "description": "Include per-post score breakdown in response (viewer-only)." },
          "echoPersistence": { "type": "number", "minimum": 0, "maximum": 1, "description": "Client-computed H1 echo-chamber persistence in [0,1]. Aggregate scalar only — raw topic history never leaves the browser." },
          "sessionDwellMs": { "type": "integer", "minimum": 0, "maximum": 86400000, "description": "Session dwell in ms. Used for doom-scroll guardrail." },
          "sessionDistinctTopics": { "type": "integer", "minimum": 0, "maximum": 10000, "description": "Distinct topic buckets observed in session (b0 analogue)." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["feed"],
          "properties": {
            "feed": { "type": "array", "items": { "type": "ref", "ref": "com.etzhayyim.yoro.getTimeline#defs/feedViewPost" } },
            "cursor": { "type": "string" },
            "debug": {
              "type": "object",
              "description": "Present only when `debug=true`.",
              "properties": {
                "eta": { "type": "number", "description": "Shannon efficiency H_shown/H_max, in [0,1]." },
                "candidateCount": { "type": "integer" },
                "hardGateDropped": { "type": "integer" },
                "personalizeOff": { "type": "boolean" },
                "nightMode": { "type": "boolean" },
                "botSuspect": { "type": "boolean" }
              }
            }
          }
        }
      }
    }
  }
}
