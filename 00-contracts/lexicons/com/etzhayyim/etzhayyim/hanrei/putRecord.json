{
  "lexicon": 1,
  "id": "com.etzhayyim.hanrei.putRecord",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Generic domain vertex write for hanrei tables (ADR-2605111200 Phase 2).",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["ownerDid", "collection", "rkey", "recordJson"],
          "properties": {
            "ownerDid":   { "type": "string" },
            "collection": { "type": "string" },
            "rkey":       { "type": "string" },
            "recordJson": { "type": "string" }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["ok"],
          "properties": { "ok": { "type": "boolean" } }
        }
      }
    }
  }
}
