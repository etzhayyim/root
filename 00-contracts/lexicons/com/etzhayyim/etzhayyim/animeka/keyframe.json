{
  "lexicon": 1,
  "id": "com.etzhayyim.animeka.keyframe",
  "defs": {
    "main": {
      "type": "record",
      "description": "Tier 2 domain record for a key animation frame (原画 / genga). Consecutive keyframe pairs on the same cut trigger the `keyframePair-inbetween` derive rule which invokes the inbetweener actor to generate tween frames.",
      "key": "any",
      "record": {
        "type": "object",
        "required": ["cutRef", "frameNum", "imageCid", "createdAt"],
        "properties": {
          "cutRef": {
            "type": "ref",
            "ref": "com.atproto.repo.strongRef"
          },
          "frameNum": {
            "type": "integer",
            "description": "Frame number within the cut (0-indexed)."
          },
          "imageCid": {
            "type": "string",
            "format": "cid",
            "description": "Blob CID of the keyframe drawing."
          },
          "layersCid": {
            "type": "string",
            "format": "cid",
            "description": "Blob CID of the KAMI layered doc (line + tie-down layers)."
          },
          "layerRole": {
            "type": "string",
            "enum": ["A", "B", "C", "D", "book", "effect"],
            "description": "X-sheet layer column (A = foreground / main char, B/C/D = secondary, book = static overlay)."
          },
          "tiePose": {
            "type": "string",
            "description": "Pose / expression tag for cross-frame consistency check."
          },
          "author": {
            "type": "string"
          },
          "status": {
            "type": "string",
            "enum": ["rough", "cleanup", "tiedown", "approved", "rejected"]
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    }
  }
}
