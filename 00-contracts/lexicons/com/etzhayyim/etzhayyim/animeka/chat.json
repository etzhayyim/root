{
  "lexicon": 1,
  "id": "com.etzhayyim.animeka.chat",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Send a chat message to one of the 12 animeka actors (or broadcast to director when actorSlug omitted). Writes the user turn + the actor reply as com.etzhayyim.apps.animeka.chatMessage records scoped to the episode convoId, and returns the reply synchronously for UI rendering.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["message"],
          "properties": {
            "message": { "type": "string" },
            "actorSlug": {
              "type": "string",
              "description": "One of: director, screenwriter, storyboarder, layout, keyAnimator, inbetweener, colorDesigner, finisher, bgArtist, compositor, soundDesigner, editor. Omit for broadcast (director)."
            },
            "episodeId": {
              "type": "string",
              "description": "Episode rkey; defaults to 'latest'."
            },
            "convoId": {
              "type": "string",
              "description": "Optional: project convoId override. Defaults to episode convoId lookup or the slug itself."
            }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["reply", "sender"],
          "properties": {
            "reply": { "type": "string" },
            "sender": { "type": "string", "description": "Display name of the responding actor." },
            "actorSlug": { "type": "string" },
            "actorDid": { "type": "string" },
            "userMsgUri": { "type": "string", "format": "at-uri" },
            "replyMsgUri": { "type": "string", "format": "at-uri" },
            "elapsedMs": { "type": "integer" }
          }
        }
      }
    }
  }
}
