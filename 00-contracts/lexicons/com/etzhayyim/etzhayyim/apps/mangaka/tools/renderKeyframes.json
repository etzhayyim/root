{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.mangaka.tools.renderKeyframes",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Pregel step 7 of com.etzhayyim.apps.mangaka.composeScene3d. GPU-bound headless render via kami-mangaka-scene PyO3 wheel + B2 content-addressed PUT. When the wheel or B2 is missing, returns placeholder blobKeys ('pending-*') so the rest of the Pregel still progresses. Runs on the VKE render pod pool (`pod_pool: vke-render-pool` per topology spec). ADR-2605082000 §2 — kind='mcp_tool'. SSoT: lg_mangaka.tools:tool_render_keyframes.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["cameraPlan", "sceneDag", "panelRkey", "iteration"],
          "properties": {
            "cameraPlan":   { "type": "unknown", "description": "{ camera: CameraSpec, lights: LightSpec[], llm: bool }." },
            "sceneDag":     { "type": "unknown", "description": "Scene jsonld emitted by tools.placeScene." },
            "panelRkey":    { "type": "string" },
            "iteration":    { "type": "integer", "minimum": 0 },
            "renderAngles": { "type": "integer", "minimum": 1, "maximum": 5, "description": "Default 3." },
            "simSeed":      { "type": "integer", "description": "Deterministic seed for spring-bone / particle simulation." }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["renders", "iteration"],
          "properties": {
            "renders":   { "type": "array", "items": { "type": "ref", "ref": "#renderEntry" } },
            "iteration": { "type": "integer", "description": "Echo of input iteration + 1." }
          }
        }
      }
    },
    "renderEntry": {
      "type": "object",
      "required": ["blobKey"],
      "properties": {
        "blobKey":        { "type": "string", "description": "Content-addressed B2 key (blobs/anonymous/{sha256hex}) or pending-* placeholder." },
        "depthBlobKey":   { "type": "string" },
        "outlineBlobKey": { "type": "string" },
        "score":          { "type": "number" },
        "angle":          { "type": "string" }
      }
    }
  }
}
