{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.mangaka.tools.simulateCharacter",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Pregel step 6 of com.etzhayyim.apps.mangaka.composeScene3d. Per-character spring-bone + cloth settle. Called via Send fan-out (one invocation per character key in pose_plan). Output is merged into the sim_result channel by the shallow-dict reducer. ADR-2605082000 §2 — kind='mcp_tool'. SSoT: lg_mangaka.tools:tool_simulate_character.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["charRkey"],
          "properties": {
            "charRkey": { "type": "string" },
            "pose":     { "type": "unknown", "description": "Single character's pose record from pose_plan." },
            "ticks":    { "type": "integer", "minimum": 1, "maximum": 240, "description": "Verlet steps to advance. Default 30." }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "simResult": { "type": "unknown", "description": "Single-key map { charRkey → { settled, ticks, pose_label } }. Reducer-merged on the Pregel side." }
          }
        }
      }
    }
  }
}
