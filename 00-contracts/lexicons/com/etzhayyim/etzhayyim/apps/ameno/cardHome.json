{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.ameno.cardHome",
  "defs": {
    "main": {
      "type": "query",
      "description": "Protocol canvas card for ameno.etzhayyim.com. Returns the home card metadata (title, description, model picker, recent history snippet). Pure compute on CF Worker edge — no DB access.",
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["title", "description", "embedUrl"],
          "properties": {
            "title": { "type": "string" },
            "description": { "type": "string" },
            "embedUrl": { "type": "string", "format": "uri" },
            "defaultModelId": { "type": "string" },
            "availableModels": { "type": "array", "items": { "type": "string" } },
            "webgpuRequired": { "type": "boolean" },
            "tagline": { "type": "string" }
          }
        }
      }
    }
  }
}
