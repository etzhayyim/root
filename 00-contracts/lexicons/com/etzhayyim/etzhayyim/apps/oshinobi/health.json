{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.oshinobi.health",
  "defs": {
    "main": {
      "type": "query",
      "description": "Liveness / readiness probe for oshinobi appview.",
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["status"],
          "properties": {
            "status": { "type": "string", "enum": ["ok", "degraded"] },
            "version": { "type": "string" }
          }
        }
      }
    }
  }
}
