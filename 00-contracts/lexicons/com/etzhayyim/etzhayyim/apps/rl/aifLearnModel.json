{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.rl.aifLearnModel",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Active Inference Phase 1B — online Dirichlet update of generative model matrices A and B from accumulated trajectory data. Runs daily via BPMN contract timer BPMN.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "min_steps": {
              "type": "integer",
              "description": "Minimum trajectory steps required before model update",
              "default": 20
            },
            "max_actors": {
              "type": "integer",
              "description": "Maximum (actor_did, action_nsid) pairs to update per invocation",
              "default": 20
            }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["ok"],
          "properties": {
            "ok": { "type": "boolean" },
            "models_updated": { "type": "integer" }
          }
        }
      }
    }
  }
}
