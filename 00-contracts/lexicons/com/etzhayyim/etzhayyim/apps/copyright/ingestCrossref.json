{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.copyright.ingestCrossref",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Trigger a Crossref REST API ingest batch into vertex_work. Cursor-based pagination, polite pool.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "rowsPerPage": {"type": "integer", "default": 100, "minimum": 1, "maximum": 1000},
            "maxPages": {"type": "integer", "default": 5, "minimum": 1, "maximum": 50},
            "sortField": {"type": "string", "default": "indexed"},
            "sortOrder": {"type": "string", "default": "desc"},
            "fromYear": {"type": "integer", "default": 2020}
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["ok", "rowsIngested", "registry"],
          "properties": {
            "ok": {"type": "boolean"},
            "rowsIngested": {"type": "integer"},
            "nextCursor": {"type": "string"},
            "registry": {"type": "string"},
            "error": {"type": "string"}
          }
        }
      }
    }
  }
}
