{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.openAirplane.notifyNoFlyZone",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Defence (stub): 制限空域 通知. Starts BPMN process open_airplane_notify_no_fly_zone.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "vertexId": { "type": "string" },
            "zoneVid": { "type": "string" },
            "reason": { "type": "string" },
            "effectiveFrom": { "type": "string", "format": "datetime" },
            "effectiveTo": { "type": "string", "format": "datetime" }
          },
          "required": ["vertexId", "zoneVid", "reason", "effectiveFrom"]
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "ok":          { "type": "boolean" },
            "instanceKey": { "type": "integer" },
            "error":       { "type": "string" }
          }
        }
      }
    }
  }
}
