{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.telecom.portOutRequest",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "MNP port-out request — release a subscriber to a recipient partner. authCodeIssued is the porting auth code returned to the subscriber.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["msisdn", "subscriberId", "recipientPartnerId", "requestedAt"],
          "properties": {
            "requestId": { "type": "string" },
            "msisdn": { "type": "string" },
            "subscriberId": { "type": "string" },
            "recipientPartnerId": { "type": "string" },
            "requestedAt": { "type": "string", "format": "datetime" },
            "scheduledCutoverAt": { "type": "string", "format": "datetime" },
            "authCodeIssued": { "type": "string" }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": { "type": "object", "properties": {
          "ok": { "type": "boolean" }, "vertexId": { "type": "string" },
          "requestId": { "type": "string" }, "status": { "type": "string" },
          "instanceKey": { "type": "integer" }, "error": { "type": "string" }
        } }
      }
    }
  }
}
