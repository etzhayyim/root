{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.ongakuka.track",
  "description": "Generated music track. T2 domain record (internal). Public release surface = derived AppBskyFeedPost.",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["title", "lyrics", "style", "status"],
        "properties": {
          "title": { "type": "string", "maxLength": 200 },
          "lyrics": { "type": "string", "description": "Plaintext lyrics, line-broken. Field-encrypt with signal:v1: prefix if private." },
          "style": { "type": "string", "description": "Style prompt (genre, mood, instrumentation, era, language)" },
          "styleRefUri": { "type": "string", "description": "AT URI to com.etzhayyim.apps.ongakuka.style reference (CLAP embedding)" },
          "language": { "type": "string", "description": "BCP-47 (ja, en, ko, zh, ...)" },
          "bpm": { "type": "integer", "minimum": 30, "maximum": 300 },
          "durationSec": { "type": "integer", "minimum": 5, "maximum": 600 },
          "status": {
            "type": "string",
            "enum": ["queued", "lyric", "compose", "vocal", "mix", "review", "published", "rejected", "failed"]
          },
          "blobKey": { "type": "string", "description": "SHA-256 hex of final mixed master (B2 content-addressed)" },
          "mimeType": { "type": "string", "description": "audio/wav | audio/flac | audio/mpeg" },
          "projectId": { "type": "string", "description": "convoId scoping all actor work (ADR project-actor-composition)" },
          "modelId": { "type": "string", "description": "kotodama:inference/audio model id (e.g. diffrhythm-1.2-ja)" },
          "seed": { "type": "integer" },
          "createdAt": { "type": "string", "format": "datetime" }
        }
      }
    }
  }
}
