{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.ongakuka.style",
  "description": "Style reference. Either a text prompt or a CLAP/MuLan embedding extracted from a permissioned reference clip. NOT for storing copyrighted source audio.",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["name", "kind"],
        "properties": {
          "name": { "type": "string", "maxLength": 200 },
          "kind": { "type": "string", "enum": ["prompt", "embedding"] },
          "prompt": { "type": "string", "description": "Free-form style description (genre, mood, instruments, era)" },
          "embeddingBlobKey": { "type": "string", "description": "SHA-256 of float32 embedding blob (CLAP 512-d / MuLan 128-d)" },
          "embeddingDim": { "type": "integer" },
          "embeddingModel": { "type": "string", "description": "e.g. clap-htsat-fused-v1" },
          "license": { "type": "string", "description": "permissive | own | licensed | unknown — embeddings of unknown-license sources MUST NOT be published as record" },
          "createdAt": { "type": "string", "format": "datetime" }
        }
      }
    }
  }
}
