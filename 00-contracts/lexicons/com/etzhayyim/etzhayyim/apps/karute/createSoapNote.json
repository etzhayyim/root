{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.karute.createSoapNote",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Create a SOAP progress note. Encrypted-write inner type com.etzhayyim.karute.soapNote. Triggers Matrix notification (PHI redacted).",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["record", "recipientDids", "publicMeta"],
          "properties": {
            "record": {"type": "unknown"},
            "recipientDids": {"type": "array", "items": {"type": "string"}},
            "publicMeta": {
              "type": "object",
              "required": ["patientDid", "encounterDid", "authorDid", "occurredAt"],
              "properties": {
                "patientDid": {"type": "string"},
                "encounterDid": {"type": "string"},
                "authorDid": {"type": "string"},
                "occurredAt": {"type": "string", "format": "datetime"},
                "signed": {"type": "boolean"}
              }
            }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "rkey": {"type": "string"},
            "encryptedCid": {"type": "string"},
            "error": {"type": "string"}
          }
        }
      }
    }
  }
}
