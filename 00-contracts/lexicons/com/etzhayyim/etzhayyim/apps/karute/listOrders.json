{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.karute.listOrders",
  "defs": {
    "main": {
      "type": "query",
      "description": "List ServiceRequests (orders) for a patient (public meta only).",
      "parameters": {
        "type": "params",
        "required": ["patientDid"],
        "properties": {
          "patientDid": {"type": "string"},
          "status": {"type": "string"},
          "category": {"type": "string"},
          "limit": {"type": "integer", "minimum": 1, "maximum": 200, "default": 50},
          "offset": {"type": "integer", "minimum": 0, "default": 0}
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "items": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "rkey": {"type": "string"},
                  "encryptedCid": {"type": "string"},
                  "encounterDid": {"type": "string"},
                  "requesterDid": {"type": "string"},
                  "category": {"type": "string"},
                  "status": {"type": "string"},
                  "priority": {"type": "string"},
                  "scheduledFor": {"type": "string", "format": "datetime"}
                }
              }
            },
            "offset": {"type": "integer"},
            "limit": {"type": "integer"}
          }
        }
      }
    }
  }
}
