{
  "lexicon": 1,
  "id": "com.etzhayyim.legalEntity.collectNor",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Collect Norwegian corporate data from Bronnoysund Register Centre (Enhetsregisteret). Organisation number as primary key.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "pages": { "type": "integer", "description": "Number of pages to fetch (default 5, max 50)" },
            "pageSize": { "type": "integer", "description": "Records per page (max 100)" },
            "startPage": { "type": "integer", "description": "Starting page number (default 0)" },
            "organisasjonsform": { "type": "string", "description": "Filter by org form code (AS, ASA, ENK, NUF, etc.)" }
          },
          "required": []
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "ok": { "type": "boolean" },
            "source": { "type": "string" },
            "pagesProcessed": { "type": "integer" },
            "totalInserted": { "type": "integer" },
            "totalSkipped": { "type": "integer" },
            "apiTotal": { "type": "integer" }
          }
        }
      }
    }
  }
}
