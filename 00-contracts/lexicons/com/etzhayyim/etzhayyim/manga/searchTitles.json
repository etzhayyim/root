{
  "lexicon": 1,
  "id": "com.etzhayyim.manga.searchTitles",
  "defs": {
    "main": {
      "type": "query",
      "description": "Search manga titles by keyword or tag",
      "parameters": {
        "type": "params",
        "required": ["q"],
        "properties": {
          "q": { "type": "string" },
          "genre": { "type": "string" },
          "tag": { "type": "string" },
          "offset": { "type": "integer" },
          "limit": { "type": "integer" }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["titles", "total", "offset", "limit"],
          "properties": {
            "titles": { "type": "array", "items": { "type": "object" } },
            "total": { "type": "integer" },
            "offset": { "type": "integer" },
            "limit": { "type": "integer" }
          }
        }
      }
    }
  }
}
