{
  "lexicon": 1,
  "id": "com.etzhayyim.kanayama.rollingAttestation",
  "description": "Hot rolling pass log attestation (L5a). Multi-pass hot rolling ~500°C, slab → hot band ~3mm. Per ADR-2605252400 L5a.",
  "defs": {
    "main": {
      "type": "record",
      "record": {
        "type": "object",
        "properties": {
          "lotId": {"type": "string"},
          "reheatTempC": {"type": "integer"},
          "passes": {"type": "array", "items": {"type": "unknown"}},
          "finalGaugeMm": {"type": "number"},
          "hotBandCoilId": {"type": "string"},
          "hotBandMassKg": {"type": "number"},
          "rollingStage": {"type": "string", "knownValues": ["hot", "cold", "temper"]},
          "recordedAt": {"type": "string", "format": "datetime"}
        },
        "required": ["lotId", "hotBandCoilId", "rollingStage", "finalGaugeMm"]
      }
    }
  }
}
