{
  "lexicon": 1,
  "id": "com.etzhayyim.ecosystem.signal",
  "defs": {
    "main": {
      "type": "record",
      "description": "A metabolic signal or heartbeat emitted by an organism.",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["tick", "timestamp", "action"],
        "properties": {
          "tick": {
            "type": "integer",
            "description": "The current heartbeat count or tick number of the organism."
          },
          "timestamp": {
            "type": "string",
            "format": "datetime",
            "description": "When this signal was emitted."
          },
          "action": {
            "type": "string",
            "knownValues": ["grow", "consume", "spore", "decay", "idle"],
            "description": "The primary metabolic action taken in this tick."
          },
          "mass": {
            "type": "integer",
            "description": "Current mass or energy level of the organism."
          },
          "targetUri": {
            "type": "string",
            "format": "at-uri",
            "description": "If interacting with another organism (e.g., kabi consuming koke), the AT-URI of the target."
          }
        }
      }
    }
  }
}