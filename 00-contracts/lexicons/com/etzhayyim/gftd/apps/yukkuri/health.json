{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.yukkuri.health",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Health probe (bootstrap). Returns service binding liveness for murakumo / ongakuka / render pool.",
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["status"],
          "properties": {
            "status": { "type": "string" },
            "murakumo": { "type": "string" },
            "ongakuka": { "type": "string" },
            "renderPool": { "type": "string" },
            "version": { "type": "string" }
          }
        }
      }
    }
  }
}
