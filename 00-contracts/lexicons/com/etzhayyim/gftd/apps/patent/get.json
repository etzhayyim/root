{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.patent.get",
  "defs": {
    "main": {
      "type": "query",
      "description": "Fetch a patent record by rkey.",
      "parameters": {
        "type": "params",
        "required": ["rkey"],
        "properties": {"rkey": {"type": "string", "description": "{jurisdiction}-{appNumber} normalized"}}
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "rkey": {"type": "string"},
            "jurisdiction": {"type": "string"},
            "appNumber": {"type": "string"},
            "title": {"type": "string"},
            "error": {"type": "string"}
          }
        }
      }
    }
  }
}
