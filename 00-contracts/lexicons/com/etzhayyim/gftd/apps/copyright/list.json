{
  "lexicon": 1,
  "id": "com.etzhayyim.apps.copyright.list",
  "defs": {
    "main": {
      "type": "query",
      "description": "List works filtered by kind / registry / license.",
      "parameters": {
        "type": "params",
        "properties": {
          "kind": {"type": "string"},
          "registry": {"type": "string"},
          "license": {"type": "string"},
          "offset": {"type": "integer", "default": 0, "minimum": 0},
          "limit": {"type": "integer", "default": 50, "minimum": 1, "maximum": 200}
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["items", "offset", "limit", "total"],
          "properties": {
            "items": {"type": "array", "items": {"type": "unknown"}},
            "offset": {"type": "integer"},
            "limit": {"type": "integer"},
            "total": {"type": "integer"}
          }
        }
      }
    }
  }
}
