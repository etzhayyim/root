{
  "lexicon": 1,
  "id": "ai.gftd.apps.datacenter.requestAccess",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "ADR-0055. Start a physical access review workflow for datacenter entry.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "requestId": { "type": "string" },
            "facilityId": { "type": "string" },
            "orgId": { "type": "string" },
            "requesterDid": { "type": "string" },
            "employerOrgId": { "type": "string" }
          },
          "required": ["requestId", "facilityId", "orgId", "requesterDid", "employerOrgId"]
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "ok": { "type": "boolean" },
            "requestId": { "type": "string" },
            "instanceKey": { "type": "integer" },
            "error": { "type": "string" }
          }
        }
      }
    }
  }
}
