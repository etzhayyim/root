{
  "lexicon": 1,
  "id": "ai.gftd.apps.datacenter.reviewCapacity",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "ADR-0055. Capacity-manager verdict on a reservation request.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "requestId": { "type": "string" },
            "decision": { "type": "string", "enum": ["approve", "reject"] },
            "comment": { "type": "string" }
          },
          "required": ["requestId", "decision"]
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "ok": { "type": "boolean" },
            "newStatus": { "type": "string" },
            "error": { "type": "string" }
          }
        }
      }
    }
  }
}
