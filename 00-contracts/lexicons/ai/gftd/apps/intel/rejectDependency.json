{
  "lexicon": 1,
  "id": "ai.gftd.apps.intel.rejectDependency",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Reject a dependency candidate and persist reviewer rationale for future model feedback.",
      "x-permissionSet": "intel:review",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "edgeId": { "type": "string" },
            "reviewerDid": { "type": "string" },
            "reason": { "type": "string" }
          },
          "required": ["edgeId", "reason"]
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "edgeId": { "type": "string" },
            "status": { "type": "string" }
          },
          "required": ["edgeId", "status"]
        }
      }
    }
  }
}
