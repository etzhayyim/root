{
  "lexicon": 1,
  "id": "ai.gftd.apps.hospitality.ownedBy",
  "description": "Chain → property ownership / franchise / management link. Drives edge_owned_by in RisingWave. Used for chain subbrand rollup (e.g. Marriott owns W / St Regis / Ritz-Carlton / Sheraton) and for property → chain attribution at R4. ADR-0028.",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "record": {
        "type": "object",
        "required": [
          "parentDid",
          "childDid",
          "relationship"
        ],
        "properties": {
          "parentDid": {
            "type": "string",
            "description": "Owner DID (chain / group / OTA parent). Must be a hospitality actor DID."
          },
          "childDid": {
            "type": "string",
            "description": "Owned / franchised / managed DID (subbrand, individual property, subsidiary)"
          },
          "relationship": {
            "type": "string",
            "enum": ["own", "franchise", "manage", "subbrand", "jv", "acquired"]
          },
          "sinceDate": {
            "type": "string",
            "format": "date",
            "description": "Relationship start (ISO-8601)"
          },
          "untilDate": {
            "type": "string",
            "format": "date",
            "description": "Relationship end (ISO-8601). Omit when active."
          },
          "stake": {
            "type": "number",
            "description": "Equity stake 0-1 when relationship in [own, jv, acquired]"
          },
          "sourceUrl": {
            "type": "string",
            "format": "uri"
          },
          "sourceLicense": {
            "type": "string"
          }
        }
      }
    }
  }
}
