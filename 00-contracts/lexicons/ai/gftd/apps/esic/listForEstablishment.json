{
  "lexicon": 1,
  "id": "ai.gftd.apps.esic.listForEstablishment",
  "defs": {
    "main": {
      "type": "query",
      "description": "ADR-0054. Payroll-officer dashboard: list ESIC contribution rows for one establishment. Tier 1 metadata only.",
      "parameters": {
        "type": "params",
        "properties": {
          "establishmentEsiCode": { "type": "string" },
          "wageMonth": { "type": "string", "description": "Optional YYYY-MM filter" },
          "status": { "type": "string", "description": "Optional filter: draft|submitted|reviewed|rejected|amended|purged" },
          "limit": { "type": "integer", "default": 50, "minimum": 1, "maximum": 500 },
          "cursor": { "type": "string" }
        },
        "required": ["establishmentEsiCode"]
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "items": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "vertexId": { "type": "string" },
                  "wageMonth": { "type": "string" },
                  "status": { "type": "string" },
                  "totalMembers": { "type": "integer" },
                  "totalContributionInrPaise": { "type": "integer" },
                  "challanReference": { "type": "string" },
                  "submittedAt": { "type": "string" },
                  "approvedAt": { "type": "string" }
                }
              }
            },
            "cursor": { "type": "string" },
            "error": { "type": "string" }
          },
          "required": ["items"]
        }
      }
    }
  }
}
