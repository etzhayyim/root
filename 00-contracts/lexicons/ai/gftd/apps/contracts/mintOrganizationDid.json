{
  "lexicon": 1,
  "id": "ai.gftd.apps.contracts.mintOrganizationDid",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Mint a deterministic organization DID under social-contract.gftd.ai from an existing vertex_legal_entity row. Hash input: country_alpha3 + national_id + name_normalized + incorporated_date (blake3 12-char prefix). Idempotent: same input → same DID.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["legalEntityVertexId"],
          "properties": {
            "legalEntityVertexId": {
              "type": "string",
              "description": "vertex_id of the source row in vertex_legal_entity"
            },
            "projectRow": {
              "type": "boolean",
              "description": "Also write vertex_contracts_organization projection row (default true)"
            }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "ok": { "type": "boolean" },
            "did": { "type": "string", "description": "did:web:social-contract.gftd.ai:entity:{alpha3}:{blake3_prefix12}" },
            "inserted": { "type": "boolean", "description": "True if a new projection row was inserted, false if it already existed" },
            "legalEntityVertexId": { "type": "string" }
          }
        }
      }
    }
  }
}
