{
  "lexicon": 1,
  "id": "ai.gftd.apps.cards.listAuthorizations",
  "defs": {
    "main": {
      "type": "query",
      "description": "List Stripe Issuing authorization requests.",
      "parameters": {
        "type": "params",
        "required": ["userId"],
        "properties": {
          "userId": { "type": "string" },
          "cardId": { "type": "string" },
          "status": { "type": "string", "description": "pending | closed | reversed" },
          "limit": { "type": "integer" }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "items": { "type": "array", "items": { "type": "object" } },
            "total": { "type": "integer" }
          }
        }
      }
    }
  }
}
