{
  "lexicon": 1,
  "id": "ai.gftd.apps.talent.stats",
  "defs": {
    "main": {
      "type": "query",
      "description": "Talent registry aggregate stats (cohort-level only).",
      "parameters": { "type": "params", "properties": {}, "required": [] },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "cohorts": { "type": "integer" },
            "totalTalent": { "type": "integer" },
            "countries": { "type": "integer" },
            "iscoCodesCovered": { "type": "integer" }
          },
          "required": ["cohorts", "totalTalent"]
        }
      }
    }
  }
}
