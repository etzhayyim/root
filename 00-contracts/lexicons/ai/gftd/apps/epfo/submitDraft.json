{
  "lexicon": 1,
  "id": "ai.gftd.apps.epfo.submitDraft",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "ADR-0052. Persist Tier 3 ECR roster payload (signal:v1: encrypted) for a draft monthly filing.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "vertexId": { "type": "string", "description": "ECR vertex_id from startMonthlyEcr." },
            "rosterPayload": { "type": "string", "description": "signal:v1: encrypted JSON of employee roster rows." },
            "aadhaarPayload": { "type": "string", "description": "Optional signal:v1: encrypted Aadhaar bundle if separated by form renderer." },
            "panPayload": { "type": "string", "description": "Optional signal:v1: encrypted PAN bundle." },
            "bankPayload": { "type": "string", "description": "Optional signal:v1: encrypted bank account bundle." }
          },
          "required": ["vertexId", "rosterPayload"]
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "ok": { "type": "boolean" },
            "declarationHash": { "type": "string", "description": "sha256 of canonical encrypted roster snapshot, mirrored to Tier 1." },
            "error": { "type": "string" }
          },
          "required": []
        }
      }
    }
  }
}
