<?xml version="1.0" encoding="UTF-8"?>
<!--
  Timer-start BPMN — triggers maps-bulk-ingest-osm-planet K8s pod weekly.
  Resident dumper iterates 8 Geofabrik regional PBF extracts (~50GB total),
  filters via osmium-tool to named POIs (place/amenity/shop/tourism/building),
  exports to GeoJSON-Seq, writes Parquet to B2, COPY-loads into RW.

  Cadence: R/PT7D (weekly) instead of PT24H — Geofabrik dumps refresh once
  per week, and the data is heavier (~50M rows / pass) so we don't re-ingest
  it every day.

  NSID: com.etzhayyim.apps.maps.bulkRefreshOsmPlanet
  vertex_id: at://did:web:bpmn.etzhayyim.com/com.etzhayyim.apps.bpmn.processDef/maps-bulk-refresh-osm-planet-v1
-->
<bpmn:definitions
    xmlns:bpmn="http://www.omg.org/spec/BPMN/20100524/MODEL"
    xmlns:zeebe="http://camunda.org/schema/zeebe/1.0"
    id="Definitions_maps_bulk_refresh_osm_planet"
    targetNamespace="https://etzhayyim.ai/bpmn/maps"
    exporter="hand-written"
    exporterVersion="1.0">
  <bpmn:process id="maps_bulk_refresh_osm_planet" name="maps bulk refresh osm planet" isExecutable="true">

    <bpmn:documentation>
      { "nsid": "com.etzhayyim.apps.maps.bulkRefreshOsmPlanet", "version": 1, "resultTimeoutMs": 600000 }
    </bpmn:documentation>

    <bpmn:startEvent id="Start_Timer" name="every 7 days">
      <bpmn:outgoing>Flow_Timer</bpmn:outgoing>
      <bpmn:timerEventDefinition id="Timer_PT7D">
        <bpmn:timeCycle xsi:type="bpmn:tFormalExpression"
                        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">R/P7D</bpmn:timeCycle>
      </bpmn:timerEventDefinition>
    </bpmn:startEvent>
    <bpmn:startEvent id="Start_Manual" name="manual / on-demand">
      <bpmn:outgoing>Flow_Manual</bpmn:outgoing>
    </bpmn:startEvent>
    <bpmn:sequenceFlow id="Flow_Timer" sourceRef="Start_Timer" targetRef="Task_Trigger"/>
    <bpmn:sequenceFlow id="Flow_Manual" sourceRef="Start_Manual" targetRef="Task_Trigger"/>

    <bpmn:serviceTask id="Task_Trigger" name="POST /trigger to dumper pod">
      <bpmn:extensionElements>
        <zeebe:taskDefinition type="generic.http.fetch"/>
        <zeebe:ioMapping>
          <zeebe:input source="=&quot;http://bulk-ingest-osm-planet.maps-bulk-ingest.svc.cluster.local:8080/trigger&quot;" target="url"/>
          <zeebe:input source="=&quot;POST&quot;"                target="method"/>
          <zeebe:input source="=&quot;application/json&quot;"    target="contentType"/>
          <zeebe:input source="={}"                              target="body"/>
          <zeebe:input source="=30000"                           target="timeoutMs"/>
          <zeebe:output source="=status"   target="triggerStatus"/>
          <zeebe:output source="=bodyJson" target="triggerResult"/>
        </zeebe:ioMapping>
      </bpmn:extensionElements>
      <bpmn:incoming>Flow_Timer</bpmn:incoming>
      <bpmn:incoming>Flow_Manual</bpmn:incoming>
      <bpmn:outgoing>Flow_2</bpmn:outgoing>
    </bpmn:serviceTask>
    <bpmn:sequenceFlow id="Flow_2" sourceRef="Task_Trigger" targetRef="Task_Audit"/>

    <bpmn:serviceTask id="Task_Audit" name="emit bulk.refresh OCEL">
      <bpmn:extensionElements>
        <zeebe:taskDefinition type="generic.audit.emit"/>
        <zeebe:ioMapping>
          <zeebe:input source="=&quot;com.etzhayyim.apps.maps.bulk.refresh.triggered&quot;" target="eventType"/>
          <zeebe:input source="={ &quot;source&quot;: &quot;osm_planet&quot;, &quot;status&quot;: triggerStatus, &quot;startedAt&quot;: triggerResult.started_at }" target="attributes"/>
        </zeebe:ioMapping>
      </bpmn:extensionElements>
      <bpmn:incoming>Flow_2</bpmn:incoming>
      <bpmn:outgoing>Flow_3</bpmn:outgoing>
    </bpmn:serviceTask>
    <bpmn:sequenceFlow id="Flow_3" sourceRef="Task_Audit" targetRef="End"/>

    <bpmn:endEvent id="End" name="done">
      <bpmn:incoming>Flow_3</bpmn:incoming>
    </bpmn:endEvent>

  </bpmn:process>
</bpmn:definitions>
