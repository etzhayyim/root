;; Auto-generated from bb.edn (ADR-2607173000).
;; nbb scripts/run-task.cljs <task> [args…]
{:audit:test-health {:cmd ["bb" "70-tools/scripts/test-health/audit.clj" "--check"], :doc "test-health audit (70-tools/scripts/test-health/audit.clj) — reports .clj/.cljc shadow pairs + broken bb-test shims (the py→cljc port-debt register); --check self-verifies the detector's invariants (exit 1 only if the detector itself breaks, not on debt). Add --probe to classify broken shims, --write to refresh AUDIT.md"}, :report:wave11 {:cmd ["python3" "70-tools/verify_wave11.py" "--report"], :doc "Regenerate 70-tools/WAVE11-MATURITY.md scorecard (runs the bb suites)"}, :test:cell-runner {:cmd ["python3" "test_lite_runner.py"], :doc "Murakumo lite cell-runner + deploy_node tests (pure stdlib; offline)"}, :test:docs-registry {:cmd ["bb" "70-tools/scripts/docs/test_regen_registry.clj"], :doc "docs registry generator (70-tools/scripts/docs/regen-registry.clj) — parse-frontmatter / JSON+EDN encoders / byte-parity round-trip + live freshness guard (port of test_regen_registry.py)"}, :verify:wave11 {:cmd ["python3" "70-tools/verify_wave11.py" "--test"], :doc "Wave 11 compute & photonics clj/Datomic actors — cross-artifact consistency + bb contract tests (26 actors; their actor_test.cljc live under *-compat/ so they are out of test:actors auto-discovery; this drives them with the correct per-actor classpath)"}}
