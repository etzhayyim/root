;; kanayama 金 — actor manifest (Gen-3 kotoba-native)
;; Circular metallurgy (UBC recycling) — elemental complement to watatsumi 水
;; ADR-2605252400 (R0 scaffold) · 2606021139 (namespace)
;; Migration plan: TBD
{:actor/id        "kanayama"
 :actor/glyph     "金"
 :actor/tier      :tier-b
 :actor/domain    "kanayama.etzhayyim.com"
 :actor/purpose   "Closed-loop material recovery / circular metallurgy Tier-B actor. Wave 1 reference = closed-loop aluminum used beverage container (UBC) recycling adopted from European integrated recycling-rolling mill practice. Constitutional posture strongly favorable (§2(g) habitat + §2(h) circular economy + §2(e) anti-gatekeeping). Primary mining (bauxite / iron / copper / etc.) is out of kanayama scope (N1, recovery-first by preference); NOT constitutionally forbidden — extraction is gated by a multi-generational (子・孫) × wellbecoming risk assessment per ADR-2606161700 (Rider §2(l) v3.2), not a blanket ban."
 :actor/status    :r0
 :actor/adr       "2605252400"
 :actor/related   ["2605252400" "2605252415" "2605252430" "2605252445"]

 :actor/charter-rider
 {:rider/version "2.0"
  :rider/applied true}

 :actor/gates
 [{:gate/id "G1"  :gate/name "firmware-open-source" :gate/rule "All firmware (furnace, casting, rolling, robotics) open-source (Apache 2.0 + Charter Rider)"}
  {:gate/id "G2"  :gate/name "mass-balance-audit"   :gate/rule "Mass-balance audit on kotoba-datomic: input_mass = output_metal + dross + emission ≥98% closure"}
  {:gate/id "G3"  :gate/name "ipfs-pinned-proofs"   :gate/rule "Per-batch + per-coil IPFS-pinned photo + video"}
  {:gate/id "G4"  :gate/name "witness-quorum"       :gate/rule "Every pour signed by witness quorum ≥2 distinct robots (Ed25519, DID-bound)"}
  {:gate/id "G5"  :gate/name "murakumo-only"        :gate/rule "Inference paths via Murakumo no-VKE mesh only (ADR-2605214000 / ADR-2605215000)"}
  {:gate/id "G6"  :gate/name "kotoba-eavt-native"   :gate/rule "State is kotoba EAVT Datoms (no SQL/RisingWave/Cypher)"}
  {:gate/id "G7"  :gate/name "tithe-non-fiat"       :gate/rule "Payments are USDC Base L2 + ERC-4337 + TitheRouter 10%; no Stripe/fiat"}
  {:gate/id "G8"  :gate/name "air-emissions-cap"    :gate/rule "Air emissions ≤ EU IED 2010/75/EU + 日本大気汚染防止法 + EN 12457 leachate"}
  {:gate/id "G12" :gate/name "kpi-caps"             :gate/rule "Recovery rate ≥95%; energy ≤6 kWh/kg for Wave 1 Al"}
  {:gate/id "G13" :gate/name "renewable-energy"     :gate/rule "Renewable + grid-balanced energy only (captive coal / petcoke prohibited)"}
  {:gate/id "G14" :gate/name "waste-tracking"       :gate/rule "Waste outputs IPFS lot-tracked + quarterly §2(h) report"}
  {:gate/id "G15" :gate/name "no-server-key"        :gate/rule "Platform holds no key (member/operator signs)"}
  {:gate/id "G16" :gate/name "consent-bound"        :gate/rule "Explicit member consent for outward actions"}
  {:gate/id "G17" :gate/name "pii-encrypted"        :gate/rule "PII encrypted in storage and transit"}]

 :actor/cells
 [{:cell/id "intake_qa"              :cell/kind :datalog   :cell/runtime :kotoba} ;; L1, naphtali
  {:cell/id "decoating_separation"   :cell/kind :langgraph :cell/runtime :wasm} ;; L2, zebulun
  {:cell/id "melting_furnace"        :cell/kind :langgraph :cell/runtime :wasm} ;; L3, joseph
  {:cell/id "dross_recovery"         :cell/kind :langgraph :cell/runtime :wasm} ;; cross, joseph
  {:cell/id "dc_casting"             :cell/kind :langgraph :cell/runtime :wasm} ;; L4, simeon
  {:cell/id "hot_rolling"            :cell/kind :langgraph :cell/runtime :wasm} ;; L5a, dan
  {:cell/id "cold_rolling_finishing" :cell/kind :langgraph :cell/runtime :wasm} ;; L5b, dan
  {:cell/id "air_emissions_audit"    :cell/kind :langgraph :cell/runtime :wasm} ;; cross, levi
  {:cell/id "mass_balance_binder"    :cell/kind :datalog   :cell/runtime :kotoba}] ;; terminal, judah

 :actor/lex
 [{:lex/id "intakeRecord"}
  {:lex/id "decoatingAttestation"}
  {:lex/id "meltingAttestation"}
  {:lex/id "dcCastingAttestation"}
  {:lex/id "rollingAttestation"}
  {:lex/id "coilQualificationRecord"}
  {:lex/id "airEmissionsAuditRecord"}
  {:lex/id "silenRecyclingReview"}]

 ;; legacy artifacts (from manifest.jsonld)
 :actor/legacy
 {:manifest    "manifest.jsonld"
  :wit-packages []
  :note "wit-packages are placeholders, to be determined"}
}
