{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([edifact_tty.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'edifact_tty.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
