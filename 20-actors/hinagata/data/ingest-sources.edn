;; hinagata 雛形 — bounded PUBLIC statute-source allowlist (ingest input; G7 outward-gated)
;; ADR-2606111954. The G7-gated legal-corpus binding (ADR-2605262800) consumes ONLY these
;; public official primary sources to anchor template clauses to actual law. PUBLIC, primary,
;; non-paywalled only (Rider §2(e)). Scope expansion beyond this allowlist requires Council +
;; operator DID. hinagata never re-judges a statute (N3) — it records the DISCLOSED citation +
;; its official source URL, nothing more.

{:ingest/adr "2606111954"
 :ingest/gate "G7 — Council + operator DID for any source beyond this allowlist"
 :ingest/discipline "PUBLIC official primary sources only; record citation + URL, never re-judge (N3)"

 :ingest/sources
 [{:src/id :uncitral :src/label "UNCITRAL texts (CISG / NY Convention / Model Law)" :src/base "https://uncitral.un.org/" :src/enabled true :src/jurisdiction "INTL"}
  {:src/id :eur-lex :src/label "EUR-Lex (GDPR / eIDAS / EU regulations)" :src/base "https://eur-lex.europa.eu/" :src/enabled true :src/jurisdiction "EU"}
  {:src/id :jp-egov :src/label "日本 e-Gov 法令検索 (民法 / 借地借家法 / 利息制限法 / 特定商取引法 / 電子署名法)" :src/base "https://laws.e-gov.go.jp/" :src/enabled true :src/jurisdiction "JP"}
  {:src/id :ilo-normlex :src/label "ILO NORMLEX (core labour conventions)" :src/base "https://normlex.ilo.org/" :src/enabled true :src/jurisdiction "INTL"}
  {:src/id :us-govinfo :src/label "US GovInfo / USC (ESIGN Act 15 USC §7001)" :src/base "https://www.govinfo.gov/" :src/enabled true :src/jurisdiction "US"}
  {:src/id :uniform-laws :src/label "Uniform Law Commission (UETA)" :src/base "https://www.uniformlaws.org/" :src/enabled true :src/jurisdiction "US"}
  {:src/id :apache :src/label "Apache License 2.0 (open-source license law)" :src/base "https://www.apache.org/licenses/" :src/enabled true :src/jurisdiction "INTL"}
  {:src/id :uk-legislation :src/label "legislation.gov.uk (next-wave; not yet seeded)" :src/base "https://www.legislation.gov.uk/" :src/enabled false :src/jurisdiction "UK"}]}
