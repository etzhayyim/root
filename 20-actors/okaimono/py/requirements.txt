# okaimono 御買物 — langgraph WASM actor deps (ADR-2606012100)
# LLM access is Murakumo-only via the kotoba host `llm` binding (127.0.0.1:4000);
# no external LLM client is listed here by design (G5).
langgraph>=0.2
typing_extensions>=4.0
