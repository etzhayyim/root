[project]
name = "etzhayyim-first-breath"
version = "0.0.0"
description = "Proof-of-life cell for the etzhayyim substrate — periodic anchor of cell state to EtzhayyimAnchor on Base L2 / geth-private / local anvil."
license = "Apache-2.0"
requires-python = ">=3.11"
dependencies = [
    "web3>=7.0",
    "eth-account>=0.13",
]

[tool.uv]
dev-dependencies = []
