[project]
name = "kubernetes-compat"
version = "0.1.0"
description = "Clean room Kubernetes compatible actor"

[dependencies]
kotoba = "workspace"
kotodama-wasm = "workspace"
datomic-client = "workspace"
