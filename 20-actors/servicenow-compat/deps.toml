[project]
name = "servicenow-compat"
version = "0.1.0"
description = "Clean room Servicenow compatible actor"

[dependencies]
kotoba = "workspace"
kotodama-wasm = "workspace"
datomic-client = "workspace"
