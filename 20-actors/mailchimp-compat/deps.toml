[project]
name = "mailchimp-compat"
version = "0.1.0"
description = "Clean room Mailchimp compatible actor"

[dependencies]
kotoba = "workspace"
kotodama-wasm = "workspace"
datomic-client = "workspace"
