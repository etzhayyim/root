[project]
name = "bigcommerce-compat"
version = "0.1.0"
description = "Clean room Bigcommerce compatible actor"

[dependencies]
kotoba = "workspace"
kotodama-wasm = "workspace"
datomic-client = "workspace"
