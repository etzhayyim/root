[project]
name = "cloudflare-compat"
version = "0.1.0"
description = "Clean room Cloudflare compatible actor"

[dependencies]
kotoba = "workspace"
kotodama-wasm = "workspace"
datomic-client = "workspace"
