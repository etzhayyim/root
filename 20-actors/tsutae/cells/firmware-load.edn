;; tsutae 伝え — firmware-load cell (langgraph, WASM runtime) · ADR-2605261300
{:cell/id "firmware-load"
 :cell/glyph "伝え"
 :cell/kind :langgraph
 :cell/runtime :wasm
 :cell/entry "py/agent.py"
 :cell/node "joseph"
 :cell/doc "L4 firmware flash: open stack (coreboot → Linux mainline → GrapheneOS-class) + SHA-256 verify over IPFS-pinned image. G7 blob-ratio ≤5% guard + G2 bootloader-unlock-default guard. ameno/mitate baseline option."
 :cell/gates ["G2" "G7" "G16" "G17"]
 :cell/kotoba
 {:reads [:attestation/cid]
  :writes [:production-progress/stage :attestation/id :attestation/type :attestation/cid]}}
