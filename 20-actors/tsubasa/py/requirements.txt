# tsubasa 翼 — flight discovery commons deps (ADR-2606072800)
# LLM access is Murakumo-only via the kotoba host `llm` binding (127.0.0.1:4000);
# no external LLM client is listed here by design (G6).
langgraph>=0.2
typing_extensions>=4.0
