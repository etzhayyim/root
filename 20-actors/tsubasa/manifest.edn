;; tsubasa 翼 — actor manifest
;; Flight-route/fare discovery commons (Skyscanner inversion) — data-only, affiliate-stripped,
;; member self-books, emissions-honest. ADR-2606072800
{:actor/id        "tsubasa"
 :actor/glyph     "翼"
 :actor/tier      :tier-b
 :actor/domain    "tsubasa.etzhayyim.com"
 :actor/purpose   "Skyscanner-inverse flight discovery commons — honest fare/route meta-search whose every onward link is AFFILIATE-STRIPPED and where the member SELF-BOOKS on the airline's own site (no commission inflow). Ranks by true total cost (fare + baggage) with CO₂ emissions surfaced, never hidden; no ads, no urgency ('price will rise'), no person fare-tracking. Live GDS/airline ingest is Council Lv7+ gated; R0 ships representative data. Sibling of watari (live position). kotoba-EAVT-native, Murakumo-only."
 :actor/status    :r1
 :actor/adr       "2606072800"

 :actor/charter-rider
 {:rider/version "2.0"
  :rider/applied true}

 :actor/gates
 [{:gate/id "G1"  :gate/name "no-affiliate-no-inflow"   :gate/rule "onward links AFFILIATE-STRIPPED; member books on the airline's OWN site; tsubasa is never merchant-of-record and takes no commission/inflow (§1.3); no commission field representable"}
  {:gate/id "G2"  :gate/name "no-ads"                   :gate/rule "data-only; no sponsored ranking / paid placement (Charter Rider §2)"}
  {:gate/id "G3"  :gate/name "wellbecoming-anti-dark"   :gate/rule "honest fares only; no urgency/scarcity ('price will rise') / fare-watch pressure; no predictive-pressure field"}
  {:gate/id "G4"  :gate/name "emissions-honest"         :gate/rule "rank by true total cost (fare + baggage); CO₂ emissions SURFACED on every option, never hidden or de-ranked away"}
  {:gate/id "G5"  :gate/name "no-person-tracking"       :gate/rule "no per-searcher fare-watch / pattern-of-life; a search is stateless w.r.t. the searcher"}
  {:gate/id "G6"  :gate/name "murakumo-only"            :gate/rule "NL query / narration via KotobaLLM 127.0.0.1:4000; no external LLM (ADR-2605215000)"}
  {:gate/id "G7"  :gate/name "kotoba-eavt-native"       :gate/rule "fares/routes are kotoba Datoms; no RisingWave/SQL as canonical"}
  {:gate/id "G8"  :gate/name "outward-gated"            :gate/rule "live GDS/airline fare ingest = Council Lv7+ + operator; R0 ships :representative data only; tsubasa transacts no booking (member self-books)"}]

 :actor/cells
 [{:cell/id "fares"    :cell/kind :datalog   :cell/runtime :kotoba :cell/doc "route/fare/emissions Datoms (:representative)"}
  {:cell/id "search"   :cell/kind :langgraph :cell/runtime :wasm   :cell/doc "query → honest ranked options (total cost + emissions surfaced, G4)"}
  {:cell/id "handoff"  :cell/kind :langgraph :cell/runtime :wasm   :cell/doc "affiliate-stripped self-book handoff to airline's own site (G1)"}]

 :actor/lexicons
 ["com.etzhayyim.tsubasa.fare"
  "com.etzhayyim.tsubasa.searchResult"]

 :actor/siblings
 {:watari   "live aircraft POSITION (ADS-B) — tsubasa plans, watari tracks; both observational"
  :okaimono "affiliate-strip + external-mirror pattern reused"
  :shukubo  "Ring-2 self-book-handoff pattern reused"}}
