{:cell/id "segment_build"
 :cell/glyph "筋"
 :cell/kind :langgraph
 :cell/runtime :wasm
 :cell/node "naphtali"
 :cell/doc "Build the sagittal segment chain from anthropometry (methods/segment.py): total body mass + stature → per-segment mass / length / CoM via de Leva/Winter fractions. Emits a bodyModel Datom. :representative population avg (G7); a real per-member scan carries 要配慮 PII and is kizashi's encrypted-gated job (G4). R0 scaffold (.solve() raises)."
 :cell/entry "cells/segment_build/cell.py"
 :cell/handler "SegmentBuildCell"
 :cell/state-graph {:nodes [:read-anthro :build-segments :emit] :entry :read-anthro
                    :edges [[:read-anthro :build-segments] [:build-segments :emit] [:emit :end]]}
 :cell/gates ["G4" "G7" "G9"]
 :cell/kotoba {:reads [:body/total-mass-kg :body/stature-m] :writes [:body/id :body/representative]}}
