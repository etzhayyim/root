"""karakuri command_plan (絡繰) cell — R0 coded reference cell.

NL brief -> ordered normalized ServiceOps (Murakumo-only, G4 — deterministic fallback at R0, the
live LLM path is operator-gated), each safety-classified and Charter-Rider scanned (N6). Emits a
dry-run plan with mutate ops marked :awaiting-member-sig (G5). Plan only at R0 (G6). .solve() raises
until Council activation; the state_machine transitions are unit-tested.
"""
