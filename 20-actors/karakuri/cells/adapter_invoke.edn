{:cell/id "adapter_invoke"
 :cell/glyph "繰"
 :cell/kind :langgraph
 :cell/runtime :wasm
 :cell/node "judah"
 :cell/doc "Execute a single ServiceOp via the selected adapter (T1 official-API > T2 ToS-permitted headless > T3 export). Refuses T2 on an :automation-prohibited service (G2) and never evades bot-detection. :read ships at R0 as a dry-run; mutate ops require member-sig (G5). EVERY live execution is Council Lv6+ + operator gated (G6) and written to the kotoba Datom log (G7)."
 :cell/entry "cells/adapter_invoke/cell.py"
 :cell/handler "AdapterInvokeCell"
 :cell/state-graph
 {:nodes [:tos-gate :mutate-gate :dry-run :execute-gated] :entry :tos-gate
  :edges [[:tos-gate :mutate-gate] [:mutate-gate :dry-run] [:dry-run :execute-gated] [:execute-gated :end]]}
 :cell/llm {:provider :murakumo :endpoint "127.0.0.1:4000" :model "gemma3:4b" :charter-rider true}
 :cell/gates ["G2" "G5" "G6" "G7"]
 :cell/kotoba {:reads [:op/adapter-tier :op/tos-gate :op/mutate-gate] :writes [:op/dry-run :op/executed-at]}}
