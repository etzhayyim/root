{:cell/id "export_roundtrip"
 :cell/glyph "繰"
 :cell/kind :langgraph
 :cell/runtime :wasm
 :cell/node "zebulun"
 :cell/doc "The data-portability / anti-lock-in leg (T3): pull the MEMBER's OWN data out of a service into a portable kotoba-native form, or push it into another service. Own-data-only + encrypted (G9). The structural inverse of vendor lock-in. Live fetch G6-gated; R0 produces an export plan only."
 :cell/entry "cells/export_roundtrip/cell.py"
 :cell/handler "ExportRoundtripCell"
 :cell/state-graph
 {:nodes [:owner-check :format-select :export-plan] :entry :owner-check
  :edges [[:owner-check :format-select] [:format-select :export-plan] [:export-plan :end]]}
 :cell/llm {:provider :murakumo :endpoint "127.0.0.1:4000" :model "gemma3:4b" :charter-rider true}
 :cell/gates ["G6" "G7" "G9"]
 :cell/kotoba {:reads [:service/id] :writes [:export/owner :export/encrypted :export/format :export/cid]}}
