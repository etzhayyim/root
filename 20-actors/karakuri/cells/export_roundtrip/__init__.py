"""karakuri export_roundtrip (絡繰) cell — R0 coded reference cell.

The data-portability / anti-lock-in leg (T3): pull the MEMBER's OWN data out of a service into a
portable kotoba-native form. Own-data-only + encrypted (G9). Live fetch G6-gated; R0 produces an
export plan only. .solve() raises until Council activation; the state_machine transitions are
unit-tested.
"""
