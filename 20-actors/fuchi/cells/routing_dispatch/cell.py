"""LangGraph Pregel wrapper for 扶持 (fuchi) routing_dispatch — R0 scaffold.

The G3 in-kind rail decomposition. Coded core in state_machine.py. .solve() raises at R0 —
live provisioning intents to the producing actors (mitsuho/hikari/okaimono/warifu) are G10-gated.
"""
from __future__ import annotations

from typing import Any


class RoutingDispatchCell:
    """Envelope → in-kind delivery rails. G3."""

    def solve(self, input_state: dict[str, Any]) -> dict[str, Any]:
        raise RuntimeError(
            "fuchi R0 scaffold: routing_dispatch decomposes to in-kind rails offline; live "
            "provisioning intents are Council Lv6+ + operator gated (G10)."
        )
