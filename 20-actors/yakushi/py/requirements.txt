# yakushi 薬師 — Python dependencies (offline, no external LLM)
# ADR-2605250500 · R0 scaffold
#
# NOTE: No openai / anthropic / litellm / requests imports.
# LLM access is Murakumo-only via kotoba WASM host bindings (G15).
# Settlement via USDC Base L2 only; no Stripe/fiat (G17).
#
# For testing offline (no kotoba host):
#   python3 -m pytest test_agent.py
#
# For WASM componentization (kotoba deploy):
#   pip install componentize-py
