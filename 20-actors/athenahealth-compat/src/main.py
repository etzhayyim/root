"""
Py Kotodama WASM entrypoint for the Athenahealth clean-room actor.

Clean-room, API-shaped CRUD over a Datomic-backed Kotoba schema.
Generated by 70-tools/deepen_actors.py (ADR 260607 deepening phase).
No proprietary code or credentials; resource shapes only.
"""
from kotodama import Runtime
from kotoba import load_schema
from datomic import DatomicClient
import uuid
import datetime

schema = load_schema("../schema/athenahealth.kotoba")
db = DatomicClient.connect()
app = Runtime("athenahealth-compat")


def now():
    return datetime.datetime.utcnow().isoformat()


def new_id(prefix):
    return f"{prefix}_" + uuid.uuid4().hex[:16]


def _persist(entity, rec):
    """Transact a record into Datomic as namespaced EAVT facts."""
    facts = {}
    for k, v in rec.items():
        facts[f"athenahealth.{entity}/{k}"] = v
    db.transact([facts])
    return rec


def _query(entity, eid=None):
    """Read records of an entity from Datomic (optionally by id)."""
    pattern = {"entity": f"athenahealth.{entity}"}
    if eid is not None:
        pattern["id"] = eid
    return db.query(pattern)


def _require(data, fields):
    missing = [f for f in fields if not data.get(f)]
    if missing:
        return {"error": {"message": "Missing required fields: " + ", ".join(missing)}}
    return None


@app.route("/v1/patients", methods=["POST"])
def create_patient(request):
    """Create a Patient."""
    data = request.json or request.form or {}
    err = _require(data, ['homeboundyn', 'assignedsexatbirth'])
    if err:
        return err, 400
    rec = {"id": new_id("athenahe_pat")}
    rec["homeboundyn"] = bool(data.get('homeboundyn'))
    rec["assignedsexatbirth"] = data.get('assignedsexatbirth')
    rec["altfirstname"] = data.get('altfirstname')
    rec["ethnicitycode"] = data.get('ethnicitycode')
    rec["industrycode"] = int(data.get('industrycode') or 0)
    rec["language6392code"] = data.get('language6392code')
    rec["localpatientid"] = data.get('localpatientid')
    rec["deceaseddate"] = data.get('deceaseddate')
    rec["firstappointment"] = data.get('firstappointment')
    rec["primaryproviderid"] = int(data.get('primaryproviderid') or 0)
    rec["genderidentityother"] = data.get('genderidentityother')
    rec["preferredpronouns"] = data.get('preferredpronouns')
    rec["lastappointment"] = data.get('lastappointment')
    rec["donotcallyn"] = bool(data.get('donotcallyn'))
    rec["primarydepartmentid"] = int(data.get('primarydepartmentid') or 0)
    rec["status"] = data.get('status')
    rec["lastemail"] = data.get('lastemail')
    rec["racecode"] = data.get('racecode')
    rec["sexualorientation"] = data.get('sexualorientation')
    rec["genderidentity"] = data.get('genderidentity')
    rec["emailexistsyn"] = bool(data.get('emailexistsyn'))
    rec["occupationcode"] = int(data.get('occupationcode') or 0)
    rec["sexualorientationother"] = data.get('sexualorientationother')
    rec["patientid"] = int(data.get('patientid') or 0)
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Patient", rec)
    return rec, 201

@app.route("/v1/patients", methods=["GET"])
def list_patients(request):
    """List Patients."""
    items = _query("Patient")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/patients/<eid>", methods=["GET"])
def get_patient(request, eid):
    """Retrieve a Patient by id."""
    rows = _query("Patient", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/patients/<eid>", methods=["POST", "PATCH"])
def update_patient(request, eid):
    """Update a Patient."""
    rows = _query("Patient", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Patient", rec)
    return rec, 200

@app.route("/v1/patients/<eid>", methods=["DELETE"])
def delete_patient(request, eid):
    """Delete a Patient."""
    rows = _query("Patient", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"athenahealth.Patient", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/appointments", methods=["POST"])
def create_appointment(request):
    """Create a Appointment."""
    data = request.json or request.form or {}
    err = _require(data, ['reasonid', 'appointmentstatus'])
    if err:
        return err, 400
    rec = {"id": new_id("athenahe_app")}
    rec["reasonid"] = int(data.get('reasonid') or 0)
    rec["appointmentstatus"] = data.get('appointmentstatus')
    rec["cancelleddatetime"] = data.get('cancelleddatetime')
    rec["chargeentrynotrequired"] = bool(data.get('chargeentrynotrequired'))
    rec["hl7providerid"] = int(data.get('hl7providerid') or 0)
    rec["cancelreasonname"] = data.get('cancelreasonname')
    rec["chargeentrynotrequiredreason"] = data.get('chargeentrynotrequiredreason')
    rec["lastmodified"] = data.get('lastmodified')
    rec["departmentid"] = int(data.get('departmentid') or 0)
    rec["checkoutdatetime"] = data.get('checkoutdatetime')
    rec["copay"] = data.get('copay')
    rec["encounterid"] = data.get('encounterid')
    rec["scheduledby"] = data.get('scheduledby')
    rec["checkindatetime"] = data.get('checkindatetime')
    rec["cancelledby"] = data.get('cancelledby')
    rec["stopintakedatetime"] = data.get('stopintakedatetime')
    rec["encounterstatus"] = data.get('encounterstatus')
    rec["frozenyn"] = bool(data.get('frozenyn'))
    rec["appointmenttype"] = data.get('appointmenttype')
    rec["appointmenttypeid"] = int(data.get('appointmenttypeid') or 0)
    rec["cancelreasonid"] = int(data.get('cancelreasonid') or 0)
    rec["cancelreasonnoshow"] = bool(data.get('cancelreasonnoshow'))
    rec["cancelreasonslotavailable"] = bool(data.get('cancelreasonslotavailable'))
    rec["coordinatorenterpriseyn"] = bool(data.get('coordinatorenterpriseyn'))
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Appointment", rec)
    return rec, 201

@app.route("/v1/appointments", methods=["GET"])
def list_appointments(request):
    """List Appointments."""
    items = _query("Appointment")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/appointments/<eid>", methods=["GET"])
def get_appointment(request, eid):
    """Retrieve a Appointment by id."""
    rows = _query("Appointment", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/appointments/<eid>", methods=["POST", "PATCH"])
def update_appointment(request, eid):
    """Update a Appointment."""
    rows = _query("Appointment", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Appointment", rec)
    return rec, 200

@app.route("/v1/appointments/<eid>", methods=["DELETE"])
def delete_appointment(request, eid):
    """Delete a Appointment."""
    rows = _query("Appointment", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"athenahealth.Appointment", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/encounters", methods=["POST"])
def create_encounter(request):
    """Create a Encounter."""
    data = request.json or request.form or {}
    err = _require(data, ['encounterid', 'appointmentid'])
    if err:
        return err, 400
    rec = {"id": new_id("athenahe_enc")}
    rec["encounterid"] = int(data.get('encounterid') or 0)
    rec["appointmentid"] = int(data.get('appointmentid') or 0)
    rec["departmentid"] = int(data.get('departmentid') or 0)
    rec["encountervisitname"] = data.get('encountervisitname')
    rec["encountertype"] = data.get('encountertype')
    rec["status"] = data.get('status')
    rec["patientlocationid"] = int(data.get('patientlocationid') or 0)
    rec["patientlocation"] = data.get('patientlocation')
    rec["patientstatusid"] = int(data.get('patientstatusid') or 0)
    rec["patientstatus"] = data.get('patientstatus')
    rec["encounterdate"] = data.get('encounterdate')
    rec["stage"] = data.get('stage')
    rec["providerid"] = int(data.get('providerid') or 0)
    rec["providerfirstname"] = data.get('providerfirstname')
    rec["providerlastname"] = data.get('providerlastname')
    rec["providerphone"] = data.get('providerphone')
    rec["lastupdated"] = data.get('lastupdated')
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Encounter", rec)
    return rec, 201

@app.route("/v1/encounters", methods=["GET"])
def list_encounters(request):
    """List Encounters."""
    items = _query("Encounter")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/encounters/<eid>", methods=["GET"])
def get_encounter(request, eid):
    """Retrieve a Encounter by id."""
    rows = _query("Encounter", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/encounters/<eid>", methods=["POST", "PATCH"])
def update_encounter(request, eid):
    """Update a Encounter."""
    rows = _query("Encounter", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Encounter", rec)
    return rec, 200

@app.route("/v1/encounters/<eid>", methods=["DELETE"])
def delete_encounter(request, eid):
    """Delete a Encounter."""
    rows = _query("Encounter", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"athenahealth.Encounter", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/claims", methods=["POST"])
def create_claim(request):
    """Create a Claim."""
    data = request.json or request.form or {}
    err = _require(data, ['referringproviderid', 'claimcreateddate'])
    if err:
        return err, 400
    rec = {"id": new_id("athenahe_cla")}
    rec["referringproviderid"] = int(data.get('referringproviderid') or 0)
    rec["claimcreateddate"] = data.get('claimcreateddate')
    rec["billedservicedate"] = data.get('billedservicedate')
    rec["billedproviderid"] = int(data.get('billedproviderid') or 0)
    rec["appointmentid"] = int(data.get('appointmentid') or 0)
    rec["chargeamount"] = data.get('chargeamount')
    rec["transactionid"] = int(data.get('transactionid') or 0)
    rec["claimid"] = int(data.get('claimid') or 0)
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Claim", rec)
    return rec, 201

@app.route("/v1/claims", methods=["GET"])
def list_claims(request):
    """List Claims."""
    items = _query("Claim")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/claims/<eid>", methods=["GET"])
def get_claim(request, eid):
    """Retrieve a Claim by id."""
    rows = _query("Claim", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/claims/<eid>", methods=["POST", "PATCH"])
def update_claim(request, eid):
    """Update a Claim."""
    rows = _query("Claim", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Claim", rec)
    return rec, 200

@app.route("/v1/claims/<eid>", methods=["DELETE"])
def delete_claim(request, eid):
    """Delete a Claim."""
    rows = _query("Claim", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"athenahealth.Claim", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/providers", methods=["POST"])
def create_provider(request):
    """Create a Provider."""
    data = request.json or request.form or {}
    err = _require(data, ['billable', 'ansispecialtycode'])
    if err:
        return err, 400
    rec = {"id": new_id("athenahe_pro")}
    rec["billable"] = bool(data.get('billable'))
    rec["ansispecialtycode"] = data.get('ansispecialtycode')
    rec["firstname"] = data.get('firstname')
    rec["entitytype"] = data.get('entitytype')
    rec["otherprovideridlist"] = data.get('otherprovideridlist')
    rec["ansinamecode"] = data.get('ansinamecode')
    rec["displayname"] = data.get('displayname')
    rec["homedepartment"] = data.get('homedepartment')
    rec["providerid"] = int(data.get('providerid') or 0)
    rec["providertypeid"] = data.get('providertypeid')
    rec["providerusername"] = data.get('providerusername')
    rec["supervisingproviderid"] = int(data.get('supervisingproviderid') or 0)
    rec["providertype"] = data.get('providertype')
    rec["createencounterprovideridlist"] = data.get('createencounterprovideridlist')
    rec["schedulingname"] = data.get('schedulingname')
    rec["usualdepartmentid"] = data.get('usualdepartmentid')
    rec["createencounteroncheckinyn"] = bool(data.get('createencounteroncheckinyn'))
    rec["specialty"] = data.get('specialty')
    rec["hideinportalyn"] = bool(data.get('hideinportalyn'))
    rec["lastname"] = data.get('lastname')
    rec["npi"] = int(data.get('npi') or 0)
    rec["providergrouplist"] = data.get('providergrouplist')
    rec["federalidnumber"] = data.get('federalidnumber')
    rec["supervisingproviderusername"] = data.get('supervisingproviderusername')
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Provider", rec)
    return rec, 201

@app.route("/v1/providers", methods=["GET"])
def list_providers(request):
    """List Providers."""
    items = _query("Provider")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/providers/<eid>", methods=["GET"])
def get_provider(request, eid):
    """Retrieve a Provider by id."""
    rows = _query("Provider", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/providers/<eid>", methods=["POST", "PATCH"])
def update_provider(request, eid):
    """Update a Provider."""
    rows = _query("Provider", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Provider", rec)
    return rec, 200

@app.route("/v1/providers/<eid>", methods=["DELETE"])
def delete_provider(request, eid):
    """Delete a Provider."""
    rows = _query("Provider", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"athenahealth.Provider", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/departments", methods=["POST"])
def create_department(request):
    """Create a Department."""
    data = request.json or request.form or {}
    err = _require(data, ['timezoneoffset', 'singleappointmentcontractmax'])
    if err:
        return err, 400
    rec = {"id": new_id("athenahe_dep")}
    rec["timezoneoffset"] = int(data.get('timezoneoffset') or 0)
    rec["singleappointmentcontractmax"] = data.get('singleappointmentcontractmax')
    rec["state"] = data.get('state')
    rec["placeofservicefacility"] = bool(data.get('placeofservicefacility'))
    rec["latitude"] = data.get('latitude')
    rec["departmentid"] = int(data.get('departmentid') or 0)
    rec["address"] = data.get('address')
    rec["placeofservicetypeid"] = data.get('placeofservicetypeid')
    rec["longitude"] = data.get('longitude')
    rec["clinicals"] = data.get('clinicals')
    rec["timezone"] = int(data.get('timezone') or 0)
    rec["name"] = data.get('name')
    rec["patientdepartmentname"] = data.get('patientdepartmentname')
    rec["chartsharinggroupid"] = data.get('chartsharinggroupid')
    rec["placeofservicetypename"] = data.get('placeofservicetypename')
    rec["zip"] = data.get('zip')
    rec["timezonename"] = data.get('timezonename')
    rec["communicatorbrandid"] = data.get('communicatorbrandid')
    rec["medicationhistoryconsent"] = bool(data.get('medicationhistoryconsent'))
    rec["ishospitaldepartment"] = bool(data.get('ishospitaldepartment'))
    rec["providergroupid"] = data.get('providergroupid')
    rec["portalurl"] = data.get('portalurl')
    rec["city"] = data.get('city')
    rec["servicedepartment"] = bool(data.get('servicedepartment'))
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Department", rec)
    return rec, 201

@app.route("/v1/departments", methods=["GET"])
def list_departments(request):
    """List Departments."""
    items = _query("Department")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/departments/<eid>", methods=["GET"])
def get_department(request, eid):
    """Retrieve a Department by id."""
    rows = _query("Department", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/departments/<eid>", methods=["POST", "PATCH"])
def update_department(request, eid):
    """Update a Department."""
    rows = _query("Department", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Department", rec)
    return rec, 200

@app.route("/v1/departments/<eid>", methods=["DELETE"])
def delete_department(request, eid):
    """Delete a Department."""
    rows = _query("Department", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"athenahealth.Department", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/healthz", methods=["GET"])
def healthz(request):
    return {"status": "ok", "actor": "athenahealth-compat", "entities": ['Patient', 'Appointment', 'Encounter', 'Claim', 'Provider', 'Department']}, 200


if __name__ == "__main__":
    app.start()
