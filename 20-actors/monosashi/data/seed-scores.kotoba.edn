;; monosashi 物差し — offline seed of mitooshi score residuals. FICTIONAL fixture for deterministic
;; tests / autorun demo. Each residual is one scored, leak-free mitooshi forecast (G5:
;; :score/observed-at is required and is > the forecast's info-as-of upstream).
;; :score/skill = 1 − score_model/score_baseline (>0 ⇒ beats the documented baseline).
;;
;; NOTE on coherence: no tsuchifumi structural model here is over the SAME series as these forecasts
;; (transit-load / adoption ≠ earthing-burden), so :sysdyn is intentionally OMITTED and the published
;; bands carry no coherence figure. Cross-actor band-width "coherence" would be dimensionally
;; meaningless, so monosashi refuses to fabricate it (see score/coherence — same-series only).
{:residuals
 [;; ── mitooshi vs climatology (transit-load series) ───────────────────────────────────────────
  {:forecast/actor "mitooshi" :forecast/series-id "transit-load-shibuya" :forecast/fid "f-2026-06-20-transit" :forecast/band-width 22.0
   :score/observed-at "2026-06-22T12:00:00Z" :score/skill 0.41 :score/pit 0.55 :score/baseline-id "climatology"}
  {:forecast/actor "mitooshi" :forecast/series-id "transit-load-shibuya" :forecast/fid "f-2026-06-21-transit" :forecast/band-width 25.0
   :score/observed-at "2026-06-23T12:00:00Z" :score/skill 0.33 :score/pit 0.18 :score/baseline-id "climatology"}
  {:forecast/actor "mitooshi" :forecast/series-id "transit-load-shibuya" :forecast/fid "f-2026-06-24-transit" :forecast/band-width 23.0
   :score/observed-at "2026-06-26T12:00:00Z" :score/skill 0.47 :score/pit 0.82 :score/baseline-id "climatology"}
  ;; ── mitooshi vs persistence (grid-load series) — a DIFFERENT baseline, so a separate band (G12) ─
  {:forecast/actor "mitooshi" :forecast/series-id "grid-load-kanto" :forecast/fid "f-2026-06-22-load" :forecast/band-width 26.0
   :score/observed-at "2026-06-24T12:00:00Z" :score/skill 0.28 :score/pit 0.39 :score/baseline-id "persistence"}
  {:forecast/actor "mitooshi" :forecast/series-id "grid-load-kanto" :forecast/fid "f-2026-06-23-load" :forecast/band-width 23.0
   :score/observed-at "2026-06-25T12:00:00Z" :score/skill 0.52 :score/pit 0.66 :score/baseline-id "persistence"}
  ;; ── hakoniwa vs random-walk (adoption series) — marginal skill ──────────────────────────────
  {:forecast/actor "hakoniwa" :forecast/series-id "adoption-townA" :forecast/fid "h-2026-06-20-adoption" :forecast/band-width 14.0
   :score/observed-at "2026-06-24T12:00:00Z" :score/skill 0.08 :score/pit 0.12 :score/baseline-id "random-walk"}
  {:forecast/actor "hakoniwa" :forecast/series-id "adoption-townA" :forecast/fid "h-2026-06-21-adoption" :forecast/band-width 13.0
   :score/observed-at "2026-06-25T12:00:00Z" :score/skill -0.05 :score/pit 0.88 :score/baseline-id "random-walk"}
  {:forecast/actor "hakoniwa" :forecast/series-id "adoption-townA" :forecast/fid "h-2026-06-22-adoption" :forecast/band-width 15.0
   :score/observed-at "2026-06-26T12:00:00Z" :score/skill 0.11 :score/pit 0.31 :score/baseline-id "random-walk"}]}
