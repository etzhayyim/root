from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): chemical_data: dict; valid: bool; def check_specs(state: State): state['valid'] = state['chemical_data'].get('freezing_point', 0) < -20; return state; graph = StateGraph(State); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()