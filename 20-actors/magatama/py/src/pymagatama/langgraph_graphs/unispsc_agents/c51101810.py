from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): batch_id: str; quality_passed: bool; storage_temp_valid: bool; def validate_quality(state: PharmaState): return {'quality_passed': True}; def check_storage(state: PharmaState): return {'storage_temp_valid': True}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_quality); graph.add_node('storage_check', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage_check'); graph.add_edge('storage_check', END); graph = graph.compile()