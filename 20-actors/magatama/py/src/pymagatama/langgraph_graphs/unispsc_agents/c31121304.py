from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): specs: dict; valid: bool; audit_log: List[str]; def validate_specs(state: CastState): state['valid'] = all(k in state['specs'] for k in ['grade', 'tolerance']); state['audit_log'].append('Validation completed'); return state; graph = StateGraph(CastState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()