from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabTongsState(TypedDict): specs: dict; compliance_check: bool; review_status: str; def validate_specs(state: LabTongsState): state['compliance_check'] = 'Heat resistance rating' in state['specs'] and state['specs']['Material composition'] == 'Stainless Steel'; return state; def final_decision(state: LabTongsState): state['review_status'] = 'Approved' if state['compliance_check'] else 'Rejected'; return state; graph = StateGraph(LabTongsState); graph.add_node('validate', validate_specs); graph.add_node('decision', final_decision); graph.set_entry_point('validate'); graph.add_edge('validate', 'decision'); graph.add_edge('decision', END); graph = graph.compile()