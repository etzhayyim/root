from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input: str; validation_results: Annotated[list, operator.add]; status: str; def validate_purity(state: ProcState): return {'validation_results': ['Purity check passed'], 'status': 'VALIDATED'}; def check_regulatory(state: ProcState): return {'validation_results': ['CAS check passed'], 'status': 'COMPLIANT'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulatory); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compile_graph = graph.compile()