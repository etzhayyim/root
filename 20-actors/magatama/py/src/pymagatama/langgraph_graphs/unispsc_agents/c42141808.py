from langgraph.graph import StateGraph, END; from typing import TypedDict; class DiathermyState(TypedDict): electrode_type: str; sterile_cert: bool; approved: bool; def validate_electrode(state: DiathermyState): state['approved'] = state['sterile_cert'] and state['electrode_type'] in ['monopolar', 'bipolar']; return state; graph = StateGraph(DiathermyState); graph.add_node('validate', validate_electrode); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()