from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpiceState(TypedDict): vendor_docs: List[str]; qc_passed: bool; shelf_life_days: int; def validate_quality(state: SpiceState): return {'qc_passed': len(state['vendor_docs']) > 2}; def check_expiry(state: SpiceState): return {'qc_passed': state['shelf_life_days'] > 365}; graph = StateGraph(SpiceState); graph.add_node('validate_docs', validate_quality); graph.add_node('check_expiry', check_expiry); graph.set_entry_point('validate_docs'); graph.add_edge('validate_docs', 'check_expiry'); graph.add_edge('check_expiry', END); graph = graph.compile()