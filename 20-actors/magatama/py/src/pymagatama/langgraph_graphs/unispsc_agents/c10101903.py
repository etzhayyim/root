from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class LiveAnimalState(TypedDict): animal_id: str; health_status: str; transit_logs: Annotated[Sequence[str], operator.add]; graph = StateGraph(LiveAnimalState); def validate_health(state: LiveAnimalState): return {'health_status': 'verified' if state['animal_id'] else 'pending'}; def log_transit(state: LiveAnimalState): return {'transit_logs': ['Transport initiated']}; graph.add_node('validate_health', validate_health); graph.add_node('log_transit', log_transit); graph.set_entry_point('validate_health'); graph.add_edge('validate_health', 'log_transit'); graph.add_edge('log_transit', END); compiled_graph = graph.compile()