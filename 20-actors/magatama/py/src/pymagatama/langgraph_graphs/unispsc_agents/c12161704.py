from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class ChemicalProcessState(TypedDict): chemical_data: dict; validation_report: list[str]; def validate_purity(state: ChemicalProcessState): return {'validation_report': ['Purity verification passed.']}; def check_safety(state: ChemicalProcessState): return {'validation_report': ['SDS safety protocols confirmed.']}; graph = StateGraph(ChemicalProcessState); graph.add_node('purity_check', validate_purity); graph.add_node('safety_check', check_safety); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'safety_check'); graph.add_edge('safety_check', END); compiled_graph = graph.compile()