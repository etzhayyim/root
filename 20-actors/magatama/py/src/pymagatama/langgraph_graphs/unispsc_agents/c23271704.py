from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DesolderingState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: DesolderingState): errors = []; min_temp = state['specs'].get('min_temp', 300); if min_temp < 200: errors.append('Temperature too low for lead-free solder'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(DesolderingState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()