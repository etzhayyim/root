from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DispenserState(TypedDict): vendor_specs: dict; compliance_check: bool; final_report: str; def validate_specs(state: DispenserState): return {'compliance_check': 'ADA' in state['vendor_specs'].get('certifications', [])}; def generate_approval(state: DispenserState): return {'final_report': 'Dispenser meets safety and capacity standards.' if state['compliance_check'] else 'Requirement mismatch.'}; graph = StateGraph(DispenserState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_approval); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()