from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): cold_chain_verified: bool; gmp_certified: bool; batch_id: str; def verify_gmp(state: PharmState): state['gmp_certified'] = True; return state; def check_cold_chain(state: PharmState): state['cold_chain_verified'] = True; return state; graph = StateGraph(PharmState); graph.add_node('verify_gmp', verify_gmp); graph.add_node('check_cold_chain', check_cold_chain); graph.set_entry_point('verify_gmp'); graph.add_edge('verify_gmp', 'check_cold_chain'); graph.add_edge('check_cold_chain', END); graph = graph.compile()