from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RecorderState(TypedDict): data: dict; validation_errors: List[str]; graph = StateGraph(RecorderState); def validate_specs(state: RecorderState): state['validation_errors'] = [] if 'sampling_rate' not in state['data'] else []; return state; def route_approval(state: RecorderState): return 'approval' if not state['validation_errors'] else END; graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate')