from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RugState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(RugState); def validate_wool_quality(state: RugState): state['validation_results'].append('Wool grade verified'); return state; def check_fire_rating(state: RugState): state['validation_results'].append('Fire safety standard checked'); return state; graph.add_node('quality_check', validate_wool_quality); graph.add_node('safety_check', check_fire_rating); graph.set_entry_point('quality_check'); graph.add_edge('quality_check', 'safety_check'); graph.add_edge('safety_check', END); compiled_graph = graph.compile()