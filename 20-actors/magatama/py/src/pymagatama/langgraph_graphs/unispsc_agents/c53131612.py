from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BathGelState(TypedDict): ingredients: List[str]; safety_approved: bool; status: str; def validate_ingredients(state: BathGelState): status = 'approved' if len(state['ingredients']) > 0 else 'rejected'; return {'status': status, 'safety_approved': status == 'approved'}; graph = StateGraph(BathGelState); graph.add_node('validate', validate_ingredients); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()