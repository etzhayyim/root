from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EMGState(TypedDict): device_specs: dict; validation_results: List[str]; graph = StateGraph(EMGState); def validate_spec(state: EMGState): state['validation_results'].append('Checking IEC60601 compliance'); return state; def check_calibration(state: EMGState): state['validation_results'].append('Verifying calibration certificate'); return state; graph.add_node('validate', validate_spec); graph.add_node('calibrate', check_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); graph = graph.compile()