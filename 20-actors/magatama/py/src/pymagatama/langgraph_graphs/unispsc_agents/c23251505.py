from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): tube_params: dict; validation_result: bool; def validate_specs(state: State): state['validation_result'] = 'bending_radius' in state['tube_params']; return state; builder = StateGraph(State); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()