from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: CompressorState): state['validation_passed'] = 'pressure_rating' in state['specs'] and state['specs']['pressure_rating'] > 0; return state; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()