from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BoardState(TypedDict): input: str; validation_status: str; inspection_results: list[dict]; def validate_specs(state: BoardState): return {'validation_status': 'verified'} if state['input'] else {'validation_status': 'pending'}; def perform_inspection(state: BoardState): return {'inspection_results': [{'item': 'ESD_compliance', 'status': 'pass'}]}; graph = StateGraph(BoardState); graph.add_node('validate', validate_specs); graph.add_node('inspect', perform_inspection); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()