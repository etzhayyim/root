from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EducationTileState(TypedDict): tiles: List[int]; validated: bool; compliance_check: str; def validate_tiles(state: EducationTileState): return {'validated': len(state['tiles']) == 100, 'compliance_check': 'PASS'}; def check_safety(state: EducationTileState): return {'compliance_check': 'SAFETY_VERIFIED'}; graph = StateGraph(EducationTileState); graph.add_node('validate', validate_tiles); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()