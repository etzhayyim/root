from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): procurement_flow: Annotated[list, operator.add]; status: str; spec_validation: bool; def validate_api_standard(state: ProcureState): print('Validating API compliance...'); return {'status': 'validated', 'spec_validation': True}; def initiate_procurement(state: ProcureState): print('Initializing medical procurement supply chain...'); return {'procurement_flow': ['compliance_check', 'supplier_verification']}; graph = StateGraph(ProcureState); graph.add_node('init', initiate_procurement); graph.add_node('validate', validate_api_standard); graph.add_edge('init', 'validate'); graph.add_edge('validate', END); graph.set_entry_point('init'); graph = graph.compile()