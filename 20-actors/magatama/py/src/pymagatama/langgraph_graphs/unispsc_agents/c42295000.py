from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class EndoscopyState(TypedDict): device_id: str; compliance_passed: bool; inspection_result: str; def validate_equipment(state: EndoscopyState): return {'compliance_passed': True, 'inspection_result': 'Verified for surgical use'}; def generate_report(state: EndoscopyState): return {'inspection_result': 'Final inspection report generated'}; graph = StateGraph(EndoscopyState); graph.add_node('validate', validate_equipment); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled = graph.compile()