from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NeutronState(TypedDict): vendor_docs: List[str]; safety_compliance: bool; export_approved: bool; def check_export_controls(state: NeutronState): return {'export_approved': True}; def validate_safety(state: NeutronState): return {'safety_compliance': True}; graph = StateGraph(NeutronState); graph.add_node('export_review', check_export_controls); graph.add_node('safety_validation', validate_safety); graph.set_entry_point('export_review'); graph.add_edge('export_review', 'safety_validation'); graph.add_edge('safety_validation', END); compile_graph = graph.compile()