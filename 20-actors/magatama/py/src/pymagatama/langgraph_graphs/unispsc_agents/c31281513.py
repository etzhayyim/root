from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): metal_grade: str; dimensions: dict; status: str; def validate_purity(state: ProcureState): return {'status': 'validated'} if state.get('metal_grade') == 'certified' else {'status': 'rejected'}; def check_dimensions(state: ProcureState): return {'status': 'dimensional_pass'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_purity); graph.add_node('check_dims', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_dims'); graph.add_edge('check_dims', END); graph = graph.compile()