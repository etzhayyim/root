from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): component_id: str; quality_report: dict; compliance_cleared: bool; def validate_physics(state: SpecState): return {'compliance_cleared': state.get('quality_report', {}).get('is_sound', False)}; def check_export_controls(state: SpecState): return {'compliance_cleared': True if state['compliance_cleared'] else False}; graph = StateGraph(SpecState); graph.add_node('physics_val', validate_physics); graph.add_node('export_check', check_export_controls); graph.set_entry_point('physics_val'); graph.add_edge('physics_val', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()