from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReHabState(TypedDict): items: List[str]; validation_results: dict; class Pipeline: def validate_specs(self, state: ReHabState): state['validation_results'] = {'latex_check': True, 'tensile_check': True}; return state; def finalize_approval(self, state: ReHabState): return state; pipeline = Pipeline(); graph = StateGraph(ReHabState); graph.add_node('validate', pipeline.validate_specs); graph.add_node('approve', pipeline.finalize_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()