from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CageState(TypedDict): cage_specs: dict; validation_errors: List[str]; def validate_material(state: CageState): print('Validating autoclave resistance...'); return state; def check_welfare(state: CageState): print('Checking compliance with welfare standards...'); return state; graph = StateGraph(CageState); graph.add_node('material_check', validate_material); graph.add_node('welfare_check', check_welfare); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'welfare_check'); graph.add_edge('welfare_check', END); graph = graph.compile()