from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): harvest_data: dict; quality_report: str; approved: bool; def check_quality(state: State): return {'quality_report': 'passed' if state['harvest_data']['brix'] > 12 else 'failed'}; def update_status(state: State): return {'approved': state['quality_report'] == 'passed'}; graph = StateGraph(State); graph.add_node('check', check_quality); graph.add_node('verify', update_status); graph.set_entry_point('check'); graph.add_edge('check', 'verify'); graph.add_edge('verify', END); graph.compile()