from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BakingState(TypedDict): ingredients: List[str]; validated_items: List[dict]; status: str; def validate_ingredient(state: BakingState): validated = [{'item': i, 'safe': True} for i in state['ingredients']]; return {'validated_items': validated, 'status': 'validated'}; def check_compliance(state: BakingState): return {'status': 'compliant' if all(i['safe'] for i in state['validated_items']) else 'failed'}; graph = StateGraph(BakingState); graph.add_node('validate', validate_ingredient); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()