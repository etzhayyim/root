from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validated_specs: List[bool]; graph = StateGraph(State); def validate_item(state: State): return {'validated_specs': [True for _ in state['items']]} if state['items'] else {'validated_specs': []}; graph.add_node('validate', validate_item); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()