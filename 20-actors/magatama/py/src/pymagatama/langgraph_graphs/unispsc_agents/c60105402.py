from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): materials: List[str]; validated_content: bool; def validate_content(state: State): return {'validated_content': all('skill' in item.lower() for item in state['materials'])}; def finalize_procurement(state: State): return {'validated_content': True}; graph = StateGraph(State); graph.add_node('validate', validate_content); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()