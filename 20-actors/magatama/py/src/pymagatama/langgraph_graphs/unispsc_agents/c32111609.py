from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IGBTState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: IGBTState): errs = []; required = ['Vces', 'Ic', 'Package']; for field in required: if field not in state['specs']: errs.append(f'Missing {field}'); return {'validation_errors': errs, 'approved': len(errs) == 0}; graph = StateGraph(IGBTState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()