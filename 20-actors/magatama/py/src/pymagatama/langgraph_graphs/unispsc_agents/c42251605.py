from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReHabState(TypedDict): device_specs: dict; certification_status: bool; workflow_log: List[str]; def validate_medical_specs(state: ReHabState): state['workflow_log'].append('Validating medical device compliance'); state['certification_status'] = True; return state; def check_safety_standards(state: ReHabState): state['workflow_log'].append('Verifying ISO 13485 compliance'); return state; graph = StateGraph(ReHabState); graph.add_node('validate', validate_medical_specs); graph.add_node('safety', check_safety_standards); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()