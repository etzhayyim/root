from langgraph.graph import StateGraph, END; from typing import TypedDict; class FuserState(TypedDict): model: str; quality_score: float; approved: bool; def validate_compatibility(state: FuserState): state['approved'] = state['model'].startswith('OEM-'); return state; def check_quality(state: FuserState): state['quality_score'] = 1.0; return state; graph = StateGraph(FuserState); graph.add_node('validate', validate_compatibility); graph.add_node('quality', check_quality); graph.add_edge('validate', 'quality'); graph.add_edge('quality', END); graph.set_entry_point('validate'); graph = graph.compile()