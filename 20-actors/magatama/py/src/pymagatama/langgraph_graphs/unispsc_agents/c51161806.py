from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_name: str; validation_passed: bool; logs: List[str]; def validate_chemical(state: ProcureState): state['validation_passed'] = bool(state.get('chemical_name') == 'Ammonium chloride'); state['logs'].append('Validation completed'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_chemical); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()