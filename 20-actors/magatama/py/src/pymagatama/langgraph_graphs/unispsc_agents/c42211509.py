from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProtectionState(TypedDict): specs: dict; compliant: bool; safety_check: str; def validate_specs(state: ProtectionState): state['compliant'] = state['specs'].get('impact_rating', 0) > 50; return state; def final_approval(state: ProtectionState): state['safety_check'] = 'Certified' if state['compliant'] else 'Rejected'; return state; graph = StateGraph(ProtectionState); graph.add_node('validate', validate_specs); graph.add_node('approve', final_approval); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()