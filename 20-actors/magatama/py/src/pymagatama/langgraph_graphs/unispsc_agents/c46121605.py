from langgraph.graph import StateGraph, END; from typing import TypedDict; class ControlState(TypedDict): specs: dict; valid: bool; log: list; def validate_specs(state: ControlState): state['valid'] = 'thrust_rating' in state['specs']; state['log'].append('Validation complete'); return state; def check_export(state: ControlState): state['log'].append('Export control cleared'); return state; graph = StateGraph(ControlState); graph.add_node('validate', validate_specs); graph.add_node('export', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); app = graph.compile()