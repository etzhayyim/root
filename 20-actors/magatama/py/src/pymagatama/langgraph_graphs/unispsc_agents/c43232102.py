from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoftwareState(TypedDict): vendor: str; license_type: str; specs_verified: bool; compliance_report: str; def validate_license(state): state['specs_verified'] = state['license_type'] in ['Subscription', 'Perpetual']; return state; def check_compliance(state): state['compliance_report'] = 'Verified' if state['specs_verified'] else 'Rejected'; return state; graph = StateGraph(SoftwareState); graph.add_node('validate', validate_license); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); app = graph.compile()