from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PrinterProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state): specs = state.get('specs', {}); errors = []; if not specs.get('resolution'): errors.append('Resolution missing'); return {'validation_errors': errors}; def check_compliance(state): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(PrinterProcurementState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()