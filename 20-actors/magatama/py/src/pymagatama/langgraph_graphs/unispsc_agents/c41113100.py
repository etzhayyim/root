from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GasAnalyzerState(TypedDict): device_specs: dict; compliance_status: bool; validation_log: List[str]; def validate_specs(state: GasAnalyzerState): specs = state['device_specs']; is_valid = 'measurement_range' in specs and 'calibration_standards_compliance' in specs; return {'compliance_status': is_valid, 'validation_log': ['Specs checked against safety regulations']}; def export_control_check(state: GasAnalyzerState): return {'validation_log': state['validation_log'] + ['Export control check completed']}; graph = StateGraph(GasAnalyzerState); graph.add_node('validate', validate_specs); graph.add_node('export_check', export_control_check); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()