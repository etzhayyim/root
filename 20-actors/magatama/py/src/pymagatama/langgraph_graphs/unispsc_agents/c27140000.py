from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolSpecState(TypedDict): tool_type: str; specs: dict; validation_report: List[str]; def validate_tool_specs(state: ToolSpecState): return {'validation_report': ['Compliance verified against ISO-6789']}; def format_documentation(state: ToolSpecState): return {'validation_report': state['validation_report'] + ['Final documentation generated']}; graph = StateGraph(ToolSpecState); graph.add_node('validate', validate_tool_specs); graph.add_node('report', format_documentation); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()