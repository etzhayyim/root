from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): n_code: str; spec: dict; approved: bool; def validate_gmp(state: ProcState): state['approved'] = state['spec'].get('gmp_cert') is not None; return state; def check_purity(state: ProcState): state['approved'] = state['approved'] and state['spec'].get('purity', 0) >= 99.0; return state; graph = StateGraph(ProcState); graph.add_node('verify_gmp', validate_gmp); graph.add_node('check_purity', check_purity); graph.set_entry_point('verify_gmp'); graph.add_edge('verify_gmp', 'check_purity'); graph.add_edge('check_purity', END); graph = graph.compile()