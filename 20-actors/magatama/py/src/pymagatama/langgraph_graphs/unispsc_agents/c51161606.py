from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): batch_data: dict; approval: bool; graph = StateGraph(PharmaState); def validate_compliance(state: PharmaState): state['approval'] = 'CoA' in state['batch_data'] and state['batch_data']['expiry_date'] > '2025-01-01'; return state; graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()