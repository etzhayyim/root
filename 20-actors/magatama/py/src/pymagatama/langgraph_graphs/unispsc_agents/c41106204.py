from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MediaState(TypedDict): media_type: str; is_sterile: bool; defects: List[str]; def validate_sterility(state: MediaState): return {'is_sterile': True}; def inspect_media(state: MediaState): return {'defects': [] if state['is_sterile'] else ['Contamination Detected']}; graph = StateGraph(MediaState); graph.add_node('validate', validate_sterility); graph.add_node('inspect', inspect_media); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()