from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SplintRollState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_grade(state: SplintRollState): state['validated'] = all('ISO' in c for c in state['certs']); return state; def route_storage_setup(state: SplintRollState): return 'validate' if state['validated'] else END; graph = StateGraph(SplintRollState); graph.add_node('validate', validate_medical_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()