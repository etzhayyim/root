from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class QuayState(TypedDict): status: str; spec_docs: List[str]; validation_results: dict; def validate_structural_specs(state: QuayState): state['validation_results'] = {'structural_integrity': 'pending', 'compliance': 'verified'}; return state; def check_maritime_regulations(state: QuayState): state['status'] = 'regulatory_passed'; return state; graph = StateGraph(QuayState); graph.add_node('structural_validation', validate_structural_specs); graph.add_node('regulatory_check', check_maritime_regulations); graph.set_entry_point('structural_validation'); graph.add_edge('structural_validation', 'regulatory_check'); graph.add_edge('regulatory_check', END); graph = graph.compile()