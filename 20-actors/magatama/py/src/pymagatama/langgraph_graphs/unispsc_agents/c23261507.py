from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PrintingState(TypedDict): model_spec: dict; validation_errors: List[str]; def validate_specs(state: PrintingState): errors = []; if state['model_spec'].get('layer_thickness', 0) < 10: errors.append('Resolution exceeds threshold.'); return {'validation_errors': errors}; graph = StateGraph(PrintingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()