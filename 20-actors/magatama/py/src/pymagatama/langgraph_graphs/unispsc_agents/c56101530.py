from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CabinetState(TypedDict): dimensions: dict; material: str; validated: bool; graph = StateGraph(CabinetState); def validate_specs(state: CabinetState): return {'validated': all([state.get('dimensions'), state.get('material')])}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()