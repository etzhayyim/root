from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SteeringState(TypedDict): component_id: str; specs: dict; validation_status: bool; def val_step(state: SteeringState): state['validation_status'] = 'material_grade' in state['specs'] and 'load_capacity' in state['specs']; return state; def report_step(state: SteeringState): print(f'Processing {state['component_id']}: Status={state['validation_status']}'); return state; graph = StateGraph(SteeringState); graph.add_node('validate', val_step); graph.add_node('report', report_step); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()