from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TireState(TypedDict): tire_specs: dict; validation_results: List[str]; def validate_size(state: TireState): result = 'Valid' if 'ETRTO_size_standard' in state['tire_specs'] else 'Invalid'; return {'validation_results': [result]}; def check_pressure(state: TireState): return {'validation_results': state['validation_results'] + ['Pressure_Checked']}; graph = StateGraph(TireState); graph.add_node('validate', validate_size); graph.add_node('pressure', check_pressure); graph.set_entry_point('validate'); graph.add_edge('validate', 'pressure'); graph.add_edge('pressure', END); compiled_graph = graph.compile()