from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): tension_spec: dict; validation_passed: bool; def check_spec(state: State): state['validation_passed'] = 'tension_range' in state['tension_spec']; return state; def report_result(state: State): return {'result': 'verified' if state['validation_passed'] else 'rejected'}; graph = StateGraph(State); graph.add_node('check', check_spec); graph.add_node('report', report_result); graph.set_entry_point('check'); graph.add_edge('check', 'report'); graph.add_edge('report', END); graph = graph.compile()