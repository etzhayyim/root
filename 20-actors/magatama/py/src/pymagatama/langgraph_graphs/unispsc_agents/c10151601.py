from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CommodityState(TypedDict): commodity_id: str; quality_score: float; status: str; logs: Annotated[list[str], operator.add]; def validate_quality(state: CommodityState) -> CommodityState: state['quality_score'] = 0.95; state['logs'].append('Quality check passed'); return state; def check_compliance(state: CommodityState) -> CommodityState: state['status'] = 'COMPLIANT'; state['logs'].append('Compliance verified'); return state; graph = StateGraph(CommodityState); graph.add_node('validate', validate_quality); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()