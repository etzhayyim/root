from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; qc_passed: bool; def validate_food_safety(state: ProcureState): return {'qc_passed': True}; def check_expiry(state: ProcureState): return {'qc_passed': False} if 'expired' in state['items'] else {'qc_passed': True}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_food_safety); graph.add_node('expiry', check_expiry); graph.set_entry_point('validate'); graph.add_edge('validate', 'expiry'); graph.add_edge('expiry', END); graph = graph.compile()