from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TerminalState(TypedDict): vendor_id: str; structural_specs: dict; compliance_docs: List[str]; approved: bool; def validate_specs(state: TerminalState): state['approved'] = all(k in state['structural_specs'] for k in ['load_cap', 'gate_ai']); return state; def check_compliance(state: TerminalState): state['compliance_docs'].append('ISPS_VALIDATED'); return state; graph = StateGraph(TerminalState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()