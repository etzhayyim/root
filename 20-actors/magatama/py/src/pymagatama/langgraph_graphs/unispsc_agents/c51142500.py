from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): medicine_name: str; dosage_form: str; is_approved: bool; batch_status: str; def validate_regulatory(state: DrugState): state['is_approved'] = True; return state; def check_supply_chain(state: DrugState): state['batch_status'] = 'verified' if state['is_approved'] else 'rejected'; return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_regulatory); graph.add_node('supply', check_supply_chain); graph.set_entry_point('validate'); graph.add_edge('validate', 'supply'); graph.add_edge('supply', END); app = graph.compile()