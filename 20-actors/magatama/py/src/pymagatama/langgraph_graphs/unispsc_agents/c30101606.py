from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: SupplyState): alloy = state['specs'].get('alloy'); valid = alloy in ['6061', '7075']; return {'valid': valid, 'error': '' if valid else 'Invalid alloy type'}; graph = StateGraph(SupplyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()