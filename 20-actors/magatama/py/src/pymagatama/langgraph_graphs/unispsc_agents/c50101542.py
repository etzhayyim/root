from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): input_data: dict; valid: bool; def validate_specs(state: ProcState): return {'valid': 'moisture_content_percentage' in state['input_data']}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()