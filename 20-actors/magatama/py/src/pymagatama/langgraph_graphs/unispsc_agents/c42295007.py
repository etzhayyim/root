from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ImagingState(TypedDict): vendor_docs: List[str]; compliance_check: bool; spec_validation: bool; def validate_specs(state: ImagingState): return {'spec_validation': True} if 'FDA' in state['vendor_docs'] else {'spec_validation': False}; def compliance_audit(state: ImagingState): return {'compliance_check': True}; graph = StateGraph(ImagingState); graph.add_node('validate', validate_specs); graph.add_node('audit', compliance_audit); graph.set_entry_point('validate'); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); graph = graph.compile()