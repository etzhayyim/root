from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): bearing_specs: dict; validation_report: str; def validate_specs(state: BearingState): specs = state['bearing_specs']; report = 'PASS' if specs.get('load_rating', 0) > 0 else 'FAIL'; return {'validation_report': report}; def route_procurement(state: BearingState): return 'end' if state['validation_report'] == 'PASS' else 'end'; graph = StateGraph(BearingState); graph.add_node('validate', validate_specs); graph.add_edge('validate', 'end'); graph.set_entry_point('validate'); graph = graph.compile()