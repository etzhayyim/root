from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): rivets: List[dict]; material_specs: dict; validated: bool; def validate_hastelloy_specs(state: AssemblyState): state['validated'] = 'HeatTreatment' in state['material_specs'] and len(state['rivets']) > 0; return state; def check_compliance(state: AssemblyState): return 'passed' if state['validated'] else 'failed'; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_hastelloy_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); app = graph.compile()