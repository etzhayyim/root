from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PolymerProcessState(TypedDict): chemical_id: str; purity: float; status: str; graph = StateGraph(PolymerProcessState); def validate_purity(state: PolymerProcessState) -> PolymerProcessState: return {'status': 'validated' if state['purity'] >= 99.0 else 'rejected'}; def process_safety_check(state: PolymerProcessState) -> PolymerProcessState: return {'status': 'approved'}; graph.add_node('validate', validate_purity); graph.add_node('safety', process_safety_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); app = graph.compile()