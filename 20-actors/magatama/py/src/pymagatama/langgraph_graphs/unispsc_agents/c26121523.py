from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WireState(TypedDict): specs: dict; valid: bool; def validate_specs(state: WireState): state['valid'] = 'Diameter' in state['specs'] and 'Material' in state['specs']; return state; builder = StateGraph(WireState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()