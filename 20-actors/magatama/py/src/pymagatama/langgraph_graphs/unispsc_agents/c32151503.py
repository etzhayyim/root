from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: State): state['validation_errors'] = []; if 'voltage' not in state['specs']: state['validation_errors'].append('Voltage missing'); return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()