from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WireState(TypedDict): wire_spec: dict; validation_results: List[str]; graph = StateGraph(WireState); def validate_spec(state: WireState): results = []; spec = state['wire_spec']; if spec.get('voltage', 0) < 0: results.append('Invalid voltage'); return {'validation_results': results}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()