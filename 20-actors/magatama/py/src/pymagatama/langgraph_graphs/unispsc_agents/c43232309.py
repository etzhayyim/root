from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SearchState(TypedDict): data_sources: list; query_params: dict; validation_report: str; def validate_config(state: SearchState): return {'validation_report': 'Config validated against security protocols.'} if state.get('query_params') else {'validation_report': 'Missing params'}; def run_indexing(state: SearchState): return {'validation_report': 'Indexing complete'}; graph = StateGraph(SearchState); graph.add_node('validate', validate_config); graph.add_node('index', run_indexing); graph.add_edge('validate', 'index'); graph.add_edge('index', END); graph.set_entry_point('validate'); graph = graph.compile()