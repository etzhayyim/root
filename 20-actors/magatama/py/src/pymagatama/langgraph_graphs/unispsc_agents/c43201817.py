from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CDState(TypedDict): drive_specs: dict; validation_results: list[str]; graph = StateGraph(CDState); def validate_specs(state: CDState): results = []; specs = state.get('drive_specs', {}); if 'interface' not in specs: results.append('Missing interface'); return {'validation_results': results}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()