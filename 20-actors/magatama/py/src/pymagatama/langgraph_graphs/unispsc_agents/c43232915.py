from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): vendor_api: str; compliance_check: bool; connectivity_specs: dict; graph = StateGraph(State); def validate_compatibility(state: State): return {'compliance_check': True}; def update_specs(state: State): return {'connectivity_specs': {'protocols': ['REST', 'gRPC']}}; graph.add_node('validate', validate_compatibility); graph.add_node('specs', update_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'specs'); graph.add_edge('specs', END); app = graph.compile()