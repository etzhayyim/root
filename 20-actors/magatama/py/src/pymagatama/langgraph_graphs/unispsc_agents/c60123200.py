from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RibbonState(TypedDict): materials: List[str]; validation_status: bool; def validate_materials(state: RibbonState): return {'validation_status': all('spec' in m.lower() for m in state['materials'])}; graph = StateGraph(RibbonState); graph.add_node('validate', validate_materials); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()