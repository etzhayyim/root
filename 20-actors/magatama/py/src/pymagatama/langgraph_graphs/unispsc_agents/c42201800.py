from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class XRayState(TypedDict): specs: dict; validation_report: List[str]; def validate_radiation_shielding(state: XRayState): return {'validation_report': ['Shielding compliant']} if state['specs'].get('radiation_shielding_certification') else {'validation_report': ['Shielding failed']}; def check_regulatory_docs(state: XRayState): return {'validation_report': state['validation_report'] + ['Docs verified']}; graph = StateGraph(XRayState); graph.add_node('shielding', validate_radiation_shielding); graph.add_node('docs', check_regulatory_docs); graph.set_entry_point('shielding'); graph.add_edge('shielding', 'docs'); graph.add_edge('docs', END); graph = graph.compile()