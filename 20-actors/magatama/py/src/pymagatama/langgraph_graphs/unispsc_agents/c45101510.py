from typing import TypedDict, List; import operator; from langgraph.graph import StateGraph, END; class PrinterState(TypedDict): vendor_specs: dict; compliance_score: int; approval_status: str; def validate_specs(state: PrinterState): print('Validating flexographic mechanical specs...'); return {'compliance_score': 90 if state['vendor_specs'].get('width') else 0}; def finalize_procurement(state: PrinterState): return {'approval_status': 'APPROVED' if state['compliance_score'] > 80 else 'REJECTED'}; graph = StateGraph(PrinterState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()