from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): medicine_name: str; temperature_logs: List[float]; validation_status: bool; def validate_cold_chain(state: DrugProcurementState): return {'validation_status': all(t <= 8.0 for t in state['temperature_logs'])}; def verify_compliance(state: DrugProcurementState): print(f'Processing {state["medicine_name"]} compliance...'); return {'validation_status': True}; graph = StateGraph(DrugProcurementState); graph.add_node('cold_chain', validate_cold_chain); graph.add_node('compliance', verify_compliance); graph.set_entry_point('cold_chain'); graph.add_edge('cold_chain', 'compliance'); graph.add_edge('compliance', END); graph.compile()