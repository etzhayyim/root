from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_id: str; quality_passed: bool; storage_validated: bool; def validate_reagent(state: ReagentState): state['quality_passed'] = True; return state; def check_storage(state: ReagentState): state['storage_validated'] = True; return state; graph = StateGraph(ReagentState); graph.add_node('validate', validate_reagent); graph.add_node('storage', check_storage); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph.set_entry_point('validate'); graph = graph.compile()