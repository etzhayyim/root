from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor: str; compliant: bool; item_type: str; def validate_safety(state: ProcState): state['compliant'] = state['item_type'] in ['accessory', 'storage']; return state; def check_regulations(state: ProcState): return state; graph = StateGraph(ProcState); graph.add_node('safety_check', validate_safety); graph.add_node('reg_audit', check_regulations); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'reg_audit'); graph.add_edge('reg_audit', END); graph = graph.compile()