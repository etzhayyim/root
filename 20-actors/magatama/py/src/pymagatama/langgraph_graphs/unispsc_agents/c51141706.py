from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): commodity: str; compliance_docs: List[str]; approved: bool; def validate_docs(state: ProcureState): state['approved'] = 'GMP' in state['compliance_docs'] and 'COA' in state['compliance_docs']; return state; builder = StateGraph(ProcureState); builder.add_node('validate', validate_docs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()