from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoftwareState(TypedDict): vendor: str; version: str; security_audit_passed: bool; deployment_type: str; def validate_security(state: SoftwareState): state['security_audit_passed'] = True; return state; def prepare_deployment(state: SoftwareState): return {'deployment_type': 'cloud'}; graph = StateGraph(SoftwareState); graph.add_node('validate', validate_security); graph.add_node('deploy', prepare_deployment); graph.set_entry_point('validate'); graph.add_edge('validate', 'deploy'); graph.add_edge('deploy', END); app = graph.compile()