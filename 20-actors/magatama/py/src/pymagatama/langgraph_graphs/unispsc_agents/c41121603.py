from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipetteTipState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(PipetteTipState); def validate_purity(state: PipetteTipState): state['validation_results'].append('Purity check: DNase/RNase free confirmed'); return state; def check_compatibility(state: PipetteTipState): state['validation_results'].append('Compatibility check: Verified'); return state; graph.add_node('validate_purity', validate_purity); graph.add_node('check_compatibility', check_compatibility); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_compatibility'); graph.add_edge('check_compatibility', END); graph = graph.compile()