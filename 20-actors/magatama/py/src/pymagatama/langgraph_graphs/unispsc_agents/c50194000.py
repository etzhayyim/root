from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FruitState(TypedDict): fruit_type: str; inspection_passed: bool; shelf_life: int; def validate_quality(state: FruitState): print(f'Validating quality for {state["fruit_type"]}'); return {'inspection_passed': True} if state['shelf_life'] > 0 else {'inspection_passed': False}; def check_safety(state: FruitState): print(f'Checking food safety regulations...'); return {'inspection_passed': state['inspection_passed']}; graph = StateGraph(FruitState); graph.add_node('validate', validate_quality); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()