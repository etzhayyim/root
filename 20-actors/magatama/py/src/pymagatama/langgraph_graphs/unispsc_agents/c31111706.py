from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtrusionState(TypedDict): part_id: str; specs: dict; valid: bool; def validate_specs(state: ExtrusionState): state['valid'] = 'Material Grade' in state['specs'] and 'Dimensional Tolerance' in state['specs']; return state; def check_risk(state: ExtrusionState): print('Checking export controls...'); return state; graph = StateGraph(ExtrusionState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()