from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; def validate_specs(state: State): results = ["Spec Compliant" if 'voltage' in state['input_data'] else "Invalid Spec"]; return {'validation_results': results}; def log_procurement(state: State): return {'validation_results': ['Logging complete']}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('log', log_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph = graph.compile()