from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ParasailingState(TypedDict): equipment_type: str; safety_checks: List[str]; approved: bool; def validate_equipment(state: ParasailingState): state['approved'] = 'breaking_strength' in state['safety_checks'] and 'tensile_test' in state['safety_checks']; return state; builder = StateGraph(ParasailingState); builder.add_node('validate', validate_equipment); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()