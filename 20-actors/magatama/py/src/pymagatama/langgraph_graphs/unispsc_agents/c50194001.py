from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PreservedApricotState(TypedDict): specs: dict; validation_results: List[str]; def validate_moisture(state: PreservedApricotState): moisture = state['specs'].get('moisture_content_percent', 0); state['validation_results'].append('Pass' if 0 < moisture < 20 else 'Fail'); return state; def validate_compliance(state: PreservedApricotState): has_cert = state['specs'].get('certification_haccp_iso22000', False); state['validation_results'].append('Compliant' if has_cert else 'Non-Compliant'); return state; graph = StateGraph(PreservedApricotState); graph.add_node('moisture_check', validate_moisture); graph.add_node('compliance_check', validate_compliance); graph.set_entry_point('moisture_check'); graph.add_edge('moisture_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()