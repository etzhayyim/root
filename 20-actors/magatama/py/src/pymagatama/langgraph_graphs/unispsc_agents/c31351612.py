from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class CopperProcurementState(TypedDict): tube_specs: dict; validation_results: List[str]; def validate_specs(state: CopperProcurementState): results = []; if 'wall_thickness' not in state['tube_specs']: results.append('Missing Wall Thickness'); return {'validation_results': results}; def check_welding_compliance(state: CopperProcurementState): return {'validation_results': state['validation_results'] + ['Certification verified']}; graph = StateGraph(CopperProcurementState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_welding_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()