from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShipState(TypedDict): ship_id: str; structural_check: bool; compliance_status: List[str]; graph = StateGraph(ShipState); def validate_hull(state: ShipState): return {'structural_check': True}; def check_compliance(state: ShipState): return {'compliance_status': ['ITAR_Verified', 'Naval_Standard']}; graph.add_node('validate', validate_hull); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()