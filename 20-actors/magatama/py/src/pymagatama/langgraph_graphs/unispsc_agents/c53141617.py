from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FabricState(TypedDict): fabric_type: str; specs: dict; valid: bool; def validate_specs(state: FabricState): state['valid'] = all(key in state['specs'] for key in ['weight', 'composition']); return state; graph = StateGraph(FabricState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()