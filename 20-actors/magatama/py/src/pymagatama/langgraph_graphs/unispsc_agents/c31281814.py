from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): component_specs: dict; compliance_status: bool; task_list: List[str]; def validate_lead_specs(state: State): specs = state.get('component_specs', {}); return {'compliance_status': specs.get('purity', 0) > 99.9}; def check_hazmat(state: State): return {'task_list': ['MSDS verification', 'Hazardous shipping label audit']}; graph = StateGraph(State); graph.add_node('validate', validate_lead_specs); graph.add_node('compliance', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()