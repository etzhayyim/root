from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrillState(TypedDict): model_number: str; material: str; verified: bool; graph = StateGraph(DrillState); def validate_specs(state: DrillState): state['verified'] = all([state['model_number'], state['material']]); return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()