from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StampState(TypedDict): stamp_code: str; specs: dict; approved: bool; def validate_spec(state: StampState) -> StampState: state['approved'] = state['specs'].get('hardness', 0) > 50; return state; def finalize_order(state: StampState) -> StampState: return state; graph = StateGraph(StampState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()