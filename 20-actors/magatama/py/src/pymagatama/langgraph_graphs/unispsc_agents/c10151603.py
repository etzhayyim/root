from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class GrainProcurementState(TypedDict): grain_code: str; quality_metrics: Dict; steps: List[str]; def validate_moisture(state: GrainProcurementState) -> GrainProcurementState: moisture = state['quality_metrics'].get('moisture', 0); state['steps'].append('moisture_validated' if moisture < 14 else 'moisture_rejected'); return state; def check_purity(state: GrainProcurementState) -> GrainProcurementState: state['steps'].append('purity_verified'); return state; graph = StateGraph(GrainProcurementState); graph.add_node('validate', validate_moisture); graph.add_node('purity', check_purity); graph.set_entry_point('validate'); graph.add_edge('validate', 'purity'); graph.add_edge('purity', END); graph = graph.compile()