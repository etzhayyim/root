from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnchorState(TypedDict): specs: dict; valid: bool; def validate_specs(state: AnchorState): state['valid'] = 'material' in state['specs'] and 'tensile' in state['specs']; return state; builder = StateGraph(AnchorState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()