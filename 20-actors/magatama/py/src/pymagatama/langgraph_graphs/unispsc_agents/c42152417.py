from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): input_data: dict; validation_results: List[str]; graph = StateGraph(State); def validate_investment_data(state: State): results = []; if 'expansion_rate' not in state['input_data']: results.append('Missing thermal expansion rate'); return {'validation_results': results}; graph.add_node('validate', validate_investment_data); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()