from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): part_id: str; specs: dict; valid: bool; def validate_specs(state: ForgingState): state['valid'] = all(k in state['specs'] for k in ['material', 'dimensions']); return state; def check_quality(state: ForgingState): return 'valid' if state['valid'] else 'invalid'; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()