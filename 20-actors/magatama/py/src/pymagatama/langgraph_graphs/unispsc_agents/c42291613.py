from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalSupplyState(TypedDict): items: List[str]; validated_items: List[str]; inspection_status: str; def validate_medical_device(state: SurgicalSupplyState): print('Validating sterile integrity...'); return {'validated_items': state['items'], 'inspection_status': 'COMPLIANT'}; graph = StateGraph(SurgicalSupplyState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()