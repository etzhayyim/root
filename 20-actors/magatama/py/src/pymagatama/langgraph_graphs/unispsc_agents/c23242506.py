from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MachineState(TypedDict): specs: dict; compliance_checked: bool; error: str; graph = StateGraph(MachineState); def validate_specs(state: MachineState): return {'compliance_checked': 'Spindle Torque' in state['specs']}; def export_control_check(state: MachineState): return {'compliance_checked': True}; graph.add_node('validate', validate_specs); graph.add_node('export_check', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.compile()