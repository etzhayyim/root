from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TurnbuckleState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(TurnbuckleState); def validate_spec(state: TurnbuckleState): result = 'Valid' if state['specs'].get('load_limit') else 'Incomplete'; return {'validation_results': [result]}; def check_certification(state: TurnbuckleState): return {'validation_results': state['validation_results'] + ['CertVerified']}; graph.add_node('validate', validate_spec); graph.add_node('certify', check_certification); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); app = graph.compile()