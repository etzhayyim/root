from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DentalLightState(TypedDict): spec_data: dict; verification_passed: bool; def validate_spec(state: DentalLightState): lux = state['spec_data'].get('lux', 0); cri = state['spec_data'].get('cri', 0); state['verification_passed'] = lux >= 8000 and cri >= 90; return state; graph = StateGraph(DentalLightState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()