from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): medicine: str; temp_log: List[float]; is_valid: bool; def validate_conditions(state: PharmaState) -> PharmaState: state['is_valid'] = all(2 <= t <= 8 for t in state['temp_log']); return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_conditions); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()