from langgraph.graph import StateGraph, END; from typing import TypedDict; class PaperBagState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PaperBagState): state['valid'] = all(k in state['specs'] for k in ['gsm', 'size']); return state; graph = StateGraph(PaperBagState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()