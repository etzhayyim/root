from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DeviceState(TypedDict): components: Annotated[list, operator.add]; status: str; def validate_catheter_dev(state: DeviceState): return {'status': 'Validated' if len(state['components']) > 0 else 'Error'}; graph = StateGraph(DeviceState); graph.add_node('validate', validate_catheter_dev); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()