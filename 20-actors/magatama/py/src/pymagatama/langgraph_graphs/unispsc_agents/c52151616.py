from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpatulaState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: SpatulaState): if state['specs'].get('heat_resistance_degree_celsius', 0) > 100: return {'compliance': True, 'status': 'Validated'}; return {'compliance': False, 'status': 'Failed_Heat_Resistance'}; def check_food_safety(state: SpatulaState): return {'compliance': 'food_grade' in state['specs'].get('certifications', [])}; graph = StateGraph(SpatulaState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_food_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()