from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CargoState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: CargoState): state['valid'] = 'Load capacity rating' in state['specs']; state['log'].append('Validation completed'); return state; graph = StateGraph(CargoState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()