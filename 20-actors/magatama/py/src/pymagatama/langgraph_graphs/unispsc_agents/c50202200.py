from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AlcoholState(TypedDict): items: List[str]; compliance_check: bool; storage_temp: float; graph = StateGraph(AlcoholState); def check_legal_compliance(state: AlcoholState): return { "compliance_check": True }; def validate_storage(state: AlcoholState): return { "storage_temp": 15.0 if state.get("storage_temp", 0) < 18 else state["storage_temp"] }; graph.add_node("compliance", check_legal_compliance); graph.add_node("storage", validate_storage); graph.set_entry_point("compliance"); graph.add_edge("compliance", "storage"); graph.add_edge("storage", END); graph = graph.compile()