from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): specs: dict; valid: bool; def val_specs(state: ProcState): state['valid'] = all(k in state['specs'] for k in ['alloy', 'pressure']); return state; graph = StateGraph(ProcState); graph.add_node('validate', val_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()