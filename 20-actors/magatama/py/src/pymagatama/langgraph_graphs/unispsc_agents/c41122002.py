from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AnalysisState(TypedDict): needle_specs: dict; validation_results: List[str]; graph = StateGraph(AnalysisState); def validate_specs(state: AnalysisState): return {'validation_results': ['Specs Verified'] if state['needle_specs']['gauge'] > 0 else ['Invalid Gauge']}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()