from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; valid: bool; workflow_log: list; graph = StateGraph(State); def validate_spec(state: State): state['valid'] = all(k in state['input_data'] for k in ['iso_cert', 'material_safety']); state['workflow_log'].append('Validation completed'); return state; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()