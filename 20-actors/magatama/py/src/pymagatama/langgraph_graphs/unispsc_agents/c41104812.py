from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DistillationSpec(TypedDict): specs: dict; errors: List[str]; def validate_materials(state: DistillationSpec): material = state['specs'].get('material'); if material not in ['SS316L', 'Hastelloy']: state['errors'].append('Invalid material specification.'); return state; def validate_pressure(state: DistillationSpec): rating = state['specs'].get('pressure_rating', 0); if rating < 150: state['errors'].append('Pressure rating below industrial threshold.'); return state; graph = StateGraph(DistillationSpec); graph.add_node('validate_materials', validate_materials); graph.add_node('validate_pressure', validate_pressure); graph.set_entry_point('validate_materials'); graph.add_edge('validate_materials', 'validate_pressure'); graph.add_edge('validate_pressure', END); graph = graph.compile()