from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EggState(TypedDict): freshness_score: float; safety_cert: bool; supplier_compliant: bool; graph = StateGraph(EggState); def validate_perishability(state: EggState) -> EggState: return {'freshness_score': 0.95}; def verify_compliance(state: EggState) -> EggState: return {'safety_cert': True, 'supplier_compliant': True}; graph.add_node('check_freshness', validate_perishability); graph.add_node('verify_safety', verify_compliance); graph.set_entry_point('check_freshness'); graph.add_edge('check_freshness', 'verify_safety'); graph.add_edge('verify_safety', END); graph.compile()