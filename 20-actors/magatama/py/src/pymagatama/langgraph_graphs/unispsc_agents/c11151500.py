from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class RawMaterialState(TypedDict): chemical_spec: dict; validation_results: Annotated[Sequence[str], operator.add]; compliance_status: bool; def validate_purity(state: RawMaterialState): purity = state['chemical_spec'].get('purity', 0); return {'validation_results': ['Purity checked'] if purity > 99.0 else ['Purity check failed'], 'compliance_status': purity > 99.0}; def check_safety(state: RawMaterialState): return {'validation_results': ['MSDS verified']}; graph = StateGraph(RawMaterialState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()