from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplyState(TypedDict): nutrition_data: dict; compliance_status: bool; workflow_steps: List[str]; def validate_spec(state: SupplyState): return {'compliance_status': state.get('nutrition_data', {}).get('is_compliant', False)}; def check_expiry(state: SupplyState): return {'workflow_steps': ['audit_expiry', 'log_batch']}; graph = StateGraph(SupplyState); graph.add_node('validate', validate_spec); graph.add_node('expiry', check_expiry); graph.add_edge('validate', 'expiry'); graph.add_edge('expiry', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()