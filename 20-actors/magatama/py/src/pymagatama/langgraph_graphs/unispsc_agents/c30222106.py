from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FireStationState(TypedDict): facility_id: str; compliance_docs: List[str]; approved: bool; def validate_fire_safety(state: FireStationState): state['approved'] = 'Fire-Safety-Cert' in state['compliance_docs']; return state; builder = StateGraph(FireStationState); builder.add_node('validate', validate_fire_safety); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()