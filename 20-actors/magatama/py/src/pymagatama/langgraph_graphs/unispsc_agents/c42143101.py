from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_id: str; sterilization_cert: bool; compliance_status: str; def validate_cert(state: ProcureState): return {'compliance_status': 'compliant' if state['sterilization_cert'] else 'rejected'}; def finalize_procurement(state: ProcureState): return {'compliance_status': 'approved'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_cert); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.set_entry_point('validate'); graph.add_edge('finalize', END); graph = graph.compile()