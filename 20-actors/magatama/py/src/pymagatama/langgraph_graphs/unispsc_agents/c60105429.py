from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): content_url: str; validated: bool; metadata: dict; def validate_video(state: ProcState): return {'validated': state.get('content_url', '').startswith('https://')}; def workflow(): graph = StateGraph(ProcState); graph.add_node('validate', validate_video); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = workflow()