from langgraph.graph import StateGraph, END; from typing import TypedDict; class LouverState(TypedDict): specs: dict; compliance: bool; def validate_specs(state: LouverState): state['compliance'] = all(k in state['specs'] for k in ['material', 'size']); return state; graph = StateGraph(LouverState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()