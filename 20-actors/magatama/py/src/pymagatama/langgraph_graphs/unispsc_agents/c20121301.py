from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RobotMotorState(TypedDict): input_specs: dict; validation_results: Annotated[list, operator.add]; finalized_procurement: bool; def validate_specs(state: RobotMotorState): results = []; if 'torque_rating' not in state['input_specs']: results.append('Missing torque rating'); return {'validation_results': results}; def approve_order(state: RobotMotorState): return {'finalized_procurement': True}; graph = StateGraph(RobotMotorState); graph.add_node('validator', validate_specs); graph.add_node('approver', approve_order); graph.set_entry_point('validator'); graph.add_edge('validator', 'approver'); graph.add_edge('approver', END); graph = graph.compile()