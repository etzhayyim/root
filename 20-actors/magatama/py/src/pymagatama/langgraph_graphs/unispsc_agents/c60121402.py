from langgraph.graph import StateGraph, END; from typing import TypedDict; class FrameState(TypedDict): dimensions: dict; quality_passed: bool; def check_dimensions(state: FrameState): state['quality_passed'] = state['dimensions']['depth'] > 0; return state; graph = StateGraph(FrameState); graph.add_node('validate', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()