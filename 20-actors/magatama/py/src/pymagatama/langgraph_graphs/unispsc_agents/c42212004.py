from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_results: list; status: str; def validate_compatibility(state: ProcurementState): state['validation_results'].append('Compatibility check passed'); return state; def check_safety(state: ProcurementState): state['validation_results'].append('Safety standards verified'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_compatibility); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()