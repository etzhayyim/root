from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadioNucleotideState(TypedDict): nuc_data: dict; analysis_results: List[str]; def validate_isotopes(state: RadioNucleotideState): radiation_level = state['nuc_data'].get('activity', 0); return {'analysis_results': ['Safety compliant'] if radiation_level < 1000 else ['Requires HazMat handling']}; def finalize_procurement(state: RadioNucleotideState): return {'analysis_results': state['analysis_results'] + ['Workflow complete']}; graph = StateGraph(RadioNucleotideState); graph.add_node('validate', validate_isotopes); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()