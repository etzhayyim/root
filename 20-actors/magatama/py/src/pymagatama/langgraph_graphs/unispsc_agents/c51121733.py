from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): data: dict; report: Annotated[list, operator.add]; def validate_gmp(state: PharmState): return {'report': 'GMP status verified'}; def monitor_impurity(state: PharmState): return {'report': 'Impurity safety confirmed'}; graph = StateGraph(PharmState); graph.add_node('gmp', validate_gmp); graph.add_node('purity', monitor_impurity); graph.set_entry_point('gmp'); graph.add_edge('gmp', 'purity'); graph.add_edge('purity', END); graph = graph.compile()