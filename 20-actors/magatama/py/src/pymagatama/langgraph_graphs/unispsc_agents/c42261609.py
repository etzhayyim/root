from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): tags: List[str]; validated: bool; def check_compliance(state: ProcState): state['validated'] = 'ISO10993' in state.get('tags', []); return state; def finalize_order(state: ProcState): return {'validated': True}; graph = StateGraph(ProcState); graph.add_node('compliance', check_compliance); graph.add_node('finalize', finalize_order); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()