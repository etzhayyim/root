from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabelState(TypedDict): label_specs: dict; validation_results: List[str]; graph = StateGraph(LabelState); def validate_material(state: LabelState): return {'validation_results': ['Material verified']}; def check_adhesion(state: LabelState): return {'validation_results': ['Adhesion standards met']}; graph.add_node('material', validate_material); graph.add_node('adhesion', check_adhesion); graph.set_entry_point('material'); graph.add_edge('material', 'adhesion'); graph.add_edge('adhesion', END); compiled_graph = graph.compile()