from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): chemical_data: dict; validation_report: str; def validate_purity(state: State): return {'validation_report': 'Purity > 99% validated' if state['chemical_data'].get('purity', 0) >= 99 else 'Failed'}; def check_compliance(state: State): return {'validation_report': 'Compliance checks passed for pharma standards'}; graph = StateGraph(State); graph.add_node('purity_check', validate_purity); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()