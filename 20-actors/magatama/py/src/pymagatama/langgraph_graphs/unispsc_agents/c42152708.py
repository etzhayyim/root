from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OrthoState(TypedDict): vendor_id: str; item_spec: dict; compliance_verified: bool; def validate_specs(state: OrthoState): return {'compliance_verified': 'Biocompatibility' in state['item_spec']}; def route_procurement(state: OrthoState): return 'complete' if state['compliance_verified'] else 'review'; graph = StateGraph(OrthoState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()