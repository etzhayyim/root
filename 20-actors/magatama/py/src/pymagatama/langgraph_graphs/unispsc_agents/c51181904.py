from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): status: str; spec: dict; graph = StateGraph(ProcState); def validate_pharma(state): return {'status': 'validated'} if 'GMP' in state['spec'] else {'status': 'rejected'}; graph.add_node('validate', validate_pharma); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()