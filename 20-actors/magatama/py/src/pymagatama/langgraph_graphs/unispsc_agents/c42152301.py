from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalLaserState(TypedDict): model_info: str; compliance_checked: bool; inspection_report: str; def validate_specs(state: DentalLaserState): return {'compliance_checked': True}; def generate_report(state: DentalLaserState): return {'inspection_report': 'Certified'}; graph = StateGraph(DentalLaserState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()