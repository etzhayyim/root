from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: PumpState): results = []; if state['specs'].get('flow_rate', 0) <= 0: results.append('Invalid flow rate'); return {'validation_results': results}; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile = graph.compile()