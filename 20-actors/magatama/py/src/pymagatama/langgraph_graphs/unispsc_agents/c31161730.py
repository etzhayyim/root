from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): items: List[str]; validation_results: List[str]; def validate_specs(state: GraphState): return {'validation_results': ['Passed ISO standards']}; def generate_report(state: GraphState): print('Report generated'); return {}; graph = StateGraph(GraphState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()