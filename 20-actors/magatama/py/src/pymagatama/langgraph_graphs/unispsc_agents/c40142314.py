from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PipeSpecState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: PipeSpecState): specs = state['specs']; issues = []; if 'Pressure Rating' not in specs: issues.append('Missing Pressure Rating'); status = 'PASS' if not issues else f'FAILED: {','.join(issues)}'; return {'validation_report': status}; graph = StateGraph(PipeSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()