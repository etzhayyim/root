from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BrushState(TypedDict): brush_specs: dict; validation_results: List[str]; def validate_specs(state: BrushState): specs = state['brush_specs']; errors = []; if 'diameter' not in specs: errors.append('Missing diameter'); return {'validation_results': errors}; def finalize_order(state: BrushState): return {'validation_results': state['validation_results'] + ['Order validated']}; graph = StateGraph(BrushState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()