from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(FastenerState); def validate_specs(state: FastenerState): return {'validation_results': ['Compliance Verified']}; def check_inventory(state: FastenerState): return {'validation_results': ['Inventory Available']}; graph.add_node('validate', validate_specs); graph.add_node('inventory', check_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); compiled_graph = graph.compile()