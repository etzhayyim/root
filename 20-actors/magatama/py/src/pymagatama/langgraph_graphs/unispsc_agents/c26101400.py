from langgraph.graph import StateGraph, END; from typing import TypedDict; class MotorPartState(TypedDict): part_id: str; specs: dict; valid: bool; def check_specs(state: MotorPartState) -> MotorPartState: state['valid'] = 'Insulation Class' in state['specs']; return state; builder = StateGraph(MotorPartState); builder.add_node('validate', check_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()