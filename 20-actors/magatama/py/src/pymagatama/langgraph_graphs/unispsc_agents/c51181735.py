from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): ingredient: str; grade: str; compliance_check: bool; graph = StateGraph(ProcureState); def validate_purity(state: ProcureState): return {'compliance_check': True} if state.get('grade') == 'Pharma' else {'compliance_check': False}; def final_review(state: ProcureState): return {'compliance_check': state['compliance_check']}; graph.add_node('validate', validate_purity); graph.add_node('review', final_review); graph.add_edge('validate', 'review'); graph.add_edge('review', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()