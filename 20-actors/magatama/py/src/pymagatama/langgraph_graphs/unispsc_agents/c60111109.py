from langgraph.graph import StateGraph, END; from typing import TypedDict; class BoardState(TypedDict): content: str; validated: bool; def check_content(state: BoardState): state['validated'] = 'social studies' in state['content'].lower(); return state; graph = StateGraph(BoardState); graph.add_node('validate', check_content); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()