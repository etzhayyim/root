from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AstronomyProcurementState(TypedDict): specs: dict; validation_passed: bool; def validate_equipment(state: AstronomyProcurementState): state['validation_passed'] = 'Aperture' in state['specs'] and state['specs']['Aperture'] > 0; return state; graph = StateGraph(AstronomyProcurementState); graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()