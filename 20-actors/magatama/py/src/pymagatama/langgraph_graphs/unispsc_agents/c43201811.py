from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DVDState(TypedDict): data: dict; status: str; def validate_specs(state: DVDState): capacity = state['data'].get('capacity', 0); state['status'] = 'VALID' if capacity > 0 else 'INVALID'; return state; graph = StateGraph(DVDState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); compiled_graph = graph.compile()