from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TractorState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: TractorState): specs = state['specs']; state['compliance'] = specs.get('emission') == 'Euro6' and specs.get('sleeper_size') > 2.0; return state; def final_decision(state: TractorState): state['approved'] = state['compliance']; return state; graph = StateGraph(TractorState); graph.add_node('validate', validate_specs); graph.add_node('decide', final_decision); graph.set_entry_point('validate'); graph.add_edge('validate', 'decide'); graph.add_edge('decide', END); graph.compile()