from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AutopsyKitState(TypedDict): kit_id: str; biohazard_rating: int; compliance_status: bool; workflow_steps: List[str]; def validate_safety_specs(state: AutopsyKitState): state['compliance_status'] = state['biohazard_rating'] >= 3; return state; def prepare_distribution(state: AutopsyKitState): state['workflow_steps'] = ['sterility_check', 'bioseal_verification', 'logistics_dispatch']; return state; graph = StateGraph(AutopsyKitState); graph.add_node('validation', validate_safety_specs); graph.add_node('logistics', prepare_distribution); graph.set_entry_point('validation'); graph.add_edge('validation', 'logistics'); graph.add_edge('logistics', END); compiled_graph = graph.compile()