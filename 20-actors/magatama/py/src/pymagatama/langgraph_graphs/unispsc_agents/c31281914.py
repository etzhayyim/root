from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): metal_type: str; purity: float; dimensions: dict; validation_passed: bool; def validate_purity(state: SpecState): state['validation_passed'] = state['purity'] >= 0.99; return state; def final_approval(state: SpecState): return 'approved' if state['validation_passed'] else 'rejected'; graph = StateGraph(SpecState); graph.add_node('validate', validate_purity); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()