from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcessState(TypedDict): blade_id: str; compliance: bool; status: str; def validate_spec(state: ProcessState): state['compliance'] = True; state['status'] = 'Validated'; return state; def check_regulatory(state: ProcessState): print(f'Verifying regulatory status for {state[blade_id]}'); return state; graph = StateGraph(ProcessState); graph.add_node('validate', validate_spec); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()