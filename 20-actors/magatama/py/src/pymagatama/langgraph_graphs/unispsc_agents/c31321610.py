from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): material_spec: str; ultrasonic_test: bool; compliance: bool; def validate_specs(state: State): state['compliance'] = state['material_spec'].startswith('ASTM') and state['ultrasonic_test']; return state; def route_compliance(state: State): return 'pass' if state['compliance'] else 'fail'; graph = StateGraph(State); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END);