from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RazorProcurementState(TypedDict): vendor: str; item_specs: dict; compliance_checked: bool; def validate_specs(state: RazorProcurementState): state['compliance_checked'] = 'blade_material' in state['item_specs'] and state['item_specs']['blade_material'] == 'High-Carbon Steel'; return state; def route_procurement(state: RazorProcurementState): return 'end' if state['compliance_checked'] else 'end'; graph = StateGraph(RazorProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()