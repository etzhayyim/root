from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity: float; cold_chain_req: bool; compliance: bool; def validate_purity(state: PharmaState): return {'compliance': state['purity'] >= 99.9}; def check_cold_chain(state: PharmaState): return {'compliance': state['cold_chain_req'] == True}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('logistics', check_cold_chain); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph.set_entry_point('validate'); graph = graph.compile()