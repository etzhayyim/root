from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ManicureState(TypedDict): specs: dict; valid: bool; def validate_sharpness(state: ManicureState): state['valid'] = state['specs'].get('sharpness_level', 0) > 0; return state; def check_materials(state: ManicureState): state['valid'] = 'stainless_steel' in state['specs'].get('material', ''); return state; graph = StateGraph(ManicureState); graph.add_node('sharpness_check', validate_sharpness); graph.add_node('material_check', check_materials); graph.set_entry_point('sharpness_check'); graph.add_edge('sharpness_check', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()