from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): content: str; validated: bool; def val_step(state: State): state['validated'] = 'German' in state['content']; return state; graph = StateGraph(State); graph.add_node('validate', val_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()