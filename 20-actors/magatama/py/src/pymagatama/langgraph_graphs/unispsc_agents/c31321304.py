from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): n_code: str; spec_docs: List[str]; validation_passed: bool; def validate_specs(state: AssemblyState): state['validation_passed'] = all(['ASTM' in doc for doc in state['spec_docs']]); return state; def check_export(state: AssemblyState): return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()