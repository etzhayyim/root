from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurveyState(TypedDict): input_data: dict; validation_report: list; status: str; def validate_specs(state: SurveyState): spec = state['input_data']; errors = []; required = ['range', 'calibration']; for field in required: if field not in spec: errors.append(f'Missing {field}'); return {'validation_report': errors, 'status': 'valid' if not errors else 'invalid'}; def compile_graph(): graph = StateGraph(SurveyState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); return graph.compile(); graph = compile_graph()