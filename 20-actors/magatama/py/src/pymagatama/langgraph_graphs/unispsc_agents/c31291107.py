from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtrusionState(TypedDict): material_purity: float; dimensions: dict; status: str; def validate_lead_purity(state: ExtrusionState): state['status'] = 'PASS' if state['material_purity'] >= 99.9 else 'FAIL'; return state; def check_dimensions(state: ExtrusionState): state['status'] = 'COMPLIANT' if state['status'] == 'PASS' else 'REJECTED'; return state; graph = StateGraph(ExtrusionState); graph.add_node('validate_purity', validate_lead_purity); graph.add_node('check_dims', check_dimensions); graph.add_edge('validate_purity', 'check_dims'); graph.add_edge('check_dims', END); graph.set_entry_point('validate_purity'); graph = graph.compile()