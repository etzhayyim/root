from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: MachineState): errors = []; if state['specs'].get('power', 0) < 0.5: errors.append('Insufficient power rating'); return {'validation_errors': errors}; def check_safety(state: MachineState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(MachineState); graph.add_node('validate', validate_specs); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()