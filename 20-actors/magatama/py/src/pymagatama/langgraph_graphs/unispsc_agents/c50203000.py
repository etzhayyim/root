from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class JuiceState(TypedDict): items: List[str]; inspection_passed: bool; def check_quality(state: JuiceState) -> JuiceState: state['inspection_passed'] = all(len(i) > 0 for i in state['items']); return state; def cold_chain_validation(state: JuiceState) -> JuiceState: print('Verifying temperature logs'); return state; graph = StateGraph(JuiceState); graph.add_node('quality_check', check_quality); graph.add_node('logistics_check', cold_chain_validation); graph.set_entry_point('quality_check'); graph.add_edge('quality_check', 'logistics_check'); graph.add_edge('logistics_check', END); compiled_graph = graph.compile()