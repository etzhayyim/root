from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoftwareState(TypedDict): vendor_check: bool; security_audit: bool; final_approval: bool; def validate_vendor(state: SoftwareState): return {'vendor_check': True}; def perform_audit(state: SoftwareState): return {'security_audit': True}; def finalize(state: SoftwareState): return {'final_approval': True}; graph = StateGraph(SoftwareState); graph.add_node('vendor', validate_vendor); graph.add_node('audit', perform_audit); graph.add_node('finalize', finalize); graph.set_entry_point('vendor'); graph.add_edge('vendor', 'audit'); graph.add_edge('audit', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()