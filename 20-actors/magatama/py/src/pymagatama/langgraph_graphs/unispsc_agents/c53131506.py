from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalKitState(TypedDict): kit_id: str; instruments: List[str]; sterile_cert: bool; approved: bool; def validate_kit(state: DentalKitState): state['approved'] = len(state['instruments']) > 0 and state['sterile_cert']; return state; builder = StateGraph(DentalKitState); builder.add_node('validate', validate_kit); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()