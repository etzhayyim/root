from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnchorState(TypedDict): anchor_specs: dict; validation_results: List[str]; graph = StateGraph(AnchorState); def validate_spec(state: AnchorState): results = []; m = state.get('anchor_specs', {}); if m.get('grade') not in ['304', '316', 'carbon_steel']: results.append('Invalid material grade'); return {'validation_results': results}; graph.add_node('validation', validate_spec); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()