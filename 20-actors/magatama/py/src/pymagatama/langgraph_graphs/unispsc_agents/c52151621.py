from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CookieState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: CookieState): state['compliance'] = 'Food Grade Certificate' in state['specs']; return state; def finalize(state: CookieState): state['status'] = 'Approved' if state['compliance'] else 'Rejected'; return state; graph = StateGraph(CookieState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()