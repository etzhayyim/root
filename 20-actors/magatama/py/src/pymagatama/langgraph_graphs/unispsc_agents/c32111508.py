from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SolarState(TypedDict): diode_specs: dict; validation_report: list[str]; graph = StateGraph(SolarState); def validate_electrical(state: SolarState): specs = state['diode_specs']; errors = []; if specs.get('peak_voltage', 0) < 45: errors.append('Voltage too low for PV panel strings.'); return {'validation_report': errors}; def finalize_procurement(state: SolarState): return {'validation_report': ['Specs cleared'] if not state['validation_report'] else state['validation_report']}; graph.add_node('validate', validate_electrical); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()