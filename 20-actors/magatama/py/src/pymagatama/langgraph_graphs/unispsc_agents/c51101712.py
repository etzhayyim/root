from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_name: str; purity: float; hazard_verified: bool; def validate_purity(state: ChemicalState): state['purity_verified'] = state['purity'] >= 95.0; return state; def check_hazard(state: ChemicalState): state['hazard_verified'] = True; return state; builder = StateGraph(ChemicalState); builder.add_node('validate', validate_purity); builder.add_node('safety_check', check_hazard); builder.set_entry_point('validate'); builder.add_edge('validate', 'safety_check'); builder.add_edge('safety_check', END); graph = builder.compile()