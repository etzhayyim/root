from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProbeCoverState(TypedDict): vendor_docs: List[str]; compliance_status: bool; validation_log: List[str]; def validate_medical_cert(state: ProbeCoverState): state['compliance_status'] = 'ISO' in str(state['vendor_docs']); return state; def check_sterility(state: ProbeCoverState): validation_msg = 'Sterility certificate verified' if 'sterile' in state['vendor_docs'] else 'Missing sterility'; state['validation_log'].append(validation_msg); return state; graph = StateGraph(ProbeCoverState); graph.add_node('validate_cert', validate_medical_cert); graph.add_node('check_sterility', check_sterility); graph.set_entry_point('validate_cert'); graph.add_edge('validate_cert', 'check_sterility'); graph.add_edge('check_sterility', END); graph = graph.compile()