from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LadderState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: LadderState): specs = state.get('specs', {}); required = ['load_capacity_kg', 'material_specification']; is_valid = all(k in specs for k in required) and specs.get('load_capacity_kg', 0) > 0; return {'valid': is_valid, 'error': '' if is_valid else 'Missing specs'}; graph = StateGraph(LadderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()