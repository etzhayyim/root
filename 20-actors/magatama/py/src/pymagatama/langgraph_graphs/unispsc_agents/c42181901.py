from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FetalMonitorState(TypedDict): device_id: str; data_points: List[float]; validation_passed: bool; def validate_sensors(state: FetalMonitorState): state['validation_passed'] = len(state['data_points']) > 0; return state; def check_compliance(state: FetalMonitorState): print('Checking 13485 certs...'); return state; graph = StateGraph(FetalMonitorState); graph.add_node('validate', validate_sensors); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()