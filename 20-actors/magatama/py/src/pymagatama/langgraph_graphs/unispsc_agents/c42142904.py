from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HardwareState(TypedDict): hardware_parts: List[str]; validation_results: dict; graph = StateGraph(HardwareState); def validate_parts(state: HardwareState): return {"validation_results": {part: "passed" for part in state["hardware_parts"]}}; graph.add_node("validate", validate_parts); graph.set_entry_point("validate"); graph.add_edge("validate", END); app = graph.compile()