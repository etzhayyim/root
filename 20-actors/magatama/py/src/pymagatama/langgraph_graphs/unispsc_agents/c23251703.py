from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ForgingSpec(TypedDict): tonnage: int; certs: List[str]; validated: bool; def check_safety(state: ForgingSpec): state['validated'] = state['tonnage'] > 0 and 'ISO-12100' in state['certs']; return state; graph = StateGraph(ForgingSpec); graph.add_node('validate', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()