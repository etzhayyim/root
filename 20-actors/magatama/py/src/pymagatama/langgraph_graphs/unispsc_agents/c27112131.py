from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ClampState(TypedDict): specs: dict; validation_report: list[str]; def validate_specs(state: ClampState): specs = state['specs']; errors = []; if specs.get('clamping_capacity_mm', 0) <= 0: errors.append('Invalid capacity'); return {'validation_report': errors}; graph = StateGraph(ClampState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()