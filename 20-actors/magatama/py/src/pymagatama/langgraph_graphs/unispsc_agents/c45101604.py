from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): input_spec: str; validation_status: bool; chemical_safety_checked: bool; def validate_plate_proc(state: ProcureState): state['validation_status'] = 'width' in state['input_spec']; return state; def check_chemicals(state: ProcureState): state['chemical_safety_checked'] = True; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_plate_proc); graph.add_node('check_safety', check_chemicals); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_safety'); graph.add_edge('check_safety', END); app = graph.compile()