from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; def validate_electrical(state: ProcState): return {'validation_results': ['Voltage and Wattage checked']} if 'Voltage' in state['specs'] else {'validation_results': ['Missing Voltage']}; def validate_safety(state: ProcState): return {'validation_results': ['PSE certification verified']}; graph = StateGraph(ProcState); graph.add_node('elec_check', validate_electrical); graph.add_node('safety_check', validate_safety); graph.set_entry_point('elec_check'); graph.add_edge('elec_check', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()