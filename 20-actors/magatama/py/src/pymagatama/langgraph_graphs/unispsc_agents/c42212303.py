from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class OpenerState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: OpenerState): errors = []; if 'grip_strength' not in state['specs']: errors.append('Missing grip strength data'); return {'validation_errors': errors}; def check_approval(state: OpenerState): return 'approved' if not state['validation_errors'] else 'rejected'; graph = StateGraph(OpenerState); graph.add_node('validate', validate_specs); graph.add_node('check', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.compile()