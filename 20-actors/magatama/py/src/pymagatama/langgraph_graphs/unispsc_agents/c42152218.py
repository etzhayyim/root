from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalVibratorState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: DentalVibratorState): specs = state['specs']; is_valid = all(key in specs for key in ['vibration_frequency_hz', 'power_supply_voltage']); return {'valid': is_valid, 'error_log': [] if is_valid else ['Missing mandatory specifications']}; graph = StateGraph(DentalVibratorState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()