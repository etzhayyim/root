from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FiltrationSpec(TypedDict): hardware_type: str; specs: dict; approved: bool; def validate_specs(state: FiltrationSpec): state['approved'] = 'pressure_tolerance' in state['specs'] and state['specs']['pressure_tolerance'] > 0; return state; graph = StateGraph(FiltrationSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()