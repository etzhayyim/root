from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class RelaySpecState(TypedDict): relay_code: str; specs: dict; validation_results: Annotated[Sequence[str], operator.add]; graph = StateGraph(RelaySpecState); def validate_electrical(state: RelaySpecState): return {'validation_results': ['Voltage and current ratings verified']}; def check_compliance(state: RelaySpecState): return {'validation_results': ['Export control check passed']}; graph.add_node('validate_elec', validate_electrical); graph.add_node('check_comp', check_compliance); graph.set_entry_point('validate_elec'); graph.add_edge('validate_elec', 'check_comp'); graph.add_edge('check_comp', END); graph = graph.compile()