from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): commodity: str; spec_data: dict; approved: bool; def validate_spec(state: ProcurementState): spec = state['spec_data']; state['approved'] = 'GMP' in spec.get('certifications', []) and 'purity' in spec; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()