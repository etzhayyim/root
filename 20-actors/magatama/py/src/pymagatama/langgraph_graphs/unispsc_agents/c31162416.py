from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PinProcurementState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PinProcurementState): state['valid'] = state['specs'].get('tensile_strength') > 500; return state; def check_compliance(state: PinProcurementState): return 'valid' if state['valid'] else END; graph = StateGraph(PinProcurementState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()