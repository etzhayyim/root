from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaundrySpecState(TypedDict): specs: dict; validation_errors: List[str]; def validate_mesh_durability(state: LaundrySpecState): print('Validating mesh density...'); return state; def validate_closure_integrity(state: LaundrySpecState): print('Checking zipper/fastener strength...'); return state; graph = StateGraph(LaundrySpecState); graph.add_node('mesh_check', validate_mesh_durability); graph.add_node('fastener_check', validate_closure_integrity); graph.set_entry_point('mesh_check'); graph.add_edge('mesh_check', 'fastener_check'); graph.add_edge('fastener_check', END); graph = graph.compile()