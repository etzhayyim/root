from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_device(state: ProcState): state['validated'] = all(c in ['ISO13485', 'CE', 'FDA'] for c in state['certs']); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()