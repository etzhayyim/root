from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class HRWorkflowState(TypedDict): vendor_docs: list; validation_results: list; compliance_check: bool; def validate_hr_software(state: HRWorkflowState): state['validation_results'].append('API integration standards verified'); return state; def check_privacy(state: HRWorkflowState): state['compliance_check'] = True; return state; graph = StateGraph(HRWorkflowState); graph.add_node('validate', validate_hr_software); graph.add_node('compliance', check_privacy); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()