from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): data: dict; logs: Annotated[list, operator.add]; def validate_spec(state: State): state['logs'].append('Validating medical safety compliance.'); return state; def check_calibration(state: State): state['logs'].append('Checking calibration expiration.'); return state; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('calibration', check_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibration'); graph.add_edge('calibration', END); graph = graph.compile()