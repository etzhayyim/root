from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ConduitState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: ConduitState): specs = state['specs']; results = []; if specs.get('diameter', 0) <= 0: results.append('Invalid Diameter'); return {'validation_results': results, 'status': 'invalid' if results else 'ready'}; graph = StateGraph(ConduitState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()