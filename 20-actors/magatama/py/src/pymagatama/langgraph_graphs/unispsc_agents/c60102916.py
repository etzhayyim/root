from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TimerKitState(TypedDict): kit_id: str; specs: dict; validation: bool; def validate_specs(state: TimerKitState): return {'validation': 'accuracy' in state['specs']}; def finalize(state: TimerKitState): return {'validation': True}; graph = StateGraph(TimerKitState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()