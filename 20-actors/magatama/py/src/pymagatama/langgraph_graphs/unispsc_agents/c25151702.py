from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SatelliteState(TypedDict): specs: dict; compliance_approved: bool; graph = StateGraph(SatelliteState); def validate_specs(state: SatelliteState): return {'compliance_approved': True} if 'sensor' in state['specs'] else {'compliance_approved': False}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()