from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BevelingGraphState(TypedDict): input_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: BevelingGraphState): specs = state['input_specs']; errors = []; if specs.get('power', 0) < 0.5: errors.append('Insufficient power rating'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(BevelingGraphState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()