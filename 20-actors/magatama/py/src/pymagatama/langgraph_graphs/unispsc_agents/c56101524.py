from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IroningBoardState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: IroningBoardState): errors = []; if state['specs'].get('height_adjustment') is None: errors.append('Missing adjustment specs'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(IroningBoardState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()