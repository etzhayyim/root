from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IDState(TypedDict): items: List[str]; validated_items: List[str]; def validate_id_card(state: IDState): return {'validated_items': [item for item in state['items'] if 'secured' in item]}; graph = StateGraph(IDState); graph.add_node('validate', validate_id_card); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()