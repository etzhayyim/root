from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): vendor_id: str; quality_docs: List[str]; compliant: bool; def validate_gmp(state: PharmaState): state['compliant'] = 'GMP_CERT' in state['quality_docs']; return state; def final_check(state: PharmaState): return 'approved' if state['compliant'] else 'rejected'; graph = StateGraph(PharmaState); graph.add_node('verify_gmp', validate_gmp); graph.add_node('final_approval', final_check); graph.set_entry_point('verify_gmp'); graph.add_edge('verify_gmp', 'final_approval'); graph.add_edge('final_approval', END); graph = graph.compile()