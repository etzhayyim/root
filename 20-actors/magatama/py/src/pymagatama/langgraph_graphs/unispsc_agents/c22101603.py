from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: BearingState): return {'validation_report': 'Passed' if 'load_rating' in state['specs'] else 'Failed'}; def finalize(state: BearingState): return {'validation_report': 'Finalized'}; graph = StateGraph(BearingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()