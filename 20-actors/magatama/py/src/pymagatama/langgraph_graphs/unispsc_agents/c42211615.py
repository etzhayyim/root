from langgraph.graph import StateGraph, END; from typing import Dict, Any; class ProcurementState(Dict[str, Any]): pass; def validate_specs(state: ProcurementState): return {'validated': True}; def check_compliance(state: ProcurementState): return {'compliant': True}; graph = StateGraph(ProcurementState); graph.add_node('validate_specs', validate_specs); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', 'check_compliance'); graph.add_edge('check_compliance', END); graph = graph.compile()