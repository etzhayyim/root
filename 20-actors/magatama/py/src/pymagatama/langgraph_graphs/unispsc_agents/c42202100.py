from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BrachyState(TypedDict): items: List[str]; qc_passed: bool; def validate_isotopes(state: BrachyState): print('Validating radioactive components...'); return {'qc_passed': True}; def update_records(state: BrachyState): print('Logging material to radiation safety registry.'); return state; graph = StateGraph(BrachyState); graph.add_node('validate', validate_isotopes); graph.add_node('record', update_records); graph.set_entry_point('validate'); graph.add_edge('validate', 'record'); graph.add_edge('record', END); compiled_graph = graph.compile()