from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabEquipmentState(TypedDict): funnel_specs: dict; validation_results: List[str]; def validate_material(state: LabEquipmentState): is_valid = state['funnel_specs'].get('material') in ['Borosilicate Glass', 'PTFE']; result = 'Pass' if is_valid else 'Fail: Incompatible Material'; return {'validation_results': [result]}; graph = StateGraph(LabEquipmentState); graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()