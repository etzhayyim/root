from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MagnesiumState(TypedDict): profile_specs: dict; compliance_status: bool; def validate_specs(state: MagnesiumState): state['compliance_status'] = 'AZ31B' in state['profile_specs'].get('grade', ''); return state; def check_export_control(state: MagnesiumState): return state; graph = StateGraph(MagnesiumState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()