from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; specs: dict; validation_passed: bool; def validate_structural_integrity(state: AssemblyState): state['validation_passed'] = state['specs'].get('tensile_strength', 0) > 200; return state; def check_compliance(state: AssemblyState): return 'valid' if state['validation_passed'] else 'flag_for_review'; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_structural_integrity); graph.add_conditional_edges('validate', check_compliance, {'valid': END, 'flag_for_review': END}); graph.set_entry_point('validate'); graph = graph.compile()