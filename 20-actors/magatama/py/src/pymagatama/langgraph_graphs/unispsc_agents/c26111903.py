from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ClutchState(TypedDict): rpm: float; engagement_threshold: float; ready: bool; def validate_specs(state: ClutchState): state['ready'] = state['rpm'] >= state['engagement_threshold']; return state; builder = StateGraph(ClutchState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()