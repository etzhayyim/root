from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ChemState(TypedDict): chemical_name: str; concentration: float; valid: bool; def check_safety(state: ChemState): return {'valid': state['concentration'] <= 2.5}; graph = StateGraph(ChemState); graph.add_node('safety_check', check_safety); graph.add_edge('safety_check', END); graph.set_entry_point('safety_check'); compiled_graph = graph.compile()