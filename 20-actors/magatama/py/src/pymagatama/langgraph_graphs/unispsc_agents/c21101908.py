from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class IrrigationState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: IrrigationState): return {'valid': 'flow_rate' in state['specs']}; def compliance_check(state: IrrigationState): return {'valid': state['specs'].get('pressure', 0) > 0}; graph = StateGraph(IrrigationState); graph.add_node('validate', validate_specs); graph.add_node('compliance', compliance_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()