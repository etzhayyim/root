from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DamperState(TypedDict): specs: dict; compliance_check: bool; ready_for_procurement: bool; def validate_specs(state: DamperState): state['compliance_check'] = 'Operating Temperature Limit' in state['specs'] and float(state['specs']['Operating Temperature Limit']) > 0; return state; def approve_procurement(state: DamperState): state['ready_for_procurement'] = state['compliance_check']; return state; graph = StateGraph(DamperState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()