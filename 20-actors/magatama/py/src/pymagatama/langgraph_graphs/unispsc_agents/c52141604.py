from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FootwearDryerState(TypedDict): vendor_specs: dict; compliance_check: bool; validation_log: List[str]; def validate_electrical_safety(state: FootwearDryerState): state['validation_log'].append('Checking UL/CE compliance'); state['compliance_check'] = True; return state; def check_airflow_specs(state: FootwearDryerState): state['validation_log'].append('Verifying CFM rates'); return state; graph = StateGraph(FootwearDryerState); graph.add_node('safety_check', validate_electrical_safety); graph.add_node('airflow_check', check_airflow_specs); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'airflow_check'); graph.add_edge('airflow_check', END); graph = graph.compile()