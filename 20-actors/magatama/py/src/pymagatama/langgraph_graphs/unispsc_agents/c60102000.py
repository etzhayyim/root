from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LearningResourceState(TypedDict): content: str; is_compliant: bool; tags: List[str]; def validate_material(state: LearningResourceState): state['is_compliant'] = True; return state; def tag_resource(state: LearningResourceState): state['tags'] = ['education', 'language_learning']; return state; graph = StateGraph(LearningResourceState); graph.add_node('validate', validate_material); graph.add_node('tagging', tag_resource); graph.set_entry_point('validate'); graph.add_edge('validate', 'tagging'); graph.add_edge('tagging', END); graph = graph.compile()