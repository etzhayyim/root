from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: FilterState): errors = []; if state['specs'].get('pore_size', 0) <= 0: errors.append('Invalid pore size'); return {'validation_errors': errors}; def check_compliance(state: FilterState): approved = len(state.get('validation_errors', [])) == 0; return {'approved': approved}; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()