from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def material_check(state: AssemblyState): state['validation_results'].append('Material chemistry verified'); return state; def structural_validation(state: AssemblyState): state['validation_results'].append('UL/AS9100 bond integrity passed'); state['approved'] = True; return state; graph = StateGraph(AssemblyState); graph.add_node('chemistry', material_check); graph.add_node('structural', structural_validation); graph.set_entry_point('chemistry'); graph.add_edge('chemistry', 'structural'); graph.add_edge('structural', END); graph = graph.compile()