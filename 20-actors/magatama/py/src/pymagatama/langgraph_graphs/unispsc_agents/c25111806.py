from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RaftState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: RaftState): specs = state['specs']; state['valid'] = 'ISO' in specs.get('certs', '') and specs.get('pressure', 0) > 0; state['log'].append('Validation completed'); return state; graph = StateGraph(RaftState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()