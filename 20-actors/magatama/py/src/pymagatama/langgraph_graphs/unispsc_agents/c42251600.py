from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RehabState(TypedDict): device_specs: dict; compliance_verified: bool; def validate_specs(state: RehabState): state['compliance_verified'] = 'ISO_13485_Certification' in state['device_specs']; return state; def check_risk(state: RehabState): return 'compliance_verified' if state['compliance_verified'] else END; graph = StateGraph(RehabState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()