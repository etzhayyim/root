from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AirwayState(TypedDict): device_specs: dict; validation_results: List[str]; def validate_material_safety(state: AirwayState): state['validation_results'].append('Biocompatibility_verified'); return state; def check_regulatory_compliance(state: AirwayState): state['validation_results'].append('FDA_CE_compliant'); return state; graph = StateGraph(AirwayState); graph.add_node('validate_material', validate_material_safety); graph.add_node('check_regulatory', check_regulatory_compliance); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_regulatory'); graph.add_edge('check_regulatory', END); graph = graph.compile()