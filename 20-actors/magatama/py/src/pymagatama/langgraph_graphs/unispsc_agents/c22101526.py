from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): vendor_specs: str; compliance_score: int; output: str; def validate_specs(state: SpecState): return {'compliance_score': 100 if 'ISO' in state['vendor_specs'] else 50}; def finalize_procurement(state: SpecState): return {'output': 'Procurement Approved'}; graph = StateGraph(SpecState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()