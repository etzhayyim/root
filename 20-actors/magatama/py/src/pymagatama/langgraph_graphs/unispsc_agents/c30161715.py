from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DuckboardState(TypedDict): specs: dict; valid: bool; audit_log: List[str]; def validate_specs(state: DuckboardState): specs = state['specs']; is_valid = all(key in specs for key in ['load_capacity', 'material']); return {'valid': is_valid, 'audit_log': ['Specs checked']}; graph = StateGraph(DuckboardState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()