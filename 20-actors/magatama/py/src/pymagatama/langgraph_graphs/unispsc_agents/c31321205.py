from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): items: List[str]; validated: bool; analysis_report: str; def validate_weld_integrity(state: AssemblyState): return {'validated': True} if 'welding_log' in state['items'] else {'validated': False}; def generate_report(state: AssemblyState): return {'analysis_report': 'Integrity check complete.'}; graph = StateGraph(AssemblyState); graph.add_node('check_weld', validate_weld_integrity); graph.add_node('report', generate_report); graph.set_entry_point('check_weld'); graph.add_edge('check_weld', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()