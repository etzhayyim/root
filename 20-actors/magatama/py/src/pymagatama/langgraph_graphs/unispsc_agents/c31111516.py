from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TitaniumState(TypedDict): material_spec: dict; validation_results: list; status: str; def validate_grade(state: TitaniumState): state['validation_results'].append('Grade Verified'); return state; def check_export_control(state: TitaniumState): state['validation_results'].append('Export Compliance Checked'); return state; graph = StateGraph(TitaniumState); graph.add_node('validate', validate_grade); graph.add_node('export', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()