from langgraph.graph import StateGraph, END; from typing import TypedDict; class SANState(TypedDict): port_count: int; throughput: str; validated: bool; def validate_switch(state: SANState): state['validated'] = state['port_count'] >= 24; return state; graph = StateGraph(SANState); graph.add_node('validate', validate_switch); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()