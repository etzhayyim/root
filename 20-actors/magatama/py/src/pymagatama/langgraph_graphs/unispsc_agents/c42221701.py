from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InfusionState(TypedDict): bag_id: str; spec_compliance: bool; status: str; def validate_specs(state: InfusionState): state['spec_compliance'] = True; return state; def check_regulatory(state: InfusionState): if state['spec_compliance']: state['status'] = 'APPROVED'; else: state['status'] = 'REJECTED'; return state; graph = StateGraph(InfusionState); graph.add_node('validate', validate_specs); graph.add_node('regulate', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulate'); graph.add_edge('regulate', END); graph = graph.compile()