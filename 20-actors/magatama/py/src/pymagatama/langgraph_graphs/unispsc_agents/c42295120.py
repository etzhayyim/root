from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LithotripterState(TypedDict): device_id: str; compliance_docs: List[str]; inspection_status: str; def validate_spec(state: LithotripterState): return {'inspection_status': 'Validated'} if 'ISO_13485' in state['compliance_docs'] else {'inspection_status': 'Rejected'}; graph = StateGraph(LithotripterState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()