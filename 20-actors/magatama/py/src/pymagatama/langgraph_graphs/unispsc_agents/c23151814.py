from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; compliance: bool; validation_log: Annotated[Sequence[str], operator.add]; def validate_specs(state: RobotState): return {'validation_log': ['Specs verified'], 'compliance': True if state['specs'].get('load') else False}; def check_safety(state: RobotState): return {'validation_log': ['Safety protocols validated']}; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()