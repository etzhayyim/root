from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotProcurementState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def validate_specs(state: RobotProcurementState): print('Validating industrial robot specs...'); return {'validation_results': ['Safety Standard ISO 10218-1 verified']}; def check_export_control(state: RobotProcurementState): return {'approved': True}; graph = StateGraph(RobotProcurementState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()