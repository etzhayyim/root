from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AntiSkidState(TypedDict): specs: dict; compliance_status: bool; workflow_log: Annotated[list, operator.add]; def validate_aerospace_specs(state: AntiSkidState): status = all(key in state['specs'] for key in ['DO-178C_Level', 'AS9100_Certification']); return {'compliance_status': status, 'workflow_log': ['Specs validated']}; def export_control_check(state: AntiSkidState): return {'workflow_log': ['Dual-use export check completed']}; graph = StateGraph(AntiSkidState); graph.add_node('validate', validate_aerospace_specs); graph.add_node('export', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph = graph.compile()