from langgraph.graph import StateGraph, END; from typing import TypedDict; class DrumHandlingState(TypedDict): capacity: float; type: str; compliance: bool; def validate_spec(state: DrumHandlingState): return {'compliance': state['capacity'] > 0}; def finalize_procurement(state: DrumHandlingState): return {'compliance': True}; graph = StateGraph(DrumHandlingState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()