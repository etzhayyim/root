from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): items: list; status: str; def validate_pans(state: ProcureState): state['status'] = 'validation_complete'; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_pans); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()