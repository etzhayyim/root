from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): dimensions: dict; material_grade: str; status: str; def validate_specs(state: SpecState): state['status'] = 'VALIDATED' if state['material_grade'] in ['304', '316'] else 'REJECTED'; return state; def check_mill_cert(state: SpecState): state['status'] = 'CERTIFIED' if state['status'] == 'VALIDATED' else 'ERROR'; return state; graph = StateGraph(SpecState); graph.add_node('validate', validate_specs); graph.add_node('certify', check_mill_cert); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()