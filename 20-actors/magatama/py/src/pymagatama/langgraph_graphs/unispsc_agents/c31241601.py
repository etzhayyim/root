from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): blanks: List[dict]; validation_passed: bool; def validate_specs(state: FilterState): state['validation_passed'] = all(b.get('material_cert') for b in state['blanks']); return state; def route_logic(state: FilterState): return 'valid' if state['validation_passed'] else END; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()