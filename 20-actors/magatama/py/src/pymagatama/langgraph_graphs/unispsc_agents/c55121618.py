from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabelState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_specs(state: LabelState): for item in state['items']: if 'material' not in item: state['validation_errors'].append('Missing material'); return state; def check_dimensions(state: LabelState): print('Processing dimensions'); return state; graph = StateGraph(LabelState); graph.add_node('validate', validate_specs); graph.add_node('check_dims', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_dims'); graph.add_edge('check_dims', END); compiled_graph = graph.compile()