from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BandageState(TypedDict): bandage_type: str; is_sterile: bool; compliance_check: bool; def validate_specs(state: BandageState): return {'compliance_check': state.get('is_sterile', False)}; graph = StateGraph(BandageState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()