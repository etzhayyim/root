from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcessedFoodState(TypedDict): fruit_type: str; quality_metrics: dict; compliant: bool; def validate_quality(state: ProcessedFoodState): state['compliant'] = state['quality_metrics'].get('brix', 0) > 10; return state; def route_step(state: ProcessedFoodState): return 'validate' if state['fruit_type'] == 'tangelo' else END; graph = StateGraph(ProcessedFoodState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()