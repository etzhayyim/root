from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PLCState(TypedDict): specs: dict; valid: bool; log: List[str]; def validation_node(state: PLCState): state['valid'] = all(k in state['specs'] for k in ['IP', 'Protocol']); return state; graph = StateGraph(PLCState); graph.add_node('validate', validation_node); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()