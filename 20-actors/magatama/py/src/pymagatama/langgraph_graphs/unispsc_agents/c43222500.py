from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): vendor_certs: dict; compliance_check: bool; graph = StateGraph(GraphState); def validate_security_certs(state: GraphState): return {'compliance_check': 'FIPS' in state.get('vendor_certs', {})}; def audit_export_control(state: GraphState): return {'compliance_check': True}; graph.add_node('cert_validation', validate_security_certs); graph.add_node('export_audit', audit_export_control); graph.set_entry_point('cert_validation'); graph.add_edge('cert_validation', 'export_audit'); graph.add_edge('export_audit', END); graph = graph.compile()