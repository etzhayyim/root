from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CatheterState(TypedDict): vendor_docs: List[str]; qc_passed: bool; certs_valid: bool; def check_compliance(state: CatheterState): return {'certs_valid': len(state['vendor_docs']) >= 3}; def run_quality_check(state: CatheterState): return {'qc_passed': state['certs_valid']}; graph = StateGraph(CatheterState); graph.add_node('compliance_check', check_compliance); graph.add_node('qc_process', run_quality_check); graph.set_entry_point('compliance_check'); graph.add_edge('compliance_check', 'qc_process'); graph.add_edge('qc_process', END); app = graph.compile()