from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlumbingState(TypedDict): specs: dict; compliance_verified: bool; def validate_specs(state: PlumbingState): state['compliance_verified'] = 'Pressure Rating' in state['specs'] and 'Material Grade' in state['specs']; return state; def finalize_order(state: PlumbingState): print('Procurement finalized'); return state; graph = StateGraph(PlumbingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()