from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MetalState(TypedDict): metal_type: str; purity: float; verification_passed: bool; graph = StateGraph(MetalState); def verify_purity(state: MetalState): return { 'verification_passed': state['purity'] >= 0.999 }; def check_compliance(state: MetalState): return { 'verification_passed': state['metal_type'] in ['Gold', 'Silver', 'Platinum'] }; graph.add_node('verify', verify_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'verify'); graph.add_edge('verify', END); compiled_graph = graph.compile()