from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PumpSpec(TypedDict): capacity: float; head: float; motor_kw: float; validation_passed: bool; def validate_specs(state: PumpSpec): state['validation_passed'] = state['capacity'] > 0 and state['head'] > 0; return state; graph = StateGraph(PumpSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()