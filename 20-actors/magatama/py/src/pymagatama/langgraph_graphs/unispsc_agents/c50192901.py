from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PastaState(TypedDict): items: List[str]; temperature_validated: bool; shelf_life_ok: bool; def check_temp(state: PastaState): return {'temperature_validated': True}; def check_shelf_life(state: PastaState): return {'shelf_life_ok': True}; graph = StateGraph(PastaState); graph.add_node('temp_check', check_temp); graph.add_node('shelf_check', check_shelf_life); graph.set_entry_point('temp_check'); graph.add_edge('temp_check', 'shelf_check'); graph.add_edge('shelf_check', END); graph = graph.compile()