from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class PeptideState(TypedDict): input_data: dict; validation_results: Annotated[Sequence[str], operator.add]; compliance_status: bool; def validate_purity(state: PeptideState) -> PeptideState: purity = state['input_data'].get('purity', 0); return {'validation_results': ['Purity check passed' if purity >= 98.0 else 'Purity check failed']}; def check_compliance(state: PeptideState) -> PeptideState: return {'compliance_status': True}; graph = StateGraph(PeptideState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()