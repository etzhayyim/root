from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): hardware_list: List[str]; validation_results: List[str]; def validate_accessory(state: State): results = []; for item in state['hardware_list']: results.append(f'Validated {item} for compatibility'); return {'validation_results': results}; graph = StateGraph(State); graph.add_node('validate', validate_accessory); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()