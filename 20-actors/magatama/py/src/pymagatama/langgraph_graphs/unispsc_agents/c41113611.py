from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): data: dict; specs: List[str]; validated: bool; def validate_specs(state: GraphState): compliance = all(k in state['data'] for k in state['specs']); return {'validated': compliance}; graph = StateGraph(GraphState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()