from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): chemical_metadata: dict; processing_steps: Annotated[Sequence[str], operator.add]; status: str; def validate_spec(state: CatalystState) -> dict: return {'status': 'validated'} if state['chemical_metadata'].get('purity_percentage', 0) > 99.0 else {'status': 'rejected'}; def process_catalyst(state: CatalystState) -> dict: return {'processing_steps': ['purification', 'packaging', 'hazard_labeling']}; graph = StateGraph(CatalystState); graph.add_node('validate', validate_spec); graph.add_node('process', process_catalyst); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()