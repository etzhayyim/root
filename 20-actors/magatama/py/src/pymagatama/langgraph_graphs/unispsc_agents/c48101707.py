from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoftServeState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_machine_specs(state: SoftServeState): errors = []; if state['specs'].get('power', 0) < 200: errors.append('Insufficient voltage'); return {'validation_errors': errors}; def determine_procurement_path(state: SoftServeState): return 'approval' if not state['validation_errors'] else 'reject'; graph = StateGraph(SoftServeState); graph.add_node('validate', validate_machine_specs); graph.add_edge('validate', 'approve'); graph.set_entry_point('validate'); graph.add_edge('validate', END)