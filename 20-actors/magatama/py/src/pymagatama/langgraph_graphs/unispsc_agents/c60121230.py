from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PaintBrushState(TypedDict): brush_specs: dict; validation_report: list[str]; def validate_materials(state: PaintBrushState): specs = state.get('brush_specs', {}); report = []; if not specs.get('bristle_material'): report.append('Missing bristle material'); return {'validation_report': report}; def check_compatibility(state: PaintBrushState): return {'validation_report': state['validation_report'] + ['Compatibility check passed']}; graph = StateGraph(PaintBrushState); graph.add_node('validate', validate_materials); graph.add_node('check', check_compatibility); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.set_entry_point('validate'); graph = graph.compile()