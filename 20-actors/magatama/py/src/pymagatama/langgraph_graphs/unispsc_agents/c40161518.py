from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): specs: dict; validation_passed: bool; logs: List[str]; def validate_specs(state: FilterState): p_size = state['specs'].get('pore_size', 0); passed = p_size > 0; return {'validation_passed': passed, 'logs': ['Pore size validated', f'Result: {passed}']}; def generate_report(state: FilterState): return {'logs': state['logs'] + ['Report generated']}; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()