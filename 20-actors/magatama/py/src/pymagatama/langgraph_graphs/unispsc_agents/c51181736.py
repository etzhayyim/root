from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): content: str; quality_score: float; validated: bool; def validate_purity(state: PharmaState): return {'validated': state.get('quality_score', 0) > 99.0}; def quality_check_node(state: PharmaState): return {'quality_score': 99.5}; graph = StateGraph(PharmaState); graph.add_node('check', quality_check_node); graph.add_node('validate', validate_purity); graph.set_entry_point('check'); graph.add_edge('check', 'validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()