from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WellState(TypedDict): depth: float; location: str; compliance_docs: List[str]; approved: bool; def val_drilling_depth(state: WellState): return {'approved': state['depth'] > 0 and state['depth'] < 5000}; def check_compliance(state: WellState): return {'approved': len(state['compliance_docs']) >= 3}; graph = StateGraph(WellState); graph.add_node('validate_depth', val_drilling_depth); graph.add_node('check_certs', check_compliance); graph.set_entry_point('validate_depth'); graph.add_edge('validate_depth', 'check_certs'); graph.add_edge('check_certs', END); graph = graph.compile()