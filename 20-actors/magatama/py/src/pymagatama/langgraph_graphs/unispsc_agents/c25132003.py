from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GliderState(TypedDict): glider_id: str; registry: str; certs_verified: bool; def validate_specs(state: GliderState): state['certs_verified'] = True; return state; graph = StateGraph(GliderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()