from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class CarbonState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; finalized_procurement: dict; def validate_purity(state: CarbonState): purity = state['input_spec'].get('purity', 0); return {'validation_results': ['Purity OK' if purity >= 99.0 else 'Purity Low']} ; def finalize_order(state: CarbonState): return {'finalized_procurement': {'status': 'approved', 'commodity': '11111605'}}; graph = StateGraph(CarbonState); graph.add_node('validate', validate_purity); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()