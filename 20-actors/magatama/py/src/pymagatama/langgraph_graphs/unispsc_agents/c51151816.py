from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; compliance_certs: List[str]; def validate_purity(state: PharmState): return {'purity': state['purity'] >= 99.0}; def check_certs(state: PharmState): return {'compliance_certs': state['compliance_certs']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('certify', check_certs); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); compiled_graph = graph.compile()