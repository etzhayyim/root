from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ValveState(TypedDict): valve_id: str; specs: dict; approved: bool; def validate_specs(state: ValveState): state['approved'] = all(k in state['specs'] for k in ['pressure', 'gas_type', 'cert']); return state; graph = StateGraph(ValveState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()