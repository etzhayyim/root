from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): component_id: str; spec_data: dict; validation_log: Annotated[list, operator.add]; def validate_dimensions(state: State): return {'validation_log': ['Geometric validation passed']}; def perform_pressure_test(state: State): return {'validation_log': ['Hydrostatic pressure test complete']}; graph = StateGraph(State); graph.add_node('validate', validate_dimensions); graph.add_node('pressure_test', perform_pressure_test); graph.set_entry_point('validate'); graph.add_edge('validate', 'pressure_test'); graph.add_edge('pressure_test', END); compiled_graph = graph.compile()