from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class XRayFilmState(TypedDict): sensitivity: str; storage_temp: float; validated: bool; def check_compliance(state: XRayFilmState) -> XRayFilmState: state['validated'] = state['storage_temp'] <= 20.0; return state; graph = StateGraph(XRayFilmState); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('check_compliance'); graph.add_edge('check_compliance', END); compiled_graph = graph.compile()