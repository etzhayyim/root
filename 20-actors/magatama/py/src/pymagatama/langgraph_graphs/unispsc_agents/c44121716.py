from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HighlighterState(TypedDict): items: List[str]; validated_items: List[dict]; def validate_highlighter(state: HighlighterState): return {'validated_items': [{'item': i, 'status': 'compliant'} for i in state['items']]}; graph = StateGraph(HighlighterState); graph.add_node('validate', validate_highlighter); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()