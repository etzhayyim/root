from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class SolderState(TypedDict): commodity_code: str; spec_data: dict; validation_results: Annotated[Sequence[str], operator.add]; def validate_solder_spec(state: SolderState): results = []; if 'viscosity_range' not in state['spec_data']: results.append('MISSING_VISCOSITY'); return {'validation_results': results}; def finalize_procurement(state: SolderState): return {'validation_results': ['READY_FOR_RFQ']}; graph = StateGraph(SolderState); graph.add_node('validate', validate_solder_spec); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()