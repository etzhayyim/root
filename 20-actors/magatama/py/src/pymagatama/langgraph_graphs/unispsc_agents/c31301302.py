from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: ForgingState): state['validation_results'].append('Dimensional check passed'); return state; def check_material_cert(state: ForgingState): state['validation_results'].append('Material cert verified'); return state; graph = StateGraph(ForgingState); graph.add_node('validate_dims', validate_dimensions); graph.add_node('verify_cert', check_material_cert); graph.add_edge('validate_dims', 'verify_cert'); graph.add_edge('verify_cert', END); graph.set_entry_point('validate_dims'); graph = graph.compile()