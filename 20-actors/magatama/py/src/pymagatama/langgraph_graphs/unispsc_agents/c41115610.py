from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): conductivity_cell_specs: dict; validation_results: List[str]; def validate_specs(state: GraphState): specs = state['conductivity_cell_specs']; results = []; if 'cell_constant' not in specs: results.append('Missing Cell Constant'); return {'validation_results': results}; def generate_report(state: GraphState): return {'validation_results': state['validation_results'] + ['Report Generated']}; graph = StateGraph(GraphState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled = graph.compile()