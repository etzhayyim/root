from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; carbon_spec: str; def validate_weld(state: ProcState): state['validated'] = True; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_weld); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()