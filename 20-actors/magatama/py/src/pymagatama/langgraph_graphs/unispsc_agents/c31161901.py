from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpringState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_spring(state: SpringState): specs = state['specs']; state['valid'] = all(k in specs for k in ['spring_constant', 'material']); return state; graph = StateGraph(SpringState); graph.add_node('validate', validate_spring); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()