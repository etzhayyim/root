from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClevisState(TypedDict): clevis_id: str; spec_data: dict; approved: bool; def validate_spec(state: ClevisState) -> ClevisState: state['approved'] = all(k in state['spec_data'] for k in ['material', 'strength']); return state; def route_approval(state: ClevisState) -> str: return 'approved' if state['approved'] else 'rejected'; graph = StateGraph(ClevisState); graph.add_node('validate', validate_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()