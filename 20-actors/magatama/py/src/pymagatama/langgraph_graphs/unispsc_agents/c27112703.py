from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrillState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_drill_specs(state: DrillState): errors = []; if not state['specs'].get('voltage'): errors.append('Missing voltage'); if not state['specs'].get('chuck_size'): errors.append('Missing chuck size'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(DrillState); graph.add_node('validate', validate_drill_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()