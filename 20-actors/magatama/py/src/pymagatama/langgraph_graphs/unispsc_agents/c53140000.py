from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SewingState(TypedDict): items: List[str]; validated_items: List[str]; status: str; def validate_supplies(state: SewingState): return {'validated_items': [i for i in state['items'] if 'non-toxic' in i], 'status': 'validated'}; def compile_graph(): builder = StateGraph(SewingState); builder.add_node('validate', validate_supplies); builder.set_entry_point('validate'); builder.add_edge('validate', END); return builder.compile(); graph = compile_graph()