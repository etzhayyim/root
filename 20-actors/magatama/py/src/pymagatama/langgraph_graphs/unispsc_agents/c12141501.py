from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_purity(state: CatalystState): purity = state['input_data'].get('purity', 0); return {'validation_results': ['purity_checked'] if purity >= 99.9 else ['purity_failed']}; def check_compliance(state: CatalystState): return {'status': 'compliant' if 'purity_checked' in state['validation_results'] else 'blocked'}; graph = StateGraph(CatalystState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()