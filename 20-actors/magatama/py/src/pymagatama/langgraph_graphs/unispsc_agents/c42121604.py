from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VeterinaryProcurementState(TypedDict): items: List[str]; compliance_checks: List[str]; approved: bool; def validate_product(state: VeterinaryProcurementState): state['compliance_checks'].append('ISO_13485_Verified'); return state; def check_regulatory(state: VeterinaryProcurementState): state['approved'] = True; return state; graph = StateGraph(VeterinaryProcurementState); graph.add_node('validate', validate_product); graph.add_node('regulatory', check_regulatory); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); compiled = graph.compile()