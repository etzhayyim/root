from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClassificationState(TypedDict): input_data: str; model_config: dict; classification_result: str; metadata: dict; def validate_data(state): return {'metadata': {'status': 'validated'}}; def run_classification(state): return {'classification_result': 'automated_category_alpha'}; graph = StateGraph(ClassificationState); graph.add_node('validate', validate_data); graph.add_node('classify', run_classification); graph.add_edge('validate', 'classify'); graph.add_edge('classify', END); graph.set_entry_point('validate'); graph = graph.compile()