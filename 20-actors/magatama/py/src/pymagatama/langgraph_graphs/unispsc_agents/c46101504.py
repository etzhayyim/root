from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): firearm_data: dict; compliance_verified: bool; def verify_compliance(state: State) -> State: state['compliance_verified'] = state['firearm_data'].get('permit') is not None; return state; def log_transfer(state: State) -> State: print('Logging serial number to secure registry...'); return state; graph = StateGraph(State); graph.add_node('verify', verify_compliance); graph.add_node('log', log_transfer); graph.set_entry_point('verify'); graph.add_edge('verify', 'log'); graph.add_edge('log', END); graph = graph.compile()