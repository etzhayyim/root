from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GolfSpecState(TypedDict): specs: dict; valid: bool; golf_clubs: List[str]; def validate_specs(state: GolfSpecState): state['valid'] = 'USGA_compliance' in state['specs']; return state; def route_step(state: GolfSpecState): return 'validate' if state['golf_clubs'] else END; graph = StateGraph(GolfSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()