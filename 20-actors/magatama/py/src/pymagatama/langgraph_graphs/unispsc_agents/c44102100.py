from langgraph.graph import StateGraph, END; from typing import TypedDict; class MailMachineState(TypedDict): vendor_specs: dict; compliance_check: bool; workflow_step: str; def validate_specs(state: MailMachineState): state['compliance_check'] = state['vendor_specs'].get('processing_speed', 0) > 1000; return state; graph = StateGraph(MailMachineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()