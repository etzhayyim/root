from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GreaseFittingState(TypedDict): specs: dict; validation_report: List[str]; def validate_fitting(state: GreaseFittingState): valid = all(k in state['specs'] for k in ['thread_size', 'pressure_rating']); return {'validation_report': ['Passed' if valid else 'Failed']}; graph = StateGraph(GreaseFittingState); graph.add_node('validator', validate_fitting); graph.set_entry_point('validator'); graph.add_edge('validator', END); compiled_graph = graph.compile()