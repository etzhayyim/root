from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrawingState(TypedDict): file_path: str; errors: List[str]; validated: bool; def validate_cad(state: DrawingState): # Simulation of CAD/Dimension validation logic; state['validated'] = True if state.get('file_path') else False; return state; def report_status(state: DrawingState): print(f"Validation status: {state['validated']}"); return state; graph = StateGraph(DrawingState); graph.add_node("validate", validate_cad); graph.add_node("report", report_status); graph.set_entry_point("validate"); graph.add_edge("validate", "report"); graph.add_edge("report", END); graph = graph.compile()