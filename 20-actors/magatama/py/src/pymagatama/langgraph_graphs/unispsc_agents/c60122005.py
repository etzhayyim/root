from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LoomState(TypedDict): loom_specs: dict; validation_errors: List[str]; def validate_loom(state: LoomState): errors = []; if state['loom_specs'].get('shaft_count', 0) < 2: errors.append('Minimum 2 shafts required for weaving'); return {'validation_errors': errors}; graph = StateGraph(LoomState); graph.add_node('validate', validate_loom); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()