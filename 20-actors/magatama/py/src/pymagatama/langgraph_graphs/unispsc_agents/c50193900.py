from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FruitState(TypedDict): items: List[str]; validated: bool; compliance_report: str; def validate_pome_fruit(state: FruitState): state['validated'] = all(['certificate' in item for item in state['items']]); state['compliance_report'] = 'Validation Complete' if state['validated'] else 'Missing Certs'; return state; graph = StateGraph(FruitState); graph.add_node('validator', validate_pome_fruit); graph.set_entry_point('validator'); graph.add_edge('validator', END); compiled_graph = graph.compile()