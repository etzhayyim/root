from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): commodity: str; validation_status: bool; compliance_docs: List[str]; def validate_gmp(state: PharmState): state['validation_status'] = True; return state; def check_compliance(state: PharmState): state['compliance_docs'] = ['GMP', 'GDP', 'COA']; return state; builder = StateGraph(PharmState); builder.add_node('validate', validate_gmp); builder.add_node('compliance', check_compliance); builder.set_entry_point('validate'); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); graph = builder.compile()