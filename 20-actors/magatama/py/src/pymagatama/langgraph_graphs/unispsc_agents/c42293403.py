from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ImplantState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: ImplantState): state['validation_results'].append('Material ISO verified'); return state; def inspect_geometry(state: ImplantState): state['validation_results'].append('Geometry verified'); return state; graph = StateGraph(ImplantState); graph.add_node('validate_materials', validate_materials); graph.add_node('inspect_geometry', inspect_geometry); graph.set_entry_point('validate_materials'); graph.add_edge('validate_materials', 'inspect_geometry'); graph.add_edge('inspect_geometry', END); graph = graph.compile()