from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_certs: List[str]; safety_verified: bool; product_type: str; def validate_safety(state: ProcState) -> ProcState: state['safety_verified'] = 'ASTM_F963' in state['vendor_certs']; return state; def route_verification(state: ProcState) -> str: return 'pass' if state['safety_verified'] else 'fail'; graph = StateGraph(ProcState); graph.add_node('validate', validate_safety); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()