from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): item_id: str; specs: dict; valid: bool; def validate_specs(state: ProcurementState): state['valid'] = state['specs'].get('temp_limit', 0) < 45; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()