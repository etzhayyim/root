from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): data: dict; status: str; def validate_specs(state: State): state['status'] = 'validation_passed' if 'alloy_grade' in state['data'] else 'missing_specs'; return state; def check_export_control(state: State): state['status'] = 'restricted' if state['data'].get('requires_export_license') else 'approved'; return state; graph = StateGraph(State); graph.add_node('validator', validate_specs); graph.add_node('control', check_export_control); graph.set_entry_point('validator'); graph.add_edge('validator', 'control'); graph.add_edge('control', END); compiled_graph = graph.compile()