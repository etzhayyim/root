from langgraph.graph import StateGraph, END; from typing import TypedDict; class OxygenState(TypedDict): device_spec: dict; validation_report: str; def validate_timer(state: OxygenState): if state['device_spec'].get('accuracy') < 0.01: return {'validation_report': 'Passed'}; return {'validation_report': 'Failed'}; graph = StateGraph(OxygenState); graph.add_node('validate', validate_timer); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()