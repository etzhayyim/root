from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class JewelryState(TypedDict): metadata: dict; inspection_status: bool; workflow_log: Annotated[Sequence[str], operator.add]; def validate_gem(state: JewelryState): return {'inspection_status': True if state['metadata'].get('cert_id') else False}; def final_review(state: JewelryState): return {'workflow_log': ['Finished high-value inspection']}; graph = StateGraph(JewelryState); graph.add_node('validate', validate_gem); graph.add_node('review', final_review); graph.add_edge('validate', 'review'); graph.add_edge('review', END); graph.set_entry_point('validate'); graph = graph.compile()