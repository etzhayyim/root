from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_id: str; quality_score: float; storage_temp: float; compliance_met: bool; def validate_cold_chain(state: ProcureState) -> dict: return {'compliance_met': state['storage_temp'] <= 2.0}; workflow = StateGraph(ProcureState); workflow.add_node('validate_cold_chain', validate_cold_chain); workflow.set_entry_point('validate_cold_chain'); workflow.add_edge('validate_cold_chain', END); graph = workflow.compile()