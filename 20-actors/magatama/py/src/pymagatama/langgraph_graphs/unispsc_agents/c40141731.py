from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NozzleSpec(TypedDict): specs: dict; compliance_status: str; def validate_specs(state: NozzleSpec): state['compliance_status'] = 'PASS' if 'material' in state['specs'] else 'FAIL'; return state; graph = StateGraph(NozzleSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()