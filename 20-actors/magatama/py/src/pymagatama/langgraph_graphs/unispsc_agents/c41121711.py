from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UrinalysisState(TypedDict): tubes_list: List[dict]; qc_passed: bool; def validate_tubes(state: UrinalysisState): return {'qc_passed': all(t.get('sterile') for t in state['tubes_list'])}; def process_logistics(state: UrinalysisState): return {'qc_passed': state['qc_passed']}; graph = StateGraph(UrinalysisState); graph.add_node('validate', validate_tubes); graph.add_node('logistics', process_logistics); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph.set_entry_point('validate'); graph = graph.compile()