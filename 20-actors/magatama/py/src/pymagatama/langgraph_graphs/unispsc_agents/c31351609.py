from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipelineState(TypedDict): tube_spec: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: PipelineState): state['validation_errors'] = [] if state['tube_spec'].get('pressure_rating', 0) < 100: state['validation_errors'].append('Insufficient pressure rating'); return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(PipelineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()