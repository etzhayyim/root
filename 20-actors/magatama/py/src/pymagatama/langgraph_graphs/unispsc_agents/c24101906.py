from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): data: dict; steps: Annotated[list, operator.add]; def validate_specs(state: ProcurementState): return {'steps': ['spec_validation_passed']}; def check_export_control(state: ProcurementState): return {'steps': ['dual_use_check_cleared']}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('export', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()