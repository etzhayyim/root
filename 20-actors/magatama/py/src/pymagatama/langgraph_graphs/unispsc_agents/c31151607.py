from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChainState(TypedDict): chain_type: str; load_capacity: float; safety_docs: bool; approved: bool; def validate_load(state: ChainState): state['approved'] = state['load_capacity'] > 0; return state; graph = StateGraph(ChainState); graph.add_node('validate', validate_load); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()