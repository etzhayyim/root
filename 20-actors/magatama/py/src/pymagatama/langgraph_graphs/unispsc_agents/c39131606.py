from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[dict]; status: str; spec_validated: bool; def validate_specs(state: ProcurementState): state['spec_validated'] = all('thickness' in item and 'coating' in item for item in state['items']); return state; def route_procurement(state: ProcurementState): return 'validate' if state['items'] else END; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()