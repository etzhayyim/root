from langgraph.graph import StateGraph, END; from typing import TypedDict; class ChemicalState(TypedDict): chemical_name: str; purity: float; validated: bool; def check_purity(state: ChemicalState): return { 'validated': state['purity'] >= 99.0 }; graph = StateGraph(ChemicalState); graph.add_node('validate_purity', check_purity); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', END); graph = graph.compile()