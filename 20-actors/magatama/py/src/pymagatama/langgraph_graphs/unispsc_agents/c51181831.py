from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NorgestrelState(TypedDict): chemical_purity: float; gmp_compliant: bool; status: str; def validate_purity(state: NorgestrelState): state['status'] = 'validated' if state['chemical_purity'] >= 99.0 else 'rejected'; return state; def check_compliance(state: NorgestrelState): state['status'] = 'compliant' if state['gmp_compliant'] else 'non-compliant'; return state; graph = StateGraph(NorgestrelState); graph.add_node('validate', validate_purity); graph.add_node('check', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()