from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LightingState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: LightingState): specs = state['specs']; errors = []; if specs.get('wattage', 0) > 1000: errors.append('Wattage exceeds safe thermal limits'); return {'valid': len(errors) == 0, 'error_log': errors}; def compile_graph(): graph = StateGraph(LightingState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); return graph.compile(); graph = compile_graph()