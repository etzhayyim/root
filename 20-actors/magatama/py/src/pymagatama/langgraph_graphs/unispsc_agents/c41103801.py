from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MixerState(TypedDict): specs: dict; validation_passed: bool; motor_speed: int; def validate_specs(state: MixerState): return {'validation_passed': state['specs'].get('torque', 0) > 0}; def run_safety_check(state: MixerState): return {'validation_passed': state['validation_passed'] and state['motor_speed'] < 10000}; graph = StateGraph(MixerState); graph.add_node('validate', validate_specs); graph.add_node('safety', run_safety_check); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()