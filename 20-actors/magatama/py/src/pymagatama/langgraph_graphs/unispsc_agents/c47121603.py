from langgraph.graph import StateGraph, END; from typing import TypedDict; class FloorPolisherState(TypedDict): specs: dict; valid: bool; def validate_specs(state: FloorPolisherState): state['valid'] = state['specs'].get('motor_power_watts', 0) > 500; return state; graph = StateGraph(FloorPolisherState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()