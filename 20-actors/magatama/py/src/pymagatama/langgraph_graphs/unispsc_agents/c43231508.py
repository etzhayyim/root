from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): vendor_data: str; validation_result: bool; def val_step(state: State): return {'validation_result': True}; def process_step(state: State): return {'vendor_data': 'Processed'}; graph = StateGraph(State); graph.add_node('validate', val_step); graph.add_node('process', process_step); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.compile()