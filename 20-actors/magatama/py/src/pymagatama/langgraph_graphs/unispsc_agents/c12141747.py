from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): purity_level: float; batch_id: str; validation_logs: Annotated[list[str], operator.add]; graph = StateGraph(ChemicalState); def validate_purity(state: ChemicalState): return {'validation_logs': ['Purity check passed'] if state['purity_level'] > 0.99 else ['Purity check failed']} ; def process_batch(state: ChemicalState): return {'validation_logs': [f'Processing batch {state['batch_id']}']} ; graph.add_node('validate', validate_purity); graph.add_node('process', process_batch); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()