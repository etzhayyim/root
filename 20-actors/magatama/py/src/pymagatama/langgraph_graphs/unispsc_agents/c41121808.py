from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BuretState(TypedDict): capacity: float; accuracy_class: str; specs_verified: bool; def validate_buret(state: BuretState): state['specs_verified'] = state['capacity'] > 0 and state['accuracy_class'] in ['A', 'B']; return state; builder = StateGraph(BuretState); builder.add_node('validate', validate_buret); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()