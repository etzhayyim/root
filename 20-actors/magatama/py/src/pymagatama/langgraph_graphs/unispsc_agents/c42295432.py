from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UrologicalStentState(TypedDict): stent_type: str; is_sterile: bool; specs_validated: bool; def validate_specs(state: UrologicalStentState): return {'specs_validated': True} if state.get('is_sterile') else {'specs_validated': False}; def check_compliance(state: UrologicalStentState): print(f'Processing {state.get(stent_type)} with strict ISO compliance'); return {'specs_validated': True}; graph = StateGraph(UrologicalStentState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()