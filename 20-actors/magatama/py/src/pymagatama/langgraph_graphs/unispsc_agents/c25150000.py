from langgraph.graph import StateGraph, END; from typing import TypedDict; class SpacecraftState(TypedDict): status: str; spec_data: dict; approval_required: bool; def validate_specs(state: SpacecraftState): state['approval_required'] = True if state['spec_data'].get('radiation_level', 0) > 50 else False; return state; def check_export_control(state: SpacecraftState): state['status'] = 'ITAR_REVIEW' if state['approval_required'] else 'APPROVED'; return state; graph = StateGraph(SpacecraftState); graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph = graph.compile()