from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SatelliteState(TypedDict): specs: dict; compliance_status: bool; workflow_steps: List[str]; def validate_security(state: SatelliteState): state['compliance_status'] = True; state['workflow_steps'].append('Security Validation'); return state; def export_control_check(state: SatelliteState): state['workflow_steps'].append('Export Control Review'); return state; graph = StateGraph(SatelliteState); graph.add_node('security', validate_security); graph.add_node('export', export_control_check); graph.set_entry_point('security'); graph.add_edge('security', 'export'); graph.add_edge('export', END); graph = graph.compile()