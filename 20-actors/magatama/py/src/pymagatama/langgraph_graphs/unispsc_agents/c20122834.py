from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MotorState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_spec(state: MotorState): result = 'Valid' if state['input_spec'].get('torque_rating_nm', 0) > 0 else 'Invalid'; return {'validation_results': [f'Spec check: {result}'], 'status': result}; def compile_graph(): graph = StateGraph(MotorState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()