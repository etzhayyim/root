from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TitaniumState(TypedDict): vendor_certs: List[str]; inspection_results: dict; validated: bool; def check_weld_integrity(state: TitaniumState): state['validated'] = all(c in state['vendor_certs'] for c in ['ISO9001', 'AS9100']); return state; graph = StateGraph(TitaniumState); graph.add_node('validate', check_weld_integrity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()