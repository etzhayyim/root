from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GoniometerState(TypedDict): data: dict; validation_errors: List[str]; approved: bool; def validate_goniometer(state: GoniometerState): data = state['data']; errors = []; if not data.get('calibration_cert'): errors.append('Missing Calibration Certificate'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(GoniometerState); graph.add_node('validate', validate_goniometer); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()