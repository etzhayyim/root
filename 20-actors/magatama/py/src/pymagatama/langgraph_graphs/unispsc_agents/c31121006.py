from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): alloy: str; specs: dict; valid: bool; def validate_specs(state: CastingState): return {'valid': state['specs'].get('tolerance', 0) < 0.05}; def finalize(state: CastingState): return {'valid': True}; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()