from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): input_data: dict; processing_results: Annotated[list, operator.add]; status: str; def validate_purity(state: AgentState): return {'processing_results': [f'Purity validated: {state[input_data].get(purity_percentage)}'], 'status': 'validated'}; def check_compliance(state: AgentState): return {'processing_results': ['Compliance check passed'], 'status': 'compliant'}; builder = StateGraph(AgentState); builder.add_node(validate_purity, validate_purity); builder.add_node(check_compliance, check_compliance); builder.set_entry_point(validate_purity); builder.add_edge(validate_purity, check_compliance); builder.add_edge(check_compliance, END); graph = builder.compile()