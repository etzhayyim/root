from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class SupplyChainState(TypedDict): items: Annotated[list, operator.add]; validation_log: list; status: str; def validate_product(state: SupplyChainState): state['validation_log'].append('Validating quality and storage conditions'); return {'status': 'validated'} if len(state['items']) > 0 else {'status': 'rejected'}; def process_shipment(state: SupplyChainState): state['validation_log'].append('Processing logistics for perishable goods'); return {'status': 'shipped'}; graph = StateGraph(SupplyChainState); graph.add_node('validate', validate_product); graph.add_node('process', process_shipment); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()