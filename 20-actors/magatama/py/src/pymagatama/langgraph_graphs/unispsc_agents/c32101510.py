from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoardState(TypedDict): gerber_files: List[str]; validation_results: dict; ready_for_fab: bool; def validate_gerbers(state: BoardState) -> BoardState: state['validation_results'] = {'drc_passed': True}; state['ready_for_fab'] = True; return state; graph = StateGraph(BoardState); graph.add_node('validate', validate_gerbers); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()