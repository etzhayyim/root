from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StrainReliefState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(StrainReliefState); def validate_specs(state: StrainReliefState): report = []; required = ['material', 'cable_range']; [report.append(f'Missing {f}') for f in required if f not in state['specs']]; return {'validation_report': report}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()