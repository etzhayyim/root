from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaperState(TypedDict): paper_specs: dict; validation_report: List[str]; def validate_specs(state: PaperState): specs = state['paper_specs']; errors = []; if specs.get('basis_weight', 0) < 50: errors.append('Basis weight too low'); return {'validation_report': errors}; def check_inventory(state: PaperState): return {'validation_report': state['validation_report'] + ['Inventory check complete']}; graph = StateGraph(PaperState); graph.add_node('validate', validate_specs); graph.add_node('inventory', check_inventory); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()