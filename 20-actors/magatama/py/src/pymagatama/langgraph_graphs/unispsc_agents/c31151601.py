from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetyChainState(TypedDict): chain_spec: dict; validation_results: List[str]; def validate_specs(state: SafetyChainState): results = []; capacity = state['chain_spec'].get('load_capacity', 0); if capacity <= 0: results.append('Invalid capacity'); return {'validation_results': results}; def generate_report(state: SafetyChainState): return {'validation_results': ['Compliance Verified']}; graph = StateGraph(SafetyChainState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()