from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class OphthalmicState(TypedDict): vendor_certs: List[str]; qc_passed: bool; temp_logs: dict; graph = StateGraph(OphthalmicState); def validate_certs(state: OphthalmicState): return {'qc_passed': 'FDA' in state['vendor_certs']}; def check_cold_chain(state: OphthalmicState): return {'qc_passed': state['temp_logs'].get('stable', False)}; graph.add_node('validate', validate_certs); graph.add_node('cold_chain', check_cold_chain); graph.set_entry_point('validate'); graph.add_edge('validate', 'cold_chain'); graph.add_edge('cold_chain', END); compile_graph = graph.compile()