from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ContrastState(TypedDict): chemical_name: str; purity: float; is_certified: bool; approved: bool; def validate_purity(state: ContrastState): return {'approved': state['purity'] >= 99.9}; def check_certification(state: ContrastState): return {'approved': state['is_certified']}; graph = StateGraph(ContrastState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_certification', check_certification); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_certification'); graph.add_edge('check_certification', END); graph = graph.compile()