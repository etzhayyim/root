from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class GearSpecState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: GearSpecState): errors = []; if state['specs'].get('backlash', 10) > 5: errors.append('Backlash exceeds high-precision threshold'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(GearSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()