from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): dimensions: dict; material_spec: str; validation_passed: bool; def validate_forging(state: ForgingState): state['validation_passed'] = 'tensile_strength' in state['material_spec'] and all(v > 0 for v in state['dimensions'].values()); return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_forging); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()