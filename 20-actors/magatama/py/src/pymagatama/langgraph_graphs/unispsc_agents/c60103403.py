from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SocialStudiesState(TypedDict): content_url: str; compliance_check: bool; metadata: dict; def validate_content(state: SocialStudiesState): return {'compliance_check': True if state['content_url'] else False}; def format_manifest(state: SocialStudiesState): return {'metadata': {'status': 'processed', 'source': state['content_url']}}; graph = StateGraph(SocialStudiesState); graph.add_node('validate', validate_content); graph.add_node('manifest', format_manifest); graph.add_edge('validate', 'manifest'); graph.add_edge('manifest', END); graph.set_entry_point('validate'); graph = graph.compile()