from langgraph.graph import StateGraph, END; from typing import TypedDict; class SoupCrockState(TypedDict): specs: dict; approval: bool; graph = StateGraph(SoupCrockState); def validate_thermal_specs(state: SoupCrockState): state['approval'] = state['specs'].get('heat_resistance', 0) > 200; return state; graph.add_node('validate', validate_thermal_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()