from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): device_id: str; compliance_docs: List[str]; validated: bool; def validate_device(state: State): state['validated'] = all(doc in state['compliance_docs'] for doc in ['ISO13485', 'IEC60601']); return state; graph = StateGraph(State); graph.add_node('validate', validate_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()