from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrainageState(TypedDict): spec_data: dict; validation_passed: bool; warnings: List[str]; def validate_spec(state: DrainageState): state['validation_passed'] = 'Capacity' in state['spec_data'] and 'Sterility' in state['spec_data']; return state; builder = StateGraph(DrainageState); builder.add_node('validate', validate_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()