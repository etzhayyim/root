from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): status: str; spec_validated: bool; def check_purity(state: ProcureState): return {'spec_validated': True, 'status': 'VALIDATED'}; graph = StateGraph(ProcureState); graph.add_node('validate', check_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()