from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): status: str; specs: dict; compliance: list; def validate_specs(state: ProcState): state['compliance'].append('CERT_CHECKED'); return state; def check_regulatory(state: ProcState): state['compliance'].append('REG_VERIFIED'); return state; graph = StateGraph(ProcState); graph.add_node('spec_validation', validate_specs); graph.add_node('regulatory_check', check_regulatory); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', 'regulatory_check'); graph.add_edge('regulatory_check', END); graph = graph.compile()