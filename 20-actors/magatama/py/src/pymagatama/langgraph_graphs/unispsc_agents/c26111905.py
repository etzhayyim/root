from langgraph.graph import StateGraph, END; from typing import TypedDict; class ClutchState(TypedDict): torque_rating: float; bore_size: float; approved: bool; def validate_spec(state: ClutchState): return {'approved': state['torque_rating'] > 0 and state['bore_size'] > 0}; graph = StateGraph(ClutchState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()