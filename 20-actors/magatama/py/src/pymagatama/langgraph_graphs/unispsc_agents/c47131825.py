from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): chemical_data: str; validation_passed: bool; def validate_solvent(state: State): state['validation_passed'] = 'flammable' not in state['chemical_data'].lower(); return state; def route_approval(state: State): return 'approve' if state['validation_passed'] else 'reject'; graph = StateGraph(State); graph.add_node('validate', validate_solvent); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()