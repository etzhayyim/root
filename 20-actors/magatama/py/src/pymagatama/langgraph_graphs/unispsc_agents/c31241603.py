from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GlassState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: GlassState): return {'compliance': 'Tolerance' in state['specs'] and 'Material' in state['specs']}; def check_safety(state: GlassState): return {'status': 'Approved' if state['compliance'] else 'Rejected'}; graph = StateGraph(GlassState); graph.add_node('validation', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validation'); graph.add_edge('validation', 'safety'); graph.add_edge('safety', END); graph = graph.compile()