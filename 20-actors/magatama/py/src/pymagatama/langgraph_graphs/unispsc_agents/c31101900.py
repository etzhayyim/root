from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): component_specs: dict; validation_results: List[str]; def validate_material(state: CastingState): state['validation_results'].append('Material chemistry verified'); return state; def check_t tolerances(state: CastingState): state['validation_results'].append('Tolerance check passed'); return state; graph = StateGraph(CastingState); graph.add_node('material_check', validate_material); graph.add_node('tolerance_check', check_t tolerances); graph.add_edge('material_check', 'tolerance_check'); graph.add_edge('tolerance_check', END); graph.set_entry_point('material_check'); graph = graph.compile()