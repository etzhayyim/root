from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InsulationState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(InsulationState); def validate_thermal_specs(state: InsulationState): return {'validation_results': ['Thermal conductivity verified']}; def check_fire_standards(state: InsulationState): return {'validation_results': ['UL94 fire rating confirmed']}; graph.add_node('thermal_check', validate_thermal_specs); graph.add_node('fire_check', check_fire_standards); graph.set_entry_point('thermal_check'); graph.add_edge('thermal_check', 'fire_check'); graph.add_edge('fire_check', END); graph = graph.compile()