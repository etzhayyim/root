from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HairToolState(TypedDict): tool_type: str; specs: dict; compliance_check: bool; def validate_material(state: HairToolState): state['compliance_check'] = 'plastic' in state['specs'].get('material', '').lower(); return state; def finalize_procurement(state: HairToolState): return {'status': 'approved' if state['compliance_check'] else 'rejected'}; graph = StateGraph(HairToolState); graph.add_node('validate', validate_material); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate');