from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; valid: bool; risk_levels: List[str]; def validate_medical_device(state: ProcState): state['valid'] = 'ISO_13485_certification' in state['specs'] and state['specs']['pressure_regulation_range'] is not None; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()