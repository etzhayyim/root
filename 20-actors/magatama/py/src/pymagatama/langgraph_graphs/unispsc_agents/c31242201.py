from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OpticalSpec(TypedDict): specs: dict; compliance_status: bool; def validate_specs(state: OpticalSpec): state['compliance_status'] = 'Transmission_Reflection_Ratio' in state['specs'] and state['specs']['Transmission_Reflection_Ratio'] > 0; return state; graph = StateGraph(OpticalSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()