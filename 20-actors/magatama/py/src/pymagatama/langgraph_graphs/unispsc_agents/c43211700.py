from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InputDeviceState(TypedDict): device_type: str; specs: dict; validation_results: List[str]; def validate_specs(state: InputDeviceState): return {'validation_results': ['Interface verified', 'Compliance checked']}; def approve_specs(state: InputDeviceState): return {'validation_results': state['validation_results'] + ['Approved']}; graph = StateGraph(InputDeviceState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()