from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; gmp_certified: bool; quality_report: str; approved: bool; def check_gmp(state: DrugState): return {'gmp_certified': True}; def validate_quality(state: DrugState): return {'approved': True if state['quality_report'] == 'pass' else False}; graph = StateGraph(DrugState); graph.add_node('check_gmp', check_gmp); graph.add_node('validate_quality', validate_quality); graph.set_entry_point('check_gmp'); graph.add_edge('check_gmp', 'validate_quality'); graph.add_edge('validate_quality', END); graph = graph.compile()