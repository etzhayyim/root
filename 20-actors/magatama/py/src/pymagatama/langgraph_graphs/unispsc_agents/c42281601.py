from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DeconState(TypedDict): chemical_report: str; compliance_status: bool; workflow_steps: List[str]; def validate_chemical(state: DeconState): state['compliance_status'] = True if 'SDS' in state['chemical_report'] else False; return state; def finalize_order(state: DeconState): state['workflow_steps'] = ['Safety Check', 'Compliance Verification', 'Shipping Approval']; return state; graph = StateGraph(DeconState); graph.add_node('validate', validate_chemical); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()