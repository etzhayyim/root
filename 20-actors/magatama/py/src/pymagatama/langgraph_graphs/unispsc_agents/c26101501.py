from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HydraulicState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: HydraulicState): errors = []; if state['specs'].get('max_pressure', 0) > 700: errors.append('Pressure exceeding high-stress threshold'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(HydraulicState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()