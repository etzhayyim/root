from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; gmp_certified: bool; quality_check_passed: bool; def check_gmp(state: PharmaState): return {'gmp_certified': True}; def validate_quality(state: PharmaState): return {'quality_check_passed': True}; graph = StateGraph(PharmaState); graph.add_node('gmp_check', check_gmp); graph.add_node('quality', validate_quality); graph.add_edge('gmp_check', 'quality'); graph.add_edge('quality', END); graph.set_entry_point('gmp_check'); graph = graph.compile()