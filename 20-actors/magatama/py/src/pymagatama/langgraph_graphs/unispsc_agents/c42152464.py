from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalKitState(TypedDict): kit_items: List[str]; compliance_status: bool; validation_log: List[str]; def validate_medical_compliance(state: DentalKitState): state['compliance_status'] = True; state['validation_log'].append('ISO 13485 validated.'); return state; def check_inventory(state: DentalKitState): state['validation_log'].append('Inventory levels verified.'); return state; graph = StateGraph(DentalKitState); graph.add_node('validate', validate_medical_compliance); graph.add_node('inventory', check_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); compiled_graph = graph.compile()