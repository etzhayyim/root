from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec_data: dict; graph = StateGraph(State); def validate_compliance(state): return {'status': 'validated' if 'ISO 13485' in state['spec_data'] else 'rejected'}; def check_sterility(state): return {'status': 'sterile_verified'}; graph.add_node('compliance', validate_compliance); graph.add_node('sterility', check_sterility); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'sterility'); graph.add_edge('sterility', END); graph = graph.compile()