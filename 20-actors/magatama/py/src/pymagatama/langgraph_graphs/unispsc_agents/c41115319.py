from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): data: dict; valid: bool; def validate_instrument(state: State): state['valid'] = 'calibration_doc' in state['data'] and state['data']['optical_range'] > 0; return state; graph = StateGraph(State); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()