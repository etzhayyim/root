from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PummeloState(TypedDict): data: dict; analysis_result: str; def validate_pummelo(state: PummeloState): state['analysis_result'] = 'Approved' if state['data'].get('brix', 0) > 40 else 'Rejected'; return state; graph = StateGraph(PummeloState); graph.add_node('validation', validate_pummelo); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()