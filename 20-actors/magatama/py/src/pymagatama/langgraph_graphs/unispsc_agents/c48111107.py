from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: str; validation_result: bool; status: str; def validate_machine_specs(state: State): return {'validation_result': True, 'status': 'Specs Verified'}; def check_regulatory_compliance(state: State): return {'status': 'Compliance Passed'}; graph = StateGraph(State); graph.add_node('validate', validate_machine_specs); graph.add_node('compliance', check_regulatory_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); 