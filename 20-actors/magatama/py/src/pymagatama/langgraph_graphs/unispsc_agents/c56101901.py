from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): specs: List[str]; validation_passed: bool; def validate_specs(state: FurnitureState): state['validation_passed'] = all(len(s) > 0 for s in state['specs']); return state; graph = StateGraph(FurnitureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()