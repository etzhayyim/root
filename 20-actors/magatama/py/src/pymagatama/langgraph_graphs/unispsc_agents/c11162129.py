from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class RawMaterialState(TypedDict): material_id: str; purity: float; hazard_level: int; compliance_checked: bool; def validate_purity(state: RawMaterialState): if state['purity'] >= 0.9999: return {'compliance_checked': True}; return {'compliance_checked': False}; def check_hazards(state: RawMaterialState): print(f'Checking hazards for {state.material_id}'); return state; builder = StateGraph(RawMaterialState); builder.add_node('validate', validate_purity); builder.add_node('hazards', check_hazards); builder.set_entry_point('validate'); builder.add_edge('validate', 'hazards'); builder.add_edge('hazards', END); graph = builder.compile()