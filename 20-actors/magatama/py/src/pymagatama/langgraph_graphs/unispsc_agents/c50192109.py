from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SnackProcurementState(TypedDict): items: List[str]; inspection_status: str; next_step: str; def validate_shelf_life(state): return {'inspection_status': 'passed' if all(['expiration' in item for item in state['items']]) else 'failed'}; def process_logistics(state): return {'next_step': 'warehouse_entry'}; graph = StateGraph(SnackProcurementState); graph.add_node('validate', validate_shelf_life); graph.add_node('logistics', process_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph.compile()