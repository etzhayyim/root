from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SutureCartState(TypedDict): specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_medical_specs(state: SutureCartState): state['compliance_status'] = 'iso_13485_certification' in state['specs']; state['workflow_log'].append('Validation complete'); return state; graph = StateGraph(SutureCartState); graph.add_node('validate', validate_medical_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()