from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): device_id: str; data: dict; status: str; def validate_sensors(state: State): return {'status': 'sensor_checked'}; def clinical_compliance(state: State): return {'status': 'compliant'}; graph = StateGraph(State); graph.add_node('validate', validate_sensors); graph.add_node('compliance', clinical_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()