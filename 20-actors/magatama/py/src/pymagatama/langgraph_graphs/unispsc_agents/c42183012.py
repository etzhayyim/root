from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class OphthalmicState(TypedDict): device_id: str; validation_status: bool; compliance_docs: List[str]; def validate_device(state: OphthalmicState): return {'validation_status': True if state['device_id'] else False}; def check_compliance(state: OphthalmicState): return {'compliance_docs': ['ISO13485_Verified'] if len(state['compliance_docs']) > 0 else []}; graph = StateGraph(OphthalmicState); graph.add_node('validate', validate_device); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()