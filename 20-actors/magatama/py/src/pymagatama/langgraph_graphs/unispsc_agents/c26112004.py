from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClutchRepairState(TypedDict): kit_id: str; verified: bool; components: List[str]; def validate_parts(state: ClutchRepairState): state['verified'] = len(state['components']) > 0; return state; def finalize_specs(state: ClutchRepairState): return {'verified': True}; graph = StateGraph(ClutchRepairState); graph.add_node('validate', validate_parts); graph.add_node('finalize', finalize_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()