from langgraph.graph import StateGraph, END; from typing import TypedDict; class SkylightState(TypedDict): specs: dict; compliance: bool; valid: bool; def check_specs(state: SkylightState): return {'compliance': state['specs'].get('u_value', 0) < 1.5}; def validate(state: SkylightState): return {'valid': state['compliance']}; graph = StateGraph(SkylightState); graph.add_node('check_specs', check_specs); graph.add_node('validate', validate); graph.add_edge('check_specs', 'validate'); graph.add_edge('validate', END); graph.set_entry_point('check_specs'); graph = graph.compile()