from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShredderState(TypedDict): specs: dict; validation_results: List[str]; def validate_shredder(state: ShredderState): result = 'Pass' if state['specs'].get('din_level', 0) >= 4 else 'Fail'; return {'validation_results': [result]}; graph = StateGraph(ShredderState); graph.add_node('validate', validate_shredder); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()