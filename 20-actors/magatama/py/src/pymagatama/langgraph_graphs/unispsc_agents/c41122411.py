from langgraph.graph import StateGraph, END; from typing import TypedDict; class TimerState(TypedDict): specs: dict; valid: bool; def validate_timer(state: TimerState): state['valid'] = 'calibration_certificate' in state['specs'] and state['specs']['accuracy'] < 0.01; return state; graph = StateGraph(TimerState); graph.add_node('validate', validate_timer); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()