from langgraph.graph import StateGraph, END; from typing import TypedDict; class BushingState(TypedDict): specs: dict; valid: bool; def validate_dimensions(state: BushingState): id_val = state['specs'].get('ID', 0); od_val = state['specs'].get('OD', 0); state['valid'] = od_val > id_val; return state; graph = StateGraph(BushingState); graph.add_node('validate', validate_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()