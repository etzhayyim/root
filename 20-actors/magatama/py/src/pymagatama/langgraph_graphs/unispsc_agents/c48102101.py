from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DisplayCaseState(TypedDict): vendor: str; specifications: dict; approval: bool; def validate_specs(state: DisplayCaseState): return {'approval': 'glass_safety_certification' in state['specifications']}; def procurement_workflow(state: DisplayCaseState): return {'approval': True if state.get('approval') else False}; graph = StateGraph(DisplayCaseState); graph.add_node('validate', validate_specs); graph.add_node('procure', procurement_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'procure'); graph.add_edge('procure', END); graph = graph.compile()