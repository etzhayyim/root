from langgraph.graph import StateGraph, END; from typing import TypedDict; class PinState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PinState): state['valid'] = 'Material Grade' in state['specs'] and 'Dimensions' in state['specs']; return state; builder = StateGraph(PinState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()