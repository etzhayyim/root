from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; purity: float; compliance_metadata: dict; def validate_purity(state: DrugState): return {'compliance_metadata': {'status': 'passed' if state['purity'] >= 99.0 else 'failed'}}; def finalize_procurement(state: DrugState): return {'compliance_metadata': {**state['compliance_metadata'], 'finalized': True}}; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()