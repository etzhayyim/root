from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TapeProcurementState(TypedDict): vendor_specs: dict; validation_report: str; graph = StateGraph(TapeProcurementState); def validate_spec(state: TapeProcurementState): adhesion = state['vendor_specs'].get('adhesion_strength', 0); return {'validation_report': 'Passed' if adhesion > 5 else 'Failed'}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()