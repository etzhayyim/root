from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChromatographyState(TypedDict): vendor_docs: List[str]; inspection_passed: bool; compliance_status: bool; def validate_instrument(state: ChromatographyState): state['inspection_passed'] = all(['calibration' in doc.lower() for doc in state['vendor_docs']]); return state; def check_compliance(state: ChromatographyState): state['compliance_status'] = state['inspection_passed'] and True; return state; graph = StateGraph(ChromatographyState); graph.add_node('validate', validate_instrument); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()