from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SecurityBarState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: SecurityBarState): state['approved'] = 'Tensile Strength' in state['specs'] and state['specs']['Tensile Strength'] > 500; return state; graph = StateGraph(SecurityBarState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()