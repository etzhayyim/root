from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabEquipmentState(TypedDict): equipment_id: str; specifications: dict; validation_passed: bool; def validate_specs(state: LabEquipmentState): state['validation_passed'] = 'Material Grade' in state['specifications'] and state['specifications']['Material Grade'] == 'AISI 304'; return state; graph = StateGraph(LabEquipmentState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()