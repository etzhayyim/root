from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class DrillingPartState(TypedDict): part_id: str; specs: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_specs(state: DrillingPartState): return {'validation_results': ['dimensional_check_passed', 'metallurgical_check_passed']}; def finalize_procurement(state: DrillingPartState): return {'status': 'procurement_ready'}; graph = StateGraph(DrillingPartState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()