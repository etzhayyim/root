from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MagnetState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_magnets(state: MagnetState): errors = []; if 'grade' not in state['specs']: errors.append('Missing magnetic grade'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(MagnetState); graph.add_node('validate', validate_magnets); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()