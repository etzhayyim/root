from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): component_id: str; weld_inspect_passed: bool; certs: List[str]; def validate_assembly(state: AssemblyState): return {'weld_inspect_passed': True} if state.get('certs') else {'weld_inspect_passed': False}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_assembly); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()