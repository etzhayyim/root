from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ToolProcurementState(TypedDict): tool_specs: dict; validation_results: List[str]; graph = StateGraph(ToolProcurementState); def validate_spec(state: ToolProcurementState): results = []; hardness = state['tool_specs'].get('hrc', 0); results.append('Passed' if 45 <= hardness <= 60 else 'Failed'); return {'validation_results': results}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()