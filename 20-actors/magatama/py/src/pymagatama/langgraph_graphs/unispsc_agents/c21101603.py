from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadiatorState(TypedDict): specs: List[str]; validation_result: bool; def validate_specs(state: RadiatorState): return {'validation_result': len(state['specs']) > 3}; def process_procurement(state: RadiatorState): print('Processing radiator procurement...'); return {'validation_result': True}; graph = StateGraph(RadiatorState); graph.add_node('validate', validate_specs); graph.add_node('procure', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'procure'); graph.add_edge('procure', END);