from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MachineSpec(TypedDict): specs: dict; compliance_cleared: bool; graph = StateGraph(MachineSpec); def validate_specs(state: MachineSpec): return {'compliance_cleared': len(state['specs']) > 0}; def export_control_check(state: MachineSpec): return {'compliance_cleared': state.get('eccn') is not None}; graph.add_node('validate', validate_specs); graph.add_node('compliance', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()