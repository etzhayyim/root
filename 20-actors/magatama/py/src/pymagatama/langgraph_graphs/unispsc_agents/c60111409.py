from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ButtonSpec(TypedDict): material: str; dimensions: List[float]; color_fastness: int; graph = StateGraph(ButtonSpec); def validate_material(state: ButtonSpec): return {'material': state['material'].upper()}; def check_safety(state: ButtonSpec): return {'color_fastness': max(state['color_fastness'], 3)}; graph.add_node('validate', validate_material); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()