from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): messages: Annotated[list, operator.add]; content_type: str; safety_standard_compliance: bool; def validator(state: State): state['safety_standard_compliance'] = 'safety_data' in state; return state; graph = StateGraph(State); graph.add_node('validate', validator); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()