from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SwitchState(TypedDict): key_code: str; specs_verified: bool; def validate_switch(state: SwitchState): return {'specs_verified': len(state.get('key_code', '')) > 3}; def classify_risk(state: SwitchState): return 'high' if state['specs_verified'] else 'low'; graph = StateGraph(SwitchState); graph.add_node('validate', validate_switch); graph.add_node('risk_check', classify_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'risk_check'); graph.add_edge('risk_check', END); graph = graph.compile()