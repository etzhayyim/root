from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class HydrocarbonState(TypedDict): fuel_type: str; purity_check: bool; safety_compliance: bool; audit_log: Annotated[list, operator.add]; def validate_fuel(state: HydrocarbonState): state['purity_check'] = True; state['audit_log'] = ['Purity validated']; return state; def check_safety(state: HydrocarbonState): state['safety_compliance'] = True; state['audit_log'] = ['Safety checks cleared']; return state; graph = StateGraph(HydrocarbonState); graph.add_node('validate', validate_fuel); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()