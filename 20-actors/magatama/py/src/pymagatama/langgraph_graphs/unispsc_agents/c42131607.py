from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MedicalApparelState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: MedicalApparelState): errors = []; required = ['material_composition', 'antistatic_property']; for field in required: if field not in state['specs']: errors.append(f'Missing {field}'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(MedicalApparelState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()