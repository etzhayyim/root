from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StickerBoxState(TypedDict): specs: dict; validation_errors: List[str]; def validate_specs(state: StickerBoxState): errors = []; if not state['specs'].get('dimensions'): errors.append('Missing dimensions'); return {'validation_errors': errors}; graph = StateGraph(StickerBoxState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()