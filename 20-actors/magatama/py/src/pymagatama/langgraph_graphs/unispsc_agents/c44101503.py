from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class MFPState(TypedDict): print_speed: int; security_features: list[str]; status: str; def validate_speed(state: MFPState): return {'status': 'valid' if state['print_speed'] > 20 else 'insufficient'}; def check_security(state: MFPState): return {'status': 'secured' if 'TLS' in state['security_features'] else 'insecure'}; graph = StateGraph(MFPState); graph.add_node('speed_check', validate_speed); graph.add_node('security_check', check_security); graph.set_entry_point('speed_check'); graph.add_edge('speed_check', 'security_check'); graph.add_edge('security_check', END); graph = graph.compile()