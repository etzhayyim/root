from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SweeperState(TypedDict): specs: dict; validation_errors: Annotated[list, operator.add]; status: str; def validate_specs(state: SweeperState): errors = []; if not state['specs'].get('cleaning_efficiency'): errors.append('Missing efficiency rating'); return {'validation_errors': errors}; def finalize_procurement(state: SweeperState): return {'status': 'READY_FOR_RFQ' if not state['validation_errors'] else 'REJECTED'}; graph = StateGraph(SweeperState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()