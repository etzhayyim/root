from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_code: str; quality_checks: List[str]; approved: bool; def validate_purity(state: DrugState) -> DrugState: state['quality_checks'].append('Purity Verified'); state['approved'] = True; return state; def check_storage(state: DrugState) -> DrugState: state['quality_checks'].append('Temp Control Validated'); return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('storage', check_storage); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph.set_entry_point('validate'); graph = graph.compile()