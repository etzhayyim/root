from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TesterState(TypedDict): device_id: str; test_specs: dict; validation_status: bool; def validate_spec(state: TesterState): state['validation_status'] = all(k in state['test_specs'] for k in ['frequency', 'pins']); return state; def run_diagnostic(state: TesterState): print(f'Running hardware diagnostic for {state['device_id']}'); return state; graph = StateGraph(TesterState); graph.add_node('validate', validate_spec); graph.add_node('diagnostic', run_diagnostic); graph.set_entry_point('validate'); graph.add_edge('validate', 'diagnostic'); graph.add_edge('diagnostic', END); compiled_graph = graph.compile()