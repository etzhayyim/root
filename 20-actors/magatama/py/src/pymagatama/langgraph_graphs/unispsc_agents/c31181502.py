from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GasketState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: GasketState): return {'validation_results': state['validation_results'] + ['Material Check Passed']}; def validate_dimensions(state: GasketState): return {'validation_results': state['validation_results'] + ['Dimensions Verified']}; graph = StateGraph(GasketState); graph.add_node('material', validate_material); graph.add_node('dimensions', validate_dimensions); graph.set_entry_point('material'); graph.add_edge('material', 'dimensions'); graph.add_edge('dimensions', END); graph = graph.compile()