from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoltState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: BoltState): state['valid'] = state['specs'].get('tensile_strength', 0) > 400; return state; graph = StateGraph(BoltState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()