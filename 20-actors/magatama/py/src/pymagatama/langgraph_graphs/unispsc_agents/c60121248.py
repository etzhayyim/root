from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EaselState(TypedDict): dimensions: dict; material_type: str; is_stable: bool; validation_logs: List[str]; def validate_dimensions(state: EaselState): state['validation_logs'].append('Checking easel footprint.'); return state; def check_stability(state: EaselState): state['is_stable'] = state['dimensions'].get('width', 0) > 0; return state; graph_builder = StateGraph(EaselState); graph_builder.add_node('validate', validate_dimensions); graph_builder.add_node('check', check_stability); graph_builder.set_entry_point('validate'); graph_builder.add_edge('validate', 'check'); graph_builder.add_edge('check', END); graph = graph_builder.compile();