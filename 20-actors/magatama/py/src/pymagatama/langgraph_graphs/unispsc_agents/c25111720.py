from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShipProcurementState(TypedDict): vessel_id: str; specs_verified: bool; export_licensed: bool; def validate_specs(state: ShipProcurementState): return {'specs_verified': True}; def check_export(state: ShipProcurementState): return {'export_licensed': True}; graph = StateGraph(ShipProcurementState); graph.add_node('validate_specs', validate_specs); graph.add_node('check_export', check_export); graph.add_edge('validate_specs', 'check_export'); graph.add_edge('check_export', END); graph.set_entry_point('validate_specs'); graph = graph.compile()