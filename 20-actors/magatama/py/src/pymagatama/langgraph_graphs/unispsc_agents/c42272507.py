from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AnesthesiaState(TypedDict): specs: dict; compliant: bool; workflow: List[str]; def validate_specs(state: AnesthesiaState): state['compliant'] = 'ISO 13485' in state['specs'].get('certs', []); state['workflow'].append('Validation completed'); return state; def route_approval(state: AnesthesiaState): return 'approved' if state['compliant'] else 'rejected'; graph = StateGraph(AnesthesiaState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END)