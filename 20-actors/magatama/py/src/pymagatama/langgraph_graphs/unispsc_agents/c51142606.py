from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): chemical_data: str; validation_result: str; compliance_check: bool; def validate_substance(state: GraphState): state['validation_result'] = 'Compliance Verified' if 'CAS_OK' in state['chemical_data'] else 'Rejected'; return state; def regulatory_check(state: GraphState): state['compliance_check'] = True; return state; graph = StateGraph(GraphState); graph.add_node('validate', validate_substance); graph.add_node('compliance', regulatory_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END);