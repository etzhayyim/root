from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_data: dict; compliance_status: bool; def validate_purity(state: PharmaState): state['compliance_status'] = state['chemical_data'].get('purity', 0) > 99.0; return state; def check_storage(state: PharmaState): state['compliance_status'] = state['chemical_data'].get('temp') == '2-8C'; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); app = graph.compile()