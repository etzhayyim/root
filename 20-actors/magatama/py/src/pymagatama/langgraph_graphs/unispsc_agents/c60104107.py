from langgraph.graph import StateGraph, END; from typing import TypedDict; class ModelState(TypedDict): model_ref: str; validation_passed: bool; def check_specs(state: ModelState): state['validation_passed'] = 'Scale/Ratio' in state.get('model_ref', ''); return state; graph = StateGraph(ModelState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()