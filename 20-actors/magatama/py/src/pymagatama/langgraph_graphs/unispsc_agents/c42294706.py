from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PerfusionState(TypedDict): specs: dict; validation_passed: bool; device_id: str; def validate_bubble_trap(state: PerfusionState): state['validation_passed'] = 'ISO_13485_certification' in state['specs'] and state['specs']['pressure_rating'] > 0; return state; graph = StateGraph(PerfusionState); graph.add_node('validate', validate_bubble_trap); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()