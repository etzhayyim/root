from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): status: str; specs: dict; validation_log: List[str]; def validate_purity(state: ProcureState): state['validation_log'].append('Validating chemical purity...'); return {'status': 'validated'}; def check_hazmat(state: ProcureState): state['validation_log'].append('Verifying storage requirements...'); return {'status': 'ready_to_ship'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_purity); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph.compile()