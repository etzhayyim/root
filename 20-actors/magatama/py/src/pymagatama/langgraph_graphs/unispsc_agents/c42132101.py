from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; valid: bool;  def validate_pads(state: State): state['valid'] = state['input_data'].get('absorbency') > 500; return state;  def check_safety(state: State): state['valid'] = state['valid'] and state['input_data'].get('sterile', False); return state;  graph = StateGraph(State); graph.add_node('validate', validate_pads); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()