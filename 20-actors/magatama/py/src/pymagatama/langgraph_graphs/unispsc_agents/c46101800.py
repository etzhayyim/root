from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): doc_id: str; compliance_cleared: bool; export_license: str; def validate_export(state): return {'compliance_cleared': True if state.get('export_license') else False}; def final_review(state): return 'approved' if state['compliance_cleared'] else 'rejected'; graph = StateGraph(State); graph.add_node('validate', validate_export); graph.add_node('review', final_review); graph.set_entry_point('validate'); graph.add_edge('validate', 'review'); graph.add_edge('review', END); graph.compile()