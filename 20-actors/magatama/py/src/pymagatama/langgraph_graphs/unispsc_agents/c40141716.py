from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): pipe_spec: dict; validation_errors: List[str]; approved: bool; def validate_p_trap(state: PipeState): spec = state['pipe_spec']; errors = []; if 'diameter' not in spec: errors.append('Missing diameter'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(PipeState); graph.add_node('validate', validate_p_trap); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()