from langgraph.graph import StateGraph, END; from typing import TypedDict; class LaundryState(TypedDict): chemical_composition: dict; safety_compliant: bool; def check_sds(state: LaundryState): return {'safety_compliant': True} if 'sds' in state['chemical_composition'] else {'safety_compliant': False}; graph = StateGraph(LaundryState); graph.add_node('check_sds', check_sds); graph.set_entry_point('check_sds'); graph.add_edge('check_sds', END); graph = graph.compile()