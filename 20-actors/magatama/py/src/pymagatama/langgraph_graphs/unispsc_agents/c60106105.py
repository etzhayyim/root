from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; budget_check: bool; def val_step(state: ProcState): state['validated'] = len(state['items']) > 0; return state; def bud_step(state: ProcState): state['budget_check'] = True; return state; graph = StateGraph(ProcState); graph.add_node('validate', val_step); graph.add_node('budget', bud_step); graph.add_edge('validate', 'budget'); graph.add_edge('budget', END); graph.set_entry_point('validate'); graph = graph.compile()