from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): site_specs: str; compliance: bool; approved: bool; def validate_specs(state: State): return {'compliance': 'dimensions' in state['site_specs']}; def finalize(state: State): return {'approved': state['compliance']}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('approval', finalize); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph.set_entry_point('validate'); graph = graph.compile()