from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): device_id: str; sterility_verified: bool; shelf_life_valid: bool; def check_sterility(state: ProcState) -> ProcState: state['sterility_verified'] = True; return state; def validate_shelf_life(state: ProcState) -> ProcState: state['shelf_life_valid'] = True; return state; graph = StateGraph(ProcState); graph.add_node('check_sterility', check_sterility); graph.add_node('validate_shelf_life', validate_shelf_life); graph.add_edge('check_sterility', 'validate_shelf_life'); graph.add_edge('validate_shelf_life', END); graph.set_entry_point('check_sterility'); graph = graph.compile()