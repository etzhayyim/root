from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): dimensions: dict; quality_report: str; approved: bool; def validate_dimensions(state: CastingState): return {'approved': state['dimensions'].get('tolerance', 0.01) < 0.05}; def check_quality(state: CastingState): return {'quality_report': 'Passed' if state['approved'] else 'Rework'}; graph = StateGraph(CastingState); graph.add_node('validate', validate_dimensions); graph.add_node('qc', check_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'qc'); graph.add_edge('qc', END); graph = graph.compile()