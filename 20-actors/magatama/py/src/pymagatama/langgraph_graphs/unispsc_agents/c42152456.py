from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): masking_agent_details: dict; compliance_status: List[str]; def validate_regulatory(state: AgentState): state['compliance_status'].append('FDA_Validated'); return state; def check_expiration(state: AgentState): state['compliance_status'].append('Storage_Checked'); return state; graph = StateGraph(AgentState); graph.add_node('validate', validate_regulatory); graph.add_node('expiry', check_expiration); graph.set_entry_point('validate'); graph.add_edge('validate', 'expiry'); graph.add_edge('expiry', END); app = graph.compile()