from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SawBladeState(TypedDict): dimensions: dict; material: str; p_approved: bool; graph = StateGraph(StateBladeState); def validate_specs(state: SawBladeState): return {'p_approved': state['material'] in ['carbide', 'bi-metal']}; def finalize_procurement(state: SawBladeState): return {'p_approved': True}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()