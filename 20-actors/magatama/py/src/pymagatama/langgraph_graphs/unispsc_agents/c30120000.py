from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class InfrastructureState(TypedDict): spec: dict; validation_results: List[str]; def validate_materials(state: InfrastructureState): return {'validation_results': ['Material check passed']}; def assign_logistics(state: InfrastructureState): return {'validation_results': state['validation_results'] + ['Logistics assigned']}; graph = StateGraph(InfrastructureState); graph.add_node('validate', validate_materials); graph.add_node('logistics', assign_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph = graph.compile()