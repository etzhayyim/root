from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[dict]; approval: bool; constraints: dict; def validate_specs(state: ProcurementState): return {'approval': all('food_grade' in item for item in state['items'])}; graph = StateGraph(ProcurementState); graph.add_node('cns_validation', validate_specs); graph.set_entry_point('cns_validation'); graph.add_edge('cns_validation', END); graph = graph.compile()