from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_name: str; safety_checks: List[str]; inspection_passed: bool; def validate_reagent(state: ReagentState): state['safety_checks'].append('SDS Verified'); state['inspection_passed'] = True; return state; graph = StateGraph(ReagentState); graph.add_node('validate', validate_reagent); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()