from langgraph.graph import StateGraph, END; from typing import TypedDict; class PostmortemNeedleState(TypedDict): needle_type: str; specs: dict; compliant: bool; def validate_specs(state: PostmortemNeedleState): state['compliant'] = 'ISO' in str(state['specs'].get('cert', '')); return state; def check_safety(state: PostmortemNeedleState): print(f'Verifying safety for {state["needle_type"]}'); return state; graph = StateGraph(PostmortemNeedleState); graph.add_node('validation', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validation'); graph.add_edge('validation', 'safety'); graph.add_edge('safety', END); graph = graph.compile()