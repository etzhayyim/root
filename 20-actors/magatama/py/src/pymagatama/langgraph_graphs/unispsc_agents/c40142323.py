from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): burst_rating: float; material: str; compliant: bool; graph = StateGraph(ProcState); def validate_specs(state: ProcState): state['compliant'] = state['burst_rating'] > 0 and state['material'] != 'unknown'; return state; def check_safety(state: ProcState): return 'compliant' if state['compliant'] else 'fail'; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()