from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CleaningKitState(TypedDict): kit_id: str; materials: List[str]; safety_verified: bool; def validate_materials(state: CleaningKitState): state['safety_verified'] = 'hazardous' not in state['materials']; return {'safety_verified': state['safety_verified']}; def check_workflow(state: CleaningKitState): return 'end_node' if state['safety_verified'] else 'flag_safety_team'; graph = StateGraph(CleaningKitState); graph.add_node('validate', validate_materials); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()