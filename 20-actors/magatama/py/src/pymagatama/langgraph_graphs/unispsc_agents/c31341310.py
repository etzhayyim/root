from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): vendor_docs: List[str]; inspection_results: dict; approved: bool; def validate_specs(state: AssemblyState): return {'approved': len(state['vendor_docs']) >= 3}; def verify_welds(state: AssemblyState): return {'inspection_results': {'nondestructive': 'pass'}}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('weld_check', verify_welds); graph.set_entry_point('validate'); graph.add_edge('validate', 'weld_check'); graph.add_edge('weld_check', END); graph = graph.compile()