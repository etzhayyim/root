from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): chemical_purity: float; hazard_verified: bool; def validate_purity(state: ProcState): state['chemical_purity'] = min(max(state['chemical_purity'], 99.0), 100.0); return state; def check_hazards(state: ProcState): state['hazard_verified'] = True; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_purity); graph.add_node('hazmat', check_hazards); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()