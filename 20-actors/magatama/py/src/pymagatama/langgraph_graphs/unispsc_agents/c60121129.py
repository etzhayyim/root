from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MasoniteState(TypedDict): dimensions: dict; density: float; status: str; def validate_specs(state: MasoniteState): is_valid = state['density'] > 800; return {'status': 'approved' if is_valid else 'rejected'}; graph = StateGraph(MasoniteState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()