from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PublicStructureState(TypedDict): structure_id: str; structural_specs: dict; compliance_verified: bool; def validate_specs(state: PublicStructureState): state['compliance_verified'] = 'structural_certification' in state['structural_specs']; return state; def check_safety(state: PublicStructureState): if not state['compliance_verified']: raise ValueError('Safety compliance missing'); return state; graph = StateGraph(PublicStructureState); graph.add_node('validate', validate_specs); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_safety'); graph.add_edge('check_safety', END); compiled_graph = graph.compile()