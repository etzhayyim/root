from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): brush_specs: dict; validation_results: List[str]; def validate_brush(state: ProcureState): return {'validation_results': ['ISO-13485 confirmed', 'Material safety checked']}; def assemble_po(state: ProcureState): return {'validation_results': state['validation_results'] + ['PO generated']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_brush); graph.add_node('assemble', assemble_po); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph.set_entry_point('validate'); graph = graph.compile()