from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalCaseState(TypedDict): case_specs: dict; validation_results: List[str]; def validate_materials(state: DentalCaseState): state['validation_results'].append('Material compliance verified') if state['case_specs'].get('autoclave_compatible') else state['validation_results'].append('Material validation failed'); return state; def check_dimensions(state: DentalCaseState): state['validation_results'].append('Dimensions within tolerance') if state['case_specs'].get('length') < 500 else None; return state; graph = StateGraph(DentalCaseState); graph.add_node('validate', validate_materials); graph.add_node('dimensions', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', 'dimensions'); graph.add_edge('dimensions', END); graph = graph.compile()