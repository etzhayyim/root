from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ConveyorState(TypedDict): load_capacity: float; safety_check: bool; graph = StateGraph(ConveyorState); def validate_spec(state: ConveyorState): return {'safety_check': state['load_capacity'] > 0}; def finalize_procurement(state: ConveyorState): return {'safety_check': True}; graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()