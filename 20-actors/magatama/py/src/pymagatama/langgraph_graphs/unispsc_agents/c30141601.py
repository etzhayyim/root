from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InsulationState(TypedDict): specs: dict; validation_results: List[str]; def validate_acoustics(state: InsulationState): nrc = state['specs'].get('nrc', 0); return {'validation_results': ['NRC > 0.8 passed' if nrc > 0.8 else 'NRC below threshold']}; graph = StateGraph(InsulationState); graph.add_node('acoustic_check', validate_acoustics); graph.set_entry_point('acoustic_check'); graph.add_edge('acoustic_check', END); compiled_graph = graph.compile()