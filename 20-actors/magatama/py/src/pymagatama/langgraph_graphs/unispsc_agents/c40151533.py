from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): pump_specs: dict; status: str; def validate_pressure(state: PumpState): return {'status': 'VALIDATED' if state['pump_specs'].get('pressure', 0) > 0 else 'INVALID'}; graph = StateGraph(PumpState); graph.add_node('validate', validate_pressure); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()