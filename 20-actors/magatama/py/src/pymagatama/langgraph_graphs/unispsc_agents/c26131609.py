from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: ProcurementState): state['validation_results'].append('Material grade validated.'); return state; def check_safety(state: ProcurementState): state['validation_results'].append('Safety compliance checked.'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate_materials', validate_materials); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate_materials'); graph.add_edge('validate_materials', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()