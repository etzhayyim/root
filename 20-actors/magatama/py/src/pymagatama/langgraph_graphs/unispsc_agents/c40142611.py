from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TubeState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: TubeState): specs = state['specs']; is_valid = all(k in specs for k in ['material', 'diameter', 'pressure']); return {'valid': is_valid, 'error': '' if is_valid else 'Missing mandatory attributes'}; workflow = StateGraph(TubeState); workflow.add_node('validate', validate_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()