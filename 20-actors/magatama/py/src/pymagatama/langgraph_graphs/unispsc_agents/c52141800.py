from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ApplianceState(TypedDict): appliance_type: str; specs: dict; compliant: bool; def validate_specs(state: ApplianceState): state['compliant'] = 'voltage' in state['specs'] and 'safety_mark' in state['specs']; return state; builder = StateGraph(ApplianceState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()