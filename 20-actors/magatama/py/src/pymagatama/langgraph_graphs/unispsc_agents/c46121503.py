from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MissileState(TypedDict): items: List[str]; compliance_checks: List[str]; approved: bool; def validate_security_clearance(state: MissileState): state['compliance_checks'].append('Security Clearance Verified'); return state; def run_export_verification(state: MissileState): state['compliance_checks'].append('Export Control Verified'); state['approved'] = True; return state; graph = StateGraph(MissileState); graph.add_node('security', validate_security_clearance); graph.add_node('export', run_export_verification); graph.set_entry_point('security'); graph.add_edge('security', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()