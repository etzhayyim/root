from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): material_id: str; quality_score: float; compliance_verified: bool; steps: List[str]; def validate_material(state: MaterialState): state['steps'].append('Validating material purity'); state['quality_score'] = 0.95; state['compliance_verified'] = True; return state; def prepare_shipping(state: MaterialState): state['steps'].append('Preparing high-value shipping documentation'); return state; graph = StateGraph(MaterialState); graph.add_node('validate', validate_material); graph.add_node('ship', prepare_shipping); graph.set_entry_point('validate'); graph.add_edge('validate', 'ship'); graph.add_edge('ship', END); graph = graph.compile()