from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): species: str; location: str; temp_log: List[float]; valid: bool; def validate_cold_chain(state: ProcureState): state['valid'] = all(t <= 4.0 for t in state['temp_log']); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_cold_chain); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()