from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicroState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_items(state: MicroState): return {'validated': all(item.startswith('REF-') for item in state['items'])}; def check_certs(state: MicroState): return {'certs': ['ISO13485', 'CE-IVD']}; graph = StateGraph(MicroState); graph.add_node('validate', validate_items); graph.add_node('certify', check_certs); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()