from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class CommodityState(TypedDict): code: str; status: str; validation_logs: List[str]; def validate_commodity(state: CommodityState) -> CommodityState: state['validation_logs'].append('Validating UNSPSC 13102030 standards'); state['status'] = 'VALIDATED'; return state; def route_procurement(state: CommodityState) -> str: return 'end'; graph = StateGraph(CommodityState); graph.add_node('validator', validate_commodity); graph.set_entry_point('validator'); graph.add_edge('validator', END); compiled_graph = graph.compile()