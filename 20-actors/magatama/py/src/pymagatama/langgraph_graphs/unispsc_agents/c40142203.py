from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class KitState(TypedDict): vendor_part_number: str; compatibility_verified: bool; compliance_docs: list; graph = StateGraph(KitState); def validate_compatibility(state: KitState): return {'compatibility_verified': True}; def check_compliance(state: KitState): return {'compliance_docs': ['ISO_Certificate']}; graph.add_node('validate', validate_compatibility); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()