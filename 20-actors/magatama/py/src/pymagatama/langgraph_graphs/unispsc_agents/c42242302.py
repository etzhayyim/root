from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OrthoState(TypedDict): device_id: str; compliance_docs: List[str]; approved: bool; def validate_cert(state: OrthoState): state['approved'] = 'ISO13485' in state['compliance_docs']; return state; builder = StateGraph(OrthoState); builder.add_node('validation', validate_cert); builder.set_entry_point('validation'); builder.add_edge('validation', END); graph = builder.compile()