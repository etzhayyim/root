from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): drug_name: str; batch_id: str; compliance_check: bool; storage_verified: bool; def check_compliance(state: DrugProcurementState): return {'compliance_check': True}; def verify_storage(state: DrugProcurementState): return {'storage_verified': True}; graph = StateGraph(DrugProcurementState); graph.add_node('compliance', check_compliance); graph.add_node('storage', verify_storage); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'storage'); graph.add_edge('storage', END); graph = graph.compile()