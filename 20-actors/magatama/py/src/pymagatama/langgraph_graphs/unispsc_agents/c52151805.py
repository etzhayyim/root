from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WokState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: WokState): errors = []; if not state['specs'].get('food_safe'): errors.append('Missing food safety certification'); return {'validation_errors': errors}; def finalize_procurement(state: WokState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(WokState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()