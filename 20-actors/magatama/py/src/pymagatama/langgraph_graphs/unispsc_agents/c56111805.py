from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): data: dict; validated: bool; def validate_specs(state: State): state['validated'] = 'dimensions' in state['data'] and 'material' in state['data']; return state; def finalize_order(state: State): return {'status': 'processed'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()