from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; class LaserProcState(TypedDict): specs: dict; valid: bool; export_check: bool; def validate_specs(state: LaserProcState): state['valid'] = all(k in state['specs'] for k in ['wavelength', 'power']); return state; def check_export(state: LaserProcState): state['export_check'] = state['specs'].get('power', 0) > 2000; return state; graph = StateGraph(LaserProcState); graph.add_node('validate', validate_specs); graph.add_node('export', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph = graph.compile()