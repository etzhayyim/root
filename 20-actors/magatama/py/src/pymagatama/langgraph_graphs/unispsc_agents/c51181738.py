from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_data: dict; approval_status: bool; def validate_purity(state: PharmState): purity = state['chemical_data'].get('purity', 0); state['approval_status'] = purity >= 99.0; return state; def check_storage(state: PharmState): return {'storage_safe': state['chemical_data'].get('temp') < 25}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); compiled_graph = graph.compile()