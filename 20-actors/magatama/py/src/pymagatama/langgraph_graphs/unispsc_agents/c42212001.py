from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DoorOpenerState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(DoorOpenerState); def validate_specs(state: DoorOpenerState): state['compliance_check'] = 'ISO' in state['specs'].get('standard', ''); return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()