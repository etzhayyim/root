from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): component_id: str; spec_data: dict; validation_passed: bool; def validate_specs(state: CompressorState): state['validation_passed'] = all(k in state['spec_data'] for k in ['pressure', 'material']); return state; def route_inspection(state: CompressorState): return 'valid' if state['validation_passed'] else 'invalid'; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()