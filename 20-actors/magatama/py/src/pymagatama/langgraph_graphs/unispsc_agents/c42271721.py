from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): model_number: str; efficiency: float; compliant: bool; def validate_filter(state: FilterState): state['compliant'] = state['efficiency'] > 0.99; return state; graph = StateGraph(FilterState); graph.add_node('validate', validate_filter); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()