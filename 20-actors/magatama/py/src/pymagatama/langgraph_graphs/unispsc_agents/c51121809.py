from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): commodity: str; spec_check: bool; compliance_status: str; def validate_gmp(state: ProcureState): state['spec_check'] = True; return {'spec_check': True}; def check_compliance(state: ProcureState): state['compliance_status'] = 'APPROVED'; return {'compliance_status': 'APPROVED'}; graph = StateGraph(ProcureState); graph.add_node('validate_gmp', validate_gmp); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', 'check_compliance'); graph.add_edge('check_compliance', END); graph = graph.compile()