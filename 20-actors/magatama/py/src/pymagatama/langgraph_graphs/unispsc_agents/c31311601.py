from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipeState(TypedDict): specs: dict; validation_results: List[str]; def validate_welding(state: PipeState): state['validation_results'].append('Checking sonic weld integrity index'); return state; def check_dimensions(state: PipeState): state['validation_results'].append('Verifying tolerance ranges'); return state; graph = StateGraph(PipeState); graph.add_node('validate_weld', validate_welding); graph.add_node('check_dims', check_dimensions); graph.set_entry_point('validate_weld'); graph.add_edge('validate_weld', 'check_dims'); graph.add_edge('check_dims', END); graph = graph.compile()