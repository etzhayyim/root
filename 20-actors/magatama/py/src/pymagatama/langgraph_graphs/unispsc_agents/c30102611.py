from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: ProcurementState): alloy = state['specs'].get('alloy'); return {'validation_results': ['Alloy verified'] if alloy else ['Alloy missing']}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()