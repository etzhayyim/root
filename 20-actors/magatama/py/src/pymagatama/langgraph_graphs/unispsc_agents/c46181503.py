from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoverallState(TypedDict): specs: dict; compliant: bool; approved: bool; def check_standards(state: CoverallState): state['compliant'] = 'iso_standard' in state['specs']; return state; def validate_procurement(state: CoverallState): state['approved'] = state.get('compliant', False); return state; graph = StateGraph(CoverallState); graph.add_node('check_standards', check_standards); graph.add_node('validate', validate_procurement); graph.set_entry_point('check_standards'); graph.add_edge('check_standards', 'validate'); graph.add_edge('validate', END); graph = graph.compile()