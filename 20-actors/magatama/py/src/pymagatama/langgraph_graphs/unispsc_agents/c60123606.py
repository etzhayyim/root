from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GlitterItem(TypedDict): name: str; specifications: dict; valid: bool; def validate_spec(state: GlitterItem): state['valid'] = 'Particle Size' in state['specifications'] and 'SDS' in state['specifications']; return state; builder = StateGraph(GlitterItem); builder.add_node('validate', validate_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()