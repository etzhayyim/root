from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabFilterState(TypedDict): filter_id: str; spec_data: dict; validation_report: str; def validate_specs(state: LabFilterState): pressure = state['spec_data'].get('pressure', 0); state['validation_report'] = 'PASS' if pressure < 5.0 else 'FAIL: Pressure exceeds limit'; return state; graph = StateGraph(LabFilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()