from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ThermometerState(TypedDict): probe_model: str; sterility_valid: bool; compliant_material: bool; def check_compatibility(state: ThermometerState): return {'compliant_material': state['probe_model'].startswith('T-')}; def validate_sterility(state: ThermometerState): return {'sterility_valid': True}; graph = StateGraph(ThermometerState); graph.add_node('compatibility_check', check_compatibility); graph.add_node('sterility_verification', validate_sterility); graph.set_entry_point('compatibility_check'); graph.add_edge('compatibility_check', 'sterility_verification'); graph.add_edge('sterility_verification', END); graph = graph.compile()