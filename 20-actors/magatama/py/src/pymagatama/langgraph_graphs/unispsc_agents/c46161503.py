from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MarineSignalState(TypedDict): signal_type: str; specs: dict; compliant: bool; def validate_specs(state: MarineSignalState): state['compliant'] = state['specs'].get('IMO_certified', False); return state; def check_risk(state: MarineSignalState): return 'compliant' if state['compliant'] else 'flag_for_review'; graph = StateGraph(MarineSignalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()