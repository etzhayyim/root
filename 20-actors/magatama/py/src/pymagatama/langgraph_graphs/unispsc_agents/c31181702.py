from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class GlandState(TypedDict): gland_type: str; specs: dict; validation_report: list[str]; def validate_specs(state: GlandState): report = []; if not state['specs'].get('pressure_rating'): report.append('Missing pressure rating'); return {'validation_report': report}; def finalize_order(state: GlandState): return {'validation_report': state['validation_report'] + ['Order ready for procurement']}; graph = StateGraph(GlandState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()