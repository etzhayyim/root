from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmergencyVehicleState(TypedDict): component_id: str; safety_specs: dict; approved: bool; def validate_specs(state: EmergencyVehicleState): state['approved'] = all(val is not None for val in state['safety_specs'].values()); return state; def route_approval(state: EmergencyVehicleState): return 'approved' if state['approved'] else END; graph = StateGraph(EmergencyVehicleState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()