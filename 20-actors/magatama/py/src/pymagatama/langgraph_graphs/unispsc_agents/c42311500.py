from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec: dict; def validate_sterile(state: State): state['status'] = 'validated' if state['spec'].get('is_sterile') else 'rejected'; return state; graph = StateGraph(State); graph.add_node('validation', validate_sterile); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()