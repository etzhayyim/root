from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotProcState(TypedDict): robot_id: str; specs: dict; validation_errors: List[str]; def validate_robot_specs(state: RobotProcState): errors = []; if state['specs'].get('payload', 0) <= 0: errors.append('Invalid payload'); return {'validation_errors': errors}; graph = StateGraph(RobotProcState); graph.add_node('validate', validate_robot_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()