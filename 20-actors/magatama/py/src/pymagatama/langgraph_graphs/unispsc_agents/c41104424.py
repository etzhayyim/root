from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IncubatorState(TypedDict): accessory_type: str; specs: dict; compliance_passed: bool; def validate_specs(state: IncubatorState): state['compliance_passed'] = 'ISO' in state['specs'].get('cert', '') and state['specs'].get('temp_range', 0) > 0; return state; graph = StateGraph(IncubatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()