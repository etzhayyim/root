from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PessaryState(TypedDict): device_id: str; compliance_checked: bool; inspection_passed: bool; def check_regulatory(state: PessaryState): return {'compliance_checked': True}; def perform_quality_inspection(state: PessaryState): return {'inspection_passed': True}; graph = StateGraph(PessaryState); graph.add_node('regulatory', check_regulatory); graph.add_node('inspection', perform_quality_inspection); graph.add_edge('regulatory', 'inspection'); graph.add_edge('inspection', END); graph.set_entry_point('regulatory'); graph = graph.compile()