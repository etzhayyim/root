from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): equipment_id: str; compliance_docs: List[str]; approved: bool; def validate_equipment(state: State): state['approved'] = all(['ethics' in doc for doc in state['compliance_docs']]); return state; graph = StateGraph(State); graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()