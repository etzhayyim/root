from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): cold_chain_verified: bool; gmp_certified: bool; batch_id: str; approval_status: str; def verify_storage(state: PharmState): state['cold_chain_verified'] = True; return state; def validate_compliance(state: PharmState): state['gmp_certified'] = True; return state; graph = StateGraph(PharmState); graph.add_node('storage', verify_storage); graph.add_node('compliance', validate_compliance); graph.add_edge('storage', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('storage'); graph = graph.compile()