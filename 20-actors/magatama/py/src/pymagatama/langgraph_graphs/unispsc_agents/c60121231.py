from langgraph.graph import StateGraph, END; from typing import TypedDict; class PaintToolState(TypedDict): specs: dict; valid: bool; def validate_palette_knife(state: PaintToolState): state['valid'] = 'material' in state['specs'] and 'blade_length' in state['specs']; return state; graph = StateGraph(PaintToolState); graph.add_node('validate', validate_palette_knife); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()