from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UrnSandbagState(TypedDict): unit_count: int; package_specs: List[str]; validation_status: str; def validate_specs(state: UrnSandbagState): return {'validation_status': 'verified' if len(state.get('package_specs', [])) > 0 else 'failed'}; def process_logistics(state: UrnSandbagState): return {'validation_status': 'dispatched'}; graph = StateGraph(UrnSandbagState); graph.add_node('validate', validate_specs); graph.add_node('logistics', process_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); compiled_graph = graph.compile()