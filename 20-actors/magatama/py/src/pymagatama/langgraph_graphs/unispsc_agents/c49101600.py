from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CollectibleState(TypedDict): item_id: str; appraisal_status: bool; validation_required: bool; graph = StateGraph(CollectibleState); def validate_provenance(state: CollectibleState): state['appraisal_status'] = True; print('Verifying provenance...'); return state; def verify_condition(state: CollectibleState): print('Checking condition report...'); return state; graph.add_node('validate', validate_provenance); graph.add_node('check_condition', verify_condition); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_condition'); graph.add_edge('check_condition', END); compiled_graph = graph.compile()