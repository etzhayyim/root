from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: ForgingState): state['validation_results'].append('Material OK'); return state; def check_dimensions(state: ForgingState): state['validation_results'].append('Dimensions Verified'); return state; graph = StateGraph(ForgingState); graph.add_node('material_check', validate_material); graph.add_node('dim_check', check_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dim_check'); graph.add_edge('dim_check', END); graph = graph.compile()