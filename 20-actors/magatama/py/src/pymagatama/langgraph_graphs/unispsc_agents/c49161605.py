from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): balls: List[dict]; status: str; def validate_specs(state: State): for ball in state['balls']: ball['valid'] = ball.get('rebound_height', 0) > 40; return {'status': 'validated'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()