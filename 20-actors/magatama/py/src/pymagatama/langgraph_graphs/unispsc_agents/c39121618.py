from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FuseState(TypedDict): wire_material: str; current_rating: float; compliance_certs: List[str]; approved: bool; def check_safety(state: FuseState): state['approved'] = state['current_rating'] > 0 and 'IEC' in state['compliance_certs']; return state; graph = StateGraph(FuseState); graph.add_node('safety_check', check_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()