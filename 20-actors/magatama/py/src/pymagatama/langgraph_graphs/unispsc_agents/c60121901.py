from langgraph.graph import StateGraph, END; from typing import TypedDict; class FabricState(TypedDict): fabric_type: str; specs: dict; approved: bool; def check_muslin_specs(state: FabricState): state['approved'] = state['specs'].get('thread_count', 0) > 40; return state; graph = StateGraph(FabricState); graph.add_node('validate', check_muslin_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()