from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): gateway_specs: dict; security_check: bool; validation_log: List[str]; def validate_gateway(state: NetworkState): state['validation_log'].append('Validating interface standards...'); state['security_check'] = True; return state; def check_export_compliance(state: NetworkState): state['validation_log'].append('Checking dual-use export control...'); return state; graph = StateGraph(NetworkState); graph.add_node('validate', validate_gateway); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()