from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DiodeConfig(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state): return {'validation_results': ['Voltage Check Passed', 'Thermal Rating Verified'], 'status': 'COMPLIANT'}; def check_compliance(state): return {'status': 'READY_FOR_PROCUREMENT'}; graph = StateGraph(DiodeConfig); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate')