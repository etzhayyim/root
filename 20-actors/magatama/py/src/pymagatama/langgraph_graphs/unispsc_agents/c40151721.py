from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): parts_list: List[str]; validated: bool; spec_check: bool; def validate_parts(state: PumpState): return { 'validated': all(p for p in state['parts_list']) }; def compliance_check(state: PumpState): return { 'spec_check': True }; graph = StateGraph(PumpState); graph.add_node('validate', validate_parts); graph.add_node('compliance', compliance_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()