from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BoxState(TypedDict): specs: dict; validation_report: list[str]; def validate_specs(state: BoxState): report = []; if not state['specs'].get('load_capacity'): report.append('Missing load capacity'); return {'validation_report': report}; def finalize_procurement(state: BoxState): return {'validation_report': state['validation_report'] + ['Finalized']}; graph = StateGraph(BoxState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()