from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StampingState(TypedDict): specs: dict; compliance: bool; validation_log: List[str]; def check_tolerances(state: StampingState): state['validation_log'].append('Validating stamping tolerances...'); state['compliance'] = True; return state; def verify_material(state: StampingState): state['validation_log'].append('Verifying base material certification...'); return state; graph = StateGraph(StampingState); graph.add_node('check_dims', check_tolerances); graph.add_node('check_mat', verify_material); graph.set_entry_point('check_dims'); graph.add_edge('check_dims', 'check_mat'); graph.add_edge('check_mat', END); graph = graph.compile()