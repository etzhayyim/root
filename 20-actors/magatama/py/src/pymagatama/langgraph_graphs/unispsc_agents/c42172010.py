from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_standards(state: KitState): state['validated'] = 'ISO13485' in state['certs']; return state; def check_expiration(state: KitState): return 'continue' if state.get('validated') else 'blocked'; graph = StateGraph(KitState); graph.add_node('validate', validate_medical_standards); graph.add_node('expiry_check', check_expiration); graph.set_entry_point('validate'); graph.add_edge('validate', 'expiry_check'); graph.add_edge('expiry_check', END); graph = graph.compile()