from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): alloy_compliance: bool; pressure_test: bool; material_cert: bool; def check_material(state: ProcureState): return {'alloy_compliance': True}; def validate_pressure(state: ProcureState): return {'pressure_test': True}; def finalize_assembly(state: ProcureState): return {'material_cert': True}; graph = StateGraph(ProcureState); graph.add_node('material_check', check_material); graph.add_node('pressure_test', validate_pressure); graph.add_node('final_cert', finalize_assembly); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'pressure_test'); graph.add_edge('pressure_test', 'final_cert'); graph.add_edge('final_cert', END); graph = graph.compile()