from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StairClimberState(TypedDict): vendor: str; specs: dict; compliance_check: bool; graph = StateGraph(StairClimberState); def validate_load_capacity(state: StairClimberState): state['compliance_check'] = state['specs'].get('capacity', 0) > 0; return state; def check_safety_certs(state: StairClimberState): state['compliance_check'] = 'ISO' in state['specs'].get('cert', ''); return state; graph.add_node('validate_specs', validate_load_capacity); graph.add_node('verify_certs', check_safety_certs); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', 'verify_certs'); graph.add_edge('verify_certs', END); app = graph.compile()