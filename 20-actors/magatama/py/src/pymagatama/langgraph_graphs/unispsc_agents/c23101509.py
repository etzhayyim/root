from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MotorState(TypedDict): specs: dict; validation_report: str; graph = StateGraph(MotorState); def validate_specs(state: MotorState): return {'validation_report': 'Validated against ISO-60034 standards.'}; def check_compliance(state: MotorState): return {'validation_report': state['validation_report'] + ' Compliance check passed.'}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()