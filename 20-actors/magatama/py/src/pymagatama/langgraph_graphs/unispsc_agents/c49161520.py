from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoftballState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: SoftballState): state['compliance'] = 'JSA' in state['specs'].get('cert', '') and state['specs'].get('weight') > 0; return state; def finalize_order(state: SoftballState): state['approved'] = state['compliance']; return state; graph = StateGraph(SoftballState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()