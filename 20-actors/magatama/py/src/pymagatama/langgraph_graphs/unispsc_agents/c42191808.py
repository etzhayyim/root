from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PatientBedState(TypedDict): specs: dict; compliance_check: bool; output: str; def validate_specs(state: PatientBedState): state['compliance_check'] = 'FDA' in state['specs'].get('cert', '') and state['specs'].get('load', 0) > 150; return state; def finalize_order(state: PatientBedState): state['output'] = 'Approved' if state['compliance_check'] else 'Rejected'; return state; graph = StateGraph(PatientBedState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); compile_graph = graph.compile()