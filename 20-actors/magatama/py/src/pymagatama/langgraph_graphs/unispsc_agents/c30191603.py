from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScaffoldingState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_load(state: ScaffoldingState): load = state['specs'].get('load_capacity', 0); state['approved'] = load >= 500; return state; def check_compliance(state: ScaffoldingState): if 'ISO-12811' not in state['specs'].get('certs', []): state['validation_errors'].append('Missing ISO-12811'); return state; graph = StateGraph(ScaffoldingState); graph.add_node('validate_load', validate_load); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_load'); graph.add_edge('validate_load', 'check_compliance'); graph.add_edge('check_compliance', END); graph = graph.compile()