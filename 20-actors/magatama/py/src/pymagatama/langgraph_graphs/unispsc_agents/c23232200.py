from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: RobotState): state['compliant'] = state['specs'].get('load', 0) > 0; return state; graph = StateGraph(RobotState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()