from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoardState(TypedDict): items: List[str]; validated: bool; def check_components(state: BoardState): return {'validated': len(state['items']) > 0}; graph = StateGraph(BoardState); graph.add_node('validate', check_components); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()