from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RodState(TypedDict): rod_id: str; specs: dict; validation_score: float; graph = StateGraph(RodState); def validate_specs(state: RodState): state['validation_score'] = 0.95 if 'Material Grade' in state['specs'] else 0.0; return state; def check_export(state: RodState): return 'approved' if state['validation_score'] > 0.9 else 'rejected'; graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()