from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpatterState(TypedDict): chemical_report: str; safety_standard: str; approval_status: bool; def validate_safety(state: SpatterState): state['approval_status'] = 'SDS' in state['chemical_report'] and 'non-toxic' in state['safety_standard']; return state; def finalize_order(state: SpatterState): return {'approval_status': state['approval_status']}; graph = StateGraph(SpatterState); graph.add_node('validate', validate_safety); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()