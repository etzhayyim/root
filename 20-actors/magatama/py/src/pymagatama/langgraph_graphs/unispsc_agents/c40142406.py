from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OrificeState(TypedDict): specs: dict; compliance: bool; log: List[str]; def validate_specs(state: OrificeState): state['log'].append('Validating ASME B16.36 compliance...'); state['compliance'] = 'bore_dimension' in state['specs']; return state; def check_export(state: OrificeState): state['log'].append('Checking export controls...'); return state; graph = StateGraph(OrificeState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()