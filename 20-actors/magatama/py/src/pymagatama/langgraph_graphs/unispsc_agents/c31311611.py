from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeAssemblyState(TypedDict): status: str; specs: dict; compliance: bool; def validate_weld(state: PipeAssemblyState): state['compliance'] = state['specs'].get('ultrasonic_pass', False); return state; def route_approval(state: PipeAssemblyState): return 'approved' if state['compliance'] else 'rejected'; graph = StateGraph(PipeAssemblyState); graph.add_node('validate', validate_weld); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()