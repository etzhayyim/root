from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WallplateState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(WallplateState); def validate_specs(state: WallplateState): report = []; required = ['material_composition', 'flammability_rating_ul94']; for field in required: if field not in state['specs']: report.append(f'Missing {field}'); return {'validation_report': report}; def finalize_order(state: WallplateState): return {'validation_report': state['validation_report'] + ['Order ready for procurement approval']}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()