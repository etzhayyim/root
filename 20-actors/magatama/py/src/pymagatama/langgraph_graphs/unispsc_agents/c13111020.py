from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class MetalState(TypedDict): commodity_code: str; purity: float; security_cleared: bool; logs: List[str]; def validate_purity(state: MetalState): if state['purity'] < 0.999: state['logs'].append('Low purity') return {'security_cleared': False} return {'security_cleared': True} def verify_origin(state: MetalState): state['logs'].append('Checking provenance') return {'security_cleared': state['security_cleared']} graph = StateGraph(MetalState); graph.add_node('validate', validate_purity); graph.add_node('verify', verify_origin); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); graph.set_entry_point('validate'); graph = graph.compile()