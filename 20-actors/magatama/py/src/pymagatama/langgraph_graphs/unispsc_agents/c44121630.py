from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StaplerState(TypedDict): items: List[str]; validated: bool; def validate_stapler(state: StaplerState): return {'validated': all('stapler' in i.lower() for i in state['items'])}; graph = StateGraph(StaplerState); graph.add_node('validation', validate_stapler); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph.compile()