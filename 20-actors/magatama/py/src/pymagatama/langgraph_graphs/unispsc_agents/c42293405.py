from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): instrument_data: dict; validation_logs: Annotated[list, operator.add]; def validate_instrument(state: SurgicalState): return {'validation_logs': ['Checking material compliance', 'Verifying ISO 13485 certification']}; def finalize_procurement(state: SurgicalState): return {'validation_logs': ['Approval granted for clinical use']}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_instrument); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()