from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class RobotControlState(TypedDict): controller_id: str; specs: dict; validation_score: float; graph = StateGraph(RobotControlState); def validate_controller(state: RobotControlState): return {'validation_score': 1.0}; def check_export_compliance(state: RobotControlState): return {'validation_score': 0.9}; graph.add_node('validate', validate_controller); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()