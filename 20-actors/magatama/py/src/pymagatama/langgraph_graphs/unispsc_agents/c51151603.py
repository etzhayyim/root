from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): data: dict; checks: Annotated[list, operator.add]; def validate_purity(state: PharmState): return {'checks': ['Purity Verified'] if state['data'].get('purity', 0) > 99 else ['Purity Rejected']}; def verify_compliance(state: PharmState): return {'checks': ['Compliance Passed']}; graph = StateGraph(PharmState); graph.add_node('purity', validate_purity); graph.add_node('compliance', verify_compliance); graph.set_entry_point('purity'); graph.add_edge('purity', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()