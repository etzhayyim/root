from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TangeloState(TypedDict): fruit_batch_id: str; freshness_score: float; is_compliant: bool; def validate_freshness(state: TangeloState): state['is_compliant'] = state['freshness_score'] > 0.8; return state; def route_storage(state: TangeloState): return 'storage_logic' if state['is_compliant'] else 'reject'; graph = StateGraph(TangeloState); graph.add_node('validate', validate_freshness); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()