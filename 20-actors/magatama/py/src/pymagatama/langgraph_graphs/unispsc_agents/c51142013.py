from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): vendor_id: str; quality_docs: List[str]; compliant: bool; def check_quality(state: ProcurementState): state['compliant'] = all(d in state['quality_docs'] for d in ['gmp_cert', 'coa']); return state; graph = StateGraph(ProcurementState); graph.add_node('validate_docs', check_quality); graph.set_entry_point('validate_docs'); graph.add_edge('validate_docs', END); graph = graph.compile()