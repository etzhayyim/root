from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OrderState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_assembly(state: OrderState): specs = state['specs']; is_valid = all(k in specs for k in ['grade', 'shear_strength']); return {'valid': is_valid, 'log': ['Validation complete'] if is_valid else ['Missing specs']}; graph = StateGraph(OrderState); graph.add_node('validate', validate_assembly); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()