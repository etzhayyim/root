from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LampState(TypedDict): input_spec: dict; validation_report: list; compliant: bool; def validate_lamp_specs(state: LampState): wattage = state['input_spec'].get('wattage', 0); state['validation_report'] = ['Passed' if wattage > 500 else 'Failed: Insufficient wattage']; return {'compliant': wattage > 500}; graph = StateGraph(LampState); graph.add_node('validator', validate_lamp_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()