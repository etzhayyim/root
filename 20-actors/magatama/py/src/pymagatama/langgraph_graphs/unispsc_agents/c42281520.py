from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SterilizationState(TypedDict): test_pack_id: str; compliance_docs: List[str]; validation_status: bool; def validate_compliance(state: SterilizationState): state['validation_status'] = len(state['compliance_docs']) > 0; return state; graph = StateGraph(SterilizationState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()