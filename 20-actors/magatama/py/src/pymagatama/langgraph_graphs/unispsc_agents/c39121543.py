from langgraph.graph import StateGraph, END; from typing import TypedDict; class RelayState(TypedDict): relay_specs: dict; validation_report: str; def validate_specs(state: RelayState): specs = state['relay_specs']; report = 'PASS' if specs.get('coil_voltage') else 'FAIL: Missing Voltage'; return {'validation_report': report}; graph = StateGraph(RelayState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()