from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MetalProcurementState(TypedDict): material_specs: dict; inspection_status: str; quality_score: float; def validate_composition(state: MetalProcurementState): state['inspection_status'] = 'validation_complete'; return state; def check_compliance(state: MetalProcurementState): state['quality_score'] = 1.0; return state; graph = StateGraph(MetalProcurementState); graph.add_node('validate', validate_composition); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()