from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medicine: str; status: str; certification: bool; def validate_gmp(state): return {'status': 'validated' if state['certification'] else 'rejected'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()