from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AluminumState(TypedDict): specs: dict; valid: bool; def validate_specs(state: AluminumState): alloy = state['specs'].get('alloy'); state['valid'] = alloy in ['6061', '6063']; return state; builder = StateGraph(AluminumState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()