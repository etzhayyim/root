from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalPartState(TypedDict): part_id: str; specifications: dict; validation_passed: bool; def check_compliance(state: DentalPartState): state['validation_passed'] = 'ISO 13485' in state['specifications'].get('certs', []); return state; def route_approval(state: DentalPartState): return 'approve' if state['validation_passed'] else 'reject'; graph = StateGraph(DentalPartState); graph.add_node('validate', check_compliance); graph.add_edge('validate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()