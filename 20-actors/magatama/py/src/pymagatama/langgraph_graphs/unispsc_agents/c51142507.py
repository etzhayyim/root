from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_name: str; gmp_certified: bool; temp_logs: List[float]; approved: bool; def validate_gmp(state: ProcureState): return {'approved': state['gmp_certified'] and min(state['temp_logs']) > 2 and max(state['temp_logs']) < 8}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()