from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BookProcurementState(TypedDict): book_list: List[str]; validation_errors: List[str]; approved_books: List[str]; def validate_book_info(state: BookProcurementState): validated = [b for b in state['book_list'] if 'ISBN' in b]; return {'approved_books': validated}; def check_content_policy(state: BookProcurementState): return {'validation_errors': []}; graph = StateGraph(BookProcurementState); graph.add_node('validate', validate_book_info); graph.add_node('policy_check', check_content_policy); graph.set_entry_point('validate'); graph.add_edge('validate', 'policy_check'); graph.add_edge('policy_check', END); graph = graph.compile()