from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class State(TypedDict): messages: Annotated[list, add_messages]; status: str; def validate_spec(state: State): return {'status': 'validated_medical_grade'} if 'cert' in state['messages'][-1] else {'status': 'rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()