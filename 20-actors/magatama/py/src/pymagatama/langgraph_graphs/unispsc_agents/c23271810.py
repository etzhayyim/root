from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeldingState(TypedDict): electrode_type: str; specs: dict; compliant: bool; def validate_specs(state: WeldingState): state['compliant'] = all(k in state['specs'] for k in ['tensile_strength', 'material']); return state; graph = StateGraph(WeldingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()