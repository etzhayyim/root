from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RecorderState(TypedDict): input_data: dict; validation_results: List[str]; is_valid: bool; def validate_specs(state: RecorderState): specs = state['input_data'].get('specs', {}); errors = []; if not specs.get('calibration_cert'): errors.append('Missing Calibration Cert'); state['validation_results'] = errors; state['is_valid'] = len(errors) == 0; return state; graph = StateGraph(RecorderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()