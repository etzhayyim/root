from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): vendor_id: str; specs: dict; verified: bool; def validate_specs(state: RobotState): state['verified'] = 'payload_capacity_kg' in state['specs']; return state; builder = StateGraph(RobotState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()