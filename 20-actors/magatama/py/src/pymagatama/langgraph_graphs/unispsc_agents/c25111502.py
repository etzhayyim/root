from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VesselState(TypedDict): vessel_specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_specs(state: VesselState): validated = 'hull_material' in state['vessel_specs']; return {'compliance_status': validated, 'workflow_log': ['Specs validated']}; def check_maritime_regulations(state: VesselState): return {'workflow_log': state['workflow_log'] + ['Regulatory check passed']}; graph = StateGraph(VesselState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_maritime_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()