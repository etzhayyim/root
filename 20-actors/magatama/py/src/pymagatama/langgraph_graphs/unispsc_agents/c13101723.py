from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AbrasiveState(TypedDict): input_material: str; process_steps: Annotated[list[str], operator.add]; validation_results: dict; def validate_grade(state: AbrasiveState): state['validation_results'] = {'passed': True, 'log': 'Hardness within specification'}; return state; def run_processing(state: AbrasiveState): state['process_steps'].append('Micronization'); state['process_steps'].append('Purity Grading'); return state; graph = StateGraph(AbrasiveState); graph.add_node('validate', validate_grade); graph.add_node('process', run_processing); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()