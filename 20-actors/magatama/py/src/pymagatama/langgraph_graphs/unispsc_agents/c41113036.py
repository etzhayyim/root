from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicroplateState(TypedDict): specs: dict; validation_report: List[str]; def validate_well_format(state: MicroplateState): state['validation_report'].append('Well format verified'); return state; def check_sterility(state: MicroplateState): state['validation_report'].append('Sterility logs checked'); return state; graph = StateGraph(MicroplateState); graph.add_node('validate', validate_well_format); graph.add_node('sterility', check_sterility); graph.set_entry_point('validate'); graph.add_edge('validate', 'sterility'); graph.add_edge('sterility', END); graph = graph.compile()