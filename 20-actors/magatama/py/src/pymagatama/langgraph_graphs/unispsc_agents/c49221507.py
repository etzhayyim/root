from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): procurement_item: str; specs: dict; verified: bool; def validate_specs(state: State): specs = state['specs']; is_valid = all(key in specs for key in ['material', 'dimensions']); return {'verified': is_valid}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()