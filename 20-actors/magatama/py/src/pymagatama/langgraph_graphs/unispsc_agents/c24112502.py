from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PackagingState(TypedDict): dimensions: dict; board_grade: str; status: str; def validate_specs(state: PackagingState): return {'status': 'validated'} if state['board_grade'] else {'status': 'rejected'}; def process_shipment(state: PackagingState): return {'status': 'ready_for_production'}; graph = StateGraph(PackagingState); graph.add_node('validate', validate_specs); graph.add_node('process', process_shipment); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()