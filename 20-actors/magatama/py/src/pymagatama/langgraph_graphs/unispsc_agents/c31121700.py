from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachiningState(TypedDict): specs: dict; validation_results: List[str]; validated: bool; def validate_specs(state: MachiningState): errors = []; if state['specs'].get('tolerance', 0) > 0.05: errors.append('Tolerance exceeds precision limit'); return {'validation_results': errors, 'validated': len(errors) == 0}; def check_material(state: MachiningState): return {'validation_results': ['Material cert verified']}; graph = StateGraph(MachiningState); graph.add_node('validate', validate_specs); graph.add_node('material', check_material); graph.add_edge('validate', 'material'); graph.add_edge('material', END); graph.set_entry_point('validate'); graph = graph.compile()