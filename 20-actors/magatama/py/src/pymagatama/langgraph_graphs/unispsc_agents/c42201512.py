from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CTTableState(TypedDict): specs: dict; validation_report: List[str]; approved: bool; def validate_specs(state: CTTableState): report = []; if state['specs'].get('max_weight', 0) < 200: report.append('Low weight capacity'); state['validation_report'] = report; state['approved'] = len(report) == 0; return state; graph = StateGraph(CTTableState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()