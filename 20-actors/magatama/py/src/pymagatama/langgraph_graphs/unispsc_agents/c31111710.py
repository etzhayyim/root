from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PlasticPartState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_plastic_specs(state: PlasticPartState): state['valid'] = 'resin_grade' in state['specs'] and 'tolerance' in state['specs']; state['logs'].append('Validation complete'); return state; graph = StateGraph(PlasticPartState); graph.add_node('validate', validate_plastic_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()