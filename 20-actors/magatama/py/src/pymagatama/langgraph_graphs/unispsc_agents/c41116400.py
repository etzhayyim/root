from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VibrationState(TypedDict): sensor_specs: dict; validation_results: List[str]; def validate_calibration(state: VibrationState): state['validation_results'].append('Calibration certificate verified'); return state; def check_export_compliance(state: VibrationState): state['validation_results'].append('Export control check completed'); return state; graph = StateGraph(VibrationState); graph.add_node('validate_cal', validate_calibration); graph.add_node('export_check', check_export_compliance); graph.set_entry_point('validate_cal'); graph.add_edge('validate_cal', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()