from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoinState(TypedDict): coin_data: dict; validation_errors: List[str]; def validate_grading(state: CoinState): if 'grade' not in state['coin_data']: state['validation_errors'].append('Missing grade'); return state; def verify_authenticity(state: CoinState): if state['coin_data'].get('cert_id') is None: state['validation_errors'].append('Missing certification ID'); return state; graph = StateGraph(CoinState); graph.add_node('validate_grade', validate_grading); graph.add_node('verify_cert', verify_authenticity); graph.set_entry_point('validate_grade'); graph.add_edge('validate_grade', 'verify_cert'); graph.add_edge('verify_cert', END); compile_graph = graph.compile()