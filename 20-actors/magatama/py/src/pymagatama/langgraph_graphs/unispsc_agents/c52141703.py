from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HairDryerState(TypedDict): specs: dict; compliance_status: bool; graph = StateGraph(HairDryerState); def validate_specs(state: HairDryerState): return {'compliance_status': state['specs'].get('wattage', 0) < 2000}; def check_safety(state: HairDryerState): return {'compliance_status': state['specs'].get('certification') == 'PSE'}; graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()