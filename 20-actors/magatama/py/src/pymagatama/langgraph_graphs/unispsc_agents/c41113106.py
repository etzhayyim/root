from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): analyzer_type: str; specs: dict; ready_for_export: bool; def validate_specs(state: AnalysisState): return {'ready_for_export': all(val is not None for val in state['specs'].values())}; def export_check(state: AnalysisState): return 'end' if state['ready_for_export'] else 'end'; graph = StateGraph(AnalysisState); graph.add_node('validate', validate_specs); graph.add_node('export', export_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); app = graph.compile()