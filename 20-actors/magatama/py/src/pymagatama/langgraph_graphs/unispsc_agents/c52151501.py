from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input: str; compliance_score: int; passed: bool; def validate_safety(state: State): return {'passed': True if state.get('compliance_score', 0) > 80 else False}; graph = StateGraph(State); graph.add_node('safety_check', validate_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()