from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OphthalmicState(TypedDict): spec: dict; compliance: bool; approved: bool; def validate_spec(state: OphthalmicState): state['compliance'] = 'ISO 13485' in state['spec'].get('certs', []) and state['spec'].get('weight_mg', 0) > 0; return state; def verify_approval(state: OphthalmicState): state['approved'] = state['compliance']; return state; graph = StateGraph(OphthalmicState); graph.add_node('validate', validate_spec); graph.add_node('approve', verify_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()