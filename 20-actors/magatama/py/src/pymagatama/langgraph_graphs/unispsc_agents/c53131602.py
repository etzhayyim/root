from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HairCareState(TypedDict): items: List[str]; qc_passed: bool; compliance_docs: List[str]; def validate_ingredients(state: HairCareState): state['qc_passed'] = all(['banned' not in item for item in state['items']]); return state; def check_docs(state: HairCareState): return {'qc_passed': len(state['compliance_docs']) > 2}; graph = StateGraph(HairCareState); graph.add_node('validate', validate_ingredients); graph.add_node('verify_docs', check_docs); graph.set_entry_point('validate'); graph.add_edge('validate', 'verify_docs'); graph.add_edge('verify_docs', END); compiled_graph = graph.compile()