from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): data: dict; status: str; def validate_specs(state: GraphState): return {'status': 'validated' if 'certification_standard' in state['data'] else 'rejected'}; def check_compliance(state: GraphState): print('Checking spark arrester compliance...'); return {'status': 'compliant'}; graph = StateGraph(GraphState); graph.add_node('validation', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validation'); graph.add_edge('validation', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()