from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlateState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(PlateState); def validate_specs(state: PlateState): return {'validation_results': ['Material check passed', 'Sterility verified']}; def finalize_order(state: PlateState): return {'validation_results': state['validation_results'] + ['Order ready for procurement']}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()