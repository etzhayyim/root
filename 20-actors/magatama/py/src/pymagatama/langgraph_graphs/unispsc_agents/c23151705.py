from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserState(TypedDict): specs: dict; valid: bool; log: List[str]; def validation_step(state: LaserState): state['valid'] = state['specs'].get('laser_safety_class') == 'Class 4'; return state; def assembly_step(state: LaserState): state['log'].append('Configuring laser optics'); return state; graph = StateGraph(LaserState); graph.add_node('validate', validation_step); graph.add_node('assemble', assembly_step); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph = graph.compile()