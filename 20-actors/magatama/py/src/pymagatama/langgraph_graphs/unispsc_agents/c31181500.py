from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GasketState(TypedDict): specs: dict; validation_results: List[str]; def validate_material_specs(state: GasketState): return {'validation_results': ['Material check passed']}; def perform_dimensional_analysis(state: GasketState): return {'validation_results': ['CAD dimension verification complete']}; graph = StateGraph(GasketState); graph.add_node('material_check', validate_material_specs); graph.add_node('dimension_check', perform_dimensional_analysis); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dimension_check'); graph.add_edge('dimension_check', END); graph = graph.compile()