from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): metal_code: str; purity_level: float; status: str; def validate_purity(state: CatalystState): if state['purity_level'] < 0.99: return {'status': 'rejected_low_purity'}; return {'status': 'validated'}; def finalize_batch(state: CatalystState): return {'status': 'ready_for_shipment'}; graph = StateGraph(CatalystState); graph.add_node('validate', validate_purity); graph.add_node('finalize', finalize_batch); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()