from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpaceSupportState(TypedDict): equipment_specs: dict; security_clearance: bool; compliance_status: str; def validate_specs(state: SpaceSupportState): return {'compliance_status': 'verified' if state['security_clearance'] else 'pending_review'}; def export_control_check(state: SpaceSupportState): return {'compliance_status': 'restricted' if 'dual-use' in state['equipment_specs'] else 'approved'}; graph = StateGraph(SpaceSupportState); graph.add_node('validate', validate_specs); graph.add_node('export_check', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()