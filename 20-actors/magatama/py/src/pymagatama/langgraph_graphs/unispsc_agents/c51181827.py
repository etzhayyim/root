from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; standard_check: bool; safety_clearance: bool; graph = StateGraph(PharmaState); def validate_purity(state: PharmaState): return {'standard_check': True} if state.get('chemical_name') == 'Medroxyprogesterone acetate' else {'standard_check': False}; def check_safety(state: PharmaState): return {'safety_clearance': True}; graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()