from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MagnesiumState(TypedDict): alloy_type: str; specs: dict; approved: bool; def validate_alloy(state: MagnesiumState): state['approved'] = state['alloy_type'] in ['AZ31B', 'ZK60A']; return state; def check_specs(state: MagnesiumState): return state; graph = StateGraph(MagnesiumState); graph.add_node('validate', validate_alloy); graph.add_node('check', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); compiled_graph = graph.compile()