from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): sample_type: str; purity_required: float; reagent_status: str; def validate_spec(state: KitState): print('Validating DNA kit specs...'); return {'reagent_status': 'verified'} if state['purity_required'] >= 1.8 else {'reagent_status': 'failed'}; def inspect_cold_chain(state: KitState): print('Checking temperature logs...'); return {'reagent_status': 'compliant'}; graph = StateGraph(KitState); graph.add_node('validate', validate_spec); graph.add_node('inspect', inspect_cold_chain); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); compile_graph = graph.compile()