from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): specs: dict; valid: bool; def val_func(state: ProcState): state['valid'] = 'Interface' in state['specs'] and 'Material' in state['specs']; return state; graph = StateGraph(ProcState); graph.add_node('validate', val_func); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()