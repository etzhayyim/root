from langgraph.graph import StateGraph, END; from typing import TypedDict; class AssemblyState(TypedDict): ncr_status: bool; compliance: bool; def validate_specs(state: AssemblyState): return {'compliance': True} if 'AMS' in 'Waspalloy Specs' else {'compliance': False}; def check_ncr(state: AssemblyState): return {'ncr_status': True}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('ncr_check', check_ncr); graph.set_entry_point('validate'); graph.add_edge('validate', 'ncr_check'); graph.add_edge('ncr_check', END); graph = graph.compile()