from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EyeglassState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: EyeglassState): state['valid'] = 'lens_prescription' in state['specs'] and 'frame_material' in state['specs']; return state; drawer = StateGraph(EyeglassState); drawer.add_node('validate', validate_specs); drawer.set_entry_point('validate'); drawer.add_edge('validate', END); graph = drawer.compile()