from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CartCleanerState(TypedDict): vendor_docs: List[str]; validation_result: bool; compliance_status: str; def validate_specs(state: CartCleanerState): state['validation_result'] = len(state['vendor_docs']) > 0; return state; def check_compliance(state: CartCleanerState): state['compliance_status'] = 'COMPLIANT' if state['validation_result'] else 'NON-COMPLIANT'; return state; graph = StateGraph(CartCleanerState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()