from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; def validate_tools(state: ProcureState): state['validated'] = all(len(i) > 0 for i in state['items']); return state; def route_step(state: ProcureState): return 'validate' if not state['validated'] else END; graph = StateGraph(ProcureState); graph.add_node('validate', validate_tools); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()