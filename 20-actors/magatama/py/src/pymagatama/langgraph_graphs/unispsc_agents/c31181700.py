from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PackingState(TypedDict): input_specs: dict; validation_report: list; status: str; def validate_specs(state: PackingState): specs = state['input_specs']; reports = []; if specs.get('pressure') > 50: reports.append('High pressure rating requires safety audit'); return {'validation_report': reports, 'status': 'PENDING'}; def finalize_approval(state: PackingState): return {'status': 'APPROVED'}; graph = StateGraph(PackingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_approval); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()