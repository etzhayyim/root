from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): pipe_id: str; specs: dict; compliance: bool; def validate_uv_specs(state: PipeState) -> dict: return {'compliance': state['specs'].get('pressure', 0) > 10}; def finalize_assembly(state: PipeState) -> dict: return {'compliance': True}; graph = StateGraph(PipeState); graph.add_node('validate', validate_uv_specs); graph.add_node('finalize', finalize_assembly); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()