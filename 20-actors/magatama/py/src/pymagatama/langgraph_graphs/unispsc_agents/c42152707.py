from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OrthodonticState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: OrthodonticState): specs = state['specs']; required = ['material', 'slot_size', 'reg_id']; state['valid'] = all(k in specs for k in required); return state; graph = StateGraph(OrthodonticState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()