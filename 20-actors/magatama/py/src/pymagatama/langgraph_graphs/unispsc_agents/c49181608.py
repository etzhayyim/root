from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ArcheryState(TypedDict): specs: dict; valid: bool;  def validate_specs(state: ArcheryState): state['valid'] = 'length' in state['specs'] and 'material' in state['specs']; return state; builder = StateGraph(ArcheryState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()