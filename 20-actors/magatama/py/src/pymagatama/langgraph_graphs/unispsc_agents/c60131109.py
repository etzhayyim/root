from langgraph.graph import StateGraph, END; from typing import TypedDict; class MellophoneState(TypedDict): specs: dict; valid: bool; def validate_instrument(state: MellophoneState): state['valid'] = 'material' in state['specs'] and 'pitch' in state['specs']; return state; graph = StateGraph(MellophoneState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()