from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DentalState(TypedDict): model_data: dict; validation_results: List[str]; graph = StateGraph(DentalState); def validate_model(state: DentalState): return {'validation_results': ['CAD check', 'Material test']}; graph.add_node('validate', validate_model); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()