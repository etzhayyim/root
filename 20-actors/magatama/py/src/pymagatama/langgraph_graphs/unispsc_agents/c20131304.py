from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(BearingState); def validate_specs(state: BearingState): report = []; missing = [f for f in ['ISO_standard', 'material_composition'] if f not in state['specs']]; report.append('PASSED' if not missing else f'FAILED: missing {missing}'); return {'validation_report': report}; graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()