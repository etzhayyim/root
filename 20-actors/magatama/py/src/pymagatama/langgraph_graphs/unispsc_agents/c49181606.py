from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TrapState(TypedDict): items: List[str]; safety_approved: bool; def validate_equipment(state: TrapState): return {'safety_approved': True} if 'clay_thrower' in state['items'] else {'safety_approved': False}; def assemble_order(state: TrapState): return state; graph = StateGraph(TrapState); graph.add_node('validate', validate_equipment); graph.add_node('assemble', assemble_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); compile_graph = graph.compile()