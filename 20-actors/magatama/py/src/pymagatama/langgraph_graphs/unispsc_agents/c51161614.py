from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): data: dict; status: str; validation_results: List[str]; def validate_purity(state: PharmaState): state['validation_results'].append('Purity check completed'); return {'status': 'validated'}; def check_regulatory_compliance(state: PharmaState): state['validation_results'].append('Regulatory check passed'); return {'status': 'compliant'}; graph = StateGraph(PharmaState); graph.add_node('validate_purity', validate_purity); graph.add_node('compliance', check_regulatory_compliance); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()