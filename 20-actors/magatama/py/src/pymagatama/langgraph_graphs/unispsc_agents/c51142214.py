from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_name: str; validation_passed: bool; compliance_docs: List[str]; def validate_chemical(state: ProcState): state['validation_passed'] = 'narcotic_control_license' in state['compliance_docs']; return state; def route_procurement(state: ProcState): return 'end' if state['validation_passed'] else 'end'; graph = StateGraph(ProcState); graph.add_node('validate', validate_chemical); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()