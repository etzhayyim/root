from langgraph.graph import StateGraph, END; from typing import TypedDict; class PiezoState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: PiezoState): state['validation_passed'] = 'Resonant Frequency' in state['specs'] and state['specs']['Resonant Frequency'] > 0; return state; graph = StateGraph(PiezoState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()