from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SackHolderState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SackHolderState): errors = []; if state['specs'].get('load_capacity_kg', 0) <= 0: errors.append('Load capacity must be a positive number'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(SackHolderState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()