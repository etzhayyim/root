from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): septa_specs: dict; validation_results: list; graph = StateGraph(GraphState); def validate_thermal_spec(state: GraphState): return {'validation_results': ['Thermal limit checked']}; def check_compatibility(state: GraphState): return {'validation_results': ['Compatibility confirmed']}; graph.add_node('validate_thermal', validate_thermal_spec); graph.add_node('check_comp', check_compatibility); graph.set_entry_point('validate_thermal'); graph.add_edge('validate_thermal', 'check_comp'); graph.add_edge('check_comp', END); compile_graph = graph.compile()