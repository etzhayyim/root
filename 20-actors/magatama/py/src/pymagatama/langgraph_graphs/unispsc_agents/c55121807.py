from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IDHolderState(TypedDict): vendor_id: str; specifications: dict; compliance_checked: bool; def validate_specs(state: IDHolderState): state['compliance_checked'] = all(k in state['specifications'] for k in ['material', 'strength']); return state; graph = StateGraph(IDHolderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()