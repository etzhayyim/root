from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CastingState(TypedDict): alloy_specs: dict; inspection_passed: bool; graph = StateGraph(CastingState); def validate_chemistry(state: CastingState): state['inspection_passed'] = 'Nickel' in state['alloy_specs'].get('elements', []); return state; def check_tolerances(state: CastingState): return {'inspection_passed': state.get('alloy_specs', {}).get('tolerance', 0) < 0.05}; graph.add_node('validate', validate_chemistry); graph.add_node('check_tol', check_tolerances); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_tol'); graph.add_edge('check_tol', END); graph = graph.compile()