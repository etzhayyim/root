from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): status: str; docs_verified: bool; compliance_clearance: bool; def check_compliance(state: ProcState): return {'compliance_clearance': True} if state.get('license_number') else {'compliance_clearance': False}; def verify_docs(state: ProcState): return {'docs_verified': True}; graph = StateGraph(ProcState); graph.add_node('compliance', check_compliance); graph.add_node('verification', verify_docs); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'verification'); graph.add_edge('verification', END); graph.compile()