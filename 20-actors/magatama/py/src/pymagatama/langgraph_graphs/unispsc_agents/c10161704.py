from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class FeedState(TypedDict): composition_data: dict; safety_checks: Annotated[Sequence[str], operator.add]; status: str; def validate_minerals(state: FeedState): return {'safety_checks': ['Mineral ratio verified'], 'status': 'VALIDATED'} if state['composition_data'].get('ratio') > 0 else {'status': 'REJECTED'}; def perform_check(state: FeedState): return {'safety_checks': ['Safety criteria met']}; graph = StateGraph(FeedState); graph.add_node('validate', validate_minerals); graph.add_node('safety', perform_check); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()