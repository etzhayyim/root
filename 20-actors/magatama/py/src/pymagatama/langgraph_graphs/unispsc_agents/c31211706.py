from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StainProcessState(TypedDict): chemical_data: dict; compliance_check: bool; output_report: str; def validate_chemical(state: StainProcessState): state['compliance_check'] = 'voc_level' in state.get('chemical_data', {}) and state['chemical_data']['voc_level'] < 250; return state; def finalize_workflow(state: StainProcessState): state['output_report'] = 'Compliance Verified' if state['compliance_check'] else 'Compliance Required'; return state; graph = StateGraph(StainProcessState); graph.add_node('validate', validate_chemical); graph.add_node('finalize', finalize_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()