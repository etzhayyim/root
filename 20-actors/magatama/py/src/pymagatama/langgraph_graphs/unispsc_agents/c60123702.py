from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MacrameState(TypedDict): beads_type: str; specs: dict; approved: bool; def validate_specs(state: MacrameState): state['approved'] = 'Hole Diameter' in state['specs'] and state['specs']['Hole Diameter'] > 0; return state; graph = StateGraph(MacrameState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()