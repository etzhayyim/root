from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class APIState(TypedDict): chemical_id: str; purity: float; storage_temp: float; validated: bool; def validate_purity(state: APIState): return {'validated': state['purity'] >= 0.98}; def check_storage(state: APIState): return {'validated': state['storage_temp'] <= -20.0}; graph = StateGraph(APIState); graph.add_node('check_purity', validate_purity); graph.add_node('check_storage', check_storage); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'check_storage'); graph.add_edge('check_storage', END); graph = graph.compile()