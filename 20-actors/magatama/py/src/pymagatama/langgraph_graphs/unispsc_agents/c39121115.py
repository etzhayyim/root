from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SwitchgearState(TypedDict): specs: dict; compliance: bool; valid: bool; def validate_specs(state: SwitchgearState): return {'valid': 'rated_voltage_kv' in state['specs']}; def check_regulatory(state: SwitchgearState): return {'compliance': state['specs'].get('iec_standard_compliance', False)}; graph = StateGraph(SwitchgearState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()