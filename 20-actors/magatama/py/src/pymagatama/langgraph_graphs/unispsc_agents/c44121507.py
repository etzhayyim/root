from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EnvelopeProcurementState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_spec(state: EnvelopeProcurementState): errors = []; for item in state['items']: if 'gsm' not in item: errors.append('Missing gsm specification.'); return {'validation_errors': errors}; graph = StateGraph(EnvelopeProcurementState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()