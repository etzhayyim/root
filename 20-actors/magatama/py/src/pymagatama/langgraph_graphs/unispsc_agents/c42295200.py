from langgraph.graph import StateGraph, END; from typing import TypedDict; class SurgicalState(TypedDict): status: str; spec_validated: bool; workflow_step: str; def validate_specs(state: SurgicalState): state['spec_validated'] = True; state['workflow_step'] = 'SAFETY_CHECK'; return state; def check_compliance(state: SurgicalState): state['status'] = 'COMPLIANT' if state['spec_validated'] else 'REJECTED'; return state; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()