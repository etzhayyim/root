from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MaskState(TypedDict): vendor_certs: List[str]; filtration_level: str; is_approved: bool; def check_certification(state: MaskState): return {'is_approved': 'ISO_CERT' in state['vendor_certs']}; def validate_specs(state: MaskState): return {'is_approved': state['filtration_level'] == 'N95'}; graph = StateGraph(MaskState); graph.add_node('cert', check_certification); graph.add_node('spec', validate_specs); graph.set_entry_point('cert'); graph.add_edge('cert', 'spec'); graph.add_edge('spec', END); compiled_graph = graph.compile()