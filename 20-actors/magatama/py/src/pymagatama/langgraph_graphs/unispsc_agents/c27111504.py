from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KnifeState(TypedDict): blade_length: float; has_lock: bool; compliance_status: bool; graph = StateGraph(KnifeState); def validate_safety(state: KnifeState): state['compliance_status'] = state['blade_length'] < 10.0 and state['has_lock']; return state; graph.add_node('safety_check', validate_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()