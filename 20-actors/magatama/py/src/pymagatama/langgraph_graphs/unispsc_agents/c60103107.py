from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; def validate_bands(state: ProcureState): return {'validated': all('rubber' in item.lower() for item in state['items'])}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_bands); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()