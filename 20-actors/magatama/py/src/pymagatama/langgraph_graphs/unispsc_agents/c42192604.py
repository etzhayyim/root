from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugSystemState(TypedDict): device_id: str; compliance_docs: List[str]; approved: bool; def validate_specs(state: DrugSystemState): state['approved'] = 'ISO_13485' in state['compliance_docs']; return state; builder = StateGraph(DrugSystemState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()