from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): specs: dict; validation_log: List[str]; approved: bool; def validate_specs(state: AssemblyState): state['validation_log'].append('Checking material certs...'); return {'approved': True if 'Inconel' in str(state['specs']) else False}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()