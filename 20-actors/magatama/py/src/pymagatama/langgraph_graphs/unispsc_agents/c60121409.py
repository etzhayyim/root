from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_name: str; specs: dict; validation_status: bool; def validate_specs(state: ToolState): state['validation_status'] = all(k in state['specs'] for k in ['precision', 'material']); return state; def finalize_order(state: ToolState): return {'validation_status': True}; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()