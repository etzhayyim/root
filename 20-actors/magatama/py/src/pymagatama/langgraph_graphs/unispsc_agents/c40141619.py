from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ValveState(TypedDict): valve_id: str; specs: dict; compliant: bool; def validate_specs(state: ValveState): state['compliant'] = 'pressure_rating' in state['specs'] and state['specs']['pressure_rating'] > 0; return state; graph = StateGraph(ValveState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()