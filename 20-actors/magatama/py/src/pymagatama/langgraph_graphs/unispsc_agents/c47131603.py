from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpongeState(TypedDict): vendor_id: str; sponge_spec: dict; validation_passed: bool; def validate_specs(state: SpongeState): state['validation_passed'] = 'AbsorptionCapacity' in state['sponge_spec'] and state['sponge_spec']['AbsorptionCapacity'] > 0; return state; graph = StateGraph(SpongeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()