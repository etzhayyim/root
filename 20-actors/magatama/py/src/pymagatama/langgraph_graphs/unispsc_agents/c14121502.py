from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PalletState(TypedDict): capacity: int; material: str; verified: bool; graph = StateGraph(PalletState); def check_specs(state: PalletState): return {'verified': state['capacity'] > 1000}; def finalize_procurement(state: PalletState): return {'verified': True}; graph.add_node('check', check_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('check'); graph.add_edge('check', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()