from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: State): specs = state['specs']; is_valid = all(key in specs for key in ['ISO_13485', 'pressure_kPa']) and specs['pressure_kPa'] > 0; return {'valid': is_valid, 'error': '' if is_valid else 'Missing compliance data'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()