from langgraph.graph import StateGraph, END; from typing import TypedDict; class DrawerState(TypedDict): specs: dict; valid: bool; def check_specs(state: DrawerState): state['valid'] = 'Load Capacity' in state['specs']; return state; graph = StateGraph(DrawerState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()