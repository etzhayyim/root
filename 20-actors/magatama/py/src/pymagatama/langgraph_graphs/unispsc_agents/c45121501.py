from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CameraState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def validate_specs(state: CameraState): return {'validation_results': ['Resolution Check: OK', 'Sync Port: Verified'] if state['specs'].get('resolution') > 20 else ['Resolution Check: Fail']}; graph = StateGraph(CameraState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()