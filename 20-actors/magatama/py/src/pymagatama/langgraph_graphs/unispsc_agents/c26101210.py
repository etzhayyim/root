from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MotorProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_motor_specs(state: MotorProcurementState): specs = state['specs']; errors = []; if specs.get('rated_power_kw', 0) <= 0: errors.append('Invalid power rating'); if not specs.get('ip_rating'): errors.append('Missing IP rating'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(MotorProcurementState); graph.add_node('validate', validate_motor_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()