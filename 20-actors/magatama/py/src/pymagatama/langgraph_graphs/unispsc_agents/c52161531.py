from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; valid: bool; def validate_specs(state: State): spec = state['input_data']; state['valid'] = 'Rated Power Output' in spec and 'Safety Certification' in spec; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()