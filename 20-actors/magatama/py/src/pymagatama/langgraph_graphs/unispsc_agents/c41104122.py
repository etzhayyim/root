from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): commodity_code: str; spec_data: dict; validation_results: List[str]; def validate_medical_grade(state: ProcurementState): is_valid = state['spec_data'].get('leak_proof', False); result = 'Pass' if is_valid else 'Fail'; state['validation_results'].append(f'Medical Grade Check: {result}'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_medical_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()