from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: PipeState): errors = []; if state['specs'].get('pressure_rating', 0) <= 0: errors.append('Invalid pressure rating'); return {'validation_errors': errors}; def check_compliance(state: PipeState): return {'status': 'COMPLIANT' if not state['validation_errors'] else 'REJECTED'}; graph = StateGraph(PipeState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()