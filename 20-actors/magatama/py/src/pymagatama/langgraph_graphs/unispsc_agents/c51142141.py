from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): input_data: dict; validation_report: list; status: str; def validate_gmp(state: DrugState): state['validation_report'].append('GMP verified'); return state; def check_purity(state: DrugState): state['validation_report'].append('Purity > 99%'); return state; graph = StateGraph(DrugState); graph.add_node('gmp', validate_gmp); graph.add_node('purity', check_purity); graph.add_edge('gmp', 'purity'); graph.add_edge('purity', END); graph.set_entry_point('gmp'); graph = graph.compile()