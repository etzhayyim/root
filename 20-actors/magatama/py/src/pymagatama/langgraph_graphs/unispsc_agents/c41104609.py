from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnaceState(TypedDict): specs: dict; validation_results: List[str]; def validate_safety(state: FurnaceState): furnace_type = state['specs'].get('type'); errors = ['Interlock failure' if furnace_type == 'high-temp' else 'none']; return {'validation_results': errors}; graph = StateGraph(FurnaceState); graph.add_node('safety_check', validate_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph_app = graph.compile()