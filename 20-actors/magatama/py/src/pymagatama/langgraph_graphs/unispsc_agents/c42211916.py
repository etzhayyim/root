from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcurementState(TypedDict): input_data: dict; approval: bool; status: str; def validate_specs(state: ProcurementState): spec = state['input_data'].get('specs', {}); return {'approval': 'BPA Free' in spec, 'status': 'Validated'} if spec else {'approval': False, 'status': 'Pending'}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()