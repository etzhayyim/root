from langgraph.graph import StateGraph, END; from typing import TypedDict; class RiotShieldState(TypedDict): shield_type: str; compliant: bool; review_status: str; def validate_security_compliance(state: RiotShieldState): state['compliant'] = state['shield_type'] == 'standard_riot' return state; def check_export_controls(state: RiotShieldState): state['review_status'] = 'PENDING_EXPORT_APPROVAL' if state['compliant'] else 'REJECTED' return state; graph = StateGraph(RiotShieldState); graph.add_node('validate', validate_security_compliance); graph.add_node('export', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()