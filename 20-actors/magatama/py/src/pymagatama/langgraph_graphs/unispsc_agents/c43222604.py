from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): hardware_specs: dict; compliance_status: bool; validation_log: List[str]; def validate_specs(state: NetworkState): throughput = state['hardware_specs'].get('throughput', 0); return {'compliance_status': throughput > 0, 'validation_log': ['Specs validated']}; def check_export_controls(state: NetworkState): return {'validation_log': state['validation_log'] + ['Export control check passed']}; graph = StateGraph(NetworkState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()