from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnchorState(TypedDict): anchor_type: str; load_specs: dict; validated: bool; def validate_load(state: AnchorState): state['validated'] = state['load_specs']['max_kg'] > 0; return state; def check_compliance(state: AnchorState): print(f'Validating specs for {state['anchor_type']}'); return 'validated' if state['validated'] else 'failed'; graph = StateGraph(AnchorState); graph.add_node('validate', validate_load); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()