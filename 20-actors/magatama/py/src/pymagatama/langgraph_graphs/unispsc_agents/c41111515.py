from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class WeighingState(TypedDict): material: str; volume: float; anti_static: bool; validated: bool; def validate_ware(state: WeighingState): state['validated'] = state['anti_static'] and state['volume'] > 0; return state; graph = StateGraph(WeighingState); graph.add_node('validate', validate_ware); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()