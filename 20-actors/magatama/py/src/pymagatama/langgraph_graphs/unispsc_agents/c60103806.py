from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HistoryCardSpec(TypedDict): card_data: dict; validation_errors: List[str]; def validate_content(state: HistoryCardSpec): state['validation_errors'] = []; if 'content' not in state['card_data']: state['validation_errors'].append('Missing content'); return state; def check_quality(state: HistoryCardSpec): return state; graph = StateGraph(HistoryCardSpec); graph.add_node('validate', validate_content); graph.add_node('quality', check_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'quality'); graph.add_edge('quality', END); compiled_graph = graph.compile()