from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: CastingState): state['validation_results'].append('Material chemistry verified'); return state; def validate_dimensions(state: CastingState): state['validation_results'].append('Dimensional tolerance check passed'); return state; graph = StateGraph(CastingState); graph.add_node('MaterialCheck', validate_material); graph.add_node('DimensionCheck', validate_dimensions); graph.set_entry_point('MaterialCheck'); graph.add_edge('MaterialCheck', 'DimensionCheck'); graph.add_edge('DimensionCheck', END); graph = graph.compile()