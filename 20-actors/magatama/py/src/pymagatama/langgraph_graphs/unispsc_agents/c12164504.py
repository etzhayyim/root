from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): material_id: str; specifications: dict; validation_results: Annotated[Sequence[str], operator.add]; def validate_material_spec(state: MaterialState): return {'validation_results': ['Spec compliant with ISO standards']}; def check_export_control(state: MaterialState): return {'validation_results': ['Dual-use check passed']}; graph = StateGraph(MaterialState); graph.add_node('validate', validate_material_spec); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()