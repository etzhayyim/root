from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_load(state: ProcureState): state['valid'] = state['specs'].get('load_capacity', 0) > 0; return state; def check_surface(state: ProcureState): state['log'].append('Surface compatibility match checked'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_load); graph.add_node('surface', check_surface); graph.set_entry_point('validate'); graph.add_edge('validate', 'surface'); graph.add_edge('surface', END); graph = graph.compile()