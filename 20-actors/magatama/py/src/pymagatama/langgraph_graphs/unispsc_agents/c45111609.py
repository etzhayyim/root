from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProjectorState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ProjectorState): return {'valid': state['specs'].get('Brightness_Lumens', 0) > 2000}; def finalize(state: ProjectorState): return {'valid': True}; graph = StateGraph(ProjectorState); graph.add_node('validate', validate_specs); graph.add_node('final', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'final'); graph.add_edge('final', END); graph = graph.compile()