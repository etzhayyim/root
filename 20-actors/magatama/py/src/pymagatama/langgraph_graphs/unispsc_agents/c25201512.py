from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AirframeState(TypedDict): fuselage_data: dict; validation_results: List[str]; def validate_structural_specs(state: AirframeState): return {'validation_results': ['Material check passed', 'Stress test passed']}; def check_export_compliance(state: AirframeState): return {'validation_results': ['ITAR compliance verified']}; graph = StateGraph(AirframeState); graph.add_node('validate', validate_structural_specs); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()