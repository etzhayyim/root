from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ROState(TypedDict): pump_specs: dict; validation_report: list; status: str; def validate_pressure(state: ROState): state['validation_report'].append('Pressure check passed') if state['pump_specs'].get('pressure', 0) > 0 else None; return state; def check_compliance(state: ROState): state['validation_report'].append('Compliance check complete'); return state; graph = StateGraph(ROState); graph.add_node('validate', validate_pressure); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()