from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): sensor_specs: dict; validation_passed: bool; def validate_speed_sensor(state: GraphState): specs = state.get('sensor_specs', {}); state['validation_passed'] = 'precision' in specs and 'ip_rating' in specs; return state; graph = StateGraph(GraphState); graph.add_node('validate', validate_speed_sensor); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()