from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): purity_level: float; compliance_docs: bool; result: str; def validate_purity(state: PharmaState): return {'result': 'PURITY_VALIDATED' if state['purity_level'] >= 99.0 else 'REJECTED'}; def verify_compliance(state: PharmaState): return {'result': 'COMPLIANT' if state['compliance_docs'] else 'REQUIRED_DOCS_MISSING'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('compliance', verify_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()