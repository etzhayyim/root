from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; specs: dict; validation_report: str; def validate_specs(state: ComponentState): specs = state['specs']; report = 'Passed' if specs.get('thickness', 0) > 0 else 'Failed'; return {'validation_report': report}; def post_process(state: ComponentState): return {'validation_report': state['validation_report'] + ' - Ready for assembly'}; graph = StateGraph(ComponentState); graph.add_node('validate', validate_specs); graph.add_node('process', post_process); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()