from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; defect_log: List[str]; graph = StateGraph(ProcureState); def validate_skis(state: ProcureState): return {'validated': all('buoyancy' in item.lower() for item in state['items'])}; graph.add_node('validate', validate_skis); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()