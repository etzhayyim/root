from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): cold_chain_verified: bool; qty: int; compliance_status: str; def verify_cold_chain(state: DrugState): return { 'cold_chain_verified': True }; def check_regulatory(state: DrugState): return { 'compliance_status': 'APPROVED' }; graph = StateGraph(DrugState); graph.add_node('cold_chain', verify_cold_chain); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('cold_chain'); graph.add_edge('cold_chain', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()