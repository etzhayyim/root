from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EngraverState(TypedDict): engraving_specs: dict; validation_results: List[str]; graph = StateGraph(EngraverState); def validate_device(state: EngraverState): state['validation_results'].append('Specifications Verified'); return state; def check_compliance(state: EngraverState): state['validation_results'].append('Compliance Checked'); return state; graph.add_node('validate', validate_device); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()