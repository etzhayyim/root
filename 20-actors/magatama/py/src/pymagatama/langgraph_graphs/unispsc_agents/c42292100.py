from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_instruments(state: SurgicalState): state['validated'] = all('ISO_13485' in c for c in state['certs']); return state; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_instruments); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()