from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_id: str; quality_passed: bool; storage_temp: float; def validate_reagent(state: ReagentState): return {'quality_passed': True if state['storage_temp'] <= 4.0 else False}; def route_by_quality(state: ReagentState): return 'approval' if state['quality_passed'] else 'quarantine'; graph = StateGraph(ReagentState); graph.add_node('validate', validate_reagent); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()