from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WasteState(TypedDict): equipment_id: str; safety_checks: List[str]; compliant: bool; def validate_equipment(state: WasteState): state['compliant'] = all(check in state['safety_checks'] for check in ['shielding', 'leak_test']); return state; graph = StateGraph(WasteState); graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()