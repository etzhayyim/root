from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TransferBoardState(TypedDict): board_specs: dict; compliance_passed: bool; def validate_specs(state: TransferBoardState): specs = state.get('board_specs', {}); weight_capacity = specs.get('weight_capacity', 0); state['compliance_passed'] = weight_capacity >= 150; return state; def check_certification(state: TransferBoardState): certs = state.get('certifications', []); state['compliance_passed'] = 'ISO10993' in certs; return state; graph = StateGraph(TransferBoardState); graph.add_node('validate', validate_specs); graph.add_node('cert_check', check_certification); graph.set_entry_point('validate'); graph.add_edge('validate', 'cert_check'); graph.add_edge('cert_check', END); graph = graph.compile()