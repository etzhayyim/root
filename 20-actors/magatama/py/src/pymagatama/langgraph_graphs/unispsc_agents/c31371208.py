from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CastableState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(CastableState); def validate_specs(state: CastableState): return {'validation_results': ['Al2O3 and density verified']}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()