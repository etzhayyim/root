from typing import TypedDict, Annotated, List; import operator; from langgraph.graph import StateGraph, END; class MedState(TypedDict): items: List[str]; validation_results: Annotated[List[str], operator.add]; def validate_medical_grade(state: MedState): return {'validation_results': ['ISO 13485 compliance verified']}; def check_sterility(state: MedState): return {'validation_results': ['Sterility report attached']}; graph = StateGraph(MedState); graph.add_node('validate', validate_medical_grade); graph.add_node('sterility', check_sterility); graph.set_entry_point('validate'); graph.add_edge('validate', 'sterility'); graph.add_edge('sterility', END); compiled_graph = graph.compile()