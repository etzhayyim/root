from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): cold_chain_verified: bool; data: dict; def validate_cold_chain(state: AgentState): return {'cold_chain_verified': state['data'].get('temp') <= 8}; def verify_regulatory(state: AgentState): return {'data': {**state['data'], 'gmp': True}}; graph = StateGraph(AgentState); graph.add_node('validate', validate_cold_chain); graph.add_node('regulatory', verify_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()