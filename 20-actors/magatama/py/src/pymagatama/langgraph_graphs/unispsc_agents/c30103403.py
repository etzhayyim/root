from langgraph.graph import StateGraph, END; from typing import TypedDict; class LeadState(TypedDict): chemical_report: str; purity: float; hazard_cleared: bool; def check_purity(state: LeadState): return {'purity': state['purity'] >= 0.99}; def validate_hazmat(state: LeadState): return {'hazard_cleared': True}; graph = StateGraph(LeadState); graph.add_node('check_purity', check_purity); graph.add_node('validate_hazmat', validate_hazmat); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'validate_hazmat'); graph.add_edge('validate_hazmat', END); graph = graph.compile()