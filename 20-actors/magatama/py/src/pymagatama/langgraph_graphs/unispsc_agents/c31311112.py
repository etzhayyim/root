from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipeState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_material(state: PipeState): state['valid'] = state['specs'].get('material') == 'C1220'; return state; def check_pressure(state: PipeState): state['valid'] = state['valid'] and state['specs'].get('pressure', 0) > 0; return state; graph = StateGraph(PipeState); graph.add_node('validate_material', validate_material); graph.add_node('check_pressure', check_pressure); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_pressure'); graph.add_edge('check_pressure', END); graph = graph.compile()