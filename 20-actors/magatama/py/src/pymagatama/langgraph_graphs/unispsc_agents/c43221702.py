from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): device_id: str; specs: dict; valid: bool; import json; def validate_specs(state: State): state['valid'] = 'compatibility_standard' in state['specs']; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()