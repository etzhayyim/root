from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoapwortState(TypedDict): vendor: str; purity: float; reports: List[str]; approved: bool; def validate_purity(state: SoapwortState): state['approved'] = state['purity'] >= 95.0; return state; def check_reports(state: SoapwortState): state['approved'] = state['approved'] and len(state['reports']) >= 2; return state; graph = StateGraph(SoapwortState); graph.add_node('purity_check', validate_purity); graph.add_node('doc_check', check_reports); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'doc_check'); graph.add_edge('doc_check', END); graph = graph.compile()