from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OilBathState(TypedDict): spec_data: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: OilBathState): errors = []; if state['spec_data'].get('temp_max', 0) > 300: errors.append('High risk: Temp exceeds safe thermal threshold.'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(OilBathState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()