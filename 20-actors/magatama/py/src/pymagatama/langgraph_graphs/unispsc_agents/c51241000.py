from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): doc: str; approval: bool; def validate_pharma(state: State): return {'approval': state['doc'].startswith('GMP-')}; def check_final(state: State): return 'approved' if state['approval'] else 'rejected'; graph = StateGraph(State); graph.add_node('validate', validate_pharma); graph.add_node('check', check_final); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); compiled_graph = graph.compile()