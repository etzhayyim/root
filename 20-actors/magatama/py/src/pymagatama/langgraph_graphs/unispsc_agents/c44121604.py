from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class StampState(TypedDict): stamp_text: str; is_verified: bool; defects: List[str]; graph = StateGraph(StampState); def validate_text(state: StampState): return {'is_verified': len(state['stamp_text']) > 0}; def check_defects(state: StampState): return {'defects': [] if state['is_verified'] else ['Empty text']}; graph.add_node('validate', validate_text); graph.add_node('check', check_defects); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()