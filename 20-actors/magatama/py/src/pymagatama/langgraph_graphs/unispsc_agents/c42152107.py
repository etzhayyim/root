from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): instrument_type: str; specs: dict; approved: bool; def check_compliance(state: DentalState): state['approved'] = 'ISO13485' in state['specs'].get('certs', []); return state; graph = StateGraph(DentalState); graph.add_node('compliance', check_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', END); graph = graph.compile()