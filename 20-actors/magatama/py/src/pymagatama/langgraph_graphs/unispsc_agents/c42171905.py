from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class IVCaseState(TypedDict): components: List[str]; validated: bool; state: str; def validate_specs(state: IVCaseState): state['validated'] = len(state['components']) > 0; return state; graph = StateGraph(IVCaseState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()