from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RetractorState(TypedDict): specs: dict; validation_passed: bool; report: str; def validate_specs(state: RetractorState): state['validation_passed'] = 'material_composition' in state['specs'] and 'sterilization_method' in state['specs']; return state; def finalize_order(state: RetractorState): state['report'] = 'Validation Successful' if state['validation_passed'] else 'Manual Review Required'; return state; graph = StateGraph(RetractorState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()