from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_logs: Annotated[list[str], operator.add]; status: str; def validate_power_unit(state: State): results = []; data = state['input_data']; if 'engine_power' not in data: results.append('Missing required field: engine_power'); return {'validation_logs': results, 'status': 'validated' if not results else 'error'}; graph = StateGraph(State); graph.add_node('validate', validate_power_unit); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()