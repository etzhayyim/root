from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_docs: dict; compliance_status: bool; def validate_spec(state: ProcState): state['compliance_status'] = 'SDS' in state['vendor_docs'] and 'GMP' in state['vendor_docs']; return state; def check_danger(state: ProcState): return 'hazmat_approval' if state.get('compliance_status') else END; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.add_node('hazmat_approval', lambda x: x); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', check_danger); graph.add_edge('hazmat_approval', END); graph.compile()