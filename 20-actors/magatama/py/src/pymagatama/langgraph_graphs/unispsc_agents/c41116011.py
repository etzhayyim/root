from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_id: str; status: str; safety_checks: List[str]; def validate_reagent(state: ReagentState): state['safety_checks'].append('temp_check_passed'); state['status'] = 'validated'; return state; def final_approval(state: ReagentState): state['status'] = 'approved'; return state; graph = StateGraph(ReagentState); graph.add_node('validate', validate_reagent); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()