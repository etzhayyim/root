from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): medicine_data: dict; compliance_checks: List[str]; def validate_drug(state: State): state['compliance_checks'].append('regulatory_license_verified'); return state; def check_safety(state: State): state['compliance_checks'].append('safety_protocol_active'); return state; graph = StateGraph(State); graph.add_node('validate', validate_drug); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()