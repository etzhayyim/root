from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DoorSweepState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: DoorSweepState): results = []; if 'material' not in state['specs']: results.append('Missing material info'); return {'validation_results': results}; def check_compliance(state: DoorSweepState): return {'validation_results': state['validation_results'] + ['Compliance verified']}; graph = StateGraph(DoorSweepState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()