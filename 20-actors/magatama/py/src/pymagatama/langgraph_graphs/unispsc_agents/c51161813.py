from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): input_data: dict; validation_score: float; approved: bool; def validate_gmp(state: ProcState): state['validation_score'] = 1.0 if 'gmp_cert' in state['input_data'] else 0.0; return state; def check_approval(state: ProcState): state['approved'] = state['validation_score'] >= 1.0; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_gmp); graph.add_node('check', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()