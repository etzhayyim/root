from langgraph.graph import StateGraph, END; from typeddict import TypedDict; class MedicalGasState(TypedDict): unit_id: str; specifications: dict; compliance_check: bool; graph = StateGraph(MedicalGasState); def validate_specs(state: MedicalGasState): return {'compliance_check': all(k in state['specifications'] for k in ['gas_types', 'load_rating'])}; def check_regulatory(state: MedicalGasState): return {'compliance_check': state['compliance_check'] and True}; graph.add_node('validate', validate_specs); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()