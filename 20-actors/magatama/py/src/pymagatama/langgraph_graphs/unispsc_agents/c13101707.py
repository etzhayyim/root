from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CommodityState(TypedDict): input_data: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_commodity(state: CommodityState): return {'validation_results': ['Quality checked against 13101707 standards']}; def process_procurement(state: CommodityState): return {'status': 'PROCESSED'}; graph = StateGraph(CommodityState); graph.add_node('validate', validate_commodity); graph.add_node('process', process_procurement); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()