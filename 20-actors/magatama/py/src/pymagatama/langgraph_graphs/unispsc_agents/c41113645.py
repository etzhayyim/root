from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): device_specs: dict; is_compliant: bool; def validate_leakage_device(state: State): specs = state['device_specs']; compliant = specs.get('rated_current') > 0 and 'iec' in specs.get('standards', '').lower(); return {'is_compliant': compliant}; graph = StateGraph(State); graph.add_node('validate', validate_leakage_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()