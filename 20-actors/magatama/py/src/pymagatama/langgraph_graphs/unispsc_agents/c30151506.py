from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RoofingState(TypedDict): tiles: dict; validation_errors: List[str]; def validate_specs(state: RoofingState): state['validation_errors'] = [e for e in ['fire_rating', 'material_grade'] if e not in state['tiles']]; return state; def check_compliance(state: RoofingState): return 'valid' if not state['validation_errors'] else 'invalid'; graph = StateGraph(RoofingState); graph.add_node('validate', validate_specs); graph.add_conditional_edges('validate', check_compliance, {'valid': END, 'invalid': END}); graph.set_entry_point('validate')