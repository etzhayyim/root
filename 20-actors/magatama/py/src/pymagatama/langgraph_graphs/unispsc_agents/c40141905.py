from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DuctProcurementState(TypedDict): material_cert: str; pressure_test_result: bool; compliance_status: bool; def validate_grade(state: DuctProcurementState): print('Validating titanium grade...'); return {'compliance_status': 'Grade 2' in state['material_cert']}; def check_safety(state: DuctProcurementState): print('Running pressure simulation...'); return {'pressure_test_result': True}; graph = StateGraph(DuctProcurementState); graph.add_node('validate', validate_grade); graph.add_node('pressure', check_safety); graph.add_edge('validate', 'pressure'); graph.add_edge('pressure', END); graph.set_entry_point('validate'); graph = graph.compile()