from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GatewayState(TypedDict): vendor: str; protocol_list: List[str]; validation_status: bool; def validate_protocols(state: GatewayState): state['validation_status'] = len(state['protocol_list']) > 0; return state; graph = StateGraph(GatewayState); graph.add_node('validate', validate_protocols); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()