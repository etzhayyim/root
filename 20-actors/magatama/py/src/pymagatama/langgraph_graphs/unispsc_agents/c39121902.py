from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LockoutState(TypedDict): model_id: str; specs: dict; valid: bool; def validate_specs(state: LockoutState): state['valid'] = 'material_composition' in state['specs'] and 'ip_rating' in state['specs']; return state; def route_approval(state: LockoutState): return 'approved' if state['valid'] else 'rectify'; graph = StateGraph(LockoutState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()