from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class VisionState(TypedDict): input_spec: dict; validation_passed: bool; export_compliance: bool; def validate_sensors(state: VisionState): state['validation_passed'] = 'lux_rating' in state['input_spec'] and state['input_spec']['lux_rating'] < 0.01; return state; def check_export(state: VisionState): state['export_compliance'] = state['input_spec'].get('eccn') is not None; return state; graph = StateGraph(VisionState); graph.add_node('validate', validate_sensors); graph.add_node('export', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()