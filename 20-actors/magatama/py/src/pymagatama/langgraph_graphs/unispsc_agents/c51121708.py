from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): batch_id: str; quality_passed: bool; compliance_checked: bool; def validate_purity(state: DrugState): state['quality_passed'] = True; return state; def check_regulations(state: DrugState): state['compliance_checked'] = True; return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()