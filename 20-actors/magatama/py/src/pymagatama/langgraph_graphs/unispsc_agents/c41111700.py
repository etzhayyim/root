from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class OpticalState(TypedDict): specs: dict; valid: bool; error: str; def validate_optics(state: OpticalState): state['valid'] = 'Magnification Power' in state['specs'] and 'Resolution Limit' in state['specs']; return state; builder = StateGraph(OpticalState); builder.add_node('validate', validate_optics); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()