from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftPartState(TypedDict): part_id: str; cert_verified: bool; integrity_check_passed: bool; def verify_cert(state: AircraftPartState): return {'cert_verified': True}; def perform_integrity_check(state: AircraftPartState): return {'integrity_check_passed': True}; graph = StateGraph(AircraftPartState); graph.add_node('verify', verify_cert); graph.add_node('integrity', perform_integrity_check); graph.set_entry_point('verify'); graph.add_edge('verify', 'integrity'); graph.add_edge('integrity', END); graph = graph.compile()