from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabState(TypedDict): enclosure_specs: dict; inspection_status: str; compliance_check: bool; def validate_enclosure(state: LabState): state['compliance_check'] = state['enclosure_specs'].get('containment_level') == 'BSL-3'; return state; def final_approval(state: LabState): state['inspection_status'] = 'APPROVED' if state['compliance_check'] else 'REJECTED'; return state; graph = StateGraph(LabState); graph.add_node('validate', validate_enclosure); graph.add_node('approval', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph = graph.compile()