from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): alloy_code: str; specs: dict; approved: bool; def validate_specs(state: CastingState): state['approved'] = 'ASTM' in state['alloy_code']; return state; builder = StateGraph(CastingState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()