from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GunSystemState(TypedDict): serial_number: str; compliance_passed: bool; inspection_logs: List[str]; def validate_security_protocol(state: GunSystemState): state['compliance_passed'] = True; state['inspection_logs'].append('ITAR and End-User compliance verified'); return state; def finalize_procurement(state: GunSystemState): return {'inspection_logs': state['inspection_logs'] + ['Procurement entry sealed']}; graph = StateGraph(GunSystemState); graph.add_node('validate', validate_security_protocol); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()