from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): specs: dict; verified: bool; log: List[str]; def validate_specs(state: ProcurementState): state['verified'] = 'material' in state['specs'] and 'load_capacity' in state['specs']; state['log'].append('Validation complete'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()