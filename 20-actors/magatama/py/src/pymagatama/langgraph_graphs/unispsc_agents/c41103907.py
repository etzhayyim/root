from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CentrifugeState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(CentrifugeState); def validate_specs(state: CentrifugeState): results = []; rpm = state['specs'].get('rpm', 0); results.append('PASS' if rpm > 50000 else 'FAIL_SPEED'); return {'validation_results': results}; def log_risk(state: CentrifugeState): print('Export control check triggered for high-speed centrifuge.'); return {}; graph.add_node('validate', validate_specs); graph.add_node('compliance', log_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.compile()