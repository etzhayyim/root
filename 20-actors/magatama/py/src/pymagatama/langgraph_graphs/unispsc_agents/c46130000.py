from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RocketState(TypedDict): component_id: str; compliance_docs: List[str]; inspection_status: str; def validate_export(state: RocketState): print('Checking ITAR and Dual-Use compliance...'); return {'inspection_status': 'COMPLIANT'}; def process_subsystem(state: RocketState): print('Processing propulsion/guidance logic...'); return {'inspection_status': 'VERIFIED'}; graph = StateGraph(RocketState); graph.add_node('validate_export', validate_export); graph.add_node('process_subsystem', process_subsystem); graph.set_entry_point('validate_export'); graph.add_edge('validate_export', 'process_subsystem'); graph.add_edge('process_subsystem', END); graph = graph.compile()