from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RiggingState(TypedDict): equipment_specs: dict; passed_safety: bool; def validate_load(state: RiggingState): return {'passed_safety': state['equipment_specs'].get('load_capacity', 0) > 0}; def finalize(state: RiggingState): return {'passed_safety': True}; graph = StateGraph(RiggingState); graph.add_node('validate', validate_load); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()