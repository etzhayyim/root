from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; isValid: bool; def validate_hid_specs(state: State): return {'isValid': state['input_data'].get('ip_rating', 0) >= 65}; def route_procurement(state: State): return 'approval' if state['isValid'] else 'rejected'; graph = StateGraph(State); graph.add_node('validate', validate_hid_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()