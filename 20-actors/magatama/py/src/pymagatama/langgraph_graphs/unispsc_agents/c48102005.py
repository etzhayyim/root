from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureSpec(TypedDict): model_id: str; load_capacity: float; safety_certified: bool; graph = StateGraph(FurnitureSpec); def validate_load(state: FurnitureSpec): return {'safety_certified': state['load_capacity'] > 100}; graph.add_node('load_check', validate_load); graph.set_entry_point('load_check'); graph.add_edge('load_check', END); graph = graph.compile()