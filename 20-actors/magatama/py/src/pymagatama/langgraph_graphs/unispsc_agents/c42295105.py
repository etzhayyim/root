from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; validation_passed: bool; def validate_medical_specs(state: ProcureState): state['validation_passed'] = 'ISO 13485' in state['specs'].get('certifications', []); return state; def check_corrosion_resistance(state: ProcureState): state['validation_passed'] = state['specs'].get('material') == '316L Stainless Steel'; return state; graph = StateGraph(ProcureState); graph.add_node('validate_certs', validate_medical_specs); graph.add_node('check_material', check_corrosion_resistance); graph.set_entry_point('validate_certs'); graph.add_edge('validate_certs', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()