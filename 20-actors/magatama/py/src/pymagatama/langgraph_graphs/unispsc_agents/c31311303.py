from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeAssemblyState(TypedDict): vendor_docs: List[str]; validation_results: dict; assembly_id: str; def validate_metallurgy(state: PipeAssemblyState): return {'validation_results': {'material': 'Hastelloy X Verified'}}; def check_structural_integrity(state: PipeAssemblyState): return {'validation_results': {'pressure_test': 'Passed'}}; graph = StateGraph(PipeAssemblyState); graph.add_node('verify_material', validate_metallurgy); graph.add_node('structural_check', check_structural_integrity); graph.set_entry_point('verify_material'); graph.add_edge('verify_material', 'structural_check'); graph.add_edge('structural_check', END); graph = graph.compile()