from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VibrometerState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: VibrometerState): if state['specs'].get('frequency_range') and state['specs'].get('calibration'): state['validation_report'] = 'Validated'; state['approved'] = True; return state; else: state['validation_report'] = 'Missing critical specs'; state['approved'] = False; return state; graph = StateGraph(VibrometerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()