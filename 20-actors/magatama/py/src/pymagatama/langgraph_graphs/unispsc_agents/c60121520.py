from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validation_results: dict; def validate_spec(state: ProcState): return {'validation_results': {'status': 'approved', 'details': 'Specs compliant'}}; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()