from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): chemical_name: str; grade: str; compliance_docs: List[str]; inspection_status: str; def validate_quality(state: PharmaState): return 'certified' if 'USP' in state['compliance_docs'] else 'rejected'; def process_logistics(state: PharmaState): return {'inspection_status': 'ready_for_shipment'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_quality); graph.add_node('logistics', process_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph = graph.compile()