from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BPGraphState(TypedDict): unit_id: str; compliance_verified: bool; sensor_calibrated: bool; def verify_compliance(state: BPGraphState): return {'compliance_verified': True}; def check_calibration(state: BPGraphState): return {'sensor_calibrated': True}; graph = StateGraph(BPGraphState); graph.add_node('verify', verify_compliance); graph.add_node('calibrate', check_calibration); graph.add_edge('verify', 'calibrate'); graph.add_edge('calibrate', END); graph.set_entry_point('verify'); graph = graph.compile()