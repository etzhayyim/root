from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class UterineRetractorState(TypedDict): specs: dict; compliant: bool; workflow_log: List[str]; def validate_specs(state: UterineRetractorState): state['compliant'] = all(k in state['specs'] for k in ['ISO_13485', 'material']); return state; def check_regulatory(state: UterineRetractorState): state['workflow_log'].append('Checking FDA/MDR compliance'); return state; graph = StateGraph(UterineRetractorState); graph.add_node('validate', validate_specs); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()