from langgraph.graph import StateGraph, END; from typing import TypedDict; class SwitchState(TypedDict): specs: dict; valid: bool; def validate_specs(state: SwitchState): specs = state.get('specs', {}); state['valid'] = 'Rated Voltage' in specs and 'Contact Configuration' in specs; return state; graph = StateGraph(SwitchState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()