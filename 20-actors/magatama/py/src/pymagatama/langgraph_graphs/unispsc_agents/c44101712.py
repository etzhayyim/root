from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MailboxState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: MailboxState): errors = []; if state['specs'].get('load_capacity', 0) <= 0: errors.append('Load capacity must be positive'); return {'validation_errors': errors, 'status': 'validated' if not errors else 'failed'}; graph = StateGraph(MailboxState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()