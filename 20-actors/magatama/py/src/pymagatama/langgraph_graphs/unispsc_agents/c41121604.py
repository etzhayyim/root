from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipetteState(TypedDict): model_ids: List[str]; qc_passed: bool; certs: List[str]; def validate_compatibility(state: PipetteState): state['qc_passed'] = len(state['model_ids']) > 0; return state; def check_purity(state: PipetteState): state['qc_passed'] = 'ISO_Certified' in state['certs']; return state; builder = StateGraph(PipetteState); builder.add_node('validate', validate_compatibility); builder.add_node('purity', check_purity); builder.add_edge('validate', 'purity'); builder.add_edge('purity', END); builder.set_entry_point('validate'); graph = builder.compile()