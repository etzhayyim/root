from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ConveyorState(TypedDict): mount_type: str; specs: dict; valid: bool; def validate_load(state): return {'valid': state['specs'].get('load', 0) > 0}; def check_material(state): return {'valid': state['specs'].get('material') is not None}; graph = StateGraph(ConveyorState); graph.add_node('load_check', validate_load); graph.add_node('material_check', check_material); graph.set_entry_point('load_check'); graph.add_edge('load_check', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()