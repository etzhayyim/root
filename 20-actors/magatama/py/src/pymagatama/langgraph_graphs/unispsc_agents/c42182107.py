from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; validation_passed: bool; auditor_notes: Annotated[list, operator.add]; def validate_medical_specs(state: ProcureState): state['validation_passed'] = 'acoustic_frequency_range' in state['specs']; return {'validation_passed': state['validation_passed']}; def regulatory_check(state: ProcureState): return {'auditor_notes': ['Verified compliance with medical grade sanitization standards']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_medical_specs); graph.add_node('regulatory', regulatory_check); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); graph = graph.compile()