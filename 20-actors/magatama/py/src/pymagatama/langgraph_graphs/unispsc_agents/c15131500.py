from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): material_id: str; purity_check: bool; safety_compliance: bool; audit_log: Annotated[Sequence[str], operator.add]; def check_purity(state: MaterialState) -> dict: return {purity_check: True, audit_log: [Purity verified above 99.9%]} ; def verify_safety(state: MaterialState) -> dict: return {safety_compliance: True, audit_log: [Safety protocols validated]} ; graph = StateGraph(MaterialState); graph.add_node(purity, check_purity); graph.add_node(safety, verify_safety); graph.set_entry_point(purity); graph.add_edge(purity, safety); graph.add_edge(safety, END); compiled_graph = graph.compile()