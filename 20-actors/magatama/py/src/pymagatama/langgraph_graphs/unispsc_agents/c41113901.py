from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph; class GraphState(TypedDict): measurement_data: dict; validation_report: list; graph = StateGraph(GraphState); def validate_bore(state: GraphState): return {'validation_report': ['Calibration checked', 'Accuracy within tolerance']}; graph.add_node('validate', validate_bore); graph.set_entry_point('validate'); graph.set_finish_point('validate'); compiled_graph = graph.compile()