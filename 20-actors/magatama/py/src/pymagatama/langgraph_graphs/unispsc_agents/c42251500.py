from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TherapyState(TypedDict): items: List[str]; validated_items: List[dict]; approval_status: bool; def validate_device(state: TherapyState): validated = []; for item in state['items']: validated.append({'name': item, 'certified': True}); return {'validated_items': validated}; def check_compliance(state: TherapyState): return {'approval_status': len(state['validated_items']) > 0}; graph = StateGraph(TherapyState); graph.add_node('validate', validate_device); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.compile()