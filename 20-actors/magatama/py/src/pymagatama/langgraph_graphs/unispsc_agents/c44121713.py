from langgraph.graph import StateGraph, END; from typing import TypedDict; class PenNibState(TypedDict): nib_type: str; specs_verified: bool; def validate_nib(state: PenNibState): return {'specs_verified': state['nib_type'] in ['gold', 'stainless', 'iridium']}; graph = StateGraph(PenNibState); graph.add_node('validate', validate_nib); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()