from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class OSSState(TypedDict): hardware_specs: dict; compliance_status: bool; def validate_specs(state: OSSState): return {'compliance_status': 'Hardware_Standard' in state['hardware_specs']}; def finalize_procurement(state: OSSState): return {'compliance_status': True}; graph = StateGraph(OSSState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()