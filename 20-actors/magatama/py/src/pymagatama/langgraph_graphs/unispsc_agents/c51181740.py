from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity: float; status: str; def validate_purity(state: PharmaState): return {'status': 'PASSED' if state['purity'] >= 99.0 else 'FAILED'}; def check_compliance(state: PharmaState): return {'status': 'COMPLIANT' if state['status'] == 'PASSED' else 'NON-COMPLIANT'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()