from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): membrane_specs: dict; compliance_status: bool; def validate_specs(state: FilterState): state['compliance_status'] = state['membrane_specs'].get('pore_size', 0) > 0; return state; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()