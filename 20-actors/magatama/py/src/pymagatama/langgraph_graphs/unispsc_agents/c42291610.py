from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): specs: dict; valid: bool; surgical_planer_graph = StateGraph(SurgicalState); def validate_spec(state: SurgicalState): state['valid'] = 'ISO_13485' in state['specs'] and 'material' in state['specs']; return state; surgical_planer_graph.add_node('validate', validate_spec); surgical_planer_graph.set_entry_point('validate'); surgical_planer_graph.add_edge('validate', END); graph = surgical_planer_graph.compile()