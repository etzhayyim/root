from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SteelState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(SteelState); def validate_material(state: SteelState): state['compliance_check'] = 'Grade' in state['specs'] and 'MTC' in state['specs']; return state; graph.add_node('validation', validate_material); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()