from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): component_type: str; specs: dict; compliance_passed: bool; def validate_lead(state: ProcState): return { "compliance_passed": state["specs"].get("lead_purity", 0) > 99.0 }; def finalize(state: ProcState): return {"status": "ready_for_production" if state["compliance_passed"] else "rejected"}; graph = StateGraph(ProcState); graph.add_node("validate", validate_lead); graph.add_node("finalize", finalize); graph.add_edge("validate", "finalize"); graph.set_entry_point("validate"); graph.add_edge("finalize", END); graph = graph.compile()