from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeldingState(TypedDict): specs: dict; status: str; certification_valid: bool; def validate_specs(state: WeldingState) -> WeldingState: state['status'] = 'validation_complete'; state['certification_valid'] = 'calibration_certificate' in state['specs']; return state; def check_export_control(state: WeldingState) -> WeldingState: state['status'] = 'compliance_check'; return state; graph = StateGraph(WeldingState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()