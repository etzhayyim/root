from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoftwareState(TypedDict): software_name: str; languages: List[str]; validated: bool; def check_compatibility(state: SoftwareState): print(f'Validating dictionary software: {state['software_name']}') state['validated'] = True; return state; graph = StateGraph(SoftwareState); graph.add_node('validate', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()