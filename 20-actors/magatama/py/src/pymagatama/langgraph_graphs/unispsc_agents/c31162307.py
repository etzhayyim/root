from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlateState(TypedDict): specs: dict; valid: bool; error: str; def validate_plate(state: PlateState): state['valid'] = 'Material Grade' in state['specs'] and 'Load Capacity' in state['specs']; return state; builder = StateGraph(PlateState); builder.add_node('validate', validate_plate); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()