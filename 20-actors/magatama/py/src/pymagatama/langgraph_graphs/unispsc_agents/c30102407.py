from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MagnesiumState(TypedDict): specs: dict; valid: bool; risk_assessment: str; def validate_specs(state: MagnesiumState): state['valid'] = 'Alloy Grade' in state['specs'] and float(state['specs'].get('Diameter', 0)) > 0; return state; def check_safety(state: MagnesiumState): state['risk_assessment'] = 'Requires Dangerous Goods Handling' if state['valid'] else 'Invalid'; return state; graph = StateGraph(MagnesiumState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()