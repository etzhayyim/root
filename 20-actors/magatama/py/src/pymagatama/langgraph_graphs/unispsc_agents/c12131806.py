from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_data: dict; validation_logs: Annotated[Sequence[str], operator.add]; status: str; def validate_purity(state: ChemicalState): return {validation_logs: [f'Purity check passed: {state[chemical_data].get(purity, 0)}%']}; def check_compliance(state: ChemicalState): return {status: compliant if state[chemical_data].get(reach, False) else non-compliant}; graph = StateGraph(ChemicalState); graph.add_node(validate, validate_purity); graph.add_node(compliance, check_compliance); graph.add_edge(validate, compliance); graph.add_edge(compliance, END); graph.set_entry_point(validate); graph = graph.compile()