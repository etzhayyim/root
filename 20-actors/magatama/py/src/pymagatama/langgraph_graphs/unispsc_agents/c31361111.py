from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: List[str]; validated: bool; def validate_specs(state: ProcurementState): state['validated'] = all(s in state['specs'] for s in ['strength', 'material_cert']); return {'validated': state['validated']}; def route_procurement(state: ProcurementState): return 'approve' if state['validated'] else 'reject'; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()