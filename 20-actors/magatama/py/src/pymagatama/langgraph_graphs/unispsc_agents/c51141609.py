from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; compliance_docs: List[str]; qc_passed: bool; def validate_purity(state: PharmaState): state['qc_passed'] = True; return state; def check_regulations(state: PharmaState): return 'compliance_check' if state['chemical_name'] else 'error'; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('compliance_check', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance_check'); graph.add_edge('compliance_check', END); compiled_graph = graph.compile()