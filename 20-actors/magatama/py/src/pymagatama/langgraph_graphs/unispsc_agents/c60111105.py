from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MathBoardState(TypedDict): board_type: str; compliance_docs: List[str]; approved: bool; def validate_spec(state: MathBoardState): state['approved'] = all(['certificate' in doc.lower() for doc in state['compliance_docs']]); return state; graph = StateGraph(MathBoardState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()