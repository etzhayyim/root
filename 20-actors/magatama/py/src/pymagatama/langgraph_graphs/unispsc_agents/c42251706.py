from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GaitTrainState(TypedDict): device_type: str; specs: dict; compliance_verified: bool; def validate_specs(state: GaitTrainState): state['compliance_verified'] = 'ISO 13485' in state['specs'].get('certifications', []) return state; graph = StateGraph(GaitTrainState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()