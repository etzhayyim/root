from typing import TypedDict, List; from langgraph.graph import StateGraph; class GameState(TypedDict): items: List[str]; validated: bool; state: str; def validate_educational_content(state: GameState) -> GameState: state['validated'] = True; return state; def finalize_procurement(state: GameState) -> GameState: state['state'] = 'READY_FOR_ORDER'; return state; graph = StateGraph(GameState); graph.add_node('validate', validate_educational_content); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', None); graph = graph.compile()