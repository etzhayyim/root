from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SightFlowState(TypedDict): flow_window_spec: dict; validation_results: List[str]; def validate_pressure(state: SightFlowState): state['validation_results'].append('Pressure check passed'); return state; def validate_material(state: SightFlowState): state['validation_results'].append('Material certification verified'); return state; graph = StateGraph(SightFlowState); graph.add_node('pressure_check', validate_pressure); graph.add_node('material_check', validate_material); graph.add_edge('pressure_check', 'material_check'); graph.add_edge('material_check', END); graph.set_entry_point('pressure_check'); app = graph.compile()