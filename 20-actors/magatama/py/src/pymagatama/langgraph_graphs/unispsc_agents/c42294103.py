from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TractionState(TypedDict): device_specs: dict; validation_log: Annotated[list, operator.add]; compliance_status: bool; def validate_clinical_standards(state: TractionState): compliance = 'iso_13485' in state['device_specs'] and state['device_specs'].get('tensile_rated', 0) > 50; return {'compliance_status': compliance, 'validation_log': ['Clinical standard verification complete']}; def finalize_procurement(state: TractionState): return {'validation_log': ['Procurement documentation finalized']}; graph = StateGraph(TractionState); graph.add_node('validate', validate_clinical_standards); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()