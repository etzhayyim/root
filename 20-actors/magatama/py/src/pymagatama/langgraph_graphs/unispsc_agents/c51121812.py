from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CholineState(TypedDict): chemical_data: dict; validation_passed: bool; def validate_purity(state: CholineState): state['validation_passed'] = state['chemical_data'].get('purity', 0) >= 99.0; return state; def check_compliance(state: CholineState): print('Checking GMP status...'); return state; graph = StateGraph(CholineState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_compliance'); graph.add_edge('check_compliance', END); graph = graph.compile()