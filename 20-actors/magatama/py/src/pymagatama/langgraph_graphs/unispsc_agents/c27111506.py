from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ToolState(TypedDict): tool_specs: dict; validation_errors: List[str]; approved: bool; def validate_shear_specs(state: ToolState): specs = state['tool_specs']; errors = []; if not specs.get('blade_material'): errors.append('Missing blade material'); if not specs.get('max_capacity'): errors.append('Missing capacity spec'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_shear_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()