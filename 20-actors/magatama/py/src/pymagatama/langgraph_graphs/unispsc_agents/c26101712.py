from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ValveState(TypedDict): valve_id: str; specs: dict; validation_score: float; graph = StateGraph(ValveState); def validate_specs(state: ValveState) -> ValveState: state['validation_score'] = 1.0 if 'pressure' in state['specs'] else 0.0; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()