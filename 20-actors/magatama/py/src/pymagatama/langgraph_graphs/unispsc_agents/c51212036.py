from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): botanical_data: dict; quality_status: str; def validate_purity(state: State): return {'quality_status': 'verified' if state['botanical_data'].get('purity', 0) > 95 else 'rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()