from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AirportSystemState(TypedDict): hardware_spec: dict; compliance_verified: bool; inspection_result: str; def validate_specs(state: AirportSystemState): state['compliance_verified'] = state['hardware_spec'].get('intensity') > 10000; return state; def check_safety_standards(state: AirportSystemState): state['inspection_result'] = 'Approved' if state['compliance_verified'] else 'Rejected'; return state; graph = StateGraph(AirportSystemState); graph.add_node('validate', validate_specs); graph.add_node('safety_check', check_safety_standards); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph.set_entry_point('validate'); graph = graph.compile()