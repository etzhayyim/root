from langgraph.graph import StateGraph, END; from typing import TypedDict; class AirScrubberState(TypedDict): spec: dict; valid: bool; def validate_specs(state: AirScrubberState): state['valid'] = all(k in state['spec'] for k in ['HEPA', 'CFM']); return state; graph = StateGraph(AirScrubberState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()