from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipeState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: PipeState): state['valid'] = 'Pressure Rating' in state['specs'] and state['specs']['Pressure Rating'] > 0; state['logs'].append('Validation complete'); return state; graph = StateGraph(PipeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()