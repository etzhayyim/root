from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WiringState(TypedDict): wire_gauge: float; voltage_rating: int; compliance_docs: List[str]; approved: bool; def validate_specs(state: WiringState): return {'approved': state['voltage_rating'] >= 250}; def route_compliance(state: WiringState): return 'compliance_check' if not state['compliance_docs'] else 'final'; graph = StateGraph(WiringState); graph.add_node('validate', validate_specs); graph.add_node('compliance_check', lambda s: s); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()