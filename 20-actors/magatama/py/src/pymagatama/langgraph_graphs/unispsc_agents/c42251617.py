from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CushionState(TypedDict): items: List[dict]; validation_results: List[str]; def validate_medical_grade(state: CushionState): results = []; for item in state['items']: results.append('Passed' if item.get('iso_cert') else 'Flagged'); return {'validation_results': results}; graph = StateGraph(CushionState); graph.add_node('validate', validate_medical_grade); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()