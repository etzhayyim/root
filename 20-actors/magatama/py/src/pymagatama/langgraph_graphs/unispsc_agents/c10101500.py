from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class LiveAnimalState(TypedDict): animal_id: str; health_status: str; transit_log: Annotated[Sequence[str], operator.add]; compliance_check: bool; def validate_health(state: LiveAnimalState): return {'compliance_check': state['health_status'] == 'verified'}; def route_to_facility(state: LiveAnimalState): return 'facility_node' if state['compliance_check'] else END; graph = StateGraph(LiveAnimalState); graph.add_node('health_check', validate_health); graph.add_node('facility_node', lambda x: {'transit_log': ['facility_entry']}); graph.set_entry_point('health_check'); graph.add_conditional_edges('health_check', route_to_facility); graph.add_edge('facility_node', END); graph = graph.compile()