from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PPEState(TypedDict): vendor_certs: List[str]; safety_standards: List[str]; compliance_score: float; def validate_cert(state: PPEState) -> PPEState: state['compliance_score'] = 1.0 if 'ISO' in state['vendor_certs'] else 0.5; return state; def inspect_safety(state: PPEState) -> PPEState: return state; graph = StateGraph(PPEState); graph.add_node('validate', validate_cert); graph.add_node('inspect', inspect_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()