from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabCoatState(TypedDict): coat_specs: dict; validation_results: List[str]; def validate_materials(state: LabCoatState): return {'validation_results': ['Material check passed']}; def finalize_order(state: LabCoatState): return {'validation_results': ['Order finalized']}; graph = StateGraph(LabCoatState); graph.add_node('material_check', validate_materials); graph.add_node('finalizer', finalize_order); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'finalizer'); graph.add_edge('finalizer', END); run = graph.compile()