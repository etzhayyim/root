from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): specs: dict; validation_report: list[str]; def validate_specs(state: FastenerState): report = []; required = ['material', 'standard']; for key in required: if key not in state['specs']: report.append(f'Missing {key}'); return {'validation_report': report}; def finalize_order(state: FastenerState): return {'validation_report': state['validation_report'] + ['Order ready for procurement']}; graph = StateGraph(FastenerState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()