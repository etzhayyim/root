from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadiationSignState(TypedDict): sign_type: str; specs: dict; compliance_check: bool; def validate_specs(state: RadiationSignState): state['compliance_check'] = 'ISO' in state['specs'].get('standard', ''); return state; def route_signography(state: RadiationSignState): return 'validate' if state.get('sign_type') else END; graph = StateGraph(RadiationSignState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()