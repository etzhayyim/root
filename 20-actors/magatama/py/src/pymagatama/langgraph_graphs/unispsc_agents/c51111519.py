from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChemState(TypedDict): chemical_name: str; purity: float; validated: bool; state = ChemState(chemical_name="Temozolomide", purity=0.0, validated=False); def validate_purity(state: ChemState) -> ChemState: state["validated"] = state["purity"] >= 0.99; return state; graph = StateGraph(ChemState); graph.add_node("validate", validate_purity); graph.set_entry_point("validate"); graph.add_edge("validate", END); compiled_graph = graph.compile()