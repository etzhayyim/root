from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class State(TypedDict): messages: Annotated[list, add_messages]; spec_data: dict; validation_report: str; def validate_specs(state: State): specs = state['spec_data']; errors = []; if specs.get('noise_level', 0) > 75: errors.append('Noise level exceeds facility limits.'); return {'validation_report': 'PASSED' if not errors else 'FAILED: ' + '; '.join(errors)}; def finalize_procurement(state: State): return {'validation_report': 'Finalized: Ready for quote generation'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()