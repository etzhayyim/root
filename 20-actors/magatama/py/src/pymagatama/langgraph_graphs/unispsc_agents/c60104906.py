from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BatteryState(TypedDict): specs: dict; compliance_checked: bool; danger_level: str; def validate_battery(state: BatteryState): state['compliance_checked'] = 'UN38.3' in state['specs']; return state; def flag_danger(state: BatteryState): state['danger_level'] = 'high' if state['specs'].get('chemistry') == 'Li-ion' else 'low'; return state; graph = StateGraph(BatteryState); graph.add_node('validate', validate_battery); graph.add_node('flag', flag_danger); graph.add_edge('validate', 'flag'); graph.add_edge('flag', END); graph.set_entry_point('validate'); graph = graph.compile()