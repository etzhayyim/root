from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: CompressorState): state['log'].append('Validating pressure and power specs'); state['valid'] = state['specs'].get('pressure_rating_bar', 0) > 0; return state; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()