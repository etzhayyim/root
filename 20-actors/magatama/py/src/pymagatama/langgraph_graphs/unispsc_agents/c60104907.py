from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GeneratorState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: GeneratorState): errors = []; if state['specs'].get('output_w', 0) < 0: errors.append('Negative power'); return {'validation_errors': errors}; def finalize_procurement(state: GeneratorState): return {'status': 'READY_FOR_ORDER' if not state['validation_errors'] else 'REJECTED'}; graph = StateGraph(GeneratorState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()