from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BushingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(BushingState); def validate_material(state: BushingState): state['validation_results'].append('Material verified'); return state; def check_thermal_rating(state: BushingState): state['validation_results'].append('Thermal rating checked'); return state; graph.add_node('material_check', validate_material); graph.add_node('thermal_check', check_thermal_rating); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'thermal_check'); graph.add_edge('thermal_check', END); graph = graph.compile()