from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MagnetState(TypedDict): specs: dict; validation_results: List[str]; def validate_magnets(state: MagnetState): results = []; if state['specs'].get('temp_limit', 0) > 200: results.append('High-temp grade required'); return {'validation_results': results}; def export_control_check(state: MagnetState): return {'validation_results': state['validation_results'] + ['Export license review initiated']}; graph = StateGraph(MagnetState); graph.add_node('validate', validate_magnets); graph.add_node('export_check', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()