from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MeatProcurementState(TypedDict): vendor_docs: List[str]; qc_status: str; status: str; def validate_docs(state: MeatProcurementState): return {'qc_status': 'verified' if 'HACCP' in state['vendor_docs'] else 'pending'}; def finalize_procurement(state: MeatProcurementState): return {'status': 'approved' if state['qc_status'] == 'verified' else 'rejected'}; graph = StateGraph(MeatProcurementState); graph.add_node('validate', validate_docs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()