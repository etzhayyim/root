from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; valid: bool; workflow_log: List[str]; def validate_hazardous_specs(state: ProcState): specs = state.get('specs', {}); required = ['ATEX_Certification_Number', 'Hazardous_Location_Class_Division_Rating']; state['valid'] = all(k in specs for k in required); state['workflow_log'].append('Validation complete'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_hazardous_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()