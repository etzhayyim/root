from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MixerState(TypedDict): model_id: str; specs: dict; valid: bool; def validate_specs(state: MixerState): return {'valid': state['specs'].get('rpm', 0) > 0}; def finalize(state: MixerState): return {'valid': True}; graph = StateGraph(MixerState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()