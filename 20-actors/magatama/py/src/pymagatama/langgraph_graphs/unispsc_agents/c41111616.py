from langgraph.graph import StateGraph, END; from typing import TypedDict; class Schema(TypedDict): accuracy: float; passed: bool; def validate_accuracy(state: Schema): state['passed'] = state['accuracy'] <= 0.005; return state; graph = StateGraph(Schema); graph.add_node('check', validate_accuracy); graph.set_entry_point('check'); graph.add_edge('check', END); graph = graph.compile()