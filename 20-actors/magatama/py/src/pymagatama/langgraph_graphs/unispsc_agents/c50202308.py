from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrinkState(TypedDict): ingredients: List[str]; safety_approved: bool; def validate_ingredients(state: DrinkState): state['safety_approved'] = 'alcohol' not in [i.lower() for i in state['ingredients']]; return state; def route_step(state: DrinkState): return 'end' if state['safety_approved'] else 'end'; graph = StateGraph(DrinkState); graph.add_node('validate', validate_ingredients); graph.add_edge('validate', END); graph.set_entry_point('validate'); app = graph.compile()