from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class NickelProcessingState(TypedDict): content: str; purity: float; particle_size: float; is_valid: bool; def validate_purity(state: NickelProcessingState): return {'is_valid': state['purity'] >= 99.9}; def check_particle_size(state: NickelProcessingState): return {'is_valid': state['is_valid'] and state['particle_size'] <= 45.0}; graph = StateGraph(NickelProcessingState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_size', check_particle_size); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_size'); graph.add_edge('check_size', END); compiled_graph = graph.compile()