from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CartState(TypedDict): items: List[str]; validated: bool; compliance_checked: bool; def validate_cart_specs(state): return {'validated': True}; def check_regulatory_compliance(state): return {'compliance_checked': True}; graph = StateGraph(CartState); graph.add_node('validate', validate_cart_specs); graph.add_node('compliance', check_regulatory_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()