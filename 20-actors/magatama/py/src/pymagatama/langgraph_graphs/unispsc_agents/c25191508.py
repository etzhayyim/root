from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class JetwayState(TypedDict): input_specs: dict; validation_results: list; status: str; def validate_specs(state: JetwayState): results = []; specs = state['input_specs']; if specs.get('length', 0) < 10: results.append('Length insufficient'); return {'validation_results': results}; def finalize_procurement(state: JetwayState): return {'status': 'READY_FOR_TENDER'}; graph = StateGraph(JetwayState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()