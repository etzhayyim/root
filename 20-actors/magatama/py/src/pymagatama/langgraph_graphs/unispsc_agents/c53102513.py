from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class HeadbandState(TypedDict): vendor_specs: dict; validation_score: float; approved: bool; def validate_materials(state: HeadbandState): state['validation_score'] = 1.0 if 'hypoallergenic' in state['vendor_specs'].get('certifications', []) else 0.5; return state; def check_compliance(state: HeadbandState): state['approved'] = state['validation_score'] >= 0.8; return state; graph = StateGraph(HeadbandState); graph.add_node('validate', validate_materials); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()