from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): tolerance_check: bool; material_compliant: bool; validation_log: list; def validate_specs(state: CastingState): state['tolerance_check'] = True; state['validation_log'].append('Tolerance validated'); return state; def check_materials(state: CastingState): state['material_compliant'] = True; state['validation_log'].append('Material cert verified'); return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.add_node('materials', check_materials); graph.set_entry_point('validate'); graph.add_edge('validate', 'materials'); graph.add_edge('materials', END); graph = graph.compile()