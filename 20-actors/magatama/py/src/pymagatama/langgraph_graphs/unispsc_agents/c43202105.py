from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CabinetState(TypedDict): specs: dict; valid: bool; graph = StateGraph(CabinetState); def validate_specs(state: CabinetState): state['valid'] = all(k in state['specs'] for k in ['load_capacity_kg', 'rack_unit_size']); return state; graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()