from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class HalideGraphState(TypedDict): input_data: dict; validation_status: bool; workflow_log: list; graph = StateGraph(HalideGraphState); def validate_spec(state: HalideGraphState): state['validation_status'] = 'gas_compatibility' in state['input_data']; return state; def check_hazmat(state: HalideGraphState): state['workflow_log'].append('Checking for hazardous refrigerant compatibility requirements.'); return state; graph.add_node('validate', validate_spec); graph.add_node('hazmat_check', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat_check'); graph.add_edge('hazmat_check', END); graph.compile()