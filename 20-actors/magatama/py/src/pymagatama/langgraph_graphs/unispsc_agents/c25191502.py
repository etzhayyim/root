from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class GSESupportState(TypedDict): tech_spec: str; compliance_docs: List[str]; inspection_status: str; def validate_specs(state: GSESupportState): return {'inspection_status': 'verified' if state['tech_spec'] else 'pending'}; def check_export_controls(state: GSESupportState): return {'compliance_docs': ['ITAR_Certified']}; graph = StateGraph(GSESupportState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()