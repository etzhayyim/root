from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class LightingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: LightingState): state['valid'] = 'protocol' in state['specs'] and 'voltage' in state['specs']; return state; builder = StateGraph(LightingState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()