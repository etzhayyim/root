from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): device_id: str; specs: dict; approval: bool; graph = StateGraph(State); def validate_device(state): print(f'Validating device {state[device_id]}'); return {'approval': True}; def process_order(state): print('Processing clinical order'); return {'specs': {}}; graph.add_node('validate', validate_device); graph.add_node('order', process_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'order'); graph.add_edge('order', END); app = graph.compile()