from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class InsulationState(TypedDict): insulation_specs: dict; validation_report: str; def validate_specs(state: InsulationState): state['validation_report'] = 'Validation Successful' if state['insulation_specs'].get('thermal_r') > 20 else 'Failed'; return state; graph = StateGraph(InsulationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()