from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): model_id: str; compliance_checked: bool; validation_report: str; def check_compatibility(state: State): state['compliance_checked'] = True if state['model_id'] else False; return state; def generate_report(state: State): state['validation_report'] = 'Compatible' if state['compliance_checked'] else 'Incompatible'; return state; graph = StateGraph(State); graph.add_node('verify', check_compatibility); graph.add_node('report', generate_report); graph.add_edge('verify', 'report'); graph.add_edge('report', END); graph.set_entry_point('verify'); graph = graph.compile()