from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class ScaleState(TypedDict): input_spec: dict; validation_report: list[str]; def validate_capacity(state: ScaleState): return {'validation_report': ['Capacity check complete']}; def validate_calibration(state: ScaleState): return {'validation_report': ['Calibration compliance verified']}; graph = StateGraph(ScaleState); graph.add_node('check_capacity', validate_capacity); graph.add_node('check_calibration', validate_calibration); graph.set_entry_point('check_capacity'); graph.add_edge('check_capacity', 'check_calibration'); graph.add_edge('check_calibration', END); graph = graph.compile()