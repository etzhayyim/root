from langgraph.graph import StateGraph, END; from typing import TypedDict; class BarSpecs(TypedDict): material: str; dimensions: str; status: str; def validate_material(state: BarSpecs): return {'status': 'validated' if state['material'] in ['Steel', 'Aluminum'] else 'rejected'}; graph = StateGraph(BarSpecs); graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()