from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CiderState(TypedDict): vendor_id: str; compliance_docs: List[str]; inspection_passed: bool; def validate_compliance(state: CiderState): return {'inspection_passed': len(state['compliance_docs']) >= 3}; def route_approval(state: CiderState): return 'approved' if state['inspection_passed'] else 'rejected'; graph = StateGraph(CiderState); graph.add_node('validate', validate_compliance); graph.add_node('approve', lambda x: {'status': 'ok'}); graph.add_edge('validate', 'approve'); graph.set_entry_point('validate'); graph.set_finish_point('approve'); graph = graph.compile()