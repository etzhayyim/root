from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): spec: dict; validated: bool; state: str; def validate_spec(state: ProcureState): state['validated'] = 'material_grade' in state['spec']; return state; def check_export(state: ProcureState): return 'control_passed' if state.get('validated') else 'flag_review'; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()