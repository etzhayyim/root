from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DoughMachineState(TypedDict): specs: dict; validation_report: str; graph = StateGraph(DoughMachineState); def validate_electrical(state: DoughMachineState): state['validation_report'] = 'Voltage and IP standards verified'; return state; def check_food_safety(state: DoughMachineState): state['validation_report'] += ' - Food Grade materials compliant'; return state; graph.add_node('electrical_check', validate_electrical); graph.add_node('safety_check', check_food_safety); graph.set_entry_point('electrical_check'); graph.add_edge('electrical_check', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()