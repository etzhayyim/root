from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MelonState(TypedDict): freshness_score: float; brix_level: float; status: str; def validate_quality(state: MelonState): if state.get('brix_level', 0) < 12: return {'status': 'rejected'}; return {'status': 'approved'}; def finalize_delivery(state: MelonState): return {'status': 'dispatched'}; graph = StateGraph(MelonState); graph.add_node('validate', validate_quality); graph.add_node('ship', finalize_delivery); graph.set_entry_point('validate'); graph.add_edge('validate', 'ship'); graph.add_edge('ship', END); graph = graph.compile()