from langgraph.graph import StateGraph, END; from typing import TypedDict; class SurgicalToolState(TypedDict): tool_id: str; passed: bool; certs: list; def validate_spec(state: SurgicalToolState): state['passed'] = 'ISO13485' in state['certs']; return state; def final_check(state: SurgicalToolState): return 'approved' if state['passed'] else 'rejected'; graph = StateGraph(SurgicalToolState); graph.add_node('validate', validate_spec); graph.add_node('check', final_check); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.set_entry_point('validate'); graph = graph.compile()