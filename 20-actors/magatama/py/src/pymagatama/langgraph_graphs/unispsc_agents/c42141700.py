from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DecubitusState(TypedDict): items: List[str]; validation_results: dict; def validate_spec(state: DecubitusState): state['validation_results'] = {'compliance': True}; return state; def check_bio_compat(state: DecubitusState): state['validation_results']['bio_certified'] = True; return state; graph = StateGraph(DecubitusState); graph.add_node('validate', validate_spec); graph.add_node('biocomp', check_bio_compat); graph.set_entry_point('validate'); graph.add_edge('validate', 'biocomp'); graph.add_edge('biocomp', END); graph = graph.compile()