from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): door_type: str; specs: dict; approved: bool; def validate_specs(state: State): state['approved'] = state['specs'].get('safety_standard') == 'JIS-A-4722'; return state; def check_approval(state: State): return 'approved' if state['approved'] else 'rejected'; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()