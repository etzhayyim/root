from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EduKitState(TypedDict): kit_data: dict; validation_errors: List[str]; approved: bool; def validate_safety(state: EduKitState): errors = []; if 'voltage' in state['kit_data'] and state['kit_data']['voltage'] > 24: errors.append('Voltage exceeds safety limits for educational use.'); return {'validation_errors': errors}; def check_compliance(state: EduKitState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(EduKitState); graph.add_node('validate', validate_safety); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()