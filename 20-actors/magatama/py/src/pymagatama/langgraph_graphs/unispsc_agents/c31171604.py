from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class BushingState(TypedDict): specs: dict; valid: bool; def validate_tolerances(state: BushingState): state['valid'] = state['specs'].get('tolerance') <= 0.005; return state; graph = StateGraph(BushingState); graph.add_node('validate', validate_tolerances); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()