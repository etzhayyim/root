from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): input_data: dict; validation_results: List[str]; status: str; def validate_strip_spec(state: State): state['validation_results'].append('Checking shelf life'); state['validation_results'].append('Verifying chemical sensitivity'); state['status'] = 'ready'; return state; def check_compliance(state: State): state['validation_results'].append('Validating SDS documentation'); return state; graph = StateGraph(State); graph.add_node('validation', validate_strip_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validation'); graph.add_edge('validation', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()