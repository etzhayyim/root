from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec: dict; valid: bool; def validate_spec(state: ProcState): state['valid'] = 'Material composition' in state['spec'] and 'Size range' in state['spec']; return state; graph = StateGraph(ProcState); graph.add_node('validator', validate_spec); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()