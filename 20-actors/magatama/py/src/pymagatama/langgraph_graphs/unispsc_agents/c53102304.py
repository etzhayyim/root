from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec: dict; valid: bool; def validate_spec(state: State): status = 'valid' if 'size' in state['spec'] and 'material' in state['spec'] else 'invalid'; return {'status': status, 'valid': status == 'valid'}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()