from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FlowmeterState(TypedDict): device_id: str; data: dict; status: str; def validate_sensor(state): state['status'] = 'VALIDATED' if state['data'].get('accuracy') > 0.95 else 'REJECTED'; return state; def report_generation(state): return {'status': 'REPORT_COMPLETED'}; graph = StateGraph(FlowmeterState); graph.add_node('validate', validate_sensor); graph.add_node('report', report_generation); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()