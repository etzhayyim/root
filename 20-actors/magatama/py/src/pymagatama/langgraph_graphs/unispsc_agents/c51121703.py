from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): chemical_name: str; purity_percent: float; compliance_verified: bool; def validate_purity(state: PharmaState): print(f'Validating {state["chemical_name"]}'); state['compliance_verified'] = state['purity_percent'] >= 99.0; return {**state}; def update_log(state: PharmaState): print('Logging audit trail'); return {**state}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('log', update_log); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph = graph.compile()