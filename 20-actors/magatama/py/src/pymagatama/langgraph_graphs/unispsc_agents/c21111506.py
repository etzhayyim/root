from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SprayState(TypedDict): specs: dict; compliance: bool; validated: bool; def check_specs(state: SprayState): return {'validated': state['specs'].get('capacity', 0) > 0}; def verify_compliance(state: SprayState): return {'compliance': 'ISO_standard' in state['specs']}; builder = StateGraph(SprayState); builder.add_node('validate', check_specs); builder.add_node('compliance', verify_compliance); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); builder.set_entry_point('validate'); graph = builder.compile()