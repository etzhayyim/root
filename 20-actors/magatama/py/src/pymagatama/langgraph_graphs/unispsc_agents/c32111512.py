from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserSpecState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: LaserSpecState): errors = []; if state['specs'].get('optical_output_power_mw', 0) <= 0: errors.append('Invalid optical power'); return {'validation_errors': errors, 'approved': len(errors) == 0}; def export_control_check(state: LaserSpecState): return {'approved': state['approved'] and state['specs'].get('laser_safety_class') != 'Class 4'}; builder = StateGraph(LaserSpecState); builder.add_node('validate', validate_specs); builder.add_node('export_check', export_control_check); builder.set_entry_point('validate'); builder.add_edge('validate', 'export_check'); builder.add_edge('export_check', END); graph = builder.compile()