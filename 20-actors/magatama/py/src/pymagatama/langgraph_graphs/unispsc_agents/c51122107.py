from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_id: str; purity: float; compliance_check: bool; workflow_steps: List[str]; def validate_chemical(state: ProcureState): state['compliance_check'] = state['purity'] >= 0.99; state['workflow_steps'] = ['KYC_Check', 'Safety_Data_Validation', 'Export_Control_Check']; return state; builder = StateGraph(ProcureState); builder.add_node('validate_chemical', validate_chemical); builder.set_entry_point('validate_chemical'); builder.add_edge('validate_chemical', END); graph = builder.compile()