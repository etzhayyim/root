from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChainState(TypedDict): chain_type: str; specs: dict; valid: bool; def validate_chain(state: ChainState): state['valid'] = state['specs'].get('tensile_strength', 0) > 0; return state; def check_compliance(state: ChainState): print(f'Validating {state["chain_type"]}'); return state; graph = StateGraph(ChainState); graph.add_node('validate', validate_chain); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()