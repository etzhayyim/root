from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): parts_list: List[str]; validation_status: bool; def validate_parts(state: PumpState): return {"validation_status": all(part for part in state['parts_list'])}; def approve_procurement(state: PumpState): return {"validation_status": True}; graph = StateGraph(PumpState); graph.add_node("validate", validate_parts); graph.add_node("approve", approve_procurement); graph.set_entry_point("validate"); graph.add_edge("validate", "approve"); graph.add_edge("approve", END); graph = graph.compile()