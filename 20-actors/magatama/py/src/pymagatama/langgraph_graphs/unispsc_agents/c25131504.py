from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftProcureState(TypedDict): vendor_docs: List[str]; safety_cleared: bool; graph = StateGraph(AircraftProcureState); def validate_airworthiness(state): return {'safety_cleared': True} if 'airworthiness_cert' in state['vendor_docs'] else {'safety_cleared': False}; def export_control_check(state): return {'safety_cleared': True}; graph.add_node('validate_cert', validate_airworthiness); graph.add_node('export_check', export_control_check); graph.set_entry_point('validate_cert'); graph.add_edge('validate_cert', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()