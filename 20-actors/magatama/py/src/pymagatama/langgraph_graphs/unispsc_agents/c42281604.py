from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validation_results: dict; def validate_compliance(state: ProcureState): return {'validation_results': {'is_registered': True}}; def check_hazmat(state: ProcureState): return {'validation_results': {'is_hazardous': True}}; graph = StateGraph(ProcureState); graph.add_node('compliance', validate_compliance); graph.add_node('hazmat_check', check_hazmat); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'hazmat_check'); graph.add_edge('hazmat_check', END); graph = graph.compile()