from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): status: str; spec_data: dict; approval: bool; def validate_spec(state: State): return {'approval': state['spec_data'].get('sterilized', False)}; def finalize_procurement(state: State): return {'status': 'Approved' if state['approval'] else 'Rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()