from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmbossState(TypedDict): tool_specs: dict; validation_results: List[str]; def validate_tool_specs(state: EmbossState): results = []; if 'pattern_depth' not in state['tool_specs']: results.append('Missing depth'); return {'validation_results': results}; def assemble_graph(): graph = StateGraph(EmbossState); graph.add_node('validate', validate_tool_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = assemble_graph()