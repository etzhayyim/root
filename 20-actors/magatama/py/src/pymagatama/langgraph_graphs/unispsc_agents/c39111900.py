from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): hardware_specs: dict; compliance_verified: bool; def check_hazloc_compliance(state: State): state['compliance_verified'] = 'ATEX_certification' in state['hardware_specs'] and 'IECEx_compliance' in state['hardware_specs']; return state; def validate_specs(state: State): return 'compliance_verified' if state['compliance_verified'] else END; graph = StateGraph(State); graph.add_node('validate', check_hazloc_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()