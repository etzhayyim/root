from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): data: dict; analysis: str; compliance_checked: bool; def check_regulatory_status(state: PharmState): state['compliance_checked'] = True; return state; def validate_coa(state: PharmState): return {'analysis': 'COA Verified'}; builder = StateGraph(PharmState); builder.add_node('regulatory_check', check_regulatory_status); builder.add_node('coa_validation', validate_coa); builder.add_edge('regulatory_check', 'coa_validation'); builder.add_edge('coa_validation', END); builder.set_entry_point('regulatory_check'); graph = builder.compile()