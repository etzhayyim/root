from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DeviceState(TypedDict): device_specs: dict; compliance_status: bool; validation_log: List[str]; def validate_medical_device(state: DeviceState): state['compliance_status'] = all(k in state['device_specs'] for k in ['iso_standard', 'sterility_date']); return state; def finalize_order(state: DeviceState): return {'validation_log': ['Compliance verified']}; graph = StateGraph(DeviceState); graph.add_node('validate', validate_medical_device); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate')