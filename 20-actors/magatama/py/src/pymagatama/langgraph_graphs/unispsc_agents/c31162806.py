from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScrewCoverState(TypedDict): material: str; diameter: float; status: str; def validate_spec(state: ScrewCoverState): if state['diameter'] <= 0: return {'status': 'invalid_diameter'}; return {'status': 'validated'}; def finalize_order(state: ScrewCoverState): return {'status': 'ready_for_procurement'}; graph = StateGraph(ScrewCoverState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()