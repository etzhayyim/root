from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_instrument_compatibility(state: State): compatibility = state['input_data'].get('instrument_type'); result = 'valid' if compatibility else 'invalid'; return {'validation_results': [f'Compatibility check: {result}'], 'status': result}; def process_procurement_data(state: State): return {'status': 'processed'}; graph = StateGraph(State); graph.add_node('validate', validate_instrument_compatibility); graph.add_node('process', process_procurement_data); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()