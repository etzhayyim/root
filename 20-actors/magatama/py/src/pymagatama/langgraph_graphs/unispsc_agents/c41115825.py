from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class RadioState(TypedDict): radiation_source: str; compliance_checked: bool; status: str; def check_regulations(state: RadioState) -> dict: return {'compliance_checked': True if state['radiation_source'] else False}; def validate_safety(state: RadioState) -> dict: return {'status': 'CLEARED' if state['compliance_checked'] else 'PENDING_REVIEW'}; graph = StateGraph(RadioState); graph.add_node('check_regs', check_regulations); graph.add_node('safety_check', validate_safety); graph.set_entry_point('check_regs'); graph.add_edge('check_regs', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()