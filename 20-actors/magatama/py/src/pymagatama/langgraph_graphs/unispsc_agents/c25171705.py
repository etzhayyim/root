from typing import TypedDict, Annotated, List; import operator; from langgraph.graph import StateGraph, END; class RotorState(TypedDict): rotor_specs: dict; inspection_results: List[str]; status: str; def validate_rotor(state: RotorState): specs = state.get('rotor_specs', {}); status = 'approved' if specs.get('hardness') > 50 else 'rejected'; return {'status': status, 'inspection_results': ['Hardness check completed']}; def finalize_procurement(state: RotorState): return {'status': 'processed'}; graph = StateGraph(RotorState); graph.add_node('validate', validate_rotor); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compile_graph = graph.compile()