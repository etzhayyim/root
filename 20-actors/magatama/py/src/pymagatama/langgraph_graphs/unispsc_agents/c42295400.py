from langgraph.graph import StateGraph, END; from typing import TypedDict; class SurgicalState(TypedDict): items: list; valid: bool; def validate_cert(state): state['valid'] = 'ISO' in str(state.get('items', '')); return state; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_cert); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()