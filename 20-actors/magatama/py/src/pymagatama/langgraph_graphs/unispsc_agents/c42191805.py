from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MedicalColumnState(TypedDict): model_specs: dict; inspection_required: bool; compliance_status: bool; def validate_specs(state: MedicalColumnState): state['compliance_status'] = 'load_capacity_kg' in state['model_specs'] and state['model_specs']['load_capacity_kg'] > 0; return state; def check_safety(state: MedicalColumnState): state['inspection_required'] = True; return state; graph = StateGraph(MedicalColumnState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()