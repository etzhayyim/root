from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipeState(TypedDict): specs: dict; verified: bool; pipe_graph = StateGraph(PipeState); def check_welding(state: PipeState): state['verified'] = 'WeldingCert' in state['specs']; return state; def validate_dimensions(state: PipeState): return {'verified': state['verified'] and state['specs']['thickness'] > 0}; pipe_graph.add_node('weld_check', check_welding); pipe_graph.add_node('dim_check', validate_dimensions); pipe_graph.set_entry_point('weld_check'); pipe_graph.add_edge('weld_check', 'dim_check'); pipe_graph.add_edge('dim_check', END); graph = pipe_graph.compile()