from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): input: str; validation_results: Annotated[list, operator.add]; status: str; def validate_materials(state: ProcurementState): return {'validation_results': ['Material stability check passed']}; def check_safety(state: ProcurementState): return {'validation_results': ['Thermal safety rating verified']}; graph = StateGraph(ProcurementState); graph.add_node('material_check', validate_materials); graph.add_node('safety_check', check_safety); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'safety_check'); graph.add_edge('safety_check', END); compiled_graph = graph.compile()