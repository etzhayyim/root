from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipetteState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(PipetteState); def validate_racks(state: PipetteState): state['validation_results'] = {'passed': all(item for item in state['items'])}; return state; graph.add_node('validate', validate_racks); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()