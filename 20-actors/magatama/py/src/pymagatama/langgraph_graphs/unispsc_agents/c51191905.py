from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplementState(TypedDict): items: List[str]; qc_passed: bool; def check_regulatory(state: SupplementState): state['qc_passed'] = all('GMP' in item for item in state['items']); return state; def finalize(state: SupplementState): return {'status': 'processed'}; graph = StateGraph(SupplementState); graph.add_node('regulatory', check_regulatory); graph.add_node('finalize', finalize); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()