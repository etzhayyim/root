from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WaferState(TypedDict): vendor_specs: dict; validation_results: List[str]; approved: bool; def validate_wafer_specs(state: WaferState): results = []; specs = state.get('vendor_specs', {}); if specs.get('purity', 0) < 99.9999: results.append('Purity below semiconductor grade'); return {'validation_results': results, 'approved': len(results) == 0}; graph = StateGraph(WaferState); graph.add_node('validate', validate_wafer_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()