from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): spec: dict; validated: bool; state: str; def validate_specs(state: ProcureState): state['validated'] = 'material' in state['spec'] and 'dimensions' in state['spec']; return state; def route_procurement(state: ProcureState): return 'end' if state['validated'] else 'error'; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_edge('validate', 'end'); graph.set_entry_point('validate'); graph = graph.compile()