from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): spec: dict; validated: bool; from pydantic import BaseModel, Field; class DrainPlugSpec(BaseModel): material: str; thread: str; pressure_bar: float; def validate_spec(state: ProcurementState): spec_data = state['spec']; try: DrainPlugSpec(**spec_data); state['validated'] = True; except: state['validated'] = False; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()