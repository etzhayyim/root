from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MedalWorkflowState(TypedDict): medal_type: str; specs: dict; approved: bool; def validate_specs(state: MedalWorkflowState): is_valid = 'material' in state['specs'] and 'size' in state['specs']; return {'approved': is_valid}; def generate_engraving_preview(state: MedalWorkflowState): print(f'Generating preview for {state['medal_type']}'); return {'approved': True}; graph = StateGraph(MedalWorkflowState); graph.add_node('validate', validate_specs); graph.add_node('preview', generate_engraving_preview); graph.set_entry_point('validate'); graph.add_edge('validate', 'preview'); graph.add_edge('preview', END); graph = graph.compile()