from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DVRState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: DVRState): state['valid'] = 'Storage Capacity (TB)' in state['specs'] and state['specs']['Storage Capacity (TB)'] > 0; return state; graph = StateGraph(DVRState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()