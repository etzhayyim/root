from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VaccineState(TypedDict): vendor_id: str; supply_chain_temp_log: List[float]; qc_verified: bool; def check_temperature(state: VaccineState): return 'valid' if all(2 <= t <= 8 for t in state['supply_chain_temp_log']) else 'invalid'; def verify_documentation(state: VaccineState): return {'qc_verified': True}; graph = StateGraph(VaccineState); graph.add_node('check_temp', check_temperature); graph.add_node('verify_docs', verify_documentation); graph.set_entry_point('check_temp'); graph.add_edge('check_temp', 'verify_docs'); graph.add_edge('verify_docs', END); graph = graph.compile()