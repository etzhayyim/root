from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlangeState(TypedDict): specs: dict; validation_report: List[str]; validated: bool; def validate_spec(state: FlangeState): report = []; r = state['specs']; if r.get('pressure_rating') and r.get('material'): report.append('Specs valid'); else: report.append('Missing specs'); return {'validation_report': report, 'validated': True}; graph = StateGraph(FlangeState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()