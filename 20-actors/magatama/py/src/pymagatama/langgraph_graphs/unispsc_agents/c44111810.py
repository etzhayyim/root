from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DraftState(TypedDict): input_spec: str; validation_result: str; def validate_spec(state: DraftState): return {'validation_result': 'Validated: Spec meets professional drafting standards' if 'degree' in state['input_spec'].lower() else 'Error: Missing precision specs'}; def finalize_order(state: DraftState): return {'validation_result': 'Order Ready: ' + state['validation_result']}; graph = StateGraph(DraftState); graph.add_node('validator', validate_spec); graph.add_node('finalizer', finalize_order); graph.set_entry_point('validator'); graph.add_edge('validator', 'finalizer'); graph.add_edge('finalizer', END); graph = graph.compile()