from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ShimState(TypedDict): input_spec: dict; validation_report: list; status: str; def validate_spec(state: ShimState): return {'validation_report': ['Tolerance verified', 'Material ID verified'], 'status': 'VALID'}; def approve_procurement(state: ShimState): return {'status': 'APPROVED'}; graph = StateGraph(ShimState); graph.add_node('validate', validate_spec); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()