from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class MonitorState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: MonitorState): specs = state['specs']; state['validation_passed'] = all(k in specs for k in ['resolution', 'mount']); return state; def check_compliance(state: MonitorState): return state; graph = StateGraph(MonitorState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()