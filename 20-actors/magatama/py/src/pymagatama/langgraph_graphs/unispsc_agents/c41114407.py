from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SolarState(TypedDict): specs: dict; calibration_status: bool; validation_log: List[str]; def validate_sensor(state: SolarState): state['validation_log'].append('Verifying sensor calibration'); state['calibration_status'] = True; return state; def check_specs(state: SolarState): state['validation_log'].append('Checking spec compatibility'); return state; graph = StateGraph(SolarState); graph.add_node('validate', validate_sensor); graph.add_node('check', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()