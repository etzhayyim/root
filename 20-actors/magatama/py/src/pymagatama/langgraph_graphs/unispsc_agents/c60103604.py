from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GenealogyState(TypedDict): input_data: str; validation_status: bool; workflow_steps: List[str]; def validate_records(state: GenealogyState): state['validation_status'] = True; state['workflow_steps'].append('Record Validation'); return state; def format_output(state: GenealogyState): state['workflow_steps'].append('Final Formatting'); return state; graph = StateGraph(GenealogyState); graph.add_node('validate', validate_records); graph.add_node('format', format_output); graph.add_edge('validate', 'format'); graph.add_edge('format', END); graph.set_entry_point('validate'); graph = graph.compile()