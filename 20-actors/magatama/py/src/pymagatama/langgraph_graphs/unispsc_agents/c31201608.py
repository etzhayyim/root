from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FoamAdhesiveState(TypedDict): adhesive_type: str; specs: dict; validation_passed: bool; def validate_specs(state: FoamAdhesiveState): state['validation_passed'] = 'VOC_emission_rating' in state['specs'] and state['specs']['tensile_strength_mpa'] > 0; return state; def check_compliance(state: FoamAdhesiveState): return 'valid' if state['validation_passed'] else 'invalid'; graph = StateGraph(FoamAdhesiveState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()