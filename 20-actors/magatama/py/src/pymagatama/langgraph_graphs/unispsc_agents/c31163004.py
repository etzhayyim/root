from langgraph.graph import StateGraph, END; from typing import TypedDict; class Config(TypedDict): specs: dict; def validate_coupling(state: Config): return {'status': 'validated'} if state.get('specs', {}).get('torque') else {'status': 'error'}; graph = StateGraph(Config); graph.add_node('validate', validate_coupling); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()