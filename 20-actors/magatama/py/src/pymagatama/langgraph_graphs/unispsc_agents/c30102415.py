from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlasticRodState(TypedDict): rod_specs: dict; validation_results: List[str]; graph = StateGraph(PlasticRodState); def validate_material(state: PlasticRodState): return {'validation_results': ['Material check passed']}; def perform_dim_check(state: PlasticRodState): return {'validation_results': ['Dimension check passed']}; graph.add_node('validate', validate_material); graph.add_node('dim_check', perform_dim_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'dim_check'); graph.add_edge('dim_check', END); graph.compile()