from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlantState(TypedDict): species: str; specs: dict; approved: bool; def validate_biosecurity(state: PlantState): state['approved'] = 'Phytosanitary Cert' in state['specs']; return state; def check_health(state: PlantState): state['approved'] = state['approved'] and state['specs'].get('pest_free', False); return state; graph = StateGraph(PlantState); graph.add_node('validate', validate_biosecurity); graph.add_node('health_check', check_health); graph.add_edge('validate', 'health_check'); graph.add_edge('health_check', END); graph.set_entry_point('validate'); graph = graph.compile()