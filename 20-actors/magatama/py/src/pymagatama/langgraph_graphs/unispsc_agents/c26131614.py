from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DamperState(TypedDict): specs: dict; valid: bool; workflow_steps: List[str]; def validate_specs(state: DamperState) -> DamperState: state['valid'] = 'pressure_rating' in state['specs'] and 'material' in state['specs']; return state; def finalize_order(state: DamperState) -> DamperState: state['workflow_steps'] = ['technical_review', 'leakage_test', 'shipping_prep']; return state; graph = StateGraph(DamperState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()