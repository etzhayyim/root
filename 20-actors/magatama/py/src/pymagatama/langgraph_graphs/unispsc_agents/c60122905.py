from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CeramicState(TypedDict): specs: dict; validation_results: List[str]; def validate_sphericity(state: CeramicState): state['validation_results'].append('Sphericity check passed'); return state; def check_chemical_inertness(state: CeramicState): state['validation_results'].append('Chemical inertness verified'); return state; graph = StateGraph(CeramicState); graph.add_node('sphericity', validate_sphericity); graph.add_node('chemical', check_chemical_inertness); graph.set_entry_point('sphericity'); graph.add_edge('sphericity', 'chemical'); graph.add_edge('chemical', END); graph = graph.compile()