from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TurbineState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: TurbineState): state['valid'] = state['specs'].get('efficiency', 0) > 0.8; state['logs'].append('Specs validated'); return state; def check_compliance(state: TurbineState): state['logs'].append('Compliance check complete'); return state; graph = StateGraph(TurbineState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()