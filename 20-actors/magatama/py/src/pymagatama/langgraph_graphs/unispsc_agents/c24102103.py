from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CasingState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: CasingState): return {'validation_results': ['Capacity check passed', 'ISO compliance check passed']}; def finalize_workflow(state: CasingState): return {'status': 'READY_FOR_PROCUREMENT'}; graph = StateGraph(CasingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()