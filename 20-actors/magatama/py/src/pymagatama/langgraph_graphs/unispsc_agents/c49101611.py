from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CharmState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_dimensions(state: CharmState): state['validation_report'] = 'Dimensions verified'; return state; def check_material(state: CharmState): state['approved'] = state['specs'].get('material_safe', False); return state; graph = StateGraph(CharmState); graph.add_node('validate', validate_dimensions); graph.add_node('compliance', check_material); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()