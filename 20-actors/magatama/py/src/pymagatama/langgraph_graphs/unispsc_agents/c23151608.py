from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; verified: bool; errors: List[str]; def validate_specs(state: RobotState): specs = state.get('specs', {}); state['verified'] = all(k in specs for k in ['payload', 'precision']); return state; def check_compliance(state: RobotState): if state.get('verified'): state['errors'] = [] else: state['errors'] = ['Missing required specs']; return state; builder = StateGraph(RobotState); builder.add_node('validate', validate_specs); builder.add_node('compliance', check_compliance); builder.set_entry_point('validate'); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); graph = builder.compile()