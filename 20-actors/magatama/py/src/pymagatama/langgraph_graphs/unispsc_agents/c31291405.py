from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec_data: dict; def validate_dimensions(state: State): return {'status': 'validated'} if 'tolerance' in state['spec_data'] else {'status': 'rejected'}; def check_material(state: State): return {'status': 'approved'} if state['spec_data'].get('material') == 'Copper' else {'status': 'flagged'}; graph = StateGraph(State); graph.add_node('validate', validate_dimensions); graph.add_node('check_material', check_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_material'); graph.add_edge('check_material', END); graph.compile()