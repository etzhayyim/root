from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SprocketSpec(TypedDict): teeth: int; pitch: float; material: str; verified: bool; def validate_specs(state: SprocketSpec): state['verified'] = state['teeth'] > 0 and state['pitch'] > 0; return state; graph = StateGraph(SprocketSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()