from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BioState(TypedDict): cold_chain_validated: bool; batch_verified: bool; records_complete: bool; def check_temp(state: BioState): state['cold_chain_validated'] = True; return state; def verify_batch(state: BioState): state['batch_verified'] = True; return state; graph = StateGraph(BioState); graph.add_node('cold_chain', check_temp); graph.add_node('batch_check', verify_batch); graph.set_entry_point('cold_chain'); graph.add_edge('cold_chain', 'batch_check'); graph.add_edge('batch_check', END); graph = graph.compile()