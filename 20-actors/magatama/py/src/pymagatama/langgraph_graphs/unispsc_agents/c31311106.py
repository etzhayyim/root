from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeSpec(TypedDict): specs: List[str]; validated: bool; def validate_specs(state: PipeSpec): return {'validated': all([s in state['specs'] for s in ['Material', 'Pressure']])}; graph = StateGraph(PipeSpec); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()