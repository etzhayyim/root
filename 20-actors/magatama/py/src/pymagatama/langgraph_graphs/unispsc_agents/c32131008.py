from langgraph.graph import StateGraph, END; from typing import TypedDict; class ThermalState(TypedDict): conductivity: float; status: str; def validate_specs(state: ThermalState): return {"status": "approved" if state["conductivity"] > 1.0 else "rejected"}; graph = StateGraph(ThermalState); graph.add_node("validate", validate_specs); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()