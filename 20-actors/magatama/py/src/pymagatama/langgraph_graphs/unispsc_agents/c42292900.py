from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SutureState(TypedDict): items: List[str]; validation_results: List[bool]; graph = StateGraph(SutureState); def validate_medical_device(state: SutureState): print('Validating regulatory status...'); state['validation_results'] = [True for _ in state['items']]; return state; def check_sterility(state: SutureState): print('Verifying sterility documentation...'); return state; graph.add_node('validate', validate_medical_device); graph.add_node('sterility', check_sterility); graph.add_edge('validate', 'sterility'); graph.add_edge('sterility', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()