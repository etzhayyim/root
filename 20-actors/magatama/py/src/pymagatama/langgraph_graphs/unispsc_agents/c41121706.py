from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CultureTubeState(TypedDict): tube_type: str; specs: dict; is_valid: bool; def validate_specs(state: CultureTubeState): state['is_valid'] = state['specs'].get('autoclavable', False) and state['specs'].get('sterility') is not None; return state; graph = StateGraph(CultureTubeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()