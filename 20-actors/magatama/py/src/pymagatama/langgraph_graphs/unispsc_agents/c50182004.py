from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): data: dict; status: str; def validate_cold_chain(state: State): return {'status': 'validated'} if state['data'].get('temp') <= -18 else {'status': 'rejected'}; def process_logistics(state: State): return {'status': 'logistics_ready'}; graph = StateGraph(State); graph.add_node('validate', validate_cold_chain); graph.add_node('logistics', process_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); compiled_graph = graph.compile()