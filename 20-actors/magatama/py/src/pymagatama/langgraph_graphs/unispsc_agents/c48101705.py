from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): specs: dict; compliant: bool; errors: List[str]; def validate_specs(state: MachineState): required = ['pressure', 'certification']; state['compliant'] = all(k in state['specs'] for k in required); state['errors'] = [f'Missing {k}' for k in required if k not in state['specs']]; return state; graph = StateGraph(MachineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()