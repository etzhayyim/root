from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): chemical_name: str; molecular_weight: float; purity: float; ready_to_procure: bool; def validate_purity(state: ProcurementState): state['ready_to_procure'] = state['purity'] >= 99.0; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()