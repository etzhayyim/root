from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_data: dict; validation_logs: Annotated[list, operator.add]; def validate_specs(state: ComponentState): return {'validation_logs': ['Checking tolerances against ISO standards']}; def check_export_compliance(state: ComponentState): return {'validation_logs': ['Reviewing dual-use export control registry']}; graph = StateGraph(ComponentState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_compliance); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()