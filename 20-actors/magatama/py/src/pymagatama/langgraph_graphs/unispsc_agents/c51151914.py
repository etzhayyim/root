from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_data: str; validation_status: bool; compliance_docs: List[str]; def validate_purity(state: ProcState): state['validation_status'] = True if '99%' in state['chemical_data'] else False; return state; def check_compliance(state: ProcState): state['compliance_docs'] = ['GMP_Cert', 'CoA']; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()