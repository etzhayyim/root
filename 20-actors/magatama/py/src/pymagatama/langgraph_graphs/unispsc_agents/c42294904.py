from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): device_id: str; compliance_docs: List[str]; approved: bool; def validate_specs(state: ProcState): state['approved'] = 'ISO 13485' in state['compliance_docs']; return state; def check_medical_grade(state: ProcState): print(f'Validating medical grade for {state['device_id']}'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('check_grade', check_medical_grade); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_grade'); graph.add_edge('check_grade', END); graph = graph.compile()