from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TinRodState(TypedDict): purity: float; specs: dict; valid: bool; def validate_purity(state: TinRodState): state['valid'] = state['purity'] >= 99.9; return state; graph = StateGraph(TinRodState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()