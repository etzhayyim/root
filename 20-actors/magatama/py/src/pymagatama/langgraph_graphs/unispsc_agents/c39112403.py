from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LightingSupplyState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: LightingSupplyState): s = state['specs']; state['valid'] = 'voltage' in s and 'wattage' in s; return state; graph = StateGraph(LightingSupplyState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); compiled_graph = graph.compile()