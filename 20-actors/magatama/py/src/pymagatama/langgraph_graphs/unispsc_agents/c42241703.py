from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KneeBraceState(TypedDict): specs: dict; compliance_verified: bool; def validate_medical_cert(state: KneeBraceState): state['compliance_verified'] = 'ISO_13485' in state['specs']; return state; def check_fit_specs(state: KneeBraceState): state['fit_check'] = 'size_chart' in state['specs']; return state; graph = StateGraph(KneeBraceState); graph.add_node('validate', validate_medical_cert); graph.add_node('fit', check_fit_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'fit'); graph.add_edge('fit', END); compiled_graph = graph.compile()