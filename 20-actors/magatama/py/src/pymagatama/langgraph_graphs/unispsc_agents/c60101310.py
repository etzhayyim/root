from langgraph.graph import StateGraph, END; from typing import TypedDict; class StickerState(TypedDict): sticker_data: dict; validation_report: list; graph = StateGraph(StickerState); def validate_spec(state: StickerState): print('Validating sticker specs...'); return {'validation_report': ['Specs OK']}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()