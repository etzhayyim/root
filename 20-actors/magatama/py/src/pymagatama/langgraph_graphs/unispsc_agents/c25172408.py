from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): part_id: str; verified: bool; payload: dict; def validate_spec(state: ProcureState): return {'verified': state['payload'].get('pressure_rating', 0) > 0}; def finalize(state: ProcureState): return {'verified': True}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()