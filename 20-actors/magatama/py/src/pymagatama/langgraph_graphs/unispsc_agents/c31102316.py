from langgraph.graph import StateGraph, END; from typing import TypedDict; class CastingState(TypedDict): metal_purity: float; weight: float; assay_verified: bool; def verify_purity(state: CastingState): return {'assay_verified': state['metal_purity'] >= 99.9}; def finalize_procurement(state: CastingState): return {'assay_verified': True}; graph = StateGraph(CastingState); graph.add_node('verify', verify_purity); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('verify'); graph.add_edge('verify', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()