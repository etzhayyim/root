from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PiroxicamState(TypedDict): vendor_docs: List[str]; qc_passed: bool; def validate_gmp(state: PiroxicamState): return {'qc_passed': 'GMP_CERT' in state['vendor_docs']}; def check_safety(state: PiroxicamState): return {'qc_passed': 'SDS' in state['vendor_docs'] and state['qc_passed']}; graph = StateGraph(PiroxicamState); graph.add_node('validate_gmp', validate_gmp); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()