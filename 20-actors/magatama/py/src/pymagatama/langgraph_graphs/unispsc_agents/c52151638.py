from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BrushProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: BrushProcurementState): specs = state.get('specs', {}); errors = []; if 'Material-Composition' not in specs: errors.append('Missing material composition'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(BrushProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()