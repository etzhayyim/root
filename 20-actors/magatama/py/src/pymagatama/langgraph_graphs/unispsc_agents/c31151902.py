from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LeatherSpecState(TypedDict): specs: dict; valid: bool; def validate_specs(state: LeatherSpecState): state['valid'] = 'thickness' in state['specs'] and state['specs']['thickness'] > 0; return state; graph = StateGraph(LeatherSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()