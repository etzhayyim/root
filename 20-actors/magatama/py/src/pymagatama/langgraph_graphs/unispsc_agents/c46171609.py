from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MirrorState(TypedDict): mirror_specs: dict; validation_report: list[str]; graph = StateGraph(MirrorState); def validate_specs(state: MirrorState): specs = state['mirror_specs']; errors = []; if specs.get('diameter', 0) < 300: errors.append('Diameter too small for surveillance'); return {'validation_report': errors}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()