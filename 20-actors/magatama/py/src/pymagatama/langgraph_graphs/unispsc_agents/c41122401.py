from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StirBarState(TypedDict): dimensions: dict; material: str; quantity: int; validation_errors: List[str]; def validate_specs(state: StirBarState): errors = []; if state.get('material') != 'PTFE': errors.append('Material must be PTFE for chemical resistance'); return {'validation_errors': errors}; graph = StateGraph(StirBarState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()