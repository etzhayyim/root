from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): component_specs: dict; compliance_check: bool; validation_log: List[str]; def validate_alloy(state: ProcureState): state['validation_log'].append('Validating composition...'); state['compliance_check'] = True; return state; def check_pressure_logs(state: ProcureState): state['validation_log'].append('Verifying pressure logs...'); return state; graph = StateGraph(ProcureState); graph.add_node('validate_alloy', validate_alloy); graph.add_node('check_pressure', check_pressure_logs); graph.set_entry_point('validate_alloy'); graph.add_edge('validate_alloy', 'check_pressure'); graph.add_edge('check_pressure', END); graph = graph.compile()