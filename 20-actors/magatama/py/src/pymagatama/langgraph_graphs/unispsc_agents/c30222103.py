from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_certs: List[str]; security_cleared: bool; budget_approved: bool; def validate_security(state: ProcureState): state['security_cleared'] = 'top-secret' in state['vendor_certs']; return state; def check_budget(state: ProcureState): state['budget_approved'] = True; return state; graph = StateGraph(ProcureState); graph.add_node('validate_security', validate_security); graph.add_node('check_budget', check_budget); graph.add_edge('validate_security', 'check_budget'); graph.add_edge('check_budget', END); graph.set_entry_point('validate_security'); graph = graph.compile()