from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[str]; validated: bool; graph = StateGraph(State); def validate_paints(state: State): state['validated'] = all(item.startswith('Non-Toxic') for item in state['items']); return state; graph.add_node('validate', validate_paints); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()