from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DeicingState(TypedDict): equipment_id: str; spec_validation: bool; compliance_status: List[str]; def validate_specs(state: DeicingState) -> DeicingState: state['spec_validation'] = True; return state; def check_compliance(state: DeicingState) -> DeicingState: state['compliance_status'] = ['ISO_9001', 'FAA_Safety_Req']; return state; graph = StateGraph(DeicingState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()