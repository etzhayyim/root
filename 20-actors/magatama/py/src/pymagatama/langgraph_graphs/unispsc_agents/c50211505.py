from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): ingredients: List[str]; compliant: bool; approved: bool; def analyze_ingredients(state: ProcurementState): return {'compliant': all('tobacco' not in i.lower() for i in state['ingredients'])}; def final_review(state: ProcurementState): return {'approved': state['compliant']}; graph = StateGraph(ProcurementState); graph.add_node('Analyze', analyze_ingredients); graph.add_node('Review', final_review); graph.add_edge('Analyze', 'Review'); graph.add_edge('Review', END); graph.set_entry_point('Analyze'); graph = graph.compile()