from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EpoxyState(TypedDict): epoxy_specs: dict; validation_report: List[str]; def validate_epoxy_data(state: EpoxyState): specs = state['epoxy_specs']; report = []; if not specs.get('viscosity'): report.append('Missing viscosity data.'); if specs.get('expiry_date') < '2025-01-01': report.append('Material expired.'); return {'validation_report': report}; graph = StateGraph(EpoxyState); graph.add_node('validate', validate_epoxy_data); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()