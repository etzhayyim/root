from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AircraftState(TypedDict): specs: dict; validation_report: List[str]; approved: bool; def validate_sensors(state: AircraftState): state["validation_report"].append("Sensor suite check passed") if state["specs"].get("sensor") else state["validation_report"].append("Sensor missing"); return state; def check_export_control(state: AircraftState): state["approved"] = "export_license" in state["specs"]; return state; graph = StateGraph(AircraftState); graph.add_node("validate", validate_sensors); graph.add_node("compliance", check_export_control); graph.add_edge("validate", "compliance"); graph.add_edge("compliance", END); graph.set_entry_point("validate"); graph = graph.compile()