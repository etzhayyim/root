from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_log: List[str]; approved: bool; def val_step(state: AssemblyState): state['validation_log'].append('Verifying material grade and weld integrity'); return state; graph = StateGraph(AssemblyState); graph.add_node('validate', val_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()