from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[str]; validated: bool; graph = StateGraph(State); def check_quality(state: State): return {'validated': all('stainless' in item.lower() for item in state['items'])}; graph.add_node('validate', check_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()