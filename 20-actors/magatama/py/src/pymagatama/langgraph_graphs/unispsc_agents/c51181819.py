from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; is_compliant: bool; def validate_purity(state: PharmState): state['is_compliant'] = state['purity'] >= 99.0; return state; def check_regulatory(state: PharmState): print(f'Checking import compliance for {state['chemical_name']}'); return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('record', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'record'); graph.add_edge('record', END); compiled_graph = graph.compile()