from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WireState(TypedDict): wire_specs: dict; status: str; def validate_specs(state: WireState): return {'status': 'validated' if 'awg' in state['wire_specs'] else 'error'}; graph = StateGraph(WireState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()