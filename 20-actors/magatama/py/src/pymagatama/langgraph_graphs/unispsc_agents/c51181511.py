from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BuforminState(TypedDict): supplier_name: str; gmp_certified: bool; purity: float; approved: bool; def validate_gmp(state: BuforminState): return {'approved': state['gmp_certified'] and state['purity'] >= 99.0}; graph = StateGraph(BuforminState); graph.add_node('validate', validate_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()