from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class IceProcureState(TypedDict): vendor: str; quantity: int; specs: dict; ready: bool; def validate_quality(state): return {'ready': state['specs'].get('food_safe', False)}; def finalize_order(state): return {'ready': True}; graph = StateGraph(IceProcureState); graph.add_node('validate', validate_quality); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()