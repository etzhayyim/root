from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CartState(TypedDict): specs: dict; ready: bool; def validate_specs(state: CartState): state['ready'] = 'load_capacity_kg' in state['specs']; return state; graph = StateGraph(CartState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()