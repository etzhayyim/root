from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity: float; is_compliant: bool; graph = StateGraph(PharmaState); def validate_purity(state: PharmaState): return {'is_compliant': state['purity'] >= 99.0}; def check_safety(state: PharmaState): return {'is_compliant': state['is_compliant'] and True}; graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); app = graph.compile()