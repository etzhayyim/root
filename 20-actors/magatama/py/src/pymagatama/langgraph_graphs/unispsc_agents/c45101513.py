from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PrintGraphState(TypedDict): vendor_specs: dict; validation_passed: bool; processing_log: List[str]; def validate_specs(state: PrintGraphState): state['validation_passed'] = 'temp_range' in state['vendor_specs'] and state['vendor_specs']['temp_range'] > 0; state['processing_log'].append('Validation completed'); return state; def finalize_order(state: PrintGraphState): return {'processing_log': state['processing_log'] + ['Order ready for approval']}; graph = StateGraph(PrintGraphState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()