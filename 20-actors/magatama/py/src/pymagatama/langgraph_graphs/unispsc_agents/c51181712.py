from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): api_data: dict; compliance_status: bool; def validate_api(state): state['compliance_status'] = 'GMP' in state['api_data'].get('certs', []); return state; def check_purity(state): state['compliance_status'] = state['compliance_status'] and state['api_data'].get('purity', 0) >= 99.0; return state; graph = StateGraph(State); graph.add_node('validate', validate_api); graph.add_node('check_purity', check_purity); graph.add_edge('validate', 'check_purity'); graph.add_edge('check_purity', END); graph.set_entry_point('validate'); graph = graph.compile()