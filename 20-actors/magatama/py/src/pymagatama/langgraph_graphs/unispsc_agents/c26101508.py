from langgraph.graph import StateGraph, END; from typing import TypedDict; class EngineState(TypedDict): engine_spec: dict; compliance_verified: bool; def validation_step(state: EngineState): return {'compliance_verified': state['engine_spec'].get('emissions') == 'EU-Stage-V'}; def assembly_step(state: EngineState): return {'engine_spec': {**state['engine_spec'], 'status': 'validated'}}; graph = StateGraph(EngineState); graph.add_node('validate', validation_step); graph.add_node('assemble', assembly_step); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph.set_entry_point('validate'); graph = graph.compile()