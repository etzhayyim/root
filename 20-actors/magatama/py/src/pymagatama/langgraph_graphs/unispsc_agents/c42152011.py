from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validation_results: List[bool]; def validate_medical_grade(state: State): return {'validation_results': [True for _ in state['items']]}; def log_compliance(state: State): print('Compliance checked'); return {}; graph = StateGraph(State); graph.add_node('validate', validate_medical_grade); graph.add_node('log', log_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); compiled_graph = graph.compile()