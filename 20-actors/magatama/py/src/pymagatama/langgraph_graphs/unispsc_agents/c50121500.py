from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FishProcurementState(TypedDict): species: str; temp_log: List[float]; qcm_passed: bool; graph = StateGraph(FishProcurementState); def validate_temperature(state): return {'qcm_passed': all(t < -18.0 for t in state['temp_log'])}; graph.add_node('qc_check', validate_temperature); graph.set_entry_point('qc_check'); graph.add_edge('qc_check', END); graph = graph.compile()