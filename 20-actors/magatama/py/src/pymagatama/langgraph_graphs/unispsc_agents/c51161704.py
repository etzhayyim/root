from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec_validated: bool; def check_medical_compliance(state: State): return {'spec_validated': True, 'status': 'PHARMA_COMPLIANT'} if True else {'status': 'FAILED'}; graph = StateGraph(State); graph.add_node('validate', check_medical_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()