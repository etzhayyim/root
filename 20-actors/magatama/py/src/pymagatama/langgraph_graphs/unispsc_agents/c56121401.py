from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TableSpecState(TypedDict): specs: dict; errors: List[str]; def validate_load(state: TableSpecState): state['specs']['validated_load'] = state['specs'].get('load_kg', 0) > 0; return state; def validate_mobility(state: TableSpecState): state['specs']['validated_mobility'] = 'caster_type' in state['specs']; return state; graph = StateGraph(TableSpecState); graph.add_node('validate_load', validate_load); graph.add_node('validate_mobility', validate_mobility); graph.set_entry_point('validate_load'); graph.add_edge('validate_load', 'validate_mobility'); graph.add_edge('validate_mobility', END); graph = graph.compile()