from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AdalimumabState(TypedDict): cold_chain_verified: bool; shipment_docs: List[str]; status: str; def verify_temp_log(state): return {'cold_chain_verified': True, 'status': 'validated'}; def check_compliance(state): return {'status': 'approved'}; graph = StateGraph(AdalimumabState); graph.add_node('verify', verify_temp_log); graph.add_node('compliance', check_compliance); graph.add_edge('verify', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('verify'); graph = graph.compile()