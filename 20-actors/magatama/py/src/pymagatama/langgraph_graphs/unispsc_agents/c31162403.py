from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HingeState(TypedDict): hinge_specs: dict; compliance_status: bool; def validate_load_spec(state: HingeState): state['compliance_status'] = state['hinge_specs'].get('load_capacity_kg', 0) > 0; return state; graph = StateGraph(HingeState); graph.add_node('validate', validate_load_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()