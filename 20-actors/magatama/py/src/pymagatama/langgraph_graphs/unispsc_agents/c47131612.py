from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RubberState(TypedDict): part_specs: dict; validation_results: list[str]; graph = StateGraph(RubberState); def validate_rubber_specs(state: RubberState): return {'validation_results': ['Dimension Check OK', 'Hardness Check OK']}; graph.add_node('validate', validate_rubber_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()