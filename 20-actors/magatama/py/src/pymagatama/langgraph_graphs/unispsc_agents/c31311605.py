from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_pipe(state: PipeState): state['valid'] = state['specs'].get('weld_depth', 0) > 95; state['log'].append('Validation completed'); return state; def report_result(state: PipeState): return {'result': 'APPROVED' if state['valid'] else 'REJECTED'}; workflow = StateGraph(PipeState); workflow.add_node('validate', validate_pipe); workflow.add_node('report', report_result); workflow.set_entry_point('validate'); workflow.add_edge('validate', 'report'); workflow.add_edge('report', END); graph = workflow.compile()