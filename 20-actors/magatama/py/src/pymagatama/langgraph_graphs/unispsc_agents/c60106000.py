from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CurriculumState(TypedDict): content_data: str; validation_report: List[str]; graph = StateGraph(CurriculumState); def validate_curriculum(state: CurriculumState): return { "validation_report": ["Format Check Passed", "Pedagogical Standard Checked"] }; graph.add_node("validate", validate_curriculum); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph.compile()