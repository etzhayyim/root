from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; verification_status: str; def validate_instrument_specs(state: State): status = 'verified' if state['input_data'].get('iso_compliant') else 'requires_review'; return {'verification_status': status}; def finalize_report(state: State): print(f'Finalizing report, status: {state['verification_status']}'); return {'verification_status': 'finalized'}; builder = StateGraph(State); builder.add_node('validate', validate_instrument_specs); builder.add_node('finalize', finalize_report); builder.add_edge('validate', 'finalize'); builder.add_edge('finalize', END); builder.set_entry_point('validate'); graph = builder.compile()