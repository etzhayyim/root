from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnchorNutState(TypedDict): part_number: str; specs: dict; validation_results: List[str]; def validate_specs(state: AnchorNutState): results = []; if 'tensile_strength' not in state['specs']: results.append('Missing tensile strength'); return {'validation_results': results}; def log_compliance(state: AnchorNutState): print('Compliance checked for:', state['part_number']); return {}; graph = StateGraph(AnchorNutState); graph.add_node('validate', validate_specs); graph.add_node('log', log_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph = graph.compile()