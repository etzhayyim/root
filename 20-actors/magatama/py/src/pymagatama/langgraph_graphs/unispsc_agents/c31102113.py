from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): specs: dict; validation_report: str; def validate_casting(state: MoldState): return {'validation_report': 'Success' if 'material' in state['specs'] else 'Fail'}; graph = StateGraph(MoldState); graph.add_node('validate', validate_casting); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()