from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; status: str; def validate_weld_quality(state: AssemblyState): state['validation_results'].append('Weld integrity verified'); return state; def check_export_compliance(state: AssemblyState): state['status'] = 'COMPLIANT' if 'ISO' in state['specs'].get('standard', '') else 'PENDING'; return state; graph = StateGraph(AssemblyState); graph.add_node('validate_weld', validate_weld_quality); graph.add_node('compliance_check', check_export_compliance); graph.set_entry_point('validate_weld'); graph.add_edge('validate_weld', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()