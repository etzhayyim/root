from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; valid: bool; error: str; def validate_specs(state: State): specs = state['input_data'].get('specs', {}); state['valid'] = all(k in specs for k in ['weight_capacity', 'iso_cert']); return state; def process_compliance(state: State): return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('compliance', process_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()