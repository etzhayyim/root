from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; validation_results: list[str]; status: str; def validate_specs(state: RobotState): res = 'PASS' if state['specs'].get('repeatability_mm', 1.0) < 0.5 else 'FAIL_PRECISION'; return {'validation_results': [res]}; def assemble_controller(state: RobotState): return {'status': 'READY_FOR_DEPLOYMENT'}; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.add_node('assemble', assemble_controller); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph.set_entry_point('validate'); graph = graph.compile()