from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MicrofilmState(TypedDict): component_id: str; specs: dict; validation_report: list[str]; def validate_component(state: MicrofilmState): report = []; missing = [f for f in ['optical_resolution', 'model_code'] if f not in state['specs']]; report.append('Valid') if not missing else report.append(f'Missing: {missing}'); return {'validation_report': report}; graph = StateGraph(MicrofilmState); graph.add_node('validate', validate_component); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()