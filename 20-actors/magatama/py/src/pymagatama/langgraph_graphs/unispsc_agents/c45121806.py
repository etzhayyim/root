from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): part_id: str; verified: bool; def validate_part(state: ProcState): state['verified'] = True if state['part_id'].startswith('MF-') else False; return {'verified': state['verified']}; def check_inventory(state: ProcState): return {'verified': True}; graph = StateGraph(ProcState); graph.add_node('validate', validate_part); graph.add_node('check_inv', check_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_inv'); graph.add_edge('check_inv', END); graph = graph.compile()