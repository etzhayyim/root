from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PuzzleState(TypedDict): pieces: List[str]; qc_passed: bool; def validate_geometry(state: PuzzleState): return {'qc_passed': len(state['pieces']) == 12}; def check_safety(state: PuzzleState): return {'qc_passed': True}; graph = StateGraph(PuzzleState); graph.add_node('geometry_check', validate_geometry); graph.add_node('safety_check', check_safety); graph.set_entry_point('geometry_check'); graph.add_edge('geometry_check', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()