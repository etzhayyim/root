from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BasketState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: BasketState): specs = state['specs']; required = ['material', 'dimensions', 'load_capacity']; state['valid'] = all(k in specs for k in required) and specs['load_capacity'] > 0; return state; graph = StateGraph(BasketState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()