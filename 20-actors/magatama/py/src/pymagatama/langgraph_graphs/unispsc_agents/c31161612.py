from langgraph.graph import StateGraph, END; from typing import TypedDict; class BoltState(TypedDict): vendor_specs: dict; compliance_score: float; def validate_dimensions(state: BoltState): return {'compliance_score': 1.0 if 'dimensions' in state['vendor_specs'] else 0.0}; def check_material_cert(state: BoltState): return {'compliance_score': state['compliance_score'] * 0.9}; graph = StateGraph(BoltState); graph.add_node('validate', validate_dimensions); graph.add_node('certify', check_material_cert); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph.set_entry_point('validate'); graph = graph.compile()