from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): vendor_id: str; doc_compliance: bool; material_spec: dict; audit_status: str; def validate_compliance(state: KitState): state['doc_compliance'] = True if 'ISO13485' in state['material_spec'] else False; return state; def finalize_order(state: KitState): state['audit_status'] = 'APPROVED' if state['doc_compliance'] else 'REJECTED'; return state; graph = StateGraph(KitState); graph.add_node('validate', validate_compliance); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()