from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): data: dict; log: Annotated[list, operator.add]; def validate_gmp(state: PharmState): state['log'].append('Validating GMP status...'); return state; def check_regulatory(state: PharmState): state['log'].append('Verifying regulatory approval...'); return state; graph = StateGraph(PharmState); graph.add_node('gmp', validate_gmp); graph.add_node('regulatory', check_regulatory); graph.add_edge('gmp', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('gmp'); graph = graph.compile()