from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SteeringState(TypedDict): linkage_specs: dict; validation_results: list[str]; graph = StateGraph(SteeringState); def validate_specs(state: SteeringState): return {'validation_results': ['CAD verified', 'Material stress-tested']}; def finalize_order(state: SteeringState): return {'validation_results': ['Workflow complete']}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()