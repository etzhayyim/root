from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TapeRemoverState(TypedDict): chemical_composition: str; batch_id: str; is_approved: bool; def validate_composition(state: TapeRemoverState): safe_ingredients = ['silicone', 'solvent']; state['is_approved'] = any(i in state['chemical_composition'].lower() for i in safe_ingredients); return state; def final_approval(state: TapeRemoverState): return {'is_approved': state['is_approved']}; graph = StateGraph(TapeRemoverState); graph.add_node('validate', validate_composition); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()