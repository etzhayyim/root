from langgraph.graph import StateGraph, END; from typing import TypedDict; class PomState(TypedDict): input: str; validated: bool; results: list; def validate_poms(state: PomState): state['validated'] = True; return state; graph = StateGraph(PomState); graph.add_node('validate', validate_poms); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()