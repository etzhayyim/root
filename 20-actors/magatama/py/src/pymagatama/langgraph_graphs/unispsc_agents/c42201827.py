from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_components: List[str]; inspection_passed: bool; cert_verified: bool; def check_components(state: KitState): return { "inspection_passed": "adhesive" in state["kit_components"] }; def verify_cert(state: KitState): return { "cert_verified": True }; graph = StateGraph(KitState); graph.add_node("inspect", check_components); graph.add_node("verify", verify_cert); graph.set_entry_point("inspect"); graph.add_edge("inspect", "verify"); graph.add_edge("verify", END); graph = graph.compile()