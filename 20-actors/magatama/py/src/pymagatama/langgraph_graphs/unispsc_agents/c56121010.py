from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KioskState(TypedDict): data: dict; status: str; def validate_specs(state: KioskState): state['status'] = 'validation_passed' if 'power_consumption_watts' in state['data'] else 'validation_failed'; return state; def deploy_kiosk(state: KioskState): state['status'] = 'deployed'; return state; graph = StateGraph(KioskState); graph.add_node('validate', validate_specs); graph.add_node('deploy', deploy_kiosk); graph.set_entry_point('validate'); graph.add_edge('validate', 'deploy'); graph.add_edge('deploy', END); graph = graph.compile()