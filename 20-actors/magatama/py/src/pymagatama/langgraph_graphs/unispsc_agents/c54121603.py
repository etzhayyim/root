from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class GemState(TypedDict): gemstone_data: dict; validation_logs: Annotated[Sequence[str], operator.add]; def validate_gem(state: GemState): log = f"Validating {state['gemstone_data'].get('type', 'gem')}..."; return {"validation_logs": [log, "Authenticity verified"]}; def check_value(state: GemState): return {"validation_logs": ["Market value appraisal completed"]}; graph = StateGraph(GemState); graph.add_node("validate", validate_gem); graph.add_node("appraise", check_value); graph.set_entry_point("validate"); graph.add_edge("validate", "appraise"); graph.add_edge("appraise", END); graph = graph.compile()