from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; specs: dict; validation_score: float; approved: bool; def validate_specs(state: ComponentState): return {'validation_score': 0.95 if 'material' in state['specs'] else 0.0}; def check_approval(state: ComponentState): return {'approved': state['validation_score'] > 0.9}; graph = StateGraph(ComponentState); graph.add_node('validate', validate_specs); graph.add_node('approve', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()