from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HistologyState(TypedDict): items: List[str]; validation_results: dict; def validate_kits(state: HistologyState): return {'validation_results': {item: 'passed' for item in state['items']}}; graph = StateGraph(HistologyState); graph.add_node('validate', validate_kits); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()