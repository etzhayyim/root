from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StepStoolState(TypedDict): stool_id: str; load_capacity: float; material: str; approved: bool; def validate_spec(state: StepStoolState): state['approved'] = state['load_capacity'] >= 100.0; return state; graph = StateGraph(StepStoolState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()