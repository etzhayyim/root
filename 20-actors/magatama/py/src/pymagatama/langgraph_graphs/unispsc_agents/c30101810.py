from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BrassState(TypedDict): input_data: dict; validation_report: list[str]; finished: bool; def validate_dimensions(state: BrassState): print('Validating dimension specs...'); return {'validation_report': ['Dimensions meet ASTM B36']}; def check_material_cert(state: BrassState): print('Verifying material composition...'); return {'validation_report': ['Composition verified']}; graph = StateGraph(BrassState); graph.add_node('validate', validate_dimensions); graph.add_node('certify', check_material_cert); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph.set_entry_point('validate'); graph = graph.compile()