from langgraph.graph import StateGraph, END; from typing import TypedDict; class ECGState(TypedDict): model: str; compliance: bool; status: str; def validate_spec(state: ECGState): state['compliance'] = True if len(state['model']) > 0 else False; return state; graph = StateGraph(ECGState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()