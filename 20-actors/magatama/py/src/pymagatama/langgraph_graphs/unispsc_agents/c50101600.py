from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FruitState(TypedDict): fruit_type: str; quality_score: float; needs_cold_chain: bool; log: List[str]; def validate_quality(state: FruitState): state['log'].append('Validating quality'); state['quality_score'] = 1.0; return state; def check_logistics(state: FruitState): state['needs_cold_chain'] = True; state['log'].append('Logistics verified'); return state; graph = StateGraph(FruitState); graph.add_node('validate', validate_quality); graph.add_node('logistics', check_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); compiled_graph = graph.compile()