from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HoopState(TypedDict): hoop_id: str; specifications: dict; compliance_status: bool; def validate_hoop(state: HoopState): state['compliance_status'] = 'diameter' in state['specifications'] and 'material' in state['specifications']; print(f'Validating hoop: {state['hoop_id']}'); return state; graph = StateGraph(HoopState); graph.add_node('validation', validate_hoop); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()