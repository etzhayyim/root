from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): tube_type: str; force_required: float; approved: bool; def validate_ergonomics(state: State): return {'approved': state['force_required'] < 2.0}; def build_workflow(): graph = StateGraph(State); graph.add_node('validate', validate_ergonomics); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = build_workflow()