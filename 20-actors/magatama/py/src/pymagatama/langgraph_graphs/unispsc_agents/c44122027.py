from langgraph.graph import StateGraph, END; from typing import TypedDict; class FileFolderState(TypedDict): input_data: dict; validation: bool; output: str; def validate_folder_specs(state: FileFolderState): return {'validation': True if 'expansion_capacity' in state['input_data'] else False}; def format_procurement(state: FileFolderState): return {'output': 'Specification verified for procurement process.'}; graph = StateGraph(FileFolderState); graph.add_node('validate', validate_folder_specs); graph.add_node('format', format_procurement); graph.add_edge('validate', 'format'); graph.add_edge('format', END); graph.set_entry_point('validate'); graph = graph.compile()