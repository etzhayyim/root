from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): doc_id: str; compliance_checked: bool; inspection_passed: bool; def validate_beryllium_docs(state: ProcurementState): return {'compliance_checked': True}; def perform_inspection(state: ProcurementState): return {'inspection_passed': True}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_beryllium_docs); graph.add_node('inspect', perform_inspection); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()