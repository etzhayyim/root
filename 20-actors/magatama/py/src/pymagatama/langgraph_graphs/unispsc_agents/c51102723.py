from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): chemical_name: str; safety_checks: List[str]; approved: bool; def check_compliance(state: ProcurementState): state['safety_checks'].append('Mercurial content validated'); state['approved'] = True; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()