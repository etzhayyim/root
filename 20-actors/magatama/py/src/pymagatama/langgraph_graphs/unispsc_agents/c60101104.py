from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class QuizMachineState(TypedDict): device_type: str; specs: dict; ready_for_procurement: bool; def validate_specs(state: QuizMachineState) -> QuizMachineState: state['ready_for_procurement'] = 'battery_specifications' in state['specs']; return state; def finalize_order(state: QuizMachineState) -> QuizMachineState: return state; graph = StateGraph(QuizMachineState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()