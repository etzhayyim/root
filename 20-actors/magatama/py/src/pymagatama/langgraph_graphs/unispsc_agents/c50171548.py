from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HerbProcurementState(TypedDict): herb_type: str; quality_score: float; is_approved: bool; def validate_quality(state: HerbProcurementState): return {'is_approved': state['quality_score'] > 0.8}; def route_delivery(state: HerbProcurementState): return 'complete'; graph = StateGraph(HerbProcurementState); graph.add_node('validate', validate_quality); graph.add_node('delivery', route_delivery); graph.set_entry_point('validate'); graph.add_edge('validate', 'delivery'); graph.add_edge('delivery', END); compiled_graph = graph.compile()