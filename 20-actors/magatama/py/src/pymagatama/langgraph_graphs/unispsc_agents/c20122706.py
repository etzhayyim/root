from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class GreaseState(TypedDict): input_data: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_viscosity(state: GreaseState): return {'validation_results': ['Viscosity within tolerance']}; def check_safety_compliance(state: GreaseState): return {'validation_results': ['SDS verified'], 'status': 'APPROVED'}; graph = StateGraph(GreaseState); graph.add_node('validate', validate_viscosity); graph.add_node('compliance', check_safety_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()