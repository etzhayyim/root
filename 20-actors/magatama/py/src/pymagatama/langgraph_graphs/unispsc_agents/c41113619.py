from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GalvanometerState(TypedDict): sensitivity: float; specs: dict; compliant: bool; def validate_specs(state: GalvanometerState): state['compliant'] = state['specs'].get('accuracy', 0) > 0.99; return state; def export_check(state: GalvanometerState): return 'dual_use' if state['specs'].get('high_precision', False) else 'standard'; graph = StateGraph(GalvanometerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()