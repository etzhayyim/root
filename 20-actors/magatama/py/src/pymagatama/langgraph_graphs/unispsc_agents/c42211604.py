from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class BathAidState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: BathAidState): state['compliance'] = 'material_composition' in state['specs'] and 'durability' in state['specs']; return state; def final_decision(state: BathAidState): state['approved'] = state['compliance']; return state; graph = StateGraph(BathAidState); graph.add_node('validate', validate_specs); graph.add_node('decision', final_decision); graph.set_entry_point('validate'); graph.add_edge('validate', 'decision'); graph.add_edge('decision', END); graph.compile()