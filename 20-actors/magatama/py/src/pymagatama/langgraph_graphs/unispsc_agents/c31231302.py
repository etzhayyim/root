from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CopperProcurementState(TypedDict): tubing_specs: dict; validation_results: List[str]; graph = StateGraph(CopperProcurementState); def validate_specs(state: CopperProcurementState): return {'validation_results': ['diameter_ok', 'grade_ok'] if state['tubing_specs'].get('pressure_rating') else ['error']}; def route_procurement(state: CopperProcurementState): return 'complete'; graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()