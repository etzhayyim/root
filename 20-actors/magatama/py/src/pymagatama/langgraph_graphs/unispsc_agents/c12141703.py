from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class MaterialState(TypedDict): material_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_material(state: MaterialState): result = {'valid': True, 'log': 'Chemical properties within acceptable ranges'} if 'purity' in state['material_data'] else {'valid': False, 'log': 'Missing purity certification'}; return {'validation_results': [result], 'status': 'validated' if result['valid'] else 'failed'}; def process_safety(state: MaterialState): return {'status': 'safety_checked'}; graph = StateGraph(MaterialState); graph.add_node('validate', validate_material); graph.add_node('safety', process_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()