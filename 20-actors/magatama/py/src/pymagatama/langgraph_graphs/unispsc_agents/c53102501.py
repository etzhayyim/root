from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ApparelState(TypedDict): vendor_certs: List[str]; material_spec: str; quality_score: float; approved: bool; def validate_spec(state: ApparelState): if 'ISO-105' in state['vendor_certs'] and state['quality_score'] > 0.8: return {'approved': True}; return {'approved': False}; graph = StateGraph(ApparelState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()