from langgraph.graph import StateGraph, END; from typing import TypedDict; class AircraftState(TypedDict): aircraft_type: str; is_airworthy: bool; export_licensed: bool; def validate_airworthiness(state: AircraftState): return {'is_airworthy': True} if state['aircraft_type'] == 'civil' else {'is_airworthy': False}; def check_export_status(state: AircraftState): return {'export_licensed': True}; graph = StateGraph(AircraftState); graph.add_node('validate', validate_airworthiness); graph.add_node('compliance', check_export_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.compile()