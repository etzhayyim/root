from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AshtraState(TypedDict): specs: dict; compliance_status: bool; def validate_material(state: AshtraState) -> AshtraState: state['compliance_status'] = state['specs'].get('material') in ['Stainless Steel', 'Ceramic']; return state; def check_fire_safety(state: AshtraState) -> AshtraState: if state['compliance_status']: state['compliance_status'] = state['specs'].get('fire_resistant', False); return state; graph = StateGraph(AshtraState); graph.add_node('validate_material', validate_material); graph.add_node('check_fire_safety', check_fire_safety); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_fire_safety'); graph.add_edge('check_fire_safety', END); graph = graph.compile()