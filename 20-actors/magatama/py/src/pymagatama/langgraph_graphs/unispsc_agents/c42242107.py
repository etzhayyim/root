from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TractionState(TypedDict): supply_id: str; compliance_docs: List[str]; inspection_status: str; def validate_compliance(state: TractionState): return {'inspection_status': 'passed' if len(state['compliance_docs']) > 2 else 'failed'}; def process_logistics(state: TractionState): return {'inspection_status': 'shipped'}; graph = StateGraph(TractionState); graph.add_node('validate', validate_compliance); graph.add_node('logistic', process_logistics); graph.add_edge('validate', 'logistic'); graph.add_edge('logistic', END); graph.set_entry_point('validate'); graph = graph.compile()