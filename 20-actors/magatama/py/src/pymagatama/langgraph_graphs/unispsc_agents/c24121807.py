from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): content: str; validated: bool; def validate_spec(state: State): return {'validated': 'UN-certified' in state['content']}; def finalize_order(state: State): return {'content': 'Order finalized for specs: ' + state['content']}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()