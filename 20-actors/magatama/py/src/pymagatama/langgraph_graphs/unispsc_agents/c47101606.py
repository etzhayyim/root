from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ChemState(TypedDict): chemical_name: str; p_value: float; hazard_log: List[str]; graph = StateGraph(ChemState); def validate_safety(state: ChemState): return {'hazard_log': ['SDS_VERIFIED'] if state['p_value'] < 5.0 else ['HAZARD_FLAG']}; def export_control(state: ChemState): return {'hazard_log': state['hazard_log'] + ['EXPORT_APPROVED']}; graph.add_node('safety', validate_safety); graph.add_node('export', export_control); graph.set_entry_point('safety'); graph.add_edge('safety', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()