from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FlightCommState(TypedDict): specs: dict; validation_results: List[str]; def validate_certs(state: FlightCommState): return {'validation_results': ['DO-178C_Validated']}; def check_export(state: FlightCommState): return {'validation_results': ['ITAR_Cleared']}; graph = StateGraph(FlightCommState); graph.add_node('CertCheck', validate_certs); graph.add_node('ExportCheck', check_export); graph.set_entry_point('CertCheck'); graph.add_edge('CertCheck', 'ExportCheck'); graph.add_edge('ExportCheck', END); graph = graph.compile()