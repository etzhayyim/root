from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmergencyCaseState(TypedDict): vendor_id: str; specs: dict; compliance_verified: bool; def validate_specs(state: EmergencyCaseState): state['compliance_verified'] = 'certificate' in state['specs']; return state; def check_medical_standards(state: EmergencyCaseState): return {'compliance_verified': True} if state['specs'].get('rating') == 'IP67' else {'compliance_verified': False}; graph = StateGraph(EmergencyCaseState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_medical_standards); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()