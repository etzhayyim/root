from langgraph.graph import StateGraph, END; from typing import TypedDict; class MachineToolState(TypedDict): spec_data: dict; validation_report: str; def validate_specs(state: MachineToolState): return {'validation_report': 'Spec validated against ISO-230 standards'} if state['spec_data'] else {'validation_report': 'Missing data'}; graph = StateGraph(MachineToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()