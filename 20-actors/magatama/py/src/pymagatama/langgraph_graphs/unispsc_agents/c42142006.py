from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalInstrumentState(TypedDict): instrument_id: str; quality_checks: List[str]; approved: bool; def validate_instrument(state: SurgicalInstrumentState): state['quality_checks'].append('ISO_COMPLIANCE_CHECK'); state['approved'] = True; return state; graph = StateGraph(SurgicalInstrumentState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()