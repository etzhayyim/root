from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MarketingState(TypedDict): content: str; validated: bool; metadata: dict; graph = StateGraph(MarketingState); def validate_material(state: MarketingState): return {'validated': 'branding' in state['content'].lower()}; def finalize_material(state: MarketingState): return {'metadata': {'status': 'approved'}}; graph.add_node('validate', validate_material); graph.add_node('finalize', finalize_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()