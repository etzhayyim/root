from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class StationeryState(TypedDict): items: List[str]; validated: bool; state = StationeryState(items=[], validated=False); def validate_specs(state: StationeryState): state['validated'] = len(state['items']) > 0; return state; graph = StateGraph(StationeryState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()