from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LCState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: LCState): status = 'Pass' if state['specs'].get('detector') else 'Fail'; return {'validation_results': [status]}; def perform_calibration(state: LCState): return {'validation_results': ['Calibration completed']}; graph = StateGraph(LCState); graph.add_node('validation', validate_specs); graph.add_node('calibration', perform_calibration); graph.set_entry_point('validation'); graph.add_edge('validation', 'calibration'); graph.add_edge('calibration', END); compiled_graph = graph.compile()