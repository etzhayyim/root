from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DinnerSetState(TypedDict): items: List[str]; validated: bool; inspection_passed: bool; def validate_materials(state: DinnerSetState): print('Validating food safety standards...'); return {'validated': True}; def perform_quality_check(state: DinnerSetState): print('Checking for breakage risks...'); return {'inspection_passed': True}; graph = StateGraph(DinnerSetState); graph.add_node('validate', validate_materials); graph.add_node('qc', perform_quality_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'qc'); graph.add_edge('qc', END); graph = graph.compile()