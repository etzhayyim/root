from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlantState(TypedDict): species: str; temp_logs: List[float]; qc_passed: bool; def validate_temperature(state: PlantState): state['qc_passed'] = all(t <= -18.0 for t in state['temp_logs']); return state; def check_quarantine(state: PlantState): return state; graph = StateGraph(PlantState); graph.add_node('validate_temp', validate_temperature); graph.add_node('check_quarantine', check_quarantine); graph.set_entry_point('validate_temp'); graph.add_edge('validate_temp', 'check_quarantine'); graph.add_edge('check_quarantine', END); graph = graph.compile()