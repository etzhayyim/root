from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): items: List[str]; inspection_passed: bool; state = SupplyState(items=['frozen_blueberries'], inspection_passed=False); def validate_temp(state: SupplyState): state['inspection_passed'] = True; return state; graph = StateGraph(SupplyState); graph.add_node('validate', validate_temp); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()