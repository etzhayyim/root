from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AxeState(TypedDict): specs: dict; valid: bool; error: str; def validate_spec(state: AxeState): state['valid'] = 'blade_material' in state['specs'] and 'head_hardness_hrc' in state['specs']; return state; builder = StateGraph(AxeState); builder.add_node('validate', validate_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()