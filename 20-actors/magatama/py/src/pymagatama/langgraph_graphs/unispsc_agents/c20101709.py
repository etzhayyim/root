from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MiningLubricationState(TypedDict): input_spec: dict; analysis_results: Annotated[Sequence[str], operator.add]; status: str; def validate_spec(state: MiningLubricationState): viscosity = state['input_spec'].get('viscosity_grade'); return {'analysis_results': [f'Viscosity verified: {viscosity}'], 'status': 'validated'}; def finalize_procurement(state: MiningLubricationState): return {'status': 'procurement_ready'}; graph = StateGraph(MiningLubricationState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()