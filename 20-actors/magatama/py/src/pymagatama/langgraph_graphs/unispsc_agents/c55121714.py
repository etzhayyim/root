from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PennantState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_spec(state: PennantState): errors = []; if not state['specs'].get('material_composition'): errors.append('Missing material'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(PennantState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()