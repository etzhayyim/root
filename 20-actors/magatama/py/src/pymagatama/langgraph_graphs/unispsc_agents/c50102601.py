from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FrozenFruitState(TypedDict): fruit_type: str; temp_celsius: float; compliance_check: bool; def validate_temp(state: FrozenFruitState): return {'compliance_check': state['temp_celsius'] <= -18}; def finalize_procurement(state: FrozenFruitState): return {'compliance_check': True}; graph = StateGraph(FrozenFruitState); graph.add_node('validate', validate_temp); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()