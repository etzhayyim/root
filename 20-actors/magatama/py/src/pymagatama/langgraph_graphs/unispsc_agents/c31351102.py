from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipeState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: PipeState): state['validation_results'].append('Material grade checked'); return state; def check_pressure(state: PipeState): state['validation_results'].append('Pressure rating verified'); return state; graph = StateGraph(PipeState); graph.add_node('validate', validate_material); graph.add_node('pressure_check', check_pressure); graph.set_entry_point('validate'); graph.add_edge('validate', 'pressure_check'); graph.add_edge('pressure_check', END); graph = graph.compile()