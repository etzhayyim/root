from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SurgicalSupplyState(TypedDict): supply_data: dict; validation_score: float; def validate_sterility(state: SurgicalSupplyState): state['validation_score'] = 1.0 if 'ISO11135' in state['supply_data'].get('certifications', []) else 0.0; return state; def check_compliance(state: SurgicalSupplyState): return 'valid' if state['validation_score'] > 0 else 'flag_for_review'; graph = StateGraph(SurgicalSupplyState); graph.add_node('validate', validate_sterility); graph.add_edge('validate', END); graph.set_entry_point('validate')