from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SeatingState(TypedDict): model_specs: dict; compliance_check: bool; verified: bool; def validate_specs(state: SeatingState): state['compliance_check'] = 'BIFMA' in state['model_specs'].get('certs', []); return state; def final_approve(state: SeatingState): state['verified'] = state['compliance_check']; return state; graph = StateGraph(SeatingState); graph.add_node('validate', validate_specs); graph.add_node('approve', final_approve); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()