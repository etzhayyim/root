from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_pressure_specs(state: ProcState): specs = state['input_data'].get('specs', {}); is_valid = 'pressure_range' in specs and 'flow_rate' in specs; return {'validation_results': ['Pressure specs validated' if is_valid else 'Missing pressure specs'], 'status': 'validated' if is_valid else 'error'}; def check_regulatory(state: ProcState): return {'validation_results': ['Regulatory docs verified']}; graph = StateGraph(ProcState); graph.add_node('spec_validator', validate_pressure_specs); graph.add_node('regulatory_check', check_regulatory); graph.set_entry_point('spec_validator'); graph.add_edge('spec_validator', 'regulatory_check'); graph.add_edge('regulatory_check', END); graph = graph.compile()