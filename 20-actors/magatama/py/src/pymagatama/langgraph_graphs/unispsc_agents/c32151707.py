from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PLCState(TypedDict): device_specs: dict; validation_results: list[str]; def validate_specs(state: PLCState): specs = state['device_specs']; results = ['Compatibility Verified'] if specs.get('model') else ['Model Missing']; return {'validation_results': results}; graph = StateGraph(PLCState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()