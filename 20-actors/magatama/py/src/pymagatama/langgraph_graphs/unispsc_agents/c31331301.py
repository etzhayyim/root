from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; pass_fail: bool; defect_logs: List[str]; def validate_welds(state: AssemblyState): state['pass_fail'] = state['specs'].get('weld_integrity') == 'passed'; return state; def check_compliance(state: AssemblyState): if state['pass_fail']: return 'complete'; return 'flag_for_review'; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_welds); graph.add_edge('validate', 'complete'); graph.set_entry_point('validate'); compile_graph = graph.compile()