from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): status: str; specs: dict; valid: bool; def validate_pharma(state: ProcureState): state['valid'] = state['specs'].get('purity', 0) >= 99.0; return state; def check_storage(state: ProcureState): state['valid'] = state['specs'].get('temp_range') == '2-8C'; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_pharma); graph.add_node('storage', check_storage); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph.set_entry_point('validate'); graph = graph.compile()