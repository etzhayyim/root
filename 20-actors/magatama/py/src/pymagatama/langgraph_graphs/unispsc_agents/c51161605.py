from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_name: str; validation_passed: bool; certs: List[str]; def validate_purity(state: ProcureState): state['validation_passed'] = True; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()