from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PenState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: PenState):    required = ['Ink Type', 'Point Size']; missing = [f for f in required if f not in state['specs']]; state['validation_errors'] = missing; state['approved'] = len(missing) == 0; return state; graph = StateGraph(PenState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()