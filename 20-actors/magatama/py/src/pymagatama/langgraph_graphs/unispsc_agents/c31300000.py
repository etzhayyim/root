from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ForgingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(ForgingState); def validate_machining(state: ForgingState): state['validation_results'].append('Dimensional check passed'); return state; def inspect_forging(state: ForgingState): state['validation_results'].append('NDT inspection complete'); return state; graph.add_node('validate', validate_machining); graph.add_node('inspect', inspect_forging); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); compiled_graph = graph.compile()