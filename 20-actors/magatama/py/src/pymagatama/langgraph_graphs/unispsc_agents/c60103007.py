from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): content: dict; approved: bool; def validate_content(state: State): return {'approved': 'curriculum_alignment' in state['content']}; graph = StateGraph(State); graph.add_node('validate', validate_content); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()