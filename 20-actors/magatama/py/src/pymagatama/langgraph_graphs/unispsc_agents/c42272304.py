from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ResuscitatorState(TypedDict): component_specs: dict; validation_results: list; status: str; def validate_specs(state: ResuscitatorState): results = []; required = ['ISO_13485', 'Biocompatibility']; for field in required: results.append(field in state['component_specs']); return {'validation_results': results, 'status': 'VALIDATED' if all(results) else 'REJECTED'}; graph = StateGraph(ResuscitatorState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()