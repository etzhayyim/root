from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DetectionState(TypedDict): specs: dict; valid: bool; error: List[str]; def validate_specs(state: DetectionState): s = state['specs']; state['valid'] = s.get('temp_threshold') > 0 and s.get('cert') is not None; return state; graph = StateGraph(DetectionState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()