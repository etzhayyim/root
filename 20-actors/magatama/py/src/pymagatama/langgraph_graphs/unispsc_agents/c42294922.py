from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LigatorState(TypedDict): device_id: str; quality_passed: bool; validation_log: List[str]; def validate_medical_device(state: LigatorState): state['validation_log'].append('Checking for ISO 13485 compliance...'); state['quality_passed'] = True; return state; graph = StateGraph(LigatorState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()