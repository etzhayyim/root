from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medicine_name: str; compliance_passed: bool; temp_check: bool; def validate_pharma(state: PharmaState): state['compliance_passed'] = True; return state; def check_storage(state: PharmaState): state['temp_check'] = True; return state; graph_builder = StateGraph(PharmaState); graph_builder.add_node('validate', validate_pharma); graph_builder.add_node('storage', check_storage); graph_builder.add_edge('validate', 'storage'); graph_builder.add_edge('storage', END); graph_builder.set_entry_point('validate'); graph = graph_builder.compile()