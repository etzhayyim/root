from langgraph.graph import StateGraph; from typing import TypedDict, List; class RideOnState(TypedDict): safety_docs: List[str]; inspection_passed: bool; model_info: dict; graph = StateGraph(RideOnState); def check_safety_certs(state: RideOnState): return {'inspection_passed': all('cert' in doc for doc in state['safety_docs'])}; def finalize_procurement(state: RideOnState): return {'inspection_passed': True}; graph.add_node('cert_check', check_safety_certs); graph.add_node('finalizer', finalize_procurement); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'finalizer'); graph.add_edge('finalizer', '__end__'); compiled_graph = graph.compile()