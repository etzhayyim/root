from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): pump_specs: dict; validation_log: Annotated[list, operator.add]; status: str; def validate_specs(state: PumpState): specs = state['pump_specs']; errors = []; if specs.get('flow', 0) <= 0: errors.append('Invalid flow rate'); return {'validation_log': errors, 'status': 'validated' if not errors else 'failed'}; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()