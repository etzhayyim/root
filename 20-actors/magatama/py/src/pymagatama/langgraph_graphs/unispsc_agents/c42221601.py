from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): sensor_data: dict; compliance_report: str; graph = StateGraph(State); def validate_spec(state: State): return {'compliance_report': 'Verified: IEC 60601 compliance pending'}; def analyze_bubble_threshold(state: State): return {'sensor_data': {'status': 'Calibration Required'}}; graph.add_node('validate', validate_spec); graph.add_node('analyze', analyze_bubble_threshold); graph.add_edge('validate', 'analyze'); graph.add_edge('analyze', END); graph.set_entry_point('validate'); app = graph.compile()