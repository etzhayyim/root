from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): active_ingredient: str; purity: float; compliance_verified: bool; def check_purity(state: ProcState): return {'compliance_verified': state['purity'] >= 99.0}; def validate_gmp(state: ProcState): return {'compliance_verified': True}; graph = StateGraph(ProcState); graph.add_node('verify_purity', check_purity); graph.add_node('gmp_audit', validate_gmp); graph.set_entry_point('verify_purity'); graph.add_edge('verify_purity', 'gmp_audit'); graph.add_edge('gmp_audit', END); compiled_graph = graph.compile()