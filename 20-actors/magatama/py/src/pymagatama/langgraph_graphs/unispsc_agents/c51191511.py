from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): input_data: dict; validation_results: List[str]; verified: bool; def validate_purity(state: PharmaState): state['validation_results'].append('Purity check complete'); state['verified'] = True; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()