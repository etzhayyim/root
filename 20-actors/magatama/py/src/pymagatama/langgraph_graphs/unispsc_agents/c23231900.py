from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_data: dict; validation_results: Annotated[list, operator.add]; def validate_component(state: ComponentState): cert = state['component_data'].get('iso_cert'); return {'validation_results': ['Certified'] if cert else ['Missing Certification']}; def check_specs(state: ComponentState): tol = state['component_data'].get('tolerance'); return {'validation_results': ['Tolerance OK'] if tol < 0.01 else ['Tolerance Out of Range']}; graph = StateGraph(ComponentState); graph.add_node('validate', validate_component); graph.add_node('specs', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'specs'); graph.add_edge('specs', END); graph = graph.compile()