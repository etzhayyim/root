from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): assay_results: dict; validated: bool; def validate_purity(state: PharmaState): state['validated'] = state['assay_results'].get('purity', 0) >= 99.0; return {'validated': state['validated']}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()