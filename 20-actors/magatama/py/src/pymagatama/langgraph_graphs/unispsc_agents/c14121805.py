from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class State(TypedDict): commodity_code: str; spec_data: dict; validation_report: list[str]; graph = StateGraph(State); def validate_material(state: State) -> State: state['validation_report'].append('Material verified for durability'); return state; def check_dimensions(state: State) -> State: state['validation_report'].append('Dimensions conform to ergonomic standards'); return state; graph.add_node('validate_material', validate_material); graph.add_node('check_dimensions', check_dimensions); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_dimensions'); graph.add_edge('check_dimensions', END); compiled_graph = graph.compile()