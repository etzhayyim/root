from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): stencil_specs: dict; validation_report: List[str]; def validate_materials(state: State): state['validation_report'].append('Checking material durability...'); return state; def check_scale_accuracy(state: State): state['validation_report'].append('Verifying scale precision...'); return state; graph = StateGraph(State); graph.add_node('material_check', validate_materials); graph.add_node('accuracy_check', check_scale_accuracy); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'accuracy_check'); graph.add_edge('accuracy_check', END); graph = graph.compile()