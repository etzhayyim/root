from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ZincState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_dimensions(state: ZincState): state['validation_errors'] = [] if state['specs'].get('tolerance') == 'standard' else ['Invalid tolerance']; state['approved'] = len(state['validation_errors']) == 0; return state; graph = StateGraph(ZincState); graph.add_node('validate', validate_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()