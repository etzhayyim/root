from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class DentalMercuryState(TypedDict): input_data: dict; validation_report: list; status: str; def validate_hazmat_docs(state: DentalMercuryState): docs = state['input_data'].get('docs', []); report = ['MSDS found'] if 'MSDS' in docs else ['Missing MSDS']; return {'validation_report': report}; def check_safety_protocols(state: DentalMercuryState): return {'status': 'COMPLIANT' if 'MSDS' in state['validation_report'] else 'FLAGGED'}; graph = StateGraph(DentalMercuryState); graph.add_node('validate', validate_hazmat_docs); graph.add_node('check', check_safety_protocols); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()