from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ActuatorState(TypedDict): input_spec: dict; validation_results: Annotated[list[str], operator.add]; status: str; def validate_spec(state: ActuatorState): results = []; if 'torque_rating_nm' not in state['input_spec']: results.append('Missing torque_rating_nm'); return {'validation_results': results}; def check_safety(state: ActuatorState): status = 'READY' if not state['validation_results'] else 'INVALID'; return {'status': status}; graph = StateGraph(ActuatorState); graph.add_node('validate', validate_spec); graph.add_node('safety_check', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()