from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SpectrographState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(SpectrographState); def validate_specs(state: SpectrographState): print('Validating spectral range and resolution...'); return {'validation_results': ['Specs Verified']}; def check_export_controls(state: SpectrographState): print('Checking dual-use compliance...'); return {'validation_results': ['Export Compliant']}; graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()