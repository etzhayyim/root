from langgraph.graph import StateGraph, END; from typing import TypedDict; class PrintInkState(TypedDict): ink_type: str; is_toxic: bool; approval_required: bool; def check_safety(state: PrintInkState): return {'approval_required': state['is_toxic']}; def finalize_order(state: PrintInkState): return {'approval_required': False}; graph = StateGraph(PrintInkState); graph.add_node('safety_check', check_safety); graph.add_node('finalize', finalize_order); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()