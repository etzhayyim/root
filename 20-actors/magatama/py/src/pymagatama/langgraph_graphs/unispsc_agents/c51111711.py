from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; temp_compliance: bool; audit_passed: bool; def check_purity(state: PharmState): state['audit_passed'] = state['purity'] >= 99.0; return state; def validate_storage(state: PharmState): state['temp_compliance'] = True; return state; graph = StateGraph(PharmState); graph.add_node('check_purity', check_purity); graph.add_node('validate_storage', validate_storage); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'validate_storage'); graph.add_edge('validate_storage', END); graph = graph.compile()