from langgraph.graph import StateGraph, END; from typing import TypedDict; class FenceState(TypedDict): specs: dict; valid: bool; def check_specs(state: FenceState): is_valid = 'wood_species' in state['specs'] and 'treatment' in state['specs']; return {'valid': is_valid}; graph = StateGraph(FenceState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()