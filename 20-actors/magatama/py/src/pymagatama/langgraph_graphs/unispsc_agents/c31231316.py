from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StoneSpecState(TypedDict): spec: dict; valid: bool; logs: List[str]; def validate_dimensions(state: StoneSpecState): spec = state['spec']; is_valid = 'inner_diameter' in spec and 'outer_diameter' in spec; return {'valid': is_valid, 'logs': ['Dimensions validated: ' + str(is_valid)]}; def check_material(state: StoneSpecState): return {'logs': state['logs'] + ['Material standard check completed']}; graph = StateGraph(StoneSpecState); graph.add_node('dimension_check', validate_dimensions); graph.add_node('material_check', check_material); graph.add_edge('dimension_check', 'material_check'); graph.add_edge('material_check', END); graph.set_entry_point('dimension_check'); graph = graph.compile()