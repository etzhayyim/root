from langgraph.graph import StateGraph, END; from typing import TypedDict; class LeatherTestState(TypedDict): spec_sheet: dict; status: str; def validate_spec(state: LeatherTestState): return {'status': 'validated' if 'iso_standard' in state['spec_sheet'] else 'rejected'}; def process_calibration(state: LeatherTestState): return {'status': 'calibrated'}; graph = StateGraph(LeatherTestState); graph.add_node('validate', validate_spec); graph.add_node('calibrate', process_calibration); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); graph.set_entry_point('validate'); graph = graph.compile()