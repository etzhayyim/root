from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class BinderState(TypedDict): dimensions: dict; material: str; approved: bool; def validate_specs(state: BinderState): return {'approved': state.get('material') in ['steel', 'nylon']}; graph = StateGraph(BinderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()