from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TransferSheetState(TypedDict): specs: dict; validation_passed: bool; warnings: List[str]; def validate_specs(state: TransferSheetState): state['validation_passed'] = 'thermal_tolerance' in state['specs'] and 'adhesive_type' in state['specs']; return state; def finalize_order(state: TransferSheetState): return {'validation_passed': state['validation_passed']}; graph = StateGraph(TransferSheetState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()