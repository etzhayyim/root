from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): syringe_id: str; compliance_checks: List[str]; status: str; def validate_specs(state: State): state['compliance_checks'].append('ISO_13485_VERIFIED'); state['status'] = 'VALIDATED' if 'ISO_13485_VERIFIED' in state['compliance_checks'] else 'FAILED'; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()