from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcessState(TypedDict): device_id: str; compliance_checked: bool; inspection_passed: bool; graph = StateGraph(ProcessState); def validate_equipment(state: ProcessState): return {'compliance_checked': True}; def perform_inspection(state: ProcessState): return {'inspection_passed': True}; graph.add_node('validate', validate_equipment); graph.add_node('inspect', perform_inspection); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()