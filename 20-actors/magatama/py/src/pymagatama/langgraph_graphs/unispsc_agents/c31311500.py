from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeAssemblyState(TypedDict): specs: dict; verified: bool; inspection_logs: List[str]; def validate_specs(state: PipeAssemblyState): return {'verified': all('mpa' in state['specs'], 'material' in state['specs'])}; def run_ndt_check(state: PipeAssemblyState): return {'inspection_logs': ['NDT inspection initiated', 'Pass']}; graph = StateGraph(PipeAssemblyState); graph.add_node('validate', validate_specs); graph.add_node('ndt', run_ndt_check); graph.add_edge('validate', 'ndt'); graph.add_edge('ndt', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()