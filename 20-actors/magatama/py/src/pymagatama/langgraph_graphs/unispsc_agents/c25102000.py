from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VehicleState(TypedDict): vehicle_id: str; compliance_docs: List[str]; inspection_status: str; def validate_compliance(state: VehicleState): return {'inspection_status': 'verified' if len(state['compliance_docs']) > 2 else 'pending'}; def check_export_controls(state: VehicleState): return {'inspection_status': 'restricted' if 'ITAR' not in state['compliance_docs'] else 'approved'}; graph = StateGraph(VehicleState); graph.add_node('validate', validate_compliance); graph.add_node('export_review', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()