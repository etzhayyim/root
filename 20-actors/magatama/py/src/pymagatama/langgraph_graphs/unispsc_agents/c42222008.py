from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InfusionState(TypedDict): doc_id: str; compliance_checked: bool; supply_chain_verified: bool; def check_compliance(state: InfusionState): return {'compliance_checked': True}; def verify_supply(state: InfusionState): return {'supply_chain_verified': True}; graph = StateGraph(InfusionState); graph.add_node('compliance', check_compliance); graph.add_node('supply', verify_supply); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'supply'); graph.add_edge('supply', END); graph = graph.compile()