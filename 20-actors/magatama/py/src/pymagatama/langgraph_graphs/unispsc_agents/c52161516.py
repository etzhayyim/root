from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DeviceProcessingState(TypedDict): device_id: str; specs: dict; valid: bool; def validate_specs(state: DeviceProcessingState): state['valid'] = 'HDCP' in state['specs'] and 'region' in state['specs']; return state; def check_compliance(state: DeviceProcessingState): print(f'Checking compliance for {state['device_id']}'); return state; graph = StateGraph(DeviceProcessingState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()