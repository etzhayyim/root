from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WheelchairState(TypedDict): accessory_type: str; specs: dict; compliant: bool; def validate_compatibility(state: WheelchairState) -> WheelchairState: state['compliant'] = state['specs'].get('weight_rating', 0) > 0; return state; def check_safety(state: WheelchairState) -> WheelchairState: return state; builder = StateGraph(WheelchairState); builder.add_node('validate', validate_compatibility); builder.add_node('safety', check_safety); builder.set_entry_point('validate'); builder.add_edge('validate', 'safety'); builder.add_edge('safety', END); graph = builder.compile()