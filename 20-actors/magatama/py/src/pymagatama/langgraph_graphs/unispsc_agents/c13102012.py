from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GasState(TypedDict): commodity: str; pressure: float; safety_check: bool; output: str; def validate_pressure(state: GasState): state['safety_check'] = state['pressure'] < 5000; return state; def check_quality(state: GasState): state['output'] = 'Quality Approved' if state['safety_check'] else 'Pressure Alert'; return state; graph = StateGraph(GasState); graph.add_node('validate', validate_pressure); graph.add_node('quality', check_quality); graph.add_edge('validate', 'quality'); graph.add_edge('quality', END); graph.set_entry_point('validate'); graph = graph.compile()