from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BastingState(TypedDict): tasks: Annotated[list, operator.add]; validation_status: str; def validate_tool(state: BastingState): return {'validation_status': 'passed'}; def finalize_order(state: BastingState): return {'tasks': ['order_finalized']}; graph = StateGraph(BastingState); graph.add_node('validate', validate_tool); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()