from langgraph.graph import StateGraph, END; from typing import TypedDict; class VehicleState(TypedDict): vehicle_id: str; specs: dict; approved: bool; def validate_specs(state: VehicleState): state['approved'] = 'emission_standard' in state['specs'] and state['specs']['safety_rating'] >= 5; return state; graph = StateGraph(VehicleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()