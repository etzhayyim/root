from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LeadComponentState(TypedDict): component_id: str; purity_levels: List[float]; validation_passed: bool; def validate_lead_purity(state: LeadComponentState): return {'validation_passed': all(p >= 0.99 for p in state['purity_levels'])}; def check_density(state: LeadComponentState): return {'validation_passed': state.get('validation_passed', False)}; graph = StateGraph(LeadComponentState); graph.add_node('validate_purity', validate_lead_purity); graph.add_node('check_density', check_density); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_density'); graph.add_edge('check_density', END); graph = graph.compile()