from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): chemical_name: str; purity: float; cold_chain_verified: bool; compliance: List[str]; def validate_purity(state: PharmState): state['compliance'].append('Purity Verified') if state['purity'] >= 99.0 else state['compliance'].append('Rejected'); return state; def check_chain(state: PharmState): state['cold_chain_verified'] = True; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('cold_chain', check_chain); graph.set_entry_point('cold_chain'); graph.add_edge('cold_chain', 'validate'); graph.add_edge('validate', END); graph = graph.compile()