from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UniformState(TypedDict): vendor_id: str; item_specs: dict; compliance_check: bool; workflow_log: List[str]; def validate_design(state: UniformState): state['compliance_check'] = 'insignia' in state['item_specs'] and state['item_specs']['rank'] != ''; state['workflow_log'].append('Rank design validated'); return state; def check_materials(state: UniformState): state['compliance_check'] = state['item_specs'].get('durability') == 'high'; state['workflow_log'].append('Material quality checked'); return state; graph = StateGraph(UniformState); graph.add_node('validate', validate_design); graph.add_node('materials', check_materials); graph.set_entry_point('validate'); graph.add_edge('validate', 'materials'); graph.add_edge('materials', END); graph = graph.compile()