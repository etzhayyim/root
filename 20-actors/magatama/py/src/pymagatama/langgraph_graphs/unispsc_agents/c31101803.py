from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: CastingState): state['validation_results'].append('Material verified'); return state; def check_geometry(state: CastingState): state['validation_results'].append('Geometry validated'); return state; graph = StateGraph(CastingState); graph.add_node('material_check', validate_material); graph.add_node('geometry_check', check_geometry); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'geometry_check'); graph.add_edge('geometry_check', END); graph = graph.compile()