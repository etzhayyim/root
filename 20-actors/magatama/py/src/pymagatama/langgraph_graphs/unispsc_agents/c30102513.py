from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TinSheetState(TypedDict): dimensions: dict; quality_specs: dict; approved: bool; def validate_specs(state: TinSheetState): state['approved'] = state['quality_specs'].get('thickness', 0) > 0; return state; graph = StateGraph(TinSheetState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()