from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SandState(TypedDict): data: dict; errors: List[str]; graph = StateGraph(SandState); def validate_spec(state: SandState): state['errors'] = [] if 'accuracy' not in state['data'] else []; return state; def run_analysis(state: SandState): print('Processing sand test parameters...'); return state; graph.add_node('validate', validate_spec); graph.add_node('analyze', run_analysis); graph.set_entry_point('validate'); graph.add_edge('validate', 'analyze'); graph.add_edge('analyze', END); graph.compile()