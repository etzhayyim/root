from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): items: List[str]; inspection_passed: bool; def validate_specs(state: SpecState): state['inspection_passed'] = all(len(item) > 0 for item in state['items']); return state; graph = StateGraph(SpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()