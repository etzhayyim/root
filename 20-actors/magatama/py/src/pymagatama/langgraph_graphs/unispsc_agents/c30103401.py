from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AlloyState(TypedDict): purity_level: float; composition: dict; is_certified: bool; approved: bool; def check_purity(state: AlloyState): state['approved'] = state['purity_level'] >= 99.7; return state; graph = StateGraph(AlloyState); graph.add_node('verify_specs', check_purity); graph.set_entry_point('verify_specs'); graph.add_edge('verify_specs', END); app = graph.compile()