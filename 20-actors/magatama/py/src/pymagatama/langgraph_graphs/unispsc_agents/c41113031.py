from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SaccharometerState(TypedDict): data: dict; specs_approved: bool; def validate_specs(state: SaccharometerState): return {'specs_approved': state['data'].get('accuracy', 0) <= 0.1}; def check_calibration(state: SaccharometerState): return {'specs_approved': state['data'].get('calibration_cert') is not None}; graph = StateGraph(SaccharometerState); graph.add_node('validation', validate_specs); graph.add_node('calibration', check_calibration); graph.set_entry_point('validation'); graph.add_edge('validation', 'calibration'); graph.add_edge('calibration', END); compiled_graph = graph.compile()