from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DWDMState(TypedDict): specs: dict; compliance_status: bool; def validate_specs(state: DWDMState): return {'compliance_status': state['specs'].get('insertion_loss', 1) < 0.5}; def export_check(state: DWDMState): return {'compliance_status': 'dual-use' in state.get('tags', [])}; graph = StateGraph(DWDMState); graph.add_node('validate', validate_specs); graph.add_node('export_review', export_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph = graph.compile()