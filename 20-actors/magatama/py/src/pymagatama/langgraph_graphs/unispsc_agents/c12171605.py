from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): chemical_spec: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_purity(state: CatalystState): purity = state['chemical_spec'].get('purity', 0); return {'validation_results': ['Purity verified' if purity > 0.99 else 'Purity failed'], 'status': 'IN_PROGRESS'}; def check_safety(state: CatalystState): return {'validation_results': ['SDS and Dual-use check completed'], 'status': 'READY'}; graph = StateGraph(CatalystState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()