from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: ForgingState): state['validation_results'].append('Material grade verification complete'); return state; def inspect_dimensions(state: ForgingState): state['validation_results'].append('Dimensional tolerance check passed'); return state; graph = StateGraph(ForgingState); graph.add_node('material_check', validate_materials); graph.add_node('dimension_check', inspect_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dimension_check'); graph.add_edge('dimension_check', END); graph = graph.compile()