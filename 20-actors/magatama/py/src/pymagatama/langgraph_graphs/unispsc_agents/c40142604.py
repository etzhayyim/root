from langgraph.graph import StateGraph, END; from typing import TypedDict; class PipelineState(TypedDict): spec: dict; valid: bool; error: str; def validate_spec(state: PipelineState): pressure = state['spec'].get('pressure_rating_mpa', 0); state['valid'] = pressure > 0; state['error'] = '' if state['valid'] else 'Invalid pressure rating'; return state; graph = StateGraph(PipelineState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()