from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): equipment_id: str; validation_status: bool; security_clearance: bool; graph = StateGraph(State); def validate_equipment(state: State): state['validation_status'] = True if state['equipment_id'].startswith('BX') else False; return state; def verify_security(state: State): state['security_clearance'] = True; return state; graph.add_node('validate', validate_equipment); graph.add_node('security', verify_security); graph.add_edge('validate', 'security'); graph.add_edge('security', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()