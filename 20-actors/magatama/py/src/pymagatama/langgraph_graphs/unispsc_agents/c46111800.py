from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NavalProcurementState(TypedDict): vendor_id: str; compliance_docs: List[str]; approved: bool; def validate_security(state: NavalProcurementState): state['approved'] = all(doc in ['ITAR_Cert', 'EUC'] for doc in state['compliance_docs']); return state; graph = StateGraph(NavalProcurementState); graph.add_node('security_check', validate_security); graph.set_entry_point('security_check'); graph.add_edge('security_check', END); graph = graph.compile()