from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrillHoseState(TypedDict): data: dict; validations: List[str]; graph = StateGraph(DrillHoseState); def validate_spec(state: DrillHoseState): state['validations'] = ['Pressure Rating Check', 'Material Compliance'] if state['data'].get('pressure') else ['Error']; return state; def final_check(state: DrillHoseState): return state; graph.add_node('validate', validate_spec); graph.add_node('final', final_check); graph.add_edge('validate', 'final'); graph.add_edge('final', END); graph.set_entry_point('validate'); app = graph.compile()