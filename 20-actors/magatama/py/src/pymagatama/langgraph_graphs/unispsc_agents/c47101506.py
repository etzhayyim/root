from langgraph.graph import StateGraph, END; from typing import TypedDict; class CorrosionState(TypedDict): equipment_type: str; specs: dict; compliant: bool; def validate_specs(state: CorrosionState): state['compliant'] = 'ISO' in state['specs'].get('standard', '') ; return state; graph = StateGraph(CorrosionState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()