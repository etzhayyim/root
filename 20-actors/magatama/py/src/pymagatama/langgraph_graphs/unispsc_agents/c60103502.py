from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): doc_id: str; content: str; validated: bool; metadata: dict; graph = StateGraph(ProcurementState); def validate_metadata(state: ProcurementState): state['validated'] = 'ISBN' in state['metadata'] and 'Agency Source' in state['metadata']; return state; def finalize_order(state: ProcurementState): return {'validated': state['validated']}; graph.add_node('validate', validate_metadata); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()