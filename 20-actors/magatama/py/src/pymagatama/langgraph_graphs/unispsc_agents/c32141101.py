from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(SpecState); def validate_material(state: SpecState): return {'validation_results': ['Material purity verified']}; def validate_geometry(state: SpecState): return {'validation_results': ['Geometry within tolerance']}; graph.add_node('material_check', validate_material); graph.add_node('geo_check', validate_geometry); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'geo_check'); graph.add_edge('geo_check', END); compiled_graph = graph.compile()