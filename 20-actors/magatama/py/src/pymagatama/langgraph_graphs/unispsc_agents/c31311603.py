from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeSpecs(TypedDict): material_cert: str; weld_audit: bool; export_check: bool; def validate_specs(state: PipeSpecs): return {'weld_audit': True} if 'Hastelloy' in state['material_cert'] else {'weld_audit': False}; def compliance_check(state: PipeSpecs): return {'export_check': True}; graph = StateGraph(PipeSpecs); graph.add_node('validation', validate_specs); graph.add_node('compliance', compliance_check); graph.set_entry_point('validation'); graph.add_edge('validation', 'compliance'); graph.add_edge('compliance', END); graph.compile()