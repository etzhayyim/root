from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validation_status: bool; def validate_grade(state: ProcState): state['validation_status'] = 'ASTM' in state['items']; return state; def check_cert(state: ProcState): return 'certified' if state['validation_status'] else 'rejected'; graph = StateGraph(ProcState); graph.add_node('validate', validate_grade); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()