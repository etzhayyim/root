from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AxleScaleState(TypedDict): capacity: float; compliance_cert: bool; calibration_valid: bool; status: str; def validate_specs(state: AxleScaleState): return {'status': 'VALIDED' if state['compliance_cert'] and state['calibration_valid'] else 'REJECTED'}; def generate_report(state: AxleScaleState): return {'status': 'REPORT_GENERATED'}; graph = StateGraph(AxleScaleState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()