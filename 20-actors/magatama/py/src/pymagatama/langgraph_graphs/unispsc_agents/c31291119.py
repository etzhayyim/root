from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_geometry(state: GraphState): return {'validation_results': ['CAD_Check_Pass']}; def verify_material(state: GraphState): return {'validation_results': ['Material_Compliance_Pass']}; graph = StateGraph(GraphState); graph.add_node('validate_geometry', validate_geometry); graph.add_node('verify_material', verify_material); graph.set_entry_point('validate_geometry'); graph.add_edge('validate_geometry', 'verify_material'); graph.add_edge('verify_material', END); graph = graph.compile()