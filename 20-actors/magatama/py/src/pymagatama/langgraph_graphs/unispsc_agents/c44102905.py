from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): items: List[dict]; inspection_passed: bool; def check_moisture_capacity(state: SupplyState): state['inspection_passed'] = all(item.get('capacity', 0) > 20 for item in state['items']); return state; graph = StateGraph(SupplyState); graph.add_node('validate', check_moisture_capacity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()