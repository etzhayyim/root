from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DissectionSpecState(TypedDict): spec_data: dict; validation_errors: List[str]; def validate_material(state: DissectionSpecState): if 'material' not in state['spec_data']: state['validation_errors'].append('Material missing'); return state; def check_dimensions(state: DissectionSpecState): if state['spec_data'].get('width', 0) <= 0: state['validation_errors'].append('Invalid dimensions'); return state; graph = StateGraph(DissectionSpecState); graph.add_node('validate_material', validate_material); graph.add_node('check_dimensions', check_dimensions); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_dimensions'); graph.add_edge('check_dimensions', END); app = graph.compile()