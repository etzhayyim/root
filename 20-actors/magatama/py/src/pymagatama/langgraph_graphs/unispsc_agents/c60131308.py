from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CelloState(TypedDict): model: str; quality_score: int; requires_appraisal: bool; def validate_instrument(state: CelloState): state['requires_appraisal'] = state['quality_score'] > 80; return state; def finalize_procurement(state: CelloState): return {'procurement_validated': True}; graph = StateGraph(CelloState); graph.add_node('validate', validate_instrument); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()