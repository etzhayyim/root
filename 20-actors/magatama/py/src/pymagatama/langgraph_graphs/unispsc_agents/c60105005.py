from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EduMaterialState(TypedDict): content: str; validation_logs: List[str]; approved: bool; def val_step(state: EduMaterialState): state['validation_logs'].append('Checking transparency material specs'); return {'approved': True if 'physical_data' in state['content'] else False}; graph = StateGraph(EduMaterialState); graph.add_node('validate', val_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()