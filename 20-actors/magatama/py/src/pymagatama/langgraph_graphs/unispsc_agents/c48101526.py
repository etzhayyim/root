from langgraph.graph import StateGraph, END; from typing import TypedDict; class WaffleState(TypedDict): specs: dict; approved: bool; def validate_specs(state: WaffleState): state['approved'] = 'power_watts' in state['specs'] and 'material' in state['specs']; return state; graph = StateGraph(WaffleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()