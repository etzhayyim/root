from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IronCastingState(TypedDict): part_id: str; specifications: dict; compliance_status: bool; def validate_specs(state: IronCastingState): return {'compliance_status': 'tensile_strength' in state['specifications']}; def approve_casting(state: IronCastingState): return {'compliance_status': True}; graph = StateGraph(IronCastingState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_casting); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()