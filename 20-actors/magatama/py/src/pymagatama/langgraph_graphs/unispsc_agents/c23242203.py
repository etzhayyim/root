from langgraph.graph import StateGraph, END; from typing import TypedDict; class MachineState(TypedDict): specs: dict; valid: bool; def validate_specs(state: MachineState): state['valid'] = all(k in state['specs'] for k in ['diameter', 'precision', 'interface']); return state; graph = StateGraph(MachineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()