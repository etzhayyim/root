from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): chemical_data: dict; compliance_checked: bool; validation_log: List[str]; def validate_purity(state: ProcurementState): purity = state['chemical_data'].get('purity', 0); state['compliance_checked'] = purity >= 99.0; state['validation_log'].append(f'Purity check at {purity}%'); return state; def check_hazmat(state: ProcurementState): state['validation_log'].append('Hazmat verification complete'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_purity); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()