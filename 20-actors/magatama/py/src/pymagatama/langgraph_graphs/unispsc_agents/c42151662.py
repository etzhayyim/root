from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): input_data: dict; validation_report: list[str]; approved: bool; def validate_specs(state: DentalState): report = []; if 'iso_cert' not in state['input_data']: report.append('Missing ISO 13485'); return {'validation_report': report}; def check_approval(state: DentalState): return {'approved': len(state['validation_report']) == 0}; graph = StateGraph(DentalState); graph.add_node('validate', validate_specs); graph.add_node('approval', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph = graph.compile()