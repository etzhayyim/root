from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UpholsteryNailState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(UpholsteryNailState); def validate_spec(state: UpholsteryNailState): return {'validation_results': ['Material check passed', 'Dimension tolerance confirmed']}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()