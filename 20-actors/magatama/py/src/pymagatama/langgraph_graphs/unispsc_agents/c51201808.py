from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_data: dict; approval_status: bool; workflow_log: Annotated[list, operator.add]; def validate_purity(state: ProcureState): p = state['chemical_data'].get('purity', 0); return {'approval_status': p >= 99.0, 'workflow_log': ['Purity check passed' if p >= 99.0 else 'Purity failed']}; def finalize_procurement(state: ProcureState): return {'workflow_log': ['Procurement finalized']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_purity); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()