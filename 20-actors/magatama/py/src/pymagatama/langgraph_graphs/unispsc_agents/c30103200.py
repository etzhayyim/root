from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GratingState(TypedDict): specs: dict; validation_report: List[str]; def validate_load(state): return {'validation_report': state['validation_report'] + ['Load validation complete']} if state['specs'].get('load_bearing') else {'validation_report': state['validation_report'] + ['Missing load specs']}; def check_material(state): return {'validation_report': state['validation_report'] + ['Material compliance checked']}; graph = StateGraph(GratingState); graph.add_node('validate_load', validate_load); graph.add_node('check_material', check_material); graph.set_entry_point('validate_load'); graph.add_edge('validate_load', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()