from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MediaSystemState(TypedDict): input_data: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: MediaSystemState): errors = []; if 'io_config' not in state['input_data']: errors.append('Missing IO configuration'); return {'validation_errors': errors, 'approved': len(errors) == 0}; def finalize_procurement(state: MediaSystemState): return {'approved': True}; graph = StateGraph(MediaSystemState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.set_entry_point('validate'); graph.add_edge('finalize', END); compiled_graph = graph.compile()