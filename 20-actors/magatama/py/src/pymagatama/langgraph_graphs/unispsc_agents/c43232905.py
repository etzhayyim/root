from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkSoftwareState(TypedDict): vendor_info: str; comp_matrix: List[str]; validation_status: bool; def validate_compliance(state: NetworkSoftwareState): state['validation_status'] = True if 'ISO27001' in state['comp_matrix'] else False; return {'validation_status': state['validation_status']}; def build_graph(): graph = StateGraph(NetworkSoftwareState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = build_graph()