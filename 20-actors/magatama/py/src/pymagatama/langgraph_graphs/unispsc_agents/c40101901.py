from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VaporizerState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: VaporizerState): specs = state['specs']; state['valid'] = 'pressure' in specs and 'material' in specs; return state; graph = StateGraph(VaporizerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()