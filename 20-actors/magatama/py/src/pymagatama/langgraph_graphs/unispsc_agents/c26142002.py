from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MagnetState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: MagnetState): errors = []; if state['specs'].get('tesla', 0) > 10: errors.append('High intensity permit required'); return {'validation_errors': errors}; def finalize(state: MagnetState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(MagnetState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()