from langgraph.graph import StateGraph, END; from typing import TypedDict; class BleachState(TypedDict): chemical_composition: str; safety_compliant: bool; def validate_sds(state: BleachState): return {'safety_compliant': True if 'hypochlorite' in state['chemical_composition'].lower() else False}; def route_by_compliance(state: BleachState): return 'approve' if state['safety_compliant'] else END; graph = StateGraph(BleachState); graph.add_node('validate', validate_sds); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()