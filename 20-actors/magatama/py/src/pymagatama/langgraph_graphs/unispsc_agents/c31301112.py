from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; valid: bool; log: List[str]; def analyze_specs(state: ForgingState): state['log'].append('Validating material grade and hardness...'); state['valid'] = 'grade' in state['specs'] and 'hardness' in state['specs']; return state; builder = StateGraph(ForgingState); builder.add_node('analysis', analyze_specs); builder.set_entry_point('analysis'); builder.add_edge('analysis', END); graph = builder.compile()