from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChemState(TypedDict): chemical_name: str; specs: dict; valid: bool; def validate_purity(state: ChemState): state['valid'] = state['specs'].get('purity', 0) >= 99.0; return state; graph = StateGraph(ChemState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()