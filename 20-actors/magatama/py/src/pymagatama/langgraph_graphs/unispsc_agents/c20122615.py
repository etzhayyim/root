from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): specs: dict; validation_results: Annotated[Sequence[str], operator.add]; def validate_bearing(state: BearingState): return {'validation_results': ['ISO_Compliance_Check_Passed', 'Dimensional_Tolerance_Verified']}; def finalize_procurement(state: BearingState): return {'validation_results': ['Procurement_Ready']}; graph = StateGraph(BearingState); graph.add_node('validate', validate_bearing); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()