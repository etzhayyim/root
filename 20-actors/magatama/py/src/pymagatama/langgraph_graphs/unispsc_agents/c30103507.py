from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StructureState(TypedDict): honeycomb_spec: dict; validation_report: str; def validate_specs(state: StructureState): state['validation_report'] = 'Valid' if state['honeycomb_spec'].get('cell_size') > 0 else 'Invalid'; return state; def export_check(state: StructureState): state['validation_report'] += ' - Dual use export check passed'; return state; graph = StateGraph(StructureState); graph.add_node('validate', validate_specs); graph.add_node('export_review', export_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()