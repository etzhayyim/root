from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validated: bool; graph = StateGraph(State); def validate_specs(state: State): state['validated'] = 'resolution' in state['input_data'] and 'frame_rate' in state['input_data']; return state; graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()