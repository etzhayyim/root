from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): material_data: dict; compliance_verified: bool; def validate_lead_specs(state: State): state['compliance_verified'] = 'purity' in state['material_data'] and state['material_data']['purity'] >= 99.9; return state; def check_hazmat(state: State): state['is_hazmat'] = True; return state; graph = StateGraph(State); graph.add_node('validate', validate_lead_specs); graph.add_node('hazmat_check', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat_check'); graph.add_edge('hazmat_check', END); compiled_graph = graph.compile()