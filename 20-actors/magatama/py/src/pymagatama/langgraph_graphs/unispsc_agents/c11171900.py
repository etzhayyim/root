from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProcessorState(TypedDict): input_material: str; purity_level: float; status: str; logs: Annotated[Sequence[str], operator.add]; def validate_purity(state: ProcessorState): is_valid = state['purity_level'] >= 99.9; return {'status': 'validated' if is_valid else 'rejected', 'logs': [f'Purity check: {is_valid} for {state['input_material']}']}; def chemical_refine(state: ProcessorState): return {'status': 'refined', 'logs': ['Process: Thermal stabilization']}; graph = StateGraph(ProcessorState); graph.add_node('validate', validate_purity); graph.add_node('refine', chemical_refine); graph.add_edge('validate', 'refine'); graph.add_edge('refine', END); graph.set_entry_point('validate'); graph = graph.compile()