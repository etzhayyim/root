from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FitnessState(TypedDict): equipment_type: str; specs: dict; validation_errors: List[str]; def validate_safety(state: FitnessState): state['validation_errors'] = [] if 'ISO 20957' not in state['specs'].get('certifications', []) else []; return state; def route_inspection(state: FitnessState): return 'end' if not state['validation_errors'] else 'end'; graph = StateGraph(FitnessState); graph.add_node('safety_check', validate_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()