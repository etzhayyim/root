from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_license: bool; radiation_compliance: bool; status: str; def check_license(state: ProcState): state['vendor_license'] = True; return {'status': 'LICENSE_VERIFIED'}; def verify_compliance(state: ProcState): state['radiation_compliance'] = True; return {'status': 'COMPLIANCE_VERIFIED'}; graph = StateGraph(ProcState); graph.add_node('verify_license', check_license); graph.add_node('verify_compliance', verify_compliance); graph.set_entry_point('verify_license'); graph.add_edge('verify_license', 'verify_compliance'); graph.add_edge('verify_compliance', END); graph = graph.compile()