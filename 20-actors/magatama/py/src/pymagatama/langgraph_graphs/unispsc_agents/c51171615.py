from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity_check: bool; standard_compliance: str; def validate_purity(state: PharmaState): print(f'Validating {state[chemical_name]} purity...'); return {'purity_check': True}; def check_regulations(state: PharmaState): return {'standard_compliance': 'USP/EP'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()