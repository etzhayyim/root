from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input_spec: str; validation_status: bool; cert_ok: bool; def validate_instrument(state: ProcState): return {'validation_status': 'Calibration Certificate' in state['input_spec']}; def check_compliance(state: ProcState): return {'cert_ok': True}; graph = StateGraph(ProcState); graph.add_node('validate', validate_instrument); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()