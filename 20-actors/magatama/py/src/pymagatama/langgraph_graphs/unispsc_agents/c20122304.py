from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class SensorState(TypedDict): sensor_id: str; specs: dict; validation_results: Annotated[Sequence[str], operator.add]; graph = StateGraph(SensorState); def validate_sensor(state: SensorState): return {'validation_results': ['Voltage Check Passed', 'Range Calibration Validated']}; def finalize_spec(state: SensorState): return {'validation_results': ['Documentation Complete']}; graph.add_node('validate', validate_sensor); graph.add_node('finalize', finalize_spec); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()