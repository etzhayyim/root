from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CableState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: CableState): min_temp = state['specs'].get('min_temp', 200); state['validation_passed'] = min_temp >= 200; return state; graph = StateGraph(CableState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()