from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class DrillState(TypedDict): bit_specs: Dict; inspection_results: List[str]; approved: bool; def validate_drill_bit(state: DrillState): bit_specs = state['bit_specs']; results = []; approved = True; if bit_specs.get('hardness', 0) < 60: results.append('Insufficient hardness'); approved = False; return {'inspection_results': results, 'approved': approved}; graph = StateGraph(DrillState); graph.add_node('validate', validate_drill_bit); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()