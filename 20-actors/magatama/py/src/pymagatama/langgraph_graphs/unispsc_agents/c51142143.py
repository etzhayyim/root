from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_id: str; quality_checks: List[str]; approved: bool; def validate_purity(state: ProcState): state['quality_checks'].append('purity_verified'); state['approved'] = True; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()