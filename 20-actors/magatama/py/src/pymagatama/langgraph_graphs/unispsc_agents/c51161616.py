from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): medicine_name: str; dosage: str; compliance_check: bool; def validate_gmp(state: DrugState): return {'compliance_check': True} if state['medicine_name'] == 'Betahistine' else {'compliance_check': False}; def final_log(state: DrugState): print(f'Processing {state[medicine_name]} for delivery.'); return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_gmp); graph.add_node('log', final_log); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.set_entry_point('validate'); graph = graph.compile()