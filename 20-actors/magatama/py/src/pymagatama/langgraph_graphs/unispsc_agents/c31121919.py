from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GraphiteState(TypedDict): mold_specs: dict; validation_results: list; status: str; def validate_graphite(state: GraphiteState): specs = state['mold_specs']; results = []; if specs.get('density', 0) < 1.7: results.append('Density below 1.7 g/cc'); return {'validation_results': results, 'status': 'PASS' if not results else 'FAIL'}; graph = StateGraph(GraphiteState); graph.add_node('validate', validate_graphite); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()