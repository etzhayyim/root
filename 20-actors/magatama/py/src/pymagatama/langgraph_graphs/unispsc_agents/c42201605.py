from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MRIState(TypedDict): specs: dict; validation_report: dict; approved: bool; def val_coil(state: MRIState): return {'validation_report': {'status': 'processed', 'compliant': state['specs'].get('snr', 0) > 30}}; def check_compliance(state: MRIState): return 'approved' if state['validation_report']['compliant'] else 'rejected'; graph = StateGraph(MRIState); graph.add_node('validate', val_coil); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()