from langgraph.graph import StateGraph, END; from typing import TypedDict; class PipeState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PipeState): state['valid'] = 'Pressure Rating' in state['specs'] and 'Welding Certification' in state['specs']; return state; def route_logic(state: PipeState): return 'end' if state['valid'] else 'end'; graph = StateGraph(PipeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()