from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ActivityBookState(TypedDict): content_checked: bool; age_group_verified: bool; data: dict; def validate_content(state: ActivityBookState): return {'content_checked': True}; def verify_age_suitability(state: ActivityBookState): return {'age_group_verified': True}; graph = StateGraph(ActivityBookState); graph.add_node('validate_content', validate_content); graph.add_node('verify_age_suitability', verify_age_suitability); graph.set_entry_point('validate_content'); graph.add_edge('validate_content', 'verify_age_suitability'); graph.add_edge('verify_age_suitability', END); graph = graph.compile()