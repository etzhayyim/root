from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): items: List[str]; qc_passed: bool; def val_proc(state: SurgicalState): state['qc_passed'] = all('sterile' in item.lower() and 'absorb' in item.lower() for item in state['items']); return state; graph = StateGraph(SurgicalState); graph.add_node('inbound_check', val_proc); graph.set_entry_point('inbound_check'); graph.add_edge('inbound_check', END); graph = graph.compile()