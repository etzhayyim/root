from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; specs: dict; valid: bool;  def validate_dims(state: ComponentState): state['valid'] = all(k in state['specs'] for k in ['gauge', 'alloy']); return state; def check_export(state: ComponentState): return 'valid' if state['valid'] else 'flagged'; graph = StateGraph(ComponentState); graph.add_node('validate', validate_dims); graph.add_node('export_review', check_export); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph.set_entry_point('validate'); graph.set_finish_point('export_review')