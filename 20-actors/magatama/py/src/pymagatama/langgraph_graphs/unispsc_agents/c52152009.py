from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): bowl_type: str; specs_met: bool; def val_specs(state: ProcState): return {'specs_met': True} if 'material_composition' in state else {'specs_met': False}; graph = StateGraph(ProcState); graph.add_node('validation', val_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph.compile()