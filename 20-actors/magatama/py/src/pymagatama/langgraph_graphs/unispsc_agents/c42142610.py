from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SyringeState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(SyringeState); def validate_spec(state: SyringeState): state['compliance_check'] = 'ISO_standard_compliance' in state['specs']; return state; def approve_procurement(state: SyringeState): return 'approved' if state['compliance_check'] else 'rejected'; graph.add_node('validate', validate_spec); graph.add_node('approval', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); app = graph.compile()