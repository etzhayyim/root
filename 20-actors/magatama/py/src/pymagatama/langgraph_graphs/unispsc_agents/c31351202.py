from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeState(TypedDict): tube_specs: dict; validation_results: List[str]; def validate_material(state: TubeState): return {'validation_results': ['Material Grade OK']}; def perform_pressure_test(state: TubeState): return {'validation_results': state['validation_results'] + ['Pressure Test Passed']}; graph = StateGraph(TubeState); graph.add_node('material_check', validate_material); graph.add_node('pressure_check', perform_pressure_test); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'pressure_check'); graph.add_edge('pressure_check', END); graph = graph.compile()