from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MarkerState(TypedDict): input_data: dict; risk_score: int; approved: bool; def validate_solvent(state: MarkerState): state['risk_score'] = 10 if 'xylene' in state['input_data']['ingredients'] else 0; return state; def check_compliance(state: MarkerState): state['approved'] = state['risk_score'] < 5; return state; graph = StateGraph(MarkerState); graph.add_node('validate', validate_solvent); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()