from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WorkbenchState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: WorkbenchState): specs = state['specs']; errors = []; if specs.get('load_capacity', 0) <= 0: errors.append('Load capacity must be positive'); return {'valid': len(errors) == 0, 'error_log': errors}; graph = StateGraph(WorkbenchState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()