from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TunnelState(TypedDict): specs: List[str]; validation_status: str; def validate_specs(state: TunnelState): return {'validation_status': 'verified' if len(state['specs']) > 2 else 'pending'}; graph = StateGraph(TunnelState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()