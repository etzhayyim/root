from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CupolaState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: CupolaState): required = ['thermal_transmittance', 'material']; state['valid'] = all(k in state['specs'] for k in required); return state; graph = StateGraph(CupolaState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()