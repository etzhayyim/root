from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HarnessState(TypedDict): items: List[str]; validated: bool; defect_rate: float; def validate_nails(state: HarnessState): return {'validated': all(item.startswith('Nail-') for item in state['items'])}; def check_specs(state: HarnessState): return {'defect_rate': 0.0}; graph = StateGraph(HarnessState); graph.add_node('validate', validate_nails); graph.add_node('spec_check', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'spec_check'); graph.add_edge('spec_check', END); graph = graph.compile()