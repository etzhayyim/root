from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; result: dict; def validate_specs(state: State): specs = state['input_data']; return {'result': {'valid': 'power_rating' in specs and 'certification' in specs}}; def finalize_order(state: State): return {'result': {'status': 'processed'}}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()