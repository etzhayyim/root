from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_specs: dict; validation_results: List[str]; def validate_specs(state: ProcurementState): results = []; if state['vendor_specs'].get('weight_capacity', 0) < 5: results.append('Insufficient capacity'); state['validation_results'] = results; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()