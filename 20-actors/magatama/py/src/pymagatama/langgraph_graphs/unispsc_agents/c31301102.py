from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: ForgingState): return {'validation_results': ['Material Grade OK']}; def validate_dimensions(state: ForgingState): return {'validation_results': ['Dimensions compliant']}; graph = StateGraph(ForgingState); graph.add_node('check_material', validate_material); graph.add_node('check_dims', validate_dimensions); graph.set_entry_point('check_material'); graph.add_edge('check_material', 'check_dims'); graph.add_edge('check_dims', END); compiled_graph = graph.compile()