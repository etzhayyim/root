from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabDocState(TypedDict): doc_id: str; content: str; validated: bool; material_spec: str; def validate_specs(state: LabDocState): state['validated'] = 'sterility' in state['material_spec'].lower(); return state; graph = StateGraph(LabDocState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()