from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): component_id: str; specs: dict; validation_report: list; workflow: List[str]; def validate_tolerance(state: GraphState): state['validation_report'].append('Tolerance check passed'); return {'workflow': state['workflow'] + ['tolerance_validation']}; def inspect_surface(state: GraphState): state['validation_report'].append('Surface inspection complete'); return {'workflow': state['workflow'] + ['surface_inspection']}; graph = StateGraph(GraphState); graph.add_node('validate', validate_tolerance); graph.add_node('inspect', inspect_surface); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()