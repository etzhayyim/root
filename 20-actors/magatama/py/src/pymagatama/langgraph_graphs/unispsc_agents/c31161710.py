from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): input_specs: dict; validated: bool; validation_log: List[str]; def validate_specs(state: FastenerState): specs = state['input_specs']; errors = []; if 'material' not in specs: errors.append('Missing material'); return {'validated': len(errors) == 0, 'validation_log': errors}; graph = StateGraph(FastenerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()