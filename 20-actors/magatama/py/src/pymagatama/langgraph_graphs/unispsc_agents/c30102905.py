from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_specs(state: ProcureState): specs = state.get('specs', {}); errors = []; if not specs.get('tensile_strength'): errors.append('Missing tensile strength'); return {'validation_results': errors, 'status': 'validated' if not errors else 'failed'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()