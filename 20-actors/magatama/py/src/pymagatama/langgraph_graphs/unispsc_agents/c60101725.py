from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BookProcurementState(TypedDict): title: str; isbn: str; verified: bool; workflow_log: List[str]; def validate_book(state: BookProcurementState): state['verified'] = True if len(state['isbn']) == 13 else False; state['workflow_log'].append('ISBN validation complete'); return state; def finalize_order(state: BookProcurementState): state['workflow_log'].append('Order finalized'); return state; graph = StateGraph(BookProcurementState); graph.add_node('validate', validate_book); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()