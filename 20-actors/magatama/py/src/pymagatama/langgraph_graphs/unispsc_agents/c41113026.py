from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class NephelometerState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: NephelometerState): state['valid'] = 'measurement_range_ntu' in state['specs'] and state['specs'].get('compliance_iso_7027', False); return state; graph = StateGraph(NephelometerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()