from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PunchBowlState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_material(state: PunchBowlState): if any(i.get('material') == 'unknown' for i in state['items']): state['validation_errors'].append('Material safety check failed'); return state; def check_capacity(state: PunchBowlState): for i in state['items']: if i.get('capacity_liters', 0) <= 0: state['validation_errors'].append('Invalid capacity'); return state; graph = StateGraph(PunchBowlState); graph.add_node('validate_material', validate_material); graph.add_node('check_capacity', check_capacity); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_capacity'); graph.add_edge('check_capacity', END); graph = graph.compile()