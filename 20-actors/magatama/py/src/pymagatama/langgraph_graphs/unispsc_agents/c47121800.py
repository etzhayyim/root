from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CleaningEquipmentState(TypedDict): equipment_type: str; specs: dict; validation_score: int; graph = StateGraph(CleaningEquipmentState); def validate_equipment(state: CleaningEquipmentState): return {'validation_score': 10 if state['specs'].get('power') else 0}; def check_safety(state: CleaningEquipmentState): return {'validation_score': state['validation_score'] + 5}; graph.add_node('validate', validate_equipment); graph.add_node('safety_check', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); compiled_graph = graph.compile()