from langgraph.graph import StateGraph, END; from typing import TypedDict; class ThermometerState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ThermometerState): state['valid'] = 'accuracy_tolerance' in state['specs'] and state['specs']['accuracy_tolerance'] < 0.5; return state; graph = StateGraph(ThermometerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()