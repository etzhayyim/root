from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_certs: List[str]; qc_passed: bool; inspection_notes: str; def validate_certifications(state: ProcurementState): state['qc_passed'] = 'ISO13485' in state['vendor_certs']; return state; def final_approval(state: ProcurementState): return 'approved' if state['qc_passed'] else 'rejected'; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_certifications); graph.add_node('approval', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph.compile()