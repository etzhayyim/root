from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EjectionSystemState(TypedDict): component_id: str; doc_refs: List[str]; safety_approved: bool; def validate_safety(state: EjectionSystemState): return {'safety_approved': True}; def update_records(state: EjectionSystemState): return {'doc_refs': state['doc_refs'] + ['certified']}; graph = StateGraph(EjectionSystemState); graph.add_node('safety_check', validate_safety); graph.add_node('record_update', update_records); graph.add_edge('safety_check', 'record_update'); graph.add_edge('record_update', END); graph.set_entry_point('safety_check'); graph = graph.compile()