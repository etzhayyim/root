from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_name: str; grade: str; purity: float; validated: bool; def validate_chemical(state: ChemicalState): state['validated'] = state['purity'] >= 0.95; return {'validated': state['validated']}; graph = StateGraph(ChemicalState); graph.add_node('validation', validate_chemical); graph.set_entry_point('validation'); graph.add_edge('validation', END); compiled_graph = graph.compile()