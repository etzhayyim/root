from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CTComponentState(TypedDict): component_id: str; quality_score: float; compliance_verified: bool; workflow_steps: List[str]; def validate_component(state: CTComponentState): state['compliance_verified'] = True; state['workflow_steps'].append('Hardware Integrity Check'); return state; def radiation_test(state: CTComponentState): state['workflow_steps'].append('Radiation Hardness Validation'); return state; graph = StateGraph(CTComponentState); graph.add_node('validate', validate_component); graph.add_node('radiation', radiation_test); graph.set_entry_point('validate'); graph.add_edge('validate', 'radiation'); graph.add_edge('radiation', END); graph = graph.compile()