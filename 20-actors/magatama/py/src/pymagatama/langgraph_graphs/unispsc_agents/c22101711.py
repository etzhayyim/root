from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserState(TypedDict): component_id: str; specs: dict; valid: bool; def validate_specs(state: LaserState): state['valid'] = 'laser_class_rating' in state['specs']; return state; builder = StateGraph(LaserState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()