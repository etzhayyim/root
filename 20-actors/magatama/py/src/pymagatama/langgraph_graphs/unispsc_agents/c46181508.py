from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ApparelState(TypedDict): specs: dict; compliant: bool; safety_audit: str; def validate_specs(state: ApparelState): rating = state['specs'].get('flame_retardance_rating'); state['compliant'] = rating >= 'UL-94-V0'; return state; def final_audit(state: ApparelState): state['safety_audit'] = 'Certified' if state['compliant'] else 'Rejected'; return state; graph = StateGraph(ApparelState); graph.add_node('validate', validate_specs); graph.add_node('audit', final_audit); graph.set_entry_point('validate'); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); graph = graph.compile()