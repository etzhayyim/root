from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrainingGearState(TypedDict): gear_name: str; specifications: List[str]; validation_status: bool; def validate_equipment(state: TrainingGearState): state['validation_status'] = len(state['specifications']) >= 3; return state; graph = StateGraph(TrainingGearState); graph.add_node('validator', validate_equipment); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()