from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabEquipmentState(TypedDict): status: str; spec_validated: bool; equipment_type: str; def validate_specs(state: LabEquipmentState): return {'spec_validated': True if state['equipment_type'] else False}; def check_safety(state: LabEquipmentState): return {'status': 'Compliant' if state['spec_validated'] else 'Error'}; graph = StateGraph(LabEquipmentState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()