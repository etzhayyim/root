from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): vendor_certs: List[str]; purity_check: float; is_compliant: bool; def validate_gmp(state: DrugProcurementState): state['is_compliant'] = 'GMP' in state['vendor_certs'] and state['purity_check'] >= 99.0; return state; graph = StateGraph(DrugProcurementState); graph.add_node('validate_gmp', validate_gmp); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', END); graph = graph.compile()