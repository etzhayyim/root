from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): items: List[str]; validation_results: List[str]; def validate_material_cert(state: ForgingState): return {'validation_results': ['Material cert verified']}; def check_dimensional_spec(state: ForgingState): return {'validation_results': ['Dimensional check passed']}; graph = StateGraph(ForgingState); graph.add_node('cert_validation', validate_material_cert); graph.add_node('dimensional_check', check_dimensional_spec); graph.set_entry_point('cert_validation'); graph.add_edge('cert_validation', 'dimensional_check'); graph.add_edge('dimensional_check', END); graph = graph.compile()