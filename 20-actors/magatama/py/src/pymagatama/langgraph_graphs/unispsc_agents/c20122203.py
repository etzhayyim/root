from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class RobotGearState(TypedDict): gear_id: str; specs: dict; validation_results: list; status: str; def validate_specs(state: RobotGearState): return {'validation_results': ['passed' if state['specs'].get('tolerance') == 'P4' else 'failed'], 'status': 'validated'}; def assemble_gear(state: RobotGearState): return {'status': 'assembled'}; graph = StateGraph(RobotGearState); graph.add_node('validate', validate_specs); graph.add_node('assemble', assemble_gear); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph.set_entry_point('validate'); graph = graph.compile()