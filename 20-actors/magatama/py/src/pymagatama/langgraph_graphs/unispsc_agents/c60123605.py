from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GlitterState(TypedDict): input_spec: dict; validation_results: List[str]; approved: bool; def validate_particle_size(state: GlitterState): state['validation_results'].append('Size_Checked'); return state; def check_toxicity(state: GlitterState): state['validation_results'].append('Safety_Verified'); state['approved'] = True; return state; graph = StateGraph(GlitterState); graph.add_node('val_size', validate_particle_size); graph.add_node('check_tox', check_toxicity); graph.set_entry_point('val_size'); graph.add_edge('val_size', 'check_tox'); graph.add_edge('check_tox', END); graph = graph.compile()