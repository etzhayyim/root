from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BeamState(TypedDict): dimensions: dict; alloy_specs: dict; validated: bool; def validate_specs(state: BeamState): state['validated'] = 'alloy_composition' in state['alloy_specs'] and 'dimensions' in state; return state; graph = StateGraph(BeamState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()