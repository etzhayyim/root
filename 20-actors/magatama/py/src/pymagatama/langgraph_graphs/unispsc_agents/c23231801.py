from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): input_data: dict; validation: str; graph = StateGraph(GraphState); def validate_clamp_specs(state: GraphState): val = 'Pass' if 'force' in state['input_data'] and state['input_data']['force'] > 0 else 'Fail'; return {'validation': val}; graph.add_node('validate', validate_clamp_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()