from langgraph.graph import StateGraph, END; from typing import TypedDict; class HeaterState(TypedDict): specs: dict; approved: bool; def val_spec(state: HeaterState): return {'approved': state['specs'].get('temp_range') is not None}; def finalize(state: HeaterState): return {'approved': True}; graph = StateGraph(HeaterState); graph.add_node('validate', val_spec); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()