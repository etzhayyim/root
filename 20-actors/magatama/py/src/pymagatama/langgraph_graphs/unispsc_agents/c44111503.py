from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OrgState(TypedDict): items: List[str]; validated: bool; def validate_spec(state: OrgState): return {'validated': len(state['items']) > 0}; graph = StateGraph(OrgState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()