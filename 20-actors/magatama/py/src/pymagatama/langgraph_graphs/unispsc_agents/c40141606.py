from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ReliefValveState(TypedDict): specs: dict; validation_report: list; status: str; def validate_specs(state: ReliefValveState): issues = []; if state['specs'].get('set_pressure', 0) <= 0: issues.append('Invalid pressure'); return {'validation_report': issues, 'status': 'VALIDATED' if not issues else 'REJECTED'}; graph = StateGraph(ReliefValveState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()