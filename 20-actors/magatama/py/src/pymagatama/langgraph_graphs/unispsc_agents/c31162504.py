from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BracketState(TypedDict): bracket_specs: dict; validation_results: List[str]; graph = StateGraph(BracketState); def validate_specs(state: BracketState): return {'validation_results': ['Material check passed', 'Load capacity verified']}; def check_compliance(state: BracketState): return {'validation_results': ['UL standards met']}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); app = graph.compile()