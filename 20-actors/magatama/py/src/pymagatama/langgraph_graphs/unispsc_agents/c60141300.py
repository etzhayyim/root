from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToySpec(TypedDict): items: List[str]; safety_verified: bool; def validate_safety(state: ToySpec): state['safety_verified'] = True; return state; def finalize_order(state: ToySpec): return {'items': state['items']}; graph = StateGraph(ToySpec); graph.add_node('safety_check', validate_safety); graph.add_node('finalize', finalize_order); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()