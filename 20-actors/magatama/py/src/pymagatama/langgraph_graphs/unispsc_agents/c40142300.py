from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_pressure_range(state: PipeState): state['valid'] = state['specs'].get('pressure_rating', 0) > 0; return state; def check_material_cert(state: PipeState): if 'material' not in state['specs']: state['valid'] = False; state['error_log'].append('Missing material cert'); return state; graph = StateGraph(PipeState); graph.add_node('validate_pressure', validate_pressure_range); graph.add_node('check_material', check_material_cert); graph.set_entry_point('validate_pressure'); graph.add_edge('validate_pressure', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()