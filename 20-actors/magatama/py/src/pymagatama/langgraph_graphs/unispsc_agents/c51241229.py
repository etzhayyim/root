from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SalivaSolutionState(TypedDict): vendor_certs: List[str]; ph_level: float; approved: bool; def validate_ph(state: SalivaSolutionState): state['approved'] = 6.0 <= state['ph_level'] <= 8.0; return state; def check_certs(state: SalivaSolutionState): state['approved'] = 'ISO13485' in state['vendor_certs'] and state['approved']; return state; builder = StateGraph(SalivaSolutionState); builder.add_node('validate_ph', validate_ph); builder.add_node('check_certs', check_certs); builder.set_entry_point('validate_ph'); builder.add_edge('validate_ph', 'check_certs'); builder.add_edge('check_certs', END); graph = builder.compile()