from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; components: List[str]; validated: bool; certs: List[str]; def validate_components(state: KitState): return {"validated": all(len(c) > 0 for c in state["components"])}; def verify_certs(state: KitState): return {"validated": len(state["certs"]) > 0}; graph = StateGraph(KitState); graph.add_node("validate", validate_components); graph.add_node("certify", verify_certs); graph.set_entry_point("validate"); graph.add_edge("validate", "certify"); graph.add_edge("certify", END); app = graph.compile()