from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RubberState(TypedDict): rod_id: str; specifications: dict; compliance_check: bool; graph = StateGraph(RubberState); def validate_specs(state: RubberState): state['compliance_check'] = state['specifications'].get('hardness', 0) > 40; return state; def route_procurement(state: RubberState): return 'end' if state['compliance_check'] else 'end'; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()