from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UroPercolatorState(TypedDict): device_id: str; compliance_docs: List[str]; qc_status: str; def validate_compliance(state: UroPercolatorState): return {'qc_status': 'passed' if len(state['compliance_docs']) >= 3 else 'failed'}; def setup_workflow(): graph = StateGraph(UroPercolatorState); graph.add_node('validation', validate_compliance); graph.set_entry_point('validation'); graph.add_edge('validation', END); return graph.compile(); graph = setup_workflow()