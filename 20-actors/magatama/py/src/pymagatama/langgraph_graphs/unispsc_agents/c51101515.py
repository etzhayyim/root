from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MedicalDeviceState(TypedDict): device_id: str; safety_checks: Annotated[Sequence[str], operator.add]; validation_passed: bool; def check_safety(state: MedicalDeviceState): return {"safety_checks": ["radiation_emission_test", "calibration_check"]}; def validate_specs(state: MedicalDeviceState): return {"validation_passed": len(state["safety_checks"]) >= 2}; graph = StateGraph(MedicalDeviceState); graph.add_node("safety", check_safety); graph.add_node("validate", validate_specs); graph.set_entry_point("safety"); graph.add_edge("safety", "validate"); graph.add_edge("validate", END); graph = graph.compile()