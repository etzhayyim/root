from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BeverageState(TypedDict): vendor: str; items: List[str]; safety_approved: bool; def check_compliance(state: BeverageState): state['safety_approved'] = True; return state; graph = StateGraph(BeverageState); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()