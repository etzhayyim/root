from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OfficeProcurementState(TypedDict): items: List[dict]; approval_required: bool; validated: bool; def validate_items(state: OfficeProcurementState): state['validated'] = all('part_number' in item for item in state['items']); return state; def check_approval(state: OfficeProcurementState): state['approval_required'] = len(state['items']) > 10; return state; graph = StateGraph(OfficeProcurementState); graph.add_node('validate', validate_items); graph.add_node('approval', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); compiled_graph = graph.compile()