from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlumbingState(TypedDict): fixtures: List[str]; validation_results: dict; def validate_fixture_spec(state: PlumbingState): return {'validation_results': {'compliant': all(f for f in state['fixtures'] if 'NSF' in f)}}; def finalize_order(state: PlumbingState): return {'validation_results': {'status': 'approved'}}; graph = StateGraph(PlumbingState); graph.add_node('validate', validate_fixture_spec); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()