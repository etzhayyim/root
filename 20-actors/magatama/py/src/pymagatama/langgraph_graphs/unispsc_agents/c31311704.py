from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: List[str]; validated: bool; certs: List[str]; def validate_material(state: PipeState): state['validated'] = 'Inconel' in state['certs']; return state; def check_pressure(state: PipeState): return state; graph = StateGraph(PipeState); graph.add_node('validate_material', validate_material); graph.add_node('check_pressure', check_pressure); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_pressure'); graph.add_edge('check_pressure', END); compile_graph = graph.compile()