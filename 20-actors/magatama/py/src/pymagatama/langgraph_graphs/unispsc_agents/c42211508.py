from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MobilityState(TypedDict): device_id: str; specs: dict; validation_passed: bool; def validate_specs(state: MobilityState): state['validation_passed'] = 'bearing_capacity_kg' in state['specs']; return state; def check_compliance(state: MobilityState): print('Checking ISO compliance...'); return state; graph = StateGraph(MobilityState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()