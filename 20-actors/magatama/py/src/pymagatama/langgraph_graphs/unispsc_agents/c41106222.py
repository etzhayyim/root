from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class YeastKitState(TypedDict): kit_id: str; compliance_checked: bool; storage_verified: bool; def validate_reagents(state: YeastKitState): state['compliance_checked'] = True; return state; def check_cold_chain(state: YeastKitState): state['storage_verified'] = True; return state; graph = StateGraph(YeastKitState); graph.add_node('validate', validate_reagents); graph.add_node('cold_chain', check_cold_chain); graph.add_edge('validate', 'cold_chain'); graph.add_edge('cold_chain', END); graph.set_entry_point('validate'); graph = graph.compile()