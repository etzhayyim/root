from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BatteryState(TypedDict): battery_type: str; specs: dict; compliant: bool; def validate_battery(state: BatteryState): v = state['specs'].get('voltage', 0); c = state['specs'].get('capacity', 0); state['compliant'] = v >= 12 and c > 0; return state; graph = StateGraph(BatteryState); graph.add_node('validate', validate_battery); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()