from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; valid: bool; graph = StateGraph(State); def validate_specs(state: State): state['valid'] = 'wattage' in state['specs'] and 'ip_rating' in state['specs']; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()