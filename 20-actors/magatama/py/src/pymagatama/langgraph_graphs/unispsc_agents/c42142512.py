from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): needle_gauge: str; is_sterile: bool; compliance_status: List[str]; def validate_needle_spec(state: ProcurementState): return {'compliance_status': ['ISO 13485 Verified'] if state['is_sterile'] else ['Rejected']}; def finalize_procurement(state: ProcurementState): return {'compliance_status': state['compliance_status'] + ['Procurement Validated']}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_needle_spec); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()