from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): data: dict; status: str; def validate_gmp(state: PharmState): state['status'] = 'VALIDATED' if 'gmp_cert' in state['data'] else 'REJECTED'; return state; graph = StateGraph(PharmState); graph.add_node('validate_gmp', validate_gmp); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', END); graph = graph.compile()