from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GoggleSpec(TypedDict): model: str; compliance: str; valid: bool; def validate_specs(state: GoggleSpec): state['valid'] = state['compliance'] in ['ANSI Z87.1', 'EN 166']; return state; builder = StateGraph(GoggleSpec); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()