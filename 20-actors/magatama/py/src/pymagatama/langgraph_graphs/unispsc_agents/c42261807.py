from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AutopsyCartState(TypedDict): cart_id: str; specs: dict; compliance_status: bool; def validate_specs(state: AutopsyCartState): state.update({'compliance_status': '304_stainless' in state['specs'].get('material', '')}); return state; def check_sanitation(state: AutopsyCartState): return state; graph = StateGraph(AutopsyCartState); graph.add_node('validate', validate_specs); graph.add_node('sanitize', check_sanitation); graph.set_entry_point('validate'); graph.add_edge('validate', 'sanitize'); graph.add_edge('sanitize', END); graph = graph.compile()