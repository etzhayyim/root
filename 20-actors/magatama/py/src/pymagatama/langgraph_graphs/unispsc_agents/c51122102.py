from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; compliance_docs: List[str]; def validate_purity(state: PharmState): return {'compliance_docs': state['compliance_docs'] + ['Purity Validated']} if state['purity'] >= 99.0 else {'compliance_docs': state['compliance_docs'] + ['Purity Rejected']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()