from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class SemiconductorState(TypedDict): input_data: dict; processing_steps: list[str]; compliance_checks: list[bool]; def validate_purity(state: SemiconductorState): return {'compliance_checks': [state['input_data'].get('purity', 0) > 0.999999]}; def approve_procurement(state: SemiconductorState): return {'processing_steps': state['processing_steps'] + ['approved']}; graph = StateGraph(SemiconductorState); graph.add_node('validate', validate_purity); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()