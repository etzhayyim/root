from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): specs: List[str]; validated: bool; def validate_specs(state: SurgicalState) -> dict: return {'validated': all(len(s) > 0 for s in state['specs'])}; graph = StateGraph(SurgicalState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph.compile()