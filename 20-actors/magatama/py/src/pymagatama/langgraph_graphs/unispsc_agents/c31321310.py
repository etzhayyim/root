from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): material_certification: bool; tolerance_check: bool; final_approval: bool; def validate_materials(state: AssemblyState): return {'material_certification': True}; def check_geometrics(state: AssemblyState): return {'tolerance_check': True}; def finalize_process(state: AssemblyState): return {'final_approval': True}; graph = StateGraph(AssemblyState); graph.add_node('material_check', validate_materials); graph.add_node('geometric_check', check_geometrics); graph.add_node('approval', finalize_process); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'geometric_check'); graph.add_edge('geometric_check', 'approval'); graph.add_edge('approval', END); graph = graph.compile()