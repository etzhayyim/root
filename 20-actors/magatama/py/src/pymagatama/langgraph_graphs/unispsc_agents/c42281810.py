from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SterilizationState(TypedDict): pack_id: str; compliance_check: bool; metadata: dict; def validate_pack(state: SterilizationState): state['compliance_check'] = True; return state; graph = StateGraph(SterilizationState); graph.add_node('validate', validate_pack); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()