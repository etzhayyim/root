from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AxleState(TypedDict): specs: dict; valid: bool; error: str; def validate_axle(state: AxleState): state['valid'] = all(k in state['specs'] for k in ['load_rating', 'material']); return state; graph = StateGraph(AxleState); graph.add_node('validate', validate_axle); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()