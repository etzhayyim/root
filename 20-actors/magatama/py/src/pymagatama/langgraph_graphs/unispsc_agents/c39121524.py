from langgraph.graph import StateGraph, END; from typing import TypedDict; class PhotocellState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PhotocellState): state['valid'] = state['specs'].get('rated_voltage') is not None; return state; graph = StateGraph(PhotocellState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()