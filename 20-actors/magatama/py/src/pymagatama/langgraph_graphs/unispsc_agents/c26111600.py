from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GeneratorState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: GeneratorState): errors = []; if state['specs'].get('output_capacity_kva', 0) <= 0: errors.append('Invalid capacity'); return {'validation_errors': errors}; def check_compliance(state: GeneratorState): return {'approved': len(state.get('validation_errors', [])) == 0}; graph = StateGraph(GeneratorState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()