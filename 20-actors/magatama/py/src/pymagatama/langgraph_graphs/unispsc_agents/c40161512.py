from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): specs: dict; valid: bool; requirements: List[str]; def validate_specs(state: FilterState): state['valid'] = 'Frequency Range' in state['specs'] and 'Impedance' in state['specs']; return state; def check_export_control(state: FilterState): state['requirements'].append('Export Control Check Required'); return state; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()