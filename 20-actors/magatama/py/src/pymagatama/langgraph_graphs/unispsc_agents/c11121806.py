from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; import operator; class RefiningState(TypedDict): input_material: str; purity_check: float; refined_output: List[str]; class RefinementManager: def validate_purity(self, state: RefiningState): return {'purity_check': 0.999} if state['input_material'] else {'purity_check': 0.0}; def process_refinement(self, state: RefiningState): return {'refined_output': ['processed_mineral_batch_001']}; manager = RefinementManager(); graph = StateGraph(RefiningState); graph.add_node('validate', manager.validate_purity); graph.add_node('process', manager.process_refinement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()