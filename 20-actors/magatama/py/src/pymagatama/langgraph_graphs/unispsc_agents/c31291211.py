from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtrusionState(TypedDict): metal_purity: float; specs_compliant: bool; validation_log: List[str]; def check_purity(state: ExtrusionState): return {'specs_compliant': state['metal_purity'] >= 99.9, 'validation_log': ['Purity check passed'] if state['metal_purity'] >= 99.9 else ['Purity check failed']}; def structural_validate(state: ExtrusionState): return {'validation_log': state['validation_log'] + ['Structural integrity verified']}; graph = StateGraph(ExtrusionState); graph.add_node('check_purity', check_purity); graph.add_node('structural_validate', structural_validate); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'structural_validate'); graph.add_edge('structural_validate', END); graph = graph.compile()