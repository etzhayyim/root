from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): n_alloy_specs: dict; validated: bool; certs: List[str]; def validate_specs(state: ProcurementState): state['validated'] = 'AMS_standard' in state['certs']; return state; def check_compliance(state: ProcurementState): print('Checking export compliance...'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()