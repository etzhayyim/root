from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoltState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: BoltState): s = state['specs']; state['valid'] = 'material_grade' in s and 'thread_pitch' in s; return state; graph = StateGraph(BoltState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()