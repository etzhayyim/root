from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): data: dict; analysis_results: Annotated[list, operator.add]; def validate_purity(state: PharmState): purity = state['data'].get('purity_percent', 0); return {'analysis_results': ['Purity OK' if purity >= 99.0 else 'Purity Fail']}; def check_safety(state: PharmState): return {'analysis_results': ['Safety protocols verified']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()