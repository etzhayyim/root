from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CameraState(TypedDict): sensitivity_check: bool; compliance_status: bool; def validate_specs(state: CameraState): return {'sensitivity_check': True}; def check_compliance(state: CameraState): return {'compliance_status': True}; graph = StateGraph(CameraState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()