from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalSpecState(TypedDict): wells_id: str; spec_validation: bool; compliance_status: List[str]; def validate_material(state: DentalSpecState): print(f'Validating material for {state["wells_id"]}'); state['spec_validation'] = True; return state; def check_compliance(state: DentalSpecState): state['compliance_status'] = ['ISO_13485_Verified']; return state; graph = StateGraph(DentalSpecState); graph.add_node('validate', validate_material); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()