from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine: str; compliance_checked: bool; stock_status: str; def check_regulation(state: PharmState): return {'compliance_checked': True}; def verify_stock(state: PharmState): return {'stock_status': 'verified'}; graph = StateGraph(PharmState); graph.add_node('regulation', check_regulation); graph.add_node('stock', verify_stock); graph.add_edge('regulation', 'stock'); graph.add_edge('stock', END); graph.set_entry_point('regulation'); graph = graph.compile()