from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class SicProcurementState(TypedDict): purity: float; particle_size: float; compliance_check: bool; def validate_purity(state: SicProcurementState) -> SicProcurementState: state['compliance_check'] = state['purity'] >= 99.5; return state; def validate_size(state: SicProcurementState) -> SicProcurementState: return state; workflow = StateGraph(SicProcurementState); workflow.add_node('validate_purity', validate_purity); workflow.add_node('validate_size', validate_size); workflow.set_entry_point('validate_purity'); workflow.add_edge('validate_purity', 'validate_size'); workflow.add_edge('validate_size', END); graph = workflow.compile()