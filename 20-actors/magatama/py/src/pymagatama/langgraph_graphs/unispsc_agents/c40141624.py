from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ValveState(TypedDict): valve_id: str; specs: dict; validation_errors: List[str]; def validate_valve(state: ValveState): specs = state.get('specs', {}); errors = []; if 'psi_rating' not in specs: errors.append('Missing pressure rating'); return {'validation_errors': errors}; graph = StateGraph(ValveState); graph.add_node('validate', validate_valve); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()