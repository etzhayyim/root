from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class BillRackState(TypedDict): dimensions: dict; material_compliance: bool; status: str; def validate_specs(state: BillRackState): return {'material_compliance': True, 'status': 'VALIDATED'}; def finalize_procurement(state: BillRackState): return {'status': 'READY_FOR_ORDER'}; graph = StateGraph(BillRackState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()