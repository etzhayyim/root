from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoltState(TypedDict): bolt_specs: dict; validation_results: List[str]; def validate_material(state: BoltState): state['validation_results'].append('Material verified against ISO/AS standards'); return state; def check_dimensions(state: BoltState): state['validation_results'].append('Dimensional check passed'); return state; graph = StateGraph(BoltState); graph.add_node('validate_material', validate_material); graph.add_node('check_dimensions', check_dimensions); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_dimensions'); graph.add_edge('check_dimensions', END); graph = graph.compile()