from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StorageState(TypedDict): items: List[str]; validated: bool; budget_check: bool; def val_spec(state: StorageState): state['validated'] = True; return state; def check_budget(state: StorageState): state['budget_check'] = True; return state; graph = StateGraph(StorageState); graph.add_node('validate', val_spec); graph.add_node('budget', check_budget); graph.add_edge('validate', 'budget'); graph.add_edge('budget', END); graph.set_entry_point('validate'); graph = graph.compile()