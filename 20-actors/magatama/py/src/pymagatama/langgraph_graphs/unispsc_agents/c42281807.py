from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SterilizationState(TypedDict): items: List[str]; validated_specs: dict; status: str; def validate_tape_specs(state: SterilizationState): state['validated_specs'] = {'compliance': 'ISO-11140'}; state['status'] = 'validation_complete'; return state; graph = StateGraph(SterilizationState); graph.add_node('validate', validate_tape_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()