from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoardState(TypedDict): board_type: str; components: List[str]; validated: bool; def validate_components(state: BoardState): state['validated'] = len(state['components']) > 0; return state; def route_board(state: BoardState): return 'process'; graph = StateGraph(BoardState); graph.add_node('validate', validate_components); graph.add_node('process', lambda s: s); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()