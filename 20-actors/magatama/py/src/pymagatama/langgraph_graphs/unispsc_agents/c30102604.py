from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_flag: bool; def validate_specs(state: ProcurementState): specs = state['specs']; is_valid = all(k in specs for k in ['steel_grade_standard', 'thickness_mm']); return {'validation_flag': is_valid}; def approve_procurement(state: ProcurementState): print('Processing steel strip procurement...'); return {'validation_flag': True}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.compile()