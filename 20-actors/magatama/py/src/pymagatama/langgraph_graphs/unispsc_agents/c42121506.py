from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): items: List[str]; qc_passed: bool; def validate_specs(state: SurgicalState): return {'qc_passed': all(['material' in item for item in state['items']])}; def check_sterility(state: SurgicalState): return {'qc_passed': True}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_specs); graph.add_node('sterility', check_sterility); graph.add_edge('validate', 'sterility'); graph.add_edge('sterility', END); graph.set_entry_point('validate'); app = graph.compile()