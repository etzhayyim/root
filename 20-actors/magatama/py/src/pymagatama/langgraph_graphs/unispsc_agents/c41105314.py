from langgraph.graph import StateGraph, END; from typing import TypedDict; class GelDocState(TypedDict): vendor_specs: dict; is_compliant: bool; def validate_specs(state: GelDocState): state['is_compliant'] = 'camera_resolution_megapixels' in state['vendor_specs'] and state['vendor_specs']['camera_resolution_megapixels'] > 5; return state; graph = StateGraph(GelDocState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()