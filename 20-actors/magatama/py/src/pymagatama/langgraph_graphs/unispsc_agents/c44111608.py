from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_id: str; quality_specs: List[str]; approved: bool; def validate_specs(state: ProcurementState): return {'approved': all(spec in ['Material', 'Lock'] for spec in state['quality_specs'])}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()