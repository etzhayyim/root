from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; validation_status: bool; compliance_docs: List[str]; def validate_purity(state: PharmaState): print(f'Validating purity for {state[chemical_name]}'); state['validation_status'] = True; return state; def check_compliance(state: PharmaState): state['compliance_docs'].append('GMP_CERT_V1'); return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()