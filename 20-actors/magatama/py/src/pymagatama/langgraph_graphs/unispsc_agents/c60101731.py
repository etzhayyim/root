from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(ProcurementState); def validate_items(state: ProcurementState): return {'validation_results': {'status': 'approved'}}; graph.add_node('validate', validate_items); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()