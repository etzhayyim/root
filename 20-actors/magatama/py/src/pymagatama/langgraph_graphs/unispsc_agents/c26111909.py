from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ClutchState(TypedDict): specs: dict; validation_errors: list; status: str; def validate_specs(state: ClutchState): errors = []; if state['specs'].get('rated_torque_nm', 0) <= 0: errors.append('Invalid torque'); return {'validation_errors': errors, 'status': 'validated' if not errors else 'rejected'}; graph = StateGraph(ClutchState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()