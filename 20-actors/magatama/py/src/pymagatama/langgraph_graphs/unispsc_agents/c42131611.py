from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: ProcurementState): certs = state['specs'].get('certifications', []); state['validation_passed'] = 'ISO' in str(certs); return state; def finalize_order(state: ProcurementState): return {'status': 'approved'} if state['validation_passed'] else {'status': 'rejected'}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()