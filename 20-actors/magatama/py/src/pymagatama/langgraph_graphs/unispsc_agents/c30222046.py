from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RoadState(TypedDict): road_id: str; spec_docs: List[str]; validation_passed: bool; def plan_road_check(state: RoadState): return {'validation_passed': all(['asphalt' in doc or 'concrete' in doc for doc in state['spec_docs']])}; def finalize_road(state: RoadState): return {'validation_passed': True}; graph = StateGraph(RoadState); graph.add_node('check', plan_road_check); graph.add_node('finalize', finalize_road); graph.add_edge('check', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('check'); graph = graph.compile()