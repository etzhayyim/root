from langgraph.graph import StateGraph, END; from typing import TypedDict; class IncubatorState(TypedDict): specs: dict; valid: bool; def validate_specs(state: IncubatorState): state['valid'] = 'temperature' in state['specs'] and 'co2_level' in state['specs']; return state; builder = StateGraph(IncubatorState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()