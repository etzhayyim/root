from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): ingredient: str; compliance_check: bool; safety_clearance: bool; graph = StateGraph(PharmState); def check_regulatory(state: PharmState): return {'compliance_check': True}; def verify_safety(state: PharmState): return {'safety_clearance': True}; graph.add_node('regulatory', check_regulatory); graph.add_node('safety', verify_safety); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'safety'); graph.add_edge('safety', END); graph.compile()