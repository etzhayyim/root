from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ZincPartState(TypedDict): part_specs: dict; validation_report: List[str]; def validate_stamping(state: ZincPartState): errors = []; if not state['part_specs'].get('material'): errors.append('Missing material grade'); return {'validation_report': errors}; graph = StateGraph(ZincPartState); graph.add_node('validate', validate_stamping); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()