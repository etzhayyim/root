from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ArmBoardState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: ArmBoardState): state['valid'] = 'Sterilization Method' in state['specs']; state['log'].append('Validation complete'); return state; graph = StateGraph(ArmBoardState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()