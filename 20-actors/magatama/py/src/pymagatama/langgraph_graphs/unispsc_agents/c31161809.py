from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class WasherState(TypedDict): dimensions: dict; material_specs: str; compliance_passed: bool; def validate_specs(state: WasherState): return {'compliance_passed': all(val > 0 for val in state['dimensions'].values())}; def check_material(state: WasherState): valid = state['material_specs'] in ['steel', 'stainless', 'nylon']; return {'compliance_passed': state['compliance_passed'] and valid}; graph = StateGraph(WasherState); graph.add_node('validate', validate_specs); graph.add_node('material', check_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'material'); graph.add_edge('material', END); graph = graph.compile()