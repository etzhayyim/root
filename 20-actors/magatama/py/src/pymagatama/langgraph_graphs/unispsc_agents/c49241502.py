from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlaygroundState(TypedDict): vendor_docs: List[str]; safety_verified: bool; compliance_score: float; def validate_safety(state: PlaygroundState): return {'safety_verified': True, 'compliance_score': 0.95}; def finalize_procurement(state: PlaygroundState): return {'status': 'approved'}; graph = StateGraph(PlaygroundState); graph.add_node('safety_check', validate_safety); graph.add_node('finalization', finalize_procurement); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'finalization'); graph.add_edge('finalization', END); compiled_graph = graph.compile()