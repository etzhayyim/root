from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipetteTipState(TypedDict): tip_type: str; specs: dict; validation_report: str; def validate_compatibility(state: PipetteTipState): state['validation_report'] = 'Validating cross-brand compatibility for: ' + state['tip_type']; return state; def check_purity(state: PipetteTipState): state['validation_report'] += ' | Checking sterility and RNase/DNase status.'; return state; graph = StateGraph(PipetteTipState); graph.add_node('validate', validate_compatibility); graph.add_node('purity', check_purity); graph.add_edge('validate', 'purity'); graph.add_edge('purity', END); graph.set_entry_point('validate'); graph = graph.compile()