from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): vendor_id: str; compliance: bool; status: str; def validate_compliance(state: ProcureState): state['compliance'] = True; return state; def update_status(state: ProcureState): state['status'] = 'Verified'; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_compliance); graph.add_node('status', update_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'status'); graph.add_edge('status', END); compile_graph = graph.compile()