from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; components: List[str]; validated: bool; def check_components(state: KitState): return {"validated": len(state["components"]) > 0}; def finalize_kit(state: KitState): print(f"Kit {state['kit_id']} validated."); return {}; graph = StateGraph(KitState); graph.add_node("validator", check_components); graph.add_node("finalizer", finalize_kit); graph.set_entry_point("validator"); graph.add_edge("validator", "finalizer"); graph.add_edge("finalizer", END); graph = graph.compile()