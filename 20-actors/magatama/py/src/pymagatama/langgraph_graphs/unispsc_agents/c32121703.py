from langgraph.graph import StateGraph, END; from typing import TypedDict; class FerriteState(TypedDict): specs: dict; compliance: bool; graph = StateGraph(FerriteState); def validate_specs(state: FerriteState): return {'compliance': all(k in state['specs'] for k in ['permeability', 'curie_temp'])}; def check_export(state: FerriteState): return {'compliance': state.get('compliance', False) and state['specs'].get('frequency_limit', 0) < 1000}; graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate')