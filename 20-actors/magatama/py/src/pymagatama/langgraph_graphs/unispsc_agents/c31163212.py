from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): specs: dict; validation_results: List[str]; def validate_spec(state: FastenerState): state['validation_results'].append('Size_Checked'); state['validation_results'].append('Thread_Pitch_Verified'); return state; graph = StateGraph(FastenerState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()