from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CabinetState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: CabinetState): errors = []; if state['specs'].get('load_capacity', 0) < 50: errors.append('Load capacity below minimum safety threshold'); return {'validation_errors': errors}; def check_compliance(state: CabinetState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(CabinetState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()