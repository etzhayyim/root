from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OphthalmicState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_instrument(state: OphthalmicState): errors = []; if 'material_grade' not in state['specs']: errors.append('Missing material grade'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(OphthalmicState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()