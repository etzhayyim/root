from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): dimensions: dict; quality_report: str; approved: bool; def validate_specs(state: ForgingState): return {'approved': 'compliance' in state.get('quality_report', '').lower()}; def finalize_order(state: ForgingState): return {'status': 'processed'}; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()