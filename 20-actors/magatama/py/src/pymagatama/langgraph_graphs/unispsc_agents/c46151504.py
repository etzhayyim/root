from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input: str; validation_report: dict; approved: bool; def validator_node(state: State): state['validation_report'] = {'nij_compliant': True, 'export_license_required': True}; return state; def approval_node(state: State): state['approved'] = state['validation_report']['nij_compliant']; return state; graph = StateGraph(State); graph.add_node('validate', validator_node); graph.add_node('approve', approval_node); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()