from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): cold_chain: bool; validated: bool; def check_temp(state: ProcureState): return {'cold_chain': True}; def validate_lot(state: ProcureState): return {'validated': True}; graph = StateGraph(ProcureState); graph.add_node('verify_cold_chain', check_temp); graph.add_node('validate_lot', validate_lot); graph.set_entry_point('verify_cold_chain'); graph.add_edge('verify_cold_chain', 'validate_lot'); graph.add_edge('validate_lot', END); graph = graph.compile()