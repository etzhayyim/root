from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SplashBlockState(TypedDict): dimensions: dict; material: str; validation_status: bool; def validate_specs(state: SplashBlockState) -> SplashBlockState: state['validation_status'] = 'dimensions' in state and 'material' in state; return state; graph = StateGraph(SplashBlockState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()