from langgraph.graph import StateGraph, END; from typing import TypedDict; class TrussState(TypedDict): specs: dict; valid: bool; def validate_truss(state: TrussState): state['valid'] = all(k in state['specs'] for k in ['load', 'grade']); return state; graph = StateGraph(TrussState); graph.add_node('validate', validate_truss); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()