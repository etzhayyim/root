from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: list; status: str; def validate_dimensions(state: CastingState): state['validation_results'].append('Dimensional check passed'); return state; def verify_material_compliance(state: CastingState): state['validation_results'].append('Material chemistry verified'); return state; graph = StateGraph(CastingState); graph.add_node('dimension_check', validate_dimensions); graph.add_node('material_check', verify_material_compliance); graph.set_entry_point('dimension_check'); graph.add_edge('dimension_check', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()