from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): safety_docs: list; specs_validated: bool; def validate_ex_specs(state: GraphState): return {'specs_validated': True} if 'ATEX' in state['safety_docs'] else {'specs_validated': False}; def compliance_check(state: GraphState): return 'compliance_pass' if state['specs_validated'] else 'compliance_fail'; graph = StateGraph(GraphState); graph.add_node('validate', validate_ex_specs); graph.add_node('check', compliance_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()