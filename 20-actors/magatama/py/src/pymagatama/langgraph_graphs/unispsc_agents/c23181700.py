from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def validate_robot_specs(state: RobotState): payload = state['specs'].get('payload', 0); is_valid = payload > 0; return {'validation_results': ['Payload valid'] if is_valid else ['Invalid payload'], 'approved': is_valid}; graph = StateGraph(RobotState); graph.add_node('validate', validate_robot_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()