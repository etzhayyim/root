from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec: dict; validated: bool; def validate_tray(state: ProcState): tray = state['spec']; state['validated'] = 'material' in tray and 'dimensions' in tray; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_tray); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()