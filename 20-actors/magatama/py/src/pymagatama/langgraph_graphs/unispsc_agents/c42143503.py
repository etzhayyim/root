from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): device_id: str; compliance_checked: bool; status: str; def check_compliance(state: State): return {'compliance_checked': True, 'status': 'Verified'}; def finalize_procurement(state: State): return {'status': 'Approved'}; graph = StateGraph(State); graph.add_node('check', check_compliance); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('check'); graph.add_edge('check', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()