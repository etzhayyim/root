from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): specs: dict; validation_results: List[str]; code: str; def validate_spec(state: ProcurementState): cert_paper_weight = state['specs'].get('paper_gsm_weight', 0); state['validation_results'].append('Pass' if cert_paper_weight >= 120 else 'Fail'); return state; def finalize_procurement(state: ProcurementState): return {'validation_results': state['validation_results'] + ['Certified for Printing']}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()