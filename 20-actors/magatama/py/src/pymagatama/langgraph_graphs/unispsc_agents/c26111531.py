from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: list; status: str; def validate_dimensions(state: State): specs = state['input_data']; valid = 'tolerance' in specs and 'diameter' in specs; return {'validation_results': ['Dimension Check: OK' if valid else 'FAIL']}; def verify_material(state: State): return {'validation_results': ['Material Cert: Present']}; graph = StateGraph(State); graph.add_node('validate', validate_dimensions); graph.add_node('material', verify_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'material'); graph.add_edge('material', END); graph = graph.compile()