from langgraph.graph import StateGraph, END; from typing import TypedDict; class GlassState(TypedDict): vendor_specs: dict; is_compliant: bool; def validate_spec(state: GlassState): state['is_compliant'] = state['vendor_specs'].get('thickness', 0) >= 4.0; return state; graph = StateGraph(GlassState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()