from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; valid: bool; def val_spec(s: State): s['valid'] = 'Sterilization Method' in s['input_data'] and 'ISO 13485' in s['input_data']; return s; def check_flow(s: State): return 'process' if s['valid'] else 'end'; graph = StateGraph(State); graph.add_node('validate', val_spec); graph.add_edge('START', 'validate'); graph.add_conditional_edges('validate', check_flow, {'process': 'process', 'end': END}); graph.add_node('process', lambda s: s); graph.add_edge('process', END); graph = graph.compile()