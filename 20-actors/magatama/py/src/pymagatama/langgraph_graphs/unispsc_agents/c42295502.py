from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TissueState(TypedDict): items: List[str]; validation_status: bool; def validate_integrity(state: TissueState): state['validation_status'] = all(item.startswith('CERT-') for item in state['items']); return state; def check_storage(state: TissueState): state['validation_status'] = state['validation_status'] and True; return state; graph = StateGraph(TissueState); graph.add_node('validate', validate_integrity); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph = graph.compile()