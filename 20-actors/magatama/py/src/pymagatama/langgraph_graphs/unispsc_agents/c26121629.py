from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableState(TypedDict): specs: dict; valid: bool; cable_graph = StateGraph(CableState); def validate_specs(state: CableState): state['valid'] = state['specs'].get('voltage', 0) > 0; return state; cable_graph.add_node('validate', validate_specs); cable_graph.set_entry_point('validate'); cable_graph.add_edge('validate', END); graph = cable_graph.compile()