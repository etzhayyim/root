from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TouringBikeState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: TouringBikeState): errors = []; if not state['specs'].get('frame_material'): errors.append('Missing frame material'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(TouringBikeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()