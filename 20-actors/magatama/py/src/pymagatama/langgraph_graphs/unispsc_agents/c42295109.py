from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): bucket_type: str; specs: List[str]; validated: bool; def check_specs(state: ProcureState): # Validation of sterile-grade material specifications; state['validated'] = 'sus304' in state.get('specs', []) return state; graph = StateGraph(ProcureState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()