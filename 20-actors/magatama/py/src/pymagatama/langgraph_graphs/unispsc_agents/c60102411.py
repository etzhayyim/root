from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): model_specs: dict; status: str; def validate_dimensions(state: GraphState): return {'status': 'validated_dimensions'}; def check_safety(state: GraphState): return {'status': 'safety_check_passed'}; graph = StateGraph(GraphState); graph.add_node('validate', validate_dimensions); graph.add_node('safety', check_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()