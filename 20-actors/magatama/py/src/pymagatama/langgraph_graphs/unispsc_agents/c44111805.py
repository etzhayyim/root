from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): items: Annotated[list, operator.add]; status: str; def validate_curve_spec(state: GraphState): return {'status': 'validated' if all(i.get('accuracy') for i in state['items']) else 'rejected'}; graph = StateGraph(GraphState); graph.add_node('validate', validate_curve_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()