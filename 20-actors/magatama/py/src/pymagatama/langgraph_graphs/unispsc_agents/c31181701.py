from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PackingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(PackingState); def validate_specs(state: PackingState): results = []; if 'material' not in state['specs']: results.append('Missing material spec'); return {'validation_results': results}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()