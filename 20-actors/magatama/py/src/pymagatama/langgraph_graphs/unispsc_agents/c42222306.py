from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WasteProcessState(TypedDict): items: List[str]; status: str; safety_checks: bool; def validate_waste(state: WasteProcessState): state['safety_checks'] = True; return state; def verify_compliance(state: WasteProcessState): state['status'] = 'COMPLIANT' if state['safety_checks'] else 'FAILED'; return state; graph = StateGraph(WasteProcessState); graph.add_node('validate', validate_waste); graph.add_node('compliance', verify_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()