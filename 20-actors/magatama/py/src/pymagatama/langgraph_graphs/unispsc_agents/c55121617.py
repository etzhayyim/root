from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LabelState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: LabelState): state['validation_passed'] = all(k in state['specs'] for k in ['thickness', 'material']); return state; graph = StateGraph(LabelState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()