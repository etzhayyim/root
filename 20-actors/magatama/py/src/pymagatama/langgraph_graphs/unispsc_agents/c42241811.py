from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcessState(TypedDict): hardware_id: str; bio_compatibility_passed: bool; specs: dict; def validate_specs(state: ProcessState) -> ProcessState: state['bio_compatibility_passed'] = state['specs'].get('iso_cert') == '13485'; return state; def final_decision(state: ProcessState) -> str: return 'APPROVED' if state['bio_compatibility_passed'] else 'REJECTED'; graph = StateGraph(ProcessState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()