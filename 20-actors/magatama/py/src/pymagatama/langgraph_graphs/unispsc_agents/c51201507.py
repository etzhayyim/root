from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): cold_chain_passed: bool; batch_verified: bool; logs: List[str]; def check_temperature(state: PharmaState): state['cold_chain_passed'] = True; return state; def verify_batch(state: PharmaState): state['batch_verified'] = True; return state; graph = StateGraph(PharmaState); graph.add_node('verify_batch', verify_batch); graph.add_node('check_temp', check_temperature); graph.set_entry_point('verify_batch'); graph.add_edge('verify_batch', 'check_temp'); graph.add_edge('check_temp', END); app = graph.compile()