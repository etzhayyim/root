from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class JanitorialState(TypedDict): equipment_list: List[str]; validation_checks: List[str]; def validate_specs(state: JanitorialState): state['validation_checks'].append('Safety check confirmed'); return state; def asset_tagging(state: JanitorialState): state['validation_checks'].append('Tagging complete'); return state; graph = StateGraph(JanitorialState); graph.add_node('validate', validate_specs); graph.add_node('tagging', asset_tagging); graph.set_entry_point('validate'); graph.add_edge('validate', 'tagging'); graph.add_edge('tagging', END); graph = graph.compile()