from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): vendor_specs: dict; verification_result: bool; def validate_rubber_specs(state: ProcState): return {'verification_result': 'hardness' in state['vendor_specs'] and 'material' in state['vendor_specs']}; graph = StateGraph(ProcState); graph.add_node('validate', validate_rubber_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()