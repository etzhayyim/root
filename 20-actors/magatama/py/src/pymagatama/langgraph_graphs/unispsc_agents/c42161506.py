from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): device_id: str; compliance: bool; status: str; def validate_compliance(state: ProcureState): return {'compliance': True, 'status': 'Validated'} if state['device_id'] else {'compliance': False, 'status': 'Invalid ID'}; def update_status(state: ProcureState): return {'status': 'Ready for procurement'} if state['compliance'] else {'status': 'Review Required'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_compliance); graph.add_node('status_update', update_status); graph.add_edge('validate', 'status_update'); graph.add_edge('status_update', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()