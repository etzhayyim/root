from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): specs: dict; validation_results: list[str]; graph = StateGraph(BearingState); def validate_specs(state): return {'validation_results': ['CAD dimension check passed', 'Material grade verified']}; def check_inventory(state): return {'validation_results': state['validation_results'] + ['Stock availability confirmed']}; graph.add_node('validate', validate_specs); graph.add_node('inventory', check_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); compiled_graph = graph.compile()