from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AccumulatorState(TypedDict): bladders: List[str]; validated: bool; log: List[str]; def validation_node(state: AccumulatorState): return {'validated': all(b.endswith('_cert') for b in state.get('bladders', []))}; def logger_node(state: AccumulatorState): return {'log': ['Validation complete']}; graph = StateGraph(AccumulatorState); graph.add_node('validate', validation_node); graph.add_node('log', logger_node); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.set_entry_point('validate'); graph = graph.compile()