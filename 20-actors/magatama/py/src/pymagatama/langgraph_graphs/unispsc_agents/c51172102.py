from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): chemical_name: str; purity: float; compliance_doc: bool; approved: bool; def validate_purity(state: PharmaState): state['approved'] = state['purity'] >= 99.0; return state; def check_compliance(state: PharmaState): state['approved'] = state['approved'] and state['compliance_doc']; return state; builder = StateGraph(PharmaState); builder.add_node('validate', validate_purity); builder.add_node('compliance', check_compliance); builder.set_entry_point('validate'); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); graph = builder.compile()