from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): vendor_id: str; quality_score: float; storage_compliant: bool; graph = StateGraph(ProcureState); def check_quality(state: ProcureState): return {'quality_score': 1.0 if state['quality_score'] >= 0.95 else 0.0}; def verify_storage(state: ProcureState): return {'storage_compliant': True}; graph.add_node('quality_check', check_quality); graph.add_node('storage_verify', verify_storage); graph.set_entry_point('quality_check'); graph.add_edge('quality_check', 'storage_verify'); graph.add_edge('storage_verify', END); compiled_graph = graph.compile()