from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoverallState(TypedDict): specs: dict; validation_results: List[str]; def validate_barrier_performance(state: CoverallState): props = state['specs']; logs = []; if props.get('ASTM_F1671') != 'Pass': logs.append('Barrier failed'); return {'validation_results': logs}; def check_density(state: CoverallState): return {'validation_results': ['Density checked']}; graph = StateGraph(CoverallState); graph.add_node('validate_barrier', validate_barrier_performance); graph.add_node('check_density', check_density); graph.set_entry_point('validate_barrier'); graph.add_edge('validate_barrier', 'check_density'); graph.add_edge('check_density', END); app = graph.compile()