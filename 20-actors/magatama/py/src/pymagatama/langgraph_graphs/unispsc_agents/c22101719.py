from langgraph.graph import StateGraph, END; from typing import TypedDict; class SteelState(TypedDict): specs: dict; valid: bool; def validate_steel(state: SteelState): state['valid'] = all(k in state['specs'] for k in ['grade', 'thickness']); return state; graph = StateGraph(SteelState); graph.add_node('validate', validate_steel); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()