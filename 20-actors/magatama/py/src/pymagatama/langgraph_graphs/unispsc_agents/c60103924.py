from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BiologyResourceState(TypedDict): resource_id: str; content_type: str; validation_passed: bool; def validate_resource(state: BiologyResourceState): state['validation_passed'] = bool(state.get('resource_id')); return state; def check_compliance(state: BiologyResourceState): return {'validation_passed': True} if state['validation_passed'] else {'validation_passed': False}; graph = StateGraph(BiologyResourceState); graph.add_node('validate', validate_resource); graph.add_node('check', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()