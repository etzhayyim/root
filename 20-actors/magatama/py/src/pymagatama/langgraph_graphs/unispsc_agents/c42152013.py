from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AnalyzerState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: AnalyzerState): state['valid'] = 'DICOM_compatibility' in state['specs'] and state['specs']['resolution'] > 600; return state; graph = StateGraph(AnalyzerState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()