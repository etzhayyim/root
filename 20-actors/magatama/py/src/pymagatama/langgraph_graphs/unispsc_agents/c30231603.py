from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SiteOfficeState(TypedDict): site_id: str; structural_specs: dict; compliance_check: bool; def val_structural(state): return {'compliance_check': all(k in state['structural_specs'] for k in ['dimension', 'load_bearing'])}; def log_deployment(state): print(f'Deploying structure to {state['site_id']}'); return {}; graph = StateGraph(SiteOfficeState); graph.add_node('validate', val_structural); graph.add_node('deploy', log_deployment); graph.set_entry_point('validate'); graph.add_edge('validate', 'deploy'); graph.add_edge('deploy', END); compiled_graph = graph.compile()