from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalKitState(TypedDict): kit_id: str; components: List[str]; is_sterile: bool; validation_passed: bool; def validate_kit(state: DentalKitState): state['validation_passed'] = state['is_sterile'] and len(state['components']) > 0; return state; graph = StateGraph(DentalKitState); graph.add_node('validate', validate_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()