from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(ProcState); def validate_specs(state: ProcState): state['compliance_check'] = 'Axis' in state['specs'] and 'Spindle' in state['specs']; return state; def export_review(state: ProcState): if state['compliance_check']: print('Routing to dual-use export automated review.'); return state; graph.add_node('validation', validate_specs); graph.add_node('export_review', export_review); graph.set_entry_point('validation'); graph.add_edge('validation', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()