from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalDeviceState(TypedDict): device_specs: dict; compliance_checks: List[str]; approved: bool; def validate_specs(state: DentalDeviceState): print('Validating biocompatibility and ISO standards...'); state['compliance_checks'].append('ISO_13485_VERIFIED'); return state; def check_regulatory(state: DentalDeviceState): state['approved'] = True if 'ISO_13485_VERIFIED' in state['compliance_checks'] else False; return state; graph = StateGraph(DentalDeviceState); graph.add_node('validate', validate_specs); graph.add_node('verify', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); graph.compile()