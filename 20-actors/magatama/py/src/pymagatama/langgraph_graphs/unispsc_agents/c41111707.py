from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): magnification: float; measured_data: List[float]; calibration_valid: bool; def validate_spec(state: GraphState): return {'calibration_valid': state['magnification'] > 0}; def process_data(state: GraphState): return {'measured_data': [x * state['magnification'] for x in state['measured_data']]}; graph = StateGraph(GraphState); graph.add_node('validate', validate_spec); graph.add_node('process', process_data); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()