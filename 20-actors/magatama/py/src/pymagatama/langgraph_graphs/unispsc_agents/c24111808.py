from langgraph.graph import StateGraph, END; from typing import TypedDict; class TankProcurementState(TypedDict): capacity: int; material: str; approved: bool; def validate_tank(state: TankProcurementState): return {'approved': state['capacity'] > 0 and state['material'] != 'none'}; graph = StateGraph(TankProcurementState); graph.add_node('validation', validate_tank); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()