from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): medicine: str; status: str; specs: dict; graph = StateGraph(DrugState); def validate_compliance(state: DrugState): return {'status': 'validated' if state['specs'].get('GMP_Certification_Number') else 'rejected'}; def check_temperature(state: DrugState): return {'status': 'temp_ok'}; graph.add_node('validate', validate_compliance); graph.add_node('check_temp', check_temperature); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_temp'); graph.add_edge('check_temp', END); app = graph.compile()