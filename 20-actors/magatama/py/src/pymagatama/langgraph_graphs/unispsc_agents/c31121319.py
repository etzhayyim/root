from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(CastingState); def validate_dimensions(state: CastingState): state['validation_results'].append('Size tolerance verified'); return state; def check_material_integrity(state: CastingState): state['validation_results'].append('Material audit passed'); return state; graph.add_node('validate', validate_dimensions); graph.add_node('material', check_material_integrity); graph.set_entry_point('validate'); graph.add_edge('validate', 'material'); graph.add_edge('material', END); graph = graph.compile()