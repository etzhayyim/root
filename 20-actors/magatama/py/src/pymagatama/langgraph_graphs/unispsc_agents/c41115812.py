from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GenState(TypedDict): items: List[str]; validated_items: List[str]; status: str; def validate_reagent(state: GenState): return {'validated_items': [i for i in state['items'] if 'Certification' in i], 'status': 'validated'}; graph = StateGraph(GenState); graph.add_node('validate', validate_reagent); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()