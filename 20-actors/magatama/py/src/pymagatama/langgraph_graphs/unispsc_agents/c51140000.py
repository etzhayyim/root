from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CNSDrugState(TypedDict): medicine_name: str; dosage: str; compliance_docs: List[str]; approved: bool; def validate_regulations(state: CNSDrugState): return {'approved': len(state['compliance_docs']) > 2}; def check_inventory(state: CNSDrugState): return {'approved': True if 'GMP' in state['compliance_docs'] else False}; graph = StateGraph(CNSDrugState); graph.add_node('validate', validate_regulations); graph.add_node('inventory', check_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); compiled_graph = graph.compile()