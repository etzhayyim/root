from langgraph.graph import StateGraph, END; from typing import TypedDict; class PrinterState(TypedDict): doc_id: str; compliance_check: bool; image_quality_score: float; def validate_dicom(state: PrinterState): return {'compliance_check': True} if state['doc_id'] else {'compliance_check': False}; def assess_performance(state: PrinterState): return {'image_quality_score': 95.0}; graph = StateGraph(PrinterState); graph.add_node('validate', validate_dicom); graph.add_node('assess', assess_performance); graph.add_edge('validate', 'assess'); graph.add_edge('assess', END); graph.set_entry_point('validate'); graph = graph.compile()