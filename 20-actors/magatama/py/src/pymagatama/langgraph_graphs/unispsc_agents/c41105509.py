from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): vendor_id: str; qc_passed: bool; storage_temp: float; def validate_integrity(state: KitState): state['qc_passed'] = state['storage_temp'] <= -20.0; return state; def check_compliance(state: KitState): return 'compliance_check' if state['qc_passed'] else END; graph = StateGraph(KitState); graph.add_node('validate', validate_integrity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()