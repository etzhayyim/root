from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupportState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: SupportState): state['compliance'] = all(k in state['specs'] for k in ['material', 'compression']); return state; def check_certification(state: SupportState): state['approved'] = state['specs'].get('iso_cert', False); return state; graph = StateGraph(SupportState); graph.add_node('validate', validate_specs); graph.add_node('certify', check_certification); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph.compile()