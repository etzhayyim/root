from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; validation_passed: bool; certifications: List[str]; def validate_purity(state: DrugState): state['validation_passed'] = 'USP' in state['certifications']; return state; builder = StateGraph(DrugState); builder.add_node('validate', validate_purity); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()