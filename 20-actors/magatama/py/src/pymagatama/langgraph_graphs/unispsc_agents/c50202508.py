from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class JuiceState(TypedDict): juice_type: str; brix_value: float; certification: List[str]; inspection_passed: bool; def check_quality(state: JuiceState): state['inspection_passed'] = state['brix_value'] >= 12.0 and 'organic' in state['certification']; return state; builder = StateGraph(JuiceState); builder.add_node('quality_check', check_quality); builder.set_entry_point('quality_check'); builder.add_edge('quality_check', END); graph = builder.compile()