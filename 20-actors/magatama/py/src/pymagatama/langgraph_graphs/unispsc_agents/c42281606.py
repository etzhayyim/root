from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FumigatorState(TypedDict): vendor_certs: List[str]; safety_check_passed: bool; specs_verified: bool; def check_compliance(state: FumigatorState): return {'safety_check_passed': 'ISO_13485' in state['vendor_certs']}; def validate_specs(state: FumigatorState): return {'specs_verified': state['safety_check_passed']}; graph = StateGraph(FumigatorState); graph.add_node('compliance', check_compliance); graph.add_node('validation', validate_specs); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'validation'); graph.add_edge('validation', END); compile_graph = graph.compile()