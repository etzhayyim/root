from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): input_specs: dict; validated_specs: dict; def validate_specs(state: FurnitureState): return {'validated_specs': {k: v for k, v in state['input_specs'].items() if v is not None}}; def assemble_workflow(state: FurnitureState): return {'validated_specs': {**state['validated_specs'], 'status': 'ready_for_procurement'}}; graph = StateGraph(FurnitureState); graph.add_node('validate', validate_specs); graph.add_node('assemble', assemble_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph = graph.compile()