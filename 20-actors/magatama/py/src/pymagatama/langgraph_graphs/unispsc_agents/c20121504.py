from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class HydraulicState(TypedDict): pressure: float; flow: float; validated: bool; log: Annotated[list[str], operator.add]; def validate_valve_specs(state: HydraulicState): state['validated'] = state['pressure'] > 0 and state['flow'] > 0; state['log'] = ['Specs validated']; return state; def check_compliance(state: HydraulicState): state['log'] = ['Compliance checked for dual-use criteria']; return state; graph = StateGraph(HydraulicState); graph.add_node('validate', validate_valve_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()