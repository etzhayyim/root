from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AutopsyState(TypedDict): vendor_certs: List[str]; airflow_test: bool; compliance_status: str; def validate_spec(state: AutopsyState): return {'compliance_status': 'compliant' if 'HEPA' in state['vendor_certs'] else 'non-compliant'}; graph = StateGraph(AutopsyState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()