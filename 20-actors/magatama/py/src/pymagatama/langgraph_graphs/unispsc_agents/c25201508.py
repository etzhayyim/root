from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftPartState(TypedDict): part_id: str; quality_docs: List[str]; approved: bool; def validate_airworthiness(state: AircraftPartState): state['approved'] = len(state['quality_docs']) >= 3; return state; def check_export_control(state: AircraftPartState): if 'ITAR_Export_License' in state['quality_docs']: state['approved'] = True; return state; graph = StateGraph(AircraftPartState); graph.add_node('validate', validate_airworthiness); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()