from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CitrusState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_batch(state: CitrusState):  for item in state['items']:    if 'brix' not in item: state['validation_errors'].append('Missing Brix data'); return state; def check_compliance(state: CitrusState):  # Logic for FDA/Health compliance verification; return state; graph = StateGraph(CitrusState); graph.add_node('validate', validate_batch); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); app = graph.compile()