from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; def check_spec(state: ProcState): state['validated'] = all(['material' in item.lower() for item in state['items']]); return state; workflow = StateGraph(ProcState); workflow.add_node('validate', check_spec); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()