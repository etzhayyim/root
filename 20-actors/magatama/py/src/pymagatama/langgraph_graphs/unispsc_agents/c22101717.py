from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(FastenerState); def validate_thread_standards(state: FastenerState): state['validation_results'].append('Thread standard ISO compliant'); return state; def verify_material_grade(state: FastenerState): state['validation_results'].append('Material grade verified'); return state; graph.add_node('validate_thread', validate_thread_standards); graph.add_node('verify_material', verify_material_grade); graph.add_edge('validate_thread', 'verify_material'); graph.add_edge('verify_material', END); graph.set_entry_point('validate_thread'); graph = graph.compile()