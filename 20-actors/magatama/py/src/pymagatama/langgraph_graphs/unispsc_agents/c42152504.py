from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DentalOrderState(TypedDict): items: Annotated[list, operator.add]; validation_results: dict; def validate_dental_material(state: DentalOrderState): return {'validation_results': {'compliant': True}}; def process_order(state: DentalOrderState): return {'items': ['Processed Dressing Kit']}; graph = StateGraph(DentalOrderState); graph.add_node('validate', validate_dental_material); graph.add_node('process', process_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()