from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PressColumnState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: PressColumnState): return {'valid': state['specs'].get('tensile_strength', 0) > 800}; def finalize(state: PressColumnState): return {'logs': ['Column specs approved']}; graph = StateGraph(PressColumnState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()