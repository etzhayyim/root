from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: FilterState): s = state['specs']; state['valid'] = 'Micron Rating' in s and s['Micron Rating'] > 0; return state; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()