from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): items: List[str]; validated: bool; certs: dict; def validate_assembly_specs(state: AssemblyState): return {'validated': all(item for item in state['items'])}; def check_certs(state: AssemblyState): return {'certs': {'status': 'verified'}}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_assembly_specs); graph.add_node('certify', check_certs); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); compiled_graph = graph.compile()