from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetySleeveState(TypedDict): specs: dict; compliance_score: float; approved: bool; def validate_sleeve(state: SafetySleeveState): rating = state['specs'].get('cut_rating', 0); state['compliance_score'] = 1.0 if rating >= 3 else 0.5; return state; graph = StateGraph(SafetySleeveState); graph.add_node('validate', validate_sleeve); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()