from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; valid: bool; audit_log: List[str]; def validate_purity(state: ProcState): state['valid'] = state['specs'].get('purity', 0) >= 99.0; state['audit_log'].append('Purity check completed'); return state; def check_regulation(state: ProcState): state['audit_log'].append('Regulatory verification performed'); return state; graph = StateGraph(ProcState); graph.add_node('purity_check', validate_purity); graph.add_node('regulatory_check', check_regulation); graph.add_edge('purity_check', 'regulatory_check'); graph.add_edge('regulatory_check', END); graph.set_entry_point('purity_check'); graph = graph.compile()