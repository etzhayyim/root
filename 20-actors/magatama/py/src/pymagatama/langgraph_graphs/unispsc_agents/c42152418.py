from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; result: dict; def validate_gypsum(state: State): return {'result': {'valid': 'ISO6873' in state['input_data']}}; def process_procurement(state: State): return {'result': {**state['result'], 'status': 'approved'}}; graph = StateGraph(State); graph.add_node('validate', validate_gypsum); graph.add_node('process', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()