from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SteelState(TypedDict): ingot_data: dict; verification_passed: bool; certs: List[str]; def validate_material(state: SteelState) -> dict: material = state['ingot_data']; passed = material.get('purity') > 99.0 and 'ISO_cert' in state['certs']; return {'verification_passed': passed}; graph = StateGraph(SteelState); graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()