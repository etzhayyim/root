from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RadioState(TypedDict): items: List[str]; validated: bool; safety_check: bool; def check_safety(state: RadioState): return {'safety_check': True}; def validate_specs(state: RadioState): return {'validated': True}; graph = StateGraph(RadioState); graph.add_node('safety', check_safety); graph.add_node('validate', validate_specs); graph.set_entry_point('safety'); graph.add_edge('safety', 'validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()