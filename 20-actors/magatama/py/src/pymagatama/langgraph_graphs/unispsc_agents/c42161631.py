from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): water_purity: float; validation_log: List[str]; graph = StateGraph(AnalysisState); def validate_purity(state: AnalysisState): return {'validation_log': state['validation_log'] + ['Purity verified']}; def check_filtration(state: AnalysisState): return {'validation_log': state['validation_log'] + ['Filter integrity checked']}; graph.add_node('purity', validate_purity); graph.add_node('filtration', check_filtration); graph.set_entry_point('purity'); graph.add_edge('purity', 'filtration'); graph.add_edge('filtration', END); graph = graph.compile()