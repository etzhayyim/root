from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CameraAssemblyState(TypedDict): camera_specs: dict; validation_results: List[str]; def validate_sensor(state: CameraAssemblyState): res = 'Pass' if 'resolution' in state['camera_specs'] else 'Fail'; return {'validation_results': [f'Sensor Validation: {res}']}; def check_export_compliance(state: CameraAssemblyState): return {'validation_results': state['validation_results'] + ['Compliance Check: Approved']}; graph = StateGraph(CameraAssemblyState); graph.add_node('validate_sensor', validate_sensor); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate_sensor'); graph.add_edge('validate_sensor', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()