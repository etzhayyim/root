from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_id: str; compliance_docs: List[str]; qc_verified: bool; def check_regulatory(state: ProcState): return {'compliance_docs': ['DEA_Permit', 'GMP_Cert']}; def verify_batch_data(state: ProcState): return {'qc_verified': True}; graph = StateGraph(ProcState); graph.add_node('regulatory', check_regulatory); graph.add_node('qc', verify_batch_data); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'qc'); graph.add_edge('qc', END); graph = graph.compile()