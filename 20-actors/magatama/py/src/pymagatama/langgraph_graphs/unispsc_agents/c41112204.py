from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; validation_report: list; status: str; def validate_pyrometer_specs(state: ProcureState): specs = state['specs']; errors = []; if 'range' not in specs: errors.append('Range missing'); return {'validation_report': errors, 'status': 'valid' if not errors else 'invalid'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_pyrometer_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()