from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadioBioState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_physics_specs(state: RadioBioState): specs = state['specs']; errors = []; if 'dose_rate_range' not in specs: errors.append('Missing dose rate'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(RadioBioState); graph.add_node('validate', validate_physics_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()