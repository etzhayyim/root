from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SequenceState(TypedDict): vector_id: str; sequence_data: str; validation_status: bool; def validate_sequence(state: SequenceState) -> SequenceState: print(f'Validating vector {state["vector_id"]}...'); state['validation_status'] = len(state['sequence_data']) > 0; return state; graph = StateGraph(SequenceState); graph.add_node('validate', validate_sequence); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()