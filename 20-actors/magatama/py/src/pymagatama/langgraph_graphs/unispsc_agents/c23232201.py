from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_id: str; specs: dict; valid: bool; def validate_specs(state: ToolState) -> ToolState: state['valid'] = all(k in state['specs'] for k in ['material', 'tolerance']); return state; def finalize_order(state: ToolState) -> ToolState: print(f'Processing procurement for {state['tool_id']}'); return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()