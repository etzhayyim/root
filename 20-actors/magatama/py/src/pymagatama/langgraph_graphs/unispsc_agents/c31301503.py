from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ForgingState): state['valid'] = all(k in state['specs'] for k in ['material', 'tolerance']); return {'valid': state['valid']}; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()