from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HydraulicState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: HydraulicState): state['valid'] = 'Pressure Rating' in state['specs'] and 'Thread Type' in state['specs']; state['log'].append('Validation completed'); return state; graph = StateGraph(HydraulicState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()