from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CameraState(TypedDict): camera_specs: dict; validation_results: list[str]; graph = StateGraph(CameraState); def validate_sensor(state: CameraState): return {'validation_results': ['Sensor spec verified']}; def check_export_control(state: CameraState): return {'validation_results': ['Export compliance checked']}; graph.add_node('validate_sensor', validate_sensor); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate_sensor'); graph.add_edge('validate_sensor', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()