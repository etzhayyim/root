from langgraph.graph import StateGraph, END; from typing import TypedDict; class BearingState(TypedDict): spec: dict; is_valid: bool; def validate_spec(state: BearingState): state['is_valid'] = 'precision_grade' in state['spec'] and state['spec']['load_capacity_rating'] > 0; return state; graph = StateGraph(BearingState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()