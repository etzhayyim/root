from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): equipment_id: str; data: dict; status: str; def validate_specs(state: State): state['status'] = 'VALIDATED' if 'frequency_range_mhz' in state['data'] else 'INCOMPLETE'; return state; def check_compliance(state: State): state['status'] = 'COMPLIANT' if state.get('status') == 'VALIDATED' else 'REJECTED'; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()