from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LaserProcState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; compliance_flag: bool; def validate_laser_spec(state: LaserProcState): result = 'Valid' if state['input_spec'].get('power_output_watts', 0) > 0 else 'Invalid'; return {'validation_results': [result], 'compliance_flag': result == 'Valid'}; def check_export_control(state: LaserProcState): return {'compliance_flag': state['input_spec'].get('dual_use_certified', False)}; graph = StateGraph(LaserProcState); graph.add_node('validate', validate_laser_spec); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()