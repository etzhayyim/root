from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class APIState(TypedDict): input_data: dict; validation_results: List[str]; approved: bool; def validate_api_spec(state: APIState): results = []; certs = state['input_data'].get('certs', []); if 'GMP' not in certs: results.append('Missing GMP certification'); return {'validation_results': results, 'approved': len(results) == 0}; graph = StateGraph(APIState); graph.add_node('validate', validate_api_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()