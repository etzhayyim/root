from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IncubatorState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def check_specs(state: IncubatorState): errors = []; if state['specs'].get('temp_range', 0) > 60: errors.append('Temperature range exceeds laboratory safety limits.'); return {'validation_errors': errors}; def validate_procurement(state: IncubatorState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(IncubatorState); graph.add_node('check_specs', check_specs); graph.add_node('validate', validate_procurement); graph.add_edge('check_specs', 'validate'); graph.add_edge('validate', END); graph.set_entry_point('check_specs'); compile_graph = graph.compile()