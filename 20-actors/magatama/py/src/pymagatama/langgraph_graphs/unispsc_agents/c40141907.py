from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DuctSpecState(TypedDict): specs: dict; validation_report: list[str]; def validate_materials(state: DuctSpecState): material = state['specs'].get('material'); status = 'Valid' if material == 'Brass' else 'Invalid'; return {'validation_report': [f'Material check: {status}']}; def build_graph(): graph = StateGraph(DuctSpecState); graph.add_node('validate', validate_materials); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = build_graph()