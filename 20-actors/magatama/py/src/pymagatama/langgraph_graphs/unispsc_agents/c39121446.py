from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): items: List[dict]; validation_results: dict; def validate_terminals(state: GraphState): validated = [i for i in state['items'] if i.get('awg') and i.get('voltage')]; return {'validation_results': {'status': 'success' if validated else 'fail'}}; graph = StateGraph(GraphState); graph.add_node('validate', validate_terminals); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()