from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SculptureProcurement(TypedDict): art_data: dict; verification_status: str; def validate_provenance(state: SculptureProcurement): state['verification_status'] = 'verified' if 'doc' in state['art_data'] else 'pending'; return state; def handle_shipping(state: SculptureProcurement): state['verification_status'] = 'ready_for_transit'; return state; graph = StateGraph(SculptureProcurement); graph.add_node('verify', validate_provenance); graph.add_node('ship', handle_shipping); graph.set_entry_point('verify'); graph.add_edge('verify', 'ship'); graph.add_edge('ship', END); compiled_graph = graph.compile()