from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): content: str; validated: bool; metadata: dict; def validate_resource(state: State): return {'validated': 'precalculus' in state['content'].lower()}; def finalize_record(state: State): return {'metadata': {'status': 'approved' if state['validated'] else 'rejected'}}; graph = StateGraph(State); graph.add_node('validate', validate_resource); graph.add_node('finalize', finalize_record); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()