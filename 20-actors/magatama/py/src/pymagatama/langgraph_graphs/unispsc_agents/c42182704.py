from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CaliperState(TypedDict): vendor_id: str; specs: dict; validation_passed: bool; graph = StateGraph(CaliperState); def validate_specs(state: CaliperState): state['validation_passed'] = 'accuracy_range' in state['specs'] and state['specs']['accuracy_range'] <= 0.5; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()