from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: List[str]; validation_results: List[str]; def validate_specs(state: ForgingState): return {'validation_results': ['PASS' if 'Mill Test Certificate' in state['specs'] else 'FAIL']}; def route_processing(state: ForgingState): return 'complete' if state['validation_results'][0] == 'PASS' else END; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()