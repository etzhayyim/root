from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BoltState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(BoltState); def validate_specs(state: BoltState): return {'validation_results': ['Strength verified'] if state['specs'].get('tensile_strength') else ['Error: Missing strength']}; def report_status(state: BoltState): return {'validation_results': state['validation_results'] + ['Certification check complete']}; graph.add_node('validate', validate_specs); graph.add_node('report', report_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()