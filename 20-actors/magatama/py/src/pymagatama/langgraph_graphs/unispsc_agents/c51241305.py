from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TannicAcidState(TypedDict): chemical_data: dict; validation_report: List[str]; graph = StateGraph(TannicAcidState); def validate_purity(state: TannicAcidState): state['validation_report'].append('Purity check completed'); return state; def check_hazmat(state: TannicAcidState): state['validation_report'].append('Hazmat classification verified'); return state; graph.add_node('purity_check', validate_purity); graph.add_node('hazmat_check', check_hazmat); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'hazmat_check'); graph.add_edge('hazmat_check', END); graph = graph.compile()