from langgraph.graph import StateGraph, END; from typing import TypedDict; class ManufacturingState(TypedDict): specs: dict; valid: bool; def check_specs(state: ManufacturingState): state['valid'] = 'alloy_composition' in state['specs'] and state['specs']['tensile_strength_mpa'] > 0; return state; graph = StateGraph(ManufacturingState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()