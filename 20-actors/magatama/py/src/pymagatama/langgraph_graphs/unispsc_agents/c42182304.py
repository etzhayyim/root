from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PsychGraphState(TypedDict): kit_id: str; compliance_verified: bool; validation_report: str; def validate_kit(state: PsychGraphState): compliance = True if state['kit_id'] else False; return {'compliance_verified': compliance, 'validation_report': 'Validated' if compliance else 'Missing ID'}; graph = StateGraph(PsychGraphState); graph.add_node('validate', validate_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()