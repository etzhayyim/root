from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DilatorState(TypedDict): vendor_id: str; kit_contents: List[str]; qc_passed: bool; def validate_kit(state: DilatorState): return {'qc_passed': len(state['kit_contents']) > 0}; def check_compliance(state: DilatorState): return {'qc_passed': state['qc_passed'] and True}; graph = StateGraph(DilatorState); graph.add_node('validate', validate_kit); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()