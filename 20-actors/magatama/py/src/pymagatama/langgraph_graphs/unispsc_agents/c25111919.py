from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KeelState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: KeelState): state['validation_report'] = 'Valid' if state['specs'].get('material') else 'Invalid'; return state; def check_durability(state: KeelState): return {'validation_report': 'Passed durability test'} if state['specs'].get('impact_rating', 0) > 8 else {'validation_report': 'Failed'}; graph = StateGraph(KeelState); graph.add_node('validate', validate_specs); graph.add_node('durability', check_durability); graph.set_entry_point('validate'); graph.add_edge('validate', 'durability'); graph.add_edge('durability', END); graph = graph.compile()