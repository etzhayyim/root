from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MarketState(TypedDict): nips: List[str]; validation_results: List[str]; def validate_tip(state: MarketState): return {'validation_results': ['Integrity check passed']}; def finalize_logistics(state: MarketState): return {'validation_results': ['Shipping compliance confirmed']}; graph = StateGraph(MarketState); graph.add_node('validate', validate_tip); graph.add_node('logistics', finalize_logistics); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph.set_entry_point('validate'); graph = graph.compile()