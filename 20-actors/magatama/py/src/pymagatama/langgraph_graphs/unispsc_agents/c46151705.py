from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class InkRollerState(TypedDict): specs: dict; validation_report: list[str]; def validate_roller(state: InkRollerState): report = []; if not state['specs'].get('roller_material'): report.append('Missing material spec'); return {'validation_report': report}; def finalize_procurement(state: InkRollerState): return {'validation_report': state['validation_report'] + ['Procurement ready']}; graph = StateGraph(InkRollerState); graph.add_node('validate', validate_roller); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()