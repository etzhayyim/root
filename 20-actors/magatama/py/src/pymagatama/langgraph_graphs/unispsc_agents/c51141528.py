from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): batch_id: str; quality_score: float; is_compliant: bool; def validate_purity(state: DrugProcurementState) -> DrugProcurementState: state['is_compliant'] = state['quality_score'] >= 99.5; return state; def check_regulatory(state: DrugProcurementState) -> DrugProcurementState: return state; graph = StateGraph(DrugProcurementState); graph.add_node('validate', validate_purity); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()