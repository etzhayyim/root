from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OrgState(TypedDict): data: dict; status: str; def validate_spec(state: OrgState): state['status'] = 'validation_complete'; return state; def check_compatibility(state: OrgState): state['status'] = 'compatibility_verified'; return state; graph = StateGraph(OrgState); graph.add_node('validate', validate_spec); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()