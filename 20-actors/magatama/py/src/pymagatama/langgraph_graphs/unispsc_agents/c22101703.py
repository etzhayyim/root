from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MachineState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: MachineState): valid = state['specs'].get('power_rating_kw', 0) > 0; return {'valid': valid, 'log': ['Specs validated'] if valid else ['Invalid specs']}; graph = StateGraph(MachineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()