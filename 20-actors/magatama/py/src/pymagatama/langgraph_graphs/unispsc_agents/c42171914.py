from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TraumaKitState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_specs(state: TraumaKitState): state['validated'] = all(c in ['ISO13485', 'CE_MDR'] for c in state['certs']); return {'validated': state['validated']}; graph = StateGraph(TraumaKitState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()