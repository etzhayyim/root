from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; validation: bool; def validate_device(state: State): state['validation'] = 'ISO13485' in state['specs'].get('certifications', []) and state['specs'].get('is_compatible', False); return state; graph = StateGraph(State); graph.add_node('validate', validate_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()