from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class RobotState(TypedDict): messages: Annotated[Sequence[str], add_messages]; component_id: str; status: str; def validate_component(state: RobotState) -> RobotState: return {**state, 'status': 'VALIDATED'}; def process_integration(state: RobotState) -> RobotState: return {**state, 'status': 'INTEGRATED'}; graph = StateGraph(RobotState); graph.add_node('validate', validate_component); graph.add_node('integrate', process_integration); graph.set_entry_point('validate'); graph.add_edge('validate', 'integrate'); graph.add_edge('integrate', END); graph = graph.compile()