from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StorageState(TypedDict): items: List[dict]; validation_results: List[str]; def validate_storage_spec(state: StorageState): results = []; for item in state['items']: if item.get('load_capacity', 0) <= 0: results.append('Invalid load capacity'); return {'validation_results': results}; graph = StateGraph(StorageState); graph.add_node('validate', validate_storage_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()