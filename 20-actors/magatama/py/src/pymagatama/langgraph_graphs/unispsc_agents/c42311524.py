from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DressingState(TypedDict): items: List[str]; validated: bool; def validate_dressing(state: DressingState): state['validated'] = all(['sterile' in item.lower() for item in state['items']]); return state; graph = StateGraph(DressingState); graph.add_node('validate', validate_dressing); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()