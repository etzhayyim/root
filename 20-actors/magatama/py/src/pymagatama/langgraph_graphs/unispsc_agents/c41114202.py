from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): rod_specs: dict; validation_results: List[str]; def validate_accuracy(state: State): state['validation_results'].append('Accuracy verified against ISO standards'); return state; def check_material(state: State): state['validation_results'].append('Material grade confirmed'); return state; graph = StateGraph(State); graph.add_node('validate_accuracy', validate_accuracy); graph.add_node('check_material', check_material); graph.set_entry_point('validate_accuracy'); graph.add_edge('validate_accuracy', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()