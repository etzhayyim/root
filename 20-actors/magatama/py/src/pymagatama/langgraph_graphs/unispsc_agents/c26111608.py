from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SteamGenState(TypedDict): steam_pressure: float; compliance_certs: list; is_approved: bool; def validate_pressure(state: SteamGenState): return {'is_approved': state['steam_pressure'] < 500}; def check_certs(state: SteamGenState): return {'is_approved': 'ASME' in state['compliance_certs']}; graph = StateGraph(SteamGenState); graph.add_node('validate', validate_pressure); graph.add_node('certs', check_certs); graph.add_edge('validate', 'certs'); graph.add_edge('certs', END); graph.set_entry_point('validate'); graph = graph.compile()