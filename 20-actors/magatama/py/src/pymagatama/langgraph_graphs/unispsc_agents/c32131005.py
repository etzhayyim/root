from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ICProcurementState(TypedDict): package_specs: dict; validation_status: bool; export_control_flag: bool; def validate_specs(state: ICProcurementState): state['validation_status'] = 'JEDEC' in state['package_specs']; return state; def check_export_compliance(state: ICProcurementState): state['export_control_flag'] = state['package_specs'].get('pins', 0) > 1000; return state; graph = StateGraph(ICProcurementState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()