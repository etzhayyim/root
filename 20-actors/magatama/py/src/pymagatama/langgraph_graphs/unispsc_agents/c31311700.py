from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipeState(TypedDict): specs: dict; validation_results: List[str]; def validate_bonding(state: PipeState): results = []; if 'bonding_method' not in state['specs']: results.append('Missing bonding method'); return {'validation_results': results}; def check_specs(state: PipeState): return {'validation_results': ['Pressure test passed']}; graph = StateGraph(PipeState); graph.add_node('validate', validate_bonding); graph.add_node('spec_check', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'spec_check'); graph.add_edge('spec_check', END); graph = graph.compile()