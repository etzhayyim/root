from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeBendingState(TypedDict): tool_specs: dict; validation_results: List[str]; def validate_hickey(state: PipeBendingState): specs = state['tool_specs']; res = ['Passed' if specs.get('material_tensile') > 500 else 'Failed']; return {'validation_results': res}; graph = StateGraph(PipeBendingState); graph.add_node('validation', validate_hickey); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()