from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: BearingState): state['valid'] = 'tolerance' in state['specs'] and 'material' in state['specs']; return state; builder = StateGraph(BearingState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()