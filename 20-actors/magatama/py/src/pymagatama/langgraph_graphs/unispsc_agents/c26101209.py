from langgraph.graph import StateGraph, END; from typing import TypedDict; class MotorState(TypedDict): specs: dict; valid: bool; def validate_motor(state: MotorState): state['valid'] = 'rated_voltage' in state['specs'] and 'torque' in state['specs']; return state; builder = StateGraph(MotorState); builder.add_node('validate', validate_motor); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()