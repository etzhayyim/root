from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class UVState(TypedDict): model_id: str; intensity: float; validated: bool; def validate_intensity(state: UVState): state['validated'] = state['intensity'] > 250; return state; graph = StateGraph(UVState); graph.add_node('validate', validate_intensity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()