from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GlueState(TypedDict): vendor_certs: List[str]; safety_data: bool; spec_compliance: bool; def check_safety(state: GlueState): return {'safety_data': 'MSDS' in state['vendor_certs']}; def validate_specs(state: GlueState): return {'spec_compliance': True}; graph = StateGraph(GlueState); graph.add_node('safety', check_safety); graph.add_node('specs', validate_specs); graph.set_entry_point('safety'); graph.add_edge('safety', 'specs'); graph.add_edge('specs', END); graph = graph.compile()