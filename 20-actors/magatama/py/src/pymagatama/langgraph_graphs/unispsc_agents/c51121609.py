from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medication_name: str; validation_status: bool; compliance_docs: List[str]; def validate_batch(state: PharmState): state['validation_status'] = True; return state; def check_compliance(state: PharmState): state['compliance_docs'] = ['GMP', 'CoA']; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_batch); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()