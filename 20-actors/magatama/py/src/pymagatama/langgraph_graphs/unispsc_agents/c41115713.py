from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): status: str; spec_data: dict; approved: bool; def validate_tank_specs(state: ProcState): return {'approved': state['spec_data'].get('material') == 'Borosilicate'}; def check_seal(state: ProcState): return {'status': 'Seal Tested'} if state['spec_data'].get('seal') else {'status': 'Failed'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_tank_specs); graph.add_node('seal_test', check_seal); graph.add_edge('validate', 'seal_test'); graph.add_edge('seal_test', END); graph.set_entry_point('validate'); graph = graph.compile()