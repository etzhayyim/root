from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): vendor_certs: List[str]; supply_chain_valid: bool; api_purity: float; def validate_gmp(state: DrugProcurementState): return {'supply_chain_valid': 'GMP' in state['vendor_certs']}; def check_purity(state: DrugProcurementState): return {'supply_chain_valid': state['api_purity'] >= 99.0}; graph = StateGraph(DrugProcurementState); graph.add_node('gmp_check', validate_gmp); graph.add_node('purity_check', check_purity); graph.set_entry_point('gmp_check'); graph.add_edge('gmp_check', 'purity_check'); graph.add_edge('purity_check', END); graph = graph.compile()