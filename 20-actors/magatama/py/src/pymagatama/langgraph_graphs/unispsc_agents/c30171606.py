from langgraph.graph import StateGraph, END; from typing import Dict, Any; class WindowState(Dict[str, Any]): pass; graph = StateGraph(WindowState); def validate_specs(state: WindowState): return state; def check_compliance(state: WindowState): return state; graph.add_node('validate_specs', validate_specs); graph.add_node('compliance_check', check_compliance); graph.add_edge('validate_specs', 'compliance_check'); graph.add_edge('compliance_check', END); graph.set_entry_point('validate_specs'); graph = graph.compile()