from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medicine_name: str; validation_status: bool; compliance_docs: List[str]; def validate_batch(state: PharmaState): return {'validation_status': True}; def check_compliance(state: PharmaState): return {'compliance_docs': ['GMP', 'CoA']}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_batch); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); app = graph.compile()