from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CableState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: CableState): results = []; if state['specs'].get('tensile_strength', 0) < 500: results.append('Insufficient tensile strength'); return {'validation_results': results}; def report_status(state: CableState): return {'validation_results': state['validation_results'] + ['Validation Complete']}; graph = StateGraph(CableState); graph.add_node('validate', validate_specs); graph.add_node('report', report_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()