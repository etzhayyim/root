from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): active_ingredient: str; purity: float; status: str; def validate_purity(state: PharmaState): return {'status': 'approved' if state['purity'] >= 0.99 else 'rejected'}; def finalize_procurement(state: PharmaState): return {'status': 'procurement_complete'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()