from langgraph.graph import StateGraph, END; from typing import TypedDict; class IVState(TypedDict): vendor_certs: dict; quality_check: bool; graph = StateGraph(IVState); def validate_certs(state: IVState): return {'quality_check': 'ISO_13485' in state['vendor_certs']}; def log_compliance(state: IVState): print('Compliance Verified'); return state; graph.add_node('validate', validate_certs); graph.add_node('log', log_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.compile()