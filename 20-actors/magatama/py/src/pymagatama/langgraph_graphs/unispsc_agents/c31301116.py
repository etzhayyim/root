from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): material_spec: str; purity_check: bool; weight: float; approval_status: str; def validate_metallurgy(state: ForgingState): state['purity_check'] = state['material_spec'] == '99.99'; return state; def check_high_value(state: ForgingState): state['approval_status'] = 'PENDING_SECURE_VERIFICATION' if state['weight'] > 0 else 'REJECTED'; return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_metallurgy); graph.add_node('finance_check', check_high_value); graph.set_entry_point('validate'); graph.add_edge('validate', 'finance_check'); graph.add_edge('finance_check', END); graph = graph.compile()