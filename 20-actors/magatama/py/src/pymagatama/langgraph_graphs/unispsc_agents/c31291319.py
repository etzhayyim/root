from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtrusionState(TypedDict): vendor_certs: List[str]; material_compliance: bool; geometry_ok: bool; def check_material(state): return {'material_compliance': True}; def validate_geometry(state): return {'geometry_ok': True}; graph = StateGraph(ExtrusionState); graph.add_node('check_material', check_material); graph.add_node('validate_geometry', validate_geometry); graph.set_entry_point('check_material'); graph.add_edge('check_material', 'validate_geometry'); graph.add_edge('validate_geometry', END); graph = graph.compile()