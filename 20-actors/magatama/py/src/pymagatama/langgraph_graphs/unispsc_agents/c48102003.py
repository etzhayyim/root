from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SaladBarState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: SaladBarState): specs = state['specs']; errors = []; if specs.get('temp_range', 0) > 4: errors.append('Temperature above 4C'); state['valid'] = len(errors) == 0; state['error_log'] = errors; return state; graph = StateGraph(SaladBarState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()