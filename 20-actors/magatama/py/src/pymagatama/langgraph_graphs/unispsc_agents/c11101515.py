from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): commodity_code: str; specs: Dict; quality_passed: bool; compliance_checked: bool; def validate_specs(state: ProcureState): return {'quality_passed': state['specs'].get('purity_percentage', 0) > 99.5}; def check_compliance(state: ProcureState): return {'compliance_checked': 'hazmat_certification' in state['specs']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()