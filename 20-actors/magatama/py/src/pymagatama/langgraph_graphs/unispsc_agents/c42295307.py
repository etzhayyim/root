from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_certs: List[str]; qc_passed: bool; item_id: str; def validate_certs(state: ProcState): return { 'qc_passed': 'ISO_13485' in state['vendor_certs'] }; def route_step(state: ProcState): return 'qc' if state['qc_passed'] else END; graph = StateGraph(ProcState); graph.add_node('validate', validate_certs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()