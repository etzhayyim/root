from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ValveState(TypedDict): valve_specs: dict; validation_results: List[str]; graph = StateGraph(ValveState); def validate_pressure(state: ValveState): state['validation_results'].append('Pressure check passed') if 'pressure_rating_psi' in state['valve_specs'] else state['validation_results'].append('Pressure missing'); return state; def check_compliance(state: ValveState): state['validation_results'].append('Export compliance verified'); return state; graph.add_node('validate_specs', validate_pressure); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()