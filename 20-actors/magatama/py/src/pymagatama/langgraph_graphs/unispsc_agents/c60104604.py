from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExperimentState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_inclined_plane_specs(state: ExperimentState): specs = state['specs']; errors = []; if specs.get('angle_max', 0) > 90: errors.append('Angle exceeds physical limit'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(ExperimentState); graph.add_node('validate', validate_inclined_plane_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()