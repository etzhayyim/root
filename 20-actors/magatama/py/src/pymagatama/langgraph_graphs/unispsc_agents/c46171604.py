from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class AlarmState(TypedDict): data: dict; validated: bool; router = StateGraph(AlarmState); def validate_specs(state: AlarmState): state['validated'] = all(k in state['data'] for k in ['cert', 'voltage']); return state; router.add_node('validate', validate_specs); router.set_entry_point('validate'); router.add_edge('validate', END); graph = router.compile()