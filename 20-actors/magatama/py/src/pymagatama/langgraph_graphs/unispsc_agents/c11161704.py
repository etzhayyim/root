from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalProcurementState(TypedDict): input_data: dict; validation_steps: Annotated[Sequence[str], operator.add]; status: str; def validate_purity(state: ChemicalProcurementState): return {'validation_steps': ['Purity check compliant'], 'status': 'VALIDATING'}; def check_safety_compliance(state: ChemicalProcurementState): return {'validation_steps': ['Safety Data Sheet verified'], 'status': 'COMPLIANT'}; builder = StateGraph(ChemicalProcurementState); builder.add_node('purity', validate_purity); builder.add_node('safety', check_safety_compliance); builder.set_entry_point('purity'); builder.add_edge('purity', 'safety'); builder.add_edge('safety', END); graph = builder.compile()