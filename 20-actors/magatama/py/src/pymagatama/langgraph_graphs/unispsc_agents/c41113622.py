from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class InductanceState(TypedDict): device_id: str; cal_data: dict; verified: bool; graph = StateGraph(InductanceState); def validate_calibration(state: InductanceState): state['verified'] = 'calibration_certificate' in state['cal_data']; return state; graph.add_node('validate', validate_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()