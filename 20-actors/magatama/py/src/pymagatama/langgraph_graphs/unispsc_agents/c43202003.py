from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MediaState(TypedDict): media_type: str; capacity_gb: float; valid: bool; def validate_media(state: MediaState): state['valid'] = state['capacity_gb'] >= 4.7; return state; graph = StateGraph(MediaState); graph.add_node('validate', validate_media); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()