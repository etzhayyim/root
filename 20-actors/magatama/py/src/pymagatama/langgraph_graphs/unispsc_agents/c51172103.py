from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): chemical_data: dict; risk_score: int; approved: bool; def validate_purity(state: ProcurementState): state['approved'] = state['chemical_data'].get('purity', 0) >= 99.0; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()