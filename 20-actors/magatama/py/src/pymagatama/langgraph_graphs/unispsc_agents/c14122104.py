from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ThermalState(TypedDict): code: str; specs: dict; validation: bool; def validate_specs(state: ThermalState): specs = state.get('specs', {}); state['validation'] = all([k in specs for k in ['gsm_weight', 'width_mm']]); return state; def route_procurement(state: ThermalState): return 'end' if state['validation'] else END; graph = StateGraph(ThermalState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()