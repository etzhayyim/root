from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RivetState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_rivets(state: RivetState): valid = state['specs'].get('tensile_strength', 0) > 400; return {'valid': valid, 'error_log': [] if valid else ['Insufficient tensile strength']}; graph = StateGraph(RivetState); graph.add_node('validate', validate_rivets); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()