from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): data: dict; errors: Annotated[list, operator.add]; graph = StateGraph(AssemblyState); def validate_weld(state: AssemblyState): return {'data': {'weld_status': 'passed'}}; def check_material(state: AssemblyState): return {'data': {'material': 'titanium_certified'}}; graph.add_node('validate_weld', validate_weld); graph.add_node('check_material', check_material); graph.set_entry_point('check_material'); graph.add_edge('check_material', 'validate_weld'); graph.add_edge('validate_weld', END); graph = graph.compile()