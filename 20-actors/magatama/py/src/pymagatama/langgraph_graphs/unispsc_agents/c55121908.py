from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SignHolderState(TypedDict): vendor: str; specs: dict; validation_passed: bool; def validate_specs(state: SignHolderState): state['validation_passed'] = 'Base Weight' in state['specs'] and state['specs']['Base Weight'] > 0; return state; def route_step(state: SignHolderState): return 'validate' if state['validation_passed'] else END; graph = StateGraph(SignHolderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()