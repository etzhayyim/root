from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(FurnitureState); def validate_specs(state: FurnitureState): report = []; missing = [f for f in ['material_type', 'load_capacity_kg'] if f not in state['specs']]; if missing: report.append(f'Missing required fields: {missing}'); return {'validation_report': report}; graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()