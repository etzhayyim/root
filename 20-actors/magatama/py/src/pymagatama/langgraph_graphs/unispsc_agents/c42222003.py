from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PumpState(TypedDict): device_id: str; specs: dict; approved: bool; def validate_specs(state: PumpState): state['approved'] = 'IEC 60601' in state['specs'].get('standards', []) return state; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()