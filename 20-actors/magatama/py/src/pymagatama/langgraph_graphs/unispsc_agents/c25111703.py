from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShipProcurementState(TypedDict): vessel_id: str; compliance_docs: List[str]; inspection_status: str; def validate_cargo_regs(state: ShipProcurementState): print('Validating ordnance safety...'); return {'inspection_status': 'verified'}; graph = StateGraph(ShipProcurementState); graph.add_node('validate', validate_cargo_regs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()