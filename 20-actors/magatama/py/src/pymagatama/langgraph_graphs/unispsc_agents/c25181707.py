from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrailerState(TypedDict): capacity: float; status: str; spec_validated: bool; def validate_specs(state: TrailerState): return {'spec_validated': state['capacity'] > 0}; def check_status(state: TrailerState): return {'status': 'Approved' if state['spec_validated'] else 'Rejected'}; graph = StateGraph(TrailerState); graph.add_node('validate', validate_specs); graph.add_node('check', check_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()