from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalSpecState(TypedDict): spec_data: dict; approval_status: bool; validation_log: List[str]; def validate_medical_spec(state: SurgicalSpecState): state['validation_log'].append('Checking ISO 13485 compliance'); state['approval_status'] = True if 'ISO_13485' in state['spec_data'] else False; return state; graph = StateGraph(SurgicalSpecState); graph.add_node('validate', validate_medical_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()