from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LivestockState(TypedDict): species: str; health_status: bool; certs: List[str]; def validate_health(state: LivestockState): return {'health_status': len(state['certs']) > 0}; def check_origin(state: LivestockState): return {'species': state['species']}; graph = StateGraph(LivestockState); graph.add_node('validate', validate_health); graph.add_node('origin', check_origin); graph.set_entry_point('validate'); graph.add_edge('validate', 'origin'); graph.add_edge('origin', END); graph = graph.compile()