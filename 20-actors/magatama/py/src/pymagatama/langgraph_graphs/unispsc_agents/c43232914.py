from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ModemState(TypedDict): firmware_version: str; compatibility_check: bool; export_control_flag: bool; def check_compatibility(state: ModemState): state['compatibility_check'] = True; return state; def verify_compliance(state: ModemState): state['export_control_flag'] = True; return state; graph = StateGraph(ModemState); graph.add_node('compatibility', check_compatibility); graph.add_node('compliance', verify_compliance); graph.set_entry_point('compatibility'); graph.add_edge('compatibility', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()