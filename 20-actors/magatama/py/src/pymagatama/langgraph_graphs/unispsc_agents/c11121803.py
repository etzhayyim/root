from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class MagnesiumState(TypedDict): chemical_purity: float; structural_integrity_score: float; compliance_verified: bool; log: Annotated[list[str], operator.add]; def validate_purity(state: MagnesiumState) -> dict: return {'compliance_verified': state['chemical_purity'] >= 99.9, 'log': ['Purity validation complete']}; def assess_structural(state: MagnesiumState) -> dict: return {'structural_integrity_score': 0.95, 'log': ['Structural assessment performed']}; graph = StateGraph(MagnesiumState); graph.add_node('purity_check', validate_purity); graph.add_node('structural_check', assess_structural); graph.add_edge('purity_check', 'structural_check'); graph.add_edge('structural_check', END); graph.set_entry_point('purity_check'); graph = graph.compile()