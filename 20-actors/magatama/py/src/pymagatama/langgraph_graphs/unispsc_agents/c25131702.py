from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftState(TypedDict): procurement_id: str; compliance_checks: List[str]; approved: bool; def check_compliance(state: AircraftState): state['compliance_checks'].append('ITAR_VERIFIED'); return {'compliance_checks': state['compliance_checks']}; def validate_specs(state: AircraftState): is_valid = True; return {'approved': is_valid}; graph = StateGraph(AircraftState); graph.add_node('compliance', check_compliance); graph.add_node('specs', validate_specs); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'specs'); graph.add_edge('specs', END); graph = graph.compile()