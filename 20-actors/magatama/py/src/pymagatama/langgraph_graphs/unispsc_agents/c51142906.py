from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_data: dict; validation_results: List[str]; graph = StateGraph(PharmState); def validate_purity(state: PharmState): state['validation_results'].append('Purity check passed') if state['chemical_data'].get('purity', 0) > 99.0 else state['validation_results'].append('Purity check failed'); return state; def check_compliance(state: PharmState): state['validation_results'].append('Compliance verified') if state['chemical_data'].get('regulatory_file') else state['validation_results'].append('Compliance missing'); return state; graph.add_node('purity_check', validate_purity); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()