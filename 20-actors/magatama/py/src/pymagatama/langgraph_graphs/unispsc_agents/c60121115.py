from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validation_passed: bool; def validate_materials(state: State): state['validation_passed'] = all(item in ['paper', 'plastic'] for item in state['items']); return state; graph = StateGraph(State); graph.add_node('validate', validate_materials); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()