from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CommodityState(TypedDict): content: str; validation_status: bool; alerts: List[str]; def validate_purity(state: CommodityState): state['validation_status'] = 'preservative' in state['content'].lower(); return state; def check_compliance(state: CommodityState): state['alerts'] = ['Regulated Health Product'] if state['validation_status'] else []; return state; graph = StateGraph(CommodityState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()