from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; compliance_verified: bool; def validate_quality(state: PharmState): state['compliance_verified'] = state['purity'] >= 99.0; return state; def check_regulatory(state: PharmState): print(f'Checking {state["chemical_name"]} regulations...'); return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_quality); graph.add_node('compliance', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()