from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HarnessState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: HarnessState) -> HarnessState: state['valid'] = 'wire_gauge' in state['specs'] and 'voltage_rating' in state['specs']; state['logs'].append('Validation completed'); return state; graph = StateGraph(HarnessState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()