from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; components: List[str]; inspection_passed: bool; def validate_components(state: KitState): state['inspection_passed'] = 'O2_tank' in state['components'] and 'mask' in state['components']; return state; def check_regulatory(state: KitState): print(f'Checking FDA/ISO status for {state['kit_id']}'); return state; graph = StateGraph(KitState); graph.add_node('validate', validate_components); graph.add_node('regulatory', check_regulatory); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); graph = graph.compile()