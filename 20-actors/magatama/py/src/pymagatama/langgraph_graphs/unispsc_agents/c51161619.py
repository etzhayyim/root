from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): status: str; spec_data: dict; compliance_check: bool; def validate_spec(state: ProcState): state['compliance_check'] = 'Purity' in state['spec_data'] and state['spec_data']['Purity'] >= 99.0; return state; def check_reg(state: ProcState): state['status'] = 'APPROVED' if state['compliance_check'] else 'REJECTED'; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_reg); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()