from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotProcurementState(TypedDict): vendor_specs: dict; compliance_check: bool; final_report: str; def validate_specs(state: RobotProcurementState): return {'compliance_check': state['vendor_specs'].get('payload', 0) > 0}; def export_control_check(state: RobotProcurementState): return {'final_report': 'Dual-use export check completed.'}; graph = StateGraph(RobotProcurementState); graph.add_node('spec_validation', validate_specs); graph.add_node('export_compliance', export_control_check); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', 'export_compliance'); graph.add_edge('export_compliance', END);