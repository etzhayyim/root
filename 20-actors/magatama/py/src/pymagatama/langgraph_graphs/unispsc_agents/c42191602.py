from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ORLightingState(TypedDict): specs: dict; compliance_status: bool; approved: bool; def validate_specs(state: ORLightingState): state['compliance_status'] = 'Luminous_Intensity_Lux' in state['specs'] and 'IEC_60601_Certification' in state['specs']; return state; def final_approval(state: ORLightingState): state['approved'] = state['compliance_status']; return state; graph = StateGraph(ORLightingState); graph.add_node('validate', validate_specs); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()