from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LaunchState(TypedDict): specs: dict; validation_passed: bool; risk_assessment: str; def validate_specs(state: LaunchState): state['validation_passed'] = 'LoadCapacityMetric' in state['specs'] and state['specs']['LoadCapacityMetric'] > 0; return state; def assess_risk(state: LaunchState): state['risk_assessment'] = 'High' if state['specs'].get('PropulsionType') == 'Rocket' else 'Standard'; return state; graph = StateGraph(LaunchState); graph.add_node('validate', validate_specs); graph.add_node('assess', assess_risk); graph.add_edge('validate', 'assess'); graph.add_edge('assess', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()