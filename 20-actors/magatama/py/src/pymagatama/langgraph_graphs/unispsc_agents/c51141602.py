from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): status: str; purity: float; validated: bool; def validate_api(state: ProcureState): return {'validated': state['purity'] >= 99.0}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_api); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()