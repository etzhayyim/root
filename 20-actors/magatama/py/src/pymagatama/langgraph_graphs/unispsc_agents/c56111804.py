from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): vendor_specs: dict; compliance_check: bool; final_report: str; def validate_specs(state: FurnitureState): state['compliance_check'] = state['vendor_specs'].get('load_capacity') > 50; return state; def generate_summary(state: FurnitureState): state['final_report'] = 'Validation complete' if state['compliance_check'] else 'Validation failed'; return state; graph = StateGraph(FurnitureState); graph.add_node('validate', validate_specs); graph.add_node('summary', generate_summary); graph.set_entry_point('validate'); graph.add_edge('validate', 'summary'); graph.add_edge('summary', END); graph = graph.compile()