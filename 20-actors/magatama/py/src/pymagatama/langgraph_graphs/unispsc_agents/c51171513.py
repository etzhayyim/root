from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): chemical_data: dict; quality_status: str; def validate_purity(state: ProcState): state['quality_status'] = 'pass' if state['chemical_data'].get('purity_pct', 0) > 99.0 else 'fail'; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()