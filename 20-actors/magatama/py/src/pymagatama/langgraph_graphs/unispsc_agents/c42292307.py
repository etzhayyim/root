from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalToolState(TypedDict): tool_id: str; specs: dict; validation_passed: bool; def validate_specs(state: SurgicalToolState): state['validation_passed'] = 'material' in state['specs'] and 'calibration' in state['specs']; return state; def route_step(state: SurgicalToolState): return 'validate' if state['validation_passed'] else END; graph = StateGraph(SurgicalToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()