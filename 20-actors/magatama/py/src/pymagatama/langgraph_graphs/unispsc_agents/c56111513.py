from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RoomPackageState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: RoomPackageState): errs = []; if not state['specs'].get('fire_safety_certification'): errs.append('Missing Fire Safety Cert'); return {'validation_errors': errs}; def route_procurement(state: RoomPackageState): return 'complete' if not state['validation_errors'] else 'review'; graph = StateGraph(RoomPackageState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_procurement, {'complete': END, 'review': 'validate'}); graph.compile()