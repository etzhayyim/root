from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ICProcurementState(TypedDict): vendor_id: str; quality_clearance: bool; screening_required: bool; def validate_spec(state: ICProcurementState): print('Validating IC specifications...'); return {'quality_clearance': True}; def route_screening(state: ICProcurementState): return 'screening' if state['screening_required'] else 'procure'; graph = StateGraph(ICProcurementState); graph.add_node('validate', validate_spec); graph.add_node('screening', lambda x: x); graph.add_node('procure', lambda x: x); graph.add_conditional_edges('validate', route_screening); graph.add_edge('screening', 'procure'); graph.add_edge('procure', END); graph.set_entry_point('validate'); graph = graph.compile()