from langgraph.graph import StateGraph, END; from typing import TypedDict; class CleanroomState(TypedDict): apparel_type: str; iso_class: int; is_compliant: bool; def validate_specs(state: CleanroomState): state['is_compliant'] = state['iso_class'] <= 7; return state; graph = StateGraph(CleanroomState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()