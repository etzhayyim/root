from langgraph.graph import StateGraph, END; from typing import TypedDict; class DSLState(TypedDict): specs: dict; valid: bool; def validator(state: DSLState): state['valid'] = 'Insertion Loss' in state['specs'] and state['specs']['Insertion Loss'] < 1.0; return state; graph = StateGraph(DSLState); graph.add_node('validate', validator); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()