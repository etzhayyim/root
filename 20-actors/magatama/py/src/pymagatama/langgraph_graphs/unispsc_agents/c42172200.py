from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmergencySupplyState(TypedDict): items: List[dict]; validation_results: List[str]; def validate_medical_item(state: EmergencySupplyState): results = []; for item in state['items']: if 'ISO_13485' not in item.get('certs', []): results.append('Missing ISO 13485'); return {'validation_results': results}; def log_compliance(state: EmergencySupplyState): print('Compliance checked for items'); return {}; graph = StateGraph(EmergencySupplyState); graph.add_node('validate', validate_medical_item); graph.add_node('logging', log_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'logging'); graph.add_edge('logging', END); app = graph.compile()