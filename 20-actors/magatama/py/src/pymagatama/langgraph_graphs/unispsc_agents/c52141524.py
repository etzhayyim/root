from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BlenderState(TypedDict): vendor_specs: dict; compliance_passed: bool; def validate_specs(state: BlenderState): state['compliance_passed'] = 'voltage' in state['vendor_specs'] and 'material_cert' in state['vendor_specs']; return state; builder = StateGraph(BlenderState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()