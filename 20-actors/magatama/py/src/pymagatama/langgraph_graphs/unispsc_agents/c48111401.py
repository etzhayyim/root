from langgraph.graph import StateGraph, END; from typing import TypedDict; class ATMState(TypedDict): status: str; spec_validated: bool; def validate_specs(state: ATMState): return {'spec_validated': True, 'status': 'Specs Checked'}; def initialize_procurement(state: ATMState): return {'status': 'Initialized'}; graph = StateGraph(ATMState); graph.add_node('Init', initialize_procurement); graph.add_node('Validate', validate_specs); graph.set_entry_point('Init'); graph.add_edge('Init', 'Validate'); graph.add_edge('Validate', END); graph = graph.compile()