from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): doc_id: str; specs: dict; valid: bool; log: Annotated[list, operator.add]; def validate_specs(state: AssemblyState): return {'valid': 'AISI' in state['specs'], 'log': ['Specs validated']}; def check_weld_quality(state: AssemblyState): return {'valid': state['specs'].get('weld_cert', False), 'log': ['Weld quality confirmed']}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('weld_check', check_weld_quality); graph.add_edge('validate', 'weld_check'); graph.add_edge('weld_check', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()