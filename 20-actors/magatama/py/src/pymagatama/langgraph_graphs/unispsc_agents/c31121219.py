from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CastState(TypedDict): input_spec: str; validation_result: bool; status: str; def validate_casting(state: CastState): state['validation_result'] = 'tolerance' in state['input_spec'].lower(); state['status'] = 'Validated' if state['validation_result'] else 'Rejected'; return state; graph = StateGraph(CastState); graph.add_node('validate', validate_casting); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()