from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GageState(TypedDict): blocks: List[dict]; validation_passed: bool; def validate_grade(state: GageState): state['validation_passed'] = all(b.get('grade') in ['0', '1', '2'] for b in state['blocks']); return state; def report_status(state: GageState): print(f'Validation result: {state['validation_passed']}'); return state; graph = StateGraph(GageState); graph.add_node('validate', validate_grade); graph.add_node('report', report_status); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()