from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class State(TypedDict): messages: Annotated[list, add_messages]; spec_data: dict; def validate_instrument(state: State): status = 'approved' if 'ISO' in state['spec_data'].get('cert', '') else 'rejected'; return {'messages': [f'Validation status: {status}']}; graph = StateGraph(State); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()