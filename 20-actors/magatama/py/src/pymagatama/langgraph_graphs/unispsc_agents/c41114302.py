from langgraph.graph import StateGraph, END; from typing import TypedDict; class LoggingState(TypedDict): specs: dict; validation_passed: bool; def validate_instrument(state: LoggingState): state['validation_passed'] = 'accuracy' in state['specs'] and 'ip_rating' in state['specs']; return state; def report_result(state: LoggingState): return {'result': 'Validated' if state['validation_passed'] else 'Rejected'}; graph = StateGraph(LoggingState); graph.add_node('validate', validate_instrument); graph.add_node('report', report_result); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()