from langgraph.graph import StateGraph, END; from typing import TypedDict; class FruitState(TypedDict): fruit_type: str; quality_score: float; status: str; def validate_freshness(state: FruitState): return {'status': 'approved' if state['quality_score'] > 0.8 else 'rejected'}; graph = StateGraph(FruitState); graph.add_node('validate', validate_freshness); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()