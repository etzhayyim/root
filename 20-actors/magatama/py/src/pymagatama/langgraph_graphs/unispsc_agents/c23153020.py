from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ControllerState(TypedDict): controller_specs: dict; validation_passed: bool; def validate_specs(state: ControllerState): state['validation_passed'] = 'ISO 10218' in state['controller_specs'].get('certifications', []); return state; def route_logic(state: ControllerState): return 'end' if state['validation_passed'] else 'end'; graph = StateGraph(ControllerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()