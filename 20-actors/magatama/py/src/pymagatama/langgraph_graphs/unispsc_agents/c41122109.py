from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PetriState(TypedDict): items: List[dict]; qc_passed: bool; graph = StateGraph(PetriState); def validate_sterility(state: PetriState): state['qc_passed'] = all(item.get('sterile') for item in state['items']); return state; def finalize_order(state: PetriState): return 'complete' if state['qc_passed'] else 'reject'; graph.add_node('validate', validate_sterility); graph.add_node('process', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()