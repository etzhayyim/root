from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; p_license: bool; compliance_status: bool; def check_license(state: DrugState): return {'p_license': True}; def validate_compliance(state: DrugState): return {'compliance_status': state['p_license']}; graph = StateGraph(DrugState); graph.add_node('verify_license', check_license); graph.add_node('validate', validate_compliance); graph.add_edge('verify_license', 'validate'); graph.add_edge('validate', END); graph.set_entry_point('verify_license'); graph = graph.compile()