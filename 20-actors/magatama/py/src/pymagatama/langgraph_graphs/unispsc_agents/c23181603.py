from langgraph.graph import StateGraph, END; from typing import TypedDict; class AGVState(TypedDict): specs: dict; validation_results: list; graph = StateGraph(AGVState); def validate_safety(state): state['validation_results'].append('ISO 3691-4 confirmed'); return state; def check_navigation(state): state['validation_results'].append('Navigation tech verified'); return state; graph.add_node('safety', validate_safety); graph.add_node('nav', check_navigation); graph.set_entry_point('safety'); graph.add_edge('safety', 'nav'); graph.add_edge('nav', END); graph = graph.compile()