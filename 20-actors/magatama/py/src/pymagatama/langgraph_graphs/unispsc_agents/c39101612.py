from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LampState(TypedDict): specs: dict; compliance: bool; valid: bool; def validate_specs(state: LampState): state['valid'] = all(k in state['specs'] for k in ['wattage', 'base_type']); return state; def check_compliance(state: LampState): state['compliance'] = state['specs'].get('energy_efficiency') == 'rated'; return state; graph = StateGraph(LampState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()