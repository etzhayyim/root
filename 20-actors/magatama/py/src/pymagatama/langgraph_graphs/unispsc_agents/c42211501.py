from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CaneState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: CaneState): specs = state['specs']; is_valid = all([specs.get('weight_capacity', 0) > 0, specs.get('iso_certified', False)]); return {'valid': is_valid, 'error': '' if is_valid else 'Invalid weight spec or certification'}; graph = StateGraph(CaneState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()