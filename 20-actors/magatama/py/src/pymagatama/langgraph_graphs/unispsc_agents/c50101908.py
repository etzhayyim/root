from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StrawberryState(TypedDict): freshness_score: float; lab_report: str; quality_passed: bool; def check_freshness(state): return {'quality_passed': state['freshness_score'] > 0.8}; def validate_lab(state): return {'lab_report': 'Certified Pass' if 'clean' in state.get('lab_report', '') else 'Pending'}; graph = StateGraph(StrawberryState); graph.add_node('check_freshness', check_freshness); graph.add_node('validate_lab', validate_lab); graph.set_entry_point('check_freshness'); graph.add_edge('check_freshness', 'validate_lab'); graph.add_edge('validate_lab', END); graph = graph.compile()