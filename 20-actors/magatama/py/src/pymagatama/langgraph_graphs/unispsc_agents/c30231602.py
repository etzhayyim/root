from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToiletsState(TypedDict): vendor_specs: dict; inspection_passed: bool; def validate_specs(state: ToiletsState): return {'inspection_passed': 'sanitation_certification' in state['vendor_specs']}; def route_procurement(state: ToiletsState): return 'end' if state['inspection_passed'] else 'revise'; graph = StateGraph(ToiletsState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_procurement, {'end': END, 'revise': 'validate'}); graph.compile()