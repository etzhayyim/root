from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ZincCastingState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: ZincCastingState): report = 'Validation passed' if state['specs'].get('ASTM_compliant') else 'Validation failed'; return {'validation_report': report, 'approved': state['specs'].get('ASTM_compliant', False)}; graph = StateGraph(ZincCastingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()