from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): content: str; validated: bool; metadata: dict; def validate_materials(state: ProcureState): return {'validated': True if 'calculus' in state['content'].lower() else False}; def route_task(state: ProcureState): return 'validate' if not state.get('validated') else END; graph = StateGraph(ProcureState); graph.add_node('validate', validate_materials); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()