from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GrapeJuiceState(TypedDict): vendor_id: str; quality_certificate_url: str; brix_level: float; is_approved: bool; def validate_quality(state: GrapeJuiceState): return {'is_approved': state['brix_level'] > 12.0}; def finalize_procurement(state: GrapeJuiceState): print('Processing grape juice order...'); return {'is_approved': True}; graph = StateGraph(GrapeJuiceState); graph.add_node('validate', validate_quality); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()