from langgraph.graph import StateGraph, END; from typing import TypedDict; class RadiosondeState(TypedDict): data: dict; validated: bool; def validate_telemetry(state: RadiosondeState): state['validated'] = state['data'].get('frequency', 0) > 400; return state; graph = StateGraph(RadiosondeState); graph.add_node('validate', validate_telemetry); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()