from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): data: dict; validated: bool; def check_specs(state: ProcState): state['validated'] = 'UV_Rating' in state['data'] and state['data']['UV_Rating'] > 0; return state; graph = StateGraph(ProcState); graph.add_node('validate_specs', check_specs); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', END); graph = graph.compile()