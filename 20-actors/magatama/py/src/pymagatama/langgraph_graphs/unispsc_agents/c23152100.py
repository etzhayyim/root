from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): unit_id: str; test_results: dict; validated: bool; def validate_dynamometer(state: GraphState): state['validated'] = state['test_results'].get('load_capacity', 0) > 0; return state; graph = StateGraph(GraphState); graph.add_node('validate', validate_dynamometer); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()