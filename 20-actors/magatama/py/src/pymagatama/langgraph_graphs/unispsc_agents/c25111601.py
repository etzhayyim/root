from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; specs: dict; graph = StateGraph(State); def validate_solas(state: State): return {'status': 'SOLAS_VALIDATED' if state['specs'].get('cert') == 'SOLAS' else 'REJECTED'}; graph.add_node('validate', validate_solas); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()