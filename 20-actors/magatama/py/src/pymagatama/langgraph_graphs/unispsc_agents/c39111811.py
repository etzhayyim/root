from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrackState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: TrackState): specs = state['specs']; state['valid'] = 'Voltage Rating' in specs and 'Compliance Standards' in specs; return state; graph = StateGraph(TrackState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()