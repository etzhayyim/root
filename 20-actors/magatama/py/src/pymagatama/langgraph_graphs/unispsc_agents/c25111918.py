from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class FurlerState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(FurlerState); def validate_specs(state: FurlerState): return {'compliance_check': 'max_sail_area' in state['specs']}; def finalize_procurement(state: FurlerState): return {'compliance_check': True}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()