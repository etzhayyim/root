from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purities: float; compliance_check: bool; def validate_purity(state: PharmaState): return {'compliance_check': state['purities'] >= 99.0}; def graph_builder(): workflow = StateGraph(PharmaState); workflow.add_node('validate', validate_purity); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); return workflow.compile(); graph = graph_builder()