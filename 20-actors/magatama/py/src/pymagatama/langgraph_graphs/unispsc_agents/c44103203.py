from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RibbonProcurementState(TypedDict): model_number: str; supply_type: str; validation_errors: List[str]; def validate_model(state: RibbonProcurementState): print(f'Validating compatibility for {state[\'model_number\']}'); return {'validation_errors': [] if state['model_number'] else ['Missing model']}; def check_supply(state: RibbonProcurementState): print('Checking stock availability...'); return state; graph = StateGraph(RibbonProcurementState); graph.add_node('validate', validate_model); graph.add_node('stock', check_supply); graph.add_edge('validate', 'stock'); graph.add_edge('stock', END); graph.set_entry_point('validate'); graph = graph.compile()