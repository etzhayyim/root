from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CTProcurementState(TypedDict): specs: dict; compliance_status: bool; installation_ready: bool; def validate_specs(state: CTProcurementState): return {'compliance_status': 'imaging_resolution_lp_cm' in state['specs']}; def verify_compliance(state: CTProcurementState): return {'installation_ready': state['compliance_status']}; graph = StateGraph(CTProcurementState); graph.add_node('validate', validate_specs); graph.add_node('verify', verify_compliance); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); graph.set_entry_point('validate'); graph = graph.compile()