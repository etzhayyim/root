from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BatteryState(TypedDict): specs: dict; valid: bool; risk_assessment: str; def validate_specs(state: BatteryState): required = ['activation_time_ms', 'mil-spec_certification']; state['valid'] = all(k in state['specs'] for k in required); return state; def assess_risk(state: BatteryState): state['risk_assessment'] = 'High: Export restrictions apply'; return state; graph = StateGraph(BatteryState); graph.add_node('validate', validate_specs); graph.add_node('risk', assess_risk); graph.add_edge('validate', 'risk'); graph.add_edge('risk', END); graph.set_entry_point('validate'); graph = graph.compile()