from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FoosballState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: FoosballState): results = []; s = state['specs']; if s.get('weight', 0) < 20 or s.get('weight', 0) > 40: results.append('Weight out of range'); return {'validation_results': results}; graph = StateGraph(FoosballState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()