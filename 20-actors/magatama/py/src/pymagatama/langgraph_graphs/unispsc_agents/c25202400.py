from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftFuelState(TypedDict): fuel_system_data: dict; compliance_checks: List[str]; def validate_materials(state: AircraftFuelState): state['compliance_checks'].append('Material validation passed'); return state; def verify_regulatory(state: AircraftFuelState): state['compliance_checks'].append('Regulatory clearance verified'); return state; graph = StateGraph(AircraftFuelState); graph.add_node('material_check', validate_materials); graph.add_node('regulatory_check', verify_regulatory); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'regulatory_check'); graph.add_edge('regulatory_check', END); graph = graph.compile()