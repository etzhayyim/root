from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PLCState(TypedDict): chassis_id: str; specs: dict; compliance_check: bool; def validate_specs(state: PLCState): state['compliance_check'] = 'IP_rating' in state['specs'] and state['specs']['number_of_slots'] > 0; return state; def route_step(state: PLCState): return 'complete' if state['compliance_check'] else END; graph = StateGraph(PLCState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()