from langgraph.graph import StateGraph, END; from typing import TypedDict; class AnchorState(TypedDict): anchor_type: str; tensile_load: float; is_approved: bool; def validate_spec(state: AnchorState): state['is_approved'] = state['tensile_load'] > 10.0; return state; graph = StateGraph(AnchorState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()