from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): batch_id: str; purity: float; is_valid: bool; audit_logs: List[str]; def validate_batch(state: PharmaState): state['is_valid'] = state['purity'] >= 99.0; state['audit_logs'].append('Purity check completed'); return state; def flag_regulatory(state: PharmaState): state['audit_logs'].append('Regulatory screening passed'); return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_batch); graph.add_node('regulatory', flag_regulatory); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); graph = graph.compile()