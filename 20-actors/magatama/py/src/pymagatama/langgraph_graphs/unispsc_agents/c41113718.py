from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProtocolState(TypedDict): protocol_data: dict; validation_errors: List[str]; def analyze_specs(state: ProtocolState): print('Validating interface and protocol specs...'); return {'validation_errors': []}; def check_compliance(state: ProtocolState): print('Checking dual-use compliance...'); return {}; graph = StateGraph(ProtocolState); graph.add_node('analysis', analyze_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('analysis'); graph.add_edge('analysis', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()