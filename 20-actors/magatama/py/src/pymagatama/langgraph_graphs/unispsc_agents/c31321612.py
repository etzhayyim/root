from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CopperAssemblyState(TypedDict): assembly_id: str; specs: dict; validation_results: List[str]; def validate_sonic_weld(state: CopperAssemblyState): return {'validation_results': ['Weld depth: PASS', 'Conductivity: PASS']}; def check_tolerances(state: CopperAssemblyState): return {'validation_results': state['validation_results'] + ['Dimensional Check: PASS']}; graph = StateGraph(CopperAssemblyState); graph.add_node('validate_weld', validate_sonic_weld); graph.add_node('check_dims', check_tolerances); graph.set_entry_point('validate_weld'); graph.add_edge('validate_weld', 'check_dims'); graph.add_edge('check_dims', END); compiled_graph = graph.compile()