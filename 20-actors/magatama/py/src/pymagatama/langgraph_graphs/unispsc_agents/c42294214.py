from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TracheotomyState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_instruments(state: TracheotomyState): return {'validated': len(state['items']) > 0 and 'ISO13485' in state['certs']}; def check_compliance(state: TracheotomyState): return {'validated': state['validated']}; graph = StateGraph(TracheotomyState); graph.add_node('validate', validate_instruments); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()