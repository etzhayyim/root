from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableState(TypedDict): specs: dict; valid: bool; cable_type: str; def validate_specs(state: CableState): state['valid'] = 'voltage_rating_v' in state['specs'] and 'shielding_type' in state['specs']; return state; builder = StateGraph(CableState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()