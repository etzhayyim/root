from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CotState(TypedDict): model_specs: dict; compliance_passed: bool; def validate_specs(state: CotState): return {'compliance_passed': 'ASTM' in state['model_specs'].values()}; def determine_workflow(state: CotState): return 'safe' if state['compliance_passed'] else 'flagged'; graph = StateGraph(CotState); graph.add_node('validate', validate_specs); graph.add_conditional_edges('validate', determine_workflow, {'safe': END, 'flagged': END}); graph.set_entry_point('validate')