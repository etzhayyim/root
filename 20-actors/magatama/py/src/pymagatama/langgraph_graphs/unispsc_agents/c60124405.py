from langgraph.graph import StateGraph, END; from typing import TypedDict; class BrassWireState(TypedDict): wire_gauge: float; alloy_spec: str; is_compliant: bool; def check_specs(state: BrassWireState): state['is_compliant'] = state['wire_gauge'] > 0 and 'Cu' in state['alloy_spec']; return state; graph = StateGraph(BrassWireState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()