from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RFProcurementState(TypedDict): specs: dict; validation_log: list[str]; def validate_rf_specs(state: RFProcurementState): impedance = state['specs'].get('impedance_ohms'); logs = state['validation_log']; if impedance not in [50, 75]: logs.append('Invalid impedance: must be 50 or 75 ohms.'); return {'validation_log': logs}; workflow = StateGraph(RFProcurementState); workflow.add_node('validate', validate_rf_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()