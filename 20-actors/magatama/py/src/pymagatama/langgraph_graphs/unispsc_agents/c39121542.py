from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SSRState(TypedDict): spec: dict; is_valid: bool; def validate_ssr(state: SSRState): load = state['spec'].get('load_current'); state['is_valid'] = load is not None and load > 0; return state; graph = StateGraph(SSRState); graph.add_node('validate', validate_ssr); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()