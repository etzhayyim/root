from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrailerState(TypedDict): trailer_specs: dict; validation_results: List[str]; graph = StateGraph(TrailerState); def validate_weight(state: TrailerState): state['validation_results'].append('Weight capacity verified against ISO standards.'); return state; def inspect_hitch(state: TrailerState): state['validation_results'].append('Coupling mechanism checked.'); return state; graph.add_node('weight_check', validate_weight); graph.add_node('hitch_inspect', inspect_hitch); graph.set_entry_point('weight_check'); graph.add_edge('weight_check', 'hitch_inspect'); graph.add_edge('hitch_inspect', END); graph = graph.compile()