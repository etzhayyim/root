from langgraph.graph import StateGraph, END; from typing import TypedDict; class UVTubeState(TypedDict): tube_specs: dict; validation_report: str; def validate_specs(state: UVTubeState): spec = state['tube_specs']; state['validation_report'] = 'Valid' if spec.get('wavelength') in range(250, 400) else 'Invalid'; return state; graph = StateGraph(UVTubeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()