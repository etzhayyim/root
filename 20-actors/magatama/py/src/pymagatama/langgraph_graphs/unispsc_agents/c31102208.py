from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CastingState(TypedDict): vendor_certs: List[str]; material_spec: str; validated: bool; graph = StateGraph(CastingState); def validate_certs(state: CastingState): return {'validated': 'ISO9001' in state['vendor_certs']}; def process_metallurgy(state: CastingState): return {'validated': state.get('material_spec') == 'ASTM-B367'}; graph.add_node('cert_check', validate_certs); graph.add_node('metal_check', process_metallurgy); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'metal_check'); graph.add_edge('metal_check', END); graph = graph.compile()