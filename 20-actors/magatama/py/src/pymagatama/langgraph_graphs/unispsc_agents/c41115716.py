from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LCState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: LCState): errors = []; if state['specs'].get('pressure_rating', 0) < 500: errors.append('Pressure rating below HPLC requirement'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(LCState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()