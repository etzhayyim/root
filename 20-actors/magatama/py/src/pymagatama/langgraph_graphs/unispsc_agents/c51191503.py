from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): medicine_data: dict; valid: bool; def validate_pharma(state: State): state['valid'] = state['medicine_data'].get('batch_id') is not None; return state; def check_storage(state: State): state['valid'] = state['medicine_data'].get('temp') < 25; return state; graph = StateGraph(State); graph.add_node('validate', validate_pharma); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph = graph.compile()