from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def validate_material(state: CastState): state['validation_results'].append('Material grade validated against ASTM standards.'); return state; def check_machining(state: CastState): state['validation_results'].append('Dimensional tolerance checked against CAD design.'); return state; graph = StateGraph(CastState); graph.add_node('validate', validate_material); graph.add_node('machining', check_machining); graph.set_entry_point('validate'); graph.add_edge('validate', 'machining'); graph.add_edge('machining', END); app = graph.compile()