from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CalcimineState(TypedDict): input_data: dict; validation_report: list; status: str; def validate_specs(state: CalcimineState): report = []; if state['input_data'].get('voc_level', 0) > 50: report.append('VOC limit exceeded'); return {'validation_report': report, 'status': 'FLAGGED' if report else 'APPROVED'}; graph = StateGraph(CalcimineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()