from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScannerState(TypedDict): vendor_certs: List[str]; radiation_safety_approved: bool; resolution_metrics: dict; status: str; def validate_compliance(state: ScannerState): state['radiation_safety_approved'] = 'iso_radiology_cert' in state['vendor_certs']; return {'status': 'compliant' if state['radiation_safety_approved'] else 'rejected'}; def finalize_procurement(state: ScannerState): return {'status': 'ready_for_purchase'}; graph = StateGraph(ScannerState); graph.add_node('validate', validate_compliance); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()