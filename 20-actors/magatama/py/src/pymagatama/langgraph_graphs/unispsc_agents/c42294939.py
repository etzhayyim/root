from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EndoscopeState(TypedDict): valve_id: str; compliance_docs: List[str]; approved: bool; def validate_specs(state: EndoscopeState): return {'approved': len(state['compliance_docs']) >= 3}; def route_procurement(state: EndoscopeState): return 'approval' if state['approved'] else END; graph = StateGraph(EndoscopeState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()