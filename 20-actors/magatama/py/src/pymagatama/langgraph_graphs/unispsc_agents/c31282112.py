from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ComponentState(TypedDict): component_data: dict; validation_results: List[str]; def validate_alloy(state: ComponentState): state['validation_results'].append('Alloy composition verified'); return state; def inspect_geometry(state: ComponentState): state['validation_results'].append('Geometry tolerance confirmed'); return state; graph = StateGraph(ComponentState); graph.add_node('validate', validate_alloy); graph.add_node('inspect', inspect_geometry); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()