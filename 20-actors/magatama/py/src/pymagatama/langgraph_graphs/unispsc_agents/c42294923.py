from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_id: str; device_specs: dict; compliance_verified: bool; def validate_specs(state: ProcureState): return {'compliance_verified': 'ISO_13485' in state['device_specs']}; def approve_order(state: ProcureState): return {'compliance_verified': True}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()