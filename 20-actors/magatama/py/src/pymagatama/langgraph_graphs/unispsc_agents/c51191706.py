from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_data: dict; validation_results: List[str]; graph = StateGraph(ProcureState); def validate_purity(state: ProcureState): return {'validation_results': ['Purity verified']}; def check_regulatory(state: ProcureState): return {'validation_results': state['validation_results'] + ['Regulatory compliance checked']}; graph.add_node('validate', validate_purity); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.compile()