from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GolfTeeState(TypedDict): specs: dict; compliance_passed: bool; def validate_specs(state: GolfTeeState): return {'compliance_passed': 'Material composition' in state['specs']}; def finalize_procurement(state: GolfTeeState): return {'compliance_passed': True}; graph = StateGraph(GolfTeeState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()