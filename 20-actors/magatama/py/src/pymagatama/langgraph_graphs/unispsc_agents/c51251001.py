from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ButaphosphanState(TypedDict): chemical_data: dict; quality_check_passed: bool; compliance_docs: List[str]; def validate_purity(state: ButaphosphanState): state['quality_check_passed'] = state['chemical_data'].get('purity', 0) >= 99.0; return state; def verify_compliance(state: ButaphosphanState): state['compliance_docs'] = ['GMP_CERT', 'MSDS']; return state; graph = StateGraph(ButaphosphanState); graph.add_node('validate', validate_purity); graph.add_node('compliance', verify_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()