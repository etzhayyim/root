from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): specs: dict; validation_report: list; status: str; def validate_specs(state: ToolState): specs = state['specs']; errors = []; if specs.get('operating_pressure', 0) > 120: errors.append('Pressure exceeds safety limit'); return {'validation_report': errors, 'status': 'validated' if not errors else 'rejected'}; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()