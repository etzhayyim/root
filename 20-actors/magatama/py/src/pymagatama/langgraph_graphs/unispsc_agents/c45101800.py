from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BindingState(TypedDict): capacity: int; specs_valid: bool; machine_type: str; def validate_specs(state: BindingState): return {'specs_valid': state['capacity'] > 0}; def finalize_procurement(state: BindingState): return {'specs_valid': True}; graph = StateGraph(BindingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compile_graph = graph.compile()