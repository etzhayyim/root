from langgraph.graph import StateGraph, END; from typing import TypedDict; class TitaniumState(TypedDict): spec: dict; validated: bool; export_check: bool; def validate_specs(state: TitaniumState): state['validated'] = all(k in state['spec'] for k in ['ASTM_Grade', 'Weld_Certification_Standard']); return state; def check_export_control(state: TitaniumState): state['export_check'] = True; return state; graph = StateGraph(TitaniumState); graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph = graph.compile()