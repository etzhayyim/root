from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TapeState(TypedDict): items: List[str]; validation_flags: dict; def validate_specs(state: TapeState): state['validation_flags'] = {'sterile': True, 'material': 'non-adherent'}; return state; def check_compliance(state: TapeState): print('Compliance check for medical tape completed'); return state; graph = StateGraph(TapeState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()