from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ToyState(TypedDict): items: List[str]; safety_verified: bool; compliance_report: str; graph = StateGraph(ToyState); def check_safety(state: ToyState): return {'safety_verified': True}; def generate_procurement_report(state: ToyState): return {'compliance_report': 'Passed Safety Inspection'}; graph.add_node('safety_check', check_safety); graph.add_node('gen_report', generate_procurement_report); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'gen_report'); graph.add_edge('gen_report', END); app = graph.compile()