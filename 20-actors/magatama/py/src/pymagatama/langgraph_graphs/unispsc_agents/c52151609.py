from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PeelerState(TypedDict): vendor_specs: dict; compliance_check: bool; approved: bool; def validate_specs(state: PeelerState): state['compliance_check'] = 'grade_304_ss' in state['vendor_specs'].get('material', '').lower(); return state; def final_decision(state: PeelerState): state['approved'] = state['compliance_check']; return state; graph = StateGraph(PeelerState); graph.add_node('validate', validate_specs); graph.add_node('decision', final_decision); graph.set_entry_point('validate'); graph.add_edge('validate', 'decision'); graph.add_edge('decision', END); graph = graph.compile()