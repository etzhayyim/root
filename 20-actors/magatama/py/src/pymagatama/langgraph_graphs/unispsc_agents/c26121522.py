from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WireSpecState(TypedDict): wire_type: str; specs: dict; validation_report: str; def validate_specs(state: WireSpecState): if state['specs'].get('conductivity', 0) < 95: return {'validation_report': 'FAIL: Low conductivity'} return {'validation_report': 'PASS'}; graph = StateGraph(WireSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()