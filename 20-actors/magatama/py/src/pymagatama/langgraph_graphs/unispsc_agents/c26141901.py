from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DustSensorState(TypedDict): sensor_id: str; radiation_safety_checked: bool; calibration_valid: bool; graph = StateGraph(DustSensorState); def validate_radiation_safety(state: DustSensorState): return {'radiation_safety_checked': True}; def verify_calibration(state: DustSensorState): return {'calibration_valid': True}; graph.add_node('safety_check', validate_radiation_safety); graph.add_node('calib_verify', verify_calibration); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'calib_verify'); graph.add_edge('calib_verify', END); graph = graph.compile()