from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; def validate_instrument_part(state: ProcureState): return {'validated': all(item for item in state['items'])}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_instrument_part); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()