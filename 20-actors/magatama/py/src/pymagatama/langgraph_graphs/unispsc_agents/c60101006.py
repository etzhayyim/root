from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): items: list; valid: bool; def validate_math_kit(state: ProcState): state['valid'] = all(['material' in item for item in state['items']]); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_math_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()