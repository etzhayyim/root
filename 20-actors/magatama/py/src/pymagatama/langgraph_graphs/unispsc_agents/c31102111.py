from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: CastingState): state['valid'] = 'ASTM_Grade' in state['specs']; state['logs'].append('Validate casting specs'); return state; def check_quality(state: CastingState): state['logs'].append('Checking material chemistry'); return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.add_node('qc', check_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'qc'); graph.add_edge('qc', END); graph = graph.compile()