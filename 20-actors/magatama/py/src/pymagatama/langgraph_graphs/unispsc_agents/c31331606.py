from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): data: dict; errors: Annotated[list, operator.add]; def validate_specs(state: AssemblyState): state['data']['validated'] = True; return state; def check_quality(state: AssemblyState): return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('check_quality', check_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_quality'); graph.add_edge('check_quality', END); compiled_graph = graph.compile()