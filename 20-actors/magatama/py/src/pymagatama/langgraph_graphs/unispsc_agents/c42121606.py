from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class VetDrugState(TypedDict): medicine_name: str; compliance_check: bool; dosage_specs: dict; graph = StateGraph(VetDrugState); def validate_gmp(state: VetDrugState): state['compliance_check'] = True; return state; graph.add_node('validate_gmp', validate_gmp); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', END); compiled_graph = graph.compile()