from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EnginePartState(TypedDict): part_code: str; specs: dict; validation_report: str; def validate_nozzle(state: EnginePartState): if state['specs'].get('pressure') < 100: return {'validation_report': 'Failed: Pressure too low'}; return {'validation_report': 'Passed'}; def export_control_check(state: EnginePartState): return {'validation_report': state['validation_report'] + ' | Export compliant'}; graph = StateGraph(EnginePartState); graph.add_node('validate', validate_nozzle); graph.add_node('compliance', export_control_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()