from langgraph.graph import StateGraph, END; from typing import TypedDict; class StorageState(TypedDict): specs: dict; valid: bool; def check_load(state: StorageState): state['valid'] = state['specs'].get('load', 0) > 0; return state; graph = StateGraph(StorageState); graph.add_node('validate', check_load); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()