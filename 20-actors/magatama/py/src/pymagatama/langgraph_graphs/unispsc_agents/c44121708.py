from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MarkerState(TypedDict): vendor: str; ink_safety_doc: str; quality_score: float; approved: bool; def validate_ink(state: MarkerState): return {'approved': 'VOC-Free' in state['ink_safety_doc']}; graph = StateGraph(MarkerState); graph.add_node('validate', validate_ink); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()