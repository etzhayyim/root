from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WaterSamplerState(TypedDict): sampler_id: str; specs: dict; validation_status: bool; def validate_sampler(state: WaterSamplerState): state['validation_status'] = 'material' in state['specs'] and 'volume' in state['specs']; return state; def finalize_order(state: WaterSamplerState): print(f'Processing {state["sampler_id"]}'); return state; graph = StateGraph(WaterSamplerState); graph.add_node('validate', validate_sampler); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()