from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RackState(TypedDict): specs: dict; validation_report: List[str]; def validate_specs(state: RackState): report = []; if state['specs'].get('load_capacity', 0) <= 0: report.append('Invalid load capacity'); return {'validation_report': report}; def assemble_graph(): graph = StateGraph(RackState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = assemble_graph()