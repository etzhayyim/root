from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): hardware_id: str; compliance_check: bool; stage: str; def validate_hw(state: NetworkState): return {'compliance_check': True, 'stage': 'hardware_validated'}; def configure_network(state: NetworkState): return {'stage': 'configured'}; graph = StateGraph(NetworkState); graph.add_node('validate', validate_hw); graph.add_node('configure', configure_network); graph.set_entry_point('validate'); graph.add_edge('validate', 'configure'); graph.add_edge('configure', END); graph = graph.compile()