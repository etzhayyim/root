from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BannerState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: BannerState): errors = []; if not state['specs'].get('flame_retardancy'): errors.append('Missing fire safety certification'); return {'validation_errors': errors}; def finalize(state: BannerState): return {'approved': len(state.get('validation_errors', [])) == 0}; graph = StateGraph(BannerState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()