from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DispenserState(TypedDict): vendor: str; specs: dict; compliance: bool; def validate_specs(state: DispenserState): state['compliance'] = 'food_grade' in state['specs']; return state; def check_safety(state: DispenserState): return 'end' if state['compliance'] else 'end'; graph = StateGraph(DispenserState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()