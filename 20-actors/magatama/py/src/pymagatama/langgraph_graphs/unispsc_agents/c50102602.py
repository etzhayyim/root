from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; inspection_passed: bool; proc_status: str; def validate_cold_chain(state: ProcureState): return {'inspection_passed': True, 'proc_status': 'verified'} if all(item for item in state['items']) else {'inspection_passed': False}; graph = StateGraph(ProcureState); graph.add_node('validate_cold_chain', validate_cold_chain); graph.set_entry_point('validate_cold_chain'); graph.add_edge('validate_cold_chain', END); graph = graph.compile()