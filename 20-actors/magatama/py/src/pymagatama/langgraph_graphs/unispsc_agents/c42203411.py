from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CartState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: CartState): errors = []; if state['specs'].get('material') != 'SUS304': errors.append('Material must be medical grade stainless steel'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(CartState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()