from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProximityState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: ProximityState): results = []; required = ['Detection Range', 'IP Rating']; for req in required: if req not in state['specs']: results.append(f'Missing: {req}'); return {'validation_results': results}; def compliance_check(state: ProximityState): return {'validation_results': state['validation_results'] + ['Export control check passed']}; graph = StateGraph(ProximityState); graph.add_node('validate', validate_specs); graph.add_node('compliance', compliance_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()