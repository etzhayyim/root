from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PastaState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PastaState): state['valid'] = 'Food grade certification' in state.get('specs', {}) and state['specs'].get('load_capacity', 0) > 0; return state; graph = StateGraph(PastaState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()