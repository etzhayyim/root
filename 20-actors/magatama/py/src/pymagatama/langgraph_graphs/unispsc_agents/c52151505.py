from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): commodity: str; compliance_checks: List[str]; approved: bool; def material_validator(state: ProcurementState): state['compliance_checks'].append('food_safe_contact_check'); return state; def spec_finalizer(state: ProcurementState): state['approved'] = True; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', material_validator); graph.add_node('finalize', spec_finalizer); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()