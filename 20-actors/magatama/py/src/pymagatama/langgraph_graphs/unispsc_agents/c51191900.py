from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplementState(TypedDict): ingredients: List[str]; safety_checked: bool; certs: List[str]; def validate_ingredients(state: SupplementState): return {'safety_checked': all(['toxin' not in i.lower() for i in state['ingredients']])}; def check_certs(state: SupplementState): return {'safety_checked': state['safety_checked'] and len(state['certs']) > 0}; graph = StateGraph(SupplementState); graph.add_node('validate', validate_ingredients); graph.add_node('cert_check', check_certs); graph.add_edge('validate', 'cert_check'); graph.add_edge('cert_check', END); graph.set_entry_point('validate'); graph = graph.compile()