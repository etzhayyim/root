from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SutureRemoverState(TypedDict): device_id: str; compliance_checks: List[str]; approved: bool; def validate_sterility(state: SutureRemoverState): state['compliance_checks'].append('Sterility Verified'); return state; def check_regulatory(state: SutureRemoverState): state['compliance_checks'].append('ISO-13485 Compliance'); state['approved'] = True; return state; graph = StateGraph(SutureRemoverState); graph.add_node('sterility', validate_sterility); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('sterility'); graph.add_edge('sterility', 'regulatory'); graph.add_edge('regulatory', END); app = graph.compile()