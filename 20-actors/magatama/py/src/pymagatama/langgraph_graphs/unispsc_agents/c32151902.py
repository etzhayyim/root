from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(State); def validate_specs(state: State): specs = state['input_data'].get('specs', {}); result = 'Pass' if 'IP_rating' in specs and 'voltage' in specs else 'Fail'; return {'validation_results': [result]}; graph.add_node('ValidateSpecs', validate_specs); graph.set_entry_point('ValidateSpecs'); graph.add_edge('ValidateSpecs', END); compiled_graph = graph.compile()