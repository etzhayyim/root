from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DiffractionState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(DiffractionState); def validate_optics(state: DiffractionState): res = 'Valid' if state['specs'].get('accuracy') else 'Invalid'; return {'validation_results': [res]}; def export_control_check(state: DiffractionState): return {'validation_results': state['validation_results'] + ['Export Check Passed']}; graph.add_node('validate', validate_optics); graph.add_node('compliance', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()