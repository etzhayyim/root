from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CartState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_materials(state: CartState): if 'material' not in state['specs']: state['validation_errors'].append('Material missing'); return state; def validate_load(state: CartState): if state['specs'].get('load_capacity', 0) < 50: state['validation_errors'].append('Load capacity too low'); return state; graph = StateGraph(CartState); graph.add_node('validate_materials', validate_materials); graph.add_node('validate_load', validate_load); graph.set_entry_point('validate_materials'); graph.add_edge('validate_materials', 'validate_load'); graph.add_edge('validate_load', END); graph = graph.compile()