from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SiliconState(TypedDict): specs: dict; valid_purity: bool; exports_cleared: bool; def check_purity(state: SiliconState): state['valid_purity'] = state['specs'].get('purity', 0) >= 99.9999; return state; def check_export(state: SiliconState): state['exports_cleared'] = True; return state; graph = StateGraph(SiliconState); graph.add_node('verify_purity', check_purity); graph.add_node('verify_export', check_export); graph.set_entry_point('verify_purity'); graph.add_edge('verify_purity', 'verify_export'); graph.add_edge('verify_export', END); compiled_graph = graph.compile()