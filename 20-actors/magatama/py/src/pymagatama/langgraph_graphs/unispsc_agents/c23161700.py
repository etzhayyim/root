from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_id: str; specs: dict; critical_flag: bool; def validate_spec(state: ToolState): return {'critical_flag': state['specs'].get('tolerance', 0) < 0.001}; def check_export_control(state: ToolState): return {'critical_flag': state.get('critical_flag') or True}; graph = StateGraph(ToolState); graph.add_node('validate', validate_spec); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()