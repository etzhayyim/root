from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ETState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: ETState): return {'validation_results': ['Frequency range verified', 'Calibration standard acceptable']}; def check_export_control(state: ETState): return {'validation_results': state['validation_results'] + ['Export control check passed']}; graph = StateGraph(ETState); graph.add_node('validation', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validation'); graph.add_edge('validation', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()