from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class BerylliumState(TypedDict): component_id: str; certification: bool; safety_check: bool; def validate_composition(state: BerylliumState): state['certification'] = True; return state; def check_hazmat_compliance(state: BerylliumState): state['safety_check'] = True; return state; graph = StateGraph(BerylliumState); graph.add_node('validate', validate_composition); graph.add_node('safety', check_hazmat_compliance); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()