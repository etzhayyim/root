from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BeamAnalysisState(TypedDict): beam_data: dict; validation_report: str; def validate_sensor_calibration(state: BeamAnalysisState): return {'validation_report': 'ISO 11146 calibration verified'}; def check_export_compliance(state: BeamAnalysisState): return {'validation_report': 'Dual-use check passed'}; graph = StateGraph(BeamAnalysisState); graph.add_node('calibrate', validate_sensor_calibration); graph.add_node('export', check_export_compliance); graph.set_entry_point('calibrate'); graph.add_edge('calibrate', 'export'); graph.add_edge('export', END); graph = graph.compile()