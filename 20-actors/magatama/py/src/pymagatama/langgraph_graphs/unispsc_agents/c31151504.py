from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RopeProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: RopeProcurementState): specs = state.get('specs', {}); errors = []; if specs.get('tensile_strength_kn', 0) <= 0: errors.append('Invalid tensile strength'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(RopeProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()