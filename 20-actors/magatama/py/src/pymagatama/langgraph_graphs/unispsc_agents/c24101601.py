from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ElevatorState(TypedDict): vendor_certs: List[str]; inspection_passed: bool; safety_score: int; def validate_vendor(state: ElevatorState): return {'inspection_passed': 'ISO9001' in state['vendor_certs']}; def assess_safety(state: ElevatorState): return {'safety_score': 100 if state['inspection_passed'] else 0}; graph = StateGraph(ElevatorState); graph.add_node('validate_vendor', validate_vendor); graph.add_node('assess_safety', assess_safety); graph.set_entry_point('validate_vendor'); graph.add_edge('validate_vendor', 'assess_safety'); graph.add_edge('assess_safety', END); graph = graph.compile()