from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): doc_validation: bool; expiry_check: bool; graph = StateGraph(State); def validate_pharma(state: State): return {'doc_validation': True}; def check_expiry(state: State): return {'expiry_check': True}; graph.add_node('Validate', validate_pharma); graph.add_node('Expiry', check_expiry); graph.set_entry_point('Validate'); graph.add_edge('Validate', 'Expiry'); graph.add_edge('Expiry', END); app = graph.compile()