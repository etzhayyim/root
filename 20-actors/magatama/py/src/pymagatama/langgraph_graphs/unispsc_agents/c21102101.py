from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class TillageState(TypedDict): data: dict; plan: Annotated[Sequence[str], operator.add]; def validate_specs(state: TillageState): return {'plan': ['spec_validation']}; def check_safety(state: TillageState): return {'plan': ['safety_compliance_check']}; graph = StateGraph(TillageState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()