from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_name: str; components: List[str]; validated: bool; def validate_components(state: KitState): state['validated'] = len(state['components']) > 0; return state; graph = StateGraph(KitState); graph.add_node('validate', validate_components); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()