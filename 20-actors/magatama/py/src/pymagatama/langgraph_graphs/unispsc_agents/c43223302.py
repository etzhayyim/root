from langgraph.graph import StateGraph, END; from typing import TypedDict; class LabelState(TypedDict): specs: dict; validation: bool; def validate_labels(state: LabelState): state['validation'] = 'UL' in state['specs'].get('certifications', []) and state['specs'].get('temp_max', 0) > 80; return state; graph = StateGraph(LabelState); graph.add_node('validate', validate_labels); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()