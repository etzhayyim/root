from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_id: str; purity: float; status: str; safety_checks: Annotated[Sequence[str], operator.add]; def validate_purity(state: ChemicalState): return {'status': 'PASSED' if state['purity'] >= 99.999 else 'FAILED'}; def check_safety(state: ChemicalState): return {'safety_checks': ['compliance_verified', 'hazmat_screened']}; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()