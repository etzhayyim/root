from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine_name: str; validation_status: bool; inventory_data: dict; def validate_drug(state: PharmState): state['validation_status'] = True if state['medicine_name'] else False; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_drug); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()