from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): data: dict; status: list[str]; def validate_specs(state: ProcState): state['status'].append('Spec Validated'); return state; def run_qa(state: ProcState): state['status'].append('QA Passed'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('qa', run_qa); graph.set_entry_point('validate'); graph.add_edge('validate', 'qa'); graph.add_edge('qa', END); compiled_graph = graph.compile()