from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DieCastState(TypedDict): alloy_data: dict; quality_check: bool; inspection_report: str; def validate_composition(state): return {'quality_check': state['alloy_data'].get('copper_pct', 0) > 80}; def generate_report(state): return {'inspection_report': 'Certified' if state['quality_check'] else 'Rejected'}; graph = StateGraph(DieCastState); graph.add_node('validate', validate_composition); graph.add_node('report', generate_report); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()