from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): chemical_data: dict; validation_status: bool; def validate_purity(state: State): purity = state['chemical_data'].get('purity', 0); return {'validation_status': purity >= 99.0}; def check_cold_chain(state: State): temp = state['chemical_data'].get('storage_temp', 25); return {'validation_status': state['validation_status'] and temp <= 8}; graph = StateGraph(State); graph.add_node('validate_purity', validate_purity); graph.add_node('check_cold_chain', check_cold_chain); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_cold_chain'); graph.add_edge('check_cold_chain', END); graph = graph.compile()