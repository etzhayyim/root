from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BlowerState(TypedDict): blower_specs: dict; validation_report: list[str]; graph = StateGraph(BlowerState); def validate_specs(state: BlowerState): valid = state['blower_specs'].get('flow_rate', 0) > 0; return {'validation_report': ['Specs validated'] if valid else ['Invalid flow rate']}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()