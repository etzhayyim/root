from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): assembly_id: str; specs: dict; valid: bool; graph = StateGraph(AssemblyState); def validate_welds(state: AssemblyState): state['valid'] = 'ISO-3834' in state['specs'].get('certifications', []); return state; graph.add_node('validate', validate_welds); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()