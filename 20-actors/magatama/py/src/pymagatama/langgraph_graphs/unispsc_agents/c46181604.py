from langgraph.graph import StateGraph, END; from typing import TypedDict; class ComplianceState(TypedDict): specs: dict; compliant: bool; def check_compliance(state: ComplianceState): state['compliant'] = 'Safety Standard' in state['specs'] and state['specs']['Impact Resistance'] >= '200J'; return state; graph = StateGraph(ComplianceState); graph.add_node('validate_specs', check_compliance); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', END); app = graph.compile()