from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LithiumState(TypedDict): probe_id: str; certification: bool; validated: bool; def validate_probe(state: LithiumState): return {'validated': state.get('certification', False)}; def check_certs(state: LithiumState): return {'certification': True}; graph = StateGraph(LithiumState); graph.add_node('cert_check', check_certs); graph.add_node('validation', validate_probe); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'validation'); graph.add_edge('validation', END); compiled_graph = graph.compile()