from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: MoldState): return {'validation_passed': 'Material Grade' in state['specs']}; def check_lead_time(state: MoldState): return {'validation_passed': state['validation_passed'] and True}; graph = StateGraph(MoldState); graph.add_node('validate', validate_specs); graph.add_node('time_check', check_lead_time); graph.add_edge('validate', 'time_check'); graph.add_edge('time_check', END); graph.set_entry_point('validate'); graph = graph.compile()