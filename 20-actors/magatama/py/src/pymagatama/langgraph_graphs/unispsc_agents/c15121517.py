from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class PipelineState(TypedDict): items: Annotated[Sequence[dict], operator.add]; validation_results: dict; def validate_fitting(state: PipelineState): item = state['items'][-1]; result = {'valid': True, 'reason': 'Pass'} if item.get('pressure_rating') else {'valid': False, 'reason': 'Missing Rating'}; return {'validation_results': result}; graph = StateGraph(PipelineState); graph.add_node('validate', validate_fitting); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()