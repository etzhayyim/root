from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SteelState(TypedDict): dimensions: dict; material_grade: str; mtc_verified: bool; graph = StateGraph(SteelState); def validate_specs(state: SteelState): return {'mtc_verified': True} if state['material_grade'] else {'mtc_verified': False}; def check_dimensions(state: SteelState): print(f'Processing plate: {state['dimensions']}'); return state; graph.add_node('validate', validate_specs); graph.add_node('check_dims', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_dims'); graph.add_edge('check_dims', END); app = graph.compile()