from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LeadState(TypedDict): content: str; specs: dict; compliance_check: bool; def validate_lead_content(state: LeadState) -> LeadState: state['compliance_check'] = state['specs'].get('purity', 0) > 99.0; return state; def hazard_screening(state: LeadState) -> LeadState: state['content'] = 'Lead item flagged for safety protocol'; return state; graph = StateGraph(LeadState); graph.add_node('validate', validate_lead_content); graph.add_node('safety', hazard_screening); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()