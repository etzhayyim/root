from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): chemical_data: dict; compliance_status: bool; graph = StateGraph(ProcState); def validate_purity(state: ProcState): state['compliance_status'] = state['chemical_data'].get('purity', 0) >= 99.0; return state; graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()