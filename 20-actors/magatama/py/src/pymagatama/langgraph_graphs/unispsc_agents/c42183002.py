from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): data: dict; status: list[str]; graph = StateGraph(State); def validate_device(state: State): state['status'].append('Validation: Checking ISO 13485 compliance'); return state; def check_calibration(state: State): state['status'].append('Validation: Verifying calibration certificate'); return state; graph.add_node('validate', validate_device); graph.add_node('calibrate', check_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); compiled_graph = graph.compile()