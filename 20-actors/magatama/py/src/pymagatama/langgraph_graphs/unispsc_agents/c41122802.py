from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RackState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: RackState): specs = state['specs']; is_valid = all(key in specs for key in ['material', 'slots']); return {'valid': is_valid, 'log': ['Validation complete']}; graph = StateGraph(RackState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()