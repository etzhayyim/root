from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TitaniumState(TypedDict): component_id: str; specs: dict; validation_pass: bool; def validate_specs(state: TitaniumState): print(f'Validating specs for {state[component_id]}'); state[validation_pass] = all(k in state[specs] for k in ['material_cert', 'tolerance']); return state; graph = StateGraph(TitaniumState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()