from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: ProcurementState): spec = state['specs']; results = []; if spec.get('weight_capacity') < 100: results.append('Capacity insufficient'); return {'validation_results': results}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()