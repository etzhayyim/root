from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ChartState(TypedDict): metadata: dict; validation_report: list[str]; def validate_format(state: ChartState): return {'validation_report': ['Format verified']}; def check_content_accuracy(state: ChartState): return {'validation_report': ['Content verified']}; graph = StateGraph(ChartState); graph.add_node('validate', validate_format); graph.add_node('check_content', check_content_accuracy); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_content'); graph.add_edge('check_content', END); graph = graph.compile()