from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EraserState(TypedDict): model_number: str; material_spec: str; status: str; def validate_compatibility(state: EraserState): return {'status': 'COMPATIBLE' if state['model_number'] else 'INVALID'}; def check_material(state: EraserState): return {'status': 'PASSED' if 'rubber' in state['material_spec'].lower() else 'FAILED'}; graph = StateGraph(EraserState); graph.add_node('validate', validate_compatibility); graph.add_node('check_material', check_material); graph.add_edge('validate', 'check_material'); graph.add_edge('check_material', END); graph.set_entry_point('validate'); graph = graph.compile()