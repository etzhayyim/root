from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KitState(TypedDict): kit_name: str; temp_req: str; validated: bool; graph = StateGraph(KitState); def check_storage(state: KitState): print(f'Verifying storage for {state["kit_name"]}'); return {'validated': True}; graph.add_node('storage_check', check_storage); graph.set_entry_point('storage_check'); graph.add_edge('storage_check', END); graph = graph.compile()