from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PCRKitState(TypedDict): kit_id: str; temp_req: str; validated: bool; next_step: str; def validate_cold_chain(state: PCRKitState): state['validated'] = state['temp_req'] == 'stored_at_neg20'; return state; def check_quality(state: PCRKitState): state['next_step'] = 'verify_lot_expiration'; return state; graph = StateGraph(PCRKitState); graph.add_node('cold_chain', validate_cold_chain); graph.add_node('quality', check_quality); graph.add_edge('cold_chain', 'quality'); graph.add_edge('quality', END); graph.set_entry_point('cold_chain'); graph = graph.compile()