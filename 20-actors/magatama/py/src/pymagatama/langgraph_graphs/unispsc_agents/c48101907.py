from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PitcherState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(PitcherState); def validate_material(state: PitcherState): material = state['specs'].get('material', ''); is_food_grade = 'food-grade' in material.lower(); return {'validation_results': ['Material check passed' if is_food_grade else 'Material verification failed']}; def check_capacity(state: PitcherState): return {'validation_results': state['validation_results'] + ['Capacity verification completed']}; graph.add_node('material_check', validate_material); graph.add_node('capacity_check', check_capacity); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'capacity_check'); graph.add_edge('capacity_check', END); graph = graph.compile()