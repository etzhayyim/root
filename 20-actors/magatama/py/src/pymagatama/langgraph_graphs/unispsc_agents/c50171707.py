from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class VinegarState(TypedDict): acidity: float; quality_cert: bool; approved: bool; def validate_acidity(state: VinegarState): state['approved'] = 4.0 <= state['acidity'] <= 6.0; return state; graph = StateGraph(VinegarState); graph.add_node('validate', validate_acidity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()