from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class HeatExchangerState(TypedDict): specs: dict; compliance_check: bool; needs_inspection: bool; def validate_specs(state: HeatExchangerState): state['compliance_check'] = 'ASME' in state['specs'].get('standards', []) return state; def schedule_inspection(state: HeatExchangerState): state['needs_inspection'] = True return state; graph = StateGraph(HeatExchangerState); graph.add_node('validate', validate_specs); graph.add_node('inspect', schedule_inspection); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()