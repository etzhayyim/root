from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class RugProcurementState(TypedDict): vendor_id: str; quality_score: int; spec_compliant: bool; def validate_spec(state: RugProcurementState): state['spec_compliant'] = state['quality_score'] > 80; return state; def route_procurement(state: RugProcurementState): return 'complete' if state['spec_compliant'] else 'reject'; graph = StateGraph(RugProcurementState); graph.add_node('validation', validate_spec); graph.set_entry_point('validation'); graph.add_conditional_edges('validation', route_procurement, {'complete': END, 'reject': END}); graph.compile()