from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplyState(TypedDict): items: List[str]; inspection_passed: bool; state: str; def validate_cold_chain(state: SupplyState): state['inspection_passed'] = True; return state; graph = StateGraph(SupplyState); graph.add_node('validate_cold_chain', validate_cold_chain); graph.set_entry_point('validate_cold_chain'); graph.add_edge('validate_cold_chain', END); compiled_graph = graph.compile()