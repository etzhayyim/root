from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureProcurement(TypedDict): vendor: str; items: List[str]; inspection_status: bool; def validate_spec(state: FurnitureProcurement): return {'inspection_status': True}; def confirm_order(state: FurnitureProcurement): return {'inspection_status': True}; graph = StateGraph(FurnitureProcurement); graph.add_node('validate', validate_spec); graph.add_node('confirm', confirm_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'confirm'); graph.add_edge('confirm', END); compiled_graph = graph.compile()