from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ScreenState(TypedDict): screen_specs: dict; validation_errors: Annotated[list, operator.add]; def validate_screen(state: ScreenState): errors = []; if state['screen_specs'].get('gain_factor', 0) < 0: errors.append('Invalid gain factor'); return {'validation_errors': errors}; graph = StateGraph(ScreenState); graph.add_node('validate', validate_screen); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()