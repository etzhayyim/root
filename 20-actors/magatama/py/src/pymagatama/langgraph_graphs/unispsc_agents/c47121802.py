from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LintRemoverState(TypedDict): model_id: str; specs: dict; valid: bool; def validate_specs(state: LintRemoverState): state['valid'] = 'blade_durability' in state['specs'] and 'power_source' in state['specs']; return state; builder = StateGraph(LintRemoverState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()