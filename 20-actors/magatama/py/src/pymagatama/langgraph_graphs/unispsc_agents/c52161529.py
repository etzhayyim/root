from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class VCRState(TypedDict): model: str; legacy_format: bool; needs_refurb: bool; output: str; def validate_vcr_specs(state: VCRState): status = 'verified' if state['model'] else 'missing'; return {'output': f'Validation status: {status}'}; def check_refurb_status(state: VCRState): return {'output': 'Requires certified refurbishment provider' if state['needs_refurb'] else 'Standard logistics'}; graph = StateGraph(VCRState); graph.add_node('validate', validate_vcr_specs); graph.add_node('logistics_check', check_refurb_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics_check'); graph.add_edge('logistics_check', END); compiled_graph = graph.compile()