from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): pump_specs: dict; validation_report: list[str]; graph = StateGraph(PumpState); def validate_specs(state: PumpState): specs = state['pump_specs']; report = []; if specs.get('motor_power_kw', 0) <= 0: report.append('Invalid motor power'); return {'validation_report': report}; graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); compiled_graph = graph.compile()