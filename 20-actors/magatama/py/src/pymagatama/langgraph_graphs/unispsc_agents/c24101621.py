from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CraneState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: CraneState): return {"validation_results": ["ISO 9001 verified"] if state["specs"].get("capacity") > 0 else ["Error"]}; def check_compliance(state: CraneState): return {"status": "COMPLIANT" if "ISO 9001 verified" in state["validation_results"] else "REJECTED"}; graph = StateGraph(CraneState); graph.add_node("validate", validate_specs); graph.add_node("compliance", check_compliance); graph.set_entry_point("validate"); graph.add_edge("validate", "compliance"); graph.add_edge("compliance", END); graph = graph.compile()