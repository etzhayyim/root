from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FixationState(TypedDict): vendor_certs: List[str]; status: str; graph = StateGraph(FixationState); def validate_certs(state: FixationState): return {'status': 'VALIDATED' if 'ISO_13485' in state['vendor_certs'] else 'REJECTED'}; graph.add_node('cert_check', validate_certs); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', END); graph = graph.compile()