from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_data: dict; validation_results: List[str]; def validate_compatibility(state: ToolState): state['validation_results'].append('Compatibility check passed'); return state; def check_safety(state: ToolState): state['validation_results'].append('Safety compliance checked'); return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_compatibility); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()