from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlateState(TypedDict): model: str; specs: dict; valid: bool; def validate_plate(state: PlateState): state['valid'] = 'dnase_rnase' in state['specs'] and state['specs']['well_capacity_ul'] > 0; return state; graph = StateGraph(PlateState); graph.add_node('validate', validate_plate); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()