from langgraph.graph import StateGraph, END; from typing import TypedDict; class ToolState(TypedDict): tool_id: str; spec_validated: bool; def validate_tool(state: ToolState): state['spec_validated'] = state['tool_id'].startswith('W-'); return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_tool); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()