from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TapProcurementState(TypedDict): specs: dict; compliance_status: bool; validation_log: List[str]; def validate_specs(state): specs = state['specs']; log = []; status = True; if 'material' not in specs: log.append('Missing material'); status = False; return {'compliance_status': status, 'validation_log': log}; graph = StateGraph(TapProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()