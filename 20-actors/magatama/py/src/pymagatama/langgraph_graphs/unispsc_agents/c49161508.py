from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PitchingMachineState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: PitchingMachineState): state['valid'] = state['specs'].get('speed', 0) > 0; return state; graph = StateGraph(PitchingMachineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()