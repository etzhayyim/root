from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BiotechSpec(TypedDict): items: List[str]; safety_validated: bool; def validate_materials(state: BiotechSpec): state['safety_validated'] = all(['SDS' in item for item in state['items']]); return state; def check_compliance(state: BiotechSpec): return 'compliant' if state['safety_validated'] else 'review_needed'; graph = StateGraph(BiotechSpec); graph.add_node('validate', validate_materials); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()