from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AntiqueState(TypedDict): items: List[str]; verification_status: bool; insurance_approved: bool; def verify_provenance(state: AntiqueState): state['verification_status'] = True; return state; def check_insurance(state: AntiqueState): state['insurance_approved'] = True; return state; graph = StateGraph(AntiqueState); graph.add_node('verify', verify_provenance); graph.add_node('insure', check_insurance); graph.add_edge('verify', 'insure'); graph.add_edge('insure', END); graph.set_entry_point('verify'); graph = graph.compile()