from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LiveInvertState(TypedDict): species: str; transport_conditions: dict; validated: bool; def validate_conditions(state: LiveInvertState): state['validated'] = 'temp_range' in state['transport_conditions']; return state; def quarantine_check(state: LiveInvertState): print(f'Checking quarantine status for {state['species']}'); return state; graph = StateGraph(LiveInvertState); graph.add_node('validate', validate_conditions); graph.add_node('quarantine', quarantine_check); graph.add_edge('validate', 'quarantine'); graph.add_edge('quarantine', END); graph.set_entry_point('validate'); graph = graph.compile()