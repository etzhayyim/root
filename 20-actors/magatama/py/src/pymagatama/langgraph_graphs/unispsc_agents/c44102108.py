from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; def validate_parts(state: ProcState): validated = all(len(item) > 0 for item in state['items']); return {'validated': validated}; graph = StateGraph(ProcState); graph.add_node('validate', validate_parts); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()