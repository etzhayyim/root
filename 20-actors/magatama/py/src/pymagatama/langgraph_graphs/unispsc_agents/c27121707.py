from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): data: dict; valid: bool; def validate_spec(state: AgentState): data = state['data']; state['valid'] = 'wire_gauge' in data and 'material' in data; return state; def route_logic(state: AgentState): return 'valid' if state['valid'] else 'invalid'; graph = StateGraph(AgentState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()