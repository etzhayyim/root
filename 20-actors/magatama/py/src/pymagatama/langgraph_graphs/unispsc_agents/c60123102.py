from langgraph.graph import StateGraph, END; from typing import TypedDict; class CraftSupplyState(TypedDict): vendor: str; specs: dict; approved: bool; def validate_specs(state: CraftSupplyState): state['approved'] = 'wire_gauge' in state['specs'] and 'material' in state['specs']; return state; builder = StateGraph(CraftSupplyState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()