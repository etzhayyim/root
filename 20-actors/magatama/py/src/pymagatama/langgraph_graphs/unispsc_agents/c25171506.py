from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpProcState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: PumpProcState): specs = state['specs']; errors = []; if 'voltage' not in specs: errors.append('Missing voltage'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(PumpProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()