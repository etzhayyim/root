from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DeviceState(TypedDict): device_id: str; specs: dict; approved: bool; def check_compliance(state: DeviceState): specs = state.get('specs', {}); state['approved'] = specs.get('buoyancy_n', 0) > 50; return state; graph = StateGraph(DeviceState); graph.add_node('compliance', check_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()