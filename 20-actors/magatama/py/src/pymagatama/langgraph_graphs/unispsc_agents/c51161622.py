from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): vendor_gmp_cert: bool; purity_level: float; workflow_complete: bool; def check_gmp(state): return {'vendor_gmp_cert': True}; def validate_purity(state): return {'purity_level': 99.9}; def finalize_procurement(state): return {'workflow_complete': True}; graph = StateGraph(PharmaState); graph.add_node('check_gmp', check_gmp); graph.add_node('validate_purity', validate_purity); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('check_gmp'); graph.add_edge('check_gmp', 'validate_purity'); graph.add_edge('validate_purity', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()