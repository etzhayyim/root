from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RackState(TypedDict): specs: dict; valid: bool; def validate_specs(state: RackState): state['valid'] = 'Load capacity' in state['specs'] and state['specs']['Load capacity'] > 0; return state; graph = StateGraph(RackState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()