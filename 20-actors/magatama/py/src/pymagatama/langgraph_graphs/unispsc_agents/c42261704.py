from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class NecropsyState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: NecropsyState) -> NecropsyState: errors = []; if state['specs'].get('material') != '316_SS': errors.append('Inadequate corrosion resistance'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(NecropsyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()