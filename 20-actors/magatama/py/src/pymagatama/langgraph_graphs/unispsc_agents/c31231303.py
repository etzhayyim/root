from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TitaniumSpecState(TypedDict): material_spec: str; validation_passed: bool; specs: List[str]; def validate_titanium_specs(state: TitaniumSpecState): state['validation_passed'] = all(s in state['specs'] for s in ['ASTM-standard', 'chemical-analysis']); return state; graph = StateGraph(TitaniumSpecState); graph.add_node('validate', validate_titanium_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()