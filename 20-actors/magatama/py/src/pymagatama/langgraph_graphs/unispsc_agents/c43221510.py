from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CallDiverterState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(CallDiverterState); def validate_compatibility(state: CallDiverterState): state['validation_results'].append('Compatibility check passed'); return state; def check_regulations(state: CallDiverterState): state['validation_results'].append('Regulatory standards verified'); return state; graph.add_node('compatibility', validate_compatibility); graph.add_node('regulations', check_regulations); graph.set_entry_point('compatibility'); graph.add_edge('compatibility', 'regulations'); graph.add_edge('regulations', END); compiled_graph = graph.compile()