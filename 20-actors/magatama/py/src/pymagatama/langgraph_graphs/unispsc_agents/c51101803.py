from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): chemical_name: str; purity: float; compliant: bool; def check_purity(state: PharmState): state['compliant'] = state['purity'] >= 99.0; return state; def regulatory_check(state: PharmState): print(f'Verifying {state["chemical_name"]} compliance'); return state; graph = StateGraph(PharmState); graph.add_node('purity_check', check_purity); graph.add_node('regulatory', regulatory_check); graph.add_edge('purity_check', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('purity_check'); graph = graph.compile()