from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StereotacticState(TypedDict): data: dict; validation_results: List[str]; def validate_regulatory(state: StereotacticState): state['validation_results'].append('Regulatory checks passed'); return state; def validate_technical(state: StereotacticState): state['validation_results'].append('Precision specs verified'); return state; graph = StateGraph(StereotacticState); graph.add_node('regulatory', validate_regulatory); graph.add_node('technical', validate_technical); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'technical'); graph.add_edge('technical', END); graph = graph.compile()