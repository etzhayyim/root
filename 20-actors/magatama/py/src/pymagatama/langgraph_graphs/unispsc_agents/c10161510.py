from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class SeedState(TypedDict): seeds: Annotated[list, operator.add]; validation_status: bool; def check_purity(state: SeedState): state['validation_status'] = len(state['seeds']) > 0; return state; graph = StateGraph(SeedState); graph.add_node('check_purity', check_purity); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', END); graph = graph.compile()