from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DecorationState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(DecorationState); def validate_specs(state: DecorationState): return {'validation_results': {'dimensions': 'verified', 'material': 'compliant'}}; def finalize_order(state: DecorationState): return {'validation_results': 'finalized'}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()