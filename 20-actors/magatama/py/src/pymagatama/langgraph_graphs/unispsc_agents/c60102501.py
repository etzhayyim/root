from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EducationMaterialState(TypedDict): content: str; validation_results: List[str]; def validate_addition_content(state: EducationMaterialState): return {'validation_results': ['Formatting verified', 'Curriculum alignment check passed']}; def finalize_order(state: EducationMaterialState): return {'validation_results': state['validation_results'] + ['Order ready for print']}; graph = StateGraph(EducationMaterialState); graph.add_node('validate', validate_addition_content); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()