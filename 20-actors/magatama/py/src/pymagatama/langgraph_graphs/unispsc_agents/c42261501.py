from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AutopsyToolState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: AutopsyToolState): state['validation_results'].append('Material grade confirmed'); return state; def check_sterilization(state: AutopsyToolState): state['validation_results'].append('Autoclave compliance check passed'); return state; graph = StateGraph(AutopsyToolState); graph.add_node('validate_material', validate_materials); graph.add_node('check_sterilization', check_sterilization); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_sterilization'); graph.add_edge('check_sterilization', END); graph = graph.compile()