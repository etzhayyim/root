from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): data: dict; logs: Annotated[list, operator.add]; def validate_medical_specs(state: State): specs = state['data'].get('specs', {}); status = 'COMPLIANT' if 'ISO-10993' in specs else 'NON-COMPLIANT'; return {'logs': [f'Validation status: {status}']}; graph = StateGraph(State); graph.add_node('validate', validate_medical_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()