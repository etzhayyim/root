from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): content: dict; safety_check: bool; def safety_validator(state): state['safety_check'] = 'ST' in state['content'].get('certifications', []) return state; def finalizer(state): return {'status': 'processed', 'compliant': state['safety_check']}; graph = StateGraph(State); graph.add_node('validate', safety_validator); graph.add_node('finalize', finalizer); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()