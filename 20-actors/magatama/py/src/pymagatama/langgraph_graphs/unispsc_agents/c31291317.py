from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): specs: dict; valid: bool; def check_specs(state: ProcState): state['valid'] = 'tolerance' in state['specs'] and 'material' in state['specs']; return state; graph = StateGraph(ProcState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()