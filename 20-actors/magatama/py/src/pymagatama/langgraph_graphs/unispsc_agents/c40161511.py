from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): specs: dict; validation_report: list; is_approved: bool; def check_pressure(state: FilterState): state['validation_report'].append('Pressure rating verified'); return {'is_approved': True} if state['specs'].get('pressure', 0) > 0 else {'is_approved': False}; graph = StateGraph(FilterState); graph.add_node('validate_pressure', check_pressure); graph.set_entry_point('validate_pressure'); graph.add_edge('validate_pressure', END); graph = graph.compile()