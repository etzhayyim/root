from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): doc_id: str; compliance_check: bool; metadata_valid: bool; def validate_metadata(state: ProcurementState): return {'metadata_valid': True}; def check_compliance(state: ProcurementState): return {'compliance_check': True}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_metadata); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()