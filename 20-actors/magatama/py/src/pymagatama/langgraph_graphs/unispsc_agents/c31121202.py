from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): geometry_data: str; alloy_spec: str; validation_result: bool; def validate_geometry(state: CastingState): return {'validation_result': True if state['geometry_data'] else False}; def check_alloy(state: CastingState): return {'validation_result': 'FE_ALLOY' in state['alloy_spec']}; graph = StateGraph(CastingState); graph.add_node('validate_geo', validate_geometry); graph.add_node('check_alloy', check_alloy); graph.set_entry_point('validate_geo'); graph.add_edge('validate_geo', 'check_alloy'); graph.add_edge('check_alloy', END); graph = graph.compile()