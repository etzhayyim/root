from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OilTestState(TypedDict): input_data: dict; validation_results: List[str]; status: str; def validate_instrument(state: OilTestState): results = []; if 'calibration_cert' not in state['input_data']: results.append('Missing Calibration Certificate'); return {'validation_results': results, 'status': 'validated' if not results else 'error'}; graph = StateGraph(OilTestState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()