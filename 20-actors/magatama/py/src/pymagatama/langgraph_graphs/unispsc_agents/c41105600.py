from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DNAProcurementState(TypedDict): items: List[str]; validation_results: dict; status: str; def validate_integrity(state: DNAProcurementState): return {'validation_results': {'integrity': 'passed'}}; def check_regulatory(state: DNAProcurementState): return {'status': 'regulatory_check_complete'}; graph = StateGraph(DNAProcurementState); graph.add_node('validate', validate_integrity); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); app = graph.compile()