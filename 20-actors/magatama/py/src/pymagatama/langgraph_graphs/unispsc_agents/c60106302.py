from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ForensicState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(ForensicState); def validate_lab_grade(state): state['validation_results'] = {'compliant': True}; return state; def check_regulatory_compliance(state): state['validation_results']['regulated'] = True; return state; graph.add_node('validate', validate_lab_grade); graph.add_node('compliance', check_regulatory_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()