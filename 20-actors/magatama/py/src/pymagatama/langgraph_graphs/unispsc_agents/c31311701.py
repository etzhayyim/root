from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): pipe_specs: dict; validation_results: List[str]; def validate_bonding(state: PipeState): return {'validation_results': ['Bonding integrity checked']}; def check_pressure(state: PipeState): return {'validation_results': ['Pressure test passed']}; def export_control_check(state: PipeState): return {'validation_results': ['Dual-use compliance verified']}; graph = StateGraph(PipeState); graph.add_node('validate_bond', validate_bonding); graph.add_node('pressure_check', check_pressure); graph.add_node('export_review', export_control_check); graph.set_entry_point('validate_bond'); graph.add_edge('validate_bond', 'pressure_check'); graph.add_edge('pressure_check', 'export_review'); graph.add_edge('export_review', END); graph = graph.compile()