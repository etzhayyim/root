from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AirFreshenerState(TypedDict): chemical_data: dict; compliance_status: bool; def validate_sds(state: AirFreshenerState): return {'compliance_status': 'SDS' in state['chemical_data']}; def check_voc(state: AirFreshenerState): return {'compliance_status': state['chemical_data'].get('voc_level', 100) < 50}; graph = StateGraph(AirFreshenerState); graph.add_node('validate_sds', validate_sds); graph.add_node('check_voc', check_voc); graph.set_entry_point('validate_sds'); graph.add_edge('validate_sds', 'check_voc'); graph.add_edge('check_voc', END); compiled_graph = graph.compile()