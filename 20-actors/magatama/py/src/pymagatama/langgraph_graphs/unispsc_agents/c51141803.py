from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): chemical_name: str; hazard_level: int; approved: bool; def validate_compliance(state: ProcurementState): if state['hazard_level'] > 3: state['approved'] = True; return state; def finalize_order(state: ProcurementState): print(f'Processing order for {state.chemical_name}'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_compliance); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()