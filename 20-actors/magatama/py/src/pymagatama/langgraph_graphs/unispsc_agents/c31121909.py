from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): mold_material: str; dimensions: List[float]; safety_cleared: bool; def validate_beryllium(state: MoldState): return {'safety_cleared': 'beryllium' in state['mold_material'].lower()}; def check_specs(state: MoldState): return {'safety_cleared': len(state['dimensions']) == 3}; graph = StateGraph(MoldState); graph.add_node('validate', validate_beryllium); graph.add_node('check', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); compiled_graph = graph.compile()