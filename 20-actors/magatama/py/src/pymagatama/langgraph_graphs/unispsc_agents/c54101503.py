from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class JewelryState(TypedDict): metal_purity: str; gemstone_grading: str; is_verified: bool; def validate_purity(state: JewelryState): state['is_verified'] = state['metal_purity'] in ['18k', '24k', 'Pt950']; return state; def verify_grading(state: JewelryState): state['is_verified'] = state['is_verified'] and (len(state['gemstone_grading']) > 0); return state; graph = StateGraph(JewelryState); graph.add_node('validate_metal', validate_purity); graph.add_node('verify_gem', verify_grading); graph.set_entry_point('validate_metal'); graph.add_edge('validate_metal', 'verify_gem'); graph.add_edge('verify_gem', END); graph = graph.compile()