from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(FastenerState); def validate_thread_standards(state: FastenerState): return {'validation_results': ['Thread pitch verified']}; def check_material_specs(state: FastenerState): return {'validation_results': ['Material grade ISO 898-1 verified']}; graph.add_node('validate_thread', validate_thread_standards); graph.add_node('check_material', check_material_specs); graph.set_entry_point('validate_thread'); graph.add_edge('validate_thread', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()