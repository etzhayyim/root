from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_lift_specs(state: State): specs = state['specs']; errors = []; if specs.get('load_capacity_kg', 0) < 250: errors.append('Insufficient load capacity'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(State); graph.add_node('validate', validate_lift_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()