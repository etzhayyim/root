from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FractionToolState(TypedDict): items: List[str]; validated: bool; def validate_specs(state: FractionToolState): return {'validated': all(item for item in state['items'])}; graph = StateGraph(FractionToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()