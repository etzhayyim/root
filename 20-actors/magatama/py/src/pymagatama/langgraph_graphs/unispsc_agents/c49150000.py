from langgraph.graph import StateGraph, END; from typing import TypedDict; class WinterSportsState(TypedDict): equipment_type: str; is_certified: bool; quality_score: float; def validate_equipment(state: WinterSportsState): return {'is_certified': state['equipment_type'] == 'professional'}; def check_compliance(state: WinterSportsState): return {'quality_score': 100.0 if state['is_certified'] else 50.0}; graph = StateGraph(WinterSportsState); graph.add_node('validate', validate_equipment); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()