from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class NuclearComponentState(TypedDict): component_id: str; alloy_composition: dict; safety_certifications: list[str]; validation_status: bool; workflow_log: Annotated[Sequence[str], operator.add]; def validate_material(state: NuclearComponentState) -> dict: print(f'Validating alloy for {state[component_id]}'); return {validation_status: True, workflow_log: ['Material verified against IAEA standards']}; def check_security(state: NuclearComponentState) -> dict: return {workflow_log: ['Security clearance checked for dual-use components']}; graph = StateGraph(NuclearComponentState); graph.add_node(validate, validate_material); graph.add_node(security, check_security); graph.set_entry_point(validate); graph.add_edge(validate, security); graph.add_edge(security, END); graph = graph.compile()