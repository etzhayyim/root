from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class PaperState(TypedDict): paper_type: str; weight_gsm: int; status: str; graph = StateGraph(PaperState); def validate_paper(state: PaperState): return {'status': 'validated' if state['weight_gsm'] > 70 else 'rejected'}; def process_procurement(state: PaperState): return {'status': 'processed'}; graph.add_node('validate', validate_paper); graph.add_node('process', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()