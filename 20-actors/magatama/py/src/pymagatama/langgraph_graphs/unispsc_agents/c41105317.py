from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_type: str; purity_check: bool; specs: dict; graph = StateGraph(ReagentState); def validate_purity(state: ReagentState): return {'purity_check': state['specs'].get('purity_grade') == 'Molecular Biology Grade'}; def check_specs(state: ReagentState): return {'purity_check': True}; graph.add_node('validate', validate_purity); graph.add_node('approve', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()