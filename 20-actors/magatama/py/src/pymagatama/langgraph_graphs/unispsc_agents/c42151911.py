from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): doc_id: str; compliance_status: bool; specs: dict; def validate_dental_material(state: ProcState): return {'compliance_status': 'ISO10993' in state['specs']}; def approve_procurement(state: ProcState): return {'compliance_status': True}; graph = StateGraph(ProcState); graph.add_node('validate', validate_dental_material); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', lambda s: 'approve' if s['compliance_status'] else END); graph.add_edge('approve', END); graph = graph.compile()