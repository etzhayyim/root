from langgraph.graph import StateGraph, END; from typing import TypedDict; class EngineState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: EngineState): state['compliant'] = state['specs'].get('emission_standard') == 'EU-Stage-V'; return state; graph = StateGraph(EngineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()