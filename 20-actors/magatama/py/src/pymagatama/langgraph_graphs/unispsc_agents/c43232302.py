from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): nodes: List[str]; config: dict; status: str; def validate_cluster_config(state: GraphState): return {'status': 'validated' if 'nodes' in state else 'error'}; def deploy_cluster(state: GraphState): return {'status': 'deployed'}; graph = StateGraph(GraphState); graph.add_node('validate', validate_cluster_config); graph.add_node('deploy', deploy_cluster); graph.set_entry_point('validate'); graph.add_edge('validate', 'deploy'); graph.add_edge('deploy', END); compiled_graph = graph.compile()