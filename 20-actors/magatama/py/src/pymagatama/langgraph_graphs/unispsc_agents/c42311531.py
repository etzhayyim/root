from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_standards(state: ProcurementState): state['validated'] = all(c in state.get('certs', []) for c in ['ISO-13485', 'CE-Mark']); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_medical_standards); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()