from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GarnetState(TypedDict): specs: dict; validation_results: dict; graph = StateGraph(GarnetState); def validate_specs(state: GarnetState): if 'mesh_size' not in state['specs']: return {'validation_results': {'status': 'failed', 'reason': 'Missing mesh size'}}; return {'validation_results': {'status': 'passed'}}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()