from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GameProcurementState(TypedDict): vendor_check: str; compliance_status: bool; workflow_steps: Annotated[list, operator.add]; def validate_vendor(state: GameProcurementState): return {'compliance_status': True, 'workflow_steps': ['Vendor Verified']}; def verify_safety(state: GameProcurementState): return {'workflow_steps': ['Safety Standards Confirmed']}; graph = StateGraph(GameProcurementState); graph.add_node('vendor', validate_vendor); graph.add_node('safety', verify_safety); graph.set_entry_point('vendor'); graph.add_edge('vendor', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()