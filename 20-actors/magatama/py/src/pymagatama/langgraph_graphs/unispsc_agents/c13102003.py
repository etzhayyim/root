from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CommodityState(TypedDict): input_data: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_chemical(state: CommodityState): results = []; flash_point = state['input_data'].get('flash_point', 0); if flash_point < 23: results.append('HIGH_FLAMMABILITY_ALERT'); return {'validation_results': results, 'status': 'VALIDATED'}; def check_regulatory(state: CommodityState): results = []; if state['input_data'].get('is_sanctioned'): results.append('EXPORT_CONTROL_RESTRICTION'); return {'validation_results': results, 'status': 'CHECKED'}; graph = StateGraph(CommodityState); graph.add_node('validate', validate_chemical); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()