from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicroscopeState(TypedDict): specs: dict; compliance_checked: bool; validation_report: str; def validate_specs(state: MicroscopeState): state['compliance_checked'] = True; state['validation_report'] = 'Validation Successful'; return state; def check_export_control(state: MicroscopeState): if state['specs'].get('resolution') < 100: state['compliance_checked'] = False; return state; graph = StateGraph(MicroscopeState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()