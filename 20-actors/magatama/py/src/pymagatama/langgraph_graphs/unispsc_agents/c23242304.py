from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineSpec(TypedDict): model_id: str; specs_verified: bool; export_check_passed: bool; def validate_specs(state: MachineSpec): state['specs_verified'] = True; return state; def check_export_controls(state: MachineSpec): state['export_check_passed'] = True; return state; graph = StateGraph(MachineSpec); graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph = graph.compile()