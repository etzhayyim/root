from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class KitState(TypedDict): items: Annotated[list, operator.add]; status: str; def validate_instrument(state: KitState): print('Validating instrument specs...'); return {'status': 'validated'}; def finalize_procurement(state: KitState): print('Finalizing order...'); return {'status': 'ordered'}; graph = StateGraph(KitState); graph.add_node('validate', validate_instrument); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()