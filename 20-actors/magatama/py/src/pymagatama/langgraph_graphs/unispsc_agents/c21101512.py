from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class IrrigationState(TypedDict): components: list; specifications: dict; validated: bool; def check_pressure_spec(state: IrrigationState): state['validated'] = state['specifications'].get('psi', 0) > 0; return state; def finalize_order(state: IrrigationState): return {'validated': True}; graph_builder = StateGraph(IrrigationState); graph_builder.add_node('validate', check_pressure_spec); graph_builder.add_node('finalize', finalize_order); graph_builder.add_edge('validate', 'finalize'); graph_builder.add_edge('finalize', END); graph_builder.set_entry_point('validate'); graph = graph_builder.compile()