from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validated: bool; def validate_hook(state: State): return {'validated': all('J-hook' in item for item in state['items'])}; graph = StateGraph(State); graph.add_node('validate', validate_hook); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()