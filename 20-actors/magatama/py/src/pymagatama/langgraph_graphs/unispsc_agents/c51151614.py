from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; validation_status: bool; inventory_docs: List[str]; def validate_purity(state: PharmState): state['validation_status'] = True; return state; def check_compliance(state: PharmState): return {'inventory_docs': ['GMP_Cert', 'COA_Verified']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()