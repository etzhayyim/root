from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_validation: bool; compliance_check: bool; logistics_approved: bool; def check_medical_compliance(state: ProcurementState): return {'compliance_check': True}; def validate_logistics(state: ProcurementState): return {'logistics_approved': True}; graph = StateGraph(ProcurementState); graph.add_node('compliance', check_medical_compliance); graph.add_node('logistics', validate_logistics); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'logistics'); graph.add_edge('logistics', END); compiled_graph = graph.compile()