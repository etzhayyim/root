from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): sensitivity_check: bool; compliance_status: List[str]; def conduct_sensitivity_test(state: AnalysisState): print("Validating luminescence threshold"); return {"sensitivity_check": True}; def verify_regulatory_standards(state: AnalysisState): print("Verifying FDA/CE compliance"); return {"compliance_status": ["ISO13485", "CE-IVD"]}; graph = StateGraph(AnalysisState); graph.add_node("sensitivity", conduct_sensitivity_test); graph.add_node("compliance", verify_regulatory_standards); graph.set_entry_point("sensitivity"); graph.add_edge("sensitivity", "compliance"); graph.add_edge("compliance", END); graph = graph.compile()