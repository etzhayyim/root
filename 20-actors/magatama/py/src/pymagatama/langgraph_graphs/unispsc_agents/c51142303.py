from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): naltrexone_batch_id: str; compliance_checks: List[str]; approved: bool; def validate_api_purity(state: PharmaState): state['compliance_checks'].append('purity_verified'); return state; def check_regulatory_status(state: PharmaState): state['compliance_checks'].append('regulatory_passed'); state['approved'] = True; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_api_purity); graph.add_node('compliance', check_regulatory_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()