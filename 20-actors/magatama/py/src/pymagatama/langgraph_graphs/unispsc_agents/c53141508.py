from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RivetState(TypedDict): rivet_type: str; specs: dict; valid: bool; def validate_specs(state: RivetState): state['valid'] = 'material_composition' in state['specs'] and 'plating_specifications' in state['specs']; return state; builder = StateGraph(RivetState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()