from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class VDIState(TypedDict): vdi_provisioning_queue: list; validation_results: Annotated[list, operator.add]; security_audit_passed: bool; def validate_vdi_node(state: VDIState): return {validation_results: [f'Validating VDI provisioning for {len(state["vdi_provisioning_queue"])} units']}; def security_scan_node(state: VDIState): return {security_audit_passed: True}; builder = StateGraph(VDIState); builder.add_node("validator", validate_vdi_node); builder.add_node("security", security_scan_node); builder.add_edge("validator", "security"); builder.add_edge("security", END); builder.set_entry_point("validator"); graph = builder.compile()