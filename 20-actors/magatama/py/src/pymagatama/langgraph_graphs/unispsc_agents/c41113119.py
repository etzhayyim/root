from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input_spec: dict; validation_report: list[str]; def validate_sensor(state: ProcState): report = []; if state['input_spec'].get('accuracy') < 0.01: report.append('Accuracy verified'); return {'validation_report': report}; def finalize(state: ProcState): return {'validation_report': state['validation_report'] + ['Finalized']}; graph = StateGraph(ProcState); graph.add_node('validate', validate_sensor); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()