from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; log: List[str]; def validate_safety(state: ProcureState): return {'validated': True, 'log': ['Safety certification verified']}; def check_expiry(state: ProcureState): return {'log': ['Expiry compliance checked']}; graph = StateGraph(ProcureState); graph.add_node('safety', validate_safety); graph.add_node('expiry', check_expiry); graph.set_entry_point('safety'); graph.add_edge('safety', 'expiry'); graph.add_edge('expiry', END); graph = graph.compile()