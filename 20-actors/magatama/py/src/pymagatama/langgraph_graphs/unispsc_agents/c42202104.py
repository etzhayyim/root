from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): doc_id: str; compliance_status: bool; radiation_shielding: bool; def validate_compliance(state: State): state['compliance_status'] = True; return state; def check_shielding(state: State): state['radiation_shielding'] = True; return state; graph = StateGraph(State); graph.add_node('validate', validate_compliance); graph.add_node('shielding_check', check_shielding); graph.set_entry_point('validate'); graph.add_edge('validate', 'shielding_check'); graph.add_edge('shielding_check', END); graph = graph.compile()