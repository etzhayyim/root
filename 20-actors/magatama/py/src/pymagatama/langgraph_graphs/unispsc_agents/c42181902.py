from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ICPState(TypedDict): device_id: str; quality_checks: List[str]; approved: bool; def validate_sensor(state: ICPState): state['quality_checks'].append('sensor_calibrated'); return state; def verify_compliance(state: ICPState): state['approved'] = True; return state; graph = StateGraph(ICPState); graph.add_node('validate', validate_sensor); graph.add_node('verify', verify_compliance); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); graph.set_entry_point('validate'); graph = graph.compile()