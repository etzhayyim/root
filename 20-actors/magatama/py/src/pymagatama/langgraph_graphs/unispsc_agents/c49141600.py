from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class PurchaseOrder(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_specs(state: PurchaseOrder): state['validated'] = all(c in state.get('certs', []) for c in ['CE', 'ISO']); return state; graph = StateGraph(PurchaseOrder); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()