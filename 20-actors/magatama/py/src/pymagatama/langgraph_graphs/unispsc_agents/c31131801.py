from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): alloy_type: str; specs_met: bool; inspections: List[str]; def validate_alloy(state: ForgingState): return {'specs_met': state['alloy_type'] in ['Aluminum', 'Titanium', 'Copper']}; def check_testing(state: ForgingState): return {'inspections': ['Ultrasonic', 'Hardness']}; graph = StateGraph(ForgingState); graph.add_node('validate', validate_alloy); graph.add_node('inspect', check_testing); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()