from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): content: str; validated: bool; compliance: List[str]; def validate_material(state: MaterialState): state['validated'] = len(state['content']) > 0; return state; def check_compliance(state: MaterialState): state['compliance'] = ['Accessibility', 'Educational Standard']; return state; graph = StateGraph(MaterialState); graph.add_node('validate', validate_material); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()