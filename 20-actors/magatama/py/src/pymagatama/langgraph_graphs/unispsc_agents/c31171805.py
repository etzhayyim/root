from langgraph.graph import StateGraph, END; from typing import TypedDict; class BrushState(TypedDict): specs: dict; valid: bool; def validate_specs(state: BrushState): state['valid'] = state['specs'].get('max_rpm_rating', 0) > 0; return state; graph = StateGraph(BrushState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()