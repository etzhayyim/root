from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ChartState(TypedDict): content: str; validated: bool; metadata: dict; graph = StateGraph(ChartState); def validate_content(state: ChartState): return {'validated': 'biology' in state['content'].lower()}; def finalize_spec(state: ChartState): return {'metadata': {'status': 'approved_for_procurement'}}; graph.add_node('validate', validate_content); graph.add_node('finalize', finalize_spec); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()