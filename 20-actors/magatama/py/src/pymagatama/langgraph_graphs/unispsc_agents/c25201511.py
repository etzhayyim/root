from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftWingState(TypedDict): wing_id: str; compliance_checks: List[str]; approved: bool; def validate_engineering(state: AircraftWingState): state['compliance_checks'].append('AS9100_CHECK'); return {'approved': True if 'ITAR_CLEARED' in state['compliance_checks'] else False}; graph = StateGraph(AircraftWingState); graph.add_node('validate', validate_engineering); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()