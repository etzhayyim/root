from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): badge_data: dict; compliance_check: bool; def validate_badge(state: State): state['compliance_check'] = 'calibration_cert' in state['badge_data'] and state['badge_data']['sensitivity'] > 0; return state; graph = StateGraph(State); graph.add_node('validate', validate_badge); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()