from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): vendor_check: bool; spec_compliance: bool; def validate_composition(state: ProcureState): return {'spec_compliance': True}; def verify_certification(state: ProcureState): return {'vendor_check': True}; graph = StateGraph(ProcureState); graph.add_node('val_comp', validate_composition); graph.add_node('ver_cert', verify_certification); graph.set_entry_point('val_comp'); graph.add_edge('val_comp', 'ver_cert'); graph.add_edge('ver_cert', END); graph = graph.compile()