from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_passed: bool; issues: List[str]; def validate_safety(state: State): state['validation_passed'] = state['input_data'].get('safety_certified', False); state['issues'] = [] if state['validation_passed'] else ['Safety certification missing']; return state; builder = StateGraph(State); builder.add_node('safety_check', validate_safety); builder.set_entry_point('safety_check'); builder.add_edge('safety_check', END); graph = builder.compile()