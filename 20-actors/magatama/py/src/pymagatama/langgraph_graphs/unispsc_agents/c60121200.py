from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaintProcurementState(TypedDict): items: List[str]; validated_items: List[str]; inspection_required: bool; def validate_compliance(state: PaintProcurementState): return {'validated_items': [item for item in state['items'] if 'Non-Toxic' in item], 'inspection_required': True}; def update_inventory(state: PaintProcurementState): return {'inspection_required': False}; graph = StateGraph(PaintProcurementState); graph.add_node('validate', validate_compliance); graph.add_node('archive', update_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'archive'); graph.add_edge('archive', END); compiled_graph = graph.compile()