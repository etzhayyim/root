from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TarpaulinState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: TarpaulinState): state['compliant'] = state['specs'].get('tensile_strength', 0) > 500; return state; graph = StateGraph(TarpaulinState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()