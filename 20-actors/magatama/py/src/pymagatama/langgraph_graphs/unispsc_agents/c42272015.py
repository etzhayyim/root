from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): content: dict; status: str; def validate_sterile_spec(state: State): state['status'] = 'VALIDATED' if state['content'].get('sterile') else 'REJECTED'; return state; graph = StateGraph(State); graph.add_node('validate', validate_sterile_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()