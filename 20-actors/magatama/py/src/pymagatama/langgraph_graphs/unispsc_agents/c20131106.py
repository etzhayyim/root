from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RobotState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: RobotState): state['valid'] = all(k in state['specs'] for k in ['input_voltage_range', 'control_protocol']); return state; def check_compliance(state: RobotState): state['log'].append('Compliance checked against ISO standards'); return state; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()