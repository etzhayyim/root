from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmergencyKitState(TypedDict): kit_id: str; components: List[str]; validated: bool; state: EmergencyKitState = EmergencyKitState(kit_id="", components=[], validated=False); def validate_components(state: EmergencyKitState): state["validated"] = all(c is not None for c in state["components"]); return state; def check_expiration(state: EmergencyKitState): return state; graph = StateGraph(EmergencyKitState); graph.add_node("validate", validate_components); graph.add_node("expiration", check_expiration); graph.add_edge("validate", "expiration"); graph.add_edge("expiration", END); graph.set_entry_point("validate"); compiled_graph = graph.compile()