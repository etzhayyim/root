from langgraph.graph import StateGraph, END; from typing import TypedDict; class ForensicState(TypedDict): magnification: float; material: str; validated: bool; def check_optics(state: ForensicState): state['validated'] = state['magnification'] > 0; return state; graph = StateGraph(ForensicState); graph.add_node('check', check_optics); graph.set_entry_point('check'); graph.add_edge('check', END); graph = graph.compile()