from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; validation_passed: bool; def check_specs(state: CompressorState): return {'validation_passed': 'max_pressure_rating' in state['specs']}; def approve_procurement(state: CompressorState): return {'validation_passed': True}; graph = StateGraph(CompressorState); graph.add_node('validate', check_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()