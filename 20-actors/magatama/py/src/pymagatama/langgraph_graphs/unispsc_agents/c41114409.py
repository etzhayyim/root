from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WindSensorState(TypedDict): sensor_specs: dict; validation_status: bool; logs: List[str]; def validate_sensor(state: WindSensorState): specs = state['sensor_specs']; valid = all(key in specs for key in ['accuracy', 'range', 'ip_rating']); return {'validation_status': valid, 'logs': ['Validation complete']}; def generate_report(state: WindSensorState): print('Generating procurement report...'); return {}; graph = StateGraph(WindSensorState); graph.add_node('validate', validate_sensor); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()