from langgraph.graph import StateGraph, END; from typing import TypedDict; class OrthoState(TypedDict): status: str; certification_valid: bool; def validate_cert(state: OrthoState): return {'certification_valid': True, 'status': 'Validated'}; def check_sterile(state: OrthoState): return {'status': 'Ready_for_Ship'}; graph = StateGraph(OrthoState); graph.add_node('validate', validate_cert); graph.add_node('check_sterile', check_sterile); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_sterile'); graph.add_edge('check_sterile', END); graph.compile()