from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrimState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(TrimState); def validate_sequins(state: TrimState): return {'validation_results': {'status': 'passed', 'check': 'material_compliance'}}; graph.add_node('validate', validate_sequins); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()