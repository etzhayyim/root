from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): doorbell_model: str; spec_validated: bool; def validate_specs(state: State): return {'spec_validated': True} if 'model' in state['doorbell_model'] else {'spec_validated': False}; def finish_node(state: State): return; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('finish', finish_node); graph.set_entry_point('validate'); graph.add_edge('validate', 'finish'); graph.add_edge('finish', END); graph = graph.compile()