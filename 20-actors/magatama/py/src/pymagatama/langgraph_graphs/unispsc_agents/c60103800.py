from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class HistoryResourceState(TypedDict): content: str; validated: bool; compliance_report: str; def validate_resource(state: HistoryResourceState): print('Validating content alignment...'); return {'validated': True}; def generate_report(state: HistoryResourceState): return {'compliance_report': 'Aligned with national curriculum standards'}; graph = StateGraph(HistoryResourceState); graph.add_node('validate', validate_resource); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()