from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): specs: dict; quality_passed: bool; graph = StateGraph(SupplyState); def validate_specs(state: SupplyState): state['quality_passed'] = state['specs'].get('brix', 0) > 60; return state; def check_logistics(state: SupplyState): return 'approved' if state['quality_passed'] else 'rejected'; graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()