from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: str; validation_status: str; def validate_specs(state: State): return {'validation_status': 'verified' if state['input_data'] else 'failed'}; def process_accessories(state: State): return {'validation_status': 'processed'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('process', process_accessories); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()