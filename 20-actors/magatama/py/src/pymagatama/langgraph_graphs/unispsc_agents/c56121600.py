from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): items: list; valid_items: list; status: str; def validate_furniture(state: FurnitureState): validated = [item for item in state['items'] if item.get('cert') == 'safety_approved']; return {'valid_items': validated, 'status': 'completed'}; graph = StateGraph(FurnitureState); graph.add_node('validate', validate_furniture); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()