from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CleanerState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_parts(state: CleanerState): state['valid'] = state['specs'].get('pressure_rating_mpa', 0) > 0; return state; graph = StateGraph(CleanerState); graph.add_node('validate', validate_parts); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()