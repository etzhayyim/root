from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; inspection_results: dict; status: str; def validate_quality(state: ProcurementState): return {'inspection_results': {'freshness': 'passed', 'compliance': 'verified'}, 'status': 'validated'}; def finalize_order(state: ProcurementState): return {'status': 'approved'}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_quality); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()