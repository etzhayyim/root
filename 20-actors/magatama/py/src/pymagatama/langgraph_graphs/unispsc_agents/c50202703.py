from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class JuiceState(TypedDict): supplier_cert: bool; brix_level: float; quality_passed: bool; def check_quality(state: JuiceState): state['quality_passed'] = state['brix_level'] >= 10.0 and state['supplier_cert']; return state; builder = StateGraph(JuiceState); builder.add_node('qc', check_quality); builder.set_entry_point('qc'); builder.add_edge('qc', END); graph = builder.compile()