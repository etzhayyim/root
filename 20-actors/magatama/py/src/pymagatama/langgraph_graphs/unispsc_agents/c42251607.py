from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RehabState(TypedDict): specs: dict; validation_results: List[str]; def validate_pulley_spec(state: RehabState): results = []; capacity = state['specs'].get('capacity', 0); if capacity < 0: results.append('Invalid capacity'); state['validation_results'] = results; return state; graph = StateGraph(RehabState); graph.add_node('validate', validate_pulley_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()