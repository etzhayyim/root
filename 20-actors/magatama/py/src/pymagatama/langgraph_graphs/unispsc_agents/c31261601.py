from langgraph.graph import StateGraph, END; from typing import TypedDict; class ShellState(TypedDict): specs: dict; valid: bool; def validate_molding(state: ShellState): state['valid'] = all(k in state['specs'] for k in ['material', 'tolerance']); return state; def report_status(state: ShellState): print(f'Molding validation result: {state['valid']}'); return state; graph = StateGraph(ShellState); graph.add_node('validate', validate_molding); graph.add_node('report', report_status); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()