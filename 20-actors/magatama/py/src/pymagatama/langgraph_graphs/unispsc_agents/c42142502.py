from langgraph.graph import StateGraph, END; from typing import TypedDict; class AnesthesiaState(TypedDict): needle_spec: dict; validation_passed: bool; def validate_spec(state: AnesthesiaState): state['validation_passed'] = 'gauge' in state['needle_spec'] and 'sterile' in state['needle_spec']; return state; def route_step(state: AnesthesiaState): return 'valid' if state['validation_passed'] else 'error'; graph = StateGraph(AnesthesiaState); graph.add_node('validate', validate_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()