from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): specs: dict; validation_results: list[str]; def validate_pore_size(state: FilterState): if 'pore_size' not in state['specs']: state['validation_results'].append('Missing pore size'); return state; def validate_material(state: FilterState): return state; graph = StateGraph(FilterState); graph.add_node('validate_pore', validate_pore_size); graph.add_node('validate_material', validate_material); graph.set_entry_point('validate_pore'); graph.add_edge('validate_pore', 'validate_material'); graph.add_edge('validate_material', END); graph = graph.compile()