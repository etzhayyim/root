from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MountState(TypedDict): specs: dict; validation_passed: bool; def validate_load(state: MountState): state['validation_passed'] = state['specs'].get('load_kg', 0) > 0; return state; def check_vesa(state: MountState): state['validation_passed'] = 'VESA' in state['specs'].get('standards', ''); return state; graph = StateGraph(MountState); graph.add_node('load_check', validate_load); graph.add_node('vesa_check', check_vesa); graph.set_entry_point('load_check'); graph.add_edge('load_check', 'vesa_check'); graph.add_edge('vesa_check', END); graph = graph.compile()