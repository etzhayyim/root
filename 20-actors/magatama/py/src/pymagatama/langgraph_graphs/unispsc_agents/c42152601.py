from langgraph.graph import StateGraph, END; from typing import TypedDict; class DentalState(TypedDict): content: str; validation_result: bool; def validate_spec(state: DentalState): state['validation_result'] = 'toxicity' in state['content'].lower(); return state; graph = StateGraph(DentalState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()