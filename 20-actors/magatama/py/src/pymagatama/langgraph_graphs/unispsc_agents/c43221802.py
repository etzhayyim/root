from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FilterState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: FilterState): s = state.get('specs', {}); state['valid'] = 'insertion_loss' in s and 'wavelength' in s; return state; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()