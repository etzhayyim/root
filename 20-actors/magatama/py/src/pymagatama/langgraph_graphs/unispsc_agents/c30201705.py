from langgraph.graph import StateGraph, END; from typing import TypedDict; class GuardhouseState(TypedDict): specs: dict; valid: bool; def validate_specs(state: GuardhouseState): state['valid'] = 'structural_integrity_certification' in state['specs']; return state; builder = StateGraph(GuardhouseState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()