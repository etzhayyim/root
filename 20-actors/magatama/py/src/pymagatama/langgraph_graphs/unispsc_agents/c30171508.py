from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_door_specs(state: ProcurementState): specs = state['specs']; errors = []; if specs.get('load_rating', 0) < 50: errors.append('Load capacity too low'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_door_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()