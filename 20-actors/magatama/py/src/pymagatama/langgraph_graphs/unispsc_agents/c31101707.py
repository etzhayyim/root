from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_alloy(state: CastingState): state['validation_results'].append('Alloy ASTM B94 validation complete'); return state; def check_dimensions(state: CastingState): state['validation_results'].append('Tolerance check passed'); return state; graph = StateGraph(CastingState); graph.add_node('alloy_check', validate_alloy); graph.add_node('dim_check', check_dimensions); graph.set_entry_point('alloy_check'); graph.add_edge('alloy_check', 'dim_check'); graph.add_edge('dim_check', END); graph = graph.compile()