from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StampingState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: StampingState): state['validation_results'].append('Dimensional check passed'); return state; def check_material_specs(state: StampingState): state['validation_results'].append('Material grade verified'); return state; graph = StateGraph(StampingState); graph.add_node('dimension_check', validate_dimensions); graph.add_node('material_check', check_material_specs); graph.set_entry_point('dimension_check'); graph.add_edge('dimension_check', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()