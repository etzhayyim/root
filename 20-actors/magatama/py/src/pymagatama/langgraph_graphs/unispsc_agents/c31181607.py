from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MastBootState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_materials(state: MastBootState): state['compliance'] = state['specs'].get('material') in ['EPDM', 'Neoprene']; return state; def check_dimensions(state: MastBootState): state['approved'] = state['specs'].get('diameter_mm', 0) > 0; return state; graph = StateGraph(MastBootState); graph.add_node('validate', validate_materials); graph.add_node('dimensions', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', 'dimensions'); graph.add_edge('dimensions', END); graph = graph.compile()