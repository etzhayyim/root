from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): data: dict; status: list[str]; def validate_aviation_soft(state: GraphState): state['status'].append('validation_complete'); return state; def check_compliance(state: GraphState): state['status'].append('compliance_checked'); return state; workflow = StateGraph(GraphState); workflow.add_node('validate', validate_aviation_soft); workflow.add_node('compliance', check_compliance); workflow.set_entry_point('validate'); workflow.add_edge('validate', 'compliance'); workflow.add_edge('compliance', END); graph = workflow.compile()