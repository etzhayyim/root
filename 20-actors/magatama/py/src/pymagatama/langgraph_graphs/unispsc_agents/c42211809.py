from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_specs: dict; compliance_verified: bool; errors: List[str]; def validate_materials(state: ProcurementState): state['compliance_verified'] = 'hypoallergenic' in state['vendor_specs'].get('certifications', []); return state; def check_strength(state: ProcurementState): if state['vendor_specs'].get('tensile', 0) < 5: state['errors'].append('Insufficient tensile strength'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate_materials', validate_materials); graph.add_node('check_strength', check_strength); graph.set_entry_point('validate_materials'); graph.add_edge('validate_materials', 'check_strength'); graph.add_edge('check_strength', END); app = graph.compile()