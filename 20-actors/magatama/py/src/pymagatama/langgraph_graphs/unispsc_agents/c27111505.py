from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ToolState(TypedDict): items: List[str]; inspection_passed: bool; def validate_hardness(state: ToolState): return {'inspection_passed': True}; def check_dimensions(state: ToolState): return {'inspection_passed': True}; graph = StateGraph(ToolState); graph.add_node('validate', validate_hardness); graph.add_node('measure', check_dimensions); graph.add_edge('validate', 'measure'); graph.add_edge('measure', END); graph.set_entry_point('validate'); graph = graph.compile()