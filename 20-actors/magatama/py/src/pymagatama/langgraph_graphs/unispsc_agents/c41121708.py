from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeProcurementState(TypedDict): tube_type: str; volume: float; is_compliant: bool; def validate_tube_specs(state: TubeProcurementState): return {'is_compliant': state['volume'] > 0 and state['tube_type'] in ['EDTA', 'Citrate', 'Heparin']}; def route_procurement(state: TubeProcurementState): return 'valid' if state['is_compliant'] else 'invalid'; graph = StateGraph(TubeProcurementState); graph.add_node('validation', validate_tube_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()