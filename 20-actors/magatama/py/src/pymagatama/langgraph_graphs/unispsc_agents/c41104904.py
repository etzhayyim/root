from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_pore_size(state: FilterState): val = state['input_spec'].get('pore_size'); passed = 0.1 <= val <= 50.0; return {'validation_results': [f'Pore size {val}um check: {passed}'], 'status': 'validated' if passed else 'failed'}; def perform_integrity_test(state: FilterState): return {'validation_results': ['Integrity pressure test: pass']}; graph = StateGraph(FilterState); graph.add_node('pore_check', validate_pore_size); graph.add_node('integrity_test', perform_integrity_test); graph.set_entry_point('pore_check'); graph.add_edge('pore_check', 'integrity_test'); graph.add_edge('integrity_test', END); compiled_graph = graph.compile()