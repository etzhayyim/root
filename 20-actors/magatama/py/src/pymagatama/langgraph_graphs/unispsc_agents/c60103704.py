from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LearningResourceState(TypedDict): content: str; validated: bool; format_check: bool; def validate_curriculum(state: LearningResourceState): state['validated'] = 'alignment_score' in state; return state; def check_format(state: LearningResourceState): state['format_check'] = True; return state; graph = StateGraph(LearningResourceState); graph.add_node('validate_curriculum', validate_curriculum); graph.add_node('check_format', check_format); graph.set_entry_point('validate_curriculum'); graph.add_edge('validate_curriculum', 'check_format'); graph.add_edge('check_format', END); graph = graph.compile()