from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StructuralState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(StructuralState); def validate_spec(state: StructuralState): return {'validation_results': ['Material check passed', 'Tensile test verified']}; def check_compliance(state: StructuralState): return {'validation_results': state['validation_results'] + ['Compliance verified']}; graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()