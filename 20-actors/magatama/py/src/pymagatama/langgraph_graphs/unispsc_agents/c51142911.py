from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_id: str; compliance_docs: List[str]; approved: bool; def check_compliance(state: ProcState): state['approved'] = 'GMP_Cert' in state['compliance_docs'] and 'COA' in state['compliance_docs']; return state; builder = StateGraph(ProcState); builder.add_node('compliance', check_compliance); builder.set_entry_point('compliance'); builder.add_edge('compliance', END); graph = builder.compile()