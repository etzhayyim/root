from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ISDNState(TypedDict): data: dict; status: str; def validate_specs(state: ISDNState): state['status'] = 'VALIDATED' if 'interface' in state['data'] else 'REJECTED'; return state; def run_diagnostic(state: ISDNState): state['status'] = 'TESTED' if state['status'] == 'VALIDATED' else 'ERROR'; return state; graph = StateGraph(ISDNState); graph.add_node('validate', validate_specs); graph.add_node('diagnostic', run_diagnostic); graph.add_edge('validate', 'diagnostic'); graph.add_edge('diagnostic', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()