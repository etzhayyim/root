from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FloorStandState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: FloorStandState): state['valid'] = state['specs'].get('load_capacity', 0) > 0; state['log'].append('Validation completed'); return state; graph = StateGraph(FloorStandState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()