from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipeState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: PipeState): results = []; if 'Material' not in state['specs']: results.append('Missing material spec'); return {'validation_results': results}; def check_pressure(state: PipeState): return {'validation_results': state['validation_results'] + ['Pressure check passed']}; graph = StateGraph(PipeState); graph.add_node('validate', validate_materials); graph.add_node('pressure_check', check_pressure); graph.set_entry_point('validate'); graph.add_edge('validate', 'pressure_check'); graph.add_edge('pressure_check', END); graph.compile()