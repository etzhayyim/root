from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; gmp_verified: bool; compliance_check: bool; def verify_gmp(state: PharmState): return {'gmp_verified': True}; def validate_compliance(state: PharmState): return {'compliance_check': True}; graph = StateGraph(PharmState); graph.add_node('verify_gmp', verify_gmp); graph.add_node('compliance', validate_compliance); graph.set_entry_point('verify_gmp'); graph.add_edge('verify_gmp', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()