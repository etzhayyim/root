from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): chemical_data: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_purity(state: MaterialState): purity = state['chemical_data'].get('purity', 0); return {'validation_results': ['Purity check passed'] if purity > 99.9 else ['Purity check failed'], 'status': 'validated' if purity > 99.9 else 'rejected'}; def process_material(state: MaterialState): return {'status': 'processed', 'validation_results': ['Material processing completed']}; graph = StateGraph(MaterialState); graph.add_node('validate', validate_purity); graph.add_node('process', process_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()