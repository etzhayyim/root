from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): data: dict; tasks: Annotated[list, operator.add]; graph = StateGraph(ProcState); def validate_medical_cert(state: ProcState): certs = state['data'].get('certs', []); return {'tasks': ['verify_iso_13485'] if 'ISO13485' in certs else ['flag_compliance_error']}; def process_shipping(state: ProcState): return {'tasks': ['check_sterilization_expiry']}; graph.add_node('validate', validate_medical_cert); graph.add_node('shipping', process_shipping); graph.set_entry_point('validate'); graph.add_edge('validate', 'shipping'); graph.add_edge('shipping', END); compiled = graph.compile()