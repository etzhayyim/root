from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrumLidState(TypedDict): lid_spec: dict; validation_results: List[str]; graph = StateGraph(DrumLidState); def validate_specs(state: DrumLidState): state['validation_results'] = ['Material check passed' if state['lid_spec'].get('material') else 'Material missing']; return state; def check_compatibility(state: DrumLidState): state['validation_results'].append('Compatibility compliant with standard'); return state; graph.add_node('validate', validate_specs); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph.compile()