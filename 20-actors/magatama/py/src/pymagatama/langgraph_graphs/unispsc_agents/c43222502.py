from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VPNState(TypedDict): device_specs: dict; compliance_status: bool; workflow_path: List[str]; def validate_security(state: VPNState): state['compliance_status'] = 'encryption_standards_compliance' in state['device_specs']; return state; def route_step(state: VPNState): return 'compliance_check' if state['device_specs'] else END; graph = StateGraph(VPNState); graph.add_node('compliance_check', validate_security); graph.set_entry_point('compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()