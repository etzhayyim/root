from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IrradiatorState(TypedDict): vendor_certs: List[str]; safety_clearance: bool; export_license: bool; def validate_safety(state: IrradiatorState): return {'safety_clearance': 'ISO_RADIO' in state['vendor_certs']}; def verify_export(state: IrradiatorState): return {'export_license': True}; graph = StateGraph(IrradiatorState); graph.add_node('safety', validate_safety); graph.add_node('export', verify_export); graph.add_edge('safety', 'export'); graph.add_edge('export', END); graph.set_entry_point('safety'); graph = graph.compile()