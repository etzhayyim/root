from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): content_material: str; compliance_check: bool; target_age: int; def validate_curriculum(state: State): return {'compliance_check': state['target_age'] >= 5}; def finalize_materials(state: State): return {'content': 'Finalized for distribution'}; graph = StateGraph(State); graph.add_node('validation', validate_curriculum); graph.add_node('finalization', finalize_materials); graph.set_entry_point('validation'); graph.add_edge('validation', 'finalization'); graph.add_edge('finalization', END); graph = graph.compile()