from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): assay_data: dict; validation_results: Annotated[Sequence[str], operator.add]; storage_check: bool; def validate_reagent(state: ReagentState): return {'validation_results': ['assay_spec_verified']}; def check_storage(state: ReagentState): return {'storage_check': True}; graph = StateGraph(ReagentState); graph.add_node('validate', validate_reagent); graph.add_node('storage', check_storage); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph.set_entry_point('validate'); graph = graph.compile()