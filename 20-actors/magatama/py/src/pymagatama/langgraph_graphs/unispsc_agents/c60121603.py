from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input_data: dict; validation_report: dict; status: str; def validate_spec(state: ProcureState): state['validation_report'] = {'passed': True, 'notes': 'Standard plastic verification completed'}; state['status'] = 'VALIDATED'; return state; def finalize_order(state: ProcureState): state['status'] = 'READY_FOR_RFQ'; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()