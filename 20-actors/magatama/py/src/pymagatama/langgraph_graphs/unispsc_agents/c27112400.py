from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): tool_id: str; specifications: dict; validation_passed: bool; def validate_specs(state: FastenerState): state['validation_passed'] = 'torque_capacity' in state['specifications'] and state['specifications']['torque_capacity'] > 0; return state; graph = StateGraph(FastenerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()