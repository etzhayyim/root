from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CementProcurementState(TypedDict): vendor_specs: dict; compliance_passed: bool; inspection_report: str; def validate_specs(state: CementProcurementState): return {'compliance_passed': state['vendor_specs'].get('strength', 0) > 42.5}; def generate_report(state: CementProcurementState): return {'inspection_report': 'Analysis complete: Compliant'}; graph = StateGraph(CementProcurementState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()