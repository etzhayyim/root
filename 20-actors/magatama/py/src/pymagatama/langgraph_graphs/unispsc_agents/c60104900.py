from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PhysicsSpecState(TypedDict): specs: List[str]; validated: bool; def validate_specs(state: PhysicsSpecState): state['validated'] = len(state['specs']) > 0; return state; graph = StateGraph(PhysicsSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()