from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TelephonyState(TypedDict): specs: dict; validation_results: List[str]; def validate_hardware(state: TelephonyState): specs = state.get('specs', {}); results = []; results.append('Check signal latency' if specs.get('latency') else 'Fail'); return {'validation_results': results}; graph = StateGraph(TelephonyState); graph.add_node('validate', validate_hardware); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()