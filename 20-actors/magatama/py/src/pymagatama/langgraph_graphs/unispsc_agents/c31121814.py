from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: MoldState): result = 'Pass' if state['specs'].get('tolerance') < 0.005 else 'Fail'; state['validation_results'].append(result); return state; def check_material(state: MoldState): state['validation_results'].append('MaterialVerified'); return state; graph = StateGraph(MoldState); graph.add_node('validate_dim', validate_dimensions); graph.add_node('check_mat', check_material); graph.set_entry_point('validate_dim'); graph.add_edge('validate_dim', 'check_mat'); graph.add_edge('check_mat', END); compiled_graph = graph.compile()