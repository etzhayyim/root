from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CastState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def check_specs(state: CastState): state['validation_errors'] = [f'Missing {f}' for f in ['tensile_strength', 'purity'] if f not in state['specs']]; state['approved'] = len(state['validation_errors']) == 0; return state; graph = StateGraph(CastState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()