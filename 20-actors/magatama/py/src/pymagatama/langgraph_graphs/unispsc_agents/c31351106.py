from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeState(TypedDict): tube_specs: dict; validation_results: List[str]; def validate_uv_specs(state: TubeState): results = []; if state['tube_specs'].get('pressure_tested'): results.append('Pressure check passed'); return {'validation_results': results}; def export_control_check(state: TubeState): return {'validation_results': state['validation_results'] + ['Export compliance verified']}; graph = StateGraph(TubeState); graph.add_node('validate', validate_uv_specs); graph.add_node('compliance', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()