from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): active_ingredient: str; purity_percent: float; compliance_standards: List[str]; qc_passed: bool; def validate_api(state: PharmState): state['qc_passed'] = state['purity_percent'] >= 99.0 and 'USP' in state['compliance_standards']; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_api); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()