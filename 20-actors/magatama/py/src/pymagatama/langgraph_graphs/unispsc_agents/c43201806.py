from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TapeArrayState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: TapeArrayState): errors = []; if 'capacity_per_slot' not in state['specs']: errors.append('Missing capacity'); return {'validation_errors': errors}; def check_export_compliance(state: TapeArrayState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(TapeArrayState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate');