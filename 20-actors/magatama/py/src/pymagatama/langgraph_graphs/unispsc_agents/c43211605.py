from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcessorState(TypedDict): input_spec: dict; validation_results: List[str]; approved: bool; def validate_specs(state: ProcessorState): results = []; specs = state['input_spec']; if specs.get('ghz', 0) < 1.0: results.append('Insufficient clock speed'); return {'validation_results': results, 'approved': len(results) == 0}; graph = StateGraph(ProcessorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()