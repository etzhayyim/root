from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; gmp_certified: bool; approved: bool; def check_gmp(state: PharmaState): return {'gmp_certified': True} if 'Felbamate' == state['chemical_name'] else {'gmp_certified': False}; def validate_reg(state: PharmaState): return {'approved': True} if state['gmp_certified'] else {'approved': False}; graph = StateGraph(PharmaState); graph.add_node('check_gmp', check_gmp); graph.add_node('validate_reg', validate_reg); graph.set_entry_point('check_gmp'); graph.add_edge('check_gmp', 'validate_reg'); graph.add_edge('validate_reg', END); graph = graph.compile()