from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(State); def validate_purity(state: State): return {'validation_results': ['Purity verified']}; def check_tolerances(state: State): return {'validation_results': ['Tolerance within spec']}; graph.add_node('purity_check', validate_purity); graph.add_node('tolerance_check', check_tolerances); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'tolerance_check'); graph.add_edge('tolerance_check', END); graph = graph.compile()