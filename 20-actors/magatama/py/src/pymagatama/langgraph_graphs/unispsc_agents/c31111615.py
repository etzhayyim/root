from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; validation_report: List[str]; approved: bool; def validate_tin_spec(state: ProcureState): specs = state['specs']; errors = []; if specs.get('purity_pct', 0) < 99.9: errors.append('Insufficient Tin purity'); if not specs.get('iso_cert', False): errors.append('Missing ISO certification'); return {'validation_report': errors, 'approved': len(errors) == 0}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_tin_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()