from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AgriState(TypedDict): machinery_spec: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: AgriState): errors = []; if not state['machinery_spec'].get('engine_output'): errors.append('Missing Engine Output'); return {'validation_errors': errors, 'approved': len(errors) == 0}; def finalize(state: AgriState): return {'approved': True}; graph = StateGraph(AgriState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.finish(allowed_nodes=['finalize'])