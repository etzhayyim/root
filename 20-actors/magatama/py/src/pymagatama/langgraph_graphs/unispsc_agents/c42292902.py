from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalSupplyState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SurgicalSupplyState): errors = []; missing = [f for f in ['sterile_level', 'iso_cert'] if f not in state['specs']]; if missing: errors.append(f'Missing required fields: {missing}'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(SurgicalSupplyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()