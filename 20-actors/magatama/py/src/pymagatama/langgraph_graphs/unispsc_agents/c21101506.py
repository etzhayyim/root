from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): model_id: str; specs: dict; ready_for_procurement: bool; def validate_specs(state: MachineState): state['ready_for_procurement'] = 'engine_tier_rating' in state['specs'] and state['specs']['operating_weight_kg'] > 0; return state; graph = StateGraph(MachineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()