from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ProcState): state['valid'] = 'Temperature Control Range' in state['specs']; return state; def check_compliance(state: ProcState): return 'compliance_check' if state['valid'] else END; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()