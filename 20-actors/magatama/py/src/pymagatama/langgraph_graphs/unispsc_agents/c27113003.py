from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ApplicatorState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: ApplicatorState): errors = []; if not state['specs'].get('solvent_compatibility'): errors.append('Missing solvent compatibility'); return {'validation_errors': errors, 'status': 'valid' if not errors else 'invalid'}; graph = StateGraph(ApplicatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()