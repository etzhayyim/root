from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): component_data: dict; quality_report: dict; status: str; def validate_welds(state: AssemblyState): state['status'] = 'VALIDATING_WELDS'; return state; def check_surface(state: AssemblyState): state['status'] = 'CHECKING_SURFACE'; return state; graph = StateGraph(AssemblyState); graph.add_node('validate_welds', validate_welds); graph.add_node('check_surface', check_surface); graph.set_entry_point('validate_welds'); graph.add_edge('validate_welds', 'check_surface'); graph.add_edge('check_surface', END); graph = graph.compile()