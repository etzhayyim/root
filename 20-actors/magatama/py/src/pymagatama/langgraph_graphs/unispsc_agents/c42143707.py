from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): device_id: str; specs: dict; validation_errors: List[str]; def validate_medical_specs(state: State): specs = state.get('specs', {}); errors = []; if not specs.get('iso_13485'): errors.append('Missing ISO 13485'); if not specs.get('temp_precision'): errors.append('Missing temp precision'); return {'validation_errors': errors}; graph = StateGraph(State); graph.add_node('validate', validate_medical_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()