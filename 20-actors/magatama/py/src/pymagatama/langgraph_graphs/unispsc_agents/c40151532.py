from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FuelPumpState(TypedDict): pump_id: str; specs: dict; compliance_check: bool; graph = StateGraph(FuelPumpState); def validate_specs(state: FuelPumpState): return {'compliance_check': all(k in state['specs'] for k in ['flow_rate', 'pressure'])}; def check_safety(state: FuelPumpState): return {'compliance_check': state['specs'].get('explosion_proof', False)}; graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()