from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LuggageState(TypedDict): specs: dict; validation_results: List[str]; def validate_durability(state: LuggageState): state['validation_results'].append('Durability check passed'); return state; def validate_material(state: LuggageState): state['validation_results'].append('Material safety verified'); return state; graph = StateGraph(LuggageState); graph.add_node('durability', validate_durability); graph.add_node('material', validate_material); graph.set_entry_point('durability'); graph.add_edge('durability', 'material'); graph.add_edge('material', END); graph = graph.compile()