from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_specs: dict; compliance_check: bool; graph = StateGraph(ProcureState); def validate_specs(state: ProcureState): return {'compliance_check': 'ergonomics' in state['vendor_specs']}; def finalize_order(state: ProcureState): return {'compliance_check': True}; graph.add_node('validation', validate_specs); graph.add_node('finalization', finalize_order); graph.set_entry_point('validation'); graph.add_edge('validation', 'finalization'); graph.add_edge('finalization', END); graph = graph.compile()