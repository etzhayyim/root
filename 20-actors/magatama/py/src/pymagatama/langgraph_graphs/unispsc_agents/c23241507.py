from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec: dict; valid: bool; def validate_spec(state: ProcState): state['valid'] = 'motor_power_kw' in state['spec'] and state['spec']['motor_power_kw'] > 0; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()