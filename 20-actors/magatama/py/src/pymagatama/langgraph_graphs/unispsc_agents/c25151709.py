from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SatelliteState(TypedDict): status: str; specs: dict; compliance: bool; def validate_specs(state: SatelliteState): state['compliance'] = 'orbital_slot_clearance' in state['specs']; return state; def check_export(state: SatelliteState): return 'status' if state['compliance'] else 'FAILED'; graph = StateGraph(SatelliteState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()