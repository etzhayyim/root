from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): norgestimate_batch: str; compliance_check: bool; def validate_gmp(state: PharmaState): return {'compliance_check': True} if 'GMP' in state['norgestimate_batch'] else {'compliance_check': False}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()