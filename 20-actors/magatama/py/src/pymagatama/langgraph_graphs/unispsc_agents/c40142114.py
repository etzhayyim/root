from langgraph.graph import StateGraph, END; from typing import TypedDict; class PipeState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PipeState): state['valid'] = 'alloy-composition' in state['specs']; return state; builder = StateGraph(PipeState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()