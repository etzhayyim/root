from langgraph.graph import StateGraph, END; from typing import TypedDict; class WeldingState(TypedDict): welding_params: dict; safety_check: bool; graph = StateGraph(WeldingState); def validate_laser_params(state: WeldingState): state['safety_check'] = state['welding_params'].get('power_kw', 0) < 5.0; return state; graph.add_node('validate', validate_laser_params); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()