from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TitaniumState(TypedDict): material_spec: dict; compliance: bool; log: Annotated[list, operator.add]; def validate_grade(state: TitaniumState): grade = state['material_spec'].get('grade'); valid = grade in ['Grade 5', 'Grade 23']; return {'compliance': valid, 'log': [f'Grade validation: {valid}']}; def check_export(state: TitaniumState): return {'log': ['ITAR/EAR export check completed']}; graph = StateGraph(TitaniumState); graph.add_node('validate', validate_grade); graph.add_node('export', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()