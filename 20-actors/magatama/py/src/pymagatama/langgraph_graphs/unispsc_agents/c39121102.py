from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MeterSpecState(TypedDict): spec: dict; validated: bool; errors: List[str]; def validate_electrical_specs(state: MeterSpecState): specs = state['spec']; errors = []; if specs.get('voltage') < 120: errors.append('Voltage too low'); return {'validated': len(errors) == 0, 'errors': errors}; graph = StateGraph(MeterSpecState); graph.add_node('validate', validate_electrical_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()