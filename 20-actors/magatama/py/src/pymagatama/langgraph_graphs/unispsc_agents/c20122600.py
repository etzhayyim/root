from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class ActuatorState(TypedDict): specs: Dict; validation_results: List[str]; approved: bool; def validate_specs(state: ActuatorState): results = []; specs = state.get('specs', {}); if 'torque' not in specs: results.append('Missing torque'); return {'validation_results': results, 'approved': len(results) == 0}; graph = StateGraph(ActuatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()