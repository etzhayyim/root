from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): alloy: str; dimensions: dict; certs: List[str]; approved: bool; def check_alloy(state): return {'approved': state['alloy'] in ['AZ31B', 'ZK60A']}; def validate_specs(state): return {'approved': state['approved'] and all(c in state['certs'] for c in ['ISO9001', 'AS9100'])}; graph = StateGraph(State); graph.add_node('check_material', check_alloy); graph.add_node('verify_certs', validate_specs); graph.set_entry_point('check_material'); graph.add_edge('check_material', 'verify_certs'); graph.add_edge('verify_certs', END); graph = graph.compile()