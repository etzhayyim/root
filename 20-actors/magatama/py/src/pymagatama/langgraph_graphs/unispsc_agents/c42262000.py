from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmbalmingState(TypedDict): items: List[str]; safety_compliance: bool; verified: bool; def validate_compliance(state: EmbalmingState) -> EmbalmingState: state['safety_compliance'] = True; return state; def inspection_step(state: EmbalmingState) -> EmbalmingState: state['verified'] = True; return state; graph = StateGraph(EmbalmingState); graph.add_node('validate', validate_compliance); graph.add_node('inspect', inspection_step); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()