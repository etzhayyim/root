from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GloveBoxState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: GloveBoxState): if "leak_rate" in state['specs']: return {'compliance': True, 'status': 'Validated'} return {'compliance': False, 'status': 'Missing Specs'}; def finalize_order(state: GloveBoxState): return {'status': 'Approved'}; graph = StateGraph(GloveBoxState); graph.add_node('validation', validate_specs); graph.add_node('approval', finalize_order); graph.set_entry_point('validation'); graph.add_edge('validation', 'approval'); graph.add_edge('approval', END); graph = graph.compile()