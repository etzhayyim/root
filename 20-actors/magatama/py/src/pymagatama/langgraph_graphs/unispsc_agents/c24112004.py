from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BinProcurementState(TypedDict): vendor_id: str; bin_specs: dict; compliance_check: bool; def validate_specs(state: BinProcurementState): load = state['bin_specs'].get('load_capacity', 0); return {'compliance_check': load > 0}; def finalize_order(state: BinProcurementState): return {'status': 'processed'}; graph = StateGraph(BinProcurementState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); app = graph.compile()