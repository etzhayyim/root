from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: AssemblyState): return {"validation_results": state["validation_results"] + ["Material composition verified"]}; def check_welds(state: AssemblyState): return {"validation_results": state["validation_results"] + ["X-ray weld inspection passed"]}; graph = StateGraph(AssemblyState); graph.add_node("verify_material", validate_material); graph.add_node("weld_inspection", check_welds); graph.set_entry_point("verify_material"); graph.add_edge("verify_material", "weld_inspection"); graph.add_edge("weld_inspection", END); graph = graph.compile()