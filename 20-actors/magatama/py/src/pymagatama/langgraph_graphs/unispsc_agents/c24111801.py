from langgraph.graph import StateGraph, END; from typing import TypedDict; class ReservoirState(TypedDict): specs: dict; valid: bool; def check_specs(state: ReservoirState): state['valid'] = 'pressure_rating_mpa' in state['specs'] and state['specs']['capacity_liters'] > 0; return state; graph = StateGraph(ReservoirState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()