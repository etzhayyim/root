from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: ProcState): state['compliance'] = state['specs'].get('purity', 0) > 95; return state; def check_export(state: ProcState): state['approved'] = state['compliance'] and state['specs'].get('export_permit', False); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()