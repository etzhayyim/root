from langgraph.graph import StateGraph, END; from typing import TypedDict; class CouplingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: CouplingState): state['valid'] = 'Material Grade' in state['specs'] and 'Torque Rating' in state['specs']; return state; def route_logic(state: CouplingState): return 'validate' if state.get('specs') else END; graph = StateGraph(CouplingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()