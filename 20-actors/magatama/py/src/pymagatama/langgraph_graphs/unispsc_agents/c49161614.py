from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: Annotated[list, operator.add]; status: str; def validate_equipment(state: ProcState): return { "status": "validated" if len(state['items']) > 0 else "error" }; def check_compliance(state: ProcState): return { "status": "compliant" }; graph = StateGraph(ProcState); graph.add_node("validate", validate_equipment); graph.add_node("compliance", check_compliance); graph.set_entry_point("validate"); graph.add_edge("validate", "compliance"); graph.add_edge("compliance", END); graph = graph.compile()