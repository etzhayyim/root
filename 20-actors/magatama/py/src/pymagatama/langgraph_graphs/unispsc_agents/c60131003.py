from langgraph.graph import StateGraph, END; from typing import TypedDict; class OrganState(TypedDict): organ_specs: dict; validation_results: list; graph = StateGraph(OrganState); def validate_specs(state): return {'validation_results': ['Specs Verified'] if 'Acoustic Range' in state['organ_specs'] else ['Missing Specs']}; def finalize_order(state): return {'validation_results': state['validation_results'] + ['Order Ready']}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()