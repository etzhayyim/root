from langgraph.graph import StateGraph, END; from typing import TypedDict; class ToyProcurementState(TypedDict): safety_certified: bool; material_spec_valid: bool; graph = StateGraph(ToyProcurementState); def check_safety(state: ToyProcurementState): return {'safety_certified': True}; def validate_specs(state: ToyProcurementState): return {'material_spec_valid': True}; graph.add_node('safety_check', check_safety); graph.add_node('spec_validation', validate_specs); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'spec_validation'); graph.add_edge('spec_validation', END); graph = graph.compile()