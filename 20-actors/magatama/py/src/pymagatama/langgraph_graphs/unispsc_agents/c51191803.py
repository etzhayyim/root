from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplierState(TypedDict): vendor_id: str; quality_score: float; material_specs: dict; compliance_verified: bool; def validate_purity(state: SupplierState): state['compliance_verified'] = state['material_specs'].get('assay', 0) >= 99.0; return state; def check_pharmacopoeia(state: SupplierState): state['compliance_verified'] = state['material_specs'].get('standard') in ['USP', 'EP', 'JP']; return state; graph = StateGraph(SupplierState); graph.add_node('check_purity', validate_purity); graph.add_node('check_standard', check_pharmacopoeia); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'check_standard'); graph.add_edge('check_standard', END); graph = graph.compile()