from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnchorState(TypedDict): specs: dict; valid: bool; def validate_specs(state: AnchorState): state['valid'] = state['specs'].get('strength_kn', 0) >= 22; return state; graph = StateGraph(AnchorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()