from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): input_specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_composition(state: MoldState): return {'validation_results': ['Material composition checked']}; def check_tolerances(state: MoldState): return {'validation_results': ['Dimensional tolerances verified']}; workflow = StateGraph(MoldState); workflow.add_node('validate_material', validate_composition); workflow.add_node('check_dimensional', check_tolerances); workflow.set_entry_point('validate_material'); workflow.add_edge('validate_material', 'check_dimensional'); workflow.add_edge('check_dimensional', END); graph = workflow.compile()