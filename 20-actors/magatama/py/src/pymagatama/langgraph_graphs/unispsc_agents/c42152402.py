from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MouthguardState(TypedDict): material_spec: dict; validation_results: List[str]; graph = StateGraph(MouthguardState); def validate_material(state: MouthguardState): status = "Pass" if state['material_spec'].get('iso_certified') else "Fail"; return {'validation_results': [f'Material compliance: {status}']}; graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()