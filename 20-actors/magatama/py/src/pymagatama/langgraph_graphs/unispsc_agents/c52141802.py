from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HeaterState(TypedDict): specs: dict; safety_compliant: bool; def validate_specs(state: HeaterState): state['safety_compliant'] = state['specs'].get('Safety Certification') in ['PSE', 'UL']; return state; def check_capacity(state: HeaterState): return 'valid' if state['specs'].get('Heating Capacity') > 0 else 'invalid'; graph = StateGraph(HeaterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()