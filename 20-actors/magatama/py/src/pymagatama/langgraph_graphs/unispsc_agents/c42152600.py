from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): items: List[str]; validated: bool; from_gmp_certified: bool; def check_medical_compliance(state: DentalState) -> DentalState: state['validated'] = all(item.startswith('REF-') for item in state['items']); return state; def verify_gmp_status(state: DentalState) -> DentalState: state['from_gmp_certified'] = True; return state; graph = StateGraph(DentalState); graph.add_node('compliance_check', check_medical_compliance); graph.add_node('gmp_verification', verify_gmp_status); graph.set_entry_point('compliance_check'); graph.add_edge('compliance_check', 'gmp_verification'); graph.add_edge('gmp_verification', END); graph = graph.compile()