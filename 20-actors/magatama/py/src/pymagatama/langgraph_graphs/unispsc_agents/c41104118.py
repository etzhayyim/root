from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecimenState(TypedDict): specimen_type: str; is_sterile: bool; validation_log: List[str]; def validate_specimen_packet(state: SpecimenState): state['validation_log'].append('Checking sterility status'); if state.get('is_sterile'): state['validation_log'].append('Validation Passed'); else: state['validation_log'].append('Validation Failed'); return state; graph = StateGraph(SpecimenState); graph.add_node('validate', validate_specimen_packet); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()