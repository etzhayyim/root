from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeAssemblyState(TypedDict): specs: dict; validation_results: List[str]; def validate_material_certs(state: PipeAssemblyState): return {'validation_results': state['validation_results'] + ['Material Check Passed']}; def run_pressure_test(state: PipeAssemblyState): return {'validation_results': state['validation_results'] + ['Pressure Test Verified']}; graph = StateGraph(PipeAssemblyState); graph.add_node('material_check', validate_material_certs); graph.add_node('pressure_test', run_pressure_test); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'pressure_test'); graph.add_edge('pressure_test', END); compiled_graph = graph.compile()