from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): bowl_specs: dict; validation_results: List[str]; def validate_specs(state: State): specs = state['bowl_specs']; results = []; results.append('Passed' if specs.get('flush_vol', 0) <= 6 else 'Failed'); return {'validation_results': results}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()