from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; spill_rating: float; graph = StateGraph(ProcureState); def check_spec(state: ProcureState): return {'validated': state['spill_rating'] > 10.0}; def finalize(state: ProcureState): return {'items': ['Absorbent Sock SKU verified']}; graph.add_node('validate', check_spec); graph.add_node('finalize', finalize); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); app = graph.compile()