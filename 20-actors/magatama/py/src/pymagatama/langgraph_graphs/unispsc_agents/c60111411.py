from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SprayState(TypedDict): content: str; hazards: List[str]; compliant: bool; def validate_safety(state: SprayState): state['compliant'] = 'flammable' not in state['hazards']; return state; def route_step(state: SprayState): return 'validate' if state['content'] else END; graph = StateGraph(SprayState); graph.add_node('validate', validate_safety); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()