from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CleaningState(TypedDict): status: str; specs: dict; valid: bool; def validate_spec(state: CleaningState): state['valid'] = 'chemical_composition_msds' in state['specs']; return state; def check_compatibility(state: CleaningState): state['status'] = 'COMPATIBLE' if state['valid'] else 'REJECTED'; return state; graph = StateGraph(CleaningState); graph.add_node('validate', validate_spec); graph.add_node('check', check_compatibility); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()