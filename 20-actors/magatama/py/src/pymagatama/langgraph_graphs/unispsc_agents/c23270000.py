from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeldingState(TypedDict): items: List[str]; safety_verified: bool; export_check_passed: bool; def verify_safety(state: WeldingState): state['safety_verified'] = True; return state; def check_export(state: WeldingState): state['export_check_passed'] = True; return state; graph = StateGraph(WeldingState); graph.add_node('safety', verify_safety); graph.add_node('export', check_export); graph.add_edge('safety', 'export'); graph.add_edge('export', END); graph.set_entry_point('safety'); graph = graph.compile()