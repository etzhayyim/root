from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HoneycombState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: HoneycombState): specs = state['specs']; is_valid = all(key in specs for key in ['density', 'cell_size']); return {'validation_report': 'Success' if is_valid else 'Failed', 'approved': is_valid}; graph = StateGraph(HoneycombState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()