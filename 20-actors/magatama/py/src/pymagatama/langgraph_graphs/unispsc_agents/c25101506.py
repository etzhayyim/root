from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class LimousineState(TypedDict): vehicle_vin: str; compliance_verified: bool; inspections_passed: bool; def verify_compliance(state: LimousineState): return {'compliance_verified': True}; def perform_inspection(state: LimousineState): return {'inspections_passed': True}; graph = StateGraph(LimousineState); graph.add_node('compliance', verify_compliance); graph.add_node('inspection', perform_inspection); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'inspection'); graph.add_edge('inspection', END); graph = graph.compile()