from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input: str; validation_results: Annotated[list, operator.add]; status: str; def validate_calcium_oxide(state: State): results = []; content = state.get('input', ''); if 'purity' not in content: results.append('Missing purity requirement'); return {'validation_results': results, 'status': 'validated' if not results else 'error'}; graph = StateGraph(State); graph.add_node('validate', validate_calcium_oxide); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()