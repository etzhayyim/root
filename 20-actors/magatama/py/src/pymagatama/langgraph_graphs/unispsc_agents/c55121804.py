from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BadgeState(TypedDict): vendor_specs: dict; inspection_status: str; graph = StateGraph(BadgeState); def validate_badge_specs(state: BadgeState): return {'inspection_status': 'verified' if state['vendor_specs'].get('material') else 'rejected'}; graph.add_node('validate', validate_badge_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()