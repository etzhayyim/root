from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): capacity: float; chemical_type: str; is_approved: bool; def validate_spec(state: State): return {'is_approved': state['capacity'] > 0 and state['chemical_type'] in ['alkaline', 'solvent']}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()