from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TENSState(TypedDict): device_specs: dict; compliance_check: bool; workflow_log: Annotated[list, operator.add]; def validate_technical_specs(state: TENSState): is_valid = 'medical_device_class' in state['device_specs'] and 'battery_certification_iec62133' in state['device_specs']; return {'compliance_check': is_valid, 'workflow_log': ['Technical specs validated']}; def finalize_procurement(state: TENSState): return {'workflow_log': ['Procurement finalized successfully']}; graph = StateGraph(TENSState); graph.add_node('validate', validate_technical_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()