from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcessState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: ProcessState): pressure = state['specs'].get('pressure', 0); state['valid'] = pressure > 0; state['log'].append('Pressure check complete'); return state; graph = StateGraph(ProcessState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()