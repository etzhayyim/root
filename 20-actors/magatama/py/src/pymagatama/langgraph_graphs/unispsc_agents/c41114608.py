from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FatigueState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: FatigueState): errors = []; if state['specs'].get('load_capacity', 0) <= 0: errors.append('Invalid load capacity'); return {'validation_errors': errors, 'status': 'VALIDATED' if not errors else 'FAILED'}; graph = StateGraph(FatigueState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()