from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class NuclearProcurementState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_instrument(state: NuclearProcurementState): results = []; if 'calibration_date' not in state['input_data']: results.append('Missing Calibration'); return {'validation_results': results}; def export_control_check(state: NuclearProcurementState): return {'status': 'EXPORT_CONTROL_REQUIRED'}; graph = StateGraph(NuclearProcurementState); graph.add_node('validate', validate_instrument); graph.add_node('security', export_control_check); graph.add_edge('validate', 'security'); graph.add_edge('security', END); graph.set_entry_point('validate'); graph = graph.compile()