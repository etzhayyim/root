from typing import TypedDict, List; from langgraph.graph import StateGraph; class CondensingState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: CondensingState): if 'Cooling Capacity' in state['specs']: state['compliance'] = True; return state; def final_approval(state: CondensingState): state['approved'] = state['compliance']; return state; graph = StateGraph(CondensingState); graph.add_node('validate', validate_specs); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph = graph.compile()