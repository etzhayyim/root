from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: AssemblyState): print('Validating low alloy steel grade...'); return {'validation_results': ['Material Certified']}; def check_welding(state: AssemblyState): print('Inspecting solvent weld bonding...'); return {'validation_results': ['Weld Integrity Confirmed']}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_material); graph.add_node('weld_check', check_welding); graph.set_entry_point('validate'); graph.add_edge('validate', 'weld_check'); graph.add_edge('weld_check', END); graph = graph.compile()