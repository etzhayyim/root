from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): spec: dict; validation_errors: List[str]; approved: bool; def validate_pressure_rating(state: PipeState): state['spec']['valid_pressure'] = state['spec'].get('PN', 0) >= 10; return state; def validate_coating(state: PipeState): state['spec']['coating_ok'] = 'epoxy' in state['spec'].get('coating_type', '').lower(); return state; graph = StateGraph(PipeState); graph.add_node('val_pressure', validate_pressure_rating); graph.add_node('val_coating', validate_coating); graph.set_entry_point('val_pressure'); graph.add_edge('val_pressure', 'val_coating'); graph.add_edge('val_coating', END); graph = graph.compile()