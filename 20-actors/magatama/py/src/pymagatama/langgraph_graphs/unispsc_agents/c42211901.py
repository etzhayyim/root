from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetyState(TypedDict): material_spec: dict; compliance_verified: bool; def validate_friction(state: SafetyState): state['compliance_verified'] = state['material_spec'].get('friction_coefficient', 0) >= 0.6; return state; workflow = StateGraph(SafetyState); workflow.add_node('validate', validate_friction); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()