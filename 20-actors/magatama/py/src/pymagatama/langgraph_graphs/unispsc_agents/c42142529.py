from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrayState(TypedDict): tray_id: str; specs: dict; compliant: bool; def validate_specs(state: TrayState): state['compliant'] = 'autoclavability_rating' in state['specs']; return state; def route_next(state: TrayState): return 'end' if state['compliant'] else 'reject'; graph = StateGraph(TrayState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_next, {'end': END, 'reject': END}); graph.add_edge('validate', END); graph = graph.compile()