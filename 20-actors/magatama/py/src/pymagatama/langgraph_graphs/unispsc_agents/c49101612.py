from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class HologramState(TypedDict): data: dict; specs: List[str]; validation_results: List[str]; def validate_optical_specs(state: HologramState): state['validation_results'].append('Checking diffraction parity'); return {'validation_results': state['validation_results']}; def check_export_compliance(state: HologramState): state['validation_results'].append('Reviewing dual-use status'); return {'validation_results': state['validation_results']}; graph = StateGraph(HologramState); graph.add_node('validate', validate_optical_specs); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()