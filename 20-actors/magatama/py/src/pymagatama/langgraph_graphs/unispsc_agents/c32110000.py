from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SemiconductorState(TypedDict): spec: dict; validation_report: dict; status: str; def validate_specs(state: SemiconductorState): import re; errors = []; if 'MSL' not in state['spec']: errors.append('Missing MSL rating'); return {'validation_report': {'errors': errors, 'valid': len(errors) == 0}}; def final_check(state: SemiconductorState): return {'status': 'Approved' if state['validation_report']['valid'] else 'Rejected'}; graph = StateGraph(SemiconductorState); graph.add_node('validate', validate_specs); graph.add_node('finalize', final_check); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()