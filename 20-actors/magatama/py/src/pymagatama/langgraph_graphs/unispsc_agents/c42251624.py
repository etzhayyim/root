from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RehabMachineState(TypedDict): input_specs: dict; validation_results: List[str]; approved: bool; def val_func(state: RehabMachineState): res = 'PASS' if 'ISO13485' in state['input_specs'].get('certs', []) else 'FAIL'; return {'validation_results': [res], 'approved': res == 'PASS'}; graph = StateGraph(RehabMachineState); graph.add_node('validator', val_func); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()