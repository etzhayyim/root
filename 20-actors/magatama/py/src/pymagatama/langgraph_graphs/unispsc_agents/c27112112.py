from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PliersState(TypedDict): specs: dict; validation_passed: bool; graph = StateGraph(PliersState); def validate_specs(state: PliersState): state['validation_passed'] = 'jaw_hardness_hrc' in state['specs'] and state['specs']['jaw_hardness_hrc'] >= 45; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()