from langgraph.graph import StateGraph, END; from typing import TypedDict; class GlutethimideState(TypedDict): chemical_data: dict; compliance_status: bool; def validate_purity(state: GlutethimideState): state['compliance_status'] = state['chemical_data'].get('purity', 0) >= 0.99; return state; def check_regulatory(state: GlutethimideState): state['compliance_status'] = state.get('compliance_status') and 'license_id' in state['chemical_data']; return state; graph = StateGraph(GlutethimideState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_regulatory', check_regulatory); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_regulatory'); graph.add_edge('check_regulatory', END); graph = graph.compile()