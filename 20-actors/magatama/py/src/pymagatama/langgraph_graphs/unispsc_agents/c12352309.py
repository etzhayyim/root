from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AdhesiveState(TypedDict): chemical_compliance: bool; material_integrity: float; log: Annotated[list[str], operator.add]; def validate_chemical_safety(state: AdhesiveState) -> AdhesiveState: state['chemical_compliance'] = True; state['log'] = ['Chemical safety verified']; return state; def check_structural_integrity(state: AdhesiveState) -> AdhesiveState: state['material_integrity'] = 0.98; state['log'] = ['Integrity test passed']; return state; graph = StateGraph(AdhesiveState); graph.add_node('validate', validate_chemical_safety); graph.add_node('check', check_structural_integrity); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()