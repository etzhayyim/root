from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PatientSupportState(TypedDict): cushion_specs: dict; validation_report: List[str]; def validate_cushion_compliance(state: PatientSupportState): report = []; if 'material_grade' not in state['cushion_specs']: report.append('Missing material grade'); return {'validation_report': report}; def finalize_procurement(state: PatientSupportState): return {'validation_report': state['validation_report'] + ['Procurement approved']}; graph = StateGraph(PatientSupportState); graph.add_node('validate', validate_cushion_compliance); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()