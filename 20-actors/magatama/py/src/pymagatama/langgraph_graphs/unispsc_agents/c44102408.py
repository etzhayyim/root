from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LabelingState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: LabelingState): result = 'Valid' if state['specs'].get('speed', 0) > 0 else 'Invalid'; return {'validation_results': [result]}; def check_safety(state: LabelingState): return {'validation_results': state['validation_results'] + ['Safety Verified']}; graph = StateGraph(LabelingState); graph.add_node('validation', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validation'); graph.add_edge('validation', 'safety'); graph.add_edge('safety', END); graph = graph.compile()