from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class MicrobiologicalState(TypedDict): kit_code: str; validation_status: bool; storage_verified: bool; def check_kit_integrity(state: MicrobiologicalState): return {'validation_status': True}; def verify_storage_requirements(state: MicrobiologicalState): return {'storage_verified': True}; graph = StateGraph(MicrobiologicalState); graph.add_node('integrity_check', check_kit_integrity); graph.add_node('storage_verification', verify_storage_requirements); graph.set_entry_point('integrity_check'); graph.add_edge('integrity_check', 'storage_verification'); graph.add_edge('storage_verification', END); graph = graph.compile()