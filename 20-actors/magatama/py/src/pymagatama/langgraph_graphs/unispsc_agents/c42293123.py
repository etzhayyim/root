from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlasticSurgeryState(TypedDict): vendor_certs: List[str]; inspection_passed: bool; target_grade: str; def validate_cert(state: PlasticSurgeryState): return state.update({'inspection_passed': 'ISO_13485' in state['vendor_certs']}); def approve_procurement(state: PlasticSurgeryState): return {'inspection_passed': True}; graph = StateGraph(PlasticSurgeryState); graph.add_node('validation', validate_cert); graph.add_node('approval', approve_procurement); graph.set_entry_point('validation'); graph.add_edge('validation', 'approval'); graph.add_edge('approval', END); compiled_graph = graph.compile()