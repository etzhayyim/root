from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ArmrestState(TypedDict): specs: dict; validation_results: List[str]; def validate_ergonomics(state: ArmrestState): state['validation_results'].append('Ergonomics check passed'); return state; def check_compatibility(state: ArmrestState): state['validation_results'].append('Mounting compatibility verified'); return state; graph = StateGraph(ArmrestState); graph.add_node('ergonomics', validate_ergonomics); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('ergonomics'); graph.add_edge('ergonomics', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()