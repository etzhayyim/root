from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input: str; validation_status: bool; def check_medical_grade(state: State): state['validation_status'] = 'MEDICAL_ISO' in state['input']; return state; def document_review(state: State): return {'validation_status': state['validation_status'] and True}; graph = StateGraph(State); graph.add_node('check_grade', check_medical_grade); graph.add_node('doc_check', document_review); graph.set_entry_point('check_grade'); graph.add_edge('check_grade', 'doc_check'); graph.add_edge('doc_check', END); graph = graph.compile()