from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class JaundiceState(TypedDict): device_id: str; data: dict; validated: bool; graph = StateGraph(JaundiceState); def validate_sensor(state: JaundiceState): state['validated'] = state['data'].get('accuracy') > 0.95; return state; graph.add_node('validate', validate_sensor); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()