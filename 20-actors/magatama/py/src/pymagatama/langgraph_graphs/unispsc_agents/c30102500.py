from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SheetState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: SheetState): return {'validation_results': ['Material check passed'] if state['specs'].get('grade') else ['Missing grade']}; def check_thickness(state: SheetState): return {'validation_results': state['validation_results'] + ['Thickness within tolerance']}; graph = StateGraph(SheetState); graph.add_node('material_check', validate_material); graph.add_node('thickness_check', check_thickness); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'thickness_check'); graph.add_edge('thickness_check', END); graph = graph.compile()