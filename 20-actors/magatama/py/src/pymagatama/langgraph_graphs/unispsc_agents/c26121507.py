from langgraph.graph import StateGraph, END; from typing import TypedDict; class WireGraphState(TypedDict): spec: dict; valid: bool; def validate_specs(state: WireGraphState): state['valid'] = state['spec'].get('impedance') == 75; return state; graph = StateGraph(WireGraphState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()