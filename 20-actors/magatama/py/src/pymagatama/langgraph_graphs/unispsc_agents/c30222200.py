from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UtilityState(TypedDict): structure_specs: dict; validation_passed: bool; inspection_reports: List[str]; def validate_specs(state: UtilityState): state['validation_passed'] = all(k in state['structure_specs'] for k in ['material', 'load_capacity']); return state; def check_compliance(state: UtilityState): state['inspection_reports'].append('Compliance checked against regional safety codes'); return state; graph = StateGraph(UtilityState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()