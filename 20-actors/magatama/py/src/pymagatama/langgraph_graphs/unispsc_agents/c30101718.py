from langgraph.graph import StateGraph, END; from typing import TypedDict; class CommodityState(TypedDict): metal_type: str; purity: float; status: str; def validate_spec(state: CommodityState): return {'status': 'verified' if state['purity'] >= 0.999 else 'rejected'}; def secure_log(state: CommodityState): print(f'Processing {state["metal_type"]} for high-value entry'); return {'status': 'secure_logged'}; graph = StateGraph(CommodityState); graph.add_node('validate', validate_spec); graph.add_node('log', secure_log); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); compiled_graph = graph.compile()