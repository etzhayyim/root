from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): spec: dict; verified: bool; def check_specs(state: State): state['verified'] = 'current_rating' in state['spec'] and state['spec']['current_rating'] > 0; return state; graph = StateGraph(State); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()