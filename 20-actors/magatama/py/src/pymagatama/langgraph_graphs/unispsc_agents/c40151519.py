from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): pump_specs: dict; compliance_verified: bool; def validate_sanitary_specs(state: PumpState): specs = state['pump_specs']; is_compliant = specs.get('material') == 'ss316' and specs.get('cip_ready', False); return {'compliance_verified': is_compliant}; graph = StateGraph(PumpState); graph.add_node('validate', validate_sanitary_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()