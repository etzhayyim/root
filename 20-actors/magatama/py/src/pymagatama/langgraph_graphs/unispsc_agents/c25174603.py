from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SeatFrameState(TypedDict): frame_specs: dict; validation_results: List[str]; def validate_material(state: SeatFrameState): state['validation_results'].append('Material verified'); return state; def check_durability(state: SeatFrameState): state['validation_results'].append('Durability test passed'); return state; graph = StateGraph(SeatFrameState); graph.add_node('material_check', validate_material); graph.add_node('durability_check', check_durability); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'durability_check'); graph.add_edge('durability_check', END); graph = graph.compile()