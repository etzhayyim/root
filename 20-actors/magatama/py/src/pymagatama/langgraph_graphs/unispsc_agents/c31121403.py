from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): input_specs: dict; validated: bool; validation_log: List[str]; def validate_specs(state: CastingState): state['validated'] = 'Material Grade' in state['input_specs'] and 'Dimensional Tolerance' in state['input_specs']; state['validation_log'].append('Spec check complete'); return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()