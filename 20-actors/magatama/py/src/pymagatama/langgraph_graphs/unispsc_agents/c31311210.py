from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipeState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_material(state: PipeState): state['valid'] = state['specs'].get('grade') in ['Grade 2', 'Grade 5']; return state; def check_export(state: PipeState): state['logs'].append('Dual-use review complete'); return state; graph = StateGraph(PipeState); graph.add_node('validate', validate_material); graph.add_node('compliance', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()