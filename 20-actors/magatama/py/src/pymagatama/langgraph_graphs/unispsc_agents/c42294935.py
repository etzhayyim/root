from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MedicalDeviceState(TypedDict): device_id: str; compliance_checked: bool; sterility_verified: bool; def check_compliance(state: MedicalDeviceState): return {'compliance_checked': True}; def verify_sterility(state: MedicalDeviceState): return {'sterility_verified': True}; graph = StateGraph(MedicalDeviceState); graph.add_node('compliance', check_compliance); graph.add_node('sterility', verify_sterility); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'sterility'); graph.add_edge('sterility', END); compiled_graph = graph.compile()