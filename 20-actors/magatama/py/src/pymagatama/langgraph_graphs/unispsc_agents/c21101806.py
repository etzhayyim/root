from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SprayerState(TypedDict): specs: dict; validation_results: bool; def check_corrosion_resistance(state: SprayerState): state['validation_results'] = state['specs'].get('material') == 'Stainless Steel'; return state; def check_pressure(state: SprayerState): state['validation_results'] = state['specs'].get('pressure', 0) < 10.0; return state; graph = StateGraph(SprayerState); graph.add_node('corrosion_check', check_corrosion_resistance); graph.add_node('pressure_check', check_pressure); graph.set_entry_point('corrosion_check'); graph.add_edge('corrosion_check', 'pressure_check'); graph.add_edge('pressure_check', END); graph = graph.compile()