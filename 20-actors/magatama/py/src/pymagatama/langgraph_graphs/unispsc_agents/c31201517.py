from langgraph.graph import StateGraph, END; from typing import TypedDict; class TapeState(TypedDict): vendor: str; adhesive_type: str; compliance_check: bool; def validate_tape_spec(state: TapeState): state['compliance_check'] = state['adhesive_type'] in ['acrylic', 'rubber']; return state; graph = StateGraph(TapeState); graph.add_node('validation', validate_tape_spec); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()