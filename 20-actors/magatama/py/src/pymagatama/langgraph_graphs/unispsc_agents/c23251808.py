from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): mandrel_specs: dict; validation_report: str; def validate_mandrel(state: State): specs = state['mandrel_specs']; is_valid = all(key in specs for key in ['outer_diameter', 'material']); return {'validation_report': 'Valid' if is_valid else 'Failed'}; graph = StateGraph(State); graph.add_node('validate', validate_mandrel); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()