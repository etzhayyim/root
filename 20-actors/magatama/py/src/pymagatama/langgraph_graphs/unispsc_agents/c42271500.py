from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MonitorState(TypedDict): device_id: str; data_logs: List[str]; validation_passed: bool; def validate_sensors(state: MonitorState): state['validation_passed'] = True; return state; def analyze_readings(state: MonitorState): state['data_logs'].append('Readings processed'); return state; graph = StateGraph(MonitorState); graph.add_node('validate', validate_sensors); graph.add_node('analyze', analyze_readings); graph.set_entry_point('validate'); graph.add_edge('validate', 'analyze'); graph.add_edge('analyze', END); graph = graph.compile()