from langgraph.graph import StateGraph, END; from typing import TypedDict; class SyrupState(TypedDict): input_data: dict; analysis: dict; status: str; def validate_spec(state: SyrupState): return {'analysis': {'brix_valid': state['input_data'].get('brix', 0) > 60}, 'status': 'validated'}; def compile_graph(): graph = StateGraph(SyrupState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()