from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToxinState(TypedDict): status: str; certification: bool; validated: bool; def validate_toxin(state: ToxinState): return { 'validated': state['status'] == 'verified' and state['certification'] }; def check_compliance(state: ToxinState): return { 'validated': True } if state['certification'] else { 'validated': False }; graph = StateGraph(ToxinState); graph.add_node('validate', validate_toxin); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()