from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CDProcState(TypedDict): cd_type: str; capacity_check_passed: bool; def validate_capacity(state: CDProcState): return {'capacity_check_passed': state.get('cd_type') == 'CD-ROM'}; graph = StateGraph(CDProcState); graph.add_node('validate', validate_capacity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()