from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): items: List[str]; validated: bool; compliance_status: str; def validate_pharma(state: PharmState): state['validated'] = all(item for item in state['items']); state['compliance_status'] = 'APPROVED' if state['validated'] else 'REJECTED'; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_pharma); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()