from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SpectState(TypedDict): vendor_docs: dict; compliance_status: bool; validation_log: List[str]; def validate_radiation_safety(state: SpectState): state['validation_log'].append('Verifying radiation shielding protocols.'); state['compliance_status'] = True; return state; graph = StateGraph(SpectState); graph.add_node('radiation_check', validate_radiation_safety); graph.set_entry_point('radiation_check'); graph.add_edge('radiation_check', END); graph = graph.compile()