from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): safety_docs: List[str]; compliant: bool; def validate_compliance(state: State): compliance = 'ECE_R129' in state['safety_docs']; return {'compliant': compliance}; graph = StateGraph(State); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()