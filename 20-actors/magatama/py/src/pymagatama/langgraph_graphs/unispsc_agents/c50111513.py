from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BeefState(TypedDict): meat_grade: str; sanitary_cert: bool; temp_logs: List[float]; approved: bool; def validate_quality(state: BeefState) -> BeefState: state['approved'] = state['sanitary_cert'] and all(t < 4.0 for t in state['temp_logs']); return state; graph = StateGraph(BeefState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()