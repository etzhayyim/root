from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): device_id: str; compliance: bool; status: str; def validate_medical_device(state: ProcureState): print(f'Validating skin stapler {state[device_id]}'); return {'compliance': True, 'status': 'Validated'}; def route_workflow(state: ProcureState): return 'complete' if state[compliance] else 'failed'; graph = StateGraph(ProcureState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()