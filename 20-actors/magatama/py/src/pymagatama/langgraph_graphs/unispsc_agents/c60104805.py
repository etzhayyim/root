from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TuningForkState(TypedDict): frequency: float; material: str; verified: bool; def check_frequency(state: TuningForkState): state['verified'] = state['frequency'] > 0; return state; def validate_material(state: TuningForkState): return {'verified': state['material'] == 'Stainless Steel'}; graph = StateGraph(TuningForkState); graph.add_node('check_freq', check_frequency); graph.add_node('check_mat', validate_material); graph.set_entry_point('check_freq'); graph.add_edge('check_freq', 'check_mat'); graph.add_edge('check_mat', END); graph = graph.compile()