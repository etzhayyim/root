from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SupplyChainState(TypedDict): commodity_type: str; purity: float; origin: str; status: list[str]; def validate_purity(state: SupplyChainState): return {'status': ['Purity Verified'] if state['purity'] > 0.95 else ['Purity Rejection']}; def route_logistics(state: SupplyChainState): return 'end' if 'Purity Verified' in state['status'] else 'end'; graph = StateGraph(SupplyChainState); graph.add_node('validate', validate_purity); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()