from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): input_spec: dict; valid: bool; logs: List[str]; def validate_extrusion(state: State): spec = state['input_spec']; is_valid = 'alloy' in spec and 'tolerance' in spec; return {'valid': is_valid, 'logs': ['Validated alloy and tolerance'] if is_valid else ['Invalid spec']}; graph = StateGraph(State); graph.add_node('validate', validate_extrusion); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()