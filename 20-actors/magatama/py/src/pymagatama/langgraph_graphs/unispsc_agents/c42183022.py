from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VisuometerState(TypedDict): device_id: str; certification_valid: bool; calibration_status: str; graph = StateGraph(VisuometerState); def validate_certs(state: VisuometerState): return {'certification_valid': True}; def check_calibration(state: VisuometerState): return {'calibration_status': 'verified'}; graph.add_node('validate', validate_certs); graph.add_node('calibrate', check_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); app = graph.compile()