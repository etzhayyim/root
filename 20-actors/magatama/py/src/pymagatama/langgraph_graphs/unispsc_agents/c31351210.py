from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TitaniumAssemblyState(TypedDict): tube_specs: dict; inspection_status: str; graph = StateGraph(TitaniumAssemblyState); def validate_material(state): state['inspection_status'] = 'verified' if 'ASTM' in state['tube_specs'] else 'failed'; return state; def check_welding(state): state['inspection_status'] = 'passed' if state['inspection_status'] == 'verified' else 'rejected'; return state; graph.add_node('validate', validate_material); graph.add_node('weld_check', check_welding); graph.add_edge('validate', 'weld_check'); graph.add_edge('weld_check', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()