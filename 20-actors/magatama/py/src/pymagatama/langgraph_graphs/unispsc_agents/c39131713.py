from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WiringState(TypedDict): vendor_certs: List[str]; material_spec: str; validation_result: bool; def check_specs(state: WiringState): state['validation_result'] = 'UL94' in state['material_spec'] and len(state['vendor_certs']) > 0; return state; graph = StateGraph(WiringState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()