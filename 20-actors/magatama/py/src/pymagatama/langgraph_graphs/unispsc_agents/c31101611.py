from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CastingState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_composition(state: CastingState): state['valid'] = 'Cu' in state['specs'] and 'Zn' in state['specs']; state['logs'].append('Composition validated'); return state; pipeline = StateGraph(CastingState); pipeline.add_node('validate', validate_composition); pipeline.set_entry_point('validate'); pipeline.add_edge('validate', END); graph = pipeline.compile()