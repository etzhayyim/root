from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DefibCaseState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: DefibCaseState): print('Validating industrial durability standards...'); state['compliance'] = 'IP67' in state['specs'].get('certifications', []); return state; def finalize_approval(state: DefibCaseState): state['approved'] = state['compliance']; return state; graph = StateGraph(DefibCaseState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_approval); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()