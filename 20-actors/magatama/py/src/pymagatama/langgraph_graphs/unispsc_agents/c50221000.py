from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PulseState(TypedDict): vendor_docs: List[str]; validation_results: dict; status: str; def validate_quality(state: PulseState): return {'validation_results': {'passed': True}, 'status': 'READY'}; def check_compliance(state: PulseState): return {'status': 'COMPLIANT'}; graph = StateGraph(PulseState); graph.add_node('validate', validate_quality); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()