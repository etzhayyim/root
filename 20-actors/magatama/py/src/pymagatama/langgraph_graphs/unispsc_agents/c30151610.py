from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_specs: dict; validation_results: List[str]; def validate_specs(state: ToolState): results = []; specs = state.get('tool_specs', {}); if specs.get('material_grade') != 'high-carbon': results.append('Warning: Non-standard material grade.'); return {'validation_results': results}; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()