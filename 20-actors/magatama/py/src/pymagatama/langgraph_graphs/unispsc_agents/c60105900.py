from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EduMaterialState(TypedDict): content_topic: str; age_group: str; compliance_check: bool; material_list: List[str]; def validate_curriculum(state: EduMaterialState): state['compliance_check'] = True; return state; def format_materials(state: EduMaterialState): state['material_list'] = ['Curriculum Guide', 'Parenting Handbook']; return state; builder = StateGraph(EduMaterialState); builder.add_node('validate', validate_curriculum); builder.add_node('format', format_materials); builder.set_entry_point('validate'); builder.add_edge('validate', 'format'); builder.add_edge('format', END); graph = builder.compile()