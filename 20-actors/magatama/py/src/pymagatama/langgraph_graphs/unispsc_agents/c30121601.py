from langgraph.graph import StateGraph, END; from typing import TypedDict; class AsphaltState(TypedDict): specs: dict; valid: bool; def validate_asphalt(state: AsphaltState): required = ['penetration', 'softening_point']; state['valid'] = all(k in state['specs'] for k in required); return state; graph = StateGraph(AsphaltState); graph.add_node('validate', validate_asphalt); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()