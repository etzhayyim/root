from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlashcardState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(FlashcardState); def validate_cards(state: FlashcardState): results = ['Validated content' if 'content' in item else 'Missing content' for item in state['items']]; return {'validation_results': results}; graph.add_node('validate', validate_cards); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()