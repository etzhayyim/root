from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class StickerState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(StickerState); def validate_compliance(state: StickerState): results = []; for item in state['items']: if 'non_toxic' not in item: results.append(f'Error: {item["name"]} missing safety cert.'); return {'validation_results': results}; def log_procurement(state: StickerState): print('Processing Procurement for Incentive Stickers'); return {}; graph.add_node('validate', validate_compliance); graph.add_node('log', log_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.compile()