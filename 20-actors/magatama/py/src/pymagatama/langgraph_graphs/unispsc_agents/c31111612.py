from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RubberState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: RubberState): state['compliance'] = all(k in state['specs'] for k in ['durometer', 'dimensions']); return {'status': 'validated' if state['compliance'] else 'rejected'}; graph = StateGraph(RubberState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()