from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadioState(TypedDict): equipment_id: str; specs: dict; valid: bool; def validate_specs(state: RadioState) -> RadioState: state['valid'] = 'frequency_range_ghz' in state.get('specs', {}) and 'compliance_standards' in state.get('specs', {}); return state; graph = StateGraph(RadioState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()