from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShieldState(TypedDict): specs: dict; valid: bool; audit_log: List[str]; def validate_specs(state: ShieldState): state['valid'] = 'Food Grade Certification' in state['specs'] and state['specs'].get('Heat Resistance') > 200; state['audit_log'].append('Validation completed'); return state; graph = StateGraph(ShieldState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()