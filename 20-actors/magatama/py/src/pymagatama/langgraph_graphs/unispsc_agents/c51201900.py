from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MSState(TypedDict): medicine_name: str; temperature_logs: List[float]; verified: bool; def validate_cold_chain(state: MSState): state['verified'] = all(2.0 <= t <= 8.0 for t in state['temperature_logs']); return state; graph = StateGraph(MSState); graph.add_node('cold_chain_check', validate_cold_chain); graph.set_entry_point('cold_chain_check'); graph.add_edge('cold_chain_check', END); graph = graph.compile()