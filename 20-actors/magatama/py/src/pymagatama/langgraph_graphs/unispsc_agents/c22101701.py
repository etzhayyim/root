from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; ready_for_procurement: bool; def validate_specs(state: RobotState): state['ready_for_procurement'] = 'Torque Rating' in state['specs'] and 'Voltage' in state['specs']; return state; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()