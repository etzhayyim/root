from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VaccineState(TypedDict): cold_chain_logs: List[float]; regulatory_check: bool; final_approval: bool; def validate_temp(state: VaccineState): return {'regulatory_check': all(t < 8.0 for t in state['cold_chain_logs'])}; def finalize_procurement(state: VaccineState): return {'final_approval': state['regulatory_check']}; graph = StateGraph(VaccineState); graph.add_node('validate', validate_temp); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()