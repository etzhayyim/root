from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CleaningToolState(TypedDict): tool_id: str; spec_data: dict; compliance_status: bool; def validate_specs(state: CleaningToolState): is_valid = 'autoclavability' in state['spec_data'] and state['spec_data']['autoclavability'] is True; return {'compliance_status': is_valid}; graph = StateGraph(CleaningToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()