from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: ForgingState): state['validation_errors'] = [e for e in ['Material Grade', 'Conductivity'] if e not in state['specs']]; state['status'] = 'VALID' if not state['validation_errors'] else 'INVALID'; return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()