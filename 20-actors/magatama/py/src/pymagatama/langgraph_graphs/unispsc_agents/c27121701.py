from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HydraulicState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def check_pressure_rating(state: HydraulicState): pressure = state['specs'].get('max_pressure', 0); if pressure < 350: state['validation_errors'].append('Insufficient pressure rating for industrial use'); return state; def validate_compliance(state: HydraulicState): state['approved'] = len(state['validation_errors']) == 0; return state; graph = StateGraph(HydraulicState); graph.add_node('check_pressure', check_pressure_rating); graph.add_node('validate_compliance', validate_compliance); graph.set_entry_point('check_pressure'); graph.add_edge('check_pressure', 'validate_compliance'); graph.add_edge('validate_compliance', END); graph = graph.compile()