from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): device_specs: dict; compliance_checked: bool; def validate_endoscopic_device(state: State): state['compliance_checked'] = 'ISO 13485' in state['device_specs'].get('certifications', []); return state; def finalize_procurement(state: State): return {'status': 'approved' if state['compliance_checked'] else 'rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_endoscopic_device); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()