from langgraph.graph import StateGraph, END; from typing import TypedDict; class TherapyState(TypedDict): specs: dict; approved: bool; def validate_specs(state: TherapyState): state['approved'] = 'Material Toxicity Certification' in state['specs'] and 'Leak Resistance Test' in state['specs']; return state; builder = StateGraph(TherapyState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()