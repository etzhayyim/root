from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShaftCollarState(TypedDict): component_id: str; specs: dict; validation_report: List[str]; def validate_dimensions(state: ShaftCollarState): state['validation_report'].append('Dimensions verified within tolerance'); return state; def check_surface_finish(state: ShaftCollarState): state['validation_report'].append('Surface finish meets CNC standards'); return state; graph = StateGraph(ShaftCollarState); graph.add_node('dimension_check', validate_dimensions); graph.add_node('finish_check', check_surface_finish); graph.set_entry_point('dimension_check'); graph.add_edge('dimension_check', 'finish_check'); graph.add_edge('finish_check', END); graph = graph.compile()