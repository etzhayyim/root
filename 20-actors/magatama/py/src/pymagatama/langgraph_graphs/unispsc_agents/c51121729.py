from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class APIState(TypedDict): active_ingredient: str; purity: float; gmp_compliant: bool; valid: bool; def validate_api(state: APIState): state['valid'] = state['purity'] >= 99.0 and state['gmp_compliant']; return state; builder = StateGraph(APIState); builder.add_node('validate', validate_api); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()