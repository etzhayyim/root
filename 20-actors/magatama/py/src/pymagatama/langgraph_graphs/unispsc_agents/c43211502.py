from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ServerProcurementState(TypedDict): specs: dict; compliance_check: bool; vendor_approved: bool; def validate_specs(state: ServerProcurementState): return {'compliance_check': 'ECCN' in state['specs']}; def verify_vendor(state: ServerProcurementState): return {'vendor_approved': True}; graph = StateGraph(ServerProcurementState); graph.add_node('validate', validate_specs); graph.add_node('vendor', verify_vendor); graph.add_edge('validate', 'vendor'); graph.add_edge('vendor', END); graph.set_entry_point('validate'); graph = graph.compile()