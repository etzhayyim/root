from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FIBCState(TypedDict): spec: dict; is_compliant: bool; def validate_load_capacity(state: FIBCState): state['is_compliant'] = state['spec'].get('SWL', 0) > 0; return state; def check_un_rating(state: FIBCState): return state; graph = StateGraph(FIBCState); graph.add_node('validate_load', validate_load_capacity); graph.add_node('check_un', check_un_rating); graph.set_entry_point('validate_load'); graph.add_edge('validate_load', 'check_un'); graph.add_edge('check_un', END); graph = graph.compile()