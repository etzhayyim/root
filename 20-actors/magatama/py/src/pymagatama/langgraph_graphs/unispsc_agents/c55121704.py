from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetySignSpec(TypedDict): sign_type: str; dimensions: str; compliance: bool; def validate_spec(state: SafetySignSpec): print(f'Validating spec: {state}'); state['compliance'] = True; return state; graph = StateGraph(SafetySignSpec); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()