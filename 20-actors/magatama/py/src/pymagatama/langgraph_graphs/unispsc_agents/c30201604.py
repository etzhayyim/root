from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GarageState(TypedDict): site_survey: str; permit_status: bool; specs: dict; approved: bool; def check_permit(state: GarageState): return {'permit_status': True}; def validate_specs(state: GarageState): return {'approved': len(state['specs']) > 3}; graph = StateGraph(GarageState); graph.add_node('survey', check_permit); graph.add_node('validate', validate_specs); graph.set_entry_point('survey'); graph.add_edge('survey', 'validate'); graph.add_edge('validate', END); graph = graph.compile()