from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TankerState(TypedDict): specs: dict; compliance_status: bool; def validate_specs(state: TankerState): state['compliance_status'] = 'pressure_rating_mpa' in state['specs'] and state['specs']['pressure_rating_mpa'] > 0; return state; graph = StateGraph(TankerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()