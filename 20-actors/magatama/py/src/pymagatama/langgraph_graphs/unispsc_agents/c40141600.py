from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ValveState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: ValveState): if state['specs'].get('pressure_rating'): return {'validation_results': ['Pressure Pass']}; return {'validation_results': ['Pressure Fail']}; graph = StateGraph(ValveState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()