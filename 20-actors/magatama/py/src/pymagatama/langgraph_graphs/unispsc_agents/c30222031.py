from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ViaductState(TypedDict): specs: List[str]; validated: bool; def validate_specs(state: ViaductState): state['validated'] = all(len(s) > 0 for s in state['specs']); return state; def construction_check(state: ViaductState): print('Checking structural integrity specs...'); return state; graph = StateGraph(ViaductState); graph.add_node('validate', validate_specs); graph.add_node('integrity_check', construction_check); graph.add_edge('validate', 'integrity_check'); graph.add_edge('integrity_check', END); graph.set_entry_point('validate'); graph = graph.compile()