from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): vendor_id: str; compliance_docs: List[str]; status: str; def validate_compliance(state: DrugState): state['status'] = 'COMPLIANT' if 'GMP' in state['compliance_docs'] else 'REJECTED'; return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()