from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; validation_passed: bool; issues: List[str]; def validate_purity(state: PharmState): state['validation_passed'] = True; return state; def check_regulatory(state: PharmState): if not state.get('regulatory_approval_certified_copy'): state['validation_passed'] = False; state['issues'].append('Missing regulatory approval document'); return state; graph = StateGraph(PharmState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_regulatory', check_regulatory); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_regulatory'); graph.add_edge('check_regulatory', END); app = graph.compile()