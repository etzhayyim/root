from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoftwareProcurementState(TypedDict): vendor_id: str; compliance_check: bool; license_type: str; def validate_software(state: SoftwareProcurementState): return {'compliance_check': True} if state['license_type'] in ['Enterprise', 'SaaS'] else {'compliance_check': False}; graph = StateGraph(SoftwareProcurementState); graph.add_node('validate', validate_software); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()