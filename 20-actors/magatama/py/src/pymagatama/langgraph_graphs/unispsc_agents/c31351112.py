from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeSpec(TypedDict): specs: dict; compliance_verified: bool; def validate_weld_standards(state: TubeSpec): state['compliance_verified'] = state['specs'].get('uv_weld_certification') == 'ISO-UV-2024'; return state; def check_pressure_capacity(state: TubeSpec): if state['specs'].get('pressure_rating_mpa', 0) > 10: state['specs']['risk_level'] = 'high'; return state; graph = StateGraph(TubeSpec); graph.add_node('validate', validate_weld_standards); graph.add_node('check_pressure', check_pressure_capacity); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_pressure'); graph.add_edge('check_pressure', END); app = graph.compile()