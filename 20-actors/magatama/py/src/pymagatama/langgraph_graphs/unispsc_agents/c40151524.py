from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): pump_specs: dict; validation_results: List[str]; def validate_specs(state: PumpState): specs = state['pump_specs']; results = []; if specs.get('max_pressure_bar', 0) > 100: results.append('High pressure alert'); return {'validation_results': results}; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()