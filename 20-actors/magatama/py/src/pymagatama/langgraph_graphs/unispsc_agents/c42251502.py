from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): data: dict; risks: Annotated[list, operator.add]; status: str; def validate_cert(state: ProcureState): state['data']['validated'] = 'MedicalDeviceCertification' in state['data']; return state; def assess_risk(state: ProcureState): state['risks'].append('regulated-health-product'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_cert); graph.add_node('risk', assess_risk); graph.add_edge('validate', 'risk'); graph.add_edge('risk', END); graph.set_entry_point('validate'); graph = graph.compile()