from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SmokingSetState(TypedDict): items: List[str]; validated: bool; safety_check: bool; def val_step(state: SmokingSetState): return {'validated': True}; def safety_step(state: SmokingSetState): return {'safety_check': True}; graph = StateGraph(SmokingSetState); graph.add_node('validate', val_step); graph.add_node('safety', safety_step); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()