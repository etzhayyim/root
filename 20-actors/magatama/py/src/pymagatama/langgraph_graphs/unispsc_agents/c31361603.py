from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): n_plate: int; weld_integrity_check: bool; material_spec: str; graph = StateGraph(AssemblyState); def validate_material(state: AssemblyState): return {'material_spec': 'Hastelloy X Verified'} if state.get('material_spec') else {'material_spec': 'Pending'}; def check_welds(state: AssemblyState): return {'weld_integrity_check': True}; graph.add_node('validate', validate_material); graph.add_node('inspect', check_welds); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()