from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; msg: str; def validate_materials(state: ProcState): state['validated'] = all(len(item) > 3 for item in state['items']); state['msg'] = 'Validated' if state['validated'] else 'Invalid'; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_materials); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()