from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_name: str; purity: float; is_compliant: bool; def check_purity(state: ProcState): state['is_compliant'] = state['purity'] >= 99.0; return state; def documentation_check(state: ProcState): print(f'Validating GMP for {state['chemical_name']}'); return state; graph = StateGraph(ProcState); graph.add_node('validate_purity', check_purity); graph.add_node('verify_docs', documentation_check); graph.add_edge('validate_purity', 'verify_docs'); graph.add_edge('verify_docs', END); graph.set_entry_point('validate_purity'); graph = graph.compile()