from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FootCareState(TypedDict): items: List[str]; validation_results: dict; def validate_product(state: FootCareState): state['validation_results'] = {item: 'compliant' for item in state['items']}; return state; graph = StateGraph(FootCareState); graph.add_node('validate', validate_product); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()