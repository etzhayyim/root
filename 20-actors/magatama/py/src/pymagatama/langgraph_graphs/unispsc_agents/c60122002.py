from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeavingSpecState(TypedDict): specs: List[str]; validation_results: dict; graph = StateGraph(WeavingSpecState); def validate_parts(state: WeavingSpecState): return {'validation_results': {'status': 'passed', 'details': 'Dimensions within tolerance'}}; def finalize_specs(state: WeavingSpecState): return {'validation_results': {'status': 'finalized'}}; graph.add_node('validate', validate_parts); graph.add_node('finalize', finalize_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()