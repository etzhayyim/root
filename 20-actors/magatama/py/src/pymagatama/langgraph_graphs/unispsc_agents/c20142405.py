from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotProcurementState(TypedDict): vendor_specs: dict; compliance_check: bool; final_report: str; def validate_specs(state: RobotProcurementState): return {'compliance_check': True if 'ISO 10218' in state['vendor_specs'].get('certifications', []) else False}; def finalize_procurement(state: RobotProcurementState): return {'final_report': 'Procurement Approved' if state['compliance_check'] else 'Rejected'}; graph = StateGraph(RobotProcurementState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()