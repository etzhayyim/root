from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecimenLabelState(TypedDict): label_specs: dict; validation_report: List[str]; graph = StateGraph(SpecimenLabelState); def validate_specs(state: SpecimenLabelState): report = []; specs = state.get('label_specs', {}); if 'solvent_resistance' not in specs: report.append('Missing solvent resistance validation'); return {'validation_report': report}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()