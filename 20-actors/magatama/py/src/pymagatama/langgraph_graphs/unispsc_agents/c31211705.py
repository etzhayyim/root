from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ShellacSpecs(TypedDict): purity_level: float; hazard_verified: bool; graph = StateGraph(ShellacSpecs); def validate_purity(state: ShellacSpecs): state['purity_level'] = min(max(state.get('purity_level', 0), 95.0), 100.0); return state; def verify_hazmat(state: ShellacSpecs): state['hazard_verified'] = True; return state; graph.add_node('validate_purity', validate_purity); graph.add_node('verify_hazmat', verify_hazmat); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'verify_hazmat'); graph.add_edge('verify_hazmat', END); graph = graph.compile()