from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec: dict; valid: bool; def validate_specs(state: ProcState): state['valid'] = all(k in state['spec'] for k in ['material', 'tolerance']); return state; graph = StateGraph(ProcState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()