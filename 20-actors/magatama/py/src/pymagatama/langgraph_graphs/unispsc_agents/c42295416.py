from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): suction_flow_rate: float; is_sterile: bool; compliance: List[str]; def validate_specs(state: SurgicalState): return {'compliance': state['compliance'] + ['ISO_VALIDATED']} if state['is_sterile'] else {'compliance': state['compliance'] + ['MANUAL_REVIEW']}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()