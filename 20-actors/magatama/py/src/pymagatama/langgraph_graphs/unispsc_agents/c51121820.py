from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): data: dict; logs: Annotated[list, operator.add]; def validate_purity(state: ProcureState): print('Validating purity levels...'); return {'logs': ['Purity verification complete']}; def gmp_check(state: ProcureState): print('Checking GMP credentials...'); return {'logs': ['GMP verification complete']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_purity); graph.add_node('gmp', gmp_check); graph.add_edge('validate', 'gmp'); graph.add_edge('gmp', END); graph.set_entry_point('validate'); graph = graph.compile()