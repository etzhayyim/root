from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ActuatorState(TypedDict): specs: dict; validation_report: str; graph = StateGraph(ActuatorState); def validate_specs(state: ActuatorState): is_valid = 'torque' in state['specs'] and 'ip_rating' in state['specs']; return {'validation_report': 'Passed' if is_valid else 'Failed'}; def export_control_check(state: ActuatorState): return {'validation_report': state['validation_report'] + ' | Export Control Checked'}; graph.add_node('validation', validate_specs); graph.add_node('export_check', export_control_check); graph.set_entry_point('validation'); graph.add_edge('validation', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()