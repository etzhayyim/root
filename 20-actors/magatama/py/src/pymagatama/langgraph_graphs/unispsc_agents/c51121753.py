from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medicine: str; status: str; quality_checks: List[str]; def validate_api(state: PharmaState): print(f'Validating API: {state[medicine]}'); return {'status': 'validated', 'quality_checks': ['purity_test', 'gmp_verify']}; def log_compliance(state: PharmaState): print(f'Compliance logged for {state[medicine]}'); return {'status': 'compliant'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_api); graph.add_node('compliance', log_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()