from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class TransistorState(TypedDict): component_id: str; quality_metrics: dict; export_control_flag: bool; def validate_specs(state: TransistorState): return {'quality_metrics': {'passed': True, 'score': 95}}; def check_compliance(state: TransistorState): return {'export_control_flag': True}; graph = StateGraph(TransistorState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()