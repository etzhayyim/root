from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; validation_results: List[str]; def validate_adapter(state: State): specs = state.get('specs', {}); results = []; if 'heat_resistance' not in specs: results.append('Missing heat resistance rating'); return {'validation_results': results}; graph = StateGraph(State); graph.add_node('validate', validate_adapter); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()