from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; is_certified: bool; approved: bool; def validate_api(state: PharmState): return {'approved': state['purity'] >= 99.9 and state['is_certified']}; def route_procurement(state: PharmState): return 'approved' if state['approved'] else END; graph = StateGraph(PharmState); graph.add_node('validate', validate_api); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()