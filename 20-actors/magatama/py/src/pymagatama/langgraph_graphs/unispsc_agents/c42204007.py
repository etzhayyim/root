from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShieldingState(TypedDict): panel_specs: dict; compliance_checked: bool; def validate_physics(state: ShieldingState): state['compliance_checked'] = state['panel_specs'].get('lead_eq', 0) >= 2.0; return state; def finalize_order(state: ShieldingState): return {'result': 'approved' if state['compliance_checked'] else 'rejected'}; graph = StateGraph(ShieldingState); graph.add_node('validate', validate_physics); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()