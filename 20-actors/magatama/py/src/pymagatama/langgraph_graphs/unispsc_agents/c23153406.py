from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LaserState(TypedDict): specs: dict; validation_report: list[str]; approved: bool; def validate_specs(state: LaserState): specs = state['specs']; errors = []; if specs.get('output_power', 0) <= 0: errors.append('Invalid power output'); return {'validation_report': errors, 'approved': len(errors) == 0}; graph = StateGraph(LaserState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()