from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RIMState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(RIMState); def validate_mold_design(state: RIMState): state['validation_results'].append('Mold geometry verified'); return state; def check_material_compliance(state: RIMState): state['validation_results'].append('Resin spec compliant'); return state; graph.add_node('design_check', validate_mold_design); graph.add_node('material_check', check_material_compliance); graph.set_entry_point('design_check'); graph.add_edge('design_check', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()