from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class XrayState(TypedDict): specs: dict; compliance: bool; log: List[str]; def validate_radiation_safety(state: XrayState): state['compliance'] = state['specs'].get('radiation_shielding', False); return state; def check_dicom(state: XrayState): state['log'].append('Checking DICOM protocols'); return state; graph = StateGraph(XrayState); graph.add_node('validate_shielding', validate_radiation_safety); graph.add_node('check_dicom', check_dicom); graph.set_entry_point('validate_shielding'); graph.add_edge('validate_shielding', 'check_dicom'); graph.add_edge('check_dicom', END); graph = graph.compile()