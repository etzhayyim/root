from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; def validate_dimensions(state: ProcurementState): return {'validation_results': ['Dimension check passed']}; def verify_material_certs(state: ProcurementState): return {'validation_results': ['Material certification verified']}; graph = StateGraph(ProcurementState); graph.add_node('validate_dim', validate_dimensions); graph.add_node('verify_certs', verify_material_certs); graph.set_entry_point('validate_dim'); graph.add_edge('validate_dim', 'verify_certs'); graph.add_edge('verify_certs', END); app = graph.compile()