from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: FurnitureState): state['validation_results'].append('Size dimensions verified.'); return state; def check_material_safety(state: FurnitureState): state['validation_results'].append('VOC and finish safety checked.'); return state; graph = StateGraph(FurnitureState); graph.add_node('validate_dims', validate_dimensions); graph.add_node('check_safety', check_material_safety); graph.set_entry_point('validate_dims'); graph.add_edge('validate_dims', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()