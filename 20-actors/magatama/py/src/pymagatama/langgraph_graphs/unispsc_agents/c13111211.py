from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CrudeOilState(TypedDict): input_spec: dict; analysis_results: Annotated[Sequence[str], operator.add]; validation_passed: bool; def inspect_quality(state: CrudeOilState): return {'analysis_results': ['Quality check performed on sulfur and density'], 'validation_passed': state['input_spec'].get('sulfur_content', 0) < 0.5}; def check_compliance(state: CrudeOilState): return {'analysis_results': ['Compliance check passed for sanctions and origin']}; graph = StateGraph(CrudeOilState); graph.add_node('inspect', inspect_quality); graph.add_node('compliance', check_compliance); graph.set_entry_point('inspect'); graph.add_edge('inspect', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()