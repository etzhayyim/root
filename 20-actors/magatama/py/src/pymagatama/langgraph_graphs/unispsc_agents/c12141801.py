from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): commodity_code: str; purity: float; status: str; def validate_purity(state: GraphState): return {'status': 'PASSED' if state['purity'] >= 99.999 else 'FAILED'}; def finalize_procurement(state: GraphState): return {'status': 'READY_FOR_ORDER'}; graph = StateGraph(GraphState); graph.add_node('validate', validate_purity); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()