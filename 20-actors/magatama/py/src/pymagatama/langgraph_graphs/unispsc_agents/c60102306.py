from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlannelBoardState(TypedDict): board_specs: dict; validation_results: List[str]; def validate_specs(state: FlannelBoardState) -> FlannelBoardState: results = []; if 'dimensions' not in state['board_specs']: results.append('Missing dimensions'); state['validation_results'] = results; return state; graph = StateGraph(FlannelBoardState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()