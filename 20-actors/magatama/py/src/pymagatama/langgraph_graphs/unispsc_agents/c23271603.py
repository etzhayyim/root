from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SolderingProcessState(TypedDict): specs: dict; is_compliant: bool; validation_log: List[str]; def validate_specs(state: SolderingProcessState): specs = state['specs']; logs = []; compliant = True; if specs.get('temp_range', 0) < 200: logs.append('Insufficient thermal range'); compliant = False; return {'is_compliant': compliant, 'validation_log': logs}; graph = StateGraph(SolderingProcessState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()