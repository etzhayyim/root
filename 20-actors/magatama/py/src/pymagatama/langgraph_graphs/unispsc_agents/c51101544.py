from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CultureMediaState(TypedDict): media_id: str; quality_metrics: dict; approval_status: bool; def validate_purity(state: CultureMediaState): state['quality_metrics']['purity_check'] = 'passed'; return state; def check_gmp_compliance(state: CultureMediaState): state['approval_status'] = True; return state; graph = StateGraph(CultureMediaState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_gmp_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()