from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AmmunitionState(TypedDict): vendor_certs: List[str]; inspection_results: dict; compliance_status: bool; def validate_compliance(state: AmmunitionState): state['compliance_status'] = 'ITAR' in state['vendor_certs']; return state; builder = StateGraph(AmmunitionState); builder.add_node('validate', validate_compliance); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()