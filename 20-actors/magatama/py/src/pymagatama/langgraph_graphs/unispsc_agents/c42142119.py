from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HypothermiaState(TypedDict): device_id: str; specs: dict; compliant: bool; def validate_specs(state: HypothermiaState): state['compliant'] = all(k in state['specs'] for k in ['temp_range', 'safety_certs']); return state; def finalize_approval(state: HypothermiaState): return {'compliant': state['compliant']}; graph = StateGraph(HypothermiaState); graph.add_node('validate', validate_specs); graph.add_node('approve', finalize_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()