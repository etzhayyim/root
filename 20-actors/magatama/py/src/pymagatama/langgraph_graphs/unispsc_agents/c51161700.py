from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RespiratoryDrugState(TypedDict): drug_details: dict; validation_results: List[str]; def validate_drug(state: RespiratoryDrugState): state['validation_results'].append('GMP_Verified'); state['validation_results'].append('ColdChain_Checked'); return state; graph = StateGraph(RespiratoryDrugState); graph.add_node('validate', validate_drug); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()