from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class BathState(TypedDict): specs: dict; validation_report: list; def validate_specs(state: BathState): current = state['specs']; errors = []; if current.get('temp_range', 0) > 100: errors.append('Temperature range exceeds limit'); return {'validation_report': errors}; def finalize_process(state: BathState): print('Compliance Verified'); return state; graph = StateGraph(BathState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_process); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()