from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; approval_status: bool; def validate_spec(state: State): state['approval_status'] = 'material_composition' in state['input_data'] and 'food_grade_certification' in state['input_data']; return state; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()