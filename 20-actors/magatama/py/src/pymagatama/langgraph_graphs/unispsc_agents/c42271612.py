from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_certs: List[str]; clinical_compliance: bool; status: str; def validate_compliance(state: ProcureState): return {'clinical_compliance': all('ISO' in c for c in state['vendor_certs'])}; def finalize_order(state: ProcureState): return {'status': 'READY_FOR_PURCHASE' if state['clinical_compliance'] else 'REJECTED'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_compliance); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()