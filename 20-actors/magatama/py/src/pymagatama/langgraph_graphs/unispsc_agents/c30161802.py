from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CountertopState(TypedDict): dimensions: dict; material: str; validated: bool; def validate_specs(state: CountertopState): state['validated'] = 'length' in state['dimensions'] and 'width' in state['dimensions']; return state; builder = StateGraph(CountertopState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()