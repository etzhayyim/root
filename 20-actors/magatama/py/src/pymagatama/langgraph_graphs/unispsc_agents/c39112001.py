from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LightTowerState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: LightTowerState): errors = []; if 'lumen_output' not in state['specs']: errors.append('Missing lumen output'); return {'validation_errors': errors}; def verify_compliance(state: LightTowerState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(LightTowerState); graph.add_node('validate', validate_specs); graph.add_node('compliance', verify_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()