from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TracheostomyState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_medical_specs(state: TracheostomyState): state['valid'] = 'ISO_13485_certification' in state['specs'] and state['specs'].get('sterilization_method') is not None; return state; graph = StateGraph(TracheostomyState); graph.add_node('validate', validate_medical_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()