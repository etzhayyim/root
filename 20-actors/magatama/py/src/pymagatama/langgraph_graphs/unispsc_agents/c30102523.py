from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): specs: dict; validation_errors: List[str]; def validate_specs(state: SpecState): errors = []; if state['specs'].get('asbestos_content') != 'none': errors.append('Asbestos prohibited'); return {'validation_errors': errors}; graph = StateGraph(SpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()