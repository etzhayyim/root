from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_data: dict; validation_logs: Annotated[list, operator.add]; graph = StateGraph(ProcState); def validate_purity(state: ProcState): purity = state['chemical_data'].get('purity', 0); return {'validation_logs': ['Purity Validated'] if purity >= 98 else ['Purity Warning']}; def safety_check(state: ProcState): return {'validation_logs': ['Safety protocols verified']}; graph.add_node('validate', validate_purity); graph.add_node('safety', safety_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); app = graph.compile()