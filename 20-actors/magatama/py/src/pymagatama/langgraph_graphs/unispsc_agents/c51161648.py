from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class APIState(TypedDict): supplier_name: str; purity: float; compliance_docs: List[str]; approved: bool; def validate_purity(state: APIState): state['approved'] = state['purity'] >= 99.0; return state; def verify_docs(state: APIState): if len(state['compliance_docs']) >= 3: return state; else: state['approved'] = False; return state; graph = StateGraph(APIState); graph.add_node('validate', validate_purity); graph.add_node('verify', verify_docs); graph.set_entry_point('validate'); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); graph = graph.compile()