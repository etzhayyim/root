from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): items: List[dict]; validation_results: List[str]; class Validator: @staticmethod def check_safety(state: FurnitureState): results = ["Passed ASTM testing" for item in state['items'] if "cert" in item]; return {"validation_results": results}; graph = StateGraph(FurnitureState); graph.add_node("validate", Validator.check_safety); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()