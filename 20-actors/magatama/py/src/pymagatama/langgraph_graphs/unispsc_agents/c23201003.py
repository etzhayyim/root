from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EngineState(TypedDict): engine_specs: dict; validation_results: List[str]; def validate_specs(state: EngineState): results = []; if 'torque' not in state['engine_specs']: results.append('Missing torque spec'); return {'validation_results': results}; def compile_graph(): graph = StateGraph(EngineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()