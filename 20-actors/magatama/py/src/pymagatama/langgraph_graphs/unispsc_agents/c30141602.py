from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SleevingState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: SleevingState): required = ['Material', 'Diameter']; missing = [f for f in required if f not in state['specs']]; state['validation_passed'] = len(missing) == 0; return state; graph = StateGraph(SleevingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()