from langgraph.graph import StateGraph, END; from typing import TypedDict; class GrillState(TypedDict): specs: dict; valid: bool; def validate_grill_specs(state: GrillState): state['valid'] = 'BTU' in state['specs'] and 'material' in state['specs']; return state; builder = StateGraph(GrillState); builder.add_node('validate', validate_grill_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()