from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): device_specs: dict; compliance_passed: bool; def validate_specs(state: SpecState): state['compliance_passed'] = all(k in state['device_specs'] for k in ['sterility', 'iso_code']); return state; def check_regulatory(state: SpecState): return {'compliance_passed': state['device_specs'].get('regulatory_approval') is not None}; graph = StateGraph(SpecState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()