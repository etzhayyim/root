from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClimateSystemState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ClimateSystemState): errors = []; if state['specs'].get('cooling_capacity', 0) <= 0: errors.append('Invalid cooling capacity'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(ClimateSystemState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()