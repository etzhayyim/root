from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): component_data: dict; validation_report: list; def validate_specs(state: RobotState): report = []; if state['component_data'].get('tolerance') == 'standard': report.append('Verified tolerance'); return {'validation_report': report}; def check_export_compliance(state: RobotState): return {'validation_report': state['validation_report'] + ['Export control check passed']}; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.add_node('export', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()