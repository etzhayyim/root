from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; analysis_result: str; def validate_spec(state: State): spec = state['input_data']; brix = spec.get('brix', 0); status = 'Approved' if 50 <= brix <= 70 else 'Rejected'; return {'analysis_result': status}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()