from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class HookState(TypedDict): hook_id: str; wll: float; certified: bool; graph = StateGraph(HookState); def validate_spec(state: HookState): return {'certified': state['wll'] > 0}; def check_safety(state: HookState): print(f'Processing hook {state["hook_id"]}'); return state; graph.add_node('validate', validate_spec); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()