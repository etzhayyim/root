from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoneDensitometerState(TypedDict): device_id: str; compliance_docs: List[str]; inspection_passed: bool; def validate_cal(state): state['inspection_passed'] = 'ISO13485' in state['compliance_docs']; return state; def check_regulatory(state): return {'status': 'Approved' if state['inspection_passed'] else 'Rejected'}; graph = StateGraph(BoneDensitometerState); graph.add_node('validate', validate_cal); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()