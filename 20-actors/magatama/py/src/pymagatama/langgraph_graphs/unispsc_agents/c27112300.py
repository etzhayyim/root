from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): marking_data: str; validation_status: str; def validate_tool_specs(state: AgentState): status = 'Valid' if state['marking_data'] else 'Invalid'; return {'validation_status': status}; def finalize_order(state: AgentState): return {'status': 'Complete'}; graph = StateGraph(AgentState); graph.add_node('validate', validate_tool_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()