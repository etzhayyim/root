from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PanelState(TypedDict): panel_specs: dict; validation_results: List[str]; def validate_material(state: PanelState): return {'validation_results': ['Material check passed']}; def validate_dimensions(state: PanelState): return {'validation_results': ['Dimensions within tolerance']}; graph = StateGraph(PanelState); graph.add_node('material_check', validate_material); graph.add_node('dimension_check', validate_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dimension_check'); graph.add_edge('dimension_check', END); graph = graph.compile()