from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GolfEntry(TypedDict): items: List[str]; validation_status: bool; def validate_specs(state: GolfEntry): state['validation_status'] = all(len(item) > 0 for item in state['items']); return state; graph = StateGraph(GolfEntry); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()