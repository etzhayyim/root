from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class PlasticState(TypedDict): material_type: str; thickness: float; quantity: int; valid: bool; log: Annotated[Sequence[str], operator.add]; def validate_material(state: PlasticState): is_valid = state['thickness'] > 0 and state['quantity'] > 0; return {'valid': is_valid, 'log': ['Material spec validated']}; def process_order(state: PlasticState): return {'log': ['Order processed for shipment']}; graph = StateGraph(PlasticState); graph.add_node('validate', validate_material); graph.add_node('process', process_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()