from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class EndorserState(TypedDict): doc_specs: dict; validation_report: str; def validate_specs(state: EndorserState): specs = state['doc_specs']; if specs.get('dpi', 0) < 300: return {'validation_report': 'FAIL: DPI insufficient'}; return {'validation_report': 'PASS: DPI sufficient'}; def hardware_init(state: EndorserState): return {'validation_report': 'Hardware Initialized'}; graph = StateGraph(EndorserState); graph.add_node('validate', validate_specs); graph.add_node('init', hardware_init); graph.set_entry_point('init'); graph.add_edge('init', 'validate'); graph.add_edge('validate', END); graph = graph.compile()