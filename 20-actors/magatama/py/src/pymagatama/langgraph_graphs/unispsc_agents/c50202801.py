from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FoodState(TypedDict): input_data: dict; quality_check: bool; status: str; def validate_specs(state: FoodState): return {'quality_check': all(k in state['input_data'] for k in ['Brix', 'pH', 'expiry']), 'status': 'validated'}; def approve_procurement(state: FoodState): return {'status': 'approved' if state['quality_check'] else 'rejected'}; graph = StateGraph(FoodState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()