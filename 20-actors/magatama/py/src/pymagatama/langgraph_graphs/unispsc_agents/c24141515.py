from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NettingState(TypedDict): specs: dict; validation_report: List[str]; def validate_specs(state: NettingState): issues = []; if state['specs'].get('tensile_strength', 0) < 500: issues.append('Insufficient strength'); state['validation_report'] = issues; return state; graph = StateGraph(NettingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()