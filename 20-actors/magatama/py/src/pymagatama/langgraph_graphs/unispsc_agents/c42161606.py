from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DialysateState(TypedDict): data: dict; verification_log: Annotated[list, operator.add]; graph = StateGraph(DialysateState); def validate_spec(state: DialysateState): return {'verification_log': ['Spec valid']}; def check_regulatory(state: DialysateState): return {'verification_log': ['Regulatory compliant']}; graph.add_node('validate_spec', validate_spec); graph.add_node('check_regulatory', check_regulatory); graph.set_entry_point('validate_spec'); graph.add_edge('validate_spec', 'check_regulatory'); graph.add_edge('check_regulatory', END); compiled_graph = graph.compile()