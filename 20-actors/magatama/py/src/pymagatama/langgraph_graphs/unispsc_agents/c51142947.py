from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): content: str; purity: float; compliant: bool; def validate_purity(state: State): return {'compliant': state['purity'] >= 99.0}; def check_regulations(state: State): return {'compliant': True}; graph = StateGraph(State); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()