from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; validation: bool; def validate_specs(state: State): specs = state['specs']; state['validation'] = all(k in specs for k in ['material', 'bore_diameter', 'valve_type']); return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()