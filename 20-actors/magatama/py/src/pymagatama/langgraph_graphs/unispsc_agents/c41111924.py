from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): sensor_type: str; specs_verified: bool; def check_specs(state: ProcState): state['specs_verified'] = True; return state; graph = StateGraph(ProcState); graph.add_node('verify', check_specs); graph.set_entry_point('verify'); graph.add_edge('verify', END); compiled_graph = graph.compile()