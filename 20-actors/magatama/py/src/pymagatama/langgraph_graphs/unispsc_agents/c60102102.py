from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BookProcurementState(TypedDict): isbn: str; category: str; validation_passed: bool; graph = StateGraph(BookProcurementState); def validate_metadata(state: BookProcurementState): return {'validation_passed': len(state['isbn']) == 13}; def check_inventory(state: BookProcurementState): return {'validation_passed': True}; graph.add_node('validate', validate_metadata); graph.add_node('inventory', check_inventory); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph.set_entry_point('validate'); app = graph.compile()