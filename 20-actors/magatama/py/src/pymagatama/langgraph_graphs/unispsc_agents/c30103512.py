from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: State): state['compliance'] = 'AMS_Grade' in state['specs'] and state['specs']['cell_size'] > 0; state['status'] = 'Validated' if state['compliance'] else 'Failed'; return state; def export_check(state: State): state['status'] = 'Export Review Required' if state['compliance'] else 'Rejected'; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('export', export_check); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()