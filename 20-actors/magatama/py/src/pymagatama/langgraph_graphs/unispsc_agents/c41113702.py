from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComparatorState(TypedDict): input_spec: dict; validation_results: List[str]; approved: bool; def validate_specs(state: ComparatorState): res = []; spec = state['input_spec']; if spec.get('resolution') < 0.001: res.append('High precision validation required'); return {'validation_results': res}; def finalize(state: ComparatorState): return {'approved': len(state['validation_results']) == 0}; graph = StateGraph(ComparatorState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()