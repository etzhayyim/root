from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): spec: dict; is_compliant: bool; def validate_spec(state: State): state['is_compliant'] = state['spec'].get('weight_capacity', 0) >= 100; return state; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph_app = graph.compile()