from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaperState(TypedDict): items: List[str]; validated_items: List[str]; def validate_paper(state: PaperState): return {'validated_items': [item for item in state['items'] if 'foil' in item.lower()]}; graph = StateGraph(PaperState); graph.add_node('validate', validate_paper); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()