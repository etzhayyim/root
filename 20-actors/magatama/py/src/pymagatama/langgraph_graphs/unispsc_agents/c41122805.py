from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RackState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def val_material(state: RackState): state['validation_errors'].append('Material missing') if 'material' not in state['specs'] else None; return state; def check_load(state: RackState): state['approved'] = state['specs'].get('load_capacity', 0) > 0; return state; graph = StateGraph(RackState); graph.add_node('validate_material', val_material); graph.add_node('check_capacity', check_load); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_capacity'); graph.add_edge('check_capacity', END); graph = graph.compile()