from langgraph.graph import StateGraph, END; from typing import TypedDict; class ReacherState(TypedDict): device_specs: dict; compliance: bool; approved: bool; def validate_specs(state: ReacherState): state['compliance'] = state['device_specs'].get('load_kg', 0) > 0; return state; def check_compliance(state: ReacherState): return 'approved' if state['compliance'] else 'review'; graph = StateGraph(ReacherState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()