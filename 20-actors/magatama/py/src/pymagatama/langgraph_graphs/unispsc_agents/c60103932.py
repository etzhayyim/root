from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EcosystemState(TypedDict): display_specs: dict; validation_status: bool; def val_step(state: EcosystemState): state['validation_status'] = 'Resolution' in state['display_specs']; return state; def proc_step(state: EcosystemState): return {'validation_status': True}; graph = StateGraph(EcosystemState); graph.add_node('val', val_step); graph.add_node('proc', proc_step); graph.add_edge('val', 'proc'); graph.add_edge('proc', END); graph.set_entry_point('val'); app = graph.compile()