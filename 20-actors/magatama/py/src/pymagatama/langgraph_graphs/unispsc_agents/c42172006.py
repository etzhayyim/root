from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ObstetricKitState(TypedDict): kit_id: str; contents: List[str]; sterile: bool; workflow_step: str; def validate_contents(state: ObstetricKitState): state['workflow_step'] = 'VALIDATING_CONTENTS'; return {'workflow_step': 'CONTENTS_CHECKED'}; def check_sterile(state: ObstetricKitState): state['sterile'] = True; return {'workflow_step': 'STERILITY_VERIFIED'}; graph = StateGraph(ObstetricKitState); graph.add_node('validate', validate_contents); graph.add_node('sterility', check_sterile); graph.set_entry_point('validate'); graph.add_edge('validate', 'sterility'); graph.add_edge('sterility', END); compiled_graph = graph.compile()