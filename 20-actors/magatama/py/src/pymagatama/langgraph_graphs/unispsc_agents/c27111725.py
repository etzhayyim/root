from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): input_specs: dict; validated: bool; log: Annotated[list, operator.add]; def validate_wrench(state: ToolState): spec = state['input_specs']; is_valid = 'material' in spec and 'drive_size' in spec; return {'validated': is_valid, 'log': [f'Validation result: {is_valid}']}; graph = StateGraph(ToolState); graph.add_node('validate', validate_wrench); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()