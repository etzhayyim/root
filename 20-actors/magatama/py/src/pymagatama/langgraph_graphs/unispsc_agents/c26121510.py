from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrolleyWireState(TypedDict): specs: dict; compliance_status: bool; inspection_logs: List[str]; def validate_specs(state: TrolleyWireState): state['compliance_status'] = state['specs'].get('tensile_strength', 0) > 400; return state; graph = StateGraph(TrolleyWireState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()