from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalTrayState(TypedDict): tray_id: str; specs: dict; compliance_check: bool; def validate_specs(state: SurgicalTrayState): return {'compliance_check': all(k in state['specs'] for k in ['material', 'sterilization'])}; graph = StateGraph(SurgicalTrayState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()