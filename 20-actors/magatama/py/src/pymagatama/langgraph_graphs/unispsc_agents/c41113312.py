from langgraph.graph import StateGraph, END; from typing import TypedDict; class AnalyzerState(TypedDict): data: dict; valid: bool; def validate_specs(state: AnalyzerState): state['valid'] = all(k in state['data'] for k in ['accuracy', 'range']); return state; def route_step(state: AnalyzerState): return 'process' if state['valid'] else END; graph = StateGraph(AnalyzerState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()