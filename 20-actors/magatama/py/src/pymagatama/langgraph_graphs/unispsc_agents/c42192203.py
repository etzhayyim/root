from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChairProcureState(TypedDict): specs: dict; compliance_check: bool; final_report: str; def validate_specs(state: ChairProcureState): return {'compliance_check': 'Weight Capacity' in state['specs']}; def finalize_order(state: ChairProcureState): return {'final_report': 'Validated geriatric equipment procurement.'}; graph = StateGraph(ChairProcureState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()