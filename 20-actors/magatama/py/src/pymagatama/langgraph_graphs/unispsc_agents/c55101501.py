from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MapState(TypedDict): data: dict; validation_errors: List[str]; def validate_map_data(state: MapState): import datetime; d = state['data']; errors = []; target_date = d.get('pub_date', '2000-01-01'); if datetime.datetime.strptime(target_date, '%Y-%m-%d') < datetime.datetime(2020, 1, 1): errors.append('Map data is obsolete'); return {'validation_errors': errors}; graph = StateGraph(MapState); graph.add_node('validate', validate_map_data); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()