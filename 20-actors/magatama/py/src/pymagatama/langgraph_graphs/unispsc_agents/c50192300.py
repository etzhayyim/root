from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OrderState(TypedDict): items: List[str]; inspection_passed: bool; temp_log: dict; def validate_cold_chain(state: OrderState): state['inspection_passed'] = state['temp_log'].get('avg') <= 4.0; return state; graph = StateGraph(OrderState); graph.add_node('validate', validate_cold_chain); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()