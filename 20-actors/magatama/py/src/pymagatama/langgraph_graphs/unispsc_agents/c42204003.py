from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): shielding_thickness: float; test_passed: bool; def validate_shielding(state: ProcState): return {'test_passed': state['shielding_thickness'] > 50.0}; def compliance_check(state: ProcState): return 'COMPLIANT' if state['test_passed'] else 'REJECTED'; graph = StateGraph(ProcState); graph.add_node('validate', validate_shielding); graph.add_node('check', compliance_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()