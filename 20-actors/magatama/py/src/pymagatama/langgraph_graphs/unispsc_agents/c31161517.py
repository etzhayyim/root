from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerSpec(TypedDict): specs: dict; errors: List[str]; def validate_specs(state: FastenerSpec): ...; def check_material(state: FastenerSpec): ...; graph = StateGraph(FastenerSpec); graph.add_node('validate', validate_specs); graph.add_node('check_material', check_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_material'); graph.add_edge('check_material', END); graph.set_finish_point('check_material'); graph = graph.compile()