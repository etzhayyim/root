from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): capacity: float; compliance: bool; status: str; def validate_specs(state: State): state['status'] = 'COMPLIANT' if state['capacity'] > 0 and state['compliance'] else 'REJECTED'; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()