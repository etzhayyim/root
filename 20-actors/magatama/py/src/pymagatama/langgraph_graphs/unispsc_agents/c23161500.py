from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineSpec(TypedDict): specs: List[str]; validation_passed: bool; def validate_specs(state: MachineSpec): state['validation_passed'] = len(state['specs']) > 0; return state; graph = StateGraph(MachineSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()