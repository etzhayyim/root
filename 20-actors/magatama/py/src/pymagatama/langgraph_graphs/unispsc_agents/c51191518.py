from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine: str; compliance_checks: List[str]; approved: bool; def validate_quality(state: PharmState): state['compliance_checks'].append('FDA/EMA Grade Verified'); state['approved'] = True; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()