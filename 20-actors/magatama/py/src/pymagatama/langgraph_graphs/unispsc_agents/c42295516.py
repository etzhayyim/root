from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UroImplantsState(TypedDict): items: List[str]; validation_results: dict; def validate_cert(state: UroImplantsState): return {'validation_results': {'iso_check': 'passed'}}; def check_regulatory(state: UroImplantsState): return {'validation_results': {'reg_check': 'compliant'}}; graph = StateGraph(UroImplantsState); graph.add_node('cert', validate_cert); graph.add_node('reg', check_regulatory); graph.set_entry_point('cert'); graph.add_edge('cert', 'reg'); graph.add_edge('reg', END); compiled_graph = graph.compile()