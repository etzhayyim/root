from langgraph.graph import StateGraph, END; from typing import TypedDict; class LEDState(TypedDict): specs: dict; valid: bool; def validate_specs(state: LEDState): state['valid'] = 'Forward Voltage' in state['specs'] and 'Peak Wavelength' in state['specs']; return state; builder = StateGraph(LEDState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()