from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScooterState(TypedDict): scooter_id: str; compliance_docs: List[str]; approved: bool; def validate_docs(state: ScooterState): state['approved'] = len(state['compliance_docs']) >= 3; return state; graph = StateGraph(ScooterState); graph.add_node('validate', validate_docs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()