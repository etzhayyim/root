from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TesterState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(TesterState); def validate_specs(state: TesterState): return {'validation_results': ['ISO_Compliance_Check_Passed']}; def perform_calibration(state: TesterState): return {'validation_results': ['Calibration_Data_Verified']}; graph.add_node('validate', validate_specs); graph.add_node('calibrate', perform_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); app = graph.compile()