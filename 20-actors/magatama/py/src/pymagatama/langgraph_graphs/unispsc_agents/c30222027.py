from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class BridgeProjectState(TypedDict): tasks: Annotated[Sequence[str], operator.add]; structural_integrity: bool; compliance_status: bool; def validate_structural_data(state: BridgeProjectState): return {'structural_integrity': True}; def check_compliance(state: BridgeProjectState): return {'compliance_status': True}; graph = StateGraph(BridgeProjectState); graph.add_node('validate', validate_structural_data); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()