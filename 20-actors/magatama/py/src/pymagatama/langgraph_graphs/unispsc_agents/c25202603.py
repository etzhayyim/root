from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AircraftTurbineState(TypedDict): part_id: str; compliance_docs: List[str]; inspection_status: str; def validate_part(state: AircraftTurbineState): return {'inspection_status': 'verified' if state['part_id'].startswith('TURB-') else 'rejected'}; def check_compliance(state: AircraftTurbineState): return {'compliance_docs': ['FAA_FORM_8130', 'CofC']}; graph = StateGraph(AircraftTurbineState); graph.add_node('validate', validate_part); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()