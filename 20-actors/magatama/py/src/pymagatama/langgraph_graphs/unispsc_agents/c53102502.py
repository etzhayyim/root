from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; next_step: str; def validate_spec(state: ProcState): return { 'validated': True, 'next_step': 'approval' }; def approval(state: ProcState): return { 'next_step': 'complete' }; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.add_node('approve', approval); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()