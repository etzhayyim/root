from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): data: dict; valid: bool; def validate_sensor_data(state: State): state['valid'] = 'accuracy' in state['data'] and state['data']['accuracy'] > 0.95; return state; def check_compliance(state: State): return 'valid' if state['valid'] else END; graph = StateGraph(State); graph.add_node('validate', validate_sensor_data); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()