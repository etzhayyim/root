from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaintState(TypedDict): ingredients: List[str]; safety_pass: bool; def validate_ingredients(state: PaintState): state['safety_pass'] = all('toxic' not in i.lower() for i in state['ingredients']); return state; def final_decision(state: PaintState): return 'APPROVED' if state['safety_pass'] else 'REJECTED'; graph = StateGraph(PaintState); graph.add_node('validate', validate_ingredients); graph.add_node('decision', final_decision); graph.set_entry_point('validate'); graph.add_edge('validate', 'decision'); graph.add_edge('decision', END); app = graph.compile()