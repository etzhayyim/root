from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CommodityState(TypedDict): input_material: str; purity_check: bool; batch_id: str; history: Annotated[Sequence[str], operator.add]; def validate_purity(state: CommodityState) -> CommodityState: state['purity_check'] = True; return state; def finalize_procurement(state: CommodityState) -> CommodityState: return state; graph = StateGraph(CommodityState); graph.add_node('validate', validate_purity); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()