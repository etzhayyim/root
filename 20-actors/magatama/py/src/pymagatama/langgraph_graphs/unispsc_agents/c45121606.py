from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CameraHarnessState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_harness_specs(state: CameraHarnessState): specs = state['specs']; errors = []; if specs.get('max_load', 0) <= 0: errors.append('Invalid load capacity'); return {'valid': len(errors) == 0, 'error_log': errors}; graph = StateGraph(CameraHarnessState); graph.add_node('validator', validate_harness_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); compiled_graph = graph.compile()