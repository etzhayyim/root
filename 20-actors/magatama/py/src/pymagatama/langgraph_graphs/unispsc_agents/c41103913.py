from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CentrifugeProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: CentrifugeProcurementState): rpm = state['specs'].get('rpm', 0); state['approved'] = rpm > 0; return state; graph = StateGraph(CentrifugeProcurementState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()