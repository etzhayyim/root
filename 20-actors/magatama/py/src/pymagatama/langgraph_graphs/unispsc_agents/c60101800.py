from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): resource_type: str; is_approved: bool; specs: List[str]; def validate_content(state: ProcureState): state['is_approved'] = 'religious_alignment' in state['specs']; return state; def check_standards(state: ProcureState): return 'approved' if state['is_approved'] else 'rejected'; graph = StateGraph(ProcureState); graph.add_node('validate', validate_content); graph.add_edge('validate', END); graph.set_entry_point('validate'); app = graph.compile()