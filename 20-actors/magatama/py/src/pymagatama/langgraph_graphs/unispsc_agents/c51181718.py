from langgraph.graph import StateGraph, END; from typing import TypedDict; class DrugState(TypedDict): medicine: str; status: str; safety_check: bool; def validate_drug(state: DrugState): return {'safety_check': state['medicine'] == 'Deflazacort' and True}; def route_procurement(state: DrugState): return 'approval' if state.get('safety_check') else END; graph = StateGraph(DrugState); graph.add_node('validate', validate_drug); graph.add_node('approval', lambda x: {'status': 'Approved'}); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph = graph.compile()