from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): data: dict; logs: Annotated[list, operator.add]; def validate_specs(state: ComponentState): return {'logs': ['Validating ASTM compliance', 'Checking dimensional tolerances']}; def export_check(state: ComponentState): return {'logs': ['Checking ITAR/EAR compliance for titanium goods']}; graph = StateGraph(ComponentState); graph.add_node('validate', validate_specs); graph.add_node('export_review', export_check); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph.set_entry_point('validate'); graph = graph.compile()