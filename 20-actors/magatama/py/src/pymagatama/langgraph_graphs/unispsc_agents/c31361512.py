from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; valid: bool; workflow_log: List[str]; def validate_assembly(state: ProcureState): specs = state.get('specs', {}); is_valid = all(k in specs for k in ['material_purity_percent', 'leak_test_standard']); return {'valid': is_valid, 'workflow_log': ['Validation complete']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_assembly); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()