from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class YeastMediaState(TypedDict): media_type: str; quality_passed: bool; specs: dict; def validate_media_specs(state: YeastMediaState): return {'quality_passed': state['specs'].get('sterility', False)}; def finalize(state: YeastMediaState): print('Processing release...'); return state; graph = StateGraph(YeastMediaState); graph.add_node('validate', validate_media_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()