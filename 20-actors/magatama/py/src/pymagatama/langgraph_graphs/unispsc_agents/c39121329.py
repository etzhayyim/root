from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FuseCabinetState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: FuseCabinetState): specs = state['specs']; errors = []; if specs.get('voltage', 0) < 0: errors.append('Invalid Voltage'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(FuseCabinetState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()