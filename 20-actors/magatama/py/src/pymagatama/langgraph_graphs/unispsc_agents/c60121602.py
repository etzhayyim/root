from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AcrylicState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: AcrylicState): errors = []; if state['specs'].get('thickness', 0) <= 0: errors.append('Invalid thickness'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(AcrylicState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()