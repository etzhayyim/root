from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InoculationState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: InoculationState): return {'validation_report': 'Passed' if 'sterility_certification' in state['specs'] else 'Failed'}; def finalize(state: InoculationState): return {'approved': state['validation_report'] == 'Passed'}; graph = StateGraph(InoculationState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate');