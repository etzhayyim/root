from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input_data: dict; validation_errors: List[str]; approved: bool; def validate_geometry(state: ProcState): state['validation_errors'] = [] if 'tolerance' not in state['input_data'] else []; return state; def check_export_control(state: ProcState): state['approved'] = True; return state; graph = StateGraph(ProcState); graph.add_node('geometry_check', validate_geometry); graph.add_node('export_compliance', check_export_control); graph.set_entry_point('geometry_check'); graph.add_edge('geometry_check', 'export_compliance'); graph.add_edge('export_compliance', END);