from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): metal_grade: str; dimensions: dict; verified: bool; def validate_spec(state: ProcurementState): state['verified'] = 'purity_percentage' in state and state['dimensions'] is not None; return {'verified': state['verified']}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()