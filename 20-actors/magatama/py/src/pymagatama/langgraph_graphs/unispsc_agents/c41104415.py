from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IncubationState(TypedDict): specs: dict; validation: bool; def check_precision(state: IncubationState): state['validation'] = state['specs']['temp_var'] < 0.1; return state; graph = StateGraph(IncubationState); graph.add_node('check_precision', check_precision); graph.set_entry_point('check_precision'); graph.add_edge('check_precision', END); graph = graph.compile()