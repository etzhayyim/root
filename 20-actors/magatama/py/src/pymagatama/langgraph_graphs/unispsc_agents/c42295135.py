from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): data: dict; logs: Annotated[list[str], operator.add]; def validate_medical_cert(state: ProcState): state['logs'].append('Verifying ISO 13485...'); return state; def check_regulatory_compliance(state: ProcState): state['logs'].append('Checking FDA/PMDA registration...'); return state; graph = StateGraph(ProcState); graph.add_node('cert', validate_medical_cert); graph.add_node('reg', check_regulatory_compliance); graph.add_edge('cert', 'reg'); graph.add_edge('reg', END); graph.set_entry_point('cert'); graph = graph.compile()