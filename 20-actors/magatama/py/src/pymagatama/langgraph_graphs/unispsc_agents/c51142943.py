from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): chemical_id: str; purity_check: bool; regulatory_approval: bool; validated_specs: List[str]; def validate_purity(state: DrugProcurementState): state['purity_check'] = True; return state; def check_regulations(state: DrugProcurementState): state['regulatory_approval'] = True; return state; graph = StateGraph(DrugProcurementState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_regulations', check_regulations); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_regulations'); graph.add_edge('check_regulations', END); graph = graph.compile()