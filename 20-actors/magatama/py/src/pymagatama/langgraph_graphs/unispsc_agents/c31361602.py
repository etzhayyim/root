from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): component_id: str; weld_specs: dict; validated: bool; def validate_weld(state: AssemblyState): print(f'Validating sonic weld for {state[component_id]}'); return {'validated': True}; def finalize_assembly(state: AssemblyState): return {'validated': True}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_weld); graph.add_node('finalize', finalize_assembly); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()