from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; specs: dict; validation_results: list[str]; graph = StateGraph(ComponentState); def validate_lead_purity(state: ComponentState): purity = state['specs'].get('lead_purity_percentage', 0); return {'validation_results': ['Purity OK' if purity >= 99.9 else 'Warning: Low Purity']}; def check_safety(state: ComponentState): return {'validation_results': state['validation_results'] + ['SDS_Verified']}; graph.add_node('validate_purity', validate_lead_purity); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_safety'); graph.add_edge('check_safety', END); app = graph.compile()