from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): items: List[str]; validation_status: bool; inventory_check: bool; def validate_drugs(state: PharmState): state['validation_status'] = all(['GMP' in item for item in state['items']]); return state; def check_stock(state: PharmState): state['inventory_check'] = True; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_drugs); graph.add_node('inventory', check_stock); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph.set_entry_point('validate'); graph = graph.compile()