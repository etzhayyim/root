from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_copper_assembly(state: PipeState): state['valid'] = all(k in state['specs'] for k in ['grade', 'pressure']); return state; def report_status(state: PipeState): state['log'].append('Validation completed'); return state; graph = StateGraph(PipeState); graph.add_node('validate', validate_copper_assembly); graph.add_node('report', report_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()