from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; from operator import add; class MagState(TypedDict): metal_grade: str; purity_check: bool; logistics_log: List[str]; def validate_purity(state: MagState): return {'purity_check': True if state.get('metal_grade') == '99.9' else False}; def route_logistics(state: MagState): return {'logistics_log': ['Hazardous storage initialized', 'Export controls verified']}; graph = StateGraph(MagState); graph.add_node('validate', validate_purity); graph.add_node('logistics', route_logistics); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()