from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SanitizerState(TypedDict): vendor_id: str; specs: dict; compliance_status: bool; def validate_specs(state: SanitizerState): return {'compliance_status': 'temp_range' in state['specs']}; def approve_procurement(state: SanitizerState): return {'compliance_status': True}; graph = StateGraph(SanitizerState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()