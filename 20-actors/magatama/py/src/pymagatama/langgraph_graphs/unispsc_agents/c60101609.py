from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CertState(TypedDict): content: str; validated: bool; cert_type: str; def validate_cert(state: CertState): state['validated'] = len(state['content']) > 0; return state; def check_compliance(state: CertState): return 'end' if state['validated'] else 'error'; graph = StateGraph(CertState); graph.add_node('validate', validate_cert); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()