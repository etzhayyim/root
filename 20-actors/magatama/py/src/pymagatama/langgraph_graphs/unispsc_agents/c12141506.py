from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class OrganosiliconState(TypedDict): chemical_spec: dict; validation_logs: Annotated[list, operator.add]; safety_cleared: bool; def validate_purity(state: OrganosiliconState): purity = state['chemical_spec'].get('purity', 0); return {'validation_logs': ['Purity check passed' if purity >= 99.9 else 'Purity failed'], 'safety_cleared': purity >= 99.9}; def export_control_check(state: OrganosiliconState): return {'validation_logs': ['Dual-use screening complete'], 'safety_cleared': state['safety_cleared']}; graph = StateGraph(OrganosiliconState); graph.add_node('purity_check', validate_purity); graph.add_node('export_check', export_control_check); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()