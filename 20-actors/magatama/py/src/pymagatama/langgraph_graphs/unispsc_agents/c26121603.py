from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableState(TypedDict): vendor_specs: dict; compliance_status: bool; validation_log: List[str]; def validate_spec(state: CableState): state['validation_log'].append('Validating cable specs'); return {'compliance_status': True}; def update_records(state: CableState): return {'validation_log': state['validation_log'] + ['Record updated']}; graph = StateGraph(CableState); graph.add_node('validate', validate_spec); graph.add_node('record', update_records); graph.add_edge('validate', 'record'); graph.add_edge('record', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()