from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[str]; status: str; def validate_sorter(state): return {'status': 'validated' if len(state['items']) > 0 else 'error'}; graph = StateGraph(State); graph.add_node('validate', validate_sorter); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()