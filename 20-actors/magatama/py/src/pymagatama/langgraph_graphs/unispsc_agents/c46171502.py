from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class LockState(TypedDict): specs: dict; valid: bool; def val_mech(state: LockState): state['valid'] = 'mechanism' in state['specs']; return state; def val_strength(state: LockState): state['valid'] = state['valid'] and state['specs'].get('tensile_strength', 0) > 10; return state; graph = StateGraph(LockState); graph.add_node('validate_mech', val_mech); graph.add_node('validate_strength', val_strength); graph.add_edge('validate_mech', 'validate_strength'); graph.add_edge('validate_strength', END); graph.set_entry_point('validate_mech'); graph = graph.compile()