from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): mold_id: str; specs: dict; valid: bool; def validate_specs(state: MoldState): state['valid'] = state['specs'].get('hardness', 0) > 50; return state; def CAD_check(state: MoldState): return state; graph = StateGraph(MoldState); graph.add_node('validate', validate_specs); graph.add_node('cad_audit', CAD_check); graph.add_edge('validate', 'cad_audit'); graph.add_edge('cad_audit', END); graph.set_entry_point('validate'); graph = graph.compile()