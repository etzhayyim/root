from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_name: str; compliance_passed: bool; storage_verified: bool; def check_regulatory(state: ProcState): return {'compliance_passed': True}; def verify_storage(state: ProcState): return {'storage_verified': True}; graph = StateGraph(ProcState); graph.add_node('regulatory', check_regulatory); graph.add_node('storage', verify_storage); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'storage'); graph.add_edge('storage', END); compiled_graph = graph.compile()