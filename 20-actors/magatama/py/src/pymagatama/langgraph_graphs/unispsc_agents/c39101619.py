from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LampState(TypedDict): specs: dict; valid: bool; error: str; def validate_cfl(state: LampState): specs = state.get('specs', {}); required = ['wattage', 'base_type', 'mercury_content']; if all(k in specs for k in required): state['valid'] = True; else: state['valid'] = False; state['error'] = 'Missing mandatory spec fields'; return state; graph = StateGraph(LampState); graph.add_node('validate', validate_cfl); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()