from langgraph.graph import StateGraph, END; from typing import TypedDict; class SEMState(TypedDict): specs: dict; compliance_approved: bool; def validate_specs(state: SEMState): resolution = state['specs'].get('resolution', 0); return {'compliance_approved': resolution < 5.0}; def compliance_check(state: SEMState): return {'compliance_approved': True} if 'export_permit' in state['specs'] else {'compliance_approved': False}; graph = StateGraph(SEMState); graph.add_node('validate', validate_specs); graph.add_node('compliance', compliance_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()