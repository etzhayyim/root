from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(SupplyState); def validate_specs(state: SupplyState): return {"validation_results": ["Validated dimensions and ECT rating for all items"]}; def finalize_order(state: SupplyState): return {"validation_results": ["Order finalized for warehouse dispatch"]}; graph.add_node("validate", validate_specs); graph.add_node("finalize", finalize_order); graph.set_entry_point("validate"); graph.add_edge("validate", "finalize"); graph.add_edge("finalize", END); app = graph.compile()