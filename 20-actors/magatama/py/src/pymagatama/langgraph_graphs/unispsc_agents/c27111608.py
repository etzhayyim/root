from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): wire_gauge: float; material: str; approved: bool; def validate_spec(state: State): state['approved'] = state['wire_gauge'] > 0 and 'steel' in state['material'].lower(); return state; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()