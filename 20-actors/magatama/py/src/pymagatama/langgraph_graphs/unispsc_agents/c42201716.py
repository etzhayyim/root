from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UltrasoundState(TypedDict): device_id: str; compliance_docs: List[str]; inspection_passed: bool; def check_compliance(state: UltrasoundState): return {'inspection_passed': all(doc in state['compliance_docs'] for doc in ['FDA', 'IEC60601'])}; def validate_specs(state: UltrasoundState): print(f'Validating specs for {state['device_id']}'); return {'inspection_passed': True}; graph = StateGraph(UltrasoundState); graph.add_node('compliance', check_compliance); graph.add_node('validation', validate_specs); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'validation'); graph.add_edge('validation', END); graph = graph.compile()