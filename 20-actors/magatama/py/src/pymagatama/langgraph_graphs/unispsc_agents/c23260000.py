from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PrototypingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PrototypingState): state['valid'] = 'build_volume_mm' in state['specs'] and state['specs']['build_volume_mm'] > 0; return state; graph = StateGraph(PrototypingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()