from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SeismicProcurementState(TypedDict): device_id: str; specs: dict; validation_passed: bool; def validate_sensors(state: SeismicProcurementState): state['validation_passed'] = 'sensitivity' in state['specs'] and state['specs']['sensitivity'] > 0; return state; graph = StateGraph(SeismicProcurementState); graph.add_node('validate', validate_sensors); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()