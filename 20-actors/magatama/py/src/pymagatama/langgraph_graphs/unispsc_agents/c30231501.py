from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GrandstandState(TypedDict): capacity: int; compliance_docs: List[str]; approved: bool; def validate_compliance(state: GrandstandState): state['approved'] = len(state['compliance_docs']) > 0; return state; graph = StateGraph(GrandstandState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()