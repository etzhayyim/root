from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; validation_passed: bool; specs: List[str]; def validate_purity(state: DrugState): state['validation_passed'] = 'purity_percentage' in state['specs']; return state; def check_compliance(state: DrugState): print(f'Verifying {state['chemical_name']} compliance'); return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()