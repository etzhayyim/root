from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): items: List[str]; validated: bool; results: str; def validate_mirror_specs(state: SpecState): state['validated'] = all(len(i) > 0 for i in state['items']); state['results'] = 'Validated' if state['validated'] else 'Invalid'; return state; graph = StateGraph(SpecState); graph.add_node('validate', validate_mirror_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()