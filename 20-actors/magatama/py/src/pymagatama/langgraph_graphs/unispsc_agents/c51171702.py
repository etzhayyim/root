from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): chemical_data: dict; verification_logs: List[str]; def validate_api(state: State): state['verification_logs'].append('Checking GMP compliance'); return state; def check_purity(state: State): state['verification_logs'].append('Verifying Purity > 99%'); return state; graph = StateGraph(State); graph.add_node('validate', validate_api); graph.add_node('purity', check_purity); graph.set_entry_point('validate'); graph.add_edge('validate', 'purity'); graph.add_edge('purity', END); graph = graph.compile()