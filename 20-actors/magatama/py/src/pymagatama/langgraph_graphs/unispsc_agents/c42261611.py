from langgraph.graph import StateGraph, END; from typing import TypedDict; class PostmortemState(TypedDict): data: dict; validated: bool; def check_precision(state: PostmortemState): state['validated'] = state['data'].get('precision', 0) < 0.1; return state; graph = StateGraph(PostmortemState); graph.add_node('validate', check_precision); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()