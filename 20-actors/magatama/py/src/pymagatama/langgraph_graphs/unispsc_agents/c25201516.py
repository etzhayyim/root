from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CanopyState(TypedDict): vendor_certs: List[str]; material_test_results: dict; validated: bool; def validate_materials(state: CanopyState): state['validated'] = 'ISO-9001' in state['vendor_certs'] and state['material_test_results'].get('strength') > 5000; return state; graph = StateGraph(CanopyState); graph.add_node('validate', validate_materials); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()