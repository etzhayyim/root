from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class HammerState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: HammerState): errors = []; if state['specs'].get('sensitivity', 0) <= 0: errors.append('Invalid sensitivity'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(HammerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()