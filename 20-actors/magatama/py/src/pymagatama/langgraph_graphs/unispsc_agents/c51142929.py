from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): content: str; quality_passed: bool; hazard_log: List[str]; def validate_purity(state: State): return {'quality_passed': True}; def check_hazmat(state: State): return {'hazard_log': ['Temperature-controlled shipment verified']}; graph = StateGraph(State); graph.add_node('validate', validate_purity); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()