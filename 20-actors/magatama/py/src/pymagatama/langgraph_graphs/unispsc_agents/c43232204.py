from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoftwareState(TypedDict): input_code: str; validation_status: bool; compliance_flags: List[str]; def validate_embedded_code(state: SoftwareState): state['validation_status'] = len(state['input_code']) > 0; return state; def check_compliance(state: SoftwareState): state['compliance_flags'] = ['encryption_compliant'] if state['validation_status'] else []; return state; graph = StateGraph(SoftwareState); graph.add_node('validate', validate_embedded_code); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()