from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): chemical_data: dict; validation_results: List[str]; graph = StateGraph(AgentState); def validate_chemical_purity(state: AgentState): state['validation_results'].append('Purity Check Completed'); return state; def check_regulatory_compliance(state: AgentState): state['validation_results'].append('License Verified'); return state; graph.add_node('purity_check', validate_chemical_purity); graph.add_node('reg_compliance', check_regulatory_compliance); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'reg_compliance'); graph.add_edge('reg_compliance', END); graph.compile()