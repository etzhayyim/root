from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FunnelState(TypedDict): funnel_specs: dict; validation_results: List[str]; def validate_material(state: FunnelState): material = state['funnel_specs'].get('material', 'unknown'); result = 'Valid' if material in ['Polypropylene', 'Stainless Steel'] else 'Invalid'; return {'validation_results': [f'Material check: {result}']}; def check_safety_compliance(state: FunnelState): return {'validation_results': state['validation_results'] + ['Compliance verified']}; graph = StateGraph(FunnelState); graph.add_node('validate_material', validate_material); graph.add_node('safety_check', check_safety_compliance); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'safety_check'); graph.add_edge('safety_check', END); compiled_graph = graph.compile()