from langgraph.graph import StateGraph, END; from typing import TypedDict; class IrrigationState(TypedDict): specs: dict; validation_report: str; def validate_irrigation_specs(state: IrrigationState): return {'validation_report': 'Specs validated for ISO standards'} if state['specs'].get('pressure') else {'validation_report': 'Missing pressure rating'}; graph = StateGraph(IrrigationState); graph.add_node('validate', validate_irrigation_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()