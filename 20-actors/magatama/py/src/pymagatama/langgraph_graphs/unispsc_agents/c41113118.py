from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GasMonitorState(TypedDict): model_specs: dict; compliance_status: bool; sensor_health_check: str; def validate_specs(state: GasMonitorState): return {'compliance_status': 'ATEX' in state['model_specs'].get('certs', [])}; def analyze_sensors(state: GasMonitorState): return {'sensor_health_check': 'CALIBRATED'}; graph = StateGraph(GasMonitorState); graph.add_node('validate', validate_specs); graph.add_node('analyze', analyze_sensors); graph.set_entry_point('validate'); graph.add_edge('validate', 'analyze'); graph.add_edge('analyze', END); compile_graph = graph.compile()