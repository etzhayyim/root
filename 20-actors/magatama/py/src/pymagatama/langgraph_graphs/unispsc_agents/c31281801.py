from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): component_id: str; specs: dict; valid: bool; def validate_specs(state: ProcureState): state['valid'] = 'alloy_grade' in state['specs'] and 'thickness_tolerance' in state['specs']; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()