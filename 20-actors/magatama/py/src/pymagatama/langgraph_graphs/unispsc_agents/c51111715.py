from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_id: str; quality_passed: bool; storage_validated: bool; def validate_purity(state: DrugState): state['quality_passed'] = True; return state; def check_cold_chain(state: DrugState): state['storage_validated'] = True; return state; graph = StateGraph(DrugState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_cold_chain', check_cold_chain); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_cold_chain'); graph.add_edge('check_cold_chain', END); graph = graph.compile()