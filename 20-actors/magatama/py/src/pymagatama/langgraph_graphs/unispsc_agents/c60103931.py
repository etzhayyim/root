from langgraph.graph import StateGraph, END; from typing import TypedDict; class SpecState(TypedDict): species: str; status: str; def validate_spec(state: SpecState): return {'status': 'validated'} if state['species'] else {'status': 'failed'}; def log_handling(state: SpecState): return {'status': 'logged'}; graph = StateGraph(SpecState); graph.add_node('validate', validate_spec); graph.add_node('log', log_handling); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph = graph.compile()