from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeState(TypedDict): tube_specs: dict; validation_report: str; def validate_specs(state: TubeState): specs = state.get('tube_specs', {}); required = ['tire_size', 'valve_type']; missing = [f for f in required if f not in specs]; state['validation_report'] = 'Valid' if not missing else f'Missing: {missing}'; return state; graph = StateGraph(TubeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()