from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validated: bool; state = State(); def validate_specs(state: State): state['validated'] = 'accuracy_tolerance' in state['input_data']; return state; builder = StateGraph(State); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()