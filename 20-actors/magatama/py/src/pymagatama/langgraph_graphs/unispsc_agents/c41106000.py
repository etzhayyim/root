from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NucleicState(TypedDict): assay_type: str; sensitivity: float; validated: bool; def validate_assay(state: NucleicState): state['validated'] = state['sensitivity'] > 0.95; return state; graph = StateGraph(NucleicState); graph.add_node('validate', validate_assay); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()