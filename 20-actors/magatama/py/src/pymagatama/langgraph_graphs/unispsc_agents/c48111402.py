from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ChangerState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_currency_config(state: ChangerState): return {"validation_results": ["Currency config verified"]}; def check_security_compliance(state: ChangerState): return {"validation_results": ["Security protocol passed"]}; graph = StateGraph(ChangerState); graph.add_node("validate_currency", validate_currency_config); graph.add_node("check_security", check_security_compliance); graph.add_edge("validate_currency", "check_security"); graph.add_edge("check_security", END); graph.set_entry_point("validate_currency"); graph = graph.compile()