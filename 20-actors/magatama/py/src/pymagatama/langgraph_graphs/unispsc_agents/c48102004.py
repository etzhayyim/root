from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TableSpecState(TypedDict): dimensions: dict; material: str; status: str; def validate_dimensions(state: TableSpecState): state['status'] = 'VALIDATED' if state['dimensions']['width'] > 0 else 'INVALID'; return state; def check_material(state: TableSpecState): state['status'] = 'APPROVED' if state['material'] in ['Wood', 'Laminate', 'Metal'] else 'REJECTED'; return state; graph = StateGraph(TableSpecState); graph.add_node('validate', validate_dimensions); graph.add_node('check_material', check_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()