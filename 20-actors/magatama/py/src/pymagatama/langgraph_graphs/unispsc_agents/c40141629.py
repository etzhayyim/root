from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ValveState(TypedDict): specs: dict; validation_results: List[str]; validated: bool; def validate_specs(state: ValveState): specs = state['specs']; results = []; if 'sleeve_material' not in specs: results.append('Missing sleeve material'); if specs.get('pressure_rating_psi', 0) <= 0: results.append('Invalid pressure rating'); return {'validation_results': results, 'validated': len(results) == 0}; graph = StateGraph(ValveState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()