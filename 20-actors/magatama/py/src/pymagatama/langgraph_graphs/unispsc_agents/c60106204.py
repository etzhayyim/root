from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CSState(TypedDict): aids: list; status: str; def validate_specs(state: CSState): return {'status': 'validated' if state['aids'] else 'error'}; def compile_graph(): graph = StateGraph(CSState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()