from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RubberCordState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: RubberCordState): validation = 'Passed' if state['specs'].get('tensile_strength') > 5 else 'Failed'; return {'validation_results': [f'Material check: {validation}']}; def check_dimensions(state: RubberCordState): status = 'Verified' if state['specs'].get('diameter_mm') > 0 else 'Error'; return {'validation_results': state['validation_results'] + [f'Dimension check: {status}']}; graph = StateGraph(RubberCordState); graph.add_node('validate_material', validate_material); graph.add_node('check_dimensions', check_dimensions); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_dimensions'); graph.add_edge('check_dimensions', END); graph = graph.compile()