from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ClockProcurementState(TypedDict): specs: dict; validation_report: list; approved: bool; def validate_clock_specs(state: ClockProcurementState): specs = state['specs']; report = []; if not specs.get('movement_type'): report.append('Missing movement type'); if specs.get('accuracy', 0) > 60: report.append('Accuracy rating exceeds tolerance'); return {'validation_report': report, 'approved': len(report) == 0}; graph = StateGraph(ClockProcurementState); graph.add_node('validate', validate_clock_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()