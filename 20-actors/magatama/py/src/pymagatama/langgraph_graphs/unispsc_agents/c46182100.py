from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ESDState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_esd_compliance(state: ESDState): errors = []; for item in state['items']: if 'resistance' not in item: errors.append(f'Missing resistance for {item["id"]}'); state['validation_errors'] = errors; return state; graph = StateGraph(ESDState); graph.add_node('validate', validate_esd_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()