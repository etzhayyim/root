from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_name: str; purity: float; hazard_verified: bool; def validate_purity(state: ChemicalState): return {'hazard_verified': state['purity'] >= 99.0}; def check_safety(state: ChemicalState): return {'hazard_verified': True}; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()