from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KitState(TypedDict): items: List[str]; validated: bool; defect_count: int; def validator(state: KitState): return { "validated": len(state['items']) > 0, "defect_count": 0 }; builder = StateGraph(KitState); builder.add_node("validate", validator); builder.set_entry_point("validate"); builder.add_edge("validate", END); graph = builder.compile()