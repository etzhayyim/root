from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SplintCaseState(TypedDict): case_data: dict; validation_results: List[str]; def validate_dimensions(state: SplintCaseState): state['validation_results'].append('Dimensions verified'); return state; def check_compliance(state: SplintCaseState): state['validation_results'].append('Compliance verified'); return state; graph = StateGraph(SplintCaseState); graph.add_node('validate', validate_dimensions); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()