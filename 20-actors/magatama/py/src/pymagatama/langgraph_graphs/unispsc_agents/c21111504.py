from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CultivatorState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: CultivatorState): specs = state['specs']; errors = []; if specs.get('power_output_hp', 0) < 1: errors.append('Insufficient power output'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(CultivatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()