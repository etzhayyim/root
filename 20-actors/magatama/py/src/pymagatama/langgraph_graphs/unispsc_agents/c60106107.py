from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; def val_step(state: ProcureState): return {'validated': True} if len(state['items']) > 0 else {'validated': False}; graph = StateGraph(ProcureState); graph.add_node('validate_materials', val_step); graph.set_entry_point('validate_materials'); graph.add_edge('validate_materials', END); graph = graph.compile()