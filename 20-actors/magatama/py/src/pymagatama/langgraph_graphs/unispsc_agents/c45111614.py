from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProjectorState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ProjectorState): state['valid'] = state['specs'].get('brightness', 0) > 2000; return state; graph = StateGraph(ProjectorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()