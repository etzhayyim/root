from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: Annotated[list, operator.add]; status: str; def validate_sterile(state: ProcureState): return {'status': 'validated' if 'sterile' in state.get('items', []) else 'failed'}; def build_graph(): graph = StateGraph(ProcureState); graph.add_node('validate', validate_sterile); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = build_graph()