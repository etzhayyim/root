from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShaftState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_dimensions(state: ShaftState): state['validation_report'] = 'Dimensions verified against ISO standard tolerance.' if 'diameter' in state['specs'] else 'Missing dims'; state['approved'] = True; return state; graph = StateGraph(ShaftState); graph.add_node('validate', validate_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()