from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class FurnaceState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_furnace(state: FurnaceState): specs = state['specs']; errors = []; if specs.get('temp', 0) > 2000: errors.append('High-temp regulation required'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(FurnaceState); graph.add_node('validate', validate_furnace); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()