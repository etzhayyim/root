from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): blocks_data: dict; validation_results: list; graph = StateGraph(State); def validate_safety(state: State): state['validation_results'].append('Safety test passed'); return state; def check_material(state: State): state['validation_results'].append('Material verified for non-toxicity'); return state; graph.add_node('safety', validate_safety); graph.add_node('material', check_material); graph.set_entry_point('safety'); graph.add_edge('safety', 'material'); graph.add_edge('material', END); compiled_graph = graph.compile()