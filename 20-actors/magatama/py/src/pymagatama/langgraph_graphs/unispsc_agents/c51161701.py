from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity_percent: float; compliance_passed: bool; def validate_purity(state: PharmState): state['compliance_passed'] = state['purity_percent'] >= 99.0; return state; def check_storage(state: PharmState): return {'compliance_passed': state['compliance_passed'] and True}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); compiled_graph = graph.compile()