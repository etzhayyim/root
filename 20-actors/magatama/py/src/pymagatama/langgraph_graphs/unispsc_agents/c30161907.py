from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StairState(TypedDict): specs: dict; compliant: bool; def validate_stairs(state: StairState): load = state['specs'].get('load_bearing', 0); state['compliant'] = load >= 500; return state; graph = StateGraph(StairState); graph.add_node('validate', validate_stairs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()