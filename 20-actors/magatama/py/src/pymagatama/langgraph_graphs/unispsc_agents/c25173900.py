from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): component_data: dict; compliance_status: bool; graph = StateGraph(ProcureState); def validate_specs(state: ProcureState): state['compliance_status'] = 'Voltage_Rating' in state['component_data']; return state; def check_quality(state: ProcureState): return state; graph.add_node('validate', validate_specs); graph.add_node('quality', check_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'quality'); graph.add_edge('quality', END); compiled_graph = graph.compile()