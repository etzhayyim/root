from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeAssemblyState(TypedDict): vendor_certs: List[str]; inspection_passed: bool; needs_ndtr: bool; def validate_certs(state: TubeAssemblyState): return {'inspection_passed': 'aws_d1.1' in state['vendor_certs']}; def assess_risk(state: TubeAssemblyState): return {'needs_ndtr': True if state['inspection_passed'] else False}; graph = StateGraph(TubeAssemblyState); graph.add_node('validation', validate_certs); graph.add_node('risk_assessment', assess_risk); graph.set_entry_point('validation'); graph.add_edge('validation', 'risk_assessment'); graph.add_edge('risk_assessment', END); graph = graph.compile()