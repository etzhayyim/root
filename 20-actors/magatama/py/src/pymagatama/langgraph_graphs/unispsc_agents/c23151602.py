from langgraph.graph import StateGraph, END; from typing import TypedDict; class WeldingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: WeldingState): state['valid'] = all(k in state['specs'] for k in ['amps', 'duty_cycle']); return state; graph = StateGraph(WeldingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()