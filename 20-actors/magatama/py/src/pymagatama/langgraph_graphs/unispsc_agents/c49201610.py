from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input_specs: dict; validated: bool; analysis: str; def validate_specs(state: State): specs = state.get('input_specs', {}); resistance = specs.get('resistance', 0); is_valid = resistance > 0 and 'material' in specs; return {'validated': is_valid, 'analysis': 'Compliance check passed' if is_valid else 'Quality error found'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()