from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): device_specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_medical_specs(state: ProcState): state['compliance_status'] = 'Pressure_Range' in state['device_specs']; state['workflow_log'].append('Validation completed'); return state; def finalize_order(state: ProcState): state['workflow_log'].append('Order ready for fulfillment'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_medical_specs); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()