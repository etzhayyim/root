from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolingState(TypedDict): tool_data: dict; validation_report: List[str]; def validate_specs(state: ToolingState): specs = state['tool_data']; errors = []; required = ['material', 'diameter']; for r in required: if r not in specs: errors.append(f'Missing: {r}'); return {'validation_report': errors}; graph = StateGraph(ToolingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()