from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BackshellState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(BackshellState); def validate_specs(state: BackshellState): return {'compliance_check': all(k in state['specs'] for k in ['shell_size', 'ip_rating'])}; def finalize(state: BackshellState): return {'compliance_check': True}; graph.add_node('validation', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validation'); graph.add_edge('validation', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()