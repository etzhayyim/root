from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List; compliance_passed: bool; def validate_grade(state: ProcureState): state['compliance_passed'] = all(item.get('grade') == 'Pharma' for item in state['items']); return state; def check_safety(state: ProcureState): print('Checking shelf life and sterility'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_grade); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); app = graph.compile()