from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(AssemblyState); def validate_material(state): return {'validation_results': ['Material Composition Verified']}; def validate_welding(state): return {'validation_results': ['UV Weld Integrity Confirmed']}; graph.add_node('material_check', validate_material); graph.add_node('weld_check', validate_welding); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'weld_check'); graph.add_edge('weld_check', END); graph = graph.compile()