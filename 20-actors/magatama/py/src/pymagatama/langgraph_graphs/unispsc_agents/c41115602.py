from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TitrationState(TypedDict): specs: dict; validation_results: List[str]; def validate_spec(state: TitrationState): state['validation_results'].append('Checking calibration std...'); return state; def check_compliance(state: TitrationState): state['validation_results'].append('Compliance verified.'); return state; graph = StateGraph(TitrationState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()