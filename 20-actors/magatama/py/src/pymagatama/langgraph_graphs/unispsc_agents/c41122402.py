from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): data: dict; valid: bool; def check_specs(state: AgentState): data = state['data']; state['valid'] = 'length' in data and 'gauss' in data; return state; builder = StateGraph(AgentState); builder.add_node('validate', check_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()