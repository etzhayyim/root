from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChamberState(TypedDict): specs: dict; validation_errors: List[str]; def validate_chamber(state: ChamberState): e = []; s = state.get('specs', {}); if 'temp_range' not in s: e.append('Missing temp_range'); return {'validation_errors': e}; builder = StateGraph(ChamberState); builder.add_node('validate', validate_chamber); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()