from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RailState(TypedDict): specs: dict; validation_passed: bool; logs: List[str]; def validate_specs(state: RailState): specs = state['specs']; state['validation_passed'] = all([specs.get('load_capacity'), specs.get('rail_gauge')]); state['logs'].append('Specs validated'); return state; def check_compliance(state: RailState): state['logs'].append('Checking AAR/UIC compliance'); return state; graph = StateGraph(RailState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()