from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GearState(TypedDict): gear_specs: dict; validation_results: List[str]; def validate_material(state: GearState): state['validation_results'].append('Material confirmed.'); return state; def check_tolerances(state: GearState): state['validation_results'].append('Tolerances verified.'); return state; graph = StateGraph(GearState); graph.add_node('material_check', validate_material); graph.add_node('tolerance_check', check_tolerances); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'tolerance_check'); graph.add_edge('tolerance_check', END); graph = graph.compile()