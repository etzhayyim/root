from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): chemical_name: str; purity_check: bool; compliance_docs: bool; def validate_purity(state: ProcureState): return {'purity_check': True} if state.get('chemical_name') == 'Dihydroergocristine mesilate' else {'purity_check': False}; def check_compliance(state: ProcureState): return {'compliance_docs': True}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()