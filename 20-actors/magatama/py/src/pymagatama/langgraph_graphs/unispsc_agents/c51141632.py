from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): chemical_name: str; compliance_checks: List[str]; approved: bool; def validate_pharma(state: State): state['compliance_checks'].append('GMP_Verified'); state['approved'] = True; return state; graph = StateGraph(State); graph.add_node('validate', validate_pharma); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()