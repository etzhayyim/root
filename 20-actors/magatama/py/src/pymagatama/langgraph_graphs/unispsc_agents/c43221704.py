from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadioState(TypedDict): hardware_specs: dict; compliance_docs: List[str]; inspection_status: str; def validate_specs(state: RadioState): state['inspection_status'] = 'COMPLIANT' if '3GPP' in state['compliance_docs'] else 'FAILED'; return state; def route_inspection(state: RadioState): return 'certified' if state['inspection_status'] == 'COMPLIANT' else END; graph = StateGraph(RadioState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()