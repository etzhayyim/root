from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class SurgeryToolState(TypedDict): tool_id: str; compliance_checks: Annotated[Sequence[str], operator.add]; status: str; def validate_iso_cert(state: SurgeryToolState): state['compliance_checks'].append('ISO_13485_VERIFIED'); return {'status': 'CERTIFIED'}; def check_laser_safety(state: SurgeryToolState): state['compliance_checks'].append('LASER_SAFETY_MET'); return {'status': 'READY'}; graph = StateGraph(SurgeryToolState); graph.add_node('validate_iso', validate_iso_cert); graph.add_node('check_safety', check_laser_safety); graph.set_entry_point('validate_iso'); graph.add_edge('validate_iso', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()