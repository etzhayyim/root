from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: PumpState): results = []; if 'Material Grade' not in state['specs']: results.append('Missing material grade'); return {'validation_results': results}; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()