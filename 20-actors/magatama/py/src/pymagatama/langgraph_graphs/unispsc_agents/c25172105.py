from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CollisionSystemState(TypedDict): vendor_specs: dict; compliance_passed: bool; def validate_specs(state: CollisionSystemState): specs = state['vendor_specs']; compliance = specs.get('ISO_26262', False) and specs.get('latency', 100) < 50; return {'compliance_passed': compliance}; graph = StateGraph(CollisionSystemState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()