from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class HandlingState(TypedDict): specs: dict; validation_report: list; status: str; def validate_specs(state: HandlingState): is_valid = 'capacity' in state['specs'] and 'hygiene' in state['specs']; return {'status': 'approved' if is_valid else 'rejected'}; graph = StateGraph(HandlingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()