from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ResinProcurementState(TypedDict): resin_specs: dict; inspection_results: list; approved: bool; graph = StateGraph(ResinProcurementState); def validate_specs(state: ResinProcurementState): return {'approved': state['resin_specs'].get('purity', 0) > 0.99}; def run_inspection(state: ResinProcurementState): return {'inspection_results': ['thermal_test_passed', 'tensile_test_passed']}; graph.add_node('validate', validate_specs); graph.add_node('inspect', run_inspection); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()