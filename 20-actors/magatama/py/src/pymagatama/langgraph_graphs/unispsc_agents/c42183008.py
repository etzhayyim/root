from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EuthyscopeState(TypedDict): data: dict; validation_errors: List[str]; def validate_specs(state: EuthyscopeState): errors = []; if 'iso13485' not in state['data']: errors.append('Missing ISO 13485 certification'); return {'validation_errors': errors}; def finalize_procurement(state: EuthyscopeState): return {'data': {**state['data'], 'status': 'APPROVED'}}; graph = StateGraph(EuthyscopeState); graph.add_node('validation', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validation'); graph.add_edge('validation', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()