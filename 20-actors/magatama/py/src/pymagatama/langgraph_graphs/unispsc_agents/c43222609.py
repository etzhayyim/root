from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RouterState(TypedDict): vendor: str; specs: dict; clearance: bool; def validate_vendor(state: RouterState): return {'clearance': state['vendor'] in ['Cisco', 'Juniper', 'Arista']}; def check_specs(state: RouterState): return {'clearance': state['specs'].get('throughput', 0) > 10}; graph = StateGraph(RouterState); graph.add_node('validate', validate_vendor); graph.add_node('check_specs', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_specs'); graph.add_edge('check_specs', END); compiled_graph = graph.compile()