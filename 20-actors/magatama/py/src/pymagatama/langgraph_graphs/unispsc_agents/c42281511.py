from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LampState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_uv_specs(state: LampState): state['compliance'] = state['specs'].get('wavelength') == 254; return state; def check_safety(state: LampState): state['approved'] = state['compliance'] and state['specs'].get('safety_cert', False); return state; graph_builder = StateGraph(LampState); graph_builder.add_node('validate', validate_uv_specs); graph_builder.add_node('safety', check_safety); graph_builder.add_edge('validate', 'safety'); graph_builder.add_edge('safety', END); graph_builder.set_entry_point('validate'); graph = graph_builder.compile()