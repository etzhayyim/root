from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: str; validation_status: bool; workflow_log: str; def validate_refill(state: State): state['validation_status'] = True; state['workflow_log'] = 'Validation passed: Ink type matches specifications.'; return state; def finalize_request(state: State): state['workflow_log'] += ' Finalized for procurement processing.'; return state; graph_builder = StateGraph(State); graph_builder.add_node('validate', validate_refill); graph_builder.add_node('finalize', finalize_request); graph_builder.add_edge('validate', 'finalize'); graph_builder.add_edge('finalize', END); graph_builder.set_entry_point('validate'); graph = graph_builder.compile()