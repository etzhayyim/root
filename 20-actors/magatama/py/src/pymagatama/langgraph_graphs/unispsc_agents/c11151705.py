from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PreciousMetalState(TypedDict): metal_code: str; purity: float; status: str; validation_log: Annotated[list[str], operator.add]; def validate_purity(state: PreciousMetalState): if state['purity'] < 0.999: return {'status': 'rejected', 'validation_log': ['Purity below threshold']}; return {'status': 'verified', 'validation_log': ['Purity standard met']}; def check_export_control(state: PreciousMetalState): return {'validation_log': ['Dual-use compliance checked']}; graph = StateGraph(PreciousMetalState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()