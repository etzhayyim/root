from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BicycleState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: BicycleState): errors = []; if not state['specs'].get('safety_certification'): errors.append('Missing safety certification'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(BicycleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()