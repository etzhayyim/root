from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class OstomyState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: OstomyState): certs = state['specs'].get('certifications', []); state['valid'] = 'ISO_13485' in certs; return state; graph = StateGraph(OstomyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()