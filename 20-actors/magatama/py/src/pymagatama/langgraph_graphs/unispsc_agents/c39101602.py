from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MedicalLampState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(MedicalLampState); def validate_iec_compliance(state: MedicalLampState): state['validation_results'].append('IEC 60601-1 compliance checked'); return state; def check_luminous_intensity(state: MedicalLampState): state['validation_results'].append('Luminous flux within medical range'); return state; graph.add_node('iec_check', validate_iec_compliance); graph.add_node('light_check', check_luminous_intensity); graph.set_entry_point('iec_check'); graph.add_edge('iec_check', 'light_check'); graph.add_edge('light_check', END); graph = graph.compile()