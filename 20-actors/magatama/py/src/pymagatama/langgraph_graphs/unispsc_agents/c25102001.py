from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TankProcurementState(TypedDict): specs: dict; compliance_cleared: bool; inspection_passed: bool; def check_compliance(state: TankProcurementState): return {'compliance_cleared': True}; def validate_specs(state: TankProcurementState): return {'inspection_passed': True if 'armor' in state['specs'] else False}; graph = StateGraph(TankProcurementState); graph.add_node('compliance', check_compliance); graph.add_node('validation', validate_specs); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'validation'); graph.add_edge('validation', END); compile_graph = graph.compile()