from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SwitchState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: SwitchState): state['compliance'] = all(k in state['specs'] for k in ['voltage', 'iec_cert']); return state; def final_decision(state: SwitchState): state['approved'] = state['compliance']; return state; graph = StateGraph(SwitchState); graph.add_node('validate', validate_specs); graph.add_node('decision', final_decision); graph.set_entry_point('validate'); graph.add_edge('validate', 'decision'); graph.add_edge('decision', END); app = graph.compile()