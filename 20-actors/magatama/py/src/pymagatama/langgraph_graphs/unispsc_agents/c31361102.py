from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlateState(TypedDict): specs: List[str]; validation_results: dict; def validate_specs(state: PlateState): state['validation_results'] = {'passed': all(['Bond Strength' in s for s in state['specs']])}; return state; builder = StateGraph(PlateState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()