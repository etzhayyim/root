from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; quality_score: float; compliance_check: bool; def validate_titanium_spec(state: AssemblyState): print(f'Validating specs for {state['assembly_id']}'); return {'quality_score': 0.95}; def check_export_control(state: AssemblyState): return {'compliance_check': True}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_titanium_spec); graph.add_node('compliance', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()