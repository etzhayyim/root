from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[dict]; qc_passed: bool; def validate_specs(state: ProcureState): state['qc_passed'] = all('dimensions' in item for item in state['items']); return state; def finalize(state: ProcureState): return {'result': 'approved' if state['qc_passed'] else 'rejected'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()