from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BerylliumState(TypedDict): alloy_compliance: bool; dimensions_validated: bool; export_approved: bool; def check_compliance(state): return {'alloy_compliance': True}; def validate_dims(state): return {'dimensions_validated': True}; def verify_export(state): return {'export_approved': True}; graph = StateGraph(BerylliumState); graph.add_node('compliance', check_compliance); graph.add_node('dimensions', validate_dims); graph.add_node('export', verify_export); graph.add_edge('compliance', 'dimensions'); graph.add_edge('dimensions', 'export'); graph.add_edge('export', END); graph.set_entry_point('compliance'); graph = graph.compile()