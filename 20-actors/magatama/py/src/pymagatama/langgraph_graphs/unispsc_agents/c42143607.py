from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PatientSensorState(TypedDict): sensor_id: str; data: dict; status: str; def validate_sensor(state: PatientSensorState): state['status'] = 'VALIDATING'; print(f'Validating sensor: {state["sensor_id"]}'); return state; def check_alarm_config(state: PatientSensorState): state['status'] = 'CONFIG_CHECK'; print('Checking alarm configuration threshold'); return state; graph = StateGraph(PatientSensorState); graph.add_node('validate', validate_sensor); graph.add_node('config', check_alarm_config); graph.set_entry_point('validate'); graph.add_edge('validate', 'config'); graph.add_edge('config', END); app = graph.compile()