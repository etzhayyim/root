from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): color_options: List[str]; validation_status: bool; def validate_specs(state: ProcurementState): return {'validation_status': len(state['color_options']) > 0}; def finalize_order(state: ProcurementState): return {'validation_status': True}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()