from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_specs: dict; validation_status: bool; def validate_spec(state: ToolState): specs = state['tool_specs']; is_valid = specs.get('rpm', 0) > 0 and 'collet' in specs; return {'validation_status': is_valid}; graph = StateGraph(ToolState); graph.add_node('validator', validate_spec); graph.set_entry_point('validator'); graph.add_edge('validator', END); compiled_graph = graph.compile()