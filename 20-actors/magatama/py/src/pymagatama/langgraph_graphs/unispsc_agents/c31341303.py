from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): material_cert: bool; weld_integrity: bool; assembly_steps: Annotated[list[str], operator.add]; def validate_material(state: AssemblyState): return {'material_cert': True, 'assembly_steps': ['check_astm_b435']}; def inspect_welds(state: AssemblyState): return {'weld_integrity': True, 'assembly_steps': ['uv_weld_scan']}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_material); graph.add_node('inspect', inspect_welds); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()