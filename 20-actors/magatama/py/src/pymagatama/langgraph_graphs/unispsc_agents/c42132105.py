from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): linen_specs: dict; inspection_results: List[str]; approved: bool; def val_quality(state: State): state['inspection_results'].append('Checking GSM and fabric integrity'); return state; def val_regulatory(state: State): state['approved'] = state['linen_specs'].get('cert') == 'ISO-13485'; return state; graph = StateGraph(State); graph.add_node('quality', val_quality); graph.add_node('regulatory', val_regulatory); graph.set_entry_point('quality'); graph.add_edge('quality', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()