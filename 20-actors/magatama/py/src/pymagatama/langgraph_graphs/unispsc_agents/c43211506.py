from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ThinClientState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ThinClientState): specs = state['specs']; errors = []; if 'vdi_protocol_support' not in specs: errors.append('Missing protocol support'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(ThinClientState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()