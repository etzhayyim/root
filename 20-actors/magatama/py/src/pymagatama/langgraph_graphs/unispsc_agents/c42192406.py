from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CartProcessingState(TypedDict): vendor_id: str; specs: dict; compliance_checked: bool; def validate_specs(state): return {'compliance_checked': True if state['specs'].get('sterilization_compatibility') else False}; def route_processing(state): return 'approved' if state['compliance_checked'] else 'manual_review'; graph = StateGraph(CartProcessingState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); workflow = graph.compile()