from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ResuscitatorState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: ResuscitatorState): state['compliance'] = all(k in state['specs'] for k in ['ISO_10651', 'batch_id']); return state; def check_approval(state: ResuscitatorState): state['approved'] = state.get('compliance', False) and state['specs'].get('regulatory_status') == 'CLEARED'; return state; graph = StateGraph(ResuscitatorState); graph.add_node('validate', validate_specs); graph.add_node('approve', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.compile()