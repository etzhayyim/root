from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): info: dict; valid: bool; def validate_api(state: ProcState): state['valid'] = all(k in state['info'] for k in ['purity', 'gmp_cert']); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_api); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()