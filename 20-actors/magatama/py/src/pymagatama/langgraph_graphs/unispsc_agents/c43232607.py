from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlightSoftwareState(TypedDict): software_version: str; cert_level: str; compliance_check: bool; def validate_compliance(state: FlightSoftwareState): state['compliance_check'] = state['cert_level'] == 'Level-A'; return state; def final_approval(state: FlightSoftwareState): return 'APPROVED' if state['compliance_check'] else 'REJECTED'; graph = StateGraph(FlightSoftwareState); graph.add_node('validate', validate_compliance); graph.add_node('finalize', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()