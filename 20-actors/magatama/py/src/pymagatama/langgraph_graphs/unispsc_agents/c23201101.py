from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TruckState(TypedDict): vehicle_id: str; specs: dict; compliance: bool; def validate_specs(state: TruckState): state['compliance'] = state['specs'].get('emission_rating') == 'Euro6'; return state; def check_hydraulic(state: TruckState): print(f'Checking hydraulics for {state['vehicle_id']}'); return state; graph = StateGraph(TruckState); graph.add_node('validate', validate_specs); graph.add_node('hydraulic_check', check_hydraulic); graph.set_entry_point('validate'); graph.add_edge('validate', 'hydraulic_check'); graph.add_edge('hydraulic_check', END); compiled_graph = graph.compile()