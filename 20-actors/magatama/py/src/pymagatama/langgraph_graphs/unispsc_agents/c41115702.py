from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): data: dict; status: str; def validate_spec(state: GraphState): return {'status': 'validated'} if 'calibration' in state['data'] else {'status': 'rejected'}; def process_data(state: GraphState): return {'status': 'processed'}; graph = StateGraph(GraphState); graph.add_node('validate', validate_spec); graph.add_node('process', process_data); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()