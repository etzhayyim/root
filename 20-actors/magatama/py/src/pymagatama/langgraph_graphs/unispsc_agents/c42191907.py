from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StorageState(TypedDict): specs: dict; validation_errors: List[str]; def validate_medical_spec(state: StorageState): errors = []; if state['specs'].get('load_capacity', 0) <= 0: errors.append('Load capacity is required'); state['validation_errors'] = errors; return state; graph = StateGraph(StorageState); graph.add_node('validate', validate_medical_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()