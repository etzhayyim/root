from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; validated: bool; def validate_purity(state: PharmState): return {'validated': state['purity'] >= 99.0}; def packaging_check(state: PharmState): return {'validated': state['validated']}; graph = StateGraph(PharmState); graph.add_node('purity_check', validate_purity); graph.add_node('storage_check', packaging_check); graph.add_edge('purity_check', 'storage_check'); graph.add_edge('storage_check', END); graph.set_entry_point('purity_check'); graph = graph.compile()