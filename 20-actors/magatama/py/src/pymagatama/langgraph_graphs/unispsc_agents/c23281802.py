from langgraph.graph import StateGraph, END; from typing import TypedDict; class PVDState(TypedDict): specs: dict; valid: bool; def validate_spec(state: PVDState): state['valid'] = 'material' in state['specs'] and state['specs']['temp'] > 500; return state; graph = StateGraph(PVDState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()