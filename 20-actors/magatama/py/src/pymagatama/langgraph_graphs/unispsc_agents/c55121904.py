from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BillboardState(TypedDict): vendor_docs: List[str]; structural_compliance: bool; permit_valid: bool; def check_structural_specs(state: BillboardState): state['structural_compliance'] = True; return state; def check_permits(state: BillboardState): state['permit_valid'] = True; return state; graph = StateGraph(BillboardState); graph.add_node('structural_check', check_structural_specs); graph.add_node('permit_check', check_permits); graph.set_entry_point('structural_check'); graph.add_edge('structural_check', 'permit_check'); graph.add_edge('permit_check', END); graph = graph.compile()