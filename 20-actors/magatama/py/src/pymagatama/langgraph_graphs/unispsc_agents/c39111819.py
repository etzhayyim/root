from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LampFittingState(TypedDict): specs: dict; compliance: bool; validation_log: List[str]; def validate_electrical_specs(state: LampFittingState): is_compliant = state['specs'].get('voltage') > 0 and 'IP' in state['specs']; return {'compliance': is_compliant, 'validation_log': ['Electrical spec check completed']}; def finalize_procurement(state: LampFittingState): return {'validation_log': state['validation_log'] + ['Procurement ready']}; graph = StateGraph(LampFittingState); graph.add_node('validate', validate_electrical_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()