from langgraph.graph import StateGraph, END; from typing import TypedDict; class SlateState(TypedDict): specs: dict; valid: bool; def validate_slate(state: SlateState): state['valid'] = state['specs'].get('absorption', 0) < 0.25; return state; graph = StateGraph(SlateState); graph.add_node('validate', validate_slate); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()