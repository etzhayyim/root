from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): composition: dict; compliance: bool; approved: bool; def validate_ingredients(state: State): state['compliance'] = 'protein' in state['composition'] and state['composition']['sugar_content'] < 5; return state; def check_quality(state: State): state['approved'] = state['compliance'] and 'haccp_cert' in state['composition']; return state; graph = StateGraph(State); graph.add_node('validate', validate_ingredients); graph.add_node('qc', check_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'qc'); graph.add_edge('qc', END); app = graph.compile()