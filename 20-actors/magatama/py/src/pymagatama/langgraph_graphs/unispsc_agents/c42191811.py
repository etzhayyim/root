from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): spec: dict; valid: bool; def validate_cradle(state: State): state['valid'] = 'ISO_13485' in state['spec'] and state['spec'].get('material') == 'medical_grade'; return state; graph = StateGraph(State); graph.add_node('validate', validate_cradle); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()