from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): input_spec: dict; analysis_result: str; compliance_check: bool; workflow_log: Annotated[Sequence[str], operator.add]; def validate_purity(state: CatalystState): purity = state['input_spec'].get('purity_percentage', 0); is_compliant = purity >= 99.9; return {'compliance_check': is_compliant, 'workflow_log': [f'Purity check at {purity}%: {is_compliant}']}; def execute_safety_review(state: CatalystState): return {'analysis_result': 'High-purity chemical pathway validated.', 'workflow_log': ['Safety review completed']}; graph = StateGraph(CatalystState); graph.add_node('validate', validate_purity); graph.add_node('safety', execute_safety_review); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()