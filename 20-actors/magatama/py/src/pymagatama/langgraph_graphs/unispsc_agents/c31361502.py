from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; def validate_welds(state: AssemblyState): results = ["Weld integrity check passed" if state['specs'].get('weld_quality_grade') == 'A' else "Weld inspection required"]; return {'validation_results': results}; def inspect_dimensions(state: AssemblyState): return {'validation_results': state['validation_results'] + ["Dimensional check complete"]}; graph = StateGraph(AssemblyState); graph.add_node('weld_check', validate_welds); graph.add_node('dim_check', inspect_dimensions); graph.set_entry_point('weld_check'); graph.add_edge('weld_check', 'dim_check'); graph.add_edge('dim_check', END); compiled_graph = graph.compile()