from langgraph.graph import StateGraph, END; from typing import TypedDict; class LifeRingState(TypedDict): doc_id: str; compliance_check: bool; float_capacity: float; def validate_solas(state: LifeRingState): state['compliance_check'] = state['float_capacity'] >= 14.5; return state; graph = StateGraph(LifeRingState); graph.add_node('validate', validate_solas); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()