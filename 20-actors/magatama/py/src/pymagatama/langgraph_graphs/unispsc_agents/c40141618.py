from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ValveState(TypedDict): valve_type: str; specs: dict; validation_passed: bool; def validate_specs(state: ValveState): state['validation_passed'] = 'Pressure Rating' in state['specs'] and 'Body Material' in state['specs']; return state; builder = StateGraph(ValveState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()