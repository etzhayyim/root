from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BoosterState(TypedDict): specs: dict; validation_report: List[str]; def validate_specs(state: BoosterState): return {'validation_report': ['Thrust check passed', 'Material integrity verified']}; def compile_system(): graph = StateGraph(BoosterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_system()