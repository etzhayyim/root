from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GrinderState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: GrinderState): errors = []; if state['specs'].get('disc_diameter_mm', 0) <= 0: errors.append('Invalid diameter'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(GrinderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()