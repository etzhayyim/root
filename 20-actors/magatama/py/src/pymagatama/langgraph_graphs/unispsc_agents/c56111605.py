from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PanelState(TypedDict): component_type: str; specs: dict; compliant: bool; def validator(state: PanelState): state['compliant'] = all(k in state['specs'] for k in ['Voltage', 'IP_Rating']); return state; graph = StateGraph(PanelState); graph.add_node('validate', validator); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()