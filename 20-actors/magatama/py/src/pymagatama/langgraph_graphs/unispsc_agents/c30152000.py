from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FencingState(TypedDict): specs: dict; validation_passed: bool; def validate_fencing(state: FencingState): state['validation_passed'] = 'Material Grade' in state['specs'] and 'Corrosion Resistance Rating' in state['specs']; return state; def route_final(state: FencingState): return 'end' if state['validation_passed'] else 'end'; graph_builder = StateGraph(FencingState); graph_builder.add_node('validate', validate_fencing); graph_builder.add_edge('validate', END); graph_builder.set_entry_point('validate'); graph = graph_builder.compile();