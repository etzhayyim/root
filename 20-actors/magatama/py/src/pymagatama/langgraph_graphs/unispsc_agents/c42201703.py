from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MammographyState(TypedDict): vendor_docs: List[str]; validation_results: dict; clinical_compliance: bool; def validate_specs(state: MammographyState): state['validation_results'] = {'approved': True, 'checks': ['ISO', 'FDA', 'DICOM']}; return state; def check_compliance(state: MammographyState): state['clinical_compliance'] = True; return state; graph = StateGraph(MammographyState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()