from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MattressState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: MattressState): errors = []; if 'fire_retardancy_standard' not in state['specs']: errors.append('Missing fire retardancy certification'); return {'validation_errors': errors}; def check_approval(state: MattressState): return 'approved' if not state['validation_errors'] else 'rejected'; graph = StateGraph(MattressState); graph.add_node('validate', validate_specs); graph.add_node('approval', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph.compile()