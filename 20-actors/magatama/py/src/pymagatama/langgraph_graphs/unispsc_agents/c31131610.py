from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): dimensions: dict; quality_certs: List[str]; approved: bool; def validate_specs(state: ForgingState): state['approved'] = 'MTR' in state['quality_certs'] and all(v > 0 for v in state['dimensions'].values()); return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()