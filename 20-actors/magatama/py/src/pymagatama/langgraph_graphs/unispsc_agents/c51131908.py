from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): chemical_data: dict; approval: bool; def validate_spec(state: ProcState): state['approval'] = 'Molecular Weight' in state['chemical_data'] and state['chemical_data']['Endotoxin'] < 0.5; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()