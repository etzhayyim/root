from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): metal_grade: str; dimensions: dict; compliance_check: bool; def validate_alloy(state: CastingState): return {'compliance_check': state['metal_grade'] == 'C36000'}; def approve_spec(state: CastingState): return {'compliance_check': True}; graph = StateGraph(CastingState); graph.add_node('validate', validate_alloy); graph.add_node('approve', approve_spec); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()