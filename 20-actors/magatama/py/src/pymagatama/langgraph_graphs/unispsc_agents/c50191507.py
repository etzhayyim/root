from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoupState(TypedDict): items: List[str]; qc_pass: bool; def validate(state: SoupState): return {'qc_pass': all(len(i) > 0 for i in state['items'])}; graph = StateGraph(SoupState); graph.add_node('validate', validate); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()