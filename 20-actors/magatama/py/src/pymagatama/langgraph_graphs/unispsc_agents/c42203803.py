from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): unit_id: str; specs: dict; verified: bool; def validate_specs(state: State): state['verified'] = 'radiotransparency' in state['specs'] and state['specs']['weight_capacity'] > 0; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()