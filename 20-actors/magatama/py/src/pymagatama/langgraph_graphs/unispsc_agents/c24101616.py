from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LiftingState(TypedDict): device_id: str; safety_checks: List[str]; inspection_status: str; def validate_load(state): return {'inspection_status': 'verified' if len(state['safety_checks']) > 2 else 'failed'}; def compliance_check(state): return {'safety_checks': state['safety_checks'] + ['ASTM_checked']}; graph = StateGraph(LiftingState); graph.add_node('compliance', compliance_check); graph.add_node('validation', validate_load); graph.add_edge('compliance', 'validation'); graph.add_edge('validation', END); graph.set_entry_point('compliance'); graph = graph.compile()