from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; compliance_checks: Annotated[list, operator.add]; status: str; def validate_material(state: AssemblyState): return {'compliance_checks': ['Material composition validated'], 'status': 'Material Verified'}; def check_structural_specs(state: AssemblyState): return {'compliance_checks': ['Bolt torque limits verified'], 'status': 'Specs Validated'}; graph = StateGraph(AssemblyState); graph.add_node('validate_material', validate_material); graph.add_node('check_structural_specs', check_structural_specs); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_structural_specs'); graph.add_edge('check_structural_specs', END); graph = graph.compile()