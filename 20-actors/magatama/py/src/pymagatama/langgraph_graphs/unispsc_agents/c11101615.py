from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MetalProcureState(TypedDict): metal_type: str; purity_percent: float; quality_certs: Annotated[Sequence[str], operator.add]; status: str; def validate_purity(state: MetalProcureState): return {'status': 'PASSED' if state['purity_percent'] >= 99.9 else 'FAILED'}; def check_certs(state: MetalProcureState): return {'status': 'VALIDATED' if 'ISO9001' in state['quality_certs'] else 'REQUIRES_MANUAL_INSPECTION'}; builder = StateGraph(MetalProcureState); builder.add_node('validate', validate_purity); builder.add_node('certify', check_certs); builder.add_edge('validate', 'certify'); builder.add_edge('certify', END); builder.set_entry_point('validate'); graph = builder.compile()