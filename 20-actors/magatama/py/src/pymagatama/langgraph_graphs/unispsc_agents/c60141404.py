from langgraph.graph import StateGraph, END; from typing import TypedDict; class ToyState(TypedDict): vendor_check: bool; material_compliant: bool; age_grade_verified: bool; def check_material(state: ToyState): state['material_compliant'] = True; return state; def check_safety(state: ToyState): state['age_grade_verified'] = True; return state; graph = StateGraph(ToyState); graph.add_node('material_check', check_material); graph.add_node('safety_check', check_safety); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()