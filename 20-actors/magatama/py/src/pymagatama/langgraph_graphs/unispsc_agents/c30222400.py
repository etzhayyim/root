from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class HospitalStructureState(TypedDict): status: str; safety_checks: Annotated[Sequence[str], operator.add]; structural_integrity: bool; def validate_compliance(state: HospitalStructureState): return {'safety_checks': ['ISO_Compliance_Check', 'Medical_Grade_Material_Check']}; def structural_inspection(state: HospitalStructureState): return {'structural_integrity': True}; graph = StateGraph(HospitalStructureState); graph.add_node('validate', validate_compliance); graph.add_node('inspect', structural_inspection); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()