from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(GraphState); def validate_material(state: GraphState): state['validation_results'].append('Material chemistry verified'); return state; def check_tolerance(state: GraphState): state['validation_results'].append('Tolerance report confirmed'); return state; graph.add_node('material_check', validate_material); graph.add_node('tolerance_check', check_tolerance); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'tolerance_check'); graph.add_edge('tolerance_check', END); graph = graph.compile()