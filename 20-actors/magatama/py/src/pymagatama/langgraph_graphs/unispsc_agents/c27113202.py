from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ToolKitState(TypedDict): items: List[str]; validated: bool;  def validate_kit(state: ToolKitState): return {'validated': all([i != '' for i in state['items']])}; def route_decision(state: ToolKitState): return 'end' if state['validated'] else 'end'; graph = StateGraph(ToolKitState); graph.add_node('validate', validate_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()