from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): device_id: str; specs: dict; verified: bool; def check_compliance(state: State): state['verified'] = 'ISO13485' in state['specs'].get('certs', []); return state; def finalize_order(state: State): return {'verified': True}; graph = StateGraph(State); graph.add_node('compliance', check_compliance); graph.add_node('finalize', finalize_order); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()