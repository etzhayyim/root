from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FrozenFoodState(TypedDict): items: List[str]; inspection_passed: bool; storage_temp: float; graph = StateGraph(FrozenFoodState); def validate_temp(state: FrozenFoodState): return {'inspection_passed': state['storage_temp'] <= -18.0}; graph.add_node('validation', validate_temp); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()