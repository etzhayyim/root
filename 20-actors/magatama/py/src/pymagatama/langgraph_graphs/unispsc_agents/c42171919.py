from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MedBagState(TypedDict): bag_type: str; specs: dict; compliant: bool; def validate_spec(state: MedBagState): state['compliant'] = all(k in state['specs'] for k in ['tensile_strength', 'material']); return state; def check_medical_grade(state: MedBagState): state['compliant'] = state['specs'].get('iso_cert', False); return state; graph = StateGraph(MedBagState); graph.add_node('validate', validate_spec); graph.add_node('cert_check', check_medical_grade); graph.set_entry_point('validate'); graph.add_edge('validate', 'cert_check'); graph.add_edge('cert_check', END); compiled_graph = graph.compile()