from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CurtainWallState(TypedDict): specs: dict; compliance: bool; validation_log: List[str]; def validate_specs(state: CurtainWallState): state['compliance'] = all(k in state['specs'] for k in ['wind_load', 'thermal_uv']); state['validation_log'].append('Specs validated'); return state; def check_regulatory(state: CurtainWallState): state['validation_log'].append('Regulatory check passed'); return state; graph = StateGraph(CurtainWallState); graph.add_node('validate', validate_specs); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()