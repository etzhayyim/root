from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabelState(TypedDict): model_number: str; compatibility_check: bool; specifications: dict; graph = StateGraph(LabelState); def validate_compatibility(state: LabelState): return {'compatibility_check': state['model_number'].startswith('LQ-')}; def finalize_spec(state: LabelState): return {'specifications': {'status': 'verified'}} ; graph.add_node('validate', validate_compatibility); graph.add_node('finalize', finalize_spec); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()