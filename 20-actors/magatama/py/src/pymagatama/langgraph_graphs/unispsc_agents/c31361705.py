from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: AssemblyState): state['compliance'] = 'Material Grade' in state['specs'] and 'Tensile Strength' in state['specs']; state['status'] = 'Validated' if state['compliance'] else 'Failed'; return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()