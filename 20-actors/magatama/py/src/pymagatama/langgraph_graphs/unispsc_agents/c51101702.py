from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): medicine_name: str; validation_passed: bool; certs: List[str]; def validate_gmp(state: DrugState): state['validation_passed'] = 'GMP' in state['certs']; return state; def check_purity(state: DrugState): return state; graph = StateGraph(DrugState); graph.add_node('validate_gmp', validate_gmp); graph.add_node('check_purity', check_purity); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', 'check_purity'); graph.add_edge('check_purity', END); graph = graph.compile()