from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class HazardState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: HazardState): return {'valid': 'ATEX_certification_number' in state['specs'] and 'hazardous_zone_classification' in state['specs']}; def check_safety(state: HazardState): return {'valid': state['valid'] and state['specs'].get('IP_ingress_protection_class', 0) >= 65}; graph = StateGraph(HazardState); graph.add_node('validate', validate_specs); graph.add_node('safety_check', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()