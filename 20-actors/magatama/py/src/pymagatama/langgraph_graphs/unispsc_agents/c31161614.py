from langgraph.graph import StateGraph, END; from typing import TypedDict; class BoltState(TypedDict): spec: dict; verified: bool; def validate_grade(state: BoltState): state['verified'] = 'grade' in state['spec']; return state; def check_cert(state: BoltState): return 'mt_report' in state['spec']; graph = StateGraph(BoltState); graph.add_node('validate', validate_grade); graph.add_node('check_cert', check_cert); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_cert'); graph.add_edge('check_cert', END); graph = graph.compile()