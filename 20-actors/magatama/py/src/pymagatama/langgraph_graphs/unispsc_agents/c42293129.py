from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class MicrosurgeryState(TypedDict): procurement_req: dict; validation_status: bool; def check_certification(state): return {'validation_status': 'ISO_13485_CERT' in state['procurement_req'].get('certs', [])}; def finalize_order(state): return {'validation_status': True}; graph = StateGraph(MicrosurgeryState); graph.add_node('cert_check', check_certification); graph.add_node('finalizer', finalize_order); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'finalizer'); graph.add_edge('finalizer', END); app = graph.compile()