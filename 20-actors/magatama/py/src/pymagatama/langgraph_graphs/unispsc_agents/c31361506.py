from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; specs: dict; valid: bool; def validate_plate(state: AssemblyState): print(f'Validating plate {state["assembly_id"]}'); state['valid'] = 'UV_RESISTANT' in state['specs']; return state; def check_assembly(state: AssemblyState): return 'validate' if state.get('assembly_id') else '__end__'; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_plate); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()