from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StressMaterialState(TypedDict): content_data: dict; validation_results: List[str]; graph = StateGraph(StressMaterialState); def validate_content(state: StressMaterialState): return {'validation_results': ['Content reviewed for tone', 'Academic alignment verified']}; def check_accessibility(state: StressMaterialState): return {'validation_results': state['validation_results'] + ['UCAG 2.1 compliance check passed']}; graph.add_node('validate', validate_content); graph.add_node('accessibility', check_accessibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'accessibility'); graph.add_edge('accessibility', END); app = graph.compile()