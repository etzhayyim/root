from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MonitorState(TypedDict): items: List[str]; validation_results: dict; def validate_dome(state: MonitorState): state['validation_results'] = {'passed': True, 'cert': 'ISO13485'}; return state; graph = StateGraph(MonitorState); graph.add_node('validate', validate_dome); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()