from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): chemical_name: str; purity: float; compliant: bool; workflow_steps: List[str]; def validate_pharmaceutical(state: PharmState): state['compliant'] = state['purity'] >= 99.0; return {'workflow_steps': ['purity_check', 'regulatory_validation']}; def ship_pharmaceutical(state: PharmState): return {'workflow_steps': state['workflow_steps'] + ['cold_chain_dispatch']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_pharmaceutical); graph.add_node('ship', ship_pharmaceutical); graph.set_entry_point('validate'); graph.add_edge('validate', 'ship'); graph.add_edge('ship', END); graph = graph.compile()