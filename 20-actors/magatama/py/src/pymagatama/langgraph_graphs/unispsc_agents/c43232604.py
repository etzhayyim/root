from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CADState(TypedDict): vendor: str; license_model: str; compliance_check: bool; export_control_cleared: bool; def check_export(state: CADState) -> CADState: state['export_control_cleared'] = True; return state; def validate_license(state: CADState) -> CADState: state['compliance_check'] = state['license_model'] in ['Perpetual', 'Subscription']; return state; graph = StateGraph(CADState); graph.add_node('export_check', check_export); graph.add_node('license_validator', validate_license); graph.set_entry_point('export_check'); graph.add_edge('export_check', 'license_validator'); graph.add_edge('license_validator', END); graph = graph.compile()