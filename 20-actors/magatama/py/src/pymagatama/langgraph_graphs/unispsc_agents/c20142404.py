from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: MoldState): results = []; if 'hardness_hrc' not in state['specs']: results.append('Missing hardness specs'); return {'validation_results': results}; def generate_procurement_plan(state: MoldState): return {'validation_results': ['Plan generated']}; graph = StateGraph(MoldState); graph.add_node('validate', validate_specs); graph.add_node('plan', generate_procurement_plan); graph.set_entry_point('validate'); graph.add_edge('validate', 'plan'); graph.add_edge('plan', END); graph = graph.compile()