from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GasRegState(TypedDict): pressure_rating: float; gas_type: str; is_compliant: bool; def validate_pressure(state: GasRegState): state['is_compliant'] = state['pressure_rating'] > 0; return state; def check_compatibility(state: GasRegState): state['is_compliant'] = state['is_compliant'] and (state['gas_type'] in ['O2', 'N2', 'Ar']); return state; graph = StateGraph(GasRegState); graph.add_node('validate', validate_pressure); graph.add_node('compatibility', check_compatibility); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph.set_entry_point('validate'); graph = graph.compile()