from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; validation_status: bool; def validate_specs(state: ProcState): state['validation_status'] = 'Pressure Rating' in state['specs'] and 'Material Specification' in state['specs']; return state; def check_compliance(state: ProcState): return 'compliant' if state['validation_status'] else 'reject'; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()