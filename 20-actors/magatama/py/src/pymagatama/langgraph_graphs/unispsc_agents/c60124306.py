from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PottersWheelState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: PottersWheelState): state['compliance'] = state['specs'].get('motor_power_wattage', 0) > 250; return state; def final_decision(state: PottersWheelState): state['approved'] = state['compliance']; return state; graph = StateGraph(PottersWheelState); graph.add_node('validate', validate_specs); graph.add_node('decide', final_decision); graph.add_edge('validate', 'decide'); graph.add_edge('decide', END); graph.set_entry_point('validate'); graph = graph.compile()