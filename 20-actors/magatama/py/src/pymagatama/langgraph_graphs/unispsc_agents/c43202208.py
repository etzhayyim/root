from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): wire_gauge: str; insulation: str; compliant: bool; graph = StateGraph(AssemblyState); def validate_specs(state: AssemblyState): return {'compliant': state['wire_gauge'] != '' and state['insulation'] != ''}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()