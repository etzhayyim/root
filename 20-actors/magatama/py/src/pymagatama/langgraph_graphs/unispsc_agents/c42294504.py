from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class OphthalmicState(TypedDict): commodity_code: str; spec_data: dict; validation_report: List[str]; def validate_medical_device(state: OphthalmicState): results = []; if 'ISO_13485_certification' not in state['spec_data']: results.append('Missing ISO 13485'); state['validation_report'] = results; return state; graph = StateGraph(OphthalmicState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()