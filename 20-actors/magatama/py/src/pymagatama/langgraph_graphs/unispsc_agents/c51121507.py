from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): chemical_name: str; analysis_result: str; compliance_checked: bool; def check_purity(state: GraphState): return {'compliance_checked': True if 'purity' in state['analysis_result'] else False}; def validate_regulations(state: GraphState): return {'analysis_result': 'Validated for Pharma Use'}; graph = StateGraph(GraphState); graph.add_node('validate', validate_regulations); graph.add_node('check', check_purity); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.set_entry_point('validate'); graph = graph.compile()