from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MedicalDeviceState(TypedDict): device_id: str; specs: dict; compliance_status: bool; graph = StateGraph(MedicalDeviceState); def validate_specs(state: MedicalDeviceState): state['compliance_status'] = 'ISO 13485' in state['specs'].get('certifications', []); return state; def check_sterilization(state: MedicalDeviceState): state['compliance_status'] = state['compliance_status'] and state['specs'].get('sterile', False); return state; graph.add_node('validate', validate_specs); graph.add_node('sterility', check_sterilization); graph.set_entry_point('validate'); graph.add_edge('validate', 'sterility'); graph.add_edge('sterility', END); compiled_graph = graph.compile()