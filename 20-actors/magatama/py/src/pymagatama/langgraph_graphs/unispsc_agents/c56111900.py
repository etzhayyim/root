from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): vendor_id: str; specs: dict; approved: bool; def validate_specs(state: FurnitureState): state['approved'] = state['specs'].get('load_capacity_kg', 0) > 500; return state; graph = StateGraph(FurnitureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()