from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BikeState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(BikeState); def validate_specs(state: BikeState): return {'validation_report': ['Frame integrity confirmed', 'Ergonomic geometry verified']}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()