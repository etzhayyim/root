from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlayHouseState(TypedDict): specs: dict; safety_compliant: bool; def validate_safety(state: PlayHouseState): state['safety_compliant'] = 'iso_8124' in state['specs'].get('certifications', []); return state; def build_workflow(): graph = StateGraph(PlayHouseState); graph.add_node('validate', validate_safety); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = build_workflow()