from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MotorProcurementState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: MotorProcurementState): results = []; motor_kw = state['specs'].get('rated_output_kw', 0); if motor_kw <= 0: results.append('Invalid Output'); return {'validation_results': results}; def generate_procurement_plan(state: MotorProcurementState): return {'status': 'READY_FOR_RFQ'}; graph = StateGraph(MotorProcurementState); graph.add_node('validate', validate_specs); graph.add_node('plan', generate_procurement_plan); graph.set_entry_point('validate'); graph.add_edge('validate', 'plan'); graph.add_edge('plan', END); app = graph.compile()