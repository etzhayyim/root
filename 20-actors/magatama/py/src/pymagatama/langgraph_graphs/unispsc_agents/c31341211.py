from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WaspalloyState(TypedDict): assembly_data: dict; compliance_checks: List[str]; approved: bool; def validate_materials(state: WaspalloyState): state['compliance_checks'].append('AMS_5544_Verified'); return state; def check_aerospace_compliance(state: WaspalloyState): state['approved'] = True; return state; graph = StateGraph(WaspalloyState); graph.add_node('verify_material', validate_materials); graph.add_node('verify_compliance', check_aerospace_compliance); graph.set_entry_point('verify_material'); graph.add_edge('verify_material', 'verify_compliance'); graph.add_edge('verify_compliance', END); graph = graph.compile()