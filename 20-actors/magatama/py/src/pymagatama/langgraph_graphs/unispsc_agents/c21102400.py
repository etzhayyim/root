from langgraph.graph import StateGraph, END; from typing import TypedDict; class IrrigationState(TypedDict): specs: dict; valid: bool; def validate_specs(state: IrrigationState): state['valid'] = 'Operating Pressure Range' in state['specs']; return state; builder = StateGraph(IrrigationState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()