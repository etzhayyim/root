from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_purity(state: AgentState): return {'validation_results': ['Purity check passed']}; def check_regulatory_compliance(state: AgentState): return {'validation_results': ['Compliance status verified']}; graph = StateGraph(AgentState); graph.add_node('purity', validate_purity); graph.add_node('compliance', check_regulatory_compliance); graph.add_edge('purity', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('purity'); graph = graph.compile()