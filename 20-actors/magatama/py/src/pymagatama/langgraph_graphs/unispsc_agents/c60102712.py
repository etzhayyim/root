from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphicState(TypedDict): card_specs: dict; validation_results: List[str]; graph = StateGraph(GraphicState); def validate_cards(state: GraphicState): return {'validation_results': ['Material check passed', 'ASTM F963 compliance verified']}; def finalize_export(state: GraphicState): return {'validation_results': state['validation_results'] + ['Export ready']}; graph.add_node('validate', validate_cards); graph.add_node('finalize', finalize_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()