from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; purity: float; compliant: bool; def validate_purity(state: DrugState): state['compliant'] = state['purity'] >= 99.0; return {'compliant': state['compliant']}; def check_regulation(state: DrugState): print(f'Checking pharma regulation for {state['chemical_name']}'); return {'compliant': True}; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('regulate', check_regulation); graph.add_edge('validate', 'regulate'); graph.add_edge('regulate', END); graph.set_entry_point('validate'); graph = graph.compile()