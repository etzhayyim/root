from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: CastingState): state['validation_results'].append('Dimensions validated against ISO standards'); return state; def check_metallurgy(state: CastingState): state['validation_results'].append('Material composition verified'); return state; graph = StateGraph(CastingState); graph.add_node('validate_dim', validate_dimensions); graph.add_node('check_metal', check_metallurgy); graph.set_entry_point('validate_dim'); graph.add_edge('validate_dim', 'check_metal'); graph.add_edge('check_metal', END); graph = graph.compile()