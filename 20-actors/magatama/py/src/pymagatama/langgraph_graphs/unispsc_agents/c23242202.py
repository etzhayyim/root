from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class HobbingState(TypedDict): specs: dict; validation_report: list; def validate_specs(state: HobbingState): report = []; if state['specs'].get('max_module') > 20: report.append('Oversized module capacity check required'); return {'validation_report': report}; def check_export(state: HobbingState): return {'validation_report': state['validation_report'] + ['Dual-use compliance check: PASS']}; graph = StateGraph(HobbingState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()