from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): input_specs: dict; inspection_results: list; is_approved: bool; def validate_specs(state: ForgingState): return {'inspection_results': ['Dimension Check: OK', 'NDT: Passed']}; def finalize_procurement(state: ForgingState): return {'is_approved': True}; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()