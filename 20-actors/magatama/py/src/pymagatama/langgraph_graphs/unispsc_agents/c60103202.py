from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CubeState(TypedDict): items: List[dict]; validation_results: List[bool]; graph = StateGraph(CubeState); def validate_dimensions(state: CubeState): return {'validation_results': [item.get('size_mm') == 10.0 for item in state['items']]}; graph.add_node('validate', validate_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()