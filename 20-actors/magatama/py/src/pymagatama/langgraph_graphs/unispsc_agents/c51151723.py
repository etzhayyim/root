from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_name: str; grade: str; compliance_docs: List[str]; approved: bool; def validate_purity(state: ProcState): state['approved'] = state.get('purity_percentage', 0) >= 99.0; return state; def check_regulations(state: ProcState): state['compliance_docs'] = ['COA', 'SDS', 'GMP_Certificate']; return state; graph = StateGraph(ProcState); graph.add_node('Validate', validate_purity); graph.add_node('Regulations', check_regulations); graph.set_entry_point('Regulations'); graph.add_edge('Regulations', 'Validate'); graph.add_edge('Validate', END); graph = graph.compile()