from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PayloadState(TypedDict): payload_id: str; structural_integrity: bool; compliance: bool; def validate_specs(state: PayloadState): return {'structural_integrity': True}; def check_compliance(state: PayloadState): return {'compliance': True}; graph = StateGraph(PayloadState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()