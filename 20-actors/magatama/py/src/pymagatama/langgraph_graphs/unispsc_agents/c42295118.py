from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class IrrigationState(TypedDict): input_data: dict; validation_report: list[str]; status: str; def validate_specs(state: IrrigationState): errors = []; spec = state['input_data']; if 'flow_rate' not in spec: errors.append('Missing flow_rate'); return {'validation_report': errors, 'status': 'valid' if not errors else 'invalid'}; graph = StateGraph(IrrigationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()