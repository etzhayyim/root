from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GreenhouseState(TypedDict): specs: dict; valid: bool; def validate_specs(state: GreenhouseState): state['valid'] = 'structural_load_rating' in state['specs'] and 'material' in state['specs']; return state; builder = StateGraph(GreenhouseState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()