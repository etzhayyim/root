from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): content: str; validated: bool; spec_fields: List[str]; def validate_spec(state: ProcState) -> ProcState: state['validated'] = len(state['spec_fields']) >= 3; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()