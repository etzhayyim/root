from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilmState(TypedDict): metadata: dict; quality_check: bool; graph = StateGraph(FilmState); def validate_film(state: FilmState): return {'quality_check': state['metadata'].get('temp') < 25}; def finalize_procurement(state: FilmState): return {'quality_check': True}; graph.add_node('validation', validate_film); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validation'); graph.add_edge('validation', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()