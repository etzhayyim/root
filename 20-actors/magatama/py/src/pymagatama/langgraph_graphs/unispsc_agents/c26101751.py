from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EngineControlState(TypedDict): specs: dict; validation_report: List[str]; approved: bool; def validate_specs(state: EngineControlState): specs = state.get('specs', {}); report = []; is_valid = all([k in specs for k in ['voltage', 'protocol']]); return {'validation_report': report, 'approved': is_valid}; graph = StateGraph(EngineControlState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()