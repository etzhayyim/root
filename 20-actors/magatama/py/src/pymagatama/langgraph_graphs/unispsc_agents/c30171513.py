from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; verified: bool; graph = StateGraph(ProcureState); def validate_specs(state: ProcureState): state['verified'] = 'material_grade' in state['specs'] and 'dimensions_mm' in state['specs']; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()