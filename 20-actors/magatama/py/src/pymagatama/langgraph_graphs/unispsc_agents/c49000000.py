from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: list; status: str; validation_report: dict; def validate_equipment(state: ProcureState): print('Validating sports equipment specs...'); state['validation_report'] = {'status': 'passed', 'compliance': True}; return state; def finalize_order(state: ProcureState): state['status'] = 'ready_for_procurement'; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_equipment); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()