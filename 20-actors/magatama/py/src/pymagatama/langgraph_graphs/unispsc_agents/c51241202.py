from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): chemical_name: str; purity: float; validated: bool; def check_purity(state: ProcureState): state['validated'] = state['purity'] >= 99.0; return state; def finalize_order(state: ProcureState): return {'status': 'approved' if state['validated'] else 'rejected'}; graph = StateGraph(ProcureState); graph.add_node('validate', check_purity); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()