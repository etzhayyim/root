from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolStorageState(TypedDict): cabinet_id: str; specifications: dict; validation_passed: bool; def validate_cabinet(state: ToolStorageState): specs = state.get('spec_fields', {}); state['validation_passed'] = 'weight_capacity' in specs and 'locking_type' in specs; return state; graph = StateGraph(ToolStorageState); graph.add_node('validate', validate_cabinet); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()