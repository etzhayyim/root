from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DosingPumpState(TypedDict): part_id: str; specs: dict; valid: bool; def validate_specs(state: DosingPumpState): state['valid'] = state['specs'].get('pressure_rating', 0) > 0; return state; def check_compatibility(state: DosingPumpState): state['valid'] = state['valid'] and 'chemical_grade' in state['specs']; return state; builder = StateGraph(DosingPumpState); builder.add_node('validation', validate_specs); builder.add_node('compatibility', check_compatibility); builder.set_entry_point('validation'); builder.add_edge('validation', 'compatibility'); builder.add_edge('compatibility', END); graph = builder.compile()