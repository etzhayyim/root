from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SuitProcurementState(TypedDict): data: dict; errors: Annotated[list, operator.add]; def validate_specs(state): state['data']['validated'] = True; return state; def check_compliance(state): if 'fabric' not in state['data']: state['errors'].append('Missing fabric info'); return state; graph = StateGraph(SuitProcurementState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()