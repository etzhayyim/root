from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine: str; status: str; batch_id: str; qc_passed: bool; def validate_batch(state: PharmState): return {'qc_passed': True} if state['batch_id'] else {'qc_passed': False}; def release_shipment(state: PharmState): return {'status': 'Approved'} if state['qc_passed'] else {'status': 'Quarantined'}; graph = StateGraph(PharmState); graph.add_node('validate', validate_batch); graph.add_node('release', release_shipment); graph.set_entry_point('validate'); graph.add_edge('validate', 'release'); graph.add_edge('release', END); compiled_graph = graph.compile()