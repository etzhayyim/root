from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TableClothState(TypedDict): dimensions: dict; material: str; quality_score: float; approved: bool; def validate_specs(state: TableClothState): if state['material'] == 'polyester' and state['quality_score'] > 0.8: return {'approved': True} else: return {'approved': False}; workflow = StateGraph(TableClothState); workflow.add_node('validate', validate_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()