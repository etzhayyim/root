from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class NutritionState(TypedDict): items: List[dict]; shelf_life_valid: bool; certification_passed: bool; def validate_shelf_life(state: NutritionState): state['shelf_life_valid'] = all(i.get('expiry') > '2024-01-01' for i in state['items']); return state; def check_compliance(state: NutritionState): state['certification_passed'] = all(i.get('cert') == 'ISO' for i in state['items']); return state; graph = StateGraph(NutritionState); graph.add_node('validate', validate_shelf_life); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()