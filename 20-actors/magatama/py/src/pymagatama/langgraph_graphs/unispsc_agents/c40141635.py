from langgraph.graph import StateGraph, END; from typing import TypedDict; class TurbineState(TypedDict): spec: dict; validated: bool; graph = StateGraph(TurbineState); def validate_specs(state: TurbineState): state['validated'] = all(k in state['spec'] for k in ['pressure', 'material']); return state; def check_export(state: TurbineState): return {'validated': state['validated'] and True}; graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()