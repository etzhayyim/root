from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MotorState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: MotorState): specs = state['specs']; is_valid = all(k in specs for k in ['kW', 'voltage', 'ip_rating']); return {'valid': is_valid, 'error': '' if is_valid else 'Missing mandatory specs'}; def compile_graph(): workflow = StateGraph(MotorState); workflow.add_node('validator', validate_specs); workflow.set_entry_point('validator'); workflow.add_edge('validator', END); return workflow.compile(); graph = compile_graph()