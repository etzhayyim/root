from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): specs: dict; validation_passed: bool; async def validate_chemical_specs(state: AgentState): return {'validation_passed': 'CAS_number' in state['specs'] and 'purity' in state['specs']}; async def regulatory_check(state: AgentState): return {'validation_passed': state['validation_passed'] and 'export_license' in state['specs']}; graph = StateGraph(AgentState); graph.add_node('validate', validate_chemical_specs); graph.add_node('regulatory', regulatory_check); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); graph = graph.compile()