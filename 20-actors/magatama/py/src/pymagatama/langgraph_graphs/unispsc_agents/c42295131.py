from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_docs: List[str]; compliance_status: bool; workflow_log: List[str]; def validate_medical_grade(state: ProcurementState): state['compliance_status'] = 'ISO13485' in state['vendor_docs']; state['workflow_log'].append('Validation: Material compliance confirmed.'); return state; def check_load_limit(state: ProcurementState): state['workflow_log'].append('Validation: Load capacity verification complete.'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate_spec', validate_medical_grade); graph.add_node('load_check', check_load_limit); graph.set_entry_point('validate_spec'); graph.add_edge('validate_spec', 'load_check'); graph.add_edge('load_check', END); graph = graph.compile()