from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TVSpecState(TypedDict): tv_model: str; specs: dict; valid: bool; def validate_specs(state: TVSpecState): state["valid"] = "resolution" in state["specs"] and "energy_rating" in state["specs"]; return state; graph = StateGraph(TVSpecState); graph.add_node("validate", validate_specs); graph.set_entry_point("validate"); graph.add_edge("validate", END); compiled_graph = graph.compile()