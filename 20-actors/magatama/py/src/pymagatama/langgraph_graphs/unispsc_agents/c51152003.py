from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine_name: str; temp_log: List[float]; valid: bool; def validate_cold_chain(state: PharmState): state['valid'] = all(2 <= t <= 8 for t in state['temp_log']); return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_cold_chain); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()