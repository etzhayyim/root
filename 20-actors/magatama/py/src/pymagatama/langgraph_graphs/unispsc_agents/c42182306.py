from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class NeuroState(TypedDict): device_id: str; validation_passed: bool; clinical_data_valid: bool; def validate_device(state: NeuroState): state['validation_passed'] = True; return state; def check_clinical_integrity(state: NeuroState): state['clinical_data_valid'] = True; return state; graph = StateGraph(NeuroState); graph.add_node('validate', validate_device); graph.add_node('clinical_check', check_clinical_integrity); graph.set_entry_point('validate'); graph.add_edge('validate', 'clinical_check'); graph.add_edge('clinical_check', END); graph = graph.compile()