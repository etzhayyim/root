from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_id: str; compliance_checked: bool; supply_chain_verified: bool; def check_compliance(state: PharmState): return {'compliance_checked': True}; def verify_logistics(state: PharmState): return {'supply_chain_verified': True}; graph = StateGraph(PharmState); graph.add_node('compliance', check_compliance); graph.add_node('logistics', verify_logistics); graph.add_edge('compliance', 'logistics'); graph.add_edge('logistics', END); graph.set_entry_point('compliance'); graph = graph.compile()