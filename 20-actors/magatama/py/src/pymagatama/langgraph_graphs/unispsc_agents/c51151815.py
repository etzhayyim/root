from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_name: str; analysis_result: str; status: str; def validate_purity(state: ProcureState): return {'analysis_result': 'PASS' if '99%' in state['chemical_name'] else 'FAIL', 'status': 'VALIDATED'}; def generate_compliance_report(state: ProcureState): return {'status': 'COMPLIANT' if state['analysis_result'] == 'PASS' else 'REJECTED'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_purity); graph.add_node('report', generate_compliance_report); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()