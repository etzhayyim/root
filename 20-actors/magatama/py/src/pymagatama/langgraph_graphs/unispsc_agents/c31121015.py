from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LeadCastState(TypedDict): material_cert: str; dimensions_ok: bool; toxicity_report: str; approved: bool; def validate_casting(state: LeadCastState): return {'dimensions_ok': True} if 'spec' in state['material_cert'] else {'dimensions_ok': False}; def check_compliance(state: LeadCastState): return {'approved': True} if 'compliant' in state['toxicity_report'] else {'approved': False}; graph = StateGraph(LeadCastState); graph.add_node('validate', validate_casting); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()