from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: CastState): state['validation_results'].append('Material chemistry verified'); return state; def check_dimensions(state: CastState): state['validation_results'].append('Dimensional accuracy confirmed'); return state; graph = StateGraph(CastState); graph.add_node('chemistry_check', validate_material); graph.add_node('dimension_check', check_dimensions); graph.set_entry_point('chemistry_check'); graph.add_edge('chemistry_check', 'dimension_check'); graph.add_edge('dimension_check', END); graph = graph.compile()