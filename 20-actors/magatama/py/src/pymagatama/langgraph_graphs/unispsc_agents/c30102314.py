from langgraph.graph import StateGraph, END; from typing import TypedDict; class LeadProcurementState(TypedDict): purity: float; dimensions: dict; compliant: bool; def validate_purity(state: LeadProcurementState): state['compliant'] = state['purity'] >= 99.9; return state; def check_safety(state: LeadProcurementState): print('Checking hazardous material compliance...'); return state; graph = StateGraph(LeadProcurementState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()