from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ThoracentesisState(TypedDict): items: List[str]; qc_passed: bool; def validate_sterile_spec(state: ThoracentesisState): state['qc_passed'] = all('sterile' in item.lower() for item in state['items']); return state; def route_verification(state: ThoracentesisState): return 'qc_check' if state['qc_passed'] else END; graph = StateGraph(ThoracentesisState); graph.add_node('qc_check', validate_sterile_spec); graph.set_entry_point('qc_check'); graph.add_edge('qc_check', END); graph = graph.compile()