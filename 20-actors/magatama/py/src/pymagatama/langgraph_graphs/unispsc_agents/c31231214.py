from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlateState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_material(state: PlateState): state['valid'] = 'grade' in state['specs'] and state['specs']['grade'] is not None; return state; def check_compliance(state: PlateState): state['log'].append('Checking AMS standards'); return state; graph = StateGraph(PlateState); graph.add_node('validate', validate_material); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()