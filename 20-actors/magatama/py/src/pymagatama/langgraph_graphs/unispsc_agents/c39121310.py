from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): box_type: str; specs: dict; compliant: bool; def validate_specs(state: State): state['compliant'] = state['specs'].get('ip_rating', 0) >= 54; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()