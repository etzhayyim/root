from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabBenchState(TypedDict): vendor_id: str; specifications: dict; compliance_check: bool; def validate_specs(state: LabBenchState): state['compliance_check'] = state['specifications'].get('chemical_resistance') == 'Grade-A'; return state; def finalize_order(state: LabBenchState): return {'status': 'processed'}; graph = StateGraph(LabBenchState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()