from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtrusionState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ExtrusionState): state['valid'] = all(k in state['specs'] for k in ['alloy', 'tolerance']); return state; def route_logic(state: ExtrusionState): return 'validate' if state['valid'] else END; graph = StateGraph(ExtrusionState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()