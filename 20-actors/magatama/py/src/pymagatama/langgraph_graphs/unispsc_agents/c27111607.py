from langgraph.graph import StateGraph, END; from typing import TypedDict; class SwageState(TypedDict): specs: dict; valid: bool; def validate_specs(state: SwageState): state['valid'] = all(k in state['specs'] for k in ['force', 'diameter']); return state; graph = StateGraph(SwageState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()