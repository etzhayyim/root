from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StructureState(TypedDict): material_type: str; specs: dict; valid: bool; def validate_specs(state: StructureState): state['valid'] = 'ASTM_ISO_Standard' in state['specs'] and state['specs']['Material Grade'] is not None; return state; graph = StateGraph(StructureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()