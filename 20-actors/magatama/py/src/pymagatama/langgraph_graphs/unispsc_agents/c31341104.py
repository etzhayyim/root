from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InconelState(TypedDict): ntd_results: str; spec_compliance: bool; export_control_cleared: bool; def validate_metallurgy(state: InconelState): state['spec_compliance'] = len(state['ntd_results']) > 0; return state; def check_export(state: InconelState): state['export_control_cleared'] = True; return state; graph = StateGraph(InconelState); graph.add_node('metallurgy_check', validate_metallurgy); graph.add_node('export_check', check_export); graph.add_edge('metallurgy_check', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('metallurgy_check'); graph = graph.compile()