from langgraph.graph import StateGraph, END; from typing import TypedDict; class DentalTongsState(TypedDict): status: str; spec: dict; validated: bool; def validate_spec(state: DentalTongsState): state['validated'] = 'material' in state['spec'] and state['spec']['material'] == 'SS' return state; def route_logic(state: DentalTongsState): return 'validate' if not state['validated'] else END; graph = StateGraph(DentalTongsState); graph.add_node('validate', validate_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()