from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): specs: dict; compliance_cleared: bool; CAD_verified: bool; def check_specs(state: MachineState): return {'compliance_cleared': len(state['specs']) > 3}; def verify_cad(state: MachineState): return {'CAD_verified': True}; graph = StateGraph(MachineState); graph.add_node('check_specs', check_specs); graph.add_node('verify_cad', verify_cad); graph.set_entry_point('check_specs'); graph.add_edge('check_specs', 'verify_cad'); graph.add_edge('verify_cad', END); graph = graph.compile()