from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VehicleState(TypedDict): vehicle_id: str; specs: dict; clearance: bool; def validate_police_specs(state: VehicleState): state['clearance'] = 'radio' in state['specs'] and 'lightbar' in state['specs']; return state; def check_export_controls(state: VehicleState): state['clearance'] = state['clearance'] and state.get('compliance_check', False); return state; graph = StateGraph(VehicleState); graph.add_node('validate', validate_police_specs); graph.add_node('export', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()