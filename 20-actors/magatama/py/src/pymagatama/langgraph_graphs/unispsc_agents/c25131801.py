from langgraph.graph import StateGraph, END; from typing import TypedDict; class AirshipState(TypedDict): spec: dict; verified: bool; graph = StateGraph(AirshipState); def validate_specs(state: AirshipState): state['verified'] = all(k in state['spec'] for k in ['cert', 'payload']); return state; def check_export(state: AirshipState): return 'end'; graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph.compile()