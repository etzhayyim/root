from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TapeState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: TapeState): required = ['dielectric_strength', 'flame_rating']; state['compliance'] = all(k in state['specs'] for k in required); return state; def route_status(state: TapeState): return 'approved' if state['compliance'] else 'rejected'; graph = StateGraph(TapeState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()