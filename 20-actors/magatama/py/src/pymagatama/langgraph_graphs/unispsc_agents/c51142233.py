from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CodeineState(TypedDict): vendor_license: str; quantity: float; compliance_check: bool; def verify_regulatory_status(state: CodeineState) -> CodeineState: state['compliance_check'] = len(state['vendor_license']) > 10; return state; def validate_distribution(state: CodeineState) -> CodeineState: if not state['compliance_check']: raise ValueError('Invalid License'); return state; graph = StateGraph(CodeineState); graph.add_node('verify', verify_regulatory_status); graph.add_node('validate', validate_distribution); graph.set_entry_point('verify'); graph.add_edge('verify', 'validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()