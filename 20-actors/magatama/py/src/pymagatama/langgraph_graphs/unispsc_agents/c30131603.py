from typing import TypedDict, List; from langgraph.graph import StateGraph; class ConcreteState(TypedDict): specs: dict; validation_results: list; graph = StateGraph(ConcreteState); def validate_compression(state: ConcreteState): return {'validation_results': ['Compression test passed'] if state['specs'].get('strength', 0) >= 20 else ['Compression test failed']}; graph.add_node('validate_compression', validate_compression); graph.set_entry_point('validate_compression'); graph.set_finish_point('validate_compression'); app = graph.compile()