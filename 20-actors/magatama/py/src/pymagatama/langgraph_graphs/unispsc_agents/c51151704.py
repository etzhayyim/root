from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_data: dict; compliance_checks: List[str]; def validate_purity(state: PharmState): state['compliance_checks'].append('Purity Verified'); return state; def check_storage(state: PharmState): state['compliance_checks'].append('Storage Validated'); return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph = graph.compile()