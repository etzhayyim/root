from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GypsumState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(GypsumState); def validate_specs(state: GypsumState): report = []; if state['specs'].get('thickness', 0) < 9.5: report.append('Thickness below standard'); return {'validation_report': report}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()