from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoxingOrderState(TypedDict): items: List[str]; inspection_required: bool; approved: bool; def validate_equipment(state: BoxingOrderState) -> BoxingOrderState: state['inspection_required'] = True; return state; def final_approve(state: BoxingOrderState) -> BoxingOrderState: state['approved'] = True; return state; graph = StateGraph(BoxingOrderState); graph.add_node('validate', validate_equipment); graph.add_node('approve', final_approve); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.compile()