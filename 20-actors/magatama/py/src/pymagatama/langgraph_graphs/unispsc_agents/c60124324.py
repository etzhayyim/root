from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClayKitState(TypedDict): kit_components: List[str]; safety_verified: bool; compliance_docs: List[str]; def validate_standards(state: ClayKitState): return {'safety_verified': True} if 'ASTM_D4236' in state['compliance_docs'] else {'safety_verified': False}; def assemble_kit(state: ClayKitState): return {'kit_components': state['kit_components'] + ['modeling_clay', 'sculpting_tools']}; graph = StateGraph(ClayKitState); graph.add_node('validate', validate_standards); graph.add_node('assemble', assemble_kit); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph = graph.compile()