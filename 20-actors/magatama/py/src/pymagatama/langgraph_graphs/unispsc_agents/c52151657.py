from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): status: str; specs: dict; valid: bool; def validate_torch(state: ProcureState): state['valid'] = 'Fuel type' in state['specs'] and state['specs'].get('safety_lock', False); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_torch); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()