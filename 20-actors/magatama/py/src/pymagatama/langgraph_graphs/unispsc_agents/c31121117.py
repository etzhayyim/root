from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): input_specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_casting_specs(state: ProcurementState): spec = state['input_specs']; errors = []; if spec.get('tolerance') > 0.05: errors.append('Tolerance exceeds precision limit'); return {'validation_results': errors, 'status': 'validated' if not errors else 'rejected'}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_casting_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()