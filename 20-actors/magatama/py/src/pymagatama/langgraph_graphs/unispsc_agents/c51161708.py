from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): cold_chain_verified: bool; inventory_check: bool; compliance_status: bool; def verify_cold_chain(state: DrugProcurementState): state['cold_chain_verified'] = True; return state; def check_compliance(state: DrugProcurementState): state['compliance_status'] = True; return state; graph = StateGraph(DrugProcurementState); graph.add_node('verify', verify_cold_chain); graph.add_node('compliance', check_compliance); graph.add_edge('verify', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('verify'); graph = graph.compile()