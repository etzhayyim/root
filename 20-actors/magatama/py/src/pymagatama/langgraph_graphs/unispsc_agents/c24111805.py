from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class TankState(TypedDict): messages: Annotated[list, add_messages]; spec_data: dict; approval_status: bool; def validate_spec(state: TankState): print('Validating chemical tank material specs...'); state['approval_status'] = 'compatible' in state['spec_data']; return state; def hazard_check(state: TankState): print('Performing HAZMAT safety compliance check...'); return state; graph = StateGraph(TankState); graph.add_node('validation', validate_spec); graph.add_node('hazmat_check', hazard_check); graph.set_entry_point('validation'); graph.add_edge('validation', 'hazmat_check'); graph.add_edge('hazmat_check', END); graph = graph.compile()