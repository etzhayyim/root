from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DentalToolState(TypedDict): tool_name: str; specs: dict; approved: bool; graph = StateGraph(DentalToolState); def validate_spec(state: DentalToolState): return {'approved': 'Material Grade' in state['specs']}; def finalize(state: DentalToolState): return {'approved': True}; graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()