from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): instrument_id: str; sterilization_status: bool; compliance_checked: bool; def validate_instrument(state: SurgicalState): return {'compliance_checked': True}; def update_inventory(state: SurgicalState): return {'instrument_id': 'validated_' + state['instrument_id']}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_instrument); graph.add_node('inventory', update_inventory); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph.set_entry_point('validate'); graph = graph.compile()