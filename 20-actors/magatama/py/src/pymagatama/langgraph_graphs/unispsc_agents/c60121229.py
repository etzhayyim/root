from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BrushState(TypedDict): specs: dict; valid: bool; def validate_specs(state: BrushState): state['valid'] = 'Bristle Material' in state['specs'] and 'Chemical Resistance Rating' in state['specs']; return state; builder = StateGraph(BrushState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()