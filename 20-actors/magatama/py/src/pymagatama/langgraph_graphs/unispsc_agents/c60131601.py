from langgraph.graph import StateGraph, END; from typing import TypedDict; class RhythmState(TypedDict): items: list; valid: bool; def validate_instrument(state: RhythmState): state['valid'] = all(i.get('safety') == 'certified' for i in state['items']); return state; graph = StateGraph(RhythmState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()