from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): dimensions: dict; material_cert: bool; inspection_result: bool; graph = StateGraph(ForgingState); def validate_spec(state: ForgingState): return {'inspection_result': True if state['material_cert'] else False}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()