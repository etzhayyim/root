from langgraph.graph import StateGraph, END; from typing import TypedDict; class HeaterState(TypedDict): spec: dict; verified: bool; def validate_specs(state: HeaterState): state['verified'] = all(k in state['spec'] for k in ['kw_rating', 'voltage', 'safety_std']); return state; graph = StateGraph(HeaterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()