from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_data: dict; compliance_status: bool; validation_log: List[str]; def validate_purity(state: PharmState): purity = state['chemical_data'].get('purity', 0); state['compliance_status'] = purity >= 99.0; state['validation_log'].append('Purity check complete'); return state; def check_docs(state: PharmState): state['validation_log'].append('COA Verified'); return state; graph = StateGraph(PharmState); graph.add_node('purity_check', validate_purity); graph.add_node('doc_check', check_docs); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'doc_check'); graph.add_edge('doc_check', END); graph = graph.compile()