from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: ForgingState): state['valid'] = all(k in state['specs'] for k in ['grade', 'tolerance', 'ut_report']); state['logs'].append('Validation complete'); return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()