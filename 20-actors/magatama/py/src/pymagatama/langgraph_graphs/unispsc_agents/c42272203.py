from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BiLevelState(TypedDict): device_id: str; specs: dict; valid: bool; def check_medical_compliance(state: BiLevelState): state['valid'] = 'ISO_13485_Compliance' in state['specs']; return state; def validate_calibration(state: BiLevelState): state['valid'] = state.get('valid') and state['specs'].get('Calibration_Certification') is not None; return state; graph = StateGraph(BiLevelState); graph.add_node('compliance', check_medical_compliance); graph.add_node('calibration', validate_calibration); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'calibration'); graph.add_edge('calibration', END); graph = graph.compile()