from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GamePlatformState(TypedDict): vendor_url: str; compliance_checks: List[str]; approved: bool; def check_api(state: GamePlatformState): return {'compliance_checks': ['API_v2_valid']}; def validate_compliance(state: GamePlatformState): return {'approved': True}; graph = StateGraph(GamePlatformState); graph.add_node('check_api', check_api); graph.add_node('compliance', validate_compliance); graph.set_entry_point('check_api'); graph.add_edge('check_api', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()