from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EndoState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(EndoState); def validate_specs(state: EndoState): result = 'Valid' if state['specs'].get('sterilization_method') else 'Missing Certification'; state['validation_results'].append(result); return state; def check_compatibility(state: EndoState): state['validation_results'].append('Compatibility Verified'); return state; graph.add_node('validate', validate_specs); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); app = graph.compile()