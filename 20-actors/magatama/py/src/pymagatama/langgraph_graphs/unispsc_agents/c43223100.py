from langgraph.graph import StateGraph, END; from typing import TypedDict; class NetworkSpecState(TypedDict): spec: dict; compliance_check: bool; graph = StateGraph(NetworkSpecState); def validate_specs(state: NetworkSpecState): state['compliance_check'] = 'Frequency Band' in state['spec'] and 'Protocol' in state['spec']; return {'compliance_check': state['compliance_check']}; def route_procurement(state: NetworkSpecState): return 'approval' if state['compliance_check'] else 'revision'; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END)