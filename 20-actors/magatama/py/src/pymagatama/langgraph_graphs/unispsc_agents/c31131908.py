from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgeState(TypedDict): specs: dict; validation: bool; def validate_specs(state: ForgeState): state['validation'] = all(k in state['specs'] for k in ['alloy', 'dimensions']); return state; def check_export(state: ForgeState): print('Checking export compliance...'); return state; graph = StateGraph(ForgeState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()