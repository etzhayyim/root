from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkBoxState(TypedDict): box_id: str; specs: dict; validation_report: str; def validate_specs(state: NetworkBoxState): return {'validation_report': 'Spec compliant' if 'IP rating' in state['specs'] else 'Missing IP rating'}; def route_step(state: NetworkBoxState): return 'end' if state['validation_report'] == 'Spec compliant' else 'end'; graph = StateGraph(NetworkBoxState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()