from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PEGState(TypedDict): device_id: str; compliance_docs: List[str]; approved: bool; def check_compliance(state: PEGState): state['approved'] = all(doc in state['compliance_docs'] for doc in ['ISO10993', 'SterilityCert']); return {'approved': state['approved']}; graph = StateGraph(PEGState); graph.add_node('compliance', check_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', END); graph = graph.compile()