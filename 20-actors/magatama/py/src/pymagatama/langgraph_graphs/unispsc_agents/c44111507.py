from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BookEndState(TypedDict): items: Annotated[list, operator.add]; validation_results: dict; def validate_specs(state: BookEndState): return {'validation_results': {'status': 'approved'}}; def procurement_approval(state: BookEndState): return {'items': ['Purchase Order Generated']}; graph = StateGraph(BookEndState); graph.add_node('validate', validate_specs); graph.add_node('approve', procurement_approval); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()