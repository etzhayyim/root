from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RefractoryState(TypedDict): specs: dict; compliance: bool; safety_check: str; graph = StateGraph(RefractoryState); def validate_thermal_grade(state: RefractoryState): return {'compliance': state['specs'].get('temp_limit', 0) > 1500}; def check_safety(state: RefractoryState): return {'safety_check': 'PASS'} if state['compliance'] else {'safety_check': 'FAIL'}; graph.add_node('validate', validate_thermal_grade); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()