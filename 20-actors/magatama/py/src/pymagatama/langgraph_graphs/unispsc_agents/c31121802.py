from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_alloy(state: CastingState): state['status'] = 'validation'; if 'composition' not in state['specs']: state['validation_errors'].append('Missing alloy composition'); return state; def check_dimensional_accuracy(state: CastingState): if state.get('status') == 'validation': state['status'] = 'dimension_check'; return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_alloy); graph.add_node('dimension_check', check_dimensional_accuracy); graph.set_entry_point('validate'); graph.add_edge('validate', 'dimension_check'); graph.add_edge('dimension_check', END); compiled_graph = graph.compile()