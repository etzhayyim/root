from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ControlState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: ControlState): specs = state['specs']; state['valid'] = 'voltage' in specs and 'compliance' in specs; return state; graph = StateGraph(ControlState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()