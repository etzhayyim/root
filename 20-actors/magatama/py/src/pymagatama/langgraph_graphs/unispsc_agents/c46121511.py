from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MissileState(TypedDict): vendor_clearance: bool; export_license: bool; technical_spec: dict; graph = StateGraph(MissileState); def check_compliance(state: MissileState): return {'vendor_clearance': True} if state.get('vendor_clearance') else {'vendor_clearance': False}; def validate_license(state: MissileState): return {'export_license': True}; graph.add_node('compliance', check_compliance); graph.add_node('export', validate_license); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()