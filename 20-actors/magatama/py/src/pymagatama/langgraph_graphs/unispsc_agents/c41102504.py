from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): items: List[str]; validated: bool; def validate_pins(state: SpecState): state['validated'] = all('pin' in item.lower() for item in state['items']); return state; graph = StateGraph(SpecState); graph.add_node('validate', validate_pins); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()