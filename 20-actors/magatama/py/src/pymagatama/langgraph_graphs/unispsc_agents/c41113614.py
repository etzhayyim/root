from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): data: dict; status: str; def validate_em_spec(state: GraphState): spec = state['data']; is_valid = 'frequency_range' in spec and 'calibration' in spec; return {'status': 'validated' if is_valid else 'rejected'}; def process_export_control(state: GraphState): return {'status': 'cleared_for_export'}; graph = StateGraph(GraphState); graph.add_node('validate', validate_em_spec); graph.add_node('compliance', process_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END);