from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): chemical_name: str; validation_status: bool; compliance_docs: List[str]; def validate_api(state: PharmaState): state['validation_status'] = True; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_api); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()