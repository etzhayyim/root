from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DentalStoneState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(DentalStoneState); def validate_iso_compliance(state: DentalStoneState): return {'validation_results': ['ISO 6873 verified']}; def check_moisture_storage(state: DentalStoneState): return {'validation_results': ['Storage specs met']}; graph.add_node('validate_iso', validate_iso_compliance); graph.add_node('check_storage', check_moisture_storage); graph.set_entry_point('validate_iso'); graph.add_edge('validate_iso', 'check_storage'); graph.add_edge('check_storage', END); app = graph.compile()