from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpoonState(TypedDict): items: List[dict]; validation_results: List[bool]; def validate_material(state: SpoonState): return {'validation_results': [item.get('material') == 'stainless_steel' for item in state['items']]}; def approve_order(state: SpoonState): print('Spoon order validated.'); return {}; graph = StateGraph(SpoonState); graph.add_node('validate', validate_material); graph.add_node('approve', approve_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()