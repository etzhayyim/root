from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IdeaBookState(TypedDict): content: str; specs: dict; approved: bool; def validate_specs(state: IdeaBookState): specs = state.get('specs', {}); state['approved'] = 'paper_gsm' in specs and 'binding_type' in specs; return state; graph = StateGraph(IdeaBookState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()