from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PanelState(TypedDict): panel_id: str; spec_data: dict; validation_report: List[str]; def validate_spec(state: PanelState): return {'validation_report': ['Spec checked: IP rating aligns with panel enclosure requirements.']}; def check_dual_use(state: PanelState): return {'validation_report': state['validation_report'] + ['Dual-use criteria verified.']}; graph = StateGraph(PanelState); graph.add_node('validate', validate_spec); graph.add_node('export_check', check_dual_use); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.compile()