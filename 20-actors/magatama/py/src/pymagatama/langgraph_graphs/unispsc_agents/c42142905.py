from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): sunglasses_specs: dict; inspection_result: bool; def validate_uv(state: State): state['inspection_result'] = state['sunglasses_specs'].get('uv_rating') >= 400; return state; def check_durability(state: State): return {'inspection_result': state['sunglasses_specs'].get('impact_test', False)}; graph = StateGraph(State); graph.add_node('validate_uv', validate_uv); graph.add_node('durability', check_durability); graph.set_entry_point('validate_uv'); graph.add_edge('validate_uv', 'durability'); graph.add_edge('durability', END); compiled_graph = graph.compile()