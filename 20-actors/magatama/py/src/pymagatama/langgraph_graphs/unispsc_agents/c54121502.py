from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class EmeraldState(TypedDict): gem_data: dict; validation_logs: List[str]; def validate_authenticity(state: EmeraldState): state['validation_logs'].append('Verifying GIA certificate'); return state; def verify_value(state: EmeraldState): state['validation_logs'].append('Checking market value consistency'); return state; graph = StateGraph(EmeraldState); graph.add_node('cert_check', validate_authenticity); graph.add_node('value_check', verify_value); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'value_check'); graph.add_edge('value_check', END); graph = graph.compile()