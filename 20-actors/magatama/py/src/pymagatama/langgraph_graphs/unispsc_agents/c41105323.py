from langgraph.graph import StateGraph, END; from typing import TypedDict; class BufferState(TypedDict): buffer_type: str; purity_check: bool; final_report: str; def validate_purity(state: BufferState): return {'purity_check': True} if state['buffer_type'] else {'purity_check': False}; def generate_report(state: BufferState): return {'final_report': 'Validated Buffer Batch Ready'}; graph = StateGraph(BufferState); graph.add_node('validate', validate_purity); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled = graph.compile()