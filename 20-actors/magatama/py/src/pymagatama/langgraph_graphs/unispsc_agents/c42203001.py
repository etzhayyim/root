from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LinearAcceleratorState(TypedDict): device_id: str; safety_checks: List[str]; status: str; def validate_safety(state: LinearAcceleratorState): state['safety_checks'].append('Radiation Shielding Verified'); return {'status': 'Safety Validated'}; def run_compliance_check(state: LinearAcceleratorState): state['safety_checks'].append('Regulatory Compliance Confirmed'); return {'status': 'Ready for Deployment'}; graph = StateGraph(LinearAcceleratorState); graph.add_node('safety', validate_safety); graph.add_node('compliance', run_compliance_check); graph.add_edge('safety', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('safety'); graph = graph.compile()