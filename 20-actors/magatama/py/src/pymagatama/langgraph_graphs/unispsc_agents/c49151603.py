from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HockeyProcurementState(TypedDict): specs: dict; valid: bool; workflow_log: List[str]; def validate_specs(state: HockeyProcurementState): state['valid'] = all(k in state['specs'] for k in ['flex', 'length']); state['workflow_log'].append('Validation complete'); return state; graph = StateGraph(HockeyProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()