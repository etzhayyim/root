from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class ImpactGraphState(TypedDict): sensor_data: dict; validation_results: List[str]; def validate_impact_specs(state: ImpactGraphState): return {'validation_results': ['Compliance Verified'] if state['sensor_data'].get('sensitivity') > 0 else ['Validation Failed']}; def execute_safety_logic(state: ImpactGraphState): return {'validation_results': state['validation_results'] + ['Logic Compiled']}; graph = StateGraph(ImpactGraphState); graph.add_node('validate', validate_impact_specs); graph.add_node('execute', execute_safety_logic); graph.set_entry_point('validate'); graph.add_edge('validate', 'execute'); graph.add_edge('execute', END); graph = graph.compile()