from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): device_id: str; compliance_passed: bool; specs: dict; def validate_medical_device(state: ProcureState): state['compliance_passed'] = 'FDA_id' in state['specs']; return state; def finalize_order(state: ProcureState): return {'status': 'approved'} if state['compliance_passed'] else {'status': 'rejected'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_medical_device); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()