from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalToolState(TypedDict): tool_type: str; is_sterile: bool; specs: List[str]; def validate_specs(state: SurgicalToolState): return {'specs': state['specs']} if state['is_sterile'] else {'specs': ['REJECTED']}; graph = StateGraph(SurgicalToolState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()