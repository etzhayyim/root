from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_certs: List[str]; material: str; compliant: bool; def validate_spec(state: ProcureState): return {'compliant': all(['ISO13485' in state['vendor_certs'], state['material'] == 'Non-woven'])}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()