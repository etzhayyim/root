from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; temperature: float; def validate_temp(state: State): return {'status': 'pass' if state['temperature'] <= -18 else 'fail'}; graph = StateGraph(State); graph.add_node('validate', validate_temp); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()