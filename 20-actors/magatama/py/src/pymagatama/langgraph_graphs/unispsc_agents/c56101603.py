from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class TableState(TypedDict): specs: dict; compliance: bool; flow: List[str]; def validate_specs(state: TableState): state['compliance'] = all(k in state['specs'] for k in ['material', 'weight_capacity']); state['flow'].append('Specs validated'); return state; def check_durability(state: TableState): state['flow'].append('Durability assessment complete'); return state; graph = StateGraph(TableState); graph.add_node('validate', validate_specs); graph.add_node('durability', check_durability); graph.set_entry_point('validate'); graph.add_edge('validate', 'durability'); graph.add_edge('durability', END); compiled_graph = graph.compile()