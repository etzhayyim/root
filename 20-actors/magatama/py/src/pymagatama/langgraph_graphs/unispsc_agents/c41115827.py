from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UrinalysisState(TypedDict): device_id: str; compliance: bool; diagnostic_logs: List[str]; def validate_regulatory(state: UrinalysisState): state['compliance'] = True; return state; def check_test_logs(state: UrinalysisState): state['diagnostic_logs'] = ['Calibration Passed', 'Sensor Check OK']; return state; graph = StateGraph(UrinalysisState); graph.add_node('validate', validate_regulatory); graph.add_node('check_logs', check_test_logs); graph.add_edge('validate', 'check_logs'); graph.add_edge('check_logs', END); graph.set_entry_point('validate'); graph = graph.compile()