from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): status: str; specs: dict; compliance: bool; def validate_specs(state: AssemblyState): print('Validating Hastelloy X metallurgy...'); state['compliance'] = True; return {'status': 'validated'}; def run_workflow(state: AssemblyState): print('Processing sonic weld inspection data...'); return {'status': 'processed'}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('process', run_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()