from langgraph.graph import StateGraph, END; from typing import TypedDict; class FreezerState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(FreezerState); def validate_temp_range(state: FreezerState): return {'compliance_check': state['specs'].get('temp_range') == '2-6C'}; def finalize_order(state: FreezerState): return {'compliance_check': True}; graph.add_node('validate', validate_temp_range); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()