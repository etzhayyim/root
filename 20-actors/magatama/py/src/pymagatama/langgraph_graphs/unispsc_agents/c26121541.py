from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BusConductorState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(BusConductorState); def validate_electrical_specs(state: BusConductorState): return {'validation_results': ['current_rating_verified']}; def check_material_integrity(state: BusConductorState): return {'validation_results': ['material_compliance_confirmed']}; graph.add_node('validate', validate_electrical_specs); graph.add_node('integrity', check_material_integrity); graph.set_entry_point('validate'); graph.add_edge('validate', 'integrity'); graph.add_edge('integrity', END); graph = graph.compile()