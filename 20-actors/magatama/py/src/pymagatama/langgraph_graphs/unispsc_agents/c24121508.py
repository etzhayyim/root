from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EggTrayState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(EggTrayState); def validate_integrity(state: EggTrayState): return {'validation_report': ['Structural integrity checked']}; def approve_specs(state: EggTrayState): return {'validation_report': ['Food safety compliance verified']}; graph.add_node('validate', validate_integrity); graph.add_node('approve', approve_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); app = graph.compile()