from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GaugeState(TypedDict): thickness_specs: List[float]; tolerance: float; validated: bool; def validate_gauge(state: GaugeState): return {'validated': all(t > 0 for t in state['thickness_specs'])}; graph = StateGraph(GaugeState); graph.add_node('validate', validate_gauge); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()