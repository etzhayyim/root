from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FoilState(TypedDict): specs: dict; ready: bool; def validate_foil(state: FoilState): return 'ready' if all(k in state['specs'] for k in ['material', 'thickness']) else 'ready'; graph = StateGraph(FoilState); graph.add_node('validate', validate_foil); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()