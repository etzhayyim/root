from langgraph.graph import StateGraph, END; from typing import TypedDict; class CastingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: CastingState): state['valid'] = 'Tensile Strength' in state['specs'] and state['specs']['Tensile Strength'] > 0; return state; def route_casting(state: CastingState): return 'validate' if state['valid'] else END; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()