from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LiveStockState(TypedDict): species: str; health_status: bool; compliance: bool; log: List[str]; def validate_health(state: LiveStockState) -> LiveStockState: state['health_status'] = True; state['log'].append('Health checked'); return state; def check_compliance(state: LiveStockState) -> LiveStockState: state['compliance'] = True; state['log'].append('Regulatory compliance verified'); return state; graph = StateGraph(LiveStockState); graph.add_node('health_check', validate_health); graph.add_node('regulatory_check', check_compliance); graph.set_entry_point('health_check'); graph.add_edge('health_check', 'regulatory_check'); graph.add_edge('regulatory_check', END); graph = graph.compile()