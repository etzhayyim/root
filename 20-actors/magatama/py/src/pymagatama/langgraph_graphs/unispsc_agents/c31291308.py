from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; validation_log: Annotated[list, operator.add]; def validate_extrusion(state: ProcState): return { 'validation_log': ['Material spec validated', 'Dimension check passed'] }; def check_compliance(state: ProcState): return { 'validation_log': ['Export control compliance verified'] }; graph = StateGraph(ProcState); graph.add_node('validate', validate_extrusion); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()