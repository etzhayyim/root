from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): status: str; spec_validated: bool; def validate_specs(state: ProcureState): state['spec_validated'] = True; return {'status': 'Validated'}; workflow = StateGraph(ProcureState); workflow.add_node('validate', validate_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()