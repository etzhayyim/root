from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TubingState(TypedDict): specs: dict; compliance_verified: bool; export_control_flag: bool; def validate_specs(state: TubingState): state['compliance_verified'] = 'Material Grade' in state['specs'] and 'Wall Thickness' in state['specs']; return state; def check_export(state: TubingState): state['export_control_flag'] = True; return state; graph = StateGraph(TubingState); graph.add_node('validate', validate_specs); graph.add_node('check_export', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_export'); graph.add_edge('check_export', END); graph = graph.compile()