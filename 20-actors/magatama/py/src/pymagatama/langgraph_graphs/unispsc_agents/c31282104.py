from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(State); def validate_material(state: State): return {'validation_results': ['Material check passed']}; def check_tolerances(state: State): return {'validation_results': ['Tolerance check passed']}; graph.add_node('material_check', validate_material); graph.add_node('tolerance_check', check_tolerances); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'tolerance_check'); graph.add_edge('tolerance_check', END); graph = graph.compile()