from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SteamState(TypedDict): spec: dict; validated: bool; results: List[str]; def validate_specs(state: SteamState): specs = state['spec']; is_valid = all(key in specs for key in ['tank_capacity', 'power_rating']); return {'validated': is_valid}; graph = StateGraph(SteamState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()