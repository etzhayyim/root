from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HPLCState(TypedDict): specs: dict; validation_passed: bool; def check_specs(state: HPLCState): state['validation_passed'] = 'particle_size' in state['specs'] and 'pressure_rating' in state['specs']; return state; builder = StateGraph(HPLCState); builder.add_node('validate', check_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()