from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LightningProcureState(TypedDict): spec_data: dict; approval_required: bool; safety_audit_results: List[str]; def validate_standards(state: LightningProcureState): state['approval_required'] = state['spec_data'].get('standard_compliant', False); return state; def perform_safety_check(state: LightningProcureState): state['safety_audit_results'] = ['Insulation check', 'Surge discharge test']; return state; graph = StateGraph(LightningProcureState); graph.add_node('validate', validate_standards); graph.add_node('safety', perform_safety_check); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()