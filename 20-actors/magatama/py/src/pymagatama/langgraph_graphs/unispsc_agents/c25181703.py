from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrailerState(TypedDict): trailer_specs: dict; inspection_status: List[str]; def validate_load_capacity(state: TrailerState): state['inspection_status'].append('Capacity Validated'); return state; def check_ventilation(state: TrailerState): state['inspection_status'].append('Ventilation Compliant'); return state; graph = StateGraph(TrailerState); graph.add_node('check_cap', validate_load_capacity); graph.add_node('check_ven', check_ventilation); graph.add_edge('check_cap', 'check_ven'); graph.add_edge('check_ven', END); graph.set_entry_point('check_cap'); graph = graph.compile()