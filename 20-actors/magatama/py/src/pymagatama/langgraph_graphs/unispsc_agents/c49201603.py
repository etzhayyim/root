from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_results: dict; def validate_equipment(state: State): return {'validation_results': {'status': 'passed', 'specs': 'ISO20957 compliant'}}; def finalize_procurement(state: State): return {'validation_results': {'status': 'procurement_ready'}}; graph = StateGraph(State); graph.add_node('validate', validate_equipment); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()