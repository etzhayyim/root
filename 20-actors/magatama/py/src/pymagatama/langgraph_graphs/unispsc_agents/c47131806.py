from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): chemical_data: dict; compliance_passed: bool; def validate_sds(state: ProcurementState): state['compliance_passed'] = state['chemical_data'].get('voc_compliant', False); return state; def check_durability(state: ProcurementState): state['compliance_passed'] = state['compliance_passed'] and state['chemical_data'].get('durability_score', 0) > 8; return state; graph = StateGraph(ProcurementState); graph.add_node('validate_sds', validate_sds); graph.add_node('check_durability', check_durability); graph.set_entry_point('validate_sds'); graph.add_edge('validate_sds', 'check_durability'); graph.add_edge('check_durability', END); graph = graph.compile()