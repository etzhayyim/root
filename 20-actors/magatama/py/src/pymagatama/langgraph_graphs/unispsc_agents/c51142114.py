from langgraph.graph import StateGraph, END; from typing import TypedDict; class ChemState(TypedDict): chemical_name: str; purity: float; validated: bool; def validate_purity(state: ChemState): return {'validated': state['purity'] >= 99.0}; def route_step(state: ChemState): return 'end' if state['validated'] else 'end'; graph = StateGraph(ChemState); graph.add_node('validate', validate_purity); graph.add_edge('validate', END); graph.set_entry_point('validate')