from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; analysis_result: str; def validate_specs(state: State): spec = state['input_data']; return {'analysis_result': 'Valid' if 'accuracy' in spec else 'Invalid'}; def run_calibration(state: State): return {'analysis_result': 'Calibrated'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('calibrate', run_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); graph = graph.compile()