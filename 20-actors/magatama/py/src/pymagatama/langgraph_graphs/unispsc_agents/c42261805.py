from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MorgueState(TypedDict): spec_data: dict; compliance_passed: bool; installation_ready: bool; def validate_specs(state: MorgueState): state['compliance_passed'] = 'temp_range' in state['spec_data'] and state['spec_data']['temp_range'] <= 4; return state; def check_readiness(state: MorgueState): state['installation_ready'] = state['compliance_passed']; return state; graph = StateGraph(MorgueState); graph.add_node('validate', validate_specs); graph.add_node('check', check_readiness); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()