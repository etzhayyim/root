from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoftwareProcurementState(TypedDict): vendor_check: bool; compliance_pass: bool; software_type: str; def validate_vendor(state: SoftwareProcurementState): return {'vendor_check': True}; def check_compliance(state: SoftwareProcurementState): return {'compliance_pass': True}; graph = StateGraph(SoftwareProcurementState); graph.add_node('validate', validate_vendor); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()