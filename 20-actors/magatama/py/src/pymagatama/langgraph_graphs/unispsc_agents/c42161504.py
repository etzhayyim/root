from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[dict]; status: str; def validate_medical_device(state: State): status = 'verified' if all(i.get('sterile') for i in state['items']) else 'rejected'; return {'status': status}; graph = StateGraph(State); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()