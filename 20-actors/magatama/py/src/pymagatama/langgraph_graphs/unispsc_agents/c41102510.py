from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): content: str; validated: bool; errors: list; def validate_spec(state: SpecState): errors = []; if not state['content']: errors.append('Missing specification content'); return {'validated': len(errors) == 0, 'errors': errors}; graph = StateGraph(SpecState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()