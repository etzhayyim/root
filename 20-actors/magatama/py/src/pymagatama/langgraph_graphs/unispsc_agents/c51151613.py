from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_certs: List[str]; chemical_purity: float; needs_review: bool; def check_compliance(state: ProcureState): state['needs_review'] = state['chemical_purity'] < 0.99 or 'GDP_CERT' not in state['vendor_certs']; return state; builder = StateGraph(ProcureState); builder.add_node('compliance_check', check_compliance); builder.set_entry_point('compliance_check'); builder.add_edge('compliance_check', END); graph = builder.compile()