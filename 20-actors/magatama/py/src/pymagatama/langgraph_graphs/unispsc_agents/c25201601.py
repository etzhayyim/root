from langgraph.graph import StateGraph, END; from typing import TypedDict; class AircraftState(TypedDict): spec_data: dict; compliance_verified: bool; def validate_avionics(state: AircraftState): state['compliance_verified'] = 'DO-178C' in state['spec_data'] and 'DO-254' in state['spec_data']; return state; def export_check(state: AircraftState): print('Checking dual-use export status...'); return state; graph = StateGraph(AircraftState); graph.add_node('validate', validate_avionics); graph.add_node('export', export_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()