from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MessagingState(TypedDict): platform_name: str; security_specs: List[str]; validation_status: str; def validate_security(state: MessagingState): state['validation_status'] = 'Passed' if 'TLS 1.3' in state['security_specs'] else 'Failed'; return state; graph = StateGraph(MessagingState); graph.add_node('validate', validate_security); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()