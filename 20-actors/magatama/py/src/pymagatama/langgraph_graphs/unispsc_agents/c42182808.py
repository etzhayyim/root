from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ScaleCoverState(TypedDict): model_id: str; specs: dict; valid: bool; def validate_compatibility(state: ScaleCoverState): state['valid'] = state.get('model_id') is not None; return state; graph = StateGraph(ScaleCoverState); graph.add_node('validate', validate_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()