from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ActuatorState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: ActuatorState): specs = state['specs']; is_valid = all(k in specs for k in ['stroke', 'load']); return {'valid': is_valid, 'error': '' if is_valid else 'Missing mandatory parameters'}; graph = StateGraph(ActuatorState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()