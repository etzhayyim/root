from langgraph.graph import StateGraph, END; from typing import TypedDict; class CuttingState(TypedDict): spec: dict; valid: bool; error: str; def validate_specs(state: CuttingState): state['valid'] = 'power' in state['spec'] and state['spec']['power'] > 0; return state; graph = StateGraph(CuttingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()