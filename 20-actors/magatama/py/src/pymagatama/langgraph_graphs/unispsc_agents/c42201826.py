from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): model: str; kit_id: str; verified: bool; def validate_kit(state: State): state['verified'] = state['model'].startswith('XRAY-') and len(state['kit_id']) > 5; return state; graph = StateGraph(State); graph.add_node('validate', validate_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()