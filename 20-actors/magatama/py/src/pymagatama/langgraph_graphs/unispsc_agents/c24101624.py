from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CraneState(TypedDict): capacity: float; site_requirements: List[str]; approved: bool; def validate_capacity(state: CraneState): state['approved'] = state['capacity'] > 10; return state; def check_safety(state: CraneState): state['site_requirements'].append('OSHA_CERT'); return state; graph = StateGraph(CraneState); graph.add_node('validate', validate_capacity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()