from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; validation_passed: bool; logs: List[str]; def validate_purity(state: PharmState): state['validation_passed'] = True; state['logs'].append('Purity check compliant with USP-NF'); return state; def check_storage(state: PharmState): state['logs'].append('Cold chain logs verified'); return state; graph_builder = StateGraph(PharmState); graph_builder.add_node('validate', validate_purity); graph_builder.add_node('storage', check_storage); graph_builder.set_entry_point('validate'); graph_builder.add_edge('validate', 'storage'); graph_builder.add_edge('storage', END); graph = graph_builder.compile()