from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WearTestState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: WearTestState): return {'validation_results': ['Specs compliant'] if 'load' in state['specs'] else ['Invalid specs']}; def run_test_protocol(state: WearTestState): return {'validation_results': state['validation_results'] + ['Protocol set']}; graph = StateGraph(WearTestState); graph.add_node('validate', validate_specs); graph.add_node('protocol', run_test_protocol); graph.set_entry_point('validate'); graph.add_edge('validate', 'protocol'); graph.add_edge('protocol', END); compile_graph = graph.compile()