from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity_check: bool; safety_compliance: bool; graph = StateGraph(PharmState); def check_purity(state: PharmState): return {'purity_check': True}; def verify_compliance(state: PharmState): return {'safety_compliance': True}; graph.add_node('check_purity', check_purity); graph.add_node('verify_compliance', verify_compliance); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'verify_compliance'); graph.add_edge('verify_compliance', END); compile_graph = graph.compile()