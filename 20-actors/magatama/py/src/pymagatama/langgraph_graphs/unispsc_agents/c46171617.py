from langgraph.graph import StateGraph, END; from typing import TypedDict; class DoorViewerState(TypedDict): door_thickness: float; lens_angle: int; verified: bool; def validate_specs(state: DoorViewerState): state['verified'] = 25 <= state['door_thickness'] <= 60 and state['lens_angle'] >= 160; return state; graph = StateGraph(DoorViewerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()