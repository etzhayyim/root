from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StrawberryState(TypedDict): vendor: str; quality_score: float; lab_report: dict; approved: bool; def validate_quality(state: StrawberryState): state['approved'] = state['quality_score'] > 0.9; return state; graph = StateGraph(StrawberryState); graph.add_node('quality_check', validate_quality); graph.set_entry_point('quality_check'); graph.add_edge('quality_check', END); compiled_graph = graph.compile()