from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DeviceState(TypedDict): specs: dict; validation_results: List[str]; def validate_switch(state: DeviceState): specs = state['specs']; results = []; results.append('Voltage check complete') if 'voltage' in specs else results.append('Voltage missing'); return {'validation_results': results}; graph = StateGraph(DeviceState); graph.add_node('validate', validate_switch); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()