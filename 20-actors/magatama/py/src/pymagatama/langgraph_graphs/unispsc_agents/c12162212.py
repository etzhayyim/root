from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): input_data: dict; validation_errors: Annotated[list[str], operator.add]; processing_steps: Annotated[list[str], operator.add]; def validate_catalyst_data(state: CatalystState): errors = []; if 'cas_number' not in state['input_data']: errors.append('Missing CAS number'); return {'validation_errors': errors}; def process_catalyst_material(state: CatalystState): return {'processing_steps': ['Check purity levels', 'Verify export control status', 'Assign storage zone']}; graph = StateGraph(CatalystState); graph.add_node('validate', validate_catalyst_data); graph.add_node('process', process_catalyst_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()