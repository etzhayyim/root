from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): content: str; validated: bool; certs: list; def validate_api(state): state['validated'] = 'API_CERT' in state.get('certs', []); return state; def verify_regulatory(state): return 'approved' if state['validated'] else 'rejected'; graph = StateGraph(ProcState); graph.add_node('validate', validate_api); graph.add_edge('validate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()