from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ValveState(TypedDict): valve_spec: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(ValveState); def validate_spec(state: ValveState): return {'validation_results': ['Pressure rating verified', 'Seal compatibility check passed']}; def check_export(state: ValveState): return {'validation_results': ['Dual-use compliance verified']}; graph.add_node('validate_spec', validate_spec); graph.add_node('check_export', check_export); graph.set_entry_point('validate_spec'); graph.add_edge('validate_spec', 'check_export'); graph.add_edge('check_export', END); graph = graph.compile()