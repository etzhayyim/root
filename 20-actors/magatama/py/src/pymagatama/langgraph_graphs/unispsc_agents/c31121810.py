from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): pass; def validate_spec(state: State): return {'status': 'validated'}; def quality_check(state: State): return {'status': 'passed'}; graph = StateGraph(State); graph.add_node('spec_validation', validate_spec); graph.add_node('quality_inspection', quality_check); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', 'quality_inspection'); graph.add_edge('quality_inspection', END); graph = graph.compile()