from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserProcurementState(TypedDict): specs: dict; compliant: bool; review_required: bool; def validate_specs(state: LaserProcurementState): return {'compliant': state['specs'].get('laser_power_kw', 0) > 0}; def check_export(state: LaserProcurementState): return {'review_required': state['specs'].get('laser_power_kw', 0) > 5.0}; graph = StateGraph(LaserProcurementState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()