from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PenProcurementState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(PenProcurementState); def validate_specs(state: PenProcurementState): return {'validation_results': {'is_valid': True, 'msg': 'Specs verified for office compliance'}}; def finalize_procurement(state: PenProcurementState): print('Processing order for standard writing instruments.'); return {'validation_results': {'status': 'processed'}}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()