from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ToolState(TypedDict): items: List[str]; inspection_passed: bool; def validate_instrument(state: ToolState): state['inspection_passed'] = all(['stainless' in i.lower() for i in state['items']]); return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()