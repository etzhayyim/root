from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): doc_content: str; is_compliant: bool; def check_esd_compliance(state: State): state['is_compliant'] = 'ESD' in state['doc_content']; return state; graph = StateGraph(State); graph.add_node('validate', check_esd_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()