from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalSpecs(TypedDict): specs: dict; validation_log: List[str]; def validate_table(state: DentalSpecs): state["validation_log"].append("Verifying ISO 17664 compliance"); return state; def check_load(state: DentalSpecs): state["validation_log"].append("Checking structural load specs"); return state; graph = StateGraph(DentalSpecs); graph.add_node("validate", validate_table); graph.add_node("capacity", check_load); graph.set_entry_point("validate"); graph.add_edge("validate", "capacity"); graph.add_edge("capacity", END); graph = graph.compile()