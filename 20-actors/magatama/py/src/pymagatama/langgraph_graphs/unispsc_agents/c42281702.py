from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items_list: List[str]; inspection_results: dict; def validate_specs(state: ProcureState): print('Validating soaking pan dimensions and material compatibility.'); return {'inspection_results': {'passed': True}}; def finalize_procurement(state: ProcureState): print('Archiving sterilization records.'); return {}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()