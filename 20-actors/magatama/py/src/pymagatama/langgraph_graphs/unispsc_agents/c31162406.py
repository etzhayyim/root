from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): buckle_material: str; strap_width_mm: float; order_quantity: int; validation_status: bool; def material_check(state: ProcurementState): return {'validation_status': state['buckle_material'] in ['Steel', 'Polypropylene']}; def workflow(): graph = StateGraph(ProcurementState); graph.add_node('material_check', material_check); graph.set_entry_point('material_check'); graph.add_edge('material_check', END); return graph.compile(); graph = workflow()