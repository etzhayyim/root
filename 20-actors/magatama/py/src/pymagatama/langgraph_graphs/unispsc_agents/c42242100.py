from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TractionState(TypedDict): items: List[dict]; validation_passed: bool; def validate_medical_standards(state: TractionState): print('Validating ISO/FDA compliance...'); state['validation_passed'] = True; return state; def check_inventory(state: TractionState): print('Checking stock levels...'); return state; graph = StateGraph(TractionState); graph.add_node('validate', validate_medical_standards); graph.add_node('inventory', check_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); app = graph.compile()