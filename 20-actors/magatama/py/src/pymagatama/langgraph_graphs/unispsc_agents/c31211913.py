from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaintNozzleState(TypedDict): nozzle_specs: dict; inspection_status: str; graph = StateGraph(PaintNozzleState); def validate_nozzle_specs(state: PaintNozzleState): print('Validating nozzle specification...'); return {'inspection_status': 'passed'} if 'orifice_diameter' in state['nozzle_specs'] else {'inspection_status': 'failed'}; def process_workflow(state: PaintNozzleState): return {'inspection_status': 'optimized'}; graph.add_node('validate', validate_nozzle_specs); graph.add_node('optimize', process_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'optimize'); graph.add_edge('optimize', END); graph = graph.compile()