from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MnO2State(TypedDict): chemical_spec: dict; analysis_status: str; audit_trail: Annotated[Sequence[str], operator.add]; def validate_purity(state: MnO2State): return {'analysis_status': 'validated' if state['chemical_spec'].get('purity', 0) > 99.0 else 'rejected', 'audit_trail': ['Purity check completed']}; def handle_hazmat(state: MnO2State): return {'audit_trail': ['Hazmat documentation verified']}; graph = StateGraph(MnO2State); graph.add_node('purity', validate_purity); graph.add_node('hazmat', handle_hazmat); graph.set_entry_point('purity'); graph.add_edge('purity', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()