from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_id: str; item_spec: dict; compliance_check: bool; def validate_specs(state: ProcurementState): return {'compliance_check': 'tensile_strength_rating' in state['item_spec']}; def route_procurement(state: ProcurementState): return 'end' if state['compliance_check'] else 'end'; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()