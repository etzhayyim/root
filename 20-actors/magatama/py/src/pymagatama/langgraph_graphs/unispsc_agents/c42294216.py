from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AngioState(TypedDict): specs: dict; compliance_checked: bool; validation_log: List[str]; def validate_instruments(state: AngioState): state['validation_log'].append('Verifying sterilization specs...'); state['compliance_checked'] = True; return state; graph = StateGraph(AngioState); graph.add_node('validate', validate_instruments); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()