from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FireToolState(TypedDict): tool_specs: dict; validation_results: List[str]; def validate_tools(state: FireToolState): return {'validation_results': ['Strength verified', 'Material checked']}; def finalize_procurement(state: FireToolState): return {'validation_results': state['validation_results'] + ['Approved']}; graph = StateGraph(FireToolState); graph.add_node('validate', validate_tools); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()