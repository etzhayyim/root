from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_results: List[str]; def validate_acrylic_specs(state: ProcurementState): specs = state['specs']; results = []; if specs.get('light_transmittance', 0) < 92: results.append('Low optical clarity alert'); return {'validation_results': results}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_acrylic_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()