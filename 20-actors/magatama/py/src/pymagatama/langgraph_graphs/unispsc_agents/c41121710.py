from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): spec_data: dict; validation_errors: List[str]; def validate_specs(state: ProcState): specs = state['spec_data']; errors = []; required = ['Material Grade', 'Sterilization Method']; for r in required: if r not in specs: errors.append(f'Missing requirement: {r}'); return {'validation_errors': errors}; graph = StateGraph(ProcState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()