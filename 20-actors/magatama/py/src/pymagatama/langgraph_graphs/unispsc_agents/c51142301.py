from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; compliance_docs: List[str]; def validate_purity(state: PharmState): return {'purity': state['purity'] >= 99.0}; def check_compliance(state: PharmState): return {'compliance_docs': ['GMP', 'CoA']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()