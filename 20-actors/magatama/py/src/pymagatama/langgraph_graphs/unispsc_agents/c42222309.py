from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TransfusionState(TypedDict): data: dict; sensors: List[str]; validated: bool; def validate_sensors(state: TransfusionState): state['validated'] = 'Temperature' in state.get('sensors', []) and 'Pressure' in state.get('sensors', []); return state; def route_flow(state: TransfusionState): return 'validate' if state.get('sensors') else END; graph = StateGraph(TransfusionState); graph.add_node('validate', validate_sensors); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()