from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MemoryState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_specs(state: MemoryState): valid = 'clock_speed_mhz' in state['specs'] and 'interface_standard' in state['specs']; return {'validation_results': ['Specs validated'] if valid else ['Invalid specs'], 'status': 'COMPLETE' if valid else 'FAILED'}; graph = StateGraph(MemoryState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()