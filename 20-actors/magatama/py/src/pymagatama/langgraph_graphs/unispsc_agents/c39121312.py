from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph; class BushingState(TypedDict): specs: dict; compliance: bool; validation_log: list; def validate_specs(state: BushingState): compliance = state['specs'].get('voltage_rating', 0) > 0; return {'compliance': compliance, 'validation_log': ['Voltage checked']}; def finalize_procurement(state: BushingState): return {'validation_log': state['validation_log'] + ['Approved']}; graph = StateGraph(BushingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.set_finish_point('finalize'); graph = graph.compile()