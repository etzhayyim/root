from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SwimwearState(TypedDict): specs: dict; valid: bool; def validate_specs(state: SwimwearState): state['valid'] = all(k in state['specs'] for k in ['material', 'size', 'upf']); return state; def finalize_procurement(state: SwimwearState): print(f'Processing procurement data: {state}'); return state; graph = StateGraph(SwimwearState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()