from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KiteProcurementState(TypedDict): specs: dict; validation_results: List[str]; def validate_kite_specs(state: KiteProcurementState): results = []; if state['specs'].get('wingspan', 0) > 500: results.append('Large size detected: verify wind safety.'); return {'validation_results': results}; def finalize_procurement(state: KiteProcurementState): return {'validation_results': state['validation_results'] + ['Procurement approved.']}; graph = StateGraph(KiteProcurementState); graph.add_node('validate', validate_kite_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()