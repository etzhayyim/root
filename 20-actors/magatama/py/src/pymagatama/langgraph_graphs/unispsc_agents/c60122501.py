from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_specs: List[str]; validation_passed: bool; def validate_specs(state: ToolState): return {'validation_passed': len(state['tool_specs']) > 0}; def finalize_order(state: ToolState): return {'validation_passed': True}; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()