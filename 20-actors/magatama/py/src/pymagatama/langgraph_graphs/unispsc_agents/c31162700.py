from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HardwareState(TypedDict): hardware_specs: dict; validation_results: List[str]; graph = StateGraph(HardwareState); def validate_specs(state: HardwareState): load = state['hardware_specs'].get('load_capacity_kg', 0); return {'validation_results': ['Pass' if load > 0 else 'Fail']}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()