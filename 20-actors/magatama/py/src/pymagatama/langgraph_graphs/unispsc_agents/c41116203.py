from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; finalized_spec: dict; def validate_accessory(state: ProcureState): item = state['input_data']; result = "Valid" if 'compatibility' in item else "MissingData"; return {'validation_results': [result]}; def build_spec(state: ProcureState): return {'finalized_spec': {'status': 'READY', 'spec': state['input_data']}}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_accessory); graph.add_node('build', build_spec); graph.add_edge('validate', 'build'); graph.add_edge('build', END); graph.set_entry_point('validate'); graph = graph.compile()