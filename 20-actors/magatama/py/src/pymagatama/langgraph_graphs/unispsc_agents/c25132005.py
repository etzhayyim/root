from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftState(TypedDict): aircraft_data: dict; compliance_status: bool; validation_log: List[str]; def validate_airworthiness(state: AircraftState): state['validation_log'].append('Checking FAA/EASA airworthiness...'); state['compliance_status'] = True; return state; def check_export_controls(state: AircraftState): state['validation_log'].append('Verifying dual-use export status...'); return state; graph = StateGraph(AircraftState); graph.add_node('validate', validate_airworthiness); graph.add_node('export', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph = graph.compile()