from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CardboardState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: CardboardState): return {'validation_passed': all(k in state['specs'] for k in ['thickness', 'flute'])}; def route_decision(state: CardboardState): return 'valid' if state['validation_passed'] else 'invalid'; graph = StateGraph(CardboardState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_decision, {'valid': END, 'invalid': END}); graph = graph.compile()