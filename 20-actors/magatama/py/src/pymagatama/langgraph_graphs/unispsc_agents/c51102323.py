from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): quality_checked: bool; supply_chain_validated: bool; payload: str; def validate_quality(state: PharmaState): return {'quality_checked': True}; def validate_supply(state: PharmaState): return {'supply_chain_validated': True}; graph = StateGraph(PharmaState); graph.add_node('quality', validate_quality); graph.add_node('logistics', validate_supply); graph.add_edge('quality', 'logistics'); graph.add_edge('logistics', END); graph.set_entry_point('quality'); graph = graph.compile()