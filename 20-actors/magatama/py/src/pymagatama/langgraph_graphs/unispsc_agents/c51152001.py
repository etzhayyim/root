from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): batch_id: str; temp_log: List[float]; is_compliant: bool; def validate_temp(state: PharmaState): return {'is_compliant': all(t <= 8.0 for t in state['temp_log'])}; def process_batch(state: PharmaState): return {'is_compliant': True if state['batch_id'] else False}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_temp); graph.add_node('process', process_batch); graph.set_entry_point('process'); graph.add_edge('process', 'validate'); graph.add_edge('validate', END); graph = graph.compile()