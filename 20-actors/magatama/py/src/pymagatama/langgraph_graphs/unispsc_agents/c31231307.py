from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; hazards_cleared: bool; graph = StateGraph(ProcState); def validate_hazmat(state: ProcState): return {'hazards_cleared': state['specs'].get('lead_purity', 0) > 0}; def check_specs(state: ProcState): return {'specs': state['specs']}; graph.add_node('validate', validate_hazmat); graph.add_node('check', check_specs); graph.set_entry_point('check'); graph.add_edge('check', 'validate'); graph.add_edge('validate', END); graph = graph.compile()