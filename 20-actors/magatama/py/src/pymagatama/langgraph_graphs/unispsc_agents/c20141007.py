from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BearingState(TypedDict): specs: dict; valid: bool; engine_logs: List[str]; def validate_bearing(state: BearingState): state['valid'] = 'load_capacity' in state['specs'] and state['specs']['load_capacity'] > 0; return state; graph = StateGraph(BearingState); graph.add_node('validate', validate_bearing); graph.add_edge('validate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()