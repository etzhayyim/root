from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AmmunitionState(TypedDict): items: List[dict]; compliance_status: bool; def validate_compliance(state: AmmunitionState): state['compliance_status'] = all(item.get('end_user_certificate') for item in state['items']); return state; def check_danger(state: AmmunitionState): print('Checking hazardous material protocols...'); return state; graph = StateGraph(AmmunitionState); graph.add_node('compliance', validate_compliance); graph.add_node('safety', check_danger); graph.add_edge('compliance', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('compliance'); graph = graph.compile()