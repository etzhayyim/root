from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; purity: float; compliance_check: bool; def validate_purity(state: DrugState): return { 'compliance_check': state['purity'] >= 99.0 }; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()