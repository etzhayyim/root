from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): instrument_id: str; compliance_docs: List[str]; approved: bool; def validate_instruments(state: ProcureState): state['approved'] = all(['iso_13485' in state['compliance_docs'], 'sterilize_report' in state['compliance_docs']]); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_instruments); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()