from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WoundPouchState(TypedDict): pouch_id: str; spec_data: dict; compliance_status: bool; def validate_specs(state: WoundPouchState): state['compliance_status'] = 'ISO 10993' in state['spec_data'].get('certifications', []); return state; def check_expiration(state: WoundPouchState): return state; graph = StateGraph(WoundPouchState); graph.add_node('validate', validate_specs); graph.add_node('expiry', check_expiration); graph.set_entry_point('validate'); graph.add_edge('validate', 'expiry'); graph.add_edge('expiry', END); graph = graph.compile()