from langgraph.graph import StateGraph, END; from typing import TypedDict; class DiplomaState(TypedDict): doc_type: str; specs: dict; verified: bool; def validate_specs(state: DiplomaState): state['verified'] = 'paper_gsm' in state['specs'] and 'hologram_security' in state['specs']; return state; def finalize_order(state: DiplomaState): return {'status': 'approved' if state['verified'] else 'rejected'}; graph = StateGraph(DiplomaState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()