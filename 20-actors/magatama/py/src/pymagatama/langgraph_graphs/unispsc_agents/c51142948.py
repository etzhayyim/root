from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_id: str; purity: float; is_compliant: bool; steps: List[str]; def validate_compliance(state: ChemicalState): state['is_compliant'] = state['purity'] >= 99.9; return state; def report_status(state: ChemicalState): print(f'Validation complete. Compliance: {state['is_compliant']}'); return state; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_compliance); graph.add_node('report', report_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()