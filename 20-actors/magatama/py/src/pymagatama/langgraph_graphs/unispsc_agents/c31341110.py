from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TitaniumState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: TitaniumState): errors = []; required = ['ASTM_Grade', 'Welding_Certification']; for key in required: if key not in state['specs']: errors.append(f'Missing {key}'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(TitaniumState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()