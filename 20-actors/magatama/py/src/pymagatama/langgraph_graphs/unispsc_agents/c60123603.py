from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; status: str; def validate_specs(state: State): return {'status': 'validated' if 'Material-Composition' in state['input_data'] else 'rejected'}; def finalize_order(state: State): return {'status': 'ready_for_procurement'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()