from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ColorProcState(TypedDict): color_data: dict; validation_report: List[str]; graph = StateGraph(ColorProcState); def validate_spec(state: ColorProcState): return {'validation_report': ['Spec checked against ISO 12647']} if state['color_data'].get('pantone') else {'validation_report': ['Missing Pantone ID']}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()