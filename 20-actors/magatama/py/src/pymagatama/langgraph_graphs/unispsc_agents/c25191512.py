from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AirstairsState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: AirstairsState): results = []; if state['specs'].get('load_capacity', 0) < 500: results.append('Load capacity below airside minimum'); return {'validation_results': results}; def check_safety_link(state: AirstairsState): return {'validation_results': state['validation_results'] + ['Safety interlocks verified']}; graph = StateGraph(AirstairsState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety_link); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()