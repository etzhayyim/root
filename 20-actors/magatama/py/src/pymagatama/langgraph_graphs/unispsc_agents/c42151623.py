from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): model: str; specs: dict; compliant: bool; def validate_specs(state: DentalState): state['compliant'] = 'ISO' in state['specs'].get('certifications', []) and state['specs'].get('material') == 'surgical_steel'; return state; graph = StateGraph(DentalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()