from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalSpecState(TypedDict): specs: List[str]; validated: bool; def validate_specs(state: DentalSpecState): return {'validated': all(item in state['specs'] for item in ['iso_13485', 'material_grade'])}; graph = StateGraph(DentalSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()