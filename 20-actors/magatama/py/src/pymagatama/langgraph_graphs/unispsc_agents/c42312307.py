from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DebridementState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_grade(state: DebridementState): state['validated'] = all('ISO' in c for c in state['certs']); return state; graph = StateGraph(DebridementState); graph.add_node('validation', validate_medical_grade); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()