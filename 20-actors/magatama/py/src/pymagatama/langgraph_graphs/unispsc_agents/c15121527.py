from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class SteelProcurementState(TypedDict): alloy_type: str; tensile_strength: float; cert_verified: bool; approval_status: str; def validate_alloy(state: SteelProcurementState): return {'cert_verified': state.get('alloy_type') == 'Aerospace-Grade'}; def check_compliance(state: SteelProcurementState): return {'approval_status': 'COMPLIANT' if state['cert_verified'] else 'REJECTED'}; graph = StateGraph(SteelProcurementState); graph.add_node('validate', validate_alloy); graph.add_node('check', check_compliance); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.set_entry_point('validate'); graph = graph.compile()