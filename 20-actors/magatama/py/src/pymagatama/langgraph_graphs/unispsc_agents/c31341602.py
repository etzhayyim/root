from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; specs: dict; validation_passed: bool; def validate_specs(state: AssemblyState): print(f'Validating specs for {state["assembly_id"]}'); state['validation_passed'] = 'Tensile strength' in state['specs']; return state; def route_step(state: AssemblyState): return 'end' if state['validation_passed'] else 'error'; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()