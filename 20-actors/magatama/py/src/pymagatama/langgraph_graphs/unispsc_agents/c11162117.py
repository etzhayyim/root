from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MineralState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_purity(state: MineralState): return {validation_results: [f'Purity check passed for {state[input_data].get(id)}']}; def compliance_check(state: MineralState): return {status: 'compliance_verified'}; graph = StateGraph(MineralState); graph.add_node(purity, validate_purity); graph.add_node(compliance, compliance_check); graph.add_edge(purity, compliance); graph.add_edge(compliance, END); graph.set_entry_point(purity); graph = graph.compile()