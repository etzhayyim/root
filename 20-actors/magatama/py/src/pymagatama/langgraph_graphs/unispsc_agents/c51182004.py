from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): chemical_specs: dict; compliance_docs: List[str]; workflow_status: str; def validate_compliance(state: ProcurementState): return {'workflow_status': 'COMPLIANCE_CHECKED'} if 'fda_approval' in state['compliance_docs'] else {'workflow_status': 'FLAGGED'}; def finalize_order(state: ProcurementState): return {'workflow_status': 'APPROVED'}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_compliance); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()