from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PCAState(TypedDict): assembly_id: str; compliance_checks: List[str]; approved: bool; def validate_assembly(state: PCAState): print(f'Validating PCA: {state[assembly_id]}'); return {compliance_checks: ['IPC-A-610', 'RoHS']}; def final_review(state: PCAState): return {approved: len(state[compliance_checks]) >= 2}; graph = StateGraph(PCAState); graph.add_node('validate', validate_assembly); graph.add_node('review', final_review); graph.add_edge('validate', 'review'); graph.add_edge('review', END); graph.set_entry_point('validate'); graph = graph.compile()