from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WiringState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(WiringState); def validate_material(state: WiringState): state['validation_results'].append('Material verified'); return state; def check_ip_rating(state: WiringState): state['validation_results'].append('IP rating verified'); return state; graph.add_node('ValidateMaterial', validate_material); graph.add_node('CheckIP', check_ip_rating); graph.set_entry_point('ValidateMaterial'); graph.add_edge('ValidateMaterial', 'CheckIP'); graph.add_edge('CheckIP', END); graph.compile()