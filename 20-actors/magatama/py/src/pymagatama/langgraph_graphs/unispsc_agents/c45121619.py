from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WalletState(TypedDict): items: List[str]; validated: bool; def validate_specs(state: WalletState) -> WalletState: state['validated'] = all(len(item) > 0 for item in state['items']); return state; def check_quality(state: WalletState) -> str: return 'pass' if state['validated'] else 'fail'; graph = StateGraph(WalletState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()