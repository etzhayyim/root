from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; class MagGraphState(TypedDict): chemical_purity: float; moisture_content: float; supplier_coa_verified: bool; def check_purity(state: MagGraphState): return {'purity_check_passed': state['chemical_purity'] > 98.5}; def verify_coa(state: MagGraphState): return {'supplier_coa_verified': True}; graph = StateGraph(MagGraphState); graph.add_node('verify_coa', verify_coa); graph.add_node('check_purity', check_purity); graph.set_entry_point('verify_coa'); graph.add_edge('verify_coa', 'check_purity'); graph.add_edge('check_purity', END); graph = graph.compile()