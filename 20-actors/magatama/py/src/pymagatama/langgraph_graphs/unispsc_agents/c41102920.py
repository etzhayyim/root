from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HistologyState(TypedDict): cassette_type: str; is_solvent_resistant: bool; inspection_passed: bool; def validate_cassette(state: HistologyState): state['inspection_passed'] = state['is_solvent_resistant'] == True; return state; def route_inspection(state: HistologyState): return 'process' if state['inspection_passed'] else END; graph = StateGraph(HistologyState); graph.add_node('process', validate_cassette); graph.set_entry_point('process'); graph.add_edge('process', END); graph = graph.compile()