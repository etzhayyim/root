from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: List[str]; def validate_specs(state: State): results = []; specs = state['input_data'].get('specs', {}); if 'temperature_range' not in specs: results.append('Missing temperature settings'); return {'validation_results': results}; def check_regulatory(state: State): results = state['validation_results']; if 'regulatory_approval' not in state['input_data']: results.append('Missing medical device certification'); return {'validation_results': results}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); app = graph.compile()