from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EKGState(TypedDict): device_id: str; test_status: str; calibration_data: dict; def validate_cal(state: EKGState): state['test_status'] = 'VALIDATED' if 'cal_cert' in state['calibration_data'] else 'REJECTED'; return state; def run_diagnostic(state: EKGState): state['test_status'] = 'DIAGNOSING'; return state; graph = StateGraph(EKGState); graph.add_node('validate', validate_cal); graph.add_node('diagnose', run_diagnostic); graph.set_entry_point('validate'); graph.add_edge('validate', 'diagnose'); graph.add_edge('diagnose', END); app = graph.compile()