from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FilterState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(FilterState); def validate_mesh_size(state: FilterState): mesh = state['specs'].get('mesh_size'); report = [] if mesh and 50 <= mesh <= 400 else ['Invalid mesh size']; return {'validation_report': report}; graph.add_node('mesh_validator', validate_mesh_size); graph.add_edge('mesh_validator', END); graph.set_entry_point('mesh_validator'); graph = graph.compile()