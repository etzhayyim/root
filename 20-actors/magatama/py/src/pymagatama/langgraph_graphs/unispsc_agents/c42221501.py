from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ArterialCatheterState(TypedDict): vendor_docs: List[str]; qc_passed: bool; certs_valid: bool; def validate_certs(state: ArterialCatheterState): return {'certs_valid': len(state['vendor_docs']) > 0}; def run_qc(state: ArterialCatheterState): return {'qc_passed': state.get('certs_valid', False)}; graph = StateGraph(ArterialCatheterState); graph.add_node('validate', validate_certs); graph.add_node('qc', run_qc); graph.add_edge('validate', 'qc'); graph.add_edge('qc', END); graph.set_entry_point('validate'); graph = graph.compile()