from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input_data: dict; validation_report: list[str]; def validate_applicator(state: ProcState): specs = state['input_data']; reports = []; if not specs.get('chemical_compatibility'): reports.append('Missing chemical compatibility data'); return {'validation_report': reports}; def compile_graph(): graph = StateGraph(ProcState); graph.add_node('validate', validate_applicator); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()