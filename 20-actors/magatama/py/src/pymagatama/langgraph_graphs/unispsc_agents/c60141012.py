from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToyProcessState(TypedDict): items: List[str]; safety_approved: bool; def validate_safety(state: ToyProcessState) -> ToyProcessState: state['safety_approved'] = True; return state; def finalize_order(state: ToyProcessState) -> ToyProcessState: return state; graph = StateGraph(ToyProcessState); graph.add_node('validate', validate_safety); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()