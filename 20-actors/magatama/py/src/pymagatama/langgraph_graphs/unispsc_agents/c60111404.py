from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DecorState(TypedDict): specs: dict; ready_for_procurement: bool; def validate_specs(state: DecorState): return {'ready_for_procurement': 'Dimensions' in state['specs']}; def finalize_order(state: DecorState): return {'ready_for_procurement': True}; graph = StateGraph(DecorState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()