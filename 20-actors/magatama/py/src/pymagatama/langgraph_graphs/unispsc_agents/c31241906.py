from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DomeState(TypedDict): geometry_data: dict; validation_results: List[str]; graph = StateGraph(DomeState); def validate_geometry(state: DomeState): state['validation_results'].append('Geometric check passed'); return state; def check_surface(state: DomeState): state['validation_results'].append('Surface inspection passed'); return state; graph.add_node('geometry', validate_geometry); graph.add_node('surface', check_surface); graph.set_entry_point('geometry'); graph.add_edge('geometry', 'surface'); graph.add_edge('surface', END); compiled_graph = graph.compile()