from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StorageState(TypedDict): container_id: str; shielding_mm: float; is_certified: bool; verification_logs: List[str]; def validate_shielding(state: StorageState): if state['shielding_mm'] < 2.0: state['verification_logs'].append('Shielding insufficient'); return {'is_certified': False}; return {'is_certified': True}; def check_certs(state: StorageState): return {'verification_logs': state['verification_logs'] + ['Certificates verified']}; graph = StateGraph(StorageState); graph.add_node('validate', validate_shielding); graph.add_node('certs', check_certs); graph.set_entry_point('validate'); graph.add_edge('validate', 'certs'); graph.add_edge('certs', END); app = graph.compile()