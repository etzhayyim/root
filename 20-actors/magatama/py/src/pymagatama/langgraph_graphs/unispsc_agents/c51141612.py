from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): supplier_id: str; purity_data: float; gmp_status: bool; approved: bool; def validate_api(state: PharmaState): return {'approved': state['purity_data'] > 99.0 and state['gmp_status']}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_api); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()