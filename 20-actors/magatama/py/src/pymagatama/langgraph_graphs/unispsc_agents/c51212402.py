from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): chemical_name: str; purity: float; compliant: bool; def check_purity(state: State): return {'compliant': state['purity'] >= 99.0}; def validate_gmp(state: State): pass; graph = StateGraph(State); graph.add_node('check_purity', check_purity); graph.add_node('validate_gmp', validate_gmp); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'validate_gmp'); graph.add_edge('validate_gmp', END); graph = graph.compile()