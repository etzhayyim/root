from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StaplerState(TypedDict): sheets_count: int; staple_type: str; is_jammed: bool; def check_jam_node(state: StaplerState): return {'is_jammed': False}; def validate_capacity(state: StaplerState): return {'is_jammed': state['sheets_count'] > 50}; graph = StateGraph(StaplerState); graph.add_node('check', check_jam_node); graph.add_node('validate', validate_capacity); graph.set_entry_point('check'); graph.add_edge('check', 'validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()