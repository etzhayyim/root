from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): items: List[str]; validation_status: bool; def validate_instrument(state: GraphState): state['validation_status'] = all(len(item) > 0 for item in state['items']); return state; graph = StateGraph(GraphState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()