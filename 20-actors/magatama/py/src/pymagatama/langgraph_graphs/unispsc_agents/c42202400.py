from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PETProcessState(TypedDict): device_sn: str; radiation_safety_doc: bool; compliance_checked: bool; def check_safety(state: PETProcessState): state['radiation_safety_doc'] = True; return state; def validate_specs(state: PETProcessState): state['compliance_checked'] = True; return state; graph = StateGraph(PETProcessState); graph.add_node('safety', check_safety); graph.add_node('compliance', validate_specs); graph.set_entry_point('safety'); graph.add_edge('safety', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()