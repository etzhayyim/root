from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StoolState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_spec(state: StoolState): errors = []; if state['specs'].get('load_capacity', 0) < 120: errors.append('Load capacity below industry standard'); return {'validation_errors': errors}; def finalize(state: StoolState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(StoolState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()