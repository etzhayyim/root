from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ColdTrapState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: ColdTrapState): temp = state['specs'].get('temp'); if temp and temp > -20: state['validation_errors'].append('Temp too high'); return state; def check_compliance(state: ColdTrapState): if 'vacuum_rating' not in state['specs']: state['validation_errors'].append('Missing vacuum rating'); return state; graph = StateGraph(ColdTrapState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()