from langgraph.graph import StateGraph, END; from typing import TypedDict; class FuelTankState(TypedDict): specs: dict; approval_status: bool; def validate_specs(state: FuelTankState): return {'approval_status': state['specs'].get('pressure_tested', False)}; def check_safety(state: FuelTankState): return {'approval_status': state['approval_status'] and state['specs'].get('leak_proof', False)}; graph = StateGraph(FuelTankState); graph.add_node('validate', validate_specs); graph.add_node('safety_check', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()