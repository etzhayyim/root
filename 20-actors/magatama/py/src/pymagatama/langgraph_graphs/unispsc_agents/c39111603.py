from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LightingState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: LightingState): specs = state.get('specs', {}); required = ['IP_Rating', 'Luminous_Flux_Lumens']; state['valid'] = all(k in specs for k in required) and specs.get('IP_Rating', 0) >= 65; state['error'] = None if state['valid'] else 'Missing specs or insufficient IP rating'; return state; graph = StateGraph(LightingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()