from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): dimensions: dict; compliance: bool; approved: bool; def check_specs(state: State): state['compliance'] = all(k in state['dimensions'] for k in ['width', 'height', 'depth']); return {'compliance': state['compliance']}; def evaluate(state: State): state['approved'] = state['compliance']; return {'approved': state['approved']}; graph = StateGraph(State); graph.add_node('spec_validator', check_specs); graph.add_node('evaluator', evaluate); graph.add_edge('spec_validator', 'evaluator'); graph.add_edge('evaluator', END); graph.set_entry_point('spec_validator'); graph = graph.compile()