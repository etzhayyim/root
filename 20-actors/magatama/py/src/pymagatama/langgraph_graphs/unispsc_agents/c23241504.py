from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RobotState(TypedDict): specs: dict; validated: bool; next_step: str; def validate_specs(state: RobotState): params = state['specs']; state['validated'] = all(k in params for k in ['payload', 'reach']); return state; def route_step(state: RobotState): return 'complete' if state['validated'] else 'error'; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate')