from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoverallState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: CoverallState): state['compliant'] = 'tensile_strength' in state['specs'] and 'flame_retardancy' in state['specs']; return state; builder = StateGraph(CoverallState); builder.add_node('validator', validate_specs); builder.set_entry_point('validator'); builder.add_edge('validator', END); graph = builder.compile()