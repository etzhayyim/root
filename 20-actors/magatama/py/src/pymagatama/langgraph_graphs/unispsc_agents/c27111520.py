from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_name: str; validated: bool; inspection_logs: List[str]; def validate_tool(state: ToolState): state['validated'] = True; state['inspection_logs'].append('Material grade validated.'); return state; def prepare_report(state: ToolState): state['inspection_logs'].append('Report finalized.'); return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_tool); graph.add_node('report', prepare_report); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()