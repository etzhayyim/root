from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DuffelState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_materials(state: DuffelState): state['validation_errors'] = [] if 'material' not in state['specs'] else []; return state; def check_durability(state: DuffelState): return state; graph = StateGraph(DuffelState); graph.add_node('validate', validate_materials); graph.add_node('durability', check_durability); graph.set_entry_point('validate'); graph.add_edge('validate', 'durability'); graph.add_edge('durability', END); graph = graph.compile()