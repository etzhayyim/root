from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GeneratorState(TypedDict): specs: dict; compliance_check: bool; def validate_specs(state: GeneratorState): state['compliance_check'] = 'Rated Power' in state['specs'] and 'Emission Standard' in state['specs']; return state; def check_emissions(state: GeneratorState): return state; workflow = StateGraph(GeneratorState); workflow.add_node('validate', validate_specs); workflow.add_node('emission_check', check_emissions); workflow.set_entry_point('validate'); workflow.add_edge('validate', 'emission_check'); workflow.add_edge('emission_check', END); graph = workflow.compile()