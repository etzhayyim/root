from langgraph.graph import StateGraph, END; from typing import TypedDict; class CraneState(TypedDict): capacity_kg: float; safety_cert: bool; approved: bool; def validate_capacity(state: CraneState): return {'approved': state['capacity_kg'] > 0 and state['safety_cert']}; graph = StateGraph(CraneState); graph.add_node('validate', validate_capacity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()