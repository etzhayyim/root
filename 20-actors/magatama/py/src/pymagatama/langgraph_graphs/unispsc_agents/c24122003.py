from langgraph.graph import StateGraph, END; from typing import TypedDict; class GlassBottleState(TypedDict): bottle_specs: dict; inspection_status: str; def validate_specs(state: GlassBottleState): specs = state['bottle_specs']; status = 'PASSED' if 'Capacity_ml' in specs and 'Food_Grade_Certification' in specs else 'FAILED'; return {'inspection_status': status}; def handle_procurement(state: GlassBottleState): return {'inspection_status': 'COMPLETED'}; graph = StateGraph(GlassBottleState); graph.add_node('validate', validate_specs); graph.add_node('process', handle_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()