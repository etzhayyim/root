from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class ProcureState(TypedDict): input_data: dict; status: str; validation_results: list; graph = StateGraph(ProcureState); def validate_input(state: ProcureState): return {'status': 'validated', 'validation_results': ['origin_verified', 'safety_check_pass']}; def check_logistics(state: ProcureState): return {'status': 'logistics_ready'}; graph.add_node('validate', validate_input); graph.add_node('logistics', check_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); compiled_graph = graph.compile()