from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SeismoState(TypedDict): sensor_data: dict; validation_results: List[str]; graph = StateGraph(SeismoState); def validate_calibration(state: SeismoState): state['validation_results'].append('calibration_verified'); return state; def check_export_compliance(state: SeismoState): state['validation_results'].append('export_approved'); return state; graph.add_node('calibrate', validate_calibration); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('calibrate'); graph.add_edge('calibrate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()