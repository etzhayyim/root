from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): instrument_id: str; spec_data: dict; compliance_status: bool; def validate_specs(state: ProcState): state['compliance_status'] = 'FDA_CE' in state['spec_data']; return state; def check_sterility(state: ProcState): state['compliance_status'] = state['compliance_status'] and state['spec_data'].get('sterilization', False); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('sterility', check_sterility); graph.set_entry_point('validate'); graph.add_edge('validate', 'sterility'); graph.add_edge('sterility', END); graph = graph.compile()