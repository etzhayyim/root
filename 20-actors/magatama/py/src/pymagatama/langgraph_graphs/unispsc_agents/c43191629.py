from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class GraphicsProcState(TypedDict): gpu_model: str; status: str; validation_passed: bool; async def validate_gpu(state: GraphicsProcState): return {'validation_passed': True if state['gpu_model'] else False}; async def process_workload(state: GraphicsProcState): return {'status': 'processed'}; graph = StateGraph(GraphicsProcState); graph.add_node('validate', validate_gpu); graph.add_node('process', process_workload); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()