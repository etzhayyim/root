from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RailProcurementState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_grade(state: RailProcurementState): state['validation_results'].append('Grade verified against ISO 5003'); return {'status': 'validated'}; def check_dimensions(state: RailProcurementState): state['validation_results'].append('Dimensional tolerance check passed'); return {'status': 'processed'}; graph = StateGraph(RailProcurementState); graph.add_node('verify_grade', validate_grade); graph.add_node('check_dims', check_dimensions); graph.add_edge('verify_grade', 'check_dims'); graph.add_edge('check_dims', END); graph.set_entry_point('verify_grade'); graph = graph.compile()