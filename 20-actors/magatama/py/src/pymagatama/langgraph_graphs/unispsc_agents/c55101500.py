from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): doc_type: str; specs_met: bool; def validate_print_specs(state: ProcState): return {'specs_met': True} if state['doc_type'] in ['book', 'manual'] else {'specs_met': False}; graph = StateGraph(ProcState); graph.add_node('validate', validate_print_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()