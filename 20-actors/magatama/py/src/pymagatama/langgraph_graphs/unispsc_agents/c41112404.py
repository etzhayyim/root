from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class RegulatorState(TypedDict): pressure_input: float; validation_result: bool; compliance_docs: list; graph = StateGraph(RegulatorState); def validate_specs(state: RegulatorState): state['validation_result'] = state['pressure_input'] > 0; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()