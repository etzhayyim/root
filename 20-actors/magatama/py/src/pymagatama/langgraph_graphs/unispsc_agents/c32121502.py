from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CapacitorState(TypedDict): component_id: str; specs: dict; validation_results: List[str]; def validate_specs(state: CapacitorState): return {'validation_results': ['Voltage rating verified', 'Capacitance range checked']}; def assemble_part(state: CapacitorState): return {'validation_results': state['validation_results'] + ['Assembly ready']}; graph = StateGraph(CapacitorState); graph.add_node('validate', validate_specs); graph.add_node('assemble', assemble_part); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph.set_entry_point('validate'); graph = graph.compile()