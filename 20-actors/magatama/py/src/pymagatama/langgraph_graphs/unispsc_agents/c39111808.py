from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GrilleState(TypedDict): items: list; valid_items: list; status: str; def validate_specs(state: GrilleState): validated = [item for item in state['items'] if 'fire_rating' in item and 'dimensions' in item]; return {'valid_items': validated, 'status': 'validated'}; def compile_graph(): graph = StateGraph(GrilleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()