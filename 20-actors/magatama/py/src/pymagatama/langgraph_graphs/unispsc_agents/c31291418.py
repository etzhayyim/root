from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): input_data: dict; validation_results: list[str]; def validate_tolerance(state: GraphState): state['validation_results'].append('Tolerance Check Passed') if state['input_data'].get('tolerance') else state['validation_results'].append('Tolerance Check Failed'); return state; def check_export_compliance(state: GraphState): state['validation_results'].append('Export Control Verified'); return state; graph = StateGraph(GraphState); graph.add_node('validate', validate_tolerance); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()