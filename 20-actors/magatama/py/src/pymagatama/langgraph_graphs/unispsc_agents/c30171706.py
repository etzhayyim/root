from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): specs: dict; validation_errors: List[str]; def validate_glass_specs(state: SpecState): errors = []; s = state['specs']; if s.get('thickness_mm', 0) < 3: errors.append('Thickness below safety standard'); state['validation_errors'] = errors; return state; graph = StateGraph(SpecState); graph.add_node('validate', validate_glass_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()