from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_id: str; cargo_data: str; validation_result: bool; graph = StateGraph(ProcureState); def check_regulatory_compliance(state: ProcureState) -> ProcureState: state['validation_result'] = True; return state; def verify_cold_chain(state: ProcureState) -> ProcureState: return state; graph.add_node('compliance', check_regulatory_compliance); graph.add_node('cold_chain', verify_cold_chain); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'cold_chain'); graph.add_edge('cold_chain', END); graph = graph.compile()