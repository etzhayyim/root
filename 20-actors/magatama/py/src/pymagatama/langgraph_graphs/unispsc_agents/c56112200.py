from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DeskState(TypedDict): specs: dict; valid: bool; workflow: List[str]; def validate_specs(state: DeskState): state['valid'] = 'Load Capacity' in state['specs'] and 'Material Composition' in state['specs']; state['workflow'].append('validation'); return state; def route_step(state: DeskState): return 'complete' if state['valid'] else END; graph_builder = StateGraph(DeskState); graph_builder.add_node('validate', validate_specs); graph_builder.set_entry_point('validate'); graph_builder.add_edge('validate', END); graph = graph_builder.compile()