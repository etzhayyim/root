from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): pharmaceutical_agent: str; purity: float; is_gmp_certified: bool; approved: bool; def check_purity(state: PharmState): return {'approved': state['purity'] >= 99.0 and state['is_gmp_certified']}; graph = StateGraph(PharmState); graph.add_node('verify_purity', check_purity); graph.set_entry_point('verify_purity'); graph.add_edge('verify_purity', END); graph = graph.compile()