from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: List[str]; graph = StateGraph(State); def validate_plaster_spec(state: State): results = []; data = state.get('input_data', {}); if data.get('setting_time', 0) > 300: results.append('Setting time exceeds standard limit.'); return {'validation_results': results}; graph.add_node('validate', validate_plaster_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()