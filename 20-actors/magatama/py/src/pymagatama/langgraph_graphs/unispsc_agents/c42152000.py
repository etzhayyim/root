from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ImagingState(TypedDict): device_id: str; compliance_passed: bool; specs: dict; graph = StateGraph(ImagingState); def validate_regulatory(state: ImagingState): state['compliance_passed'] = 'license_number' in state['specs']; return state; def check_specs(state: ImagingState): return {'compliance_passed': state['compliance_passed']}; graph.add_node('validate', validate_regulatory); graph.add_node('check', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()