from langgraph.graph import StateGraph, END; from typing import TypedDict; class TitaniumState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: TitaniumState): state['validation_results'].append('Dimensional check passed'); return {'status': 'Validated'}; def check_compliance(state: TitaniumState): state['validation_results'].append('ISO/AS9100 confirmed'); return {'status': 'Compliant'}; graph = StateGraph(TitaniumState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()