from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): items: List[str]; validated: bool; state = ProcurementState(items=[], validated=False); def validate_specs(state: ProcurementState): state['validated'] = all(['material' in item.lower() for item in state['items']]); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()