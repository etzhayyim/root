from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class WasteState(TypedDict): container_type: str; specs: dict; compliant: bool; def validate_specs(state: WasteState): status = state['specs'].get('UN_Rating') is not None; return {'compliant': status}; graph = StateGraph(WasteState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()