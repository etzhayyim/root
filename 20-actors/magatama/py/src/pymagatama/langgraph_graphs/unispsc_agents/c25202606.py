from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AircraftSpecState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_spec(state: AircraftSpecState): return {'validation_errors': ['Missing AS9100 Cert'] if 'AS9100' not in state['specs'] else []}; def check_export_control(state: AircraftSpecState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(AircraftSpecState); graph.add_node('validate', validate_spec); graph.add_node('check_export', check_export_control); graph.add_edge('validate', 'check_export'); graph.add_edge('check_export', END); graph.set_entry_point('validate'); graph = graph.compile()