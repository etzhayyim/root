from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MountState(TypedDict): vendor_specs: dict; compliance_check: bool; mount_graph = StateGraph(MountState); def validate_specs(state: MountState): return {'compliance_check': 'vibration_rating' in state['vendor_specs']}; def approve_procurement(state: MountState): return {'compliance_check': True}; mount_graph.add_node('validate', validate_specs); mount_graph.add_node('approve', approve_procurement); mount_graph.set_entry_point('validate'); mount_graph.add_edge('validate', 'approve'); mount_graph.add_edge('approve', END); graph = mount_graph.compile()