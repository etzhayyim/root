from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): signal_format: str; conversion_validated: bool; def validate_signal(state: State): return {'conversion_validated': state.get('signal_format') in ['HDMI', 'SDI', 'DisplayPort']}; graph = StateGraph(State); graph.add_node('validate', validate_signal); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()