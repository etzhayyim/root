from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EduState(TypedDict): items: List[str]; validated: bool; graph = StateGraph(EduState); def validate_safety(state: EduState): return {'validated': all('non-toxic' in item.lower() for item in state.get('items', []))}; graph.add_node('safety_check', validate_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()