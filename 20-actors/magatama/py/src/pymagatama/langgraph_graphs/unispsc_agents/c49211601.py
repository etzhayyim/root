from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GolfBagState(TypedDict): bag_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: GolfBagState): errors = []; if 'weight' in state['bag_specs'] and state['bag_specs']['weight'] > 5.0: errors.append('Weight exceeds optimal performance limit'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(GolfBagState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()