from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RefractoryState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_castable(state: RefractoryState): state['validation_errors'] = [] if float(state['specs'].get('SiC_content', 0)) < 70: state['validation_errors'].append('Insufficient SiC content for industrial grade.') state['approved'] = len(state['validation_errors']) == 0; return state; graph = StateGraph(RefractoryState); graph.add_node('validate', validate_castable); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()