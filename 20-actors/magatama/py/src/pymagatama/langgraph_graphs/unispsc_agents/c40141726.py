from langgraph.graph import StateGraph, END; from typing import TypedDict; class HydrantState(TypedDict): spec: dict; valid: bool; def validate_pressure(state: HydrantState): state['valid'] = state['spec'].get('pressure', 0) > 10; return state; def check_compliance(state: HydrantState): pass; graph = StateGraph(HydrantState); graph.add_node('validate', validate_pressure); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()