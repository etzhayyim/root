from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): cargo: str; certs: List[str]; approved: bool; def validate_purity(state: ProcState) -> ProcState: print('Validating Isoflurane USP standards...'); state['approved'] = True; return state; def check_hazmat(state: ProcState) -> ProcState: print('Reviewing DG shipping protocols...'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_purity); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()