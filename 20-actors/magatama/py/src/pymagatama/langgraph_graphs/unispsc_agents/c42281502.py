from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SterilizerState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_compliance(state: SterilizerState): errors = []; if 'iso_13485' not in state['specs']: errors.append('Missing ISO 13485 certification'); return {'validation_errors': errors}; def check_specs(state: SterilizerState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(SterilizerState); graph.add_node('validate', validate_compliance); graph.add_node('verify', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); graph.compile()