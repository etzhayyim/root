from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ForensicState(TypedDict): content: str; validation_status: bool; def validate_forensics(state: ForensicState): state['validation_status'] = 'Forensic' in state['content'] and 'Kit' in state['content']; return state; builder = StateGraph(ForensicState); builder.add_node('validate', validate_forensics); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()