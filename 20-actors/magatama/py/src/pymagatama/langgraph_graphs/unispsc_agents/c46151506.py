from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BatonState(TypedDict): baton_id: str; compliance_check: bool; material_spec: str; status: str; def validate_specs(state: BatonState): is_valid = 'steel' in state['material_spec'].lower(); return {'compliance_check': is_valid, 'status': 'validated' if is_valid else 'rejected'}; def export_control_check(state: BatonState): return {'status': 'flagged_for_review' if state['compliance_check'] else 'denied'}; graph = StateGraph(BatonState); graph.add_node('validate', validate_specs); graph.add_node('export_review', export_control_check); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()