from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): pipe_specs: dict; validation_results: List[str]; def validate_material_cert(state: PipeState): state['validation_results'].append('AMS-5708 Verified'); return state; def check_pressure(state: PipeState): state['validation_results'].append('Pressure Test Passed'); return state; graph = StateGraph(PipeState); graph.add_node('verify_material', validate_material_cert); graph.add_node('verify_pressure', check_pressure); graph.set_entry_point('verify_material'); graph.add_edge('verify_material', 'verify_pressure'); graph.add_edge('verify_pressure', END); graph = graph.compile()