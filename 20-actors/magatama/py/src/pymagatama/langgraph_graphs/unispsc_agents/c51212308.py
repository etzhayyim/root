from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadiopharmState(TypedDict): chemical_code: str; purity_level: float; compliance_verified: bool; def validate_integrity(state: RadiopharmState): state['compliance_verified'] = state['purity_level'] > 0.99; return state; def check_half_life(state: RadiopharmState): print('Checking I-131 decay status'); return state; graph = StateGraph(RadiopharmState); graph.add_node('validate', validate_integrity); graph.add_node('decay_check', check_half_life); graph.set_entry_point('validate'); graph.add_edge('validate', 'decay_check'); graph.add_edge('decay_check', END); graph = graph.compile()