from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class MachineSpec(TypedDict): model: str; compliance: bool; status: str; def validate_specs(state: MachineSpec): state['compliance'] = 'CNC' in state['model']; return state; def finalize_order(state: MachineSpec): state['status'] = 'APPROVED' if state['compliance'] else 'REJECTED'; return state; graph = StateGraph(MachineSpec); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()