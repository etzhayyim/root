from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class StainingDishState(TypedDict): specs: dict; validation_report: str; graph = StateGraph(StainingDishState); def validate_specs(state: StainingDishState): return {'validation_report': 'Passed' if state['specs'].get('autoclavable') else 'Review Required'}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()