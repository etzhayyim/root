from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_device(state: ProcureState): return {'validation_results': ['Calibration validated'], 'status': 'READY'} if 'calibration_cert' in state['input_data'] else {'validation_results': ['Missing calibration'], 'status': 'REJECTED'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()