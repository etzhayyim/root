from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; status: str; def validate_tool(state: State): return {'status': 'validated' if 'suction_force' in state['input_data'] else 'rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_tool); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()