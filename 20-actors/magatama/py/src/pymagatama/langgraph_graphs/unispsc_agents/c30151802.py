from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SidingState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: SidingState): state['compliance'] = 'fire_rating_certification' in state['specs']; return {'compliance': state['compliance']}; def approve_procurement(state: SidingState): state['approved'] = state['compliance']; return {'approved': state['approved']}; graph = StateGraph(SidingState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()