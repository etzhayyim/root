from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LinenState(TypedDict): items: List[dict]; validation_results: List[str]; def validate_quality(state: LinenState): results = []; for item in state['items']: if 'material' not in item: results.append(f'Missing material for {item.get('id')}'); return {'validation_results': results}; graph = StateGraph(LinenState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()