from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; valid: bool; workflow_log: List[str]; def validate_specs(state: CompressorState): specs = state['specs']; is_valid = specs.get('pressure', 0) < 1.0; return {'valid': is_valid, 'workflow_log': ['Pressure validation complete']}; def route(state: CompressorState): return 'end' if state['valid'] else END; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()