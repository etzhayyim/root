from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class JuiceProcurementState(TypedDict): specs: dict; valid: bool; status: str; def validate_specs(state: JuiceProcurementState): specs = state.get('specs', {}); required = ['Brix_level', 'Shelf_life_days']; is_valid = all(key in specs for key in required); return {'valid': is_valid, 'status': 'Validated' if is_valid else 'Rejected'}; graph = StateGraph(JuiceProcurementState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()