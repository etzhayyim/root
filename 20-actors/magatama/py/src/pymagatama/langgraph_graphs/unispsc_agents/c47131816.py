from typing import TypedDict, Annotated, List; import operator; from langgraph.graph import StateGraph, END; class DeodorizerState(TypedDict): chemical_data: dict; safety_check: bool; verified_specs: List[str]; def validate_chemical_safety(state: DeodorizerState): return {'safety_check': 'SDS_verified' in state.get('chemical_data', {}), 'verified_specs': ['Safety Compliant']}; def approve_procurement(state: DeodorizerState): return {'verified_specs': state['verified_specs'] + ['Approved']}; graph = StateGraph(DeodorizerState); graph.add_node('validate', validate_chemical_safety); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()