from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): items: List[str]; validated: bool; next_step: str; def validate_inventory(state: KitState): state['validated'] = 'kit_id' in state['items'] and len(state['items']) > 0; return state; def check_compliance(state: KitState): return 'compliance_check' if state['validated'] else 'error'; graph = StateGraph(KitState); graph.add_node('validate', validate_inventory); graph.add_edge('validate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()