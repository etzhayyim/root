from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StampingState(TypedDict): component_specs: dict; validation_results: List[str]; def validate_specs(state: StampingState): results = []; if 'grade' not in state['component_specs']: results.append('Missing Material Grade'); return {'validation_results': results}; def report_status(state: StampingState): print(f'Validation complete: {state["validation_results"]}'); return {}; graph = StateGraph(StampingState); graph.add_node('validate', validate_specs); graph.add_node('report', report_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()