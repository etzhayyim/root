from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnesthesiaState(TypedDict): device_id: str; compliance_docs: List[str]; inspection_passed: bool; def validate_compliance(state: AnesthesiaState): return {'inspection_passed': len(state['compliance_docs']) > 2}; def finalize_order(state: AnesthesiaState): return {'inspection_passed': True}; graph = StateGraph(AnesthesiaState); graph.add_node('validate', validate_compliance); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()