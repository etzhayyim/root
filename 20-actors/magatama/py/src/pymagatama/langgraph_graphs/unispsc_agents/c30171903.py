from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WindowSpecState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: WindowSpecState): errors = []; if 'U-value' not in state['specs']: errors.append('Missing thermal U-value'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(WindowSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph_app = graph.compile()