from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BCDState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_bcd(state: BCDState): state['valid'] = state['specs'].get('burst_pressure', 0) >= 10; return state; graph = StateGraph(BCDState); graph.add_node('validate', validate_bcd); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()