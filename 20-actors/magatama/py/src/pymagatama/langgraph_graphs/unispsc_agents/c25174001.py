from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FanProcurementState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: FanProcurementState): results = []; if state['specs'].get('airflow', 0) <= 0: results.append('Invalid airflow'); return {'validation_results': results}; def check_compliance(state: FanProcurementState): return {'status': 'COMPLIANT' if not state['validation_results'] else 'FAILED'}; graph = StateGraph(FanProcurementState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()