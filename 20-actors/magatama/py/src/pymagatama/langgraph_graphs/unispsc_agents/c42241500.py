from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[dict]; validation_results: dict; graph = StateGraph(State); def validate_spec(state: State): return {'validation_results': {'status': 'pending', 'compliance': 'MDR_compliant'}}; def check_material_safety(state: State): return {'validation_results': {'status': 'approved'}}; graph.add_node('validate_spec', validate_spec); graph.add_node('check_safety', check_material_safety); graph.set_entry_point('validate_spec'); graph.add_edge('validate_spec', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()