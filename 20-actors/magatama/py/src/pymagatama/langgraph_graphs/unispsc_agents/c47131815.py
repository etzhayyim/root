from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AgentState(TypedDict): chemical_data: dict; compliance_status: bool; workflow_log: List[str]; def validate_sds(state: AgentState): state['compliance_status'] = 'SDS_Verified' in state['chemical_data']; return state; def check_hazmat(state: AgentState): state['workflow_log'].append('Hazmat check complete'); return state; graph = StateGraph(AgentState); graph.add_node('validate', validate_sds); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()