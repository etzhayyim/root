from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HemofiltrationState(TypedDict): items: List[str]; validation_results: dict; def validate_device(state: HemofiltrationState): return {'validation_results': {'sterile': True, 'certified': True}}; def compile_workflow(): graph = StateGraph(HemofiltrationState); graph.add_node('validate', validate_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_workflow()