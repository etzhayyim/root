from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; payload: dict; def validate_regulatory(state: State): status = 'verified' if 'license' in state['payload'] else 'rejected'; return {'status': status}; def process_shipping(state: State): return {'status': 'shipping_validated'}; graph = StateGraph(State); graph.add_node('regulatory', validate_regulatory); graph.add_node('shipping', process_shipping); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'shipping'); graph.add_edge('shipping', END); compiled_graph = graph.compile()