from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): items: List[str]; qc_passed: bool; certs: List[str]; def validate_medical_grade(state: SurgicalState): return {'qc_passed': all('ISO_13485' in cert for cert in state.get('certs', []))}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_medical_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()