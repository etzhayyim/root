from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CTState(TypedDict): vendor_docs: dict; specs: dict; compliance_status: bool; def validate_specs(state: CTState): state['compliance_status'] = 'gantry_bore_diameter' in state['specs'] and state['specs']['gantry_bore_diameter'] >= 80; return state; def check_regulatory(state: CTState): print('Checking FDA/PMDA compliance for CT unit'); return state; graph = StateGraph(CTState); graph.add_node('validate', validate_specs); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()