from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input: str; validation: bool; output: str; def validate_equipment(state: State): state['validation'] = 'ASTM' in state['input']; return state; def format_report(state: State): state['output'] = f'Compliance Status: {state['validation']}'; return state; graph = StateGraph(State); graph.add_node('validate', validate_equipment); graph.add_node('format', format_report); graph.add_edge('validate', 'format'); graph.add_edge('format', END); graph.set_entry_point('validate'); graph = graph.compile()