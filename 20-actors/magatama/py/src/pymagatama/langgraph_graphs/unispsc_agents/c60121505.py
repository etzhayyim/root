from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FabricMarkerState(TypedDict): marker_type: str; ink_safety_compliant: bool; validation_passed: bool; def check_safety(state: FabricMarkerState): return {'validation_passed': state['ink_safety_compliant']}; def check_type(state: FabricMarkerState): return state; graph = StateGraph(FabricMarkerState); graph.add_node('safety_check', check_safety); graph.add_node('type_check', check_type); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'type_check'); graph.add_edge('type_check', END); graph = graph.compile()