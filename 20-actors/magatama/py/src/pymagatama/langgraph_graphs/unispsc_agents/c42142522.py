from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): needle_specs: dict[str, str]; validation_errors: Annotated[list[str], operator.add]; def validate_specs(state: SurgicalState): errors = []; if not state['needle_specs'].get('sterile_status'): errors.append('Missing sterility record'); return {'validation_errors': errors}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()