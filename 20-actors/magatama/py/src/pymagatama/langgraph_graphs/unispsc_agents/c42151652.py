from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): pin_spec: dict; validation_results: List[str]; def validate_medical_device(state: ProcurementState): spec = state['pin_spec']; valid = all([spec.get('material'), spec.get('sterilization')]); return {'validation_results': ['Success' if valid else 'Failed']}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()