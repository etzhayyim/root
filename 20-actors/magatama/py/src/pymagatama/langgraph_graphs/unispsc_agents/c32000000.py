from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ElectronicState(TypedDict): component_list: List[str]; validation_results: dict; graph = StateGraph(ElectronicState); def validate_parts(state: ElectronicState): return {'validation_results': {p: 'valid' for p in state['component_list']}}; def check_compliance(state: ElectronicState): return {'validation_results': {p: 'compliant' for p in state['component_list']}}; graph.add_node('validate', validate_parts); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()