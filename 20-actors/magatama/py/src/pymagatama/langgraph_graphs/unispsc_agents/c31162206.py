from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RivetProcurementState(TypedDict): rivets_list: List[dict]; validation_results: List[str]; def validate_rivet_specs(state: RivetProcurementState): results = []; for rivet in state['rivets_list']: if rivet.get('diameter') > 0: results.append('Valid'); else: results.append('Invalid'); return {'validation_results': results}; def dispatch_order(state: RivetProcurementState): print('Processing rivets order'); return {'validation_results': ['Dispatched']}; graph = StateGraph(RivetProcurementState); graph.add_node('validation', validate_rivet_specs); graph.add_node('dispatch', dispatch_order); graph.set_entry_point('validation'); graph.add_edge('validation', 'dispatch'); graph.add_edge('dispatch', END); graph = graph.compile()