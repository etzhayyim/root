from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ZincSpecState(TypedDict): doc_id: str; specs: dict; validation_results: Annotated[list, operator.add]; def validate_material_specs(state: ZincSpecState): results = []; specs = state.get('specs', {}); if specs.get('purity', 0) < 99.0: results.append('Failure: Low zinc purity'); return {'validation_results': results}; graph = StateGraph(ZincSpecState); graph.add_node('validate', validate_material_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()