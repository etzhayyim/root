from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class StirrerState(TypedDict): specs: dict; validation_results: list; graph = StateGraph(StirrerState); def validate_specs(state: StirrerState): results = []; if state['specs'].get('speed_range') < 0: results.append('Invalid RPM'); return {'validation_results': results}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()