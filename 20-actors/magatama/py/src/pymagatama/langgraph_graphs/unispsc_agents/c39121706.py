from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): spec: dict; valid: bool; def validate_bushing(state: State): state['valid'] = all(k in state['spec'] for k in ['kV', 'A', 'standard']); return state; graph = StateGraph(State); graph.add_node('validate', validate_bushing); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()