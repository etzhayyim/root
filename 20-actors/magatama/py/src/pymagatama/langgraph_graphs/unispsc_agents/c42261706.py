from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmbalmingState(TypedDict): vendor_certs: List[str]; specs: dict; compliance_status: bool; def validate_specs(state: EmbalmingState): state['compliance_status'] = 'ISO-13485' in state['vendor_certs'] and 'ventilation_cfm' in state['specs']; return state; builder = StateGraph(EmbalmingState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()