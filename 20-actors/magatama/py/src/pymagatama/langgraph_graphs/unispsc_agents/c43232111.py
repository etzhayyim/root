from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OCRProcessState(TypedDict): input_files: List[str]; ocr_results: List[str]; validation_status: bool; def validate_ocr(state: OCRProcessState): return {'validation_status': all(len(r) > 0 for r in state['ocr_results'])}; def process_ocr(state: OCRProcessState): return {'ocr_results': ['extracted_text_placeholder']}; graph = StateGraph(OCRProcessState); graph.add_node('process', process_ocr); graph.add_node('validate', validate_ocr); graph.set_entry_point('process'); graph.add_edge('process', 'validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()