from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SandCastState(TypedDict): specs: dict; validation_errors: List[str]; def validate_material(state: SandCastState): if 'grade' not in state['specs']: state['validation_errors'].append('Missing material grade'); return state; def validate_dimensions(state: SandCastState): if state['specs'].get('tolerance', 0) > 0.5: state['validation_errors'].append('Tolerance exceeds limit'); return state; workflow = StateGraph(SandCastState); workflow.add_node('check_material', validate_material); workflow.add_node('check_dims', validate_dimensions); workflow.set_entry_point('check_material'); workflow.add_edge('check_material', 'check_dims'); workflow.add_edge('check_dims', END); graph = workflow.compile()