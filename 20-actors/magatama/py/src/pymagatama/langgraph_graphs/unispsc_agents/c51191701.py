from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_code: str; purity: float; validated: bool; def check_purity(state: ProcureState): return {'validated': state['purity'] >= 0.99}; def route_procurement(state: ProcureState): return 'end' if state['validated'] else 'reject'; graph = StateGraph(ProcureState); graph.add_node('verify', check_purity); graph.add_edge('verify', 'route'); graph.set_entry_point('verify'); graph.add_conditional_edges('route', route_procurement, {'end': END, 'reject': END}); graph = graph.compile()