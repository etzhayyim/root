from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): content: str; validated: bool; def check_safety(state: State): state['validated'] = 'EN71' in state['content']; return state; def finalize(state: State): return {'content': 'Validated' if state['validated'] else 'Rejected'}; graph = StateGraph(State); graph.add_node('safety_check', check_safety); graph.add_node('finalizer', finalize); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'finalizer'); graph.add_edge('finalizer', END); graph = graph.compile()