from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RoofFasciaState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: RoofFasciaState): state['validation_results'].append('Material ISO/ASTM check passed'); return state; def check_dimensions(state: RoofFasciaState): state['validation_results'].append('Compliance with BSI dimensional standards confirmed'); return state; graph = StateGraph(RoofFasciaState); graph.add_node('material_check', validate_material); graph.add_node('dim_check', check_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dim_check'); graph.add_edge('dim_check', END); graph = graph.compile()