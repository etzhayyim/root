from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class GearState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: GearState): errors = []; if state['specs'].get('module', 0) <= 0: errors.append('Invalid module'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(GearState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()