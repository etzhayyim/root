from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipeLayerState(TypedDict): specs: dict; compliance_checked: bool; approved: bool; def validate_specs(state: PipeLayerState): state['compliance_checked'] = all(k in state['specs'] for k in ['lifting_capacity', 'emission_standard']); return state; def check_approval(state: PipeLayerState): state['approved'] = state['compliance_checked']; return state; graph = StateGraph(PipeLayerState); graph.add_node('validate', validate_specs); graph.add_node('approve', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()