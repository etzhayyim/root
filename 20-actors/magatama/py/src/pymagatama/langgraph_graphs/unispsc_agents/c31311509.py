from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: PipeState): state['validation_results'].append('Material verified: SUS304L') if state['specs'].get('grade') == 'SUS304L' else state['validation_results'].append('Material mismatch'); return state; def validate_welding(state: PipeState): state['validation_results'].append('ASME welding standard verified') if state['specs'].get('welding') == 'ASME' else state['validation_results'].append('Welding invalid'); return state; graph = StateGraph(PipeState); graph.add_node('material_check', validate_materials); graph.add_node('weld_check', validate_welding); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'weld_check'); graph.add_edge('weld_check', END); graph = graph.compile()