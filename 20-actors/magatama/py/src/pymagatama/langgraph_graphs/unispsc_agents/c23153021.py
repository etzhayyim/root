from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ControllerState(TypedDict): controller_specs: dict; validation_results: List[str]; graph = StateGraph(ControllerState); def validate_specs(state: ControllerState): return {'validation_results': ['Voltage Check Passed', 'Protocol Compatibility Verified']}; def compile_report(state: ControllerState): return {'validation_results': state['validation_results'] + ['Report Generated']}; graph.add_node('validate', validate_specs); graph.add_node('report', compile_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); app = graph.compile()