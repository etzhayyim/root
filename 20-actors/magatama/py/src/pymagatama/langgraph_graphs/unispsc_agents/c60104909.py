from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; def validate_magnetism_specs(state: GraphState): validated = {k: True for k in state['specs']}; return {'validation_results': [f'Validation complete for {list(state.keys())}']}; graph = StateGraph(GraphState); graph.add_node('validate', validate_magnetism_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()