from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OphthalmicNeedleState(TypedDict): needle_id: str; compliance_docs: List[str]; inspection_status: bool; def validate_regulatory(state: OphthalmicNeedleState): state['compliance_docs'] = [doc for doc in state.get('compliance_docs', []) if 'ISO-13485' in doc]; return state; def perform_inspection(state: OphthalmicNeedleState): state['inspection_status'] = True; return state; graph = StateGraph(OphthalmicNeedleState); graph.add_node('validate', validate_regulatory); graph.add_node('inspect', perform_inspection); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()