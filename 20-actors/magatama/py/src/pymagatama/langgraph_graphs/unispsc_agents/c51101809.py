from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; purity: float; compliant: bool; def validate_purity(state: PharmaState): return {'compliant': state['purity'] >= 99.0}; def check_gmp(state: PharmaState): return {'compliant': state['compliant']}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('gmp_check', check_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', 'gmp_check'); graph.add_edge('gmp_check', END); graph = graph.compile()