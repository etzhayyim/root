from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RoadProjectState(TypedDict): site_specs: str; engineering_review: bool; budget_compliance: bool; def validate_specs(state: RoadProjectState): return {'engineering_review': True} if state['site_specs'] else {'engineering_review': False}; def check_budget(state: RoadProjectState): return {'budget_compliance': True}; graph = StateGraph(RoadProjectState); graph.add_node('validate', validate_specs); graph.add_node('budget', check_budget); graph.add_edge('validate', 'budget'); graph.add_edge('budget', END); graph.set_entry_point('validate'); graph = graph.compile()