from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MRPState(TypedDict): requirements: dict; validation_report: list; software_config: dict; def validate_mrp_config(state: MRPState): return {'validation_report': ['Config validated']}; def finalize_logistics_mapping(state: MRPState): return {'software_config': {'status': 'mapped'}}; graph = StateGraph(MRPState); graph.add_node('validate', validate_mrp_config); graph.add_node('finalize', finalize_logistics_mapping); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()