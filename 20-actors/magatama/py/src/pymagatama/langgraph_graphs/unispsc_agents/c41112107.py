from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TransducerState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: TransducerState): state['valid'] = all(k in state['specs'] for k in ['input', 'output']); return state; graph = StateGraph(TransducerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()