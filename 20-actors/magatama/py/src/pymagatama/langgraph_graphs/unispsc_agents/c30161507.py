from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DrywallState(TypedDict): compound_type: str; voc_compliant: bool; structural_integrity: bool; graph = StateGraph(DrywallState); def validate_spec(state: DrywallState): return {'voc_compliant': True}; def assess_logistics(state: DrywallState): return {'structural_integrity': True}; graph.add_node('spec_validation', validate_spec); graph.add_node('logistics_check', assess_logistics); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', 'logistics_check'); graph.add_edge('logistics_check', END); graph = graph.compile()