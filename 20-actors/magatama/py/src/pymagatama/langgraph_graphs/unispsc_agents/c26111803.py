from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BeltState(TypedDict): specs: dict; valid: bool; def validate_specs(state: BeltState): state['valid'] = 'outer_diameter' in state['specs'] and 'material' in state['specs']; return state; def check_compliance(state: BeltState): print('Checking compliance...'); return state; graph = StateGraph(BeltState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()