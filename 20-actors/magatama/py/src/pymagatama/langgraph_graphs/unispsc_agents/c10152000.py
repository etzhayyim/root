from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class GrainState(TypedDict): messages: Annotated[list, add_messages]; quality_score: float; inspector_id: str; def validate_quality(state: GrainState): return { 'quality_score': 0.95 if 'verified' in state['messages'][-1].content else 0.5 }; def check_compliance(state: GrainState): return { 'next': 'end' }; builder = StateGraph(GrainState); builder.add_node('validate', validate_quality); builder.add_node('compliance', check_compliance); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); builder.set_entry_point('validate'); graph = builder.compile()