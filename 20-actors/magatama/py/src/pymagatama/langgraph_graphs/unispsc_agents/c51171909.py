from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): medicine_name: str; gmp_certified: bool; quality_check_passed: bool; def validate_gmp(state: PharmState): return {'gmp_certified': True} if state['medicine_name'] == 'Omeprazole' else {'gmp_certified': False}; def final_approval(state: PharmState): return {'quality_check_passed': state['gmp_certified']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_gmp); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()