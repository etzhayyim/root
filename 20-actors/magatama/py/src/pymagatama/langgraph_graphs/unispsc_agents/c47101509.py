from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): flow_rate: float; concentration: float; validated: bool; def validate_dosing_specs(state: State): state['validated'] = state.get('flow_rate', 0) > 0 and state.get('concentration', 0) > 0; return state; graph = StateGraph(State); graph.add_node('validate', validate_dosing_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()