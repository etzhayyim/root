from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmergencyCaseState(TypedDict): specs: dict; compliance_status: bool; inventory_id: str; def validate_specs(state: EmergencyCaseState): is_valid = all(key in state['specs'] for key in ['ip_rating', 'material']); return {'compliance_status': is_valid}; def route_procurement(state: EmergencyCaseState): return 'approve' if state['compliance_status'] else 'reject'; graph = StateGraph(EmergencyCaseState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()