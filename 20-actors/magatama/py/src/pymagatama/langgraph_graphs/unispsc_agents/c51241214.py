from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_name: str; standard: str; approved: bool; def validate_grade(state: ProcureState): return {'approved': state['standard'] == 'USP'}; def check_compliance(state: ProcureState): return {'approved': state['approved'] and 'safety_data' in state}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_grade); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()