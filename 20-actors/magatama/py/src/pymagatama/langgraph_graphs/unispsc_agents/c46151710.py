from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): fingerprint_powder_spec: dict; analysis_result: str; def validate_powder(state: ProcState): return {'analysis_result': 'PASS' if state['fingerprint_powder_spec'].get('particle_size') < 20 else 'FAIL'}; graph = StateGraph(ProcState); graph.add_node('validation', validate_powder); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()