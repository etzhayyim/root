from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BookProcurementState(TypedDict): title: str; isbn: str; valid: bool; validation_log: List[str]; def validate_book(state: BookProcurementState): state['valid'] = len(state['isbn']) == 13; state['validation_log'].append('ISBN format checked'); return state; graph = StateGraph(BookProcurementState); graph.add_node('validate', validate_book); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()