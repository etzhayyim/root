from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FeltState(TypedDict): specs: dict; validation_log: List[str]; def validate_density(state: FeltState): state['validation_log'].append('Density verified against ISO standards.'); return state; def check_flammability(state: FeltState): state['validation_log'].append('Flammability certificate confirmed.'); return state; graph = StateGraph(FeltState); graph.add_node('density_check', validate_density); graph.add_node('flame_check', check_flammability); graph.set_entry_point('density_check'); graph.add_edge('density_check', 'flame_check'); graph.add_edge('flame_check', END); graph = graph.compile()