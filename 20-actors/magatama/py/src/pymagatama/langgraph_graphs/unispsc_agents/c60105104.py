from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftKitState(TypedDict): kit_components: List[str]; safety_verified: bool; export_check_passed: bool; def validate_compliance(state: AircraftKitState): state['safety_verified'] = True; return state; def check_export(state: AircraftKitState): state['export_check_passed'] = True; return state; graph = StateGraph(AircraftKitState); graph.add_node('compliance', validate_compliance); graph.add_node('export', check_export); graph.add_edge('compliance', 'export'); graph.add_edge('export', END); graph.set_entry_point('compliance'); graph = graph.compile()