from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SparState(TypedDict): spar_id: str; structural_specs: dict; inspection_results: List[str]; approved: bool; def validate_specs(state: SparState): state['approved'] = all(k in state['structural_specs'] for k in ['tensile_strength', 'alloy_grade']); return state; def run_ndt(state: SparState): state['inspection_results'].append('Ultrasonic Testing Complete'); return state; graph = StateGraph(SparState); graph.add_node('validate', validate_specs); graph.add_node('ndt', run_ndt); graph.set_entry_point('validate'); graph.add_edge('validate', 'ndt'); graph.add_edge('ndt', END); graph = graph.compile()