from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetyState(TypedDict): safety_certs: List[str]; vehicle_match: bool; compliant: bool; def validate_safety(state: SafetyState): return {'compliant': 'ECE R44/04' in state.get('safety_certs', []) and state.get('vehicle_match')}; graph = StateGraph(SafetyState); graph.add_node('validate', validate_safety); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()