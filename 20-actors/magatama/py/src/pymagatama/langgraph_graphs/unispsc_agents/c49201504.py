from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class RowingState(TypedDict): vendor_docs: dict; compliance_status: bool; workflow_steps: Annotated[Sequence[str], operator.add]; def validate_specs(state: RowingState): return {'compliance_status': 'capacity' in state['vendor_docs'], 'workflow_steps': ['spec_validation']}; def approval_node(state: RowingState): return {'workflow_steps': ['manual_approval']}; graph = StateGraph(RowingState); graph.add_node('validate', validate_specs); graph.add_node('approve', approval_node); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.compile()