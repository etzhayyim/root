from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LightingState(TypedDict): console_id: str; specs: dict; validation_passed: bool; def validate_console(state: LightingState): state['validation_passed'] = all(k in state['specs'] for k in ['dmx_ports', 'max_channels']); return state; graph = StateGraph(LightingState); graph.add_node('validate', validate_console); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()