from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_tolerance(state: CastingState): return {'validation_results': ['Tolerance verified']}; def check_alloy(state: CastingState): return {'validation_results': ['Alloy composition confirmed']}; graph = StateGraph(CastingState); graph.add_node('validate_tolerance', validate_tolerance); graph.add_node('check_alloy', check_alloy); graph.set_entry_point('validate_tolerance'); graph.add_edge('validate_tolerance', 'check_alloy'); graph.add_edge('check_alloy', END); compiled_graph = graph.compile()