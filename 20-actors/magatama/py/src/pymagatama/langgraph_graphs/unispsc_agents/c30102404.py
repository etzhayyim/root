from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SteelRodState(TypedDict): rod_id: str; tensile_strength: float; chemical_report: dict; approved: bool; def validate_specs(state: SteelRodState): state['approved'] = state['tensile_strength'] >= 400 and 'C' in state['chemical_report']; return state; def check_compliance(state: SteelRodState): return 'approved' if state['approved'] else 'rejected'; graph = StateGraph(SteelRodState); graph.add_node('validate', validate_specs); graph.add_edge('__start__', 'validate'); graph.add_edge('validate', END); graph = graph.compile()