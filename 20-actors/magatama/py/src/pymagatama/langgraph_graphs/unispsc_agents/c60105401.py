from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EducationState(TypedDict): content: str; validated: bool; compliance: bool; def validate_content(state: EducationState): return {'validated': True}; def check_compliance(state: EducationState): return {'compliance': True}; graph = StateGraph(EducationState); graph.add_node('validate', validate_content); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()