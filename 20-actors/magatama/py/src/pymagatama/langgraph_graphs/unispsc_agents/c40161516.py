from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): specs: dict; validation_passed: bool; model_code: str; def validate_specs(state: FilterState): state['validation_passed'] = state['specs'].get('pressure_rating', 0) > 0; return state; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()