from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ThermometerState(TypedDict): vendor_docs: List[str]; compliance_verfied: bool; score: float; def validate_certification(state: ThermometerState): return {'compliance_verfied': 'ISO_13485' in state['vendor_docs']}; def check_accuracy(state: ThermometerState): return {'score': 1.0 if state['compliance_verfied'] else 0.0}; graph = StateGraph(ThermometerState); graph.add_node('cert_check', validate_certification); graph.add_node('accuracy_check', check_accuracy); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'accuracy_check'); graph.add_edge('accuracy_check', END); graph = graph.compile()