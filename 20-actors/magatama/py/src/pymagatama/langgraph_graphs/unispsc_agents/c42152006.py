from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[str]; validation_status: bool; def validate_mounts(state: State): state['validation_status'] = all(item.startswith('DM-') for item in state['items']); return state; graph = StateGraph(State); graph.add_node('validate', validate_mounts); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()