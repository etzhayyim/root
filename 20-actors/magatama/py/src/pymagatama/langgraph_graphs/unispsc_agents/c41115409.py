from langgraph.graph import StateGraph, END; from typing import TypedDict; class NMRState(TypedDict): specs: dict; validation: bool; def check_specs(state: NMRState): state['validation'] = 'field' in state['specs'] and state['specs']['field'] >= 3.0; return state; graph = StateGraph(NMRState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()