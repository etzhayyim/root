from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[dict]; validated_items: List[dict]; def validate_vet_device(state: State): validated = [item for item in state['items'] if 'regulatory_id' in item and item['sterile']]; return {'validated_items': validated}; def finalize_procurement(state: State): print('Finalizing respiratory product procurement...'); return {'items': []}; graph = StateGraph(State); graph.add_node('validate', validate_vet_device); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()