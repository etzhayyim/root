from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TitaniumSpecState(TypedDict): spec_data: dict; validation_results: List[str]; def validate_material(state: TitaniumSpecState): state['validation_results'].append('Material grade verified against ASTM B265'); return state; def perform_ndt_check(state: TitaniumSpecState): state['validation_results'].append('NDT protocol confirmed for weld integrity'); return state; graph = StateGraph(TitaniumSpecState); graph.add_node('validate_material', validate_material); graph.add_node('ndt_check', perform_ndt_check); graph.add_edge('validate_material', 'ndt_check'); graph.add_edge('ndt_check', END); graph.set_entry_point('validate_material'); graph = graph.compile()