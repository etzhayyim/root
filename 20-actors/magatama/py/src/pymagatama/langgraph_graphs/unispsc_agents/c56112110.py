from langgraph.graph import StateGraph, END; from typing import TypedDict; class ChairState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ChairState): state["valid"] = "load_capacity" in state["specs"] and state["specs"]["load_capacity"] > 0; return state; graph = StateGraph(ChairState); graph.add_node("validator", validate_specs); graph.set_entry_point("validator"); graph.add_edge("validator", END); app = graph.compile()