from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TicketSystemState(TypedDict): vendor: str; specs_met: bool; logs: List[str]; def validate_specs(state: TicketSystemState): state['logs'].append('Validating ticket machine specs...'); state['specs_met'] = True; return state; def check_integration(state: TicketSystemState): state['logs'].append('Checking POS integration...'); return state; graph = StateGraph(TicketSystemState); graph.add_node('validate', validate_specs); graph.add_node('integration', check_integration); graph.set_entry_point('validate'); graph.add_edge('validate', 'integration'); graph.add_edge('integration', END); graph = graph.compile()