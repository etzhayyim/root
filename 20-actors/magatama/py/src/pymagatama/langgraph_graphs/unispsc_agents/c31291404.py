from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_passed: bool; status: str; def validate_metallurgy(state: ProcurementState): state['validation_passed'] = 'alloy_cert' in state['specs'] and 'tensile' in state['specs']; state['status'] = 'Validated' if state['validation_passed'] else 'Pending Review'; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_metallurgy); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()