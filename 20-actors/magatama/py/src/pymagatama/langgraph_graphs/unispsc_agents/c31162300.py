from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HardwareState(TypedDict): hardware_parts: List[str]; validation_results: List[str]; def validate_spec(state: HardwareState): results = ['Passed' if len(part) > 2 else 'Failed' for part in state['hardware_parts']]; return {'validation_results': results}; graph = StateGraph(HardwareState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()