from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EKGState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_medical_cert(state: EKGState): state['validation_errors'] = [i['name'] for i in state['items'] if 'cert' not in i]; return state; def check_compatibility(state: EKGState): return state; graph = StateGraph(EKGState); graph.add_node('validate_certs', validate_medical_cert); graph.add_node('check_comp', check_compatibility); graph.set_entry_point('validate_certs'); graph.add_edge('validate_certs', 'check_comp'); graph.add_edge('check_comp', END); graph = graph.compile()