from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlasmaState(TypedDict): model_info: dict; validation_results: List[str]; graph = StateGraph(PlasmaState); def validate_specs(state: PlasmaState): state['validation_results'].append('Size and power verified'); return state; def check_compatibility(state: PlasmaState): state['validation_results'].append('Mounting standard checked'); return state; graph.add_node('validate', validate_specs); graph.add_node('compat', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compat'); graph.add_edge('compat', END); compiled_graph = graph.compile()