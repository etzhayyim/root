from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProteinState(TypedDict): items: List[str]; validation_results: dict; status: str; def validate_purity(state: ProteinState): state['validation_results']['purity'] = 'verified'; return state; def check_storage(state: ProteinState): state['validation_results']['storage'] = 'compliant'; return state; graph = StateGraph(ProteinState); graph.add_node('validate', validate_purity); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); compiled_graph = graph.compile()