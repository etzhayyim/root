from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): batch_id: str; purity: float; is_approved: bool; def validate_purity(state: DrugState): state['is_approved'] = state['purity'] >= 99.0; return state; def check_batch(state: DrugState): print(f'Processing batch {state['batch_id']}'); return state; graph = StateGraph(DrugState); graph.add_node('check_batch', check_batch); graph.add_node('validate_purity', validate_purity); graph.add_edge('check_batch', 'validate_purity'); graph.add_edge('validate_purity', END); graph.set_entry_point('check_batch'); graph = graph.compile()