from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): specs: dict; valid: bool; error: str; def validate_insert(state: ToolState): state['valid'] = 'ISO_code' in state['specs'] and 'grade_classification' in state['specs']; return state; def check_export(state: ToolState): state['error'] = 'Dual-use check required' if state['valid'] else None; return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_insert); graph.add_node('export', check_export); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph.set_entry_point('validate'); graph = graph.compile()