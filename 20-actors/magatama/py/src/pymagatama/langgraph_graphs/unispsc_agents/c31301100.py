from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: ForgingState): state['validation_results'].append('DIM_CHECK_PASSED'); return state; def check_material_cert(state: ForgingState): state['validation_results'].append('MAT_CERT_VERIFIED'); return state; graph = StateGraph(ForgingState); graph.add_node('validate_dims', validate_dimensions); graph.add_node('check_certs', check_material_cert); graph.set_entry_point('validate_dims'); graph.add_edge('validate_dims', 'check_certs'); graph.add_edge('check_certs', END); compiled_graph = graph.compile()