from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class RawMaterialState(TypedDict): material_data: Dict; analysis_results: List[str]; inspection_status: bool; def validate_purity(state: RawMaterialState): state['analysis_results'].append('Purity check initiated.'); return {'inspection_status': True}; def update_compliance(state: RawMaterialState): state['analysis_results'].append('Export control verified.'); return {}; graph = StateGraph(RawMaterialState); graph.add_node('validate', validate_purity); graph.add_node('compliance', update_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()