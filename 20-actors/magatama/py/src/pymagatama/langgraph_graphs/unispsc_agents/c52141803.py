from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ElectricBlanketState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: ElectricBlanketState): state['compliance'] = 'Safety Certification' in state['specs'] and state['specs']['Rated Voltage'] == '100V'; return state; def final_decision(state: ElectricBlanketState): state['approved'] = state['compliance']; return state; graph = StateGraph(ElectricBlanketState); graph.add_node('validate', validate_specs); graph.add_node('decision', final_decision); graph.set_entry_point('validate'); graph.add_edge('validate', 'decision'); graph.add_edge('decision', END); app = graph.compile()