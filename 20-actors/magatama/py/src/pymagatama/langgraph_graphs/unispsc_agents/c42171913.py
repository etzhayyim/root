from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EmergencyBagState(TypedDict): bag_specs: dict; compliance_status: bool; validation_logs: List[str]; def validate_medical_specs(state: EmergencyBagState): state['compliance_status'] = 'waterproofing' in state['bag_specs'] and 'material' in state['bag_specs']; return state; def finalize_review(state: EmergencyBagState): state['validation_logs'].append('Compliance review completed'); return state; graph = StateGraph(EmergencyBagState); graph.add_node('validate', validate_medical_specs); graph.add_node('finalize', finalize_review); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()