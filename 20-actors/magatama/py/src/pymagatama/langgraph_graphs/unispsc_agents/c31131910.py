from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): dimensions: dict; material_cert: bool; approved: bool; def validate_dimensions(state: ForgingState): state['approved'] = all(d > 0 for d in state['dimensions'].values()); return state; def check_materials(state: ForgingState): state['material_cert'] = True; return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_dimensions); graph.add_node('materials', check_materials); graph.set_entry_point('validate'); graph.add_edge('validate', 'materials'); graph.add_edge('materials', END); graph = graph.compile()