from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): vpn_config: dict; security_audit_passed: bool; nodes: List[str]; def validate_security(state: NetworkState): state['security_audit_passed'] = 'AES-256' in str(state['vpn_config']); return state; def provision_tunnel(state: NetworkState): return {'nodes': ['tunnel_active']}; graph = StateGraph(NetworkState); graph.add_node('security_check', validate_security); graph.add_node('provision', provision_tunnel); graph.set_entry_point('security_check'); graph.add_edge('security_check', 'provision'); graph.add_edge('provision', END); app = graph.compile()