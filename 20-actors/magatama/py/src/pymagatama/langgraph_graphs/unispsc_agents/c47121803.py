from langgraph.graph import StateGraph, END; from typing import TypedDict; class CleaningToolState(TypedDict): tool_type: str; specs_verified: bool; def validate_specs(state: CleaningToolState): return {'specs_verified': state.get('tool_type') == 'squeegee'}; graph = StateGraph(CleaningToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()