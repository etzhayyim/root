from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_materials(state: AssemblyState): state['log'].append('Verifying MTR compliance...'); state['valid'] = state['specs'].get('tensile_strength', 0) > 400; return state; def inspect_dimensions(state: AssemblyState): state['log'].append('Checking dimensional tolerances...'); return state; graph = StateGraph(AssemblyState); graph.add_node('material_check', validate_materials); graph.add_node('dimension_check', inspect_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dimension_check'); graph.add_edge('dimension_check', END); graph = graph.compile()