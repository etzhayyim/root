from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; gmp_compliant: bool; temp_logs: list; validated: bool; def validate_gmp(state: PharmaState): state['validated'] = state['gmp_compliant'] and len(state['temp_logs']) > 0; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()