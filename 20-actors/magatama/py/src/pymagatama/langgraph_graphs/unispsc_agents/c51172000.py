from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): medicine_name: str; validation_status: bool; compliance_docs: List[str]; def validate_medicine(state: DrugState): status = True if state['medicine_name'] else False; return {'validation_status': status}; def check_compliance(state: DrugState): return {'compliance_docs': ['GMP_Cert', 'CoA']}; graph = StateGraph(DrugState); graph.add_node('validate', validate_medicine); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.compile()