from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BronzeChannelState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_alloy(state: BronzeChannelState): state['valid'] = 'ASTM_B' in state['specs'].get('standard', ''); return state; def check_dimensions(state: BronzeChannelState): state['logs'].append('Verifying dimensional tolerance...'); return state; graph = StateGraph(BronzeChannelState); graph.add_node('validate', validate_alloy); graph.add_node('dimensions', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', 'dimensions'); graph.add_edge('dimensions', END); app = graph.compile()