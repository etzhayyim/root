from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CastState(TypedDict): specs: dict; validation_errors: List[str]; def validate_material(state: CastState): state['validation_errors'] = [] if 'grade' in state['specs'] else ['Missing Grade']; return state; def check_dimensions(state: CastState): return state; graph = StateGraph(CastState); graph.add_node('validate', validate_material); graph.add_node('check_dims', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_dims'); graph.add_edge('check_dims', END); compiled_graph = graph.compile()