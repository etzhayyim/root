from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): specs: dict; valid: bool; def check_specs(state: ProcureState): return {'valid': state['specs'].get('voltage') is not None}; graph = StateGraph(ProcureState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()