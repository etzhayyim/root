from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: List[str]; validated: bool; log: List[str]; def validate_specs(state: State): state['validated'] = all(len(s) > 0 for s in state.get('specs', [])); state['log'].append('Validation complete'); return state; def finalize(state: State): state['log'].append('Procurement finalized'); return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()