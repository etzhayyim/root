from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CokeProcurementState(TypedDict): specs: dict; validation_results: Annotated[list[str], operator.add]; compliance_score: float; def validate_quality(state: CokeProcurementState): results = []; specs = state.get('specs', {}); if specs.get('sulfur_content', 0) > 0.5: results.append('Sulfur content exceeds limit'); return {'validation_results': results, 'compliance_score': 0.8}; def run_procurement_workflow(state: CokeProcurementState): print('Processing procurement'); return {'compliance_score': 1.0}; graph = StateGraph(CokeProcurementState); graph.add_node('validate', validate_quality); graph.add_node('process', run_procurement_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()