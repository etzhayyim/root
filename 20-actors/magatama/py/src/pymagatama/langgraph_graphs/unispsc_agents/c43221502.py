from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): input_data: str; validation_status: bool; config_log: list; graph = StateGraph(AgentState); def validate_acdspec(state: AgentState): return {'validation_status': True, 'config_log': ['Spec verified']} if 'capacity' in state['input_data'] else {'validation_status': False}; def assemble_workflow(state: AgentState): return {'config_log': state['config_log'] + ['Workflow assembled']}; graph.add_node('validate', validate_acdspec); graph.add_node('assemble', assemble_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph = graph.compile()