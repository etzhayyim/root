from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BobbinState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: BobbinState): specs = state['specs']; state['valid'] = 'material' in specs and 'tolerance' in specs; state['logs'].append('Validation complete'); return state; graph = StateGraph(BobbinState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()