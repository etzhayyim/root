from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WeldingState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: WeldingState): state['validation_errors'] = [] if state['specs'].get('Safety_Interlock_Certification') != 'ISO10218' else []; return state; def check_export_control(state: WeldingState): if state['specs'].get('Payload_Capacity_kg', 0) > 100: state['validation_errors'].append('Dual-use export license required'); return state; graph = StateGraph(WeldingState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.compile()