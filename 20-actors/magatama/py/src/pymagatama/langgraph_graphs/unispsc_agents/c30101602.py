from langgraph.graph import StateGraph, END; from typing import TypedDict; class AlloyState(TypedDict): alloy_type: str; specs: dict; valid: bool; def validate_alloy(state: AlloyState): state['valid'] = 'composition' in state['specs'] and state['specs']['tensile_strength'] > 0; return state; graph = StateGraph(AlloyState); graph.add_node('validate', validate_alloy); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()