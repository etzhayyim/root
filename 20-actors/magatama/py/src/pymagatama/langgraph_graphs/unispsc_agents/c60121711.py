from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: ProcState): specs = state['specs']; is_valid = all(key in specs for key in ['temp_range', 'surface_mat']); state['valid'] = is_valid; state['logs'].append('Validation complete.'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()