from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_name: str; specs: dict; compliance_status: bool; def validate_purity(state: ChemicalState): state['compliance_status'] = state['specs'].get('purity', 0) > 99.0; return state; def check_hazards(state: ChemicalState): state['compliance_status'] = state['compliance_status'] and not state['specs'].get('is_hazardous', False); return state; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_hazards); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()