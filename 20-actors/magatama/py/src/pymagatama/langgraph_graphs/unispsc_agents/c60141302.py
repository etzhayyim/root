from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ConstructionSetState(TypedDict): items: List[str]; validated: bool; errors: List[str]; def validate_set(state: ConstructionSetState): validated = all(len(item) > 0 for item in state['items']); return {'validated': validated, 'errors': [] if validated else ['Missing component data']}; graph = StateGraph(ConstructionSetState); graph.add_node('validate', validate_set); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()