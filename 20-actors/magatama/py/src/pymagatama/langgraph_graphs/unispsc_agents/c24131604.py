from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LyophilizerState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(LyophilizerState); def validate_vacuum(state): state['validation_results'].append('Vacuum system checked'); return state; def check_compliance(state): state['validation_results'].append('GMP compliance verified'); return state; graph.add_node('vacuum_check', validate_vacuum); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('vacuum_check'); graph.add_edge('vacuum_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()