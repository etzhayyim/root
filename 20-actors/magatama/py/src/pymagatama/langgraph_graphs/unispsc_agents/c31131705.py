from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(ForgingState); def validate_material(state): return {'validation_results': ['Material check passed']}; def validate_geometry(state): return {'validation_results': ['Geometry within tolerance']}; graph.add_node('material_check', validate_material); graph.add_node('geometry_check', validate_geometry); graph.add_edge('material_check', 'geometry_check'); graph.add_edge('geometry_check', END); graph.set_entry_point('material_check'); app = graph.compile()