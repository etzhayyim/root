from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): status: str; spec_check: bool; def validate_capsule(state: ProcState): state['spec_check'] = True; state['status'] = 'Validated'; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_capsule); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()