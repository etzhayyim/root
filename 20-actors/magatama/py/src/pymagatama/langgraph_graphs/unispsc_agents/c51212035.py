from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): botanical_data: dict; compliance_status: bool; def val_quality(state: ProcureState): state['compliance_status'] = state['botanical_data'].get('silymarin', 0) > 80; return state; def final_approval(state: ProcureState): return 'approved' if state['compliance_status'] else 'rejected'; graph = StateGraph(ProcureState); graph.add_node('validate', val_quality); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()