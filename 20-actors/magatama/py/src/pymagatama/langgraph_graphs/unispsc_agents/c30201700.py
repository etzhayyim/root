from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StructureState(TypedDict): struct_id: str; structural_specs: dict; validation_report: list; def validate_specs(state: StructureState): print(f'Validating specs for {state[struct_id]}'); return {'validation_report': ['Structural load check PASSED']}; def finalize_procurement(state: StructureState): return {'validation_report': state['validation_report'] + ['Final procurement approved']}; graph = StateGraph(StructureState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()