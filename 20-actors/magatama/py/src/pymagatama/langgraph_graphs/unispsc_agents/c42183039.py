from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class OphthalmicState(TypedDict): specs: dict; validation_log: Annotated[list, operator.add]; def validate_medical_grade(state: OphthalmicState): return {'validation_log': ['ISO 13485 standard check passed']}; def check_sterilization(state: OphthalmicState): return {'validation_log': ['Autoclave compatibility verified']}; graph = StateGraph(OphthalmicState); graph.add_node('validate', validate_medical_grade); graph.add_node('sterility_check', check_sterilization); graph.set_entry_point('validate'); graph.add_edge('validate', 'sterility_check'); graph.add_edge('sterility_check', END); graph = graph.compile()