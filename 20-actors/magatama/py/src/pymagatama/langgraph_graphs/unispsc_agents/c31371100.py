from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RefractoryState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_thermal_specs(state: RefractoryState): temp = state['specs'].get('Max-Operating-Temperature', 0); status = 'APPROVED' if temp > 1000 else 'REJECTED'; return {'validation_results': [f'Temp check: {status}'], 'status': status}; def check_safety_compliance(state: RefractoryState): return {'validation_results': ['Material safety docs verified']}; graph = StateGraph(RefractoryState); graph.add_node('thermal_check', validate_thermal_specs); graph.add_node('safety_check', check_safety_compliance); graph.add_edge('thermal_check', 'safety_check'); graph.add_edge('safety_check', END); graph.set_entry_point('thermal_check'); graph = graph.compile()