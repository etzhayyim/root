from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CementProcurementState(TypedDict): vendor_docs: List[str]; certification_verified: bool; risk_score: float; def validate_registry(state: CementProcurementState): state['certification_verified'] = True; return state; def assess_risk(state: CementProcurementState): state['risk_score'] = 0.9 if not state['certification_verified'] else 0.1; return state; graph = StateGraph(CementProcurementState); graph.add_node('validate', validate_registry); graph.add_node('assess', assess_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'assess'); graph.add_edge('assess', END); graph = graph.compile()