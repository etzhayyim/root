from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeSpec(TypedDict): alloy: str; pressure: float; weld_integrity: bool; graph = StateGraph(TubeSpec); def validate_specs(state): state['weld_integrity'] = state['pressure'] > 0; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()