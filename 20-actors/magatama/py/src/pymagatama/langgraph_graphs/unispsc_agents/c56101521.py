from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: FurnitureState): e = []; s = state['specs']; if 'Fire Retardance Grade' not in s: e.append('Missing Fire Rating'); state['validation_errors'] = e; state['approved'] = len(e) == 0; return state; graph = StateGraph(FurnitureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()