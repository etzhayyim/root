from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): component_specs: dict; validation_results: List[str]; def validate_stamping(state: State): specs = state['component_specs']; results = []; if specs.get('material') != 'Brass': results.append('Invalid Material'); if specs.get('tolerance', 0.0) > 0.05: results.append('Tolerance Exceeds Limit'); return {'validation_results': results}; graph = StateGraph(State); graph.add_node('validate', validate_stamping); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()