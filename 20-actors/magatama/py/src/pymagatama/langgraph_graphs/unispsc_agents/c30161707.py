from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlooringState(TypedDict): specs: dict; valid: bool; def validate_specs(state: FlooringState): required = ['thickness', 'wear_layer', 'fire_rating']; state['valid'] = all(k in state['specs'] for k in required); return state; graph = StateGraph(FlooringState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()