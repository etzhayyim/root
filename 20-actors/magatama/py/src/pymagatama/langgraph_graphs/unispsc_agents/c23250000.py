from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MetalFormingState(TypedDict): input_spec: str; validation_results: Annotated[list, operator.add]; status: str; def validate_machinery(state: MetalFormingState): results = ['Tonnage Verified', 'Safety Standard Check Passed'] if 'CE' in state['input_spec'] else ['Non-compliant']; return {'validation_results': results, 'status': 'Validated' if 'CE' in state['input_spec'] else 'Rejected'}; graph = StateGraph(MetalFormingState); graph.add_node('validator', validate_machinery); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()