from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): content: dict; status: str; def validate_pharma(state: State): status = 'PASSED' if 'batch_number' in state['content'] and 'temp' in state['content'] else 'FAILED'; return {'status': status}; graph = StateGraph(State); graph.add_node('validate', validate_pharma); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()