from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SampleState(TypedDict): bailer_specs: dict; validation_results: List[str]; def validate_specs(state: SampleState): results = []; specs = state['bailer_specs']; if not specs.get('chem_resistant'): results.append('Failure: Material lacks documentation'); return {'validation_results': results}; def generate_workflow(): graph = StateGraph(SampleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = generate_workflow()