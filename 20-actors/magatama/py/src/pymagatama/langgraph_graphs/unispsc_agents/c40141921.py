from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DuctSpec(TypedDict): material: str; purity: float; validated: bool; def validate_spec(state: DuctSpec): state['validated'] = state['purity'] >= 99.9; return state; def check_export(state: DuctSpec): return 'dual-use' if state['material'] in ['platinum', 'gold'] else 'standard'; graph = StateGraph(DuctSpec); graph.add_node('validate', validate_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()