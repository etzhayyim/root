from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CastState(TypedDict): alloy: str; specs: dict; approved: bool; def check_alloy(state: CastState): return {'approved': state['alloy'] in ['C83600', 'C92200']} if state else {'approved': False}; graph = StateGraph(CastState); graph.add_node('validate_alloy', check_alloy); graph.set_entry_point('validate_alloy'); graph.add_edge('validate_alloy', END); graph = graph.compile()