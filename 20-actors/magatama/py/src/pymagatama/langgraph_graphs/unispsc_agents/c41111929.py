from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadiationProcurementState(TypedDict): sensitivity: str; calibration_date: str; export_control_cleared: bool; graph = StateGraph(RadiationProcurementState); def validate_specs(state: RadiationProcurementState): return {'export_control_cleared': True if state.get('sensitivity') else False}; def check_compliance(state: RadiationProcurementState): return {'compliance': 'Verified'}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()