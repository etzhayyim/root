from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): alloy_type: str; specs: dict; approved: bool; def validate_alloy(state: ForgingState): return {'approved': state['alloy_type'] in ['Aluminum', 'Titanium', 'Copper']}; def check_specs(state: ForgingState): return {'approved': all(v is not None for v in state['specs'].values())}; graph = StateGraph(ForgingState); graph.add_node('validate', validate_alloy); graph.add_node('check_specs', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_specs'); graph.add_edge('check_specs', END); app = graph.compile()