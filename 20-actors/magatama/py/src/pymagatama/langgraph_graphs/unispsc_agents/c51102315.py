from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): ingredient: str; validated: bool; temp_check: bool; def validate_purity(state: PharmaState): return {'validated': True}; def check_storage(state: PharmaState): return {'temp_check': True}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('storage', check_storage); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph.set_entry_point('validate'); graph = graph.compile()