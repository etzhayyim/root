from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProgressState(TypedDict): material_data: dict; validation_errors: List[str]; approved: bool; def validate_spec(state: ProgressState): errors = []; spec = state['material_data']; if not spec.get('material_grade'): errors.append('Missing grade'); return {'validation_errors': errors}; def check_compliance(state: ProgressState): return {'approved': not state['validation_errors']}; graph = StateGraph(ProgressState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()