from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MotorState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; motor_ready: bool; def validate_motor_specs(state: MotorState): return {'validation_results': ['torque_checked', 'thermal_rated'], 'motor_ready': True}; def assemble_robot_arm(state: MotorState): return {'validation_results': ['arm_assembly_success']}; graph = StateGraph(MotorState); graph.add_node('validate', validate_motor_specs); graph.add_node('assemble', assemble_robot_arm); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph.set_entry_point('validate'); graph = graph.compile()