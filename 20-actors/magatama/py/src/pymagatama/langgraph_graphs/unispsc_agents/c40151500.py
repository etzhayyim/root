from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): pump_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: PumpState): specs = state['pump_specs']; errors = []; if specs.get('max_pressure_bar', 0) > 100: errors.append('Pressure rating exceeds standard safety limits'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()