from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: Annotated[list, operator.add]; validation_passed: bool; def validate_specs(state: ProcureState): return {'validation_passed': True} if state['items'] else {'validation_passed': False}; def check_inventory(state: ProcureState): return {'items': ['Part_Validated']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('check', check_inventory); graph.set_entry_point('check'); graph.add_edge('check', 'validate'); graph.add_edge('validate', END); graph = graph.compile()