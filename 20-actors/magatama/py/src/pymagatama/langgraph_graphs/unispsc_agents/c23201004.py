from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class EngineBlockState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: EngineBlockState): return {"validation_report": "Validated against metallurgy and tolerance standards"}; def update_records(state: EngineBlockState): return {"validation_report": "Records updated in ERP"}; graph = StateGraph(EngineBlockState); graph.add_node("validate", validate_specs); graph.add_node("record", update_records); graph.set_entry_point("validate"); graph.add_edge("validate", "record"); graph.add_edge("record", END); graph = graph.compile()