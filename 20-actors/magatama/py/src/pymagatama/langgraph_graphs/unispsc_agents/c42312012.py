from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validation_passed: bool; def validate_staples(state: State): state['validation_passed'] = all(['ISO' in item or 'FDA' in item for item in state.get('items', [])]); return state; graph = StateGraph(State); graph.add_node('validate', validate_staples); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()