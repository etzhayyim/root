from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PALState(TypedDict): vendor: str; part_number: str; verified: bool; export_check_passed: bool; def check_export_compliance(state: PALState): state['export_check_passed'] = True; return state; def validate_spec(state: PALState): state['verified'] = True if state['part_number'] else False; return state; graph = StateGraph(PALState); graph.add_node('export_check', check_export_compliance); graph.add_node('spec_validation', validate_spec); graph.set_entry_point('export_check'); graph.add_edge('export_check', 'spec_validation'); graph.add_edge('spec_validation', END); graph = graph.compile()