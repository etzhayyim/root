from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ToolState(TypedDict): specs: dict; ready: bool; def validate_specs(state: ToolState): return {'ready': 'depth' in state['specs'] and 'material' in state['specs']}; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate');-compiled_graph = graph.compile()