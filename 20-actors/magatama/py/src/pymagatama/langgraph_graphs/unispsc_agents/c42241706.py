from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OrthoticState(TypedDict): device_type: str; specs: dict; compliant: bool; def validate_spec(state: OrthoticState): state['compliant'] = 'fpa_medical_certification' in state['specs']; return state; def check_safety(state: OrthoticState): return 'compliant' if state['compliant'] else 'reject'; graph = StateGraph(OrthoticState); graph.add_node('validate', validate_spec); graph.add_node('safety_check', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()