from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): doc: dict; valid: bool; def validate_pharma(state: ProcState): state['valid'] = 'gmp_certification_level' in state['doc'] and state['doc'].get('purity_percentage', 0) > 99.0; return state; graph = StateGraph(ProcState); graph.add_node('validation', validate_pharma); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()