from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlankProcurementState(TypedDict): specs: dict; validation_results: List[str]; def validate_moisture(state: PlankProcurementState): moisture = state['specs'].get('moisture'); state['validation_results'].append('Pass' if moisture < 15 else 'Fail'); return state; def validate_dimensions(state: PlankProcurementState): state['validation_results'].append('Dimensions Verified'); return state; graph = StateGraph(PlankProcurementState); graph.add_node('moisture_check', validate_moisture); graph.add_node('dimension_check', validate_dimensions); graph.set_entry_point('moisture_check'); graph.add_edge('moisture_check', 'dimension_check'); graph.add_edge('dimension_check', END); graph = graph.compile()