from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BearingState(TypedDict): specs: dict; validation_report: str; graph = StateGraph(BearingState); def validate_specs(state: BearingState): state['validation_report'] = 'High precision tolerances within ISO standards' if state['specs'].get('tolerance') else 'Missing tolerance data'; return state; def check_export_control(state: BearingState): return 'control_required' if state['specs'].get('speed_rating', 0) > 20000 else 'standard'; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()