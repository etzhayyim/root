from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MowerState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: MowerState): return {'compliance': state['specs'].get('cutting_width', 0) > 0}; def approve_procurement(state: MowerState): return {'approved': state['compliance']}; graph = StateGraph(MowerState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()