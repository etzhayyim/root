from typing import TypedDict,List; from langgraph.graph import StateGraph, END; class GameAccessoryState(TypedDict): accessory_type: str; compatibility_verified: bool; is_compliant: bool; def validate_accessory(state: GameAccessoryState): state['compatibility_verified'] = True if state['accessory_type'] else False; return state; def check_compliance(state: GameAccessoryState): state['is_compliant'] = state['compatibility_verified']; return state; graph = StateGraph(GameAccessoryState); graph.add_node('validate', validate_accessory); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()