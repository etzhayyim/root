from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): pump_specs: dict; validation_errors: List[str]; def validate_pump(state: PumpState): errors = []; if state['pump_specs'].get('pressure', 0) > 50: errors.append('Pressure rating exceeds safety limits'); return {'validation_errors': errors}; graph = StateGraph(PumpState); graph.add_node('validate', validate_pump); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()