from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; components: List[str]; validated: bool; def validate_components(state: KitState): status = len(state['components']) > 0; return {'validated': status}; def finalize_procurement(state: KitState): return {'validated': True}; graph = StateGraph(KitState); graph.add_node('validate', validate_components); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()