from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class GraphState(TypedDict): input_data: dict; validation_results: List[str]; def validate_instrument(state: GraphState): return {'validation_results': ['Compliance Verified']}; def run_calibration_check(state: GraphState): return {'validation_results': state['validation_results'] + ['Calibration Logged']}; graph = StateGraph(GraphState); graph.add_node('validate', validate_instrument); graph.add_node('calibrate', run_calibration_check); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); graph.set_entry_point('validate'); graph = graph.compile()