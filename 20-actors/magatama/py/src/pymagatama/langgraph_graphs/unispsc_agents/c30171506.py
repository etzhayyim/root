from langgraph.graph import StateGraph, END; from typing import TypedDict; class DoorState(TypedDict): specs: dict; valid: bool; def check_specs(state: DoorState): state['valid'] = 'security_rating' in state['specs'] and state['specs']['thermal_u_value'] < 0.5; return state; graph = StateGraph(DoorState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()