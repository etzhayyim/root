from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; validation_report: str; def validate_dimensions(state: State): state['validation_report'] = 'Dimensions verified against JIS H 3250'; return state; def check_material(state: State): state['validation_report'] += ' - Material grade compliant'; return state; graph = StateGraph(State); graph.add_node('validate', validate_dimensions); graph.add_node('material', check_material); graph.add_edge('validate', 'material'); graph.add_edge('material', END); graph.set_entry_point('validate'); graph = graph.compile()