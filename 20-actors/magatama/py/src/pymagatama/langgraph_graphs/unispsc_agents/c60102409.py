from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EduProcurementState(TypedDict): items: List[str]; validated: bool; audit_log: List[str]; def validate_math_tokens(state: EduProcurementState): state['validated'] = all(item.startswith('Safe-') for item in state['items']); state['audit_log'].append('Safety validation complete'); return state; graph = StateGraph(EduProcurementState); graph.add_node('validate', validate_math_tokens); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()