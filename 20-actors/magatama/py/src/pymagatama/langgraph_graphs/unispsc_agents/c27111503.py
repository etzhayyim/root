from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UtilityKnifeState(TypedDict): knife_type: str; specs: dict; safety_compliant: bool; def check_safety(state: UtilityKnifeState) -> UtilityKnifeState: state['safety_compliant'] = state['specs'].get('locking_mechanism') == 'auto-retract'; return state; def validate_specs(state: UtilityKnifeState) -> str: return 'safe' if state['safety_compliant'] else 'manual_review'; graph = StateGraph(UtilityKnifeState); graph.add_node('safety_check', check_safety); graph.add_edge('safety_check', END); graph.set_entry_point('safety_check'); graph = graph.compile()