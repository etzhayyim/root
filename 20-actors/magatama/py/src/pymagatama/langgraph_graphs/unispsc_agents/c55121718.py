from langgraph.graph import StateGraph, END; from typing import TypedDict; class SignageState(TypedDict): content: str; validated: bool; spec_fields: dict; def validate_signage(state: SignageState): return {"validated": len(state['content']) > 0}; def format_output(state: SignageState): return {"content": f"Processed Sign: {state['content']}"}; graph = StateGraph(SignageState); graph.add_node("validate", validate_signage); graph.add_node("format", format_output); graph.set_entry_point("validate"); graph.add_edge("validate", "format"); graph.add_edge("format", END); graph = graph.compile()