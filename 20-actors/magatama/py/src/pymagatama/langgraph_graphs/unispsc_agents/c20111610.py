from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GearState(TypedDict): gear_spec: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_specs(state: GearState): return {'validation_results': ['Spec compliant'], 'status': 'VALIDATED'} if 'gear_ratio' in state['gear_spec'] else {'validation_results': ['Spec missing'], 'status': 'REJECTED'}; def process_gear_order(state: GearState): return {'status': 'PROCESSED'}; graph = StateGraph(GearState); graph.add_node('validate', validate_specs); graph.add_node('process', process_gear_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()