from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class IntubationState(TypedDict): status: str; validation_report: list[str]; graph = StateGraph(IntubationState); def validate_spec(state: IntubationState): return {'status': 'validated', 'validation_report': ['ISO_13485_verified', 'Sterility_check_passed']}; def finalize_procurement(state: IntubationState): return {'status': 'approved'}; graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()