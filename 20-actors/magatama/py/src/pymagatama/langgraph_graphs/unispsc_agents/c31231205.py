from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CopperProcurementState(TypedDict): vendor_certs: List[str]; specs_met: bool; inspection_result: str; def validate_specs(state: CopperProcurementState): return {'specs_met': all(cert in state['vendor_certs'] for cert in ['ISO9001', 'ASTM-B152'])}; def inspect_plate(state: CopperProcurementState): return {'inspection_result': 'PASS' if state['specs_met'] else 'FAIL'}; graph = StateGraph(CopperProcurementState); graph.add_node('validate', validate_specs); graph.add_node('inspect', inspect_plate); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()