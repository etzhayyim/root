from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GamingState(TypedDict): vendor_id: str; compliance_docs: List[str]; validation_status: bool; def check_licenses(state: GamingState): state['validation_status'] = len(state['compliance_docs']) > 0; return state; graph = StateGraph(GamingState); graph.add_node('validate_compliance', check_licenses); graph.set_entry_point('validate_compliance'); graph.add_edge('validate_compliance', END); graph = graph.compile()