from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TillerState(TypedDict): specs: dict; validation_errors: List[str]; def validate_tiller_spec(state: TillerState): if not state['specs'].get('safety_cert'): state['validation_errors'].append('Missing safety certification'); return state; def check_engine_compliance(state: TillerState): if state['specs'].get('power_kw', 0) > 50: state['validation_errors'].append('High power hazard'); return state; graph = StateGraph(TillerState); graph.add_node('validate', validate_tiller_spec); graph.add_node('compliance', check_engine_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()