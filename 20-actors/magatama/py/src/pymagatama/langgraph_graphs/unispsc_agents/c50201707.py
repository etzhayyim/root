from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoffeeSubState(TypedDict): ingredients: List[str]; qc_passed: bool; def validate_ingredients(state: CoffeeSubState): state['qc_passed'] = 'caffeine' not in state['ingredients']; return state; def route_qc(state: CoffeeSubState): return 'end' if state['qc_passed'] else 'reject'; graph = StateGraph(CoffeeSubState); graph.add_node('validate', validate_ingredients); graph.add_conditional_edges('validate', route_qc, {'end': END, 'reject': END}); graph.set_entry_point('validate'); graph = graph.compile()