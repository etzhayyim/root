from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetyState(TypedDict): procurement_reqs: List[str]; security_cleared: bool; graph = StateGraph(SafetyState); def validate_security(state: SafetyState): return {'security_cleared': True}; def process_procurement(state: SafetyState): return {'procurement_reqs': ['compliance_check', 'security_audit']}; graph.add_node('validate', validate_security); graph.add_node('process', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()