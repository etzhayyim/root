from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgeonStoolState(TypedDict): stool_id: str; specs: dict; valid: bool; def validate_specs(state: SurgeonStoolState): state['valid'] = all(k in state['specs'] for k in ['load', 'material']); return state; def check_medical_compliance(state: SurgeonStoolState): if state['valid']: print(f'Processing {state['stool_id']} medical compliance check'); return state; graph = StateGraph(SurgeonStoolState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_medical_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()