from typing import TypedDict, List; from langgraph.graph import StateGraph; class AircraftProcureState(TypedDict): data: dict; compliance_checks: List[str]; approved: bool; def validate_security_clearance(state: AircraftProcureState): return {'compliance_checks': state['compliance_checks'] + ['Security Cleared']}; def export_control_check(state: AircraftProcureState): return {'approved': True}; graph = StateGraph(AircraftProcureState); graph.add_node('security', validate_security_clearance); graph.add_node('export', export_control_check); graph.add_edge('security', 'export'); graph.set_entry_point('security'); graph.set_finish_point('export'); graph = graph.compile()