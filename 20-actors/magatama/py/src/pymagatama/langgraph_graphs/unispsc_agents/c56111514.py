from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OfficeFitoutState(TypedDict): vendor_specs: dict; compliance_check: bool; final_layout: str; def validate_specs(state: OfficeFitoutState): state['compliance_check'] = 'load_bearing' in state['vendor_specs'] and 'material' in state['vendor_specs']; return state; def finalize_order(state: OfficeFitoutState): state['final_layout'] = 'Confirmed' if state['compliance_check'] else 'Rejected'; return state; graph = StateGraph(OfficeFitoutState); graph.add_node('validate', validate_specs); graph.add_node('finish', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finish'); graph.add_edge('finish', END); compiled_graph = graph.compile()