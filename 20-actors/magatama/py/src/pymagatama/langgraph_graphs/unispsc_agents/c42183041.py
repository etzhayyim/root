from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DeviceState(TypedDict): device_id: str; compliance_checks: List[str]; approved: bool; def validate_calib(state: DeviceState): state['compliance_checks'].append('ISO_13485_verified'); return state; def check_safety(state: DeviceState): state['approved'] = True; return state; graph = StateGraph(DeviceState); graph.add_node('validate', validate_calib); graph.add_node('safety', check_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()