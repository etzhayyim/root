from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class AltitudeState(TypedDict): input_specs: dict; valid_data: bool; calibration_passed: bool; def validate_specs(state: AltitudeState): state['valid_data'] = 'measurement_range' in state['input_specs']; return state; def check_calibration(state: AltitudeState): state['calibration_passed'] = state['input_specs'].get('calibration_standard') == 'ISO/IEC 17025'; return state; graph = StateGraph(AltitudeState); graph.add_node('validate', validate_specs); graph.add_node('calibrate', check_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); graph = graph.compile()