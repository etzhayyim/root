from typing import TypedDict, Annotated, List; import operator; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: List[str]; validation_results: Annotated[List[str], operator.add]; status: str; def validate_specs(state: AssemblyState): results = ['CAD_Match' if 'tolerance' in state['specs'] else 'Flag_Review']; return {'validation_results': results}; def check_compliance(state: AssemblyState): return {'status': 'Approved' if 'CAD_Match' in state['validation_results'] else 'Rejected'}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()