from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SonarState(TypedDict): sonar_data: dict; verification_status: bool; alerts: List[str]; def validate_specs(state: SonarState): state['verification_status'] = 'frequency_range_khz' in state['sonar_data']; return state; def check_export(state: SonarState): if state.get('is_dual_use'): state['alerts'].append('Export Control Review Required'); return state; graph = StateGraph(SonarState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()