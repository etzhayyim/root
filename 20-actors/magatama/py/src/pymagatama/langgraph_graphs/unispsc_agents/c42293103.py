from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): vendor_docs: List[str]; validation_results: dict; def validate_specs(state: SurgicalState): return {'validation_results': {'iso_checked': True}}; def check_compliance(state: SurgicalState): return {'validation_results': {'compliance_passed': True}}; builder = StateGraph(SurgicalState); builder.add_node('validate', validate_specs); builder.add_node('compliance', check_compliance); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); builder.set_entry_point('validate'); graph = builder.compile()