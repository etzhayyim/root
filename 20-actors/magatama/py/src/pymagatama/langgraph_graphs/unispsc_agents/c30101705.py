from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BeamState(TypedDict): beam_specs: dict; validation_errors: List[str]; approved: bool; def validate_grade(state: BeamState): if state['beam_specs'].get('grade') in ['304', '316']: state['approved'] = True; return state; else: state['validation_errors'].append('Invalid grade'); state['approved'] = False; return state; graph = StateGraph(BeamState); graph.add_node('validate', validate_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()