from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ViralKitState(TypedDict): kit_id: str; compliance_docs: List[str]; inspection_status: str; def validate_regulations(state: ViralKitState): return {'inspection_status': 'verified' if 'iso_cert' in state['compliance_docs'] else 'pending'}; def check_thermal(state: ViralKitState): return {'inspection_status': 'thermal_validated'}; graph = StateGraph(ViralKitState); graph.add_node('validate', validate_regulations); graph.add_node('thermal_check', check_thermal); graph.set_entry_point('validate'); graph.add_edge('validate', 'thermal_check'); graph.add_edge('thermal_check', END); graph = graph.compile()