from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): model: str; specs: dict; valid: bool; def validate_specs(state: State): state['valid'] = 'Sterility Certification' in state['specs'] and state['specs']['Material Grade'] is not None; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()