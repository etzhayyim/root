from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PianoState(TypedDict): piano_type: str; specs: dict; approved: bool; def validate_specs(state: PianoState): state['approved'] = state['specs'].get('soundboard') == 'solid_spruce'; return state; def schedule_delivery(state: PianoState): return state; graph = StateGraph(PianoState); graph.add_node('validate', validate_specs); graph.add_node('delivery', schedule_delivery); graph.set_entry_point('validate'); graph.add_edge('validate', 'delivery'); graph.add_edge('delivery', END); graph.compile()