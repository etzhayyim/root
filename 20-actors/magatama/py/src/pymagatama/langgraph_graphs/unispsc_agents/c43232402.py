from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DevEnvState(TypedDict): software_id: str; dev_env_config: dict; approval_required: bool; def validate_config(state: DevEnvState): state['approval_required'] = len(state['dev_env_config']) > 0; return state; def check_compliance(state: DevEnvState): print('Checking export compliance...'); return state; graph = StateGraph(DevEnvState); graph.add_node('validate', validate_config); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()