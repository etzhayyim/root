from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_data: dict; compliance_checks: List[str]; def validate_spec(state: KitState): state['compliance_checks'].append('ISO_13485_Verified'); return state; def check_sterility(state: KitState): state['compliance_checks'].append('Sterilization_Cert_Valid'); return state; graph = StateGraph(KitState); graph.add_node('validate', validate_spec); graph.add_node('sterility', check_sterility); graph.set_entry_point('validate'); graph.add_edge('validate', 'sterility'); graph.add_edge('sterility', END); graph = graph.compile()