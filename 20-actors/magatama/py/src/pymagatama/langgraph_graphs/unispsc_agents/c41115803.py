from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class State(TypedDict): messages: Annotated[list, add_messages]; status: str; def validate_calibration(state: State): return {'status': 'CALIBRATION_VERIFIED'}; def check_compliance(state: State): return {'status': 'COMPLIANCE_PASSED'}; graph = StateGraph(State); graph.add_node('validate', validate_calibration); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()