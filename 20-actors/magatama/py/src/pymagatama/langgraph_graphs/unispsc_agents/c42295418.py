from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validated_items: List[str]; status: str; def validate_spec(state: State): validated = [item for item in state['items'] if 'ISO 13485' in item]; return {'validated_items': validated, 'status': 'COMPLETE' if validated else 'REJECTED'}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()