from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): specs: List[str]; validation_result: str; def validate_dimensions(state: SpecState): state['validation_result'] = 'Dimensions verified' if 'dimensional_tolerances' in state['specs'] else 'Missing dims'; return state; def verify_material(state: SpecState): state['validation_result'] = 'Steel grade confirmed' if 'material_grade' in state['specs'] else 'Check material'; return state; graph = StateGraph(SpecState); graph.add_node('val_dim', validate_dimensions); graph.add_node('val_mat', verify_material); graph.set_entry_point('val_dim'); graph.add_edge('val_dim', 'val_mat'); graph.add_edge('val_mat', END); graph = graph.compile()