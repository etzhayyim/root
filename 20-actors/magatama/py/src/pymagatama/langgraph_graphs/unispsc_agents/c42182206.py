from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ThermometerState(TypedDict): vendor_id: str; mercury_compliance: bool; spec_data: dict; approved: bool; def check_compliance(state: ThermometerState): return { "mercury_compliance": True if state["spec_data"].get("mercury_compliant") else False }; def approve_procurement(state: ThermometerState): return { "approved": state["mercury_compliance"] }; graph = StateGraph(ThermometerState); graph.add_node("validate", check_compliance); graph.add_node("decision", approve_procurement); graph.set_entry_point("validate"); graph.add_edge("validate", "decision"); graph.add_edge("decision", END); graph = graph.compile()