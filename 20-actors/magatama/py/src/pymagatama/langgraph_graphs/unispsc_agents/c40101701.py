from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ACProcurementState(TypedDict): specs: dict; compliance_status: bool; def validate_specs(state: ACProcurementState): return {'compliance_status': state['specs'].get('seer_rating', 0) > 15}; def finalize_procurement(state: ACProcurementState): return {'compliance_status': True}; graph = StateGraph(ACProcurementState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()