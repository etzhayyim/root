from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ActuatorState(TypedDict): specs: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_specs(state: ActuatorState): results = []; if state['specs'].get('max_load', 0) <= 0: results.append('Invalid load capacity'); return {'validation_results': results, 'status': 'validated' if not results else 'failed'}; def build_graph(): workflow = StateGraph(ActuatorState); workflow.add_node('validate', validate_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); return workflow.compile(); graph = build_graph()