from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MountingStripState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: MountingStripState): return {'validation_results': ['Material check passed']}; def validate_dimensions(state: MountingStripState): return {'validation_results': ['Dimensions compliant']}; graph = StateGraph(MountingStripState); graph.add_node('material_check', validate_material); graph.add_node('dim_check', validate_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dim_check'); graph.add_edge('dim_check', END); graph = graph.compile()