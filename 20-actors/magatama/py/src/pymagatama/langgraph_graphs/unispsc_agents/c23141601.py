from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DieCastState(TypedDict): vendor_id: str; machine_specs: dict; compliance_check: bool; def validate_specs(state: DieCastState): state['compliance_check'] = state['machine_specs'].get('clamping_force_kn', 0) > 500; return state; def route_compliance(state: DieCastState): return 'valid' if state['compliance_check'] else 'invalid'; graph = StateGraph(DieCastState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()