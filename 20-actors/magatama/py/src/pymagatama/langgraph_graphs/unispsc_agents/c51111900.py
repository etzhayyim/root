from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_id: str; purity: float; temp_compliance: bool; status: str; def validate_purity(state: PharmState): return {'status': 'validated' if state['purity'] >= 99.0 else 'rejected'}; def check_cold_chain(state: PharmState): return {'temp_compliance': True}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('cold_chain', check_cold_chain); graph.set_entry_point('validate'); graph.add_edge('validate', 'cold_chain'); graph.add_edge('cold_chain', END); graph = graph.compile()