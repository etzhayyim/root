from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_specs: dict; compliance_status: bool; def validate_spec(state: ProcureState): state['compliance_status'] = 'Gauge' in state['vendor_specs'] and 'Material' in state['vendor_specs']; return state; def route_procurement(state: ProcureState): return 'complete' if state['compliance_status'] else 'reject'; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()