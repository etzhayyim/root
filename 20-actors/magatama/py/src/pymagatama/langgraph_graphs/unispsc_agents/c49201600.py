from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrainingEquipState(TypedDict): items: List[str]; inspection_passed: bool; def validate_specs(state: TrainingEquipState): return {'inspection_passed': True}; def schedule_delivery(state: TrainingEquipState): return {'items': state['items']}; graph = StateGraph(TrainingEquipState); graph.add_node('validate', validate_specs); graph.add_node('delivery', schedule_delivery); graph.set_entry_point('validate'); graph.add_edge('validate', 'delivery'); graph.add_edge('delivery', END); compile_graph = graph.compile()