from langgraph.graph import StateGraph, END; from typing import TypedDict; class TransferPaperState(TypedDict): specs: dict; valid: bool; def validate_specs(state: TransferPaperState): state['valid'] = all(k in state['specs'] for k in ['gsm', 'printer_type']); return state; graph = StateGraph(TransferPaperState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()