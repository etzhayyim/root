from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_data: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_chemical(state: ChemicalState): return {'validation_results': ['Purity check passed', 'SDS verified']}; def process_chemical(state: ChemicalState): return {'status': 'READY_FOR_SHIPMENT'}; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_chemical); graph.add_node('process', process_chemical); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()