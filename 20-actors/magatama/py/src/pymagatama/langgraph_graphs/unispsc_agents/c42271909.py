from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AirwayState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_medical_standards(state: AirwayState): state['validation_errors'] = [e for e in state['items'] if 'iso_cert' not in e]; return state; graph = StateGraph(AirwayState); graph.add_node('validate', validate_medical_standards); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()