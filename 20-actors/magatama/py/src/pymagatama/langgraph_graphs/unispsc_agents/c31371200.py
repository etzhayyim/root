from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastableState(TypedDict): material_name: str; specs: dict; valid: bool; def validate_specs(state: CastableState): state['valid'] = 'refractoriness_sk' in state['specs'] and state['specs']['refractoriness_sk'] > 1500; return state; graph = StateGraph(CastableState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()