from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicrowaveDiodeState(TypedDict): specs: dict; valid: bool; export_check: bool; def validate_specs(state: MicrowaveDiodeState): state['valid'] = all(k in state['specs'] for k in ['frequency', 'power']); return state; def check_export(state: MicrowaveDiodeState): state['export_check'] = state['specs'].get('frequency', 0) > 10; return state; graph = StateGraph(MicrowaveDiodeState); graph.add_node('validate', validate_specs); graph.add_node('export', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph = graph.compile()