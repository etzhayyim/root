from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine: str; compliance: bool; status: str; def validate_pharm(state: PharmState): return {'compliance': True, 'status': 'Validated'} if state['medicine'] == 'Oxymetholone' else {'compliance': False, 'status': 'Rejected'}; graph = StateGraph(PharmState); graph.add_node('validate', validate_pharm); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()