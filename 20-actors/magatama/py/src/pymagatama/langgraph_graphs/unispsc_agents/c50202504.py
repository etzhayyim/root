from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GrapeConcentrateState(TypedDict): brix_level: float; certification_check: bool; approved: bool; def validate_brix(state: GrapeConcentrateState): state['approved'] = state['brix_level'] >= 65.0; return state; def check_certs(state: GrapeConcentrateState): state['certification_check'] = True; return state; graph = StateGraph(GrapeConcentrateState); graph.add_node('validate_brix', validate_brix); graph.add_node('check_certs', check_certs); graph.set_entry_point('validate_brix'); graph.add_edge('validate_brix', 'check_certs'); graph.add_edge('check_certs', END); graph = graph.compile()