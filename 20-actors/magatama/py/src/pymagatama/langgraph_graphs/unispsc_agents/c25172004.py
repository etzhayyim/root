from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShockAbsorberState(TypedDict): part_number: str; compatibility: List[str]; inspection_passed: bool; def validate_part(state: ShockAbsorberState): return {'inspection_passed': state.get('part_number', '').startswith('SHK')}; def check_compatibility(state: ShockAbsorberState): return {'compatibility': ['validated']}; graph = StateGraph(ShockAbsorberState); graph.add_node('validate', validate_part); graph.add_node('check', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); compiled_graph = graph.compile()