from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FoodImportState(TypedDict): ingredients: List[str]; safety_certs: bool; shelf_life_days: int; status: str; def validate_ingredients(state: FoodImportState): return {'status': 'validated' if all(state['ingredients']) else 'rejected'}; def check_compliance(state: FoodImportState): return {'status': 'compliant' if state['safety_certs'] and state['shelf_life_days'] > 90 else 'flagged_for_review'}; graph = StateGraph(FoodImportState); graph.add_node('validate', validate_ingredients); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()