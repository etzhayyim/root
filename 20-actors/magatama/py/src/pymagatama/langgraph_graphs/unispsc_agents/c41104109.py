from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BloodBagState(TypedDict): bag_id: str; sterility_verified: bool; quality_report: dict; graph = StateGraph(BloodBagState); def check_sterility(state: BloodBagState): return {'sterility_verified': True}; def validate_quality(state: BloodBagState): return {'quality_report': {'status': 'approved'}}; graph.add_node('check_sterility', check_sterility); graph.add_node('validate_quality', validate_quality); graph.set_entry_point('check_sterility'); graph.add_edge('check_sterility', 'validate_quality'); graph.add_edge('validate_quality', END); graph = graph.compile()