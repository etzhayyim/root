from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class HoseCutterState(TypedDict): model_number: str; hose_spec: str; validation_status: bool; def validate_compatibility(state: HoseCutterState): is_valid = 'rubber' in state['hose_spec'].lower(); return {'validation_status': is_valid}; def report_status(state: HoseCutterState): print(f'Validation complete: {state['validation_status']}'); return {}; graph = StateGraph(HoseCutterState); graph.add_node('validate', validate_compatibility); graph.add_node('report', report_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()