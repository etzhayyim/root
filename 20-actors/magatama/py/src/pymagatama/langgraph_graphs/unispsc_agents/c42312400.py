from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WoundPackingState(TypedDict): items: List[str]; validation_status: bool; def validate_sterile_spec(state: WoundPackingState): return {'validation_status': all('sterile' in item.lower() for item in state['items'])}; def check_expiry(state: WoundPackingState): return {'validation_status': True}; graph = StateGraph(WoundPackingState); graph.add_node('validate', validate_sterile_spec); graph.add_node('expiry', check_expiry); graph.add_edge('validate', 'expiry'); graph.add_edge('expiry', END); graph.set_entry_point('validate'); compile_graph = graph.compile()