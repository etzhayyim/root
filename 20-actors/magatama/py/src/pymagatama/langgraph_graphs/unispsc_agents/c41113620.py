from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DetectionState(TypedDict): input_data: dict; validation_results: List[str]; inspection_status: str; def validate_sensor(state: DetectionState): results = []; if state['input_data'].get('calibrated_date'): results.append('Calibration Active'); return {'validation_results': results}; def generate_report(state: DetectionState): return {'inspection_status': 'COMPLETED'}; graph = StateGraph(DetectionState); graph.add_node('validate', validate_sensor); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()