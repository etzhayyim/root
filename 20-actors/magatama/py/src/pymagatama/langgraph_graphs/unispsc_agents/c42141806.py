from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DiathermyState(TypedDict): unit_id: str; compliance_docs: List[str]; validation_status: bool; def validate_emc(state: DiathermyState): state['validation_status'] = 'EMC_CERT' in state['compliance_docs']; return state; def check_medical_licensure(state: DiathermyState): return {'validation_status': True if state['unit_id'].startswith('MED') else False}; graph = StateGraph(DiathermyState); graph.add_node('emc_check', validate_emc); graph.add_node('licensure_check', check_medical_licensure); graph.set_entry_point('emc_check'); graph.add_edge('emc_check', 'licensure_check'); graph.add_edge('licensure_check', END); graph = graph.compile()