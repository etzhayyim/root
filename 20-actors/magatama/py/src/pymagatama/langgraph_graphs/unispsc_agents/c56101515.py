from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BedProcurementState(TypedDict): dimensions: dict; material: str; certification: List[str]; approved: bool; def validate_specs(state: BedProcurementState): print('Validating bed dimensions and material compliance...'); state['approved'] = 'iso' in [c.lower() for c in state['certification']] and state['dimensions']['width'] > 0; return state; graph = StateGraph(BedProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()