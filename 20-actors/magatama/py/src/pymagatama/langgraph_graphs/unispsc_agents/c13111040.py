from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class OreState(TypedDict): assay_results: dict; validation_score: float; status: str; graph = StateGraph(OreState); def validate_purity(state: OreState): return {'validation_score': 0.98 if state['assay_results'].get('purity', 0) > 0.95 else 0.5}; def check_origin(state: OreState): return {'status': 'CLEARED' if state['assay_results'].get('origin') != 'RESTRICTED' else 'FLAGGED'}; graph.add_node('validate_purity', validate_purity); graph.add_node('check_origin', check_origin); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_origin'); graph.add_edge('check_origin', END); graph = graph.compile()