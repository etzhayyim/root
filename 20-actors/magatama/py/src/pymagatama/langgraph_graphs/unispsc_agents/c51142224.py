from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcState(TypedDict): status: str; docs: List[str]; def check_license(state: ProcState): state['status'] = 'VERIFIED' if 'license' in state['docs'] else 'REJECTED'; return state; def validate_purity(state: ProcState): return state; graph = StateGraph(ProcState); graph.add_node('verify', check_license); graph.add_node('purity', validate_purity); graph.add_edge('verify', 'purity'); graph.add_edge('purity', END); graph.set_entry_point('verify'); compiled_graph = graph.compile()