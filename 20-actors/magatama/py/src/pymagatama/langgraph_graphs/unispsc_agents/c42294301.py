from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BiopsyState(TypedDict): device_type: str; specs_verified: bool; compliance_status: str; def validate_specs(state: BiopsyState): return {'specs_verified': state['device_type'] in ['driver', 'firing_module', 'accessory']}; def check_compliance(state: BiopsyState): return {'compliance_status': 'FDA_PASSED' if state['specs_verified'] else 'PENDING'}; graph = StateGraph(BiopsyState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()