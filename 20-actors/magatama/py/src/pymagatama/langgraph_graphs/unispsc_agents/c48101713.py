from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input_data: dict; compliance_log: Annotated[list, operator.add]; status: str; def validate_materials(state: ProcureState): state['compliance_log'].append('Verifying food-grade certification...'); return {'status': 'validated'} if 'food_grade' in state['input_data'] else {'status': 'rejected'}; def check_specs(state: ProcureState): state['compliance_log'].append('Checking capacity and design constraints...'); return {'status': 'processed'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_materials); graph.add_node('check', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()