from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PhotometerState(TypedDict): data: dict; valid: bool; logs: List[str]; def validate_spec(state: PhotometerState): state['valid'] = 'calibration_cert' in state['data']; state['logs'].append('Validation completed'); return state; workflow = StateGraph(PhotometerState); workflow.add_node('validate', validate_spec); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()