from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GrainState(TypedDict): items: List[str]; qc_passed: bool; def check_quality(state: GrainState): return {'qc_passed': all('test_result' in item for item in state['items'])}; def finalize_procurement(state: GrainState): print('Finalizing dried beans procurement'); return {'qc_passed': True}; graph = StateGraph(GrainState); graph.add_node('check_qc', check_quality); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('check_qc'); graph.add_edge('check_qc', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()