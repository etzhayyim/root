from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgeState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: ForgeState): result = 'Valid' if state['specs'].get('tin_purity', 0) > 99.0 else 'Invalid'; return {'validation_results': [f'Material purity check: {result}']}; def check_geometry(state: ForgeState): return {'validation_results': state['validation_results'] + ['Geometry tolerance checked']}; graph = StateGraph(ForgeState); graph.add_node('validate_material', validate_material); graph.add_node('check_geometry', check_geometry); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_geometry'); graph.add_edge('check_geometry', END); app = graph.compile()