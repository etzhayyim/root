from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ProcureState): state['valid'] = 'Material Grade' in state['specs'] and 'Pressure Rating' in state['specs']; return state; builder = StateGraph(ProcureState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()