from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RescueKitState(TypedDict): kit_contents: List[str]; qc_passed: bool; certs: List[str]; def validate_contents(state: RescueKitState): state['qc_passed'] = len(state['kit_contents']) > 0; return state; def check_compliance(state: RescueKitState): state['qc_passed'] = 'ISO-13485' in state['certs']; return state; workflow = StateGraph(RescueKitState); workflow.add_node('validate', validate_contents); workflow.add_node('compliance', check_compliance); workflow.set_entry_point('validate'); workflow.add_edge('validate', 'compliance'); workflow.add_edge('compliance', END); graph = workflow.compile()