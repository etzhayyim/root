from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnchorState(TypedDict): anchor_type: str; specs: dict; validation_passed: bool; def validate_tool(state: AnchorState): state['validation_passed'] = 'torque_range' in state['specs'] and state['specs']['torque_range'] > 0; return state; graph = StateGraph(AnchorState); graph.add_node('validate', validate_tool); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()