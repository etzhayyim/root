from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): assay_data: dict; compliance_status: bool; quality_report: str; def validate_purity(state: PharmaState): state['compliance_status'] = state['assay_data'].get('purity', 0) > 99.0; return state; def generate_cert(state: PharmaState): state['quality_report'] = 'COA-verified'; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('certify', generate_cert); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph.set_entry_point('validate'); graph = graph.compile()