from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PortfolioState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(PortfolioState); def validate_specs(state: PortfolioState): report = []; if not state['specs'].get('dimensions'): report.append('Missing dimensions'); return {'validation_report': report}; graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); compiled_graph = graph.compile()