from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): doc_id: str; compliant: bool; status: str; def validate_instrument(state: State): state['compliant'] = True; state['status'] = 'QC_PASSED'; return state; def finalize_order(state: State): state['status'] = 'READY_FOR_SHIPMENT'; return state; graph = StateGraph(State); graph.add_node('validate', validate_instrument); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()