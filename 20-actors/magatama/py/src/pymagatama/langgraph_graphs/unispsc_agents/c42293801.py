from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: SurgicalState): specs = state['specs']; state['valid'] = 'iso_certification' in specs and 'material_composition_grade' in specs; return state; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()