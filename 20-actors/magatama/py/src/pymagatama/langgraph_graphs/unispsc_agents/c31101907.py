from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: List[str]; validated: bool; certs: List[str]; def validate_specs(state: CastingState): state['validated'] = len(state['specs']) > 0; return state; def check_compliance(state: CastingState): return 'passed' if state['validated'] else 'failed'; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()