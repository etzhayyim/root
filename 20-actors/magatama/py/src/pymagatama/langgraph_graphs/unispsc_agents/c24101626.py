from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EscalatorState(TypedDict): vendor_id: str; specs: dict; safety_compliant: bool; def validate_specs(state: EscalatorState): state['safety_compliant'] = state['specs'].get('load_capacity_kg', 0) > 0; return state; def route_step(state: EscalatorState): return "validate" if state['specs'] else END; graph = StateGraph(EscalatorState); graph.add_node("validate", validate_specs); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()