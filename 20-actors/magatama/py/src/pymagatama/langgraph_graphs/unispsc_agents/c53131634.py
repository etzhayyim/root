from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ChemProcState(TypedDict): items: List[dict]; compliance_check: bool; def validate_spec(state: ChemProcState): state['compliance_check'] = all('standard' in item for item in state['items']); return state; def route_approval(state: ChemProcState): return 'approve' if state['compliance_check'] else 'reject'; graph = StateGraph(ChemProcState); graph.add_node('validate', validate_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); app = graph.compile()