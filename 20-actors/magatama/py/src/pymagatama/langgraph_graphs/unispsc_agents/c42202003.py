from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProbeState(TypedDict): probe_id: str; status: str; certification_valid: bool; steps: List[str]; def validate_certification(state: ProbeState): state['certification_valid'] = True; return state; def check_sensor_specs(state: ProbeState): state['steps'].append('Validation of signal sensitivity'); return state; graph = StateGraph(ProbeState); graph.add_node('validate', validate_certification); graph.add_node('specs', check_sensor_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'specs'); graph.add_edge('specs', END); app = graph.compile()