from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): device_specs: dict; validation_results: list; status: str; def validate_specs(state: NetworkState): results = []; specs = state['device_specs']; if specs.get('throughput', 0) < 10: results.append('Insufficient throughput'); return {'validation_results': results, 'status': 'validated'}; def finalize_procurement(state: NetworkState): return {'status': 'approved'}; graph = StateGraph(NetworkState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()