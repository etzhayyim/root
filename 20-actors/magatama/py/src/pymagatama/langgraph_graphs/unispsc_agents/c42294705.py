from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PerfusionState(TypedDict): data: dict; validated: bool; def validate_spec(state: PerfusionState): pass; def check_compliance(state: PerfusionState): return {'validated': True}; graph = StateGraph(PerfusionState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()