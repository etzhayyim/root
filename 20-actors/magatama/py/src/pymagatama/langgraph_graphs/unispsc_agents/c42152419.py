from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PurchaseState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(PurchaseState); def validate_specs(state): return {'validation_results': {'compliant': True}}; def process_order(state): return {'validation_results': {'status': 'processed'}}; graph.add_node('validate', validate_specs); graph.add_node('process', process_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); app = graph.compile()