from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RugState(TypedDict): rug_data: dict; verification_logs: List[str]; def validate_origin(state: RugState): state['verification_logs'].append('Origin verified'); return state; def check_quality(state: RugState): state['verification_logs'].append('Quality check passed'); return state; graph = StateGraph(RugState); graph.add_node('verify', validate_origin); graph.add_node('check', check_quality); graph.set_entry_point('verify'); graph.add_edge('verify', 'check'); graph.add_edge('check', END); graph = graph.compile()