from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): robot_id: str; specs: dict; compliance: bool;  def validate_specs(state: RobotState): return {'compliance': state['specs'].get('payload', 0) > 0};  def risk_assessment(state: RobotState): return {'compliance': True if state['compliance'] else False};  graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.add_node('risk', risk_assessment); graph.set_entry_point('validate'); graph.add_edge('validate', 'risk'); graph.add_edge('risk', END); graph = graph.compile()