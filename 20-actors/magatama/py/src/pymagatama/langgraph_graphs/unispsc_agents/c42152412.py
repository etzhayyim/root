from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class WaxState(TypedDict): wax_data: dict; quality_checks: Annotated[Sequence[str], operator.add]; def validate_thermal_specs(state: WaxState): return {'quality_checks': ['Thermal specs passed']}; def perform_iso_compliance(state: WaxState): return {'quality_checks': ['ISO certification verified']}; graph = StateGraph(WaxState); graph.add_node('thermal_check', validate_thermal_specs); graph.add_node('iso_check', perform_iso_compliance); graph.set_entry_point('thermal_check'); graph.add_edge('thermal_check', 'iso_check'); graph.add_edge('iso_check', END); graph = graph.compile()