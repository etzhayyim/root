from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): metal_type: str; purity: float; specs: dict; approved: bool; def validate_purity(state: CastingState): state['approved'] = state['purity'] >= 0.999; return state; def check_spec(state: CastingState): print(f'Validating specs for {state['metal_type']}'); return state; graph = StateGraph(CastingState); graph.add_node('purity_check', validate_purity); graph.add_node('spec_validation', check_spec); graph.add_edge('purity_check', 'spec_validation'); graph.add_edge('spec_validation', END); graph.set_entry_point('purity_check'); compile_graph = graph.compile()