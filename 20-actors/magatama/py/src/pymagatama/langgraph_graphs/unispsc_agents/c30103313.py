from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class BilletsState(TypedDict): material_type: str; specs_verified: bool; def validate_specs(state: BilletsState): print('Validating non-metallic billet specs...'); return {'specs_verified': True}; def process_procurement(state: BilletsState): print('Processing billet procurement order...'); return {'specs_verified': True}; graph = StateGraph(BilletsState); graph.add_node('validate', validate_specs); graph.add_node('procure', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'procure'); graph.add_edge('procure', END); compiled_graph = graph.compile()