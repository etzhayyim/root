from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): instrument_list: List[str]; validation_results: dict; graph = StateGraph(SurgicalState); def validate_instruments(state: SurgicalState): return {'validation_results': {'status': 'verified', 'compliance': 'ISO-13485'}}; def check_regulatory(state: SurgicalState): return {'validation_results': {**state['validation_results'], 'regulatory': 'cleared'}}; graph.add_node('validate', validate_instruments); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()