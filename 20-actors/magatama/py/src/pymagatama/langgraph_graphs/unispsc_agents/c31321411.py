from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): specs: dict; certified: bool; risk_score: int; def validate_specs(state: AssemblyState): state['certified'] = all(k in state['specs'] for k in ['AMS', 'alloy_trace']); return state; def evaluate_risk(state: AssemblyState): state['risk_score'] = 10 if state.get('certified') else 50; return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('risk_assessment', evaluate_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'risk_assessment'); graph.add_edge('risk_assessment', END); compiled_graph = graph.compile()