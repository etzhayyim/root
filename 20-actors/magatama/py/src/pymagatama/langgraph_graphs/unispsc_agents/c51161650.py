from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): active_ingredient: str; purity: float; compliance_docs: List[str]; def validate_purity(state: ProcureState): return {'compliance_docs': state['compliance_docs'] + ['Purity Verified']} if state['purity'] >= 99.0 else {'compliance_docs': state['compliance_docs'] + ['Purity Check Failed']}; graph = StateGraph(ProcureState); graph.add_node('verify', validate_purity); graph.set_entry_point('verify'); graph.add_edge('verify', END); compiled_graph = graph.compile()