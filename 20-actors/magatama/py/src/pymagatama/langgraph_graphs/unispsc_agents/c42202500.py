from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SPECTState(TypedDict): vendor_certs: List[str]; radiation_safety_ok: bool; doc_validated: bool; def validate_certs(state: SPECTState): return {'doc_validated': all(c in state['vendor_certs'] for c in ['ISO13485', 'IEC60601'])}; def check_safety(state: SPECTState): return {'radiation_safety_ok': True}; graph = StateGraph(SPECTState); graph.add_node('validate', validate_certs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()