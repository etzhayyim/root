from langgraph.graph import StateGraph, END; from typing import TypedDict; class HeatExchangerState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: HeatExchangerState): specs = state.get('specs', {}); return {'validation_report': 'Valid' if 'pressure_rating' in specs else 'Invalid'}; def finalize_procurement(state: HeatExchangerState): return {'validation_report': 'Procurement Ready'}; graph = StateGraph(HeatExchangerState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()