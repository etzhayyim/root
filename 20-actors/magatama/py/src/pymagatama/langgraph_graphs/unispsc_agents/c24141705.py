from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeState(TypedDict): tube_type: str; specs: dict; validation_passed: bool; def check_seal_integrity(state: TubeState): state['validation_passed'] = state['specs'].get('pressure_threshold', 0) > 5.0; return state; def inspect_material(state: TubeState): state['validation_passed'] = state['tube_type'] in ['aluminum', 'laminate']; return state; graph = StateGraph(TubeState); graph.add_node('inspect', inspect_material); graph.add_node('check_seal', check_seal_integrity); graph.set_entry_point('inspect'); graph.add_edge('inspect', 'check_seal'); graph.add_edge('check_seal', END); compiled_graph = graph.compile()