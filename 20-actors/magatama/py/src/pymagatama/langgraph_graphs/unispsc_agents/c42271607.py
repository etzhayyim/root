from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; def validate_medical_compliance(state: ProcState): state['validation_results'].append('ISO-13485-Checked'); return state; def check_compatibility(state: ProcState): state['validation_results'].append('Model-Compatibility-Verified'); return state; graph = StateGraph(ProcState); graph.add_node('compliance', validate_medical_compliance); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()