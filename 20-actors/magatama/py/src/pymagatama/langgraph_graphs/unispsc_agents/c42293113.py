from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: Annotated[Sequence[str], operator.add]; validation_results: dict; def validate_instrument(state: ProcureState): return {'validation_results': {'iso_check': 'passed', 'mat_check': 'verified'}}; def check_compliance(state: ProcureState): return {'validation_results': {**state['validation_results'], 'compliance': 'cleared'}}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_instrument); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()