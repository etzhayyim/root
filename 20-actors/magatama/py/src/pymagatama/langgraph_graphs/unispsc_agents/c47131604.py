from langgraph.graph import StateGraph, END; from typing import TypedDict; class BroomState(TypedDict): spec: dict; valid: bool; def validate_spec(state: BroomState): spec = state['spec']; state['valid'] = 'bristle_material' in spec and 'handle_material' in spec; return state; graph = StateGraph(BroomState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()