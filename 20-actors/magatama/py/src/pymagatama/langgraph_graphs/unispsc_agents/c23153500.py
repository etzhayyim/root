from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotProcurementState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: RobotProcurementState): return {'validation_results': ['Specs validated against ISO standards']}; def check_export_control(state: RobotProcurementState): return {'validation_results': state['validation_results'] + ['Export control check passed']}; graph = StateGraph(RobotProcurementState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()