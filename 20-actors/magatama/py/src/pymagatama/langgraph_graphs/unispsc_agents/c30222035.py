from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CulvertState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(CulvertState); def validate_load(state: CulvertState): state['validation_report'].append('Load bearing validated against ASTM requirements'); return state; def check_durability(state: CulvertState): state['validation_report'].append('Durability coating confirmed'); return state; graph.add_node('validate_load', validate_load); graph.add_node('check_durability', check_durability); graph.set_entry_point('validate_load'); graph.add_edge('validate_load', 'check_durability'); graph.add_edge('check_durability', END); graph = graph.compile()