from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[str]; validation_results: List[str]; def validate_cable(state: State): return {'validation_results': ['Category Check Pass', 'Standard Compliant']}; def workflow(state: State): return {'validation_results': state['validation_results'] + ['Routing Completed']}; graph = StateGraph(State); graph.add_node('validate', validate_cable); graph.add_node('process', workflow); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()