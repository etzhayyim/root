from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MagnesiumState(TypedDict): material_purity: float; dimensions: dict; compliant: bool; def validate_purity(state: MagnesiumState) -> MagnesiumState: state['compliant'] = state['material_purity'] >= 99.9; return state; def hazard_check(state: MagnesiumState) -> MagnesiumState: print('Verifying dangerous goods compliance for magnesium'); return state; graph = StateGraph(MagnesiumState); graph.add_node('validate', validate_purity); graph.add_node('hazard_check', hazard_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazard_check'); graph.add_edge('hazard_check', END); graph = graph.compile()