from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class HostelState(TypedDict): vendor_id: str; compliance_score: float; confirmed: bool; def validate_facility(state: HostelState): print('Validating hostel safety specs'); state['compliance_score'] = 1.0; return {'compliance_score': 1.0, 'confirmed': True}; graph = StateGraph(HostelState); graph.add_node('validate', validate_facility); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()