from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: BearingState): errors = []; if state['specs'].get('load_rating', 0) <= 0: errors.append('Invalid load rating'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(BearingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()