from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): wire_feed_spec: dict; validation_result: bool; def validate_component(state: State): spec = state['wire_feed_spec']; is_valid = spec.get('hardness') > 50 and spec.get('material') == 'hardened_steel'; return {'validation_result': is_valid}; graph = StateGraph(State); graph.add_node('validate', validate_component); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()