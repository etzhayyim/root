from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_effector_spec(state: RobotState): payload = state['input_spec'].get('payload', 0); return {'validation_results': ['Payload within limit' if payload < 50 else 'Payload exceeds limit']}; def finalize_config(state: RobotState): return {'status': 'CONFIGURED'}; graph = StateGraph(RobotState); graph.add_node('validate', validate_effector_spec); graph.add_node('finalize', finalize_config); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()