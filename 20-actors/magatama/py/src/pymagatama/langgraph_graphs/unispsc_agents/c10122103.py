from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MineralState(TypedDict): data: dict; logs: Annotated[Sequence[str], operator.add]; status: str; def validate_purity(state: MineralState): purity = state['data'].get('purity', 0); return {'status': 'PASSED' if purity > 98.0 else 'FAILED', 'logs': ['Purity check completed']}; def compile_graph(): builder = StateGraph(MineralState); builder.add_node('validate', validate_purity); builder.set_entry_point('validate'); builder.add_edge('validate', END); return builder.compile(); graph = compile_graph()