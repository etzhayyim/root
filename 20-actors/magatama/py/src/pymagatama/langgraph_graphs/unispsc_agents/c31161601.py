from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnchorState(TypedDict): anchor_specs: dict; validation_report: str; approved: bool; def validate_specs(state: AnchorState): specs = state['anchor_specs']; is_valid = all([specs.get('tensile'), specs.get('material')]); return {'validation_report': 'Passed' if is_valid else 'Failed', 'approved': is_valid}; graph = StateGraph(AnchorState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()