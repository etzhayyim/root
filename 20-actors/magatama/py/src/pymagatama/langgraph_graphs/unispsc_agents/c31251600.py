from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RobotState(TypedDict): component_id: str; specs: dict; validation_results: List[str]; graph = StateGraph(RobotState); def validate_specs(state: RobotState): return {'validation_results': ['CAD_verified', 'Compliance_checked']}; def export_control_check(state: RobotState): return {'validation_results': state['validation_results'] + ['Export_control_passed']}; graph.add_node('validate', validate_specs); graph.add_node('export_check', export_control_check); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate');