from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SealantState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; safety_check: bool; def validate_msds(state: SealantState): return {'validation_results': ['MSDS verified'], 'safety_check': True}; def check_specs(state: SealantState): return {'validation_results': ['Specs validated']}; graph = StateGraph(SealantState); graph.add_node('msds', validate_msds); graph.add_node('specs', check_specs); graph.add_edge('msds', 'specs'); graph.add_edge('specs', END); graph.set_entry_point('msds'); graph = graph.compile()