from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): items: list; quality_passed: bool; def validate_specs(state: ProcureState): state['quality_passed'] = all('tensile_strength' in i for i in state['items']); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()