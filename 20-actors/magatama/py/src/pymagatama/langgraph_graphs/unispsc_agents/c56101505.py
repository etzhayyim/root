from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: FurnitureState): errors = []; if state['specs'].get('weight_capacity_kg', 0) <= 0: errors.append('Invalid load capacity'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(FurnitureState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()