from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DemolitionToolState(TypedDict): tool_id: str; specs: dict; safety_cleared: bool; def validate_specs(state: DemolitionToolState): state['safety_cleared'] = state['specs'].get('impact_energy', 0) > 0; return state; def check_compliance(state: DemolitionToolState): print('Checking safety standards...'); return state; graph = StateGraph(DemolitionToolState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()