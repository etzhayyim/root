from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EduState(TypedDict): content: str; validated: bool; results: List[str]; def validate_materials(state: EduState): return {'validated': True}; def generate_report(state: EduState): return {'results': ['Compliance Check Passed', 'Content Validated']}; graph = StateGraph(EduState); graph.add_node('validate', validate_materials); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()