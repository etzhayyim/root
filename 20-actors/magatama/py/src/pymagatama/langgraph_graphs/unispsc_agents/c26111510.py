from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ShaftState(TypedDict): input_specs: dict; validation_results: list; status: str; def validate_dimensions(state: ShaftState): results = []; specs = state['input_specs']; if specs.get('tolerance', 0) > 0.05: results.append('Tolerance exceeds range'); return {'validation_results': results, 'status': 'validated' if not results else 'failed'}; graph = StateGraph(ShaftState); graph.add_node('validate', validate_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()