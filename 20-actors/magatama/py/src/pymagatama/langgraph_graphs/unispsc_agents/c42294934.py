from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): device_id: str; compliant: bool; status: str; def validate_device(state: ProcureState): return {'compliant': state['device_id'].startswith('ENDO-')}; def finalize_order(state: ProcureState): return {'status': 'Approved' if state['compliant'] else 'Rejected'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_device); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()