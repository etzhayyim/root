from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class NeedleState(TypedDict): needle_id: str; gauge: int; sterile: bool; approved: bool; def validate_spec(state: NeedleState): state['approved'] = state['gauge'] > 0 and state['sterile']; return state; graph = StateGraph(NeedleState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()