from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): component_id: str; compliance_status: bool; ntd_results: dict; graph = StateGraph(ProcureState); def check_compliance(state: ProcureState): return {'compliance_status': True}; def validate_technical_specs(state: ProcureState): return {'ntd_results': {'status': 'passed'}}; graph.add_node('compliance', check_compliance); graph.add_node('technical', validate_technical_specs); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'technical'); graph.add_edge('technical', END); app = graph.compile()