from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LuggageState(TypedDict): items: List[dict]; qc_passed: bool; def validate_spec(state: LuggageState): for item in state['items']: if 'dimensions' not in item: return {'qc_passed': False}; return {'qc_passed': True}; graph = StateGraph(LuggageState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()