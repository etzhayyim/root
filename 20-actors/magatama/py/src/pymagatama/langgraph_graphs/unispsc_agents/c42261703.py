from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AutopsyTableState(TypedDict): vendor_specs: dict; compliance_check: bool; final_approval: bool; def validate_specs(state: AutopsyTableState): state['compliance_check'] = 'stainless_grade' in state['vendor_specs'] and state['vendor_specs']['material'] == '316L'; return state; def finalize_order(state: AutopsyTableState): state['final_approval'] = state['compliance_check']; return state; graph = StateGraph(AutopsyTableState); graph.add_node("validate", validate_specs); graph.add_node("finalize", finalize_order); graph.set_entry_point("validate"); graph.add_edge("validate", "finalize"); graph.add_edge("finalize", END); graph = graph.compile()