from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScreenSaverState(TypedDict): software_name: str; os_version: str; compliance_check: bool; graph = StateGraph(ScreenSaverState); def validate_software(state: ScreenSaverState): return {'compliance_check': True}; def update_os_compatibility(state: ScreenSaverState): return {'os_version': 'Certified'}; graph.add_node('validate', validate_software); graph.add_node('compatibility', update_os_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()