from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; validator_results: List[str]; graph = StateGraph(State); def validate_medical_grade(state: State): return {'validator_results': ['Grade Verified'] if state['specs'].get('material') == 'surgical_steel' else ['Invalid Material']}; def report_compliance(state: State): return {'validator_results': state['validator_results'] + ['Compliance Checked']}; graph.add_node('validate', validate_medical_grade); graph.add_node('compliance', report_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()