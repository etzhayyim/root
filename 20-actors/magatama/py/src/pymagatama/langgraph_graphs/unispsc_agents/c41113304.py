from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class BauxiteAnalysisState(TypedDict): input_data: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_spec(state: BauxiteAnalysisState): is_valid = 'calibration' in state['input_data'] and 'range' in state['input_data']; return {'validation_results': ['Spec validation complete'], 'status': 'validated' if is_valid else 'invalid'}; def perform_calibration(state: BauxiteAnalysisState): return {'validation_results': ['Calibration parameters applied']}; graph = StateGraph(BauxiteAnalysisState); graph.add_node('validate', validate_spec); graph.add_node('calibrate', perform_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); app = graph.compile()