from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ValveState(TypedDict): valve_spec: dict; validation_results: List[str]; graph = StateGraph(ValveState); def validate_spec(state: ValveState): state['validation_results'].append('Spec verified against ANSI/ASME standards'); return state; def check_pressure(state: ValveState): state['validation_results'].append('Pressure threshold validated'); return state; graph.add_node('validate_spec', validate_spec); graph.add_node('check_pressure', check_pressure); graph.set_entry_point('validate_spec'); graph.add_edge('validate_spec', 'check_pressure'); graph.add_edge('check_pressure', END); graph = graph.compile()