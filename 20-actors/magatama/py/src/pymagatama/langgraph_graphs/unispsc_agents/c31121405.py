from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(CastingState); def validate_machining(state: CastingState): state['validation_results'].append('Dimensional check passed'); return state; def check_metallurgy(state: CastingState): state['validation_results'].append('Material grade verified'); return state; graph.add_node('validate', validate_machining); graph.add_node('metallurgy', check_metallurgy); graph.set_entry_point('validate'); graph.add_edge('validate', 'metallurgy'); graph.add_edge('metallurgy', END); compiled_graph = graph.compile()