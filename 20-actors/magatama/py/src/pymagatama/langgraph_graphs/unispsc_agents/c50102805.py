from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): cold_chain_verified: bool; quality_passed: bool; def check_temperature(state: ProcureState): return {'cold_chain_verified': True}; def validate_quality(state: ProcureState): return {'quality_passed': True}; graph = StateGraph(ProcureState); graph.add_node('verify_temp', check_temperature); graph.add_node('qc_check', validate_quality); graph.set_entry_point('verify_temp'); graph.add_edge('verify_temp', 'qc_check'); graph.add_edge('qc_check', END); graph = graph.compile()