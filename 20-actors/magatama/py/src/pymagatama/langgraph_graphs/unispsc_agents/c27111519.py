from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolProcurementState(TypedDict): tool_specs: dict; validation_results: List[str]; def validate_specs(state: ToolProcurementState): results = []; if not state['tool_specs'].get('hardness'): results.append('Missing HRC rating'); return {'validation_results': results}; def generate_report(state: ToolProcurementState): print('Generating procurement spec for snips...'); return {'validation_results': ['Report Generated']}; graph = StateGraph(ToolProcurementState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()