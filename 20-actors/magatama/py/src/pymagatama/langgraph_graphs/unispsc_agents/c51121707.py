from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): vendor_validated: bool; quality_checks: List[str]; status: str; def validate_vendor(state: 'DrugProcurementState'): state['vendor_validated'] = True; state['status'] = 'Vendor Validated'; return state; def perform_compliance_check(state: 'DrugProcurementState'): state['quality_checks'] = ['FDA_Audit', 'Stability_Test']; state['status'] = 'Compliance Verified'; return state; graph = StateGraph(DrugProcurementState); graph.add_node('validate', validate_vendor); graph.add_node('compliance', perform_compliance_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()