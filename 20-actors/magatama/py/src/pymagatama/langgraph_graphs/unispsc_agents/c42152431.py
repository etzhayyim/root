from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProsthesisState(TypedDict): material_spec: str; certification: bool; validated: bool; def validate_biocompatibility(state: ProsthesisState): state['validated'] = state['certification'] == True; return state; def check_specs(state: ProsthesisState): return 'validated' if state['validated'] else END; graph = StateGraph(ProsthesisState); graph.add_node('biocompatibility_check', validate_biocompatibility); graph.set_entry_point('biocompatibility_check'); graph.add_edge('biocompatibility_check', END); graph = graph.compile()