from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: CastingState): state['validation_report'] = 'Validating ASTM standards and dimensional tolerances.' if 'material' in state['specs'] else 'Missing material spec'; return state; def check_machining(state: CastingState): state['validation_report'] += ' - Machining precision verified.'; return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.add_node('check_machining', check_machining); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_machining'); graph.add_edge('check_machining', END); app = graph.compile()