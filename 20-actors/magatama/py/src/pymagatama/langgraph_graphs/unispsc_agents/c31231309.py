from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ZincPipeState(TypedDict): specs: dict; validation_results: List[str]; def validate_alloy(state: ZincPipeState): state['validation_results'].append('Alloy standards verified'); return state; def check_dimensions(state: ZincPipeState): state['validation_results'].append('Dimensions checked'); return state; graph = StateGraph(ZincPipeState); graph.add_node('validate', validate_alloy); graph.add_node('dimensions', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', 'dimensions'); graph.add_edge('dimensions', END); graph = graph.compile()