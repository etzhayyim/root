from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec_data: dict; approved: bool; def validate_export_control(state: State): return {'status': 'EXPORT_SCREENED'}; def radiation_safety_check(state: State): return {'approved': True}; graph = StateGraph(State); graph.add_node('export_check', validate_export_control); graph.add_node('safety_audit', radiation_safety_check); graph.set_entry_point('export_check'); graph.add_edge('export_check', 'safety_audit'); graph.add_edge('safety_audit', END); graph = graph.compile()