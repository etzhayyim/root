from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: FastenerState): min_grade = 8.8; if state['specs'].get('grade', 0) >= min_grade: return {'valid': True}; return {'valid': False, 'error': 'Grade below minimum requirement'}; graph = StateGraph(FastenerState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()