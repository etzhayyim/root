from langgraph.graph import StateGraph, END; from typing import TypedDict; class FilamentState(TypedDict): specs: dict; valid: bool; def validate_specs(state: FilamentState): state['valid'] = 'Melting point' in state['specs'] and state['specs']['Melting point'] > 3000; return state; graph = StateGraph(FilamentState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()