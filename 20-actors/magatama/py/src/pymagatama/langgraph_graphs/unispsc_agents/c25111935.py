from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PoleState(TypedDict): pole_spec: dict; validation_results: list; graph = StateGraph(PoleState); def validate_specs(state: PoleState): return {'validation_results': ['Material check passed', 'Load capacity verified']}; def check_compatibility(state: PoleState): return {'validation_results': state['validation_results'] + ['Fitting compatibility verified']}; graph.add_node('validate', validate_specs); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); compiled_graph = graph.compile()