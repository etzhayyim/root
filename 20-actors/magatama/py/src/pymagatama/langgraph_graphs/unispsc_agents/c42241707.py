from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BraceState(TypedDict): specs: dict; validation_results: List[str]; def validate_medical_standards(state: BraceState): state['validation_results'].append('ISO-13485-Checked'); return state; def check_material_safety(state: BraceState): state['validation_results'].append('Biocompatibility-Verified'); return state; graph = StateGraph(BraceState); graph.add_node('validate', validate_medical_standards); graph.add_node('safety', check_material_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()