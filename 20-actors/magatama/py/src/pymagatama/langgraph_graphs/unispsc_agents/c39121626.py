from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FuseState(TypedDict): specs: dict; compliance: bool; valid: bool; def validate_specs(state: FuseState): state['valid'] = state['specs'].get('rated_voltage_kv', 0) > 0.6; return state; def check_compliance(state: FuseState): state['compliance'] = state['specs'].get('standard_compliance') is not None; return state; graph = StateGraph(FuseState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()