from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RehabitationState(TypedDict): device_id: str; safety_flags: List[str]; inspection_passed: bool; def validate_device(state: RehabitationState): state['inspection_passed'] = 'safety_iso' in state['safety_flags']; return state; def finalize_procurement(state: RehabitationState): return {'status': 'approved' if state['inspection_passed'] else 'rejected'}; graph = StateGraph(RehabitationState); graph.add_node('validate', validate_device); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()