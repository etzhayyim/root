from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NuclearState(TypedDict): material_id: str; hazard_level: float; compliance_verified: bool; def validate_hazards(state: NuclearState): return { 'compliance_verified': state['hazard_level'] < 1000 }; def check_export_controls(state: NuclearState): print('Checking ECCN compliance'); return { 'compliance_verified': True }; graph = StateGraph(NuclearState); graph.add_node('validate', validate_hazards); graph.add_node('export_check', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()