from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TWTState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; compliance_status: bool; def validate_twt_specs(state: TWTState): results = []; specs = state['input_spec']; if specs.get('power', 0) > 1000: results.append('High power check required'); return {'validation_results': results}; def check_export_control(state: TWTState): return {'compliance_status': True}; graph = StateGraph(TWTState); graph.add_node('validate', validate_twt_specs); graph.add_node('compliance', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()