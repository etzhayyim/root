from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TitaniumSpecState(TypedDict): material_grade: str; dimensions: dict; cert_files: List[str]; validation_status: bool; def validate_grade(state: TitaniumSpecState): state['validation_status'] = state['material_grade'] in ['Grade 5', 'Grade 23']; return state; def check_certs(state: TitaniumSpecState): state['validation_status'] = len(state['cert_files']) > 0; return state; graph = StateGraph(TitaniumSpecState); graph.add_node('validate', validate_grade); graph.add_node('cert_check', check_certs); graph.set_entry_point('validate'); graph.add_edge('validate', 'cert_check'); graph.add_edge('cert_check', END); graph = graph.compile()