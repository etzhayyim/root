from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validation_results: List[str]; graph = StateGraph(ProcureState); def check_spec(state: ProcureState): return {'validation_results': ['Chemical resistance verified'] if 'acid-rated' in state['items'] else ['Missing chemical rating']}; def finalize(state: ProcureState): return {'validation_results': state['validation_results'] + ['Approved']}; graph.add_node('spec_check', check_spec); graph.add_node('finalizer', finalize); graph.set_entry_point('spec_check'); graph.add_edge('spec_check', 'finalizer'); graph.add_edge('finalizer', END); compile_graph = graph.compile()