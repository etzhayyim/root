from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaintState(TypedDict): vendor_specs: dict; compliance_verified: bool; chemical_safety_check: bool; def validate_compliance(state: PaintState): state['compliance_verified'] = state['vendor_specs'].get('voc_compliant', False); return state; def check_chemicals(state: PaintState): state['chemical_safety_check'] = 'dangerous_goods' not in state['vendor_specs'].get('hazmat_flags', []); return state; graph = StateGraph(PaintState); graph.add_node('validate', validate_compliance); graph.add_node('chemicals', check_chemicals); graph.set_entry_point('validate'); graph.add_edge('validate', 'chemicals'); graph.add_edge('chemicals', END); graph.compile()