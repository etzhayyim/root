from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_data: dict; validation_results: Annotated[list, operator.add]; def validate_specs(state: ToolState): results = []; capacity = state['tool_data'].get('capacity', 0); results.append('Passed' if capacity > 0 else 'Failed'); return {'validation_results': results}; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()