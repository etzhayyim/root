from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): medicine: str; compliant: bool; def check_gmp(state: State): return {'compliant': state.get('medicine') == 'Prazosin' and True}; graph = StateGraph(State); graph.add_node('verify_gmp', check_gmp); graph.set_entry_point('verify_gmp'); graph.add_edge('verify_gmp', END); graph = graph.compile()