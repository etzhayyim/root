from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BatteryState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: BatteryState): errors = []; if not state['specs'].get('voltage'): errors.append('Missing voltage'); return {'validation_results': errors}; def check_hazmat(state: BatteryState): return {'validation_results': state['validation_results'] + ['Hazmat clearance required']}; graph = StateGraph(BatteryState); graph.add_node('validate', validate_specs); graph.add_node('hazmat', check_hazmat); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()