from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpineBoardState(TypedDict): board_id: str; compliance_docs: List[str]; approved: bool; def check_compliance(state: SpineBoardState): return {'approved': len(state['compliance_docs']) > 0}; def finalize_procurement(state: SpineBoardState): print(f'Board {state['board_id']} ready for acquisition'); return {'approved': True}; graph = StateGraph(SpineBoardState); graph.add_node('compliance', check_compliance); graph.add_node('finalizer', finalize_procurement); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'finalizer'); graph.add_edge('finalizer', END); graph.compile()