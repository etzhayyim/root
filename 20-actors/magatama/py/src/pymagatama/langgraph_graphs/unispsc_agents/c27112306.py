from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StampState(TypedDict): metal_type: str; hrc_rating: int; verification_status: bool; def validate_stamp(state: StampState): return {'verification_status': state['hrc_rating'] >= 50}; graph = StateGraph(StampState); graph.add_node('validate', validate_stamp); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()