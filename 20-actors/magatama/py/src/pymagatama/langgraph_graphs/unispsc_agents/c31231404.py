from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShimState(TypedDict): thickness: float; alloy: str; verified: bool; def validate_spec(state: ShimState): state['verified'] = state['thickness'] > 0 and state['alloy'] != ''; return state; graph = StateGraph(ShimState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()