from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_specs: dict; validation_results: List[str]; graph = StateGraph(ProcState); def validate_tolerance(state: ProcState): state['validation_results'].append('Tolerance Check Initiated'); return state; def check_material(state: ProcState): state['validation_results'].append('Material Specs Verified'); return state; graph.add_node('validate_dim', validate_tolerance); graph.add_node('check_mat', check_material); graph.add_edge('validate_dim', 'check_mat'); graph.add_edge('check_mat', END); graph.set_entry_point('validate_dim'); compiled_graph = graph.compile()