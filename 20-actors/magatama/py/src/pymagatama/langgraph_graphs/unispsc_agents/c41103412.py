from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChamberState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ChamberState): errors = []; if state['specs'].get('temp_range', 0) > 100: errors.append('High temperature exceeded safety limit'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(ChamberState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()