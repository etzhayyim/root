from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; validation_result: bool; def validate_specs(state: State): specs = state['specs']; is_valid = all(key in specs for key in ['stroke_length_mm', 'load_capacity_n']); return {'validation_result': is_valid}; def check_export_compliance(state: State): print('Checking dual-use control thresholds...'); return {'validation_result': True}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.compile()