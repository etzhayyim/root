from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: List[str]; def validate_calibration(state: State): state['validation_results'].append('Calibration check passed'); return state; def validate_accuracy(state: State): state['validation_results'].append('Measurement accuracy verified'); return state; graph = StateGraph(State); graph.add_node('validate_cal', validate_calibration); graph.add_node('validate_acc', validate_accuracy); graph.set_entry_point('validate_cal'); graph.add_edge('validate_cal', 'validate_acc'); graph.add_edge('validate_acc', END); compiled_graph = graph.compile()