from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MowerState(TypedDict): specs: dict; compliance_passed: bool; def validate_specs(state: MowerState): if 'power_source' not in state['specs']: return {'compliance_passed': False}; return {'compliance_passed': True}; graph = StateGraph(MowerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()