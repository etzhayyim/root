from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AirwayState(TypedDict): airway_type: str; is_sterile: bool; certs: List[str]; def validate_specs(state: AirwayState): if not state['is_sterile']: return 'reject'; return 'approve'; graph = StateGraph(AirwayState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()