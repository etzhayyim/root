from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DispenserState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: DispenserState): state['compliance'] = 'medical_device_certification' in state['specs']; return state; def check_approval(state: DispenserState): state['approved'] = state['compliance'] == True; return state; graph = StateGraph(DispenserState); graph.add_node('validate', validate_specs); graph.add_node('approval', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph = graph.compile()