from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; def validate_spec(state: State): return {'validation_results': ['Material check passed', 'Dimensions verified']}; def check_compatibility(state: State): return {'validation_results': ['Compatibility confirmed']}; graph = StateGraph(State); graph.add_node('spec_validation', validate_spec); graph.add_node('compatibility_check', check_compatibility); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', 'compatibility_check'); graph.add_edge('compatibility_check', END); graph = graph.compile()