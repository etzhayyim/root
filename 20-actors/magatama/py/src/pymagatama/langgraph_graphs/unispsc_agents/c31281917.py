from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; specs: dict; validation_results: list; graph = StateGraph(ComponentState); def validate_draw_specs(state: ComponentState): state['validation_results'].append('Dimensional check passed'); return state; def check_material_composition(state: ComponentState): state['validation_results'].append('Material certification verified'); return state; graph.add_node('validate', validate_draw_specs); graph.add_node('material', check_material_composition); graph.set_entry_point('validate'); graph.add_edge('validate', 'material'); graph.add_edge('material', END); graph = graph.compile()