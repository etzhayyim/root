from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): mold_specs: dict; status: str; def validate_specs(state: State): specs = state['mold_specs']; is_valid = all(k in specs for k in ['density', 'strength']); return {'status': 'validated' if is_valid else 'rejected'}; def check_export_compliance(state: State): return {'status': 'cleared_for_export'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()