from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlantState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_material(state: PlantState): errors = []; for item in state['items']: if 'flammability_rating' not in item: errors.append(f'Missing flame resistance for {item.get('name')}'); return {'validation_errors': errors}; graph = StateGraph(PlantState); graph.add_node('validate_spec', validate_material); graph.set_entry_point('validate_spec'); graph.add_edge('validate_spec', END); graph = graph.compile()