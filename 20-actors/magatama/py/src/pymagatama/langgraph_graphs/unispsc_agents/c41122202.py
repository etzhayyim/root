from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CrucibleState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_thermal_spec(state: CrucibleState): temp = state['specs'].get('temp', 0); state['approved'] = temp > 1000; return state; graph = StateGraph(CrucibleState); graph.add_node('validate', validate_thermal_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()