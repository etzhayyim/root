from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): items: Annotated[list, operator.add]; validation_results: dict; graph = StateGraph(PipeState); def validate_welds(state): return {'validation_results': {'status': 'passed', 'test_method': 'ultrasonic'}}; def approve_assembly(state): return {'validation_results': {'final': 'approved'}}; graph.add_node('validate', validate_welds); graph.add_node('approve', approve_assembly); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()