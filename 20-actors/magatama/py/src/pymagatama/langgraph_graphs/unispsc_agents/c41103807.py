from langgraph.graph import StateGraph, END; from typing import TypedDict; class MixerState(TypedDict): specs: dict; validation: bool; def validate_specs(state: MixerState): state['validation'] = all(k in state['specs'] for k in ['rpm', 'orbit_mm']); return state; graph = StateGraph(MixerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()