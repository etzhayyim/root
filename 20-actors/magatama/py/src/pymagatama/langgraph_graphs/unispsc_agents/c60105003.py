from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AppState(TypedDict): specs: dict; status: str; def validate_specs(state: AppState): return {'status': 'validated' if 'voltage' in state['specs'] else 'invalid'}; def route_approval(state: AppState): return 'complete' if state['status'] == 'validated' else 'error'; graph = StateGraph(AppState); graph.add_node('validate', validate_specs); graph.add_edge('validate', 'complete'); graph.set_entry_point('validate'); graph.set_finish_point('complete'); compiled_graph = graph.compile()