from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): needle_gauge: str; is_sterile: bool; compliance_status: str; def validate_spec(state: State): return {'compliance_status': 'passed' if state['is_sterile'] else 'failed'}; graph = StateGraph(State); graph.add_node('validation', validate_spec); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()