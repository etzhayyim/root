from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DentalState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: DentalState): specs = state['specs']; is_valid = all(key in specs for key in ['material', 'autoclavable']); return {'valid': is_valid, 'error': '' if is_valid else 'Missing required spec fields'}; graph = StateGraph(DentalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()