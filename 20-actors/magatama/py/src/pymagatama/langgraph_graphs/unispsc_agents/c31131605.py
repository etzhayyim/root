from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): material_spec: str; dimensions: dict; quality_report: str; approved: bool; def validate_spec(state: ForgingState): state['approved'] = 'grade_compliant' in state['material_spec'] and len(state['dimensions']) > 0; return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()