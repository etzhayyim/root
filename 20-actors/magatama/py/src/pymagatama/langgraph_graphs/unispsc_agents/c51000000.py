from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): items: List[str]; qc_passed: bool; compliance_docs: List[str]; def validate_regulatory(state: PharmaState): state['qc_passed'] = all(d in ['GMP', 'FDA_Approval'] for d in state.get('compliance_docs', [])); return state; def route_storage(state: PharmaState): return 'cold_chain' if state.get('qc_passed') else 'reject'; graph = StateGraph(PharmaState); graph.add_node('validate', validate_regulatory); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()