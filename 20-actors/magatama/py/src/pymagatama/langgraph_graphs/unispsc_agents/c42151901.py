from langgraph.graph import StateGraph, END; from typing import TypedDict; class DentalBrushState(TypedDict): brush_spec: dict; valid: bool; def validate_spec(state: DentalBrushState): state['valid'] = all(k in state['brush_spec'] for k in ['bristle_material', 'fda_clearance_number']); return state; graph = StateGraph(DentalBrushState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()