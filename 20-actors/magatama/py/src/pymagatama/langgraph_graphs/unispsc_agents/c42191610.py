from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CaseworkState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: CaseworkState): state['validation_results'].append('Material antimicrobial verification complete'); return state; def check_structural_load(state: CaseworkState): state['validation_results'].append('Load capacity verified'); return state; graph = StateGraph(CaseworkState); graph.add_node('material_check', validate_materials); graph.add_node('load_check', check_structural_load); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'load_check'); graph.add_edge('load_check', END); graph = graph.compile()