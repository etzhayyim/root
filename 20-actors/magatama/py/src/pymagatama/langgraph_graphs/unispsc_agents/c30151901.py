from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AwningState(TypedDict): dimensions: dict; material: str; wind_rating: float; compliance_verified: bool; def validate_specs(state: AwningState) -> AwningState: state['compliance_verified'] = state['wind_rating'] > 50.0; return state; def finalize_order(state: AwningState) -> AwningState: return state; graph = StateGraph(AwningState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()