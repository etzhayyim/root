from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): items: list; status: str; def validate_specs(state: ProcState): return { "status": "validated" if "ISO 13485" in state["items"] else "rejected" }; graph = StateGraph(ProcState); graph.add_node("validate", validate_specs); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()