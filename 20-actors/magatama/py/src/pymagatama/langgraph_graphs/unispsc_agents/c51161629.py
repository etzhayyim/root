from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): vendor_gmp_status: bool; chemical_purity: float; documents: List[str]; def validate_gmp(state: PharmState): return {'vendor_gmp_status': True} if 'GMP' in state['documents'] else {'vendor_gmp_status': False}; def check_purity(state: PharmState): return {'chemical_purity': 99.9} if state['chemical_purity'] >= 99.0 else {'chemical_purity': 0.0}; graph = StateGraph(PharmState); graph.add_node('validate_gmp', validate_gmp); graph.add_node('check_purity', check_purity); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', 'check_purity'); graph.add_edge('check_purity', END); graph = graph.compile()