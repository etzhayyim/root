from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): chemical_data: dict; compliance_check: bool; def validate_chemical(state: ProcureState): return {'compliance_check': 'malathion' in state['chemical_data'].get('compound', '').lower()}; def finalize_order(state: ProcureState): return {'compliance_check': True}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_chemical); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()