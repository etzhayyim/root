from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ToolSpecState(TypedDict): specs: List[str]; validation_passed: bool; def validate_specs(state: ToolSpecState): return {'validation_passed': all(s in state['specs'] for s in ['material', 'tolerance', 'calibration'])}; graph = StateGraph(ToolSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()