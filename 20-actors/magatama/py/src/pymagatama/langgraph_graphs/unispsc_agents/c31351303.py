from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): specs: dict; compliance_passed: bool; validation_log: List[str]; graph = StateGraph(AssemblyState); def validate_material(state: AssemblyState): state['validation_log'].append('Verifying Hastelloy X spec compliance'); state['compliance_passed'] = True; return state; def check_export(state: AssemblyState): state['validation_log'].append('Running ITAR/EAR screening'); return state; graph.add_node('material_check', validate_material); graph.add_node('export_check', check_export); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()