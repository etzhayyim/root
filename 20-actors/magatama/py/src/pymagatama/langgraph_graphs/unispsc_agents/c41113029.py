from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_instrument(state: AnalysisState): return {'validation_results': ['Calibrated'], 'status': 'Validated'}; def run_compliance_check(state: AnalysisState): return {'validation_results': ['ExportControlClear'], 'status': 'Compliant'}; graph = StateGraph(AnalysisState); graph.add_node('validate', validate_instrument); graph.add_node('compliance', run_compliance_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()