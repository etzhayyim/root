from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CleaningGraphState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_equipment(state: CleaningGraphState): state['validation_errors'] = []; if state['specs'].get('power_rating', 0) <= 0: state['validation_errors'].append('Invalid power rating'); return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(CleaningGraphState); graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()