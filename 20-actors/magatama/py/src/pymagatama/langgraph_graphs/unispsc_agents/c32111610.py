from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class JFETState(TypedDict): component_id: str; specs: dict; compliant: bool; def validate_specs(state: JFETState): state['compliant'] = all(val is not None for val in state['specs'].values()); return state; def export_classification(state: JFETState): state['export_status'] = 'ECCN_3A001' if state['compliant'] else 'Flagged'; return state; graph = StateGraph(JFETState); graph.add_node('validate', validate_specs); graph.add_node('classify', export_classification); graph.add_edge('validate', 'classify'); graph.add_edge('classify', END); graph.set_entry_point('validate'); graph = graph.compile()