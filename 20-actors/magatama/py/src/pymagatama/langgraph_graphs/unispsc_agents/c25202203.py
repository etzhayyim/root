from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftPartsState(TypedDict): part_id: str; compliance_docs: List[str]; status: str; def validate_airworthiness(state: AircraftPartsState): return {'status': 'CERTIFIED'} if len(state['compliance_docs']) > 3 else {'status': 'REJECTED'}; def perform_load_analysis(state: AircraftPartsState): return {'status': 'ANALYZED'}; graph = StateGraph(AircraftPartsState); graph.add_node('validate', validate_airworthiness); graph.add_node('analysis', perform_load_analysis); graph.set_entry_point('validate'); graph.add_edge('validate', 'analysis'); graph.add_edge('analysis', END); graph = graph.compile()