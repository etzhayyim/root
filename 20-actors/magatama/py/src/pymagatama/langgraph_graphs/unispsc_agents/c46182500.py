from langgraph.graph import StateGraph, END; from typing import TypedDict; class SafetyState(TypedDict): device_specs: dict; compliance_cleared: bool; def val_spec(state: SafetyState): state['compliance_cleared'] = 'compliance_certification' in state['device_specs']; return state; def security_check(state: SafetyState): return 'compliance' if state['compliance_cleared'] else 'manual_review'; graph = StateGraph(SafetyState); graph.add_node('validation', val_spec); graph.add_edge('validation', END); graph.set_entry_point('validation'); graph = graph.compile()