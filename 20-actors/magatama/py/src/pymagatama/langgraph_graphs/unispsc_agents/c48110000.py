from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class VendingState(TypedDict): specs: dict; valid: bool; validator_msg: str; def validate_specs(state: VendingState): state['valid'] = all(k in state['specs'] for k in ['voltage', 'temp_control']); state['validator_msg'] = 'Validated' if state['valid'] else 'Missing specs'; return state; graph = StateGraph(VendingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()