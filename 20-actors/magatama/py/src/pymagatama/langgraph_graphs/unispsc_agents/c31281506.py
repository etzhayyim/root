from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): component_specs: dict; validation_results: List[str]; def validate_stamping(state: State): specs = state['component_specs']; results = []; if 'material' not in specs: results.append('Missing material grade'); return {'validation_results': results}; graph = StateGraph(State); graph.add_node('validate', validate_stamping); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()