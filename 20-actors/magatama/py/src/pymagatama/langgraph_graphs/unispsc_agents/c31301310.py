from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_dimensions(state: ForgingState): return {'validation_results': ['Dimensional check passed']}; def check_material_purity(state: ForgingState): return {'validation_results': ['Purity check passed']}; graph = StateGraph(ForgingState); graph.add_node('dim_check', validate_dimensions); graph.add_node('material_check', check_material_purity); graph.set_entry_point('dim_check'); graph.add_edge('dim_check', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()