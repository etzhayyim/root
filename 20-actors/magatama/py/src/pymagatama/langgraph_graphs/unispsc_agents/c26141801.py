from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; payload: dict; def validate_nuclear_specs(state: State): state['status'] = 'VALIDATED' if 'radiation_rating' in state['payload'] else 'REJECTED'; return state; def check_export_controls(state: State): state['status'] = 'REVIEW_REQUIRED'; return state; graph = StateGraph(State); graph.add_node('validate', validate_nuclear_specs); graph.add_node('export_review', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()