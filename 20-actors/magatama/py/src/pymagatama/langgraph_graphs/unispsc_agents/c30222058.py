from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec: dict; approved: bool; def validate_spec(state: ProcState): if state['spec'].get('flow_rate') > 0: return {'approved': True}; return {'approved': False}; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()