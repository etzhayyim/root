from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DrugProcurement(TypedDict): status: str; logs: Annotated[list, operator.add]; def validate_license(state): return {'logs': ['License verified'], 'status': 'VALID'}; def check_compliance(state): return {'logs': ['Pharma compliance checked'], 'status': 'COMPLIANT'}; graph = StateGraph(DrugProcurement); graph.add_node('validate', validate_license); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()