from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class InsufflationState(TypedDict): needle_id: str; quality_passed: bool; compliance_docs: List[str]; def validate_medical_compliance(state: InsufflationState): state['quality_passed'] = 'ISO_13485' in state['compliance_docs']; return state; def check_needle_specs(state: InsufflationState): print(f'Processing needle: {state['needle_id']}'); return state; graph = StateGraph(InsufflationState); graph.add_node('validate', validate_medical_compliance); graph.add_node('check', check_needle_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()