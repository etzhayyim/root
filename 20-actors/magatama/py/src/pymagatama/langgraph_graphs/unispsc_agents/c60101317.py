from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): content: str; quality_score: int; def validate_educational_content(state: State): state['quality_score'] = 10 if len(state['content']) > 10 else 0; return state; graph = StateGraph(State); graph.add_node('validate', validate_educational_content); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()