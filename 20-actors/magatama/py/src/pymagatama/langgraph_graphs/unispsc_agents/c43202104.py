from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VHSStorageState(TypedDict): items: List[str]; validated_data: dict; status: str; def validate_dimensions(state: VHSStorageState): return {'validated_data': {'check': 'dimensions_verified'}, 'status': 'validated'}; def finalize_order(state: VHSStorageState): return {'status': 'ready_for_procurement'}; graph = StateGraph(VHSStorageState); graph.add_node('validate', validate_dimensions); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()