from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MercuryDeviceState(TypedDict): device_id: str; compliance_checks: Annotated[Sequence[str], operator.add]; status: str; def validate_compliance(state: MercuryDeviceState): return {'compliance_checks': ['mercury_safety_protocol'], 'status': 'validated'} if state.get('device_id') else {'status': 'failed'}; def dispose_plan(state: MercuryDeviceState): return {'status': 'archived'}; graph = StateGraph(MercuryDeviceState); graph.add_node('validate', validate_compliance); graph.add_node('dispose', dispose_plan); graph.add_edge('validate', 'dispose'); graph.add_edge('dispose', END); graph.set_entry_point('validate'); graph = graph.compile()