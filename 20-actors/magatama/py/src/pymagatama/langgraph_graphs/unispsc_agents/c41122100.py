from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProtocolState(TypedDict): items: List[str]; validated: bool; log: List[str]; def validation_node(state: ProtocolState): state['validated'] = all(['sterile' in i.lower() for i in state['items']]); state['log'].append('Validation completed'); return state; graph = StateGraph(ProtocolState); graph.add_node('validate', validation_node); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()