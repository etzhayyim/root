from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GeneratorSpec(TypedDict): specs: dict; compliance_status: bool; def validate_specs(state: GeneratorSpec): state['compliance_status'] = all(k in state['specs'] for k in ['power', 'emissions']); return state; graph = StateGraph(GeneratorSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()