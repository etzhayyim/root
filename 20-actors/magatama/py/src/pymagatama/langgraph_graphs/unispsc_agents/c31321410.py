from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TitaniumProcurementState(TypedDict): part_specs: dict; compliance_check: bool; ntw_status: str; def validate_specs(state: TitaniumProcurementState): print('Validating titanium weld specs...'); return {'compliance_check': True if 'grade' in state['part_specs'] else False}; def verify_ntw(state: TitaniumProcurementState): return {'ntw_status': 'Certified' if state.get('compliance_check') else 'Rejected'}; graph = StateGraph(TitaniumProcurementState); graph.add_node('validate', validate_specs); graph.add_node('certify', verify_ntw); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); app = graph.compile()