from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SteeringState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: SteeringState): is_valid = 'safety_certification_standard' in state.get('specs', {}) and state.get('specs', {}).get('outer_diameter_mm', 0) > 0; return {'valid': is_valid, 'error': '' if is_valid else 'Missing critical safety specs'}; graph = StateGraph(SteeringState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()