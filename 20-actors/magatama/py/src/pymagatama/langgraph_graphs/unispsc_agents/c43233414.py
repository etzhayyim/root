from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoftwareState(TypedDict): vendor: str; ver: str; valid: bool; media_types: List[str]; def validate_software(state: SoftwareState): state['valid'] = 'x86_64' in state['media_types'] and len(state['ver']) > 0; return state; graph = StateGraph(SoftwareState); graph.add_node('validate', validate_software); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()