from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LabVibratorState(TypedDict): specs: dict; validation_results: List[str]; def validate_vibrator(state: LabVibratorState): frequency = state['specs'].get('frequency', 0); return {'validation_results': ['Passed' if 10 <= frequency <= 3000 else 'Failed']}; workflow = StateGraph(LabVibratorState); workflow.add_node('validate', validate_vibrator); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()