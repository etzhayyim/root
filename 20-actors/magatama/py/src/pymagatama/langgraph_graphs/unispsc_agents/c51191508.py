from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): chemical_name: str; gmp_certified: bool; quality_report: str; approved: bool; def validate_gmp(state: PharmState): return {'gmp_certified': True}; def check_purity(state: PharmState): return {'approved': 'Purity > 99%' in state['quality_report']}; graph = StateGraph(PharmState); graph.add_node('validate_gmp', validate_gmp); graph.add_node('check_purity', check_purity); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', 'check_purity'); graph.add_edge('check_purity', END); graph = graph.compile()