from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ParagliderState(TypedDict): doc_list: List[str]; validation_results: dict; graph = StateGraph(ParagliderState); def validate_certs(state: ParagliderState): return {'validation_results': {'certs': 'verified'}}; def check_flight_record(state: ParagliderState): return {'validation_results': {'flight_record': 'ok'}}; graph.add_node('cert_check', validate_certs); graph.add_node('record_check', check_flight_record); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'record_check'); graph.add_edge('record_check', END); graph = graph.compile()