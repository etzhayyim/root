from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): data: dict; logs: Annotated[list, operator.add]; def validate_pcie(state: ProcState): state['logs'].append('Validating PCIe compatibility'); return {'data': state['data']}; def check_security(state: ProcState): state['logs'].append('Checking firmware signature'); return {'data': state['data']}; graph = StateGraph(ProcState); graph.add_node('validation', validate_pcie); graph.add_node('security', check_security); graph.add_edge('validation', 'security'); graph.add_edge('security', END); graph.set_entry_point('validation'); graph = graph.compile()