from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_data: dict; validation_report: List[str]; def validate_torque(state: ToolState): state['validation_report'].append('Validating torque specifications...'); return state; def check_pressure(state: ToolState): state['validation_report'].append('Checking pneumatic requirements...'); return state; graph = StateGraph(ToolState); graph.add_node('validate_torque', validate_torque); graph.add_node('check_pressure', check_pressure); graph.set_entry_point('validate_torque'); graph.add_edge('validate_torque', 'check_pressure'); graph.add_edge('check_pressure', END); graph = graph.compile()