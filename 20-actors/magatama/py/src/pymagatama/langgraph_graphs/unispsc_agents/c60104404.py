from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ModelProcurementState(TypedDict): model_specs: dict; validation_results: List[str]; graph = StateGraph(ModelProcurementState); def validate_specs(state: ModelProcurementState): print('Validating landform scale and material...'); return {'validation_results': ['Scale verified', 'Material compliant']}; def finalize_order(state: ModelProcurementState): return {'model_specs': state['model_specs']}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()