from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; validation_report: List[str]; def validate_material(state: PipeState): state['validation_report'].append('Material verified for NPT conformity'); return state; def check_pressure(state: PipeState): state['validation_report'].append('Pressure rating compliance confirmed'); return state; graph = StateGraph(PipeState); graph.add_node('validate', validate_material); graph.add_node('pressure_check', check_pressure); graph.add_edge('validate', 'pressure_check'); graph.add_edge('pressure_check', END); graph.set_entry_point('validate'); graph = graph.compile()