from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TinProfileState(TypedDict): specs: dict; validation_report: str; graph = StateGraph(TinProfileState); def validate_extrusion_specs(state: TinProfileState): alloy = state['specs'].get('alloy'); report = 'Valid' if alloy == 'Sn-based' else 'Invalid'; return {'validation_report': report}; graph.add_node('validate', validate_extrusion_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()