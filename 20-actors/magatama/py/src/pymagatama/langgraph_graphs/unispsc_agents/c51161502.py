from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): status: str; purity: float; compliance: bool; def validate_purity(state: PharmaState): return {'compliance': state['purity'] >= 99.0}; def check_compliance(state: PharmaState): return {'status': 'APPROVED' if state['compliance'] else 'REJECTED'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.compile()