from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WineState(TypedDict): wine_data: dict; compliance_status: bool; def validate_integrity(state: WineState): return {'compliance_status': state['wine_data'].get('temp', 0) < 18}; def approve_order(state: WineState): return {'compliance_status': True}; graph = StateGraph(WineState); graph.add_node('validate', validate_integrity); graph.add_node('approve', approve_order); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()