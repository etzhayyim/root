from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: RobotProcurementState): errors = []; if state['specs'].get('payload_capacity_kg', 0) <= 0: errors.append('Invalid payload'); return {'validation_errors': errors}; def check_safety(state: RobotProcurementState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(RobotProcurementState); graph.add_node('validate', validate_specs); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_safety'); graph.add_edge('check_safety', END); compiled_graph = graph.compile()