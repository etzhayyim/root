from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_items(state: ProcState): state['validated'] = all(['ISO13485' in c for c in state['certs']]); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_items); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()