from langgraph.graph import StateGraph, END; from typing import TypedDict; class TubeState(TypedDict): specs: dict; valid: bool; def validate_weld(state: TubeState): state['valid'] = state['specs'].get('weld_cert') == 'ISO-3834'; return state; graph = StateGraph(TubeState); graph.add_node('validate', validate_weld); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()