from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; is_compliant: bool; graph = StateGraph(PharmState); def validate_purity(state: PharmState): return {'is_compliant': state['purity'] >= 99.0}; def log_compliance(state: PharmState): print(f'Compliance check for {state["chemical_name"]}: {state["is_compliant"]}'); return state; graph.add_node('validate', validate_purity); graph.add_node('log', log_compliance); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.set_entry_point('validate'); graph = graph.compile()