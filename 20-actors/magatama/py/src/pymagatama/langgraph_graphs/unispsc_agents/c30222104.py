from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CourtState(TypedDict): site_id: str; compliance_docs: List[str]; approved: bool; def validate_security(state: CourtState): return {'approved': True if 'security_clearance' in state['compliance_docs'] else False}; graph = StateGraph(CourtState); graph.add_node('security_check', validate_security); graph.set_entry_point('security_check'); graph.add_edge('security_check', END); graph = graph.compile()