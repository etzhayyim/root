from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): specs: dict; validation_passed: bool; auditor_notes: str; def validate_washers(state: FastenerState): state['validation_passed'] = all(k in state['specs'] for k in ['material', 'standard']); return state; graph = StateGraph(FastenerState); graph.add_node('validate', validate_washers); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()