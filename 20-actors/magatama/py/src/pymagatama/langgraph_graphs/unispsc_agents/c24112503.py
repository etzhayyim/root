from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CartonState(TypedDict): dimensions: dict; board_grade: str; status: str; def validate_specs(state: CartonState): specs = state['dimensions']; return {'status': 'approved' if specs['length'] > 0 else 'rejected'}; def process_order(state: CartonState): return {'status': 'processed'}; graph = StateGraph(CartonState); graph.add_node('validate', validate_specs); graph.add_node('process', process_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()