from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RIMState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_dimensions(state: RIMState): state['validation_report'] = 'Dimensions verified against CAD'; return state; def check_material(state: RIMState): state['approved'] = state['specs'].get('material') != 'prohibited'; return state; graph = StateGraph(RIMState); graph.add_node('validate_dim', validate_dimensions); graph.add_node('check_mat', check_material); graph.set_entry_point('validate_dim'); graph.add_edge('validate_dim', 'check_mat'); graph.add_edge('check_mat', END); graph = graph.compile()