from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; valid_spec: bool; graph = StateGraph(State); def validate_specs(state: State): state['valid_spec'] = all(k in state['input_data'] for k in ['pump_pressure', 'nozzle_type']); return state; graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()