from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BandingState(TypedDict): model_spec: dict; validation_passed: bool; async def validate_specs(state: BandingState): specs = state['model_spec']; valid = all(key in specs for key in ['max_width', 'voltage']); return {'validation_passed': valid}; async def check_safety(state: BandingState): return {'validation_passed': state['validation_passed'] and state['model_spec'].get('safety_cert') == 'CE'}; graph = StateGraph(BandingState); graph.add_node('validation', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validation'); graph.add_edge('validation', 'safety'); graph.add_edge('safety', END); graph = graph.compile()