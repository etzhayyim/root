from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LeadProcessState(TypedDict): material_specs: dict; compliance_cleared: bool; inspection_result: str; def validate_specs(state: LeadProcessState): return {'compliance_cleared': state['material_specs'].get('purity', 0) > 99.0}; def perform_inspection(state: LeadProcessState): return {'inspection_result': 'PASS' if state['compliance_cleared'] else 'FAIL'}; graph = StateGraph(LeadProcessState); graph.add_node('validate', validate_specs); graph.add_node('inspect', perform_inspection); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()