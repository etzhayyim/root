from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpectrometerState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: SpectrometerState): val = 'Pass' if 'Wavelength_Range' in state['specs'] else 'Fail'; return {'validation_results': [val]}; graph = StateGraph(SpectrometerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()