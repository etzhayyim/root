from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_results: list; status: str; def validate_tensile_strength(state: ProcurementState): state['validation_results'].append('Tensile test passed.'); return state; def check_compliance(state: ProcurementState): state['status'] = 'COMPLIANT' if 'ISO' in state['specs'].get('cert', '') else 'PENDING'; return state; graph = StateGraph(ProcurementState); graph.add_node('validate_specs', validate_tensile_strength); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', 'check_compliance'); graph.add_edge('check_compliance', END); graph = graph.compile()