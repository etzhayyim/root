from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CraftState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(CraftState); def validate_materials(state: CraftState): return {'validation_results': {'status': 'pass', 'checked_fields': ['material', 'diameter']}}; def approve_order(state: CraftState): return {'validation_results': {'status': 'approved'}}; graph.add_node('validate', validate_materials); graph.add_node('approve', approve_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()