from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): specs: dict; valid: bool; def validate_hastelloy(state: State): state['valid'] = all(k in state['specs'] for k in ['mtr', 'tensile_strength']); return state; def route_verification(state: State): return 'process' if state['valid'] else END; graph = StateGraph(State); graph.add_node('process', validate_hastelloy); graph.set_entry_point('process'); graph.add_edge('process', END); graph = graph.compile()