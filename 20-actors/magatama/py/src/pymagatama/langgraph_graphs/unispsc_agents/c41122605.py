from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; valid: bool; def check_specs(state: State): state['valid'] = 'Refractive Index' in state['specs'] and 'SDS' in state['specs']; return state; builder = StateGraph(State); builder.add_node('validate', check_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()