from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KitState(TypedDict): items: List[str]; inspection_passed: bool; expiry_check: bool; graph = StateGraph(KitState); def validate_constituents(state: KitState): return {'inspection_passed': all(['bandages' in state['items'], 'antiseptics' in state['items']])}; def check_expiry(state: KitState): return {'expiry_check': True}; graph.add_node('validate', validate_constituents); graph.add_node('expiry', check_expiry); graph.set_entry_point('validate'); graph.add_edge('validate', 'expiry'); graph.add_edge('expiry', END); graph = graph.compile()