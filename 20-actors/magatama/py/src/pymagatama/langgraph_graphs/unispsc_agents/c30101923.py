from langgraph.graph import StateGraph, END; from typing import TypedDict; class SteelState(TypedDict): specs: dict; valid: bool; def validate_specs(state: SteelState): state['valid'] = 'Aluminium coating thickness' in state['specs']; return state; builder = StateGraph(SteelState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()