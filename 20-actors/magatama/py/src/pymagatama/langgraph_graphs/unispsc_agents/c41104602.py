from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FurnaceState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: FurnaceState): state['compliance'] = state['specs'].get('temp', 0) > 0; return state; def check_export(state: FurnaceState): state['status'] = 'dual-use-check-initiated'; return state; graph = StateGraph(FurnaceState); graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph.set_entry_point('validate'); graph = graph.compile()