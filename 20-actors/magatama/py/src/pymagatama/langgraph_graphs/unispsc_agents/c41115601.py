from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KarlFischerState(TypedDict): spec_data: dict; validation_results: List[str]; graph = StateGraph(KarlFischerState); def validate_specs(state: KarlFischerState): results = []; results.append('Check titration range'); return {'validation_results': results}; def approve_procurement(state: KarlFischerState): return {'validation_results': state['validation_results'] + ['Approved']}; graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()