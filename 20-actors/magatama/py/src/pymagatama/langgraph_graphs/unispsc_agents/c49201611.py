from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; status: str; def validate_equipment(state: State): return {'status': 'validated' if 'brand' in state['input_data'] else 'rejected'}; def check_safety(state: State): return {'status': 'safety_cleared'}; graph = StateGraph(State); graph.add_node('validate', validate_equipment); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()