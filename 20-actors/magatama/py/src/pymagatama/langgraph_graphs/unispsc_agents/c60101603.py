from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FrameSpecState(TypedDict): specs: dict; valid: bool; def validate_specs(state: FrameSpecState): state['valid'] = all(k in state['specs'] for k in ['material', 'size']); return state; graph = StateGraph(FrameSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()