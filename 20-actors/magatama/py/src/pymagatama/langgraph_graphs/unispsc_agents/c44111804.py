from langgraph.graph import StateGraph, END; from typing import TypedDict; class DraftingState(TypedDict): paper_specs: dict; validation_status: str; def validate_specs(state: DraftingState): return {'validation_status': 'passed' if state['paper_specs'].get('gsm', 0) > 60 else 'failed'}; graph = StateGraph(DraftingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()