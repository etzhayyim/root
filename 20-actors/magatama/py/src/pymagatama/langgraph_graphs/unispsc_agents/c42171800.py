from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AirwayState(TypedDict): equipment_type: str; safety_checks: List[str]; approved: bool; def validate_equipment(state: AirwayState): return {'approved': all(check in state['safety_checks'] for check in ['ISO_13485', 'FDA_clearance'])}; graph = StateGraph(AirwayState); graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()