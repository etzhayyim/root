from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SludgeProcurementState(TypedDict): specs: dict; valid: bool; risk_level: str; def validate_equipment(state: SludgeProcurementState): print('Validating sludge equipment technical specs...'); state['valid'] = state['specs'].get('dewatering_rate', 0) > 0; return state; def assess_risk(state: SludgeProcurementState): state['risk_level'] = 'high' if state['specs'].get('chemical_content') else 'low'; return state; graph = StateGraph(SludgeProcurementState); graph.add_node('validate', validate_equipment); graph.add_node('risk_assessment', assess_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'risk_assessment'); graph.add_edge('risk_assessment', END); compiled_graph = graph.compile()