from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EmergencyCaseState(TypedDict): items: List[str]; inspection_passed: bool; client_compliance: bool; workflow = StateGraph(EmergencyCaseState); def validate_spec(state: EmergencyCaseState): return {'inspection_passed': True}; def check_compliance(state: EmergencyCaseState): return {'client_compliance': True}; workflow.add_node('spec_validation', validate_spec); workflow.add_node('compliance_check', check_compliance); workflow.set_entry_point('spec_validation'); workflow.add_edge('spec_validation', 'compliance_check'); workflow.add_edge('compliance_check', END); graph = workflow.compile()