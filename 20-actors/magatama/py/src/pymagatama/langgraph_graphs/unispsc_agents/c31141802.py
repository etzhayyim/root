from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): specs: dict; validation_report: str; graph = StateGraph(MoldState); def validate_rim_specs(state: MoldState): status = 'Valid' if state['specs'].get('hardness') else 'Missing Hardness'; return {'validation_report': status}; def finalize_procurement(state: MoldState): return {'validation_report': f'Finalized: {state['validation_report']}'}; graph.add_node('validate', validate_rim_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()