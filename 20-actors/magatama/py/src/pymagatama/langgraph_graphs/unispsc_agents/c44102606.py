from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RibbonState(TypedDict): model: str; specs: dict; valid: bool; def validate_compatibility(state: RibbonState): valid = state['specs'].get('model_compatibility') in state['model']; return {'valid': valid}; graph = StateGraph(RibbonState); graph.add_node('check', validate_compatibility); graph.set_entry_point('check'); graph.add_edge('check', END); graph.compile()