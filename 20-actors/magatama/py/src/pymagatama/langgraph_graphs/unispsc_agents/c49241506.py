from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlaygroundState(TypedDict): vendor_certs: List[str]; safety_check: bool; final_approval: bool; def validate_compliance(state: PlaygroundState): return {'safety_check': 'ISO-EN1176' in state['vendor_certs']}; def approve_procurement(state: PlaygroundState): return {'final_approval': state['safety_check'] == True}; graph = StateGraph(PlaygroundState); graph.add_node('validate', validate_compliance); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()