from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MixerState(TypedDict): model_specs: dict; compliance_passed: bool; def validate_specs(state: MixerState): state['compliance_passed'] = state['model_specs'].get('safety_interlock_compliance', False); return state; def check_power(state: MixerState): return 'compliant' if state['model_specs'].get('rated_power_w', 0) < 1500 else 'high_draw'; graph = StateGraph(MixerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()