from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UniformState(TypedDict): vendor_specs: dict; compliance_check: bool; output_data: str; def validate_specs(state: UniformState): compliance = 'fabric_composition' in state['vendor_specs']; return {'compliance_check': compliance}; def generate_procurement_data(state: UniformState): return {'output_data': 'Vendor approved for production.' if state['compliance_check'] else 'Specs Incomplete'}; graph = StateGraph(UniformState); graph.add_node('validate', validate_specs); graph.add_node('process', generate_procurement_data); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()