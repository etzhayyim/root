from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OphthalmicState(TypedDict): items: List[str]; compliant: bool; def validate_supplies(state: OphthalmicState): state['compliant'] = all('ISO' in item for item in state['items']); return state; graph = StateGraph(OphthalmicState); graph.add_node('validate', validate_supplies); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()