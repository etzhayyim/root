from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; def validate_media(state: ProcState): state['validated'] = all('base_component' in item for item in state['items']); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_media); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()