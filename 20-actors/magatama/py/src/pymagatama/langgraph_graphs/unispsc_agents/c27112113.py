from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_name: str; specs: dict; validation_passed: bool; def validate_tool_specs(state: ToolState): state['validation_passed'] = 'HRC' in state['specs'] and state['specs']['HRC'] >= 45; return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_tool_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()