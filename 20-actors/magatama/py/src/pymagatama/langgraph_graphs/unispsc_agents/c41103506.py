from langgraph.graph import StateGraph, END; from typing import TypedDict; class PCRState(TypedDict): spec: dict; validated: bool; state: str; def validate_spec(state: PCRState): state['validated'] = 'HEPA_filter_efficiency_rating' in state['spec']; return state; def route_procurement(state: PCRState): return 'complete' if state['validated'] else 'reject'; graph = StateGraph(PCRState); graph.add_node('validate', validate_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()