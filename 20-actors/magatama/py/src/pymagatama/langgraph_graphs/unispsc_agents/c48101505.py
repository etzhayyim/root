from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class CoffeeMakerState(TypedDict): messages: Annotated[list, add_messages]; capacity_l: float; certs: list[str]; def validate_specs(state: CoffeeMakerState) -> dict: return {'messages': ['Specs Validated'] if state['capacity_l'] > 5 else ['Low Capacity Alert']}; graph = StateGraph(CoffeeMakerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()