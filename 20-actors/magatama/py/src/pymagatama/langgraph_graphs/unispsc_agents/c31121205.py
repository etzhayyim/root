from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: CastingState): state['validation_results'].append('Size tolerance check pass'); return state; def check_metallurgy(state: CastingState): state['validation_results'].append('Material grade match confirmed'); return state; graph = StateGraph(CastingState); graph.add_node('dim', validate_dimensions); graph.add_node('chem', check_metallurgy); graph.set_entry_point('dim'); graph.add_edge('dim', 'chem'); graph.add_edge('chem', END); graph = graph.compile()