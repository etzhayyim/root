from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DiodeState(TypedDict): specs: dict; valid: bool; actions: List[str]; def validate_specs(state): state['valid'] = all(k in state['specs'] for k in ['PIV', 'AnodeVoltage']); return state; def export_compliance(state): state['actions'].append('check_dual_use_export_license'); return state; graph = StateGraph(DiodeState); graph.add_node('validate', validate_specs); graph.add_node('export_check', export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()