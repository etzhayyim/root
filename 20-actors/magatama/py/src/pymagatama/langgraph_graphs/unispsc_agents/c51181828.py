from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): n_code: str; quality_pass: bool; temp_logs: list; def validate_api(state: PharmaState): state['quality_pass'] = True; return state; def check_storage(state: PharmaState): return 'valid' if all(t < 25 for t in state['temp_logs']) else 'flag'; graph = StateGraph(PharmaState); graph.add_node('validate', validate_api); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph = graph.compile()