from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): device_specs: dict; validation_results: list[str]; graph = StateGraph(NetworkState); def validate_hardware(state: NetworkState): return {'validation_results': ['Hardware compliance check passed']}; def perform_security_audit(state: NetworkState): return {'validation_results': ['Security firmware check passed']}; graph.add_node('validate', validate_hardware); graph.add_node('audit', perform_security_audit); graph.set_entry_point('validate'); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); compiled_graph = graph.compile()