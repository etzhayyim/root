from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProductState(TypedDict): items: List[dict]; shelf_life_check: bool; def validate_product(state: ProductState): state['shelf_life_check'] = all(item.get('shelf_life', 0) > 365 for item in state['items']); return state; graph = StateGraph(ProductState); graph.add_node('validate', validate_product); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()