from langgraph.graph import StateGraph, END; from typing import TypedDict; class AssemblyState(TypedDict): doc_id: str; structural_check: bool; material_cert: bool; def check_structure(state: AssemblyState): state['structural_check'] = True; return state; def verify_material(state: AssemblyState): state['material_cert'] = True; return state; graph = StateGraph(AssemblyState); graph.add_node('structure', check_structure); graph.add_node('material', verify_material); graph.set_entry_point('structure'); graph.add_edge('structure', 'material'); graph.add_edge('material', END); graph = graph.compile()