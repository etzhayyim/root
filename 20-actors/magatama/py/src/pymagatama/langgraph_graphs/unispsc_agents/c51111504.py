from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): medicine_name: str; temp_status: bool; safe_handler: bool; def validate_cold_chain(state: ProcState): return {'temp_status': True}; def verify_handling(state: ProcState): return {'safe_handler': True}; graph = StateGraph(ProcState); graph.add_node('validate_cold_chain', validate_cold_chain); graph.add_node('verify_handling', verify_handling); graph.set_entry_point('validate_cold_chain'); graph.add_edge('validate_cold_chain', 'verify_handling'); graph.add_edge('verify_handling', END); graph = graph.compile()