from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PrintState(TypedDict): squeegee_specs: dict; validation_report: str; def validate_specs(state: PrintState): hardness = state['squeegee_specs'].get('hardness', 0); return {'validation_report': 'Valid' if 60 <= hardness <= 90 else 'Invalid'}; graph = StateGraph(PrintState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()