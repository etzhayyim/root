from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StructureState(TypedDict): structure_spec: dict; validation_results: List[str]; approved: bool; def check_compliance(state: StructureState): results = []; if 'HVAC' not in state['structure_spec']: results.append('Missing HVAC Spec'); return {'validation_results': results, 'approved': len(results) == 0}; graph = StateGraph(StructureState); graph.add_node('validate_spec', check_compliance); graph.set_entry_point('validate_spec'); graph.add_edge('validate_spec', END); graph = graph.compile()