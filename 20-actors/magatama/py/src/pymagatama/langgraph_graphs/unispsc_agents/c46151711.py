from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: str; validation_status: str; def validate_lifter_specs(state: State): status = 'pass' if 'adhesion' in state['input_data'] else 'fail'; return {'validation_status': status}; def finalize_procurement(state: State): return {'validation_status': 'finalized'}; graph = StateGraph(State); graph.add_node('validate', validate_lifter_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()