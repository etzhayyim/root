from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class BoltProcurementState(TypedDict): bolt_id: str; torque_val: float; status: str; logs: Annotated[list[str], operator.add]; graph = StateGraph(BoltProcurementState); def validate_torque(state: BoltProcurementState): return {'status': 'validated' if state['torque_val'] > 10.0 else 'rejected', 'logs': ['Torque validation performed']}; def update_inventory(state: BoltProcurementState): return {'logs': ['Inventory updated']}; graph.add_node('validate', validate_torque); graph.add_node('inventory', update_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); compiled_graph = graph.compile()