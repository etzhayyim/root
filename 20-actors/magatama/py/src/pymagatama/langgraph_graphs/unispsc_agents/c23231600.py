from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: list; status: str; def validate_mold_spec(state: State): specs = state['input_data']; results = ['Passed' if 'material' in specs else 'Failed']; return {'validation_results': results}; def update_status(state: State): return {'status': 'Complete' if all(r == 'Passed' for r in state['validation_results']) else 'Failed'}; graph = StateGraph(State); graph.add_node('validate', validate_mold_spec); graph.add_node('status', update_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'status'); graph.add_edge('status', END); graph.compile()