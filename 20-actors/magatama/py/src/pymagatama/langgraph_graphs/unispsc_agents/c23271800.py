from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeldingState(TypedDict): items: List[str]; approved: bool; def val_reqs(state: WeldingState): return {'approved': all(['SDS' in item for item in state['items']])}; graph = StateGraph(WeldingState); graph.add_node('validator', val_reqs); graph.set_entry_point('validator'); graph.add_edge('validator', END); compiled_graph = graph.compile()