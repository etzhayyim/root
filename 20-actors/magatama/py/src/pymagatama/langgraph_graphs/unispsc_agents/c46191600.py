from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FireEquipmentState(TypedDict): equipment_list: List[str]; validation_results: dict; compliance_passed: bool; def validate_equipment(state: FireEquipmentState): return {'validation_results': {item: 'verified' for item in state['equipment_list']}, 'compliance_passed': True}; def check_safety_standards(state: FireEquipmentState): return {'compliance_passed': True}; graph = StateGraph(FireEquipmentState); graph.add_node('validate', validate_equipment); graph.add_node('safety_check', check_safety_standards); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph.set_entry_point('validate'); graph = graph.compile()