from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpiroSpecState(TypedDict): vendor_certs: List[str]; validation_results: dict; ink_type: str; def validate_cert(state: SpiroSpecState): state['validation_results'] = {'approved': 'ISO13485' in state['vendor_certs']}; return state; def check_ink(state: SpiroSpecState): state['validation_results']['ink_check'] = state['ink_type'] == 'archival'; return state; graph = StateGraph(SpiroSpecState); graph.add_node('cert_check', validate_cert); graph.add_node('ink_check', check_ink); graph.add_edge('cert_check', 'ink_check'); graph.add_edge('ink_check', END); graph.set_entry_point('cert_check'); graph = graph.compile()