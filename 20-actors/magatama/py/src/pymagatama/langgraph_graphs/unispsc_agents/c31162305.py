from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validated: bool; spec_check: bool; def validate_specs(state: ProcurementState): state['spec_check'] = all(len(i) > 0 for i in state['items']); return state; def finalize_order(state: ProcurementState): state['validated'] = state['spec_check']; return state; graph = StateGraph(ProcurementState); graph.add_node('spec_validation', validate_specs); graph.add_node('finalizer', finalize_order); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', 'finalizer'); graph.add_edge('finalizer', END); app = graph.compile()