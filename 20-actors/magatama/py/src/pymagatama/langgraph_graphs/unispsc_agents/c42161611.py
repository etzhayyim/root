from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialysateState(TypedDict): specs: dict; compliance_status: bool; validation_log: List[str]; def validate_specs(state: DialysateState): state['compliance_status'] = 'temperature_range' in state['specs'] and state['specs']['temperature_range'] == '37C'; state['validation_log'].append('Validation completed'); return state; def check_medical_certification(state: DialysateState): state['compliance_status'] = state['compliance_status'] and state['specs'].get('iso_cert', False); return state; graph = StateGraph(DialysateState); graph.add_node('validate', validate_specs); graph.add_node('certify', check_medical_certification); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()