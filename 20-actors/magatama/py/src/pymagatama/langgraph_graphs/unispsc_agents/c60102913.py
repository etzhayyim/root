from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): data: dict; verified: bool; def validate_guide_data(state: State): state['verified'] = 'currency_scope' in state['data']; return state; def finalize_workflow(state: State): return 'process_complete'; graph = StateGraph(State); graph.add_node('validate', validate_guide_data); graph.add_node('finalize', finalize_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()