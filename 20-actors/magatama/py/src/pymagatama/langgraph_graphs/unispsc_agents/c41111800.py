from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NDEState(TypedDict): device_type: str; compliance_docs: List[str]; inspection_passed: bool; def validate_compliance(state: NDEState): state['compliance_docs'] = ['ISO_9712', 'Radiation_Safety']; state['inspection_passed'] = True; return state; def route_inspection(state: NDEState): return 'END'; graph = StateGraph(NDEState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()