from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SteelState(TypedDict): input_spec: dict; validation_report: list[str]; status: str; def validate_grade(state: SteelState): state['validation_report'].append('Validating material grade compatibility'); return state; def check_dimensions(state: SteelState): state['validation_report'].append('Verifying dimensional tolerance'); return state; graph = StateGraph(SteelState); graph.add_node('validate', validate_grade); graph.add_node('check_dims', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_dims'); graph.add_edge('check_dims', END); graph = graph.compile()