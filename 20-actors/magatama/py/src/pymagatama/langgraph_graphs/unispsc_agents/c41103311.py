from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_specs(state: State): specs = state['input_data']; valid = 'range' in specs and 'accuracy' in specs; return {'validation_results': ['Specs valid'] if valid else ['Invalid'], 'status': 'COMPLETED' if valid else 'FAILED'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()