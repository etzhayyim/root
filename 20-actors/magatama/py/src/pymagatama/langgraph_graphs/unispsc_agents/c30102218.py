from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class MetalState(TypedDict): metal_type: str; purity: float; status: str; def validate_purity(state: MetalState): val = 'valid' if state['purity'] >= 0.999 else 'rejected'; return {'status': val}; graph = StateGraph(MetalState); graph.add_node('check', validate_purity); graph.set_entry_point('check'); graph.add_edge('check', END); graph = graph.compile()