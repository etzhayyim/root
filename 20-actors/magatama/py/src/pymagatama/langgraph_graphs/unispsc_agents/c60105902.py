from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): content_draft: str; compliance_checks: List[str]; approved: bool; def validate_content(state: ProcurementState) -> dict: return {'compliance_checks': ['medical_accuracy', 'regulatory_compliance'], 'approved': True}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_content); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()