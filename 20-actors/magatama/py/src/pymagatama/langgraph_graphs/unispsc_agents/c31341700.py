from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: List[str]; validation_results: dict; def validate_assemblies(state: AssemblyState): results = {spec: 'Pending' for spec in state['specs']}; return {'validation_results': results}; def verify_torque(state: AssemblyState): return {'validation_results': {**state['validation_results'], 'Fastener Torque': 'Verified'}}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_assemblies); graph.add_node('torque_check', verify_torque); graph.set_entry_point('validate'); graph.add_edge('validate', 'torque_check'); graph.add_edge('torque_check', END); compiled_graph = graph.compile()