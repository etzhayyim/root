from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BreathingCircuitState(TypedDict): circuit_id: str; compliance_docs: List[str]; approved: bool; def validate_compliance(state: BreathingCircuitState): state['approved'] = 'ISO_13485' in state['compliance_docs']; return state; def check_sterility(state: BreathingCircuitState): return {'approved': state['approved'] and 'Sterility_Cert' in state['compliance_docs']}; builder = StateGraph(BreathingCircuitState); builder.add_node('validate', validate_compliance); builder.add_node('check_sterility', check_sterility); builder.add_edge('validate', 'check_sterility'); builder.add_edge('check_sterility', END); builder.set_entry_point('validate'); graph = builder.compile()