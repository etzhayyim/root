from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UniformState(TypedDict): items: List[dict]; validation_results: List[str]; def validate_specs(state: UniformState): results = []; for item in state['items']: if 'material' not in item: results.append('Missing material spec'); state['validation_results'] = results; return state; graph = StateGraph(UniformState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()