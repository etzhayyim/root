from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BathState(TypedDict): temperature_c: float; compliance_met: bool; def validate_temp(state: BathState): return {'compliance_met': 37.0 <= state['temperature_c'] <= 50.0}; def check_safety(state: BathState): return {'compliance_met': state['compliance_met'] and True}; graph = StateGraph(BathState); graph.add_node('validate', validate_temp); graph.add_node('safety', check_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()