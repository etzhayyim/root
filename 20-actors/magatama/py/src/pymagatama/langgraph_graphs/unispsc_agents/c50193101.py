from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SnackState(TypedDict): ingredients: List[str]; safety_verified: bool; shelf_life_days: int; graph = StateGraph(SnackState); def validate_ingredients(state: SnackState): return {'safety_verified': all(len(i) > 0 for i in state['ingredients'])}; def check_shelf_life(state: SnackState): return {'safety_verified': state['shelf_life_days'] > 30}; graph.add_node('validate', validate_ingredients); graph.add_node('verify_life', check_shelf_life); graph.set_entry_point('validate'); graph.add_edge('validate', 'verify_life'); graph.add_edge('verify_life', END); graph = graph.compile()