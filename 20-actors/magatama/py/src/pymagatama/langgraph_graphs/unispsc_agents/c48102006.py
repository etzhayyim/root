from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): magnet_specs: dict; validation_errors: List[str]; def validate_gauss(state: GraphState): val = state['magnet_specs'].get('gauss', 0); state['validation_errors'] = [] if val > 10000 else ['Low Gauss rating']; return state; def check_housing(state: GraphState): if state['magnet_specs'].get('housing') != 'SUS316': state['validation_errors'].append('Non-compliant housing material'); return state; graph = StateGraph(GraphState); graph.add_node('validate_gauss', validate_gauss); graph.add_node('check_housing', check_housing); graph.set_entry_point('validate_gauss'); graph.add_edge('validate_gauss', 'check_housing'); graph.add_edge('check_housing', END); compiled_graph = graph.compile()