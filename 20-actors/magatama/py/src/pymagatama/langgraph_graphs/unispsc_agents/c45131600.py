from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MediaState(TypedDict): media_id: str; asset_type: str; compliance_check: bool; resolution: str; def check_compliance(state: MediaState): return {'compliance_check': True} if state['resolution'] == '4K' else {'compliance_check': False}; def validate_asset(state: MediaState): print(f'Validating {state[media_id]}'); return {'compliance_check': True}; graph = StateGraph(MediaState); graph.add_node('compliance', check_compliance); graph.add_node('validation', validate_asset); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'validation'); graph.add_edge('validation', END); app = graph.compile()