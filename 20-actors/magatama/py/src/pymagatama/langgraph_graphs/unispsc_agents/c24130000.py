from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RefrigerationSpec(TypedDict): specs: dict; validation_status: str; def validate_specs(state: RefrigerationSpec): if 'refrigerant_type' in state['specs']: state['validation_status'] = 'COMPLIANT'; else: state['validation_status'] = 'INCOMPLETE'; return state; graph = StateGraph(RefrigerationSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()