from langgraph.graph import StateGraph, END; from typing import TypedDict; class TransformerState(TypedDict): specs: dict; validation_results: list; graph = StateGraph(TransformerState); def validate_electrical_specs(state): return {'validation_results': ['Voltage_Verified', 'Insulation_Tested']}; def check_regulatory_compliance(state): return {'validation_results': state['validation_results'] + ['IEEE_Compliant']}; graph.add_node('spec_validation', validate_electrical_specs); graph.add_node('regulatory_check', check_regulatory_compliance); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', 'regulatory_check'); graph.add_edge('regulatory_check', END); graph = graph.compile()