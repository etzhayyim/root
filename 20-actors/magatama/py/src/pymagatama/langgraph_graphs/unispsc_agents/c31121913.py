from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): input_specs: dict; validation_results: list; status: str; def validate_dimensions(state: MoldState): return {'validation_results': ['dim_ok'], 'status': 'validated'} if 'dim' in state['input_specs'] else {'status': 'failed'}; def check_material(state: MoldState): return {'validation_results': state['validation_results'] + ['mat_ok']}; graph = StateGraph(MoldState); graph.add_node('dimension_check', validate_dimensions); graph.add_node('material_check', check_material); graph.set_entry_point('dimension_check'); graph.add_edge('dimension_check', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()