from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): content: str; validated: bool; errors: Annotated[list, operator.add]; def validate_casing(state: SpecState): state['validated'] = len(state['content']) > 20; return state; graph = StateGraph(SpecState); graph.add_node('validate', validate_casing); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()