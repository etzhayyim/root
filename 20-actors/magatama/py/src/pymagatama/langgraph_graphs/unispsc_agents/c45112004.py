from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; verified: bool; log: Annotated[list, operator.add]; def validate_component(state: ComponentState): verified = state['component_id'].startswith('PRT-') and len(state['component_id']) > 5; return {'verified': verified, 'log': [f'Validation for {state['component_id']}: {verified}']}; builder = StateGraph(ComponentState); builder.add_node('validate', validate_component); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()