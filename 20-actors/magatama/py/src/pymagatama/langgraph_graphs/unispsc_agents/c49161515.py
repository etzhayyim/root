from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HandballState(TypedDict): balls: List[str]; validated: bool; results: List[str]; def validate_specs(state: HandballState): print('Validating ball specs...'); return {'validated': True}; def process_logistics(state: HandballState): return {'results': ['Passed Inspection']}; graph = StateGraph(HandballState); graph.add_node('validate', validate_specs); graph.add_node('logistics', process_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph = graph.compile()