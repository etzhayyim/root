from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec: dict; validated: bool; def validate_spec(state: State): state['validated'] = 'RegulatoryApproval' in state['spec']; return state; def route_flow(state: State): return 'validated' if state['validated'] else END; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()