from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ExtrusionState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(ExtrusionState); def validate_dimensions(state): return {'validation_results': ['Dimension check passed']}; def check_material(state): return {'validation_results': ['Material cert verified']}; graph.add_node('dim_check', validate_dimensions); graph.add_node('mat_check', check_material); graph.add_edge('dim_check', 'mat_check'); graph.add_edge('mat_check', END); graph.set_entry_point('dim_check'); graph = graph.compile()