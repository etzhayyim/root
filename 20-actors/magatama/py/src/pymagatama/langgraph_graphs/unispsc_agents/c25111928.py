from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SailCoverState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SailCoverState): errors = []; if 'UV' not in state['specs']: errors.append('Missing UV resistance'); return {'validation_errors': errors, 'approved': len(errors) == 0}; builder = StateGraph(SailCoverState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()