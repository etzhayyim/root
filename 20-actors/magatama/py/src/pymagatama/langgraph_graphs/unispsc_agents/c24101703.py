from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RockBinState(TypedDict): capacity: float; material: str; status: str; def validate_integrity(state: RockBinState): return {'status': 'validated' if state['capacity'] > 0 else 'rejected'}; def check_material(state: RockBinState): return {'status': 'material_approved' if 'Hardox' in state['material'] else 'needs_review'}; graph = StateGraph(RockBinState); graph.add_node('integrity', validate_integrity); graph.add_node('material', check_material); graph.set_entry_point('integrity'); graph.add_edge('integrity', 'material'); graph.add_edge('material', END); graph = graph.compile()