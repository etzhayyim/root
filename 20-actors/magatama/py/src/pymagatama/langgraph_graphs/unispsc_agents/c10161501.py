from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class FertilizerState(TypedDict): npk_ratio: str; formulation: str; application_rate: float; is_approved: bool; def check_npk_standards(state: FertilizerState) -> FertilizerState: state['is_approved'] = len(state['npk_ratio'].split('-')) == 3; return state; def finalize_order(state: FertilizerState) -> FertilizerState: return state; graph = StateGraph(FertilizerState); graph.add_node('check', check_npk_standards); graph.add_node('finalize', finalize_order); graph.set_entry_point('check'); graph.add_edge('check', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()