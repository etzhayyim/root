from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SprayerState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: SprayerState) -> SprayerState: state['compliance'] = state['specs'].get('pressure', 0) > 0 and 'chemical_rating' in state['specs']; state['approved'] = state['compliance']; return state; graph = StateGraph(SprayerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()