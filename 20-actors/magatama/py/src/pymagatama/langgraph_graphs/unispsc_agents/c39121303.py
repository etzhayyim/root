from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoxState(TypedDict): specs: dict; valid: bool; def validate_specs(state: BoxState): state['valid'] = 'Certification_Standard' in state['specs'] and state['specs']['IP_Rating'] >= 54; return state; graph = StateGraph(BoxState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()