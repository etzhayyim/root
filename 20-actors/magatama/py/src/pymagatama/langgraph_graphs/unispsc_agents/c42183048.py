from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DeviceState(TypedDict): device_id: str; compliance_docs: List[str]; validated: bool; def validate_cert(state: DeviceState): state['validated'] = 'ISO13485' in state['compliance_docs']; return state; builder = StateGraph(DeviceState); builder.add_node('validate', validate_cert); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()