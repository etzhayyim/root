from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BindingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: BindingState): state['valid'] = 'material' in state['specs'] and 'post_length' in state['specs']; return state; builder = StateGraph(BindingState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()