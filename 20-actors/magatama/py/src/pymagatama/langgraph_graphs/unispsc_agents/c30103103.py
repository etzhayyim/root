from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; def validate_materials(state: State): results = ['check_tensile_strength', 'check_dimensions']; return {'validation_results': results}; def verify_durability(state: State): return {'validation_results': ['fatigue_test_passed']}; graph = StateGraph(State); graph.add_node('validate', validate_materials); graph.add_node('durability', verify_durability); graph.set_entry_point('validate'); graph.add_edge('validate', 'durability'); graph.add_edge('durability', END); graph = graph.compile()