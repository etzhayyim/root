from langgraph.graph import StateGraph, END; from typing import TypedDict; class TapeState(TypedDict): specs: dict; valid: bool; def validate_tape(state: TapeState): state['valid'] = 'adhesion_strength' in state['specs'] and 'material_composition' in state['specs']; return {'valid': state['valid']}; graph = StateGraph(TapeState); graph.add_node('validate', validate_tape); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()