from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AnodizingState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: AnodizingState): errors = []; if not state['specs'].get('tank_capacity'): errors.append('Missing capacity'); return {'validation_errors': errors}; def check_safety(state: AnodizingState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(AnodizingState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()