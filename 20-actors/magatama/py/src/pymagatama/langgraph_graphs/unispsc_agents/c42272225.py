from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BiPAPState(TypedDict): items: List[str]; validated_specs: dict; status: str; def validate_accessories(state: BiPAPState): state['status'] = 'Validating compliance'; return {'status': 'Validated'}; def check_compatibility(state: BiPAPState): return {'status': 'Compatibility confirmed'}; graph = StateGraph(BiPAPState); graph.add_node('validate', validate_accessories); graph.add_node('check', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); compiled_graph = graph.compile()