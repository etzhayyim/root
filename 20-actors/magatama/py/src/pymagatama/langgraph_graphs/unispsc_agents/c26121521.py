from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BronzeWireState(TypedDict): wire_gauge: float; tensile_strength: float; compliance_certs: List[str]; approved: bool; def validate_specs(state: BronzeWireState): state['approved'] = state['wire_gauge'] > 0 and state['tensile_strength'] > 200; return state; graph = StateGraph(BronzeWireState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()