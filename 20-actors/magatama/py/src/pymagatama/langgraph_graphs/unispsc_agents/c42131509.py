from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HospitalGownState(TypedDict): items: List[dict]; inspection_passed: bool; def validate_specs(state: HospitalGownState): state['inspection_passed'] = all('material' in item and 'sterilization' in item for item in state['items']); return state; def log_results(state: HospitalGownState): print(f'Processing complete. Inspection status: {state['inspection_passed']}'); return state; graph = StateGraph(HospitalGownState); graph.add_node('validation', validate_specs); graph.add_node('logging', log_results); graph.set_entry_point('validation'); graph.add_edge('validation', 'logging'); graph.add_edge('logging', END); graph = graph.compile()