from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class EndoscopeState(TypedDict): device_specs: dict; compliance_status: bool; workflow_steps: Annotated[list, operator.add]; def validate_medical_device(state: EndoscopeState): state['compliance_status'] = all(k in state['device_specs'] for k in ['ISO_13485', 'Regulatory_ID']); return {'workflow_steps': ['Validation Completed']}; graph = StateGraph(EndoscopeState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()