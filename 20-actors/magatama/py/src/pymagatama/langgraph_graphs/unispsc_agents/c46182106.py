from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; valid: bool; audit_logs: List[str]; def validate_esd_specs(state: State): state['valid'] = state['specs'].get('resistivity', 0) < 1e9; return state; graph = StateGraph(State); graph.add_node('validation', validate_esd_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()