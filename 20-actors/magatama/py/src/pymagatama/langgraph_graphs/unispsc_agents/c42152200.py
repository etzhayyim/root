from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): equipment_type: str; specs: dict; compliant: bool; def validate_specs(state: DentalState): state['compliant'] = 'sterilization_standard' in state['specs']; return state; def check_compliance(state: DentalState): return 'compliant' if state['compliant'] else 'manual_review'; graph = StateGraph(DentalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()