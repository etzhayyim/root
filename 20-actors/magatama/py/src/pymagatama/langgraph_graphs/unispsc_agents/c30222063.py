from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): vendor_certs: List[str]; safety_score: float; approved: bool; def validate_vendor(state: State): return {'approved': len(state['vendor_certs']) > 2 and state['safety_score'] > 0.8}; graph = StateGraph(State); graph.add_node('validate', validate_vendor); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()