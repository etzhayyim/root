from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BeachTowelState(TypedDict): specs: dict; compliance_status: bool; inspection_result: str; def validate_specs(state: BeachTowelState): if state['specs'].get('gsm_weight', 0) < 300: state['compliance_status'] = False; else: state['compliance_status'] = True; return state; graph = StateGraph(BeachTowelState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()