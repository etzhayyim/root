from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplyState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_device(state: SupplyState): state['validated'] = all(c in state['certs'] for c in ['ISO-13485', 'CE-Mark']); return state; graph = StateGraph(SupplyState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()