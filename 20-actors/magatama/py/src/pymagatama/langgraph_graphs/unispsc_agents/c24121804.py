from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FoodCanState(TypedDict): specs: dict; compliance_passed: bool; defects: List[str]; def validate_quality(state: FoodCanState): state['defects'] = [d for d, val in state['specs'].items() if not val]; state['compliance_passed'] = len(state['defects']) == 0; return state; graph = StateGraph(FoodCanState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()