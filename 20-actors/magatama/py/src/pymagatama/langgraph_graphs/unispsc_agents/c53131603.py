from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RazorState(TypedDict): items: List[str]; validation_results: List[str]; def validate_razor_specs(state: RazorState): state['validation_results'].append('Safety test passed'); return state; graph = StateGraph(RazorState); graph.add_node('validate', validate_razor_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()