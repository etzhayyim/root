from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StairState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: StairState): state['compliant'] = all(k in state['specs'] for k in ['material', 'load_rating']); return state; graph = StateGraph(StairState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()