from langgraph.graph import StateGraph, END; from typing import TypedDict; class CTInjectorState(TypedDict): vendor_certs: dict; device_specs: dict; compliance_check: bool; def validate_specs(state: CTInjectorState): return {'compliance_check': state['device_specs'].get('psi_limit', 0) < 350}; def route_procurement(state: CTInjectorState): return 'APPROVE' if state['compliance_check'] else 'REJECT'; graph = StateGraph(CTInjectorState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate')