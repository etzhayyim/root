from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoolerState(TypedDict): vendor: str; capacity: float; is_certified: bool; approved: bool; def validate_spec(state: CoolerState): state['approved'] = state['capacity'] > 0 and state['is_certified']; return state; graph = StateGraph(CoolerState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()