from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StairState(TypedDict): specs: dict; valid: bool; def validate_specs(state: StairState): state['valid'] = 'Load Capacity' in state['specs'] and 'Material' in state['specs']; return state; graph = StateGraph(StairState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()