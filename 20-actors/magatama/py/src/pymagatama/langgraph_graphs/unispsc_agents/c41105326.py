from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ReagentState(TypedDict): reagent_id: str; quality_passed: bool; temp_logs: List[float]; def validate_reagent(state: ReagentState): return {'quality_passed': all(t < -20.0 for t in state['temp_logs'])}; def finalize_procurement(state: ReagentState): return {'status': 'approved' if state['quality_passed'] else 'rejected'}; graph = StateGraph(ReagentState); graph.add_node('validate', validate_reagent); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()