from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipetteState(TypedDict): model: str; volume: float; compliant: bool; def validate_specs(state: PipetteState): state['compliant'] = state['volume'] > 0; return state; graph = StateGraph(PipetteState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()