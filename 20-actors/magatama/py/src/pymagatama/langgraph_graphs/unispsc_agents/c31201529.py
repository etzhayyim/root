from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TapeState(TypedDict): specs: dict; validation_log: List[str]; approved: bool; def validate_tape_specs(state: TapeState): specs = state['specs']; logs = []; if specs.get('thickness', 0) < 0.1: logs.append('Insufficient thickness for industrial use'); state['validation_log'] = logs; state['approved'] = len(logs) == 0; return state; graph = StateGraph(TapeState); graph.add_node('validate', validate_tape_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()