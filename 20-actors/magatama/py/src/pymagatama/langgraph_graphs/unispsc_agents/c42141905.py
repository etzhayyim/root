from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EnemaState(TypedDict): soap_batch: str; compliance_check: bool; reports: List[str]; def validate_purity(state: EnemaState) -> EnemaState: state['compliance_check'] = True; state['reports'].append('Purity validated'); return state; def verify_regulatory(state: EnemaState) -> EnemaState: state['compliance_check'] = True; return state; graph = StateGraph(EnemaState); graph.add_node('purity', validate_purity); graph.add_node('regulatory', verify_regulatory); graph.set_entry_point('purity'); graph.add_edge('purity', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()