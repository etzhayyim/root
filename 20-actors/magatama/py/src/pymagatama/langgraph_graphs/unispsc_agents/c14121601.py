from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): messages: Annotated[Sequence[str], operator.add]; status: str; def validate_chemical(state: AgentState): return {'status': 'validated' if 'verified' in state['messages'][-1] else 'pending'}; def safety_check(state: AgentState): return {'status': 'safe'}; graph = StateGraph(AgentState); graph.add_node('validate', validate_chemical); graph.add_node('safety', safety_check); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()