from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): device_specs: dict; compliance_check: bool; graph = StateGraph(NetworkState); def validate_specs(state: NetworkState): state['compliance_check'] = 'IEEE-802.11' in state['device_specs'].get('standard', ''); return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()