from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): ingredient: str; quality_score: float; storage_compliant: bool; def validate_quality(state: PharmaState): return {'quality_score': 0.98}; def check_storage(state: PharmaState): return {'storage_compliant': True}; graph = StateGraph(PharmaState); graph.add_node('qc', validate_quality); graph.add_node('storage', check_storage); graph.set_entry_point('qc'); graph.add_edge('qc', 'storage'); graph.add_edge('storage', END); compiled_graph = graph.compile()