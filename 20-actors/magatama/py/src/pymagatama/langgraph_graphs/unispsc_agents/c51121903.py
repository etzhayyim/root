from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): medicine: str; status: str; safety_check: bool; def validate_digitoxin(state: ProcureState): return {'safety_check': True if 'Digitoxin' in state['medicine'] else False}; def final_approval(state: ProcureState): return {'status': 'Approved' if state['safety_check'] else 'Rejected'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_digitoxin); graph.add_node('approve', final_approval); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()