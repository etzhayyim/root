from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RugState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: RugState): state['validation_passed'] = 'Fire retardancy rating' in state['specs']; return state; def finalize_procurement(state: RugState): return {'result': 'Procurement ready' if state['validation_passed'] else 'Rejected'}; graph = StateGraph(RugState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()