from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): press_specs: dict; status: str; def validate_pressure(state: State): specs = state['press_specs']; return {'status': 'validated' if specs.get('pressure', 0) > 0 else 'error'}; def check_compliance(state: State): return {'status': 'compliant'}; graph = StateGraph(State); graph.add_node('validation', validate_pressure); graph.add_node('compliance', check_compliance); graph.set_entry_point('validation'); graph.add_edge('validation', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()