from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_name: str; validation_passed: bool; specs: List[str]; def validate_chemical(state: ProcState): state['validation_passed'] = 'CAS_number' in state['specs']; return state; def check_hazmat(state: ProcState): print('Checking SDS requirements...'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_chemical); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()