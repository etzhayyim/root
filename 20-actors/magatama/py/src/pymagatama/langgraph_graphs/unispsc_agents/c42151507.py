from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShadeState(TypedDict): shade_data: dict; validation_report: List[str]; def validate_shade_standard(state: ShadeState): state['validation_report'].append('Standard check passed.'); return state; def verify_regulatory(state: ShadeState): state['validation_report'].append('Regulatory compliance verified.'); return state; graph = StateGraph(ShadeState); graph.add_node('validate', validate_shade_standard); graph.add_node('regulatory', verify_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); app = graph.compile()