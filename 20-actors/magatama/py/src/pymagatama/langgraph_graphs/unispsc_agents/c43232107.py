from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WebDevState(TypedDict): content: str; dependencies: List[str]; compliant: bool; def validate_code(state: WebDevState): return {'compliant': '<html>' in state['content']}; def compile_build(state: WebDevState): return {'dependencies': ['jquery', 'bootstrap']}; graph = StateGraph(WebDevState); graph.add_node('validate', validate_code); graph.add_node('build', compile_build); graph.add_edge('validate', 'build'); graph.add_edge('build', END); graph.set_entry_point('validate'); app = graph.compile()