from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): component_id: str; specs: dict; valid: bool; def validate_specs(state: ProcState): state['valid'] = 'Material Grade' in state['specs'] and 'Dimensional Tolerance' in state['specs']; return state; def check_export_control(state: ProcState): print('Checking dual-use status...'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()