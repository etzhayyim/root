from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): alloy_specs: dict; compliance_checked: bool; def validate_beryllium(state: State): state['compliance_checked'] = 'Be_content' in state['alloy_specs'] and state['alloy_specs']['Be_content'] > 0; return state; def export_screening(state: State): state['compliance_checked'] = state['compliance_checked'] and True; return state; graph = StateGraph(State); graph.add_node('validate', validate_beryllium); graph.add_node('export', export_screening); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph.set_entry_point('validate'); graph = graph.compile()