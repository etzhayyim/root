from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class BoringState(TypedDict): input_specs: dict; validation_log: Annotated[Sequence[str], operator.add]; status: str; def validate_specs(state: BoringState): specs = state['input_specs']; log = []; status = 'pending'; if specs.get('torque_nm', 0) > 5000: log.append('High torque detected, secondary safety check required.'); status = 'approved'; return {'validation_log': log, 'status': status}; def compile_graph(): graph = StateGraph(BoringState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()