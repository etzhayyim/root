from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SlideState(TypedDict): specs: dict; compliant: bool; graph = StateGraph(SlideState); def validate_safety(state: SlideState): state['compliant'] = 'ASTM_cert' in state['specs'] and state['specs']['safety_clearance'] > 5; return state; graph.add_node('safety_check', validate_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); compiled_graph = graph.compile()