from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): alloy_code: str; spec_check: bool; export_control: bool; def val_alloy(state: CastingState): state['spec_check'] = state['alloy_code'].startswith('AZ') or state['alloy_code'].startswith('AM'); return state; def val_export(state: CastingState): state['export_control'] = True; return state; graph = StateGraph(CastingState); graph.add_node('validate_alloy', val_alloy); graph.add_node('check_export', val_export); graph.set_entry_point('validate_alloy'); graph.add_edge('validate_alloy', 'check_export'); graph.add_edge('check_export', END); graph = graph.compile()