from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TransomState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: TransomState): return {'validation_report': 'Passed' if state['specs'].get('fire_rating') else 'Failed', 'approved': True if state['specs'].get('fire_rating') else False}; graph = StateGraph(TransomState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()