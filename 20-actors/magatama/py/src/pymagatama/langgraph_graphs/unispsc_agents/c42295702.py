from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): wire_type: str; specs: dict; approved: bool; def validate_specs(state: ProcureState): state['approved'] = 'ASTM' in state['specs'].get('standard', '') or state['specs'].get('sterile', False); return state; def finalize_order(state: ProcureState): return {'approved': state['approved']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()