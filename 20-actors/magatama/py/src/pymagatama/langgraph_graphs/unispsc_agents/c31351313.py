from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): specs: dict; validated: bool; state = ProcState(specs={}, validated=False); def validate_specs(state: ProcState): state['validated'] = all(k in state['specs'] for k in ['material', 'dimensions']); return state; graph = StateGraph(ProcState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()