from langgraph.graph import StateGraph, END; from typing import TypedDict; class HydrastisState(TypedDict): assay_results: dict; compliance_status: bool; workflow_step: str; def validate_purity(state: HydrastisState) -> HydrastisState: state['compliance_status'] = state['assay_results'].get('berberine_pct', 0) > 3.0; return state; graph = StateGraph(HydrastisState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()