from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RehaState(TypedDict): items: List[dict]; valid: bool; def validate_weight(state: RehaState): state['valid'] = all('kg' in item for item in state['items']); return state; graph = StateGraph(RehaState); graph.add_node('validate', validate_weight); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()