from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LemonState(TypedDict): harvest_date: str; temp_logs: List[float]; quality_passed: bool; def check_freshness(state: LemonState): state['quality_passed'] = all(t < 5.0 for t in state['temp_logs']); return state; def validate_origin(state: LemonState): return {'quality_passed': state['quality_passed'] and True}; graph = StateGraph(LemonState); graph.add_node('check_temp', check_freshness); graph.add_node('validate_docs', validate_origin); graph.set_entry_point('check_temp'); graph.add_edge('check_temp', 'validate_docs'); graph.add_edge('validate_docs', END); graph = graph.compile()