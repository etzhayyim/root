from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CMOSState(TypedDict): sensor_specs: dict; validation_errors: List[str]; def validate_sensor(state: CMOSState): specs = state['sensor_specs']; errors = []; if 'resolution' not in specs: errors.append('Resolution missing'); return {'validation_errors': errors}; def check_export_control(state: CMOSState): print('Checking dual-use compliance...'); return {}; graph = StateGraph(CMOSState); graph.add_node('validate', validate_sensor); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()