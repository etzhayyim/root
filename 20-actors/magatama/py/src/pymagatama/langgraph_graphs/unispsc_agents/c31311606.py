from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_weld_integrity(state: PipeState): state['valid'] = state['specs'].get('weld_integrity') == 'passed'; return state; def check_pressure_rating(state: PipeState): state['valid'] = state['valid'] and state['specs'].get('pressure', 0) > 10; return state; graph = StateGraph(PipeState); graph.add_node('validate', validate_weld_integrity); graph.add_node('pressure_check', check_pressure_rating); graph.set_entry_point('validate'); graph.add_edge('validate', 'pressure_check'); graph.add_edge('pressure_check', END); compiled_graph = graph.compile()