from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): vendor_docs: dict; compliance_status: bool; quality_check: bool; def validate_metadata(state: AssemblyState): print('Validating material properties...'); state['compliance_status'] = True; return state; def run_ndt_simulation(state: AssemblyState): print('Running virtual structural analysis...'); state['quality_check'] = True; return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_metadata); graph.add_node('ndt_sim', run_ndt_simulation); graph.set_entry_point('validate'); graph.add_edge('validate', 'ndt_sim'); graph.add_edge('ndt_sim', END); graph = graph.compile()