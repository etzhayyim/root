from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine_name: str; compliance_docs: List[str]; approved: bool; def check_compliance(state: PharmState): state['approved'] = 'GDP_Cert' in state['compliance_docs']; return state; builder = StateGraph(PharmState); builder.add_node('verify', check_compliance); builder.set_entry_point('verify'); builder.add_edge('verify', END); graph = builder.compile()