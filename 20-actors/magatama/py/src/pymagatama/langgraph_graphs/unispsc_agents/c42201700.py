from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ImagingState(TypedDict): device_id: str; specs: dict; validation_report: List[str]; def validate_dicom(state: ImagingState): state['validation_report'].append('Checking DICOM compliance...'); return state; def check_calibration(state: ImagingState): state['validation_report'].append('Verifying calibration certification...'); return state; graph = StateGraph(ImagingState); graph.add_node('validate_dicom', validate_dicom); graph.add_node('check_calibration', check_calibration); graph.set_entry_point('validate_dicom'); graph.add_edge('validate_dicom', 'check_calibration'); graph.add_edge('check_calibration', END); graph = graph.compile()