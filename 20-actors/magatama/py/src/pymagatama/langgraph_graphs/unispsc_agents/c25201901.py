from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftSystemState(TypedDict): vendor_certs: List[str]; safety_specs: dict; approved: bool; def validate_certs(state: AircraftSystemState): state['approved'] = 'AS9100' in state['vendor_certs']; return state; builder = StateGraph(AircraftSystemState); builder.add_node('validation', validate_certs); builder.set_entry_point('validation'); builder.add_edge('validation', END); graph = builder.compile()