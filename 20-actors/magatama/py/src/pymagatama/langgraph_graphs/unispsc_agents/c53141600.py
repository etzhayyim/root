from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SewingState(TypedDict): items: List[str]; validated_items: List[str]; status: str; def validate_supplies(state: SewingState): validated = [item for item in state['items'] if 'compliant' in item]; return {'validated_items': validated, 'status': 'validated'}; def compile_graph(): graph = StateGraph(SewingState); graph.add_node('validate', validate_supplies); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()