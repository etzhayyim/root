from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; verification_results: Annotated[list, operator.add]; graph = StateGraph(State); def validate_spec(state: State): return {'verification_results': ['Material check passed', 'Certification verified']}; def finalize_procurement(state: State): return {'verification_results': ['Procurement order prepared']}; graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()