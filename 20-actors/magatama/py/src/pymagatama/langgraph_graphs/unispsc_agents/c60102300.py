from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BookProcurementState(TypedDict): items: List[dict]; approval_required: bool; def validate_metadata(state: BookProcurementState) -> BookProcurementState: print('Validating ISBNs and license types...'); return state; def check_compliance(state: BookProcurementState) -> BookProcurementState: state['approval_required'] = False; return state; graph = StateGraph(BookProcurementState); graph.add_node('validate', validate_metadata); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()