from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class MopProcurementState(TypedDict): specs: dict; validation_report: list; workflow_step: str; def validate_materials(state: MopProcurementState): print('Validating material absorbency...'); return {'validation_report': state['validation_report'] + ['Material checked']}; def approve_procurement(state: MopProcurementState): print('Approval logic...'); return {'workflow_step': 'READY_FOR_ORDER'}; graph = StateGraph(MopProcurementState); graph.add_node('validate', validate_materials); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.compile()