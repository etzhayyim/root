from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SoftwareState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_license(state): return {'validation_results': ['License format verified'], 'status': 'VALIDATED'}; def check_compliance(state): return {'validation_results': ['Compliance check passed'], 'status': 'COMPLIANT'}; graph = StateGraph(SoftwareState); graph.add_node('license_check', validate_license); graph.add_node('compliance_review', check_compliance); graph.set_entry_point('license_check'); graph.add_edge('license_check', 'compliance_review'); graph.add_edge('compliance_review', END); graph = graph.compile()