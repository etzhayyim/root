from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): mercury_content: float; is_compliant: bool; component_id: str; def validate_mercury(state: State): state['is_compliant'] = state['mercury_content'] < 5.0; return state; def check_hazmat_protocol(state: State): state['is_compliant'] = state['is_compliant'] and True; return state; graph = StateGraph(State); graph.add_node('validate', validate_mercury); graph.add_node('hazmat', check_hazmat_protocol); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()