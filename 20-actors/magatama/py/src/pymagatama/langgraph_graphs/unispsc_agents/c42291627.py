from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalBurState(TypedDict): bur_specs: dict; validation_results: List[str]; def validate_materials(state: SurgicalBurState): state['validation_results'].append('Material ISO 13485 compliant'); return state; def check_sterility(state: SurgicalBurState): if state['bur_specs'].get('sterile'): state['validation_results'].append('Sterility verified'); return state; graph = StateGraph(SurgicalBurState); graph.add_node('validate', validate_materials); graph.add_node('check_sterility', check_sterility); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_sterility'); graph.add_edge('check_sterility', END); compiled_graph = graph.compile()