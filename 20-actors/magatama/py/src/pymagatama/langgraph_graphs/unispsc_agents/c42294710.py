from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PerfusionState(TypedDict): specs: dict; validated: bool; def validate_specs(state: PerfusionState): state['validated'] = all(k in state['specs'] for k in ['temp_range', 'certification']); return state; graph = StateGraph(PerfusionState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()