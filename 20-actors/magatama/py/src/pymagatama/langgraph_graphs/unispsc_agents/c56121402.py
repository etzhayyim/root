from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec: dict; validated: bool; def validate_spec(state: ProcState): state['validated'] = 'load_bearing_capacity' in state['spec'] and state['spec']['load_bearing_capacity'] > 100; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()