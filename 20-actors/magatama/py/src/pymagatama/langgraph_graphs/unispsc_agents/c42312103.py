from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OstomySupplyState(TypedDict): supply_id: str; compliance_docs: List[str]; approved: bool; def validate_specs(state: OstomySupplyState): state['approved'] = all(['biocompatibility_cert' in state['compliance_docs'], 'ph_report' in state['compliance_docs']]); return state; graph = StateGraph(OstomySupplyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()