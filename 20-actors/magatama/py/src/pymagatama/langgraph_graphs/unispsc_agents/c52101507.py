from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BathMatState(TypedDict): specs: dict; compliance: bool; review_notes: List[str]; def validate_specs(state: BathMatState) -> BathMatState: state['compliance'] = 'slip_resistance_rating' in state['specs'] and state['specs']['water_absorption_rate'] > 0; return state; graph = StateGraph(BathMatState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()