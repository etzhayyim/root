from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_components: List[str]; safety_verified: bool; compliance_report: str; def validate_components(state: KitState): return { "safety_verified": all(c != "hazardous" for c in state["kit_components"]) }; def generate_report(state: KitState): return { "compliance_report": "Verified" if state["safety_verified"] else "Rejected" }; graph = StateGraph(KitState); graph.add_node("validate", validate_components); graph.add_node("report", generate_report); graph.add_edge("validate", "report"); graph.add_edge("report", END); graph.set_entry_point("validate"); graph = graph.compile()