from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ITSupportState(TypedDict): vendor_id: str; sla_requirements: dict; compliance_status: bool; workflow_steps: List[str]; def validate_sla(state: ITSupportState): state['workflow_steps'].append('Validating SLA metrics'); return {'compliance_status': 'sla_requirements' in state}; def execute_support_audit(state: ITSupportState): state['workflow_steps'].append('Auditing IT infrastructure logs'); return {'compliance_status': True}; graph = StateGraph(ITSupportState); graph.add_node('validate', validate_sla); graph.add_node('audit', execute_support_audit); graph.set_entry_point('validate'); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); graph = graph.compile()