from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClarinetState(TypedDict): specs: dict; validation_report: List[str]; def validate_instrument(state: ClarinetState): report = []; if not state['specs'].get('material'): report.append('Missing material info'); return {'validation_report': report}; graph = StateGraph(ClarinetState); graph.add_node('validation', validateinstrument); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()