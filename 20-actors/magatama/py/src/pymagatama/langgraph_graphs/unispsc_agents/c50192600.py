from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; safety_check: bool; compliance_status: str; def validate_food_safety(state: ProcureState): return {'safety_check': True, 'compliance_status': 'HACCP_VERIFIED'}; def finalize_delivery(state: ProcureState): return {'compliance_status': 'READY_FOR_SHIPMENT'}; graph = StateGraph(ProcureState); graph.add_node('safety_check', validate_food_safety); graph.add_node('finalization', finalize_delivery); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'finalization'); graph.add_edge('finalization', END); graph = graph.compile()