from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TextileState(TypedDict): spec_data: dict; validation_results: List[str]; graph = StateGraph(TextileState); def validate_calibration(state: TextileState): state['validation_results'].append('Calibration verified'); return state; def check_compliance(state: TextileState): state['validation_results'].append('ISO compliance checked'); return state; graph.add_node('calibrate', validate_calibration); graph.add_node('compliance', check_compliance); graph.set_entry_point('calibrate'); graph.add_edge('calibrate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()