from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PistonSpec(TypedDict): dimensions: dict; material: str; validated: bool; graph = StateGraph(PistonSpec); def validate_specs(state: PistonSpec): state['validated'] = all(val > 0 for val in state['dimensions'].values()); return state; def check_material(state: PistonSpec): return {'material_ok': state['material'] == 'steel'}; graph.add_node('validate', validate_specs); graph.add_node('check_material', check_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_material'); graph.add_edge('check_material', END); graph.compile()