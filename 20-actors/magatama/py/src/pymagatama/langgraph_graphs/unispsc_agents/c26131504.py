from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlantState(TypedDict): specs: dict; validation_results: List[str]; def validate_efficiency(state: PlantState): state['validation_results'].append('Efficiency check passed'); return state; def check_emissions(state: PlantState): state['validation_results'].append('Emissions check passed'); return state; graph = StateGraph(PlantState); graph.add_node('validate_efficiency', validate_efficiency); graph.add_node('check_emissions', check_emissions); graph.set_entry_point('validate_efficiency'); graph.add_edge('validate_efficiency', 'check_emissions'); graph.add_edge('check_emissions', END); graph = graph.compile()