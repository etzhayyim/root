from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): specs: List[str]; validation_status: bool; def validate_specs(state: CastState): return {'validation_status': 'Grade' in state['specs'] and 'NDT' in state['specs']}; graph = StateGraph(CastState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()