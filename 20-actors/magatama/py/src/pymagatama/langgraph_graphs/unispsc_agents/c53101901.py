from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): items: List[dict]; qc_passed: bool; graph = StateGraph(SupplyState); def validate_specs(state: SupplyState): return {'qc_passed': all('fabric' in item for item in state['items'])}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()