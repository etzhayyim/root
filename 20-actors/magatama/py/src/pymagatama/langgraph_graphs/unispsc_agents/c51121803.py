from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): ingredient_name: str; purity_levels: float; compliance_docs: List[str]; approved: bool; def validate_purity(state: DrugState): state['approved'] = state['purity_levels'] >= 99.0; return state; def check_compliance(state: DrugState): state['approved'] = state['approved'] and 'GMP' in state['compliance_docs']; return state; graph = StateGraph(DrugState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_compliance'); graph.add_edge('check_compliance', END); graph.compile()