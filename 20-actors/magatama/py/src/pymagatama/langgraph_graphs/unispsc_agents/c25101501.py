from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MinibusState(TypedDict): vehicle_id: str; specs: dict; approved: bool; def validate_specs(state: MinibusState): state['approved'] = 'safety_certification_standard' in state['specs']; return state; def finalize_procurement(state: MinibusState): return {'status': 'processed'}; graph = StateGraph(MinibusState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()