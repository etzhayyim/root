from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChiselState(TypedDict): chisel_specs: dict; inspection_status: str; graph = StateGraph(ChiselState); def validate_hardness(state: ChiselState): return {'inspection_status': 'passed' if state['chisel_specs'].get('hardness', 0) >= 50 else 'failed'}; def check_material(state: ChiselState): return {'inspection_status': 'verified' if state['chisel_specs'].get('material') == 'chrome-vanadium' else 'manual_review'}; graph.add_node('validate_hardness', validate_hardness); graph.add_node('check_material', check_material); graph.set_entry_point('validate_hardness'); graph.add_edge('validate_hardness', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()