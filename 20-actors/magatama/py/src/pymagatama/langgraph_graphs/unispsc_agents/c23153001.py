from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): robot_id: str; specs: dict; validation: bool; def validate_robot_specs(state: RobotState) -> RobotState: state['validation'] = state['specs'].get('load', 0) > 0; return state; def check_export_compliance(state: RobotState) -> RobotState: return state; graph = StateGraph(RobotState); graph.add_node('validate', validate_robot_specs); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()