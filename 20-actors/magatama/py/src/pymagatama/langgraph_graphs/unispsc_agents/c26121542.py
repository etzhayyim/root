from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WireState(TypedDict): wire_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: WireState): specs = state['wire_specs']; errors = []; required_fields = ['voltage', 'material', 'standard']; for f in required_fields: if f not in specs: errors.append(f'Missing {f}'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(WireState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()