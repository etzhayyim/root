from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IndicatorState(TypedDict): indicators: List[dict]; validation_results: List[str]; graph = StateGraph(IndicatorState); def validate_threshold(state: IndicatorState): return {'validation_results': ['Threshold verified']}; def check_physical_integrity(state: IndicatorState): return {'validation_results': ['Integrity confirmed']}; graph.add_node('validate', validate_threshold); graph.add_node('integrity', check_physical_integrity); graph.set_entry_point('validate'); graph.add_edge('validate', 'integrity'); graph.add_edge('integrity', END); graph = graph.compile()