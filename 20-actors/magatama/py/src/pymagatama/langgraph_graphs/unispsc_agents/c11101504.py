from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class CompositeState(TypedDict): material_spec: Dict; structural_integrity: bool; compliance_check: List[str]; graph = StateGraph(CompositeState); def validate_spec(state: CompositeState): state['structural_integrity'] = state['material_spec'].get('tensile_strength', 0) > 3000; return state; def check_export(state: CompositeState): state['compliance_check'].append('dual-use-review-pass'); return state; graph.add_node('validate', validate_spec); graph.add_node('compliance', check_export); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()