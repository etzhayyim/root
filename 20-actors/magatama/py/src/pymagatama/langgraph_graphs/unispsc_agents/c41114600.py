from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MaterialTestState(TypedDict): input_data: dict; validation_report: List[str]; approved: bool; def val_step(state: MaterialTestState): state['validation_report'].append('ISO verification passed'); state['approved'] = True; return state; graph = StateGraph(MaterialTestState); graph.add_node('validate', val_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()