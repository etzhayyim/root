from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; compliance_check: bool; robot_graph = StateGraph(RobotState); def validate_specs(state: RobotState): return {'compliance_check': state['specs'].get('load_capacity_kg', 0) > 0}; def finalize_procurement(state: RobotState): return {'compliance_check': True}; robot_graph.add_node('validate', validate_specs); robot_graph.add_node('finalize', finalize_procurement); robot_graph.set_entry_point('validate'); robot_graph.add_edge('validate', 'finalize'); robot_graph.add_edge('finalize', END); graph = robot_graph.compile()