from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ContainerState(TypedDict): capacity: float; material: str; certification: list; validated: bool; def validate_spec(state: ContainerState): state['validated'] = state['capacity'] > 0 and 'food_safe' in state['certification']; return {'validated': state['validated']}; graph = StateGraph(ContainerState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()