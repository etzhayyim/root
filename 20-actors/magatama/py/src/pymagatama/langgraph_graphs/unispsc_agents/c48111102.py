from langgraph.graph import StateGraph, END; from typing import TypedDict; class MachineState(TypedDict): vendor_check: bool; safety_cert: bool; approved: bool; def check_vendor(state: MachineState): return {'vendor_check': True}; def verify_safety(state: MachineState): return {'safety_cert': True}; def finalize(state: MachineState): return {'approved': True if state['vendor_check'] and state['safety_cert'] else False}; graph = StateGraph(MachineState); graph.add_node('vendor', check_vendor); graph.add_node('safety', verify_safety); graph.add_node('finalize', finalize); graph.set_entry_point('vendor'); graph.add_edge('vendor', 'safety'); graph.add_edge('safety', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()