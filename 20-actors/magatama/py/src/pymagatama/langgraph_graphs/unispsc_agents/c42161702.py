from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_docs: List[str]; qc_status: str; def validate_cert(state: ProcState) -> ProcState: return {**state, 'qc_status': 'passed' if 'ISO13485' in state['vendor_docs'] else 'failed'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_cert); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()