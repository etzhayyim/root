from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): document: dict; validated: bool; state = State(document={}, validated=False); def validate_gmp(state: State): state['validated'] = 'gmp_cert' in state['document']; return state; builder = StateGraph(State); builder.add_node('check_gmp', validate_gmp); builder.set_entry_point('check_gmp'); builder.add_edge('check_gmp', END); graph = builder.compile()