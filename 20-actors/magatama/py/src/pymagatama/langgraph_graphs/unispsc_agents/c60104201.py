from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WaterChemState(TypedDict): chemical_name: str; purity_level: float; hazard_code: str; validation_passed: bool; def validate_reagent(state: WaterChemState): state['validation_passed'] = state['purity_level'] > 0.99; return state; graph = StateGraph(WaterChemState); graph.add_node('validate', validate_reagent); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()