from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; alloy_spec: str; validation_passed: bool; def material_check(state: ComponentState): state['validation_passed'] = 'non-ferrous' in state['alloy_spec'].lower(); return state; def inspection_check(state: ComponentState): state['validation_passed'] = state['validation_passed'] and True; return state; graph = StateGraph(ComponentState); graph.add_node('material_verification', material_check); graph.add_node('cqa_inspection', inspection_check); graph.set_entry_point('material_verification'); graph.add_edge('material_verification', 'cqa_inspection'); graph.add_edge('cqa_inspection', END); graph = graph.compile()