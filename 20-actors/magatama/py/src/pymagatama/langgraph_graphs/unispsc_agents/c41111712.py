from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicroscopeState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: MicroscopeState): print('Validating optical specs'); state['approved'] = all(k in state['specs'] for k in ['magnification', 'lighting']); return state; graph = StateGraph(MicroscopeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()