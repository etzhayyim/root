from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HardwareState(TypedDict): items: List[dict]; validation_results: dict; def validate_spec(state: HardwareState): state['validation_results'] = {'passed': all(item.get('grade') for item in state['items'])}; return state; def finalize_order(state: HardwareState): return {'status': 'processed'}; graph = StateGraph(HardwareState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()