from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FollowSpotState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: FollowSpotState): errors = []; if not state['specs'].get('wattage'): errors.append('Missing Wattage'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(FollowSpotState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()