from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; valid: bool; stage: str; def validate_specs(state: RobotState) -> RobotState: state['valid'] = state['specs'].get('IP_Rating', 0) >= 65; state['stage'] = 'validated'; return state; def route_step(state: RobotState) -> str: return 'validated' if state['valid'] else END; workflow = StateGraph(RobotState); workflow.add_node('validator', validate_specs); workflow.set_entry_point('validator'); workflow.add_edge('validator', END); graph = workflow.compile()