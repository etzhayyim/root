from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AccountingState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_spec(state: AccountingState): state['validation_results'].append('Spec verified'); state['status'] = 'ready'; return state; def check_compliance(state: AccountingState): state['validation_results'].append('Compliance checked'); return state; graph = StateGraph(AccountingState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()