from langgraph.graph import StateGraph, END; from typing import TypedDict; class DensitometerState(TypedDict): spec: dict; validated: bool; state = DensitometerState(); def validate_spec(state: DensitometerState): state['validated'] = 'calibration_certificate' in state['spec']; return state; def route_procurement(state: DensitometerState): return 'complete' if state['validated'] else 'reject'; graph = StateGraph(DensitometerState); graph.add_node('validate', validate_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()