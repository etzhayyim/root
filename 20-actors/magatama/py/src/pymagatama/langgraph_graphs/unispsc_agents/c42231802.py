from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FormulaState(TypedDict): formula_specs: dict; compliance_check: bool; output: str; def validate_composition(state: FormulaState): state['compliance_check'] = 'protein_content' in state['formula_specs'] and state['formula_specs']['protein_content'] > 0; return state; def check_shelf_life(state: FormulaState): state['output'] = 'Valid' if state['compliance_check'] else 'Invalid'; return state; graph = StateGraph(FormulaState); graph.add_node('validate', validate_composition); graph.add_node('shelf_life', check_shelf_life); graph.add_edge('validate', 'shelf_life'); graph.add_edge('shelf_life', END); graph.set_entry_point('validate'); graph = graph.compile()