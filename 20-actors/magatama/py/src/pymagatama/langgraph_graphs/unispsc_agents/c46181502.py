from langgraph.graph import StateGraph, END; from typing import TypedDict; class VestState(TypedDict): vendor_certs: list; nij_level: str; is_compliant: bool; def check_compliance(state: VestState): return {'is_compliant': state['nij_level'] in ['IIIA', 'III', 'IV'] and len(state['vendor_certs']) > 0}; graph = StateGraph(VestState); graph.add_node('validate', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()