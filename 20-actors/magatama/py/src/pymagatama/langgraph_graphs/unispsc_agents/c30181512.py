from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToiletSeatState(TypedDict): model_number: str; compatibility_verified: bool; features: List[str]; def verify_compatibility(state: ToiletSeatState): return {'compatibility_verified': True if state['model_number'] else False}; def validate_specs(state: ToiletSeatState): print('Validating specs...'); return state; graph = StateGraph(ToiletSeatState); graph.add_node('verify', verify_compatibility); graph.add_node('validate', validate_specs); graph.add_edge('verify', 'validate'); graph.add_edge('validate', END); graph.set_entry_point('verify'); graph = graph.compile()