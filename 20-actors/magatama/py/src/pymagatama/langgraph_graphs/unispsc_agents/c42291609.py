from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalToolState(TypedDict): tool_id: str; quality_checks: List[str]; approved: bool; def validate_specs(state: SurgicalToolState): print('Validating material grade and sterilization...'); return {'approved': True}; def update_inventory(state: SurgicalToolState): print('Updating medical asset registry...'); return {'quality_checks': ['ISO13485_verified']}; graph = StateGraph(SurgicalToolState); graph.add_node('validate', validate_specs); graph.add_node('inventory', update_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph = graph.compile()