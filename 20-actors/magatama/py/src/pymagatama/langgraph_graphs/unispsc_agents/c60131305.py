from langgraph.graph import StateGraph, END; from typing import TypedDict; class HarpState(TypedDict): model: str; serial: str; inspection_passed: bool; def validate_instrument(state: HarpState): state['inspection_passed'] = len(state['serial']) > 0; return state; graph = StateGraph(HarpState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()