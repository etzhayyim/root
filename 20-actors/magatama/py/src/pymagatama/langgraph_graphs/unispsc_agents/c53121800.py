from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TravelKitState(TypedDict): items: List[str]; validated: bool; log: List[str]; def validator(state: TravelKitState): return {'validated': len(state['items']) > 0, 'log': ['Validation complete']}; graph = StateGraph(TravelKitState); graph.add_node('validate', validator); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()