from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): lens_spec: dict; valid_status: bool; def validate_specs(state: State) -> State: specs = state.get('lens_spec', {}); state['valid_status'] = all(k in specs for k in ['ansi_rating', 'material']); return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()