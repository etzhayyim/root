from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TrayState(TypedDict): items: List[str]; sterile: bool; approved: bool; def validate_sterile(state: TrayState): return {'sterile': True}; def approve_procurement(state: TrayState): return {'approved': True}; graph = StateGraph(TrayState); graph.add_node('validate', validate_sterile); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()