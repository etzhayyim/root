from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SolarState(TypedDict): specs: dict; validation_results: List[str]; def validate_efficiency(state: SolarState): state['validation_results'].append('Efficiency checked') if state['specs'].get('efficiency', 0) > 15 else state['validation_results'].append('Low efficiency'); return state; def check_compliance(state: SolarState): state['validation_results'].append('IEC compliant') if 'IEC' in state['specs'].get('certs', []) else None; return state; graph = StateGraph(SolarState); graph.add_node('efficiency', validate_efficiency); graph.add_node('compliance', check_compliance); graph.set_entry_point('efficiency'); graph.add_edge('efficiency', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()