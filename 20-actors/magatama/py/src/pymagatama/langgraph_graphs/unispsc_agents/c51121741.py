from langgraph.graph import StateGraph, END; from typing import TypedDict; class ChemicalState(TypedDict): chemical_name: str; purity_check: bool; compliance_clearance: bool; def validate_purity(state: ChemicalState): return {'purity_check': True}; def check_compliance(state: ChemicalState): return {'compliance_clearance': True}; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()