from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): content: str; specs: dict; verified: bool; def validate_specs(state: GraphState): state['verified'] = 'material_durability' in state['specs'] and 'ink_safety_cert' in state['specs']; return state; builder = StateGraph(GraphState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()