from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_id: str; qc_passed: bool; temp_logs: List[float]; def validate_quality(state: ReagentState): return {'qc_passed': True} if state['temp_logs'] and max(state['temp_logs']) < 8.0 else {'qc_passed': False}; graph = StateGraph(ReagentState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()