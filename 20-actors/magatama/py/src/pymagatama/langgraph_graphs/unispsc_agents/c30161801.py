from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CabinetState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: CabinetState): state['validation_results'].append('Dimensions verified'); return {'validation_results': state['validation_results']}; def check_structural_integrity(state: CabinetState): state['validation_results'].append('Integrity checked'); return {'validation_results': state['validation_results']}; graph = StateGraph(CabinetState); graph.add_node('validate_dim', validate_dimensions); graph.add_node('check_integrity', check_structural_integrity); graph.set_entry_point('validate_dim'); graph.add_edge('validate_dim', 'check_integrity'); graph.add_edge('check_integrity', END); graph = graph.compile()