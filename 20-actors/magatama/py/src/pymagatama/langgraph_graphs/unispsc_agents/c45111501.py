from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LecternState(TypedDict): specs: dict; validation_results: List[str]; def validate_stability(state: LecternState): state['validation_results'].append('Stability test passed'); return state; def validate_materials(state: LecternState): state['validation_results'].append('Material quality verified'); return state; graph = StateGraph(LecternState); graph.add_node('stability', validate_stability); graph.add_node('materials', validate_materials); graph.set_entry_point('stability'); graph.add_edge('stability', 'materials'); graph.add_edge('materials', END); graph = graph.compile()