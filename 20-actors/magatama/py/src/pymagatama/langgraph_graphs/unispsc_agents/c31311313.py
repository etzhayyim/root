from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PipeState): state['valid'] = 'pressure_rating' in state['specs'] and 'material' in state['specs']; return state; builder = StateGraph(PipeState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()