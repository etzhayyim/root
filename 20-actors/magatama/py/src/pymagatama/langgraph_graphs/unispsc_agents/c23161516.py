from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; valid: bool; risk_score: int; def validate_specs(state: ProcurementState): state['valid'] = state['specs'].get('power', 0) > 0; return state; def check_export_controls(state: ProcurementState): state['risk_score'] = 10 if state['specs'].get('high_precision', False) else 0; return state; def build_graph(): graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('controls', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'controls'); graph.add_edge('controls', END); return graph.compile(); graph = build_graph()