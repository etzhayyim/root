from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtractionState(TypedDict): items: List[str]; validated: bool; state: str; def validate_kit(state: ExtractionState): state['validated'] = 'cold_chain' in state['items'] and 'buffer_ph' in state['items']; return {'validated': state['validated']}; graph = StateGraph(ExtractionState); graph.add_node('validate', validate_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()