from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class DentalSlabState(TypedDict): dimensions: dict; material: str; validated: bool; def check_material(state): return {'validated': state['material'] in ['glass', 'ceramic']}; graph = StateGraph(DentalSlabState); graph.add_node('verify', check_material); graph.set_entry_point('verify'); graph.add_edge('verify', END); compiled_graph = graph.compile()