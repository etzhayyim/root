from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): instrument_type: str; specs: dict; compliant: bool; def validate_specs(state: DentalState): state['compliant'] = 'ISO_13485' in state['specs']; return state; builder = StateGraph(DentalState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()