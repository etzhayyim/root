from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; grade: str; gmp_certified: bool; approved: bool; def validate_quality(state: PharmaState): return {'approved': state['gmp_certified'] == True}; def route_procurement(state: PharmaState): return 'approval' if state['approved'] else END; graph = StateGraph(PharmaState); graph.add_node('qc_check', validate_quality); graph.add_edge('qc_check', END); graph.set_entry_point('qc_check'); graph = graph.compile()