from langgraph.graph import StateGraph, END; from typing import TypedDict; class ToolState(TypedDict): tool_type: str; specs: dict; verified: bool; def validate_spec(state: ToolState): state['verified'] = all(k in state['specs'] for k in ['tip_size', 'material']); return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()