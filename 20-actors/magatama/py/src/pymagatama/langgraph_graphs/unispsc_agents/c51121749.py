from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class APIProcurementState(TypedDict): vendor_docs: List[str]; quality_passed: bool; proc_status: str; def validate_gmp(state: APIProcurementState): state['quality_passed'] = 'GMP' in state['vendor_docs']; return state; def check_purity(state: APIProcurementState): return {'proc_status': 'COMPLIANT' if state['quality_passed'] else 'REJECTED'}; graph = StateGraph(APIProcurementState); graph.add_node('validate', validate_gmp); graph.add_node('check', check_purity); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.set_entry_point('validate'); graph = graph.compile()