from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): commodity: str; purity: float; validated: bool; def validate_spec(state: ProcState): state['validated'] = state['purity'] >= 94.0; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()