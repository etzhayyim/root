from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[dict]; validation_status: bool; def validate_drainer(state: State): state['validation_status'] = all('material' in item and 'dimensions' in item for item in state['items']); return state; def check_specs(state: State): return {'validation_status': state['validation_status']}; graph = StateGraph(State); graph.add_node('validate', validate_drainer); graph.add_edge('validate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()