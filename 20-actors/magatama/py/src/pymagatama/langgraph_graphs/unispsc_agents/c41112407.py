from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): sensor_data: dict; valid: bool; def validate_spec(state: State): return { "valid": state["sensor_data"]["range"] > 0 }; def handle_compliance(state: State): return { "valid": state["valid"] and "cert" in state["sensor_data"] }; graph = StateGraph(State); graph.add_node("validate", validate_spec); graph.add_node("compliance", handle_compliance); graph.set_entry_point("validate"); graph.add_edge("validate", "compliance"); graph.add_edge("compliance", END); graph = graph.compile()