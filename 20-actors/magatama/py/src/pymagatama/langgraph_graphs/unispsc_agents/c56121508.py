from langgraph.graph import StateGraph, END; from typing import TypedDict; class DeskState(TypedDict): specs: dict; valid: bool; def validate_specs(state: DeskState) -> DeskState: state['valid'] = 'material' in state['specs'] and 'load' in state['specs']; return state; def final_decision(state: DeskState) -> str: return 'valid' if state['valid'] else 'invalid'; graph = StateGraph(DeskState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()