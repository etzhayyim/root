from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InsulinState(TypedDict): cold_chain_verified: bool; quality_control_passed: bool; shipment_id: str; def verify_cold_chain(state: InsulinState): return {'cold_chain_verified': True}; def validate_quality(state: InsulinState): return {'quality_control_passed': True}; graph = StateGraph(InsulinState); graph.add_node('verify_cold_chain', verify_cold_chain); graph.add_node('validate_quality', validate_quality); graph.add_edge('verify_cold_chain', 'validate_quality'); graph.add_edge('validate_quality', END); graph.set_entry_point('verify_cold_chain'); graph = graph.compile()