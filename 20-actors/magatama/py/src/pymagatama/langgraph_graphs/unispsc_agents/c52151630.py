from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): specs: dict; approved: bool; def validate_specs(state: ProcState): state['approved'] = 'food_grade' in state['specs'] and state['specs']['material'] == 'stainless_steel'; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()