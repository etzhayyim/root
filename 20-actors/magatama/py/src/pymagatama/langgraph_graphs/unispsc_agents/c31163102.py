from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FerruleState(TypedDict): dimensions: dict; material: str; validated: bool; def validate_specs(state: FerruleState): state['validated'] = all(k in state['dimensions'] for k in ['id', 'od', 'length']) and state['material'] is not None; return state; graph = StateGraph(FerruleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()