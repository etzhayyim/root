from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LivestockState(TypedDict): data: dict; status: list[str]; def validate_compliance(state: LivestockState): return {'status': ['compliance_checked']}; def run_quality_inspection(state: LivestockState): return {'status': ['quality_passed']}; graph = StateGraph(LivestockState); graph.add_node('validate', validate_compliance); graph.add_node('inspect', run_quality_inspection); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()