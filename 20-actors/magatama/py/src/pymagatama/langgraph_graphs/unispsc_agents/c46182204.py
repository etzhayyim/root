from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): spec: dict; validated: bool; state = State(spec={}, validated=False); def validate_specs(state: State): state['validated'] = 'size_range' in state['spec'] and 'medical_certification' in state['spec']; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()