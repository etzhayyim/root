from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CastState(TypedDict): input_spec: dict; inspection_results: list; is_approved: bool; def validate_dimensions(state: CastState): state['inspection_results'].append('Dimensional check passed'); return state; def check_material_grade(state: CastState): state['inspection_results'].append('Material grade verified'); state['is_approved'] = True; return state; graph = StateGraph(CastState); graph.add_node('validate', validate_dimensions); graph.add_node('material_check', check_material_grade); graph.add_edge('validate', 'material_check'); graph.add_edge('material_check', END); graph.set_entry_point('validate'); graph = graph.compile()