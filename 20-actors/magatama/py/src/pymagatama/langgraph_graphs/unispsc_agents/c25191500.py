from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AviationState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: AviationState): state['validation_passed'] = all(k in state['specs'] for k in ['AS9100_certification', 'load_capacity_kg']); return state; def check_compliance(state: AviationState): return 'compliance_check' if state['validation_passed'] else END; graph = StateGraph(AviationState); graph.add_node('validate', validate_specs); graph.add_node('compliance_check', lambda x: x); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance_check'); graph.add_edge('compliance_check', END); graph.compile()