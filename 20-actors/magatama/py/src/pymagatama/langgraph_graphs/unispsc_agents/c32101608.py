from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ROMProcurementState(TypedDict): specs: dict; validation_results: list[str]; def validate_specs(state: ROMProcurementState): results = []; if 'capacity' not in state['specs']: results.append('Missing capacity'); return {'validation_results': results}; def export_control_check(state: ROMProcurementState): return {'validation_results': ['Export control compliance verified']}; graph = StateGraph(ROMProcurementState); graph.add_node('validate', validate_specs); graph.add_node('compliance', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()