from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BuswayState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: BuswayState): required = ['voltage', 'current', 'ip_rating']; missing = [k for k in required if k not in state['specs']]; state['validation_results'] = missing; state['status'] = 'invalid' if missing else 'validated'; return state; graph = StateGraph(BuswayState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()