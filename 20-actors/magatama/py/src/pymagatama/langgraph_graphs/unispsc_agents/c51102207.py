from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity: float; gmp_compliant: bool; valid: bool; def validate_purity(state: PharmaState): state['valid'] = state['purity'] >= 99.0; return state; def check_compliance(state: PharmaState): state['valid'] = state['valid'] and state['gmp_compliant']; return state; graph = StateGraph(PharmaState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_compliance', check_compliance); graph.add_edge('validate_purity', 'check_compliance'); graph.add_edge('check_compliance', END); graph.set_entry_point('validate_purity'); graph = graph.compile()