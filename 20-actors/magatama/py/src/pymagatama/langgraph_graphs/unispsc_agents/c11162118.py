from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from typing import List; class CommodityState(TypedDict): commodity_code: str; purity: float; status: str; safety_flags: List[str]; def validate_purity(state: CommodityState): return {'status': 'PASSED' if state['purity'] >= 99.9 else 'FAILED'}; def check_safety(state: CommodityState): return {'safety_flags': ['DUAL_USE_REVIEW']}; graph = StateGraph(CommodityState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()