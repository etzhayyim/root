from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): vendor_docs: List[str]; compliance_status: bool; workflow_complete: bool; def check_certification(state: SurgicalState): return {'compliance_status': 'ISO_13485' in state['vendor_docs']}; def finalize_procurement(state: SurgicalState): return {'workflow_complete': True}; graph = StateGraph(SurgicalState); graph.add_node('cert_check', check_certification); graph.add_node('finalizer', finalize_procurement); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'finalizer'); graph.add_edge('finalizer', END); graph = graph.compile()