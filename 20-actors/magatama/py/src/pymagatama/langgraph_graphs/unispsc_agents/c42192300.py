from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LiftState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: LiftState): state['compliance'] = state['specs'].get('load_capacity', 0) > 0; return state; def check_safety(state: LiftState): state['status'] = 'COMPLIANT' if state['compliance'] else 'NON_COMPLIANT'; return state; graph = StateGraph(LiftState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()