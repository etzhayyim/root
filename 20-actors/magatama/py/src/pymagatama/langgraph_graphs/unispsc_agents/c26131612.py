from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DuctState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: DuctState): state['validation_report'] = 'Pass' if 'Material Grade' in state['specs'] else 'Fail'; return state; graph = StateGraph(DuctState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()