from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): code: str; specs: dict; valid: bool; def validate_purity(state: ProcurementState): state['valid'] = state['specs'].get('purity', 0) >= 95; return state; def check_safety(state: ProcurementState): state['valid'] = state['valid'] and 'COA' in state['specs']; return state; builder = StateGraph(ProcurementState); builder.add_node('validate', validate_purity); builder.add_node('safety', check_safety); builder.add_edge('validate', 'safety'); builder.add_edge('safety', END); builder.set_entry_point('validate'); graph = builder.compile()