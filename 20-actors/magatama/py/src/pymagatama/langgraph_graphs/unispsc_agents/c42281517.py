from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RecoveryState(TypedDict): water_purity_level: float; system_specs: dict; status: str; def validate_specs(state: RecoveryState): return {'status': 'validated'} if state['system_specs'].get('flow_rate') > 0 else {'status': 'failed'}; def process_recovery(state: RecoveryState): return {'status': 'operational'}; graph = StateGraph(RecoveryState); graph.add_node('validate', validate_specs); graph.add_node('process', process_recovery); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()