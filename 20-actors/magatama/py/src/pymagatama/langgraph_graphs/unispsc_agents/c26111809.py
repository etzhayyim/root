from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BeltGuardState(TypedDict): dimensions: dict; material: str; compliant: bool; def validate_specs(state: BeltGuardState): return 'compliant' if state['material'] == 'steel' else 'needs_review'; graph = StateGraph(BeltGuardState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()