from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): chemical_data: dict; validation_results: List[str]; def validate_api_purity(state: AgentState): purity = state['chemical_data'].get('purity', 0); return {'validation_results': ['Passed' if purity >= 99.0 else 'Failed']}; def check_regulatory_compliance(state: AgentState): return {'validation_results': state['validation_results'] + ['Compliance verified']}; graph = StateGraph(AgentState); graph.add_node('validate', validate_api_purity); graph.add_node('compliance', check_regulatory_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()