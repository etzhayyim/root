from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; quality_check: bool; compliance: bool; def check_specs(state): state['specs']['validated'] = True; return state; def verify_compliance(state): state['compliance'] = True; return state; graph = StateGraph(CastingState); graph.add_node('validate', check_specs); graph.add_node('compliance', verify_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()