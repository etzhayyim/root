from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; valid: bool; status: str; def validate_specs(state: AssemblyState): state['valid'] = 'tensile_strength_mpa' in state['specs'] and state['specs']['tensile_strength_mpa'] > 400; state['status'] = 'verified' if state['valid'] else 'failed_validation'; return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()