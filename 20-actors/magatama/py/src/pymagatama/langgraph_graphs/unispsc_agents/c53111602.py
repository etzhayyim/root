from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShoeState(TypedDict): specs: dict; valid: bool; def validate_shoe_specs(state: ShoeState): state['valid'] = 'size' in state['specs'] and 'material' in state['specs']; return state; def check_compliance(state: ShoeState): print('Checking compliance...'); return state; graph = StateGraph(ShoeState); graph.add_node('validate', validate_shoe_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()