from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LuggageState(TypedDict): data: dict; errors: List[str]; validated: bool; def validate_luggage(state: LuggageState): specs = state['data']; errors = []; if specs.get('weight', 0) > 30: errors.append('Weight exceeds limit'); return {'errors': errors, 'validated': len(errors) == 0}; graph = StateGraph(LuggageState); graph.add_node('validate', validate_luggage); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()