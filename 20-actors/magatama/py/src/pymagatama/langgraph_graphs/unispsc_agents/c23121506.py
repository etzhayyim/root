from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; validation_errors: Annotated[list, operator.add]; status: str; def validate_specs(state: CompressorState): errors = []; if state['specs'].get('pressure', 0) > 1000: errors.append('Pressure exceeds safety threshold'); return {'validation_errors': errors}; def check_compliance(state: CompressorState): return {'status': 'Approved' if not state['validation_errors'] else 'Rejected'}; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()