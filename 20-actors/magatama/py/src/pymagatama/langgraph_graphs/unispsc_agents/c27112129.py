from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClampState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ClampState): errors = []; if state['specs'].get('clamping_force', 0) <= 0: errors.append('Invalid clamping force'); return {'validation_errors': errors}; def check_approval(state: ClampState): return 'approved' if not state['validation_errors'] else END; graph = StateGraph(ClampState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()