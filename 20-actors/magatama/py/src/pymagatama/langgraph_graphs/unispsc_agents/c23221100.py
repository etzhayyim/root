from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): specs: dict; validation_errors: Annotated[list, operator.add]; status: str; def validate_impact_tools(state: ToolState): specs = state.get('specs', {}); errors = []; if specs.get('impact_energy') < 0: errors.append('Negative impact energy'); return {'validation_errors': errors, 'status': 'valid' if not errors else 'invalid'}; graph = StateGraph(ToolState); graph.add_node('validate', validate_impact_tools); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()