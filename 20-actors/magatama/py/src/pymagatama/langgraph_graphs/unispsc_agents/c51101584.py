from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class ChromatographState(TypedDict): consumables: Dict; validation_results: List[str]; def validate_purity(state: ChromatographState) -> ChromatographState: state['validation_results'].append('Purity check passed'); return state; def check_compatibility(state: ChromatographState) -> ChromatographState: state['validation_results'].append('Compatibility confirmed'); return state; graph = StateGraph(ChromatographState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_compatibility', check_compatibility); graph.add_edge('validate_purity', 'check_compatibility'); graph.add_edge('check_compatibility', END); graph.set_entry_point('validate_purity'); graph = graph.compile()