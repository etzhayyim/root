from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabPressState(TypedDict): press_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: LabPressState): errors = []; if state['press_specs'].get('pressure', 0) <= 0: errors.append('Invalid operating pressure'); return {'validation_errors': errors}; def check_compliance(state: LabPressState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(LabPressState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()