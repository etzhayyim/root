from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MotorState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_spec(state: MotorState): specs = state['input_spec']; results = []; status = 'PENDING'; if specs.get('voltage') and specs.get('hp'): results.append('Basic parameters validated') status = 'READY'; else: results.append('Missing required fields') status = 'FAILED'; return {'validation_results': results, 'status': status}; graph = StateGraph(MotorState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()