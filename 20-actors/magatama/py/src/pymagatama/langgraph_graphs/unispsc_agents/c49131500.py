from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FishingGearState(TypedDict): items: List[str]; validation_results: List[str]; def validate_gear(state: FishingGearState): results = [f'Validating item: {item}' for item in state['items']]; return {'validation_results': results}; graph = StateGraph(FishingGearState); graph.add_node('validate', validate_gear); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()