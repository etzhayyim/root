from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ApparelState(TypedDict): vendor_info: str; fabric_specs: dict; compliance_status: bool; def validate_specs(state: ApparelState): return {'compliance_status': 'textile_content' in state['fabric_specs']}; def approve_procurement(state: ApparelState): return {'compliance_status': True}; graph = StateGraph(ApparelState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()