from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SpecimenState(TypedDict): items: List[dict]; qc_passed: bool; def validate_spec(state: SpecimenState): state['qc_passed'] = all(item.get('sterile') and item.get('leak_proof') for item in state['items']); return state; graph = StateGraph(SpecimenState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()