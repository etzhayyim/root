from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class GrinderState(TypedDict): spec: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: GrinderState): errors = []; rpm = state['spec'].get('rpm', 0); if rpm > 25000: errors.append('RPM exceeds safety threshold'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(GrinderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()