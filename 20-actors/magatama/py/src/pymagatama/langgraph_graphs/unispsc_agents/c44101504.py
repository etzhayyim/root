from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ScannerState(TypedDict): document_path: str; destination: str; validation_status: bool; def validate_config(state: ScannerState): state['validation_status'] = True if state['destination'].startswith('sftp://') else False; return state; def route_to_target(state: ScannerState): return 'complete' if state['validation_status'] else 'error'; graph = StateGraph(ScannerState); graph.add_node('validate', validate_config); graph.add_edge('__start__', 'validate'); graph.add_conditional_edges('validate', route_to_target, {'complete': END, 'error': END}); graph = graph.compile()