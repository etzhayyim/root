from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SutureState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: SutureState): return {'compliance': 'ISO-13485' in state['specs']}; def check_biocompatibility(state: SutureState): return {'approved': state.get('compliance', False)}; graph = StateGraph(SutureState); graph.add_node('validate', validate_specs); graph.add_node('biocompatibility', check_biocompatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'biocompatibility'); graph.add_edge('biocompatibility', END); compile_graph = graph.compile()