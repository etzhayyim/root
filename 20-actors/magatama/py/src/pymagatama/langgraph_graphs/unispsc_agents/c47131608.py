from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToiletBrushState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: ToiletBrushState): state['validation_report'] = 'Pass' if 'bristle_material' in state['specs'] else 'Fail'; return state; graph = StateGraph(ToiletBrushState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()