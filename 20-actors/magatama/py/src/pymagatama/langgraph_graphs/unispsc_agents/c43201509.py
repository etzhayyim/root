from typing import TypedDict, List, Annotated; import operator; from langgraph.graph import StateGraph, END; class ImageProcState(TypedDict): image_paths: List[str]; processed_paths: List[str]; log: Annotated[List[str], operator.add]; def validate_format(state: ImageProcState): return {'log': ['Validating formats']}; def apply_filters(state: ImageProcState): return {'processed_paths': ['/tmp/proc_01.png'], 'log': ['Applying filters']}; def finalize_output(state: ImageProcState): return {'log': ['Output generated']}; graph = StateGraph(ImageProcState); graph.add_node('validate', validate_format); graph.add_node('process', apply_filters); graph.add_node('finalize', finalize_output); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()