from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): content: str; validated: bool; next_step: str; def validate_specs(state: State): state['validated'] = 'ISO' in state['content']; state['next_step'] = 'approve'; return state; def final_process(state: State): return {'next_step': 'complete'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('process', final_process); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()