from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EdibleFatState(TypedDict): fat_type: str; quality_metrics: dict; compliant: bool; def validate_quality(state: EdibleFatState): state['compliant'] = state['quality_metrics'].get('peroxide_value', 0) < 2.0; return state; def check_origin(state: EdibleFatState): state['compliant'] = state['compliant'] and ('origin_certification' in state['quality_metrics']); return state; graph = StateGraph(EdibleFatState); graph.add_node('validate', validate_quality); graph.add_node('check', check_origin); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()