from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ResourceState(TypedDict): content_url: str; validation_status: str; metadata: dict; def validate_source(state: ResourceState): print('Validating archival source...'); return {'validation_status': 'verified'}; def catalog_content(state: ResourceState): print('Categorizing resources...'); return {'metadata': {'topic': 'womens_history'}}; graph = StateGraph(ResourceState); graph.add_node('validate', validate_source); graph.add_node('catalog', catalog_content); graph.set_entry_point('validate'); graph.add_edge('validate', 'catalog'); graph.add_edge('catalog', END); compiled_graph = graph.compile()