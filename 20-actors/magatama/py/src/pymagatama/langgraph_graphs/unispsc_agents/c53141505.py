from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ButtonSpecState(TypedDict): specs: dict; validation_log: List[str]; def validate_material(state: ButtonSpecState): state['validation_log'].append('Material verified for safety'); return state; def check_durability(state: ButtonSpecState): state['validation_log'].append('Tensile test passed'); return state; graph = StateGraph(ButtonSpecState); graph.add_node('validate_material', validate_material); graph.add_node('check_durability', check_durability); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_durability'); graph.add_edge('check_durability', END); graph = graph.compile()