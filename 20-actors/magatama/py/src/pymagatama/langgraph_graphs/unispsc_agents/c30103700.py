from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BraidState(TypedDict): specs: dict; valid: bool; def validate_specs(state: BraidState): specs = state['specs']; state['valid'] = 'tensile_strength' in specs and 'diameter' in specs; return state; graph = StateGraph(BraidState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()