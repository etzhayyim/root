from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HookState(TypedDict): hook_specs: dict; status: str; safety_checks: List[str]; def validate_specs(state: HookState): load = state['hook_specs'].get('load_capacity', 0); return {'status': 'valid' if load > 0 else 'invalid', 'safety_checks': ['load_verification']}; def approve_procurement(state: HookState): return {'status': 'approved'}; graph = StateGraph(HookState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()