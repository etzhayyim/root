from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_food_safety(state: ProcureState): state['validated'] = all(['safety_cert' in cert for cert in state['certs']]); return state; def check_shelf_life(state: ProcureState): print('Checking shelf life for preserved nectarines'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_food_safety); graph.add_node('shelf_life', check_shelf_life); graph.add_edge('validate', 'shelf_life'); graph.add_edge('shelf_life', END); graph.set_entry_point('validate'); graph = graph.compile()