from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): items: List[str]; validated: bool; log: List[str]; def validate_cold_chain(state: PharmState): state['log'].append('Validating cold chain requirements'); return {'validated': True}; graph = StateGraph(PharmState); graph.add_node('cold_chain', validate_cold_chain); graph.set_entry_point('cold_chain'); graph.add_edge('cold_chain', END); graph = graph.compile()