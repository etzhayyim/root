from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; valid: bool; error: str; def validate_fittings(state: State): specs = state['input_data']; return {'valid': 'voltage_rating' in specs and 'ip_rating' in specs, 'error': None if 'voltage_rating' in specs else 'Missing technical specs'}; graph = StateGraph(State); graph.add_node('validate', validate_fittings); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()