from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): purity_level: float; weight: float; is_verified: bool; def validate_purity(state: ForgingState): state['is_verified'] = state['purity_level'] >= 0.999; return state; graph = StateGraph(ForgingState); graph.add_node('assay_check', validate_purity); graph.set_entry_point('assay_check'); graph.add_edge('assay_check', END); graph = graph.compile()