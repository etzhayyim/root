from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec: dict; validated: bool; state: str; def validate_lubricant(state: ProcState): state['validated'] = 'pH' in state['spec'] and 'toxicity_cert' in state['spec']; return state; def route_procurement(state: ProcState): return 'validate' if state['validated'] else 'error'; graph = StateGraph(ProcState); graph.add_node('validate', validate_lubricant); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()