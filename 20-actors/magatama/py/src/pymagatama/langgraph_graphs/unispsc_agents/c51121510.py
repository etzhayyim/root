from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): vendor_id: str; compliance_docs: List[str]; cert_valid: bool; graph = StateGraph(PharmState); def check_regulatory(state): return {'cert_valid': True}; def validate_batch(state): return {'compliance_docs': ['FDA_Clearance', 'GMP_Cert']}; graph.add_node('regulatory_check', check_regulatory); graph.add_node('batch_validation', validate_batch); graph.set_entry_point('regulatory_check'); graph.add_edge('regulatory_check', 'batch_validation'); graph.add_edge('batch_validation', END); compiled_graph = graph.compile()