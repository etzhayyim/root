from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FeltState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_density(state: FeltState): density = state['specs'].get('density', 0); state['valid'] = 0.1 <= density <= 0.8; return state; def check_compliance(state: FeltState): state['log'].append('Checking flame retardancy'); return state; graph = StateGraph(FeltState); graph.add_node('validate', validate_density); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()