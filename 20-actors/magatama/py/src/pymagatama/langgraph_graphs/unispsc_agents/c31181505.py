from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GasketState(TypedDict): material: str; thickness: float; test_passed: bool; def validate_specs(state: GasketState): return {'test_passed': state['thickness'] > 0.0}; def check_compatibility(state: GasketState): return {'test_passed': state['material'] == 'cork_composite'}; graph = StateGraph(GasketState); graph.add_node('validate', validate_specs); graph.add_node('compatible', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatible'); graph.add_edge('compatible', END); graph = graph.compile()