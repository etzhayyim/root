from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OlfactometerState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(OlfactometerState); def validate_calibration(state: OlfactometerState): state['validation_report'].append('Checking ISO 16000-28 calibration status'); return {'validation_report': state['validation_report']}; def check_flow_controls(state: OlfactometerState): state['validation_report'].append('Verifying airflow stability parameters'); return {'validation_report': state['validation_report']}; graph.add_node('validate_cal', validate_calibration); graph.add_node('check_flow', check_flow_controls); graph.set_entry_point('validate_cal'); graph.add_edge('validate_cal', 'check_flow'); graph.add_edge('check_flow', END); graph = graph.compile()