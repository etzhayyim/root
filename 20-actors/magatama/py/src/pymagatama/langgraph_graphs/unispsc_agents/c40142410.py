from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlangeState(TypedDict): specs: dict; validation_results: List[str]; def validate_flange_specs(state: FlangeState): results = []; required = ['material', 'pressure_rating']; for key in required: if key not in state['specs']: results.append(f'Missing {key}'); return {'validation_results': results}; def generate_report(state: FlangeState): return {'validation_results': state['validation_results'] + ['Report Generated']}; graph = StateGraph(FlangeState); graph.add_node('validate', validate_flange_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()