from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): product: str; validated: bool; specs: List[str]; def validate_api(state: State): return {'validated': len(state['specs']) > 3}; graph = StateGraph(State); graph.add_node('validate', validate_api); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()