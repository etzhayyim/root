from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PoleState(TypedDict): length: float; material: str; validated: bool; def validate_specs(state: PoleState): return {'validated': state['length'] > 0 and state['material'] != ''}; graph = StateGraph(PoleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()