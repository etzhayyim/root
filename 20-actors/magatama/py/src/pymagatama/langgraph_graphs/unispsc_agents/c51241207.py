from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): composition: str; safety_checks: Annotated[list, operator.add]; compliance_status: bool; def check_composition(state: PharmState): return {'safety_checks': ['Ingredient verification complete']}; def validate_regulations(state: PharmState): return {'compliance_status': True}; graph = StateGraph(PharmState); graph.add_node('composition', check_composition); graph.add_node('regulatory', validate_regulations); graph.set_entry_point('composition'); graph.add_edge('composition', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()