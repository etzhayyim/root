from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ZincProcurementState(TypedDict): specs: dict; validation_passed: bool; results: List[str]; def validate_specs(state: ZincProcurementState): print('Validating zinc sheet properties...'); state['validation_passed'] = 'thickness_mm' in state['specs'] and 'alloy_purity_percent' in state['specs']; return state; def finalize_order(state: ZincProcurementState): state['results'].append('Order ready for distribution'); return state; graph = StateGraph(ZincProcurementState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()