from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeSpecState(TypedDict): npt_size: str; material: str; validated: bool; def validate_spec(state: PipeSpecState): state['validated'] = state['material'] in ['Stainless Steel', 'Carbon Steel'] and bool(state['npt_size']); return state; graph = StateGraph(PipeSpecState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()