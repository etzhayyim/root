from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): commodity: str; specs: dict; safety_cleared: bool; def validate_specs(state: ProcureState): state['safety_cleared'] = True if 'blade_material' in state['specs'] else False; return state; def finalize_order(state: ProcureState): return {'status': 'approved' if state['safety_cleared'] else 'rejected'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()