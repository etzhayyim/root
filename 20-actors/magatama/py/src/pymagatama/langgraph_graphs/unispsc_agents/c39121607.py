from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FuseState(TypedDict): specs: dict; valid: bool; error: str; def validate_fuse(state: FuseState): specs = state['specs']; is_valid = all(key in specs for key in ['amps', 'volts', 'cert']); return {'valid': is_valid, 'error': '' if is_valid else 'Missing mandatory specs'}; builder = StateGraph(FuseState); builder.add_node('validation', validate_fuse); builder.set_entry_point('validation'); builder.add_edge('validation', END); graph = builder.compile()