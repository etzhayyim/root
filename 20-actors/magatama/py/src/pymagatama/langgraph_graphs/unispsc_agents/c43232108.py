from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ScheduleState(TypedDict): vendor: str; requirements: list; validation_status: bool; def check_compliance(state: ScheduleState): return { "validation_status": "GDPR" in state["requirements"] }; graph = StateGraph(ScheduleState); graph.add_node("compliance", check_compliance); graph.set_entry_point("compliance"); graph.add_edge("compliance", END); graph = graph.compile()