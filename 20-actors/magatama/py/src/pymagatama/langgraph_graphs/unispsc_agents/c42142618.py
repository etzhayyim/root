from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; integrity_check: bool; compliance_docs: List[str]; def validate_integrity(state: KitState): return { 'integrity_check': True }; def check_compliance(state: KitState): return { 'compliance_docs': ['FDA_510K', 'ISO_13485'] }; graph = StateGraph(KitState); graph.add_node('validate', validate_integrity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()