from langgraph.graph import StateGraph, END; from typing import TypedDict; class AerosolState(TypedDict): pressure_rating: float; material: str; pressure_tested: bool; def validate_pressure(state: AerosolState): return { 'pressure_tested': state['pressure_rating'] > 0 }; def check_compliance(state: AerosolState): return { 'status': 'compliant' if state['material'] == 'Aluminium' else 'review' }; graph = StateGraph(AerosolState); graph.add_node('validate', validate_pressure); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()