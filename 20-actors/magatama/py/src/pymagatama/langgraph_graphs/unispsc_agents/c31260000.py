from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HousingState(TypedDict): specs: dict; validation_report: dict; status: str; def validate_dimensions(state: HousingState): state['validation_report'] = {'dimensions_ok': True}; return state; def check_compliance(state: HousingState): state['status'] = 'COMPLIANT' if state['specs'].get('ip_rating') else 'PENDING'; return state; graph = StateGraph(HousingState); graph.add_node('validate', validate_dimensions); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()