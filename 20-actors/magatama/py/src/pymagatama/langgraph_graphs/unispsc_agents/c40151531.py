from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: PumpState): specs = state['specs']; results = []; if specs.get('flow_rate', 0) <= 0: results.append('Invalid flow rate'); return {'validation_results': results, 'status': 'failed' if results else 'validated'}; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()