from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AppServerState(TypedDict): vendor_name: str; version: str; compliance_docs: List[str]; approved: bool; def validate_compliance(state: AppServerState): return {'approved': len(state['compliance_docs']) > 0}; def deploy_provisioning(state: AppServerState): print('Processing provisioning...'); return {'approved': True}; graph = StateGraph(AppServerState); graph.add_node('validate', validate_compliance); graph.add_node('provision', deploy_provisioning); graph.set_entry_point('validate'); graph.add_edge('validate', 'provision'); graph.add_edge('provision', END); app = graph.compile()