from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TherapyDeviceState(TypedDict): device_id: str; compliance_passed: bool; specs: dict; graph = StateGraph(TherapyDeviceState); def validate_medical_specs(state: TherapyDeviceState): state['compliance_passed'] = 'cert' in state['specs'] and state['specs']['frequency'] > 0; return state; def check_regulatory(state: TherapyDeviceState): print('Checking FDA/MDR compliance...'); return state; graph.add_node('validate', validate_medical_specs); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()