from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): specs: dict; valid: bool; def validate_tube_specs(state: ProcState): return {'valid': 'Resolution' in state['specs'] and 'Operating Voltage' in state['specs']}; graph = StateGraph(ProcState); graph.add_node('validate', validate_tube_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()