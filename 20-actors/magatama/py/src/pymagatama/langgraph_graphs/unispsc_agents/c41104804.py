from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EvaporatorState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: EvaporatorState): errors = []; if 'min_rotation' not in state['specs']: errors.append('Missing rotation speed range'); return {'validation_errors': errors}; def check_compliance(state: EvaporatorState): approved = len(state['validation_errors']) == 0; return {'approved': approved}; graph = StateGraph(EvaporatorState); graph.add_node('validation', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validation'); graph.add_edge('validation', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()