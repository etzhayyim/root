from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RestroomSupplierState(TypedDict): items: List[str]; validation_results: List[str]; approved: bool; def validate_supplies(state: RestroomSupplierState): results = []; for item in state['items']: results.append(f'Inspected {item} for chemical compliance'); return {'validation_results': results}; def finalize_order(state: RestroomSupplierState): return {'approved': True}; graph = StateGraph(RestroomSupplierState); graph.add_node('validation', validate_supplies); graph.add_node('approval', finalize_order); graph.add_edge('validation', 'approval'); graph.add_edge('approval', END); graph.set_entry_point('validation'); graph = graph.compile()