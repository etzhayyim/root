from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VehicleProcurementState(TypedDict): vehicle_type: str; specs: dict; approved: bool; def validate_specs(state: VehicleProcurementState): state['approved'] = 'safety_cert' in state['specs']; return state; def route_approval(state: VehicleProcurementState): return 'approval' if state['approved'] else END; graph = StateGraph(VehicleProcurementState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()