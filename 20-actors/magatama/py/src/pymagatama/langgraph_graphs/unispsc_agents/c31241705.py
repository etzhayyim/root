from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserMirrorState(TypedDict): specs: dict; compliance: bool; validation_log: List[str]; def validate_specs(state: LaserMirrorState): specs = state['specs']; log = []; compliant = True; if specs.get('damage_threshold', 0) < 5: log.append('Low damage threshold'); compliant = False; return {'compliance': compliant, 'validation_log': log}; def finalize_procurement(state: LaserMirrorState): return {'validation_log': state['validation_log'] + ['Procurement ready']}; graph = StateGraph(LaserMirrorState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()