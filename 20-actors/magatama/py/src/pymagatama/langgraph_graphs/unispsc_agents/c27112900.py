from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DispensingState(TypedDict): tool_specs: dict; validation_report: list[str]; graph = StateGraph(DispensingState); def validate_specs(state: DispensingState): return {'validation_report': [f'Certified for {state.get("tool_specs").get("material_type")}']}; def check_safety(state: DispensingState): return {'validation_report': ['Safety protocols validated']}; graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()