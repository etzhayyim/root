from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AntiqueState(TypedDict): instrument_id: str; appraisal_docs: List[str]; verified: bool; def validate_provenance(state: AntiqueState): state['verified'] = len(state['appraisal_docs']) > 0; return state; def prepare_shipping(state: AntiqueState): print(f'Scheduling climate-controlled transit for {state["instrument_id"]}'); return state; graph = StateGraph(AntiqueState); graph.add_node('validate', validate_provenance); graph.add_node('ship', prepare_shipping); graph.set_entry_point('validate'); graph.add_edge('validate', 'ship'); graph.add_edge('ship', END); graph = graph.compile()