from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CableState(TypedDict): input_specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_bronze_specs(state: CableState): specs = state['input_specs']; results = []; if specs.get('material') != 'Bronze': results.append('Material mismatch'); return {'validation_results': results, 'status': 'validated' if not results else 'failed'}; graph = StateGraph(CableState); graph.add_node('validate', validate_bronze_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()