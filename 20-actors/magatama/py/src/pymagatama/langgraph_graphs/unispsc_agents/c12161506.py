from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_id: str; purity: float; hazard_level: int; approved: bool; def validate_purity(state: ChemicalState) -> dict: return {"approved": state["purity"] >= 0.999}; def check_hazards(state: ChemicalState) -> dict: return {"hazard_level": 5 if state["chemical_id"].startswith("F") else 1}; graph = StateGraph(ChemicalState); graph.add_node("validate", validate_purity); graph.add_node("hazard_check", check_hazards); graph.add_edge("validate", "hazard_check"); graph.add_edge("hazard_check", END); graph.set_entry_point("validate"); compiled_graph = graph.compile()