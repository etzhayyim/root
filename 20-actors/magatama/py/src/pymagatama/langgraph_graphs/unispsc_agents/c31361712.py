from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: AssemblyState): state['validation_results'].append('Material confirmed'); return state; def check_rivet_density(state: AssemblyState): state['validation_results'].append('Density checked'); return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_materials); graph.add_node('density', check_rivet_density); graph.set_entry_point('validate'); graph.add_edge('validate', 'density'); graph.add_edge('density', END); compiler = graph.compile()