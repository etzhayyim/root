from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LaryngealState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: LaryngealState): if 'iso_13485' in state['specs']: state['validation_results'].append('ISO Certified'); return {'validation_results': state['validation_results']}; def check_safety(state: LaryngealState): print('Checking biocompatibility...'); return {'validation_results': state['validation_results'] + ['Safety Verified']}; graph = StateGraph(LaryngealState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()