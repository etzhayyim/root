from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LampArmState(TypedDict): arm_type: str; specs: dict; valid: bool; def validate_specs(state: LampArmState): state['valid'] = 'load_capacity_kg' in state['specs']; return state; builder = StateGraph(LampArmState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()