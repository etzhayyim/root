from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PerfusionState(TypedDict): tubing_specs: dict; validation_results: List[str]; def validate_medical_grade(state: PerfusionState): return {'validation_results': ['ISO-10993 compliant']}; def compliance_check(state: PerfusionState): return {'validation_results': state['validation_results'] + ['Material certified']}; graph = StateGraph(PerfusionState); graph.add_node('validate', validate_medical_grade); graph.add_node('compliance', compliance_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()