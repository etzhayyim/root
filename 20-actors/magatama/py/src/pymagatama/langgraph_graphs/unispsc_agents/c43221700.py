from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: NetworkState): specs = state['specs']; errors = []; if 'throughput' not in specs: errors.append('Missing throughput'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(NetworkState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()