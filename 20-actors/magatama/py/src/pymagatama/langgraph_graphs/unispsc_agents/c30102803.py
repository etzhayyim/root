from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SteelPilingState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_spec(state: SteelPilingState): state['validation_errors'] = [] if state['specs'].get('grade') not in ['SS400', 'SM490'] else []; state['approved'] = len(state['validation_errors']) == 0; return state; graph = StateGraph(SteelPilingState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()