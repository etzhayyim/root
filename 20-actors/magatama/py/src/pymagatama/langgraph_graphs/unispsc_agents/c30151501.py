from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RoofState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_roll_specs(state: RoofState): state['valid'] = 'ASTM_compliance' in state['specs'] and state['specs']['weight'] > 0; return state; graph = StateGraph(RoofState); graph.add_node('validate', validate_roll_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()