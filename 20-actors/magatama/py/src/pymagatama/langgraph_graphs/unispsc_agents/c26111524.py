from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GearState(TypedDict): specs: dict; validation_report: list; valid: bool; def tech_review(state: GearState): state['validation_report'].append('Reviewing torque and backlash'); return {'valid': True}; def safety_audit(state: GearState): state['validation_report'].append('Checking dual-use compliance'); return {'valid': True}; graph = StateGraph(GearState); graph.add_node('tech_review', tech_review); graph.add_node('safety_audit', safety_audit); graph.set_entry_point('tech_review'); graph.add_edge('tech_review', 'safety_audit'); graph.add_edge('safety_audit', END); graph = graph.compile()