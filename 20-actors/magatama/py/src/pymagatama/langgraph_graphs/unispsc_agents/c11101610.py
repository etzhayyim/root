from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class RawMaterialState(TypedDict): material_id: str; purity_check: bool; batch_data: dict; approval_status: bool; def validate_purity(state: RawMaterialState): state['purity_check'] = state['batch_data'].get('purity', 0) > 99.5; return state; def check_safety_compliance(state: RawMaterialState): state['approval_status'] = state['purity_check']; return state; graph = StateGraph(RawMaterialState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_safety_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()