from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NateglinideState(TypedDict): batch_id: str; purity: float; status: str; def validate_purity(state: NateglinideState): return {'status': 'approved' if state['purity'] >= 99.0 else 'rejected'}; def check_compliance(state: NateglinideState): return {'status': 'compliant' if state['batch_id'] else 'non-compliant'}; graph = StateGraph(NateglinideState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()