from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): specs: dict; valid: bool; def validate_jet(state: State): state['valid'] = all(k in state['specs'] for k in ['orifice', 'material']); return state; graph = StateGraph(State); graph.add_node('validate', validate_jet); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()