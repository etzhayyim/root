from langgraph.graph import StateGraph, END; from typing import TypedDict; class SamplerState(TypedDict): dimensions: dict; material: str; validated: bool; def check_compatibility(state: SamplerState): state['validated'] = state['material'] in ['PTFE', 'PVC', 'Glass']; return state; builder = StateGraph(SamplerState); builder.add_node('validate', check_compatibility); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()