from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SidingState(TypedDict): specs: dict; validation_results: List[str]; def validate_fire_rating(state: SidingState): state['validation_results'].append('Checking fire rating compliance'); return state; def validate_dimensions(state: SidingState): state['validation_results'].append('Validating material dimensions'); return state; graph = StateGraph(SidingState); graph.add_node('fire_check', validate_fire_rating); graph.add_node('dim_check', validate_dimensions); graph.set_entry_point('fire_check'); graph.add_edge('fire_check', 'dim_check'); graph.add_edge('dim_check', END); graph = graph.compile()