from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_id: str; quality_passed: bool; safety_verified: bool; steps: List[str]; def validate_purity(state: ReagentState): state['quality_passed'] = True; state['steps'].append('Purity validated'); return state; def check_safety(state: ReagentState): state['safety_verified'] = True; state['steps'].append('SDS and Safety verified'); return state; workflow = StateGraph(ReagentState); workflow.add_node('validate', validate_purity); workflow.add_node('safety', check_safety); workflow.set_entry_point('validate'); workflow.add_edge('validate', 'safety'); workflow.add_edge('safety', END); graph = workflow.compile()