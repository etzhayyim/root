from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadiotherapyState(TypedDict): device_id: str; compliance_docs: List[str]; inspection_passed: bool; def validate_compliance(state: RadiotherapyState): state['compliance_passed'] = len(state['compliance_docs']) > 3; return state; def inspection_logic(state: RadiotherapyState): state['inspection_passed'] = True; return state; graph = StateGraph(RadiotherapyState); graph.add_node('validate', validate_compliance); graph.add_node('inspect', inspection_logic); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()