from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BallPoolState(TypedDict): vendor_specs: dict; compliance_check: bool; safety_audit_status: str; def validate_safety(state: BallPoolState): return {'compliance_check': True, 'safety_audit_status': 'passed'} if state['vendor_specs'].get('fire_rated') else {'compliance_check': False}; graph = StateGraph(BallPoolState); graph.add_node('safety_validation', validate_safety); graph.set_entry_point('safety_validation'); graph.add_edge('safety_validation', END); compiled_graph = graph.compile()