from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ChromatographyState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(ChromatographyState); def validate_hardware(state: ChromatographyState): state['validation_results'].append('Hardware verified'); return state; def check_compliance(state: ChromatographyState): state['validation_results'].append('GMP compliance checked'); return state; graph.add_node('hardware_check', validate_hardware); graph.add_node('compliance_audit', check_compliance); graph.set_entry_point('hardware_check'); graph.add_edge('hardware_check', 'compliance_audit'); graph.add_edge('compliance_audit', END); graph = graph.compile()