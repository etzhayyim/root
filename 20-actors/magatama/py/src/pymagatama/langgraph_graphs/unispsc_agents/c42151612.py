from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalSpecState(TypedDict): spec_data: dict; approval_status: bool; workflow_steps: List[str]; def validate_medical_grade(state: DentalSpecState): state['approval_status'] = 'iso13485_certification' in state['spec_data']; return state; def finalize_workflow(state: DentalSpecState): state['workflow_steps'] = ['MDR_Compliance_Check', 'Sterilization_Verification', 'QA_Inspection']; return state; graph = StateGraph(DentalSpecState); graph.add_node('validate', validate_medical_grade); graph.add_node('finalize', finalize_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()