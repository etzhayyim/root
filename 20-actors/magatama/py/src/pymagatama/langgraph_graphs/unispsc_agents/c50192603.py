from langgraph.graph import StateGraph, END; from typing import TypedDict; class FoodState(TypedDict): input_data: dict; quality_check: bool; compliance: bool; def validate_specs(state: FoodState): state['quality_check'] = 'shelf_life_months' in state['input_data']; return state; def check_compliance(state: FoodState): state['compliance'] = 'fda_certification' in state['input_data']; return state; graph = StateGraph(FoodState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()