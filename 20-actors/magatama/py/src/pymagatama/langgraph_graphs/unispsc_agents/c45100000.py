from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): print_specs: dict; status: str; def validate_specs(state: GraphState): val = state['print_specs'].get('resolution', 0); return {'status': 'approved' if val >= 600 else 'rejected'}; builder = StateGraph(GraphState); builder.add_node('validation', validate_specs); builder.set_entry_point('validation'); builder.add_edge('validation', END); graph = builder.compile()