from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabSupplyState(TypedDict): items: List[str]; validated: bool; def validate_specs(state: LabSupplyState): return {'validated': len(state['items']) > 0}; def finalize_order(state: LabSupplyState): return {'validated': True}; graph = StateGraph(LabSupplyState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()