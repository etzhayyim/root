from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CircuitTesterState(TypedDict): input_data: dict; validation_errors: List[str]; inspection_status: str; def validate_specs(state: CircuitTesterState): results = []; if 'accuracy' not in state['input_data']: results.append('Missing Calibration data'); return {'validation_errors': results}; def update_status(state: CircuitTesterState): return {'inspection_status': 'COMPLETED' if not state['validation_errors'] else 'FAILED'}; graph = StateGraph(CircuitTesterState); graph.add_node('validate', validate_specs); graph.add_node('status', update_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'status'); graph.add_edge('status', END); graph = graph.compile()