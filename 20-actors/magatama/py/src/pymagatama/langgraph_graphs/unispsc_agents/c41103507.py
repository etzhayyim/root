from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: ProcureState): return {'validation_results': ['HEPA certification verified', 'Airflow calibration confirmed']}; def check_compliance(state: ProcureState): return {'status': 'COMPLIANT' if 'HEPA' in str(state['specs']) else 'REJECTED'}; graph = StateGraph(ProcureState); graph.add_node('validator', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validator', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validator'); graph = graph.compile()