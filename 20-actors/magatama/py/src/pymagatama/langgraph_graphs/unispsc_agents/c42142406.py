from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_certs: List[str]; suction_specs: dict; validated: bool; def validate_specs(state: ProcurementState): return {'validated': 'ISO13485' in state['vendor_certs'] and state['suction_specs'].get('pressure_range') is not None}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()