from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RocketState(TypedDict): vendor: str; specs: dict; compliance_approved: bool; graph = StateGraph(RocketState); def validate_spec(state: RocketState): state['compliance_approved'] = 'ITAR_license' in state['specs']; return state; def check_safety(state: RocketState): print('Checking aerospace safety protocols...'); return state; graph.add_node('validate', validate_spec); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); app = graph.compile()