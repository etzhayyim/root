from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_certs: List[str]; specs_met: bool; output_log: str; def validate_specs(state: ProcurementState): state['specs_met'] = all(c in state['vendor_certs'] for c in ['ISO9001', 'AS9100']); state['output_log'] = 'Specs validated' if state['specs_met'] else 'Missing certifications'; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()