from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TitaniumState(TypedDict): part_id: str; specs: dict; validation_status: bool; def validate_dimensions(state: TitaniumState): state['validation_status'] = True; return state; def check_export_compliance(state: TitaniumState): print(f'Checking export for {state['part_id']}'); return state; graph = StateGraph(TitaniumState); graph.add_node('validate', validate_dimensions); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()