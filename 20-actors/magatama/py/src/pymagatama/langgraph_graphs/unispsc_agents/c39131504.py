from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MarkerState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_specs(state: MarkerState): specs = state['input_data']; results = []; if 'material' not in specs: results.append('Missing material info'); return {'validation_results': results, 'status': 'validated' if not results else 'error'}; graph = StateGraph(MarkerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()