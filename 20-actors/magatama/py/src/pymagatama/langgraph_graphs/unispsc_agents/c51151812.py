from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_name: str; gmp_certified: bool; qc_passed: bool; proc_status: str; def validate_api(state: ProcState): return {'proc_status': 'verified'} if state['gmp_certified'] else {'proc_status': 'rejected'}; def qc_check(state: ProcState): return {'qc_passed': True, 'proc_status': 'ready_for_storage'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_api); graph.add_node('qc', qc_check); graph.add_edge('validate', 'qc'); graph.add_edge('qc', END); graph.set_entry_point('validate'); graph = graph.compile()