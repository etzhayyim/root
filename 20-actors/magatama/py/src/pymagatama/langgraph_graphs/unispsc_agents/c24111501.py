from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CanvasState(TypedDict): specs: dict; valid: bool; def validate_specs(state: CanvasState): state['valid'] = 'gsm' in state['specs'] and state['specs']['gsm'] > 400; return state; graph = StateGraph(CanvasState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()