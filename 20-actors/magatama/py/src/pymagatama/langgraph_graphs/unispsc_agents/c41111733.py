from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MicroscopeSpecState(TypedDict): spec_data: dict; validation_report: list[str]; def validate_optical_spec(state: MicroscopeSpecState): issues = []; if 'refractive_index' not in state['spec_data']: issues.append('Missing refractive index'); return {'validation_report': issues}; def compile_graph(): graph = StateGraph(MicroscopeSpecState); graph.add_node('validate', validate_optical_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()