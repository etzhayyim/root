from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_data: dict; compliance_status: bool; def validate_purity(state: ProcState): state['compliance_status'] = state['chemical_data'].get('purity', 0) >= 99.0; return state; def check_hazmat(state: ProcState): return {'compliance_status': state['chemical_data'].get('is_hazardous', True)}; graph = StateGraph(ProcState); graph.add_node('validate', validate_purity); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()