from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_data: dict; validation_errors: List[str]; class ReagentValidation: @staticmethod def validate(state: ReagentState): if 'purity' not in state['reagent_data']: state['validation_errors'].append('Missing purity'); return state; graph = StateGraph(ReagentState); graph.add_node('validate', ReagentValidation.validate); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()