from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CokeProcurementState(TypedDict): commodity_code: str; quality_metrics: dict; approval_required: bool; audit_logs: Annotated[Sequence[str], operator.add]; def validate_purity_node(state): metrics = state.get('quality_metrics', {}); purity = metrics.get('fixed_carbon', 0); return {'approval_required': purity < 85.0, 'audit_logs': [f'Purity check completed: {purity}%']}; def risk_assessment_node(state): return {'audit_logs': ['Risk assessment: high-value/dangerous-goods classification assigned.']}; graph = StateGraph(CokeProcurementState); graph.add_node('validate_purity', validate_purity_node); graph.add_node('risk_assessment', risk_assessment_node); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'risk_assessment'); graph.add_edge('risk_assessment', END); compiled_graph = graph.compile()