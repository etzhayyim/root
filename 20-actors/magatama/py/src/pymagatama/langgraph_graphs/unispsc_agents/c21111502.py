from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IrrigationState(TypedDict): specs: dict; compliance_score: float; errors: List[str]; def validate_specs(state: IrrigationState): errors = []; if state['specs'].get('operating_pressure_range_psi', 0) > 150: errors.append('Pressure exceeds safety limits'); return {'errors': errors}; graph = StateGraph(IrrigationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()