from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: List[str]; validation_passed: bool; def check_specs(state: ProcState): state['validation_passed'] = 'Material Grade' in state['specs'] and 'Non-Destructive Testing' in state['specs']; return state; builder = StateGraph(ProcState); builder.add_node('validate', check_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()