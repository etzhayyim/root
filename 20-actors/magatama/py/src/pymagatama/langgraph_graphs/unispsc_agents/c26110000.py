from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PowerState(TypedDict): hardware_id: str; safety_checks: bool; compliance_docs: bool; def validate_battery(state: PowerState): state['safety_checks'] = True; return state; def check_export_compliance(state: PowerState): state['compliance_docs'] = True; return state; graph = StateGraph(PowerState); graph.add_node('validate', validate_battery); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()