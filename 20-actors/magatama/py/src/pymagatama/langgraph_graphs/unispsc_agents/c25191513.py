from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): contents: List[str]; validated: bool; serials: List[str]; def validate_kit(state: KitState): state['validated'] = all(len(item) > 3 for item in state['contents']); return state; def check_compliance(state: KitState): return {'validated': state['validated']}; graph = StateGraph(KitState); graph.add_node('validate', validate_kit); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); 