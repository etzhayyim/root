from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MetalFormingState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: MetalFormingState): errors = []; if not state['specs'].get('tonnage_capacity'): errors.append('Missing tonnage capacity'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(MetalFormingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()