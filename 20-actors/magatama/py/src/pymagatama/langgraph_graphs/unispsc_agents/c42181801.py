from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OximeterState(TypedDict): device_id: str; compliance_docs: List[str]; status: str; def validate_cert(state: OximeterState): return {'status': 'CERTIFIED' if 'ISO_13485' in state['compliance_docs'] else 'REJECTED'}; def process_procurement(state: OximeterState): return {'status': 'COMPLETED'}; graph = StateGraph(OximeterState); graph.add_node('validation', validate_cert); graph.add_node('procurement', process_procurement); graph.set_entry_point('validation'); graph.add_edge('validation', 'procurement'); graph.add_edge('procurement', END); compiled_graph = graph.compile()