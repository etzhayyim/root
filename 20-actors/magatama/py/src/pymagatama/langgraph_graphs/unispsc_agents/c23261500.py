from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PrototypeState(TypedDict): specs: dict; valid: bool; risk_score: int; def validate_specs(state: PrototypeState): state['valid'] = 'build_volume_mm' in state['specs']; return state; def check_export_controls(state: PrototypeState): state['risk_score'] = 10 if 'metal_powder' in state['specs'].get('materials', []) else 1; return state; graph = StateGraph(PrototypeState); graph.add_node('validate', validate_specs); graph.add_node('risk', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'risk'); graph.add_edge('risk', END); compiled_graph = graph.compile()