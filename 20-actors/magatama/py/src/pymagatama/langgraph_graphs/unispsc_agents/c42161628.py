from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DialysisTestState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_calib(state: DialysisTestState): state['validation_results'].append('Calibration Check Passed'); return state; def check_compliance(state: DialysisTestState): state['validation_results'].append('IEC 60601-2-16 Compliance Confirmed'); return state; graph = StateGraph(DialysisTestState); graph.add_node('validate', validate_calib); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()