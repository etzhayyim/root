from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ResinState(TypedDict): resin_specs: dict; validation_results: List[str]; def validate_chemical_composition(state: ResinState): return {'validation_results': ['Composition compliant']}; def check_regulatory_status(state: ResinState): return {'validation_results': ['Regulatory docs verified']}; graph = StateGraph(ResinState); graph.add_node('composition', validate_chemical_composition); graph.add_node('regulatory', check_regulatory_status); graph.set_entry_point('composition'); graph.add_edge('composition', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()