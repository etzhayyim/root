from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FuserState(TypedDict): model: str; voltage: int; yield_target: int; validated: bool; def validate_specs(state: FuserState): state['validated'] = state['voltage'] >= 110 and state['yield_target'] > 50000; return state; graph = StateGraph(FuserState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()