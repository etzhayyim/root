from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): cold_chain_verified: bool; quality_cert_attached: bool; supply_status: str; def verify_cold_chain(state: PharmState): state['cold_chain_verified'] = True; return state; def validate_quality(state: PharmState): state['quality_cert_attached'] = True; return state; graph = StateGraph(PharmState); graph.add_node('verify_temp', verify_cold_chain); graph.add_node('verify_qc', validate_quality); graph.add_edge('verify_temp', 'verify_qc'); graph.add_edge('verify_qc', END); graph.set_entry_point('verify_temp'); graph = graph.compile()