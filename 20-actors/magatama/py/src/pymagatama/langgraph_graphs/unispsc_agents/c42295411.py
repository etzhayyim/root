from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): vendor_certs: List[str]; material_spec: str; validated: bool; def check_cert(state: SurgicalState): return {'validated': 'ISO 13485' in state['vendor_certs']}; def validate_specs(state: SurgicalState): return {'validated': state['material_spec'] == '420 Stainless'}; graph = StateGraph(SurgicalState); graph.add_node('cert_check', check_cert); graph.add_node('spec_check', validate_specs); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'spec_check'); graph.add_edge('spec_check', END); graph = graph.compile()