from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; specs: dict; validation_report: List[str]; def validate_nickel_alloy(state: ComponentState): results = []; if 'grade' not in state['specs']: results.append('Missing Grade'); state['validation_report'] = results; return state; graph = StateGraph(ComponentState); graph.add_node('validate', validate_nickel_alloy); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()