from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WindowState(TypedDict): specs: dict; ready: bool; def validate_specs(state: WindowState) -> WindowState: state['ready'] = all(key in state['specs'] for key in ['u_value', 'dimensions']); return state; def finalize_order(state: WindowState) -> WindowState: return state; graph = StateGraph(WindowState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()