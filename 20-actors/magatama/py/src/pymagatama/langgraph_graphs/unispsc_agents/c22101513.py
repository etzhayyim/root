from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: FastenerState): state['valid'] = 'Material Grade' in state['specs'] and 'Tensile Strength' in state['specs']; return state; graph = StateGraph(FastenerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()