from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetState(TypedDict): spec: dict; valid: bool;  def validate_nets(state: NetState): state['valid'] = state['spec'].get('tensile_strength', 0) > 500; return state; graph = StateGraph(NetState); graph.add_node('validate', validate_nets); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()