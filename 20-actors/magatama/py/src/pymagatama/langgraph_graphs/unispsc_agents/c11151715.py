from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CeramicState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_purity(state: CeramicState): return {'validation_results': ['Purity > 99.9% confirmed']} if state['input_spec'].get('purity', 0) >= 99.9 else {'status': 'REJECTED'}; def process_density(state: CeramicState): return {'validation_results': ['Density standard met']}; graph = StateGraph(CeramicState); graph.add_node('validate', validate_purity); graph.add_node('density', process_density); graph.set_entry_point('validate'); graph.add_edge('validate', 'density'); graph.add_edge('density', END); graph = graph.compile()