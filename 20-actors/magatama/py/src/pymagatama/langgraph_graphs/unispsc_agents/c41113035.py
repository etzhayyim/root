from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input_spec: str; validation_results: Annotated[list, operator.add]; status: str; def validate_strip_specs(state: ProcState): validated = 'Valid' in state['input_spec'] or 'ISO' in state['input_spec']; return {'validation_results': ['Spec compliance checked'], 'status': 'Approved' if validated else 'Rejected'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_strip_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()