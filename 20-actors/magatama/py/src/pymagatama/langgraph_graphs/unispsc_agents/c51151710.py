from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; validation_passed: bool; cert_valid: bool; def check_purity(state: PharmaState): state['validation_passed'] = True; return state; def verify_cert(state: PharmaState): state['cert_valid'] = True; return state; graph = StateGraph(PharmaState); graph.add_node('validate_purity', check_purity); graph.add_node('verify_gmp', verify_cert); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'verify_gmp'); graph.add_edge('verify_gmp', END); graph.compile()