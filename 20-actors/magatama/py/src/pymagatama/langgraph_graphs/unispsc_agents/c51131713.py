from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): status: str; spec_validated: bool; def validate_quality(state: ProcState): return {'spec_validated': True, 'status': 'quality_checked'}; def finalize_procurement(state: ProcState): return {'status': 'procurement_ready'}; graph = StateGraph(ProcState); graph.add_node('quality_check', validate_quality); graph.add_node('finalization', finalize_procurement); graph.set_entry_point('quality_check'); graph.add_edge('quality_check', 'finalization'); graph.add_edge('finalization', END); compiler = graph.compile()