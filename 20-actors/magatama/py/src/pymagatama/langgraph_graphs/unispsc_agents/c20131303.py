from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MiningPartState(TypedDict): part_id: str; specs: dict; valid: bool; def validate_specs(state: MiningPartState) -> MiningPartState: state['valid'] = 'oem_compatibility_id' in state['specs']; return state; def check_export(state: MiningPartState) -> MiningPartState: state['export_cleared'] = True; return state; graph = StateGraph(MiningPartState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()