from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftState(TypedDict): part_number: str; spec_docs: List[str]; validated: bool; def validate_specs(state: AircraftState): state['validated'] = all(['AMS' in doc for doc in state['spec_docs']]); return state; def export_control_check(state: AircraftState): return state; graph = StateGraph(AircraftState); graph.add_node('validate', validate_specs); graph.add_node('export_check', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()