from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MarkerState(TypedDict): tags: List[str]; verified: bool; def validate_markers(state: MarkerState): state['verified'] = 'compliant' in state.get('tags', []); return state; graph = StateGraph(MarkerState); graph.add_node('validate', validate_markers); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()