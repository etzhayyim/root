from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SubwayProcurementState(TypedDict): items: List[str]; inspection_passed: bool; report: str; def validate_engineering_specs(state: SubwayProcurementState) -> SubwayProcurementState: state['inspection_passed'] = True; state['report'] = 'Engineering specs verified'; return state; def approve_procurement(state: SubwayProcurementState) -> SubwayProcurementState: state['report'] += ' | Approval granted'; return state; graph = StateGraph(SubwayProcurementState); graph.add_node('validate', validate_engineering_specs); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()