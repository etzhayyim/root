from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): status: str; spec: dict; validated: bool; def check_compliance(state: ProcState): state['validated'] = 'biocompatibility_certification' in state['spec']; return state; def route_step(state: ProcState): return 'validated' if state['validated'] else END; graph = StateGraph(ProcState); graph.add_node('validate', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()