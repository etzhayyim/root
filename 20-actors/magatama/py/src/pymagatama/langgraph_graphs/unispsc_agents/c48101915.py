from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrayState(TypedDict): tray_specs: dict; compliance_status: bool; def validate_specs(state: TrayState): is_valid = 'material' in state['tray_specs'] and 'heat_rating' in state['tray_specs']; return {'compliance_status': is_valid}; graph = StateGraph(TrayState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()