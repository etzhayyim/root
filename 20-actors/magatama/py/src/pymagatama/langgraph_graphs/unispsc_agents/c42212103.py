from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class BookHolderState(TypedDict): spec_data: dict; verification_passed: bool; def validate_specs(state: BookHolderState): print('Validating ergonomics and load limits...'); state['verification_passed'] = True; return state; graph = StateGraph(BookHolderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()