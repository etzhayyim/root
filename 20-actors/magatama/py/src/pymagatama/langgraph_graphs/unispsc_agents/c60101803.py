from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_id: str; book_title: str; specs_verified: bool; def validate_metadata(state: ProcurementState): state['specs_verified'] = bool(state['book_title']); return state; def check_copyright(state: ProcurementState): return {'specs_verified': state['specs_verified']}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_metadata); graph.add_node('copyright', check_copyright); graph.set_entry_point('validate'); graph.add_edge('validate', 'copyright'); graph.add_edge('copyright', END); graph = graph.compile()