from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnalyzerState(TypedDict): vendor_id: str; item_spec: dict; validation_passed: bool; def validate_compatibility(state: AnalyzerState): state['validation_passed'] = 'model' in state['item_spec']; return state; def check_regulatory(state: AnalyzerState): return state; graph = StateGraph(AnalyzerState); graph.add_node('validate', validate_compatibility); graph.add_node('regulatory', check_regulatory); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); graph = graph.compile()