from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SkiPoleState(TypedDict): pole_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SkiPoleState): errors = []; if not state['pole_specs'].get('length'): errors.append('Missing required length'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(SkiPoleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()