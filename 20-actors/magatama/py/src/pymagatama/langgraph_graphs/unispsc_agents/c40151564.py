from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_compatibility(state: PumpState): material = state['specs'].get('material'); chemical = state['specs'].get('chemical'); state['approved'] = material in ['PTFE', 'Hastelloy'] if chemical == 'acid' else True; return state; graph = StateGraph(PumpState); graph.add_node('validate', validate_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()