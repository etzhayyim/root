from langgraph.graph import StateGraph, END; from typing import TypedDict; class PipetteState(TypedDict): specs: dict; valid: bool; def val_spec(state: PipetteState): state['valid'] = all(k in state['specs'] for k in ['material', 'volume']); return state; graph = StateGraph(PipetteState); graph.add_node('validate', val_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()