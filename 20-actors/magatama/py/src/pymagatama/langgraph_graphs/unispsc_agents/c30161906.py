from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldingState(TypedDict): profile_specs: dict; validation_results: List[str]; graph = StateGraph(MoldingState); def validate_dimensions(state: MoldingState): return {'validation_results': ['dimensions_verified']}; def check_material(state: MoldingState): return {'validation_results': ['material_compliant']}; graph.add_node('validate_dimensions', validate_dimensions); graph.add_node('check_material', check_material); graph.set_entry_point('validate_dimensions'); graph.add_edge('validate_dimensions', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()