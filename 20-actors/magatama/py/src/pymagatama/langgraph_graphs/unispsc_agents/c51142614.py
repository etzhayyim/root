from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_name: str; compliance_checked: bool; hazard_level: int; def validate_regulatory(state: ProcureState): return {'compliance_checked': True}; def assess_risk(state: ProcureState): return {'hazard_level': 5}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_regulatory); graph.add_node('risk_assessment', assess_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'risk_assessment'); graph.add_edge('risk_assessment', END); compiled_graph = graph.compile()