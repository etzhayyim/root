from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastRemovalState(TypedDict): vendor_docs: List[str]; validation_results: List[str]; inspection_status: str; def validate_certifications(state: CastRemovalState): state['validation_results'].append('Checking IEC 60601 compliance'); return {'inspection_status': 'verified'}; def verify_blade_specs(state: CastRemovalState): return {'validation_results': ['Blade material analysis completed']}; graph = StateGraph(CastRemovalState); graph.add_node('cert_check', validate_certifications); graph.add_node('blade_check', verify_blade_specs); graph.add_edge('cert_check', 'blade_check'); graph.add_edge('blade_check', END); graph.set_entry_point('cert_check'); graph = graph.compile()