from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WiringState(TypedDict): component_id: str; specs: dict; validation_passed: bool; def validate_specs(state: WiringState): state['validation_passed'] = all(k in state['specs'] for k in ['voltage', 'ip_rating']); return state; def check_compliance(state: WiringState): print(f'Checking compliance for {state['component_id']}'); return state; graph = StateGraph(WiringState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); 