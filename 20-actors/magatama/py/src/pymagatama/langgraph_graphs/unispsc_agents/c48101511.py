from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class GriddleState(TypedDict): specs: dict; compliance_passed: bool; def validate_griddle(state: GriddleState): state['compliance_passed'] = 'certification' in state['specs'] and state['specs']['temp_control'] >= 300; return state; graph = StateGraph(GriddleState); graph.add_node('validate', validate_griddle); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()