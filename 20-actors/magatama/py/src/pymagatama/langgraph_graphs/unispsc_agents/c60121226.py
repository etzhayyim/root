from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BrushProcurementState(TypedDict): brush_specs: dict; validation_errors: List[str]; approved: bool; def validate_brush(state: BrushProcurementState): specs = state['brush_specs']; errors = []; if 'hair_material' not in specs: errors.append('Missing hair material'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(BrushProcurementState); graph.add_node('validate', validate_brush); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()