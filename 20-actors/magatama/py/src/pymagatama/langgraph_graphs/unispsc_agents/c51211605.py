from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): chemical_name: str; requires_review: bool; spec_compliance: bool; def check_purity(state: AgentState): state['spec_compliance'] = True; return state; def validate_grade(state: AgentState): state['requires_review'] = False; return state; graph = StateGraph(AgentState); graph.add_node('purity_check', check_purity); graph.add_node('regulatory_grade_validation', validate_grade); graph.add_edge('purity_check', 'regulatory_grade_validation'); graph.add_edge('regulatory_grade_validation', END); graph.set_entry_point('purity_check'); graph = graph.compile()