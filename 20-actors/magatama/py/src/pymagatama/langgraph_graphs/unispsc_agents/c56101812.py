from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_table_specs(state: State): errors = []; for item in state['items']: if item.get('weight_capacity_kg', 0) < 20: errors.append(f'Low capacity for item {item.get(id)}'); return {'validation_errors': errors}; graph = StateGraph(State); graph.add_node('validate', validate_table_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()