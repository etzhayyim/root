from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PhotoState(TypedDict): chemical_data: dict; approval_status: bool; def validate_materials(state: PhotoState): state['approval_status'] = 'MSDS' in state['chemical_data']; return state; builder = StateGraph(PhotoState); builder.add_node('validate', validate_materials); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()