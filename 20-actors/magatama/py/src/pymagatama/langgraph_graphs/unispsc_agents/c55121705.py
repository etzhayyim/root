from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SignState(TypedDict): input_specs: dict; validation_results: List[str]; approved: bool; def validate_specs(state: SignState): specs = state['input_specs']; errors = []; if not specs.get('material'): errors.append('Missing material'); if not specs.get('adhesive'): errors.append('Missing adhesive spec'); return {'validation_results': errors, 'approved': len(errors) == 0}; graph = StateGraph(SignState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()