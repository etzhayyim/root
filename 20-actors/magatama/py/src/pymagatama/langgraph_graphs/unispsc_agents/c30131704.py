from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TileState(TypedDict): specs: dict; valid: bool; error: str; def validate_tiles(state: TileState): specs = state['specs']; required = ['dimensions', 'water_absorption', 'slip_rating']; state['valid'] = all(k in specs for k in required) and specs['water_absorption'] < 0.5; return state; graph = StateGraph(TileState); graph.add_node('validate', validate_tiles); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()