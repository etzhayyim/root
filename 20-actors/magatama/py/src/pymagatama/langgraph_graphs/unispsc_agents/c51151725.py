from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; is_compliant: bool; def validate_purity(state: PharmState): state['is_compliant'] = state['purity'] >= 99.9; return state; def check_regulations(state: PharmState): print(f'Checking compliance for {state["chemical_name"]}'); return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('regulate', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulate'); graph.add_edge('regulate', END); compiled_graph = graph.compile()