from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class NailState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: NailState): state['validation_report'] = 'Validation passed: Material and dimensions within tolerance.' if state['specs'].get('tensile_strength') > 500 else 'Validation failed: Insufficient strength.'; return state; graph = StateGraph(NailState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()