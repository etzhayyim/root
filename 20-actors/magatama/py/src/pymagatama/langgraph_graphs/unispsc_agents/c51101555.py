from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): input_data: dict; validation_report: list; quality_score: float; approved: bool; def validate_gmp(state: PharmaState): report = []; score = 1.0; if 'cert' not in state['input_data']: report.append('Missing GMP Cert'); score -= 0.5; return {'validation_report': report, 'quality_score': score}; def check_regulatory(state: PharmaState): approved = state['quality_score'] > 0.7; return {'approved': approved}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_gmp); graph.add_node('compliance', check_regulatory); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()