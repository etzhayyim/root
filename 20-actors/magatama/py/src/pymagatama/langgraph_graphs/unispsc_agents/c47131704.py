from typing import TypedDict, List; from langgraph.graph import StateGraph; class DispenserState(TypedDict): model_number: str; material_specs: str; compliant: bool; def validate_specs(state: DispenserState): return {'compliant': 'stainless' in state['material_specs'].lower() or 'abs' in state['material_specs'].lower()}; def finalize_procurement(state: DispenserState): print('Processing dispenser order for: ' + state['model_number']); return state; graph = StateGraph(DispenserState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', '__end__'); graph = graph.compile()