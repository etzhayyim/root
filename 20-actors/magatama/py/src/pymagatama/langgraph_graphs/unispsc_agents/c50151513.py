from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OilProcurementState(TypedDict): oil_type: str; purity: float; quality_verified: bool; def validate_quality(state: OilProcurementState): print(f'Validating purity for {state["oil_type"]}...'); state['quality_verified'] = state['purity'] >= 0.99; return state; def check_compliance(state: OilProcurementState): print('Checking HACCP compliance...'); return {'quality_verified': state['quality_verified']}; graph = StateGraph(OilProcurementState); graph.add_node('validate', validate_quality); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()