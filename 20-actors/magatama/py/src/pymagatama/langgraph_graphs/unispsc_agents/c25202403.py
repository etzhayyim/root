from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TankState(TypedDict): doc_id: str; compliance_check: bool; material_spec: str; status: str; def validate_spec(state: TankState): return {'compliance_check': 'AS9100' in state['material_spec']}; def route_approval(state: TankState): return 'end' if state['compliance_check'] else 'end'; graph = StateGraph(TankState); graph.add_node('validate', validate_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()