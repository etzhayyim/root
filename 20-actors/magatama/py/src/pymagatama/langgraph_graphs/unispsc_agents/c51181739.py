from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; compliance_checked: bool; def validate_pharma(state: PharmaState): print(f'Validating {state[chemical_name]}'); return {'compliance_checked': True}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_pharma); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()