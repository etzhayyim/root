from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[str]; status: str; def validate_pharma(state: State): return {'status': 'validated' if 'license' in state['items'] else 'rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_pharma); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()