from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PlateState(TypedDict): specs: dict; validation_results: list[str]; def validate_material(state: PlateState): result = 'Valid' if 'grade' in state['specs'] else 'Invalid'; return {'validation_results': [result]}; def check_tolerances(state: PlateState): result = 'Tolerance conformant' if state['specs'].get('tolerance') else 'Tolerance missing'; return {'validation_results': [result]}; graph = StateGraph(PlateState); graph.add_node('validate', validate_material); graph.add_node('tolerance_check', check_tolerances); graph.set_entry_point('validate'); graph.add_edge('validate', 'tolerance_check'); graph.add_edge('tolerance_check', END); graph = graph.compile()