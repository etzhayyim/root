from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProfileState(TypedDict): alloy_type: str; tensile_strength: float; compliance_verified: bool; def check_alloy(state): return {'compliance_verified': state.get('tensile_strength', 0) > 200}; def validate_specs(state): return {'compliance_verified': True if state['alloy_type'] else False}; graph = StateGraph(ProfileState); graph.add_node('check_specs', validate_specs); graph.add_node('verify_strength', check_alloy); graph.add_edge('check_specs', 'verify_strength'); graph.add_edge('verify_strength', END); graph.set_entry_point('check_specs'); graph = graph.compile()