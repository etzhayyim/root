from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RailState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: RailState): results = []; if state['specs'].get('load_capacity', 0) < 150: results.append('Load capacity below standard'); return {'validation_results': results}; def compile_graph(): graph = StateGraph(RailState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()