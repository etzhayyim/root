from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): status: str; purity: float; temp_range: tuple; approved: bool; def validate_purity(state: ProcState): state['approved'] = state['purity'] >= 99.0; return state; def check_storage(state: ProcState): state['status'] = 'VALID' if 2 <= state['temp_range'][1] <= 8 else 'INVALID'; return state; graph = StateGraph(ProcState); graph.add_node('purity_check', validate_purity); graph.add_node('storage_check', check_storage); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'storage_check'); graph.add_edge('storage_check', END); graph = graph.compile()