from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): medicine: str; status: str; spec_check: bool; def validate_gmp(state: PharmaState): state['spec_check'] = True; return state; def update_status(state: PharmaState): state['status'] = 'QC_PASSED'; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_gmp); graph.add_node('mark_ready', update_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'mark_ready'); graph.add_edge('mark_ready', END); graph = graph.compile()