from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): silica_purity: float; contaminants: List[str]; approved: bool; def check_purity(state: FilterState) -> dict: return {'approved': state['silica_purity'] >= 99.9}; def validate_specs(graph: StateGraph): graph.add_node('check_purity', check_purity); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', END); return graph.compile(); graph = validate_specs(StateGraph(FilterState))