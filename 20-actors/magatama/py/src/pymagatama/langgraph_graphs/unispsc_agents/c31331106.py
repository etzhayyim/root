from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): component_id: str; structural_specs: dict; validated: bool; def validate_assembly(state: State): specs = state['structural_specs']; is_valid = 'material' in specs and 'bonding_cert' in specs; return {'validated': is_valid}; graph = StateGraph(State); graph.add_node('validate', validate_assembly); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()