from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrumGrabState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: DrumGrabState): specs = state['specs']; is_valid = specs.get('load_capacity_kg', 0) > 0 and 'safety_certification' in specs; return {'valid': is_valid, 'error': '' if is_valid else 'Invalid spec parameters'}; graph = StateGraph(DrumGrabState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()