from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TimerSpec(TypedDict): specs: dict; validation_errors: List[str]; def validate_timer_specs(state: TimerSpec): specs = state['specs']; errors = []; if 'voltage' not in specs: errors.append('Missing voltage'); if 'accuracy' not in specs: errors.append('Missing timing accuracy'); return {'validation_errors': errors}; graph = StateGraph(TimerSpec); graph.add_node('validate', validate_timer_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()