from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CerealState(TypedDict): grain_type: str; moisture_level: float; certification_docs: List[str]; approved: bool; def validate_moisture(state: CerealState): state['approved'] = state['moisture_level'] < 14.0; return state; def check_certs(state: CerealState): state['approved'] = state['approved'] and len(state['certification_docs']) > 0; return state; graph = StateGraph(CerealState); graph.add_node('validate', validate_moisture); graph.add_node('check', check_certs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()