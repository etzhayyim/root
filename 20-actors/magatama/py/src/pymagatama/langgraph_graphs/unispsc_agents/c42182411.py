from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input: str; validated: bool; specs: List[str]; graph = StateGraph(ProcureState); def validate_spec(state: ProcureState): return {'validated': 'ISO' in state['specs']}; def check_frequency(state: ProcureState): return {'specs': state['specs'] + ['verified_hz']}; graph.add_node('validate', validate_spec); graph.add_node('frequency', check_frequency); graph.add_edge('validate', 'frequency'); graph.add_edge('frequency', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()