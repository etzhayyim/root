from langgraph.graph import StateGraph, END; from typing import TypedDict; class LabSupplyState(TypedDict): supply_type: str; specs: dict; valid: bool; def validate_spec(state: LabSupplyState): state['valid'] = 'material_spec' in state['specs']; return state; def check_safety(state: LabSupplyState): return state; graph = StateGraph(LabSupplyState); graph.add_node('validate', validate_spec); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()