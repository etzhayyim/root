from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class APIState(TypedDict): chemical_data: dict; compliance_checks: List[str]; approved: bool; def validate_purity(state: APIState): purity = state['chemical_data'].get('purity', 0); state['compliance_checks'].append('Purity' if purity >= 99.0 else 'Fail'); return {'approved': purity >= 99.0}; def check_msds(state: APIState): state['compliance_checks'].append('MSDS_Verified'); return state; graph = StateGraph(APIState); graph.add_node('validate', validate_purity); graph.add_node('msds', check_msds); graph.set_entry_point('validate'); graph.add_edge('validate', 'msds'); graph.add_edge('msds', END); app = graph.compile()