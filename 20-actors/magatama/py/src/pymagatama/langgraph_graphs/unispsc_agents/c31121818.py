from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): alloy_specs: dict; inspection_results: List[str]; approved: bool; def validate_metallurgy(state: GraphState): state['inspection_results'].append('Metallurgy Verified'); return state; def perform_nx_check(state: GraphState): state['inspection_results'].append('CAD Integrity Passed'); return state; workflow = StateGraph(GraphState); workflow.add_node('validate_metallurgy', validate_metallurgy); workflow.add_node('check_cad', perform_nx_check); workflow.set_entry_point('validate_metallurgy'); workflow.add_edge('validate_metallurgy', 'check_cad'); workflow.add_edge('check_cad', END); graph = workflow.compile()