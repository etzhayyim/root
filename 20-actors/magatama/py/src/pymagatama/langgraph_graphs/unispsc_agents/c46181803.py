from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EyeShieldState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(EyeShieldState); def validate_compliance(state: EyeShieldState): results = []; results.append('ANSI Z87.1 Check: Pass') if state['specs'].get('ansi_compliant') else results.append('ANSI Z87.1 Check: Fail'); return {'validation_results': results}; graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()