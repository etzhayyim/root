from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RailState(TypedDict): items: List[dict]; validation_status: bool; def validate_specs(state: RailState): state['validation_status'] = all(['load_capacity' in i for i in state['items']]); return state; graph = StateGraph(RailState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()