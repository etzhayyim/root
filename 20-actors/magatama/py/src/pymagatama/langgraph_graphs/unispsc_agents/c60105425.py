from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): materials_list: List[str]; validation_results: dict; graph = StateGraph(State); def validate_material(state: State): return {'validation_results': {'status': 'approved'}}; graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()