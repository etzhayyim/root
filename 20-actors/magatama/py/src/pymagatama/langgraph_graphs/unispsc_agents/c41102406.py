from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class HotplateState(TypedDict): specs: dict; validation_results: list[str]; def validate_specs(state: HotplateState): results = []; if state['specs'].get('temp_range', 0) > 1000: results.append('Check safety margin'); return {'validation_results': results}; def finalize_procurement(state: HotplateState): return {'validation_results': state['validation_results'] + ['Verified']}; graph = StateGraph(HotplateState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()