from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(ProcureState); def validate_material(state): return {'validation_results': {'material_grade': 'Waspalloy-Verified'}}; def verify_welds(state): return {'validation_results': {'weld_integrity': 'PASSED'}}; graph.add_node('material_check', validate_material); graph.add_node('weld_inspection', verify_welds); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'weld_inspection'); graph.add_edge('weld_inspection', END); graph = graph.compile()