from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SalonSmockState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SalonSmockState): errors = []; if 'fabric' not in state['specs']: errors.append('Missing fabric details'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(SalonSmockState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()