from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input_data: dict; validation_logs: Annotated[list[str], operator.add]; status: str; def validate_developer_specs(state: ProcureState): logs = []; spec = state['input_data']; if 'particle_size' not in spec: logs.append('Error: Missing particle size requirement'); return {'validation_logs': logs, 'status': 'VALIDATED' if not logs else 'FAILED'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_developer_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()