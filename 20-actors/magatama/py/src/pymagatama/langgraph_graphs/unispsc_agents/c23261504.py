from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserMachineState(TypedDict): device_id: str; specs: dict; validation_errors: List[str]; def validate_laser_params(state: LaserMachineState): if state['specs'].get('laser_power', 0) < 500: state['validation_errors'].append('Insufficient laser power for industrial application'); return state; def check_export_compliance(state: LaserMachineState): if state['specs'].get('dual_use_status', True): print('Flagging as export controlled'); return state; graph = StateGraph(LaserMachineState); graph.add_node('validate', validate_laser_params); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()