from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CertState(TypedDict): certificate_id: str; validator_notes: List[str]; validated: bool; def validate_cert(state: CertState): state['validated'] = True if state['certificate_id'] else False; return state; graph = StateGraph(CertState); graph.add_node('validator', validate_cert); graph.set_entry_point('validator'); graph.add_edge('validator', END); compiled_graph = graph.compile()