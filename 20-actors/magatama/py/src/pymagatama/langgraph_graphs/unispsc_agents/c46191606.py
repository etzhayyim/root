from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FoamState(TypedDict): chemical_composition: str; compliance_certs: List[str]; approval_status: bool; def validate_spec(state: FoamState): return {'approval_status': 'PFAS_FREE' in state['chemical_composition']}; graph = StateGraph(FoamState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()