from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): model_ref: str; spec_valid: bool; certification_check: bool; def check_compatibility(state: State) -> State: state['spec_valid'] = bool(state.get('model_ref')); return state; def verify_cert(state: State) -> State: state['certification_check'] = True; return state; graph = StateGraph(State); graph.add_node('compatibility', check_compatibility); graph.add_node('certification', verify_cert); graph.set_entry_point('compatibility'); graph.add_edge('compatibility', 'certification'); graph.add_edge('certification', END); graph = graph.compile()