from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserState(TypedDict): vendor_id: str; specs: dict; validation_passed: bool; def validate_specs(state: LaserState): state['validation_passed'] = state['specs'].get('power', 0) > 0; return state; graph = StateGraph(LaserState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()