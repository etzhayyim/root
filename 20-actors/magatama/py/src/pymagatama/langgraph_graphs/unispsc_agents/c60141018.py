from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PomState(TypedDict): items: List[str]; validated: bool; defect_rate: float; def validate_quality(state: PomState): return {'validated': True, 'defect_rate': 0.01}; def check_compliance(state: PomState): return {'validated': True}; workflow = StateGraph(PomState); workflow.add_node('quality_check', validate_quality); workflow.add_node('compliance_check', check_compliance); workflow.set_entry_point('quality_check'); workflow.add_edge('quality_check', 'compliance_check'); workflow.add_edge('compliance_check', END); graph = workflow.compile()