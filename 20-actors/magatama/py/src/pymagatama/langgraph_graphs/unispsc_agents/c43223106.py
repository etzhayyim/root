from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): items: Annotated[list, operator.add]; status: str; def validate_spec(state: NetworkState): return {'status': 'validated'} if state['items'] else {'status': 'missing_specs'}; def route_procurement(state: NetworkState): return 'compliance_check'; graph = StateGraph(NetworkState); graph.add_node('validation', validate_spec); graph.add_edge('validation', END); graph.set_entry_point('validation'); graph = graph.compile()