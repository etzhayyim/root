from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WaterProcurementState(TypedDict): items: List[str]; qc_verified: bool; source_validated: bool; def validate_quality(state: WaterProcurementState): return {'qc_verified': True}; def validate_source(state: WaterProcurementState): return {'source_validated': True}; graph = StateGraph(WaterProcurementState); graph.add_node('qc', validate_quality); graph.add_node('source', validate_source); graph.add_edge('qc', 'source'); graph.add_edge('source', END); graph.set_entry_point('qc'); graph = graph.compile()