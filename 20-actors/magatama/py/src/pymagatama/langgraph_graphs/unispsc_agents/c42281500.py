from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AutoclaveState(TypedDict): spec_data: dict; validation_report: List[str]; approved: bool; def validate_pressure_vessel(state: AutoclaveState): spec = state['spec_data']; errors = []; if spec.get('pressure_rating', 0) < 200: errors.append('Pressure rating below safety threshold'); state['validation_report'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(AutoclaveState); graph.add_node('validate', validate_pressure_vessel); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()