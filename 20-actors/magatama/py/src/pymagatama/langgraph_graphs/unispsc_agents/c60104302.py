from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AstronomyState(TypedDict): charts: list; validation_report: list; def validate_chart(state: AstronomyState): findings = []; for chart in state['charts']: findings.append(f'Validating {chart}: Projection format check') if 'Projection' in chart else None; return {'validation_report': findings}; def finalize_procurement(state: AstronomyState): return {'validation_report': state['validation_report'] + ['Procurement ready']}; graph = StateGraph(AstronomyState); graph.add_node('validate', validate_chart); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()