from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BillState(TypedDict): bill_data: dict; validation_errors: List[str]; approved: bool; def validate_bill(state: BillState): errors = []; if 'amount' not in state['bill_data']: errors.append('Missing amount'); return {'validation_errors': errors}; def process_approval(state: BillState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(BillState); graph.add_node('validate', validate_bill); graph.add_node('approve', process_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()