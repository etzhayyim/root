from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TitanState(TypedDict): specs: dict; validation_report: str; approved: bool; def val_specs(state: TitanState): return {'validation_report': 'Validated against ASTM B367', 'approved': True}; graph = StateGraph(TitanState); graph.add_node('validate', val_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()