from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): supplier_docs: List[str]; validation_results: dict; status: str; def validate_purity(state: ProcurementState): state['validation_results'] = {'purity': 'passed' if 'COA' in state['supplier_docs'] else 'failed'}; return state; def check_compliance(state: ProcurementState): state['status'] = 'COMPLIANT' if state['validation_results'].get('purity') == 'passed' else 'REJECTED'; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()