from langgraph.graph import StateGraph, END; from typing import TypedDict; class AnalyzerState(TypedDict): specs: dict; valid: bool; def validate_specs(state: AnalyzerState): state['valid'] = all(k in state['specs'] for k in ['throughput', 'safety_cert']); return state; graph = StateGraph(AnalyzerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()