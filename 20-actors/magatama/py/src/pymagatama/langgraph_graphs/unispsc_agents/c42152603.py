from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): doc_id: str; specs: dict; verified: bool; def validate_specs(state: DentalState): state['verified'] = 'Material Grade' in state['specs'] and 'Biocompatibility Certification' in state['specs']; return state; workflow = StateGraph(DentalState); workflow.add_node('validate', validate_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()