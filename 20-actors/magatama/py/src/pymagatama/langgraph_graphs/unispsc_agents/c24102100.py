from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WarehouseState(TypedDict): equipment_list: List[str]; validation_results: List[str]; def validate_specs(state: WarehouseState): return {'validation_results': ['Specs verified']}; def check_safety(state: WarehouseState): return {'validation_results': state['validation_results'] + ['Safety compliance confirmed']}; graph = StateGraph(WarehouseState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()