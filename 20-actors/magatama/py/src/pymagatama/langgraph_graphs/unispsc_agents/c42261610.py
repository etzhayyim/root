from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): items: List[str]; validated: bool; state: str; graph = StateGraph(SupplyState); def validate_spec(state: SupplyState): return {'validated': True, 'state': 'BIO_HAZARD_CHECK'}; def check_bio(state: SupplyState): return {'state': 'CLEARED_FOR_PROCUREMENT'}; graph.add_node('validate', validate_spec); graph.add_node('bio_check', check_bio); graph.add_edge('validate', 'bio_check'); graph.add_edge('bio_check', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()