from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ContactLensToolState(TypedDict): items: List[dict]; inspection_passed: bool; def validate_specs(state: ContactLensToolState) -> ContactLensToolState: state['inspection_passed'] = all(item.get('sterilized') for item in state['items']); return state; def check_compliance(state: ContactLensToolState) -> str: return 'pass' if state['inspection_passed'] else 'fail'; graph = StateGraph(ContactLensToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()