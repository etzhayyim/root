from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SoftwareState(TypedDict): vendor: str; license_model: str; compliance_status: bool; def validate_license(state: SoftwareState): return {'compliance_status': state['license_model'] in ['SaaS', 'Perpetual']}; graph = StateGraph(SoftwareState); graph.add_node('validate', validate_license); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()