from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): assay_report: str; compliance_check: bool; item_name: str; def validate_purity(state: DrugState): return {'compliance_check': 'purity' in state['assay_report'].lower()}; def finalize_procurement(state: DrugState): return {'compliance_check': True}; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('final', finalize_procurement); graph.add_edge('validate', 'final'); graph.add_edge('final', END); graph.set_entry_point('validate')