from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): medicine_name: str; dosage: str; compliance_checked: bool; status: str; def validate_regulatory(state: DrugProcurementState): state['compliance_checked'] = True; return {'status': 'VALIDATED'}; def finalize_procurement(state: DrugProcurementState): return {'status': 'READY_FOR_SHIPMENT'}; graph = StateGraph(DrugProcurementState); graph.add_node('validate', validate_regulatory); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()