from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class PumpState(TypedDict): specs: dict; validation_passed: bool; graph = StateGraph(PumpState); def validate_specs(state: PumpState): return { "validation_passed": state["specs"].get("pumping_speed", 0) > 0 }; def check_compliance(state: PumpState): return { "validation_passed": state["validation_passed"] and state["specs"].get("compliance") is not None }; graph.add_node("validate", validate_specs); graph.add_node("compliance", check_compliance); graph.set_entry_point("validate"); graph.add_edge("validate", "compliance"); graph.add_edge("compliance", END); graph = graph.compile()