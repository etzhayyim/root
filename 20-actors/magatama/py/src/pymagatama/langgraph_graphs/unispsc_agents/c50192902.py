from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PastaState(TypedDict): items: List[str]; qc_passed: bool; def validate_quality(state: PastaState): return {'qc_passed': all(item.startswith('Valid') for item in state['items'])}; def finalize_order(state: PastaState): print('Order finalized'); return {'qc_passed': True}; graph = StateGraph(PastaState); graph.add_node('validate', validate_quality); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', lambda s: 'finalize' if s['qc_passed'] else END, {'finalize': 'finalize'}); graph.add_edge('finalize', END); app = graph.compile()