from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): content_type: str; specs: dict; compliance_check: bool; def validate_materials(state: ProcState): state['compliance_check'] = state['specs'].get('author') is not None; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_materials); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()