from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CeramicMoldState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: CeramicMoldState): state['validation_passed'] = 'ThermalExpansionCoefficient' in state['specs'] and state['specs']['DimensionalToleranceGrade'] != 'None'; return state; graph = StateGraph(CeramicMoldState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()