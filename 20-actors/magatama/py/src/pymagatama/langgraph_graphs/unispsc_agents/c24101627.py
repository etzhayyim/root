from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TrolleyState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: TrolleyState): s = state['specs']; state['valid'] = s.get('load_capacity', 0) > 0 and s.get('beam_width') is not None; return state; graph = StateGraph(TrolleyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()