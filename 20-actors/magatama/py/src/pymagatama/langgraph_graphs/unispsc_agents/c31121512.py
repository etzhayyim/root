from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_alloy(state: CastingState): state['validation_results'].append('Alloy checked'); return state; def inspect_dimensions(state: CastingState): state['validation_results'].append('Dimensions verified'); return state; graph = StateGraph(CastingState); graph.add_node('alloy', validate_alloy); graph.add_node('dims', inspect_dimensions); graph.add_edge('alloy', 'dims'); graph.add_edge('dims', END); graph.set_entry_point('alloy'); graph = graph.compile()