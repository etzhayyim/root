from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class InstrumentState(TypedDict): items: List[str]; validated: bool; state: str; graph = StateGraph(InstrumentState); def validate_instruments(state: InstrumentState): return {'validated': all(item for item in state['items']), 'state': 'validated'}; def finalize_procurement(state: InstrumentState): return {'state': 'ready_for_order'}; graph.add_node('validate', validate_instruments); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()