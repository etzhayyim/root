from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_name: str; grade: str; compliance_docs: List[str]; approved: bool; def check_gmp(state: ProcureState): state['approved'] = 'GMP_CERT' in state['compliance_docs']; return state; def validate_purity(state: ProcureState): return {'approved': state['approved'] and state['grade'] == 'API'}; graph = StateGraph(ProcureState); graph.add_node('check_gmp', check_gmp); graph.add_node('validate_purity', validate_purity); graph.set_entry_point('check_gmp'); graph.add_edge('check_gmp', 'validate_purity'); graph.add_edge('validate_purity', END); graph = graph.compile()