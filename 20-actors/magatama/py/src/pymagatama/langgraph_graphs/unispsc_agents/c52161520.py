from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MicrophoneState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: MicrophoneState): print('Validating microphone specs...'); state['compliance'] = 'frequency_response' in state['specs']; return state; def check_export(state: MicrophoneState): print('Checking dual-use export status...'); state['approved'] = True; return state; graph = StateGraph(MicrophoneState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()