from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmbalmingState(TypedDict): items: List[str]; validation_results: dict; def validate_kit(state: EmbalmingState): return {'validation_results': {'status': 'verified', 'hazmat_check': True}}; def process_procurement(state: EmbalmingState): return {'validation_results': {'status': 'processed'}}; graph = StateGraph(EmbalmingState); graph.add_node('validate', validate_kit); graph.add_node('process', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.compile()