from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class LabelMachineState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: LabelMachineState): state['validation_passed'] = all(k in state['specs'] for k in ['precision', 'speed']); return {'validation_passed': state['validation_passed']}; graph = StateGraph(LabelMachineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()