from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InstrumentState(TypedDict): model: str; quality_score: float; needs_tuning: bool; def check_quality(state: InstrumentState): return {'quality_score': 0.95, 'needs_tuning': False}; def validate_specs(state: InstrumentState): return {'needs_tuning': state['model'] == 'vint'}; graph = StateGraph(InstrumentState); graph.add_node('check', check_quality); graph.add_node('validate', validate_specs); graph.add_edge('check', 'validate'); graph.add_edge('validate', END); graph.set_entry_point('check'); graph = graph.compile()