from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BerylliumState(TypedDict): component_id: str; specifications: dict; compliance_status: bool; def check_export_controls(state: BerylliumState): state['compliance_status'] = True; return state; def validate_specifications(state: BerylliumState): print(f'Validating specs for {state['component_id']}'); return state; graph = StateGraph(BerylliumState); graph.add_node('export_check', check_export_controls); graph.add_node('spec_validation', validate_specifications); graph.set_entry_point('export_check'); graph.add_edge('export_check', 'spec_validation'); graph.add_edge('spec_validation', END); graph = graph.compile()