from langgraph.graph import StateGraph, END; from typing import TypedDict; class SaladState(TypedDict): model_name: str; is_food_grade: bool; rotation_check: bool; def validate_compliance(state: SaladState): return {'is_food_grade': True}; def validate_mechanism(state: SaladState): return {'rotation_check': True}; graph = StateGraph(SaladState); graph.add_node('compliance', validate_compliance); graph.add_node('mechanism', validate_mechanism); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'mechanism'); graph.add_edge('mechanism', END); compiled_graph = graph.compile()