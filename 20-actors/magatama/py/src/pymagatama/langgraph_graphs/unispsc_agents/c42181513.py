from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; status: str; def validate_medical_compliance(state: State): status = 'compliant' if all(i for i in state['items']) else 'non-compliant'; return {'status': status}; graph = StateGraph(State); graph.add_node('validate', validate_medical_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()