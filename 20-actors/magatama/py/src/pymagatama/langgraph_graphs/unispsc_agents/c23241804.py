from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[str]; status: str; def validate_cnc(state: State): state['status'] = 'validated' if 'cnc_spec' in state['items'] else 'rejected'; return state; graph = StateGraph(State); graph.add_node('validate', validate_cnc); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()