from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EyePadState(TypedDict): vendor_certs: List[str]; qc_passed: bool; inspection_report: str; def validate_compliance(state: EyePadState): return {'qc_passed': 'ISO-13485' in state['vendor_certs']}; def route_procurement(state: EyePadState): return 'approval' if state['qc_passed'] else END; graph = StateGraph(EyePadState); graph.add_node('validate', validate_compliance); graph.add_node('approval', lambda x: {'inspection_report': 'Verified compliant'}); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_procurement); graph.add_edge('approval', END); graph = graph.compile()