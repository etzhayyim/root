from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RetinoscopeState(TypedDict): data: dict; status: str; def validate_specs(state: RetinoscopeState): return {'status': 'validated' if 'calibration_cert' in state['data'] else 'rejected'}; def finalize_order(state: RetinoscopeState): return {'status': 'approved'}; graph = StateGraph(RetinoscopeState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()