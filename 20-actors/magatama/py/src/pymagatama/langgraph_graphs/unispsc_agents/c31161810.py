from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShimState(TypedDict): thickness: float; material: str; validated: bool; def val_check(state: ShimState): return {'validated': 0.05 <= state['thickness'] <= 10.0}; def mat_check(state: ShimState): return {'validated': state['material'] in ['Steel', 'Stainless', 'Brass']}; graph = StateGraph(ShimState); graph.add_node('val_check', val_check); graph.add_node('mat_check', mat_check); graph.set_entry_point('val_check'); graph.add_edge('val_check', 'mat_check'); graph.add_edge('mat_check', END); graph = graph.compile()