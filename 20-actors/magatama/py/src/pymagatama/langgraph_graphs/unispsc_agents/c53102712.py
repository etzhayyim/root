from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): procurement_data: dict; approval_required: bool; def validate_spec(state: State): state['approval_required'] = 'EN471' not in state['procurement_data'].get('standards', []); return state; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()