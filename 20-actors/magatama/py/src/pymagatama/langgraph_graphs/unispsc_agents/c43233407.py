from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DriverProcessState(TypedDict): gpu_model: str; driver_version: str; compatibility_validated: bool; error_logs: List[str]; def validate_compatibility(state: DriverProcessState): state['compatibility_validated'] = state['gpu_model'] != ''; return state; def check_compliance(state: DriverProcessState): if not state['compatibility_validated']: state['error_logs'].append('Incompatible Hardware'); return state; graph = StateGraph(DriverProcessState); graph.add_node('validate', validate_compatibility); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()