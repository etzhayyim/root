from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TableState(TypedDict): specs: dict; valid: bool; "Initializes the simulation environment"; def validate_table(state: TableState): state['valid'] = 'dimensions' in state['specs'] and 'material' in state['specs']; return state; builder = StateGraph(TableState); builder.add_node('validate', validate_table); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()