from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CanvasState(TypedDict): canvas_specs: dict; validation_results: List[str]; def validate_canvas(state: CanvasState): specs = state.get('canvas_specs', {}); results = []; if 'weight' not in specs: results.append('Missing gsm weight'); return {'validation_results': results}; graph = StateGraph(CanvasState); graph.add_node('validate', validate_canvas); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()