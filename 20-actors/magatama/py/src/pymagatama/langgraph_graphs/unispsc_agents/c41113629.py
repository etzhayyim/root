from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): input_data: dict; status: str; errors: List[str]; def validate_meter(state: State): specs = state['input_data']; return {'status': 'PASS' if specs.get('calibration_valid') else 'FAIL'}; graph = StateGraph(State); graph.add_node('validate', validate_meter); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()