from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TitaniumState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(TitaniumState); def validate_specs(state: TitaniumState): return {'validation_results': ['Material check passed', 'Dimensional check passed']}; def perform_ndt(state: TitaniumState): return {'validation_results': state['validation_results'] + ['NDT inspection complete']}; graph.add_node('validate', validate_specs); graph.add_node('ndt', perform_ndt); graph.set_entry_point('validate'); graph.add_edge('validate', 'ndt'); graph.add_edge('ndt', END); graph.compile()