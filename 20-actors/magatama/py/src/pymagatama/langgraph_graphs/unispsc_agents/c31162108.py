from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): anchor_specs: dict; validation: bool; def validate_load(state: State): state['validation'] = state['anchor_specs'].get('load', 0) > 0; return state; graph = StateGraph(State); graph.add_node('validate', validate_load); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()