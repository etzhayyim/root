from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GPSState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; compliant: bool; def validate_accuracy(state: GPSState): accuracy = state['specs'].get('accuracy', 0); is_valid = accuracy <= 5.0; return {'validation_results': [f'Accuracy Check: {accuracy}m'], 'compliant': is_valid}; def export_control_check(state: GPSState): return {'validation_results': ['Export control documentation verified']}; graph = StateGraph(GPSState); graph.add_node('validate_accuracy', validate_accuracy); graph.add_node('export_check', export_control_check); graph.set_entry_point('validate_accuracy'); graph.add_edge('validate_accuracy', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()