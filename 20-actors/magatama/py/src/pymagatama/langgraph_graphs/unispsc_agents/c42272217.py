from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VentilatorState(TypedDict): vendor_certs: List[str]; qc_passed: bool; circuit_compatibility: bool; def validate_cert(state: VentilatorState): state['qc_passed'] = 'ISO_13485' in state['vendor_certs']; return state; def check_compatibility(state: VentilatorState): state['circuit_compatibility'] = True; return state; graph = StateGraph(VentilatorState); graph.add_node('validate_cert', validate_cert); graph.add_node('check_compatibility', check_compatibility); graph.set_entry_point('validate_cert'); graph.add_edge('validate_cert', 'check_compatibility'); graph.add_edge('check_compatibility', END); graph = graph.compile()