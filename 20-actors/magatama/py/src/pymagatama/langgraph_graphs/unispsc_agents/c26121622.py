from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableState(TypedDict): specs: dict; valid: bool; cable_type: str; def validate_polyimide(state: CableState): temp = state['specs'].get('thermal_rating_celsius', 0); state['valid'] = temp >= 200; return state; graph = StateGraph(CableState); graph.add_node('validate', validate_polyimide); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()