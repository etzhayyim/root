from langgraph.graph import StateGraph, END; from typing import TypedDict; class CastingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: CastingState): state['valid'] = 'tolerance' in state['specs'] and 'material' in state['specs']; return state; def check_quality(state: CastingState): return 'valid' if state['valid'] else 'error'; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()