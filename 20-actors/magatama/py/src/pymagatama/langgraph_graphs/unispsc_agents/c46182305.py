from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LanyardState(TypedDict): specs: dict; compliance_verified: bool; def validate_specs(state: LanyardState): state['compliance_verified'] = 'ANSI_Z359_Compliance' in state['specs'] and state['specs']['Breaking_Strength_kN'] > 15; return state; graph = StateGraph(LanyardState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()