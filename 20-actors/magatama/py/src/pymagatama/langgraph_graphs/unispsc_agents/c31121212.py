from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: CastingState): state['valid'] = 'Alloy Composition' in state['specs'] and state['specs']['Alloy Composition'] == 'ASTM B'; state['logs'].append('Validation complete.'); return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()