from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HeatingState(TypedDict): stove_spec: dict; compliance_check: bool; def validate_specs(state: HeatingState): state['compliance_check'] = state['stove_spec'].get('thermal_output_kw', 0) > 0; return state; def finalize_order(state: HeatingState): return {'status': 'approved' if state['compliance_check'] else 'rejected'}; graph = StateGraph(HeatingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()