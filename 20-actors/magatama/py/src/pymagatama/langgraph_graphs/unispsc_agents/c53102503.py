from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HatState(TypedDict): vendor: str; specs: dict; compliant: bool; def validate_specs(state: HatState): state['compliant'] = all(k in state['specs'] for k in ['material', 'size']); return state; def finalize_order(state: HatState): print('Processing order for hats'); return state; graph = StateGraph(HatState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()