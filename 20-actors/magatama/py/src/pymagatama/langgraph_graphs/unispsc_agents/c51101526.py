from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_id: str; purity_check: bool; temp_log: List[float]; valid: bool; def check_purity(state: ReagentState): state['purity_check'] = True; return state; def validate_temp(state: ReagentState): state['valid'] = all(t < -20.0 for t in state['temp_log']); return state; graph = StateGraph(ReagentState); graph.add_node('check_purity', check_purity); graph.add_node('validate_temp', validate_temp); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'validate_temp'); graph.add_edge('validate_temp', END); graph = graph.compile()