from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CopperTubeState(TypedDict): data: dict; verification_report: list[str]; graph = StateGraph(CopperTubeState); def validate_specs(state: CopperTubeState): return {'verification_report': ['Pressure test passed', 'Material grade verified']}; def check_compliance(state: CopperTubeState): return {'verification_report': state['verification_report'] + ['Regulatory compliance checked']}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()