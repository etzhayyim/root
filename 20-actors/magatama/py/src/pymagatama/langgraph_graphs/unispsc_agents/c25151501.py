from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpacecraftState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def validate_life_support(state: SpacecraftState): state['validation_results'].append('Life support systems verified'); return state; def check_export_controls(state: SpacecraftState): state['validation_results'].append('ITAR compliance checked'); return state; graph = StateGraph(SpacecraftState); graph.add_node('life_support', validate_life_support); graph.add_node('export_controls', check_export_controls); graph.set_entry_point('life_support'); graph.add_edge('life_support', 'export_controls'); graph.add_edge('export_controls', END); graph = graph.compile()