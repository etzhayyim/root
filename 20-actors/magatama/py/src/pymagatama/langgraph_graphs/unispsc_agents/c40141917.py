from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DuctState(TypedDict): content: str; specs: List[str]; validated: bool; def validate_stone_spec(state: DuctState): state['validated'] = all(s in state['specs'] for s in ['fire_rating', 'airtightness']); return state; def route_step(state: DuctState): return 'validate' if state.get('specs') else END; graph = StateGraph(DuctState); graph.add_node('validate', validate_stone_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()