from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BucketState(TypedDict): specs: dict; valid: bool; def validate_bucket(state: BucketState): state['valid'] = all(k in state['specs'] for k in ['material', 'capacity']); return state; graph = StateGraph(BucketState); graph.add_node('validate', validate_bucket); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()