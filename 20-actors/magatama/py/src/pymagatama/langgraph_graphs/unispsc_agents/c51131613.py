from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): chemical_name: str; analysis_result: str; is_compliant: bool; def validate_purity(state: SupplyState): state["is_compliant"] = "purity" in state["analysis_result"].lower(); return state; def check_regulatory(state: SupplyState): return state; graph = StateGraph(SupplyState); graph.add_node("validate", validate_purity); graph.add_node("regulatory", check_regulatory); graph.set_entry_point("validate"); graph.add_edge("validate", "regulatory"); graph.add_edge("regulatory", END); graph = graph.compile()