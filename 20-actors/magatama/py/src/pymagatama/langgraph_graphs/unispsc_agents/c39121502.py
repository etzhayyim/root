from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DimmerState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: DimmerState): specs = state['specs']; errors = []; if specs.get('voltage_rating_v') not in [110, 220, 240]: errors.append('Invalid voltage'); state['valid'] = len(errors) == 0; state['error_log'] = errors; return state; graph = StateGraph(DimmerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()