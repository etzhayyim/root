from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class SilaneState(TypedDict): chemical_data: dict; validation_report: list[str]; def validate_purity(state: SilaneState): data = state['chemical_data']; status = 'PASS' if data.get('purity', 0) >= 99.9 else 'FAIL'; return {'validation_report': [f'Purity check: {status}']}; def compliance_check(state: SilaneState): return {'validation_report': ['Export control check: COMPLIANT']}; graph = StateGraph(SilaneState); graph.add_node('purity_check', validate_purity); graph.add_node('compliance_check', compliance_check); graph.add_edge('purity_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph.set_entry_point('purity_check'); graph = graph.compile()