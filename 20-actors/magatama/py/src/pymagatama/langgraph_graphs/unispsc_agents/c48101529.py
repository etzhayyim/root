from langgraph.graph import StateGraph, END; from typing import TypedDict; class PressureCookerState(TypedDict): specs: dict; safety_check: bool; graph = StateGraph(PressureCookerState); def validate_safety(state): state['safety_check'] = state['specs'].get('pressure_rating', 0) > 0; return state; graph.add_node('safety_check', validate_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()