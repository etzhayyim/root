from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CastingState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_mold_specs(state: CastingState): density = state['specs'].get('density', 0); state['valid'] = density > 1.7; state['log'].append('Validation complete') if state['valid'] else state['log'].append('Density too low'); return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_mold_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()