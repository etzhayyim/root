from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecimenBagState(TypedDict): bag_type: str; sterility_verified: bool; integrity_test_passed: bool; def validate_spec(state: SpecimenBagState): return {'sterility_verified': True} if state['bag_type'] == 'sterile' else {'sterility_verified': False}; def check_integrity(state: SpecimenBagState): return {'integrity_test_passed': True}; graph = StateGraph(SpecimenBagState); graph.add_node('validate', validate_spec); graph.add_node('integrity', check_integrity); graph.set_entry_point('validate'); graph.add_edge('validate', 'integrity'); graph.add_edge('integrity', END); graph = graph.compile()