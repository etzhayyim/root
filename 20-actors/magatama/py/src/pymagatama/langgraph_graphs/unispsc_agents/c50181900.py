from langgraph.graph import StateGraph, END; from typing import TypedDict; class BakeryState(TypedDict): items: list; valid: bool; def validate_bakery(state: BakeryState): state['valid'] = all('shelf_life' in item for item in state['items']); return state; graph = StateGraph(BakeryState); graph.add_node('validate', validate_bakery); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()