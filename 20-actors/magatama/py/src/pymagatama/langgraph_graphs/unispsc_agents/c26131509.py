from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WindPlantState(TypedDict): vendor_docs: List[str]; compliance_checks: List[str]; def validate_specs(state: WindPlantState): return {'compliance_checks': ['GRID_COMPLIANCE_PASSED' if 'grid_cert' in state['vendor_docs'] else 'GRID_FAIL']}; def review_safety(state: WindPlantState): return {'compliance_checks': state['compliance_checks'] + ['SAFETY_REVIEW_COMPLETE']}; graph = StateGraph(WindPlantState); graph.add_node('validate', validate_specs); graph.add_node('safety', review_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()