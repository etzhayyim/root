from langgraph.graph import StateGraph, END; from typing import TypedDict; class FlowmeterState(TypedDict): device_id: str; data: dict; status: str; def validate_sensor(state: FlowmeterState): state['status'] = 'validated' if state['data'].get('accuracy') > 0.9 else 'rejected'; return state; def prepare_report(state: FlowmeterState): state['status'] = 'report_generated'; return state; graph = StateGraph(FlowmeterState); graph.add_node('validate', validate_sensor); graph.add_node('report', prepare_report); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()