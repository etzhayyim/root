from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MapSpecState(TypedDict): content: str; validated: bool; errors: List[str]; def validate_map_data(state: MapSpecState): is_valid = 'errors' not in state['content'].lower(); return {'validated': is_valid}; def finalize_procurement(state: MapSpecState): return {'content': 'Finalized' if state['validated'] else 'Rejected'}; graph = StateGraph(MapSpecState); graph.add_node('validate', validate_map_data); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()