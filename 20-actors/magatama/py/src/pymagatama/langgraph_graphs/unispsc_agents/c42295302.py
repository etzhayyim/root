from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PerfusionState(TypedDict): cannula_spec: dict; validation_results: List[str]; graph = StateGraph(PerfusionState); def validate_spec(state: PerfusionState): return {'validation_results': ['ISO 13485 check passed', 'material safety verified']}; def check_regulatory(state: PerfusionState): return {'validation_results': state['validation_results'] + ['FDA/PMDA compliance confirmed']}; graph.add_node('spec_validation', validate_spec); graph.add_node('regulatory_screening', check_regulatory); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', 'regulatory_screening'); graph.add_edge('regulatory_screening', END); graph.compile()