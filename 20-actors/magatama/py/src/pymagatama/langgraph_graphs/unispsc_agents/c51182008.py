from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; compliance_checked: bool; assay_status: str; def check_compliance(state: PharmState): return {'compliance_checked': True}; def validate_assay(state: PharmState): return {'assay_status': 'verified'}; graph = StateGraph(PharmState); graph.add_node('compliance', check_compliance); graph.add_node('assay', validate_assay); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'assay'); graph.add_edge('assay', END); graph_compiled = graph.compile()