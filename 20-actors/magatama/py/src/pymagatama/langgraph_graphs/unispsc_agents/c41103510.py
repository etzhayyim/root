from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LabBlowerState(TypedDict): specs: dict; validation_report: list[str]; def validate_specs(state: LabBlowerState): specs = state['specs']; errors = []; if not specs.get('airflow'): errors.append('Missing airflow capacity'); return {'validation_report': errors}; graph = StateGraph(LabBlowerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()