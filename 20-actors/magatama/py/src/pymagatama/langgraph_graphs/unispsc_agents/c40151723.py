from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SumpPumpState(TypedDict): model_number: str; parts_required: List[str]; validation_status: bool; def validate_parts(state: SumpPumpState): state['validation_status'] = bool(state['model_number'] and state['parts_required']); return state; graph = StateGraph(SumpPumpState); graph.add_node('validate', validate_parts); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()