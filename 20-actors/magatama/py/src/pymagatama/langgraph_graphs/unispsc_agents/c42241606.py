from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): tray_specs: dict; validation_report: str; def validate_specs(state: State): specs = state['tray_specs']; result = 'Pass' if 'material' in specs and 'sterilization' in specs else 'Fail'; return {'validation_report': result}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()