from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): controller_specs: dict; validation_results: list; status: str; def validate_controller(state: AgentState): specs = state['controller_specs']; results = ['Validated voltage' if specs.get('voltage') == 24 else 'Voltage mismatch']; return {'validation_results': results}; def graph_builder(): graph = StateGraph(AgentState); graph.add_node('validate', validate_controller); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = graph_builder()