from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_spec(state: CompressorState): errors = []; if state['specs'].get('compression_ratio', 0) <= 0: errors.append('Invalid compression ratio'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(CompressorState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()