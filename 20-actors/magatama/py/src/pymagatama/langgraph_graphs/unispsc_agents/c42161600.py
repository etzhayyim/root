from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialysisState(TypedDict): doc_id: str; compliance_checked: bool; supply_chain_verified: bool; def check_medical_compliance(state: DialysisState): state['compliance_checked'] = True; return state; def verify_supply_chain(state: DialysisState): state['supply_chain_verified'] = True; return state; graph = StateGraph(DialysisState); graph.add_node('compliance', check_medical_compliance); graph.add_node('supply', verify_supply_chain); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'supply'); graph.add_edge('supply', END); graph = graph.compile()