from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ApparelState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; def validate_spec(state: ApparelState): results = []; if 'material_composition' not in state['specs']: results.append('Missing material composition'); return {'validation_results': results}; def check_compliance(state: ApparelState): return {'validation_results': ['Compliance Verified']}; graph = StateGraph(ApparelState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()