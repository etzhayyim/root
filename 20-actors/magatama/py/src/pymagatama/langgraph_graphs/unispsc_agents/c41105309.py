from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_id: str; purity: float; ready_for_storage: bool; def validate_purity(state: ReagentState): return { 'ready_for_storage': state['purity'] >= 0.99 }; def check_temp(state: ReagentState): return { 'ready_for_storage': False } if state['purity'] < 0.95 else state; graph = StateGraph(ReagentState); graph.add_node('validate', validate_purity); graph.add_node('thermal_check', check_temp); graph.set_entry_point('validate'); graph.add_edge('validate', 'thermal_check'); graph.add_edge('thermal_check', END); graph = graph.compile()