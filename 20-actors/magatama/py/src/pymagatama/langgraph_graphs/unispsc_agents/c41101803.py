from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoulometerState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(CoulometerState); def validate_specs(state: CoulometerState): state['validation_report'].append('Checking calibration requirements'); return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()