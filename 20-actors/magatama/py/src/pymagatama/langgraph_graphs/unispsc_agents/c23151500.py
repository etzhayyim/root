from langgraph.graph import StateGraph, END; from typing import TypedDict; class AutomationState(TypedDict): status: str; spec: dict; valid: bool; def validate_spec(state: AutomationState): state['valid'] = all(k in state['spec'] for k in ['payload', 'safety']); return state; def route(state: AutomationState): return 'end' if state['valid'] else 'review'; graph = StateGraph(AutomationState); graph.add_node('validate', validate_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()