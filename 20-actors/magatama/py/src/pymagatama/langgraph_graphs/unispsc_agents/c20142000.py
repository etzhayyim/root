from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AerospaceValveState(TypedDict): valve_id: str; compliance_docs: List[str]; validation_status: bool; def validate_compliance(state: AerospaceValveState): state['validation_status'] = len(state.get('compliance_docs', [])) > 2; return state; def final_approval(state: AerospaceValveState): return state; graph = StateGraph(AerospaceValveState); graph.add_node("validate", validate_compliance); graph.add_node("approve", final_approval); graph.set_entry_point("validate"); graph.add_edge("validate", "approve"); graph.add_edge("approve", END); graph = graph.compile()