from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftState(TypedDict): component_id: str; structural_integrity_passed: bool; export_compliance_checked: bool; def validate_engineering_specs(state: AircraftState): return {'structural_integrity_passed': True}; def check_export_controls(state: AircraftState): return {'export_compliance_checked': True}; graph = StateGraph(AircraftState); graph.add_node('validate', validate_engineering_specs); graph.add_node('compliance', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()