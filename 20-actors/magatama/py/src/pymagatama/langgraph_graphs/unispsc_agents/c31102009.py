from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BerylliumState(TypedDict): vendor_certs: List[str]; inspection_results: dict; validated: bool; def validate_compliance(state: BerylliumState): state['validated'] = 'ExportLicense' in state['vendor_certs']; return state; def check_quality(state: BerylliumState): state['inspection_results'] = {'passed': True}; return state; graph = StateGraph(BerylliumState); graph.add_node('compliance', validate_compliance); graph.add_node('quality', check_quality); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'quality'); graph.add_edge('quality', END); graph = graph.compile()