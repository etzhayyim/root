from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadiographicState(TypedDict): device_id: str; safety_checks: List[str]; compliant: bool; def validate_safety(state: RadiographicState): state['compliant'] = 'Radiation_Shielding' in state['safety_checks'] and 'IEC_Cert' in state['safety_checks']; return state; def report_status(state: RadiographicState): print(f'Device {state['device_id']} compliance: {state['compliant']}'); return state; graph = StateGraph(RadiographicState); graph.add_node('validate', validate_safety); graph.add_node('report', report_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()