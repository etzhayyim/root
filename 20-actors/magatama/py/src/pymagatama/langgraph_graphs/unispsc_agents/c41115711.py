from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ColumnState(TypedDict): specs: dict; validation_results: list[str]; def validate_column(state: ColumnState): result = 'Valid' if state['specs'].get('ParticleSizeMicrons') else 'Invalid'; return {'validation_results': [result]}; graph = StateGraph(ColumnState); graph.add_node('validate', validate_column); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()