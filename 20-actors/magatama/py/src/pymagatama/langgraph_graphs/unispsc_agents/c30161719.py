from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): spec: dict; validated: bool; def validate_specs(state: State): state['validated'] = 'thickness_mm' in state['spec'] and 'density_kg_m3' in state['spec']; return state; builder = StateGraph(State); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()