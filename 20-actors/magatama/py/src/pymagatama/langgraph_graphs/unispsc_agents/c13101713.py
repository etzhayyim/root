from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class ProcessingState(TypedDict): messages: Annotated[Sequence[str], add_messages]; purity: float; status: str; def validate_purity(state: ProcessingState) -> dict: return {'status': 'valid' if state['purity'] >= 98.0 else 'rejected'}; def compile_graph(): builder = StateGraph(ProcessingState); builder.add_node('validate', validate_purity); builder.set_entry_point('validate'); builder.add_edge('validate', END); return builder.compile(); graph = compile_graph()