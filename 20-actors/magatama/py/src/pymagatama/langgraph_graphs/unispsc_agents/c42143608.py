from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PatientSafetyState(TypedDict): device_specs: dict; compliance_check: bool; validation_log: List[str]; def validate_device_specs(state: PatientSafetyState): specs = state['device_specs']; is_valid = all(key in specs for key in ['weight_limit', 'biocompatibility']); return {'compliance_check': is_valid, 'validation_log': ['Specs vetted'] if is_valid else ['Specs incomplete']}; def finalize_approval(state: PatientSafetyState): return {'validation_log': state['validation_log'] + ['Approval complete']}; graph = StateGraph(PatientSafetyState); graph.add_node('validate', validate_device_specs); graph.add_node('approve', finalize_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()