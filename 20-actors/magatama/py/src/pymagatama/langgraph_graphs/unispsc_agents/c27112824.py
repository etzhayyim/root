from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChuckState(TypedDict): specs: dict; valid: bool; workflow_log: List[str]; def validate_specs(state: ChuckState): specs = state['specs']; state['valid'] = 'Taper Tolerance' in specs and specs['Hardness (HRC)'] > 50; state['workflow_log'].append('Validation complete'); return state; workflow = StateGraph(ChuckState); workflow.add_node('validate', validate_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()