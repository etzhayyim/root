from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SteelDieState(TypedDict): dimensions: dict; quality_report: str; approved: bool; def val_dim(state): return {'approved': state['dimensions'].get('tolerance', 0) < 0.05}; def gen_report(state): return {'quality_report': 'Certified'}; graph = StateGraph(SteelDieState); graph.add_node('ValidateDimensions', val_dim); graph.add_node('GenerateReport', gen_report); graph.set_entry_point('ValidateDimensions'); graph.add_edge('ValidateDimensions', 'GenerateReport'); graph.add_edge('GenerateReport', END); graph.compile()