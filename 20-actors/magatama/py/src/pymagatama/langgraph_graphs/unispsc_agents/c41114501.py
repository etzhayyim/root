from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DynoState(TypedDict): specs: dict; validation_results: List[str]; def validate_calibration(state: DynoState): state['validation_results'].append('Calibration cert verified'); return state; def check_torque_limits(state: DynoState): state['validation_results'].append('Capacity within safety limits'); return state; graph = StateGraph(DynoState); graph.add_node('calibrate', validate_calibration); graph.add_node('torque', check_torque_limits); graph.set_entry_point('calibrate'); graph.add_edge('calibrate', 'torque'); graph.add_edge('torque', END); graph = graph.compile()