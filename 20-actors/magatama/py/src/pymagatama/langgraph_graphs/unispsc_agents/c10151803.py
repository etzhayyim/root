from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class DrillState(TypedDict): input_data: dict; specs: dict; status: str; graph = StateGraph(DrillState); def validate_specs(state: DrillState): if 'tensile_strength_mpa' not in state['specs']: return {'status': 'MISSING_SPEC'}; return {'status': 'VALIDATED'}; def process_drilling_order(state: DrillState): return {'status': 'ORDER_APPROVED'}; graph.add_node('validate', validate_specs); graph.add_node('process', process_drilling_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.compile()