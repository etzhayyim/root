from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ICProcurementState(TypedDict): part_number: str; verified: bool; export_control_flag: bool; def check_export_status(state: ICProcurementState): state['export_control_flag'] = state['part_number'].startswith('ECCN'); return state; def validate_specs(state: ICProcurementState): state['verified'] = True; return state; graph = StateGraph(ICProcurementState); graph.add_node('check_export', check_export_status); graph.add_node('validate', validate_specs); graph.set_entry_point('check_export'); graph.add_edge('check_export', 'validate'); graph.add_edge('validate', END); graph = graph.compile()