from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TesterState(TypedDict): specs: dict; verified: bool; errors: List[str]; def validate_specs(state: TesterState): if 'CAT_rating' not in state['specs']: state['errors'].append('Missing CAT rating'); state['verified'] = False; return state; def check_compliance(state: TesterState): state['verified'] = True if not state['errors'] else False; return state; graph = StateGraph(TesterState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()