from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): structure_data: dict; validation_results: List[str]; def validate_material(state: AssemblyState): return {'validation_results': ['Material check passed']}; def validate_welds(state: AssemblyState): return {'validation_results': ['Weld integrity verified']}; graph = StateGraph(AssemblyState); graph.add_node('material', validate_material); graph.add_node('welds', validate_welds); graph.set_entry_point('material'); graph.add_edge('material', 'welds'); graph.add_edge('welds', END); graph = graph.compile()