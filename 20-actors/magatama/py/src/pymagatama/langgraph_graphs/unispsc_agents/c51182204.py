from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): input_data: dict; validation_errors: Annotated[list, operator.add]; compliance_status: bool; def validate_purity(state: DrugState): state['validation_errors'] = ['Low purity' if state['input_data'].get('purity', 0) < 99 else None]; return state; def check_compliance(state: DrugState): state['compliance_status'] = True if all(req in state['input_data'] for req in ['cert', 'temp']) else False; return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compile_graph = graph.compile()