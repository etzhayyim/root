from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GoserelinState(TypedDict): vendor_docs: List[str]; temp_compliance: bool; approved: bool; def validate_docs(state: GoserelinState): return {'approved': 'GMP' in state['vendor_docs'] and 'CoA' in state['vendor_docs']}; def check_cold_chain(state: GoserelinState): return {'temp_compliance': True}; graph = StateGraph(GoserelinState); graph.add_node('validate', validate_docs); graph.add_node('logistics', check_cold_chain); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph.set_entry_point('validate'); graph = graph.compile()