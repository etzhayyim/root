from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): material_spec: str; dimensions: dict; status: str; def validate_specs(state: ForgingState): state['status'] = 'validation_passed' if state.get('material_spec') else 'failed'; return state; def check_compliance(state: ForgingState): print('Checking export compliance...'); return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()