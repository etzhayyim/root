from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DividerState(TypedDict): items: Annotated[list, operator.add]; validation_results: dict; def validate_spec(state: DividerState): return {'validation_results': {'status': 'pass'}}; def finalize_ordering(state: DividerState): return {'validation_results': {'status': 'processed'}}; graph = StateGraph(DividerState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_ordering); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()