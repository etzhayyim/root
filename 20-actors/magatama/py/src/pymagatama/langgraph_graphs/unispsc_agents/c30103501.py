from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class CoreState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: CoreState): specs = state['specs']; errors = []; if not specs.get('alloy_grade'): errors.append('Missing alloy grade'); state.update({'validation_errors': errors, 'approved': len(errors) == 0}); return state; graph = StateGraph(CoreState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()