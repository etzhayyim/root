from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PhototherapyState(TypedDict): data: dict; specs: List[str]; validated: bool; def validate_specs(state: PhototherapyState): state['validated'] = all(k in state['data'] for k in ['wavelength', 'intensity']); return {'validated': state['validated']}; def device_check(state: PhototherapyState): return 'certified' if state['data'].get('cert') else 'rejected'; graph = StateGraph(PhototherapyState); graph.add_node('validate', validate_specs); graph.add_node('check_cert', device_check); graph.add_edge('validate', 'check_cert'); graph.add_edge('check_cert', END); graph.set_entry_point('validate');