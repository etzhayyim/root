from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ROState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: ROState): state['valid'] = all(k in state['specs'] for k in ['flow_rate', 'material']); state['log'].append('Validation complete'); return state; graph = StateGraph(ROState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()