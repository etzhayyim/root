from langgraph.graph import StateGraph, END; from typing import TypedDict; class IgnitronState(TypedDict): specs: dict; valid: bool; def check_specs(state: IgnitronState): state['valid'] = 'Peak Anode Voltage' in state['specs'] and state['specs']['Mercury Content Compliance'] == 'Certified'; return state; graph = StateGraph(IgnitronState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()