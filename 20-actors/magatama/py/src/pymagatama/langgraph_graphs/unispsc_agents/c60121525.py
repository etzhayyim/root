from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphicState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: GraphicState): if state['specs'].get('line_width_mm', 0) <= 0: state['validation_errors'].append('Invalid nib size'); state['approved'] = False; return state; graph = StateGraph(GraphicState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()