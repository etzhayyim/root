from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InfantApparelState(TypedDict): items: List[str]; safety_verified: bool; defects: List[str]; def validate_safety(state: InfantApparelState) -> InfantApparelState: print('Checking flammability and material safety...'); state['safety_verified'] = True; return state; def check_compliance(state: InfantApparelState) -> InfantApparelState: print('Verifying chemical test reports...'); return state; graph = StateGraph(InfantApparelState); graph.add_node('safety', validate_safety); graph.add_node('compliance', check_compliance); graph.set_entry_point('safety'); graph.add_edge('safety', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()