from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): component_id: str; quality_checks: List[str]; approved: bool; def validate_metallurgy(state: ProcurementState): state['quality_checks'].append('Metallurgy Validated'); return state; def check_export_compliance(state: ProcurementState): state['approved'] = True; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_metallurgy); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()