from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CallSysState(TypedDict): vendor: str; requirements: List[str]; validated: bool; def validate_specs(state: CallSysState): state['validated'] = 'SIP' in state['requirements']; return state; builder = StateGraph(CallSysState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()