from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HookState(TypedDict): hook_id: str; wll: float; valid: bool; def validate_hook(state: HookState): state['valid'] = state['wll'] > 0; return state; def check_compliance(state: HookState): print(f'Validating hook {state['hook_id']}'); return state; graph = StateGraph(HookState); graph.add_node('validate', validate_hook); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()