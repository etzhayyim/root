from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medicine: str; status: str; certification: bool; def validate_grade(state: PharmaState): return {'status': 'VALIDATED' if state.get('certification') else 'REJECTED'}; def generate_compliance_report(state: PharmaState): return {'status': 'COMPLIANT'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_grade); graph.add_node('report', generate_compliance_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()