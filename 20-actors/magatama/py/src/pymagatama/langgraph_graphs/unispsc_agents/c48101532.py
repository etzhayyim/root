from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CottonCandyState(TypedDict): vendor_docs: List[str]; safety_pass: bool; def check_compliance(state: CottonCandyState): return {'safety_pass': all('cert' in doc for doc in state['vendor_docs'])}; def route_procurement(state: CottonCandyState): return 'approve' if state['safety_pass'] else 'reject'; graph = StateGraph(CottonCandyState); graph.add_node('validate_certs', check_compliance); graph.set_entry_point('validate_certs'); graph.add_edge('validate_certs', END); graph = graph.compile()