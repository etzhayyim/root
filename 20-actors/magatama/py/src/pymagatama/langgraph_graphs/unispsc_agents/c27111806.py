from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): spec: dict; validation_errors: List[str]; approved: bool; def check_precision(state: State): state['validation_errors'] = [] if state['spec'].get('tolerance', 0) > 0.05 else ['Tolerance out of range']; state['approved'] = len(state['validation_errors']) == 0; return state; graph = StateGraph(State); graph.add_node('validate', check_precision); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()