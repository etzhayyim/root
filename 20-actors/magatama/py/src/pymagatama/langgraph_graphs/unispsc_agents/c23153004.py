from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MotorState(TypedDict): specs: dict; compliance: bool; validated: bool; def validate_specs(state: MotorState): state['validated'] = all(k in state['specs'] for k in ['voltage', 'torque']); return state; def check_compliance(state: MotorState): state['compliance'] = state['specs'].get('ip_rating', 0) >= 54; return state; graph = StateGraph(MotorState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()