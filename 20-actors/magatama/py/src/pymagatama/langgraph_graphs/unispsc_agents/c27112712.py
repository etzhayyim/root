from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrimmerState(TypedDict): specs: dict; safety_check: bool; approved: bool; def validate_specs(state: TrimmerState): state['safety_check'] = state['specs'].get('noise_level', 0) < 100; return state; def final_decision(state: TrimmerState): state['approved'] = state['safety_check']; return state; graph = StateGraph(TrimmerState); graph.add_node('validate', validate_specs); graph.add_node('decision', final_decision); graph.set_entry_point('validate'); graph.add_edge('validate', 'decision'); graph.add_edge('decision', END); graph = graph.compile()