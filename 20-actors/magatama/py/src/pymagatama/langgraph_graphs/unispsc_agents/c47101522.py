from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WaterSystemState(TypedDict): specs: dict; compliance_check: bool; workflow_step: str; def validate_specs(state: WaterSystemState): return {'compliance_check': all(k in state['specs'] for k in ['capacity', 'standard_compliance'])}; def finalize_procurement(state: WaterSystemState): return {'workflow_step': 'READY_FOR_BID'}; graph = StateGraph(WaterSystemState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()