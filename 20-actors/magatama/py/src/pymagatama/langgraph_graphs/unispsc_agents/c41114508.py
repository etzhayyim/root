from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurfaceTesterState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: SurfaceTesterState): is_valid = state['specs'].get('precision') > 0.001; return {'validation_report': 'Passed' if is_valid else 'Failed', 'approved': is_valid}; def export_control_check(state: SurfaceTesterState): return {'approved': state['approved'] and state['specs'].get('export_control') == False}; graph = StateGraph(SurfaceTesterState); graph.add_node('validate', validate_specs); graph.add_node('export_check', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()