from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserWelderState(TypedDict): specs: dict; validation_report: List[str]; approved: bool; def validate_physics(state: LaserWelderState): specs = state['specs']; errors = []; if specs.get('power') < 500: errors.append('Insufficient power for industrial grade'); return {'validation_report': errors, 'approved': len(errors) == 0}; graph = StateGraph(LaserWelderState); graph.add_node('validate', validate_physics); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()