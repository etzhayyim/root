from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PrintPaperState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_paper(state: PrintPaperState): errors = []; if state['specs'].get('ph_value', 7) < 7: errors.append('Acidic paper detected'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(PrintPaperState); graph.add_node('validate', validate_paper); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()