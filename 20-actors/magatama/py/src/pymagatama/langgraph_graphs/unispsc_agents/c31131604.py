from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_report: str; def validate_metallurgy(state: ForgingState): state['validation_report'] = 'Metallurgical analysis approved per ASTM standards' if 'grade' in state['specs'] else 'Missing grade'; return state; def check_geometry(state: ForgingState): state['validation_report'] += '; Geometry verified against CAD' if 'cad_file' in state['specs'] else '; Missing CAD'; return state; graph = StateGraph(ForgingState); graph.add_node('validate_metallurgy', validate_metallurgy); graph.add_node('check_geometry', check_geometry); graph.set_entry_point('validate_metallurgy'); graph.add_edge('validate_metallurgy', 'check_geometry'); graph.add_edge('check_geometry', END); graph = graph.compile()