from langgraph.graph import StateGraph, END; from typing import TypedDict; class SoapDishState(TypedDict): vendor_id: str; specs: dict; compliance_verified: bool; def validate_specs(state: SoapDishState): state['compliance_verified'] = 'material' in state['specs'] and 'drainage' in state['specs']; return state; builder = StateGraph(SoapDishState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()