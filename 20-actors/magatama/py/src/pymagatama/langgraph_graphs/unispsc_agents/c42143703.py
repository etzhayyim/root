from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PhototherapyState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: PhototherapyState): state['validation_passed'] = 'Peak Wavelength' in state['specs'] and state['specs']['Peak Wavelength'] > 0; return state; def route_procurement(state: PhototherapyState): return 'procure' if state['validation_passed'] else 'reject'; class Workflow: graph = StateGraph(PhototherapyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END)