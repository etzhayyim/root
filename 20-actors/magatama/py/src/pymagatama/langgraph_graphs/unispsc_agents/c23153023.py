from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ServoState(TypedDict): specs: dict; validation_log: list; status: str; def validate_specs(state: ServoState): specs = state['specs']; log = []; success = True; if 'torque' not in specs: log.append('Missing torque'); success = False; return {'validation_log': log, 'status': 'COMPLETED' if success else 'FAILED'}; graph = StateGraph(ServoState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()