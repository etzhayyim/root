from langgraph.graph import StateGraph, END; from typing import TypedDict; class ExtractionState(TypedDict): specs: dict; valid: bool; def validate_thimble(state: ExtractionState): specs = state.get('specs', {}); state['valid'] = all(k in specs for k in ['material', 'dimensions']); return state; graph = StateGraph(ExtractionState); graph.add_node('validate', validate_thimble); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()