from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EnclosureState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: EnclosureState): ip = state['specs'].get('IP_Rating', 0); state['valid'] = ip >= 54; state['error'] = '' if state['valid'] else 'IP rating below 54'; return state; graph = StateGraph(EnclosureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()