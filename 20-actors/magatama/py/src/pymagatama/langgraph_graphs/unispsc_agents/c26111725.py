from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BatteryState(TypedDict): vendor_specs: dict; validation_results: List[str]; approved: bool; def validate_specs(state: BatteryState): specs = state['vendor_specs']; results = []; valid = True; if specs.get('temp_range', 0) < 250: results.append('Insufficient operating temperature'); valid = False; return {'validation_results': results, 'approved': valid}; graph = StateGraph(BatteryState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()