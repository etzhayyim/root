from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BerylliumState(TypedDict): component_id: str; purity_percent: float; dimensions_ok: bool; toxicity_cleared: bool; def validate_spec(state: BerylliumState) -> BerylliumState: state['dimensions_ok'] = True; return state; def check_export_compliance(state: BerylliumState) -> BerylliumState: state['toxicity_cleared'] = (state['purity_percent'] >= 99.0); return state; graph = StateGraph(BerylliumState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()