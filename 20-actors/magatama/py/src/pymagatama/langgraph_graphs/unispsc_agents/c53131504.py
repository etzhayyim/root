from langgraph.graph import StateGraph, END; from typing import TypedDict; class DentalState(TypedDict): floss_type: str; tensile_strength: float; is_compliant: bool; def check_strength(state: DentalState): return {'is_compliant': state['tensile_strength'] > 5.0}; graph = StateGraph(DentalState); graph.add_node('validate_strength', check_strength); graph.set_entry_point('validate_strength'); graph.add_edge('validate_strength', END); graph = graph.compile()