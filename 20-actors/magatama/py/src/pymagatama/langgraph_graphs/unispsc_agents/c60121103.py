from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaperState(TypedDict): paper_type: str; specs: dict; valid: bool; def validate_paper(state: PaperState): state['valid'] = state['specs'].get('gsm', 0) > 0; return state; graph = StateGraph(PaperState); graph.add_node('validate', validate_paper); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()