from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GameState(TypedDict): items: List[str]; validated_specs: List[bool]; def validate_game(state: GameState): return {'validated_specs': [True for _ in state['items']]}; graph = StateGraph(GameState); graph.add_node('validate', validate_game); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()