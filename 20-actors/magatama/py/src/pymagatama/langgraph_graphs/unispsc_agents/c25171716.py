from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BrakeLineState(TypedDict): specs: dict; valid: bool; output: str; def validate_specs(state: BrakeLineState): state['valid'] = state['specs'].get('burst_pressure', 0) > 2000; return state; graph = StateGraph(BrakeLineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()