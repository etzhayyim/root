from typing import TypedDict, Annotated, List; import operator; from langgraph.graph import StateGraph, END; class LaserState(TypedDict): specs: dict; validation: List[str]; def validate_power(state: LaserState): state['validation'].append('Power Output Verified'); return state; def check_safety(state: LaserState): state['validation'].append('Safety Enclosure Checked'); return state; graph = StateGraph(LaserState); graph.add_node('validate_power', validate_power); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate_power'); graph.add_edge('validate_power', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()