from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): component_id: str; compliance_check: bool; specs: dict; graph = StateGraph(ProcState); def validate_spec(state: ProcState): state['compliance_check'] = 'ECCN' in state['specs']; return state; def check_export(state: ProcState): return 'compliance_check' if state['compliance_check'] else END; graph.add_node('validate', validate_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); app = graph.compile()