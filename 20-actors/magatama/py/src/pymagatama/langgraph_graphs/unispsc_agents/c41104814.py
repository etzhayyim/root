from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CondenserState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: CondenserState): return {'validation_results': ['Pressure test passed', 'Material integrity confirmed'] if state['specs'].get('pressure') > 0 else ['Validation failed']}; def finalize_procurement(state: CondenserState): return {'status': 'Approved for purchase'}; graph = StateGraph(CondenserState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()