from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medicine: str; status: str; batch_id: str; def validate_integrity(state: PharmaState): return {'status': 'validated'} if state.get('batch_id') else {'status': 'error'}; def check_compliance(state: PharmaState): return {'status': 'compliant'} if state.get('medicine') == 'Albuterol sulfate' else {'status': 'rejected'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_integrity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()