from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class HydrocarbonState(TypedDict): fuel_type: str; purity_level: float; transport_plan: str; logs: Annotated[Sequence[str], operator.add]; def validate_fuel_purity(state: HydrocarbonState): return {'logs': ['Purity check passed']}; def plan_logistics(state: HydrocarbonState): return {'logs': ['Logistics optimized for dangerous goods']}; graph = StateGraph(HydrocarbonState); graph.add_node('purity_check', validate_fuel_purity); graph.add_node('logistics', plan_logistics); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'logistics'); graph.add_edge('logistics', END); graph = graph.compile()