from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ResinState(TypedDict): chemical_data: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_chemical_integrity(state: ResinState): return {'validation_results': ['Integrity check passed'], 'status': 'validated'}; def check_hazmat_protocols(state: ResinState): return {'validation_results': ['Hazmat protocols verified'], 'status': 'approved'}; graph = StateGraph(ResinState); graph.add_node('integrity', validate_chemical_integrity); graph.add_node('hazmat', check_hazmat_protocols); graph.set_entry_point('integrity'); graph.add_edge('integrity', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()