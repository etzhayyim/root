from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeldingState(TypedDict): specs: dict; validation: bool; def validate_specs(state: WeldingState): state['validation'] = all(k in state['specs'] for k in ['load', 'speed']); return state; graph = StateGraph(WeldingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()