from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserProcurementState(TypedDict): specs: dict; safety_cleared: bool; export_license: bool; def check_specs(state: LaserProcurementState): return {'safety_cleared': state['specs'].get('laser_class') == 4}; def check_export(state: LaserProcurementState): return {'export_license': state['specs'].get('power_kw', 0) > 6.0}; graph = StateGraph(LaserProcurementState); graph.add_node('check_specs', check_specs); graph.add_node('check_export', check_export); graph.set_entry_point('check_specs'); graph.add_edge('check_specs', 'check_export'); graph.add_edge('check_export', END); compiled_graph = graph.compile()