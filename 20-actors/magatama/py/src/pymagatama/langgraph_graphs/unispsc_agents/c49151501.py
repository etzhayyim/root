from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SkiBootState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SkiBootState): errors = []; if state['specs'].get('flex_index', 0) < 60: errors.append('Flex too low for professional use'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(SkiBootState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()