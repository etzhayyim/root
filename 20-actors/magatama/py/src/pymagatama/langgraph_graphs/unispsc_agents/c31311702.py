from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeSpec(TypedDict): specs: dict; status: str; def validate_integrity(state: PipeSpec): state['status'] = 'VALIDATED' if 'Grade_of_Steel' in state['specs'] else 'FAILED'; return state; graph = StateGraph(PipeSpec); graph.add_node('validate', validate_integrity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()