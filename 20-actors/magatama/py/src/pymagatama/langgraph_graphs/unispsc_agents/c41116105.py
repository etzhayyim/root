from langgraph.graph import StateGraph, END; from typing import TypedDict; class ChemicalState(TypedDict): reagent_data: dict; validation_results: list; status: str; def validate_reagent(state: ChemicalState): state['validation_results'].append('SDS_VERIFIED'); state['status'] = 'READY' if 'CAS' in state['reagent_data'] else 'PENDING'; return state; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_reagent); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()