from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TableState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: TableState): return {'validation_passed': state['specs'].get('weight_capacity', 0) > 150}; def route_task(state: TableState): return 'validate' if state.get('specs') else END; graph = StateGraph(TableState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()