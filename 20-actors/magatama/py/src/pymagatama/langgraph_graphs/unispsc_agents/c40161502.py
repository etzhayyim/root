from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: FilterState): state['compliant'] = 'NSF_cert' in state['specs'] and state['specs']['flow_rate'] > 0; return state; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()