from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ActuatorState(TypedDict): input_spec: dict; validation_results: Annotated[list[str], operator.add]; status: str; def validate_spec(state: ActuatorState) -> ActuatorState: results = []; if state['input_spec'].get('load_capacity_kg', 0) <= 0: results.append('Invalid load capacity'); return {**state, 'validation_results': results, 'status': 'validated' if not results else 'failed'}; def compile_graph(): builder = StateGraph(ActuatorState); builder.add_node('validate', validate_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); return builder.compile(); graph = compile_graph()