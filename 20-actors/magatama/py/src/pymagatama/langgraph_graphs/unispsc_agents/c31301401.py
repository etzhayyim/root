from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): alloy_type: str; specs: dict; valid: bool; def validate_forging(state: ForgingState) -> dict: state['valid'] = state.get('alloy_type') in ['Aluminum', 'Titanium', 'Copper'] and all(k in state['specs'] for k in ['tensile', 'dims']); return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_forging); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()