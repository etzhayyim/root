from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FacilityState(TypedDict): items: List[dict]; validation_status: bool; def validate_fixtures(state: FacilityState): state['validation_status'] = all(['fire_rating' in i for i in state['items']]); return state; def finalize_procurement(state: FacilityState): return {'validation_status': True}; graph = StateGraph(FacilityState); graph.add_node('validate', validate_fixtures); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()