from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LightingState(TypedDict): specs: dict; compliance: bool; approved: bool; def check_specs(state: LightingState): state['compliance'] = 'IP67' in state['specs'].get('rating', ''); return {'compliance': state['compliance']}; def validate_graph(state: LightingState): return 'approved' if state['compliance'] else 'review'; graph = StateGraph(LightingState); graph.add_node('checker', check_specs); graph.set_entry_point('checker'); graph.add_edge('checker', END); app = graph.compile()