from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabDishState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: LabDishState): required = ['material', 'sterility']; state['valid'] = all(k in state['specs'] for k in required); return state; graph = StateGraph(LabDishState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()