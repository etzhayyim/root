from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_data: dict; validation_results: list; status: str; def validate_purity(state: ProcureState): p = state['chemical_data'].get('purity', 0); return {'validation_results': ['Purity Check Passed' if p >= 99.0 else 'Purity Fail']}; def check_hazmat(state: ProcureState): return {'status': 'HAZMAT_REVIEW_REQUIRED'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_purity); graph.add_node('hazmat_check', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat_check'); graph.add_edge('hazmat_check', END); compiled_graph = graph.compile()