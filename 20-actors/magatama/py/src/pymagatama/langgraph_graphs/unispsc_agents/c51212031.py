from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): origin: str; purity: float; validated: bool; def validate_origin(state: ProcurementState): state['validated'] = state['origin'] == 'Cameroon' or state['origin'] == 'Madagascar'; return state; def check_purity(state: ProcurementState): state['validated'] = state['validated'] and state['purity'] >= 99.0; return state; graph = StateGraph(ProcurementState); graph.add_node('check_origin', validate_origin); graph.add_node('check_purity', check_purity); graph.set_entry_point('check_origin'); graph.add_edge('check_origin', 'check_purity'); graph.add_edge('check_purity', END); graph = graph.compile()