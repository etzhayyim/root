from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CementDuctState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_duct_specs(state: CementDuctState): specs = state.get('specs', {}); required = ['strength', 'diameter']; valid = all(k in specs for k in required) and specs.get('strength', 0) > 20; return {'valid': valid, 'logs': ['Specs validated']} if valid else {'valid': False, 'logs': ['Invalid technical specs']}; graph = StateGraph(CementDuctState); graph.add_node('validate', validate_duct_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()