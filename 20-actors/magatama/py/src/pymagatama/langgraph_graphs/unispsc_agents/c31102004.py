from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): spec: dict; validation_results: List[str]; def validate_material(state: CastingState): state['validation_results'].append('Material grade verification complete'); return state; def check_dimensions(state: CastingState): state['validation_results'].append('Tolerances within specification'); return state; graph = StateGraph(CastingState); graph.add_node('material_check', validate_material); graph.add_node('dim_check', check_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dim_check'); graph.add_edge('dim_check', END); compiled_graph = graph.compile()