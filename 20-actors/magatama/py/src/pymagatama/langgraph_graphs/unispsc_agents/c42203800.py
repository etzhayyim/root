from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PositioningAidState(TypedDict): aids: List[str]; inspection_results: dict; graph = StateGraph(PositioningAidState); def validate_specs(state: PositioningAidState): return {'inspection_results': {'radiotransparency': 'verified'}}; def check_compliance(state: PositioningAidState): return {'inspection_results': {'compliance': 'ISO_13485_met'}}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); app = graph.compile()