from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DressingState(TypedDict): items: List[str]; qc_passed: bool; def validate_sterility(state: DressingState): return {'qc_passed': all('sterile' in item.lower() for item in state['items'])}; graph = StateGraph(DressingState); graph.add_node('validate', validate_sterility); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()