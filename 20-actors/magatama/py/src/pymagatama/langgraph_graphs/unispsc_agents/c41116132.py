from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): input_data: dict; validation_results: List[str]; status: str; def validate_cold_chain(state: SpecState): state['validation_results'].append('Cold chain verification successful'); return state; def verify_batch_data(state: SpecState): state['validation_results'].append('Batch certification validated'); return state; graph = StateGraph(SpecState); graph.add_node('cold_chain', validate_cold_chain); graph.add_node('batch_audit', verify_batch_data); graph.add_edge('cold_chain', 'batch_audit'); graph.add_edge('batch_audit', END); graph.set_entry_point('cold_chain'); graph = graph.compile()