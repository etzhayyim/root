from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TillerState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: TillerState): specs = state['specs']; is_valid = all(key in specs for key in ['Engine_Displacement_cc', 'Tilling_Width_mm']); return {'valid': is_valid, 'error': '' if is_valid else 'Missing required fields'}; graph = StateGraph(TillerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()