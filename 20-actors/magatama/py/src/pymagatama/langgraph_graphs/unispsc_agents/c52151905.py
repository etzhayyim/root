from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BakingSheetState(TypedDict): vendor_docs: List[str]; compliance_checks: List[str]; approved: bool; def validate_grade(state: BakingSheetState): state['compliance_checks'].append('Food Safety Verified'); state['approved'] = True; return state; graph = StateGraph(BakingSheetState); graph.add_node('validate', validate_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()