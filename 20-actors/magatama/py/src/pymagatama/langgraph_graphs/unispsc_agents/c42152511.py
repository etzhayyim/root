from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalSpecState(TypedDict): material: str; sterile: bool; specs_validated: bool; def validate_specs(state: DentalSpecState): state['specs_validated'] = state['material'] != '' and state['sterile'] is True; return state; graph = StateGraph(DentalSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()