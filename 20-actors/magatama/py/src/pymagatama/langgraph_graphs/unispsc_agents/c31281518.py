from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; specs: dict; validation_passed: bool; def validate_dimensions(state: ComponentState): state['validation_passed'] = 'tolerance' in state['specs']; return state; def check_material(state: ComponentState): return state; graph = StateGraph(ComponentState); graph.add_node('dimension_check', validate_dimensions); graph.add_node('material_check', check_material); graph.add_edge('dimension_check', 'material_check'); graph.add_edge('material_check', END); graph.set_entry_point('dimension_check'); graph = graph.compile()