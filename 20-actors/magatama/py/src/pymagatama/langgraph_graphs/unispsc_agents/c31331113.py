from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; specs: dict; validation_passed: bool; def validate_assembly(state: AssemblyState): state['validation_passed'] = all(k in state['specs'] for k in ['material', 'bond_strength']); return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_assembly); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()