from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class VentilatorState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def validate_specs(state: VentilatorState): issues = []; cols = ['ISO_13485', 'IEC_60601']; for c in cols: if c not in state['specs']: issues.append(f'Missing {c}'); return {'validation_results': issues, 'approved': len(issues) == 0}; graph = StateGraph(VentilatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()