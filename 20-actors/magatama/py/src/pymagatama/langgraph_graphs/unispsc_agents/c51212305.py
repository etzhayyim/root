from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ICGProcurementState(TypedDict): vendor_id: str; purity_check: bool; compliance_docs: List[str]; graph = StateGraph(ICGProcurementState); def validate_purity(state: ICGProcurementState): return {'purity_check': True}; def verify_compliance(state: ICGProcurementState): return {'compliance_docs': ['GMP_CERT', 'FDA_APPROVAL']}; graph.add_node('validate', validate_purity); graph.add_node('compliance', verify_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); app = graph.compile()