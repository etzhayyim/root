from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): items: list; verification_status: str; def validate_spec(state: GraphState): print('Validating basketball system specs...'); return {'verification_status': 'verified'}; def check_safety(state: GraphState): print('Checking ASTM safety ratings...'); return {'verification_status': 'safety_checked'}; graph = StateGraph(GraphState); graph.add_node('validate', validate_spec); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()