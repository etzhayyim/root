from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PetriRackState(TypedDict): dimensions: dict; material: str; needs_sterilization: bool; validation_log: List[str]; def validate_specs(state: PetriRackState): state['validation_log'].append('Checking rack dimensions and material heat tolerance.'); return state; def check_compliance(state: PetriRackState): state['validation_log'].append('Verifying sterilization grade ISO standards.'); return state; graph = StateGraph(PetriRackState); graph.add_node('spec_validation', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()