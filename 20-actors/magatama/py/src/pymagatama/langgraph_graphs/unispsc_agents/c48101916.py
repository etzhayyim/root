from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): vendor_check: bool; spec_validated: bool; def check_compliance(state: ProcureState): return {'spec_validated': True}; def approve_vendor(state: ProcureState): return {'vendor_check': True}; graph = StateGraph(ProcureState); graph.add_node('compliance', check_compliance); graph.add_node('vendor', approve_vendor); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'vendor'); graph.add_edge('vendor', END); graph = graph.compile()