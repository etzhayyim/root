from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TerrainSystemState(TypedDict): vendor_id: str; compliance_docs: List[str]; validation_passed: bool; def validate_sensors(state: TerrainSystemState): return {'validation_passed': True} if 'calibration_cert' in state['compliance_docs'] else {'validation_passed': False}; def check_export_controls(state: TerrainSystemState): return {'validation_passed': True}; graph = StateGraph(TerrainSystemState); graph.add_node('validate', validate_sensors); graph.add_node('export_check', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()