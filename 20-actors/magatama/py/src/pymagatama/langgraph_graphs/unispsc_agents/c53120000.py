from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LuggageState(TypedDict): vendor_specs: dict; compliance_score: float; approved: bool; def validate_specs(state: LuggageState): state['compliance_score'] = 1.0 if state['vendor_specs'].get('durability') == 'high' else 0.5; state['approved'] = state['compliance_score'] >= 0.8; return state; graph = StateGraph(LuggageState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()