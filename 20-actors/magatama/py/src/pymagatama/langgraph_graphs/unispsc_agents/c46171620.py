from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class SafetyGraphState(TypedDict): safety_level: str; compliance_docs: List[str]; validated: bool; def validate_safety_specs(state: SafetyGraphState): state['validated'] = state['safety_level'] in ['SIL3', 'PLe']; return state; def check_compliance(state: SafetyGraphState): state['compliance_docs'] = ['ISO_13849_Cert', 'IEC_61496_Report']; return state; graph = StateGraph(SafetyGraphState); graph.add_node('validate', validate_safety_specs); graph.add_node('certify', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()