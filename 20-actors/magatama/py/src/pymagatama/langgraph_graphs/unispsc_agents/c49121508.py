from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): mesh_density: int; is_treated: bool; compliance_docs: List[str]; approved: bool; def validate_mesh(state: ProcurementState): state['approved'] = state['mesh_density'] >= 156; return state; def check_compliance(state: ProcurementState): state['approved'] = state['approved'] and 'WHOPES' in state['compliance_docs']; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_mesh); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()