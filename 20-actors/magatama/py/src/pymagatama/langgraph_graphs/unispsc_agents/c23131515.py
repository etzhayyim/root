from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotSpec(TypedDict): model: str; payload: float; status: str; def validate_specs(state: RobotSpec) -> RobotSpec: state['status'] = 'Validated' if state['payload'] > 0 else 'Error'; return state; graph = StateGraph(RobotSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()