from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PressBrakeState(TypedDict): specs: dict; validation_errors: List[str]; graph = StateGraph(PressBrakeState); def validate_specs(state: PressBrakeState): errors = []; if state['specs'].get('force', 0) <= 0: errors.append('Force must be positive'); return {'validation_errors': errors}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()