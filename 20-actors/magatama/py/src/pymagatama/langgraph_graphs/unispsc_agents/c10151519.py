from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class ExcavatorState(TypedDict): bucket_specs: Dict; validation_results: List[str]; inspection_status: str; def validate_bucket(state: ExcavatorState): specs = state['bucket_specs']; status = 'PASS' if specs.get('material') == 'high_strength_steel' and specs.get('capacity') > 0 else 'FAIL'; return {'validation_results': ['Material check passed' if status == 'PASS' else 'Material check failed'], 'inspection_status': status}; def finalize_procurement(state: ExcavatorState): return {'inspection_status': 'READY_FOR_SHIPMENT'}; graph = StateGraph(ExcavatorState); graph.add_node('validate', validate_bucket); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()