from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_specs: dict; approved: bool; status: str; def validate_dimensions(state: ProcureState): capacity = state['vendor_specs'].get('weight_capacity_kg', 0); state['approved'] = capacity > 50; state['status'] = 'approved' if state['approved'] else 'rejected'; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()