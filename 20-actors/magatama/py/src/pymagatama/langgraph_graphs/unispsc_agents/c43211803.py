from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KeyboardSkinState(TypedDict): model: str; specs: dict; compliant: bool; def validate_specs(state: KeyboardSkinState): state['compliant'] = 'thickness' in state['specs'] and state['specs']['thickness'] <= 0.5; return state; graph = StateGraph(KeyboardSkinState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()