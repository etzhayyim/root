from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BasketryState(TypedDict): material_specs: dict; validation_report: List[str]; def validate_materials(state: BasketryState): print('Validating reed diameter and quality metrics...'); state['validation_report'].append('Quality check passed'); return state; def finalize_procurement(state: BasketryState): print('Generating procurement order...'); return state; graph = StateGraph(BasketryState); graph.add_node('validate', validate_materials); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()