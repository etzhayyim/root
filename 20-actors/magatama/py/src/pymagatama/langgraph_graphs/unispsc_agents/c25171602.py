from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: List[str]; validation_results: dict; def validate_thermal(state: State): state['validation_results']['thermal'] = 'passed'; return state; def validate_compliance(state: State): state['validation_results']['compliance'] = 'ISO_verified'; return state; graph = StateGraph(State); graph.add_node('thermal_check', validate_thermal); graph.add_node('compliance_check', validate_compliance); graph.set_entry_point('thermal_check'); graph.add_edge('thermal_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()