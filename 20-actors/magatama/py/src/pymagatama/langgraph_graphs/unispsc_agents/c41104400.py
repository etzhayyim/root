from langgraph.graph import StateGraph, END; from typing import TypedDict; class LabState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: LabState): state['validation_passed'] = 'temperature_range' in state['specs'] and 'calibration' in state['specs']; return state; builder = StateGraph(LabState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()