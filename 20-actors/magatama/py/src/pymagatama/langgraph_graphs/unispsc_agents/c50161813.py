from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; qc_passed: bool; temperature_logs: float; def validate_quality(state: ProcureState): state['qc_passed'] = state['temperature_logs'] <= 22.0; return state; def check_compliance(state: ProcureState): return 'qc_passed' if state['qc_passed'] else END; graph = StateGraph(ProcureState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()