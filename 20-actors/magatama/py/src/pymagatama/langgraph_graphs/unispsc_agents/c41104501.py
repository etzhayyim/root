from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OvenProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: OvenProcurementState): specs = state['specs']; errors = []; if specs.get('temp_range', 0) < 50: errors.append('Temperature range insufficient'); return {'validation_errors': errors}; def finalize_procurement(state: OvenProcurementState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(OvenProcurementState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()