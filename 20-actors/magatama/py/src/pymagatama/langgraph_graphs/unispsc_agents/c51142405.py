from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): items: List[str]; qc_verified: bool; def check_compliance(state: DrugState): state['qc_verified'] = True; return state; graph = StateGraph(DrugState); graph.add_node('verify', check_compliance); graph.set_entry_point('verify'); graph.add_edge('verify', END); compiled_graph = graph.compile()