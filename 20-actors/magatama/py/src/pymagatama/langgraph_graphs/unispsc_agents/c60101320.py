from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlashCardState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(FlashCardState); def validate_material(state: FlashCardState): state['validation_results'].append('Material verified for non-toxicity'); return state; def check_curriculum(state: FlashCardState): state['validation_results'].append('Curriculum match verified'); return state; graph.add_node('validate_material', validate_material); graph.add_node('check_curriculum', check_curriculum); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_curriculum'); graph.add_edge('check_curriculum', END); graph = graph.compile()