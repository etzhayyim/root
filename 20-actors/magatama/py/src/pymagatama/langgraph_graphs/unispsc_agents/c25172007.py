from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StrutProcurementState(TypedDict): part_number: str; specs_verified: bool; quality_passed: bool; def validate_specs(state: StrutProcurementState): return {'specs_verified': True}; def perform_quality_check(state: StrutProcurementState): return {'quality_passed': True}; graph = StateGraph(StrutProcurementState); graph.add_node('validate', validate_specs); graph.add_node('qc', perform_quality_check); graph.add_edge('validate', 'qc'); graph.add_edge('qc', END); graph.set_entry_point('validate'); graph = graph.compile()