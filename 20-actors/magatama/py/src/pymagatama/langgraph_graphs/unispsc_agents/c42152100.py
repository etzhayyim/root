from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalSpecState(TypedDict): items: List[str]; validated_items: List[dict]; status: str; def validate_clinical_standards(state: DentalSpecState): return {'validated_items': [{'item': i, 'compliant': True} for i in state['items']], 'status': 'VALIDATED'}; def check_regulatory_docs(state: DentalSpecState): return {'status': 'CERTIFIED'}; graph = StateGraph(DentalSpecState); graph.add_node('validate', validate_clinical_standards); graph.add_node('check_docs', check_regulatory_docs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_docs'); graph.add_edge('check_docs', END); graph = graph.compile()