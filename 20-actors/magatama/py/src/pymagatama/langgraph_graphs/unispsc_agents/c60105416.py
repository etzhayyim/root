from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetyTrainingState(TypedDict): content_material: str; compliance_check: bool; material_type: str; def validate_content(state: SafetyTrainingState): return {'compliance_check': 'Safety Guidelines' in state['content_material']}; def format_check(state: SafetyTrainingState): return {'material_type': 'Digital' if 'pdf' in state['content_material'] else 'Print'}; graph = StateGraph(SafetyTrainingState); graph.add_node('validate', validate_content); graph.add_node('format', format_check); graph.add_edge('validate', 'format'); graph.add_edge('format', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()