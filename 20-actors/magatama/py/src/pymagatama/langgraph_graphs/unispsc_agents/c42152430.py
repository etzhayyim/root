from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class EndodonticState(TypedDict): material_spec: dict; compliance_status: bool; def validate_biocompatibility(state: EndodonticState): state['compliance_status'] = 'ISO10993' in state['material_spec'].get('standards', []); return state; def check_radiopacity(state: EndodonticState): return state; graph = StateGraph(EndodonticState); graph.add_node('validate', validate_biocompatibility); graph.add_node('check', check_radiopacity); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.set_entry_point('validate'); graph = graph.compile()