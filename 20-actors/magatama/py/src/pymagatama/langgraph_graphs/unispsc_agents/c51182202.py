from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor: str; gmp_certified: bool; coa_valid: bool; steps: List[str]; def validate_gmp(state: ProcureState): state['gmp_certified'] = True; state['steps'].append('GMP Verified'); return state; def check_coa(state: ProcureState): state['coa_valid'] = True; state['steps'].append('CoA Validated'); return state; graph = StateGraph(ProcureState); graph.add_node('validate_gmp', validate_gmp); graph.add_node('check_coa', check_coa); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', 'check_coa'); graph.add_edge('check_coa', END); graph = graph.compile()