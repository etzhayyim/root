from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AudiometerState(TypedDict): data: dict; validation_errors: Annotated[list[str], operator.add]; def validate_calibration(state: AudiometerState): errors = [] if not state['data'].get('cal_cert'): errors.append('Missing calibration certificate'); return {'validation_errors': errors}; def check_regulatory(state: AudiometerState): return {}; graph = StateGraph(AudiometerState); graph.add_node('validate', validate_calibration); graph.add_node('regulatory', check_regulatory); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); graph = graph.compile()