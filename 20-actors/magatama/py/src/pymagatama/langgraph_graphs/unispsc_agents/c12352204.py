from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class MetalOxideState(TypedDict): metal_oxide_code: str; purity_check: bool; safety_clearance: bool; processed_data: dict; def validate_purity(state: MetalOxideState): state['purity_check'] = True; return state; def check_safety(state: MetalOxideState): state['safety_clearance'] = True; return state; def finalize_processing(state: MetalOxideState): state['processed_data'] = {'status': 'validated', 'compliance': 'high'}; return state; graph = StateGraph(MetalOxideState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_safety', check_safety); graph.add_node('finalize', finalize_processing); graph.add_edge('validate_purity', 'check_safety'); graph.add_edge('check_safety', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate_purity'); graph = graph.compile()