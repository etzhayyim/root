from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EyeProtectionState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: EyeProtectionState): state['compliance'] = all(k in state['specs'] for k in ['ansi_rating', 'material']); return state; def check_approval(state: EyeProtectionState): state['approved'] = state['compliance']; return state; graph = StateGraph(EyeProtectionState); graph.add_node('validate', validate_specs); graph.add_node('check', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()