from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcessorState(TypedDict): items: List[str]; validated: bool; task: str; def validate_pieces(state: ProcessorState): print('Validating pentomino count...'); return {'validated': True}; def update_inventory(state: ProcessorState): print('Updating inventory...'); return {'task': 'inventory_updated'}; graph = StateGraph(ProcessorState); graph.add_node('validate', validate_pieces); graph.add_node('inventory', update_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph = graph.compile()