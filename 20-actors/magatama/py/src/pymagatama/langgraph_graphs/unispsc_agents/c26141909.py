from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_report: list[str]; export_compliance: bool; graph = StateGraph(State); def validate_specs(state: State): return {'validation_report': ['Specs validated']}; def check_export(state: State): return {'export_compliance': True}; graph.add_node('validate', validate_specs); graph.add_node('export', check_export); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph.set_entry_point('validate'); app = graph.compile()