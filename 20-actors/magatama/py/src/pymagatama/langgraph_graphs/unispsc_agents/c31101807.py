from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_magnesium_composition(state: CastingState): state['validation_results'].append('Alloy trace elements verified'); return state; def check_casting_integrity(state: CastingState): state['validation_results'].append('Shell integrity confirmed'); return state; graph = StateGraph(CastingState); graph.add_node('validate_material', validate_magnesium_composition); graph.add_node('check_integrity', check_casting_integrity); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_integrity'); graph.add_edge('check_integrity', END); graph = graph.compile()