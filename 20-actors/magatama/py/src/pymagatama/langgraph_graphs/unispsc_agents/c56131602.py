from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DisplayGraphState(TypedDict): pole_specs: dict; status: str; def validate_specs(state: DisplayGraphState): return {'status': 'validated' if state['pole_specs'].get('load_capacity') > 0 else 'rejected'}; graph = StateGraph(DisplayGraphState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()