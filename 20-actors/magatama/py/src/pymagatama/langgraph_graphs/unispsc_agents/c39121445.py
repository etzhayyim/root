from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WireConnectorState(TypedDict): wire_gauge: float; current_rating: float; certification: str; valid: bool; def validate_connector(state: WireConnectorState): state['valid'] = state['certification'] in ['UL', 'CSA'] and state['current_rating'] > 0; return state; graph = StateGraph(WireConnectorState); graph.add_node('validate', validate_connector); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()