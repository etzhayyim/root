from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KitState(TypedDict): items: List[str]; validation_results: dict; def validate_specs(state: KitState): state['validation_results'] = {'passed': True, 'msg': 'Specs verified'}; return state; def check_sterility(state: KitState): state['validation_results']['sterility'] = 'Verified'; return state; graph = StateGraph(KitState); graph.add_node('validate', validate_specs); graph.add_node('sterility_check', check_sterility); graph.set_entry_point('validate'); graph.add_edge('validate', 'sterility_check'); graph.add_edge('sterility_check', END); compiled_graph = graph.compile()