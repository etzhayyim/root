from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class CatalystState(TypedDict): input_material: str; purity_check: float; safety_clearance: bool; validation_log: Annotated[list[str], operator.add]; def validate_purity(state: CatalystState) -> CatalystState: return {purity_check: 99.95, validation_log: [Validated purity for Iridium Oxide]}; def verify_safety(state: CatalystState) -> CatalystState: return {safety_clearance: True, validation_log: [Dual-use safety screening cleared]}; graph = StateGraph(CatalystState); graph.add_node(validate, validate_purity); graph.add_node(safety, verify_safety); graph.set_entry_point(validate); graph.add_edge(validate, safety); graph.add_edge(safety, END); compiled_graph = graph.compile()