from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RollerState(TypedDict): roller_width: float; adhesive_type: str; is_cleanroom_grade: bool; quality_passed: bool; def validate_specs(state: RollerState): state['quality_passed'] = state['roller_width'] > 0 and state['adhesive_type'] != ''; return state; graph = StateGraph(RollerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()