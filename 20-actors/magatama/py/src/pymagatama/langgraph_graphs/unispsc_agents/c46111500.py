from langgraph.graph import StateGraph, END; from typing import TypedDict; class MunitionState(TypedDict): status: str; safety_clearance: bool; def validate_security(state: MunitionState): return {'status': 'SECURE_VERIFIED'} if state['safety_clearance'] else {'status': 'REJECTED'}; graph = StateGraph(MunitionState); graph.add_node('security_check', validate_security); graph.set_entry_point('security_check'); graph.add_edge('security_check', END); graph = graph.compile()