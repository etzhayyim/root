from typing import TypedDict, Annotated, List, Union; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class ProcessingState(TypedDict): chemical_code: str; batch_id: str; purity_check: bool; safety_clearance: bool; log: Annotated[List[str], add_messages]; def validate_purity(state: ProcessingState) -> ProcessingState: state['purity_check'] = True; state['log'].append('Purity verification passed'); return state; def check_safety(state: ProcessingState) -> ProcessingState: state['safety_clearance'] = True; state['log'].append('Safety and export control protocols cleared'); return state; graph = StateGraph(ProcessingState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()