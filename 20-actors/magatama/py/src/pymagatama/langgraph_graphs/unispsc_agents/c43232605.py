from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): software_name: str; requirements: List[str]; validation_status: bool; def validate_specs(state: AnalysisState): state['validation_status'] = len(state['requirements']) > 0; return state; def check_compliance(state: AnalysisState): return {'validation_status': True if state['software_name'] else False}; graph = StateGraph(AnalysisState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()