from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): chemical_name: str; analysis_result: str; compliance_check: bool; def validate_chemical(state: ProcurementState): state['compliance_check'] = 'purity' in state and state['purity'] >= 99.0; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_chemical); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()