from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoltState(TypedDict): specs: dict; validation_report: List[str]; approved: bool; def validate_specs(state: BoltState): if state['specs'].get('tensile_strength', 0) > 500: state['validation_report'].append('Strength compliant'); else: state['validation_report'].append('Strength non-compliant'); return {'approved': True if state['specs'].get('tensile_strength', 0) > 500 else False}; builder = StateGraph(BoltState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()