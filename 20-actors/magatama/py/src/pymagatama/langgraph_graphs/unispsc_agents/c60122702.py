from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): metal_grade: str; dimensions: dict; compliance_check: bool; graph = StateGraph(ProcurementState); def validate_purity(state: ProcurementState): state['compliance_check'] = state.get('metal_grade') == 'C11000'; return state; def check_dimensions(state: ProcurementState): return {'compliance_check': state['compliance_check'] and True}; graph.add_node('validate_purity', validate_purity); graph.add_node('check_dimensions', check_dimensions); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_dimensions'); graph.add_edge('check_dimensions', END); graph = graph.compile()