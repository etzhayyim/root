from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalRollState(TypedDict): items: List[str]; validation_results: dict; def validate_roll(state: DentalRollState): state['validation_results'] = {'passed': all('sterilized' in item for item in state['items'])}; return state; graph = StateGraph(DentalRollState); graph.add_node('validate', validate_roll); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()