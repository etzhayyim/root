from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class YeastSupplyState(TypedDict): yeast_strain: str; purity_cert: bool; qc_passed: bool; def validate_purity(state: YeastSupplyState): return {'qc_passed': state['purity_cert']}; def check_storage(state: YeastSupplyState): return {'qc_passed': True}; graph = StateGraph(YeastSupplyState); graph.add_node('validate', validate_purity); graph.add_node('storage', check_storage); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph.set_entry_point('validate'); graph = graph.compile()