from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastProtectorState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: CastProtectorState): state['compliance'] = 'waterproof_rating' in state['specs'] and state['specs']['waterproof_rating'] > 0; return state; graph = StateGraph(CastProtectorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()