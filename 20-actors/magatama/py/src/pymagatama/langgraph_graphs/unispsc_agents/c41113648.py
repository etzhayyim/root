from langgraph.graph import StateGraph, END; from typing import TypedDict; class MeterState(TypedDict): status: str; spec_data: dict; validation: bool; def check_specs(state: MeterState): cat_rating = state['spec_data'].get('cat_rating', 0); return {'validation': cat_rating >= 3}; def finalize(state: MeterState): return {'status': 'Approved' if state['validation'] else 'Rejected'}; graph = StateGraph(MeterState); graph.add_node('validate', check_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()