from langgraph.graph import StateGraph, END; from typing import TypedDict; class CastingState(TypedDict): specs: dict; valid: bool; def validate_lead_specs(state: CastingState): state['valid'] = 'lead_purity' in state['specs'] and state['specs']['lead_purity'] >= 0.99; return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_lead_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()