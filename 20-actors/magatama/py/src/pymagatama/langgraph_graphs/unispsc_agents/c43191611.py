from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): data: Dict; logs: List[str]; def validate_specs(state: GraphState): return {'logs': state['logs'] + ['Validating GPU thermal and power specs']}; def run_compatibility_check(state: GraphState): return {'logs': state['logs'] + ['Checking compatibility with host motherboard']}; builder = StateGraph(GraphState); builder.add_node('validate', validate_specs); builder.add_node('compatibility', run_compatibility_check); builder.set_entry_point('validate'); builder.add_edge('validate', 'compatibility'); builder.add_edge('compatibility', END); graph = builder.compile()