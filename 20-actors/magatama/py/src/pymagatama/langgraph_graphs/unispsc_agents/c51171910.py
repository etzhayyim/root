from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): content: str; activity_level: float; status: str; def validate_purity(state: ProcState) -> ProcState: return {'status': 'validated' if state['activity_level'] > 0.8 else 'rejected'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()