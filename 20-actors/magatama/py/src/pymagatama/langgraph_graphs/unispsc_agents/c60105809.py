from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validated: bool; errors: List[str]; def validate_material(state): validated = all('Color Fastness' in item for item in state['items']); return {'validated': validated, 'errors': [] if validated else ['Missing Color Fastness specification']}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()