from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ModelingCompoundState(TypedDict): input_data: dict; validation_results: List[str]; approved: bool; def validate_compound(state: ModelingCompoundState): schema = ['safety_cert', 'plasticity_index']; results = [k for k in schema if k not in state['input_data']]; return {'validation_results': results, 'approved': len(results) == 0}; graph = StateGraph(ModelingCompoundState); graph.add_node('validate', validate_compound); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()