from langgraph.graph import StateGraph, END; from typing import TypedDict; class RopeState(TypedDict): tensile_strength: float; diameter: float; status: str; def validate_rope(state: RopeState): return {'status': 'approved' if state['tensile_strength'] > 10.0 else 'rejected'}; graph = StateGraph(RopeState); graph.add_node('validate', validate_rope); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()