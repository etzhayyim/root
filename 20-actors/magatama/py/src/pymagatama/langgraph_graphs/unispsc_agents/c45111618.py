from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): unit_id: str; spec_data: dict; approved: bool; def validate_lightbox(state: State): specs = state['spec_data']; is_valid = all(key in specs for key in ['lumens', 'power']); return {'approved': is_valid}; graph = StateGraph(State); graph.add_node('validate', validate_lightbox); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()