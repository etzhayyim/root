from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PhaseMeterState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: PhaseMeterState): errors = []; if 'accuracy' not in state['specs']: errors.append('Missing accuracy spec'); return {'validation_errors': errors, 'approved': len(errors) == 0}; def export_compliance(state: PhaseMeterState): return {'approved': state['approved'] and state['specs'].get('dual_use', False) == False}; graph = StateGraph(PhaseMeterState); graph.add_node('validate', validate_specs); graph.add_node('export_check', export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()