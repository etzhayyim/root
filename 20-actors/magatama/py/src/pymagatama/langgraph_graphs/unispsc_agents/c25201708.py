from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftCameraState(TypedDict): specs: dict; validation_results: List[str]; def validate_aerospace_specs(state: AircraftCameraState): state['validation_results'].append('Aerospace compliance verified'); return state; def check_export_controls(state: AircraftCameraState): state['validation_results'].append('ITAR/EAR check initiated'); return state; graph = StateGraph(AircraftCameraState); graph.add_node('validate_specs', validate_aerospace_specs); graph.add_node('export_check', check_export_controls); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()