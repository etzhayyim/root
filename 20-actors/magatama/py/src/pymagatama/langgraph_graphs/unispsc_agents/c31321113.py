from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): material_check: str; compliance_verified: bool; approval_status: str; def validate_spec(state: ProcureState): state['compliance_verified'] = True; return state; def check_availability(state: ProcureState): state['approval_status'] = 'PENDING'; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.add_node('check', check_availability); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()