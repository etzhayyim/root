from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_code: str; purity_check: bool; compliance_docs: List[str]; def validate_purity(state: ProcureState): return {'purity_check': True} if 'certificate_of_analysis' in state['compliance_docs'] else {'purity_check': False}; def check_regulations(state: ProcureState): print('Checking pharma standards for Mg-Trisilicate'); return {}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulations); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()