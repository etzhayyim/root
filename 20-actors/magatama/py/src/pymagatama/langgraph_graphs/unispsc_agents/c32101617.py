from langgraph.graph import StateGraph, END; from typing import TypedDict; class SmartCardState(TypedDict): card_type: str; specs: dict; valid: bool; def validate_specs(state: SmartCardState): state['valid'] = 'ISO/IEC' in state['specs'].get('cert', ''); return state; graph = StateGraph(SmartCardState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()