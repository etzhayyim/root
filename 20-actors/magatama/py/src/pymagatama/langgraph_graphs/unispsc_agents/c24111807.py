from langgraph.graph import StateGraph, END; from typing import TypedDict; class TankState(TypedDict): pressure_rating: float; capacity: float; status: str; def validate_spec(state: TankState): return {'status': 'validated' if state['pressure_rating'] > 0 else 'failed'}; graph = StateGraph(TankState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()