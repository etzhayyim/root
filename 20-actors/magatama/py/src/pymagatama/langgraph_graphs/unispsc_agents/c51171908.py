from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): supplier_data: dict; compliance_docs: list; validation_status: bool; graph = StateGraph(PharmState); def check_regulatory(state): return {'validation_status': 'GMP' in state['compliance_docs']}; def verify_supplier(state): return {'supplier_data': {'verified': True}}; graph.add_node('regulatory_check', check_regulatory); graph.add_node('supplier_verification', verify_supplier); graph.set_entry_point('regulatory_check'); graph.add_edge('regulatory_check', 'supplier_verification'); graph.add_edge('supplier_verification', END); app = graph.compile()