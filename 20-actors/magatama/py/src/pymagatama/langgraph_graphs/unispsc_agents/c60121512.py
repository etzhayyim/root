from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WatercolorSpec(TypedDict): items: List[str]; validated: bool; def validate_specs(state: WatercolorSpec): state['validated'] = all('cert' in item.lower() for item in state['items']); return state; def check_quality(state: WatercolorSpec): return state; graph = StateGraph(WatercolorSpec); graph.add_node('validation', validate_specs); graph.add_node('quality', check_quality); graph.set_entry_point('validation'); graph.add_edge('validation', 'quality'); graph.add_edge('quality', END); app = graph.compile()