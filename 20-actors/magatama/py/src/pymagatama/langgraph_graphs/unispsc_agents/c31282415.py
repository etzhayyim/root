from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: ProcState): state['validation_results'].append('Material grade validated.'); return state; def check_export_control(state: ProcState): state['validation_results'].append('Export compliance cleared.'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_materials); graph.add_node('export', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()