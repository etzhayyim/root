from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): vendor_id: str; cargo_type: str; specs: dict; validated: bool; def check_compliance(state: State): state['validated'] = True if state['specs'].get('load_capacity') > 0 else False; return state; graph = StateGraph(State); graph.add_node('compliance', check_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', END); graph = graph.compile()