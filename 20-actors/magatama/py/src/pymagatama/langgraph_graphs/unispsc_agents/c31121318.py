from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): input_data: dict; quality_report: dict; approved: bool; def validate_casting(state): return {'quality_report': {'status': 'pending'}, 'approved': False}; def check_specs(state): state['approved'] = True; return state; graph = StateGraph(CastState); graph.add_node('validate', validate_casting); graph.add_node('check_specs', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_specs'); graph.add_edge('check_specs', END); app = graph.compile()