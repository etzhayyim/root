from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): data: dict; safety_checked: bool; def check_safety(state: State): state['safety_checked'] = 'flammability_test' in state['data']; return state; def validate_specs(state: State): return {'data': state['data']}; graph = StateGraph(State); graph.add_node('safety', check_safety); graph.add_node('specs', validate_specs); graph.set_entry_point('safety'); graph.add_edge('safety', 'specs'); graph.add_edge('specs', END); app = graph.compile()