from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class IronState(TypedDict): input_data: dict; validation_report: list; status: str; def validate_purity(state: IronState): purity = state['input_data'].get('purity_percentage', 0); return {'validation_report': ['Purity OK' if purity > 99.0 else 'Purity Fail']}; def check_safety(state: IronState): return {'status': 'Flagged for DG' if state['input_data'].get('is_flammable') else 'Standard'}; graph = StateGraph(IronState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()