from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserProcState(TypedDict): vendor_specs: dict; compliance_check: bool; final_report: str; def validate_specs(state: LaserProcState): state['compliance_check'] = state['vendor_specs'].get('power') > 50; return state; def generate_report(state: LaserProcState): state['final_report'] = 'Compliance Verified' if state['compliance_check'] else 'Manual Review Required'; return state; graph = StateGraph(LaserProcState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()