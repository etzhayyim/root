from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class PaperProcurementState(TypedDict): specs: Dict; validation_results: List[str]; def validate_paper_specs(state: PaperProcurementState): print('Validating paper specs...'); results = []; if state['specs'].get('basis_weight_gsm', 0) < 50: results.append('Invalid weight'); return {'validation_results': results}; def finalize_procurement(state: PaperProcurementState): return {'validation_results': state['validation_results'] + ['Approval complete']}; graph = StateGraph(PaperProcurementState); graph.add_node('validate', validate_paper_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()