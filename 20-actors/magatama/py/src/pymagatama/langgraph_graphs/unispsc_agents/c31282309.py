from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): component_id: str; lead_purity: float; specs_met: bool; inspection_logs: List[str]; def validate_purity(state: ProcurementState): state['specs_met'] = state['lead_purity'] >= 99.9; state['inspection_logs'].append('Purity validation complete.'); return state; def check_hazmat(state: ProcurementState): state['inspection_logs'].append('Hazmat compliance verified.'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_purity); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()