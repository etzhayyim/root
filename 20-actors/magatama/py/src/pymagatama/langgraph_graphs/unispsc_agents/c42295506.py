from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; data: dict; def validate_implant(state: State): state['status'] = 'COMPLIANT' if 'ISO_13485' in state['data'] else 'REJECTED'; return state; graph = StateGraph(State); graph.add_node('validate', validate_implant); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()