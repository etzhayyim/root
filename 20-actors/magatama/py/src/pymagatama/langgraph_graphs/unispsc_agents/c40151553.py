from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class PumpState(TypedDict): pump_specs: dict; validation_report: str; def validate_pump(state: PumpState): return {"validation_report": "Specs verified against ISO standards" if "pressure" in state["pump_specs"] else "Missing pressure rating"}; graph = StateGraph(PumpState); graph.add_node("validate", validate_pump); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()