from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class HemacytometerState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_instrument(state: HemacytometerState): if state['specs'].get('precision') < 0.002: return {'approved': True}; return {'approved': False, 'validation_errors': ['Precision out of tolerance']}; graph = StateGraph(HemacytometerState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()