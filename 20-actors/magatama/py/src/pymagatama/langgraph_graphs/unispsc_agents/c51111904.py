from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PaclitaxelState(TypedDict): status: str; spec_check: bool; compliance_docs: List[str]; graph = StateGraph(PaclitaxelState); def validate_purity(state: PaclitaxelState): return {'spec_check': True} if 'batch_report' in state['compliance_docs'] else {'spec_check': False}; def check_cold_chain(state: PaclitaxelState): return {'status': 'READY' if state['spec_check'] else 'REJECTED'}; graph.add_node('validate', validate_purity); graph.add_node('compliance', check_cold_chain); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()