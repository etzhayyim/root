from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec_data: dict; validation_log: list; graph = StateGraph(State); def validate_specs(state: State): state['validation_log'] = ['Checking pressure ratings', 'Verifying material certification']; state['status'] = 'VALIDATED' if state['spec_data'].get('pressure_rating') else 'INCOMPLETE'; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()