from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BurnerState(TypedDict): burner_id: str; specifications: dict; compliance_check: bool; def validate_specs(state: BurnerState): state['compliance_check'] = 'BTU_Rating' in state['specifications'] and 'Fuel_Type' in state['specifications']; return state; def final_approval(state: BurnerState): return {'compliance_check': state['compliance_check']}; graph = StateGraph(BurnerState); graph.add_node('validate', validate_specs); graph.add_node('approval', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); compiled_graph = graph.compile()