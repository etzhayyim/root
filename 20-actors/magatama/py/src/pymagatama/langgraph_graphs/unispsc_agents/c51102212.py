from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_data: dict; compliance_flag: bool; def validate_spec(state: ProcState): purity = state['chemical_data'].get('purity', 0); state['compliance_flag'] = purity >= 99.0; return state; def check_hazmat(state: ProcState): print('Checking SDS for Acetohydroxamic Acid'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.add_node('hazard_check', check_hazmat); graph.set_entry_point('hazard_check'); graph.add_edge('hazard_check', 'validate'); graph.add_edge('validate', END); graph = graph.compile()