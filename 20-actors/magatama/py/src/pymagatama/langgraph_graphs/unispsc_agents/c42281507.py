from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadiationState(TypedDict): assembly_required: bool; safety_certs: List[str]; validation_status: str; def validate_safety(state: RadiationState): state['validation_status'] = 'verified' if 'radiation_safety_iso' in state['safety_certs'] else 'rejected'; return state; def check_assembly(state: RadiationState): state['assembly_required'] = True; return state; graph = StateGraph(RadiationState); graph.add_node('safety_check', validate_safety); graph.add_node('assembly_check', check_assembly); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'assembly_check'); graph.add_edge('assembly_check', END); graph = graph.compile()