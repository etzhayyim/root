from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RegisterState(TypedDict): vendor: str; specs: dict; compliant: bool; def validate_specs(state: RegisterState): state['compliant'] = 'fiscal_compliance_standard' in state['specs']; return state; def finalize(state: RegisterState): return {'status': 'processed'}; graph = StateGraph(RegisterState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()