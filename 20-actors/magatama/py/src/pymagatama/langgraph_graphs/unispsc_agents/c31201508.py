from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class GraphiteState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: GraphiteState): specs = state.get('specs', {}); errors = []; if specs.get('purity', 0) < 95: errors.append('Insufficient carbon purity'); return {'validation_errors': errors, 'status': 'validated' if not errors else 'rejected'}; graph = StateGraph(GraphiteState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()