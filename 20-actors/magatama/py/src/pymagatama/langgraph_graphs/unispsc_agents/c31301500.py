from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): material_data: dict; valid: bool; def validate_forgings(state: State): state['valid'] = all(k in state['material_data'] for k in ['alloy', 'tensile_strength', 'heat_treat']); return state; graph = StateGraph(State); graph.add_node('validate', validate_forgings); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()