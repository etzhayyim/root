from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): kit_id: str; specs: dict; valid: bool; def validate_specs(state: State): return {'valid': 'voltage' in state['specs']}; def finalize_order(state: State): return {'valid': True}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()