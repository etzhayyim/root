from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DockLadderState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: DockLadderState): errors = []; if state['specs'].get('load_capacity', 0) < 200: errors.append('Load capacity below industrial standard'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(DockLadderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()