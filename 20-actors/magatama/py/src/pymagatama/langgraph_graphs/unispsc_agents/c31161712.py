from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HardwareState(TypedDict): vendor_id: str; specs: dict; validation_passed: bool; def validate_specs(state: HardwareState): state['validation_passed'] = all(k in state['specs'] for k in ['material', 'thread', 'grade']); return state; def check_compliance(state: HardwareState): return 'compliance_check' if state['validation_passed'] else END; graph = StateGraph(HardwareState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()