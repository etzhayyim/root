from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VesselState(TypedDict): vessel_id: str; compliance_checks: List[str]; approved: bool; def validate_specs(state: VesselState): state['compliance_checks'].append('Maritime Standards Pass'); return state; def security_clearance(state: VesselState): state['approved'] = True; return state; graph = StateGraph(VesselState); graph.add_node('validate', validate_specs); graph.add_node('security', security_clearance); graph.set_entry_point('validate'); graph.add_edge('validate', 'security'); graph.add_edge('security', END); compiled_graph = graph.compile()