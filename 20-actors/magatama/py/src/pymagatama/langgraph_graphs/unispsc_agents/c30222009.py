from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RailStationState(TypedDict): status: str; safety_checks: List[str]; project_phase: str; def validate_compliance(state: RailStationState): return {'safety_checks': ['structural_integrity', 'fire_codes']}; def finalize_planning(state: RailStationState): return {'status': 'approved'}; graph = StateGraph(RailStationState); graph.add_node('validate', validate_compliance); graph.add_node('finalize', finalize_planning); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()