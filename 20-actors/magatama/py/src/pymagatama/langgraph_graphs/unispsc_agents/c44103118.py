from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_log: Annotated[list, operator.add]; graph = StateGraph(State); def validate_spec(state: State): return {'validation_log': ['Checking dimensions', 'Verifying heat resistance compatibility']}; def generate_report(state: State): return {'validation_log': ['Report generated successfully']}; graph.add_node('validate', validate_spec); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()