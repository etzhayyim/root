from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PaperTestState(TypedDict): test_type: str; specs: dict; validation: bool; def validate_instrument(state: PaperTestState): print('Validating instrument calibration...'); state['validation'] = True; return state; graph = StateGraph(PaperTestState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()