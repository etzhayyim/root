from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BodyToolState(TypedDict): tool_id: str; specs: dict; status: str; def validate_specs(state: BodyToolState): return {'status': 'validated'} if state.get('specs') else {'status': 'rejected'}; def inspect_tools(state: BodyToolState): return {'status': 'inspected'}; graph = StateGraph(BodyToolState); graph.add_node('validate', validate_specs); graph.add_node('inspect', inspect_tools); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()