from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; class MineralState(TypedDict): input_data: dict; validation_results: dict; process_status: str; def validate_purity(state: MineralState) -> MineralState: return {**state, 'validation_results': {'purity': 'passed'}, 'process_status': 'validated'}; def process_refining(state: MineralState) -> MineralState: return {**state, 'process_status': 'refined'}; graph = StateGraph(MineralState); graph.add_node('validate', validate_purity); graph.add_node('refine', process_refining); graph.set_entry_point('validate'); graph.add_edge('validate', 'refine'); graph.add_edge('refine', END); graph = graph.compile()