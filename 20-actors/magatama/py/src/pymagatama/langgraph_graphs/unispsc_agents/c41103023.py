from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChillerState(TypedDict): model_specs: dict; compliance_checks: List[str]; approved: bool; def validate_specs(state: ChillerState): specs = state['model_specs']; checks = []; valid = True; if specs.get('temp_stability', 0) > 0.5: checks.append('High instability'); valid = False; return {'compliance_checks': checks, 'approved': valid}; graph = StateGraph(ChillerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()