from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CortisoneState(TypedDict): medicine_name: str; purity: float; compliance_checked: bool; graph = StateGraph(CortisoneState); def validate_compliance(state: CortisoneState): return {'compliance_checked': state['purity'] >= 99.0}; def log_procurement(state: CortisoneState): print(f'Processing {state["medicine_name"]}'); return state; graph.add_node('validate', validate_compliance); graph.add_node('log', log_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.compile()