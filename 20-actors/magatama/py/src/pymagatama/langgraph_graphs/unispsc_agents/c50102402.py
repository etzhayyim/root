from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProduceState(TypedDict): harvest_date: str; quality_score: float; inspected: bool; def check_freshness(state: ProduceState): return {'inspected': state['quality_score'] > 8.0}; def finalize_order(state: ProduceState): return {'inspected': True}; graph = StateGraph(ProduceState); graph.add_node('inspect', check_freshness); graph.add_node('ship', finalize_order); graph.set_entry_point('inspect'); graph.add_edge('inspect', 'ship'); graph.add_edge('ship', END); graph = graph.compile()