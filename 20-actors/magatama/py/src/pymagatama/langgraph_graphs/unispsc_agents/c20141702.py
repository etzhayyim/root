from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MiningState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: MiningState): specs = state['specs']; state['valid'] = 'material_hardness_hrc' in specs and specs['material_hardness_hrc'] > 50; return {'valid': state['valid']}; def route_processing(state: MiningState): return 'valid' if state['valid'] else END; graph = StateGraph(MiningState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()