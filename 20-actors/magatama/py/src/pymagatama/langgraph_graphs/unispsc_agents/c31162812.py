from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SwageState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: SwageState): state['validation_results'].append('Material grade validated.'); return state; def check_load_rating(state: SwageState): state['validation_results'].append('Load rating within limits.'); return state; graph = StateGraph(SwageState); graph.add_node('validate', validate_material); graph.add_node('load_check', check_load_rating); graph.set_entry_point('validate'); graph.add_edge('validate', 'load_check'); graph.add_edge('load_check', END); graph = graph.compile()