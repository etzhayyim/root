from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): dust_collector_specs: dict; validation_results: List[str]; def validate_specs(state: State): specs = state['dust_collector_specs']; results = []; airflow = specs.get('airflow', 0); results.append('pass' if airflow > 500 else 'fail: insufficient airflow'); return {'validation_results': results}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()