from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SaladState(TypedDict): items: List[str]; qc_passed: bool; shelf_life_check: bool; def validate_cold_chain(state: SaladState): return {'qc_passed': True}; def check_expiry(state: SaladState): return {'shelf_life_check': True}; workflow = StateGraph(SaladState); workflow.add_node('cold_chain', validate_cold_chain); workflow.add_node('expiry', check_expiry); workflow.set_entry_point('cold_chain'); workflow.add_edge('cold_chain', 'expiry'); workflow.add_edge('expiry', END); graph = workflow.compile()