from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class LaserProcState(TypedDict): specs: dict; compliance_cleared: bool; graph = StateGraph(LaserProcState); def validate_specs(state: LaserProcState): return {'compliance_cleared': state['specs'].get('power_kw', 0) > 0}; def check_safety_standards(state: LaserProcState): return {'compliance_cleared': True}; graph.add_node('spec_validation', validate_specs); graph.add_node('safety_check', check_safety_standards); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', 'safety_check'); graph.add_edge('safety_check', END); compiled_graph = graph.compile()