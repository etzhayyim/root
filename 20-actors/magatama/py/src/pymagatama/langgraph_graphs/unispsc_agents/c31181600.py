from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SealState(TypedDict): seal_type: str; specs: dict; validation_passed: bool; def validate_specs(state: SealState): state['validation_passed'] = all(k in state['specs'] for k in ['pressure', 'material']); return state; def route_logic(state: SealState): return 'valid' if state['validation_passed'] else END; graph = StateGraph(SealState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()