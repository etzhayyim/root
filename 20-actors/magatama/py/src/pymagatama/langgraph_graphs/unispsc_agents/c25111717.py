from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MineShipState(TypedDict): ship_specs: dict; compliance_checks: List[str]; def validate_specs(state: MineShipState): state['compliance_checks'].append('Naval Standard Verified'); return state; def security_audit(state: MineShipState): state['compliance_checks'].append('Export Control Cleared'); return state; graph = StateGraph(MineShipState); graph.add_node('validate', validate_specs); graph.add_node('audit', security_audit); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); graph.set_entry_point('validate'); compile_graph = graph.compile()