from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LeverState(TypedDict): specs: dict; valid: bool; workflow_steps: List[str]; def validate_specs(state: LeverState): state['valid'] = 'Material Grade' in state['specs'] and 'Load Capacity' in state['specs']; return state; def finalize_order(state: LeverState): state['workflow_steps'].append('Quality Inspection Complete'); return state; graph = StateGraph(LeverState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()