from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): doc_details: dict; validation_status: bool; def validate_holder(state: State): state['validation_status'] = 'material' in state['doc_details'] and 'dimensions' in state['doc_details']; return state; builder = StateGraph(State); builder.add_node('validate', validate_holder); builder.add_edge('validate', END); builder.set_entry_point('validate'); graph = builder.compile()