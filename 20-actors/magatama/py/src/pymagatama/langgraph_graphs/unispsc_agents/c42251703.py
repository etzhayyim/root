from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReharbSpec(TypedDict): specs: dict; compliance_check: bool; def validate_specs(state: ReharbSpec): state['compliance_check'] = 'Load Capacity' in state['specs'] and 'ISO 13485' in state['specs']; return state; builder = StateGraph(ReharbSpec); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()