from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SeatbeltState(TypedDict): vendor_certs: List[str]; safety_data: dict; approved: bool; def validate_cert(state: SeatbeltState): state['approved'] = 'ISO-TEST' in state['vendor_certs']; return {'approved': state['approved']}; def check_safety(state: SeatbeltState): return 'approved' if state['approved'] else 'rejected'; graph = StateGraph(SeatbeltState); graph.add_node('validation', validate_cert); graph.add_node('safety_check', check_safety); graph.set_entry_point('validation'); graph.add_edge('validation', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()