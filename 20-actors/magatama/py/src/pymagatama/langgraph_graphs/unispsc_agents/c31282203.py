from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BrassState(TypedDict): specs: dict; validation_results: List[str]; def validate_tolerance(state: BrassState): return {'validation_results': ['Tolerance check passed']}; def perform_material_test(state: BrassState): return {'validation_results': ['Material test passed']}; graph = StateGraph(BrassState); graph.add_node('tolerance', validate_tolerance); graph.add_node('material', perform_material_test); graph.add_edge('tolerance', 'material'); graph.add_edge('material', END); graph.set_entry_point('tolerance'); compiled_graph = graph.compile()