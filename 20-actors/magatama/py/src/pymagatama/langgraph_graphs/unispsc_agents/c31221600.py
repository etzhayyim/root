from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TanningState(TypedDict): chemical_data: dict; compliance_checks: List[str]; approved: bool; def validate_chemical(state: TanningState): state['compliance_checks'].append('SDS_VERIFIED'); return {'approved': True} if 'SDS_VERIFIED' in state['compliance_checks'] else {'approved': False}; graph = StateGraph(TanningState); graph.add_node('validate', validate_chemical); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()