from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProstheticState(TypedDict): spec: dict; validated: bool; errors: List[str]; def validate_clamp(state: ProstheticState): spec = state['spec']; errors = []; if not spec.get('ISO-10328-Compliance'): errors.append('Missing ISO 10328 certification'); return {'validated': len(errors) == 0, 'errors': errors}; graph = StateGraph(ProstheticState); graph.add_node('validation', validate_clamp); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()