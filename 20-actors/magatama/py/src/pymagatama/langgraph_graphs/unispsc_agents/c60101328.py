from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): content: str; is_approved: bool; def validate_content(state: State): return {'is_approved': True if len(state['content']) > 0 else False}; graph = StateGraph(State); graph.add_node('validate', validate_content); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()