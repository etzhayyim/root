from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScooterState(TypedDict): accessory_type: str; compatibility_check: bool; safety_cert: bool; def validate_compatibility(state: ScooterState): state['compatibility_check'] = True; return state; def check_medical_safety(state: ScooterState): state['safety_cert'] = True; return state; graph = StateGraph(ScooterState); graph.add_node('validate', validate_compatibility); graph.add_node('safety', check_medical_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()