from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; valid: bool; def check_compliance(state: State): state['valid'] = 'certifications' in state['specs']; return state; builder = StateGraph(State); builder.add_node('check', check_compliance); builder.set_entry_point('check'); builder.add_edge('check', END); graph = builder.compile()