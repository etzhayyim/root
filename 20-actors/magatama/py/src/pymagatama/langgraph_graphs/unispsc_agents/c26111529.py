from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input: dict; valid: bool; validation_log: str; def validate_spec(state: State): state['valid'] = 'torque_capacity_nm' in state['input'] and state['input']['torque_capacity_nm'] > 0; state['validation_log'] = 'Validation successful' if state['valid'] else 'Missing or invalid torque specs'; return state; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()