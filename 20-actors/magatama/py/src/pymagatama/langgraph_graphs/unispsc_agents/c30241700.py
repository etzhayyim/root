from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StructureState(TypedDict): specs: dict; validation: bool; components: List[str]; def validate_specs(state: StructureState): return {'validation': all(key in state['specs'] for key in ['wind_load', 'fire_rating'])}; def approve_procurement(state: StructureState): return {'validation': True}; graph = StateGraph(StructureState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()