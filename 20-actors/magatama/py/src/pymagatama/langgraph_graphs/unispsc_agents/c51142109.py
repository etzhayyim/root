from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; validation_status: bool; compliance_docs: List[str]; def validate_pharmaceutical(state: PharmState): state['validation_status'] = 'USP' in state['compliance_docs']; return state; def check_regulatory(state: PharmState): print(f'Checking import/export laws for {state['chemical_name']}'); return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_pharmaceutical); graph.add_node('compliance', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()