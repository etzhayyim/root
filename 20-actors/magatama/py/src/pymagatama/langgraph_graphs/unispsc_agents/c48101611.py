from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScaleSpec(TypedDict): model: str; compliance: bool; capacity_kg: float; graph = StateGraph(ScaleSpec); def validate_compliance(state: ScaleSpec): return {'compliance': state.get('capacity_kg', 0) > 0}; def verify_specs(state: ScaleSpec): return {'model': 'VALIDATED_' + state['model']}; graph.add_node('validate', validate_compliance); graph.add_node('verify', verify_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); compiled_graph = graph.compile()