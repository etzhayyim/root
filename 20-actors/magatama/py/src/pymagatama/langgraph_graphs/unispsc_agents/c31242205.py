from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OpticalSpecState(TypedDict): specs: dict; validation_errors: List[str]; def validate_waveplate(state: OpticalSpecState): specs = state['specs']; errors = []; if specs.get('damage_threshold', 0) < 1.0: errors.append('Damage threshold below safety limit'); return {'validation_errors': errors}; graph = StateGraph(OpticalSpecState); graph.add_node('validate', validate_waveplate); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()