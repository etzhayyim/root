from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SecurityHardwareState(TypedDict): hardware_specs: dict; security_clearance: bool; approved: bool; def validate_specs(state: SecurityHardwareState): state['approved'] = 'Security Rating' in state['hardware_specs']; return {'approved': state['approved']}; def check_export_control(state: SecurityHardwareState): state['security_clearance'] = True; return {'security_clearance': True}; graph = StateGraph(StateHardwareState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('export_check'); graph.add_edge('export_check', 'validate'); graph.add_edge('validate', END); graph = graph.compile()