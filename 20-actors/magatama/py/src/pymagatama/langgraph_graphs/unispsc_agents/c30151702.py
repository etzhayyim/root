from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DripCapState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: DripCapState): errors = []; if not state['specs'].get('material'): errors.append('Missing material spec'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(DripCapState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()