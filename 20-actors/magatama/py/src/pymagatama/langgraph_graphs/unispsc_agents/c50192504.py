from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FillerState(TypedDict): ingredients: dict; safety_checked: bool; temp_logs: List[float]; def check_safety(state: FillerState): return {'safety_checked': all(t < -18 for t in state['temp_logs'])}; def validate_specs(state: FillerState): return {'safety_checked': state['ingredients'].get('allergen_free', False)}; graph = StateGraph(FillerState); graph.add_node('check_safety', check_safety); graph.add_node('validate_specs', validate_specs); graph.set_entry_point('check_safety'); graph.add_edge('check_safety', 'validate_specs'); graph.add_edge('validate_specs', END); app = graph.compile()