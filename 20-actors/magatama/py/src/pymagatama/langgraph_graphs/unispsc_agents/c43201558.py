from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AIState(TypedDict): model_id: str; payload: dict; status: str; def validate_model(state: AIState): return {'status': 'validated' if 'model_id' in state else 'error'}; def execute_model(state: AIState): return {'status': 'processed'}; graph = StateGraph(AIState); graph.add_node('validate', validate_model); graph.add_node('execute', execute_model); graph.set_entry_point('validate'); graph.add_edge('validate', 'execute'); graph.add_edge('execute', END); graph = graph.compile()