from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): data: dict; approval_status: bool; def validate_gmp(state: PharmState): state['approval_status'] = 'GMP_CERT' in state['data'] and state['data']['purity'] >= 99.0; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()