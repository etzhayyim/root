from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StorageState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: StorageState): results = []; if 'capacity' not in state['specs']: results.append('Missing capacity'); return {'validation_results': results}; def check_export_controls(state: StorageState): return {'validation_results': state['validation_results'] + ['Export control check passed']}; graph = StateGraph(StorageState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()