from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TSquareState(TypedDict): specs: dict; validation_report: List[str]; def validate_t_square(state: TSquareState): report = []; if state['specs'].get('accuracy', 0) > 0.5: report.append('Accuracy tolerance exceeds standard'); return {'validation_report': report}; graph = StateGraph(TSquareState); graph.add_node('validate', validate_t_square); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()