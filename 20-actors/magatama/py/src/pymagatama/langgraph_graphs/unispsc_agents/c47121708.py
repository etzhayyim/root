from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplyChainState(TypedDict): bag_specs: dict; compliance_check: bool; def validate_specs(state: SupplyChainState): thickness = state['bag_specs'].get('thickness', 0); state['compliance_check'] = thickness >= 20; return state; def finalize_order(state: SupplyChainState): return {'final_status': 'approved' if state['compliance_check'] else 'rejected'}; graph = StateGraph(SupplyChainState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()