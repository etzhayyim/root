from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SupplementState(TypedDict): ingredient_list: list; safety_check: bool; graph = StateGraph(SupplementState); def validate_purity(state: SupplementState): return {'safety_check': all('amino_acid' in item for item in state['ingredient_list'])}; graph.add_node('purity_test', validate_purity); graph.set_entry_point('purity_test'); graph.add_edge('purity_test', END); graph = graph.compile()