from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): tray_specs: dict; status: str; def validate_specs(state: State): specs = state.get('tray_specs', {}); return {'status': 'PASSED' if 'material' in specs else 'FAILED'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()