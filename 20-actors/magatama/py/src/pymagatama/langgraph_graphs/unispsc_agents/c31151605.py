from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SashState(TypedDict): chain_type: str; tensile_strength: int; quality_passed: bool; def validate_spec(state: SashState): state['quality_passed'] = state['tensile_strength'] > 500; return state; graph = StateGraph(SashState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()