from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_dimensions(state: ForgingState): tolerance = state['specs'].get('tolerance', 0.05); return {'validation_report': f'Checked to {tolerance}mm', 'approved': True}; def update_material_check(state: ForgingState): return {'approved': state['specs'].get('grade') == 'C36000'}; graph = StateGraph(ForgingState); graph.add_node('validate', validate_dimensions); graph.add_node('material', update_material_check); graph.set_entry_point('material'); graph.add_edge('material', 'validate'); graph.add_edge('validate', END); compile = graph.compile()