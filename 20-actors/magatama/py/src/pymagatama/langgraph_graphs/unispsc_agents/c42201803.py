from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): tags: list[str]; spec: dict; safety_check: bool; def validate_safety(state: ProcState) -> ProcState: state['safety_check'] = 'regulated-health-product' in state['tags']; return state; def route_step(state: ProcState) -> str: return 'safety_check' if state['safety_check'] else END; graph = StateGraph(ProcState); graph.add_node('validate', validate_safety); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()