from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MobilityState(TypedDict): specs: dict; compliant: bool; errors: List[str]; def validate_medical_specs(state: MobilityState): state['compliant'] = state['specs'].get('max_weight') > 0 and 'ISO_11199' in state['specs'].get('certs', []); return state; graph = StateGraph(MobilityState); graph.add_node('validate', validate_medical_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()