from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: ProcState): validated = all(k in state['specs'] for k in ['voltage', 'certification']); return {'validation_results': ['PASS' if validated else 'FAIL'], 'status': 'COMPLETED'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()