from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; gmp_compliant: bool; assays: List[str]; graph = StateGraph(PharmState); def validate_gmp(state: PharmState): return {'gmp_compliant': True} if 'GMP' in state.get('assays', []) else {'gmp_compliant': False}; def final_check(state: PharmState): return {'chemical_name': 'Rabeprazole Sodium'}; graph.add_node('validate', validate_gmp); graph.add_node('final', final_check); graph.add_edge('validate', 'final'); graph.add_edge('final', END); graph.set_entry_point('validate'); app = graph.compile()