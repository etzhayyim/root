from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): input_data: dict; validation_results: Annotated[Sequence[str], operator.add]; compliance_flag: bool; def validate_cnc_specs(state: MachineState) -> MachineState: return {**state, 'validation_results': ['CNC Tolerance Check Passed'], 'compliance_flag': True}; def check_export_controls(state: MachineState) -> MachineState: return {**state, 'validation_results': ['Export License Check: Standard']}; graph = StateGraph(MachineState); graph.add_node('validate', validate_cnc_specs); graph.add_node('compliance', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()