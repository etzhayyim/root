from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: RobotProcurementState): payload = state['specs'].get('payload', 0); state['approved'] = payload > 0; return state; graph = StateGraph(RobotProcurementState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()