from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class NetworkSwitchState(TypedDict): vendor_specs: dict; compliance_score: float; validated: bool; def validate_specs(state: NetworkSwitchState): state['validated'] = state['vendor_specs'].get('ports', 0) > 0; return state; graph = StateGraph(NetworkSwitchState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()