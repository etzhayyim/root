from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): model_specs: dict; validation_results: List[str]; graph = StateGraph(AgentState); def validate_accuracy(state): return {'validation_results': ['Accuracy Verified']}; def check_safety(state): return {'validation_results': ['Material Safety Pass']}; graph.add_node('validate_accuracy', validate_accuracy); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate_accuracy'); graph.add_edge('validate_accuracy', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()