from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ActuatorState(TypedDict): input_spec: dict; validation_results: Annotated[list[str], operator.add]; status: str; def validate_specs(state: ActuatorState): results = []; if state['input_spec'].get('torque_nm', 0) <= 0: results.append('Invalid Torque'); return {'validation_results': results, 'status': 'validated' if not results else 'error'}; def compile_graph(): graph = StateGraph(ActuatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()