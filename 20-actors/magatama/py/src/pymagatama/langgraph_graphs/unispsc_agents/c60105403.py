from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): content: str; validated: bool; compliance_check: List[str]; def validate_materials(state: State): return {'validated': True, 'compliance_check': ['accessibility_pass']}; graph = StateGraph(State); graph.add_node('validate', validate_materials); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()