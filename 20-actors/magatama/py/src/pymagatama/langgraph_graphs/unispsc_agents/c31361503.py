from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): specs: List[str]; validated: bool; def validate_weld_integrity(state: SpecState): print('Validating weld...'); return {'validated': True}; graph = StateGraph(SpecState); graph.add_node('weld_check', validate_weld_integrity); graph.set_entry_point('weld_check'); graph.add_edge('weld_check', END); graph = graph.compile()