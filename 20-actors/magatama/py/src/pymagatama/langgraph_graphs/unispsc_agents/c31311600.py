from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): pipe_specs: dict; validation_results: List[str]; def validate_material(state: PipeState): state['validation_results'].append('Material verified'); return state; def validate_weld_integrity(state: PipeState): state['validation_results'].append('Weld integrity certified'); return state; graph = StateGraph(PipeState); graph.add_node('validate_mat', validate_material); graph.add_node('validate_weld', validate_weld_integrity); graph.set_entry_point('validate_mat'); graph.add_edge('validate_mat', 'validate_weld'); graph.add_edge('validate_weld', END); graph = graph.compile()