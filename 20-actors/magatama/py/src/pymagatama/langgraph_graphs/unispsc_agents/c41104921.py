from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: FilterState) -> dict: required = ['Pore size rating', 'Chemical compatibility']; state['validation_passed'] = all(k in state['specs'] for k in required); return state; def check_compliance(state: FilterState) -> str: return 'pass' if state['validation_passed'] else 'fail'; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()