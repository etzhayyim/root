from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MagnifierState(TypedDict): magnification: float; lens_type: str; is_compliant: bool; def validate_magnifier(state: MagnifierState): state['is_compliant'] = state['magnification'] > 0 and 'glass' in state['lens_type']; return state; graph = StateGraph(MagnifierState); graph.add_node('validate', validate_magnifier); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()