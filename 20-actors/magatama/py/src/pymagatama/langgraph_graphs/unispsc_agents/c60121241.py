from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CleaningSupplyState(TypedDict): items: List[str]; validation_results: dict; workflow_status: str; def validate_msds(state: CleaningSupplyState): state['validation_results'] = {'msds_check': 'passed'}; state['workflow_status'] = 'SAFETY_VERIFIED'; return state; def prepare_shipping(state: CleaningSupplyState): state['workflow_status'] = 'READY_TO_SHIP'; return state; graph = StateGraph(CleaningSupplyState); graph.add_node('validate', validate_msds); graph.add_node('ship', prepare_shipping); graph.add_edge('validate', 'ship'); graph.add_edge('ship', END); graph.set_entry_point('validate'); graph = graph.compile()