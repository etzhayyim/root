from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class VibrationTesterState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: VibrationTesterState): return {'compliance': 'FrequencyRange_Hz' in state['specs']}; def approval_node(state: VibrationTesterState): return {'approved': state['compliance']}; graph = StateGraph(VibrationTesterState); graph.add_node('validate', validate_specs); graph.add_node('approve', approval_node); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph_comp = graph.compile()