from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrayState(TypedDict): tray_id: str; components: List[str]; validated: bool; def validate_tray(state: TrayState): state['validated'] = len(state['components']) > 0; return state; graph = StateGraph(TrayState); graph.add_node('validate', validate_tray); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()