from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class RollingMachineState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: RollingMachineState): state['validation_passed'] = 'rolling_force_capacity' in state['specs']; return state; def check_export_control(state: RollingMachineState): return {'validation_passed': state['validation_passed'] and True}; graph = StateGraph(RollingMachineState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()