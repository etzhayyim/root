from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TitaniumState(TypedDict): material_spec: str; inspection_results: List[str]; compliant: bool; def validate_material(state: TitaniumState): print('Validating titanium grade...'); return {'compliant': state['material_spec'].startswith('Grade 5')}; def check_export_controls(state: TitaniumState): print('Checking ECCN requirements...'); return {'inspection_results': ['Export Compliance Verified']}; graph = StateGraph(TitaniumState); graph.add_node('Validate', validate_material); graph.add_node('ExportCheck', check_export_controls); graph.set_entry_point('Validate'); graph.add_edge('Validate', 'ExportCheck'); graph.add_edge('ExportCheck', END); graph = graph.compile()