from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class HealthState(TypedDict): device_id: str; compliance_check: bool; metadata: dict; def validate_medical_device(state: HealthState): state['compliance_check'] = True; return state; graph = StateGraph(HealthState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()