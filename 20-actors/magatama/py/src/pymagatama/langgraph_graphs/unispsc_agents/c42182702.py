from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec: dict; validated: bool; def validate_tape(state: ProcState): return {'validated': 'precision_class' in state['spec']}; graph = StateGraph(ProcState); graph.add_node('validate', validate_tape); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()