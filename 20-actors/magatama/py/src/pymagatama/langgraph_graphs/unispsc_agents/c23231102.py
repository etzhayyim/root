from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LaserProcurementState(TypedDict): specs: dict; compliance_check: bool; review_passed: bool; def validate_specs(state: LaserProcurementState): state['compliance_check'] = state['specs'].get('power_output_kw', 0) > 0; return state; def security_review(state: LaserProcurementState): state['review_passed'] = True; return state; graph = StateGraph(LaserProcurementState); graph.add_node('validate', validate_specs); graph.add_node('security', security_review); graph.add_edge('validate', 'security'); graph.add_edge('security', END); graph.set_entry_point('validate');