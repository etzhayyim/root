from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; compliant: bool; state = PharmState(chemical_name='Guanethidine sulfate', purity=0.0, compliant=False); def validate_purity(state: PharmState): state['compliant'] = state['purity'] >= 99.0; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()