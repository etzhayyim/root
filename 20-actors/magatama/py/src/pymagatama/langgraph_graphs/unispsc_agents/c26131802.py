from langgraph.graph import StateGraph, END; from typing import TypedDict; class CompressorState(TypedDict): panel_specs: dict; validation_report: str; def validate_specs(state: CompressorState): return {'validation_report': 'Validated' if state['panel_specs'].get('IP_Rating') else 'Missing IP specs'}; def finalize(state: CompressorState): return {'validation_report': 'Completed'}; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.add_node('final', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'final'); graph.add_edge('final', END); compile_graph = graph.compile()