from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TurbidimeterState(TypedDict): ntu_value: float; spec_verified: bool; validation_log: List[str]; def validation_step(state: TurbidimeterState): state['spec_verified'] = state['ntu_value'] >= 0; state['validation_log'].append('Validation completed'); return state; graph = StateGraph(TurbidimeterState); graph.add_node('validate', validation_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()