from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PAState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_pa_tech(state: PAState): specs = state['specs']; is_valid = all([specs.get('power'), specs.get('standards')]); return {'valid': is_valid, 'logs': ['Spec validated' if is_valid else 'Missing requirements']}; graph = StateGraph(PAState); graph.add_node('validate', validate_pa_tech); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()