from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): model_number: str; yield_check: bool; status: str; def validate_model(state): return {'yield_check': True, 'status': 'model_verified'}; def check_compliance(state): return {'status': 'compliant'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_model); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()