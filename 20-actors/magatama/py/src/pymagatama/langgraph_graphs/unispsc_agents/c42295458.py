from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GloveProdState(TypedDict): equipment_id: str; specs: dict; validation_status: bool; def validate_specs(state: GloveProdState): state['validation_status'] = 'temperature_range' in state['specs'] and 'gmp_certification_level' in state['specs']; return state; def final_approval(state: GloveProdState): return 'APPROVED' if state['validation_status'] else 'REJECTED'; graph = StateGraph(GloveProdState); graph.add_node('validate', validate_specs); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()