from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SandingMachineState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SandingMachineState): errors = []; if state['specs'].get('operating_pressure_bar', 0) > 8: errors.append('Pressure exceeds safety limits'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(SandingMachineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()