from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WeldingState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_laser_specs(state: WeldingState): errors = [] if state['specs'].get('laser_safety_class', 0) < 4: errors.append('Safety class insufficient'); return {'validation_errors': errors}; def finalize_procurement(state: WeldingState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(WeldingState); graph.add_node('validate', validate_laser_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()