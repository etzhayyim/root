from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EyeWearState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: EyeWearState): compliance = state['specs'].get('ANSI_Z87_1'); state['approved'] = compliance is True; return state; graph = StateGraph(EyeWearState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()