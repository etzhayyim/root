from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validated_items: List[str]; def validate_garment(state: State): return { "validated_items": [item for item in state["items"] if "polyester" in item.lower()] }; graph = StateGraph(State); graph.add_node("validate", validate_garment); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()