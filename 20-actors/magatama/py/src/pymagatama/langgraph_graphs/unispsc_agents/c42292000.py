from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalMirrorState(TypedDict): mirror_id: str; compliance_docs: List[str]; inspection_passed: bool; def validate_compliance(state: SurgicalMirrorState): state['compliance_passed'] = 'ISO13485' in state.get('compliance_docs', []); return state; def inspect_product(state: SurgicalMirrorState): state['inspection_passed'] = True; return state; graph = StateGraph(SurgicalMirrorState); graph.add_node('validate', validate_compliance); graph.add_node('inspect', inspect_product); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()