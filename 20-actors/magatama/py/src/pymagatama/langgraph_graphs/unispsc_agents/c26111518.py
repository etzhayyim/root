from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TensionProcurementState(TypedDict): spec: dict; verified: bool; documentation: List[str]; def validate_specs(state: TensionProcurementState): print('Validating mechanical tolerances...'); state['verified'] = state['spec'].get('tensile_strength_mpa', 0) > 500; return state; def final_check(state: TensionProcurementState): state['documentation'].append('ISO_CERT_VERIFIED'); return state; graph = StateGraph(TensionProcurementState); graph.add_node('validate', validate_specs); graph.add_node('certify', final_check); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph.set_entry_point('validate'); graph = graph.compile()