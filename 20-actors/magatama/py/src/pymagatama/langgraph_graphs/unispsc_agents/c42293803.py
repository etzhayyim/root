from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): device_id: str; compliance_docs: List[str]; validated: bool; def validate_instrument(state: SurgicalState): return {'validated': 'ISO13485' in state['compliance_docs']}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()