from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; gmp_certified: bool; quality_check_passed: bool; def validate_gmp(state: PharmState): return {'gmp_certified': True}; def inspect_quality(state: PharmState): return {'quality_check_passed': True}; graph = StateGraph(PharmState); graph.add_node('validate', validate_gmp); graph.add_node('inspect', inspect_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()