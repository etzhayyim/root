from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(State); def validate_material(state: State): return {'validation_results': ['Material test passed']}; def validate_branding(state: State): return {'validation_results': ['Embroidery quality verified']}; graph.add_node('MaterialCheck', validate_material); graph.add_node('BrandingCheck', validate_branding); graph.add_edge('MaterialCheck', 'BrandingCheck'); graph.add_edge('BrandingCheck', END); graph.set_entry_point('MaterialCheck'); app = graph.compile()