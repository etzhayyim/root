from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalKitState(TypedDict): kit_id: str; components: List[str]; sterile_certified: bool; def validate_inventory(state: DentalKitState): return {'sterile_certified': len(state['components']) > 0}; def finalize_log(state: DentalKitState): return {'kit_id': state['kit_id']}; graph = StateGraph(DentalKitState); graph.add_node('validate', validate_inventory); graph.add_node('log', finalize_log); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.compile()