from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class StripState(TypedDict): alloy_spec: dict; validation_results: list; status: str; def validate_composition(state: StripState): print('Validating alloy composition...'); state['validation_results'].append('Composition Verified'); return state; def check_dimensions(state: StripState): print('Checking dimensional tolerances...'); state['validation_results'].append('Tolerances within limit'); return state; graph = StateGraph(StripState); graph.add_node('validate_comp', validate_composition); graph.add_node('check_dims', check_dimensions); graph.set_entry_point('validate_comp'); graph.add_edge('validate_comp', 'check_dims'); graph.add_edge('check_dims', END); compiled_graph = graph.compile()