from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): data: dict; clinical_log: Annotated[list, operator.add]; graph = StateGraph(PharmaState); def validate_purity(state: PharmaState): return {'clinical_log': ['Purity check: 99.8%']}; def check_cold_chain(state: PharmaState): return {'clinical_log': ['Cold chain integrity: Verified']}; graph.add_node('validate_purity', validate_purity); graph.add_node('check_cold_chain', check_cold_chain); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_cold_chain'); graph.add_edge('check_cold_chain', END); graph.compile()