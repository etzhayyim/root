from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validated: bool; def validate_clips(state: ProcurementState): return {'validated': all(item for item in state['items'])}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_clips); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()