from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SensorState(TypedDict): sensor_specs: dict; export_compliance: bool; status: str; def validate_sensors(state: SensorState): import_cert = state['sensor_specs'].get('calibration_cert') is not None; export_check = state['sensor_specs'].get('wavelength_nm', 0) < 1500; return {'export_compliance': export_check, 'status': 'validated' if import_cert else 'failed'}; graph = StateGraph(SensorState); graph.add_node('validate', validate_sensors); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()