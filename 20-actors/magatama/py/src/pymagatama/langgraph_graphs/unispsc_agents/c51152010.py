from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_data: dict; compliance_checks: List[str]; approved: bool; def validate_api(state: ProcState): purity = state['chemical_data'].get('purity', 0); state['compliance_checks'].append('purity_check') if purity >= 99.0 else None; return state; def check_storage(state: ProcState): state['compliance_checks'].append('cold_chain_verification'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_api); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph = graph.compile()