from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_auth: bool; inventory_check: bool; compliance_status: str; def verify_licensing(state: ProcState): return { 'vendor_auth': True }; def check_compliance(state: ProcState): return { 'compliance_status': 'RESTRICTED_ACCESS' }; def finalize_order(state: ProcState): return { 'inventory_check': True }; graph = StateGraph(ProcState); graph.add_node('verify', verify_licensing); graph.add_node('compliance', check_compliance); graph.add_node('finalize', finalize_order); graph.set_entry_point('verify'); graph.add_edge('verify', 'compliance'); graph.add_edge('compliance', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()