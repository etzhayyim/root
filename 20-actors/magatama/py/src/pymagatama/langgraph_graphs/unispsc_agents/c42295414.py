from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcedurePackState(TypedDict): items: List[str]; validated: bool; compliance_status: str; def validate_components(state: ProcedurePackState): print('Validating components...'); state['validated'] = True; return state; def check_compliance(state: ProcedurePackState): state['compliance_status'] = 'CERTIFIED' if state['validated'] else 'FAILED'; return state; graph = StateGraph(ProcedurePackState); graph.add_node('validate', validate_components); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()