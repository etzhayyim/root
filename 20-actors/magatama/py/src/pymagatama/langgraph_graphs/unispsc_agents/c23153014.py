from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WeldingState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: WeldingState): errors = []; if state['specs'].get('duty_cycle', 0) < 50: errors.append('Duty cycle below procurement threshold'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(WeldingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()