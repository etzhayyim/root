from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; gmp_certified: bool; quality_check: bool; def validate_gmp(state: PharmaState): return {'gmp_certified': True} if state['chemical_name'] == 'Testolactone' else {'gmp_certified': False}; def final_approval(state: PharmaState): return {'quality_check': True} if state['gmp_certified'] else {'quality_check': False}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_gmp); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()