from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_score: float; graph = StateGraph(State); def validate_specs(state: State): return {'validation_score': 1.0 if 'flow_rate' in state['input_data'] else 0.0}; def process_procurement(state: State): return {'input_data': state['input_data']}; graph.add_node('validate', validate_specs); graph.add_node('process', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()