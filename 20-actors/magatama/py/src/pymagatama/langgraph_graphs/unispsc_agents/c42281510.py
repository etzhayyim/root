from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SterilizationState(TypedDict): items: List[dict]; validated: bool; validation_log: List[str]; def validate_material(state: SterilizationState): specs = state['items'][0]; is_valid = specs.get('material_grade') == '316L'; return {'validated': is_valid, 'validation_log': ['Material verified'] if is_valid else ['Invalid Material']}; graph = StateGraph(SterilizationState); graph.add_node('validate_material', validate_material); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', END); graph = graph.compile()