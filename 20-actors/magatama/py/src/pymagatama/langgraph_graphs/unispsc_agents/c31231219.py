from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlateState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_plate(state: PlateState): state['valid'] = 'thickness' in state['specs'] and 'material' in state['specs']; state['log'].append('Validation complete'); return state; graph = StateGraph(PlateState); graph.add_node('validate', validate_plate); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()