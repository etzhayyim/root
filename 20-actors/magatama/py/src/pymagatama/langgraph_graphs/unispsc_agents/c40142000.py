from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HoseState(TypedDict): specs: dict; valid: bool; hose_type: str; def validate_hose(state: HoseState): state['valid'] = 'pressure' in state['specs'] and state['specs']['pressure'] > 0; return state; graph = StateGraph(HoseState); graph.add_node('validate', validate_hose); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()