from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ChokeState(TypedDict): inductance: float; current_rating: float; verified: bool; def validate_spec(state: ChokeState): state['verified'] = (state['inductance'] > 0 and state['current_rating'] > 0); return state; graph = StateGraph(ChokeState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()