from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ConverterState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_specs(state: ConverterState): state['validation_results'].append('Spec check complete'); state['status'] = 'verified'; return state; def check_export_control(state: ConverterState): state['validation_results'].append('Export compliance checked'); return state; graph = StateGraph(ConverterState); graph.add_node('validate', validate_specs); graph.add_node('export', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()