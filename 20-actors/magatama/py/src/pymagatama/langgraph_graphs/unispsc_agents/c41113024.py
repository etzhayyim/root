from langgraph.graph import StateGraph, END; from typing import TypedDict; class HydrometerState(TypedDict): specs: dict; valid: bool; graph = StateGraph(HydrometerState); def validate_specs(state: HydrometerState): state['valid'] = 'range' in state['specs'] and 'calibration' in state['specs']; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()