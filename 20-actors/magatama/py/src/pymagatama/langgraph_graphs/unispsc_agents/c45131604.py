from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TapeProcurementState(TypedDict): tape_format: str; is_verified: bool; compliance_report: List[str]; def validate_format(state: TapeProcurementState): return {'is_verified': state['tape_format'] in ['VHS', 'BetaCam', 'MiniDV']}; def generate_report(state: TapeProcurementState): return {'compliance_report': ['Format valid' if state['is_verified'] else 'Format unsupported']}; graph = StateGraph(TapeProcurementState); graph.add_node('validate', validate_format); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); app = graph.compile()