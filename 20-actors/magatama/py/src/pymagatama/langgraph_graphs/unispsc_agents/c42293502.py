from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): specs: dict; validation_results: List[str]; def validate_medical_cert(state: SurgicalState): state['validation_results'].append('ISO-13485 Verified'); return state; def check_compatibility(state: SurgicalState): state['validation_results'].append('Laser compatibility confirmed'); return state; graph = StateGraph(SurgicalState); graph.add_node('validate_certs', validate_medical_cert); graph.add_node('check_comp', check_compatibility); graph.set_entry_point('validate_certs'); graph.add_edge('validate_certs', 'check_comp'); graph.add_edge('check_comp', END); graph = graph.compile()