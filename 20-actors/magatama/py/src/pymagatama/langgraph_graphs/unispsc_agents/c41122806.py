from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CryoState(TypedDict): dimensions: dict; material: str; validated: bool; def validate_specs(state: CryoState) -> CryoState: state['validated'] = 'stainless_steel' in state['material'].lower() and all(d > 0 for d in state['dimensions'].values()); return state; graph = StateGraph(CryoState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()