from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class RoofingToolState(TypedDict): mop_spec: dict; validation_report: str; def validate_mop_specs(state: RoofingToolState): return {'validation_report': 'Passed: Thermal rating exceeds industrial standards.' if state['mop_spec'].get('heat_resistance') > 200 else 'Failed: Insufficient heat resistance.'}; def route_procurement(state: RoofingToolState): return 'end'; graph = StateGraph(RoofingToolState); graph.add_node('validator', validate_mop_specs); graph.add_edge('validator', END); graph.set_entry_point('validator'); graph = graph.compile()