from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpSpecState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: PumpSpecState): required = ['flow_rate', 'head_height']; state['valid'] = all(k in state['specs'] for k in required); return state; graph = StateGraph(PumpSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()