from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MarineSpecState(TypedDict): components: List[str]; qc_status: str; certs_valid: bool; def validate_marine_specs(state: MarineSpecState): state['qc_status'] = 'PASS' if state['certs_valid'] else 'FAIL'; return state; def check_compliance(state: MarineSpecState): return 'validate' if state['components'] else END; graph = StateGraph(MarineSpecState); graph.add_node('validate', validate_marine_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()