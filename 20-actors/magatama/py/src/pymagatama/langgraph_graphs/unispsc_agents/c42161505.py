from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialysisState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_specs(state: DialysisState): state['validated'] = all(['sterile' in cert for cert in state['certs']]); return state; workflow = StateGraph(DialysisState); workflow.add_node('validate', validate_medical_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()