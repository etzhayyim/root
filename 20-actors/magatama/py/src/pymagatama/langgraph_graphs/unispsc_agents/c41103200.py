from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class LabWashState(TypedDict): equipment_spec: str; validation_result: bool; def validate_spec(state: LabWashState): state['validation_result'] = 'iso_15883' in state['equipment_spec'].lower(); return state; def check_compliance(state: LabWashState): return 'compliant' if state['validation_result'] else 'needs_manual_review'; graph = StateGraph(LabWashState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()