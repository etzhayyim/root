from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(CompressorState); def validate_specs(state: CompressorState): return {'compliance_check': state['specs'].get('pressure', 0) > 0}; def finalize_order(state: CompressorState): return {'compliance_check': True}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()