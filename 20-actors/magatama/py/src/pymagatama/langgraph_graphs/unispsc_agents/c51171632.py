from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChemState(TypedDict): chemical_name: str; purity: float; hazard_check: bool; graph = StateGraph(ChemState); def val_purity(state: ChemState): return {'hazard_check': state['purity'] >= 99.0}; graph.add_node('validate', val_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()