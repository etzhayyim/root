from langgraph.graph import StateGraph, END; from typing import TypedDict; class PsylliumState(TypedDict): purity_level: float; compliance_pass: bool; def validate_purity(state: PsylliumState): return {'compliance_pass': state['purity_level'] >= 99.0}; def check_safety(state: PsylliumState): return {'compliance_pass': True if state['compliance_pass'] else False}; graph = StateGraph(PsylliumState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()