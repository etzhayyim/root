from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input: str; specs: Annotated[list, operator.add]; status: str; def validate_spec(state: ProcureState): print('Validating syringe specs...'); return {'status': 'validated' if state['specs'] else 'requires_review'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()