from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CorrectionFluidState(TypedDict): fluid_type: str; is_compliant: bool; validation_log: List[str]; def check_voc_compliance(state: CorrectionFluidState): state['is_compliant'] = state['fluid_type'] in ['water-based', 'low-solvent']; state['validation_log'].append('VOC compliance checked.'); return state; def finalize_procurement(state: CorrectionFluidState): state['validation_log'].append('Procurement ready for approval.'); return state; graph = StateGraph(CorrectionFluidState); graph.add_node('check_voc', check_voc_compliance); graph.add_node('finalizer', finalize_procurement); graph.set_entry_point('check_voc'); graph.add_edge('check_voc', 'finalizer'); graph.add_edge('finalizer', END); app = graph.compile()