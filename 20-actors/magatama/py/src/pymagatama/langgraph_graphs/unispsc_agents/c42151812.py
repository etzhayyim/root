from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): component_type: str; specs: dict; validation_errors: List[str]; def validate_specs(state: ProcurementState): if state['specs'].get('material') != 'medical-grade-steel': state['validation_errors'].append('Invalid material spec'); return state; def check_compliance(state: ProcurementState): if 'ISO 13485' not in state['specs'].get('certs', []): state['validation_errors'].append('Missing ISO 13485'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()