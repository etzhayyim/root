from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FenestrometerState(TypedDict): device_id: str; quality_checks: List[str]; approved: bool; def validate_instrument(state: FenestrometerState): return {'approved': len(state['quality_checks']) >= 3}; graph = StateGraph(FenestrometerState); graph.add_node('validate', validate_instrument); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()