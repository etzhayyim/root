from langgraph.graph import StateGraph, END; from typing import TypedDict; class BiCMOSState(TypedDict): spec: dict; valid: bool; def validate_spec(state: BiCMOSState): state['valid'] = 'lithography_node' in state['spec']; return state; builder = StateGraph(BiCMOSState); builder.add_node('validate', validate_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()