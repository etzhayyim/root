from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HarnessState(TypedDict): specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_specs(state: HarnessState): state['compliance_status'] = 'Tensile Strength' in state['specs'] and state['specs']['Tensile Strength'] > 15; state['workflow_log'].append('Technical Specs Validated'); return state; def check_certifications(state: HarnessState): state['workflow_log'].append('Checking EN Standards'); return state; graph = StateGraph(HarnessState); graph.add_node('validate', validate_specs); graph.add_node('certify', check_certifications); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()