from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GloveProcurementState(TypedDict): vendor_certs: List[str]; inspection_passed: bool; ready_for_procurement: bool; def validate_certifications(state: GloveProcurementState): return {'inspection_passed': 'ISO-13485' in state['vendor_certs']}; def finalize_order(state: GloveProcurementState): return {'ready_for_procurement': state['inspection_passed']}; graph = StateGraph(GloveProcurementState); graph.add_node('validate', validate_certifications); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()