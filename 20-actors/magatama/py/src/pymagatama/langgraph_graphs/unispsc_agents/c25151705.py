from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class NavSatState(TypedDict): tasks: Annotated[Sequence[str], operator.add]; status: str; def validate_specs(state: NavSatState): return {'status': 'spec_validated'}; def check_compliance(state: NavSatState): return {'status': 'compliance_verified'}; builder = StateGraph(NavSatState); builder.add_node('validate', validate_specs); builder.add_node('compliance', check_compliance); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); builder.set_entry_point('validate'); graph = builder.compile()