from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalAlloyState(TypedDict): alloy_specs: dict; compliance_verified: bool; def validate_alloy(state: DentalAlloyState): print('Validating metallurgical composition...'); state['compliance_verified'] = True; return state; def check_certification(state: DentalAlloyState): print('Verifying ISO certifications...'); return state; graph = StateGraph(DentalAlloyState); graph.add_node('validation', validate_alloy); graph.add_node('certification', check_certification); graph.set_entry_point('validation'); graph.add_edge('validation', 'certification'); graph.add_edge('certification', END); graph = graph.compile()