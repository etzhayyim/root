from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; compliance_check: bool; material_specs: List[str]; def validate_materials(state: KitState): return { "compliance_check": all(spec in ["ISO10993", "ASTM_F1980"] for spec in state["material_specs"]) }; def finalize_procurement(state: KitState): return { "status": "APPROVED" if state["compliance_check"] else "REJECTED" }; graph = StateGraph(KitState); graph.add_node("validate", validate_materials); graph.add_node("finalize", finalize_procurement); graph.set_entry_point("validate"); graph.add_edge("validate", "finalize"); graph.add_edge("finalize", END); graph = graph.compile()