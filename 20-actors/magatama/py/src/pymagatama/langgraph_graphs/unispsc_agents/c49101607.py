from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComicState(TypedDict): items: List[dict]; validation: bool; collection_value: float; def validate_grade(state: ComicState): return { "validation": all(i.get("grade") is not None for i in state["items"]) }; def calculate_total(state: ComicState): return { "collection_value": sum(i.get("price", 0) for i in state["items"]) }; graph = StateGraph(ComicState); graph.add_node("validate", validate_grade); graph.add_node("calculate", calculate_total); graph.set_entry_point("validate"); graph.add_edge("validate", "calculate"); graph.add_edge("calculate", END); app = graph.compile()