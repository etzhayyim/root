from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MoldState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: MoldState): specs = state['specs']; is_valid = all(k in specs for k in ['material', 'tolerance']); return {'validation_report': 'Passed' if is_valid else 'Failed', 'approved': is_valid}; graph = StateGraph(MoldState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()