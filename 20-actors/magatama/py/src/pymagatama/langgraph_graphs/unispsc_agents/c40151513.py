from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): pump_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: PumpState): errors = []; if state['pump_specs'].get('IP_Rating', 0) < 68: errors.append('Insufficient IP rating'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()