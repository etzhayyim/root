from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RepeaterSpec(TypedDict): frequency: float; power: float; status: str; def validate_specs(state: RepeaterSpec): state['status'] = 'VALIDATED' if state['frequency'] > 0 else 'INVALID'; return state; graph = StateGraph(RepeaterSpec); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()