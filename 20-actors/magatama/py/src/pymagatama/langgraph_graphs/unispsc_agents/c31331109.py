from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): items: List[str]; validation_results: List[str]; certified: bool; def validate_assembly(state: AssemblyState): results = ['Passed' for i in state['items']]; return {'validation_results': results, 'certified': True}; def check_compliance(state: AssemblyState): return 'certified' if state['certified'] else 'failed'; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_assembly); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()