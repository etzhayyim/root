from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TesterState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(TesterState); def check_calibration(state: TesterState): return {'validation_results': state['validation_results'] + ['Calibration verified']}; def validate_capacity(state: TesterState): return {'validation_results': state['validation_results'] + ['Capacity within limits']}; graph.add_node('check_calibration', check_calibration); graph.add_node('validate_capacity', validate_capacity); graph.set_entry_point('check_calibration'); graph.add_edge('check_calibration', 'validate_capacity'); graph.add_edge('validate_capacity', END); graph = graph.compile()