from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GameState(TypedDict): tiles: List[str]; validated: bool; defect_rate: float; def validate_tiles(state: GameState): state['validated'] = len(state['tiles']) == 28; return state; def inspect_quality(state: GameState): state['defect_rate'] = 0.01; return state; graph = StateGraph(GameState); graph.add_node('validate', validate_tiles); graph.add_node('inspect', inspect_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()