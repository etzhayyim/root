from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): data: dict; status: str; def validate_reagent(state: ReagentState): if state['data'].get('expiry_date') and state['data'].get('safety_cert'): return {'status': 'approved'}; return {'status': 'rejected'}; graph = StateGraph(ReagentState); graph.add_node('validate', validate_reagent); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()