from langgraph.graph import StateGraph, END; from typing import TypedDict; class AlverineState(TypedDict): chemical_report: str; approved: bool; def check_purity(state: AlverineState): print('Validating purity levels...'); return {'approved': True} if 'Pass' in state['chemical_report'] else {'approved': False}; graph = StateGraph(AlverineState); graph.add_node('validate', check_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()