from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToothpasteState(TypedDict): ingredients: List[str]; safety_approved: bool; def validate_composition(state: ToothpasteState): return {'safety_approved': 'fluoride' in state['ingredients']}; def check_compliance(state: ToothpasteState): return {'log': 'Compliance validated'}; graph = StateGraph(ToothpasteState); graph.add_node('validate', validate_composition); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); app = graph.compile()