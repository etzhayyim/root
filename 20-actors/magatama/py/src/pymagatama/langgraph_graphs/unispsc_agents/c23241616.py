from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_specs: dict; validation_results: list; def validate_specs(state: ToolState): results = []; capacity = state['tool_specs'].get('capacity', 0); results.append('Valid' if capacity > 0 else 'Invalid'); return {'validation_results': results}; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()