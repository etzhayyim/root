from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): vendor_id: str; device_specs: dict; compliance_check: bool; def validate_specs(state: ProcureState): return {'compliance_check': all(k in state['device_specs'] for k in ['throughput', 'mounting_medium'])}; def finalize(state: ProcureState): return {'status': 'approved' if state['compliance_check'] else 'rejected'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate')