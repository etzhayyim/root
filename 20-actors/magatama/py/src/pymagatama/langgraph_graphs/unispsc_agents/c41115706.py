from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TLCState(TypedDict): vendor_docs: List[str]; validation_results: dict; def validate_specs(state: TLCState) -> TLCState: state['validation_results'] = {'passed': True}; return state; def check_compliance(state: TLCState) -> TLCState: state['validation_results']['compliant'] = True; return state; graph = StateGraph(TLCState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()