from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class FuserState(TypedDict): model_id: str; voltage: int; is_compatible: bool; def validate_spec(state: FuserState): return {'is_compatible': state['voltage'] in [110, 220]}; graph = StateGraph(FuserState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()