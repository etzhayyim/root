from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): component_id: str; specs: dict; valid: bool; def validate_specs(state: ProcState): state['valid'] = 'Material Grade' in state['specs'] and 'Tolerance Specification' in state['specs']; return state; def approve_procurement(state: ProcState): return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()