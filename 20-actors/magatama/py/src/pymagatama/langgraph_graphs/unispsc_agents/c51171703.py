from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): document: str; compliance_passed: bool; validation_log: List[str]; def validate_regulatory(state: DrugState): state['compliance_passed'] = 'license_number' in state['document']; state['validation_log'].append('License validation complete.'); return state; def check_quality(state: DrugState): state['validation_log'].append('Quality check passed.'); return state; graph = StateGraph(DrugState); graph.add_node('regulatory', validate_regulatory); graph.add_node('quality', check_quality); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'quality'); graph.add_edge('quality', END); graph = graph.compile()