from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MotorDriveState(TypedDict): specs: dict; validation_passed: bool; logs: List[str]; def validate_specs(state: MotorDriveState): state['validation_passed'] = 'input_voltage_range' in state['specs']; return state; def check_compliance(state: MotorDriveState): state['logs'].append('Verifying dual-use export compliance'); return state; graph = StateGraph(MotorDriveState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()