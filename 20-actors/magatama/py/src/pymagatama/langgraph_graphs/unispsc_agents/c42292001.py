from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SurgicalMirrorState(TypedDict): status: str; spec_data: dict; approved: bool; def validate_specs(state: SurgicalMirrorState): state['approved'] = 'ISO_13485' in state['spec_data'].get('certs', []) and state['spec_data'].get('sterile', False); state['status'] = 'Validated' if state['approved'] else 'Rejected'; return state; graph = StateGraph(SurgicalMirrorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()