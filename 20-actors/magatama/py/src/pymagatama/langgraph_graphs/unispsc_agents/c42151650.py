from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DentalSpecState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; def validate_medical_cert(state: DentalSpecState): return {'validation_results': ['Cert validated']} if 'cert' in state['input_data'] else {'validation_results': ['Cert missing']}; def check_material_safety(state: DentalSpecState): return {'validation_results': ['Material ISO compliant']}; graph = StateGraph(DentalSpecState); graph.add_node('cert_check', validate_medical_cert); graph.add_node('material_check', check_material_safety); graph.add_edge('cert_check', 'material_check'); graph.add_edge('material_check', END); graph.set_entry_point('cert_check'); graph = graph.compile()