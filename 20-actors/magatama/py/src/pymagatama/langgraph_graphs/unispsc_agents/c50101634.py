from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FruitState(TypedDict): fruit_type: str; quality_score: float; is_passed: bool; def check_freshness(state: FruitState): state['is_passed'] = state['quality_score'] > 0.8; return state; def final_approval(state: FruitState): return {'is_passed': state['is_passed']}; graph = StateGraph(FruitState); graph.add_node('freshness_check', check_freshness); graph.add_node('approval', final_approval); graph.set_entry_point('freshness_check'); graph.add_edge('freshness_check', 'approval'); graph.add_edge('approval', END); graph = graph.compile()