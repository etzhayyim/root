from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_materials(state: AssemblyState): return {'validation_results': ['Material check passed']}; def perform_weld_inspection(state: AssemblyState): return {'validation_results': ['Weld integrity verified']}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_materials); graph.add_node('inspect', perform_weld_inspection); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()