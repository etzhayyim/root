from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity_percent: float; is_certified: bool; approved: bool; def validate_purity(state: PharmaState): return {'approved': state['purity_percent'] >= 99.0 and state['is_certified']}; def check_compliance(state: PharmaState): print(f'Processing {state["chemical_name"]} compliance'); return {'approved': True}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()