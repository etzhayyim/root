from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MacrameState(TypedDict): items: List[dict]; validation_results: dict; graph = StateGraph(MacrameState); def validate_materials(state: MacrameState): return {'validation_results': {'material_check': 'passed'}}; def check_knot_quality(state: MacrameState): return {'validation_results': {'knot_check': 'passed'}}; graph.add_node('material_validation', validate_materials); graph.add_node('knot_validation', check_knot_quality); graph.set_entry_point('material_validation'); graph.add_edge('material_validation', 'knot_validation'); graph.add_edge('knot_validation', END); compile_graph = graph.compile()