from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalProcurementState(TypedDict): chemical_id: str; purity_check: bool; safety_compliance: bool; approval_log: Annotated[Sequence[str], operator.add]; def validate_chemical_purity(state: ChemicalProcurementState): return {'purity_check': True, 'approval_log': ['Purity validated']}; def verify_safety_standards(state: ChemicalProcurementState): return {'safety_compliance': True, 'approval_log': ['Safety standards met']}; graph = StateGraph(ChemicalProcurementState); graph.add_node('purity_check', validate_chemical_purity); graph.add_node('safety_check', verify_safety_standards); graph.add_edge('purity_check', 'safety_check'); graph.add_edge('safety_check', END); graph.set_entry_point('purity_check'); graph = graph.compile()