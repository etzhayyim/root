from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BoxState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(BoxState); def validate_dims(state: BoxState): state['validation_report'].append('Dimensional check passed'); return state; def check_compliance(state: BoxState): state['validation_report'].append('Regulatory compliance verified'); return state; graph.add_node('dim_check', validate_dims); graph.add_node('compliance', check_compliance); graph.set_entry_point('dim_check'); graph.add_edge('dim_check', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()