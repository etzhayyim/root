from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_name: str; analysis_report: str; approved: bool; def validate_purity(state: ProcState): state['approved'] = '99.5%' in state['analysis_report']; return state; def check_sds(state: ProcState): return {'approved': state.get('approved', False) and True}; graph = StateGraph(ProcState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_sds', check_sds); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_sds'); graph.add_edge('check_sds', END); graph = graph.compile()