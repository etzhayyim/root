from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; analysis: dict; status: str; def validate_spec(state: State): state['status'] = 'VALIDATED' if 'material' in state['input_data'] else 'REJECTED'; return state; def check_regulations(state: State): state['analysis'] = {'iso_compliance': True}; return state; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()