from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_id: str; specs: dict; compliance_check: bool; def validate_specs(state: ProcurementState): state['compliance_check'] = all(k in state['specs'] for k in ['Material Grade', 'Load Capacity Rating']); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()