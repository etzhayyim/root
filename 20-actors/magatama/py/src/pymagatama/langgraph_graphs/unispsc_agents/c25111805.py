from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PWCState(TypedDict): specs: dict; valid: bool; engine_check: bool; graph = StateGraph(PWCState); def validate_engine(state: PWCState): state['engine_check'] = state['specs'].get('engine_displacement_cc', 0) > 0; return state; def check_compliance(state: PWCState): state['valid'] = state['engine_check'] and 'hull_certification_standard' in state['specs']; return state; graph.add_node('validate_engine', validate_engine); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_engine'); graph.add_edge('validate_engine', 'check_compliance'); graph.add_edge('check_compliance', END); graph = graph.compile()