from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DuctState(TypedDict): specs: dict; compliance: bool; flow_analysis_required: bool; def validate_specs(state: DuctState): state['compliance'] = all(k in state['specs'] for k in ['alloy', 'thickness']); return state; def check_export(state: DuctState): state['flow_analysis_required'] = True; return state; graph = StateGraph(DuctState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()