from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): optics_spec: dict; validation_results: List[str]; def validate_optical_params(state: GraphState): return {'validation_results': ['Optics within tolerance']}; def check_calibration(state: GraphState): return {'validation_results': state['validation_results'] + ['Calibration verified']}; graph = StateGraph(GraphState); graph.add_node('validate_optics', validate_optical_params); graph.add_node('check_calibration', check_calibration); graph.set_entry_point('validate_optics'); graph.add_edge('validate_optics', 'check_calibration'); graph.add_edge('check_calibration', END); graph = graph.compile()