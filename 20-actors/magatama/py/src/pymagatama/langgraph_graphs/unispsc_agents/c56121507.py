from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StudyCarrelState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: StudyCarrelState): state['validation_errors'] = []; if state['specs'].get('height', 0) < 120: state['validation_errors'].append('Height insufficient for privacy'); return state; def check_compliance(state: StudyCarrelState): state['approved'] = len(state['validation_errors']) == 0; return state; graph = StateGraph(StudyCarrelState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()