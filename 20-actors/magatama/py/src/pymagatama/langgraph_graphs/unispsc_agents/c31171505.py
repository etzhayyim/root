from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): bearing_specs: dict; validation_results: List[str]; graph = StateGraph(BearingState); def validate_specs(state: BearingState): results = []; specs = state.get('bearing_specs', {}); if 'load_rating_dynamic' not in specs: results.append('Missing load rating'); if 'iso_certification_number' not in specs: results.append('Missing certification'); return {'validation_results': results}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()