from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): doc_id: str; compliant: bool; def validate_ergonomics(state: ProcurementState): state['compliant'] = True; return state; def check_medical_reg(state: ProcurementState): return {'compliant': state['compliant']}; graph = StateGraph(ProcurementState); graph.add_node('validate_ergonomics', validate_ergonomics); graph.add_node('check_medical_reg', check_medical_reg); graph.set_entry_point('validate_ergonomics'); graph.add_edge('validate_ergonomics', 'check_medical_reg'); graph.add_edge('check_medical_reg', END); graph = graph.compile()