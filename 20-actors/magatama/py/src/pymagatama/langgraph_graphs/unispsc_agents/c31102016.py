from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): metal_type: str; purity: float; dimensions: dict; status: str; def validate_purity(state: CastingState) -> CastingState: state['status'] = 'Validated' if state['purity'] >= 99.9 else 'Rejected'; return state; def check_dimensions(state: CastingState) -> CastingState: state['status'] = 'CAD_Verified'; return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_purity); graph.add_node('check_dim', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_dim'); graph.add_edge('check_dim', END); graph = graph.compile()