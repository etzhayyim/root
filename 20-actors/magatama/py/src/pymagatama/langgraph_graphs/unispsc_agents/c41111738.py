from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FiberscopeState(TypedDict): specs: dict; valid: bool; error: List[str]; def validate_specs(state: FiberscopeState): specs = state['specs']; errors = []; if specs.get('Resolution', 0) < 30000: errors.append('Resolution below minimum requirement'); return {'valid': len(errors) == 0, 'error': errors}; graph = StateGraph(FiberscopeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()