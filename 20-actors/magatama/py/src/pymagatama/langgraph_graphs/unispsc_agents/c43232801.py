from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): vendor_id: str; compliance_check: bool; traffic_metrics: List[str]; def validate_vendor(state: NetworkState): state['compliance_check'] = True; return state; def analyze_traffic(state: NetworkState): state['traffic_metrics'] = ['latency', 'packet_loss']; return state; graph = StateGraph(NetworkState); graph.add_node('validate', validate_vendor); graph.add_node('analyze', analyze_traffic); graph.add_edge('validate', 'analyze'); graph.add_edge('analyze', END); graph.set_entry_point('validate'); graph = graph.compile()