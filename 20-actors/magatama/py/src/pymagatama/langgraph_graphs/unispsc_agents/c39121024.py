from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(State); def validate_spec(state: State): return {'validation_results': ['Spec compliance checked']}; def run_load_analysis(state: State): return {'validation_results': ['Grid capacity load analysis complete']}; graph.add_node('validate_spec', validate_spec); graph.add_node('load_analysis', run_load_analysis); graph.set_entry_point('validate_spec'); graph.add_edge('validate_spec', 'load_analysis'); graph.add_edge('load_analysis', END); compiled_graph = graph.compile()