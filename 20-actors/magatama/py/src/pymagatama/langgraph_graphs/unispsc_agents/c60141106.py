from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DiceState(TypedDict): data: dict; checks: Annotated[list, operator.add]; def validate_balance(state: DiceState): print('Validating weight balance...'); return {'checks': ['balance_pass']}; def check_material(state: DiceState): print('Checking material safety...'); return {'checks': ['material_pass']}; graph = StateGraph(DiceState); graph.add_node('validate_balance', validate_balance); graph.add_node('check_material', check_material); graph.set_entry_point('validate_balance'); graph.add_edge('validate_balance', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()