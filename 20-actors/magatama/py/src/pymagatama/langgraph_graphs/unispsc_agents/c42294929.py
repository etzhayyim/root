from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validation_status: bool; def validate_instrument(state: ProcState): return {'validation_status': all('ISO' in item for item in state['items'])}; graph = StateGraph(ProcState); graph.add_node('validate', validate_instrument); graph.add_edge('validate', END); graph.set_entry_point('validate'); app = graph.compile()