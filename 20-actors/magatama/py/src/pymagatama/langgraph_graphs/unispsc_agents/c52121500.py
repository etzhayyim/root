from langgraph.graph import StateGraph, END; from typing import TypedDict; class BedclothesState(TypedDict): items: list; valid: bool; def validate_spec(state: BedclothesState): state['valid'] = all('material' in item and 'flame_retardant' in item for item in state['items']); return state; graph = StateGraph(BedclothesState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()