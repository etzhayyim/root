from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BeltState(TypedDict): specs: dict; validation_passed: bool; def check_belt_specs(state: BeltState): state['validation_passed'] = state['specs'].get('tensile_strength', 0) > 1000; return state; def route_compliance(state: BeltState): return 'end' if state['validation_passed'] else END; graph = StateGraph(BeltState); graph.add_node('validate', check_belt_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()