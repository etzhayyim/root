from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CareerEduState(TypedDict): content: str; validation_results: List[str]; graph = StateGraph(CareerEduState); def validate_content(state: CareerEduState): results = []; if 'lesson_plan' not in state['content']: results.append('Missing lesson plan structure'); return {'validation_results': results}; graph.add_node('validate', validate_content); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()