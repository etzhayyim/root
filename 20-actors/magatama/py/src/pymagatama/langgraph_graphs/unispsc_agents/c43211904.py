from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PDPState(TypedDict): panel_size: float; resolution: str; status: str; def validate_specs(state: PDPState): return {'status': 'validated'} if state['panel_size'] > 0 else {'status': 'rejected'}; graph = StateGraph(PDPState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()