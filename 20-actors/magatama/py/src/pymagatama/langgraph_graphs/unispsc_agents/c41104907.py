from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MarketState(TypedDict): specs: dict; validation_passed: bool; risk_score: int; def validate_specs(state: MarketState): state['validation_passed'] = 'Membrane_Material' in state['specs']; return state; def check_compliance(state: MarketState): state['risk_score'] = 5 if state['validation_passed'] else 10; return state; graph = StateGraph(MarketState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END);