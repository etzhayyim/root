from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): active_ingredient: str; purity: float; compliant: bool; def validate_purity(state: PharmState): state['compliant'] = state['purity'] >= 99.0; return state; def check_regulations(state: PharmState): print(f'Checking GMP compliance for {state['active_ingredient']}'); return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulations); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()