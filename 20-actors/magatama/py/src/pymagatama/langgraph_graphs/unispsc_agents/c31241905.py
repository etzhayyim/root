from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldedDomesState(TypedDict): dimensions: dict; quality_certs: List[str]; inspection_passed: bool; def validate_specs(state: MoldedDomesState): return {'inspection_passed': all(val > 0 for val in state['dimensions'].values())}; def check_certs(state: MoldedDomesState): return {'inspection_passed': 'ISO9001' in state['quality_certs']}; graph = StateGraph(MoldedDomesState); graph.add_node('spec_validation', validate_specs); graph.add_node('cert_audit', check_certs); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', 'cert_audit'); graph.add_edge('cert_audit', END); graph = graph.compile()