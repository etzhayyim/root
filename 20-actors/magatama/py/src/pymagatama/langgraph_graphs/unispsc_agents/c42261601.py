from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoneCollectorState(TypedDict): device_specs: dict; compliance_status: bool; validation_log: List[str]; def validate_specs(state: BoneCollectorState): specs = state['device_specs']; is_valid = all([specs.get('sterilized'), specs.get('biocompatible')]); return {'compliance_status': is_valid, 'validation_log': ['Specs checked']}; def finalize_procurement(state: BoneCollectorState): return {'validation_log': state['validation_log'] + ['Procurement ready']}; graph = StateGraph(BoneCollectorState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()