from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FootwearState(TypedDict): specs: dict; compliant: bool; def validate_spec(state: FootwearState): state['compliant'] = 'material_composition' in state['specs'] and 'size_range' in state['specs']; return state; def check_quality(state: FootwearState): print('Checking slip resistance and durability'); return state; graph = StateGraph(FootwearState); graph.add_node('validate', validate_spec); graph.add_node('quality_check', check_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'quality_check'); graph.add_edge('quality_check', END); graph = graph.compile()