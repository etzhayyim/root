from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RemovalState(TypedDict): chemical_name: str; contents: List[str]; hazard_rating: int; graph = StateGraph(RemovalState); def validate_safety(state: RemovalState): return {'hazard_rating': 1 if 'Acetone' in state['contents'] else 0}; def assess_compliance(state: RemovalState): return {'hazard_rating': 2} if state['hazard_rating'] > 0 else {'hazard_rating': 0}; graph.add_node('validate', validate_safety); graph.add_node('compliance', assess_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()