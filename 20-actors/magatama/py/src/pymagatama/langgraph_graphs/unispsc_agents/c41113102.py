from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): vendor_certs: List[str]; calibration_valid: bool; compliance_status: bool; def check_calibration(state: AnalysisState): return {'calibration_valid': 'ISO-17025' in state['vendor_certs']}; def assess_compliance(state: AnalysisState): return {'compliance_status': state['calibration_valid']}; graph = StateGraph(AnalysisState); graph.add_node('verify_calibration', check_calibration); graph.add_node('check_compliance', assess_compliance); graph.add_edge('verify_calibration', 'check_compliance'); graph.add_edge('check_compliance', END); graph.set_entry_point('verify_calibration'); app = graph.compile()