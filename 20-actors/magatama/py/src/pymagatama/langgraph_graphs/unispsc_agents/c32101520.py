from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input_data: dict; validation_steps: Annotated[Sequence[str], operator.add]; result: str; def validate_rf_specs(state: ProcState): specs = state['input_data']; is_valid = specs.get('vswr', 0) < 1.5; return {'validation_steps': ['check_vswr'], 'result': 'valid' if is_valid else 'invalid'}; def finalize_procurement(state: ProcState): return {'result': 'approved'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_rf_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()