from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class LubricantState(TypedDict): lubricant_id: str; specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_lubricant_specs(state: LubricantState) -> LubricantState: state['compliance_status'] = state['specs'].get('flash_point', 0) > 150; state['workflow_log'].append('Spec validation completed'); return state; def check_regulatory_export(state: LubricantState) -> LubricantState: state['workflow_log'].append('Export control screening passed'); return state; graph = StateGraph(LubricantState); graph.add_node('validate', validate_lubricant_specs); graph.add_node('export_check', check_regulatory_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()