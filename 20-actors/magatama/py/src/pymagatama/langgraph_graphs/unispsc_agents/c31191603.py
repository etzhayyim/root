from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec: dict; valid: bool; def validate_wheel(state: ProcState): return {'valid': state['spec'].get('maximum_rpm', 0) > 0 and 'grit_size' in state['spec']}; graph = StateGraph(ProcState); graph.add_node('validate', validate_wheel); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()