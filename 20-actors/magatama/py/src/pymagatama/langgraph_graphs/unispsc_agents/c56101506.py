from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FutonState(TypedDict): specs: dict; validation_results: List[str]; def validate_fire_safety(state: FutonState): state['validation_results'].append('Fire safety check passed') if state['specs'].get('fire_rated') else None; return state; def validate_dimensions(state: FutonState): state['validation_results'].append('Dimensions verified') if state['specs'].get('dimensions') else None; return state; graph = StateGraph(FutonState); graph.add_node('fire_check', validate_fire_safety); graph.add_node('dim_check', validate_dimensions); graph.set_entry_point('fire_check'); graph.add_edge('fire_check', 'dim_check'); graph.add_edge('dim_check', END); graph = graph.compile()