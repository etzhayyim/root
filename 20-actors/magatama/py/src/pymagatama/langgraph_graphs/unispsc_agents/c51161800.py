from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(PharmaState); def validate_regulatory(state): return {'validation_results': {'compliant': True}}; def check_expiry(state): return {'validation_results': {'freshness': 'verified'}}; graph.add_node('regulatory_check', validate_regulatory); graph.add_node('expiry_check', check_expiry); graph.set_entry_point('regulatory_check'); graph.add_edge('regulatory_check', 'expiry_check'); graph.add_edge('expiry_check', END); graph = graph.compile()