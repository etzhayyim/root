from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CatheterState(TypedDict): items: List[str]; validation_results: dict; def validate_catheter(state: CatheterState): results = {item: 'Verified ISO 13485' for item in state['items']}; return {'validation_results': results}; def log_compliance(state: CatheterState): print('Compliance checked'); return {}; graph = StateGraph(CatheterState); graph.add_node('validate', validate_catheter); graph.add_node('log', log_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph = graph.compile()