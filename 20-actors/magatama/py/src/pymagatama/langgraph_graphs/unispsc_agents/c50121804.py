from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlantState(TypedDict): species: str; status: str; certification: bool; def validate_phytosanitary(state: PlantState): state['certification'] = True if 'cert' in state['species'].lower() else False; return state; def check_quality(state: PlantState): state['status'] = 'Approved' if state['certification'] else 'Rejected'; return state; graph = StateGraph(PlantState); graph.add_node('validate', validate_phytosanitary); graph.add_node('quality', check_quality); graph.add_edge('validate', 'quality'); graph.add_edge('quality', END); graph.set_entry_point('validate'); app = graph.compile()