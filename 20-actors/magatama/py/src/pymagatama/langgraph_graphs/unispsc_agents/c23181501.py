from langgraph.graph import StateGraph, END; from typing import TypedDict; class ActuatorState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ActuatorState): state['valid'] = all(k in state['specs'] for k in ['torque', 'accuracy']); return state; graph = StateGraph(ActuatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()