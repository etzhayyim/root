from langgraph.graph import StateGraph, END; from typing import TypedDict; class SignageState(TypedDict): specs: dict; valid: bool; def validate_specs(state: SignageState): state['valid'] = 'material' in state['specs'] and 'dimensions' in state['specs']; return state; graph = StateGraph(SignageState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()