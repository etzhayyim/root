from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_id: str; quality_score: float; lab_report: dict; approved: bool; def validate_purity(state: ProcurementState) -> dict: return {'approved': state['quality_score'] > 0.95}; def check_compliance(state: ProcurementState) -> dict: return {'approved': 'lab_report' in state}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()