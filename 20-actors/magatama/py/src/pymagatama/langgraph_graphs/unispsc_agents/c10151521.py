from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; class ExcavatorState(TypedDict): input_spec: dict; validation_results: list; status: str; def validate_cutter_spec(state: ExcavatorState): print('Validating cutter specification...'); return {'validation_results': ['Material check passed', 'Hardness requirement met']}; def check_compatibility(state: ExcavatorState): print('Checking machine compatibility...'); return {'status': 'COMPATIBLE'}; graph = StateGraph(ExcavatorState); graph.add_node('validate', validate_cutter_spec); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()