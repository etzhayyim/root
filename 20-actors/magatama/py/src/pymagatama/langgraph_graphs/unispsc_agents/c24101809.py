from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DockState(TypedDict): doc_id: str; specs: dict; ready: bool; def validate_specs(state: DockState): state['ready'] = 'Load Capacity' in state['specs'] and state['specs']['Load Capacity'] > 0; return state; graph = StateGraph(DockState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()