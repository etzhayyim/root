from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RampProcurementState(TypedDict): ramp_specs: dict; compliance_verified: bool; validation_log: List[str]; def validate_specs(state: RampProcurementState): specs = state['ramp_specs']; valid = specs.get('load_capacity', 0) > 0 and 'material' in specs; return {'compliance_verified': valid, 'validation_log': ['Specs checked']}; def approve_procurement(state: RampProcurementState): return {'validation_log': state['validation_log'] + ['Procurement approved']}; graph = StateGraph(RampProcurementState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()