from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validation_results: dict; def validate_case(state: State): state['validation_results'] = {'approved': all(['medical_grade' in i for i in state['items']])}; return state; builder = StateGraph(State); builder.add_node('validate', validate_case); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()