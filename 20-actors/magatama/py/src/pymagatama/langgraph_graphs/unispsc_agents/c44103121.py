from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): vendor_specs: dict; validation_passed: bool; def validate_roller(state: ProcState): return { "validation_passed": all(k in state["vendor_specs"] for k in ["durometer", "dims"]) }; graph = StateGraph(ProcState); graph.add_node("validate", validate_roller); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()