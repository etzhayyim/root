from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EMSCaseState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def validate_specs(state: EMSCaseState): status = True if 'ISO 13485' in str(state['specs']) else False; return {'validation_results': ['ISO Validation Success'] if status else ['ISO Compliance Missing'], 'approved': status}; graph = StateGraph(EMSCaseState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()