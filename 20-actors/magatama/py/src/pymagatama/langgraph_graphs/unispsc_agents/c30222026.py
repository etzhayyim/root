from langgraph.graph import StateGraph, END; from typing import TypedDict; class LighthouseState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: LighthouseState): return {'validation_report': 'Passed' if 'candela' in state['specs'] else 'Failed'}; def approve_procurement(state: LighthouseState): return {'validation_report': 'Approved'}; graph = StateGraph(LighthouseState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()