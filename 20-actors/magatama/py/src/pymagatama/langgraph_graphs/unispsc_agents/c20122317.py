from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): component_id: str; specs: dict; validation_log: Annotated[Sequence[str], operator.add]; status: str; def validate_spec(state: RobotState) -> RobotState: state['validation_log'].append('Validating servo electrical specs'); return state; def check_compliance(state: RobotState) -> RobotState: state['validation_log'].append('Checking control interface compliance'); state['status'] = 'COMPLIANT'; return state; graph = StateGraph(RobotState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()