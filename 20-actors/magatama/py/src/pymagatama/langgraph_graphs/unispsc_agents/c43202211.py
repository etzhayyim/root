from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): platters: List[str]; validation_logs: List[str]; def validate_disk(state: State): state['validation_logs'].append('Checking flatness and surface.'); return state; def export_compliance(state: State): state['validation_logs'].append('Checking EAR/ITAR export status.'); return state; graph = StateGraph(State); graph.add_node('validate', validate_disk); graph.add_node('compliance', export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()