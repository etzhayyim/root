from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ConnectorState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: ConnectorState): ip = state['specs'].get('IP_Rating', 0); return {'validation_passed': ip >= 65}; def route_processing(state: ConnectorState): return 'valid' if state['validation_passed'] else 'error'; graph = StateGraph(ConnectorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()