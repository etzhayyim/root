from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[dict]; validation_results: dict; graph = StateGraph(State); def validate_specs(state: State): return {'validation_results': {'status': 'pass'}}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()