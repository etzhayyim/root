from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class CoatingState(TypedDict): messages: Annotated[list, add_messages]; spec_data: dict; approval_status: bool; def validate_powder(state: CoatingState): spec = state['spec_data']; is_valid = spec.get('curing_temp') < 250 and spec.get('hazard_rating') != 'explosive'; return {'approval_status': is_valid}; graph = StateGraph(CoatingState); graph.add_node('validate', validate_powder); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()