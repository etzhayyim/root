from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): cold_chain_verified: bool; qty: int; gmp_certified: bool; def verify_cold_chain(state: PharmaState): print('Checking temperature logs...'); return {'cold_chain_verified': True}; def validate_gmp(state: PharmaState): print('Validating GMP certification...'); return {'gmp_certified': True}; graph = StateGraph(PharmaState); graph.add_node('verify', verify_cold_chain); graph.add_node('gmp', validate_gmp); graph.set_entry_point('verify'); graph.add_edge('verify', 'gmp'); graph.add_edge('gmp', END); graph = graph.compile()