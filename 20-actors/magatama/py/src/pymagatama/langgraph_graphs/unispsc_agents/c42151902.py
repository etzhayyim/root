from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProphyState(TypedDict): items: List[str]; validated: bool; state: str; def validate_dental_kit(state: ProphyState): state['validated'] = all(['sterile' in item.lower() for item in state['items']]); state['state'] = 'VALIDATED' if state['validated'] else 'REJECTED'; return state; graph = StateGraph(ProphyState); graph.add_node('validate', validate_dental_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()