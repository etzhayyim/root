from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DobutamineState(TypedDict): chemical_purity: float; gmp_certified: bool; temp_logs: List[float]; approved: bool; def validate_purity(state: DobutamineState): return {'approved': state['chemical_purity'] >= 99.0 and state['gmp_certified']}; def check_storage(state: DobutamineState): return {'approved': all(2.0 <= t <= 8.0 for t in state['temp_logs'])}; graph = StateGraph(DobutamineState); graph.add_node('validate', validate_purity); graph.add_node('storage_check', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage_check'); graph.add_edge('storage_check', END); graph = graph.compile()