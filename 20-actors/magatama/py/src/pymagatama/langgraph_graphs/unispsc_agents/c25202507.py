from typing import TypedDict, List; from langgraph.graph import StateGraph; class AircraftWindowSpec(TypedDict): vendor_id: str; certification_level: str; compliance_check: bool; def validate_specs(state: AircraftWindowSpec): state['compliance_check'] = state['certification_level'] == 'FAA_PMA'; return state; def check_export_controls(state: AircraftWindowSpec): return {'compliance_check': True}; graph = StateGraph(AircraftWindowSpec); graph.add_node('validate', validate_specs); graph.add_node('control', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'control'); graph.set_finish_point('control'); graph = graph.compile()