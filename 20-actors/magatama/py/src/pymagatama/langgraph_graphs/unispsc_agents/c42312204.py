from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SutureState(TypedDict): device_id: str; sterile_status: bool; validation_log: List[str]; def validate_device(state: SutureState): return {'validation_log': state['validation_log'] + ['UDI and sterility verified']}; def check_compliance(state: SutureState): return {'validation_log': state['validation_log'] + ['Compliance checked against ISO 10993']}; graph = StateGraph(SutureState); graph.add_node('validate', validate_device); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()