from langgraph.graph import StateGraph, END; from typing import TypedDict; class RouterBitState(TypedDict): specs: dict; valid: bool; def validate_specs(state: RouterBitState): return {'valid': 'shank_diameter' in state['specs'] and 'material' in state['specs']}; graph = StateGraph(RouterBitState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()