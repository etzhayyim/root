from langgraph.graph import StateGraph, END; from typing import TypedDict; class LensState(TypedDict): specs: dict; valid: bool; def validate_specs(state: LensState): state['valid'] = 'focal_length' in state['specs'] and 'refractive_index' in state['specs']; return state; builder = StateGraph(LensState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()