from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LuggageState(TypedDict): tags: List[str]; validation_results: dict; def validate_tags(state: LuggageState): return {'validation_results': {'passed': True, 'note': 'Material spec checked'}}; graph = StateGraph(LuggageState); graph.add_node('validate', validate_tags); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()