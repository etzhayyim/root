from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class FuseState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: FuseState): errors = []; if state['specs'].get('current', 0) <= 0: errors.append('Invalid current'); return {'validation_errors': errors}; def check_approval(state: FuseState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(FuseState); graph.add_node('validate', validate_specs); graph.add_node('check', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()