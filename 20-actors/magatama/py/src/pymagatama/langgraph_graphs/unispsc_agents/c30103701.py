from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): input_data: dict; validation_results: List[str]; graph = StateGraph(State); def validate_specs(state: State): results = []; spec = state['input_data']; results.append('Material check passed' if 'SUS' in spec['grade'] else 'Warning: Grade mismatch'); return {'validation_results': results}; graph.add_node('spec_validator', validate_specs); graph.set_entry_point('spec_validator'); graph.add_edge('spec_validator', END); compiled_graph = graph.compile()