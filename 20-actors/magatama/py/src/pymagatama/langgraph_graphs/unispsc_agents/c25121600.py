from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RailState(TypedDict): vehicle_spec: dict; compliance_status: bool; workflow_path: List[str]; def validate_specs(state: RailState): state['compliance_status'] = all(k in state['vehicle_spec'] for k in ['gauge', 'voltage']); state['workflow_path'].append('validation'); return state; def check_regulations(state: RailState): state['workflow_path'].append('regulation_check'); return state; graph = StateGraph(RailState); graph.add_node('validate', validate_specs); graph.add_node('regulate', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulate'); graph.add_edge('regulate', END); graph = graph.compile()