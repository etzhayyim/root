from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class TapeStorageState(TypedDict): capacity: float; speed: float; status: str; logs: Annotated[Sequence[str], operator.add]; def validate_capacity(state: TapeStorageState): return {'status': 'validated' if state['capacity'] > 0 else 'error'}; def finalize_spec(state: TapeStorageState): return {'logs': ['Spec verified successfully']}; graph = StateGraph(TapeStorageState); graph.add_node('validate', validate_capacity); graph.add_node('finalize', finalize_spec); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()