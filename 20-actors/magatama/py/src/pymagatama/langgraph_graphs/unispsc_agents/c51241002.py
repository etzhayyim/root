from langgraph.graph import StateGraph, END; from typing import TypedDict; class ChemicalState(TypedDict): chemical_data: dict; approval_status: bool; def validate_spec(state: ChemicalState): print('Validating CAS and purity...'); return {'approval_status': True if state['chemical_data'].get('purity', 0) > 99 else False}; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()