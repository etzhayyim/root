from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): material_spec: str; tolerance: float; validated: bool; def validate_spec(state: CastState): return {'validated': state['tolerance'] < 0.05}; graph = StateGraph(CastState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()