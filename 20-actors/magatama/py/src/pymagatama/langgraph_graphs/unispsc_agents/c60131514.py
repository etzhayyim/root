from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PadState(TypedDict): pad_specs: dict; validation_results: List[str]; def validate_dimensions(state: PadState): specs = state['pad_specs']; errors = []; if specs.get('diameter', 0) <= 0: errors.append('Invalid Diameter'); return {'validation_results': errors}; builder = StateGraph(PadState); builder.add_node('validate', validate_dimensions); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()