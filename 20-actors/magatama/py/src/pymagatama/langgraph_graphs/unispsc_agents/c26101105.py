from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MotorProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_motor_specs(state: MotorProcurementState): specs = state.get('specs', {}); errors = []; required = ['voltage', 'hp', 'efficiency_rating']; for field in required: if field not in specs: errors.append(f'Missing spec: {field}'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(MotorProcurementState); graph.add_node('validate', validate_motor_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()