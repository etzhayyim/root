from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftSystemState(TypedDict): component_id: str; compliance_docs: List[str]; inspection_status: bool; def validate_compliance(state: AircraftSystemState): state['compliance_docs'] = ['ISO9001', 'AS9100']; return {'inspection_status': True if 'AS9100' in state['compliance_docs'] else False}; def final_review(state: AircraftSystemState): return {'inspection_status': state['inspection_status']}; graph = StateGraph(AircraftSystemState); graph.add_node('validate', validate_compliance); graph.add_node('review', final_review); graph.add_edge('validate', 'review'); graph.add_edge('review', END); graph.set_entry_point('validate'); graph = graph.compile()