from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; validation_status: bool; compliance_docs: List[str]; def validate_purity(state: PharmaState): return {'validation_status': True} if 'USP' in state['compliance_docs'] else {'validation_status': False}; def check_storage(state: PharmaState): return {'compliance_docs': state['compliance_docs'] + ['Temp_Validated']}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('storage', check_storage); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph.set_entry_point('validate'); graph = graph.compile()