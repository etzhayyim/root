from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BrickState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: BrickState): state['valid'] = state['specs'].get('alumina_pct', 0) > 55; return state; graph = StateGraph(BrickState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()