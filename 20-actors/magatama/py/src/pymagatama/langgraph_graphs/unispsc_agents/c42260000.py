from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MortuaryState(TypedDict): data: dict; errors: Annotated[list, operator.add]; def validate_compliance(state: MortuaryState): return {'errors': ['Missing ISO 13485' if not state['data'].get('iso_cert') else None]}; def check_specs(state: MortuaryState): return {'data': {**state['data'], 'status': 'verified'}}; graph = StateGraph(MortuaryState); graph.add_node('validate', validate_compliance); graph.add_node('specs', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'specs'); graph.add_edge('specs', END); graph = graph.compile()