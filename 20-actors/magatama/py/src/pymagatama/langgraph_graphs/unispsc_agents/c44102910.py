from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): spec: dict; validated: bool; result: str; def validate_specs(state: ProcureState): specs = state['spec']; is_valid = all(k in specs for k in ['dimensions', 'heat_resistance']); return {'validated': is_valid, 'result': 'Validated' if is_valid else 'Missing mandatory attributes'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()