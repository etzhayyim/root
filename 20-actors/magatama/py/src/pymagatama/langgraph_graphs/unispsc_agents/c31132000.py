from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MetalState(TypedDict): specs: dict; validation_results: List[str]; def validate_density(state: MetalState): val = state['specs'].get('density', 0); state['validation_results'].append('Pass' if val > 6.0 else 'Fail'); return state; def validate_composition(state: MetalState): return state; graph = StateGraph(MetalState); graph.add_node('density_check', validate_density); graph.add_node('comp_check', validate_composition); graph.set_entry_point('density_check'); graph.add_edge('density_check', 'comp_check'); graph.add_edge('comp_check', END); graph = graph.compile()