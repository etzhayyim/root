from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): component_id: str; compliance_status: bool; radiation_test: bool; def validate_compliance(state: ProcureState): state['compliance_status'] = True; return state; def check_radiation(state: ProcureState): state['radiation_test'] = state.get('radiation_level', 0) > 1000; return state; graph = StateGraph(ProcureState); graph.add_node('compliance', validate_compliance); graph.add_node('radiation', check_radiation); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'radiation'); graph.add_edge('radiation', END); graph = graph.compile()