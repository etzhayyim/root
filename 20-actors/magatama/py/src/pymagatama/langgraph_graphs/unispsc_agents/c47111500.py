from langgraph.graph import StateGraph, END; from typing import TypedDict; class LaundryState(TypedDict): spec: dict; validated: bool; graph = StateGraph(LaundryState); def validate_specs(state: LaundryState): val = state['spec'].get('load_capacity') is not None; return {'validated': val}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()