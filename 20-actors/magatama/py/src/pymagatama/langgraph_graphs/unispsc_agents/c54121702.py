from langgraph.graph import StateGraph, END; from typing import TypedDict; class PearlState(TypedDict): pearl_data: dict; validity: bool; graph = StateGraph(PearlState); def validate_gem(state: PearlState): state['validity'] = 'cert_id' in state['pearl_data'] and state['pearl_data']['value'] > 0; return state; graph.add_node('validate', validate_gem); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()