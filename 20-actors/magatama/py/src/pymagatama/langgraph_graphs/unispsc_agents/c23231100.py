from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): spec: dict; status: str; def validate_specs(state: ProcureState): return {'status': 'validated' if 'machine_tolerances' in state['spec'] else 'pending'}; def execute_workflow(state: ProcureState): return {'status': 'processed'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('process', execute_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()