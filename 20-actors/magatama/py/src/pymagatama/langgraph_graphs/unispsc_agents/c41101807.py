from langgraph.graph import StateGraph, END; from typing import TypedDict; class AnalysisState(TypedDict): spec: dict; validated: bool; export_check: bool; def validate_specs(state: AnalysisState): state['validated'] = state['spec'].get('accelerating_voltage_kv', 0) > 0; return state; def check_export_compliance(state: AnalysisState): state['export_check'] = True; return state; graph = StateGraph(AnalysisState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()