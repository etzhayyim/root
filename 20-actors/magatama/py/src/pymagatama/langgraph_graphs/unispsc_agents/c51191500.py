from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): medicine_name: str; validation_status: bool; compliance_docs: List[str]; def validate_api(state: PharmState): state['validation_status'] = True if state.get('medicine_name') else False; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_api); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()