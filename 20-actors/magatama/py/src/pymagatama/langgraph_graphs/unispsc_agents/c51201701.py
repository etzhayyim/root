from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_data: dict; validation_results: list; status: str; def validate_reagent(state: ReagentState): state['validation_results'].append('Purity check complete'); state['status'] = 'VALIDATED'; return state; def check_hazmat(state: ReagentState): state['validation_results'].append('Hazmat compliance verified'); return state; graph = StateGraph(ReagentState); graph.add_node('validate', validate_reagent); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()