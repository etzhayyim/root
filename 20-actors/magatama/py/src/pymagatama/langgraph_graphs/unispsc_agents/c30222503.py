from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FacilityState(TypedDict): facility_id: str; compliance_docs: List[str]; approved: bool; def validate_facility(state: FacilityState): return {'approved': len(state['compliance_docs']) > 2}; def routing(state: FacilityState): return 'validate' if state['facility_id'] else END; graph = StateGraph(FacilityState); graph.add_node('validate', validate_facility); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()