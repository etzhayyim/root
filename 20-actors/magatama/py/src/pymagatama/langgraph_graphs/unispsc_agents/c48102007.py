from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BarProcurementState(TypedDict): specs: dict; ready_for_procurement: bool; def validate_specs(state: dict): return { 'ready_for_procurement': True if 'weight' in state['specs'] else False }; graph = StateGraph(BarProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()