from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EducationWorkflowState(TypedDict): content_id: str; content_type: str; medical_review_passed: bool; public_display_ready: bool; def validate_content(state: EducationWorkflowState): return {'medical_review_passed': True} if 'eating_disorder' in state['content_type'] else {'medical_review_passed': False}; def approve_workflow(state: EducationWorkflowState): return {'public_display_ready': True}; graph = StateGraph(EducationWorkflowState); graph.add_node('validate', validate_content); graph.add_node('approve', approve_workflow); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()