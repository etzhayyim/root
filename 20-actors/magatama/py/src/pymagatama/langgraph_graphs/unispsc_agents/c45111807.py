from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class InterfaceState(TypedDict): interface_type: str; specs: dict; valid: bool; def validate_interface(state: InterfaceState): state['valid'] = state['specs'].get('transmission_speed', 0) > 0; return state; graph = StateGraph(InterfaceState); graph.add_node('validate', validate_interface); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()