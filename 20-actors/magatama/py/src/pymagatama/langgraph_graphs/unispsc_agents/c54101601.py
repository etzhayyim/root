from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class JewelryState(TypedDict): items: Annotated[list, operator.add]; status: str; def validate_material(state): print('Validating material purity...'); return {'status': 'Material Verified'}; def check_dimensions(state): print('Verifying physical dimensions...'); return {'status': 'Dimensions Validated'}; graph = StateGraph(JewelryState); graph.add_node('material_check', validate_material); graph.add_node('dimension_check', check_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dimension_check'); graph.add_edge('dimension_check', END); graph = graph.compile()