from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CrimpState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(CrimpState); def validate_spec(state: CrimpState): return {'validation_results': ['Material check complete', 'Gauge compatibility verified']}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()