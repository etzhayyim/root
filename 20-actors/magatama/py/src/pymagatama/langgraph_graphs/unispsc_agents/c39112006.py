from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LampState(TypedDict): specs: dict; valid: bool; error: str; def validate_spec(state: LampState): state['valid'] = 'IP rating' in state['specs'] and 'lumens' in state['specs']; return state; builder = StateGraph(LampState); builder.add_node('validate', validate_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()