from langgraph.graph import StateGraph, END; from typing import TypedDict; class FilterState(TypedDict): spec: dict; valid: bool; def validate_specs(state: FilterState): state['valid'] = all(k in state['spec'] for k in ['efficiency', 'dimensions']); return state; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()