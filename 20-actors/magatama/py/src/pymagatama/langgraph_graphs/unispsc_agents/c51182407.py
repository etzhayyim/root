from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): cold_chain_compliant: bool; batch_verified: bool; def check_cold_chain(state: PharmState): return {'cold_chain_compliant': True}; def verify_batch(state: PharmState): return {'batch_verified': True}; graph = StateGraph(PharmState); graph.add_node('check_cold', check_cold_chain); graph.add_node('verify_batch', verify_batch); graph.set_entry_point('check_cold'); graph.add_edge('check_cold', 'verify_batch'); graph.add_edge('verify_batch', END); graph = graph.compile()