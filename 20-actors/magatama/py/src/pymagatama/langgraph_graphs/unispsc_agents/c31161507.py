from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): specs: dict; valid: bool; def validate_specs(state: FastenerState): state['valid'] = all(k in state['specs'] for k in ['material', 'thread_type', 'length']); return state; graph = StateGraph(FastenerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()