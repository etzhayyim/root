from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_gmp(state: PharmState): state['validation_results'].append('GMP_Verified') if 'gmp_cert' in state['input_data'] else state['validation_results'].append('GMP_Missing'); return state; def check_purity(state: PharmState): purity = state['input_data'].get('purity', 0); state['status'] = 'Approved' if purity >= 99.0 else 'Rejected'; return state; graph = StateGraph(PharmState); graph.add_node('validate_gmp', validate_gmp); graph.add_node('check_purity', check_purity); graph.add_edge('validate_gmp', 'check_purity'); graph.add_edge('check_purity', END); graph.set_entry_point('validate_gmp'); app = graph.compile()