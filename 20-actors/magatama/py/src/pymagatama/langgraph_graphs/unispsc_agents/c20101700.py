from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ExcavationState(TypedDict): cutter_id: str; alloy_spec: str; validation_status: bool; workflow_log: Annotated[list, operator.add]; def validate_cutter_spec(state: ExcavationState) -> ExcavationState: status = len(state['alloy_spec']) > 10; return {**state, 'validation_status': status, 'workflow_log': ['Spec validated']}; def finalize_procurement(state: ExcavationState) -> ExcavationState: return {**state, 'workflow_log': state['workflow_log'] + ['Procurement finalized']}; graph = StateGraph(ExcavationState); graph.add_node('validate', validate_cutter_spec); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()