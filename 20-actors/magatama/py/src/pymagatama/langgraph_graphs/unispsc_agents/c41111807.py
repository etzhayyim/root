from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadiographyState(TypedDict): source_activity: float; is_shielded: bool; compliance_docs: List[str]; def validate_safety(state: RadiographyState): return {'is_shielded': state['source_activity'] < 100}; def check_compliance(state: RadiographyState): return {'compliance_docs': ['NRC_License', 'Leak_Test_Cert']}; graph = StateGraph(RadiographyState); graph.add_node('safety_check', validate_safety); graph.add_node('compliance_audit', check_compliance); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'compliance_audit'); graph.add_edge('compliance_audit', END); graph = graph.compile()