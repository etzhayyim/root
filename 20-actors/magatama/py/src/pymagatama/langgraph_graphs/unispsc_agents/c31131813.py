from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BerylliumState(TypedDict): part_id: str; compliance_docs: List[str]; inspection_passed: bool; def validate_tolerances(state: BerylliumState): print('Validating CAD against AS9100 specs...'); state['inspection_passed'] = True; return state; def check_export_controls(state: BerylliumState): print('Performing ITAR/EAR screening...'); return state; graph = StateGraph(BerylliumState); graph.add_node('validate', validate_tolerances); graph.add_node('export_check', check_export_controls); graph.set_entry_point('export_check'); graph.add_edge('export_check', 'validate'); graph.add_edge('validate', END); graph = graph.compile()