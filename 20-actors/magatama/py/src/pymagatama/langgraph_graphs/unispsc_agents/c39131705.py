from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableTrayState(TypedDict): specs: dict; validation_results: List[str]; def validate_load(state): state['validation_results'].append('Load capacity verified') if state['specs'].get('load', 0) > 0 else state['validation_results'].append('Load failed'); return state; def validate_material(state): state['validation_results'].append('Material grade compliant'); return state; graph = StateGraph(CableTrayState); graph.add_node('load_check', validate_load); graph.add_node('material_check', validate_material); graph.add_edge('load_check', 'material_check'); graph.add_edge('material_check', END); graph.set_entry_point('load_check'); graph = graph.compile()