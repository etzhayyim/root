from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TricycleState(TypedDict): model_id: str; specs: dict; valid: bool; def validate_specs(state: TricycleState): state['valid'] = 'load_capacity_kg' in state['specs'] and state['specs']['load_capacity_kg'] > 0; return state; graph = StateGraph(TricycleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()