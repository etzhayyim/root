from langgraph.graph import StateGraph, END; from typing import TypedDict; class PaintState(TypedDict): pigment_data: dict; compliance_check: bool; def validate_pigments(state: PaintState): state['compliance_check'] = 'ASTM D4236' in state['pigment_data'].get('specs', []) return state; def finalize_specs(state: PaintState): return {'compliance_check': state['compliance_check']}; graph = StateGraph(PaintState); graph.add_node('validate', validate_pigments); graph.add_node('finalize', finalize_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()