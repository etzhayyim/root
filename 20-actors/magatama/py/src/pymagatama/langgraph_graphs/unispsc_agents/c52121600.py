from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[dict]; qc_status: str; def validate_specs(state: ProcurementState): return {'qc_status': 'verified' if all(i.get('flame_retardant') for i in state['items']) else 'rejected'}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()