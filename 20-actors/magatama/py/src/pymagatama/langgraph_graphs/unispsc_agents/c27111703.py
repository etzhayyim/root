from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolSpec(TypedDict): items: List[str]; validated: bool; defect_rate: float; def validate_specs(state: ToolSpec): return {'validated': len(state['items']) > 0}; def check_quality(state: ToolSpec): return {'defect_rate': 0.0}; graph = StateGraph(ToolSpec); graph.add_node('validate', validate_specs); graph.add_node('qc', check_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'qc'); graph.add_edge('qc', END); graph = graph.compile()