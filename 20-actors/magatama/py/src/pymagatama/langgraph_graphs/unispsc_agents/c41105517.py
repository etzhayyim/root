from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_name: str; purities: List[float]; valid: bool; def check_purity(state: KitState): state['valid'] = all(p > 1.8 for p in state['purities']); return state; graph = StateGraph(KitState); graph.add_node('validate', check_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()