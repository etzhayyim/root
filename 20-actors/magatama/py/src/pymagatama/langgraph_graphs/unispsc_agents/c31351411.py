from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): tube_specs: dict; status: str; def validate_specs(state: AssemblyState): print('Validating material grade and tolerance...'); return {'status': 'validated' if state['tube_specs'].get('material') == 'Waspalloy' else 'rejected'}; def check_compliance(state: AssemblyState): print('Checking aerospace export compliance...'); return {'status': 'compliant'}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()