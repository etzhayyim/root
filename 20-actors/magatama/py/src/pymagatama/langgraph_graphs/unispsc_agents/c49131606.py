from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AmmoOrder(TypedDict): sender: str; caliber: str; qty: int; verified: bool; payload: str; def validate_specs(state: AmmoOrder): state['verified'] = (state['caliber'] != '' and state['qty'] > 0); return state; def security_check(state: AmmoOrder): state['payload'] = 'SECURITY_CLEARED' if state['verified'] else 'REJECTED'; return state; graph = StateGraph(AmmoOrder); graph.add_node('validate', validate_specs); graph.add_node('security', security_check); graph.add_edge('validate', 'security'); graph.add_edge('security', END); graph.set_entry_point('validate'); graph = graph.compile()