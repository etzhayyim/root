from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): labels: List[str]; validated_labels: List[str]; status: str; def validate_sterilization_spec(state: State): validated = [l for l in state['labels'] if 'ISO-11140' in l]; return {'validated_labels': validated, 'status': 'COMPLETE'}; graph = StateGraph(State); graph.add_node('validate', validate_sterilization_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()