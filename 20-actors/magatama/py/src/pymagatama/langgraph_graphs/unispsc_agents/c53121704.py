from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PortfolioState(TypedDict): items: List[dict]; valid: bool; def validate_portfolio(state: PortfolioState): state['valid'] = all('material' in item for item in state['items']); return state; graph = StateGraph(PortfolioState); graph.add_node('validate', validate_portfolio); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()