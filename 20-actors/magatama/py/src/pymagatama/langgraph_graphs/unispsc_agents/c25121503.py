from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LocomotiveState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: LocomotiveState): results = []; if state['specs'].get('emission_compliance') != 'Tier 4': results.append('Emission violation'); return {'validation_results': results}; def check_safety(state: LocomotiveState): return {'validation_results': state['validation_results'] + ['Safety checks cleared']}; graph = StateGraph(LocomotiveState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()