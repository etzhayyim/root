from langgraph.graph import StateGraph, END; from typing import TypedDict; class TableState(TypedDict): specs: dict; valid: bool; def validate_specs(state: TableState): is_valid = all(key in state['specs'] for key in ['surface', 'dimensions']); return {'valid': is_valid}; graph = StateGraph(TableState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()