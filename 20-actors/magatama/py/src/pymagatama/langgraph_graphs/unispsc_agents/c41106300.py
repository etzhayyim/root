from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PCRState(TypedDict): items: List[str]; qc_passed: bool; storage_compliant: bool; def check_quality(state: PCRState): return {'qc_passed': all(item.startswith('Cert-') for item in state['items'])}; def check_cold_chain(state: PCRState): return {'storage_compliant': True}; graph = StateGraph(PCRState); graph.add_node('qc', check_quality); graph.add_node('logistics', check_cold_chain); graph.set_entry_point('qc'); graph.add_edge('qc', 'logistics'); graph.add_edge('logistics', END); graph = graph.compile()