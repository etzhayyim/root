from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): content: str; status: str; def validate_gmp(state: State): return {'status': 'Validated' if 'GMP' in state['content'] else 'Rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()