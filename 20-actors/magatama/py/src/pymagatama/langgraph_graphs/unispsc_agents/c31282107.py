from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; specs: dict; approved: bool; def validate_specs(state: ComponentState): cert = state['specs'].get('certification'); state['approved'] = cert == 'ISO9001'; return state; def export_check(state: ComponentState): state['approved'] = state['approved'] and state['specs'].get('export_license') is not None; return state; graph = StateGraph(ComponentState); graph.add_node('validate', validate_specs); graph.add_node('export_review', export_check); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph.set_entry_point('validate'); graph = graph.compile()