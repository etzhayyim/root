from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): spec: dict; status: str; def validate_spec(state: ProcureState): return {'status': 'validated' if 'thickness' in state['spec'] else 'missing'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()