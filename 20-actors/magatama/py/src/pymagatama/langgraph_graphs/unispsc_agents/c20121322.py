from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MotorState(TypedDict): motor_id: str; specs: dict; validation_log: Annotated[Sequence[str], operator.add]; def validate_motor_spec(state: MotorState): specs = state.get('specs', {}); logs = []; if 'torque' not in specs: logs.append('Error: Torque rating missing'); if 'resolution' not in specs: logs.append('Error: Resolution missing'); return {'validation_log': logs}; def compile_graph(): graph = StateGraph(MotorState); graph.add_node('validate', validate_motor_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()