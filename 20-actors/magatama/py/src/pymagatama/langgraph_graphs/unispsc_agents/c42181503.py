from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LubricantState(TypedDict): vendor_docs: dict; compliance_status: bool; validation_log: List[str]; def validate_medical_grade(state: LubricantState): state['compliance_status'] = 'ISO13485' in state['vendor_docs'].get('certs', []); state['validation_log'].append('Validation completed'); return state; graph = StateGraph(LubricantState); graph.add_node('validate', validate_medical_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()