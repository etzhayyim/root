from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GearState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: GearState): required = ['Gear Ratio', 'Material Grade']; missing = [f for f in required if f not in state['specs']]; state['valid'] = len(missing) == 0; state['error'] = f'Missing: {missing}' if missing else ''; return state; graph = StateGraph(GearState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()