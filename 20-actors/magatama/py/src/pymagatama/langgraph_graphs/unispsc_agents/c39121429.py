from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: ProcState): specs = state['specs']; is_valid = all([specs.get('insertion_loss_db', 1) < 0.5, specs.get('ferrule_material') is not None]); return {'valid': is_valid, 'error': '' if is_valid else 'Invalid spec parameters'}; graph = StateGraph(ProcState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); compiled_graph = graph.compile()