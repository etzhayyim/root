from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DistributionState(TypedDict): procurement_data: dict; validation_results: Annotated[list, operator.add]; def validate_spec(state: DistributionState): results = []; if 'capacity' not in state['procurement_data']: results.append('Missing capacity specification'); return {'validation_results': results}; def compliance_check(state: DistributionState): return {'validation_results': ['Compliance Verified']}; graph = StateGraph(DistributionState); graph.add_node('validate', validate_spec); graph.add_node('compliance', compliance_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()