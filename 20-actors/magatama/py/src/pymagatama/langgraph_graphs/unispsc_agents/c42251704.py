from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RehabState(TypedDict): device_specs: dict; compliance: bool; inspection_passed: bool; def validate_specs(state: RehabState): return {'compliance': state['device_specs'].get('load_capacity', 0) > 150}; def safety_inspection(state: RehabState): return {'inspection_passed': True}; graph = StateGraph(RehabState); graph.add_node('validate', validate_specs); graph.add_node('inspect', safety_inspection); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()