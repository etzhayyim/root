from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReportState(TypedDict): data_sources: List[str]; validation_results: dict; report_config: dict; def validate_source(state: ReportState): return {'validation_results': {'status': 'passed'}}; def generate_report(state: ReportState): return {'report_config': {'format': 'PDF'}}; graph = StateGraph(ReportState); graph.add_node('validate', validate_source); graph.add_node('generate', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'generate'); graph.add_edge('generate', END); graph = graph.compile()