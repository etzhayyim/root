from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CrownState(TypedDict): crown_specs: dict; validation_results: List[str]; graph = StateGraph(CrownState); def validate_material(state): return {'validation_results': ['Material ISO compliant']}; def check_fit(state): return {'validation_results': ['Fit within tolerance']}; graph.add_node('material_check', validate_material); graph.add_node('fit_check', check_fit); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'fit_check'); graph.add_edge('fit_check', END); graph = graph.compile()