from langgraph.graph import StateGraph, END; from typing import TypedDict; class SeatingState(TypedDict): spec: dict; validated: bool; def validate_spec(state: SeatingState): state['validated'] = all(k in state['spec'] for k in ['material', 'load_limit']); return state; graph = StateGraph(SeatingState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()