from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PinataState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(PinataState); def validate_integrity(state: PinataState): return {'validation_results': ['Integrity checked']}; def check_safety(state: PinataState): return {'validation_results': ['Safety standards met']}; graph.add_node('integrity', validate_integrity); graph.add_node('safety', check_safety); graph.set_entry_point('integrity'); graph.add_edge('integrity', 'safety'); graph.add_edge('safety', END); app = graph.compile()