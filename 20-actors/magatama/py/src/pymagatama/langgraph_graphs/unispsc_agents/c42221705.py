from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class InfusionState(TypedDict): vial_specs: dict; compliance_check: bool; graph = StateGraph(InfusionState); def validate_specs(state: InfusionState): return {'compliance_check': state['vial_specs'].get('sterility') == 'pass'}; def finalize_procurement(state: InfusionState): return {'compliance_check': True}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()