from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WindowTreatmentState(TypedDict): specs: dict; compliance_check: bool; error_log: List[str]; def validate_dimensions(state: WindowTreatmentState): state['compliance_check'] = 'width' in state['specs'] and 'drop' in state['specs']; return state; def final_report(state: WindowTreatmentState): print('Standard processed.'); return state; graph = StateGraph(WindowTreatmentState); graph.add_node('validate', validate_dimensions); graph.add_node('finalize', final_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()