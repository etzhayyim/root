from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PsychrometerState(TypedDict): input_data: dict; validation_results: List[str]; status: str; def validate_spec(state: PsychrometerState): result = 'Valid' if 'accuracy' in state['input_data'] else 'Invalid'; return {'validation_results': [result], 'status': 'Checked'}; graph = StateGraph(PsychrometerState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()