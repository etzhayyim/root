from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): data: dict; steps: Annotated[Sequence[str], operator.add]; errors: Annotated[Sequence[str], operator.add]; def validate_specs(state: RobotState): return {'steps': ['specs_validated']}; def verify_compliance(state: RobotState): return {'steps': ['compliance_verified']}; def assembly_logic(state: RobotState): return {'steps': ['assembly_workflow_executed']}; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.add_node('compliance', verify_compliance); graph.add_node('assembly', assembly_logic); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', 'assembly'); graph.add_edge('assembly', END); graph = graph.compile()