from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec: dict; validated: bool; state = ProcState(spec={}, validated=False); def validate_specs(state: ProcState): state['validated'] = 'material_composition' in state['spec'] and 'load_rating' in state['spec']; return state; builder = StateGraph(ProcState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()