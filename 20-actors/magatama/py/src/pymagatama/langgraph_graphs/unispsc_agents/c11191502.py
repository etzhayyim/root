from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; class State(TypedDict): chemical_data: dict; verification_logs: Sequence[str]; is_compliant: bool; def validate_chemical(state: State): log = 'Validating CAS and SDS compliance.'; return { 'verification_logs': state['verification_logs'] + [log], 'is_compliant': True }; def check_export_control(state: State): log = 'Checking dual-use export control registry.'; return { 'verification_logs': state['verification_logs'] + [log] }; graph = StateGraph(State); graph.add_node('validate', validate_chemical); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()