from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BilletState(TypedDict): billet_type: str; composition_data: dict; approved: bool; def validate_composition(state: BilletState): state['approved'] = state['composition_data'].get('purity', 0) > 99.0; return state; def check_dimensions(state: BilletState): state['approved'] = state['approved'] and state.get('dimensions_valid', False); return state; graph = StateGraph(BilletState); graph.add_node('validate', validate_composition); graph.add_node('dimensions', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', 'dimensions'); graph.add_edge('dimensions', END); graph = graph.compile()