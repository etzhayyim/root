from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PaintPanState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: PaintPanState): errors = []; if not state['specs'].get('material'): errors.append('Missing material'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(PaintPanState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()