from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MorgueWorkflowState(TypedDict): items: List[str]; validation_results: dict; status: str; def validate_spec(state: MorgueWorkflowState): state['validation_results'] = {'passed': True}; return state; def check_compliance(state: MorgueWorkflowState): state['status'] = 'COMPLIANT'; return state; graph = StateGraph(MorgueWorkflowState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()