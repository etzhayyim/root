from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PrintState(TypedDict): model: str; parts: List[str]; validated: bool; def check_compatibility(state: PrintState): state['validated'] = state['model'] in ['LaserJet-X', 'InkJet-Pro']; return state; def finalize_order(state: PrintState): return state; graph = StateGraph(PrintState); graph.add_node('validate', check_compatibility); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()