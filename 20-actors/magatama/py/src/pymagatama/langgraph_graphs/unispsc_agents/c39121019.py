from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: ProcureState): validation = []; specs = state.get('specs', {}); required = ['Inductance_mH', 'Isolation_Voltage_kV']; for r in required: validation.append('pass' if r in specs else 'fail'); return {'validation_results': validation}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()