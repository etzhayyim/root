from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class State(TypedDict): commodity_code: str; spec_data: Dict; validation_log: List[str]; class ConnectionValidator: def validate(self, state: State) -> State: state['validation_log'].append('Validating material grade and pressure rating.'); return state; def compile_graph(): validator = ConnectionValidator(); graph = StateGraph(State); graph.add_node('validate', validator.validate); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()