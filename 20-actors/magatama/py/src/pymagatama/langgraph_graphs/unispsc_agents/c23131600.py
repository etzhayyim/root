from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(CompressorState); def validate_specs(state: CompressorState): return {'validation_results': ['Pressure check PASS', 'ISO standard verified']}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()