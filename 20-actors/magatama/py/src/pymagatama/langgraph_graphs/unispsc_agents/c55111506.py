from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): content_type: str; is_licensed: bool; approval_status: str; def validate_license(state: ProcureState): return {'is_licensed': True} if 'license' in state['content_type'] else {'is_licensed': False}; def check_approval(state: ProcureState): return {'approval_status': 'APPROVED' if state['is_licensed'] else 'REJECTED'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_license); graph.add_node('approve', check_approval); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()