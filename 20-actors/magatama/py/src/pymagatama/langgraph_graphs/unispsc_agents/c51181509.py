from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_gmp(state: PharmState): state['validation_results'].append('GMP cert verified'); return {'status': 'validated'}; def check_purity(state: PharmState): state['validation_results'].append('Purity > 99%'); return {'status': 'ready'}; graph = StateGraph(PharmState); graph.add_node('validate_gmp', validate_gmp); graph.add_node('check_purity', check_purity); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', 'check_purity'); graph.add_edge('check_purity', END); graph = graph.compile()