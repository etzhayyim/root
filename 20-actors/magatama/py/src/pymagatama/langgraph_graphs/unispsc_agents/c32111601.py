from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: State): specs = state['specs']; is_valid = 'Peak Spectral Sensitivity' in specs and 'Dark Current' in specs; return {'valid': is_valid, 'log': ['Validation complete'] if is_valid else ['Missing critical fields']}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()