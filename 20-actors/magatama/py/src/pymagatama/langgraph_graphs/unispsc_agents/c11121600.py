from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class MineralState(TypedDict): chemical_data: dict; validation_results: List[str]; def validate_purity(state: MineralState) -> MineralState: state['validation_results'].append('Purity check passed') if state['chemical_data'].get('purity', 0) > 99.0 else state['validation_results'].append('Purity insufficient'); return state; def check_hazmat(state: MineralState) -> MineralState: state['validation_results'].append('Hazmat check complete'); return state; graph = StateGraph(MineralState); graph.add_node('purity', validate_purity); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('purity'); graph.add_edge('purity', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()