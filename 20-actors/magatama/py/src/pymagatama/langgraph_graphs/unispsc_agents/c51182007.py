from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): chemical_data: dict; compliance_status: bool; workflow_steps: List[str]; def validate_purity(state: State): state['compliance_status'] = state['chemical_data'].get('purity', 0) >= 99.0; return state; def check_regulatory_compliance(state: State): state['workflow_steps'].append('reg_check_complete'); return state; graph = StateGraph(State); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulatory_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()