from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcState(TypedDict): items: List[str]; validated: bool; state = StateGraph(ProcState); def validate_sterile_batch(s: ProcState): s['validated'] = all('sterile' in x.lower() for x in s['items']); return s; state.add_node('validate', validate_sterile_batch); state.set_entry_point('validate'); state.add_edge('validate', END); graph = state.compile()