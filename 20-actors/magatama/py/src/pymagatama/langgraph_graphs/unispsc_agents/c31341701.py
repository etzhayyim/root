from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): vendor_specs: dict; compliance_check: bool; assembly_data: dict; def validate_specs(state: AssemblyState): state['compliance_check'] = 'Alloy grade' in state['vendor_specs'] and 'torque' in state['vendor_specs']; return state; def finalize_order(state: AssemblyState): return {'finalized': True}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()