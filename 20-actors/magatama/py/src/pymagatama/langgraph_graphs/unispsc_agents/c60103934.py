from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CultureState(TypedDict): culture_id: str; viability_score: float; needs_refrigeration: bool; status: str; def validate_integrity(state: CultureState): print(f'Validating purity of {state['culture_id']}'); return {'status': 'validated' if state['viability_score'] > 0.9 else 'rejected'}; graph = StateGraph(CultureState); graph.add_node('validate', validate_integrity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()