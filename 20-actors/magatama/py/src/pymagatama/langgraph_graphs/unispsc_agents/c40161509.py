from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FilterState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: FilterState): missing = [f for f in ['material', 'rating'] if f not in state['specs']]; return {'validation_errors': missing, 'status': 'invalid' if missing else 'validated'}; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()