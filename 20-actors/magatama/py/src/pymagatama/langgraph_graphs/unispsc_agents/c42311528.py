from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WoundCareState(TypedDict): doc_id: str; compliance_checked: bool; specs: dict; graph = StateGraph(WoundCareState); def validate_specs(state: WoundCareState): state['compliance_checked'] = all(k in state['specs'] for k in ['sterility', 'biocompatibility']); return state; graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()