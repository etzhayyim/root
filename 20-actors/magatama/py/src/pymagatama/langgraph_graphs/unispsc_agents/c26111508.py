from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(ProcureState); def validate_specs(state: ProcureState): state['compliance_check'] = 'Torque Rating' in state['specs'] and 'Maximum RPM Rating' in state['specs']; return state; def check_export_controls(state: ProcureState): print('Checking dual-use export compliance for PTO units...'); return state; graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()