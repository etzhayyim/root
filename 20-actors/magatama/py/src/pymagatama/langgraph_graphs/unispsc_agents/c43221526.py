from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EntrySystemState(TypedDict): vendor_specs: dict; validation_report: list; status: str; def validate_specs(state: dict): status = 'PASS' if 'ip_connectivity' in state['vendor_specs'] else 'FAIL'; return {'validation_report': [f'Check connectivity: {status}'], 'status': status}; graph = StateGraph(EntrySystemState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()