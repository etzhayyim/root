from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GasketState(TypedDict): vendor_spec: dict; validation_report: List[str]; def validate_material(state: GasketState): state['validation_report'].append('Verifying material compatibility with ASME standards.'); return state; def validate_dimensions(state: GasketState): state['validation_report'].append('Dimensional check passed.'); return state; graph = StateGraph(GasketState); graph.add_node('material_check', validate_material); graph.add_node('dimension_check', validate_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dimension_check'); graph.add_edge('dimension_check', END); graph = graph.compile()