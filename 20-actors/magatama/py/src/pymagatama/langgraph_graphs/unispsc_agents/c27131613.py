from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; valid: bool; def validate_coupling(state: State): specs = state['specs']; state['valid'] = all(k in specs for k in ['pressure', 'size', 'material']); return state; graph = StateGraph(State); graph.add_node('validate', validate_coupling); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()