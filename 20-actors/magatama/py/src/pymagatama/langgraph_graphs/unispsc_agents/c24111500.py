from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class BagProcurementState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: BagProcurementState): print('Validating bag specs...'); state['validation_passed'] = 'material_composition' in state['specs'] and 'load_capacity_kg' in state['specs']; return state; graph = StateGraph(BagProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()