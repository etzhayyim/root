from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IrrigationState(TypedDict): device_id: str; compliance_docs: List[str]; validated: bool; def validate_specs(state: IrrigationState): state['validated'] = 'ISO13485' in state['compliance_docs']; return {'validated': state['validated']}; graph = StateGraph(IrrigationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()