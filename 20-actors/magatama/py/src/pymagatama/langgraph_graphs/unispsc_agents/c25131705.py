from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DroneProcureState(TypedDict): drone_type: str; specs: dict; compliance_check: bool; def validate_specs(state): return {'compliance_check': state['specs'].get('encryption_standard') == 'AES-256'}; def export_check(state): return {'compliance_check': state.get('compliance_check', False) and 'export_license_number' in state['specs']}; graph = StateGraph(DroneProcureState); graph.add_node('validate', validate_specs); graph.add_node('export_review', export_check); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()