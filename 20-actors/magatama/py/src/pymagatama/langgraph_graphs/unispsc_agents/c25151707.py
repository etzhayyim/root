from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SatelliteState(TypedDict): satellite_id: str; orbit_specs: dict; compliance_cleared: bool; def validate_export_control(state: SatelliteState): state['compliance_cleared'] = True; return state; def check_orbit_specs(state: SatelliteState): return state; graph = StateGraph(SatelliteState); graph.add_node('export_check', validate_export_control); graph.add_node('orbit_check', check_orbit_specs); graph.set_entry_point('export_check'); graph.add_edge('export_check', 'orbit_check'); graph.add_edge('orbit_check', END); graph = graph.compile()