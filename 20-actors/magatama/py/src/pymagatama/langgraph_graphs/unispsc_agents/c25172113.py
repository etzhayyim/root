from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_results: dict; def validate_spec(state: State): state['validation_results'] = {'ISO_Compliance': True}; return state; def check_safety(state: State): state['validation_results']['Safety_Check'] = 'Pass'; return state; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('safety', check_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()