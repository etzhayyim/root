from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; validation: bool; def validate_specs(state: ProcState): state['validation'] = all(k in state['specs'] for k in ['grade', 'tolerance', 'surface_finish']); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()