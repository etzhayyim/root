from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def material_validator(state: AssemblyState): print('Validating Inconel material specs'); return {'validated': True}; def cert_checker(state: AssemblyState): print('Verifying MTC certificates'); return {'certs': ['Verified']}; graph = StateGraph(AssemblyState); graph.add_node('validate', material_validator); graph.add_node('certify', cert_checker); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph.set_entry_point('validate'); graph = graph.compile()