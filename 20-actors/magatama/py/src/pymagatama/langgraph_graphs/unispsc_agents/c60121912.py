from langgraph.graph import StateGraph, END; from typing import TypedDict; class ArtStumpState(TypedDict): material: str; density_score: float; graph = StateGraph(ArtStumpState); def validate_material(state): return {'density_score': 0.9 if state['material'] == 'compressed_paper' else 0.0}; graph.add_node('validation', validate_material); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()