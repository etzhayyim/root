from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LanyardState(TypedDict): items: List[str]; validated_items: List[str]; import_status: str; def validate_lanyard_spec(state: LanyardState): validated = [item for item in state['items'] if 'breakaway' in item.lower()]; return {'validated_items': validated, 'import_status': 'complete'}; graph = StateGraph(LanyardState); graph.add_node('validate', validate_lanyard_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()