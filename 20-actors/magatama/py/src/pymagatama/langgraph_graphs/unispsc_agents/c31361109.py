from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(AssemblyState); def validate_specs(state: AssemblyState): return {'validation_results': ['Material check passed', 'Dimension compliance verified']}; graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); compiled_graph = graph.compile()