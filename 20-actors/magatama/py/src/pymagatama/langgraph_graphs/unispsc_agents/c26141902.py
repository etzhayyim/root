from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GaugeState(TypedDict): specs: dict; compliance_approved: bool; source_checked: bool; def validate_source(state: GaugeState): state['source_checked'] = 'isotope_id' in state['specs']; return state; def verify_compliance(state: GaugeState): state['compliance_approved'] = state.get('source_checked', False) and 'iso_certification' in state['specs']; return state; graph = StateGraph(GaugeState); graph.add_node('validate', validate_source); graph.add_node('compliance', verify_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()