from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IncubatorState(TypedDict): specs: dict; ready_for_procurement: bool; def validate_specs(state: IncubatorState): state['ready_for_procurement'] = all(k in state['specs'] for k in ['temp_range', 'co2_accuracy']); return state; graph = StateGraph(IncubatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()