from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class NapkinState(TypedDict): specs: dict; validation_results: list; status: str; def validate_material(state: NapkinState) -> NapkinState: material = state['specs'].get('material'); state['validation_results'].append('Material verified: ' + str(material)); return state; def check_safety(state: NapkinState) -> NapkinState: if 'food_contact_safety_certification' in state['specs']: state['validation_results'].append('Safety cert passed'); else: state['status'] = 'REJECTED'; return state; graph = StateGraph(NapkinState); graph.add_node('validate_material', validate_material); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()