from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_check: bool; spec_compliance: bool; item_code: str; data = {'item_code': '52151903', 'vendor_check': False, 'spec_compliance': False}; def validate_vendor(state: ProcurementState): state['vendor_check'] = True; return state; def validate_specs(state: ProcurementState): state['spec_compliance'] = True; return state; graph = StateGraph(ProcurementState); graph.add_node('vendor', validate_vendor); graph.add_node('specs', validate_specs); graph.add_edge('vendor', 'specs'); graph.add_edge('specs', END); graph.set_entry_point('vendor'); compile_graph = graph.compile()