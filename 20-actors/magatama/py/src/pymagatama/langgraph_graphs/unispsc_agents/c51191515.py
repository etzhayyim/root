from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): api_data: dict; validation_status: str; def validate_api(state: State): state['validation_status'] = 'PASS' if state['api_data'].get('purity', 0) >= 99.0 else 'FAIL'; return state; graph = StateGraph(State); graph.add_node('validate', validate_api); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()