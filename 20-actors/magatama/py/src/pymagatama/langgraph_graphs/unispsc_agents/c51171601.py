from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; next_step: str; def validate_purity(state: ProcureState): state['validated'] = True; return {'next_step': 'quality_check'}; def quality_check(state: ProcureState): return {'next_step': 'approval'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_purity); graph.add_node('quality_check', quality_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'quality_check'); graph.add_edge('quality_check', END); graph = graph.compile()