from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; valid: bool; def validate_specs(state: RobotState): state['valid'] = 'payload_capacity_kg' in state['specs']; return state; builder = StateGraph(RobotState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()