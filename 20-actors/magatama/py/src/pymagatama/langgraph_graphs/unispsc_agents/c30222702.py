from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ColdStorageState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(ColdStorageState); def validate_thermal_specs(state: ColdStorageState): report = []; if state['specs'].get('TemperatureRangeCelsius', 0) > 0: report.append('Invalid sub-zero requirement'); return {'validation_report': report}; def perform_compliance_check(state: ColdStorageState): return {'validation_report': state['validation_report'] + ['Compliance verified']}; graph.add_node('validate', validate_thermal_specs); graph.add_node('compliance', perform_compliance_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()