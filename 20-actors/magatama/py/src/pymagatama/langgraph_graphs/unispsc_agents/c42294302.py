from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BiopsyState(TypedDict): specs: dict; compliance_passed: bool; auditor_id: str; def validate_specs(state: BiopsyState): return {'compliance_passed': 'ISO_13485' in state['specs'].get('certs', [])}; def finalize_procurement(state: BiopsyState): return {'status': 'Approved' if state['compliance_passed'] else 'Rejected'}; graph = StateGraph(BiopsyState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()