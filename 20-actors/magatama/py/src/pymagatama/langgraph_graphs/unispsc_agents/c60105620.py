from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrainingSpecState(TypedDict): content: str; validated: bool; format: str; def validate_content(state: TrainingSpecState): return {'validated': len(state['content']) > 0}; def format_check(state: TrainingSpecState): return {'format': 'verified' if state['format'] in ['digital', 'print'] else 'manual_review'}; graph = StateGraph(TrainingSpecState); graph.add_node('validate', validate_content); graph.add_node('check_format', format_check); graph.add_edge('validate', 'check_format'); graph.add_edge('check_format', END); graph.set_entry_point('validate'); graph = graph.compile()