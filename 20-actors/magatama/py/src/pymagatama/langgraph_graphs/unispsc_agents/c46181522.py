from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SafetyHoodState(TypedDict): hood_specs: dict; compliance_verified: bool; output: str; def validate_specs(state: SafetyHoodState): return {'compliance_verified': 'standard' in state['hood_specs'].get('certifications', [])}; def final_report(state: SafetyHoodState): return {'output': 'Compliance status: ' + str(state['compliance_verified'])}; graph = StateGraph(SafetyHoodState); graph.add_node('validate', validate_specs); graph.add_node('report', final_report); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()