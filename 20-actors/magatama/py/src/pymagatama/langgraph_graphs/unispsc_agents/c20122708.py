from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class SensorState(TypedDict): sensor_data: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_sensor_spec(state: SensorState): result = 'Valid' if state['sensor_data'].get('range') > 0 else 'Invalid'; return {'validation_results': [f'Spec check: {result}'], 'status': result}; def process_integration(state: SensorState): return {'status': 'Integrated'}; graph = StateGraph(SensorState); graph.add_node('validate', validate_sensor_spec); graph.add_node('integrate', process_integration); graph.set_entry_point('validate'); graph.add_edge('validate', 'integrate'); graph.add_edge('integrate', END); graph = graph.compile()