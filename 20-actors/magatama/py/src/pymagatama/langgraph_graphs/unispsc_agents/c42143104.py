from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AmnioState(TypedDict): items: List[str]; validation_logs: List[str]; approved: bool; def validate_cert(state: AmnioState): state['validation_logs'].append('Verifying ISO 13485'); state['approved'] = True; return state; graph = StateGraph(AmnioState); graph.add_node('cert_check', validate_cert); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', END); graph = graph.compile()