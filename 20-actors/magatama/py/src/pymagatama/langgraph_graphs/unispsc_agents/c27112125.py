from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_name: str; specs: dict; verified: bool; def validate_specs(state: dict): state['verified'] = all(k in state['specs'] for k in ['material', 'jaw_length']); return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()