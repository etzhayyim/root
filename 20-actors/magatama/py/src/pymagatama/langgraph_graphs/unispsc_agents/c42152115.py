from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): tray_id: str; quality_score: float; compliance_met: bool; def validate_tray(state: State): state['compliance_met'] = True if state['quality_score'] > 0.9 else False; return state; graph = StateGraph(State); graph.add_node('validate', validate_tray); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()