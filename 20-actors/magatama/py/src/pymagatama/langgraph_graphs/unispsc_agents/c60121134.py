from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FoilState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_foil_specs(state: FoilState): errors = []; if state['specs'].get('thickness', 0) < 9: errors.append('Thickness below industrial standard.'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(FoilState); graph.add_node('validate', validate_foil_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()