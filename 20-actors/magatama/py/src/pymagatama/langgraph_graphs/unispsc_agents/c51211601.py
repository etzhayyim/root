from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_certs: List[str]; qc_status: bool; def validate_gmp(state: ProcureState): state['qc_status'] = 'GMP_CERT' in state['vendor_certs']; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()