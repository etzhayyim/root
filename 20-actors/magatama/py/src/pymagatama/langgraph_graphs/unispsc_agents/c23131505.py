from langgraph.graph import StateGraph, END; from typing import TypedDict; class RobotWeldingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: RobotWeldingState): state['valid'] = 'duty_cycle' in state['specs'] and state['specs']['duty_cycle'] > 0; return state; graph = StateGraph(RobotWeldingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()