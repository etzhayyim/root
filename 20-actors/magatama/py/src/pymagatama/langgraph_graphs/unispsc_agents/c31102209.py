from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): material_specs: dict; is_compliant: bool; export_license: str; def validate_composition(state: MoldState): state['is_compliant'] = state['material_specs'].get('purity', 0) > 99.0; return state; def check_export_controls(state: MoldState): state['export_license'] = 'REQUIRED' if state['material_specs'].get('is_dual_use') else 'NOT_REQUIRED'; return state; graph = StateGraph(MoldState); graph.add_node('validate', validate_composition); graph.add_node('export', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); app = graph.compile()