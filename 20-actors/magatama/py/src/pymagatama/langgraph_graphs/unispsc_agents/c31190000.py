from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): abrasive_type: str; grit: int; compliance_checked: bool; def validate_specs(state: SpecState): state['compliance_checked'] = state['grit'] > 0; return state; graph = StateGraph(SpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()