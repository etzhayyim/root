from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CleaningToolState(TypedDict): brush_id: str; specs: dict; validation_passed: bool; def check_specs(state: CleaningToolState): state['validation_passed'] = 'sterility_certification' in state['specs'] and state['specs']['shaft_diameter_mm'] > 0; return state; graph = StateGraph(CleaningToolState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()