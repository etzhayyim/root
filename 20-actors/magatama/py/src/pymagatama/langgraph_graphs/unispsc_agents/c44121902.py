from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): data: dict; errors: Annotated[list, operator.add]; def validate_lead(state: GraphState): val = state['data'].get('lead_diameter_mm'); return {'errors': ['Invalid diameter'] if not val or val <= 0 else []}; def compile_graph(): builder = StateGraph(GraphState); builder.add_node('validate', validate_lead); builder.set_entry_point('validate'); builder.add_edge('validate', END); return builder.compile(); graph = compile_graph()