from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FarmStructureState(TypedDict): structure_type: str; specs: dict; approved: bool; def validate_specs(state: FarmStructureState): is_valid = 'wind_load' in state['specs'] and 'material' in state['specs']; return {'approved': is_valid}; graph = StateGraph(FarmStructureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()