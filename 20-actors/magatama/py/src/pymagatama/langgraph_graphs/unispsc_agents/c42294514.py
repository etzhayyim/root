from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): device_id: str; compliance_checks: dict; status: str; def validate_probe_specs(state: State): state['compliance_checks'] = {'iso_13485': True, 'sterility': True}; state['status'] = 'VALIDATED'; return state; def finalize_procurement(state: State): state['status'] = 'READY_FOR_ORDER'; return state; graph = StateGraph(State); graph.add_node('validate', validate_probe_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()