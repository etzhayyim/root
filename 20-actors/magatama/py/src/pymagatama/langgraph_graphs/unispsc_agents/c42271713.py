from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HyperbaricState(TypedDict): device_id: str; pressure_specs: dict; compliance_docs: List[str]; approved: bool; def validate_specs(state: HyperbaricState): valid = state['pressure_specs'].get('rating', 0) > 0 and 'ASME-PVHO' in state['compliance_docs']; return {'approved': valid}; def finalize_procurement(state: HyperbaricState): return {'approved': True} if state['approved'] else {'approved': False}; graph = StateGraph(HyperbaricState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()