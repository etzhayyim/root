from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): component_specs: dict; validation_results: List[str]; def validate_dimensions(state: State): state['validation_results'].append('DIM_CHECK_PASSED'); return state; def check_material_grade(state: State): state['validation_results'].append('GRADE_VERIFIED'); return state; graph = StateGraph(State); graph.add_node('validate_dim', validate_dimensions); graph.add_node('check_grade', check_material_grade); graph.set_entry_point('validate_dim'); graph.add_edge('validate_dim', 'check_grade'); graph.add_edge('check_grade', END); graph = graph.compile()