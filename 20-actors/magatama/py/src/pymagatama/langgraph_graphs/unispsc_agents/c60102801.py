from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EduMatState(TypedDict): items: List[str]; validated: bool; def validate_blocks(state: EduMatState): return {'validated': len(state['items']) > 0}; graph = StateGraph(EduMatState); graph.add_node('validate', validate_blocks); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()