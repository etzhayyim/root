from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DrawingInkState(TypedDict): ink_type: str; specs: dict; approved: bool; def validate_ink_specs(state: DrawingInkState): is_safe = 'non-toxic' in state['specs'].get('certifications', []); return { 'approved': is_safe }; graph = StateGraph(DrawingInkState); graph.add_node('validate', validate_ink_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()