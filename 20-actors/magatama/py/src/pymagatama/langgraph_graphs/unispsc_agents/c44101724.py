from langgraph.graph import StateGraph, END; from typing import TypedDict; class UpgradeState(TypedDict): model: str; upgrade_kit: str; verified: bool; def validate_compatibility(state: UpgradeState): return {'verified': state['model'].startswith('MFP')} ; graph = StateGraph(UpgradeState); graph.add_node('validate', validate_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()