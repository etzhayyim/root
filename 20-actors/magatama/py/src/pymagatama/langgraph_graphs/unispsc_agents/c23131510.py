from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SheetMetalState(TypedDict): specs: dict; valid: bool; error: List[str]; def validate_specs(state: SheetMetalState): state['valid'] = 'Bending Force Capacity' in state['specs'] and state['specs']['Bending Force Capacity'] > 0; return state; graph = StateGraph(SheetMetalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()