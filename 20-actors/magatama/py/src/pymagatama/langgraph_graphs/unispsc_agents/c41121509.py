from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipetteState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(PipetteState); def validate_spec(state: PipetteState): state['validation_results'].append('Checking material grades'); return state; def check_sterility(state: PipetteState): state['validation_results'].append('Validating ISO 11137 compliance'); return state; graph.add_node('validate', validate_spec); graph.add_node('sterility', check_sterility); graph.set_entry_point('validate'); graph.add_edge('validate', 'sterility'); graph.add_edge('sterility', END); compiled_graph = graph.compile()