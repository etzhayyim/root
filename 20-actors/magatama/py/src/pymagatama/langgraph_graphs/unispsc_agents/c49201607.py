from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PilatesState(TypedDict): vendor_specs: dict; compliance_check: bool; graph = StateGraph(PilatesState); def validate_equipment(state: PilatesState): state['compliance_check'] = 'Certification' in state['vendor_specs']; return state; graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()