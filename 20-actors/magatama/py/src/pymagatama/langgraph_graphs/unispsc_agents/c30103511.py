from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): material_data: dict; validation_report: str; def validate_honeycomb(state: GraphState): val = state['material_data'].get('thickness', 0); return {'validation_report': 'Pass' if val > 0.05 else 'Fail'}; graph = StateGraph(GraphState); graph.add_node('validate', validate_honeycomb); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()