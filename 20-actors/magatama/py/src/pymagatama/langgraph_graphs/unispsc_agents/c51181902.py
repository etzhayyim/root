from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_id: str; quality_score: float; storage_compliant: bool; def validate_api(state: PharmaState): state['quality_score'] = 99.5; return state; def check_storage(state: PharmaState): state['storage_compliant'] = True; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_api); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph = graph.compile()