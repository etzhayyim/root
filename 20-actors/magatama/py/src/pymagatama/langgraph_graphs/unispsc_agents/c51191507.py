from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; gmp_certified: bool; quality_checks: List[str]; def validate_purity(state: PharmaState): return {'quality_checks': state['quality_checks'] + ['purity_verified']}; def check_compliance(state: PharmaState): return {'gmp_certified': True}; workflow = StateGraph(PharmaState); workflow.add_node('verify_gmp', check_compliance); workflow.add_node('test_purity', validate_purity); workflow.set_entry_point('verify_gmp'); workflow.add_edge('verify_gmp', 'test_purity'); workflow.add_edge('test_purity', END); graph = workflow.compile()