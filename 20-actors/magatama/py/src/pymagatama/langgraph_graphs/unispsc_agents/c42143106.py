from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; state: str; def validate_medical_device(state: ProcureState): state['validated'] = True; state['state'] = 'READY'; return state; def check_regulatory_compliance(state: ProcureState): print('Checking ISO 13485...'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_medical_device); graph.add_node('compliance', check_regulatory_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()