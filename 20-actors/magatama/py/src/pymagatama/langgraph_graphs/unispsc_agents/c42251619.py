from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TherapyState(TypedDict): clay_type: str; heat_retention: float; safety_check: bool; def validate_grade(state: TherapyState): return {'safety_check': state['clay_type'] == 'medical-grade'}; def check_thermal(state: TherapyState): return {'safety_check': state['heat_retention'] > 30.0}; graph = StateGraph(TherapyState); graph.add_node('val_grade', validate_grade); graph.add_node('val_thermal', check_thermal); graph.set_entry_point('val_grade'); graph.add_edge('val_grade', 'val_thermal'); graph.add_edge('val_thermal', END); graph = graph.compile()