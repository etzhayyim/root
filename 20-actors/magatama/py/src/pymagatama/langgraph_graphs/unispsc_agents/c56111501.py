from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReceptionState(TypedDict): specs: dict; valid: bool; workflow_log: List[str]; def validate_specs(state: ReceptionState): state['valid'] = 'dimensions' in state['specs'] and 'material' in state['specs']; state['workflow_log'].append('Validation completed'); return state; def approve_order(state: ReceptionState): state['workflow_log'].append('Order approved for procurement'); return state; graph = StateGraph(ReceptionState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()