from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): doc: dict; validation: bool; def validate_content(state: ProcState): state['validation'] = 'financial_literacy' in state['doc'].get('tags', []); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_content); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()