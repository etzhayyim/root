from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LearningState(TypedDict): content_materials: List[str]; validation_results: dict; graph = StateGraph(LearningState); def validate_module(state): return {'validation_results': {'status': 'passed', 'requirements_met': True}}; def format_curriculum(state): return {'content_materials': [m.upper() for m in state['content_materials']]}; graph.add_node('validate', validate_module); graph.add_node('format', format_curriculum); graph.set_entry_point('validate'); graph.add_edge('validate', 'format'); graph.add_edge('format', END); compiled_graph = graph.compile()