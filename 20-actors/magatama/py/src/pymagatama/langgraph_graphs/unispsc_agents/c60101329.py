from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): card_data: dict; approved: bool; def validate_materials(state: ProcureState): state['approved'] = 'safety_cert' in state['card_data']; return state; def check_readability(state: ProcureState): state['approved'] = state.get('approved', False) and state['card_data'].get('readable', True); return state; graph = StateGraph(ProcureState); graph.add_node('validate_materials', validate_materials); graph.add_node('check_readability', check_readability); graph.set_entry_point('validate_materials'); graph.add_edge('validate_materials', 'check_readability'); graph.add_edge('check_readability', END); graph = graph.compile()