from langgraph.graph import StateGraph, END; from typing import TypedDict; class DosingState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: DosingState): state['validation_passed'] = 'Dosing Accuracy Rate' in state['specs']; return state; def route_step(state: DosingState): return 'validate' if not state['validation_passed'] else END; graph = StateGraph(DosingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()