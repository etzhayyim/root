from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoinProcurementState(TypedDict): data: dict; validation_errors: List[str]; approved: bool; def validate_classroom_coins(state: CoinProcurementState): data = state['data']; errors = []; if 'material' not in data: errors.append('Missing material info'); if 'safety_cert' not in data: errors.append('Missing safety certification'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(CoinProcurementState); graph.add_node('validate', validate_classroom_coins); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()