from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): chemical_composition: str; batch_id: str; compliance_check: bool; def validate_purity(state: AgentState): state['compliance_check'] = True; return state; def check_expiration(state: AgentState): return {'compliance_check': True}; graph = StateGraph(AgentState); graph.add_node('validate', validate_purity); graph.add_node('expiry', check_expiration); graph.set_entry_point('validate'); graph.add_edge('validate', 'expiry'); graph.add_edge('expiry', END); app = graph.compile()