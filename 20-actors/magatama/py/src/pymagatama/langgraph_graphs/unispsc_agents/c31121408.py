from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_titanium_spec(state: CastingState): state['validation_results'].append('Verifying ASTM material compliance'); return state; def check_surface_finish(state: CastingState): state['validation_results'].append('Checking Ra surface threshold'); return state; graph = StateGraph(CastingState); graph.add_node('validate_material', validate_titanium_spec); graph.add_node('check_finish', check_surface_finish); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_finish'); graph.add_edge('check_finish', END); app = graph.compile()