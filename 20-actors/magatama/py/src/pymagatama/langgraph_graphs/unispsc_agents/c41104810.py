from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FractionationState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: FractionationState): errors = []; if state['specs'].get('pressure', 0) > 500: errors.append('Pressure exceeds safety threshold.'); return {'validation_errors': errors}; def determine_clearance(state: FractionationState): return 'APPROVED' if not state['validation_errors'] else 'REJECTED'; graph = StateGraph(FractionationState); graph.add_node('validate', validate_specs); graph.add_node('decision', determine_clearance); graph.add_edge('validate', 'decision'); graph.add_edge('decision', END); graph.set_entry_point('validate'); graph = graph.compile()