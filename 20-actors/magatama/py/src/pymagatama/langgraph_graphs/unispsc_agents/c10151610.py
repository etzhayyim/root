from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CommodityState(TypedDict): commodity_id: str; quality_score: float; storage_path: str; logs: List[str]; def node_check_freshness(state: CommodityState): state['logs'].append('Verifying freshness and shelf life'); return {'quality_score': 0.95}; def node_route_logistics(state: CommodityState): state['logs'].append('Routing to cold storage'); return {'storage_path': 'cold_chain_zone_a'}; graph = StateGraph(CommodityState); graph.add_node('check_freshness', node_check_freshness); graph.add_node('route_logistics', node_route_logistics); graph.add_edge('check_freshness', 'route_logistics'); graph.add_edge('route_logistics', END); graph.set_entry_point('check_freshness'); graph = graph.compile()