from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StainerState(TypedDict): vendor_docs: List[str]; compliance_check: bool; stage: str; def validate_docs(state: StainerState): return {'compliance_check': all(doc in state['vendor_docs'] for doc in ['ISO13485', 'CE_IVD'])}; def finalize_procurement(state: StainerState): return {'stage': 'procurement_ready'}; graph = StateGraph(StainerState); graph.add_node('validate', validate_docs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()