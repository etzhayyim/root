from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SteelProcurementState(TypedDict): material_grade: str; dimensions: dict; quality_cert: bool; approved: bool; def validate_material(state): state['approved'] = state['material_grade'] in ['AISI 4140', 'AISI 1018'] and state['quality_cert'] is True; return state; graph = StateGraph(SteelProcurementState); graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()