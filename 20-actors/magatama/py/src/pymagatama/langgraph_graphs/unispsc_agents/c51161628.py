from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): ingredient: str; qc_passed: bool; specs: dict; def validate_quality(state: PharmaState): print(f'Validating purity for {state["ingredient"]}'); return {"qc_passed": state["specs"].get("purity", 0) > 99.0}; def finalize(state: PharmaState): print('Processing pharmaceutical procurement record'); return {"qc_passed": True}; graph = StateGraph(PharmaState); graph.add_node("validate", validate_quality); graph.add_node("finalize", finalize); graph.set_entry_point("validate"); graph.add_edge("validate", "finalize"); graph.add_edge("finalize", END); graph = graph.compile()