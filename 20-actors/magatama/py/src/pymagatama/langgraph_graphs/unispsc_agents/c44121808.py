from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EraserState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: EraserState): state['valid'] = all(k in state['specs'] for k in ['rpm', 'battery']); return state; graph = StateGraph(EraserState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()