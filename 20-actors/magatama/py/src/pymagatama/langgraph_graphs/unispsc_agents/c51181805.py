from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; purity: float; gmp_compliant: bool; def validate_purity(state: PharmaState): return {'purity_valid': state['purity'] >= 99.0}; def check_compliance(state: PharmaState): return {'status': 'approved' if state['gmp_compliant'] else 'rejected'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()