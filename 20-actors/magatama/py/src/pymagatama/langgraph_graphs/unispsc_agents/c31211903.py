from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MaskingState(TypedDict): specs: dict; validation_report: List[str]; def validate_specs(state: MaskingState): criteria = ['heat_resistance', 'solvent_res']; report = [f'Passed {c}' for c in criteria if c in state['specs']]; return {'validation_report': report}; graph = StateGraph(MaskingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()