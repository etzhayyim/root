from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalProcurementState(TypedDict): chemical_code: str; purity: float; safety_verified: bool; inspection_passed: bool; def validate_purity(state: ChemicalProcurementState): return {'purity': state['purity'] if state['purity'] > 0.99 else 0.0}; def check_safety(state: ChemicalProcurementState): return {'safety_verified': True}; graph = StateGraph(ChemicalProcurementState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()