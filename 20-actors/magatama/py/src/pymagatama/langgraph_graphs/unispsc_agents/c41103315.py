from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input_data: dict; validation_status: str; audit_log: List[str]; def validate_instrument(state: ProcState): state['validation_status'] = 'PENDING'; return {'validation_status': 'VALID' if 'model_id' in state['input_data'] else 'INVALID'}; def generate_report(state: ProcState): state['audit_log'].append('Validation completed'); return {'audit_log': state['audit_log']}; graph = StateGraph(ProcState); graph.add_node('validate', validate_instrument); graph.add_node('report', generate_report); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); compile_graph = graph.compile()