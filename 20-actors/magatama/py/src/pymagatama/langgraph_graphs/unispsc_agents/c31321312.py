from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): material_spec: str; dimensions: dict; verified: bool; def validate_specs(state: AssemblyState): state['verified'] = True if state['material_spec'] and state['dimensions'] else False; return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()