from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SolventState(TypedDict): code: str; purity: float; hazard_level: int; approved: bool; def check_purity(state: SolventState): state['approved'] = state['purity'] >= 99.9; return state; def evaluate_hazard(state: SolventState): state['hazard_level'] = 3 if state['purity'] > 99.9 else 2; return state; builder = StateGraph(SolventState); builder.add_node('check_purity', check_purity); builder.add_node('evaluate_hazard', evaluate_hazard); builder.set_entry_point('check_purity'); builder.add_edge('check_purity', 'evaluate_hazard'); builder.add_edge('evaluate_hazard', END); graph = builder.compile()