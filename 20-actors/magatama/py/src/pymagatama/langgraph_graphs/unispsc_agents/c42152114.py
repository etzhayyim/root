from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KitState(TypedDict): kit_id: str; components: List[str]; compliant: bool; def validate_kit(state: KitState): print(f'Validating kit {state["kit_id"]}'); return {'compliant': all(c in ['dispenser', 'tips', 'base'] for c in state['components'])}; graph = StateGraph(KitState); graph.add_node('validate', validate_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()