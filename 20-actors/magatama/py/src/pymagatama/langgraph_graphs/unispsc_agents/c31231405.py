from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShimState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: ShimState): state['validation_passed'] = 'thickness' in state['specs'] and 'material' in state['specs']; return state; def check_compliance(state: ShimState): return 'success' if state['validation_passed'] else 'failure'; graph = StateGraph(ShimState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()