from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BushingState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: BushingState): state['validation_results'].append('Dimensional check passed'); return state; def material_analysis(state: BushingState): state['validation_results'].append('Material spec verified'); return state; graph = StateGraph(BushingState); graph.add_node('validate_dim', validate_dimensions); graph.add_node('check_material', material_analysis); graph.set_entry_point('validate_dim'); graph.add_edge('validate_dim', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()