from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): content: str; validation_logs: Annotated[list, operator.add]; status: str; def validate_purity(state: PharmState): return {'validation_logs': ['Validating chemical purity for Ropivacaine']}; def check_regulatory(state: PharmState): return {'validation_logs': ['Checking FDA/EMA compliance']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('regulatory', check_regulatory); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); compiled = graph.compile()