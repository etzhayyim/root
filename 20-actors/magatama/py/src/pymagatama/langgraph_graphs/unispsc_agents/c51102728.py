from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_name: str; validation_status: bool; alerts: List[str]; def validate_chemical(state: ProcureState): print(f'Validating storage for {state[chemical_name]}'); return {'validation_status': True, 'alerts': ['Check MSDS compatibility']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_chemical); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()