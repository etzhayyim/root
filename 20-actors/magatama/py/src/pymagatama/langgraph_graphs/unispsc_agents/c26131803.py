from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GeneratorState(TypedDict): panel_specs: dict; validation_results: List[str]; def validate_technical_specs(state: GeneratorState): state['validation_results'].append('Verifying IEC protection standards...'); return state; def check_export_compliance(state: GeneratorState): state['validation_results'].append('Checking dual-use export control logs...'); return state; graph = StateGraph(GeneratorState); graph.add_node('tech_spec_check', validate_technical_specs); graph.add_node('export_check', check_export_compliance); graph.set_entry_point('tech_spec_check'); graph.add_edge('tech_spec_check', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()