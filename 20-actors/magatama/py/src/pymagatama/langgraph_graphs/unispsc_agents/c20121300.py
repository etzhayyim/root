from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): commodity_code: str; spec_data: dict; validation_errors: Annotated[Sequence[str], operator.add]; approval_status: bool; def validate_spec(state: ProcureState): errors = []; if 'torque_rating_nm' not in state['spec_data']: errors.append('Missing torque rating'); return {'validation_errors': errors}; def check_approval(state: ProcureState): return {'approval_status': len(state['validation_errors']) == 0}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.add_node('approve', check_approval); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()