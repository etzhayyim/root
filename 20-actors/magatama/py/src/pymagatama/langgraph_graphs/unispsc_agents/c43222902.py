from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): input_data: dict; validation_errors: Annotated[list, operator.add]; status: str; def validate_specs(state: GraphState): errors = []; r = state['input_data']; if r.get('dew_point', 0) > -40: errors.append('Dew point must be <= -40C'); return {'validation_errors': errors}; def process_dryer(state: GraphState): return {'status': 'Validated'}; graph = StateGraph(GraphState); graph.add_node('validate', validate_specs); graph.add_node('process', process_dryer); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); app = graph.compile()