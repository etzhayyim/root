from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class RescueHeliState(TypedDict): specs: dict; compliance_status: bool; def validate_specs(state: RescueHeliState): state['compliance_status'] = 'certification' in state['specs'] and 'payload' in state['specs']; return state; def check_export_control(state: RescueHeliState): return state; workflow = StateGraph(RescueHeliState); workflow.add_node('validate', validate_specs); workflow.add_node('export_check', check_export_control); workflow.add_edge('validate', 'export_check'); workflow.add_edge('export_check', END); workflow.set_entry_point('validate'); graph = workflow.compile()