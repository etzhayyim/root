from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity: float; quality_passed: bool; def validate_spec(state: PharmaState): state['quality_passed'] = state['purity'] >= 99.0; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()