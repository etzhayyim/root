from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RobotState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: RobotState): errors = []; if state['specs'].get('payload', 0) <= 0: errors.append('Invalid payload'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()