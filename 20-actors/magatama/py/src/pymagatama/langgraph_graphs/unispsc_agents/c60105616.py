from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): content: dict; status: str; def validate_materials(state: State): return {'status': 'validated' if 'reagents' in state['content'] else 'incomplete'}; def check_safety(state: State): return {'status': 'safe' if state['content'].get('safety_certified') else 'flagged'}; graph = StateGraph(State); graph.add_node('validate', validate_materials); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()