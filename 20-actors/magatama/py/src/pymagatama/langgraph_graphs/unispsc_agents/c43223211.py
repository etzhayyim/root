from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ControllerState(TypedDict): controller_model: str; specs: dict; valid: bool; def validate_specs(state: ControllerState): state['valid'] = all(k in state['specs'] for k in ['voltage', 'protocol']); return state; graph = StateGraph(ControllerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()