from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InsulationState(TypedDict): specs: dict; approved: bool; def validate_specs(state: InsulationState): state['approved'] = state['specs'].get('temp_limit', 0) > 500; return state; graph = StateGraph(InsulationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()