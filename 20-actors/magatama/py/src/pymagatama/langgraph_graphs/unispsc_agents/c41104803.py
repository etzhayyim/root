from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EvaporatorState(TypedDict): specs: dict; validation_passed: bool; logs: List[str]; def validate_specs(state: EvaporatorState): specs = state['specs']; passed = all(key in specs for key in ['temp_range', 'vacuum_rating']); return {'validation_passed': passed, 'logs': ['Specs verified'] if passed else ['Missing technical parameters']}; graph = StateGraph(EvaporatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()