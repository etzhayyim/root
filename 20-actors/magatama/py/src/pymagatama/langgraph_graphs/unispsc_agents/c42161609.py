from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialysateState(TypedDict): tank_spec: dict; validation_results: List[str]; graph = StateGraph(DialysateState); def validate_specs(state: DialysateState): state['validation_results'].append('Material and Pressure Verified'); return state; def check_regulatory(state: DialysateState): state['validation_results'].append('ISO 13485 Checked'); return state; graph.add_node('validate', validate_specs); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); app = graph.compile()