from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): cold_chain_verified: bool; quality_control_passed: bool; document_audit: List[str]; def verify_cold_chain(state: ProcureState): return {'cold_chain_verified': True}; def validate_quality(state: ProcureState): return {'quality_control_passed': True}; graph = StateGraph(ProcureState); graph.add_node('verify', verify_cold_chain); graph.add_node('qc', validate_quality); graph.add_edge('verify', 'qc'); graph.add_edge('qc', END); graph.set_entry_point('verify'); graph = graph.compile()