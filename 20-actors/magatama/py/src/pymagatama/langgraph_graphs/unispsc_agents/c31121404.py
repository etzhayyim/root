from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CastState(TypedDict): input_specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_dimensions(state: CastState): is_valid = state['input_specs'].get('tolerance') <= 0.005; return {'validation_results': ['Dimension Check Pass'] if is_valid else ['Dimension Check Fail']}; def check_material(state: CastState): return {'validation_results': ['Material Traceability Verified']}; workflow = StateGraph(CastState); workflow.add_node('validate', validate_dimensions); workflow.add_node('material', check_material); workflow.set_entry_point('validate'); workflow.add_edge('validate', 'material'); workflow.add_edge('material', END); graph = workflow.compile()