from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DiversityState(TypedDict): content_plan: str; compliance_check: bool; final_output: str; def validate_content(state: DiversityState) -> DiversityState: state['compliance_check'] = True; return state; def finalize_delivery(state: DiversityState) -> DiversityState: state['final_output'] = 'Validated Diversity Resource Package'; return state; builder = StateGraph(DiversityState); builder.add_node('validate', validate_content); builder.add_node('finalize', finalize_delivery); builder.add_edge('validate', 'finalize'); builder.add_edge('finalize', END); builder.set_entry_point('validate'); graph = builder.compile()