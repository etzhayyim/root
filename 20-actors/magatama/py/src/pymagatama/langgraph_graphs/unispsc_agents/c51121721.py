from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(PharmaState); def validate_batch(state: PharmaState): return {'validation_results': {'gmp_compliant': True}}; def check_expiry(state: PharmaState): return {'validation_results': {'expired': False}}; graph.add_node('validate', validate_batch); graph.add_node('expiry_check', check_expiry); graph.set_entry_point('validate'); graph.add_edge('validate', 'expiry_check'); graph.add_edge('expiry_check', END); graph = graph.compile()