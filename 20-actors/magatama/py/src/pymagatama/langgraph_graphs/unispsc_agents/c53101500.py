from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ApparelState(TypedDict): items: List[dict]; validation_results: List[str]; def validate_garment_specs(state: ApparelState): results = []; for item in state['items']: if 'material' not in item: results.append(f'Missing material for {item.get('id')}'); state['validation_results'] = results; return state; graph = StateGraph(ApparelState); graph.add_node('validate', validate_garment_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()