from langgraph.graph import StateGraph, END; from typing import TypedDict; class ApparelState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ApparelState): state['valid'] = 'fabric' in state['specs'] and 'sizing' in state['specs']; return state; graph = StateGraph(ApparelState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()