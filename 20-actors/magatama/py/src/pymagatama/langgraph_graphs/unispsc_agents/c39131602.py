from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SleevingState(TypedDict): specs: dict; validation_passed: bool; error_log: List[str]; def validate_specs(state: SleevingState): state['validation_passed'] = 'UL94' in state['specs'] and 'expansion_ratio' in state['specs']; return state; builder = StateGraph(SleevingState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()