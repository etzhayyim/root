from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validation_results: dict; def validate_purity(state: ProcurementState): state['validation_results'] = {'purity_check': 'passed'}; return state; def inspect_dimensions(state: ProcurementState): state['validation_results']['dimension_check'] = 'verified'; return state; graph = StateGraph(ProcurementState); graph.add_node('purity', validate_purity); graph.add_node('dimensions', inspect_dimensions); graph.add_edge('purity', 'dimensions'); graph.add_edge('dimensions', END); graph.set_entry_point('purity'); graph = graph.compile()