from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class KneePadState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; def validate_materials(state: KneePadState): return {'validation_results': ['Material test passed'] if 'material' in state['specs'] else ['Material test failed']}; def validate_safety(state: KneePadState): return {'validation_results': ['Safety rating certified'] if 'rating' in state['specs'] else ['Safety rating missing']}; graph = StateGraph(KneePadState); graph.add_node('material_check', validate_materials); graph.add_node('safety_check', validate_safety); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()