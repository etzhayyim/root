from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): device_id: str; safety_checks: List[str]; approved: bool; def validate_safety(state: ProcureState): state['safety_checks'].append('Compliance Verified'); state['approved'] = True; return state; graph = StateGraph(ProcureState); graph.add_node('safety_check', validate_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph.compile()