from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SeawallState(TypedDict): site_data: dict; validation_results: List[str]; def validate_design(state: SeawallState): results = []; if 'geotechnical_report' not in state['site_data']: results.append('Missing geotechnical data'); return {'validation_results': results}; def construction_workflow(state: SeawallState): return {'validation_results': state['validation_results'] + ['Workflow initialized']}; graph = StateGraph(SeawallState); graph.add_node('validate', validate_design); graph.add_node('workflow', construction_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'workflow'); graph.add_edge('workflow', END); compiled_graph = graph.compile()