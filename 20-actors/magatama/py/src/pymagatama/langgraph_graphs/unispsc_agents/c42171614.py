from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RescueEquipmentState(TypedDict): equipment_type: str; specs: dict; valid: bool; def validate_specs(state: RescueEquipmentState): state['valid'] = all(k in state['specs'] for k in ['buoyancy', 'material']); return state; graph = StateGraph(RescueEquipmentState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()