from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FirearmState(TypedDict): serial_number: str; permits: List[str]; inspection_passed: bool; def validate_firearm(state: FirearmState): state['inspection_passed'] = len(state['permits']) > 0 and state['serial_number'] != ''; return state; graph = StateGraph(FirearmState); graph.add_node('validate', validate_firearm); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()