from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): data: dict; status: str; def validate_specs(state: GraphState): state['status'] = 'validated' if 'calibration_std' in state['data'] else 'missing_info'; return state; def export_compliance_check(state: GraphState): state['status'] = 'export_cleared' if state.get('is_dual_use', False) else 'standard'; return state; graph = StateGraph(GraphState); graph.add_node('validate', validate_specs); graph.add_node('compliance', export_compliance_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compile_graph = graph.compile()