from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProductState(TypedDict): items: List[str]; safety_verified: bool; def validate_safety(state: ProductState): state['safety_verified'] = True; return state; graph = StateGraph(ProductState); graph.add_node('validate', validate_safety); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()