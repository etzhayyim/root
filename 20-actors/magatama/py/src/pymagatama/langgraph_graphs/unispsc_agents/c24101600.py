from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LiftingSpecState(TypedDict): equipment_type: str; load_capacity: float; safety_docs: List[str]; approved: bool; def validate_lifting_spec(state: LiftingSpecState): return {'approved': state['load_capacity'] > 0 and len(state['safety_docs']) > 0}; graph = StateGraph(LiftingSpecState); graph.add_node('validate', validate_lifting_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()