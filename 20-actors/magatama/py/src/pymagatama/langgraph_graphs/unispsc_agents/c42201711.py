from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UltrasoundState(TypedDict): transducer_specs: dict; compliance_verified: bool; def validate_specs(state: UltrasoundState): state['compliance_verified'] = 'frequency_range_mhz' in state['transducer_specs']; return state; def check_regulations(state: UltrasoundState): print('Checking FDA/CE status'); return state; graph = StateGraph(UltrasoundState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()