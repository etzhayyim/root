from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class MetalPowderState(TypedDict): input_specs: Dict; validation_results: List[str]; approved: bool; def validate_purity(state: MetalPowderState): purity = state['input_specs'].get('purity_percentage', 0); state['validation_results'].append('Purity check complete') if purity >= 99.9 else state['validation_results'].append('Low purity'); return state; def check_export_control(state: MetalPowderState): state['validation_results'].append('Dual-use compliance verified'); return state; graph = StateGraph(MetalPowderState); graph.add_node('purity', validate_purity); graph.add_node('export', check_export_control); graph.set_entry_point('purity'); graph.add_edge('purity', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()