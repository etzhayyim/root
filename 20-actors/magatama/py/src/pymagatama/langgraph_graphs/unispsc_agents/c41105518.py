from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RNAState(TypedDict): input_spec: dict; quality_check: bool; final_report: str; def validate_specs(state: RNAState): state['quality_check'] = 'RNase_Free_Certification' in state['input_spec']; return state; def generate_report(state: RNAState): state['final_report'] = 'Validation Success' if state['quality_check'] else 'Validation Failed'; return state; graph = StateGraph(RNAState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()