from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class OrderState(TypedDict): content: str; validation_results: Annotated[list, operator.add]; def validate_spec(state: OrderState): print('Validating POP sign dimensions and material...'); return {'validation_results': ['Dimension Check Passed', 'Fire Safety Verified']}; def finalize_order(state: OrderState): print('Finalizing procurement order...'); return {'content': 'Order Confirmed'}; graph = StateGraph(OrderState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()