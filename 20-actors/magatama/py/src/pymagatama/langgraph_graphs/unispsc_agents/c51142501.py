from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): chemical_data: dict; validation_status: bool; def check_purity(state: State): state['validation_status'] = state['chemical_data'].get('purity', 0) >= 99.0; return state; def regulatory_check(state: State): state['validation_status'] = state['validation_status'] and 'gmp_cert' in state['chemical_data']; return state; graph = StateGraph(State); graph.add_node('purity_check', check_purity); graph.add_node('regulatory_check', regulatory_check); graph.add_edge('purity_check', 'regulatory_check'); graph.add_edge('regulatory_check', END); graph.set_entry_point('purity_check'); compliled_graph = graph.compile()