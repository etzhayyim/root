from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; alloy: str; specs: dict; verified: bool; def validate_specs(state: ComponentState): cert = state.get('specs', {}).get('mill_test_report'); state['verified'] = bool(cert); return state; def route_verification(state: ComponentState): return 'valid' if state['verified'] else 'failed'; graph = StateGraph(ComponentState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()