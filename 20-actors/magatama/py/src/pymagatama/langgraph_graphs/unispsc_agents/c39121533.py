from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SwitchState(TypedDict): specs: dict; valid: bool; def check_specs(state: SwitchState): state['valid'] = all(k in state['specs'] for k in ['voltage', 'current']); return state; graph = StateGraph(SwitchState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()