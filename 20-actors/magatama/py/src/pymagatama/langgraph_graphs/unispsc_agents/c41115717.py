from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GCState(TypedDict): hardware_specs: dict; validation_report: List[str]; def validate_fittings(state: GCState): specs = state['hardware_specs']; results = []; if specs.get('pressure_rating', 0) < 500: results.append('Pressure rating insufficient for high-end GC.'); state['validation_report'] = results; return state; graph = StateGraph(GCState); graph.add_node('validate', validate_fittings); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()