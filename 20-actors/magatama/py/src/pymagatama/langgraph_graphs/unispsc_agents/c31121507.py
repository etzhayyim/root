from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): model_path: str; quality_score: float; compliance_flags: List[str]; def validate_cad(state: CastingState): return {'compliance_flags': ['CAD_VALIDATED']}; def inspect_material(state: CastingState): return {'quality_score': 0.95}; graph = StateGraph(CastingState); graph.add_node('validate', validate_cad); graph.add_node('inspect', inspect_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()