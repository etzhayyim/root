from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validation_results: dict; def validate_suture_spec(state: ProcurementState): state['validation_results'] = {'sterility': True, 'compliance': 'ISO-13485'}; return state; def check_expiration(state: ProcurementState): return {'validation_results': {**state['validation_results'], 'expired': False}}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_suture_spec); graph.add_node('check_expiry', check_expiration); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_expiry'); graph.add_edge('check_expiry', END); graph = graph.compile()