from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DocosanolState(TypedDict): chemical_purity: float; gmp_compliant: bool; supplier_verified: bool; def check_purity(state: DocosanolState): return {'chemical_purity': 99.9} if state['chemical_purity'] < 0.99 else state; def check_gmp(state: DocosanolState): return {'gmp_compliant': True}; graph = StateGraph(DocosanolState); graph.add_node('verify_purity', check_purity); graph.add_node('verify_gmp', check_gmp); graph.set_entry_point('verify_purity'); graph.add_edge('verify_purity', 'verify_gmp'); graph.add_edge('verify_gmp', END); compiled_graph = graph.compile()