from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SwitchState(TypedDict): spec: dict; status: str; def validate_specs(state: SwitchState): return {'status': 'validated'} if 'IP_Rating' in state['spec'] else {'status': 'failed'}; def approve_procurement(state: SwitchState): return {'status': 'approved'}; graph = StateGraph(SwitchState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()