from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GasketState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: GasketState): specs = state['specs']; errors = []; if 'pressure_rating' not in specs: errors.append('Missing pressure rating'); return {'valid': len(errors) == 0, 'error_log': errors}; graph = StateGraph(GasketState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()