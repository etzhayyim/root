from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_name: str; purity: float; compliance_docs: List[str]; approved: bool; def validate_compliance(state: ProcureState): state['approved'] = state['purity'] >= 99.0 and len(state['compliance_docs']) >= 2; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()