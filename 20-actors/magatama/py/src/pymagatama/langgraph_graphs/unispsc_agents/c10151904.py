from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MiningState(TypedDict): commodity_code: str; spec_validation: bool; risk_assessment: list[str]; def validate_component(state: MiningState): state['spec_validation'] = True; return state; def assess_risk(state: MiningState): state['risk_assessment'] = ['dual-use-export-control', 'high-value']; return state; graph = StateGraph(MiningState); graph.add_node('validate', validate_component); graph.add_node('risk', assess_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'risk'); graph.add_edge('risk', END); compiled_graph = graph.compile()