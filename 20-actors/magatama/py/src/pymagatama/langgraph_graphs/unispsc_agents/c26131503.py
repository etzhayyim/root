from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class HydroPlantState(TypedDict): vendor_certs: List[str]; site_assessment: dict; approved: bool; def validate_specs(state: HydroPlantState): return { "approved": "ISO9001" in state["vendor_certs"] and "environmental_clearance" in state["site_assessment"] }; graph = StateGraph(HydroPlantState); graph.add_node("validate", validate_specs); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()