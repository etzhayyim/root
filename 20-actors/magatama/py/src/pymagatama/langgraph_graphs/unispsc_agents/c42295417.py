from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): model: str; sterility_verified: bool; compliance_checked: bool; def check_compatibility(state: SupplyState): state['compliance_checked'] = state['model'] is not None; return state; def verify_sterility(state: SupplyState): state['sterility_verified'] = True; return state; graph = StateGraph(SupplyState); graph.add_node('check_comp', check_compatibility); graph.add_node('check_sterility', verify_sterility); graph.set_entry_point('check_comp'); graph.add_edge('check_comp', 'check_sterility'); graph.add_edge('check_sterility', END); graph = graph.compile()