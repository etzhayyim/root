from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SupplyState(TypedDict): items: list; status: str; def validate_supplies(state: SupplyState): state['status'] = 'validation_complete'; return state; def check_hazmat(state: SupplyState): state['status'] = 'hazmat_verified'; return state; graph = StateGraph(SupplyState); graph.add_node('validate', validate_supplies); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()