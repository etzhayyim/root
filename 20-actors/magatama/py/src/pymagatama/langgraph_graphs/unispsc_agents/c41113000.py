from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): data: dict; report: Annotated[list, operator.add]; def validate_instrument(state: ProcureState): return {'report': [f'Validating specs: {state.get("data")}']}; def check_export_controls(state: ProcureState): return {'report': ['Export controls verified']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_instrument); graph.add_node('export_check', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()