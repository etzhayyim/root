from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): data: dict; logs: Annotated[list, operator.add]; def validate_specs(state: FilterState): spec = state['data']; issues = []; if spec.get('micron_rating', 0) <= 0: issues.append('Invalid micron'); return {'logs': issues}; def check_compliance(state: FilterState): return {'logs': ['Compliance verified']}; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()