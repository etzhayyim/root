from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GriddleState(TypedDict): spec: dict; status: str; def validate_griddle(state: GriddleState): status = 'PASS' if state['spec'].get('heating_capacity_watts', 0) > 500 else 'FAIL'; return {'status': status}; graph = StateGraph(GriddleState); graph.add_node('validate', validate_griddle); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()