from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): medicine: str; compliance_checked: bool; supply_chain_verified: bool; def check_regulatory(state: ProcureState): return {'compliance_checked': True}; def verify_logistics(state: ProcureState): return {'supply_chain_verified': True}; graph = StateGraph(ProcureState); graph.add_node('regulatory', check_regulatory); graph.add_node('logistics', verify_logistics); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'logistics'); graph.add_edge('logistics', END); compiled_graph = graph.compile()