from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PortraitState(TypedDict): vendor_selection: str; art_specs: dict; approval_status: bool; graph = StateGraph(PortraitState); def validate_specs(state: PortraitState): return {'approval_status': True if state['art_specs'].get('dimensions') else False}; def finalize_contract(state: PortraitState): return {'vendor_selection': 'CONFIRMED'}; graph.add_node('validate', validate_specs); graph.add_node('contract', finalize_contract); graph.set_entry_point('validate'); graph.add_edge('validate', 'contract'); graph.add_edge('contract', END); compiled_graph = graph.compile()