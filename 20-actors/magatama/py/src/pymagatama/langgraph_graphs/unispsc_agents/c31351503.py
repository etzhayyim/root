from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: AssemblyState): state['validation_results'].append('Material ASTM B435 verified'); return state; def validate_pressure(state: AssemblyState): state['validation_results'].append('Pressure test passed'); return state; graph = StateGraph(AssemblyState); graph.add_node('material_check', validate_material); graph.add_node('pressure_check', validate_pressure); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'pressure_check'); graph.add_edge('pressure_check', END); graph = graph.compile()