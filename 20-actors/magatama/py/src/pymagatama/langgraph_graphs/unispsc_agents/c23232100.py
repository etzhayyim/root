from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: RobotState): specs = state.get('specs', {}); errors = []; if specs.get('payload', 0) > 1000: errors.append('Payload exceeds safe limit'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()