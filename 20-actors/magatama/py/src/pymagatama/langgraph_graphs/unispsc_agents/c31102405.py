from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: CastingState): state['validation_results'].append('Material verified against ISO/ASTM.'); return state; def validate_dimensions(state: CastingState): state['validation_results'].append('Tolerances checked via CMM data.'); return state; graph = StateGraph(CastingState); graph.add_node('material', validate_material); graph.add_node('dimensions', validate_dimensions); graph.set_entry_point('material'); graph.add_edge('material', 'dimensions'); graph.add_edge('dimensions', END); graph = graph.compile()