from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class BlastState(TypedDict): code: str; safety_checks: List[str]; status: str; def validate_safety(state: BlastState) -> BlastState: state['safety_checks'].append('hazard_class_verified'); state['status'] = 'SAFETY_CHECKED'; return state; def approve_logistics(state: BlastState) -> BlastState: state['status'] = 'LOGISTICS_APPROVED'; return state; graph = StateGraph(BlastState); graph.add_node('safety', validate_safety); graph.add_node('logistics', approve_logistics); graph.add_edge('safety', 'logistics'); graph.add_edge('logistics', END); graph.set_entry_point('safety'); graph = graph.compile()