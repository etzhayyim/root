from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): medicine_code: str; verification_status: bool; def validate_product(state: ProcureState): return {'verification_status': state['medicine_code'] == '51141715'}; def check_compliance(state: ProcureState): return {'verification_status': True}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_product); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.compile()