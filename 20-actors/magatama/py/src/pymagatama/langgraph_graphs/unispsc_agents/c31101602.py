from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: CastingState): grade = state['specs'].get('grade'); return {'validation_results': ['Material check passed'] if grade else ['Missing grade']}; def safety_audit(state: CastingState): return {'validation_results': state['validation_results'] + ['Export control audit complete']}; graph = StateGraph(CastingState); graph.add_node('material_check', validate_material); graph.add_node('safety_audit', safety_audit); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'safety_audit'); graph.add_edge('safety_audit', END); graph = graph.compile()