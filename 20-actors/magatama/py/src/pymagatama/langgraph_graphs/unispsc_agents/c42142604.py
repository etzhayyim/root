from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): syringe_id: str; compliance: bool; status: str; def validate_spec(state: State): return {'compliance': True, 'status': 'validated'} if state.get('syringe_id') else {'compliance': False, 'status': 'error'}; def check_regulatory(state: State): return {'status': 'regulatory_check_passed'}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); app = graph.compile()