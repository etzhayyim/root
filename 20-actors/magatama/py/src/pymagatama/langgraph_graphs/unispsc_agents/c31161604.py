from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoltState(TypedDict): model_number: str; specs: dict; verified: bool; def validate_specs(state: BoltState): return {'verified': state['specs'].get('tensile_strength', 0) > 400}; def route_task(state: BoltState): return 'end' if state['verified'] else 'end'; graph = StateGraph(BoltState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()