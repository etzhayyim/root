from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlateState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: PlateState): state['validation_results'].append('Material grade confirmed'); return state; def inspect_welds(state: PlateState): state['validation_results'].append('UV weld integrity validated'); return state; graph = StateGraph(PlateState); graph.add_node('material_check', validate_materials); graph.add_node('weld_inspection', inspect_welds); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'weld_inspection'); graph.add_edge('weld_inspection', END); graph = graph.compile()