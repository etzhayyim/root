from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: ProcState): state['compliant'] = 'ISO_13485_Certification' in state['specs']; return state; def check_safety(state: ProcState): return 'compliant' if state['compliant'] else 'reject'; graph = StateGraph(ProcState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()