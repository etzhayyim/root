from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GlobeProcessState(TypedDict): input_data: dict; validation_report: list; status: str; def validate_specs(state: GlobeProcessState) -> GlobeProcessState: state['validation_report'] = ['Voltage check', 'Content update frequency check']; state['status'] = 'validation_complete'; return state; def finalize_order(state: GlobeProcessState) -> GlobeProcessState: state['status'] = 'ready_for_procurement'; return state; graph = StateGraph(GlobeProcessState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()