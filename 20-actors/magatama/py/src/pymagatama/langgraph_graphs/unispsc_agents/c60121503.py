from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MarkerState(TypedDict): vendor: str; wash_test_passed: bool; toxic_free: bool; graph = StateGraph(MarkerState); def validate_safety(state: MarkerState): return {'toxic_free': True}; def validate_quality(state: MarkerState): return {'wash_test_passed': True}; graph.add_node('safety_check', validate_safety); graph.add_node('quality_check', validate_quality); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'quality_check'); graph.add_edge('quality_check', END); graph = graph.compile()