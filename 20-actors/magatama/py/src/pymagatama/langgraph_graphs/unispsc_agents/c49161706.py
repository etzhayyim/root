from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HurdlesState(TypedDict): specs: dict; compliance: bool; approved: bool; def check_standards(state: HurdlesState) -> HurdlesState: state['compliance'] = 'standard_code' in state['specs']; return state; def final_approval(state: HurdlesState) -> HurdlesState: state['approved'] = state['compliance']; return state; graph = StateGraph(HurdlesState); graph.add_node('validate', check_standards); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.compile()