from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SalicylicState(TypedDict): content: str; validated: bool; analysis: str; def validate_purity(state: SalicylicState): state['validated'] = True if '99%' in state['content'] else False; return state; def report_analysis(state: SalicylicState): state['analysis'] = 'Purity Verified' if state['validated'] else 'Purity Mismatch'; return state; graph = StateGraph(SalicylicState); graph.add_node('validate', validate_purity); graph.add_node('report', report_analysis); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()