from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): chemical_name: str; specifications: dict; approval_status: bool; def validate_purity(state: ProcurementState): state['approval_status'] = state['specifications'].get('purity', 0) >= 98.0; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()