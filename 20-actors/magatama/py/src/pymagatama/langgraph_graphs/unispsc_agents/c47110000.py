from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaundryState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: LaundryState): return {'compliance': state['specs'].get('safety_cert') == 'ISO_CE'}; def update_status(state: LaundryState): return {'status': 'Approved' if state['compliance'] else 'Rejected'}; graph = StateGraph(LaundryState); graph.add_node('validate', validate_specs); graph.add_node('process', update_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()