from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; valid: bool;  def validate_specs(state: CompressorState): specs = state['specs']; state['valid'] = 'motor_power_kw' in specs and specs['motor_power_kw'] > 0; return state; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()