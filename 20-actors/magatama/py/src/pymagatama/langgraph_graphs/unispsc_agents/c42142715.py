from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KitState(TypedDict): kit_type: str; sterility_verified: bool; compliance_docs: List[str]; graph = StateGraph(KitState); def validate_sterility(state: KitState): return { 'sterility_verified': True } if 'ISO-11135' in state.get('compliance_docs', []) else { 'sterility_verified': False }; def check_compliance(state: KitState): return 'approved' if state['sterility_verified'] else 'rejected'; graph.add_node('validate', validate_sterility); graph.add_node('check', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); compiled_graph = graph.compile()