from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): vendor: str; model: str; compliance: bool; log: List[str]; def validate_compliance(state: NetworkState): state['compliance'] = True; state['log'].append('Validation completed'); return state; def provision_software(state: NetworkState): state['log'].append('Provisioning optical management module'); return state; graph = StateGraph(NetworkState); graph.add_node('validate', validate_compliance); graph.add_node('provision', provision_software); graph.set_entry_point('validate'); graph.add_edge('validate', 'provision'); graph.add_edge('provision', END); graph = graph.compile()