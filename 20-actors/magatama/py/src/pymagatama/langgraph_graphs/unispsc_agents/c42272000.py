from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IntubationState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_grade(state: IntubationState): state['validated'] = all('ISO' in c for c in state['certs']); return state; graph = StateGraph(IntubationState); graph.add_node('validate', validate_medical_grade); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()