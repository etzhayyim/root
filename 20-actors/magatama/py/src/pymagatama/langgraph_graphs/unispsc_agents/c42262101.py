from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MortuaryState(TypedDict): outfit_specs: dict; compliance_check: bool; final_approval: bool; def validate_specs(state: MortuaryState): state['compliance_check'] = 'material' in state['outfit_specs'] and 'size' in state['outfit_specs']; return state; def finalize_order(state: MortuaryState): state['final_approval'] = state['compliance_check']; return state; graph = StateGraph(MortuaryState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()