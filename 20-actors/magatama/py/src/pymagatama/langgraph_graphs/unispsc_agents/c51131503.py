from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): chemical_grade: str; safety_check: bool; graph = StateGraph(ProcureState); def validate_purity(state: ProcureState): return {'safety_check': True} if state['chemical_grade'] == 'Industrial' else {'safety_check': False}; graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()