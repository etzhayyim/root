from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class RobotPartState(TypedDict): part_id: str; specs: dict; validation_status: str; graph = StateGraph(RobotPartState); def validate_specs(state: RobotPartState): return {'validation_status': 'verified' if 'torque_rating_nm' in state['specs'] else 'failed'}; def assemble_workflow(state: RobotPartState): return {'validation_status': 'assembled'}; graph.add_node('validate', validate_specs); graph.add_node('assemble', assemble_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph.compile()