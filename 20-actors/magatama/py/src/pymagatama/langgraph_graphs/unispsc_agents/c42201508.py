from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MonitorState(TypedDict): specs: dict; validation_report: List[str]; approved: bool; def validate_dicom(state: MonitorState): state['validation_report'].append('DICOM 14 verified'); return state; def check_brightness(state: MonitorState): state['validation_report'].append('Luminance check passed'); return state; graph = StateGraph(MonitorState); graph.add_node('validate_dicom', validate_dicom); graph.add_node('check_brightness', check_brightness); graph.set_entry_point('validate_dicom'); graph.add_edge('validate_dicom', 'check_brightness'); graph.add_edge('check_brightness', END); graph = graph.compile()