from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; components: List[str]; validation_status: bool; def validate_components(state: KitState): state['validation_status'] = all(c != '' for c in state['components']); return state; def check_expiration(state: KitState): return 'valid' if state['validation_status'] else 'flag_expired'; graph = StateGraph(KitState); graph.add_node('validate', validate_components); graph.add_edge('validate', END); graph.set_entry_point('validate'); app = graph.compile()