from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ChartState(TypedDict): items: list[str]; validation_results: Annotated[list[str], operator.add]; def validate_medical_grade(state: ChartState): return {'validation_results': ['Material check passed', 'Ergonomic check passed']}; def finalize_procurement(state: ChartState): return {'validation_results': ['Order complete']}; graph = StateGraph(ChartState); graph.add_node('validate', validate_medical_grade); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()