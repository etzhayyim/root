from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medicine: str; status: str; docs: List[str]; def validate_batch(state: PharmaState): return {'status': 'validated' if 'COA' in state['docs'] else 'invalid'}; def compliance_check(state: PharmaState): return {'status': 'compliant' if state['status'] == 'validated' else 'hold'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_batch); graph.add_node('compliance', compliance_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()