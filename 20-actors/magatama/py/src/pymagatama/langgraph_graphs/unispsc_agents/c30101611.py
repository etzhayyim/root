from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BronzeOrder(TypedDict): dimensions: dict; quality_certs: List[str]; inspection_status: str; def validate_specs(state: BronzeOrder): return {'inspection_status': 'passed' if 'mill_report' in state['quality_certs'] else 'failed'}; def process_order(state: BronzeOrder): return {'inspection_status': 'processed'}; graph = StateGraph(BronzeOrder); graph.add_node('validate', validate_specs); graph.add_node('process', process_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compile_graph = graph.compile()