from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProduceState(TypedDict): origin: str; quality_score: float; approved: bool; def check_quality(state: ProduceState): state['approved'] = state['quality_score'] > 85; return state; graph = StateGraph(ProduceState); graph.add_node('quality_check', check_quality); graph.set_entry_point('quality_check'); graph.add_edge('quality_check', END); app = graph.compile()