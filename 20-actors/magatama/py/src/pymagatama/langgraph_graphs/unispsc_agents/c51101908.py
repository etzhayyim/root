from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): medicine_name: str; purity_check: bool; storage_validated: bool; def check_purity(state: PharmaState): state['purity_check'] = True; return state; def validate_storage(state: PharmaState): state['storage_validated'] = True; return state; graph = StateGraph(PharmaState); graph.add_node('purity', check_purity); graph.add_node('storage', validate_storage); graph.set_entry_point('purity'); graph.add_edge('purity', 'storage'); graph.add_edge('storage', END); graph = graph.compile()