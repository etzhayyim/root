from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RubberState(TypedDict): specs: dict; compliance_check: bool; def validate_specs(state: RubberState): hardness = state['specs'].get('hardness', 0); state['compliance_check'] = 50 <= hardness <= 90; return state; graph = StateGraph(RubberState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()