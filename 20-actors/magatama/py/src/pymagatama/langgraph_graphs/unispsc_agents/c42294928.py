from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): probe_specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_medical_grade(state: State): state['compliance_status'] = 'ISO13485' in state['probe_specs'].get('certifications', []); return state; def check_compatibility(state: State): state['workflow_log'].append('Checking endoscopic system compatibility'); return state; graph = StateGraph(State); graph.add_node('validate', validate_medical_grade); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()