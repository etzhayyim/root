from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PointerState(TypedDict): model_number: str; laser_class: str; is_compliant: bool; def check_compliance(state: PointerState): state['is_compliant'] = state['laser_class'] in ['Class 1', 'Class 2']; return state; def finalize_order(state: PointerState): return {'status': 'approved' if state['is_compliant'] else 'rejected'}; graph = StateGraph(PointerState); graph.add_node('check', check_compliance); graph.add_node('finalize', finalize_order); graph.set_entry_point('check'); graph.add_edge('check', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()