from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): medicine_name: str; validation_passed: bool; compliance_docs: List[str]; def validate_api(state: PharmaState): state['validation_passed'] = 'API_Standard' in state['compliance_docs']; return state; def check_storage(state: PharmaState): print(f'Checking cold chain for {state["medicine_name"]}'); return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_api); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); app = graph.compile()