from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PumpState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(PumpState); def validate_specs(state: PumpState): return { 'validation_results': ['Pressure compliance check passed', 'Material integrity verified'] }; def perform_export_check(state: PumpState): return { 'validation_results': ['Dual-use compliance verified'] }; graph.add_node('validation', validate_specs); graph.add_node('export_review', perform_export_check); graph.set_entry_point('validation'); graph.add_edge('validation', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()