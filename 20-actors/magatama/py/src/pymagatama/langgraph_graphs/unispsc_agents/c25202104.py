from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class AircraftState(TypedDict): panel_data: dict; compliance_check: bool; def validate_specs(state: AircraftState): state['compliance_check'] = 'MIL-STD' in state['panel_data'].get('certs', []); return state; def log_procurement(state: AircraftState): print(f'Processing procurement for {state['panel_data'].get('model')}'); return state; graph = StateGraph(AircraftState); graph.add_node('validate', validate_specs); graph.add_node('log', log_procurement); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()