from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PhotoTubeState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_tube_specs(state: PhotoTubeState): results = []; qe = state['specs'].get('quantum_efficiency', 0); results.append('Passed' if qe > 20 else 'Failed'); return {'validation_results': results, 'status': 'validated'}; graph = StateGraph(PhotoTubeState); graph.add_node('validate', validate_tube_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()