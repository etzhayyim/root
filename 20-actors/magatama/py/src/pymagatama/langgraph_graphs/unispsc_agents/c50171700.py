from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FoodProductState(TypedDict): product: str; acidity: float; shelf_life_days: int; compliant: bool; def check_compliance(state: FoodProductState): state['compliant'] = state['acidity'] >= 4.0 and state['shelf_life_days'] > 180; return state; graph = StateGraph(FoodProductState); graph.add_node('check', check_compliance); graph.set_entry_point('check'); graph.add_edge('check', END); graph = graph.compile()