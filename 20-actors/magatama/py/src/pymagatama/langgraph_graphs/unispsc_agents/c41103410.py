from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChamberState(TypedDict): specs: dict; validation_errors: List[str]; def validate_specs(state: ChamberState): errors = []; if 'temperature_range' not in state['specs']: errors.append('Missing T-range'); return {'validation_errors': errors}; graph = StateGraph(ChamberState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()