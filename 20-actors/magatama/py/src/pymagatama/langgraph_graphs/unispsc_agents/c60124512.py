from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScooterState(TypedDict): specs: dict; compliance: bool; validated: bool; def validate_specs(state: ScooterState): state['compliance'] = state['specs'].get('weight_capacity_kg', 0) > 0; return state; def check_safety(state: ScooterState): state['validated'] = state['compliance'] and 'safety_certification' in state['specs']; return state; graph = StateGraph(ScooterState); graph.add_node('validate', validate_specs); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_safety'); graph.add_edge('check_safety', END); compile = graph.compile()