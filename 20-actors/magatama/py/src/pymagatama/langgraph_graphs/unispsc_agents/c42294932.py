from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): doc_valid: bool; spec_approval: bool; def validate_docs(state: ProcState): return {'doc_valid': True}; def check_specs(state: ProcState): return {'spec_approval': True}; graph = StateGraph(ProcState); graph.add_node('validate', validate_docs); graph.add_node('check_specs', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_specs'); graph.add_edge('check_specs', END); app = graph.compile()