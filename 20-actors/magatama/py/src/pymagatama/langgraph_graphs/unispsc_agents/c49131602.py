from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DecoyState(TypedDict): species: str; material: str; defects: List[str]; approved: bool; def validate_spec(state: DecoyState): state['approved'] = 'plastic' in state['material'].lower(); return state; workflow = StateGraph(DecoyState); workflow.add_node('validate', validate_spec); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()