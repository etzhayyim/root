from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): chemical_data: dict; validation_report: str; def validate_purity(state: State): return {'validation_report': 'Purity Verified' if state['chemical_data'].get('purity', 0) >= 99.0 else 'Rejected'}; def chemical_workflow(state: State): return 'COMPLIANT' if state['validation_report'] == 'Purity Verified' else 'NON_COMPLIANT'; graph = StateGraph(State); graph.add_node('validate', validate_purity); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()