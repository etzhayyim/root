from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; structural_integrity_check: bool; material_spec_verified: bool; compliance_status: List[str]; def validate_materials(state: AssemblyState): return {'material_spec_verified': True}; def check_structural_specs(state: AssemblyState): return {'structural_integrity_check': True}; graph = StateGraph(AssemblyState); graph.add_node('verify_material', validate_materials); graph.add_node('check_structural', check_structural_specs); graph.set_entry_point('verify_material'); graph.add_edge('verify_material', 'check_structural'); graph.add_edge('check_structural', END); graph = graph.compile()