from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EducationMaterialState(TypedDict): items: List[str]; validated: bool; def val_quality(state: EducationMaterialState): return {'validated': all(item for item in state['items'])}; graph = StateGraph(EducationMaterialState); graph.add_node('qc', val_quality); graph.set_entry_point('qc'); graph.add_edge('qc', END); graph = graph.compile()