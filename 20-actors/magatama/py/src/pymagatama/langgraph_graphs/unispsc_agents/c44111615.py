from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DepositBagState(TypedDict): bag_id: str; integrity_check: bool; metadata: dict; def validate_tamper_seal(state: DepositBagState): state['integrity_check'] = True if state['metadata'].get('seal_intact') else False; return state; graph = StateGraph(DepositBagState); graph.add_node('validate', validate_tamper_seal); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()