from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpinnakerState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SpinnakerState): errors = []; if state['specs'].get('length', 0) <= 0: errors.append('Invalid length'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(SpinnakerState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()