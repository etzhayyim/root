from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GeoState(TypedDict): data: dict; status: str; def validate_sample(state: GeoState): state['status'] = 'validated' if 'sample_id' in state['data'] else 'rejected'; return state; def process_procurement(state: GeoState): state['status'] = 'procurement_approved'; return state; graph = StateGraph(GeoState); graph.add_node('validate', validate_sample); graph.add_node('approve', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()