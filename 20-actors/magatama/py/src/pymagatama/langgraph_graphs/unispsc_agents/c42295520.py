from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): kit_id: str; compliance_check: bool; spec_fields: List[str]; def validate_kit(state: State): return {'compliance_check': True}; def update_records(state: State): return {'kit_id': 'V-KIT-001'}; graph = StateGraph(State); graph.add_node('validate', validate_kit); graph.add_node('record', update_records); graph.set_entry_point('validate'); graph.add_edge('validate', 'record'); graph.add_edge('record', END); graph = graph.compile()