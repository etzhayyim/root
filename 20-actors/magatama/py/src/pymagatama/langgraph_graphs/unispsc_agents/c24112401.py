from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_results: dict; graph = StateGraph(State); def validate_specs(state: State): state['validation_results'] = {'passed': True, 'load_check': 'verified'}; return state; def finalize_order(state: State): return {'validation_results': 'COMPLETED'}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()