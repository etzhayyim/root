from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DiamondProcessState(TypedDict): abrasive_type: str; purity: float; particle_size: float; validation_status: str; def validate_abrasive(state: DiamondProcessState): state['validation_status'] = 'APPROVED' if state['purity'] > 0.99 else 'REJECTED'; return state; def prepare_for_procurement(state: DiamondProcessState): return {'validation_status': 'PREPARED_FOR_SHIPMENT'}; graph = StateGraph(DiamondProcessState); graph.add_node('validate', validate_abrasive); graph.add_node('prepare', prepare_for_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'prepare'); graph.add_edge('prepare', END); graph = graph.compile()