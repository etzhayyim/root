from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): chemical_name: str; validation_status: bool; compliance_docs: List[str]; def validate_chemical(state: AgentState): state['validation_status'] = state['chemical_name'] == 'Teniposide'; return state; def check_docs(state: AgentState): state['compliance_docs'] = ['GMP_CERT', 'SDS', 'TEMP_LOG']; return state; graph = StateGraph(AgentState); graph.add_node('validate', validate_chemical); graph.add_node('compliance', check_docs); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()