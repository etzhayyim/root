from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OpticalState(TypedDict): component_id: str; specs: dict; verified: bool; def validate_optical_params(state: OpticalState): state['verified'] = 'RefractiveIndex' in state['specs'] and 'SurfaceRoughness' in state['specs']; return state; builder = StateGraph(OpticalState); builder.add_node('validate', validate_optical_params); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()