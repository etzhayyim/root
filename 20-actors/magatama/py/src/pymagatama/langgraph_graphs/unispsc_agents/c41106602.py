from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BioState(TypedDict): vector_id: str; sequence_verified: bool; biosafety_level: int; status: str; def validate_sequence(state: BioState): return {'sequence_verified': True, 'status': 'VALIDATED'}; def check_regulatory(state: BioState): return {'status': 'COMPLIANT' if state['biosafety_level'] <= 2 else 'RESTRICTED'}; graph = StateGraph(BioState); graph.add_node('validate', validate_sequence); graph.add_node('regulatory', check_regulatory); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); graph = graph.compile()