from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SensorState(TypedDict): sensor_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(SensorState); def validate_spec(state: SensorState): return {'validation_results': ['Spec check passed']}; def export_control_check(state: SensorState): return {'validation_results': ['Export compliance confirmed']}; graph.add_node('validate', validate_spec); graph.add_node('compliance', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()