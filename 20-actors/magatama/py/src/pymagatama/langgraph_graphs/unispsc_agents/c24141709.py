from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CapsuleState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: CapsuleState): import re; material = state['specs'].get('material', ''); return {'validation_report': 'Passed' if material in ['Gelatin', 'HPMC'] else 'Failed'}; def finalize_order(state: CapsuleState): return {'approved': state['validation_report'] == 'Passed'}; graph = StateGraph(CapsuleState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()