from langgraph.graph import StateGraph, END; from typing import TypedDict; class WeldingState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: WeldingState): state['compliant'] = all(k in state['specs'] for k in ['voltage', 'duty_cycle']); return state; def check_danger(state: WeldingState): return 'compliant' if state['compliant'] else END; graph = StateGraph(WeldingState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()