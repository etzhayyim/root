from langgraph.graph import StateGraph, END; from typing import TypedDict; class PosterState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PosterState): return {'valid': 'dimensions' in state['specs'] and 'paper_gsm' in state['specs']}; graph = StateGraph(PosterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()