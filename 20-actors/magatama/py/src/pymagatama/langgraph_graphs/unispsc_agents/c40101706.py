from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): spec: dict; validated: bool; state: str; def validate_specs(state: ProcureState): state['validated'] = 'air_leakage_rate' in state['spec'] and 'material_composition' in state['spec']; return state; def route_procurement(state: ProcureState): return 'end' if state['validated'] else 'end'; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()