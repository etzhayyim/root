from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ERGState(TypedDict): data: dict; validation_errors: List[str]; approved: bool; def validate_compliance(state: ERGState): state['approved'] = 'ISO_13485' in state['data'].get('certs', []) and state['data'].get('safety_check', False); return state; def check_medical_reg(state: ERGState): if not state['approved']: state['validation_errors'].append('Regulatory compliance missing'); return state; graph = StateGraph(ERGState); graph.add_node('validate', validate_compliance); graph.add_node('certify', check_medical_reg); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()