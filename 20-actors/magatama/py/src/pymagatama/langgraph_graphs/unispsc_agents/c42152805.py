from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalInstrumentState(TypedDict): instrument_type: str; specs: dict; valid: bool; def validate_instrument(state: DentalInstrumentState): state['valid'] = all(k in state['specs'] for k in ['iso_cert', 'material']); return state; graph = StateGraph(DentalInstrumentState); graph.add_node('validator', validate_instrument); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()