from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): doc_id: str; compliance_check: bool; material_spec: str; graph = StateGraph(ProcState); def validate_medical_grade(state: ProcState): state['compliance_check'] = True if 'ISO' in state.get('material_spec', '') else False; return state; graph.add_node('validate', validate_medical_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()