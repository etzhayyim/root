from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AirwayGaugeState(TypedDict): model_id: str; pressure_range: float; needs_calibration: bool; validation_report: List[str]; def validate_spec(state: AirwayGaugeState): state['validation_report'].append('Validating ISO calibration standards...'); return state; def check_regulatory(state: AirwayGaugeState): state['validation_report'].append('Checking FDA/MDR compliance...'); return state; graph = StateGraph(AirwayGaugeState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()