from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcedureState(TypedDict): tray_contents: List[str]; is_sterile: bool; compliance_status: bool; def validate_tray_quality(state: ProcedureState): return {'compliance_status': state['is_sterile'] and len(state['tray_contents']) > 0}; def finalize_procurement(state: ProcedureState): return {'compliance_status': True}; graph = StateGraph(ProcedureState); graph.add_node('validation', validate_tray_quality); graph.add_node('finalization', finalize_procurement); graph.set_entry_point('validation'); graph.add_edge('validation', 'finalization'); graph.add_edge('finalization', END); graph = graph.compile()