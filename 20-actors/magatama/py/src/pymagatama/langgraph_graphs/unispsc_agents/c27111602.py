from langgraph.graph import StateGraph, END; from typing import TypedDict; class HammerState(TypedDict): spec: dict; verified: bool; def validate_specs(state: HammerState): state['verified'] = 'head_material' in state['spec'] and state['spec']['weight_oz_grams'] > 0; return state; graph = StateGraph(HammerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()