from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): vendor_specs: dict; compliance_check: bool; final_report: str; def validate_furniture_specs(state: FurnitureState): compliance = all(k in state['vendor_specs'] for k in ['load_capacity', 'fire_rating']); return {'compliance_check': compliance}; def generate_procurement_report(state: FurnitureState): return {'final_report': 'Verification complete' if state['compliance_check'] else 'Compliance failure'}; graph = StateGraph(FurnitureState); graph.add_node('validate', validate_furniture_specs); graph.add_node('report', generate_procurement_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()