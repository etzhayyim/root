from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrailerConnectorState(TypedDict): connector_type: str; pin_count: int; validated: bool; def validate_connector(state: TrailerConnectorState): state['validated'] = state['pin_count'] > 0; return state; graph = StateGraph(TrailerConnectorState); graph.add_node('validate', validate_connector); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()