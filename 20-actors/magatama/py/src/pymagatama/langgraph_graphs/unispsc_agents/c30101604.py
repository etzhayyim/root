from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SteelProcureState(TypedDict): specs: dict; valid: bool; material_grade: str; def validate_specs(state: SteelProcureState): state['valid'] = state['specs'].get('grade') is not None and state['specs'].get('diameter') > 0; return state; graph = StateGraph(SteelProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()