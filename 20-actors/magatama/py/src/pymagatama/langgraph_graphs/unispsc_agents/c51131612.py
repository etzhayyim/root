from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): medicine: str; temp_compliant: bool; gmp_certified: bool; def check_quality(state: DrugState): return {'temp_compliant': True, 'gmp_certified': True}; def validate_shipment(state: DrugState): return {'medicine': 'Danaparoid sodium' if state['temp_compliant'] and state['gmp_certified'] else 'Rejected'}; graph = StateGraph(DrugState); graph.add_node('quality', check_quality); graph.add_node('validate', validate_shipment); graph.add_edge('quality', 'validate'); graph.add_edge('validate', END); graph.set_entry_point('quality'); graph = graph.compile()