from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_name: str; purity: float; compliance_docs: List[str]; def validate_purity(state: ProcureState): return {'purity': min(state['purity'], 99.9)}; def check_compliance(state: ProcureState): return {'compliance_docs': ['GMP_CERT', 'SDS_VALIDATED']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.compile()