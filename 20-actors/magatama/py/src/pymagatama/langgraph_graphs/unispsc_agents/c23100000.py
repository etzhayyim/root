from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MachineProcurementState(TypedDict): vendor_check: str; compliance_audit: str; deployment_status: str; graph = StateGraph(MachineProcurementState); def validate_vendor(state): return {'vendor_check': 'PASSED'}; def perform_compliance(state): return {'compliance_audit': 'COMPLIANT'}; graph.add_node('validate', validate_vendor); graph.add_node('compliance', perform_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()