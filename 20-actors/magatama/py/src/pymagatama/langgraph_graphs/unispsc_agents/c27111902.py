from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FileProcurementState(TypedDict): file_specs: dict; validation_results: List[str]; def validate_specs(state: FileProcurementState): results = []; hardness = state['file_specs'].get('hardness', 0); results.append('Hardness Pass' if hardness > 60 else 'Hardness Fail'); return {'validation_results': results}; graph = StateGraph(FileProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()