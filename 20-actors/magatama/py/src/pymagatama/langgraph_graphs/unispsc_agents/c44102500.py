from langgraph.graph import StateGraph, END; from typing import TypedDict; class SortingState(TypedDict): specs: dict; validated: bool; def validate_specs(state: SortingState): state['validated'] = 'throughput_capacity_per_hour' in state['specs']; return state; def finalize_order(state: SortingState): return {'validated': True}; builder = StateGraph(SortingState); builder.add_node('validate', validate_specs); builder.add_node('finalize', finalize_order); builder.set_entry_point('validate'); builder.add_edge('validate', 'finalize'); builder.add_edge('finalize', END); graph = builder.compile()