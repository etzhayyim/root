from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class CableState(TypedDict): messages: Annotated[list, add_messages]; spec_data: dict; validation_result: bool; def validate_specs(state: CableState): spec = state.get('spec_data', {}); return {'validation_result': 'bending_radius_mm' in spec and spec['bending_radius_mm'] > 0}; def finalize_procurement(state: CableState): return {'messages': ['Procurement specifications verified and approved.']}; graph = StateGraph(CableState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()