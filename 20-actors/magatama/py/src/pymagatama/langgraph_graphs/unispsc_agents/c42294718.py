from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): pump_specs: dict; validation_results: List[str]; def validate_perf_pump(state: State): specs = state['pump_specs']; results = []; results.append('PASSED' if specs.get('flow_accuracy') < 0.1 else 'FAILED'); return {'validation_results': results}; graph = StateGraph(State); graph.add_node('validate', validate_perf_pump); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()