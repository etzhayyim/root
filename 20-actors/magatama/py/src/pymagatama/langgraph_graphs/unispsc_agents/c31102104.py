from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: CastingState): state['validation_results'].append('Material chemistry verified'); return state; def check_tolerances(state: CastingState): state['validation_results'].append('CAD footprint verified'); return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_materials); graph.add_node('tolerance_check', check_tolerances); graph.set_entry_point('validate'); graph.add_edge('validate', 'tolerance_check'); graph.add_edge('tolerance_check', END); graph = graph.compile()