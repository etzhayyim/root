from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FacilityState(TypedDict): facility_id: str; compliance_docs: List[str]; inspection_status: str; def validate_compliance(state: FacilityState): return { 'inspection_status': 'Verified' if len(state['compliance_docs']) > 2 else 'Pending' }; def construct_graph(): graph = StateGraph(FacilityState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = construct_graph()