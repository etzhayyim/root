from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; valid: bool;  def validate_riser(state: State): specs = state['specs']; state['valid'] = all(k in specs for k in ['height', 'material', 'fire_rating']); return state; graph = StateGraph(State); graph.add_node('validate', validate_riser); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()