from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ChairMatState(TypedDict): specs: dict; validation_results: list[str]; def validate_material(state: ChairMatState): state['validation_results'].append('Material verified for floor compatibility'); return state; def check_durability(state: ChairMatState): state['validation_results'].append('Durability metrics within acceptable range'); return state; graph = StateGraph(ChairMatState); graph.add_node('validate_material', validate_material); graph.add_node('check_durability', check_durability); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_durability'); graph.add_edge('check_durability', END); graph = graph.compile()