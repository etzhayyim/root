from langgraph.graph import StateGraph, END; from typing import TypedDict; class ChemState(TypedDict): purity_check: bool; regulation_approved: bool; graph = StateGraph(ChemState); def validate_purity(state: ChemState): state['purity_check'] = True; return state; def check_regulations(state: ChemState): state['regulation_approved'] = True; return state; graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.compile()