from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftState(TypedDict): aircraft_id: str; compliance_checks: List[str]; approved: bool; def validate_airworthiness(state: AircraftState): state['compliance_checks'].append('Airworthiness Validated'); state['approved'] = True; return state; graph = StateGraph(AircraftState); graph.add_node('validate', validate_airworthiness); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()