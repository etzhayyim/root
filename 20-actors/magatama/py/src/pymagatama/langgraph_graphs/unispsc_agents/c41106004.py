from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class KitState(TypedDict): input_data: dict; validation_results: dict; def validate_specs(state: KitState): return { "validation_results": { "is_compliant": all(k in state["input_data"] for k in ["purity", "storage"]) } }; def check_compliance(state: KitState): return "compliant" if state["validation_results"]["is_compliant"] else "non_compliant"; graph = StateGraph(KitState); graph.add_node("validate", validate_specs); graph.add_node("decision", check_compliance); graph.set_entry_point("validate"); graph.add_edge("validate", "decision"); graph.add_edge("decision", END); graph = graph.compile()