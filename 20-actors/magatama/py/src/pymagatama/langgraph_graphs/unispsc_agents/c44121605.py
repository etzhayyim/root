from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TapeDispenserState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: TapeDispenserState): specs = state['specs']; state['valid'] = 'tape_width' in specs and specs['tape_width'] > 0; return state; graph = StateGraph(TapeDispenserState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()