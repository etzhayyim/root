from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WheelchairState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: WheelchairState): errors = []; if state['specs'].get('weight_capacity_kg', 0) <= 0: errors.append('Invalid capacity'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(WheelchairState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()