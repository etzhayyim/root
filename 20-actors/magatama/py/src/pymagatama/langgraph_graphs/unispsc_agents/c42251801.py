from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class RehaState(TypedDict): spec: dict; validated: bool; log: list[str]; def check_compliance(state: RehaState): cert = state['spec'].get('certification'); return {'validated': cert == 'ISO_13485', 'log': ['Compliance check performed']}; def routing(state: RehaState): return 'valid' if state['validated'] else END; graph = StateGraph(RehaState); graph.add_node('compliance', check_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', END); 