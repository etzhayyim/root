from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PanicBarState(TypedDict): hardware_specs: dict; compliance_verified: bool; def validate_specs(state: PanicBarState): state['compliance_verified'] = state['hardware_specs'].get('grade') in ['Grade 1', 'Grade 2']; return state; def finalize_order(state: PanicBarState): return {'status': 'Approved' if state['compliance_verified'] else 'Rejected'}; graph = StateGraph(PanicBarState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()