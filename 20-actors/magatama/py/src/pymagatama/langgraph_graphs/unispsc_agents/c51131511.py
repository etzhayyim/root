from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): ingredient: str; grade: str; coa_validated: bool; def check_quality(state: ProcureState): return {'coa_validated': True if state['grade'] == 'Pharma' else False}; def final_review(state: ProcureState): print('Compliance Verified'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', check_quality); graph.add_node('review', final_review); graph.add_edge('validate', 'review'); graph.add_edge('review', END); graph.set_entry_point('validate'); graph = graph.compile()