from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ItemState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(ItemState); def validate_structure(state: ItemState): status = 'Valid' if state['specs'].get('tonnage', 0) > 0 else 'Invalid'; return {'validation_report': [f'Structure check: {status}']}; def check_compliance(state: ItemState): return {'validation_report': state['validation_report'] + ['Compliance verified']}; graph.add_node('validate', validate_structure); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()