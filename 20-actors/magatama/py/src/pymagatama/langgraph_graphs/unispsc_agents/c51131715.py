from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; purity: float; gmp_compliant: bool; valid: bool; def validate_purity(state: DrugState): state['valid'] = state['purity'] >= 99.0; return state; def check_regulatory(state: DrugState): state['valid'] = state['gmp_compliant'] and state['valid']; return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()