from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class LabState(TypedDict): material: str; specs: dict; compliance: bool; def check_specs(state: LabState): state['compliance'] = state['specs'].get('temp_rating', 0) > 100; return state; graph = StateGraph(LabState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()