from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PhotoChemState(TypedDict): chemical_name: str; safety_checks: List[str]; approved: bool; def validate_msds(state: PhotoChemState): state['safety_checks'].append('MSDS Verified'); return state; def hazard_screening(state: PhotoChemState): state['approved'] = True; return state; graph = StateGraph(PhotoChemState); graph.add_node('msds_check', validate_msds); graph.add_node('hazard_check', hazard_screening); graph.add_edge('msds_check', 'hazard_check'); graph.add_edge('hazard_check', END); graph.set_entry_point('msds_check'); graph = graph.compile()