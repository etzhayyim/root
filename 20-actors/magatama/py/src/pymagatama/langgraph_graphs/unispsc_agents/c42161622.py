from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): chemical_data: dict; compliance_status: List[str]; def validate_purity(state: State): return {'compliance_status': ['Purity_Verified']}; def check_safety(state: State): return {'compliance_status': ['Safety_Protocol_Passed']}; graph = StateGraph(State); graph.add_node('validate_purity', validate_purity); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()