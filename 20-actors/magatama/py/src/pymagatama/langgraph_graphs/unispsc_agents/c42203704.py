from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MedicalSuppliesState(TypedDict): items: List[str]; validated: bool; state = MedicalSuppliesState(items=[], validated=False); def validate_toner(state: MedicalSuppliesState): return {'validated': len(state['items']) > 0}; def format_report(state: MedicalSuppliesState): return {'items': ['Batch_Certified']}; graph = StateGraph(MedicalSuppliesState); graph.add_node('validate', validate_toner); graph.add_node('format', format_report); graph.add_edge('validate', 'format'); graph.add_edge('format', END); graph.set_entry_point('validate'); app = graph.compile()