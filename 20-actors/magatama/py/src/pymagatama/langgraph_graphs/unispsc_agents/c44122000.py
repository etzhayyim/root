from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilesState(TypedDict): items: List[dict]; valid: bool; def validate_specs(state: FilesState): state['valid'] = 'Material type' in state['items'][0] and 'Dimensions' in state['items'][0]; return state; graph = StateGraph(FilesState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()