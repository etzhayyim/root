from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EMSCaseState(TypedDict): case_meta: dict; validated: bool; inventory: List[str]; def validate_case_specs(state: EMSCaseState): state['validated'] = 'material' in state['case_meta'] and 'capacity' in state['case_meta']; return {'validated': state['validated']}; def check_inventory(state: EMSCaseState): return {'inventory': state.get('case_meta', {}).get('items', [])}; graph = StateGraph(EMSCaseState); graph.add_node('validate', validate_case_specs); graph.add_node('check', check_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()