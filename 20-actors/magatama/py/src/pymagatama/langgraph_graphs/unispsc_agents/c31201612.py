from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SealantState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: SealantState): return {'compliance': state['specs'].get('pressure_rating', 0) > 0 and 'temp' in state['specs']}; def check_safety(state: SealantState): return {'approved': state.get('compliance', False)}; graph = StateGraph(SealantState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()