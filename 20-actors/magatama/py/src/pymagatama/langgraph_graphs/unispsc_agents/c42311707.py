from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalTapeState(TypedDict): items: List[str]; inspection_results: dict; class SurgicalTapeProcessor: def validate_sterility(self, state: SurgicalTapeState): return {'inspection_results': {'sterile': True}}; def check_biocompatibility(self, state: SurgicalTapeState): return {'inspection_results': {'bio': 'passed'}}; workflow = SurgicalTapeProcessor(); graph = StateGraph(SurgicalTapeState); graph.add_node('validate', workflow.validate_sterility); graph.add_node('biocompatibility', workflow.check_biocompatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'biocompatibility'); graph.add_edge('biocompatibility', END); graph = graph.compile()