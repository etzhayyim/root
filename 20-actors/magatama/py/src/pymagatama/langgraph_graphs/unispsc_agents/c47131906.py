from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AbsorbentState(TypedDict): specs: dict; validation_results: List[str]; def validate_capacity(state: AbsorbentState): state['validation_results'].append('Capacity verified') if state['specs'].get('liters', 0) > 0 else None; return state; graph = StateGraph(AbsorbentState); graph.add_node('validate', validate_capacity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()