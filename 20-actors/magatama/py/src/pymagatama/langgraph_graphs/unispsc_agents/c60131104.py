from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SaxState(TypedDict): model_id: str; specs: dict; verified: bool; def validate_specs(state: SaxState): state['verified'] = 'material' in state['specs'] and 'intonation' in state['specs']; return state; def check_quality(state: SaxState): return 'approved' if state['verified'] else 'rejected'; graph = StateGraph(SaxState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()