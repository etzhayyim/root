from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InkProcessingState(TypedDict): ink_type: str; is_verified: bool; quality_checks: List[str]; def check_viscosity(state: InkProcessingState): return {'is_verified': True, 'quality_checks': ['viscosity_passed']}; def finalize_spec(state: InkProcessingState): return {'quality_checks': state['quality_checks'] + ['documentation_completed']}; graph = StateGraph(InkProcessingState); graph.add_node('viscosity_check', check_viscosity); graph.add_node('finalizer', finalize_spec); graph.set_entry_point('viscosity_check'); graph.add_edge('viscosity_check', 'finalizer'); graph.add_edge('finalizer', END); app = graph.compile()