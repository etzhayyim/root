from langgraph.graph import StateGraph, END; from typing import TypedDict; class MachineToolState(TypedDict): specs: dict; valid: bool; def validate_specs(state: MachineToolState): return {'valid': 'model' in state['specs'] and 'speed' in state['specs']}; graph = StateGraph(MachineToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()