from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ChemState(TypedDict): chemical_name: str; concentration: float; compliant: bool; graph = StateGraph(ChemState); def validate_spec(state: ChemState): return {'compliant': state['concentration'] >= 0.05}; graph.add_node('validation', validate_spec); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()