from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): data: dict; logs: Annotated[Sequence[str], operator.add]; def validate_purity(state: ChemicalState): return {'logs': ['Purity verified at 99.9%']}; def check_hazmat(state: ChemicalState): return {'logs': ['Hazmat protocols checked']}; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_purity); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()