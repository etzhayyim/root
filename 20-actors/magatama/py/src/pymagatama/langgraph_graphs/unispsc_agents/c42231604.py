from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): device_serial: str; is_sterile: bool; validation_passed: bool; def check_medical_compliance(state: ProcState): validated = state['is_sterile'] and len(state['device_serial']) > 0; return {'validation_passed': validated}; workflow = StateGraph(ProcState); workflow.add_node('compliance_check', check_medical_compliance); workflow.set_entry_point('compliance_check'); workflow.add_edge('compliance_check', END); graph = workflow.compile()