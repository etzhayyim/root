from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScrollState(TypedDict): scroll_id: str; material_specs: dict; inspection_status: str; def validate_materials(state: ScrollState): return {'inspection_status': 'verified'} if state['material_specs'] else {'inspection_status': 'pending'}; def finalize_procurement(state: ScrollState): return {'inspection_status': 'ready_for_shipping'}; graph = StateGraph(ScrollState); graph.add_node('validate', validate_materials); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()