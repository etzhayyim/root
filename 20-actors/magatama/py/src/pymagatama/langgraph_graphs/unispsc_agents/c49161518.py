from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TrainingAidState(TypedDict): input_data: dict; validation_results: list; is_approved: bool; def validate_specs(state: TrainingAidState): results = []; # Logic for checking durability and material compliance; state['validation_results'].append('specs_validated'); return {'validation_results': state['validation_results']}; def check_safety(state: TrainingAidState): return {'is_approved': True}; graph = StateGraph(TrainingAidState); graph.add_node('validate', validate_specs); graph.add_node('safety_check', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()