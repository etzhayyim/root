from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DispenserState(TypedDict): vendor_certs: List[str]; hygiene_compliant: bool; spec_verified: bool; def check_hygiene(state: DispenserState): state['hygiene_compliant'] = 'NSF' in state['vendor_certs']; return state; def verify_specs(state: DispenserState): state['spec_verified'] = state['hygiene_compliant']; return state; graph = StateGraph(DispenserState); graph.add_node('check_hygiene', check_hygiene); graph.add_node('verify_specs', verify_specs); graph.set_entry_point('check_hygiene'); graph.add_edge('check_hygiene', 'verify_specs'); graph.add_edge('verify_specs', END); graph = graph.compile()