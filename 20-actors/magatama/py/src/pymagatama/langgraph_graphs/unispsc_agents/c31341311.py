from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): doc_id: str; quality_passed: bool; export_check: bool; def check_quality(state: ProcurementState): return {'quality_passed': True}; def check_export(state: ProcurementState): return {'export_check': True}; graph = StateGraph(ProcurementState); graph.add_node('quality', check_quality); graph.add_node('export', check_export); graph.add_edge('quality', 'export'); graph.add_edge('export', END); graph.set_entry_point('quality'); graph = graph.compile()