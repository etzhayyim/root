from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LightingState(TypedDict): specs: dict; validation_report: list; status: str; def validate_specs(state: LightingState): props = state['specs']; issues = []; if props.get('ip_rating', 0) < 65: issues.append('Insufficient IP rating for outdoor use'); return {'validation_report': issues}; graph = StateGraph(LightingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()