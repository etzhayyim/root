from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CDProcurementState(TypedDict): capacity: str; write_speed: str; status: str; def validate_spec(state: CDProcurementState): return {'status': 'validated' if state['capacity'] and state['write_speed'] else 'rejected'}; def finalize_order(state: CDProcurementState): return {'status': 'processed'}; graph = StateGraph(CDProcurementState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()