from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FuseState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_fuse(state: FuseState): rating = state['specs'].get('amperage_rating_a', 0); state['valid'] = rating > 0; return state; graph = StateGraph(FuseState); graph.add_node('validate', validate_fuse); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()