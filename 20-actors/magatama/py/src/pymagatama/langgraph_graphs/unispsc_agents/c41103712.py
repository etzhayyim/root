from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CirculatorState(TypedDict): specs: dict; validation_report: list[str]; finished: bool; def validate_specs(state: CirculatorState): state['validation_report'] = ['Range check passing'] if state['specs'].get('temp_range') else ['Missing range']; return state; def finalize_order(state: CirculatorState): state['finished'] = True; return state; graph = StateGraph(CirculatorState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()