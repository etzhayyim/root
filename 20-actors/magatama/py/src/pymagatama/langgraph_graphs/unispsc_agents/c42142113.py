from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MaskState(TypedDict): mask_spec: dict; compliance_status: bool; workflow_steps: List[str]; def validate_medical_grade(state: MaskState): state['compliance_status'] = 'ISO_13485_certification' in state['mask_spec']; return state; def check_compatibility(state: MaskState): state['workflow_steps'].append('checking_compatibility'); return state; graph = StateGraph(MaskState); graph.add_node('validate', validate_medical_grade); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()