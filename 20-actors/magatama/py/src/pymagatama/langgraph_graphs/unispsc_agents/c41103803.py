from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StirringTableState(TypedDict): specs: dict; valid: bool; def validate_specs(state: StirringTableState): state['valid'] = state['specs'].get('max_rpm', 0) > 0; return state; def finalize_order(state: StirringTableState): return {'result': 'Ready for procurement' if state['valid'] else 'Invalid specs'}; graph = StateGraph(StirringTableState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()