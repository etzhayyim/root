from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FeedState(TypedDict): feed_batch_id: str; nutrient_analysis: dict; quality_status: str; audit_log: List[str]; def analyze_nutrition(state: FeedState): state['quality_status'] = 'COMPLIANT' if state['nutrient_analysis']['protein'] > 15 else 'FLAGGED'; state['audit_log'].append('Analyzed protein content'); return state; def finalize_batch(state: FeedState): state['audit_log'].append('Batch finalized and approved'); return state; graph = StateGraph(FeedState); graph.add_node('analyze', analyze_nutrition); graph.add_node('finalize', finalize_batch); graph.set_entry_point('analyze'); graph.add_edge('analyze', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()