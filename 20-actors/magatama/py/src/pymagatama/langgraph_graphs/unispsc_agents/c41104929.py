from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FilterSpec(TypedDict): grade: str; diameter: float; status: str; def validate_spec(state: FilterSpec): return {'status': 'validated' if state['grade'] else 'failed'}; graph = StateGraph(FilterSpec); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()