from langgraph.graph import StateGraph, END; from typing import TypedDict; class CastState(TypedDict): specs: dict; valid: bool; def check_specs(state: CastState): is_valid = 'material' in state['specs'] and 'tolerance' in state['specs']; return {'valid': is_valid}; graph = StateGraph(CastState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()