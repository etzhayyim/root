from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_id: str; compliance_docs: List[str]; approved: bool; def validate_cert(state: ProcState): state['approved'] = 'ISO13485' in state['compliance_docs']; return state; builder = StateGraph(ProcState); builder.add_node('validate', validate_cert); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()