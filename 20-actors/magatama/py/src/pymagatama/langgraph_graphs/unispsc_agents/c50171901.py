from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PickleState(TypedDict): items: List[str]; inspection_passed: bool; state = PickleState(items=[], inspection_passed=False); def validate_expiry(state: PickleState): state['inspection_passed'] = all(len(i) > 0 for i in state['items']); return state; graph = StateGraph(PickleState); graph.add_node('validate', validate_expiry); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()