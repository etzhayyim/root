from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EEGPaperState(TypedDict): specs: dict; validation_report: List[str]; def validate_paper(state: EEGPaperState): val = state['specs'].get('thermal_sensitivity'); status = 'Pass' if val > 0.8 else 'Fail'; return {'validation_report': [f'Sensitivity Check: {status}']}; graph = StateGraph(EEGPaperState); graph.add_node('validate', validate_paper); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()