from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; valid: bool; def validate_titanium(state: ProcState): state['valid'] = all(k in state['specs'] for k in ['grade', 'tolerance']); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_titanium); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()