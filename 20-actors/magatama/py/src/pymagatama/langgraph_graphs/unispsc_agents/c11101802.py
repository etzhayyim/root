from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplyChainState(TypedDict): material_id: str; purity_check: bool; safety_compliance: bool; status: str; def validate_purity(state: SupplyChainState): state['purity_check'] = True; return state; def check_safety_protocols(state: SupplyChainState): state['safety_compliance'] = True; state['status'] = 'READY'; return state; graph = StateGraph(SupplyChainState); graph.add_node('purity', validate_purity); graph.add_node('safety', check_safety_protocols); graph.add_edge('purity', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('purity'); graph = graph.compile()