from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): items: List[str]; validated: bool; compliance_docs: List[str]; def validate_instruments(state: SurgicalState): state['validated'] = True; return state; def check_compliance(state: SurgicalState): return {'compliance_docs': ['ISO_CERT', 'FDA_CLEARED']}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_instruments); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()