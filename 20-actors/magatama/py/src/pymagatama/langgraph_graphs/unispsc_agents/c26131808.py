from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SwitchgearState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def validate_specs(state: SwitchgearState): v = state['specs']; results = []; v_status = v.get('voltage', 0) > 0 and v.get('breaking_capacity', 0) > 0; results.append('Passed' if v_status else 'Failed'); return {'validation_results': results, 'approved': v_status}; graph = StateGraph(SwitchgearState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()