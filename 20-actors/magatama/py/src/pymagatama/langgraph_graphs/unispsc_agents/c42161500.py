from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialysisState(TypedDict): procurement_data: dict; risk_score: float; validated: bool; def validate_spec(state: DialysisState): return {'validated': 'standard' in state['procurement_data'].get('certs', [])}; def analyze_risk(state: DialysisState): return {'risk_score': 0.85}; graph = StateGraph(DialysisState); graph.add_node('validate', validate_spec); graph.add_node('risk', analyze_risk); graph.add_edge('validate', 'risk'); graph.add_edge('risk', END); graph.set_entry_point('validate'); graph = graph.compile()