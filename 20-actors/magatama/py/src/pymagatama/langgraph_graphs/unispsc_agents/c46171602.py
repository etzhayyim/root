from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AlertState(TypedDict): decibels: int; range_meters: int; status: str; def validate_specs(state: AlertState): return {'status': 'approved' if state['decibels'] > 90 else 'rejected'}; graph = StateGraph(AlertState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()