from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BoardState(TypedDict): data: dict; errors: Annotated[list, operator.add]; def validate_specs(state: BoardState): return {'errors': ['Missing connectivity spec'] if not state['data'].get('connectivity') else []}; def assemble_order(state: BoardState): return {'data': {**state['data'], 'status': 'validated'}}; graph = StateGraph(BoardState); graph.add_node('validate', validate_specs); graph.add_node('assemble', assemble_order); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph.set_entry_point('validate'); graph = graph.compile()