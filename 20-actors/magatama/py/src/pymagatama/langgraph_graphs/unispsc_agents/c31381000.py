from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MagnetState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: MagnetState): spec = state['specs']; results = []; if spec.get('coercivity', 0) < 500: results.append('Insufficient Coercivity'); return {'validation_results': results, 'status': 'FLAGGED' if results else 'PASSED'}; graph = StateGraph(MagnetState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()