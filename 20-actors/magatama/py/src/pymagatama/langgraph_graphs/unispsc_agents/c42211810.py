from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): vendor_specs: dict; compliance_checked: bool; inspection_result: str; def validate_specs(state: FastenerState): state['compliance_checked'] = all(key in state['vendor_specs'] for key in ['tensile_strength', 'iso_compliance']); return state; def check_quality(state: FastenerState): state['inspection_result'] = 'PASS' if state['compliance_checked'] else 'FAIL'; return state; graph = StateGraph(FastenerState); graph.add_node('validate', validate_specs); graph.add_node('quality', check_quality); graph.add_edge('validate', 'quality'); graph.add_edge('quality', END); graph.set_entry_point('validate'); graph = graph.compile()