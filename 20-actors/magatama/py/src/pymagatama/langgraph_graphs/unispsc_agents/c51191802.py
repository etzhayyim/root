from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PotassiumState(TypedDict): chemical_purity: float; validation_passed: bool; storage_conditions: str; def validate_purity(state: PotassiumState): return {'validation_passed': state['chemical_purity'] >= 99.0}; def check_storage(state: PotassiumState): return {'storage_conditions': 'verified' if state['storage_conditions'] == 'dry' else 'flag_for_review'}; graph = StateGraph(PotassiumState); graph.add_node('validate', validate_purity); graph.add_node('storage', check_storage); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph.set_entry_point('validate'); graph = graph.compile()