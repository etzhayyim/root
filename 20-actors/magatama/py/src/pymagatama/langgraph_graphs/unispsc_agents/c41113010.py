from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): specs: dict; calibration_status: bool; validation_log: List[str]; def validate_specs(state: AnalysisState): state['validation_log'].append('Validating thermal specifications'); return {'calibration_status': True}; def update_workflow(state: AnalysisState): state['validation_log'].append('Updating workflow for TGA'); return state; graph = StateGraph(AnalysisState); graph.add_node('validate', validate_specs); graph.add_node('workflow', update_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'workflow'); graph.add_edge('workflow', END); graph = graph.compile()