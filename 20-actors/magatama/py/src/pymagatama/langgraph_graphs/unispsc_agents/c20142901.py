from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec_data: dict; graph = StateGraph(State); def validate_aerospace_specs(state: State): return {'status': 'validated'} if 'part_number' in state['spec_data'] else {'status': 'failed'}; def perform_export_check(state: State): return {'status': 'export_cleared'}; graph.add_node('validate', validate_aerospace_specs); graph.add_node('export_review', perform_export_check); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()