from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(TubeState); def validate_specs(state: TubeState): return {'validation_results': ['Pressure check passed', 'Dimensions confirmed']}; def check_export_controls(state: TubeState): return {'validation_results': state['validation_results'] + ['Export control cleared']}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()