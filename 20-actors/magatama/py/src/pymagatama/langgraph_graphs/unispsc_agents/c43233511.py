from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: str; validation_status: str; graph = StateGraph(State); def validate_software_scope(state: 'State'): return {'validation_status': 'verified'} if 'gps' in state['input_data'].lower() else {'validation_status': 'invalid'}; def process_procurement(state: 'State'): return {'validation_status': 'ready_for_review'}; graph.add_node('validate', validate_software_scope); graph.add_node('process', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()