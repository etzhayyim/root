from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_name: str; status: str; spec_check_passed: bool; def validation_step(state: ToolState) -> ToolState: state['status'] = 'Validating'; state['spec_check_passed'] = True if 'Hardened' in state['tool_name'] else False; return state; graph = StateGraph(ToolState); graph.add_node('validate', validation_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()