from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShirtWorkflowState(TypedDict): vendor: str; specs: dict; approved: bool; def check_safety(state: ShirtWorkflowState): state['approved'] = 'safety_cert' in state['specs']; return state; def validate_sizing(state: ShirtWorkflowState): state['approved'] = state['approved'] and 'size_grid' in state['specs']; return state; graph = StateGraph(ShirtWorkflowState); graph.add_node('safety_check', check_safety); graph.add_node('size_check', validate_sizing); graph.add_edge('safety_check', 'size_check'); graph.add_edge('size_check', END); graph.set_entry_point('safety_check'); graph = graph.compile()