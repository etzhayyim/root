from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SterilizerState(TypedDict): vendor_certs: List[str]; cycle_specs: dict; compliance_check: bool; def validate_specs(state: SterilizerState): state['compliance_check'] = 'ISO13485' in state['vendor_certs']; return state; def check_hazmat(state: SterilizerState): state['compliance_check'] = state['compliance_check'] and 'GasDetectorInstalled' in state['cycle_specs']; return state; graph = StateGraph(SterilizerState); graph.add_node('validate', validate_specs); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph.compile()