from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LeadProcessState(TypedDict): input_data: dict; compliance_checks: List[str]; approved: bool; def validate_lead_specs(state: LeadProcessState): state['compliance_checks'] = ['purity_check', 'hvac_sealed']; state['approved'] = True; return state; def hazard_screening(state: LeadProcessState): return {'compliance_checks': state['compliance_checks'] + ['msds_verified']}; graph = StateGraph(LeadProcessState); graph.add_node('validate', validate_lead_specs); graph.add_node('hazard', hazard_screening); graph.add_edge('validate', 'hazard'); graph.add_edge('hazard', END); graph.set_entry_point('validate'); graph = graph.compile()