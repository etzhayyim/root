from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoardState(TypedDict): dimensions: str; material: str; validated: bool; def validate_specs(state: BoardState): state['validated'] = bool(state.get('dimensions') and state.get('material')); return state; graph = StateGraph(BoardState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()