from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RefrigeratorState(TypedDict): vendor_specs: dict; validation_passed: bool; inspection_notes: List[str]; def validate_specs(state: RefrigeratorState): state['validation_passed'] = 'R-value' in state['vendor_specs'] and state['vendor_specs']['R-value'] >= 30; return state; graph = StateGraph(RefrigeratorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()