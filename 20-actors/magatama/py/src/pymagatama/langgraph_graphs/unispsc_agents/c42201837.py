from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CoolingSystemState(TypedDict): specs: dict; compliance: bool; valid: bool; def validate_specs(state: CoolingSystemState): is_valid = state['specs'].get('flow_rate', 0) > 0 and state['specs'].get('temp_precision', 1) <= 2; return {'valid': is_valid}; def check_compliance(state: CoolingSystemState): return {'compliance': state['specs'].get('iso_13485', False)}; graph = StateGraph(CoolingSystemState); graph.add_node('validate', validate_specs); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()