from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): component_specs: dict; validation_report: str; def validate_specs(state: State): specs = state['component_specs']; is_valid = 'material' in specs and 'tolerance' in specs; return {'validation_report': 'Passed' if is_valid else 'Failed'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()