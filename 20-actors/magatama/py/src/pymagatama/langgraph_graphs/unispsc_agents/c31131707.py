from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): alloy_code: str; dimension_spec: dict; qa_passed: bool; def validate_alloy(state: ForgingState): state['qa_passed'] = state['alloy_code'].startswith('AZ') or state['alloy_code'].startswith('ZK'); return state; def check_dimensions(state: ForgingState): state['qa_passed'] = state['dimension_spec'].get('tolerance', 0) < 0.05; return state; graph = StateGraph(ForgingState); graph.add_node('validate_alloy', validate_alloy); graph.add_node('check_dimensions', check_dimensions); graph.set_entry_point('validate_alloy'); graph.add_edge('validate_alloy', 'check_dimensions'); graph.add_edge('check_dimensions', END); compiled_graph = graph.compile()