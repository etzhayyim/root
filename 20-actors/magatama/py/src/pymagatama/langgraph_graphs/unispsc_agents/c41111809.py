from langgraph.graph import StateGraph, END; from typing import TypedDict; class LeakTestState(TypedDict): sensitivity: float; pressure_range: float; compliant: bool; def validate_spec(state: LeakTestState): state['compliant'] = state['sensitivity'] > 0 and state['pressure_range'] > 0; return state; graph = StateGraph(LeakTestState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()