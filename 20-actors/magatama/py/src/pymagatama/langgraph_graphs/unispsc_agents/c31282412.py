from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): composition: str; safety_certs: List[str]; validated: bool; def validate_metallurgy(state: State): return {'validated': 'grade' in state['composition'].lower()}; def check_export_controls(state: State): return {'validated': len(state['safety_certs']) > 0}; graph = StateGraph(State); graph.add_node('validate', validate_metallurgy); graph.add_node('export', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph = graph.compile()