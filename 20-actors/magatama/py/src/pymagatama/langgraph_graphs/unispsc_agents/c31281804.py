from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; alloy_spec: str; validation_passed: bool; def validate_alloy(state: ComponentState): state['validation_passed'] = 'non-ferrous' in state['alloy_spec'].lower(); return state; def check_dimension(state: ComponentState): return state; graph = StateGraph(ComponentState); graph.add_node('validate', validate_alloy); graph.add_node('inspect', check_dimension); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()