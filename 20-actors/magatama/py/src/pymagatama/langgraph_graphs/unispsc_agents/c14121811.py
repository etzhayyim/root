from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class AdhesionState(TypedDict): substrate: str; strength_required: float; validated: bool; process_log: Annotated[Sequence[str], operator.add]; def validate_substrate(state: AdhesionState): return {validated: state[substrate] in ['metal', 'plastic', 'composite']}; def check_strength(state: AdhesionState): return {process_log: [f'Strength requirement {state[strength_required]} validated']}; graph = StateGraph(AdhesionState); graph.add_node('validate', validate_substrate); graph.add_node('check', check_strength); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.set_entry_point('validate'); graph = graph.compile()