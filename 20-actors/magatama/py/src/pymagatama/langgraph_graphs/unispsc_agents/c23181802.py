from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; validation_report: list; status: str; def validate_specs(state: RobotState): return {'validation_report': ['Specs validated against ISO 9283']}; def run_simulation(state: RobotState): return {'status': 'simulation_complete'}; graph = StateGraph(RobotState); graph.add_node('validation', validate_specs); graph.add_node('simulation', run_simulation); graph.set_entry_point('validation'); graph.add_edge('validation', 'simulation'); graph.add_edge('simulation', END); graph = graph.compile()