from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: list; graph = StateGraph(State); def validate_specs(state: State): return {'validation_results': ['Surface Quality Checked', 'Tilt Mechanism Verified']}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()