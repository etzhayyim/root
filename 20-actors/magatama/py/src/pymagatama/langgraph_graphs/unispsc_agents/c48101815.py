from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_log: Annotated[list, operator.add]; def validate_spec(state: State): spec = state['input_data']; logs = []; required = ['material', 'capacity']; for field in required: if field not in spec: logs.append(f'Missing {field}'); return {'validation_log': logs}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()