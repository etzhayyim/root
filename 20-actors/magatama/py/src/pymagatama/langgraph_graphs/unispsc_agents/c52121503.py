from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComforterState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: ComforterState): state['compliance'] = 'thread_count' in state['specs'] and 'material' in state['specs']; state['status'] = 'validated' if state['compliance'] else 'invalid'; return state; graph = StateGraph(ComforterState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()