from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CreepTestState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: CreepTestState): specs = state.get('specs', {}); errors = []; if 'max_load' not in specs: errors.append('Missing max_load'); state['validation_errors'] = errors; state['status'] = 'invalid' if errors else 'validated'; return state; graph = StateGraph(CreepTestState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()