from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FloorState(TypedDict): area_sqm: float; material: str; performance_tests: List[str]; approved: bool; def validate_specs(state: FloorState): state['approved'] = state['area_sqm'] > 0 and 'fire_rated' in state['performance_tests']; return state; builder = StateGraph(FloorState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()