from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): commodity: str; compliant: bool; def validation_step(state: State): state['compliant'] = 'ISO 13485' in state.get('certs', ''); return state; graph = StateGraph(State); graph.add_node('validate', validation_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()