from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RehabState(TypedDict): spec_data: dict; compliance: bool; approval: bool; def validate_specs(state: RehabState): state['compliance'] = state['spec_data'].get('weight_capacity_kg', 0) > 100; return state; def check_medical_reg(state: RehabState): state['approval'] = 'medical_device_registration_id' in state['spec_data']; return state; graph = StateGraph(RehabState); graph.add_node('validate', validate_specs); graph.add_node('compliance_check', check_medical_reg); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()