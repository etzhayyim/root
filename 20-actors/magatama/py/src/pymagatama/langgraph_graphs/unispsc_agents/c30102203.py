from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IronPlateState(TypedDict): plate_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: IronPlateState): specs = state['plate_specs']; errors = []; if specs.get('thickness', 0) <= 0: errors.append('Invalid thickness'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(IronPlateState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()