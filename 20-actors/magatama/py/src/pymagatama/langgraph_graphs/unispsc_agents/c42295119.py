from langgraph.graph import StateGraph, END; from typing import TypedDict; class LaserState(TypedDict): device_id: str; compliance_checked: bool; inspection_passed: bool; def check_regulatory(state: LaserState): return {'compliance_checked': True}; def perform_qa(state: LaserState): return {'inspection_passed': True}; graph = StateGraph(LaserState); graph.add_node('regulatory', check_regulatory); graph.add_node('qa', perform_qa); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'qa'); graph.add_edge('qa', END); compile_graph = graph.compile()