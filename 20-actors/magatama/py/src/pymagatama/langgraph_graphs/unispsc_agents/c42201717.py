from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UltrasoundState(TypedDict): vendor_certs: List[str]; impedance_check: bool; approved: bool; def validate_compliance(state: UltrasoundState): state['approved'] = 'ISO10993' in state['vendor_certs'] and state['impedance_check']; return state; builder = StateGraph(UltrasoundState); builder.add_node('validate', validate_compliance); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()