from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): items: list; status: str; def validate_items(state: ProcureState): return {'status': 'validated' if state.get('items') else 'error'}; def finalize_procurement(state: ProcureState): return {'status': 'finalized'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_items); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()