from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): component_data: dict; validation_errors: List[str]; def validate_alloy(state: ProcState): state['validation_errors'] = [] if 'alloy_grade' not in state['component_data'] else []; return state; def check_pressure(state: ProcState): if state['component_data'].get('pressure_rating', 0) < 500: state['validation_errors'].append('Insufficient pressure rating'); return state; graph = StateGraph(ProcState); graph.add_node('validate_material', validate_alloy); graph.add_node('check_pressure', check_pressure); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_pressure'); graph.add_edge('check_pressure', END); graph = graph.compile()