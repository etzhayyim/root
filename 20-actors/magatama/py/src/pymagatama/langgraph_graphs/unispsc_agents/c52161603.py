from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class AdapterState(TypedDict): input_spec: dict; validation_results: list; status: str; def validate_adapter(state: AdapterState): results = []; if 'format' not in state['input_spec']: results.append('Missing tape format configuration'); return {'validation_results': results, 'status': 'validated' if not results else 'error'}; workflow = StateGraph(AdapterState); workflow.add_node('validate', validate_adapter); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()