from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RehapState(TypedDict): device_spec: dict; compliance_status: bool; workflow_log: Annotated[list, operator.add]; def validate_medical_grade(state: RehapState): return {'compliance_status': 'ISO13485' in state['device_spec'].get('certs', []), 'workflow_log': ['Compliance validated']}; def compile_procurement_graph(): graph = StateGraph(RehapState); graph.add_node('validate', validate_medical_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_procurement_graph()