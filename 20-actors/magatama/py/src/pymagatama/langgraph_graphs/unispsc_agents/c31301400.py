from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): material_spec: str; dimensions: dict; quality_report: str; approved: bool; def validate_forging(state: ForgingState): state['approved'] = 'ASTM' in state['material_spec'] and state.get('quality_report') is not None; return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_forging); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()