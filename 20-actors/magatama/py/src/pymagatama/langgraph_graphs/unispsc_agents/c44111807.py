from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScaleState(TypedDict): capacity: float; accuracy: str; validated: bool; def validate_scale(state: ScaleState) -> ScaleState: state['validated'] = state['capacity'] > 0 and state['accuracy'] != ''; return state; graph = StateGraph(ScaleState); graph.add_node('validate', validate_scale); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()