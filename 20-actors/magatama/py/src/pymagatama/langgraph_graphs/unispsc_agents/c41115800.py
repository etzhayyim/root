from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): analyzer_type: str; is_calibrated: bool; compliance_docs: List[str]; def check_compliance(state: AnalysisState): return {'compliance_docs': ['FDA_Clearance', 'CE_Mark'] if state['analyzer_type'] == 'clinical' else []}; def validate_device(state: AnalysisState): return {'is_calibrated': True} if 'calibration_cert' in state['compliance_docs'] else {'is_calibrated': False}; graph = StateGraph(AnalysisState); graph.add_node('compliance', check_compliance); graph.add_node('validation', validate_device); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'validation'); graph.add_edge('validation', END); graph = graph.compile()