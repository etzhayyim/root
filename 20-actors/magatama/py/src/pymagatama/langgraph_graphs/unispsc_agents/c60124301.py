from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ClayState(TypedDict): clay_specs: dict; validation_report: list[str]; graph = StateGraph(ClayState); def validate_clay(state: ClayState): specs = state['clay_specs']; errors = []; if specs.get('moisture') < 15: errors.append('Moisture too low'); return {'validation_report': errors}; graph.add_node('validate', validate_clay); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()