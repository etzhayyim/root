from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ArcheryState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ArcheryState): errors = []; if not state['specs'].get('material_type'): errors.append('Missing material_type'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(ArcheryState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()