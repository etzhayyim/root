from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec_data: dict; validation_result: bool; def validate_surgical_specs(state: State): state['validation_result'] = 'iso_13485' in state.get('spec_data', {}) and state.get('spec_data', {}).get('sterility') == True; return state; def check_regulatory_compliance(state: State): state['status'] = 'COMPLIANT' if state['validation_result'] else 'REJECTED'; return state; graph = StateGraph(State); graph.add_node('validate', validate_surgical_specs); graph.add_node('regulatory', check_regulatory_compliance); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); graph = graph.compile()