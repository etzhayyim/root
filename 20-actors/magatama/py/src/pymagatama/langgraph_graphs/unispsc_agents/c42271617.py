from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PercussorState(TypedDict): device_specs: dict; compliance_check: bool; graph = StateGraph(PercussorState); def validate_medical_cert(state: PercussorState): state['compliance_check'] = 'ISO13485' in state['device_specs'].get('certs', []); return state; def check_vibration(state: PercussorState): return {'compliance_check': state['device_specs'].get('freq', 0) > 0}; graph.add_node('validate_cert', validate_medical_cert); graph.add_node('check_freq', check_vibration); graph.set_entry_point('validate_cert'); graph.add_edge('validate_cert', 'check_freq'); graph.add_edge('check_freq', END); graph = graph.compile()