from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): specs: dict; validation_passed: bool; def validate_caliper(state: DentalState): state['validation_passed'] = 'material_composition' in state['specs'] and state['specs']['measurement_accuracy_mm'] < 0.05; return state; graph = StateGraph(DentalState); graph.add_node('validate', validate_caliper); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()