from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_specs: dict; compliance_status: bool; workflow: List[str]; def validate_chemicals(state: ProcureState): state['compliance_status'] = 'pH_level' in state['chemical_specs']; state['workflow'].append('chemical_check_complete'); return state; def finalize_order(state: ProcureState): state['workflow'].append('order_finalized'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_chemicals); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()