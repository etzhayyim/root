from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VaccineState(TypedDict): cold_chain_validated: bool; inventory_check: bool; compliance_status: str; def validate_cold_chain(state: VaccineState): return {'cold_chain_validated': True}; def check_inventory(state: VaccineState): return {'inventory_check': True}; def verify_compliance(state: VaccineState): return {'compliance_status': 'verified'}; graph = StateGraph(VaccineState); graph.add_node('cold_chain', validate_cold_chain); graph.add_node('inventory', check_inventory); graph.add_node('compliance', verify_compliance); graph.set_entry_point('cold_chain'); graph.add_edge('cold_chain', 'inventory'); graph.add_edge('inventory', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()