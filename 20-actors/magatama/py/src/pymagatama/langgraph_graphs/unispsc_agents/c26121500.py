from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WireState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: WireState): specs = state['specs']; required = ['voltage_rating', 'conductor_material']; state['valid'] = all(k in specs for k in required) and specs.get('voltage_rating', 0) > 0; return state; graph = StateGraph(WireState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()