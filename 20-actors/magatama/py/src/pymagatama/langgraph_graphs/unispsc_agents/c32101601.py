from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RAMState(TypedDict): capacity: int; specs: dict; valid: bool; def validate_specs(state: RAMState) -> RAMState: state['valid'] = state['specs'].get('speed', 0) > 0; return state; graph = StateGraph(RAMState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()