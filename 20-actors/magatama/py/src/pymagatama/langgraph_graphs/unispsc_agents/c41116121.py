from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HematologyState(TypedDict): reagent_data: dict; validation_checks: List[str]; def validate_reagent(state: HematologyState): storage = state['reagent_data'].get('temp'); is_valid = 2 <= storage <= 8; state['validation_checks'].append('temp_ok' if is_valid else 'temp_fail'); return state; graph = StateGraph(HematologyState); graph.add_node('validate', validate_reagent); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()