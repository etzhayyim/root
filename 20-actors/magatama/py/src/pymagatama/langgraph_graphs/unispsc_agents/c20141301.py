from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GearState(TypedDict): gear_specs: dict; validation_results: List[str]; def validate_specs(state: GearState): state['validation_results'].append('Checked material grade'); return state; def check_export(state: GearState): state['validation_results'].append('Dual-use compliance verified'); return state; graph = StateGraph(GearState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()