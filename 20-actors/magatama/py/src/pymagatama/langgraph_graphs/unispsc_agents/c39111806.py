from langgraph.graph import StateGraph, END; from typing import TypedDict; class LightBoxState(TypedDict): specs: dict; valid: bool; def validate_specs(state: LightBoxState): brightness = state['specs'].get('luminance', 0); state['valid'] = brightness > 500; return state; graph = StateGraph(LightBoxState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()