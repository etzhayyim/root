from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmbalmingState(TypedDict): chemical_data: dict; compliance_report: List[str]; approved: bool; def check_toxicity(state: EmbalmingState): state['compliance_report'].append('Toxicity Verified'); return {'approved': True} if 'toxic_limit' in state['chemical_data'] else {'approved': False}; def validate_sds(state: EmbalmingState): state['compliance_report'].append('SDS validated'); return {'approved': True}; graph = StateGraph(EmbalmingState); graph.add_node('toxicity_check', check_toxicity); graph.add_node('sds_check', validate_sds); graph.set_entry_point('toxicity_check'); graph.add_edge('toxicity_check', 'sds_check'); graph.add_edge('sds_check', END); graph = graph.compile()