from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SterilizationState(TypedDict): pouch_type: str; compliance_docs: List[str]; approved: bool; def validate_pouch(state: SterilizationState): state['approved'] = 'ISO_11607' in state['compliance_docs']; return state; graph = StateGraph(SterilizationState); graph.add_node('validate', validate_pouch); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()