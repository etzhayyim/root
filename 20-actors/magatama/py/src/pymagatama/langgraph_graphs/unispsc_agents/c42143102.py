from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): device_specs: dict; compliance_status: bool; workflow_log: Annotated[list, operator.add]; def validate_medical_device(state: ProcureState): is_valid = all(key in state['device_specs'] for key in ['ISO_13485', 'Regulatory_ID']); return {'compliance_status': is_valid, 'workflow_log': ['Device validation sequence completed']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()