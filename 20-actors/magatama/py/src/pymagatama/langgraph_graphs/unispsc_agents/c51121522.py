from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_data: dict; compliance_status: bool; validation_log: list[str]; def validate_purity(state: PharmaState): state['validation_log'].append('Validating purity'); return {'compliance_status': state['chemical_data'].get('purity', 0) >= 99.0}; def check_safety(state: PharmaState): state['validation_log'].append('Checking safety protocols'); return {'compliance_status': True}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()