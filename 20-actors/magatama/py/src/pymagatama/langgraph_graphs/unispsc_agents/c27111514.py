from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class GlassCutterState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(GlassCutterState); def validate_spec(state: GlassCutterState): specs = state.get('specs', {}); errors = []; if specs.get('thickness', 0) <= 0: errors.append('Invalid thickness value'); return {'validation_report': errors}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()