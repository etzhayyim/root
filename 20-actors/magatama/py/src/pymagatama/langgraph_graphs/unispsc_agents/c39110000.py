from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LightingState(TypedDict): specs: dict; validation_errors: list[str]; approved: bool; def validate_specs(state: LightingState): errors = []; if state['specs'].get('power_rating', 0) > 1000: errors.append('High voltage hazard'); return {'validation_errors': errors}; def finalize_procurement(state: LightingState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(LightingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()