from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoxOfficeState(TypedDict): dimensions: dict; material_spec: str; status: str; def validate_dimensions(state: BoxOfficeState): return {'status': 'validated'} if state.get('dimensions') else {'status': 'error'}; def finalize_procurement(state: BoxOfficeState): return {'status': 'processed'}; graph = StateGraph(BoxOfficeState); graph.add_node('validate', validate_dimensions); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()