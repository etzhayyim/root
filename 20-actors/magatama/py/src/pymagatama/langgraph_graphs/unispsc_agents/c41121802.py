from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabSupplyState(TypedDict): items: List[dict]; validation_results: List[str]; def validate_rod_specs(state: LabSupplyState): results = []; for item in state['items']: if item.get('material') not in ['Borosilicate Glass', 'PTFE']: results.append('Invalid Material'); state['validation_results'] = results; return state; graph = StateGraph(LabSupplyState); graph.add_node('validate', validate_rod_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()