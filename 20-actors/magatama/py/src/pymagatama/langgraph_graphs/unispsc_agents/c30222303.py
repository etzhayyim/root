from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): vendor: str; requirements: list; approved: bool; def validate_curriculum(state: State): return {'approved': len(state['requirements']) > 0}; def finalize_procurement(state: State): return {'approved': True}; graph = StateGraph(State); graph.add_node('validate', validate_curriculum); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()