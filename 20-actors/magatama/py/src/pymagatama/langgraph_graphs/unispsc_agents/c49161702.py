from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): data: dict; status: str; def validate_specs(state: State): specs = state['data'].get('specs', {}); return {'status': 'approved' if all(k in specs for k in ['material', 'length']) else 'rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()