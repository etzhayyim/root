from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DieselGraphState(TypedDict): vendor_docs: List[str]; inspection_status: str; graph = StateGraph(DieselGraphState); def validate_emission_compliance(state): return {'inspection_status': 'COMPLIANT' if 'ISO' in state['vendor_docs'] else 'REQUIRED_REVIEW'}; def finalize_procurement(state): return {'inspection_status': 'APPROVED'}; graph.add_node('val_emission', validate_emission_compliance); graph.add_node('final', finalize_procurement); graph.set_entry_point('val_emission'); graph.add_edge('val_emission', 'final'); graph.add_edge('final', END); app = graph.compile()