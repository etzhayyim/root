from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FDMState(TypedDict): model_specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_specs(state: FDMState): state['compliance_status'] = all(k in state['model_specs'] for k in ['build_vol', 'material']); state['workflow_log'].append('Specs validated'); return state; def check_export_controls(state: FDMState): state['workflow_log'].append('Export control check completed'); return state; graph = StateGraph(FDMState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()