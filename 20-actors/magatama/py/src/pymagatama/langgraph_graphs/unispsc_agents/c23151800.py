from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EngineProcState(TypedDict): engine_specs: dict; compliance_check: bool; status: str; def validate_specs(state: EngineProcState): return {'compliance_check': state['engine_specs'].get('power') > 0}; def finalize_procurement(state: EngineProcState): return {'status': 'Approved' if state['compliance_check'] else 'Rejected'}; graph = StateGraph(EngineProcState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()