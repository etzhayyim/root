from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_certs: List[str]; material_spec: str; validated: bool; def validate_compliance(state: ProcureState): return {'validated': 'ISO13485' in state['vendor_certs']}; def check_material(state: ProcureState): return {'validated': state['material_spec'] == 'medical_grade'}; workflow = StateGraph(ProcureState); workflow.add_node('cert_check', validate_compliance); workflow.add_node('material_check', check_material); workflow.add_edge('cert_check', 'material_check'); workflow.add_edge('material_check', END); workflow.set_entry_point('cert_check'); graph = workflow.compile()