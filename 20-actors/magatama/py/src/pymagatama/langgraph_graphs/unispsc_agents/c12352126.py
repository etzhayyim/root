from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalProcurementState(TypedDict): chemical_data: dict; compliance_checks: Annotated[Sequence[str], operator.add]; status: str; def validate_chemical_specs(state: ChemicalProcurementState): return {compliance_checks: [f'validated_specs_{state[chemical_data].get(id)}']}; def assess_export_risk(state: ChemicalProcurementState): return {status: 'RISK_ASSESSED'}; graph = StateGraph(ChemicalProcurementState); graph.add_node(validate, validate_chemical_specs); graph.add_node(assess_risk, assess_export_risk); graph.set_entry_point(validate); graph.add_edge(validate, assess_risk); graph.add_edge(assess_risk, END); graph = graph.compile()