from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MicrowaveState(TypedDict): hardware_specs: dict; compliance_status: bool; validation_log: list[str]; graph = StateGraph(MicrowaveState); def validate_frequency(state: MicrowaveState): state['validation_log'].append('Validating frequency range'); return state; def check_compliance(state: MicrowaveState): state['compliance_status'] = True if state['hardware_specs'].get('eccn') else False; return state; graph.add_node('validate', validate_frequency); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()