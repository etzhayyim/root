from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): belt_specs: dict; validation_results: List[str]; def validate_durability(state: ToolState): state['validation_results'].append('Durability Check Passed'); return state; def validate_load(state: ToolState): state['validation_results'].append('Load Capacity Verified'); return state; graph = StateGraph(ToolState); graph.add_node('durability', validate_durability); graph.add_node('load', validate_load); graph.add_edge('durability', 'load'); graph.add_edge('load', END); graph.set_entry_point('durability'); graph = graph.compile()