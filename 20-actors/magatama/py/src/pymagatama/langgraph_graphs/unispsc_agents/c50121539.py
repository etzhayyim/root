from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FishState(TypedDict): species: str; temp: float; status: str; def validate_temp(state: FishState): return {'status': 'approved' if state['temp'] <= 4.0 else 'rejected'}; def finalize_order(state: FishState): return {'status': 'finalized'}; graph = StateGraph(FishState); graph.add_node('validate', validate_temp); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()