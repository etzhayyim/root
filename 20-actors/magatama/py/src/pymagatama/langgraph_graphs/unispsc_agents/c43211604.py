from langgraph.graph import StateGraph, END; from typing import TypedDict; class SwitchState(TypedDict): device_specs: dict; validation_results: list; status: str; def validate_specs(state: SwitchState): state['validation_results'].append('Checking connector compatibility'); return {'status': 'validated'}; def finalize_workflow(state: SwitchState): return {'status': 'ready'}; graph = StateGraph(SwitchState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()