from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: PumpState) -> PumpState: state['valid'] = all(k in state['specs'] for k in ['material', 'diameter']); state['log'].append('Validation completed'); return state; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()