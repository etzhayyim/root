from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): commodity: str; specs: dict; approved: bool; def check_compliance(state: State): state['approved'] = 'MedicalDeviceLicenseNumber' in state['specs']; return state; def validate_physics(state: State): state['approved'] = state['approved'] and state['specs'].get('FlowResistanceRange') is not None; return state; graph = StateGraph(State); graph.add_node('compliance', check_compliance); graph.add_node('physics', validate_physics); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'physics'); graph.add_edge('physics', END); graph.compile()