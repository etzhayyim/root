from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): component_data: dict; validation_results: List[str]; def validate_geometry(state: State): state['validation_results'].append('Geometry validated'); return state; def check_export_compliance(state: State): state['validation_results'].append('Export compliance verified'); return state; graph = StateGraph(State); graph.add_node('validate', validate_geometry); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()