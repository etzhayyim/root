from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): pump_specs: dict; validation_results: List[str]; def validate_specs(state: PumpState): specs = state['pump_specs']; results = []; if specs.get('power', 0) > 50: results.append('Needs industrial voltage check'); if not specs.get('oil_free', False): results.append('Hazardous waste disposal required'); return {'validation_results': results}; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()