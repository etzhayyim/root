from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_results: dict; graph = StateGraph(State); def validate_specs(state: State): state['validation_results'] = {'passed': True}; return state; def check_regulatory(state: State): state['validation_results']['regulatory'] = 'approved'; return state; graph.add_node('spec_validation', validate_specs); graph.add_node('regulatory_check', check_regulatory); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', 'regulatory_check'); graph.add_edge('regulatory_check', END); graph = graph.compile()