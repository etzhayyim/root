from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachiningState(TypedDict): material_spec: str; tolerance: float; validated: bool; def validate_specs(state: MachiningState): state['validated'] = state['tolerance'] < 0.05; return state; graph = StateGraph(MachiningState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()