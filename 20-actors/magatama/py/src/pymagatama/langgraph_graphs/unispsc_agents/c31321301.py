from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; materials: List[str]; verified: bool; graph = StateGraph(AssemblyState); def validate_specs(state: AssemblyState): return { 'verified': True }; def check_compliance(state: AssemblyState): return { 'verified': all(m == 'Aluminum-6061' for m in state.get('materials', [])) }; graph.add_node('Validate', validate_specs); graph.add_node('Compliance', check_compliance); graph.set_entry_point('Validate'); graph.add_edge('Validate', 'Compliance'); graph.add_edge('Compliance', END); graph.compile()