from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicroscopeState(TypedDict): specs: dict; compliance: bool; approved: bool; def check_specs(state: MicroscopeState): state['compliance'] = 'Resolution' in state['specs'] and state['specs']['Resolution'] < 5.0; return state; def final_approval(state: MicroscopeState): state['approved'] = state['compliance']; return state; graph = StateGraph(MicroscopeState); graph.add_node('validate', check_specs); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()