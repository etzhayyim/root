from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ValveState(TypedDict): valve_id: str; spec_data: dict; validation_errors: List[str]; graph = StateGraph(ValveState); def validate_specs(state: ValveState): errors = []; if not state['spec_data'].get('pressure_rating'): errors.append('Missing pressure rating'); state['validation_errors'] = errors; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()