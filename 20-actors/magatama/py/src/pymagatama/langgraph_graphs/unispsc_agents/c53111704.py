from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): data: dict; compliance_status: bool; def validate_specs(state: State): specs = state['data'].get('specs', {}); state['compliance_status'] = 'material' in specs and 'size' in specs; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()