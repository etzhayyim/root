from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class GearSpecState(TypedDict): gear_code: str; specs: Dict; validation_log: List[str]; def validate_material_grade(state: GearSpecState): state['validation_log'].append('Validating material grade compatibility.'); return state; def check_precision_iso(state: GearSpecState): state['validation_log'].append('Checking ISO accuracy class requirements.'); return state; def assemble_procurement_package(state: GearSpecState): state['validation_log'].append('Finalizing technical procurement documentation.'); return state; graph = StateGraph(GearSpecState); graph.add_node('validate_material', validate_material_grade); graph.add_node('check_iso', check_precision_iso); graph.add_node('finalize', assemble_procurement_package); graph.add_edge('validate_material', 'check_iso'); graph.add_edge('check_iso', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate_material'); graph = graph.compile()