from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GearState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: GearState): errors = []; if state['specs'].get('module', 0) <= 0: errors.append('Invalid module'); return {'validation_errors': errors}; def check_compliance(state: GearState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(GearState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()