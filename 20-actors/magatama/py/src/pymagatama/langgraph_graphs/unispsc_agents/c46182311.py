from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BelaySpecState(TypedDict): device_type: str; specs: dict; compliant: bool; def validate_specs(state: BelaySpecState): specs = state['specs']; state['compliant'] = all([specs.get('uiaa_certified'), specs.get('tensile_strength') > 20]); return state; graph = StateGraph(BelaySpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()