from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_purity(state: PharmaState): return {'validated': 'COA_Attached' in state['certs']}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()