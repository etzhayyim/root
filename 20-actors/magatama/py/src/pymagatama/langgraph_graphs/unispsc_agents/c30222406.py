from langgraph.graph import StateGraph, END; from typing import TypedDict; class ScreeningRoomState(TypedDict): specs: dict; ready: bool; def validate_specs(state: ScreeningRoomState): return {'ready': all(k in state['specs'] for k in ['STC', 'Lumen'])}; graph = StateGraph(ScreeningRoomState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()