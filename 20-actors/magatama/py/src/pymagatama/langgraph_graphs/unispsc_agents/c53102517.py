from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TieHolderState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: TieHolderState): errors = []; if state['specs'].get('capacity', 0) <= 0: errors.append('Capacity must be positive'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(TieHolderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()