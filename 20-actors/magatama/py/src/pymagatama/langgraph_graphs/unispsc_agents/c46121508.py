from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SAMState(TypedDict): target_coords: dict; security_clearance: bool; telemetry: dict; graph = StateGraph(SAMState); def validate_threat(state: SAMState): return {'security_clearance': True} if state.get('target_coords') else {'security_clearance': False}; def execute_launch(state: SAMState): return {'telemetry': {'status': 'locked', 'bearing': 0}}; graph.add_node('validate', validate_threat); graph.add_node('execute', execute_launch); graph.set_entry_point('validate'); graph.add_edge('validate', 'execute'); graph.add_edge('execute', END); graph.compile()