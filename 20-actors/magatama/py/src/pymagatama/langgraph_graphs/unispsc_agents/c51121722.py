from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_id: str; purity: float; compliance_docs: List[str]; approved: bool; def validate_purity(state: PharmState): state['approved'] = state['purity'] >= 99.0; return state; def verify_docs(state: PharmState): state['approved'] = state['approved'] and len(state['compliance_docs']) >= 2; return state; graph = StateGraph(PharmState); graph.add_node('purity_check', validate_purity); graph.add_node('doc_check', verify_docs); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'doc_check'); graph.add_edge('doc_check', END); graph = graph.compile()