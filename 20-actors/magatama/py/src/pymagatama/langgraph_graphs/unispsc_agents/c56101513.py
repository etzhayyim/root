from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validation_status: bool; def validate_cot_specs(state: ProcurementState): state['validation_status'] = all(['material' in item for item in state['items']]); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_cot_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()