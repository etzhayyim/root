from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ConduitState(TypedDict): specs: dict; valid: bool; """ Define workflow """ graph = StateGraph(ConduitState); def validate_specs(state: ConduitState): state['valid'] = 'Material specification' in state['specs'] and 'IP rating' in state['specs']; return state; def report_status(state: ConduitState): print(f"Validation status: {state['valid']}"); return state; graph.add_node("validate", validate_specs); graph.add_node("report", report_status); graph.set_entry_point("validate"); graph.add_edge("validate", "report"); graph.add_edge("report", END); graph = graph.compile()