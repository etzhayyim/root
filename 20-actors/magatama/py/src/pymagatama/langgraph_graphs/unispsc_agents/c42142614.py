from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InjectionState(TypedDict): items: List[str]; validation_status: bool; def val_step(state: InjectionState): state['validation_status'] = all(['sterile' in x.lower() for x in state['items']]); return state; graph = StateGraph(InjectionState); graph.add_node('validate', val_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()