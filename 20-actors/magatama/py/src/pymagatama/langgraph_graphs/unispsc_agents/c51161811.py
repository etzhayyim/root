from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity: float; compliance_met: bool; def validate_purity(state: PharmaState): return {'compliance_met': state['purity'] >= 99.0}; def check_regulations(state: PharmaState): return {'compliance_met': True} if state['compliance_met'] else {'compliance_met': False}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('regulate', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulate'); graph.add_edge('regulate', END); graph = graph.compile()