from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChemKitState(TypedDict): kit_id: str; materials: List[str]; safety_approved: bool; def validate_materials(state: ChemKitState): return {'safety_approved': all(m not in ['restricted_toxic'] for m in state['materials'])}; def route_processing(state: ChemKitState): return 'approve' if state['safety_approved'] else 'flag'; graph = StateGraph(ChemKitState); graph.add_node('validate', validate_materials); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()