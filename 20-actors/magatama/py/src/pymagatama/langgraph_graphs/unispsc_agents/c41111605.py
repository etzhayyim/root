from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StrainGraphState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: StrainGraphState): results = []; factors = state['specs'].get('gauge_factor'); if not factors: results.append('Missing gauge factor'); return {'validation_results': results}; def generate_report(state: StrainGraphState): return {'validation_results': state['validation_results'] + ['Report Generated']}; graph = StateGraph(StrainGraphState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()