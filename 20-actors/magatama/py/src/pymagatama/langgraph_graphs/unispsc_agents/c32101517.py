from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CouplerState(TypedDict): specs: dict; validation_report: list[str]; def validate_specs(state: CouplerState): report = []; r = state['specs'].get('ratio', 0); return {'validation_report': ['Ratio within range'] if 0 < r < 100 else ['Invalid Ratio']}; graph = StateGraph(CouplerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()