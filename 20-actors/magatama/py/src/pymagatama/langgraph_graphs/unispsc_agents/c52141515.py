from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_specs: dict; compliance_check: bool; final_report: str; def validate_compaction(state: ProcureState): state['compliance_check'] = state['vendor_specs'].get('compaction_ratio', 0) > 3; return state; def generate_summary(state: ProcureState): state['final_report'] = 'Compactor compliant' if state['compliance_check'] else 'Compactor non-compliant'; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_compaction); graph.add_node('summary', generate_summary); graph.set_entry_point('validate'); graph.add_edge('validate', 'summary'); graph.add_edge('summary', END); compiled_graph = graph.compile()