from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GlueState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: GlueState): state['compliant'] = state['specs'].get('is_acid_free', False) and state['specs'].get('non_toxic', False); return state; graph = StateGraph(GlueState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()