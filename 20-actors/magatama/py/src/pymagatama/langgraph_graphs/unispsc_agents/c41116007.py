from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToxicologyState(TypedDict): reagent_id: str; purity_check: bool; safety_compliance: bool; def validate_purity(state: ToxicologyState): state['purity_check'] = True; return state; def verify_safety(state: ToxicologyState): state['safety_compliance'] = True; return state; graph_builder = StateGraph(ToxicologyState); graph_builder.add_node('purity_check', validate_purity); graph_builder.add_node('safety_verification', verify_safety); graph_builder.set_entry_point('purity_check'); graph_builder.add_edge('purity_check', 'safety_verification'); graph_builder.add_edge('safety_verification', END); graph = graph_builder.compile()