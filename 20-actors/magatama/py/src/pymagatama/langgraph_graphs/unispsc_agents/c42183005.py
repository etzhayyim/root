from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): data: dict; valid: bool; def validate_calib(state: State): state['valid'] = 'calibration_date' in state['data']; return state; def report_status(state: State): print(f'Validation status: {state['valid']}'); return state; graph = StateGraph(State); graph.add_node('validate', validate_calib); graph.add_node('report', report_status); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()