from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; compliance_checks: Annotated[list, operator.add]; status: str; def validate_software_requirements(state: State): return {'compliance_checks': ['license_verification', 'data_privacy_audit'], 'status': 'validated'}; def finalize_procurement(state: State): return {'status': 'approved'}; graph = StateGraph(State); graph.add_node('validate', validate_software_requirements); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()