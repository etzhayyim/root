from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CanContainerState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_can_spec(state: CanContainerState): specs = state['specs']; errors = []; if specs.get('material') not in ['aluminum', 'steel']: errors.append('Invalid material'); state['valid'] = len(errors) == 0; state['error_log'] = errors; return state; graph = StateGraph(CanContainerState); graph.add_node('validate', validate_can_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()