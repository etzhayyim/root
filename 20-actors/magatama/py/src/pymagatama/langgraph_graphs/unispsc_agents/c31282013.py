from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_spec(state: ProcState): if 'Dimensional Tolerance' not in state['specs']: state['validation_errors'].append('Tolerance missing'); return state; def check_dual_use(state: ProcState): if state['specs'].get('Material') == 'Advanced Polymer': state['approved'] = False; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_dual_use); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()