from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PliersState(TypedDict): spec_data: dict; validation_errors: List[str]; def validate_spec(state: PliersState): errors = []; if state['spec_data'].get('hardness', 0) < 45: errors.append('Hardness too low for industrial use'); return {'validation_errors': errors}; graph = StateGraph(PliersState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()