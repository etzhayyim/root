from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): component_data: dict; verification_required: bool; security_cleared: bool; def validate_spec(state: ProcureState): state['verification_required'] = True; return state; def check_export_control(state: ProcureState): state['security_cleared'] = True; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()