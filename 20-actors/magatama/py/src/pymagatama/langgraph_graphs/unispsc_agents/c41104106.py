from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PhlebotomyState(TypedDict): tray_specs: dict; compliance_verified: bool; def validate_specs(state: PhlebotomyState): state['compliance_verified'] = 'ISO 13485' in state['tray_specs'].get('certifications', []); return state; def route_verification(state: PhlebotomyState): return 'valid' if state['compliance_verified'] else 'review'; graph = StateGraph(PhlebotomyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()