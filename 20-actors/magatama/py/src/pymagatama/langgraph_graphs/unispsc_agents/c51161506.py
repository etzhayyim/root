from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): medicine_name: str; validation_passed: bool; certs: List[str]; def validate_medicine(state: ProcurementState): state['validation_passed'] = 'GMP' in state['certs']; return state; builder = StateGraph(ProcurementState); builder.add_node('validate', validate_medicine); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()