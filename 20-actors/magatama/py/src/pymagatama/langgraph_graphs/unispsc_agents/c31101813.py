from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: CastState): return {'validation_results': ['Dimensions Verified']}; def validate_material(state: CastState): return {'validation_results': ['Alloy Composition Compliant']}; graph = StateGraph(CastState); graph.add_node('dim_check', validate_dimensions); graph.add_node('mat_check', validate_material); graph.add_edge('dim_check', 'mat_check'); graph.add_edge('mat_check', END); graph.set_entry_point('dim_check'); graph = graph.compile()