from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SterilizationState(TypedDict): tray_specs: dict; compliance_passed: bool; def validate_specs(state: SterilizationState): state['compliance_passed'] = 'heat_rating' in state['tray_specs'] and state['tray_specs']['heat_rating'] >= 134; return state; def route_approval(state: SterilizationState): return 'approved' if state['compliance_passed'] else 'rejected'; graph = StateGraph(SterilizationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_approval, {'approved': END, 'rejected': END}); graph = graph.compile()