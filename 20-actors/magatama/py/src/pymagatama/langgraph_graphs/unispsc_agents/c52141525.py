from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HotPlateState(TypedDict): specs: dict; compliance: bool; validated: bool; def validate_specs(state: HotPlateState): state['compliance'] = state['specs'].get('safety_certification_id') is not None; return state; def check_thermal(state: HotPlateState): state['validated'] = state['compliance'] and state['specs'].get('thermal_cutout_rating', 0) > 200; return state; graph = StateGraph(HotPlateState); graph.add_node('validate', validate_specs); graph.add_node('thermal_check', check_thermal); graph.add_edge('validate', 'thermal_check'); graph.add_edge('thermal_check', END); graph.set_entry_point('validate'); graph = graph.compile()