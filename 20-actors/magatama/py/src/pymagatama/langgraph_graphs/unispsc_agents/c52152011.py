from langgraph.graph import StateGraph, END; from typing import TypedDict; class FlaskInnerOrder(TypedDict): specs: dict; verified: bool; def check_thermal_integrity(state: FlaskInnerOrder): state['verified'] = 'thermal_conductivity' in state['specs'] and state['specs']['thermal_conductivity'] < 0.05; return state; graph = StateGraph(FlaskInnerOrder); graph.add_node('validate_thermal', check_thermal_integrity); graph.set_entry_point('validate_thermal'); graph.add_edge('validate_thermal', END); graph = graph.compile()