from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class JuiceState(TypedDict): ingredients: List[str]; inspection_passed: bool; graph = StateGraph(JuiceState); def validate_quality(state): return {'inspection_passed': True} if 'tangelo' in state['ingredients'] else {'inspection_passed': False}; def check_temp(state): return {'status': 'validated'} if state.get('inspection_passed') else {'status': 'failed'}; graph.add_node('validate', validate_quality); graph.add_node('storage', check_temp); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); app = graph.compile()