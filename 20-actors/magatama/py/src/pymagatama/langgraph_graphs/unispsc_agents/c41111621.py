from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CaliperState(TypedDict): specs: dict; validation_report: list[str]; def validate_accuracy(state: CaliperState): state['validation_report'].append('ISO 13385 compliance checked'); return state; def check_calibration(state: CaliperState): if state['specs'].get('calibration_certificate_required'): state['validation_report'].append('Certification verified'); return state; graph = StateGraph(CaliperState); graph.add_node('validate', validate_accuracy); graph.add_node('certify', check_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); app = graph.compile()