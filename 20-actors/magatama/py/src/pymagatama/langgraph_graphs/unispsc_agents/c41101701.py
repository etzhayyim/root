from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): code: str; specs: dict; verified: bool; def validate_specs(state: ProcureState): state['verified'] = all(k in state['specs'] for k in ['motor_power_w', 'safety_certification_standards']); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()