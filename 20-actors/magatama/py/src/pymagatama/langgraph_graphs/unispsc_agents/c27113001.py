from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BrushState(TypedDict): brush_type: str; specs: dict; approved: bool; def validate_specs(state: BrushState): state['approved'] = all(k in state['specs'] for k in ['bristle_material', 'handle_material']); return state; graph = StateGraph(BrushState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()