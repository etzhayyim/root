from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): input_data: dict; validation_results: List[str]; status: str; def validate_sensor(state: GraphState): state['validation_results'].append('Sensor calibration verified'); return state; def check_compliance(state: GraphState): state['validation_results'].append('Regulatory standards met'); return state; graph = StateGraph(GraphState); graph.add_node('validate_sensor', validate_sensor); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_sensor'); graph.add_edge('validate_sensor', 'check_compliance'); graph.add_edge('check_compliance', END); 