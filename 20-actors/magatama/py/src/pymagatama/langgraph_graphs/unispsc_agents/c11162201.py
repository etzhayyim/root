from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): material_id: str; purity: float; status: str; def validate_purity(state: MaterialState): if state['purity'] >= 0.99: return 'approve' else: return 'reject'; def process_node(state: MaterialState): return {'status': 'processed'}; graph = StateGraph(MaterialState); graph.add_node('process', process_node); graph.set_entry_point('process'); graph.add_edge('process', END); graph = graph.compile()