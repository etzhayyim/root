from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: MachineState): return {'valid': state['specs'].get('power') > 0, 'logs': ['Spec verified']}; def safety_check(state: MachineState): return {'logs': ['Safety interlock confirmed']}; graph = StateGraph(MachineState); graph.add_node('validate', validate_specs); graph.add_node('safety', safety_check); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()