from langgraph.graph import StateGraph, END; from typing import TypedDict; class DrugState(TypedDict): chemical_name: str; purity_level: float; compliance_status: bool; def validate_purity(state: DrugState): state['compliance_status'] = state['purity_level'] >= 0.99; return state; def check_regulatory(state: DrugState): return {'compliance_status': True if state['chemical_name'] == 'Tolbutamide' else False}; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'validate'); graph.add_edge('validate', END); compile_graph = graph.compile()