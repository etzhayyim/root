from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BatteryState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(BatteryState); def validate_chemistry(state: BatteryState): state['validation_results'].append('Verifying Cadmium compliance...'); return state; def check_hazmat(state: BatteryState): state['validation_results'].append('Hazmat classification complete.'); return state; graph.add_node('chemistry', validate_chemistry); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('chemistry'); graph.add_edge('chemistry', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()