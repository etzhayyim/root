from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): metal_type: str; purity: float; specs_verified: bool; graph = StateGraph(ForgingState); def validate_purity(state: ForgingState): return {'specs_verified': state['purity'] >= 0.999}; def audit_origin(state: ForgingState): return {'specs_verified': True}; graph.add_node('validate_purity', validate_purity); graph.add_node('audit_origin', audit_origin); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'audit_origin'); graph.add_edge('audit_origin', END); graph = graph.compile()