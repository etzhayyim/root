from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): data: dict; status: str; def validate_specs(state: GraphState): is_valid = 'tolerance' in state['data'] and 'material' in state['data']; return {'status': 'validated' if is_valid else 'error'}; def export_to_erp(state: GraphState): print('Exporting to ERP...'); return {'status': 'exported'}; graph = StateGraph(GraphState); graph.add_node('validate', validate_specs); graph.add_node('export', export_to_erp); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()