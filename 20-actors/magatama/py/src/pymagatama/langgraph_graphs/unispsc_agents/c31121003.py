from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimension(state: CastingState): state['validation_results'].append('Dimension check passed') if 'geometry' in state['specs'] else None; return state; def check_material_cert(state: CastingState): state['validation_results'].append('Material certification verified') if 'material_grade' in state['specs'] else None; return state; graph = StateGraph(CastingState); graph.add_node('dim_check', validate_dimension); graph.add_node('mat_check', check_material_cert); graph.add_edge('dim_check', 'mat_check'); graph.add_edge('mat_check', END); graph.set_entry_point('dim_check'); graph = graph.compile()