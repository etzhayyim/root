from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class JuiceState(TypedDict): specs: dict; valid: bool; audit_logs: List[str]; def validate_juice(state: JuiceState): state['valid'] = all(k in state['specs'] for k in ['Brix_level', 'pH_value']); state['audit_logs'].append('Validation completed'); return state; graph = StateGraph(JuiceState); graph.add_node('validate', validate_juice); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()