from langgraph.graph import StateGraph, END; from typing import TypedDict; class LabProcurementState(TypedDict): cork_specs: dict; validation_report: str; def validate_cork_specs(state: LabProcurementState): specs = state['cork_specs']; report = 'Validated' if 'diameter' in specs and 'material' in specs else 'Invalid'; return {'validation_report': report}; graph = StateGraph(LabProcurementState); graph.add_node('validator', validate_cork_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()