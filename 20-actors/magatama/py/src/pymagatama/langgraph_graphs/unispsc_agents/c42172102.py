from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CPRState(TypedDict): vendor_certs: List[str]; safety_compliance: bool; status: str; def validate_cert(state: CPRState): return {'safety_compliance': 'ISO_13485' in state['vendor_certs']}; def assess_logistics(state: CPRState): return {'status': 'Approved' if state['safety_compliance'] else 'Rejected'}; graph = StateGraph(CPRState); graph.add_node('validate', validate_cert); graph.add_node('logistics', assess_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); compiled_graph = graph.compile()