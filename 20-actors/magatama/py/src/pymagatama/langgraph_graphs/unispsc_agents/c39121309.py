from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): spec: dict; audit_trail: List[str]; def validate_nema(state: SpecState): state['audit_trail'].append('Validating NEMA rating compliance'); return state; def check_certifications(state: SpecState): state['audit_trail'].append('Checking UL/IEC certification validity'); return state; graph = StateGraph(SpecState); graph.add_node('nema_check', validate_nema); graph.add_node('cert_check', check_certifications); graph.set_entry_point('nema_check'); graph.add_edge('nema_check', 'cert_check'); graph.add_edge('cert_check', END); graph = graph.compile()