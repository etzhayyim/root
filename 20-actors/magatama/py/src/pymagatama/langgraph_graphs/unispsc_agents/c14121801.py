from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class FilmProcState(TypedDict): code: str; specs: Dict[str, any]; validated: bool; next_step: str; def validate_specs(state: FilmProcState): print(f'Validating specs for {state[code]}'); state[validated] = True; return state; def route_processing(state: FilmProcState): return 'end'; graph = StateGraph(FilmProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()