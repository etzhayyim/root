from langgraph.graph import StateGraph, END; from typing import TypedDict; class PrintingMachineState(TypedDict): specs: dict; validation_results: list; graph = StateGraph(PrintingMachineState); def validate_spec(state: PrintingMachineState): return {'validation_results': ['compliant'] if state['specs'].get('registration_accuracy', 0) < 0.1 else ['non-compliant']}; def finalize_procurement(state: PrintingMachineState): return {'validation_results': state['validation_results'] + ['procurement_approved']}; graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()