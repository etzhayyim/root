from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): spec: dict; valid: bool; def validation_node(state: State): state['valid'] = 'material' in state['spec'] and 'dimensions' in state['spec']; return state; graph = StateGraph(State); graph.add_node('validate', validation_node); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()