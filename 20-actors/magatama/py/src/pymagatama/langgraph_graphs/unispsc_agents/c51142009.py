from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MetamizolState(TypedDict): vendor_gmp_status: bool; purity_level: float; compliance_doc: str; approved: bool; def check_gmp(state: MetamizolState): return {'approved': state['vendor_gmp_status'] and state['purity_level'] > 99.0}; graph = StateGraph(MetamizolState); graph.add_node('validate_gmp', check_gmp); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', END); graph = graph.compile()