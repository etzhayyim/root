from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): specs: dict; valid: bool; def val_specs(state: ProcState): state['valid'] = 'Max_RCF' in state['specs'] and 'Material' in state['specs']; return state; graph = StateGraph(ProcState); graph.add_node('validate', val_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()