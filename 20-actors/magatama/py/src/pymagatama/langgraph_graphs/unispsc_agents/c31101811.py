from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): alloy_code: str; specs: dict; approved: bool; def check_alloy(state: CastingState): state['approved'] = state['alloy_code'].startswith('C3'); return state; def validate_specs(state: CastingState): state['approved'] = state['approved'] and 'surface_roughness' in state['specs']; return state; graph = StateGraph(CastingState); graph.add_node('verify_material', check_alloy); graph.add_node('verify_specs', validate_specs); graph.set_entry_point('verify_material'); graph.add_edge('verify_material', 'verify_specs'); graph.add_edge('verify_specs', END); graph = graph.compile()