from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalSpecState(TypedDict): spec_data: dict; verification_status: str; def validate_material(state: DentalSpecState): state['verification_status'] = 'COMPLIANT' if state['spec_data'].get('material') == 'surgical_steel' else 'REJECT'; return state; graph = StateGraph(DentalSpecState); graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()