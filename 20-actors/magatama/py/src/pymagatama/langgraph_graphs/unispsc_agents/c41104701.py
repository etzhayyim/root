from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; specs: dict; valid: bool; def validate_specs(state: State): state['valid'] = 'Capacity' in state['specs'] and 'Temperature' in state['specs']; return state; def check_compliance(state: State): state['status'] = 'COMPLIANT' if state['valid'] else 'REJECTED'; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()