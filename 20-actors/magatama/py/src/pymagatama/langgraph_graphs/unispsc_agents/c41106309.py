from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class cDNAProcurementState(TypedDict): species: str; concentration: float; qc_passed: bool; storage_temp: int; graph = StateGraph(cDNAProcurementState); def validate_spec(state: cDNAProcurementState): return {'qc_passed': state['concentration'] > 0 and state['storage_temp'] <= -20}; graph.add_node('validation', validate_spec); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()