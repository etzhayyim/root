from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): chemical_data: dict; approval_status: bool; def validate_purity(state: ProcState): state['approval_status'] = state['chemical_data'].get('purity', 0) >= 99.0; return state; def check_regulatory(state: ProcState): return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_purity); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()