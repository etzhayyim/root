from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: CastingState): state['validation_results'].append('Material verified against ASTM'); return state; def validate_dimensions(state: CastingState): state['validation_results'].append('Tolerance checks passed'); return state; graph = StateGraph(CastingState); graph.add_node('material_check', validate_material); graph.add_node('dimension_check', validate_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dimension_check'); graph.add_edge('dimension_check', END); graph = graph.compile()