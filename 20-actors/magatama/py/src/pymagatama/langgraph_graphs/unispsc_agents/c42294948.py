from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(ProcState); def validate_spec(state: ProcState): return {'validation_results': {'passed': True}}; def process_procurement(state: ProcState): return {'items': ['mouthpiece_verified']}; graph.add_node('validate', validate_spec); graph.add_node('process', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()