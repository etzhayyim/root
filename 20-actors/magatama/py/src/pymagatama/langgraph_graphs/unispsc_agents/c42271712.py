from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; compliance_status: bool; workflow_log: Annotated[list, operator.add]; def validate_specs(state: State): status = 'ISO_13485' in state['input_data'].get('certs', []) and state['input_data'].get('airtightness', 0) > 95; return {'compliance_status': status, 'workflow_log': ['Spec validation complete']}; def finalize_procurement(state: State): return {'workflow_log': ['Procurement ready for approval']}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()