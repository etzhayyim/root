from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class SurveillanceState(TypedDict): device_specs: dict; compliance_check: bool; graph = StateGraph(SurveillanceState); def validate_security(state: SurveillanceState): state['compliance_check'] = 'FIPS' in state.get('device_specs', {}); return state; def finalize_order(state: SurveillanceState): return {'compliance_check': True}; graph.add_node('validate', validate_security); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()