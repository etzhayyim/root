from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input: str; validation_report: list[str]; def validate_purity(state: ProcureState): return {'validation_report': ['Purity check: OK']}; def check_compliance(state: ProcureState): return {'validation_report': ['Compliance: USP/EP verified']}; graph = StateGraph(ProcureState); graph.add_node('purity', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('purity'); graph.add_edge('purity', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()