from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class KitState(TypedDict): specs: dict; valid: bool; def validate_specs(state: KitState) -> dict: required = ['Voltage Rating', 'Compliance Standards']; state['valid'] = all(k in state['specs'] for k in required); return state; builder = StateGraph(KitState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()