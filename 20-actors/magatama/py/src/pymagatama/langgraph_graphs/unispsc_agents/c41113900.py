from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoilState(TypedDict): data: dict; status: str; def validate_sensors(state: SoilState): return {'status': 'validated' if 'sensors' in state['data'] else 'invalid'}; def finalize_procurement(state: SoilState): return {'status': 'approved'}; graph = StateGraph(SoilState); graph.add_node('validate', validate_sensors); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()