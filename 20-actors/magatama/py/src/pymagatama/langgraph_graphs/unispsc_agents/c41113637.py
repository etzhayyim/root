from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MeterState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: MeterState): spec = state.get('specs', {}); state['valid'] = 'Calibration Certificate' in spec and 'Accuracy Class' in spec; return state; graph = StateGraph(MeterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()