from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AmmunitionState(TypedDict): items: List[dict]; approval_required: bool; compliance_checked: bool; def validate_compliance(state: AmmunitionState): state['compliance_checked'] = True; return state; def check_export_control(state: AmmunitionState): state['approval_required'] = True; return state; graph = StateGraph(AmmunitionState); graph.add_node('validate', validate_compliance); graph.add_node('export', check_export_control); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph.set_entry_point('validate'); graph = graph.compile()