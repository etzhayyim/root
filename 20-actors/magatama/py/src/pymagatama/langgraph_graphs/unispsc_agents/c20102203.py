from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_specs(state: BearingState): results = []; if state['specs'].get('load_rating_dynamic_n', 0) <= 0: results.append('Invalid load rating'); return {'validation_results': results, 'status': 'rejected' if results else 'validated'}; graph = StateGraph(BearingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()