from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BathState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(BathState); def validate_specs(state: BathState): return {'validation_results': ['Material check passed', 'Temp range validated']}; def check_compliance(state: BathState): return {'validation_results': state['validation_results'] + ['ISO compliance verified']}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()