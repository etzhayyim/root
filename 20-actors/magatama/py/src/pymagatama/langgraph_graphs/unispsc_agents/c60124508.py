from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PlayEquipmentState(TypedDict): equipment_type: str; specs_verified: bool; safety_cert: str; graph = StateGraph(PlayEquipmentState); def validate_safety(state): state['specs_verified'] = 'ASTM' in state.get('safety_cert', ''); return state; def check_durability(state): return 'check_complete'; graph.add_node('validate', validate_safety); graph.add_node('durability', check_durability); graph.set_entry_point('validate'); graph.add_edge('validate', 'durability'); graph.add_edge('durability', END); graph = graph.compile()