from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(FastenerState); def validate_dim(state: FastenerState): state['validation_report'].append('Checking dimensional tolerances for dome nut profile.'); return state; def check_material(state: FastenerState): state['validation_report'].append('Verifying material certification (A2/A4).'); return state; graph.add_node('val_dim', validate_dim); graph.add_node('check_mat', check_material); graph.set_entry_point('val_dim'); graph.add_edge('val_dim', 'check_mat'); graph.add_edge('check_mat', END); compile_graph = graph.compile()