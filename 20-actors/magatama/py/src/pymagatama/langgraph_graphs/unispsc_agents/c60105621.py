from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EducationState(TypedDict): content: str; validated: bool; age_group: str; def validate_content(state: EducationState): return {'validated': 'drug_abuse' in state['content'] or 'tobacco' in state['content']}; def approve_material(state: EducationState): return {'validated': True}; graph = StateGraph(EducationState); graph.add_node('validate', validate_content); graph.add_node('approve', approve_material); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()