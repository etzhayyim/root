from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_items: List[str]; verified: bool; def validate_kit(state: KitState): return {'verified': len(state['kit_items']) > 0}; def finalize_procurement(state: KitState): return {'verified': True}; graph = StateGraph(KitState); graph.add_node('validate', validate_kit); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()