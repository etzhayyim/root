from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DistanceMeterState(TypedDict): vendor_specs: dict; is_calibrated: bool; needs_inspection: bool; def validate_specs(state: DistanceMeterState): state['is_calibrated'] = state['vendor_specs'].get('calibration_cert') is not None; return state; def inspection_logic(state: DistanceMeterState): state['needs_inspection'] = not state['is_calibrated']; return state; graph = StateGraph(DistanceMeterState); graph.add_node('validate', validate_specs); graph.add_node('inspect', inspection_logic); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()