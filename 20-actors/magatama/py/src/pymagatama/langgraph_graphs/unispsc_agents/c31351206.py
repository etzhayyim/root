from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: AssemblyState): state['valid'] = 'Pressure Rating PSI' in state['specs'] and state['specs']['Pressure Rating PSI'] > 0; state['log'].append('Validation complete'); return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()