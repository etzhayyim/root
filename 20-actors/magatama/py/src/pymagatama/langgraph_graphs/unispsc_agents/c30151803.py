from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SidingState(TypedDict): specs: dict; validation_report: list; approved: bool; def validate_specs(state: SidingState): report = []; if "material" not in state["specs"]: report.append("Missing material spec"); return {"validation_report": report, "approved": len(report) == 0}; graph = StateGraph(SidingState); graph.add_node("validate", validate_specs); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()