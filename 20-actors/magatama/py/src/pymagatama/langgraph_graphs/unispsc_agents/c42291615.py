from langgraph.graph import StateGraph, END; from typing import TypedDict; class SurgicalState(TypedDict): model: str; sterility_verified: bool; compliance_check: bool; def verify_sterility(state: SurgicalState): return {'sterility_verified': True}; def check_compliance(state: SurgicalState): return {'compliance_check': True}; graph = StateGraph(SurgicalState); graph.add_node('verify', verify_sterility); graph.add_node('compliance', check_compliance); graph.add_edge('verify', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('verify'); graph = graph.compile()