from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LipstickState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: LipstickState): state['compliance'] = all(k in state['specs'] for k in ['material', 'seal']); return state; def finalize_approval(state: LipstickState): state['status'] = 'Approved' if state['compliance'] else 'Rejected'; return state; graph = StateGraph(LipstickState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_approval); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()