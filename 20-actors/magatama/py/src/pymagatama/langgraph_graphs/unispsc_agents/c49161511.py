from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): ncaa_compliance: bool; material_spec: str; approved: bool; def validate_specs(state): state['approved'] = state['ncaa_compliance'] and ('carbon' in state['material_spec'].lower() or 'alloy' in state['material_spec'].lower()); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()