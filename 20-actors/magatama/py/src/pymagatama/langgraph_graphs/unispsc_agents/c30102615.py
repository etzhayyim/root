from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PlasticState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: PlasticState): results = []; m = state['specs'].get('material', ''); results.append('Passed' if m in ['PVC', 'PP', 'PE'] else 'Failed'); return {'validation_results': results}; graph = StateGraph(PlasticState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()