from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DressingState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: DressingState): state['compliance'] = 'sterilization' in state['specs'] and 'biocompatibility' in state['specs']; return state; def check_approval(state: DressingState): state['approved'] = state['compliance'] and state['specs'].get('expiry_date') is not None; return state; graph = StateGraph(DressingState); graph.add_node('validate', validate_specs); graph.add_node('check', check_approval); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.set_entry_point('validate'); graph = graph.compile()