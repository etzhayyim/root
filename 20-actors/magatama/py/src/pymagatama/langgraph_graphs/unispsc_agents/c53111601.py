from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShoeState(TypedDict): specs: List[str]; validation_results: dict; def validate_specs(state: ShoeState): return {'validation_results': {'status': 'passed', 'checks': ['material', 'sizing']}}; graph = StateGraph(ShoeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()