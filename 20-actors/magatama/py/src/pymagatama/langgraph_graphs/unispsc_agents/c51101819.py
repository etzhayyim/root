from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): chemical_data: dict; approval_status: bool; alerts: List[str]; def validate_purity(state: PharmState): purity = state['chemical_data'].get('purity', 0); state['approval_status'] = purity >= 99.0; return state if state['approval_status'] else {'alerts': ['Low purity detected']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph_app = graph.compile()