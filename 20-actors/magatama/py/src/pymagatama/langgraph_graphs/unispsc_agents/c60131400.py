from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PercussionState(TypedDict): vendor_specs: dict; validation_report: List[str]; def validate_instrument(state: PercussionState): material = state['vendor_specs'].get('material'); return {'validation_report': ['Material durability verified'] if material else ['Missing material info']}; graph = StateGraph(PercussionState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()