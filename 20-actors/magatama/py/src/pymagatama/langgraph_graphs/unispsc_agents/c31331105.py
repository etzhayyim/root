from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): component_id: str; structural_specs: dict; is_certified: bool; graph = StateGraph(AssemblyState); def validate_specs(state: AssemblyState): return {'is_certified': state['structural_specs'].get('tensile_strength', 0) > 400}; def route_processing(state: AssemblyState): return 'complete' if state['is_certified'] else 'flag_for_review'; graph.add_node('validation', validate_specs); graph.add_edge('validation', END); graph.set_entry_point('validation'); graph.compile()