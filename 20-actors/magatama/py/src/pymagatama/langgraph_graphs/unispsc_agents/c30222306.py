from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LanguageLabState(TypedDict): equipment_list: List[str]; validation_status: bool; def validate_specs(state: LanguageLabState): state['validation_status'] = len(state['equipment_list']) > 0; return state; graph = StateGraph(LanguageLabState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()