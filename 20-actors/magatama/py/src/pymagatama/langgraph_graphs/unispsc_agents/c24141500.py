from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SecuringState(TypedDict): items: List[str]; validated: bool; log: List[str]; def validate_supplies(state: SecuringState): return {'validated': all('cert' in item.lower() for item in state['items']), 'log': ['Validation complete']}; graph = StateGraph(SecuringState); graph.add_node('validate', validate_supplies); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()