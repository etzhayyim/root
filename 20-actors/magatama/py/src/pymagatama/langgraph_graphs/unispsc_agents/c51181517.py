from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_report: dict; compliance_status: bool; def validate_purity(state: PharmaState): return {'compliance_status': state['chemical_report'].get('purity', 0) >= 99.9}; def export_documentation(state: PharmaState): return {'compliance_status': True}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('docs', export_documentation); graph.set_entry_point('validate'); graph.add_edge('validate', 'docs'); graph.add_edge('docs', END); graph = graph.compile()