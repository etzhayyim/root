from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec: dict; validated: bool; state = ProcState(spec={'torque': 0}, validated=False); def validate_spec(s: ProcState): s['validated'] = s['spec'].get('torque', 0) > 0; return s; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()