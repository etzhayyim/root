from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SimulatorState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: SimulatorState): state['validation_report'] = 'Validation complete' if 'payload' in state['specs'] else 'Missing payload'; state['approved'] = 'payload' in state['specs']; return state; graph = StateGraph(SimulatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()