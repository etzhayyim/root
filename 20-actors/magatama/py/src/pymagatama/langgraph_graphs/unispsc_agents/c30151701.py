from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DownspoutState(TypedDict): specs: dict; ready: bool; def validate_specs(state: DownspoutState): state['ready'] = 'material_type' in state['specs'] and 'dimensions_mm' in state['specs']; return state; builder = StateGraph(DownspoutState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()