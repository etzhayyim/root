from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BeltState(TypedDict): belt_specs: dict; validation_results: List[str]; def validate_specs(state: BeltState): results = []; specs = state['belt_specs']; if specs.get('tensile_strength', 0) < 500: results.append('Low tensile strength'); return {'validation_results': results}; def generate_procurement_data(state: BeltState): return {'validation_results': state['validation_results'] + ['Data generated']}; graph = StateGraph(BeltState); graph.add_node('validate', validate_specs); graph.add_node('generate', generate_procurement_data); graph.set_entry_point('validate'); graph.add_edge('validate', 'generate'); graph.add_edge('generate', END); compiled_graph = graph.compile()