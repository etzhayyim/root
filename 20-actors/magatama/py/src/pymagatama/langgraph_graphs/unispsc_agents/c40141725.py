from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PlumbingState(TypedDict): spec: dict; validated: bool; errors: List[str]; def validate_load(state: PlumbingState): state['validated'] = state['spec'].get('load', 0) > 0; return state; def check_compliance(state: PlumbingState): state['validated'] = state['spec'].get('iso_cert', False); return state; graph = StateGraph(PlumbingState); graph.add_node('load_check', validate_load); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('load_check'); graph.add_edge('load_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()