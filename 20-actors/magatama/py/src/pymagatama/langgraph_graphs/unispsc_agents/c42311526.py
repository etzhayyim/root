from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validated_items: List[str]; def validate_sourcing(state: State): validated = [i for i in state['items'] if 'sterile' in i.lower()]; return {'validated_items': validated}; def check_compliance(state: State): print('Checking ISO 13485 compliance'); return {'validated_items': state['validated_items']}; graph = StateGraph(State); graph.add_node('validate', validate_sourcing); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()