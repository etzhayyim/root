from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DrainageState(TypedDict): items: List[str]; status: str; def validate_spec(state: DrainageState): return {'status': 'validated_for_medical_use'} if state['items'] else {'status': 'missing_specs'}; def execute_workflow(state: DrainageState): return {'status': 'procurement_ready'}; graph = StateGraph(DrainageState); graph.add_node('validate', validate_spec); graph.add_node('process', execute_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.compile()