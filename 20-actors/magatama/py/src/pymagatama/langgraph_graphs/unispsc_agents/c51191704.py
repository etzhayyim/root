from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): electrolyte_data: dict; validation_results: List[str]; def validate_purity(state: State): state['validation_results'].append('Purity Check Passed'); return state; def verify_storage(state: State): state['validation_results'].append('Cold Chain Verification Passed'); return state; graph = StateGraph(State); graph.add_node('validate_purity', validate_purity); graph.add_node('verify_storage', verify_storage); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'verify_storage'); graph.add_edge('verify_storage', END); graph = graph.compile()