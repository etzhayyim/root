from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalTripodState(TypedDict): specs: dict; compliance_status: bool; validation_log: List[str]; def validate_medical_standards(state: DentalTripodState): state['compliance_status'] = state['specs'].get('ISO_13485', False); return state; def check_load_capacity(state: DentalTripodState): if state['specs'].get('load_capacity', 0) > 10: state['validation_log'].append('Capacity verified'); return state; graph = StateGraph(DentalTripodState); graph.add_node('validate_standards', validate_medical_standards); graph.add_node('check_capacity', check_load_capacity); graph.set_entry_point('validate_standards'); graph.add_edge('validate_standards', 'check_capacity'); graph.add_edge('check_capacity', END); graph = graph.compile()