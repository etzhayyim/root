from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_id: str; purity: float; validated: bool; def validate_purity(state: ProcState): return { 'validated': state['purity'] >= 0.99 }; def storage_check(state: ProcState): return { 'validated': True }; graph = StateGraph(ProcState); graph.add_node('validate', validate_purity); graph.add_node('storage', storage_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph = graph.compile()