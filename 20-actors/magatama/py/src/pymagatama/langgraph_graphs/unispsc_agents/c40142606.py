from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeState(TypedDict): specs: dict; valid: bool; error: str; def validate_pressure(state: TubeState): state['valid'] = state['specs'].get('pressure', 0) > 0; return state; def check_compliance(state: TubeState): state['valid'] = state['specs'].get('cert') is not None; return state; graph = StateGraph(TubeState); graph.add_node('validate_pressure', validate_pressure); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_pressure'); graph.add_edge('validate_pressure', 'check_compliance'); graph.add_edge('check_compliance', END); graph = graph.compile()