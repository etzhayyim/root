from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TriamcinoloneState(TypedDict): batch_id: str; purity: float; is_gmp_compliant: bool; status: str; def validate_purity(state: TriamcinoloneState): return {'status': 'approved' if state['purity'] >= 99.0 else 'rejected'}; def check_compliance(state: TriamcinoloneState): return {'status': 'compliant' if state['is_gmp_compliant'] else 'non-compliant'}; graph = StateGraph(TriamcinoloneState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_compliance'); graph.add_edge('check_compliance', END); graph = graph.compile()