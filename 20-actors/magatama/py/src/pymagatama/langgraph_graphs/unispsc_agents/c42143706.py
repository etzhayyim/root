from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): power_unit_specs: dict; validation_results: Annotated[list, operator.add]; def validate_medical_compliance(state: GraphState): return {'validation_results': ['IEC 60601-1 verified']}; def check_power_output(state: GraphState): return {'validation_results': ['Output stability within 1%']}; graph = StateGraph(GraphState); graph.add_node('compliance', validate_medical_compliance); graph.add_node('output', check_power_output); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'output'); graph.add_edge('output', END); graph = graph.compile()