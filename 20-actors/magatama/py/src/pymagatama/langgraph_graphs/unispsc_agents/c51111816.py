from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; validation_passed: bool; temp_logs: List[float]; def validate_cold_chain(state: DrugState): state['validation_passed'] = all(t < 8.0 for t in state['temp_logs']); return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_cold_chain); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()