from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; gmp_compliant: bool; valid: bool; def validate_purity(state: PharmState): state['valid'] = state['purity'] >= 99.0; return state; def check_compliance(state: PharmState): if not state['gmp_compliant']: state['valid'] = False; return state; graph = StateGraph(PharmState); graph.add_node('check_purity', validate_purity); graph.add_node('check_gmp', check_compliance); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'check_gmp'); graph.add_edge('check_gmp', END); graph = graph.compile()