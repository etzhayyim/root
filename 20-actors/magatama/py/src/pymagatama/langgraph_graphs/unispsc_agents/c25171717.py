from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BrakeState(TypedDict): specs: dict; valid: bool; engine_code: str; graph = StateGraph(BrakeState); def validate_specs(state: BrakeState): state['valid'] = state['specs'].get('hardness', 0) > 50; return state; def route_inspection(state: BrakeState): return 'end' if state['valid'] else 'reject'; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()