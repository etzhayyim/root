from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BeltState(TypedDict): belt_specs: dict; validation_results: List[str]; graph = StateGraph(BeltState); def validate_spec(state: BeltState): state['validation_results'].append('Size Validated'); return state; def inspect_material(state: BeltState): state['validation_results'].append('Material Verified'); return state; graph.add_node('validate', validate_spec); graph.add_node('inspect', inspect_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()