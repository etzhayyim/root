from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LiquidHandlingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(LiquidHandlingState); def validate_accuracy(state: LiquidHandlingState): return {'validation_results': ['Accuracy check passed']}; def check_compliance(state: LiquidHandlingState): return {'validation_results': ['Compliance status: Verified']}; graph.add_node('accuracy_check', validate_accuracy); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('accuracy_check'); graph.add_edge('accuracy_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()