from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RoofVentState(TypedDict): vent_details: dict; validation_results: List[str]; def validate_specs(state: RoofVentState): requirements = ['airflow', 'material', 'thermal']; results = [req for req in requirements if req in state['vent_details']]; return {'validation_results': results}; def generate_report(state: RoofVentState): return {'validation_results': state['validation_results'] + ['Certification Pending']}; graph = StateGraph(RoofVentState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()