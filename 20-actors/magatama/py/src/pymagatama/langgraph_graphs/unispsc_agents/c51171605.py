from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_data: dict; approval_status: bool; def validate_purity(state: PharmaState): return {'approval_status': state['chemical_data'].get('purity', 0) >= 99.0}; def check_cold_chain(state: PharmaState): return {'approval_status': state['chemical_data'].get('temp_stable', False)}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('cold_chain', check_cold_chain); graph.set_entry_point('validate'); graph.add_edge('validate', 'cold_chain'); graph.add_edge('cold_chain', END); graph = graph.compile()