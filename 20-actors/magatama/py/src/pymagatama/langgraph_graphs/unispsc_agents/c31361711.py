from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): doc_id: str; quality_pass: bool; material_spec: str; graph = StateGraph(AssemblyState); def validate_material(state): return {'quality_pass': state['material_spec'] == 'Waspalloy'}; def check_nondestructive_test(state): return {'quality_pass': True}; graph.add_node('verify_material', validate_material); graph.add_node('perform_ndt', check_nondestructive_test); graph.add_edge('verify_material', 'perform_ndt'); graph.add_edge('perform_ndt', END); graph.set_entry_point('verify_material'); app = graph.compile()