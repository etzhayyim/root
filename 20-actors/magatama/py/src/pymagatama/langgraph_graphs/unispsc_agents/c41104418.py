from langgraph.graph import StateGraph, END; from typing import TypedDict; class IncubatorState(TypedDict): specs: dict; validation: bool; def validate_specs(state: IncubatorState): state['validation'] = all(k in state['specs'] for k in ['CO2', 'O2', 'temp']); return state; graph = StateGraph(IncubatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()