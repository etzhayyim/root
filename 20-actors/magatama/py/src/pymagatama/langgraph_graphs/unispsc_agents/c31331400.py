from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UVAssemblyState(TypedDict): assembly_id: str; specs: dict; validation_report: List[str]; def validate_bonding(state: UVAssemblyState): state['validation_report'].append('Verifying UV bond integrity'); return state; def check_cleanliness(state: UVAssemblyState): state['validation_report'].append('Cleaning verification passed'); return state; graph = StateGraph(UVAssemblyState); graph.add_node('validate_bond', validate_bonding); graph.add_node('cleanliness_check', check_cleanliness); graph.set_entry_point('validate_bond'); graph.add_edge('validate_bond', 'cleanliness_check'); graph.add_edge('cleanliness_check', END); graph.compile()