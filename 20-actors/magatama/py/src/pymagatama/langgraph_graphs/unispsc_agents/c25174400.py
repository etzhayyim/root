from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InteriorState(TypedDict): specs: List[str]; validated: bool; next_step: str; def validate_specs(state: InteriorState): return {'validated': all(['flammability' in s.lower() for s in state['specs']])}; def assembly_workflow(state: InteriorState): return {'next_step': 'quality_inspection'}; graph = StateGraph(InteriorState); graph.add_node('validate', validate_specs); graph.add_node('assemble', assembly_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph = graph.compile()