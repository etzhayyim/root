from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; class MotorState(TypedDict): motor_spec: dict; validation_report: list[str]; def validate_motor_spec(state: MotorState): specs = state['motor_spec']; errors = []; if specs.get('rated_output_kw', 0) <= 0: errors.append('Invalid output power'); return {'validation_report': errors}; def compile_graph(): builder = StateGraph(MotorState); builder.add_node('validate', validate_motor_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); return builder.compile(); graph = compile_graph()