from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugSpecState(TypedDict): medication_batch: str; compliance_verified: bool; dosage_match: bool; graph = StateGraph(DrugSpecState); def validate_compliance(state: DrugSpecState): return {'compliance_verified': True}; def check_dosage(state: DrugSpecState): return {'dosage_match': True}; graph.add_node('verify', validate_compliance); graph.add_node('check', check_dosage); graph.set_entry_point('verify'); graph.add_edge('verify', 'check'); graph.add_edge('check', END); graph = graph.compile()