from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoverState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: CoverState): state['approved'] = all(k in state['specs'] for k in ['material', 'sterilization_type']); return state; graph = StateGraph(CoverState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()