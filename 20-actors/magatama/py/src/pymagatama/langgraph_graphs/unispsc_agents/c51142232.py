from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_license: str; purity: float; compliant: bool; def validate_license(state): state['compliant'] = state['vendor_license'] == 'VALIDATED'; return state; def check_purity(state): state['compliant'] = state['compliant'] and state['purity'] >= 99.9; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_license); graph.add_node('check_chem', check_purity); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_chem'); graph.add_edge('check_chem', END); compiled_graph = graph.compile()