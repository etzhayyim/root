from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalAlloyState(TypedDict): alloy_type: str; is_compliant: bool; report: str; def validate_composition(state: DentalAlloyState): state['is_compliant'] = 'mercury' in state['alloy_type'].lower(); return state; def generate_report(state: DentalAlloyState): state['report'] = 'Validation complete' if state['is_compliant'] else 'Non-compliant'; return state; graph = StateGraph(DentalAlloyState); graph.add_node('validate', validate_composition); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()