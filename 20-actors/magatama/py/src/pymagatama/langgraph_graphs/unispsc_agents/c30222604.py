from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HallState(TypedDict): facility_id: str; structural_requirements: List[str]; approved: bool; def check_structural_compliance(state: HallState): state['approved'] = len(state['structural_requirements']) > 0; return state; graph = StateGraph(HallState); graph.add_node('compliance', check_structural_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()