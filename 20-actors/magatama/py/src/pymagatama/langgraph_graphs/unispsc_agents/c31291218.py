from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_report: List[str]; approved: bool; def validate_geometry(state: State): state['validation_report'].append('Geometry validated'); return state; def check_material_cert(state: State): state['validation_report'].append('Material cert verified'); return state; graph = StateGraph(State); graph.add_node('validate_geometry', validate_geometry); graph.add_node('check_material', check_material_cert); graph.set_entry_point('validate_geometry'); graph.add_edge('validate_geometry', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()