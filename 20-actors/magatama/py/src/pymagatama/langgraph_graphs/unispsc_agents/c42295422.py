from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; sterility_verified: bool; compliance_check: bool; def verify_sterility(state: KitState): return { "sterility_verified": True }; def check_compliance(state: KitState): return { "compliance_check": state["sterility_verified"] }; graph = StateGraph(KitState); graph.add_node("verify", verify_sterility); graph.add_node("compliance", check_compliance); graph.set_entry_point("verify"); graph.add_edge("verify", "compliance"); graph.add_edge("compliance", END); compiled_graph = graph.compile()