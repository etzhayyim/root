from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_unit_spec(state: State): specs = state['input_specs']; errors = []; if not specs.get('ISO_13485_certification'): errors.append('Missing ISO certification'); return {'validation_results': errors, 'status': 'VALIDATED' if not errors else 'REJECTED'}; graph = StateGraph(State); graph.add_node('validate', validate_unit_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()