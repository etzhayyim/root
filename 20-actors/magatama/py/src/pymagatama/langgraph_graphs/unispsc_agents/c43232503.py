from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): input_text: str; language: str; corrected_text: str; def validate_integrity(state: State): return {'corrected_text': state['input_text']}; def process_proofing(state: State): return {'corrected_text': state['input_text'].capitalize()}; graph = StateGraph(State); graph.add_node('validate', validate_integrity); graph.add_node('proof', process_proofing); graph.add_edge('validate', 'proof'); graph.add_edge('proof', END); graph.set_entry_point('validate'); compile_graph = graph.compile()