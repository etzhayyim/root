from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BusSpecState(TypedDict): specs: dict; validation_errors: List[str]; def validate_emissions(state: BusSpecState): state['validation_errors'].append('Check Euro 6 compliance') if state['specs'].get('emission') != 'Euro6' else None; return state; graph = StateGraph(BusSpecState); graph.add_node('validate_emissions', validate_emissions); graph.set_entry_point('validate_emissions'); graph.add_edge('validate_emissions', END); app = graph.compile()