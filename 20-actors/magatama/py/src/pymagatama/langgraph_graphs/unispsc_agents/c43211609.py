from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class USBState(TypedDict): hardware_specs: dict; validation_results: Annotated[Sequence[str], operator.add]; def validate_usb_standard(state: USBState): return {'validation_results': ['USB 3.x compliance verified']}; def check_power_delivery(state: USBState): return {'validation_results': ['PD 3.0 power handshake confirmed']}; graph = StateGraph(USBState); graph.add_node('validate_usb', validate_usb_standard); graph.add_node('check_power', check_power_delivery); graph.set_entry_point('validate_usb'); graph.add_edge('validate_usb', 'check_power'); graph.add_edge('check_power', END); graph = graph.compile()