from langgraph.graph import StateGraph, END; from typing import TypedDict; class BalanceState(TypedDict): spec: dict; valid: bool; def validate_spec(state: BalanceState): capacity = state['spec'].get('capacity', 0); state['valid'] = capacity > 0; return state; graph = StateGraph(BalanceState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()