from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DVDProcState(TypedDict): model_number: str; specs: dict; validation_passed: bool; def validate_drive(state: DVDProcState): state['validation_passed'] = 'SATA' in state['specs'].get('interface', '') or 'USB' in state['specs'].get('interface', ''); return state; graph = StateGraph(DVDProcState); graph.add_node('validate', validate_drive); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()