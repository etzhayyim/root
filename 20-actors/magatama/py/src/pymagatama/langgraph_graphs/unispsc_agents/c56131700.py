from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): hardware_specs: dict; validation_results: List[str]; graph = StateGraph(ProcureState); def validate_load(state: ProcureState): return {'validation_results': ['load_capacity_verified']}; def check_material(state: ProcureState): return {'validation_results': ['material_compliance_passed']}; graph.add_node('validate_load', validate_load); graph.add_node('check_material', check_material); graph.set_entry_point('validate_load'); graph.add_edge('validate_load', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()