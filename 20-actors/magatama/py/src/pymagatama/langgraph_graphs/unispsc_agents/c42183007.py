from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class OphthalmicState(TypedDict): data: dict; status: str; def validate_specs(state: OphthalmicState): return {'status': 'validated'} if 'certification' in state['data'] else {'status': 'rejected'}; def check_compliance(state: OphthalmicState): return {'status': 'compliant'}; builder = StateGraph(OphthalmicState); builder.add_node('validate', validate_specs); builder.add_node('compliance', check_compliance); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); builder.set_entry_point('validate'); graph = builder.compile()