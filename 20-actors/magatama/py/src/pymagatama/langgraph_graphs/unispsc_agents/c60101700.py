from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TeacherResourceState(TypedDict): resources: List[str]; validated: bool; next_step: str; def validate_resource_data(state: TeacherResourceState): print('Validating content alignment...'); return {'validated': len(state['resources']) > 0}; def prepare_distribution(state: TeacherResourceState): print('Formatting materials for distribution...'); return {'next_step': 'finalized'}; graph = StateGraph(TeacherResourceState); graph.add_node('validate', validate_resource_data); graph.add_node('prepare', prepare_distribution); graph.add_edge('validate', 'prepare'); graph.add_edge('prepare', END); graph.set_entry_point('validate'); compile_graph = graph.compile()