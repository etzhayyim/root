from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InfusionState(TypedDict): status: str; spec_data: dict; compliance_check: bool; graph = StateGraph(InfusionState); def validate_medical_grade(state: InfusionState): return {'compliance_check': 'ISO10993' in state['spec_data'].get('certifications', [])}; def finalize_procurement(state: InfusionState): return {'status': 'READY_FOR_ORDER'}; graph.add_node('validate', validate_medical_grade); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()