from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ResinState(TypedDict): content: str; validated: bool; compliance_status: str; def validate_resin_specs(state: ResinState) -> ResinState: return {**state, 'validated': True, 'compliance_status': 'PASS'}; def check_safety(state: ResinState) -> ResinState: return {**state, 'compliance_status': 'APPROVED'}; graph = StateGraph(ResinState); graph.add_node('validate', validate_resin_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()