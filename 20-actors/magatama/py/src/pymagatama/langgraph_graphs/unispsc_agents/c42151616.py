from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DentalState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(DentalState); def validate_spec(state: DentalState): return {'validation_results': ['Calibration verified']}; def check_biocompatibility(state: DentalState): return {'validation_results': ['ISO 10993 compliant']}; graph.add_node('validate', validate_spec); graph.add_node('biocompatibility', check_biocompatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'biocompatibility'); graph.add_edge('biocompatibility', END); graph.compile()