from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtrusionState(TypedDict): specs: dict; validation_results: List[str]; def validate_tolerance(state: ExtrusionState): return {'validation_results': ['Tolerance checked']} if 'tolerance' in state['specs'] else {'validation_results': ['Missing tolerance']}; def check_alloy(state: ExtrusionState): return {'validation_results': state['validation_results'] + ['Alloy verified']}; graph = StateGraph(ExtrusionState); graph.add_node('tolerance', validate_tolerance); graph.add_node('alloy', check_alloy); graph.set_entry_point('tolerance'); graph.add_edge('tolerance', 'alloy'); graph.add_edge('alloy', END); graph = graph.compile()