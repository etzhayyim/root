from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BrushState(TypedDict): brush_type: str; quality_check: bool; specs: dict; def validate_brush(state: BrushState): state['quality_check'] = 'hair_material' in state['specs'] and state['specs']['hair_material'] is not None; return state; graph = StateGraph(BrushState); graph.add_node('validate', validate_brush); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()