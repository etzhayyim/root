from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): content: str; viscosity_test_passed: bool; safety_compliant: bool; def validate_viscosity(state: State): return {'viscosity_test_passed': True}; def check_regulatory(state: State): return {'safety_compliant': True}; graph = StateGraph(State); graph.add_node('validate', validate_viscosity); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()