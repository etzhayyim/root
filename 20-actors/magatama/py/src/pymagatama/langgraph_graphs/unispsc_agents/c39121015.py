from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReactorState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_reactor(state: ReactorState): state['specs']['valid'] = state['specs'].get('Inductance_mH', 0) > 0; state['log'].append('Validation completed'); return state; graph = StateGraph(ReactorState); graph.add_node('validate', validate_reactor); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()