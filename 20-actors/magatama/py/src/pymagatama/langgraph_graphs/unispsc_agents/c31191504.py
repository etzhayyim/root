from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AbrasiveState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: AbrasiveState): errors = []; if not state['specs'].get('grit_size'): errors.append('Grit size missing'); if not state['specs'].get('backing_material'): errors.append('Backing material missing'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(AbrasiveState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()