from langgraph.graph import StateGraph, END; from typing import TypedDict; class ShipState(TypedDict): vessel_id: str; clearance: bool; specs: dict; graph = StateGraph(ShipState); def validate_security(state): return {'clearance': True}; def evaluate_specs(state): return {'specs': {'validated': True}}; graph.add_node('security', validate_security); graph.add_node('evaluation', evaluate_specs); graph.set_entry_point('security'); graph.add_edge('security', 'evaluation'); graph.add_edge('evaluation', END); graph.compile()