from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): data: dict; checks: Annotated[list, operator.add]; def validate_purity(state: PharmState): return {'checks': ['Purity > 99.0% verified']}; def verify_compliance(state: PharmState): return {'checks': ['FDA/EMA compliance checked']}; graph = StateGraph(PharmState); graph.add_node('purity_check', validate_purity); graph.add_node('compliance_check', verify_compliance); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()