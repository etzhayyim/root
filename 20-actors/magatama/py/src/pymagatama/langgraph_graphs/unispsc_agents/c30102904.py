from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PostState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: PostState): specs = state['specs']; state['valid'] = 'length' in specs and 'treatment' in specs; return state; graph = StateGraph(PostState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()