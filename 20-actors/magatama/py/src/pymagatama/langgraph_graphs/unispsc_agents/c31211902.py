from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_specs: dict; validation_report: str; def validate_specs(state: ToolState): state['validation_report'] = 'Validation Complete: Specs meet industry standard.' if 'blade_material' in state['tool_specs'] else 'Validation Failed: Missing blade data'; return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()