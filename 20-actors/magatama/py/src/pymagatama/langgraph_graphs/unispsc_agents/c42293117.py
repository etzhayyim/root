from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): spec: dict; valid: bool; def validate_spec(state: ProcState): state['valid'] = 'ISO 13485' in state['spec'] and 'Material' in state['spec']; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()