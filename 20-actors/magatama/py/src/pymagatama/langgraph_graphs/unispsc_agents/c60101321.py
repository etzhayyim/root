from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlashcardState(TypedDict): card_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: FlashcardState): specs = state['card_specs']; errors = []; if specs.get('age') < 3: errors.append('Safety risk: age rating too low.'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(FlashcardState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()