from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PrisonBuildState(TypedDict): site_id: str; security_clearance: bool; compliance_docs: List[str]; graph = StateGraph(PrisonBuildState); def check_security_clearance(state: PrisonBuildState): return {'security_clearance': True}; def validate_compliance(state: PrisonBuildState): return {'compliance_docs': ['ISO-SEC-2024']}; graph.add_node('verify', check_security_clearance); graph.add_node('audit', validate_compliance); graph.set_entry_point('verify'); graph.add_edge('verify', 'audit'); graph.add_edge('audit', END); graph = graph.compile()