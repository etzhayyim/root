from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StructureState(TypedDict): site_data: dict; compliance_checked: bool; inspection_result: str; def validate_site(state: StructureState): return {'compliance_checked': True}; def perform_inspection(state: StructureState): return {'inspection_result': 'PASS'}; graph = StateGraph(StructureState); graph.add_node('validate', validate_site); graph.add_node('inspect', perform_inspection); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()