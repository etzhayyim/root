from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): data: dict; status: str; graph = StateGraph(AnalysisState); def validate_thermal_spec(state: AnalysisState): return {'status': 'validated' if 'temperature_range' in state['data'] else 'incomplete'}; def perform_calibration_check(state: AnalysisState): return {'status': 'calibrated'}; graph.add_node('validate', validate_thermal_spec); graph.add_node('calibrate', perform_calibration_check); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); graph.set_entry_point('validate'); graph = graph.compile()