from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClampState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: ClampState): specs = state['specs']; state['valid'] = 'material' in specs and 'force' in specs; return state; graph = StateGraph(ClampState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()