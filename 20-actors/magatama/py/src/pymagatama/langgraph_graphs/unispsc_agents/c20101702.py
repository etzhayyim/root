from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): messages: Annotated[Sequence[str], operator.add]; status: str; def validate_spec(state: AgentState): return {'status': 'validated' if 'ready' in state['messages'] else 'pending'}; def compute_logic(state: AgentState): return {'status': 'processing_complete'}; graph = StateGraph(AgentState); graph.add_node('validate', validate_spec); graph.add_node('compute', compute_logic); graph.set_entry_point('validate'); graph.add_edge('validate', 'compute'); graph.add_edge('compute', END); compiled_graph = graph.compile()