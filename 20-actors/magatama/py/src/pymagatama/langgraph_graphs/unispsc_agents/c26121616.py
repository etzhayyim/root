from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableState(TypedDict): specs: dict; validation_results: List[str]; def validate_standards(state: CableState): results = []; if state['specs'].get('fire_rating') != 'UL910': results.append('Fire safety non-compliant'); return {'validation_results': results}; def cable_workflow(): graph = StateGraph(CableState); graph.add_node('validate', validate_standards); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph; graph = cable_workflow()