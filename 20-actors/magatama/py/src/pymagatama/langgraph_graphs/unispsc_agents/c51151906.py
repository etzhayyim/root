from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; def validate_isoxsuprine(state: ProcState): state['validated'] = all(['purity' in item.lower() for item in state['items']]); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_isoxsuprine); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()