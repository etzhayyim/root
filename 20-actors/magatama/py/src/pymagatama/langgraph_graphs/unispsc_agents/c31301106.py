from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): dimensions: dict; material_spec: str; validated: bool; defect_check: bool; def validate_specs(state: ForgingState): state['validated'] = all(k in state['dimensions'] for k in ['length', 'width', 'weight']); return state; def inspect_quality(state: ForgingState): state['defect_check'] = True; return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.add_node('inspect', inspect_quality); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()