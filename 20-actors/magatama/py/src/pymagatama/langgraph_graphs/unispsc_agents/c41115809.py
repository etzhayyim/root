from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class State(TypedDict): messages: Annotated[list, add_messages]; sensor_data: dict; valid: bool; def validate_spec(state: State): return {'valid': 'calibration_certificate' in state['sensor_data']}; def finalize_procurement(state: State): print('Analyzer procurement finalized'); return {'messages': ['Procurement complete']}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.set_entry_point('validate'); graph.add_edge('finalize', END); app = graph.compile()