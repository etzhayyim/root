from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CompostingState(TypedDict): capacity: float; moisture_content: float; compliance_passed: bool; graph = StateGraph(CompostingState); def validate_capacity(state): return {'compliance_passed': state['capacity'] > 0}; def sensor_check(state): return {'moisture_content': 0.6 if state['moisture_content'] > 0.6 else state['moisture_content']}; graph.add_node('validate', validate_capacity); graph.add_node('sensors', sensor_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'sensors'); graph.add_edge('sensors', END); graph = graph.compile()