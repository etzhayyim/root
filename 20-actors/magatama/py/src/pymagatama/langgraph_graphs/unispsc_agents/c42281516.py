from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): spec: dict; validation_results: List[str]; def validate_filter_specs(state: FilterState): spec = state['spec']; results = []; if spec.get('pore_size', 0) > 0.45: results.append('Warning: Pore size exceeds standard sterilization threshold.'); return {'validation_results': results}; def log_compliance(state: FilterState): print('Compliance Verified'); return {'validation_results': state['validation_results'] + ['Verified']}; graph = StateGraph(FilterState); graph.add_node('validate', validate_filter_specs); graph.add_node('log', log_compliance); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.set_entry_point('validate'); graph = graph.compile()