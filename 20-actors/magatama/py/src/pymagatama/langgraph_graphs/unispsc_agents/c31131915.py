from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): material_cert: str; dimension_data: dict; approved: bool; def validate_forging(state: ForgingState): state['approved'] = 'grade_c' in state['material_cert'].lower() and state['dimension_data']['tolerance'] < 0.05; return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_forging); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()