from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabelState(TypedDict): label_specs: dict; validation_errors: List[str]; approved: bool; def validate_labels(state: LabelState): print('Validating labels...'); return {'validation_errors': [] if state['label_specs'].get('adhesive') else ['Missing adhesive spec']}; graph = StateGraph(LabelState); graph.add_node('validate', validate_labels); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()