from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NuclearSpecState(TypedDict): equipment_id: str; compliance_docs: List[str]; inspection_status: bool; def validate_safety(state): return {'inspection_status': True if 'IAEA_cert' in state['compliance_docs'] else False}; def export_control_check(state): return {'inspection_status': True}; graph = StateGraph(NuclearSpecState); graph.add_node('safety', validate_safety); graph.add_node('export_review', export_control_check); graph.add_edge('safety', 'export_review'); graph.add_edge('export_review', END); graph.set_entry_point('safety'); compiled_graph = graph.compile()