from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; approved_items: List[str]; def validate_adhesives(state: State): validated = [item for item in state['items'] if 'ISO-10993' in item]; return {'approved_items': validated}; graph = StateGraph(State); graph.add_node('validate', validate_adhesives); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()