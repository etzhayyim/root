from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; def validate_form_metadata(state: ProcState): return {'validation_results': ['metadata_validated']}; def check_procurement_compliance(state: ProcState): return {'validation_results': ['compliance_verified']}; graph = StateGraph(ProcState); graph.add_node('validate', validate_form_metadata); graph.add_node('compliance', check_procurement_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()