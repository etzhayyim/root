from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): needle_specs: dict; compliance_status: List[str]; def validate_safety_mech(state: State): state['compliance_status'].append('Safety Mechanism Verified'); return state; def check_sterilization(state: State): state['compliance_status'].append('Sterility Validated'); return state; graph = StateGraph(State); graph.add_node('ValidateSafety', validate_safety_mech); graph.add_node('CheckSterile', check_sterilization); graph.set_entry_point('ValidateSafety'); graph.add_edge('ValidateSafety', 'CheckSterile'); graph.add_edge('CheckSterile', END); graph = graph.compile()