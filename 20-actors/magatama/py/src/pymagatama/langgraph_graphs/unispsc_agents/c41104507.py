from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LabMicrowaveState(TypedDict): specs: dict; validation_results: list[str]; def validate_power_specs(state: LabMicrowaveState): state['validation_results'].append('Power specs verified'); return state; def check_safety_compliance(state: LabMicrowaveState): state['validation_results'].append('Safety compliance checked'); return state; graph = StateGraph(LabMicrowaveState); graph.add_node('validate', validate_power_specs); graph.add_node('safety', check_safety_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()