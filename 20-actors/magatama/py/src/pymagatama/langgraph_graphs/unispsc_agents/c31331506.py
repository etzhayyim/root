from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): specs: dict; validation_passed: bool; assembly_status: str; def validate_materials(state: AssemblyState): state['validation_passed'] = 'solvent_compatible' in state['specs']; return state; def check_pressure(state: AssemblyState): state['assembly_status'] = 'pressure_test_queued' if state['validation_passed'] else 'failed'; return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_materials); graph.add_node('pressure_check', check_pressure); graph.set_entry_point('validate'); graph.add_edge('validate', 'pressure_check'); graph.add_edge('pressure_check', END); compiled_graph = graph.compile()