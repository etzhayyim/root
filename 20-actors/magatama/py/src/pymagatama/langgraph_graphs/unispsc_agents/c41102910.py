from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CultivationState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: CultivationState): results = []; temp = state['specs'].get('temp_precision'); results.append('Passed' if temp and float(temp) < 0.5 else 'Failed'); return {'validation_results': results}; graph = StateGraph(CultivationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()