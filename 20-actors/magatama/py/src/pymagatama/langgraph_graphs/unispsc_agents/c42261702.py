from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AutopsyState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: AutopsyState): state['compliance'] = 'AISI316' in state['specs'].get('Material', '') and state['specs'].get('BiohazardReady', False); return state; def check_approval(state: AutopsyState): state['approved'] = state['compliance']; return state; graph = StateGraph(AutopsyState); graph.add_node('validate', validate_specs); graph.add_node('approve', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()