from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class BatteryState(TypedDict): battery_type: str; voltage: float; is_compliant: bool; def check_spec(state: BatteryState): return {'is_compliant': state['voltage'] >= 1.5}; graph = StateGraph(BatteryState); graph.add_node('validate', check_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()