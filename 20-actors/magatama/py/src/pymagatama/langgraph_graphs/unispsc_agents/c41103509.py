from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class ScrubberState(TypedDict): messages: Annotated[list, add_messages]; spec_requirements: dict; validation_status: str; def validate_spec(state: ScrubberState): state['validation_status'] = 'COMPLIANT' if state['spec_requirements'].get('flow_rate') > 0 else 'INVALID'; return state; def check_hazmat(state: ScrubberState): state['validation_status'] = 'REVIEW_REQUIRED' if state['spec_requirements'].get('handling_hazardous') else state['validation_status']; return state; graph = StateGraph(ScrubberState); graph.add_node('validate', validate_spec); graph.add_node('hazmat_check', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat_check'); graph.add_edge('hazmat_check', END); graph = graph.compile()