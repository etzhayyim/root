from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): medication_data: dict; validation_errors: List[str]; approved: bool; def validate_compliance(state: DrugState): state['validation_errors'] = []; if 'gmp' not in state['medication_data']: state['validation_errors'].append('Missing GMP cert'); return state; def final_decision(state: DrugState): state['approved'] = len(state['validation_errors']) == 0; return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_compliance); graph.add_node('decision', final_decision); graph.set_entry_point('validate'); graph.add_edge('validate', 'decision'); graph.add_edge('decision', END); compiled_graph = graph.compile()