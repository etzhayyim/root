from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GlassSpecState(TypedDict): dimensions: dict; spec_compliance: bool; validation_log: List[str]; def validate_dimensions(state: GlassSpecState): return {'validation_log': state['validation_log'] + ['Checking bevel angles...']}; def check_safety(state: GlassSpecState): return {'spec_compliance': True}; graph = StateGraph(GlassSpecState); graph.add_node('validate', validate_dimensions); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()