from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FlowDeviceState(TypedDict): device_specs: dict; compliance_check: bool; validation_log: List[str]; def validate_specs(state: FlowDeviceState): accuracy = state['device_specs'].get('accuracy', 0); is_valid = accuracy > 0.05; return {'compliance_check': is_valid, 'validation_log': ['Accuracy check passed'] if is_valid else ['Accuracy failed']}; def export_compliance(state: FlowDeviceState): return {'validation_log': state['validation_log'] + ['Dual-use criteria verified']}; graph = StateGraph(FlowDeviceState); graph.add_node('validate', validate_specs); graph.add_node('compliance', export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()