from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CabinetState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: CabinetState): state['compliance'] = all(k in state['specs'] for k in ['material', 'dimensions']); return state; def check_certification(state: CabinetState): state['approved'] = state['compliance'] and state['specs'].get('iso_cert') == True; return state; graph = StateGraph(CabinetState); graph.add_node('validate', validate_specs); graph.add_node('certify', check_certification); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()