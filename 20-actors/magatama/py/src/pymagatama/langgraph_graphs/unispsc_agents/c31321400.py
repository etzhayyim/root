from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): metal_grade: str; welds: List[str]; passed: bool; def validate_specs(state: AssemblyState): print(f'Validating material: {state["metal_grade"]}'); return {'passed': True if state['metal_grade'] else False}; def run_inspection(state: AssemblyState): print('Conducting X-ray inspection on welds'); return {'passed': True}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('inspect', run_inspection); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); compiled_graph = graph.compile()