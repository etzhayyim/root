from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnaceState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: FurnaceState): results = []; if state['specs'].get('temp', 0) < 500: results.append('Insufficient temp'); return {'validation_results': results}; def log_compliance(state: FurnaceState): print('Compliance checked for high-value equipment'); return {'validation_results': state['validation_results'] + ['Certified']}; graph = StateGraph(FurnaceState); graph.add_node('validate', validate_specs); graph.add_node('log', log_compliance); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.set_entry_point('validate'); graph = graph.compile()