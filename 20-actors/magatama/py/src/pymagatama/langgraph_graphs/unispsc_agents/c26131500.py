from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlantState(TypedDict): capacity: float; fuel_type: str; compliance_docs: List[str]; approved: bool; def validate_specs(state: PlantState): state['approved'] = state['capacity'] > 0 and len(state['compliance_docs']) > 2; return state; graph = StateGraph(PlantState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()