from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimension(state: ForgingState): state['validation_results'].append('Dimensional check passed'); return state; def check_certification(state: ForgingState): state['validation_results'].append('Certification verified'); return state; workflow = StateGraph(ForgingState); workflow.add_node('validate_dim', validate_dimension); workflow.add_node('check_cert', check_certification); workflow.set_entry_point('validate_dim'); workflow.add_edge('validate_dim', 'check_cert'); workflow.add_edge('check_cert', END); graph = workflow.compile()