from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GuyCableState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: GuyCableState): tensile = state['specs'].get('tensile_strength', 0); state['valid'] = tensile > 1500; state['log'] = ['Strength check passed'] if state['valid'] else ['Insufficient strength']; return state; builder = StateGraph(GuyCableState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()