from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WireSpecState(TypedDict): wire_type: str; specs: dict; compliant: bool; def validate_specs(state: WireSpecState): state['compliant'] = all(k in state['specs'] for k in ['gauge', 'voltage', 'temp']); return state; graph = StateGraph(WireSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()