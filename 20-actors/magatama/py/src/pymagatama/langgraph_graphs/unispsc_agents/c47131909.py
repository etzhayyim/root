from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SorbentState(TypedDict): sorbent_type: str; specs: dict; ready_for_procurement: bool; def validate_specs(state: SorbentState): state['ready_for_procurement'] = 'absorbency_rate_liters' in state['specs'] and 'material_composition_polypropylene_weight' in state['specs']; return state; builder = StateGraph(SorbentState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()