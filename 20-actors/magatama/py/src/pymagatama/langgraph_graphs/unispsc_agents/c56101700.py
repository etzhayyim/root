from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class OfficeFurnitureState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(OfficeFurnitureState); def validate_specs(state: OfficeFurnitureState): state['validation_results'] = ['passed_ergonomic' if i.get('ergonomic') else 'failed' for i in state['items']]; return state; def finalize(state: OfficeFurnitureState): return state; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()