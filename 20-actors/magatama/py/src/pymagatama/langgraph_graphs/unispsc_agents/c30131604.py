from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StoneBrickState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: StoneBrickState): min_strength = 20.0; actual_strength = state['specs'].get('compressive_strength', 0); state['approved'] = actual_strength >= min_strength; state['validation_report'] = 'Approved' if state['approved'] else 'Rejected: Low strength'; return state; graph = StateGraph(StoneBrickState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()