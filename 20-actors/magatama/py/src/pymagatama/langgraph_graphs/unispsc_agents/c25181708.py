from langgraph.graph import StateGraph, END; from typing import TypedDict; class TrailerState(TypedDict): hitch_specs: dict; validated: bool; def check_specs(state: TrailerState): capacity = state['hitch_specs'].get('capacity', 0); return {'validated': capacity > 0}; graph = StateGraph(TrailerState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()