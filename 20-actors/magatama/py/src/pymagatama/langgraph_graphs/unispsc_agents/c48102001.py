from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ChairSpecState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: ChairSpecState): state['valid'] = state['specs'].get('weight_capacity', 0) > 150; return state; graph = StateGraph(ChairSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()