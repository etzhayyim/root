from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TorpedoState(TypedDict): vendor_clearance: bool; compliance_check: bool; export_license: str; def verify_compliance(state: TorpedoState): state['compliance_check'] = True; return state; def validate_export(state: TorpedoState): state['export_license'] = 'VALIDATED' if state.get('export_license') else 'PENDING'; return state; graph = StateGraph(TorpedoState); graph.add_node('verify', verify_compliance); graph.add_node('export', validate_export); graph.set_entry_point('verify'); graph.add_edge('verify', 'export'); graph.add_edge('export', END); graph = graph.compile()