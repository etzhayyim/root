from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PiccoloState(TypedDict): piccolo_model: str; status: str; QC_passed: bool; def validate_instrument(state: PiccoloState): print(f'Validating {state[piccolo_model]}...'); state['QC_passed'] = True; return {'status': 'Validated'}; def finalize_procurement(state: PiccoloState): return {'status': 'Ready for Shipment'}; graph = StateGraph(PiccoloState); graph.add_node('validate', validate_instrument); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()