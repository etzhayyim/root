from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MyomaInstrumentState(TypedDict): instrument_id: str; quality_checks: List[str]; approved: bool; def validate_instrument(state: MyomaInstrumentState): state['approved'] = 'ISO_13485' in state['quality_checks']; return state; builder = StateGraph(MyomaInstrumentState); builder.add_node('validate', validate_instrument); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()