from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OxygenState(TypedDict): device_type: str; flow_rate: float; is_compliant: bool; def validate_spec(state: OxygenState) -> OxygenState: state['is_compliant'] = state['flow_rate'] > 0; return state; graph = StateGraph(OxygenState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()