from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): sauce_type: str; is_compliant: bool; def check_compliance(state: ProcureState): state['is_compliant'] = state.get('sauce_type') is not None; return state; graph = StateGraph(ProcureState); graph.add_node('validate', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()