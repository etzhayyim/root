from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FurnitureState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(FurnitureState); def validate_specs(state: FurnitureState): return {'validation_results': ['Specs Verified']}; def check_safety(state: FurnitureState): return {'validation_results': state['validation_results'] + ['Safety Compliance PASSED']}; graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()