from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SatelliteState(TypedDict): vendor_id: str; orbit_specs: dict; compliance_docs: List[str]; approved: bool; def check_orbit_compliance(state: SatelliteState): state['approved'] = 'ITAR' in state.get('compliance_docs', []); return state; graph = StateGraph(SatelliteState); graph.add_node('validate', check_orbit_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()