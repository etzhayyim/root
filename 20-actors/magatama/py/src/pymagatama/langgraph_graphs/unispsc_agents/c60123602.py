from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CraftState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(CraftState); def validate_safety(state: CraftState): print('Checking ASTM D-4236'); return {'validation_results': {'safety': 'passed'}}; def check_quality(state: CraftState): return {'validation_results': {'quality': 'inspected'}}; graph.add_node('safety_check', validate_safety); graph.add_node('quality_audit', check_quality); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'quality_audit'); graph.add_edge('quality_audit', END); compiled_graph = graph.compile()