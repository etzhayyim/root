from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medicine: str; compliance: bool; checks: List[str]; def validate_pharmaceutical(state: PharmaState): print(f'Validating {state[medicine]}'); return {'compliance': True, 'checks': ['GMP_CHECK', 'EXPIRY_CHECK']}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_pharmaceutical); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()