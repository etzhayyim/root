from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CircuitState(TypedDict): component_id: str; compliance_checked: bool; inspection_passed: bool; def check_compliance(state: CircuitState): return {'compliance_checked': True}; def perform_inspection(state: CircuitState): return {'inspection_passed': True}; graph = StateGraph(CircuitState); graph.add_node('check_compliance', check_compliance); graph.add_node('inspection', perform_inspection); graph.set_entry_point('check_compliance'); graph.add_edge('check_compliance', 'inspection'); graph.add_edge('inspection', END); graph = graph.compile()