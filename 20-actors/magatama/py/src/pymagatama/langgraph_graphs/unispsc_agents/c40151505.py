from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DosingState(TypedDict): flow_rate: float; fluid_type: str; pressures: List[float]; validation_status: bool; def validate_pump_specs(state: DosingState): state['validation_status'] = state['flow_rate'] > 0 and len(state['pressures']) > 0; return state; graph = StateGraph(DosingState); graph.add_node('validate', validate_pump_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()