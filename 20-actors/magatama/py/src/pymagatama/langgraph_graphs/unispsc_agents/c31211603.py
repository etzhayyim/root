from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaintDrierState(TypedDict): chemical_composition: str; hazard_level: int; compliance_check: bool; def validate_chemical(state: PaintDrierState): return {'compliance_check': state['hazard_level'] < 5}; def verify_msds(state: PaintDrierState): return {'compliance_check': True}; graph = StateGraph(PaintDrierState); graph.add_node('validate', validate_chemical); graph.add_node('verify', verify_msds); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); graph.set_entry_point('validate'); graph = graph.compile()