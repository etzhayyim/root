from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InfantApparelState(TypedDict): vendor_certs: List[str]; chemical_test_results: bool; final_approval: bool; def validate_safety(state: InfantApparelState): return {'chemical_test_results': True} if 'OEKO-TEX' in state['vendor_certs'] else {'chemical_test_results': False}; def approve_procurement(state: InfantApparelState): return {'final_approval': state['chemical_test_results']}; graph = StateGraph(InfantApparelState); graph.add_node('safety_check', validate_safety); graph.add_node('approval', approve_procurement); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'approval'); graph.add_edge('approval', END); graph = graph.compile()