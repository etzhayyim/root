from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GrainState(TypedDict): grain_type: str; moisture_level: float; quality_passed: bool; def validate_moisture(state: GrainState) -> dict: return {'quality_passed': state['moisture_level'] < 14.0}; def check_origin(state: GrainState) -> dict: return {'quality_passed': True}; graph = StateGraph(GrainState); graph.add_node('validate', validate_moisture); graph.add_node('certify', check_origin); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()