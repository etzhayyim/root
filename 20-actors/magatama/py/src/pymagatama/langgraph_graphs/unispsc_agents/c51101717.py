from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IvermectinState(TypedDict): status: str; batch_data: dict; compliance_check: bool; def validate_gmp(state: IvermectinState) -> IvermectinState: state['compliance_check'] = 'gmp_certification' in state['batch_data']; return state; def verify_regulatory(state: IvermectinState) -> IvermectinState: if state['compliance_check']: state['status'] = 'APPROVED'; return state; state['status'] = 'REJECTED'; return state; graph = StateGraph(IvermectinState); graph.add_node('validate', validate_gmp); graph.add_node('compliance', verify_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()