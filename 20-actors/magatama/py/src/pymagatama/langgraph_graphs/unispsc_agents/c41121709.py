from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class HematocritState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: HematocritState): state['valid'] = all(k in state['specs'] for k in ['material', 'diameter']); return state; graph = StateGraph(HematocritState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()