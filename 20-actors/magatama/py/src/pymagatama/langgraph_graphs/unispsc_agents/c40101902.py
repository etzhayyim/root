from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DehumidifierState(TypedDict): specs: dict; validation_errors: List[str]; graph = StateGraph(DehumidifierState); def validate_specs(state: DehumidifierState): capacity = state['specs'].get('capacity'); if not capacity or capacity <= 0: return {'validation_errors': ['Invalid capacity']}; return {'validation_errors': []}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()