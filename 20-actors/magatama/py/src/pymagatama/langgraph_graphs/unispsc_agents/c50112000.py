from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MeatProcurementState(TypedDict): items: List[str]; inspection_passed: bool; temp_logs: List[float]; def validate_cool_chain(state: MeatProcurementState): return {'inspection_passed': all(t < 5.0 for t in state['temp_logs'])}; def finalize_order(state: MeatProcurementState): return {'inspection_passed': True}; graph = StateGraph(MeatProcurementState); graph.add_node('validate', validate_cool_chain); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()