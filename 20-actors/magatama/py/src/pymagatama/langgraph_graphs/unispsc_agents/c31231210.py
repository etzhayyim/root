from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): material_spec: dict; validation_report: List[str]; graph = StateGraph(State); def validate_material(state: State): state['validation_report'] = ['Grade Verified', 'Dimensions Within Tolerance'] if state['material_spec'].get('grade') else ['Missing Grade']; return state; graph.add_node('validation', validate_material); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()