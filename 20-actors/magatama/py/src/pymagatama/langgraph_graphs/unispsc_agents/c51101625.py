from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): data: dict; logs: Annotated[list, operator.add]; def validate_purity(state: PharmState): purity = state['data'].get('purity', 0); return {'logs': ['Purity check passed'] if purity >= 98.0 else ['Purity check failed']}; def verify_compliance(state: PharmState): return {'logs': ['Compliance verified']}; graph = StateGraph(PharmState); graph.add_node('val_purity', validate_purity); graph.add_node('val_comp', verify_compliance); graph.set_entry_point('val_purity'); graph.add_edge('val_purity', 'val_comp'); graph.add_edge('val_comp', END); graph = graph.compile()