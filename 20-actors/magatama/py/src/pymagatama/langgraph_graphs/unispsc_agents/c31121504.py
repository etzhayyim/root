from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_spec(state: CastState): if 'material' in state['specs'] and 'tolerance' in state['specs']: state['validation_report'] = 'Valid specifications'; state['approved'] = True; else: state['validation_report'] = 'Missing critical specs'; state['approved'] = False; return state; graph = StateGraph(CastState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()