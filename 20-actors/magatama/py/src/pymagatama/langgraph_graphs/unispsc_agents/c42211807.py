from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OralCareState(TypedDict): product_id: str; compliance_docs: List[str]; approved: bool; def validate_compliance(state: OralCareState): state['approved'] = 'ISO_13485' in state['compliance_docs']; return state; graph = StateGraph(OralCareState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()