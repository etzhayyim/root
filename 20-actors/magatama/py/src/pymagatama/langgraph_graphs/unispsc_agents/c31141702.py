from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RubberState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_blow_molding(state: RubberState): specs = state['specs']; errors = []; if specs.get('hardness') is None: errors.append('Hardness missing'); if specs.get('tolerance') == 0: errors.append('Tolerance invalid'); state['valid'] = len(errors) == 0; state['error_log'] = errors; return state; graph = StateGraph(RubberState); graph.add_node('validate', validate_blow_molding); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()