from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TranscriptionState(TypedDict): input_file: str; language_source: str; detected_language: str; transcription_result: str; status: str; def validate_file(state: TranscriptionState): return {'status': 'validated'} if state.get('input_file') else {'status': 'error'}; def transcribe_audio(state: TranscriptionState): return {'transcription_result': 'transcribed_text_placeholder', 'status': 'completed'}; graph = StateGraph(TranscriptionState); graph.add_node('validate', validate_file); graph.add_node('transcribe', transcribe_audio); graph.set_entry_point('validate'); graph.add_edge('validate', 'transcribe'); graph.add_edge('transcribe', END); compile_graph = graph.compile()