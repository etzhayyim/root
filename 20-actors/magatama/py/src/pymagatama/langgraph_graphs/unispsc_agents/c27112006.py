from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ScytheState(TypedDict): input_spec: dict; validation_results: list; approved: bool; def validate_blade_spec(state: ScytheState): results = []; spec = state['input_spec']; if 'blade_material_grade' not in spec: results.append('Missing material specification'); return {'validation_results': results, 'approved': len(results) == 0}; graph = StateGraph(ScytheState); graph.add_node('validate', validate_blade_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()