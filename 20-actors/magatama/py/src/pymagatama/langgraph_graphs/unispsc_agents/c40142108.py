from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeSpecs(TypedDict): specs: dict; compliance_check: bool; def validate_specs(state: PipeSpecs): state['compliance_check'] = all(k in state['specs'] for k in ['alloy', 'pressure', 'dimension']); return state; graph = StateGraph(PipeSpecs); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()