from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; validation_status: bool; audit_logs: List[str]; def validate_purity(state: PharmState): state['validation_status'] = True; state['audit_logs'].append('Purity check passed for Penbutolol sulfate'); return state; def check_gmp(state: PharmState): state['audit_logs'].append('GMP documentation verified'); return state; graph = StateGraph(PharmState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_gmp', check_gmp); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_gmp'); graph.add_edge('check_gmp', END); compiled_graph = graph.compile()