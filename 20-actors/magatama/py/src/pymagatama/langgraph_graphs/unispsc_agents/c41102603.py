from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validator(state: AgentState): state['validated'] = all('ISO' in c for c in state['certs']); return state; graph = StateGraph(AgentState); graph.add_node('validate', validator); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()