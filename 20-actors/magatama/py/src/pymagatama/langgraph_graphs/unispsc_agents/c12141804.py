from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): chemical_id: str; purity: float; status: str; safety_checks: Annotated[Sequence[str], operator.add]; def validate_purity(state: CatalystState): return {'status': 'validated' if state['purity'] >= 0.99 else 'rejected'}; def perform_safety_check(state: CatalystState): return {'safety_checks': ['sds_verified', 'storage_compliant']}; graph = StateGraph(CatalystState); graph.add_node('validate', validate_purity); graph.add_node('safety', perform_safety_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()