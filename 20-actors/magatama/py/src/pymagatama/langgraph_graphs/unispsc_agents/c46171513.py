from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): component_id: str; specs: dict; valid: bool; def validate_specs(state: State): state['valid'] = 'Material Grade' in state['specs'] and 'Dimensional Tolerance' in state['specs']; return state; def check_security(state: State): return 'secure_route' if state.get('security_req', False) else 'standard_route'; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()