from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): device_id: str; compliance: bool; status: str; def validate_device(state: State): state['compliance'] = True if state['device_id'] else False; state['status'] = 'VALIDATED' if state['compliance'] else 'REJECTED'; return state; graph = StateGraph(State); graph.add_node('validate', validate_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()