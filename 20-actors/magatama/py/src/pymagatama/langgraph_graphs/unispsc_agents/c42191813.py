from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec_data: dict; def validate_specs(state: State): return {'status': 'validated' if 'material' in state['spec_data'] else 'missing_data'}; def check_compliance(state: State): return {'status': 'compliant' if state['status'] == 'validated' else 'non_compliant'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()