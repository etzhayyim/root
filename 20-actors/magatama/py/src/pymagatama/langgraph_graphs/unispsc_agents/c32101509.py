from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PCBState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: PCBState): state['validation_report'] = 'Validation Success' if 'IPC' in state['specs'] else 'Validation Failed'; state['approved'] = 'IPC' in state['specs']; return state; builder = StateGraph(PCBState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()