from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): input_data: dict; validation_results: List[str]; graph = StateGraph(GraphState); def validate_specs(state: GraphState): state['validation_results'] = ["Check throughput"] if state['input_data'].get('throughput', 0) > 0 else ["Error: Missing throughput"]; return state; graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()