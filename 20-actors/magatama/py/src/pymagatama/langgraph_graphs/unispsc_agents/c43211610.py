from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): locale: str; compliance_check: bool; components: List[str]; def validate_locale(state: KitState): return {"compliance_check": state.get("locale") is not None}; def finalize_kit(state: KitState): return {"components": ["localized_driver", "kbd_map", "reg_certs"]}; graph = StateGraph(KitState); graph.add_node("validate", validate_locale); graph.add_node("finalize", finalize_kit); graph.set_entry_point("validate"); graph.add_edge("validate", "finalize"); graph.add_edge("finalize", END); graph = graph.compile()