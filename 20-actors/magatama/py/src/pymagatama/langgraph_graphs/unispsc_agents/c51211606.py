from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): medication_name: str; validation_status: bool; compliance_docs: List[str]; def validate_regulatory(state: DrugState): state['validation_status'] = True; return state; def check_docs(state: DrugState): return {'compliance_docs': ['GMP_CERT', 'FDA_APPROVAL']}; graph = StateGraph(DrugState); graph.add_node('regulatory_check', validate_regulatory); graph.add_node('doc_verification', check_docs); graph.set_entry_point('regulatory_check'); graph.add_edge('regulatory_check', 'doc_verification'); graph.add_edge('doc_verification', END); compiled_graph = graph.compile()