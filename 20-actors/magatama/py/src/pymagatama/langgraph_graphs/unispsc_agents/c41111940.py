from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SensorState(TypedDict): specs: dict; valid: bool; error: str; def validate_sensor(state: SensorState): specs = state['specs']; state['valid'] = 'spectral_range' in specs and specs['response_time_ms'] < 100; return state; graph = StateGraph(SensorState); graph.add_node('validate', validate_sensor); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()