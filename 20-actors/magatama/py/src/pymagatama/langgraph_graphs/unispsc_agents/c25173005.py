from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PlateState(TypedDict): specs: dict; compliance: bool; log: List[str]; def validate_specs(state: PlateState): state['compliance'] = 'Material Grade' in state['specs'] and 'Dimensional Tolerance' in state['specs']; return state; builder = StateGraph(PlateState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()