from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ImpellerState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: ImpellerState): state['validation_passed'] = 'material' in state['specs'] and 'balancing' in state['specs']; return state; def check_export_control(state: ImpellerState): print('Checking dual-use compliance...'); return state; graph = StateGraph(ImpellerState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()