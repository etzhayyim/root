from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalSpecState(TypedDict): device_id: str; compliance_docs: List[str]; validated: bool; def validate_docs(state: DentalSpecState): required = {'ISO_13485', 'Toxicity_Report'}; state['validated'] = all(doc in state['compliance_docs'] for doc in required); return state; graph = StateGraph(DentalSpecState); graph.add_node('validate', validate_docs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()