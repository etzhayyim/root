from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; valid: bool; def validate_band(state: State): specs = state['specs']; state['valid'] = 'tensile_strength' in specs and specs['tensile_strength'] > 0; return state; graph = StateGraph(State); graph.add_node('validate', validate_band); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph_app = graph.compile()