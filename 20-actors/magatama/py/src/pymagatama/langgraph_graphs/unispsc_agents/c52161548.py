from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): specs: dict; is_validated: bool; def validate_turntable_specs(state: GraphState): specs = state['specs']; state['is_validated'] = all(k in specs for k in ['wow_and_flutter', 'snr']) and specs['snr'] > 60; return state; graph = StateGraph(GraphState); graph.add_node('validate', validate_turntable_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()