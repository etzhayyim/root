from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): cas_number: str; purity: float; status: str; safety_checked: bool; def validate_purity(state: CatalystState): state['status'] = 'VALIDATED' if state['purity'] > 0.99 else 'REJECTED'; return state; def check_safety(state: CatalystState): state['safety_checked'] = True; return state; graph = StateGraph(CatalystState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()