from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): item_list: List[str]; validation_results: dict; def validate_supplies(state: KitState): return {'validation_results': {'status': 'verified', 'count': len(state['item_list'])}}; graph = StateGraph(KitState); graph.add_node('validate', validate_supplies); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()