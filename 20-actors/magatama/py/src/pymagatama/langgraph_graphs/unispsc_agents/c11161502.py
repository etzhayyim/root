from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MiningState(TypedDict): commodity_code: str; purity: float; status: str; def validate_purity(state: MiningState): return {'status': 'approved' if state['purity'] > 0.95 else 'rejected'}; def process_smelting(state: MiningState): return {'status': 'processed'}; graph = StateGraph(MiningState); graph.add_node('validate', validate_purity); graph.add_node('smelt', process_smelting); graph.add_edge('validate', 'smelt'); graph.add_edge('smelt', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()