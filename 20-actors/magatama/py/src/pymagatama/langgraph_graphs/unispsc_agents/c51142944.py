from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplierState(TypedDict): supplier_name: str; compliance_docs: List[str]; approved: bool; def check_compliance(state: SupplierState): state['approved'] = 'COA' in state['compliance_docs'] and 'GMP_Cert' in state['compliance_docs']; return state; builder = StateGraph(SupplierState); builder.add_node('verify', check_compliance); builder.set_entry_point('verify'); builder.add_edge('verify', END); graph = builder.compile()