from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): component_id: str; specs: dict; valid: bool; def validate_integrity(state: AssemblyState) -> AssemblyState: state['valid'] = all(k in state['specs'] for k in ['ndt', 'bonding', 'material']); return state; def export_compliance_check(state: AssemblyState) -> AssemblyState: print('Checking ITAR/EAR compliance...'); return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_integrity); graph.add_node('compliance', export_compliance_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compile_graph = graph.compile()