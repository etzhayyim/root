from langgraph.graph import StateGraph, END; from typing import TypedDict; class DentalState(TypedDict): vendor_specs: dict; is_compliant: bool; def validate_specs(state: DentalState): state['is_compliant'] = all(k in state['vendor_specs'] for k in ['material', 'iso_cert']); return state; graph = StateGraph(DentalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()