from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): specs: dict; ready: bool; validation_errors: List[str]; def validate_specs(state: MachineState): errors = []; if 'spindle_speed' not in state['specs']: errors.append('Missing spindle speed'); return {'validation_errors': errors, 'ready': len(errors) == 0}; def finalize_procurement(state: MachineState): return {'ready': True}; graph = StateGraph(MachineState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', lambda s: 'finalize' if s['ready'] else END, {'finalize': 'finalize'}); graph.add_edge('finalize', END); graph.compile()