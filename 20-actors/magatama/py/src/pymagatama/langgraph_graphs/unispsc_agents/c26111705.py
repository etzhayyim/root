from langgraph.graph import StateGraph, END; from typing import TypedDict; class BatteryState(TypedDict): specs: dict; valid: bool; def validate_battery(state: BatteryState): return {'valid': state['specs'].get('voltage') is not None}; graph = StateGraph(BatteryState); graph.add_node('validate', validate_battery); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()