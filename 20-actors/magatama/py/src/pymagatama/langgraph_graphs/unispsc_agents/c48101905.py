from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CupSpecState(TypedDict): specs: dict; status: str; def validate_compliance(state: CupSpecState): state['status'] = 'COMPLIANT' if 'food_grade_certification' in state['specs'] else 'REJECTED'; return state; graph = StateGraph(CupSpecState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()