from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; gmp_certified: bool; purity: float; status: str; def validate_gmp(state: DrugState): state['gmp_certified'] = True; return state; def check_purity(state: DrugState): state['status'] = 'approved' if state['purity'] >= 99.0 else 'rejected'; return state; graph = StateGraph(DrugState); graph.add_node('validate_gmp', validate_gmp); graph.add_node('check_purity', check_purity); graph.add_edge('validate_gmp', 'check_purity'); graph.add_edge('check_purity', END); graph.set_entry_point('validate_gmp'); graph = graph.compile()