from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): device_id: str; quality_score: float; needs_inspection: bool; def check_compliance(state: SurgicalState): return {'needs_inspection': state['quality_score'] < 0.95}; def approve_procurement(state: SurgicalState): return {'status': 'Approved'}; graph = StateGraph(SurgicalState); graph.add_node('compliance_check', check_compliance); graph.add_node('approval', approve_procurement); graph.set_entry_point('compliance_check'); graph.add_edge('compliance_check', 'approval'); graph.add_edge('approval', END); graph = graph.compile()