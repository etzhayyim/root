from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AxleProcurementState(TypedDict): axle_specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_specs(state: AxleProcurementState): results = []; specs = state['axle_specs']; if 'gear_ratio' not in specs: results.append('Missing gear ratio'); return {'validation_results': results, 'status': 'VALIDATED' if not results else 'FAILED'}; graph = StateGraph(AxleProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()