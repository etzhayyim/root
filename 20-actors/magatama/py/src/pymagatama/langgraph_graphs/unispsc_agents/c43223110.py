from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CoreEquipmentState(TypedDict): equipment_id: str; compliance_verified: bool; network_specs: dict; def validate_specs(state: CoreEquipmentState): state['compliance_verified'] = '3GPP' in state['network_specs']; return state; def check_export_control(state: CoreEquipmentState): return {'compliance_verified': True}; graph = StateGraph(CoreEquipmentState); graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()