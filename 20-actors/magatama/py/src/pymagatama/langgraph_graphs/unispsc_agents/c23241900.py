from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): metal_spec: dict; validation_status: bool; def validate_specs(state: State): state['validation_status'] = state['metal_spec'].get('accuracy', 0) < 0.01; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()