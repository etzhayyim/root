from typing import TypedDict, Annotated, List; import operator; from langgraph.graph import StateGraph, END; class SpacerState(TypedDict): specs: dict; validation_errors: Annotated[List[str], operator.add]; status: str; def validate_specs(state: SpacerState): errs = []; if not state['specs'].get('material'): errs.append('Material missing'); if state['specs'].get('outer_diameter', 0) <= 0: errs.append('Invalid OD'); return {'validation_errors': errs, 'status': 'valid' if not errs else 'invalid'}; graph = StateGraph(SpacerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()