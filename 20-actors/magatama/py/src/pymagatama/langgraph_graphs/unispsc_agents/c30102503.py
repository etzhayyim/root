from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IronState(TypedDict): specs: dict; valid: bool; def validate_specs(state: IronState): state['valid'] = 'Thickness' in state['specs'] and 'Material Grade' in state['specs']; return state; graph = StateGraph(IronState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()