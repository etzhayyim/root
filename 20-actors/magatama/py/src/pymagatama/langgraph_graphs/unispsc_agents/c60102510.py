from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NumberKitState(TypedDict): input_data: dict; validation_results: List[str]; approved: bool; def validate_kit(state: NumberKitState): results = []; if 'pieces' not in state['input_data']: results.append('Missing piece count'); if state['input_data'].get('material') not in ['plastic', 'metal', 'wood']: results.append('Invalid material'); return {'validation_results': results, 'approved': len(results) == 0}; graph = StateGraph(NumberKitState); graph.add_node('validate', validate_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()