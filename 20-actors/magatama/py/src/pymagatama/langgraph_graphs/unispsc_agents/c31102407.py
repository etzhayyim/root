from langgraph.graph import StateGraph, END; from typing import TypedDict; class CastingState(TypedDict): specs: dict; valid: bool; def validate_casting(state: CastingState): state["valid"] = all(key in state["specs"] for key in ["alloy", "tolerance"]); return state; graph = StateGraph(CastingState); graph.add_node("validate", validate_casting); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()