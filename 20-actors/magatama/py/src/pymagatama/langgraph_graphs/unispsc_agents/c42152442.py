from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): material_spec: str; validation_results: List[str]; graph = StateGraph(State); def validate_purity(state: State): return {'validation_results': ['Purity verified against ASTM F67']}; def check_iso_cert(state: State): return {'validation_results': ['ISO 13485 certified']}; graph.add_node('purity_check', validate_purity); graph.add_node('iso_check', check_iso_cert); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'iso_check'); graph.add_edge('iso_check', END); graph = graph.compile()