from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; compliant: bool; def validate_purity(state: PharmState): return {'compliant': state['purity'] >= 99.9}; def finalize_report(state: PharmState): print('Compliance checked'); graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('finish', finalize_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'finish'); graph.add_edge('finish', END); compiled_graph = graph.compile()