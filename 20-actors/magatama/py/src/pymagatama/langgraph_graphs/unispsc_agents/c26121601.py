from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; valid: bool; def validate_specs(state: State): specs = state['specs']; is_valid = 'Voltage rating' in specs and 'Max maintenance temperature' in specs; return {'valid': is_valid}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()