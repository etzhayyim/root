from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class MineralState(TypedDict): purity: float; composition: dict; workflow_log: Annotated[list, operator.add]; def validate_purity(state: MineralState): return {'workflow_log': ['Purity check passed' if state['purity'] > 98.0 else 'Purity failed']}; def analyze_composition(state: MineralState): return {'workflow_log': ['Composition verified']}; graph = StateGraph(MineralState); graph.add_node('validate', validate_purity); graph.add_node('analyze', analyze_composition); graph.set_entry_point('validate'); graph.add_edge('validate', 'analyze'); graph.add_edge('analyze', END); graph = graph.compile()