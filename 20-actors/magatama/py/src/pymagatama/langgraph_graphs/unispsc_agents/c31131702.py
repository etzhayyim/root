from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): material_spec: str; tolerance: float; validated: bool; inspection_logs: List[str]; def validate_material(state: ForgingState): state['validated'] = state['material_spec'] in ['4140', '4340']; return state; def check_tolerance(state: ForgingState): if state['tolerance'] < 0.05: state['inspection_logs'].append('Precision OK'); return state; graph = StateGraph(ForgingState); graph.add_node('material_check', validate_material); graph.add_node('tolerance_check', check_tolerance); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'tolerance_check'); graph.add_edge('tolerance_check', END); graph = graph.compile()