from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): ingredient: str; validated: bool; compliance_docs: List[str]; def validate_purity(state: PharmaState): return {'validated': True} if 'GMP' in state.get('compliance_docs', []) else {'validated': False}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()