from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; approval_status: bool; workflow_step: str; def validate_spec(state: State): state['workflow_step'] = 'validating_spec'; state['approval_status'] = 'ISO_13485_Certification' in state['input_data']; return state; def regulatory_check(state: State): state['workflow_step'] = 'regulatory_review'; return state; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('regulatory', regulatory_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()