from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AxleState(TypedDict): specs: dict; validation_report: str; graph = StateGraph(AxleState); def validate_spindle(state: AxleState): state['validation_report'] = 'Validation passed: tolerance within 0.005mm' if state['specs'].get('precision') == 'high' else 'Validation failed: precision insufficient'; return state; graph.add_node('validate', validate_spindle); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()