from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): input_data: dict; validation_results: List[str]; graph = StateGraph(State); def validate_spec(state: State): results = []; data = state['input_data']; if 'diameter' not in data: results.append('Missing diameter'); if 'material' not in data: results.append('Missing material'); return {'validation_results': results}; graph.add_node('spec_validator', validate_spec); graph.set_entry_point('spec_validator'); graph.add_edge('spec_validator', END); graph = graph.compile()