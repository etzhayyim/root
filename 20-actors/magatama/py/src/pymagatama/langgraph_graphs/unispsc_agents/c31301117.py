from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(ForgingState); def validate_metallurgy(state: ForgingState): state['validation_results'].append('Metallurgy validated against AMS standards'); return state; def validate_geometry(state: ForgingState): state['validation_results'].append('Geometry validated via CAD simulation'); return state; graph.add_node('val_meta', validate_metallurgy); graph.add_node('val_geom', validate_geometry); graph.set_entry_point('val_meta'); graph.add_edge('val_meta', 'val_geom'); graph.add_edge('val_geom', END); graph = graph.compile()