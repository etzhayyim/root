from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medication: str; quality_checks: List[str]; approved: bool; def validate_gmp(state: PharmaState): state['quality_checks'].append('GMP_Verified'); state['approved'] = True; return state; def check_storage(state: PharmaState): state['quality_checks'].append('Temp_Controlled'); return state; graph = StateGraph(PharmaState); graph.add_node('gmp_check', validate_gmp); graph.add_node('storage_check', check_storage); graph.set_entry_point('gmp_check'); graph.add_edge('gmp_check', 'storage_check'); graph.add_edge('storage_check', END); graph = graph.compile()