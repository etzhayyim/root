from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; def validate_tolerance(state: BearingState): return {'validation_results': ['Tolerance checked against ISO 3290']}; def check_material(state: BearingState): return {'validation_results': ['Material cert verified']}; graph = StateGraph(BearingState); graph.add_node('validate_tol', validate_tolerance); graph.add_node('check_mat', check_material); graph.set_entry_point('validate_tol'); graph.add_edge('validate_tol', 'check_mat'); graph.add_edge('check_mat', END); graph = graph.compile()