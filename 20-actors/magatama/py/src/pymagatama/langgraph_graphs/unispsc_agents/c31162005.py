from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RoofingNailState(TypedDict): specs: List[str]; validation_passed: bool; def validate_spec(state: RoofingNailState) -> dict: return {'validation_passed': all(spec in state['specs'] for spec in ['material', 'coating'])}; graph = StateGraph(RoofingNailState); graph.add_node('validator', validate_spec); graph.set_entry_point('validator'); graph.add_edge('validator', END); compile_graph = graph.compile()