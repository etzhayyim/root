from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OphthalmicState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_spec(state: OphthalmicState): state['compliance'] = all(k in state['specs'] for k in ['load_capacity', 'cert']); return state; def final_decision(state: OphthalmicState): state['approved'] = state['compliance']; return state; graph = StateGraph(OphthalmicState); graph.add_node('validate', validate_spec); graph.add_node('decision', final_decision); graph.set_entry_point('validate'); graph.add_edge('validate', 'decision'); graph.add_edge('decision', END); app = graph.compile()