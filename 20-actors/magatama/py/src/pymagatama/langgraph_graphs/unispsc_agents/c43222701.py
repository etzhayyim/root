from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class TelegraphState(TypedDict): specs: dict; valid: bool; def validate_key_specs(state: TelegraphState): state['valid'] = 'contact_material' in state['specs'] and state.get('specs', {}).get('base_weight_grams', 0) > 200; return state; graph = StateGraph(TelegraphState); graph.add_node('validator', validate_key_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); app = graph.compile()