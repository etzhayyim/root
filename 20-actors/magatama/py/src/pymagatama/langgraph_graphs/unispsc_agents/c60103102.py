from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ChartState(TypedDict): specs: dict; valid: bool; def validate_spec(state: ChartState): state['valid'] = 'dimensions_mm' in state['specs'] and 'material_type' in state['specs']; return state; builder = StateGraph(ChartState); builder.add_node('validate', validate_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()