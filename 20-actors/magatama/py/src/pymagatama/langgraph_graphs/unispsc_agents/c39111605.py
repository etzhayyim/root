from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LightingState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: LightingState): status = 'Pass' if state['specs'].get('IP_rating', 0) >= 65 else 'Fail'; return {'validation_results': [status]}; def check_power(state: LightingState): return {'validation_results': state['validation_results'] + ['Power checked']}; graph = StateGraph(LightingState); graph.add_node('validation', validate_specs); graph.add_node('power_check', check_power); graph.add_edge('validation', 'power_check'); graph.add_edge('power_check', END); graph.set_entry_point('validation'); graph = graph.compile()