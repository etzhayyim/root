from langgraph.graph import StateGraph; from typing import TypedDict, List; class AssemblyState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: AssemblyState): state['valid'] = 'Material Grade' in state['specs'] and 'Pressure Rating' in state['specs']; return state; def check_compliance(state: AssemblyState): state['logs'].append('Performing ASTM standard verification'); return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', '__end__'); graph = graph.compile()