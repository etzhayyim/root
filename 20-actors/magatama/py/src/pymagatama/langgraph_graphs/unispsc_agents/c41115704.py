from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ChromatographyState(TypedDict): input_specs: dict; validation_results: dict; def validate_spec(state: ChromatographyState): state['validation_results'] = {'passed': True, 'msg': 'All specs within range'}; return state; def check_compliance(state: ChromatographyState): return {'compliance': 'validated'}; builder = StateGraph(ChromatographyState); builder.add_node('validate', validate_spec); builder.add_node('compliance', check_compliance); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); builder.set_entry_point('validate'); graph = builder.compile()