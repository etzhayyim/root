from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): items: List[str]; validation_results: dict; def validate_tools(state: KitState): state['validation_results'] = {'passed': all(['screwdriver' in state['items'], 'tweezers' in state['items']])}; return state; builder = StateGraph(KitState); builder.add_node('validate', validate_tools); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()