from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LabState(TypedDict): device_id: str; specs: dict; validation_status: bool; def validate_equipment(state: LabState): state['validation_status'] = all(k in state['specs'] for k in ['accuracy', 'cal_date']); return state; graph = StateGraph(LabState); graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()