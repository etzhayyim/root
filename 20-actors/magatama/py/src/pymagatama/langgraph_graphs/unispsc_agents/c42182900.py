from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExamTableState(TypedDict): model_info: str; compliance_docs: List[str]; validated: bool; def validate_specs(state: ExamTableState): is_valid = all(['cert' in state['compliance_docs'], 'model' in state['model_info']]); return {'validated': is_valid}; graph = StateGraph(ExamTableState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()