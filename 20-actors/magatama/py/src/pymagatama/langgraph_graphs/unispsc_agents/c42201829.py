from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LocatorState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: LocatorState): specs = state.get('specs', {}); state['valid'] = 'radiation_attenuation_rating' in specs and 'ISO_13485_certification' in specs; return state; graph = StateGraph(LocatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()