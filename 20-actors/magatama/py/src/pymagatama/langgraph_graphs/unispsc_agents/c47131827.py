from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): chemical_data: dict; compliance_flag: bool; def validate_sds(state: AgentState): return {'compliance_flag': state['chemical_data'].get('sds_present', False)}; def check_hazmat(state: AgentState): return {'compliance_flag': state['chemical_data'].get('hazard_class') != 'prohibited'}; graph = StateGraph(AgentState); graph.add_node('validate_sds', validate_sds); graph.add_node('check_hazmat', check_hazmat); graph.set_entry_point('validate_sds'); graph.add_edge('validate_sds', 'check_hazmat'); graph.add_edge('check_hazmat', END); compiled_graph = graph.compile()