from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): alloy_type: str; tolerances: dict; status: str; def validate_specs(state: CastingState): return {'status': 'validated'} if state.get('alloy_type') else {'status': 'rejected'}; def process_casting(state: CastingState): return {'status': 'processed'}; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.add_node('process', process_casting); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()