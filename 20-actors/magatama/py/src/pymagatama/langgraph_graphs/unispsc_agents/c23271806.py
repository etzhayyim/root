from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SolderState(TypedDict): alloy_spec: str; compliance: bool; lead_free: bool; def validate_alloy(state: SolderState): return {'compliance': state['lead_free'] and 'Pb' not in state['alloy_spec']}; def check_msds(state: SolderState): print('Checking MSDS requirements...'); return {'compliance': True}; graph = StateGraph(SolderState); graph.add_node('validate', validate_alloy); graph.add_node('msds', check_msds); graph.set_entry_point('validate'); graph.add_edge('validate', 'msds'); graph.add_edge('msds', END); app = graph.compile()