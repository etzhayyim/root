from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FrameState(TypedDict): specs: dict; valid: bool; def validate_specs(state: FrameState): state['valid'] = 'material_composition' in state['specs'] and 'hinge_durability_cycles' in state['specs']; return state; builder = StateGraph(FrameState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()