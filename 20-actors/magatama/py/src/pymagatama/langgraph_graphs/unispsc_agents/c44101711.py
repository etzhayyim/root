from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: CompressorState): errors = []; if state['specs'].get('pressure', 0) > 100: errors.append('Pressure exceeds threshold'); return {'validation_results': errors}; def finalize_procurement(state: CompressorState): return {'validation_results': ['Procurement Ready']}; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()