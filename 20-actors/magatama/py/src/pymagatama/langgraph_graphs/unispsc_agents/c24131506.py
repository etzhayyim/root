from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TankState(TypedDict): capacity: float; temp_range: str; certs_verified: bool; graph = StateGraph(TankState); def validate_specs(state: TankState): print(f'Validating tank capacity: {state["capacity"]}'); return {'certs_verified': True}; def check_compliance(state: TankState): print('Checking ISO/FDA compliance...'); return {'certs_verified': True}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()