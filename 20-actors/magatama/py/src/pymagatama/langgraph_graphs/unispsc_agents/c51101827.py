from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): chemical_name: str; validation_status: bool; compliance_docs: List[str]; def validate_purity(state: SupplyState): state['validation_status'] = True; return state; def check_docs(state: SupplyState): return {'validation_status': len(state['compliance_docs']) > 2}; graph = StateGraph(SupplyState); graph.add_node('validate', validate_purity); graph.add_node('doc_check', check_docs); graph.add_edge('validate', 'doc_check'); graph.add_edge('doc_check', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()