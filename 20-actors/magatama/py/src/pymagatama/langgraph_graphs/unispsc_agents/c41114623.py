from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): spec: dict; validated: bool; log: List[str]; def validate_spec(state: ProcState): state['validated'] = 'load_capacity_kn' in state['spec']; state['log'].append('Validation complete'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()