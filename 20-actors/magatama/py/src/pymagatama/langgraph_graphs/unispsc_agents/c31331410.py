from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): items: Annotated[Sequence[str], operator.add]; validation_results: dict; def validate_structural_integrity(state: AssemblyState): return {'validation_results': {'status': 'passed', 'method': 'NDT'}}; def check_export_controls(state: AssemblyState): return {'validation_results': {'regulation': 'ITAR/EAR'}}; graph = StateGraph(AssemblyState); graph.add_node('integrity_check', validate_structural_integrity); graph.add_node('export_check', check_export_controls); graph.set_entry_point('integrity_check'); graph.add_edge('integrity_check', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()