from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec: dict; approved: bool; def validate_spec(state: ProcState): state['approved'] = 'regulatory_approval_id' in state['spec']; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()