from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): metal_grade: str; safety_clearance: bool; graph = StateGraph(ProcureState); def check_toxicity(state): return {'safety_clearance': True}; def validate_specs(state): return {'metal_grade': 'Beryllium_High_Purity'}; graph.add_node('toxicity_check', check_toxicity); graph.add_node('validate', validate_specs); graph.set_entry_point('toxicity_check'); graph.add_edge('toxicity_check', 'validate'); graph.add_edge('validate', END); graph = graph.compile()