from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): tubing_specs: dict; validation_results: List[str]; def validate_specs(state: State): specs = state['tubing_specs']; results = []; if specs.get('temp_range', 0) < 80: results.append('Low temperature resistance'); return {'validation_results': results}; def check_compliance(state: State): return {'validation_results': state['validation_results'] + ['Compliance verified']}; graph = StateGraph(State); graph.add_node('validator', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validator'); graph.add_edge('validator', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()