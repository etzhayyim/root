from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CleaningState(TypedDict): chemical_data: dict; compliance_report: str; approval_status: bool; def validate_sds(state: CleaningState): return {'compliance_report': 'SDS Verified', 'approval_status': True}; def check_hazmat(state: CleaningState): return {'approval_status': 'DANGEROUS_CHECKED'}; graph = StateGraph(CleaningState); graph.add_node('validate_sds', validate_sds); graph.add_node('check_hazmat', check_hazmat); graph.set_entry_point('validate_sds'); graph.add_edge('validate_sds', 'check_hazmat'); graph.add_edge('check_hazmat', END); graph = graph.compile()