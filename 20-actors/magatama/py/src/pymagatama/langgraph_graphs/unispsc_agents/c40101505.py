from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DiffuserState(TypedDict): airflow_data: dict; validation_errors: Annotated[list, operator.add]; def validate_airflow(state: DiffuserState): result = {'validation_errors': []} if state['airflow_data'].get('cfm', 0) <= 0: result['validation_errors'].append('Invalid airflow capacity'); return result; def check_compliance(state: DiffuserState): return {'validation_errors': ['Pending regulatory compliance lookup']}; graph = StateGraph(DiffuserState); graph.add_node('validate', validate_airflow); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()