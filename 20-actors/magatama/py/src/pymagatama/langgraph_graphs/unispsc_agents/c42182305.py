from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; def validate_set(state: ProcState): return {'validated': all(item in ['needle', 'syringe', 'drape'] for item in state['items'])}; graph = StateGraph(ProcState); graph.add_node('validate', validate_set); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()