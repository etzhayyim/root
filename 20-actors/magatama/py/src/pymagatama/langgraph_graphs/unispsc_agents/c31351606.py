from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; validation_passed: bool; proc_status: str; def validate_specs(state: ProcState): state['validation_passed'] = 'Material' in state['specs'] and 'Pressure' in state['specs']; return state; def finalize_order(state: ProcState): state['proc_status'] = 'READY' if state['validation_passed'] else 'REJECTED'; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()