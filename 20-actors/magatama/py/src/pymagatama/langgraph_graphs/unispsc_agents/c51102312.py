from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): data: dict; verification_status: str; def validate_purity(state: PharmState): return {'verification_status': 'passed' if state['data'].get('purity') > 99.0 else 'failed'}; def check_compliance(state: PharmState): return {'verification_status': 'compliant' if 'GMP' in state['data'].get('certs', []) else 'non-compliant'}; graph = StateGraph(PharmState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_compliance'); graph.add_edge('check_compliance', END); graph = graph.compile()