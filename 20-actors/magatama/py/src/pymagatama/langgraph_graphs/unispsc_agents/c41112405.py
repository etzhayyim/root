from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): pressure_data: float; validation_passed: bool; logs: List[str]; def validate_sensor(state: GraphState): state['validation_passed'] = 0.0 <= state['pressure_data'] <= 1000.0; state['logs'].append('Validation complete'); return state; graph = StateGraph(GraphState); graph.add_node('validate', validate_sensor); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()