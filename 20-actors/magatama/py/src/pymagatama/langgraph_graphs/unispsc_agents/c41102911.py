from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class HistologyState(TypedDict): items: List[str]; validated_items: List[str]; error_log: List[str]; def validate_blade_spec(state: HistologyState): return {'validated_items': [i for i in state['items'] if 'quality_cert' in i]}; def check_compliance(state: HistologyState): return {'error_log': ['Compliance check passed'] if state['validated_items'] else ['Missing specs']}; graph = StateGraph(HistologyState); graph.add_node('validate', validate_blade_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()