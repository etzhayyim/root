from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ContainerState(TypedDict): vendor_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ContainerState): errors = []; if 'temperature_range' not in state['vendor_specs']: errors.append('Missing T-range'); return {'validation_errors': errors}; def check_approval(state: ContainerState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(ContainerState); graph.add_node('validate', validate_specs); graph.add_node('check', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.compile()