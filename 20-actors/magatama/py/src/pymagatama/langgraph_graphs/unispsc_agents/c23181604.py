from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PressProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: PressProcurementState): errors = []; if state['specs'].get('bending_capacity_tons', 0) < 50: errors.append('Capacity below industrial requirements.'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(PressProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()