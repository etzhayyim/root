from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ChemicalProcurementState(TypedDict): chemical_code: str; purity_check: bool; safety_compliance: bool; def validate_purity(state: ChemicalProcurementState): return {'purity_check': True} if state['chemical_code'] else {'purity_check': False}; def verify_compliance(state: ChemicalProcurementState): return {'safety_compliance': True}; graph = StateGraph(ChemicalProcurementState); graph.add_node('validate', validate_purity); graph.add_node('verify', verify_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); compiled_graph = graph.compile()