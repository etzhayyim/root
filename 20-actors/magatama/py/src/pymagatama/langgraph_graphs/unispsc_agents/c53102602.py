from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): item: str; specs: dict; approved: bool; def validate_specs(state: ProcurementState): state['approved'] = 'fabric_composition' in state['specs'] and 'flammability_rating' in state['specs']; return state; builder = StateGraph(ProcurementState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()