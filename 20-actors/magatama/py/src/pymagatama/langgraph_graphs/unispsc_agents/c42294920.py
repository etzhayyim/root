from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input_data: dict; validation_errors: List[str]; approved: bool; def check_medical_compliance(state: ProcureState): state['validation_errors'] = [] if 'iso_cert' not in state['input_data'] else []; state['approved'] = len(state['validation_errors']) == 0; return state; graph = StateGraph(ProcureState); graph.add_node('validate', check_medical_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()