from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): material_data: dict; validation_errors: List[str]; def validate_dimensions(state: MaterialState): state['validation_errors'] = []; if 'diameter' not in state['material_data']: state['validation_errors'].append('Missing diameter'); return state; def check_compliance(state: MaterialState): return state; graph = StateGraph(MaterialState); graph.add_node('validate', validate_dimensions); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()