from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_id: str; item_specs: dict; compliance_verified: bool; def validate_specs(state: ProcureState): state['compliance_verified'] = 'ISO13485' in state['item_specs'].get('certifications', []) return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()