from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GammaCounterState(TypedDict): model_specs: dict; compliance_verified: bool; export_check_passed: bool; def validate_specs(state: GammaCounterState): state['compliance_verified'] = 'safety_cert' in state['model_specs']; return state; def verify_export_control(state: GammaCounterState): state['export_check_passed'] = state['model_specs'].get('isotope_type') != 'restricted'; return state; graph = StateGraph(GammaCounterState); graph.add_node('validate', validate_specs); graph.add_node('export_check', verify_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()