from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: List[str]; validated: bool; next_step: str; def validate_specs(state: ProcState): validated = len(state['specs']) > 3; return {'validated': validated, 'next_step': 'approval' if validated else 'revision'}; def approval_node(state: ProcState): return {'next_step': 'finalized'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('approval', approval_node); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph = graph.compile()