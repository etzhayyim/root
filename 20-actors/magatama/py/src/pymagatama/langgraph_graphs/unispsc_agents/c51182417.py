from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; purity_percent: float; validated: bool; def check_purity(state: PharmaState): state['validated'] = state['purity_percent'] >= 99.0; return state; graph = StateGraph(PharmaState); graph.add_node('validate', check_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()