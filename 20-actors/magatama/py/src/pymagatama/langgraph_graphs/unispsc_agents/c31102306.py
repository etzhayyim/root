from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: CastState): is_valid = 'Alloy Grade' in state['specs']; return {'validation_results': ['Passed' if is_valid else 'Failed']}; def finalize_order(state: CastState): return {'validation_results': state['validation_results'] + ['Order Finalized']}; graph = StateGraph(CastState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()