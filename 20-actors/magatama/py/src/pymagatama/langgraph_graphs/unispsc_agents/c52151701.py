from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class UtensilState(TypedDict): items: List[dict]; compliance_check: bool; def validate_materials(state: UtensilState): state['compliance_check'] = all(item.get('food_safe') for item in state['items']); return state; def route_inspection(state: UtensilState): return 'inspect' if state['compliance_check'] else 'reject'; graph = StateGraph(UtensilState); graph.add_node('validate', validate_materials); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()