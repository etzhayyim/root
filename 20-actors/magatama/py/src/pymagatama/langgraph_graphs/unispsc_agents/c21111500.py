from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TillerState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: TillerState): specs = state.get('specs', {}); errors = []; if specs.get('tilling_width', 0) < 300: errors.append('Width too small'); return {'validation_errors': errors}; def check_final(state: TillerState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(TillerState); graph.add_node('validate', validate_specs); graph.add_node('approval', check_final); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph.set_entry_point('validate'); graph = graph.compile()