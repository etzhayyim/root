from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TabletProcurementState(TypedDict): vendor_certs: List[str]; safety_verified: bool; compliance_passed: bool; def validate_cert(state: TabletProcurementState): return {'safety_verified': 'ISO13485' in state['vendor_certs']}; def check_compliance(state: TabletProcurementState): return {'compliance_passed': state['safety_verified']}; graph = StateGraph(TabletProcurementState); graph.add_node('validate_cert', validate_cert); graph.add_node('compliance', check_compliance); graph.add_edge('validate_cert', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate_cert'); graph = graph.compile()