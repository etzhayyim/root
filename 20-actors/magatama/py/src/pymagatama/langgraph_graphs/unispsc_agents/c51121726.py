from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; gmp_certified: bool; quality_report: str; approved: bool; def validate_gmp(state: PharmaState): return {'approved': state['gmp_certified']}; def check_purity(state: PharmaState): return {'quality_report': 'Validated'} if state['chemical_name'] == 'Indapamide' else {'quality_report': 'Failed'}; graph = StateGraph(PharmaState); graph.add_node('validate_gmp', validate_gmp); graph.add_node('check_purity', check_purity); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', 'check_purity'); graph.add_edge('check_purity', END); graph = graph.compile()