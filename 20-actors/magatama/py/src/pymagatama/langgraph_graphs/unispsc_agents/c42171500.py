from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DisasterProductState(TypedDict): items: List[dict]; inspection_status: str; def validate_supplies(state: DisasterProductState): state['inspection_status'] = 'verified' if len(state['items']) > 0 else 'failed'; return state; graph = StateGraph(DisasterProductState); graph.add_node('validate', validate_supplies); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()