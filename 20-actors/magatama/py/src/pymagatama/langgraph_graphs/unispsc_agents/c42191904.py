from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NarcoticCabinetState(TypedDict): cabinet_id: str; security_specs: dict; verification_passed: bool; def validate_security(state: NarcoticCabinetState): state['verification_passed'] = 'lock_type' in state['security_specs'] and state['security_specs']['fire_rating'] >= 60; return state; graph = StateGraph(NarcoticCabinetState); graph.add_node('validate', validate_security); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()