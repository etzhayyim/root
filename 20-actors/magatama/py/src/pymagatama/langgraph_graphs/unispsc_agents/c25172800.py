from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class HydraulicState(TypedDict): component_specs: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(HydraulicState); def validate_specs(state: HydraulicState): return {'validation_results': ['Pressure spec valid' if state['component_specs'].get('pressure', 0) > 0 else 'Invalid Pressure']}; def check_export_compliance(state: HydraulicState): return {'validation_results': ['Dual-use screening complete']}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()