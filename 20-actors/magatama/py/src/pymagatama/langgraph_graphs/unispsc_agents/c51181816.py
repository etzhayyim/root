from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_name: str; purity: float; hazard_verified: bool; compliance_status: str; def validate_purity(state: ChemicalState): return {'hazard_verified': state['purity'] >= 98.0}; def check_compliance(state: ChemicalState): return {'compliance_status': 'COMPLIANT' if state['hazard_verified'] else 'NON-COMPLIANT'}; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()