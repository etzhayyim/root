from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_data: dict; validation_logs: Annotated[Sequence[str], operator.add]; status: str; def validate_chemical(state: ChemicalState): logs = []; purity = state['chemical_data'].get('purity', 0); if purity < 99.0: logs.append('Purity below high-grade threshold'); return {'validation_logs': logs, 'status': 'FLAGGED' if logs else 'APPROVED'}; def compile_graph(): graph = StateGraph(ChemicalState); graph.add_node('validate', validate_chemical); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()