from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DrumState(TypedDict): specs: dict; validation: list[str]; graph = StateGraph(DrumState); def validate_specs(state: DrumState): state['validation'].append('Specifications validated against industry standards'); return state; def check_durability(state: DrumState): state['validation'].append('Durability metrics verified'); return state; graph.add_node('validate', validate_specs); graph.add_node('durability', check_durability); graph.set_entry_point('validate'); graph.add_edge('validate', 'durability'); graph.add_edge('durability', END); graph = graph.compile()