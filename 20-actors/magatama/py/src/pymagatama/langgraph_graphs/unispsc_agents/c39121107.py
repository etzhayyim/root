from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LightingState(TypedDict): device_specs: dict; compliance_check: bool; graph = StateGraph(LightingState); def validate_specs(state: LightingState): return {'compliance_check': 'DALI' in state['device_specs'].get('protocol', '')}; def finalize_order(state: LightingState): return {'compliance_check': True}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()