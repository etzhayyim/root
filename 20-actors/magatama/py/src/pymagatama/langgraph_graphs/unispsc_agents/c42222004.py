from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PCAState(TypedDict): input_spec: dict; validation_results: List[str]; approved: bool; def validate_spec(state: PCAState): results = []; if 'flow_rate' not in state['input_spec']: results.append('Missing flow rate'); return {'validation_results': results, 'approved': len(results) == 0}; graph = StateGraph(PCAState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()