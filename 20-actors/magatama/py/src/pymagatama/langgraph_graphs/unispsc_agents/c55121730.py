from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CasualtyState(TypedDict): tags: List[str]; specs: dict; approved: bool; def validate_specs(state: CasualtyState): state['approved'] = 'reflectivity_standard' in state['specs']; return state; def route_step(state: CasualtyState): return 'end' if state['approved'] else 'error'; graph = StateGraph(CasualtyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_step, {'end': END, 'error': END}); graph.compile()