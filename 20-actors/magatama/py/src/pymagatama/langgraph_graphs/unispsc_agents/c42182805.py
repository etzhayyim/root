from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScaleState(TypedDict): vendor_docs: List[str]; calibration_status: bool; compliance_check: bool; def validate_cert(state: ScaleState): return {'compliance_check': True} if 'ISO13485' in state['vendor_docs'] else {'compliance_check': False}; def verify_calibration(state: ScaleState): return {'calibration_status': True}; graph = StateGraph(ScaleState); graph.add_node('cert_validation', validate_cert); graph.add_node('calibration_check', verify_calibration); graph.set_entry_point('cert_validation'); graph.add_edge('cert_validation', 'calibration_check'); graph.add_edge('calibration_check', END); graph = graph.compile()