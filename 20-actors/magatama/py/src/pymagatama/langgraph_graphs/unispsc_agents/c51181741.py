from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): active_ingredient: str; purity_check: bool; cold_chain_verified: bool; def check_purity(state: PharmState): return {'purity_check': True}; def verify_logistics(state: PharmState): return {'cold_chain_verified': True}; graph = StateGraph(PharmState); graph.add_node('purity_node', check_purity); graph.add_node('logistics_node', verify_logistics); graph.set_entry_point('purity_node'); graph.add_edge('purity_node', 'logistics_node'); graph.add_edge('logistics_node', END); graph = graph.compile()