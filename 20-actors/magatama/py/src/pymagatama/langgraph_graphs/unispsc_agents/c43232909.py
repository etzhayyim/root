from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): firmware_version: str; compliance_checks: List[str]; approved: bool; def validate_firmware(state: NetworkState): state['compliance_checks'].append('security_scan_passed'); state['approved'] = True; return state; graph = StateGraph(NetworkState); graph.add_node('validate', validate_firmware); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()