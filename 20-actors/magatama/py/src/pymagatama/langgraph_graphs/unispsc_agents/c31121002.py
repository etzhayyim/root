from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): input_spec: str; validation_passed: bool; inspection_report: str; def validate_dimensions(state: CastingState): state['validation_passed'] = 'tolerance' in state['input_spec'].lower(); return state; def generate_report(state: CastingState): state['inspection_report'] = 'Standard V-Process QC check complete' if state['validation_passed'] else 'Manual review required'; return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_dimensions); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()