from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DuctState(TypedDict): specs: dict; validation_results: List[str]; def validate_duct(state: DuctState): results = []; if state['specs'].get('fire_rating') != 'UL94-V0': results.append('Fire rating non-compliant'); return {'validation_results': results}; graph_builder = StateGraph(DuctState); graph_builder.add_node('validation', validate_duct); graph_builder.set_entry_point('validation'); graph_builder.add_edge('validation', END); graph = graph_builder.compile()