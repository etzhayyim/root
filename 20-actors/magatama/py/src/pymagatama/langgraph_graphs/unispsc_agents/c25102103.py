from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TruckProcurementState(TypedDict): vehicle_spec: dict; compliance_verified: bool; approval_status: str; def validate_specs(state: TruckProcurementState): return {'compliance_verified': 'Engine Emission Rating' in state['vehicle_spec']}; def route_approval(state: TruckProcurementState): return 'approved' if state['compliance_verified'] else 'rejected'; graph = StateGraph(TruckProcurementState); graph.add_node('validate', validate_specs); graph.add_node('approval', route_approval); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()