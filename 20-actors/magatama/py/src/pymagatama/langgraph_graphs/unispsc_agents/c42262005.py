from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmbalmingState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: EmbalmingState): state['compliant'] = all(k in state['specs'] for k in ['material', 'drainage']); return state; def finalize_order(state: EmbalmingState): return {'compliant': state['compliant']}; graph = StateGraph(EmbalmingState); graph.add_node('validation', validate_specs); graph.add_node('finalization', finalize_order); graph.set_entry_point('validation'); graph.add_edge('validation', 'finalization'); graph.add_edge('finalization', END); graph = graph.compile()