from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EustachianState(TypedDict): device_specs: dict; compliance_check: bool; stage: str; def validate_specs(state: EustachianState): is_valid = all(key in state['device_specs'] for key in ['ISO_cert', 'material_grade']); return {'compliance_check': is_valid, 'stage': 'VALIDATED'}; def process_order(state: EustachianState): return {'stage': 'READY_FOR_SHIPMENT'}; graph = StateGraph(EustachianState); graph.add_node('validation', validate_specs); graph.add_node('processing', process_order); graph.set_entry_point('validation'); graph.add_edge('validation', 'processing'); graph.add_edge('processing', END); compiled_graph = graph.compile()