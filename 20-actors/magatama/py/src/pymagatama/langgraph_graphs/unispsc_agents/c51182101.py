from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; temp_log: List[float]; qc_passed: bool; def validate_temp(state: PharmState): return {'qc_passed': all(t <= 8.0 for t in state['temp_log'])}; def route_task(state: PharmState): return 'end' if state['qc_passed'] else 'flag_alert'; graph = StateGraph(PharmState); graph.add_node('validation', validate_temp); graph.add_edge('validation', 'end'); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()