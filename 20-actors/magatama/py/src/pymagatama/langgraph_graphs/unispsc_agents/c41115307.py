from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserState(TypedDict): specs: dict; compliance_check: bool; export_control_flag: bool; def validate_specs(state: LaserState): state['compliance_check'] = state['specs'].get('laser_safety_class') in ['Class 1', 'Class 2', 'Class 3R', 'Class 3B', 'Class 4']; return state; def check_export_regulations(state: LaserState): state['export_control_flag'] = state['specs'].get('output_power_w', 0) > 500; return state; graph = StateGraph(LaserState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()