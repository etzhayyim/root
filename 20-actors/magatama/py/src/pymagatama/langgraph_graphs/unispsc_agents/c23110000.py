from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachinePartState(TypedDict): part_id: str; specifications: dict; approval_status: bool; def validate_specs(state: MachinePartState): print(f'Validating specs for {state[part_id]}'); return {'approval_status': True}; def update_inventory(state: MachinePartState): print('Updating inventory records'); return {'approval_status': True}; graph = StateGraph(MachinePartState); graph.add_node('validate', validate_specs); graph.add_node('inventory', update_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); compiled_graph = graph.compile()