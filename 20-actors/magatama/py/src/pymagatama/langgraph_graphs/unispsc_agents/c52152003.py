from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PunchBowlState(TypedDict): bowl_specs: dict; validation_results: List[str]; graph = StateGraph(PunchBowlState); def validate_material(state: PunchBowlState): state['validation_results'].append('Material checked for food grade compliance'); return state; def check_capacity(state: PunchBowlState): state['validation_results'].append('Capacity verified against procurement spec'); return state; graph.add_node('material_check', validate_material); graph.add_node('capacity_check', check_capacity); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'capacity_check'); graph.add_edge('capacity_check', END); graph = graph.compile()