from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaceuticalState(TypedDict): chemical_name: str; analysis_result: str; compliance_check: bool; def validate_coa(state: PharmaceuticalState) -> PharmaceuticalState: state['compliance_check'] = 'COA_VALID' in state['analysis_result']; return state; def check_storage(state: PharmaceuticalState) -> PharmaceuticalState: print(f'Verifying storage requirements for {state[chemical_name]}'); return state; graph = StateGraph(PharmaceuticalState); graph.add_node('validate', validate_coa); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); compiled_graph = graph.compile()