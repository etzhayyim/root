from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoxState(TypedDict): vendor_specs: dict; is_compliant: bool; validation_logs: List[str]; def validate_electrical_standard(state: BoxState): state['is_compliant'] = 'UL' in state.get('vendor_specs', {}).get('certifications', '') and state.get('vendor_specs', {}).get('material') == 'steel'; return state; graph = StateGraph(BoxState); graph.add_node('validate', validate_electrical_standard); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()