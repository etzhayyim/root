from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class GymState(TypedDict): facility_id: str; structural_check: bool; safety_compliance: bool; graph = StateGraph(GymState); def validate_structure(state: GymState): return {'structural_check': True}; def check_safety(state: GymState): return {'safety_compliance': True}; graph.add_node('structural', validate_structure); graph.add_node('safety', check_safety); graph.set_entry_point('structural'); graph.add_edge('structural', 'safety'); graph.add_edge('safety', END); graph = graph.compile()