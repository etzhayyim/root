from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MotorState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: MotorState): specs = state['specs']; state['valid'] = 'IP_rating' in specs and specs['IP_rating'] != '' and specs['Horsepower'] > 0; return state; graph = StateGraph(MotorState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()