from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShoeState(TypedDict): vendor_id: str; specs: dict; compliant: bool; def validate_specs(state: ShoeState): state['compliant'] = all(k in state['specs'] for k in ['toxicity_report', 'ergonomic_rating']); return state; graph = StateGraph(ShoeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()