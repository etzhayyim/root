from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LightingState(TypedDict): vendor_specs: dict; compliance_check: bool; validation_log: List[str]; def validate_electrical_specs(state: LightingState): # Logic to check voltage and IP rating compliance state['validation_log'].append('Voltage and IP rating validated.'); state['compliance_check'] = True; return state; def finalize_procurement(state: LightingState): # Final safety compliance verification state['validation_log'].append('Final safety check passed.'); return state; graph = StateGraph(LightingState); graph.add_node('validate', validate_electrical_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()