from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LaparoscopeState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: LaparoscopeState): return {'validation_report': 'Passed' if 'ISO_13485' in state['specs'] else 'Failed'}; def check_compliance(state: LaparoscopeState): return {'approved': state['validation_report'] == 'Passed'}; graph = StateGraph(LaparoscopeState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()