from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GasketState(TypedDict): vendor_specs: dict; validation_results: List[str]; def validate_material(state: GasketState): return {'validation_results': ['Material check passed']}; def check_pressure(state: GasketState): return {'validation_results': ['Pressure spec verified']}; graph = StateGraph(GasketState); graph.add_node('material_check', validate_material); graph.add_node('pressure_check', check_pressure); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'pressure_check'); graph.add_edge('pressure_check', END); graph = graph.compile()