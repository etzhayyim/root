from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetyDeviceState(TypedDict): device_specs: dict; compliance_checked: bool; status: str; def validate_specs(state: SafetyDeviceState): state['compliance_checked'] = all(k in state['device_specs'] for k in ['voltage', 'standard']); state['status'] = 'verified' if state['compliance_checked'] else 'failed'; return state; graph = StateGraph(SafetyDeviceState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()