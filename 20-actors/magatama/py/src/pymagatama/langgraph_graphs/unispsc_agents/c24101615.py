from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RampState(TypedDict): ramp_specs: dict; validation_report: List[str]; def validate_load_capacity(state: RampState): capacity = state['ramp_specs'].get('load_capacity', 0); state['validation_report'].append('Capacity validated' if capacity > 500 else 'Capacity warning'); return state; def check_safety_standards(state: RampState): state['validation_report'].append('ISO/OSHA compliance checked'); return state; graph = StateGraph(RampState); graph.add_node('load_check', validate_load_capacity); graph.add_node('safety_check', check_safety_standards); graph.add_edge('load_check', 'safety_check'); graph.add_edge('safety_check', END); graph.set_entry_point('load_check'); graph = graph.compile()