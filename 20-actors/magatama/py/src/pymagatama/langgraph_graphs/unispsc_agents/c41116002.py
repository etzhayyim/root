from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_id: str; temp_log: List[float]; valid: bool; def check_temp(state: ReagentState): return { "valid": all(2 <= t <= 8 for t in state["temp_log"]) }; def validate_reagent(state: ReagentState): print(f"Validating batch {state['reagent_id']}"); return { "valid": state["valid"] }; workflow = StateGraph(ReagentState); workflow.add_node("temp_check", check_temp); workflow.add_node("final_val", validate_reagent); workflow.set_entry_point("temp_check"); workflow.add_edge("temp_check", "final_val"); workflow.add_edge("final_val", END); graph = workflow.compile()