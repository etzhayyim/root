from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: Annotated[Sequence[str], operator.add]; status: str; def validate_supply(state: ProcurementState) -> ProcurementState: return {status: validated}; def finalize_procurement(state: ProcurementState) -> ProcurementState: return {status: finished}; graph = StateGraph(ProcurementState); graph.add_node(validate_supply, validate_supply); graph.add_node(finalize_procurement, finalize_procurement); graph.set_entry_point(validate_supply); graph.add_edge(validate_supply, finalize_procurement); graph.add_edge(finalize_procurement, END); compiled_graph = graph.compile()