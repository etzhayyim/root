from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MetalProcurementState(TypedDict): alloy_type: str; specs: dict; verified: bool; def validate_specs(state: MetalProcurementState): return {'verified': 'Alloy Composition Percentage' in state['specs']}; def approve_order(state: MetalProcurementState): return {'verified': True}; graph = StateGraph(MetalProcurementState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()