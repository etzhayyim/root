from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DentalSpecState(TypedDict): vendor_certs: List[str]; material_specs: dict; validated: bool; def validate_cert(state: DentalSpecState): return {'validated': 'ISO-13485' in state['vendor_certs']}; def check_material(state: DentalSpecState): return {'validated': state['material_specs'].get('abrasive_type') in ['Diamond', 'Alumina']}; graph = StateGraph(DentalSpecState); graph.add_node('validate_cert', validate_cert); graph.add_node('check_material', check_material); graph.set_entry_point('validate_cert'); graph.add_edge('validate_cert', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()