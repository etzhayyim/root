from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EndoscopeState(TypedDict): device_id: str; specs: dict; approved: bool; def validate_specs(state: EndoscopeState): return {'approved': 'medical_device_approval_number' in state['specs']}; def finalize_procurement(state: EndoscopeState): return {'approved': True}; graph = StateGraph(EndoscopeState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()