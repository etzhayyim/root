from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TubeState(TypedDict): tube_specs: dict; validation_results: List[str]; graph = StateGraph(TubeState); def validate_tube_specs(state: TubeState): results = []; v = state['tube_specs'].get('voltage', 0); f = state['tube_specs'].get('frequency', 0); status = 'Valid' if v < 50000 and f < 10000 else 'Review'; results.append(status); return {'validation_results': results}; graph.add_node('validator', validate_tube_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); app = graph.compile()