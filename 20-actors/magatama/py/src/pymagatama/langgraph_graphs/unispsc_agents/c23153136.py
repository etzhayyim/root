from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): commodity_code: str; processing_steps: List[str]; validation_results: Dict[str, bool]; def process_sensor_module(state: AgentState): state['processing_steps'].append('Validating specs'); state['validation_results']['spec_check'] = True; return state; def compile_graph(): builder = StateGraph(AgentState); builder.add_node('process', process_sensor_module); builder.set_entry_point('process'); builder.add_edge('process', END); return builder.compile(); graph = compile_graph()