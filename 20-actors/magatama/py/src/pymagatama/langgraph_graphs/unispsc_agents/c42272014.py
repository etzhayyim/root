from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; compliance_verified: bool; def validate_specs(state: ProcState): state['compliance_verified'] = 'ISO 13485' in state['specs'].get('certifications', []); return state; def check_pressure(state: ProcState): return {'compliance_verified': state['specs'].get('pressure_rating', 0) > 0}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('pressure', check_pressure); graph.set_entry_point('validate'); graph.add_edge('validate', 'pressure'); graph.add_edge('pressure', END); graph.compile()