from langgraph.graph import StateGraph, END; from typing import TypedDict; class TitaniumState(TypedDict): specs: dict; valid: bool; def validate_specs(state: TitaniumState): state['valid'] = 'grade' in state['specs'] and 'ndt_report' in state['specs']; return state; builder = StateGraph(TitaniumState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()