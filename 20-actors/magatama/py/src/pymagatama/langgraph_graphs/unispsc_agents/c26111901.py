from langgraph.graph import StateGraph, END; from typing import TypedDict; class ClutchState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ClutchState): state['valid'] = 'torque_capacity_nm' in state['specs'] and state['specs']['max_rpm_rating'] > 0; return state; graph = StateGraph(ClutchState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()