from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_dimensions(state: CastingState): errors = []; spec = state.get('specs', {}); if spec.get('tolerance') == 'none': errors.append('Missing tolerance data'); return {'validation_errors': errors}; def check_metallurgy(state: CastingState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(CastingState); graph.add_node('validate', validate_dimensions); graph.add_node('metallurgy', check_metallurgy); graph.set_entry_point('validate'); graph.add_edge('validate', 'metallurgy'); graph.add_edge('metallurgy', END); compiled_graph = graph.compile()