from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcessState(TypedDict): component_id: str; specs: dict; valid: bool; def validate_specs(state: ProcessState): state['valid'] = 'Material Grade' in state['specs'] and 'Dimensional Tolerance' in state['specs']; return state; def route_step(state: ProcessState): return 'validate' if state['valid'] else END; graph = StateGraph(ProcessState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()