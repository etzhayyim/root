from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine_name: str; gmp_certified: bool; quality_check: bool; graph = StateGraph(PharmState); def check_gmp(state: PharmState): return {'gmp_certified': True}; def verify_batch(state: PharmState): return {'quality_check': True}; graph.add_node('gmp', check_gmp); graph.add_node('verify', verify_batch); graph.set_entry_point('gmp'); graph.add_edge('gmp', 'verify'); graph.add_edge('verify', END); graph = graph.compile()