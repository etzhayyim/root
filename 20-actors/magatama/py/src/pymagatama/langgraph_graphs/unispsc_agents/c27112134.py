from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_name: str; specs: dict; compliant: bool; def validate_spec(state: ToolState): state['compliant'] = 'Material Grade' in state['specs'] and state['specs']['Hardness (HRC)'] > 45; return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()