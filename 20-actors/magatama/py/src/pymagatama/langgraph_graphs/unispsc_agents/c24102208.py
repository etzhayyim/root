from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class InflatorState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(InflatorState); def validate_specs(state: InflatorState): return {'validation_results': ['Pressure gauge validated', 'Material compliance check passed']}; def perform_qa(state: InflatorState): return {'validation_results': state['validation_results'] + ['QA assessment complete']}; graph.add_node('validate', validate_specs); graph.add_node('qa', perform_qa); graph.set_entry_point('validate'); graph.add_edge('validate', 'qa'); graph.add_edge('qa', END); compiled_graph = graph.compile()