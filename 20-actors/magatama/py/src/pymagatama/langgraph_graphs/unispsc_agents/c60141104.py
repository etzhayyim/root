from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GameProcurementState(TypedDict): vendor_id: str; game_title: str; region: str; rating_verified: bool; def check_compliance(state: GameProcurementState) -> GameProcurementState: state['rating_verified'] = True; return state; graph = StateGraph(GameProcurementState); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()