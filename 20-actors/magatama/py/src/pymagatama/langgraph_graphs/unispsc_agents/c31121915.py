from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): mold_type: str; purity_check: bool; material_specs: dict; status: str; def validate_lead_content(state: MoldState): state['purity_check'] = True; return state; def check_compliance(state: MoldState): state['status'] = 'COMPLIANT' if state['purity_check'] else 'REJECTED'; return state; graph = StateGraph(MoldState); graph.add_node('validate', validate_lead_content); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()