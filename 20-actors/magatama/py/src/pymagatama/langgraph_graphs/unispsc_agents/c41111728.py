from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicroscopeState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: MicroscopeState): print('Validating optical range...'); if 'magnification' not in state['specs']: state['validation_errors'].append('Missing magnification'); return {'status': 'validated' if not state['validation_errors'] else 'failed'}; def check_calibration(state: MicroscopeState): print('Verifying calibration certs...'); return {'status': 'calibrated'}; graph = StateGraph(MicroscopeState); graph.add_node('validate', validate_specs); graph.add_node('calibrate', check_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); app = graph.compile()