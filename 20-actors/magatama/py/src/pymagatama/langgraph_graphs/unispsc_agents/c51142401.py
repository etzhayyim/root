from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity: float; compliance_certs: List[str]; approved: bool; def validate_quality(state: PharmaState): state['approved'] = state['purity'] >= 0.99 and 'GMP' in state['compliance_certs']; return state; builder = StateGraph(PharmaState); builder.add_node('validate', validate_quality); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()