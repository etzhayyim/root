from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): specs: dict; validation_passed: bool; report: str; def validate_spec(state: ProcurementState): specs = state.get('specs', {}); state['validation_passed'] = 'Weight Capacity' in specs and 'Material Certification' in specs; return state; def check_compliance(state: ProcurementState): state['report'] = 'Compliant' if state['validation_passed'] else 'Non-Compliant'; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); app = graph.compile()