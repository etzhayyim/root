from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_id: str; item_specs: dict; validation_passed: bool; def validate_specs(state: ProcurementState): state['validation_passed'] = 'load_capacity_kg' in state['item_specs']; return state; def route_procurement(state: ProcurementState): return 'end' if state['validation_passed'] else 'error'; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()