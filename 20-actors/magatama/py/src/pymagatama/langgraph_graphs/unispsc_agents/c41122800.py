from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabEquipmentState(TypedDict): equipment_type: str; specs: dict; valid: bool; def validate_specs(state: LabEquipmentState) -> LabEquipmentState: state['valid'] = state['specs'].get('chemical_resistance') is not None; return state; def route_check(state: LabEquipmentState) -> str: return 'valid' if state['valid'] else 'invalid'; graph = StateGraph(LabEquipmentState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()