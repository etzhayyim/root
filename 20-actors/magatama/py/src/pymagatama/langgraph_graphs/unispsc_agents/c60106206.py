from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validated_items: List[str]; graph = StateGraph(State); def validate_material(state: State): return {'validated_items': [item for item in state['items'] if 'safety_cert' in item]}; graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()