from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CanvasState(TypedDict): fabric_type: str; specs_verified: bool; quality_report: str; def validate_specs(state: CanvasState): state['specs_verified'] = bool(state['fabric_type']); return state; def generate_report(state: CanvasState): state['quality_report'] = 'Validation complete: ' + str(state['specs_verified']); return state; graph = StateGraph(CanvasState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()