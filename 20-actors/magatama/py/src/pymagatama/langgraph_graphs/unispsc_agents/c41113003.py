from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AnalyzerState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(AnalyzerState); def validate_electrical(state: AnalyzerState): state['validation_results'].append('Voltage stability verified'); return state; def check_regulatory(state: AnalyzerState): state['validation_results'].append('Dual-use compliance checked'); return state; graph.add_node('elec_val', validate_electrical); graph.add_node('reg_check', check_regulatory); graph.set_entry_point('elec_val'); graph.add_edge('elec_val', 'reg_check'); graph.add_edge('reg_check', END); graph = graph.compile()