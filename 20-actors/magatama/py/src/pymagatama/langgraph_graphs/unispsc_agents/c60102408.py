from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LearningMaterialState(TypedDict): material_name: str; safety_specs: List[str]; approved: bool; def validate_safety(state: LearningMaterialState): state['approved'] = 'non_toxic' in state['safety_specs']; return state; graph = StateGraph(LearningMaterialState); graph.add_node('safety_check', validate_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()