from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): vendor_license: bool; quality_check: bool; compliance_status: bool; def verify_regulatory(state): return {'compliance_status': True if state['vendor_license'] else False}; def run_qc(state): return {'quality_check': True}; graph = StateGraph(PharmState); graph.add_node('regulatory', verify_regulatory); graph.add_node('qc', run_qc); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'qc'); graph.add_edge('qc', END); compiled_graph = graph.compile()