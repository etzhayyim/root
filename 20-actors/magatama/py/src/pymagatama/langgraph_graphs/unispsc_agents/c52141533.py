from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GriddleState(TypedDict): griddle_id: str; specs: dict; validation_passed: bool; def validate_specs(state: GriddleState): state['validation_passed'] = 'voltage' in state['specs'] and 'wattage' in state['specs']; return state; builder = StateGraph(GriddleState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()