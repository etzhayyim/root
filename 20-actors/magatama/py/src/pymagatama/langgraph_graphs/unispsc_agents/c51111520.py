from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_name: str; purity: float; compliant: bool; def check_purity(state: ProcState): state['compliant'] = state['purity'] >= 99.0; return state; def validate_regulatory(state: ProcState): return {'compliant': state['compliant'] and True}; graph = StateGraph(ProcState); graph.add_node('check_purity', check_purity); graph.add_node('validate_regulatory', validate_regulatory); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'validate_regulatory'); graph.add_edge('validate_regulatory', END); compiled_graph = graph.compile()