from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class OrthoState(TypedDict): items: List[dict]; approval_status: bool; def check_compliance(state: OrthoState): approval = all('iso_cert' in item for item in state['items']); return {'approval_status': approval}; graph = StateGraph(OrthoState); graph.add_node('validate', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()