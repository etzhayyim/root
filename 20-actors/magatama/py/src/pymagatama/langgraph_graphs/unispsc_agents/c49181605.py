from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DartBoardState(TypedDict): board_type: str; specs: dict; approved: bool; def check_specs(state: DartBoardState): state['approved'] = state['specs'].get('diameter') == 45.7; return state; graph = StateGraph(DartBoardState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()