from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SwingProcurementState(TypedDict): specs: dict; approved: bool; def validate_safety(state: SwingProcurementState): state['approved'] = state['specs'].get('load_capacity', 0) > 100; return state; graph = StateGraph(SwingProcurementState); graph.add_node('validate', validate_safety); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()