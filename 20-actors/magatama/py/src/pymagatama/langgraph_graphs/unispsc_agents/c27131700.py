from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec: dict; def validate_spec(state: State): return {'status': 'validated'} if 'bore' in state['spec'] else {'status': 'rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()