from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class OpticalState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(OpticalState); def validate_optic(state: OpticalState): status = "Pass" if state['specs'].get('refractive_index') else "Fail"; return {'validation_report': [f'Validation result: {status}']}; graph.add_node('validate', validate_optic); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()