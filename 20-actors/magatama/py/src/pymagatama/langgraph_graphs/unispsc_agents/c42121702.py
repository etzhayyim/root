from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StorageState(TypedDict): specs: dict; valid: bool; def validate_specs(state: StorageState): state['valid'] = 'material_composition' in state['specs'] and state['specs']['load_capacity_kg'] > 0; return state; graph = StateGraph(StorageState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()