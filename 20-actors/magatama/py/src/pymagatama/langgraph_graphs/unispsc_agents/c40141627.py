from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ValveState(TypedDict): valve_specs: dict; validation_status: bool; compliance_check: bool; def validate_specs(state: ValveState): state['validation_status'] = all(k in state['valve_specs'] for k in ['pressure_rating', 'material']); return state; def check_compliance(state: ValveState): state['compliance_check'] = state['validation_status'] and (state['valve_specs'].get('pressure_rating', 0) > 0); return state; graph = StateGraph(ValveState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()