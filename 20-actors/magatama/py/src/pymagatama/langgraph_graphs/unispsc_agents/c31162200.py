from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): rivets: List[dict]; validation_results: List[str]; def validate_rivet_specs(state: FastenerState): results = []; for r in state['rivets']: if r.get('tensile_strength') < 200: results.append(f'Low strength: {r.get('id')}'); return {'validation_results': results}; graph = StateGraph(FastenerState); graph.add_node('validate', validate_rivet_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()