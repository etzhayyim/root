from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_name: str; validation_passed: bool; safety_check: str; def validate_hazmat(state: ProcState): state['validation_passed'] = 'Amyl nitrate' in state['chemical_name']; state['safety_check'] = 'Verified hazardous protocol' if state['validation_passed'] else 'Rejected'; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()