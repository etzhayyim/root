from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AirgunState(TypedDict): model_info: dict; compliance_status: bool; messages: Annotated[list, operator.add]; def validate_specs(state: AirgunState): import re; energy = state['model_info'].get('muzzle_energy', 0); state['compliance_status'] = energy < 16.0; return state; graph = StateGraph(AirgunState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()