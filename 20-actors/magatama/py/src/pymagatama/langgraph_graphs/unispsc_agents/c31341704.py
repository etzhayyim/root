from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): metal_grade: str; torque_specs: float; is_certified: bool; approved: bool; def check_material(state): return {'approved': state['metal_grade'] in ['Inconel 625', 'Inconel 718']}; def validate_specs(state): return {'approved': state['is_certified'] and state['torque_specs'] > 0}; graph = StateGraph(AssemblyState); graph.add_node('check_material', check_material); graph.add_node('validate_specs', validate_specs); graph.add_edge('check_material', 'validate_specs'); graph.add_edge('validate_specs', END); graph.set_entry_point('check_material'); graph = graph.compile()