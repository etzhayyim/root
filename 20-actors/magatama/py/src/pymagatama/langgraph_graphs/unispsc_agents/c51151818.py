from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_id: str; quality_passed: bool; compliance_checked: bool; def validate_purity(state: PharmaState): state['quality_passed'] = True; return state; def check_regulatory(state: PharmaState): state['compliance_checked'] = True; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulatory); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()