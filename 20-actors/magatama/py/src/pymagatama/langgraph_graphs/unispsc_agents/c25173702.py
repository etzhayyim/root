from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MufflerState(TypedDict): specs: dict; valid: bool; error: str; def validate_muffler(state: MufflerState): specs = state.get('specs', {}); state['valid'] = 'Noise Decibel Rating' in specs and 'Material Grade' in specs; return state; graph = StateGraph(MufflerState); graph.add_node('validate', validate_muffler); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()