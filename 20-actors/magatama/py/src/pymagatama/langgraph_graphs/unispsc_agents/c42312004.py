from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): chemical_data: dict; approval_status: bool; workflow_steps: List[str]; def validate_chemical(state: AgentState): state['workflow_steps'].append('Validating MSDS and Chemical Composition'); state['approval_status'] = True if 'SDS' in state['chemical_data'] else False; return state; def check_compliance(state: AgentState): state['workflow_steps'].append('Checking for medical regulatory compliance'); return state; graph = StateGraph(AgentState); graph.add_node('validate', validate_chemical); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()