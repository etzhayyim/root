from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_docs: List[str]; quality_check: bool; final_approval: bool; def validate_specs(state: ProcureState): return {'quality_check': True} if 'lab_report' in state['vendor_docs'] else {'quality_check': False}; def approve_procurement(state: ProcureState): return {'final_approval': state['quality_check']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()