from langgraph.graph import StateGraph, END; from typing import TypedDict; class PanelboardState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PanelboardState): state['valid'] = 'rated_voltage' in state['specs'] and 'short_circuit_rating' in state['specs']; return state; builder = StateGraph(PanelboardState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()