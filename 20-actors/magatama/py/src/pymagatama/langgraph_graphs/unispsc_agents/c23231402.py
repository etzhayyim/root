from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SpindleState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: SpindleState): s = state['specs']; state['valid'] = s.get('rpm', 0) > 0 and 'iso' in s; return state; graph = StateGraph(SpindleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()