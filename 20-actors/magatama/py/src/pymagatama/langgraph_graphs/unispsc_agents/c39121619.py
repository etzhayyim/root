from langgraph.graph import StateGraph, END; from typing import TypedDict; class FuseState(TypedDict): specs: dict; valid: bool; def validate_specs(state: FuseState): state['valid'] = all(k in state['specs'] for k in ['rated_voltage', 'rated_current']); return state; graph = StateGraph(FuseState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()