from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoltState(TypedDict): specs: dict; validation_results: List[str]; def validate_bolt(state: BoltState): results = []; required = ['material_grade', 'tensile_strength']; for field in required: if field not in state['specs']: results.append(f'Missing {field}'); state['validation_results'] = results; return state; graph = StateGraph(BoltState); graph.add_node('validate', validate_bolt); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()