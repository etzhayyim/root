from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SomatostatinState(TypedDict): chemical_id: str; purity: float; storage_temp: float; status: str; def validate_purity(state: SomatostatinState): return {'status': 'validated' if state['purity'] >= 98.0 else 'rejected'}; def check_temp(state: SomatostatinState): return {'status': 'cold_chain' if state['storage_temp'] <= -20.0 else 'alert'}; graph = StateGraph(SomatostatinState); graph.add_node('validate', validate_purity); graph.add_node('temp_check', check_temp); graph.set_entry_point('validate'); graph.add_edge('validate', 'temp_check'); graph.add_edge('temp_check', END); graph = graph.compile()