from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): vendor_docs: list; specs: dict; validation_results: list; graph = StateGraph(RobotState); def validate_specs(state: RobotState): return {'validation_results': ['Specs met']}; def check_export_control(state: RobotState): return {'validation_results': ['Export cleared']}; graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()