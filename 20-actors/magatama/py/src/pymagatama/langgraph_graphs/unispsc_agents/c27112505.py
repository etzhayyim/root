from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): tool_data: dict; valid: bool; def validate_tool(state: State): state['valid'] = 'material' in state['tool_data'] and state['tool_data']['material'] == 'hardened_steel'; return state; def route_step(state: State): return 'process' if state['valid'] else END; graph = StateGraph(State); graph.add_node('validate', validate_tool); graph.add_node('process', lambda s: s); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_step, {'process': 'process'}); graph.add_edge('process', END); graph = graph.compile()