from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; specs: dict; valid: bool; def validate_spec(state: ComponentState): state['valid'] = 'Tensile strength' in state['specs'] and state['specs']['Tensile strength'] > 0; return state; graph = StateGraph(ComponentState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()