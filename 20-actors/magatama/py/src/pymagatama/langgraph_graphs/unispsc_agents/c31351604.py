from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(ProcureState); def validate_specs(state): return {'compliance_check': 'MaterialGrade' in state['specs']}; def export_control_check(state): return {'compliance_check': state['compliance_check'] and True}; graph.add_node('validate', validate_specs); graph.add_node('export', export_control_check); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph.set_entry_point('validate'); graph = graph.compile()