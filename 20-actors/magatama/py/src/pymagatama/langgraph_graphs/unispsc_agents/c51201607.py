from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VaccineState(TypedDict): cold_chain_data: dict; compliance_status: bool; def validate_cold_chain(state: VaccineState): state['compliance_status'] = state['cold_chain_data'].get('temp_range') == '2-8C'; return state; def check_expiry(state: VaccineState): return {'compliance_status': state['compliance_status'] and state['cold_chain_data'].get('expired') is False}; graph = StateGraph(VaccineState); graph.add_node('validate', validate_cold_chain); graph.add_node('expiry', check_expiry); graph.set_entry_point('validate'); graph.add_edge('validate', 'expiry'); graph.add_edge('expiry', END); app = graph.compile()