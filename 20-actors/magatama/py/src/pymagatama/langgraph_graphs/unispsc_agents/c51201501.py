from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): medication: str; status: str; safety_checks: List[str]; def validate_medication(state: AgentState): return {'status': 'validated', 'safety_checks': ['GMP_check', 'cold_chain_audit']}; def process_procurement(state: AgentState): return {'status': 'processed'}; graph = StateGraph(AgentState); graph.add_node('validate', validate_medication); graph.add_node('process', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.compile()