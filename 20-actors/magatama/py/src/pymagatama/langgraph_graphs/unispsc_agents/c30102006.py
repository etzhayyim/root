from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FoilState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def check_specs(state: FoilState): errors = []; if state['specs'].get('thickness', 0) < 0.009: errors.append('Thickness below threshold'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(FoilState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()