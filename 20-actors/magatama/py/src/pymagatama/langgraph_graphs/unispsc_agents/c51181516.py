from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): ingredient: str; qc_passed: bool; specs: dict; def validate_quality(state: PharmaState): state['qc_passed'] = state['specs'].get('purity', 0) >= 99.0; return state; def verify_regulatory(state: PharmaState): state['regulatory_compliant'] = 'GMP' in state['specs'].get('certs', []); return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_quality); graph.add_node('regulatory', verify_regulatory); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); graph = graph.compile()