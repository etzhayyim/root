from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TattooProcurementState(TypedDict): items: List[dict]; status: str; def validate_safety(state: 'TattooProcurementState'): return {'status': 'INSPECTING_SAFETY'}; def verify_compliance(state: 'TattooProcurementState'): return {'status': 'COMPLIANCE_VERIFIED'}; graph = StateGraph(TattooProcurementState); graph.add_node('safety_check', validate_safety); graph.add_node('compliance', verify_compliance); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()