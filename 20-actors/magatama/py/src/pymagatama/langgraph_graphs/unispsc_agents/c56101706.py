from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TableSpecState(TypedDict): dimensions: dict; material: str; assembly_required: bool; approval_status: str; def validate_specs(state: TableSpecState): return {'approval_status': 'verified' if state['material'] else 'pending'}; def finalize_order(state: TableSpecState): return {'approval_status': 'confirmed for production'}; graph = StateGraph(TableSpecState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate');