from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CardFileState(TypedDict): file_type: str; specs: dict; valid: bool; def validate_card_file(state: CardFileState): state['valid'] = 'capacity' in state['specs'] and state['specs']['capacity'] > 0; return state; graph = StateGraph(CardFileState); graph.add_node('validate', validate_card_file); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()