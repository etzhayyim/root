from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotProcurementState(TypedDict): robot_id: str; specs: dict; validation_score: float; def validate_specs(state: RobotProcurementState): state['validation_score'] = 1.0 if 'payload' in state['specs'] else 0.0; return state; def route_approval(state: RobotProcurementState): return 'approval' if state['validation_score'] > 0.5 else END; graph = StateGraph(RobotProcurementState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()