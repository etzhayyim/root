from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): items: List[str]; validation_results: List[str]; def validate_medical_cert(state: SupplyState): return {'validation_results': ['ISO 13485 verified']}; def check_spec_compliance(state: SupplyState): return {'validation_results': state['validation_results'] + ['Specs compliant']}; graph = StateGraph(SupplyState); graph.add_node('cert_check', validate_medical_cert); graph.add_node('spec_review', check_spec_compliance); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'spec_review'); graph.add_edge('spec_review', END); compiled_graph = graph.compile()