from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): specs: dict; valid: bool; def val_specs(state: ToolState): state['valid'] = 'Clamping Capacity' in state['specs'] and state['specs']['Clamping Capacity'] > 0; return state; graph = StateGraph(ToolState); graph.add_node('validation', val_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()