from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LabelingState(TypedDict): input_specs: dict; validation_results: Annotated[list, operator.add]; def validate_specs(state: LabelingState): result = 'Valid' if state['input_specs'].get('accuracy') else 'Invalid'; return {'validation_results': [result]}; def check_compatibility(state: LabelingState): return {'validation_results': ['Compatibility Checked']}; graph = StateGraph(LabelingState); graph.add_node('validate', validate_specs); graph.add_node('compatibility', check_compatibility); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph.set_entry_point('validate'); compile_graph = graph.compile()