from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): vendor_license: bool; quality_check: bool; compliance_status: str; def validate_license(state): return {'vendor_license': True}; def check_product(state): return {'quality_check': True}; def update_compliance(state): return {'compliance_status': 'verified'}; graph = StateGraph(PharmState); graph.add_node('validate', validate_license); graph.add_node('qc', check_product); graph.add_node('compliance', update_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'qc'); graph.add_edge('qc', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()