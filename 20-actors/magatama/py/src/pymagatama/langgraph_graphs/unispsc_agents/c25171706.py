from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BrakeState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: BrakeState): state['valid'] = 'piston_diameter' in state['specs'] and 'material_composition' in state['specs']; state['log'].append('Validation completed'); return state; graph = StateGraph(BrakeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()