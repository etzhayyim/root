from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SolarKitState(TypedDict): kit_specs: dict; compliance_checked: bool; def validate_specs(state: SolarKitState): return {'compliance_checked': state['kit_specs'].get('power_output', 0) > 0}; def finalize_procurement(state: SolarKitState): return {'status': 'approved'}; graph = StateGraph(SolarKitState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()