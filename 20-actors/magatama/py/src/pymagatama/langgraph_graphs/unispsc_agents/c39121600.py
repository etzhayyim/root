from typing import TypedDict, List; from langgraph.graph import StateGraph; class CircuitState(TypedDict): specs: dict; compliance: bool; approved: bool; def val_specs(state: CircuitState): return {'compliance': all(key in state['specs'] for key in ['voltage', 'rating'])}; def check_safety(state: CircuitState): return {'approved': state['compliance']}; graph = StateGraph(CircuitState); graph.add_node('validate', val_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.set_finish_point('safety'); graph = graph.compile()