from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GlasswareState(TypedDict): specs: dict; valid: bool; workflow_log: List[str]; def validate_glass(state: GlasswareState): state['valid'] = 'food_contact_safety_certification' in state['specs']; state['workflow_log'].append('Validation completed'); return state; graph = StateGraph(GlasswareState); graph.add_node('validate', validate_glass); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()