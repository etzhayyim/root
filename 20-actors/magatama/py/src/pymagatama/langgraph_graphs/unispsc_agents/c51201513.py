from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): medicine_name: str; validation_status: bool; logs: List[str]; def validate_purity(state: DrugState): return {'validation_status': True, 'logs': ['Purity check passed for Peginterferon']}; def check_cold_chain(state: DrugState): return {'logs': state['logs'] + ['Cold chain integrity verified']}; graph = StateGraph(DrugState); graph.add_node('purity', validate_purity); graph.add_node('cold_chain', check_cold_chain); graph.set_entry_point('purity'); graph.add_edge('purity', 'cold_chain'); graph.add_edge('cold_chain', END); graph = graph.compile()