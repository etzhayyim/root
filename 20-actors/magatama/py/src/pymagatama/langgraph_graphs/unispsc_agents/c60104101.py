from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ModelProcurementState(TypedDict): model_id: str; specs: dict; approved: bool; def validate_specs(state: ModelProcurementState): state['approved'] = 'anatomical_accuracy' in state['specs']; return state; def route_approval(state: ModelProcurementState): return 'approved' if state['approved'] else END; graph = StateGraph(ModelProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()