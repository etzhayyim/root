from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): data: dict; valid: bool; def validate_instrument(state: State): state['valid'] = 'torque_range' in state['data'] and state['data']['torque_range'] > 0; return state; graph = StateGraph(State); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()