from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): input_data: dict; validation: bool; def validate_kit(state: ProcureState): state['validation'] = 'pathogen_clearance' in state['input_data']; return state; def finalize_order(state: ProcureState): return {'status': 'approved' if state['validation'] else 'rejected'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_kit); graph.add_node('finish', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finish'); graph.add_edge('finish', END); compiled_graph = graph.compile()