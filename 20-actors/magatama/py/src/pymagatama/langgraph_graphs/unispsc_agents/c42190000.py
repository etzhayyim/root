from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_id: str; compliance_docs: List[str]; inspection_status: str; def validate_specs(state: ProcureState): return {'inspection_status': 'verified'} if 'ISO_13485' in state['compliance_docs'] else {'inspection_status': 'failed'}; def approve_procurement(state: ProcureState): return {'inspection_status': 'approved'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()