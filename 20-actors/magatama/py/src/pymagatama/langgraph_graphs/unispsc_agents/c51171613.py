from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AloinState(TypedDict): chemical_data: dict; quality_status: str; validation_logs: List[str]; def validate_purity(state: AloinState): print('Validating aloin purity...'); return {'quality_status': 'verified' if state['chemical_data'].get('purity', 0) > 98 else 'rejected'}; def check_compliance(state: AloinState): return {'validation_logs': ['Compliance check passed']}; graph = StateGraph(AloinState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()