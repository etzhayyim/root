from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine_name: str; validation_passed: bool; compliance_docs: List[str]; def validate_regulatory_compliance(state: PharmState): state['validation_passed'] = 'License_Validated' in state['compliance_docs']; return {'validation_passed': state['validation_passed']}; def check_batch_quality(state: PharmState): return {'validation_passed': state['validation_passed'] and True}; graph = StateGraph(PharmState); graph.add_node('validate', validate_regulatory_compliance); graph.add_node('batch_check', check_batch_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'batch_check'); graph.add_edge('batch_check', END); graph = graph.compile()