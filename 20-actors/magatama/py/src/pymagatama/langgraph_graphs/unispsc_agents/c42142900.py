from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class EyewearState(TypedDict): vendor_certs: list[str]; specs: dict; approved: bool; def validate_cert(state: EyewearState): status = 'FDA' in state.get('vendor_certs', []) return {'approved': status}; graph = StateGraph(EyewearState); graph.add_node('validate', validate_cert); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()