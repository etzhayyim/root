from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialysisState(TypedDict): kit_id: str; sterility_verified: bool; compliance_passed: bool; def check_sterility(state: DialysisState): return {'sterility_verified': True}; def validate_compliance(state: DialysisState): return {'compliance_passed': True}; graph = StateGraph(DialysisState); graph.add_node('check_sterility', check_sterility); graph.add_node('validate_compliance', validate_compliance); graph.set_entry_point('check_sterility'); graph.add_edge('check_sterility', 'validate_compliance'); graph.add_edge('validate_compliance', END); graph = graph.compile()