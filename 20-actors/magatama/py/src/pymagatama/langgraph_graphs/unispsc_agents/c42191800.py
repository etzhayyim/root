from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PatientBedState(TypedDict): specs: dict; compliance_check: bool; model_id: str; def validate_specs(state: PatientBedState): print('Validating IEC 60601-2-52...'); state['compliance_check'] = True; return state; def finalize_order(state: PatientBedState): print('Finalizing procurement request'); return state; graph = StateGraph(PatientBedState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()