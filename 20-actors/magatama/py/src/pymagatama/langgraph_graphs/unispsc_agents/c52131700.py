from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class WindowHardwareState(TypedDict): hardware_specs: dict; validation_report: list[str]; def validate_specs(state: WindowHardwareState) -> WindowHardwareState: specs = state.get('hardware_specs', {}); report = []; if not specs.get('load_capacity'): report.append('Missing load capacity data'); return {**state, 'validation_report': report}; def finalize_procurement(state: WindowHardwareState) -> WindowHardwareState: return {**state, 'validation_report': state.get('validation_report', []) + ['Procurement ready']}; graph = StateGraph(WindowHardwareState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()