from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): spec: dict; validated: bool; def check_torque(state: State): state['validated'] = 'torque_spec' in state['spec']; return state; builder = StateGraph(State); builder.add_node('validate', check_torque); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()