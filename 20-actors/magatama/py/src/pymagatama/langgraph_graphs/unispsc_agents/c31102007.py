from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): input_data: dict; result: dict; errors: List[str]; def validate_tolerance(state: CastingState): return {'result': {'valid': True}} if 'tolerance' in state['input_data'] else {'errors': ['Missing tolerance']}; def metallurgy_check(state: CastingState): return {'result': {'metallurgy_match': True}}; graph = StateGraph(CastingState); graph.add_node('validate', validate_tolerance); graph.add_node('metallurgy', metallurgy_check); graph.add_edge('validate', 'metallurgy'); graph.add_edge('metallurgy', END); graph.set_entry_point('validate'); graph = graph.compile()