from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ValveState(TypedDict): valve_type: str; pressure_rating: float; compliance: bool; def validate_specs(state: ValveState): return {'compliance': state['pressure_rating'] > 0 and state['valve_type'] != ''}; def finalize_procurement(state: ValveState): return {'status': 'approved' if state['compliance'] else 'rejected'}; graph = StateGraph(ValveState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()