from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoapState(TypedDict): soap_specs: dict; compliance_score: int; status: str; def validate_chemistry(state: SoapState): state['compliance_score'] = 100 if 'pH' in state['soap_specs'] else 50; return state; def check_certification(state: SoapState): state['status'] = 'APPROVED' if 'ISO' in state['soap_specs'] else 'PENDING'; return state; graph = StateGraph(SoapState); graph.add_node('validate', validate_chemistry); graph.add_node('certs', check_certification); graph.set_entry_point('validate'); graph.add_edge('validate', 'certs'); graph.add_edge('certs', END); app = graph.compile()