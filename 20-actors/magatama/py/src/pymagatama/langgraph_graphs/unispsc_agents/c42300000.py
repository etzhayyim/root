from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrainState(TypedDict): items: List[str]; validation_results: dict; def validate_model(state: TrainState): return {'validation_results': {'status': 'verified'}}; def approve_order(state: TrainState): return {'validation_results': {'status': 'approved'}}; graph = StateGraph(TrainState); graph.add_node('validate', validate_model); graph.add_node('approve', approve_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()