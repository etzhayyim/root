from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WireState(TypedDict): wire_spec: dict; validation_errors: List[str]; def validate_heat_rating(state: WireState): specs = state.get('wire_spec', {}); return {'validation_errors': ['Temp rating missing'] if 'temp' not in specs else []}; graph = StateGraph(WireState); graph.add_node('validate', validate_heat_rating); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()