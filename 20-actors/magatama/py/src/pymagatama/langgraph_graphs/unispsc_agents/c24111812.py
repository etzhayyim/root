from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BasinState(TypedDict): capacity: float; material: str; compliant: bool; def check_capacity(state: BasinState): state['compliant'] = state['capacity'] > 100; return state; def validate_resilience(state: BasinState): state['compliant'] = state['material'] in ['HDPE', 'Steel'] and state['compliant']; return state; graph = StateGraph(BasinState); graph.add_node('check_capacity', check_capacity); graph.add_node('validate_resilience', validate_resilience); graph.add_edge('check_capacity', 'validate_resilience'); graph.add_edge('validate_resilience', END); graph.set_entry_point('check_capacity'); graph = graph.compile()