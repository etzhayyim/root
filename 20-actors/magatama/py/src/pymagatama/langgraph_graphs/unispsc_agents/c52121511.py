from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DuvetState(TypedDict): duvet_specs: dict; validation_errors: List[str]; approved: bool; def validate_compliance(state: DuvetState): specs = state['duvets_specs']; errors = []; if 'tog_rating' not in specs: errors.append('Missing TOG rating'); if specs.get('flame_retardancy') != 'compliant': errors.append('Non-compliant flame retardancy'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(DuvetState); graph.add_node('compliance', validate_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()