from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SutureState(TypedDict): suture_type: str; specs: dict; compliance_approved: bool; def validate_specs(state: SutureState): return {'compliance_approved': all(k in state['specs'] for k in ['material', 'sterility'])}; def check_regulation(state: SutureState): return {'compliance_approved': state['compliance_approved'] and state['specs'].get('regulatory_clearance', False)}; graph = StateGraph(SutureState); graph.add_node('validate', validate_specs); graph.add_node('regulation', check_regulation); graph.add_edge('validate', 'regulation'); graph.add_edge('regulation', END); graph.set_entry_point('validate'); graph = graph.compile()