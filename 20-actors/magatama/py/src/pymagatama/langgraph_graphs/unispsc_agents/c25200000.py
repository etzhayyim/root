from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AerospaceState(TypedDict): component_id: str; compliance_passed: bool; technical_specs: dict; def validate_specs(state: AerospaceState): state['compliance_passed'] = all(k in state['technical_specs'] for k in ['material', 'tolerance']); return state; def check_export_control(state: AerospaceState): return {'compliance_passed': state['compliance_passed'] and True}; graph = StateGraph(AerospaceState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()