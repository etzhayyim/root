from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KitState(TypedDict): vendor: str; specs: dict; valid: bool; def validate_specs(state: KitState): state['valid'] = 'ISO 13485' in state['specs'].get('certifications', []); return state; def route_approval(state: KitState): return 'approve' if state['valid'] else 'reject'; graph = StateGraph(KitState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate')