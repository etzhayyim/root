from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GastroscopyState(TypedDict): instrument_list: List[str]; qc_passed: bool; compliance_docs: List[str]; def validate_instruments(state: GastroscopyState): return {'qc_passed': all(['sterile' in doc for doc in state['compliance_docs']])}; def check_compliance(state: GastroscopyState): return {'compliance_docs': ['ISO_13485_CERT', 'CE_MARK']}; graph = StateGraph(GastroscopyState); graph.add_node('check_compliance', check_compliance); graph.add_node('validate', validate_instruments); graph.set_entry_point('check_compliance'); graph.add_edge('check_compliance', 'validate'); graph.add_edge('validate', END); graph = graph.compile()