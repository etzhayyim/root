from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_assembly(state: AssemblyState): state['valid'] = 'Alloy Grade' in state['specs'] and 'Rivet Type' in state['specs']; state['logs'].append('Structural integrity validation completed.'); return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_assembly); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()