from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OilProcurementState(TypedDict): vendor_certs: List[str]; safety_check_passed: bool; order_logistics: dict; def validate_certifications(state): return {'safety_check_passed': 'ISO22000' in state['vendor_certs']}; def process_logistics(state): return {'order_logistics': {'status': 'approved' if state['safety_check_passed'] else 'rejected'}}; graph = StateGraph(OilProcurementState); graph.add_node('validate', validate_certifications); graph.add_node('logistics', process_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph = graph.compile()