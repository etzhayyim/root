from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NightwearState(TypedDict): items: List[str]; validation_results: List[str]; class NightwearProcessor:    def validate_spec(self, state: NightwearState): return {'validation_results': ['Material check passed', 'Flammability test passed']};    def finalize_order(self, state: NightwearState): return {'validation_results': state['validation_results'] + ['Order ready for shipment']}; graph = StateGraph(NightwearState); processor = NightwearProcessor(); graph.add_node('validate', processor.validate_spec); graph.add_node('finalize', processor.finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()