from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class WeldingState(TypedDict): specs: dict; valid: bool; def val_spec(state: WeldingState): state['valid'] = 'laser_wavelength_nm' in state['specs']; return state; builder = StateGraph(WeldingState); builder.add_node('validate', val_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()