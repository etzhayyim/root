from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SortingKitState(TypedDict): items: List[str]; safety_verified: bool; quality_score: float; def check_safety(state: SortingKitState): return {'safety_verified': True}; def evaluate_quality(state: SortingKitState): return {'quality_score': 0.95}; graph = StateGraph(SortingKitState); graph.add_node('safety', check_safety); graph.add_node('quality', evaluate_quality); graph.set_entry_point('safety'); graph.add_edge('safety', 'quality'); graph.add_edge('quality', END); compiled_graph = graph.compile()