from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class StationeryState(TypedDict): items: Annotated[Sequence[dict], operator.add]; validation_results: dict; def validate_stationery(state: StationeryState): return {'validation_results': {'passed': True, 'inspector': 'AutoQA_V1'}}; def finalize_procurement(state: StationeryState): return {'validation_results': {'status': 'Approved'}}; graph = StateGraph(StationeryState); graph.add_node('validate', validate_stationery); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()