from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): specs: dict; valid: bool; def validate_nut_specs(state: ProcureState): state['valid'] = 'Thread Specification' in state['specs'] and 'Material Grade' in state['specs']; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_nut_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()