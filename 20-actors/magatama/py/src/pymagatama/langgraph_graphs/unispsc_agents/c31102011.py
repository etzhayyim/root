from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CastState(TypedDict): input_spec: str; validation_report: list[str]; status: str; def validate_casting(state: CastState): report = []; if 'Material' not in state['input_spec']: report.append('Missing Material Grade'); return {'validation_report': report, 'status': 'PENDING' if report else 'VALIDATED'}; graph = StateGraph(CastState); graph.add_node('validate', validate_casting); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()