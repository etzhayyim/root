from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProcessorState(TypedDict): input_data: dict; processing_steps: Annotated[Sequence[str], operator.add]; status: str; def validate_task(state: ProcessorState): return {processing_steps: [f'validating_{state[input_data].get(type, task)}']}; def compute_task(state: ProcessorState): return {processing_steps: ['compute_intensive_optimization'], status: 'completed'}; graph = StateGraph(ProcessorState); graph.add_node('validate', validate_task); graph.add_node('compute', compute_task); graph.add_edge('validate', 'compute'); graph.add_edge('compute', END); graph.set_entry_point('validate'); graph = graph.compile()