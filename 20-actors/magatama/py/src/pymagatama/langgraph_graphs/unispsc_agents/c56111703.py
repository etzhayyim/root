from langgraph.graph import StateGraph, END; from typing import TypedDict; class FurnitureState(TypedDict): specs: dict; validation_passed: bool; def validate_furniture(state: FurnitureState): state['validation_passed'] = 'Load-bearing capacity' in state['specs'] and 'Material' in state['specs']; return state; builder = StateGraph(FurnitureState); builder.add_node('validate', validate_furniture); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()