from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SewingMachineState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: SewingMachineState): state['valid'] = 'motor_power' in state['specs'] and state['specs']['motor_power'] > 50; return state; graph = StateGraph(SewingMachineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()