from langgraph.graph import StateGraph, END; from typing import TypedDict; class LoopTurnerState(TypedDict): spec_data: dict; validated: bool; def validate_specs(state: LoopTurnerState): state['validated'] = 'material' in state['spec_data'] and 'length' in state['spec_data']; return state; def check_quality(state: LoopTurnerState): return 'passed' if state['validated'] else 'failed'; graph = StateGraph(LoopTurnerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()