from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SurgicalInstrumentState(TypedDict): instrument_id: str; sterilization_status: bool; compliance_docs: bool; graph = StateGraph(SurgicalInstrumentState); def validate_regulatory(state): return {'compliance_docs': True}; def check_sterility(state): return {'sterilization_status': True}; graph.add_node('validate_regulatory', validate_regulatory); graph.add_node('check_sterility', check_sterility); graph.set_entry_point('validate_regulatory'); graph.add_edge('validate_regulatory', 'check_sterility'); graph.add_edge('check_sterility', END); graph = graph.compile()