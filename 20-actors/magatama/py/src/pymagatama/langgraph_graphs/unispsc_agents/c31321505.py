from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: AssemblyState): state['valid'] = 'Tensile Strength' in state['specs'] and 'Welding Certification' in state['specs']; return state; def check_quality(state: AssemblyState): state['logs'].append('Performing UV weld inspection'); return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('inspect', check_quality); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()