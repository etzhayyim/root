from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeState(TypedDict): tube_id: str; alloy_spec: str; validation_passed: bool; inspection_logs: List[str]; def validate_alloy(state: TubeState): state['validation_passed'] = state['alloy_spec'] == 'low-alloy-certified'; return state; def perform_weld_check(state: TubeState): state['inspection_logs'].append('UV-weld depth and integrity confirmed'); return state; graph = StateGraph(TubeState); graph.add_node('validate', validate_alloy); graph.add_node('weld_check', perform_weld_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'weld_check'); graph.add_edge('weld_check', END); graph = graph.compile()