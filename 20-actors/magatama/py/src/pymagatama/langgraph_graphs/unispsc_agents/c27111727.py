from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): keys: Annotated[list, operator.add]; def validate_spec(state: AgentState): print('Validating compatibility...'); return {'keys': ['validated']}; def check_inventory(state: AgentState): print('Checking stock...'); return {'keys': ['stock_confirmed']}; graph = StateGraph(AgentState); graph.add_node('validate', validate_spec); graph.add_node('inventory', check_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph = graph.compile()