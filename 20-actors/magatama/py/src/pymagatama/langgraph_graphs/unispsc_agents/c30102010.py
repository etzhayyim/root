from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BrassFoilState(TypedDict): alloy_type: str; thickness: float; specs_ok: bool; def validate_specs(state: BrassFoilState): return {'specs_ok': state['thickness'] > 0}; def approve_procurement(state: BrassFoilState): return {'specs_ok': True}; graph = StateGraph(BrassFoilState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()