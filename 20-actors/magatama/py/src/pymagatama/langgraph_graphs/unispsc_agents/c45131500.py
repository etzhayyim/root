from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FilmState(TypedDict): items: List[str]; validated: bool; state: str; def validate_film_specs(state: FilmState): state['validated'] = all('ISO' in item for item in state['items']); return {'state': 'validated' if state['validated'] else 'error'}; graph = StateGraph(FilmState); graph.add_node('validate', validate_film_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()