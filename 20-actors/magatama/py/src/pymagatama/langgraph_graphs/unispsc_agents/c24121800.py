from langgraph.graph import StateGraph, END; from typing import TypedDict; class PackagingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PackagingState): state['valid'] = 'capacity' in state['specs'] and 'material' in state['specs']; return state; graph = StateGraph(PackagingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()