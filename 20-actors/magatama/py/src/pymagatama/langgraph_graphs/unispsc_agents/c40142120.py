from langgraph.graph import StateGraph, END; from typing import TypedDict; class PipeState(TypedDict): dimensions: dict; material_spec: str; compliance_verified: bool; def validate_dimensions(state: PipeState): state['compliance_verified'] = 'diameter' in state['dimensions'] and 'thickness' in state['dimensions']; return state; def check_material(state: PipeState): return {'compliance_verified': state['material_spec'] == 'Zinc-Grade-A'}; graph = StateGraph(PipeState); graph.add_node('validate', validate_dimensions); graph.add_node('material', check_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'material'); graph.add_edge('material', END); graph = graph.compile()