from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialGraphState(TypedDict): device_id: str; dial_protocol: str; is_compliant: bool; def validate_protocol(state: DialGraphState): state['is_compliant'] = state.get('dial_protocol') in ['SIP', 'PSTN']; return state; builder = StateGraph(DialGraphState); builder.add_node('validate', validate_protocol); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()