from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; def validate_head_immobilizer(state: State): results = []; data = state['input_data']; if 'material' not in data: results.append('Missing material specs'); if not data.get('radiotransparent', False): results.append('Radiolucency failure'); return {'validation_results': results}; graph = StateGraph(State); graph.add_node('validate', validate_head_immobilizer); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()