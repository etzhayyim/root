from langgraph.graph import StateGraph, END; from typing import TypedDict; class GearSpecState(TypedDict): specs: dict; valid: bool; def validate_specs(state: GearSpecState): state['valid'] = 'Hardness Rating' in state['specs'] and 'Material Composition' in state['specs']; return state; builder = StateGraph(GearSpecState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()