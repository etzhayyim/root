from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BenzocaineState(TypedDict): purity: float; compliance_docs: List[str]; approved: bool; def validate_purity(state: BenzocaineState): return {'approved': state['purity'] >= 99.0}; def check_regulatory_docs(state: BenzocaineState): return {'approved': len(state['compliance_docs']) >= 3}; graph = StateGraph(BenzocaineState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_docs', check_regulatory_docs); graph.add_edge('validate_purity', 'check_docs'); graph.add_edge('check_docs', END); graph.set_entry_point('validate_purity'); graph = graph.compile()