from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RoboticsState(TypedDict): component_id: str; specs: dict; validated: bool; def validate_specs(state: RoboticsState): state['validated'] = 'ISO' in state['specs'].get('certifications', []); return state; def check_compliance(state: RoboticsState): return 'compliance_check' if state['validated'] else END; graph = StateGraph(RoboticsState); graph.add_node('validation', validate_specs); graph.add_edge('validation', END); graph.set_entry_point('validation'); graph = graph.compile()