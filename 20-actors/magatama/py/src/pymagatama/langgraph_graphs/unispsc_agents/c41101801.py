from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ElectronGunState(TypedDict): specs: dict; validation_report: str; graph = StateGraph(ElectronGunState); def validate_vacuum_spec(state: ElectronGunState): state['validation_report'] = 'Validation passed: vacuum rating satisfied.' if state['specs'].get('vacuum') > 10**-9 else 'Validation failed: Insufficient vacuum.'; return state; graph.add_node('spec_validation', validate_vacuum_spec); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', END); graph = graph.compile()