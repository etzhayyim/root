from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class WashState(TypedDict): specs: dict; validation: bool; def validate_specs(state: WashState): return { "validation": "capacity" in state["specs"] and "energy_rating" in state["specs"] }; graph = StateGraph(WashState); graph.add_node("validate", validate_specs); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()