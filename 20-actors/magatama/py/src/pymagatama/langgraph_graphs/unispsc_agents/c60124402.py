from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolingState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: ToolingState): state['log'].append('Validating thickness and alloy...'); state['valid'] = 'Alloy Grade' in state['specs'] and state['specs']['Thickness Tolerance'] < 0.05; return state; graph = StateGraph(ToolingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()