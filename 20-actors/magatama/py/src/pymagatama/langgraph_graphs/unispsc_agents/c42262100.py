from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MortuaryState(TypedDict): equipment_type: str; specs: dict; compliant: bool; def validate_specs(state: MortuaryState): state['compliant'] = 'TemperatureRangeCelsius' in state['specs'] and state['specs']['TemperatureRangeCelsius'] <= 4; return state; graph = StateGraph(MortuaryState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()