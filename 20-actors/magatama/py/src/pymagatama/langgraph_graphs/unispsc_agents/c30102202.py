from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MetalPlateState(TypedDict): material_type: str; dimensions: dict; quality_certs: List[str]; approved: bool; def validate_specs(state: MetalPlateState): state['approved'] = all(k in state for k in ['material_type', 'dimensions', 'quality_certs']) and len(state['quality_certs']) > 0; return state; graph = StateGraph(MetalPlateState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()