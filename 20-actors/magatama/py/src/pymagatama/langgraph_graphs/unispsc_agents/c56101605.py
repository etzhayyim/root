from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BenchState(TypedDict): specs: dict; valid: bool; def validate_specs(state: BenchState): state['valid'] = 'Material' in state['specs'] and 'Load Capacity' in state['specs']; return state; builder = StateGraph(BenchState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()