from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChainState(TypedDict): chain_pitch: float; tensile_strength: float; compliance_verified: bool; def validate_specs(state: ChainState): state['compliance_verified'] = state['tensile_strength'] > 5000; return state; graph = StateGraph(ChainState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()