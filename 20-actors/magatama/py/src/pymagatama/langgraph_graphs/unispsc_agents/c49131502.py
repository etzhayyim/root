from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FishingLineState(TypedDict): specs: dict; valid: bool; validation_log: List[str]; def validate_line(state: FishingLineState): specs = state['specs']; logs = []; valid = True; if specs.get('strength', 0) <= 0: logs.append('Invalid strength'); valid = False; return {'valid': valid, 'validation_log': logs}; state = FishingLineState(specs={'strength': 10, 'material': 'PE'}, valid=False, validation_log=[]); graph = StateGraph(FishingLineState); graph.add_node('validate', validate_line); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()