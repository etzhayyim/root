from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class LegProtectorState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: LegProtectorState): if 'impact_rating' not in state['specs']: return {'validation_report': 'Missing impact rating field'}; return {'validation_report': 'Validated'}; graph = StateGraph(LegProtectorState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); compiled_graph = graph.compile()