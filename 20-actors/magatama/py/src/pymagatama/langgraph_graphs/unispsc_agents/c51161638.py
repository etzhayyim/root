from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input: str; validation: bool; compliance: bool; def check_gmp(state: State): state['validation'] = True; return state; def check_stability(state: State): state['compliance'] = True; return state; graph = StateGraph(State); graph.add_node('gmp_check', check_gmp); graph.add_node('stability_check', check_stability); graph.set_entry_point('gmp_check'); graph.add_edge('gmp_check', 'stability_check'); graph.add_edge('stability_check', END); graph = graph.compile()