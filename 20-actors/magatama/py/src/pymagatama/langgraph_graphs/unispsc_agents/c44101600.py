from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaperMachineState(TypedDict): machine_id: str; specs: dict; compliance_checked: bool; def validate_specs(state: PaperMachineState): state['compliance_checked'] = all(k in state['specs'] for k in ['voltage', 'safety_std']); return state; graph = StateGraph(PaperMachineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()