from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): drug_name: str; validation_passed: bool; logs: List[str]; def validate_regulatory_compliance(state: DrugProcurementState): state['validation_passed'] = True; state['logs'].append('Regulatory compliance checked.'); return state; def check_storage_requirements(state: DrugProcurementState): state['logs'].append('Storage requirements verified for cold chain.'); return state; graph = StateGraph(DrugProcurementState); graph.add_node('validate', validate_regulatory_compliance); graph.add_node('storage', check_storage_requirements); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph = graph.compile()