from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CertificationState(TypedDict): doc_type: str; security_level: str; validated: bool; def validate_security(state: CertificationState) -> CertificationState: state['validated'] = state['security_level'] in ['high', 'standard']; return state; def issue_document(state: CertificationState) -> str: return 'issued' if state['validated'] else 'rejected'; graph = StateGraph(CertificationState); graph.add_node('validate', validate_security); graph.add_node('issue', issue_document); graph.set_entry_point('validate'); graph.add_edge('validate', 'issue'); graph.add_edge('issue', END); graph = graph.compile()