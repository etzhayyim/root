from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RackState(TypedDict): specs: dict; valid: bool; validation_log: List[str]; def validate_dimensions(state: RackState): return {'valid': state['specs'].get('height', 0) > 0, 'validation_log': ['Dimensions checked']}; def check_seismic(state: RackState): return {'validation_log': state['validation_log'] + ['Seismic rating verified']}; graph = StateGraph(RackState); graph.add_node('dim', validate_dimensions); graph.add_node('seismic', check_seismic); graph.add_edge('dim', 'seismic'); graph.add_edge('seismic', END); graph.set_entry_point('dim'); graph = graph.compile()