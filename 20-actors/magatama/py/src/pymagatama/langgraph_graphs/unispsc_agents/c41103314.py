from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): data: dict; valid: bool; def val_spec(state: ProcState): state['valid'] = 'resolution' in state['data']; return state; def final_step(state: ProcState): return state; graph = StateGraph(ProcState); graph.add_node('validate', val_spec); graph.add_node('approve', final_step); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()