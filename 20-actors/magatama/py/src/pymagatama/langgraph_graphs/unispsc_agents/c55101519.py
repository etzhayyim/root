from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PeriodicalState(TypedDict): items: List[str]; validated: bool; sub_type: str; def validate_subscription(state: PeriodicalState): state['validated'] = True; return state; graph = StateGraph(PeriodicalState); graph.add_node('validate', validate_subscription); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()