from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CleanroomState(TypedDict): items: List[dict]; checked: bool; def validate_iso(state: CleanroomState): print('Validating ISO compliance...'); return {'checked': True}; graph = StateGraph(CleanroomState); graph.add_node('validate', validate_iso); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()