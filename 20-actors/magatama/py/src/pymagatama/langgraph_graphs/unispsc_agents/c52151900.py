from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BakewareState(TypedDict): specs: dict; approved: bool; def validate_specs(state: BakewareState): return {'approved': 'food_grade_certification' in state['specs']}; builder = StateGraph(BakewareState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()