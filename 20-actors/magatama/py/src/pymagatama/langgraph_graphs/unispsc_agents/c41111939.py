from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SensorState(TypedDict): sensor_id: str; specs: dict; valid: bool; def validate_sensors(state: SensorState): state['valid'] = 'Frequency range' in state['specs']; return state; builder = StateGraph(SensorState); builder.add_node('validate', validate_sensors); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()