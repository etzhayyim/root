from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_composition: str; temp_compliance: bool; approval_status: bool; def validate_composition(state: PharmState): return {'chemical_composition': 'Verified'} if state['chemical_composition'] else {'chemical_composition': 'Missing'}; def check_regulatory(state: PharmState): return {'approval_status': True} if 'FDA' in state.get('certs', []) else {'approval_status': False}; graph = StateGraph(PharmState); graph.add_node('composition_check', validate_composition); graph.add_node('regulatory_check', check_regulatory); graph.set_entry_point('composition_check'); graph.add_edge('composition_check', 'regulatory_check'); graph.add_edge('regulatory_check', END); graph = graph.compile()