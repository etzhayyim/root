from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PavingState(TypedDict): blocks_data: dict; validation_results: List[str]; def validate_paving(state: PavingState): return {'validation_results': ['Material density verified', 'Compressive strength within tolerance']}; def finalize_procurement(state: PavingState): return {'validation_results': state['validation_results'] + ['Project specs approved']}; graph = StateGraph(PavingState); graph.add_node('validate', validate_paving); graph.add_node('final', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'final'); graph.add_edge('final', END); compile_graph = graph.compile()