from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkOpsState(TypedDict): vendor: str; version: str; compliance_check: bool; graph = StateGraph(NetworkOpsState); def validate_compatibility(state: NetworkOpsState): return {"compliance_check": True}; def register_software(state: NetworkOpsState): return state; graph.add_node("validate", validate_compatibility); graph.add_node("register", register_software); graph.set_entry_point("validate"); graph.add_edge("validate", "register"); graph.add_edge("register", END); graph = graph.compile()