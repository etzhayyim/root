from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class AbrasiveState(TypedDict): code: str; specs: dict; validation_status: bool; def validate_purity(state: AbrasiveState) -> AbrasiveState: state['validation_status'] = state['specs'].get('purity_percentage', 0) >= 98; return state; def check_safety(state: AbrasiveState) -> AbrasiveState: if not state['validation_status']: state['validation_status'] = False; return state; graph = StateGraph(AbrasiveState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()