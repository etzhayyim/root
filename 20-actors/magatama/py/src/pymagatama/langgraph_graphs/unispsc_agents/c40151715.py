from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): specs: List[str]; validated: bool; def check_specs(state: PumpState): state['validated'] = len(state['specs']) > 0; return state; graph = StateGraph(PumpState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()