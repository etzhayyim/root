from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input_data: dict; verification_results: Annotated[list, operator.add]; status: str; def validate_cold_chain(state: ProcureState): state['verification_results'].append('Cold chain validated'); return {'status': 'validated'}; def check_safety(state: ProcureState): state['verification_results'].append('Safety compliance confirmed'); return {'status': 'approved'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_cold_chain); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()