from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PropellerState(TypedDict): propeller_id: str; compliance_docs: List[str]; approved: bool; def validate_specs(state: PropellerState): state['approved'] = 'FAA_Cert' in state['compliance_docs']; return state; def check_export_controls(state: PropellerState): print('Checking ITAR/EAR status'); return state; graph = StateGraph(PropellerState); graph.add_node('validate', validate_specs); graph.add_node('export', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph = graph.compile()