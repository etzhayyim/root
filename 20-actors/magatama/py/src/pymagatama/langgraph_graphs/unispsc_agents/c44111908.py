from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BoardState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; def validate_surface_quality(state: BoardState): return {'validation_results': ['Surface durability check passed']}; def validate_magnetic_strength(state: BoardState): return {'validation_results': ['Magnetic retention check passed']}; graph = StateGraph(BoardState); graph.add_node('surface_check', validate_surface_quality); graph.add_node('magnetic_check', validate_magnetic_strength); graph.set_entry_point('surface_check'); graph.add_edge('surface_check', 'magnetic_check'); graph.add_edge('magnetic_check', END); graph = graph.compile()