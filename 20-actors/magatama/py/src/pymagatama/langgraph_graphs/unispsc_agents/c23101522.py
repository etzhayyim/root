from langgraph.graph import StateGraph, END; from typing import TypedDict; class GeneratorState(TypedDict): capacity: int; compliant: bool; def check_compliance(state: GeneratorState): return {'compliant': state['capacity'] > 0}; graph = StateGraph(GeneratorState); graph.add_node('validate', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()