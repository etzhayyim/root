from langgraph.graph import StateGraph, END; from typing import TypedDict; class PizzaCutterState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PizzaCutterState): return {'valid': 'Food Safety Certification' in state['specs']}; graph = StateGraph(PizzaCutterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()