from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_id: str; purity_check: bool; storage_validated: bool; def validate_purity(state: PharmaState): state['purity_check'] = True; return state; def validate_storage(state: PharmaState): state['storage_validated'] = True; return state; workflow = StateGraph(PharmaState); workflow.add_node('validate_purity', validate_purity); workflow.add_node('validate_storage', validate_storage); workflow.set_entry_point('validate_purity'); workflow.add_edge('validate_purity', 'validate_storage'); workflow.add_edge('validate_storage', END); graph = workflow.compile()