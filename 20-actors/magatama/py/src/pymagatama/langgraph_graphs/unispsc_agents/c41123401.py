from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DosingCupState(TypedDict): specs: dict; validation_passed: bool; logs: List[str]; def validate_metrology(state: DosingCupState): state['validation_passed'] = state['specs'].get('accuracy') == 'high'; return state; def check_material(state: DosingCupState): state['logs'].append('Verifying material compatibility'); return state; graph = StateGraph(DosingCupState); graph.add_node('metrology', validate_metrology); graph.add_node('material', check_material); graph.set_entry_point('metrology'); graph.add_edge('metrology', 'material'); graph.add_edge('material', END); graph = graph.compile()