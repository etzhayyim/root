from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; validation_passed: bool; specs: dict; def validate_kit(state: KitState): state['validation_passed'] = 'ISO_13485_certification' in state['specs']; return state; def check_compatibility(state: KitState): if state['validation_passed']: print(f'Processing kit: {state['kit_id']}'); return 'complete'; return 'invalid'; builder = StateGraph(KitState); builder.add_node('validate', validate_kit); builder.add_node('check_comp', check_compatibility); builder.set_entry_point('validate'); builder.add_edge('validate', 'check_comp'); builder.add_edge('check_comp', END); graph = builder.compile()