from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_data: dict; validation_errors: List[str]; def validate_rivet_density(state: AssemblyState): print('Validating rivet density...'); return {'validation_errors': []}; def check_material_specs(state: AssemblyState): print('Checking material alloy specs...'); return {'validation_errors': []}; graph = StateGraph(AssemblyState); graph.add_node('density_check', validate_rivet_density); graph.add_node('material_check', check_material_specs); graph.set_entry_point('density_check'); graph.add_edge('density_check', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()