from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LeadComponentState(TypedDict): specs: dict; compliance: bool; error: str; def validate_lead_specs(state: LeadComponentState): purity = state['specs'].get('lead_purity_percentage', 0); state['compliance'] = purity >= 99.0; return state; def check_danger(state: LeadComponentState): if not state['compliance']: state['error'] = 'Purity breach in lead components'; return {'compliance': False}; return {'compliance': True}; graph = StateGraph(LeadComponentState); graph.add_node('validate', validate_lead_specs); graph.add_node('safety_check', check_danger); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph.set_entry_point('validate'); graph = graph.compile()