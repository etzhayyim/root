from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScraperState(TypedDict): data: dict; validation_errors: List[str]; graph = StateGraph(ScraperState); def validate_specs(state: ScraperState): return {'validation_errors': [] if 'blade_material' in state['data'] else ['Missing blade material']}; def finalize_procurement(state: ScraperState): return {'data': {**state['data'], 'status': 'ready'}}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()