from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HearingAidState(TypedDict): device_id: str; medical_certs: List[str]; qc_status: str; def validate_certs(state): return {'qc_status': 'verified' if 'FDA' in state['medical_certs'] else 'rejected'}; def finalize_procurement(state): return {'qc_status': 'approved'}; graph = StateGraph(HearingAidState); graph.add_node('validate', validate_certs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); compile_graph = graph.compile()