from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RugSpec(TypedDict): specs: dict; compliance: bool; graph = StateGraph(RugSpec); def validate_specs(state: RugSpec): state['compliance'] = 'Flame retardancy' in state['specs'] and 'Material' in state['specs']; return state; def finalize(state: RugSpec): return state; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()