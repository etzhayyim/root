from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class FertilizerState(TypedDict): input_data: Dict; validation_logs: Annotated[List[str], add_messages]; status: str; def validate_composition(state: FertilizerState): state['validation_logs'].append('Checking nutrient composition'); return {'status': 'validated'} if state['input_data'].get('nitrogen') else {'status': 'rejected'}; def finalize_procurement(state: FertilizerState): state['validation_logs'].append('Generating procurement order'); return {'status': 'ordered'}; graph = StateGraph(FertilizerState); graph.add_node('validate', validate_composition); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()