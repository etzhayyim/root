from langgraph.graph import StateGraph, END; from typing import TypedDict; class DiaryState(TypedDict): specs: dict; approved: bool; def validate_specs(state: DiaryState): state['approved'] = 'paper_weight' in state['specs'] and state['specs']['paper_weight'] >= 80; return state; graph = StateGraph(DiaryState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()