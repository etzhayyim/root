from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GasDetectorState(TypedDict): model_specs: dict; certification_check: bool; validation_log: List[str]; def validate_specs(state: GasDetectorState): state['validation_log'].append('Validating sensor range'); return {'certification_check': True} if 'ex_cert' in state['model_specs'] else {'certification_check': False}; def final_review(state: GasDetectorState): state['validation_log'].append('Final safety check passed'); return state; graph = StateGraph(GasDetectorState); graph.add_node('validate', validate_specs); graph.add_node('review', final_review); graph.add_edge('validate', 'review'); graph.add_edge('review', END); graph.set_entry_point('validate'); graph = graph.compile()