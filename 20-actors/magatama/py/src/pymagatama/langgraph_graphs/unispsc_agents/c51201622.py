from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class BiospecimenState(TypedDict): specimen_id: str; biohazard_level: int; status: str; def validate_safety(state: BiospecimenState): return {'status': 'CLEARED' if state['biohazard_level'] >= 3 else 'REJECTED'}; def finalize_handling(state: BiospecimenState): return {'status': 'READY_FOR_SHIPMENT'}; graph = StateGraph(BiospecimenState); graph.add_node('validate', validate_safety); graph.add_node('final', finalize_handling); graph.add_edge('validate', 'final'); graph.add_edge('final', END); graph.set_entry_point('validate'); graph = graph.compile()