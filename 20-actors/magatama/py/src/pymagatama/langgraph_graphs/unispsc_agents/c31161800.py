from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class WasherState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(WasherState); def validate_specs(state: WasherState): report = []; r = state['specs'].get('hardness', 0); report.append('Check hardness rating') if r < 50 else None; return {'validation_report': report}; def check_compliance(state: WasherState): return {'validation_report': state['validation_report'] + ['Compliance verified']}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()