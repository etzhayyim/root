from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FireExtinguisherState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(FireExtinguisherState); def validate_specs(state: FireExtinguisherState): state['compliance_check'] = 'FireRating' in state['specs'] and state['specs']['weight'] > 0; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()