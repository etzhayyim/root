from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ApparelState(TypedDict): fabric_specs: dict; inspection_status: str; graph = StateGraph(ApparelState); def validate_compliance(state: ApparelState): state['inspection_status'] = 'COMPLIANT' if 'flammability' in state['fabric_specs'] else 'PENDING'; return state; graph.add_node('compliance', validate_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', END); app = graph.compile()