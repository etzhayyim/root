from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SuctionUnitState(TypedDict): vendor_certs: List[str]; technical_specs: dict; approved: bool; def validate_specs(state: SuctionUnitState): return {'approved': 'ISO_10079_1' in state['vendor_certs']}; graph = StateGraph(SuctionUnitState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()