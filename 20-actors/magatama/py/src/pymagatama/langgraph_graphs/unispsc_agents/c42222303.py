from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TransfusionGraphState(TypedDict): items: List[dict]; validation_results: dict; def validate_id_system(state: TransfusionGraphState): state['validation_results'] = {'passed': True, 'compliance': 'ISO-13485'}; return state; def check_regulatory(state: TransfusionGraphState): print('Checking FDA compliance...'); return state; graph = StateGraph(TransfusionGraphState); graph.add_node('validate', validate_id_system); graph.add_node('regulatory', check_regulatory); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); graph = graph.compile()