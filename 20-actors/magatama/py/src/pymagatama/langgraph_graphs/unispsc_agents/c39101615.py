from langgraph.graph import StateGraph, END; from typing import TypedDict; class LampState(TypedDict): input_data: dict; compliance_status: bool; def check_hazmat(state: LampState): state['compliance_status'] = state['input_data'].get('mercury_content', 0) < 50; return state; graph = StateGraph(LampState); graph.add_node('hazmat_check', check_hazmat); graph.set_entry_point('hazmat_check'); graph.add_edge('hazmat_check', END); graph = graph.compile()