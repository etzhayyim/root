from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BellSpecState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: BellSpecState): errors = []; if not state['specs'].get('Material Grade'): errors.append('Missing material grade'); return {'validation_errors': errors, 'approved': len(errors) == 0}; builder = StateGraph(BellSpecState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()