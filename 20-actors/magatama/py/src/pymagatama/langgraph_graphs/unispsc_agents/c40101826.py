from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): unit: str; status: str; def validate_heater(state: State): return {'status': 'validated'} if state.get('unit') else {'status': 'error'}; graph = StateGraph(State); graph.add_node('validate', validate_heater); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()