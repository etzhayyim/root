from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: list; status: str; def validate_instrument(state: State): results = []; if 'material' not in state['input_data']: results.append('Missing material spec'); state['validation_results'] = results; state['status'] = 'COMPLETED' if not results else 'FAILED'; return state; graph = StateGraph(State); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()