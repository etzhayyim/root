from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): spec: dict; validated: bool; state: str; def validate_specs(state: State): specs = state['spec']; is_valid = 'load_capacity_kg' in specs and 'ip_rating' in specs; return {'validated': is_valid, 'state': 'validated' if is_valid else 'rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()