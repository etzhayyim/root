from langgraph.graph import StateGraph, END; from typing import TypedDict; class BasinState(TypedDict): input_data: dict; validation_passed: bool; def validate_basin(state: BasinState): state['validation_passed'] = all(k in state['input_data'] for k in ['material', 'sterile_status']); return state; graph = StateGraph(BasinState); graph.add_node('validate', validate_basin); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()