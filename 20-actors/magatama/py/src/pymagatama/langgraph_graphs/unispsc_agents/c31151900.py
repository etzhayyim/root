from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StraupState(TypedDict): specs: dict; validation_errors: List[str]; def validate_strap(state: StraupState): wll = state['specs'].get('working_load_limit', 0); if wll <= 0: state['validation_errors'].append('Invalid WLL'); return state; def finalize_procurement(state: StraupState): return state; graph = StateGraph(StraupState); graph.add_node('validate', validate_strap); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()