from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EggProcurementState(TypedDict): vendor_id: str; quality_docs: List[str]; inspection_passed: bool; def validate_docs(state: EggProcurementState): return {'inspection_passed': len(state['quality_docs']) >= 3}; def route_procurement(state: EggProcurementState): return 'complete' if state['inspection_passed'] else 'reject'; graph = StateGraph(EggProcurementState); graph.add_node('validate', validate_docs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()