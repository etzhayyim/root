from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WiringState(TypedDict): wire_specs: dict; status: str; def validate_specs(state: WiringState): return {'status': 'validated' if 'voltage' in state['wire_specs'] else 'error'}; def finalize_order(state: WiringState): return {'status': 'completed'}; graph = StateGraph(WiringState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()