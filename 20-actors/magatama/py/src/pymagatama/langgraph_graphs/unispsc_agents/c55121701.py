from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): nameplate_data: dict; validation_results: List[str]; graph = StateGraph(AgentState); def validate_specs(state: AgentState): return {'validation_results': ['Material check passed', 'Etching depth verified']}; def finalize_order(state: AgentState): return {'validation_results': state['validation_results'] + ['Order ready for procurement']}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()