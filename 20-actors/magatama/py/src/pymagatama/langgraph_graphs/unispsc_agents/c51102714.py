from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IrrigationState(TypedDict): nfc_code: str; quality_passed: bool; batch_id: str; def validate_purity(state: IrrigationState): return {'quality_passed': True} if 'USP' in state['nfc_code'] else {'quality_passed': False}; def check_expiry(state: IrrigationState): return {'quality_passed': True}; graph = StateGraph(IrrigationState); graph.add_node('validate', validate_purity); graph.add_node('expiry', check_expiry); graph.add_edge('validate', 'expiry'); graph.add_edge('expiry', END); graph.set_entry_point('validate'); graph = graph.compile()