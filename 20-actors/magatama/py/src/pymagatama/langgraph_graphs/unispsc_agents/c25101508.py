from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VehicleState(TypedDict): vehicle_spec: dict; compliance_status: bool; audit_log: List[str]; def validate_specs(state: VehicleState): state['compliance_status'] = state['vehicle_spec'].get('hp', 0) > 300; state['audit_log'].append('Engine power verified'); return state; def check_regulations(state: VehicleState): state['compliance_status'] = state['compliance_status'] and state['vehicle_spec'].get('emission_standard') == 'Euro6'; return state; graph = StateGraph(VehicleState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()