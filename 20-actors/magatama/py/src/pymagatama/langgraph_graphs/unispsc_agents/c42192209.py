from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RampState(TypedDict): ramp_spec: dict; compliance_status: bool; def validate_gradient(state: RampState): slope = state['ramp_spec'].get('gradient', 0); state['compliance_status'] = slope <= 8.33; return state; graph = StateGraph(RampState); graph.add_node('validate_gradient', validate_gradient); graph.set_entry_point('validate_gradient'); graph.add_edge('validate_gradient', END); graph = graph.compile()