from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_id: str; specs: dict; validation_passed: bool; def validate_impact_wrench(state: ToolState): specs = state.get('specs', {}); state['validation_passed'] = 'torque' in specs and 'pressure' in specs; return state; def report_status(state: ToolState): print(f'Validated tool: {state['tool_id']}, Status: {state['validation_passed']}'); return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_impact_wrench); graph.add_node('report', report_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()