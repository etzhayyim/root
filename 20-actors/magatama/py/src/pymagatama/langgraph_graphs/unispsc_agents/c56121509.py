from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TableSpecState(TypedDict): specs: dict; validation_errors: List[str]; def validate_dimensions(state: TableSpecState): errors = []; if state['specs'].get('height', 0) < 60: errors.append('Table height below standard ergonomic requirements'); return {'validation_errors': errors}; def check_material(state: TableSpecState): return {'validation_errors': []}; graph = StateGraph(TableSpecState); graph.add_node('validate_dims', validate_dimensions); graph.add_node('check_material', check_material); graph.set_entry_point('validate_dims'); graph.add_edge('validate_dims', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()