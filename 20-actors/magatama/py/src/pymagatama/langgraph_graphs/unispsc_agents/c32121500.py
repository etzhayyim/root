from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CapacitorState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: CapacitorState): required = ['capacitance', 'voltage']; missing = [f for f in required if f not in state['specs']]; state['valid'] = len(missing) == 0; state['error'] = f'Missing: {missing}' if missing else ''; return state; graph = StateGraph(CapacitorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()