from langgraph.graph import StateGraph, END; from typing import TypedDict; class MagnetometerState(TypedDict): sensitivity: float; coords: dict; valid: bool; def validate_sensor(state: MagnetometerState): state['valid'] = state['sensitivity'] > 0.01; return state; def check_export_compliance(state: MagnetometerState): return state; graph = StateGraph(MagnetometerState); graph.add_node('validate', validate_sensor); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()