from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalInstrumentState(TypedDict): vendor_certs: List[str]; safety_approved: bool; workflow_complete: bool; def validate_compliance(state: SurgicalInstrumentState): return {'safety_approved': 'ISO10993' in state['vendor_certs']}; def finalize_log(state: SurgicalInstrumentState): return {'workflow_complete': True}; graph = StateGraph(SurgicalInstrumentState); graph.add_node('validate', validate_compliance); graph.add_node('finalize', finalize_log); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()