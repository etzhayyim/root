from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LensState(TypedDict): lens_specs: dict; validation_report: List[str]; approved: bool; def validate_optics(state: LensState): specs = state['lens_specs']; errors = []; if specs.get('focal_length', 0) <= 0: errors.append('Invalid focal length'); state['validation_report'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(LensState); graph.add_node('validate', validate_optics); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()