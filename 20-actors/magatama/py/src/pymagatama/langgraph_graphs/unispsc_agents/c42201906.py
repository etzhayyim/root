from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class XRayClipState(TypedDict): clips_data: dict; approval_status: bool; def validate_spec(state: XRayClipState): status = state['clips_data'].get('tensile_strength', 0) > 10; return {'approval_status': status}; graph = StateGraph(XRayClipState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()