from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): safety_compliant: bool; weight_limit: float; status: str; def check_safety(state: State): state['safety_compliant'] = state['weight_limit'] >= 150; return {'status': 'passed' if state['safety_compliant'] else 'failed'}; graph = StateGraph(State); graph.add_node('safety_check', check_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()