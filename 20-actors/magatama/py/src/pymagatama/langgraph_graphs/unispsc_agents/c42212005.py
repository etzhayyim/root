from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CleaningToolState(TypedDict): tool_name: str; specifications: dict; validation_status: bool; def validate_ergonomics(state: CleaningToolState): state['validation_status'] = True if state['specifications'].get('handle_length', 0) > 60 else False; return state; graph = StateGraph(CleaningToolState); graph.add_node('validate', validate_ergonomics); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()