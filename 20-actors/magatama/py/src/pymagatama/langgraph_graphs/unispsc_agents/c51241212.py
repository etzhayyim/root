from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PapainState(TypedDict): assay_results: dict; validated: bool; analysis_report: str; graph = StateGraph(PapainState); def validate_spec(state: PapainState): return {'validated': state['assay_results'].get('activity', 0) > 600000}; def check_compliance(state: PapainState): return {'analysis_report': 'Passed' if state['validated'] else 'Failed'}; graph.add_node('validate', validate_spec); graph.add_node('check', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()