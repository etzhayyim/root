from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaperProcurementState(TypedDict): items: List[dict]; validation_results: List[str]; def validate_paper_spec(state: PaperProcurementState): print('Validating paper dimensions and GSM...'); return {'validation_results': ['Specs meet industry standards']}; graph = StateGraph(PaperProcurementState); graph.add_node('validate', validate_paper_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()