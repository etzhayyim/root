from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RailLightState(TypedDict): specs: dict; compliance: bool; validator_msg: str; def validate_rail_specs(state: RailLightState): specs = state['specs']; is_compliant = specs.get('ip_rating', 0) >= 65 and 'vibration_std' in specs; return {'compliance': is_compliant, 'validator_msg': 'Spec check pass' if is_compliant else 'Spec check fail'}; graph = StateGraph(RailLightState); graph.add_node('validate', validate_rail_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()