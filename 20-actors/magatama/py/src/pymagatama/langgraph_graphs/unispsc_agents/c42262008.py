from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EyeCapState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_sterilization(state: EyeCapState): state['validation_report'] = 'Sterility check passed' if 'ISO11135' in state['specs'].get('cert', '') else 'Failed'; state['approved'] = (state['validation_report'] == 'Sterility check passed'); return state; graph = StateGraph(EyeCapState); graph.add_node('validate', validate_sterilization); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()