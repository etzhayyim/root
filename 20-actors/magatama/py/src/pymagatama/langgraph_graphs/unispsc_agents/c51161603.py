from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): input_data: dict; validation_log: Annotated[Sequence[str], operator.add]; compliance_passed: bool; def validate_api(state: PharmaState): status = state['input_data'].get('purity', 0) >= 99.0; return {'validation_log': [f'Purity check: {status}'], 'compliance_passed': status}; def generate_report(state: PharmaState): return {'validation_log': ['Compliance report generated']}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_api); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()