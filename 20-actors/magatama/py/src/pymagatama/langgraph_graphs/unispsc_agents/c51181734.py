from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): input_data: dict; validation_report: List[str]; status: str; def validate_purity(state: PharmState): state['validation_report'].append('Assayed Purity against USP/EP standards'); return state; def check_regulatory(state: PharmState): state['validation_report'].append('Regulatory compliance verified'); return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()