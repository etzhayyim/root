from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): part_number: str; specs: dict; validation: bool; def validate_part(state: CompressorState): state['validation'] = state['part_number'].isalnum(); return state; def check_specs(state: CompressorState): state['validation'] = 'pressure' in state['specs']; return state; graph = StateGraph(CompressorState); graph.add_node('validate', validate_part); graph.add_node('spec_check', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'spec_check'); graph.add_edge('spec_check', END); graph = graph.compile()