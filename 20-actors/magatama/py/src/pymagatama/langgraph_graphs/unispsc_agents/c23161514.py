from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input_data: dict; validations: Annotated[list, operator.add]; graph = StateGraph(ProcureState); def validate_specs(state): return {'validations': ['Spec checked: Precision verified']}; def check_export_compliance(state): return {'validations': ['Export control: Cleared for Dual-Use']}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()