from langgraph.graph import StateGraph, END; from typing import TypedDict; class ToolState(TypedDict): model: str; specs: dict; validated: bool; def check_compatibility(state: ToolState): state['validated'] = 'steering_column_dia' in state['specs']; return state; graph = StateGraph(ToolState); graph.add_node('verify', check_compatibility); graph.set_entry_point('verify'); graph.add_edge('verify', END); graph = graph.compile()