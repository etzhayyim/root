from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LanguageResourceState(TypedDict): resource_list: List[str]; validation_report: str; def validate_resources(state: LanguageResourceState): return {'validation_report': 'Resources verified against curriculum standards.'}; def format_output(state: LanguageResourceState): return {'validation_report': 'Finalized for procurement.'}; graph = StateGraph(LanguageResourceState); graph.add_node('validate', validate_resources); graph.add_node('format', format_output); graph.add_edge('validate', 'format'); graph.add_edge('format', END); graph.set_entry_point('validate'); graph = graph.compile()