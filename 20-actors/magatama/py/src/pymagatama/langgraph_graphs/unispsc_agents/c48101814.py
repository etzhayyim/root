from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_passed: bool; def validate_wok_specs(state: ProcurementState): specs = state['specs']; state['validation_passed'] = all(k in specs for k in ['material_composition', 'food_grade_certification']); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_wok_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()