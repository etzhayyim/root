from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): assembly_id: str; specs: dict; valid: bool; error: str; def validate_specs(state: AssemblyState): state['valid'] = all(k in state['specs'] for k in ['alloy', 'tensile_strength']); return state; def check_export(state: AssemblyState): print('Checking EAR/ITAR compliance...'); state['valid'] = True; return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()