from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_specs: dict; validation_results: List[str]; graph = StateGraph(ProcurementState); def validate_accessibility(state: ProcurementState): state['validation_results'].append('Accessibility spec verified'); return state; def check_safety_standards(state: ProcurementState): state['validation_results'].append('Material safety checked'); return state; graph.add_node('validate', validate_accessibility); graph.add_node('safety', check_safety_standards); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()