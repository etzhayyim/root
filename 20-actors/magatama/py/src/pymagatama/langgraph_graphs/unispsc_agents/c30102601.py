from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SteelState(TypedDict): chemical_report: str; specs: dict; compliance_status: bool; def validate_composition(state: SteelState): state['compliance_status'] = 'Fe' in state['chemical_report'] and 'C' in state['chemical_report']; return state; def check_specs(state: SteelState): return {'compliance_status': state['specs'].get('thickness') < 5.0}; graph = StateGraph(SteelState); graph.add_node('validate', validate_composition); graph.add_node('spec_check', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'spec_check'); graph.add_edge('spec_check', END); compiled_graph = graph.compile()