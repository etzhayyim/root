from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): certificate_valid: bool; temperature_logs: List[float]; approved: bool; def validate_quality(state: PharmaState): return {'approved': state['certificate_valid'] and max(state['temperature_logs']) < 25.0}; graph = StateGraph(PharmaState); graph.add_node('qc_check', validate_quality); graph.set_entry_point('qc_check'); graph.add_edge('qc_check', END); graph = graph.compile()