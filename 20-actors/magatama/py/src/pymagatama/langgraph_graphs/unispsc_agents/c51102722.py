from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): vendor_check: bool; compliance_status: bool; inventory_update: bool; def check_compliance(state: ProcurementState): return {'compliance_status': True} if 'Regulatory Approval' in state else {'compliance_status': False}; def update_stock(state: ProcurementState): return {'inventory_update': True}; graph = StateGraph(ProcurementState); graph.add_node('compliance', check_compliance); graph.add_node('inventory', update_stock); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'inventory'); graph.add_edge('inventory', END); graph = graph.compile()