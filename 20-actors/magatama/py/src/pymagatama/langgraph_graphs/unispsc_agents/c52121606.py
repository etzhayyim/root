from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; valid: bool; def validate_mats(state: ProcState): state['valid'] = all(k in state['specs'] for k in ['material', 'dimensions']); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_mats); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()