from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class LabEquipmentState(TypedDict): specs: dict; calibration_valid: bool; graph = StateGraph(LabEquipmentState); def validate_specs(state: LabEquipmentState): return {'calibration_valid': 'ISO17025' in state['specs']}; def export_check(state: LabEquipmentState): return {'export_ok': True}; graph.add_node('validate', validate_specs); graph.add_node('export', export_check); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph.set_entry_point('validate'); graph = graph.compile()