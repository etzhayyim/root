from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EvacState(TypedDict): specs: dict; compliance: bool; validator_msg: str; def validate_aero_specs(state: EvacState): state['compliance'] = all(k in state['specs'] for k in ['load_cap', 'cert_type']); return state; graph = StateGraph(EvacState); graph.add_node('validator', validate_aero_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()