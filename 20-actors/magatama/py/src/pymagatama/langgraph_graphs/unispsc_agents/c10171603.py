from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MiningToolState(TypedDict): tool_id: str; specs: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_specs(state: MiningToolState): results = []; if 'material_grade' not in state['specs']: results.append('Missing material grade'); return {'validation_results': results, 'status': 'validating'}; def run_stress_sim(state: MiningToolState): return {'status': 'simulating'}; graph = StateGraph(MiningToolState); graph.add_node('validate', validate_specs); graph.add_node('simulate', run_stress_sim); graph.add_edge('validate', 'simulate'); graph.add_edge('simulate', END); graph.set_entry_point('validate'); graph = graph.compile()