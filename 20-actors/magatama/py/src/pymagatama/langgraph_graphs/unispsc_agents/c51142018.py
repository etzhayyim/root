from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): chemical_data: dict; compliance_check: bool; graph = StateGraph(PharmState); def validate_purity(state: PharmState): state['compliance_check'] = state['chemical_data'].get('purity', 0) >= 99.5; return state; def check_hazmat(state: PharmState): return {'compliance_check': True}; graph.add_node('validate_purity', validate_purity); graph.add_node('check_hazmat', check_hazmat); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_hazmat'); graph.add_edge('check_hazmat', END); graph = graph.compile()