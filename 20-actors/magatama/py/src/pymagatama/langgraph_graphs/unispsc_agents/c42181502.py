from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TransilluminatorState(TypedDict): specs: dict; compliance: bool; flow: List[str]; def validate_specs(state: TransilluminatorState): state['compliance'] = 'certification_standard' in state['specs']; return state; def check_medical_grade(state: TransilluminatorState): state['flow'].append('Verification of medical certification'); return state; graph = StateGraph(TransilluminatorState); graph.add_node('validate', validate_specs); graph.add_node('medical_check', check_medical_grade); graph.add_edge('validate', 'medical_check'); graph.add_edge('medical_check', END); graph.set_entry_point('validate'); app = graph.compile()