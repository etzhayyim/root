from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): material_spec: str; dimensions: dict; quality_report: str; approved: bool; def validate_specs(state: ForgingState): return {'approved': len(state['material_spec']) > 0 and 'report' in state['quality_report'].lower()}; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()