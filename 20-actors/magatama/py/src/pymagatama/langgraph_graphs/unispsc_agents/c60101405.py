from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_data: dict; specs: dict; approved: bool; def validate_ribbon_specs(state: ProcureState): material = state['specs'].get('fabric_material'); state['approved'] = material in ['satin', 'silk', 'polyester']; return state; builder = StateGraph(ProcureState); builder.add_node('validate', validate_ribbon_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()