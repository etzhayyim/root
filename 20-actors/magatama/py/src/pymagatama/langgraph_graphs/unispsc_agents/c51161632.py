from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): chemical_id: str; purity: float; compliant: bool; def validate_purity(state: State): state['compliant'] = state['purity'] >= 99.0; return state; def verify_regulatory(state: State): state['compliant'] = state['compliant'] and True; return state; graph = StateGraph(State); graph.add_node('validate', validate_purity); graph.add_node('regulatory', verify_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()