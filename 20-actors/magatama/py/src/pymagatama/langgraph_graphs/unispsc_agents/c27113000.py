from langgraph.graph import StateGraph, END; from typing import TypedDict; class BrushState(TypedDict): brush_spec: dict; validation_passed: bool; def validate_specs(state: BrushState): specs = state.get('brush_spec', {}); state['validation_passed'] = 'bristle_stiffness' in specs and 'brush_material' in specs; return state; graph = StateGraph(BrushState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()