from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SprayEquipmentState(TypedDict): specs: dict; compliance_checked: bool; status: str; def validate_specs(state: SprayEquipmentState): state['compliance_checked'] = all(val is not None for val in state['specs'].values()); return {'status': 'validated' if state['compliance_checked'] else 'failed'}; graph = StateGraph(SprayEquipmentState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()