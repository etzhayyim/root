from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GelDryerState(TypedDict): specs: dict; safety_check: bool; graph = StateGraph(GelDryerState); def validate_vacuum(state: GelDryerState): return {'safety_check': state['specs'].get('vacuum_pressure', 0) > 0}; def finalize_spec(state: GelDryerState): return {'specs': {**state['specs'], 'status': 'validated'}}; graph.add_node('validate', validate_vacuum); graph.add_node('finalize', finalize_spec); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()