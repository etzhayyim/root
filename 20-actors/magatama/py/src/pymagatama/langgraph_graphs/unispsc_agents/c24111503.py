from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlasticBagState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: PlasticBagState): results = []; if state['specs'].get('thickness', 0) < 10: results.append('Thickness below safety threshold'); return {'validation_results': results}; graph = StateGraph(PlasticBagState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()