from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_compliance: bool; validations: List[str]; def check_reagents(state: KitState): state['validations'].append('Reagents verified'); return {'kit_compliance': True}; def validate_kit(state: KitState): state['validations'].append('Sampling standards met'); return state; graph = StateGraph(KitState); graph.add_node('check_reagents', check_reagents); graph.add_node('validate_kit', validate_kit); graph.set_entry_point('check_reagents'); graph.add_edge('check_reagents', 'validate_kit'); graph.add_edge('validate_kit', END); graph = graph.compile()