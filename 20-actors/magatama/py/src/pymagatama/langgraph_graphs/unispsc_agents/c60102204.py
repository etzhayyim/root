from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PhonicsState(TypedDict): items: List[str]; validated_items: List[dict]; def validate_card_spec(state: PhonicsState): return {'validated_items': [{'item': i, 'status': 'approved'} for i in state['items']]}; graph = StateGraph(PhonicsState); graph.add_node('validate', validate_card_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()