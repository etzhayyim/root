from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): specs: dict; valid: bool; def validate_specs(state: State): specs = state.get('specs', {}); state['valid'] = 'tonnage' in specs and 'length' in specs; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()