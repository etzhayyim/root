from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OtologyState(TypedDict): items: List[str]; validation_results: List[str]; graph = StateGraph(OtologyState); def validate_device(state: OtologyState): return {'validation_results': ['ISO13485_verified', 'FDA_certified']}; def check_sterility(state: OtologyState): return {'validation_results': state['validation_results'] + ['Sterility_Batch_Validated']}; graph.add_node('validate', validate_device); graph.add_node('sterile_check', check_sterility); graph.add_edge('validate', 'sterile_check'); graph.add_edge('sterile_check', END); graph.set_entry_point('validate'); graph = graph.compile()