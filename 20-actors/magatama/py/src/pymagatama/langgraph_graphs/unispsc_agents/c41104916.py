from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): specs: dict; validation_results: List[str]; def validate_centrifuge_specs(state: FilterState): val = state['specs'].get('centrifugal_force_tolerance', 0); return {'validation_results': ['Passed' if val > 5000 else 'Failed']}; workflow = StateGraph(FilterState); workflow.add_node('validate', validate_centrifuge_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()