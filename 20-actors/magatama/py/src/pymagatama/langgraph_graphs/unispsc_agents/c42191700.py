from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class MedicalGasState(TypedDict): messages: Annotated[list, add_messages]; compliance_status: bool; def validate_safety_data(state: MedicalGasState): state['compliance_status'] = True; return state; graph = StateGraph(MedicalGasState); graph.add_node('validate_gas', validate_safety_data); graph.set_entry_point('validate_gas'); graph.add_edge('validate_gas', END); graph = graph.compile()