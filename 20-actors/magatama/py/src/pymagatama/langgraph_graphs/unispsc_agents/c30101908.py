from langgraph.graph import StateGraph, END; from typing import TypedDict; class TitaniumState(TypedDict): specs: dict; valid: bool; def validate_spec(state: TitaniumState): state['valid'] = all(k in state['specs'] for k in ['grade', 'tensile']); return state; graph = StateGraph(TitaniumState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()