from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): alloy_type: str; specs: dict; valid: bool; def check_specs(state: CastingState): state['valid'] = state['specs'].get('tensile_strength', 0) > 800; return state; graph = StateGraph(CastingState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()