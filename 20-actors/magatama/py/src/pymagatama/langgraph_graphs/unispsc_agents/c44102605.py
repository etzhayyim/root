from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DictationState(TypedDict): device_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: DictationState): specs = state['device_specs']; errors = []; if 'encryption' not in specs: errors.append('Missing mandatory encryption'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(DictationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()