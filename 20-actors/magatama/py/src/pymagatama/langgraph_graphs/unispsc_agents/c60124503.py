from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SculptureState(TypedDict): items: List[str]; validated_items: List[str]; def validate_sculpture_accessory(state: SculptureState): return {'validated_items': [item for item in state['items'] if 'durable' in item]}; graph = StateGraph(SculptureState); graph.add_node('validate', validate_sculpture_accessory); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()