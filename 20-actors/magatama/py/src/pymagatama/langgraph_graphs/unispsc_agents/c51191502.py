from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity: float; compliance_level: str; validated: bool; def validate_purity(state: PharmaState): state['validated'] = state['purity'] >= 99.0; return state; def check_regulatory(state: PharmaState): state['compliance_level'] = 'APPROVED' if state['validated'] else 'REJECTED'; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulatory); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()