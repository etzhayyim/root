from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AirwayBagState(TypedDict): bag_specs: dict; inspection_results: List[str]; approved: bool; def validate_specs(state: AirwayBagState): state['inspection_results'].append('Size and material validated'); return {'approved': True if state['bag_specs'].get('durability') == 'high' else False}; workflow = StateGraph(AirwayBagState); workflow.add_node('validate', validate_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()