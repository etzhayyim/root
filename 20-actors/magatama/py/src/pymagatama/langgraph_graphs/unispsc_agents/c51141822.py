from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_name: str; permit_verified: bool; compliance_status: List[str]; def verify_permit(state: ProcureState): return {'permit_verified': True}; def validate_compliance(state: ProcureState): return {'compliance_status': ['USP-Verified', 'Safety-Data-Sheet-Attached']}; graph = StateGraph(ProcureState); graph.add_node('verify', verify_permit); graph.add_node('validate', validate_compliance); graph.add_edge('verify', 'validate'); graph.add_edge('validate', END); graph.set_entry_point('verify'); graph = graph.compile()