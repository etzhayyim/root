from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): esd_rating: float; compliance_certs: List[str]; inspection_passed: bool; def validate_esd_rating(state: ProcurementState): state['inspection_passed'] = state['esd_rating'] >= 1e6; return state; def check_certifications(state: ProcurementState): state['inspection_passed'] = 'ANSI/ESD S20.20' in state['compliance_certs']; return state; graph = StateGraph(ProcurementState); graph.add_node('validate_esd', validate_esd_rating); graph.add_node('check_certs', check_certifications); graph.set_entry_point('validate_esd'); graph.add_edge('validate_esd', 'check_certs'); graph.add_edge('check_certs', END); graph = graph.compile()