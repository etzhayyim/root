from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HardwareState(TypedDict): hardware_specs: dict; validation_results: List[str]; def validate_specs(state: HardwareState): results = []; if 'material' not in state['hardware_specs']: results.append('Missing material info'); return {'validation_results': results}; def hardware_workflow(): graph = StateGraph(HardwareState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = hardware_workflow()