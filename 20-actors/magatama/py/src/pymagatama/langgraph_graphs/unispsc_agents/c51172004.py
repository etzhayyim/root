from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DMSOState(TypedDict): chemical_specs: dict; validation_results: List[str]; graph = StateGraph(DMSOState); def validate_purity(state: DMSOState): print('Validating DMSO purity...'); return {'validation_results': ['Purity Verified']}; def check_safety(state: DMSOState): print('Running SDS Check...'); return {'validation_results': ['SDS Compliant']}; graph.add_node('validate_purity', validate_purity); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_safety'); graph.add_edge('check_safety', END); compiled_graph = graph.compile()