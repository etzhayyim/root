from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_id: str; compliance_docs: List[str]; approved: bool; def validate_medical_cert(state: ProcureState): state['approved'] = 'ISO 13485' in state['compliance_docs']; return state; builder = StateGraph(ProcureState); builder.add_node('validate', validate_medical_cert); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()