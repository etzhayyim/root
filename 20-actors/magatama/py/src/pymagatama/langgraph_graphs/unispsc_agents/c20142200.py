from langgraph.graph import StateGraph, END; from typing import TypedDict; class BearingState(TypedDict): spec: dict; validated: bool; export_control: bool; def validate_spec(state: BearingState): state['validated'] = 'load_capacity_rating' in state['spec']; return state; def check_export(state: BearingState): state['export_control'] = state['spec'].get('precision_grade', 0) > 5; return state; graph = StateGraph(BearingState); graph.add_node('validate', validate_spec); graph.add_node('export_check', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.compile()