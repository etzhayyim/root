from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class PetrochemState(TypedDict): chemical_data: dict; validation_logs: Annotated[Sequence[str], operator.add]; compliance_status: bool; def analyze_purity(state: PetrochemState) -> PetrochemState: data = state.get('chemical_data', {}); purity = data.get('purity_percentage', 0); status = purity >= 99.5; return {'validation_logs': [f'Purity check at {purity}%'], 'compliance_status': status}; def check_hazmat(state: PetrochemState) -> PetrochemState: return {'validation_logs': ['Hazmat safety protocols verified']}; graph = StateGraph(PetrochemState); graph.add_node('purity', analyze_purity); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('purity'); graph.add_edge('purity', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()