from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcessorState(TypedDict): input_data: dict; validation_results: List[str]; graph = StateGraph(ProcessorState); def check_specs(state): return {'validation_results': ['Voltage Range Checked', 'Compliance Validated']}; def run_simulation(state): return {'validation_results': state['validation_results'] + ['Thermal Simulation Passed']}; graph.add_node('spec_check', check_specs); graph.add_node('simulation', run_simulation); graph.set_entry_point('spec_check'); graph.add_edge('spec_check', 'simulation'); graph.add_edge('simulation', END); graph = graph.compile()