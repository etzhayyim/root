from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GearCouplingState(TypedDict): spec: dict; valid: bool; def validate_specs(state: GearCouplingState): state['valid'] = all(key in state['spec'] for key in ['torque', 'material']); return state; def check_compliance(state: GearCouplingState): print('Checking ISO/DIN standards...'); return state; graph = StateGraph(GearCouplingState); graph.add_node('validation', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validation', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validation'); graph = graph.compile()