from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class LivestockState(TypedDict): species: str; health_status: str; next_actions: List[str]; def validate_health(state: LivestockState): return {'health_status': 'verified' if state.get('species') else 'pending'}; def schedule_quarantine(state: LivestockState): return {'next_actions': ['start_quarantine', 'notify_vet']}; graph = StateGraph(LivestockState); graph.add_node('validate', validate_health); graph.add_node('quarantine', schedule_quarantine); graph.add_edge('validate', 'quarantine'); graph.add_edge('quarantine', END); graph.set_entry_point('validate'); graph = graph.compile()