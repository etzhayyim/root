from langgraph.graph import StateGraph, END; from typing import TypedDict; class ShelfState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ShelfState): state['valid'] = 'weight_capacity' in state['specs'] and 'material' in state['specs']; return state; graph = StateGraph(ShelfState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()