from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AircraftControlState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(AircraftControlState); def validate_spec(state): return {'validation_results': ['Compliance Verified']}; def check_export_control(state): return {'validation_results': ['Export License Required']}; graph.add_node('validate', validate_spec); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.compile()