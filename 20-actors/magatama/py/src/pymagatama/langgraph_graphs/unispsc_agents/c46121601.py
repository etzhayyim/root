from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafeArmState(TypedDict): device_specs: dict; security_clearance: bool; compliance_status: List[str]; def validate_security(state: SafeArmState): return {'compliance_status': ['Security Protocol Verified'] if state['security_clearance'] else ['Security Failure']}; def check_specs(state: SafeArmState): return {'compliance_status': state['compliance_status'] + ['Specs Verified']}; graph = StateGraph(SafeArmState); graph.add_node('validate', validate_security); graph.add_node('specs', check_specs); graph.add_edge('validate', 'specs'); graph.add_edge('specs', END); graph.set_entry_point('validate'); graph = graph.compile()