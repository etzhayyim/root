from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WatercraftState(TypedDict): specs: dict; compliance_check: bool; output: str; def validate_hull(state: WatercraftState): state['compliance_check'] = state['specs'].get('hull_rating') == 'Marine-Grade'; return state; def finalize_order(state: WatercraftState): state['output'] = 'Order approved for procurement' if state['compliance_check'] else 'Order rejected'; return state; graph = StateGraph(WatercraftState); graph.add_node('validate', validate_hull); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()