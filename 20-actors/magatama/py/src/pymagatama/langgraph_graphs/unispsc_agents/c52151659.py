from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TenderizerState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(TenderizerState); def validate_specs(state: TenderizerState): state['compliance_check'] = 'stainless' in state['specs'].get('material', '').lower(); return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()