from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CaptureDeviceState(TypedDict): device_specs: dict; compliance_check: bool; final_report: str; def validate_specs(state: CaptureDeviceState): is_compliant = 'humane_certified' in state['device_specs'] and state['device_specs']['durable'] == True; return {'compliance_check': is_compliant, 'final_report': 'Validated' if is_compliant else 'Failed'}; graph = StateGraph(CaptureDeviceState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()