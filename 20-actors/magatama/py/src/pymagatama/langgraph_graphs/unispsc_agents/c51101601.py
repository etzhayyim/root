from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class IVDState(TypedDict): reagent_data: Dict; validation_results: List[str]; def validate_reagent(state: IVDState) -> IVDState: data = state.get('reagent_data', {}); results = []; if 'expiration_date' not in data: results.append('MISSING_EXPIRY'); if data.get('temp', 25) > 8: results.append('COLD_CHAIN_VIOLATION'); return {'validation_results': results}; def compile_graph(): graph = StateGraph(IVDState); graph.add_node('validate', validate_reagent); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()