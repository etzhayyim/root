from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StretcherState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_stretcher(state: StretcherState): state['valid'] = state['specs'].get('load_capacity', 0) > 150; return {'valid': state['valid']}; def log_compliance(state: StretcherState): state['logs'].append('Compliance check complete'); return {'logs': state['logs']}; graph = StateGraph(StretcherState); graph.add_node('validate', validate_stretcher); graph.add_node('log', log_compliance); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.set_entry_point('validate'); graph = graph.compile()