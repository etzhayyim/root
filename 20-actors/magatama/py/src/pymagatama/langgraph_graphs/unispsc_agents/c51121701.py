from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): status: str; specs: dict; valid: bool; def validate_pharma(state: ProcState): state['valid'] = all(k in state['specs'] for k in ['batch_number', 'gmp_certification']); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_pharma); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()