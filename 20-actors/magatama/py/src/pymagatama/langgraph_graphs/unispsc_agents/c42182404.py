from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CalibrationState(TypedDict): device_id: str; cert_data: dict; verified: bool; def validate_cal_certs(state: CalibrationState) -> CalibrationState: state['verified'] = 'cert_id' in state['cert_data'] and state['cert_data']['standard'] == 'IEC 60645'; return state; def route_verification(state: CalibrationState) -> str: return 'pass' if state['verified'] else 'fail'; graph = StateGraph(CalibrationState); graph.add_node('validate', validate_cal_certs); graph.add_edge('validate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()