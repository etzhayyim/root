from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CopperProcurementState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: CopperProcurementState): specs = state['specs']; is_valid = all(key in specs for key in ['grade', 'diameter', 'length']); return {'valid': is_valid, 'logs': ['Validation complete'] if is_valid else ['Missing fields']}; graph = StateGraph(CopperProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()