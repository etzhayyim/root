from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): species: str; preservation_type: str; is_verified: bool; def validate_spec(state: SpecState): state['is_verified'] = bool(state.get('species') and state.get('preservation_type')); return state; def route_verification(state: SpecState): return 'valid' if state['is_verified'] else 'invalid'; graph = StateGraph(SpecState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()