from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ReactorSpecState(TypedDict): specs: dict; validation_log: List[str]; approved: bool; def validate_seismic(state: ReactorSpecState): state['validation_log'].append('Checking IEEE-344 compliance...'); state['approved'] = 'ASME-NQA-1' in state['specs'] and state['specs'].get('seismic_resilience') > 8.0; return state; graph = StateGraph(ReactorSpecState); graph.add_node('validate', validate_seismic); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()