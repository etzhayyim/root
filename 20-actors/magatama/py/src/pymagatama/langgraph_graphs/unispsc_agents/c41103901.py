from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CentrifugeState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: CentrifugeState): if state['specs'].get('max_rpm', 0) > 15000: return {'validation_report': 'High-speed certified', 'approved': True}; return {'validation_report': 'Standard speed', 'approved': True}; graph = StateGraph(CentrifugeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()