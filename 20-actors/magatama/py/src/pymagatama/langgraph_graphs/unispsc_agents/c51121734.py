from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; gmp_compliant: bool; valid: bool; def validate_quality(state: PharmState) -> PharmState: state['valid'] = state['purity'] >= 99.0 and state['gmp_compliant']; return state; builder = StateGraph(PharmState); builder.add_node('validate', validate_quality); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()