from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CAMState(TypedDict): input_files: List[str]; validation_results: dict; workflow_status: str; def validate_cad_files(state: CAMState): state['validation_results'] = {'format': 'ok', 'integrity': 'check_passed'}; state['workflow_status'] = 'validated'; return state; def generate_toolpaths(state: CAMState): state['workflow_status'] = 'toolpaths_generated'; return state; graph = StateGraph(CAMState); graph.add_node('validate', validate_cad_files); graph.add_node('process', generate_toolpaths); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()