from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PearState(TypedDict): pearl_specs: dict; verification_status: bool; def validate(state: PearState): state['verification_status'] = 'lab_report' in state['pearl_specs'] and state['pearl_specs']['quality'] == 'high'; return state; graph = StateGraph(PearState); graph.add_node('validation', validate); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()