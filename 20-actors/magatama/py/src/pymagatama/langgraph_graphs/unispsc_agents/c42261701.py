from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PathologyState(TypedDict): station_id: str; compliance_checks: List[str]; approved: bool; def validate_specs(state: PathologyState): state['compliance_checks'].append('Ventilation Verified'); return {'approved': True if 'Ventilation Verified' in state['compliance_checks'] else False}; graph = StateGraph(PathologyState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()