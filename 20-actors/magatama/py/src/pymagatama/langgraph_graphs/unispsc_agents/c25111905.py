from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BallastState(TypedDict): specs: dict; compliant: bool; workflow_steps: List[str]; def validate_specs(state: BallastState): state['compliant'] = all(k in state['specs'] for k in ['IMO_D2', 'flow_rate']); state['workflow_steps'].append('Validation'); return state; def check_export_controls(state: BallastState): state['workflow_steps'].append('Export Control Check'); return state; graph = StateGraph(BallastState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()