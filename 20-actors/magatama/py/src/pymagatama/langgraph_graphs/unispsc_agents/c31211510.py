from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PrimerState(TypedDict): chemical_composition: str; compliance_checks: List[str]; approved: bool; def validate_composition(state): return {'compliance_checks': state.get('compliance_checks', []) + ['VOC_validated']}; def check_hazmat(state): return {'approved': True if 'non-toxic' in state['chemical_composition'] else False}; graph = StateGraph(PrimerState); graph.add_node('validate_composition', validate_composition); graph.add_node('check_hazmat', check_hazmat); graph.set_entry_point('validate_composition'); graph.add_edge('validate_composition', 'check_hazmat'); graph.add_edge('check_hazmat', END); graph = graph.compile()