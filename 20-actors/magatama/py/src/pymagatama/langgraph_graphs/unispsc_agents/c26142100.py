from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class NuclearSpecState(TypedDict): spec: dict; safety_validated: bool; export_licensed: bool; def validate_safety(state: NuclearSpecState): state['safety_validated'] = state['spec'].get('rad_level') < 5.0; return state; def check_export(state: NuclearSpecState): state['export_licensed'] = 'license_id' in state['spec']; return state; graph = StateGraph(NuclearSpecState); graph.add_node('safety', validate_safety); graph.add_node('export', check_export); graph.set_entry_point('safety'); graph.add_edge('safety', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()