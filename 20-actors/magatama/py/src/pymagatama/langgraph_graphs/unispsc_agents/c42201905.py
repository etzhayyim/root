from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; compliance: bool; status: str; def validate(state: ProcState): state['compliance'] = 'shielding_cert' in state['specs']; return {'status': 'validated' if state['compliance'] else 'rejected'}; graph = StateGraph(ProcState); graph.add_node('validate', validate); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()