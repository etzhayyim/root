from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrailerState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: TrailerState): errors = []; if state['specs'].get('load_capacity_tons', 0) <= 0: errors.append('Invalid payload capacity'); return {'validation_errors': errors, 'approved': len(errors) == 0}; def finalize_procurement(state: TrailerState): return {'approved': True}; graph = StateGraph(TrailerState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()