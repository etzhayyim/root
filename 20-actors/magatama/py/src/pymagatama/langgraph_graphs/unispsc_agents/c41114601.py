from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_passed: bool; report: str; def validate_specs(state: State): state['validation_passed'] = 'Measurement Range' in state['input_data']; return state; def generate_report(state: State): state['report'] = 'Validation complete' if state['validation_passed'] else 'Invalid'; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()