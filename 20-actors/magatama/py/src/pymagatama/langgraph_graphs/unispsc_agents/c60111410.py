from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DecorProcurementState(TypedDict): items: List[str]; validation_results: List[str]; def validate_material_specs(state: DecorProcurementState): print('Validating material safety and flammability...'); return {'validation_results': ['Material check passed']}; def assign_display_category(state: DecorProcurementState): print('Categorizing by usage context...'); return {'validation_results': state['validation_results'] + ['Category assigned']}; graph = StateGraph(DecorProcurementState); graph.add_node('validate', validate_material_specs); graph.add_node('categorize', assign_display_category); graph.add_edge('validate', 'categorize'); graph.add_edge('categorize', END); graph.set_entry_point('validate'); graph = graph.compile()