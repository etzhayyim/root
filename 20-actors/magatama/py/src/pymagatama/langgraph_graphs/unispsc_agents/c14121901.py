from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): material_id: str; quality_metrics: dict; valid: bool; def validate_material(state: MaterialState): state['valid'] = state['quality_metrics'].get('purity', 0) > 0.98; return state; def report_status(state: MaterialState): print(f'Material {state['material_id']} validity: {state['valid']}'); return state; graph = StateGraph(MaterialState); graph.add_node('validate', validate_material); graph.add_node('report', report_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()