from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PacingLeadState(TypedDict): lead_details: dict; compliance_checks: List[str]; approved: bool; def validate_specs(state: PacingLeadState): state['compliance_checks'].append('ISO_13485_Verified'); return state; def check_clinical_data(state: PacingLeadState): state['approved'] = True; return state; graph = StateGraph(PacingLeadState); graph.add_node('validate', validate_specs); graph.add_node('clinical', check_clinical_data); graph.set_entry_point('validate'); graph.add_edge('validate', 'clinical'); graph.add_edge('clinical', END); graph = graph.compile()