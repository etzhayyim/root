from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): commodity_code: str; spec: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(ProcurementState); def validate_purity(state): return {'validation_results': ['Purity verified']}; def safety_clearance(state): return {'validation_results': ['Clearance granted']}; graph.add_node('purity', validate_purity); graph.add_node('safety', safety_clearance); graph.set_entry_point('purity'); graph.add_edge('purity', 'safety'); graph.add_edge('safety', END); graph = graph.compile()