from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; components: list; status: str; def validate_kit(state: KitState): components = state.get('components', []); return {'status': 'validated' if len(components) > 0 else 'incomplete'}; def assemble_workflow(state: KitState): return {'status': 'assembled'}; graph = StateGraph(KitState); graph.add_node('validate', validate_kit); graph.add_node('assemble', assemble_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); workflow = graph.compile()