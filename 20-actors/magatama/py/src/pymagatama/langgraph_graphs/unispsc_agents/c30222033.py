from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OverpassState(TypedDict): vendor_docs: List[str]; structural_spec: dict; approved: bool; def validate_engineering(state: OverpassState): state['approved'] = 'structural_integrity_certification' in state['structural_spec']; return state; def final_approval(state: OverpassState): return 'approved' if state['approved'] else 'rejected'; graph = StateGraph(OverpassState); graph.add_node('validate', validate_engineering); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()