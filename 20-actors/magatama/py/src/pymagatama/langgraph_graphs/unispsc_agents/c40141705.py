from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SpoutState(TypedDict): specs: dict; valid: bool; def validate_spout(state: SpoutState): print('Validating spout specs...'); state['valid'] = 'pressure_rating' in state['specs']; return state; graph = StateGraph(SpoutState); graph.add_node('validate', validate_spout); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()