from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BookStitchingState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; def validate_stitching_specs(state: BookStitchingState): return {'validation_results': ['Specs Verified: Speed and Gauge aligned']} if state['specs'].get('speed') > 0 else {'validation_results': ['Error: Invalid Speed']}; def compliance_check(state: BookStitchingState): return {'validation_results': ['Compliance Verified: Machinery Safety Standards Met']}; graph = StateGraph(BookStitchingState); graph.add_node('validate', validate_stitching_specs); graph.add_node('compliance', compliance_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()