from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): metal_type: str; purity: float; specs: dict; approved: bool; def validate_purity(state: CastingState): state['approved'] = state['purity'] >= 0.99; return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()