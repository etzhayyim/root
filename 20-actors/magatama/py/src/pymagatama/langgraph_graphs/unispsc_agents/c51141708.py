from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcureState(TypedDict): chemical_name: str; purity: float; compliance_doc: List[str]; approved: bool; def validate_purity(state): return {'approved': state['purity'] >= 99.0}; def check_regulations(state): return {'compliance_doc': ['GMP_CERT', 'LICENSE_CHECK']}; graph = StateGraph(DrugProcureState); graph.add_node('validate', validate_purity); graph.add_node('regulate', check_regulations); graph.add_edge('validate', 'regulate'); graph.add_edge('regulate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()