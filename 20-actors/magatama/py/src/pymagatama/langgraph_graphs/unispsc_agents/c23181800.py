from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class MachineMaintenanceState(TypedDict): status: str; diagnostics: dict; service_log: list; def perform_diagnostics(state: MachineMaintenanceState): return { 'diagnostics': {'code': 'M-001', 'status': 'inspected'}}; def schedule_repair(state: MachineMaintenanceState): return { 'status': 'scheduled', 'service_log': ['Diagnostic complete, pending parts']}; graph = StateGraph(MachineMaintenanceState); graph.add_node('diagnostics', perform_diagnostics); graph.add_node('repair', schedule_repair); graph.set_entry_point('diagnostics'); graph.add_edge('diagnostics', 'repair'); graph.add_edge('repair', END); graph = graph.compile()