from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GeoState(TypedDict): data: dict; validation_errors: List[str]; approved: bool; def validate_sensors(state: GeoState): state["validation_errors"] = [] if state["data"].get("calibration_date") else ["Missing calibration"]; return {"validation_errors": state["validation_errors"]}; def route_approval(state: GeoState): return "approved" if not state["validation_errors"] else END; graph = StateGraph(GeoState); graph.add_node("sensor_check", validate_sensors); graph.add_edge("sensor_check", END); graph.set_entry_point("sensor_check"); graph.compile()