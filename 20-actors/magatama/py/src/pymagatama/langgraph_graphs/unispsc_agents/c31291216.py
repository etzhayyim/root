from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): component_data: dict; quality_checks: List[str]; approved: bool; def validate_titanium_spec(state: ProcurementState): state['quality_checks'].append('Dimensions verified'); state['quality_checks'].append('NDT status confirmed'); return {'approved': True}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_titanium_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()