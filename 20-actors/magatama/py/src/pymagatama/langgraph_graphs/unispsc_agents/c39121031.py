from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PowerStripState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: PowerStripState): errors = []; if state['specs'].get('rating', 0) < 10: errors.append('Insufficient amperage rating'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(PowerStripState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()