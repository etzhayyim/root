from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class OximeterState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: OximeterState): errors = []; if 'iso_cert' not in state['specs']: errors.append('Missing ISO certification'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(OximeterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()