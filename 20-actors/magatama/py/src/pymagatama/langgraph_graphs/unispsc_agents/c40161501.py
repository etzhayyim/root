from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class VacuumFilterState(TypedDict): filter_specs: dict; validation_results: list; status: str; def validate_specs(state: VacuumFilterState): specs = state.get('filter_specs', {}); errors = [f'Missing {k}' for k in ['micron_rating', 'material'] if k not in specs]; return {'validation_results': errors, 'status': 'PASSED' if not errors else 'FAILED'}; graph = StateGraph(VacuumFilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()