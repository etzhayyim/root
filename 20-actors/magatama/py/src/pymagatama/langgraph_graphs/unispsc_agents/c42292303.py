from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalToolState(TypedDict): tool_id: str; quality_metrics: dict; validated: bool; def validate_specs(state: SurgicalToolState): state['validated'] = state['quality_metrics'].get('iso_cert') and state['quality_metrics'].get('material') == 'surgical_steel'; return state; def report_status(state: SurgicalToolState): print(f'Processing surgical pliers {state['tool_id']}: Validated={state['validated']}'); return state; graph = StateGraph(SurgicalToolState); graph.add_node('validate', validate_specs); graph.add_node('report', report_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()