from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WasteContainerState(TypedDict): specs: dict; validation_results: List[str]; def validate_capacity(state: WasteContainerState): capacity = state['specs'].get('load_capacity_kg', 0); state['validation_results'].append('Capacity OK' if capacity > 0 else 'Capacity Error'); return state; def check_compliance(state: WasteContainerState): state['validation_results'].append('Standard Compliant'); return state; graph = StateGraph(WasteContainerState); graph.add_node('validate', validate_capacity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()