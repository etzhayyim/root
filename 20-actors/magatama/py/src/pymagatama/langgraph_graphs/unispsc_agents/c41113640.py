from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class WattmeterState(TypedDict): data: dict; status: str; def validate_specs(state: WattmeterState): return {'status': 'validated'} if 'calibration_certificate' in state['data'] else {'status': 'incomplete'}; graph = StateGraph(WattmeterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()