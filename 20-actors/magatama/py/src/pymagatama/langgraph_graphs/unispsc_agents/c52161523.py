from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadioState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: RadioState): state['validation_errors'] = []; if state['specs'].get('power') > 30: state['validation_errors'].append('Power limit exceeded'); state['approved'] = len(state['validation_errors']) == 0; return state; graph = StateGraph(RadioState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()