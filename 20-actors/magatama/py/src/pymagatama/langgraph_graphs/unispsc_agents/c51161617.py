from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; gmp_compliant: bool; validation_passed: bool; def validate_purity(state): return { 'validation_passed': state['purity'] >= 99.0 }; def check_gmp(state): return { 'validation_passed': state['gmp_compliant'] and state['validation_passed'] }; graph = StateGraph(PharmState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_gmp', check_gmp); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_gmp'); graph.add_edge('check_gmp', END); graph = graph.compile()