from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ArtSupplyState(TypedDict): items: List[str]; status: str; def validate(state: ArtSupplyState): return {'status': 'validated' if state['items'] else 'error'}; graph = StateGraph(ArtSupplyState); graph.add_node('validate', validate); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()