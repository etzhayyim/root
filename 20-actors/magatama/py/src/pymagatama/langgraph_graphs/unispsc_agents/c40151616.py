from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CompressorSpecState(TypedDict): model: str; specs: dict; valid: bool; def validate_specs(state: CompressorSpecState): state['valid'] = all(k in state['specs'] for k in ['pressure', 'material']); return state; graph = StateGraph(CompressorSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()