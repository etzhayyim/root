from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChainSpec(TypedDict): material: str; swl: float; valid: bool; def validate_spec(state: ChainSpec): state['valid'] = state['swl'] > 0 and len(state['material']) > 0; return state; graph = StateGraph(ChainSpec); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()