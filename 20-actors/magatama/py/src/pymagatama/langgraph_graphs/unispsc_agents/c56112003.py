from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StorageState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_rack_spec(state: StorageState): for item in state['items']: if item.get('load_capacity') < 10: state['errors'].append('Load capacity below safe threshold'); return state; def check_compatibility(state: StorageState): return state; graph = StateGraph(StorageState); graph.add_node('validate', validate_rack_spec); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()