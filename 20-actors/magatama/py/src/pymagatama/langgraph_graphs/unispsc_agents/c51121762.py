from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RauwolfiaState(TypedDict): material_name: str; analysis_results: dict; compliance_checked: bool; def validate_quality(state: RauwolfiaState): state['compliance_checked'] = True; return state; graph = StateGraph(RauwolfiaState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()