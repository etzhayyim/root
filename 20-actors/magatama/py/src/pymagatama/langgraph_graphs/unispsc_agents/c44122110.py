from langgraph.graph import StateGraph, END; from typing import TypedDict; class MountState(TypedDict): mount_type: str; load_capacity: float; status: str; def validate_spec(state: MountState): return {'status': 'validated' if state['load_capacity'] > 0 else 'invalid'}; graph = StateGraph(MountState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()