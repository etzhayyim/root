from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HydraulicState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(HydraulicState); def validate_specs(state: HydraulicState): return {'validation_results': ['Pressure test passed', 'Material check passed'] if state['specs'].get('pressure') > 0 else ['Failed']}; def check_safety(state: HydraulicState): return {'validation_results': state['validation_results'] + ['Safety audit verified']}; graph.add_node('validation', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validation'); graph.add_edge('validation', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()