from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TesterState(TypedDict): data: dict; logs: Annotated[list, operator.add]; graph = StateGraph(TesterState); def validate_specs(state): return {'logs': ['Validating measurement accuracy parameters']}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()