from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LabelState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(LabelState); def validate_specs(state: LabelState): state['compliance_check'] = 'material' in state['specs'] and 'durability' in state['specs']; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()