from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_magnesium_specs(state: CastingState): state['valid'] = 'tensile_strength_certificate' in state['specs'] and state['specs']['material_grade'] != ''; state['logs'].append('Casting specs validated'); return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_magnesium_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()