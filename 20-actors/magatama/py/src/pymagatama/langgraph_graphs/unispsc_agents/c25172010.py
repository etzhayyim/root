from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SwayBarState(TypedDict): bar_specs: dict; status: str; def validate_specs(state: SwayBarState): print('Validating material and dimensional specs...'); return {'status': 'validated'} if state.get('bar_specs') else {'status': 'invalid'}; def finalize_procurement(state: SwayBarState): print('Processing order for sway bars...'); return {'status': 'ordered'}; graph = StateGraph(SwayBarState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()