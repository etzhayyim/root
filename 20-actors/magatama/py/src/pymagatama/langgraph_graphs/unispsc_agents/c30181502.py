from langgraph.graph import StateGraph, END; from typing import TypedDict; class BidetState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: BidetState): state['compliant'] = 'voltage' in state['specs'] and 'water_connection' in state['specs']; return state; builder = StateGraph(BidetState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()