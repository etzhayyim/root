from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WorkshopState(TypedDict): workspace_id: str; specs: dict; compliant: bool; def validate_specs(state: WorkshopState): state['compliant'] = all(k in state['specs'] for k in ['safety', 'power']); return state; graph = StateGraph(WorkshopState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()