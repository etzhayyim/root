from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DeviceState(TypedDict): device_id: str; sterility_verified: bool; compliance_docs: List[str]; graph = StateGraph(DeviceState); def validate_sterility(state: DeviceState): return {'sterility_verified': True}; def check_compliance(state: DeviceState): return {'compliance_docs': ['ISO13485', 'FDA_510k']}; graph.add_node('validate', validate_sterility); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()