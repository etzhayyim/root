from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input: str; geometry_valid: bool; material_certified: bool; graph = StateGraph(State); def validate_geometry(state: State): return {'geometry_valid': True}; def check_certification(state: State): return {'material_certified': True}; graph.add_node('validate', validate_geometry); graph.add_node('certify', check_certification); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()