from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; purity: float; compliance_check: bool; graph = StateGraph(PharmaState); def validate_purity(state: PharmaState): return {'compliance_check': state['purity'] >= 0.99}; def audit_regulations(state: PharmaState): return {'compliance_check': True}; graph.add_node('validate', validate_purity); graph.add_node('audit', audit_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); compiled_graph = graph.compile()