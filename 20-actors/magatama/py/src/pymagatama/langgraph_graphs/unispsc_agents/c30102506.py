from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AluminumProcurementState(TypedDict): alloy_grade: str; thickness: float; status: str; graph = StateGraph(AluminumProcurementState); def validate_spec(state: AluminumProcurementState): return {'status': 'validated' if state['thickness'] > 0 else 'rejected'}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()