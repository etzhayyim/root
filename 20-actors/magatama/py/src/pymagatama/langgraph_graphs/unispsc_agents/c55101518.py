from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DrawingState(TypedDict): drawings: List[str]; validation_results: dict; class TechnicalDrawingGraph: def __init__(self): self.graph = StateGraph(DrawingState); self.graph.add_node('format_check', self.validate_format); self.graph.add_node('standard_compliance', self.verify_standards); self.graph.set_entry_point('format_check'); self.graph.add_edge('format_check', 'standard_compliance'); self.graph.add_edge('standard_compliance', END); def validate_format(self, state: DrawingState): return {'validation_results': {'format': 'PASSED'}}; def verify_standards(self, state: DrawingState): return {'validation_results': {'iso_check': 'COMPLIANT'}}; graph = TechnicalDrawingGraph().graph.compile()