from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeSpecState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(TubeSpecState); def validate_welds(state: TubeSpecState): return {'validation_report': ['Welding standard verified against ASME B31.3']}; def check_material(state: TubeSpecState): return {'validation_report': ['Material chemical composition meets ASTM A269']}; graph.add_node('weld_check', validate_welds); graph.add_node('material_check', check_material); graph.set_entry_point('weld_check'); graph.add_edge('weld_check', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()