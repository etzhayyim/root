from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_cbn_specs(state: SpecState): specs = state['specs']; errors = []; if 'grit_size' not in specs: errors.append('Missing mandatory grit size'); if specs.get('max_rpm', 0) <= 0: errors.append('Invalid RPM configuration'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(SpecState); graph.add_node('validate', validate_cbn_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()