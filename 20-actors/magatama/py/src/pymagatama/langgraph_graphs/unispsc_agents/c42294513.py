from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VitrectomyState(TypedDict): kit_id: str; quality_checks: List[str]; approved: bool; def validate_quality(state: VitrectomyState): print('Validating sterility and certification...'); state['quality_checks'].append('ISO13485_Verified'); return state; def check_compliance(state: VitrectomyState): state['approved'] = 'ISO13485_Verified' in state['quality_checks']; return state; graph = StateGraph(VitrectomyState); graph.add_node('validate', validate_quality); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()