from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PurityState(TypedDict): chemical_name: str; purity: float; is_valid: bool; def validate_purity(state: PurityState): state['is_valid'] = state['purity'] >= 99.0; return state; graph = StateGraph(PurityState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()