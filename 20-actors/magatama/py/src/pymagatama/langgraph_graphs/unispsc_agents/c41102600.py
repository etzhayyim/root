from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LabEquipmentState(TypedDict): equipment_specs: dict; validation_logs: Annotated[list, operator.add]; status: str; def validate_equipment(state: LabEquipmentState): logs = []; status = 'PENDING'; if 'bio_containment' not in state.get('equipment_specs', {}): logs.append('Missing bio-containment spec'); status = 'FAILED'; else: logs.append('Validation Passed'); status = 'APPROVED'; return {'validation_logs': logs, 'status': status}; graph = StateGraph(LabEquipmentState); graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()