from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExpertSystemState(TypedDict): input_data: dict; validation_report: list; status: str; def validate_knowledge_base(state: ExpertSystemState): state['validation_report'].append('KB integrity checked'); return {'status': 'validated'}; def compile_logic(state: ExpertSystemState): return {'status': 'compiled'}; graph = StateGraph(ExpertSystemState); graph.add_node('validate', validate_knowledge_base); graph.add_node('compile', compile_logic); graph.set_entry_point('validate'); graph.add_edge('validate', 'compile'); graph.add_edge('compile', END); graph = graph.compile()