from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MineralState(TypedDict): chemical_composition: dict; purity: float; compliance_verified: bool; logs: Annotated[Sequence[str], operator.add]; def validate_purity(state: MineralState): return {purity: state['chemical_composition'].get('purity', 0.0), compliance_verified: state['chemical_composition'].get('purity', 0.0) > 99.9}; def check_safety(state: MineralState): return {logs: [f'Purity check at {state[purity]}%'], compliance_verified: state[compliance_verified] and True}; graph = StateGraph(MineralState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); app = graph.compile()