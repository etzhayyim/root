from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FlowCompState(TypedDict): data: dict; validation_errors: Annotated[list, operator.add]; def validate_flow_specs(state: FlowCompState): errors = []; if 'accuracy' not in state['data']: errors.append('Missing accuracy spec'); return {'validation_errors': errors}; def compliance_check(state: FlowCompState): return {'validation_errors': ['Export control check required']}; graph = StateGraph(FlowCompState); graph.add_node('validate', validate_flow_specs); graph.add_node('compliance', compliance_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()