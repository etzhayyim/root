from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ConservatoryState(TypedDict): specs: dict; compliance_checked: bool; installation_ready: bool; def validate_specs(state: ConservatoryState): print('Validating architectural structural specs...'); return {'compliance_checked': True}; def confirm_readiness(state: ConservatoryState): print('Confirming installation site safety...'); return {'installation_ready': True}; graph = StateGraph(ConservatoryState); graph.add_node('validate', validate_specs); graph.add_node('confirm', confirm_readiness); graph.set_entry_point('validate'); graph.add_edge('validate', 'confirm'); graph.add_edge('confirm', END); graph = graph.compile()