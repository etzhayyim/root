from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ExerciseState(TypedDict): equipment_specs: dict; validation_report: list[str]; graph = StateGraph(ExerciseState); def validate_specs(state: ExerciseState): specs = state.get('equipment_specs', {}); report = []; if specs.get('max_weight', 0) < 50: report.append('Low load capacity'); return {'validation_report': report}; def finalize_process(state: ExerciseState): return {'validation_report': state['validation_report'] + ['Finalized']}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_process); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()