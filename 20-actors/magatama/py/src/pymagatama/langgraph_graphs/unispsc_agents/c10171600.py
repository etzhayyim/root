from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class DrillingState(TypedDict): depth: float; safety_passed: bool; logs: Annotated[list[str], operator.add]; def validate_safety(state: DrillingState): return {'safety_passed': state['depth'] < 5000}; def check_geology(state: DrillingState): return {'logs': ['Geology check passed']}; graph = StateGraph(DrillingState); graph.add_node('safety', validate_safety); graph.add_node('geology', check_geology); graph.set_entry_point('safety'); graph.add_edge('safety', 'geology'); graph.add_edge('geology', END); graph = graph.compile()