from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InstrumentState(TypedDict): instrument_type: str; specs: dict; approved: bool; def validate_instrument(state: InstrumentState): state['approved'] = state['specs'].get('material') == 'Brass'; return state; def finalize_procurement(state: InstrumentState): return {'approved': True}; graph = StateGraph(InstrumentState); graph.add_node('validate', validate_instrument); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()