from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): items: list; status: str; def validate_compliance(state: State): return {'status': 'CERTIFIED' if 'certificate' in state['items'] else 'PENDING'}; def schedule_maintenance(state: State): return {'status': 'MAINTENANCE_SCHEDULED'}; graph = StateGraph(State); graph.add_node('validate', validate_compliance); graph.add_node('maintenance', schedule_maintenance); graph.set_entry_point('validate'); graph.add_edge('validate', 'maintenance'); graph.add_edge('maintenance', END); graph = graph.compile()