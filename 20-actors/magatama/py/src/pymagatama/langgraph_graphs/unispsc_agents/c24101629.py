from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): supplier_data: dict; compliance_result: bool; payload: dict; def validate_specs(state: ProcState): return {'compliance_result': state['payload'].get('load_rating', 0) > 0}; def approve_procurement(state: ProcState): return {'compliance_result': True}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compile_graph = graph.compile()