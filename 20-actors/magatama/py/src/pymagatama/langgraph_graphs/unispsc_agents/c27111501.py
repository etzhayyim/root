from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BladeState(TypedDict): blade_specs: dict; validation_results: List[str]; def validate_blade_quality(state: BladeState): return {'validation_results': ['Quality check passed']}; def perform_safety_risk_assessment(state: BladeState): return {'validation_results': state['validation_results'] + ['Safety assessment complete']}; graph = StateGraph(BladeState); graph.add_node('validate', validate_blade_quality); graph.add_node('safety_check', perform_safety_risk_assessment); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); compiled_graph = graph.compile()