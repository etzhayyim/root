from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WorkflowState(TypedDict): content_items: List[str]; approved: bool; def validate_metadata(state: WorkflowState): return {'approved': len(state['content_items']) > 0}; def route_processing(state: WorkflowState): return 'complete' if state['approved'] else 'error'; graph = StateGraph(WorkflowState); graph.add_node('validate', validate_metadata); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_processing, {'complete': END, 'error': END}); graph = graph.compile()