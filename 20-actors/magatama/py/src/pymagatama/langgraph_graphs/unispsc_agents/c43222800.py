from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TelephonyState(TypedDict): device_specs: dict; compliance_check: bool; graph = StateGraph(TelephonyState); def validate_hw(state: TelephonyState): return {'compliance_check': True} if 'protocol' in state['device_specs'] else {'compliance_check': False}; graph.add_node('validation', validate_hw); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()