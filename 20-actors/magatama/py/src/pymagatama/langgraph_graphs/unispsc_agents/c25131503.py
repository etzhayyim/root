from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class SeaplaneState(TypedDict): aircraft_id: str; structural_check: bool; airworthiness_verified: bool; def validate_airworthiness(state: SeaplaneState): return {'airworthiness_verified': True}; def check_structure(state: SeaplaneState): return {'structural_check': True}; graph = StateGraph(SeaplaneState); graph.add_node('verify', validate_airworthiness); graph.add_node('structure', check_structure); graph.set_entry_point('verify'); graph.add_edge('verify', 'structure'); graph.add_edge('structure', END); graph = graph.compile()