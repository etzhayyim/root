from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class EEGState(TypedDict): paper_spec: dict; validation_results: list; status: str; def validate_spec(state: EEGState): results = []; if 'thermal_sensitivity' not in state['paper_spec']: results.append('Missing sensitivity'); return {'validation_results': results, 'status': 'validated' if not results else 'error'}; graph = StateGraph(EEGState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()