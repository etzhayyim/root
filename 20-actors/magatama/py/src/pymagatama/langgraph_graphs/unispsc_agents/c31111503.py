from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_dimensions(state: SpecState): results = state.get('validation_results', []) + ['Dimensions verified']; return {'validation_results': results}; def check_alloy(state: SpecState): results = state.get('validation_results', []) + ['Alloy composition checked']; return {'validation_results': results}; graph = StateGraph(SpecState); graph.add_node('validate_dim', validate_dimensions); graph.add_node('check_alloy', check_alloy); graph.set_entry_point('validate_dim'); graph.add_edge('validate_dim', 'check_alloy'); graph.add_edge('check_alloy', END); graph = graph.compile()