from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; hazardous: bool; verified: bool; def check_hazmat(state: ProcState): state['hazardous'] = True; return state; def validate_spec(state: ProcState): state['verified'] = True; return state; graph = StateGraph(ProcState); graph.add_node('check_hazmat', check_hazmat); graph.add_node('validate_spec', validate_spec); graph.add_edge('check_hazmat', 'validate_spec'); graph.add_edge('validate_spec', END); graph.set_entry_point('check_hazmat'); graph = graph.compile()