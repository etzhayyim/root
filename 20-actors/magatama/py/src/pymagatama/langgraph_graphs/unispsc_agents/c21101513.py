from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IrrigationState(TypedDict): sprayer_specs: dict; validation_results: List[str]; def validate_spray_head(state: IrrigationState): results = []; if state['sprayer_specs'].get('pressure_rating', 0) < 1.0: results.append('Pressure rating below agricultural threshold'); return {'validation_results': results}; graph = StateGraph(IrrigationState); graph.add_node('validate', validate_spray_head); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()