from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): input_data: dict; validation_results: List[str]; approved: bool; def validate_purity(state: PharmState): purity = state['input_data'].get('purity_percentage', 0); state['validation_results'].append('Purity check complete') if purity >= 99.0 else state['validation_results'].append('Purity below standard'); return state; def check_regulatory(state: PharmState): state['approved'] = True; return state; graph = StateGraph(PharmState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_regulatory', check_regulatory); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_regulatory'); graph.add_edge('check_regulatory', END); graph = graph.compile()