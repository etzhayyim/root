from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): cold_chain_verified: bool; quality_checks: List[str]; def check_temperature(state: AgentState): state['cold_chain_verified'] = True; return state; def validate_lot(state: AgentState): state['quality_checks'].append('Lot certificate validated'); return state; graph = StateGraph(AgentState); graph.add_node('verify_temp', check_temperature); graph.add_node('cert_check', validate_lot); graph.set_entry_point('verify_temp'); graph.add_edge('verify_temp', 'cert_check'); graph.add_edge('cert_check', END); graph = graph.compile()