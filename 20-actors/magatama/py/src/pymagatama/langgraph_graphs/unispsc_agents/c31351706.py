from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeAssemblyState(TypedDict): tube_specs: dict; weld_validation: bool; compliance_status: List[str]; def validate_weld(state: TubeAssemblyState): state['weld_validation'] = state['tube_specs'].get('weld_strength', 0) > 50; return state; def check_compliance(state: TubeAssemblyState): state['compliance_status'] = ['ISO13485'] if state['tube_specs'].get('is_medical') else ['StandardIndustrial']; return state; graph = StateGraph(TubeAssemblyState); graph.add_node('validate', validate_weld); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()