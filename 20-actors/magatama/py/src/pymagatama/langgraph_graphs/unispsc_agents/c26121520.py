from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class WireState(TypedDict): specs: dict; validation_report: list; valid: bool; def validate_specs(state: WireState): state['validation_report'] = ['Conductivity check', 'Diameter verification']; state['valid'] = True; return state; graph = StateGraph(WireState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()