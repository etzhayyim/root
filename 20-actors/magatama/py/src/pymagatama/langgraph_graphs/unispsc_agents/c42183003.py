from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ExophthalmometerState(TypedDict): input: str; validation_result: bool; compliance_status: bool; def validate_spec(state: ExophthalmometerState): state['validation_result'] = True; return state; def check_compliance(state: ExophthalmometerState): state['compliance_status'] = True; return state; graph = StateGraph(ExophthalmometerState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()