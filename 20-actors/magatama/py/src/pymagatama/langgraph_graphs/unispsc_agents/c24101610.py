from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): manipulator_spec: dict; validation_results: List[str]; graph = StateGraph(GraphState); def validate_specs(state: GraphState): state['validation_results'].append('Checking payload and reach limits'); return state; def check_compliance(state: GraphState): state['validation_results'].append('Verifying dual-use export controls'); return state; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()