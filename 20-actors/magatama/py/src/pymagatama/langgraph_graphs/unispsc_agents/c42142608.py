from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SyringeState(TypedDict): vendor_certs: List[str]; qc_passed: bool; specs: dict; def validate_cert(state: SyringeState): state['qc_passed'] = 'ISO13485' in state['vendor_certs']; return state; def check_specs(state: SyringeState): if state['qc_passed']: state['specs']['status'] = 'COMPLIANT'; return state; graph = StateGraph(SyringeState); graph.add_node('validate', validate_cert); graph.add_node('check', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); compiled_graph = graph.compile()