from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WritingAidState(TypedDict): aid_type: str; specs: dict; approved: bool; def validate_specs(state: WritingAidState): state['approved'] = 'ergonomic' in state['specs'] and state['specs']['durability'] > 0; return state; graph = StateGraph(WritingAidState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()