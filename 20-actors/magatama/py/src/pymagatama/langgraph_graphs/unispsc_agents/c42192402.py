from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CartState(TypedDict): input_spec: dict; validation_results: list; status: str; def validate_compliance(state: CartState): state['validation_results'].append('Checking IEC 60601-1 compliance'); return {'status': 'validated'}; builder = StateGraph(CartState); builder.add_node('validate', validate_compliance); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()