from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TitaniumState(TypedDict): chemical_data: dict; compliance_status: bool; workflow_log: List[str]; def analyze_purity(state: TitaniumState): state['chemical_data']['grade'] = 'Grade 5'; return {'compliance_status': True}; def validate_specs(state: TitaniumState): print('Validating ASTM compliance...'); return {'workflow_log': ['ASTM match confirmed']}; graph = StateGraph(TitaniumState); graph.add_node('analysis', analyze_purity); graph.add_node('validation', validate_specs); graph.set_entry_point('analysis'); graph.add_edge('analysis', 'validation'); graph.add_edge('validation', END); graph = graph.compile()