from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ThermocoupleState(TypedDict): specs: dict; validation_report: str; graph = StateGraph(ThermocoupleState); def validate_calibration(state: ThermocoupleState): return {'validation_report': 'Calibration verified' if 'cert' in state['specs'] else 'Missing cert'}; def check_material(state: ThermocoupleState): return {'validation_report': 'Material compliant'}; graph.add_node('calibrate', validate_calibration); graph.add_node('material', check_material); graph.add_edge('calibrate', 'material'); graph.add_edge('material', END); graph.set_entry_point('calibrate'); compiled_graph = graph.compile()