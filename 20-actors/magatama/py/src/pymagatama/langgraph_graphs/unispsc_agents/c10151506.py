from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ZirconiumState(TypedDict): input_data: dict; quality_report: dict; processing_steps: Annotated[Sequence[str], operator.add]; graph = StateGraph(ZirconiumState); def validate_purity(state: ZirconiumState): return {'quality_report': {'status': 'validated', 'purity_check': True}}; def run_refining(state: ZirconiumState): return {'processing_steps': ['crushing', 'leaching', 'purification']}; graph.add_node('validate', validate_purity); graph.add_node('refine', run_refining); graph.set_entry_point('validate'); graph.add_edge('validate', 'refine'); graph.add_edge('refine', END); graph.compile()