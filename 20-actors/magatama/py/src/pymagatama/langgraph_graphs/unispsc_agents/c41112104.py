from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TransducerState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: TransducerState): specs = state['specs']; required = ['freq', 'impedance', 'sensitivity']; is_valid = all(k in specs for k in required); return {'valid': is_valid, 'error': '' if is_valid else 'Missing specs'}; def final_check(state: TransducerState): return {'valid': state['valid']}; graph = StateGraph(TransducerState); graph.add_node('validate', validate_specs); graph.add_node('finalize', final_check); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()