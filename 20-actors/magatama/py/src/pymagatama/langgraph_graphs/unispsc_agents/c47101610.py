from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WaterTreatmentState(TypedDict): chemical_spec: str; safety_checks: List[str]; approved: bool; def validate_composition(state: WaterTreatmentState): return {'approved': 'calcium' in state['chemical_spec'].lower()}; def check_safety(state: WaterTreatmentState): return {'safety_checks': ['SDS_VERIFIED', 'TOXICITY_TESTED']}; graph = StateGraph(WaterTreatmentState); graph.add_node('validate', validate_composition); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()