from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AirCircState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: AirCircState): errors = []; if state['specs'].get('power_consumption', 0) > 2000: errors.append('High power consumption exceeds industrial limit'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(AirCircState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()