from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_health_product(state: ProcureState): state['validated'] = all(c in state['certs'] for c in ['FDA', 'ISO13485']); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_health_product); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()