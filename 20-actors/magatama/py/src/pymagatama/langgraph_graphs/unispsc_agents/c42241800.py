from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OrthoState(TypedDict): device_type: str; specs: dict; compliant: bool; def validate_device(state: OrthoState): return {'compliant': state['specs'].get('regulatory_compliance_certification') is not None}; def route_processing(state: OrthoState): return 'validate' if state['device_type'] == 'softgood' else END; graph = StateGraph(OrthoState); graph.add_node('validate', validate_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()