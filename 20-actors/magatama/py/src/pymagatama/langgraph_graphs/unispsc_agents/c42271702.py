from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class O2State(TypedDict): input_specs: dict; validation_report: list; is_approved: bool; def validate_purity(state: O2State): val = state['input_specs'].get('purity', 0); return {'validation_report': ['Purity check passed'] if val >= 90 else ['Purity check failed']}; def finalize(state: O2State): return {'is_approved': 'failed' not in str(state['validation_report'])}; graph = StateGraph(O2State); graph.add_node('validate_purity', validate_purity); graph.add_node('finalize', finalize); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()