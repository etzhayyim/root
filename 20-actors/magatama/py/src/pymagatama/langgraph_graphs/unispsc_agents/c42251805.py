from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RehabTableState(TypedDict): specs: dict; compliance_checked: bool; status: str; def validate_specs(state: RehabTableState): state['compliance_checked'] = state['specs'].get('load_capacity', 0) > 100; return state; def finalize_order(state: RehabTableState): state['status'] = 'READY' if state['compliance_checked'] else 'REJECTED'; return state; graph = StateGraph(RehabTableState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()