from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlasticState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_plastic(state: PlasticState): state['valid'] = 'tolerance' in state['specs'] and 'material' in state['specs']; state['log'].append('Validation completed'); return state; graph = StateGraph(PlasticState); graph.add_node('validate', validate_plastic); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()