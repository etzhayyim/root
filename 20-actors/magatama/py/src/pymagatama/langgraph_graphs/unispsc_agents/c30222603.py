from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TrackState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: TrackState): state['validation_report'] = 'Compliance checking passed' if 'Shock Absorption Rating' in state['specs'] else 'Missing criteria'; state['approved'] = 'Shock Absorption Rating' in state['specs']; return state; def finalize_order(state: TrackState): return {'validation_report': 'Order ready for procurement'}; graph = StateGraph(TrackState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()