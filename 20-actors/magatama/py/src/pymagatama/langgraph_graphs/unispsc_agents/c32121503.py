from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CircuitState(TypedDict): component_specs: dict; validation_results: list; status: str; def validate_capacitor(state: CircuitState): specs = state['component_specs']; results = [f'Checked tolerance: {specs.get("tolerance")}%']; return {'validation_results': results, 'status': 'validated'}; graph = StateGraph(CircuitState); graph.add_node('validate', validate_capacitor); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()