from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class CommodityState(TypedDict): messages: Annotated[Sequence[str], add_messages]; spec_validated: bool; def validate_spec(state: CommodityState) -> CommodityState: return {'spec_validated': True}; def process_commodity(state: CommodityState) -> CommodityState: return {'messages': ['Commodity 10151801 processing logic complete.']}; graph = StateGraph(CommodityState); graph.add_node('validate', validate_spec); graph.add_node('process', process_commodity); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()