from langgraph.graph import StateGraph, END; from typing import TypedDict; class DrugState(TypedDict): batch_id: str; compliance: bool; temp_logs: list; graph = StateGraph(DrugState); def validate_compliance(state: DrugState): return {'compliance': True}; def check_storage(state: DrugState): return {'temp_logs': ['2-8C']}; graph.add_node('validate', validate_compliance); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); compiled_graph = graph.compile()