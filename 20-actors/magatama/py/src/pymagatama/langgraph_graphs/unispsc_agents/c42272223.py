from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_report: dict; status: str; def validate_respirator_part(state: State): status = 'approved' if state['input_data'].get('niosh_certified') else 'rejected'; return {'validation_report': {'passed': status == 'approved'}, 'status': status}; graph = StateGraph(State); graph.add_node('validation', validate_respirator_part); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()