from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplierState(TypedDict): vendor_id: str; quality_score: float; parts: List[str]; def validate_torque_req(state: SupplierState): return {'quality_score': state['quality_score'] + 0.1 if state['quality_score'] < 0.9 else 1.0}; graph = StateGraph(SupplierState); graph.add_node('validate', validate_torque_req); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()