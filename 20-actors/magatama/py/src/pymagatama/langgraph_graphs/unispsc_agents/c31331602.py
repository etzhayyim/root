from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; def validate_welds(state: AssemblyState): cert = state['specs'].get('welding_cert'); return {'validation_results': ['Weld integrity verified'] if cert else ['Missing cert']}; def check_dimensions(state: AssemblyState): return {'validation_results': ['Dimensions compliant']}; graph = StateGraph(AssemblyState); graph.add_node('weld_check', validate_welds); graph.add_node('dim_check', check_dimensions); graph.set_entry_point('weld_check'); graph.add_edge('weld_check', 'dim_check'); graph.add_edge('dim_check', END); graph = graph.compile()