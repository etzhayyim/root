from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SealingToolState(TypedDict): tool_specs: dict; validation_results: List[str]; graph = StateGraph(SealingToolState); def validate_specs(state: SealingToolState): result = 'Valid' if state['tool_specs'].get('pressure_rating') else 'Invalid'; return {'validation_results': [f'Spec check: {result}']}; graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()