from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): device_type: str; certs_valid: bool; specs: dict; graph = StateGraph(ProcState); def validate_medical_certs(state: ProcState): state['certs_valid'] = 'ISO13485' in state['specs'].get('certs', []); return state; def check_audio_specs(state: ProcState): return {'specs': {**state['specs'], 'validated': True}}; graph.add_node('validate', validate_medical_certs); graph.add_node('audio_check', check_audio_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'audio_check'); graph.add_edge('audio_check', END); compiled_graph = graph.compile()