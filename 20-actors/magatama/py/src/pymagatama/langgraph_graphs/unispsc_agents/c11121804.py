from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class NaturalGasolineState(TypedDict): input_data: dict; quality_report: dict; status: str; safety_checks: Annotated[list, operator.add]; def validate_purity(state: NaturalGasolineState) -> NaturalGasolineState: state['safety_checks'].append('purity_verified'); return state; def check_hazmat(state: NaturalGasolineState) -> NaturalGasolineState: state['safety_checks'].append('hazmat_compliant'); return state; graph = StateGraph(NaturalGasolineState); graph.add_node('purity', validate_purity); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('purity'); graph.add_edge('purity', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()