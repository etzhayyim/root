from langgraph.graph import StateGraph, END; from typing import TypedDict; class PrintingProcurementState(TypedDict): spec: dict; validated: bool; graph = StateGraph(PrintingProcurementState); def validate_specs(state: PrintingProcurementState): state['validated'] = 'print_resolution_dpi' in state['spec']; return state; def route_procurement(state: PrintingProcurementState): return 'end' if state['validated'] else 'end'; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()