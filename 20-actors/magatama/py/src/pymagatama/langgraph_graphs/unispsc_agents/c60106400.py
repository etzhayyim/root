from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: list; validated: bool; log: Annotated[list, operator.add]; def validate_electronics(state: ProcState): validated = all('safety_cert' in item for item in state['items']); return {'validated': validated, 'log': ['Validation complete']}; graph_builder = StateGraph(ProcState); graph_builder.add_node('validate', validate_electronics); graph_builder.set_entry_point('validate'); graph_builder.add_edge('validate', END); graph = graph_builder.compile()