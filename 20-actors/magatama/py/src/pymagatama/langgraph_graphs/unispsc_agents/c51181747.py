from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): chemical_name: str; purity: float; compliance_docs: List[str]; def validate_purity(state: PharmaState): return {'purity': 0.99 if state['purity'] >= 0.99 else 0.0}; def check_compliance(state: PharmaState): return {'compliance_docs': ['GMP_CERT', 'COA']}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()