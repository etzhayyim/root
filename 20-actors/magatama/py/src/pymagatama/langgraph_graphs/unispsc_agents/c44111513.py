from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): item_name: str; specs: dict; ready_for_procurement: bool; def validate_specs(state: ProcurementState): specs = state.get('specs', {}); required = ['material', 'dimensions']; is_valid = all(k in specs for k in required); return {'ready_for_procurement': is_valid}; workflow = StateGraph(ProcurementState); workflow.add_node('validate', validate_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()