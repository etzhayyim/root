from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OfficeState(TypedDict): items: List[str]; validated: bool; project_id: str; def validate_specs(state: OfficeState): return {'validated': all(item for item in state['items'])}; def approve_order(state: OfficeState): return {'validated': True}; graph = StateGraph(OfficeState); graph.add_node('validate', validate_specs); graph.add_node('approval', approve_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph = graph.compile()