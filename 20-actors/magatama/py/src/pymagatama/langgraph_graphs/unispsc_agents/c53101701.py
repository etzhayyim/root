from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SweaterState(TypedDict): items: List[dict]; inspection_passed: bool; def validate_textiles(state: SweaterState): for item in state['items']: item['valid'] = all(k in item for k in ['material', 'size', 'color']); return {'inspection_passed': all(i.get('valid') for i in state['items'])}; graph = StateGraph(SweaterState); graph.add_node('validate', validate_textiles); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()