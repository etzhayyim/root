from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VacuumProcessState(TypedDict): vacuum_specs: dict; validation_results: List[str]; def validate_leak_rate(state: VacuumProcessState): return {'validation_results': ['Pressure test passed'] if state['vacuum_specs'].get('leak_rate', 0) < 1e-9 else ['Pressure test failed']}; def export_control_check(state: VacuumProcessState): return {'validation_results': state['validation_results'] + ['Export control authorized']}; graph = StateGraph(VacuumProcessState); graph.add_node('validate', validate_leak_rate); graph.add_node('export', export_control_check); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph.set_entry_point('validate'); graph = graph.compile()