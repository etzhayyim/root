from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TerminalState(TypedDict): specs: dict; validation_errors: List[str]; def validate_specs(state: TerminalState): errors = []; if not state['specs'].get('rated_voltage'): errors.append('Missing voltage'); return {'validation_errors': errors}; graph = StateGraph(TerminalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()