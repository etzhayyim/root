from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ToyState(TypedDict): vendor_certs: List[str]; safety_approved: bool; def check_compliance(state: ToyState): state['safety_approved'] = 'ST' in state['vendor_certs'] or 'CE' in state['vendor_certs']; return state; graph = StateGraph(ToyState); graph.add_node('compliance', check_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', END); graph.compile()