from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): chemical_name: str; compliance_docs: List[str]; approved: bool; def validate_gmp(state: PharmState): state['approved'] = 'GMP_CERT' in state['compliance_docs']; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()