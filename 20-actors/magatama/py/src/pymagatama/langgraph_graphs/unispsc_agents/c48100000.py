from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): equipment_type: str; specs: dict; approved: bool; def validate_equipment(state: ProcurementState): return {'approved': state['specs'].get('NSF_Certification_Status') == 'Certified'}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()