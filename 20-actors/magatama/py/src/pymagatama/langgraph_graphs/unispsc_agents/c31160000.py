from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HardwareState(TypedDict): items: List[dict]; validation_results: dict; def validate_specs(state: HardwareState): state['validation_results'] = {'compliant': True}; return state; def route_inspection(state: HardwareState): return 'inspection'; graph = StateGraph(HardwareState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()