from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChaferState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ChaferState): errors = []; if 'material' not in state['specs']: errors.append('Missing material spec'); return {'validation_errors': errors}; def check_approval(state: ChaferState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(ChaferState); graph.add_node('validate', validate_specs); graph.add_node('approval', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph = graph.compile()