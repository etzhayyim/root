from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): doc_id: str; welding_valid: bool; certification_valid: bool; def validate_wps(state: ProcState): state['welding_valid'] = True; return state; def check_certs(state: ProcState): state['certification_valid'] = True; return state; graph = StateGraph(ProcState); graph.add_node('validate_wps', validate_wps); graph.add_node('check_certs', check_certs); graph.add_edge('validate_wps', 'check_certs'); graph.add_edge('check_certs', END); graph.set_entry_point('validate_wps'); graph = graph.compile()