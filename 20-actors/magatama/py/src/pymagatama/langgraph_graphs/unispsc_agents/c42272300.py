from langgraph.graph import StateGraph, END; from typing import TypedDict; class ResuscitationState(TypedDict): supply_id: str; compliance_verified: bool; quality_passed: bool; def check_compliance(state: ResuscitationState): return {'compliance_verified': True}; def verify_quality(state: ResuscitationState): return {'quality_passed': True}; graph = StateGraph(ResuscitationState); graph.add_node('compliance', check_compliance); graph.add_node('quality', verify_quality); graph.add_edge('compliance', 'quality'); graph.add_edge('quality', END); graph.set_entry_point('compliance'); graph = graph.compile()