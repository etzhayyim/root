from langgraph.graph import StateGraph, END; from typing import TypedDict; class ShotBlastState(TypedDict): media_type: str; pressure: float; result: str; def validate_specs(state: ShotBlastState): return {'result': 'Pass' if state['pressure'] < 100 else 'Fail'}; graph = StateGraph(ShotBlastState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()