from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DraftingTableState(TypedDict): specs: dict; validation_passed: bool;  def validate_table_specs(state: DraftingTableState):  specs = state.get('specs', {});  is_valid = all(k in specs for k in ['surface_material', 'tilt_angle_range']);  return {'validation_passed': is_valid};  graph = StateGraph(DraftingTableState); graph.add_node('validate', validate_table_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()