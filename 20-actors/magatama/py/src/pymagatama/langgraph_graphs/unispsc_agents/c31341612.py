from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CopperProcurementState(TypedDict): specs: dict; validation_log: Annotated[list, operator.add]; status: str; def validate_assembly(state: CopperProcurementState): status = 'PASS' if state['specs'].get('conductivity', 0) > 95 else 'FAIL'; return {'validation_log': [f'Conductivity check: {status}'], 'status': status}; workflow = StateGraph(CopperProcurementState); workflow.add_node('validate', validate_assembly); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()