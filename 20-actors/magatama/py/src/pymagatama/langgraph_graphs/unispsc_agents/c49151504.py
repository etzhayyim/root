from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BindingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(BindingState); def validate_specs(state: BindingState): return {'validation_results': ['ISO_check_passed'] if state['specs'].get('iso') else ['failed']}; def check_compatibility(state: BindingState): return {'validation_results': state['validation_results'] + ['compatibility_verified']}; graph.add_node('validate', validate_specs); graph.add_node('compatibility', check_compatibility); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()