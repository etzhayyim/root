from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; validation_results: List[str]; def validate_temp_range(state: ProcureState): temp = state['specs'].get('target_temperature_range_celsius'); state['validation_results'].append(f'Verifying range: {temp}') if temp else None; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_temp_range); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()