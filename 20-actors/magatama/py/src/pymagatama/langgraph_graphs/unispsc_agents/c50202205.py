from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WineState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def check_compliance(state: WineState): state['validation_errors'] = [] if state['specs'].get('abv', 0) < 0: state['validation_errors'].append('Invalid ABV'); return 'validation'; def validate_storage(state: WineState): return 'end'; graph = StateGraph(WineState); graph.add_node('validation', check_compliance); graph.add_node('storage', validate_storage); graph.set_entry_point('validation'); graph.add_edge('validation', 'storage'); graph.add_edge('storage', END); compiled_graph = graph.compile()