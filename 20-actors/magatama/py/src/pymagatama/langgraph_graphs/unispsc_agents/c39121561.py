from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ControllerState(TypedDict): specs: dict; valid: bool; error_logs: List[str]; def validate_pendant(state: ControllerState): specs = state.get('specs', {}); required = ['IP_rating', 'emergency_stop']; state['valid'] = all(k in specs for k in required); return state; graph = StateGraph(ControllerState); graph.add_node('validate', validate_pendant); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()