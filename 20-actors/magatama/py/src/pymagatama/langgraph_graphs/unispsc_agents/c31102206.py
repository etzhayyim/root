from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: MoldState): return {'validation_results': ['dims_ok'] if state['specs'].get('dims') else ['dims_fail']}; def check_material(state: MoldState): return {'validation_results': ['material_verified']}; graph = StateGraph(MoldState); graph.add_node('validate', validate_dimensions); graph.add_node('material', check_material); graph.add_edge('validate', 'material'); graph.add_edge('material', END); graph.set_entry_point('validate'); graph = graph.compile()