from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): input_spec: dict; validation_results: list[str]; approved: bool; def validate_jack_box(state: GraphState): spec = state['input_spec']; results = []; if 'connector_type' not in spec: results.append('Missing connector type'); if 'impedance_rating' not in spec: results.append('Missing impedance rating'); return {'validation_results': results, 'approved': len(results) == 0}; builder = StateGraph(GraphState); builder.add_node('validate', validate_jack_box); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()