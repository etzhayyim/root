from langgraph.graph import StateGraph, END; from typing import TypedDict; class MachineState(TypedDict): specs: dict; valid: bool; def validate_specs(state: MachineState): state['valid'] = 'tolerance' in state['specs']; return state; builder = StateGraph(MachineState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()