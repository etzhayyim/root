from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_passed: bool; report: str; def validate_specs(state: ProcurementState): state['validation_passed'] = 'Material Grade' in state['specs'] and state['specs']['Load Capacity (kg)'] > 0; return state; def generate_workflow(): graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = generate_workflow()