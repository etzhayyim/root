from langgraph.graph import StateGraph, END; from typing import TypedDict; class JoystickState(TypedDict): specs: dict; valid: bool; def validate_specs(state: JoystickState): state['valid'] = all(k in state['specs'] for k in ['IP_rating', 'axes']); return state; graph = StateGraph(JoystickState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()