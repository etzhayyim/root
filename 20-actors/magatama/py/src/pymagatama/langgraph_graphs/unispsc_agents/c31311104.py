from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): material_spec: str; pressure_test_ok: bool; weld_integrity: bool; validated: bool; def validate_material(state: PipeState): state['validated'] = ('Inconel' in state['material_spec']); return state; def validate_weld(state: PipeState): state['weld_integrity'] = True; return state; graph = StateGraph(PipeState); graph.add_node('material', validate_material); graph.add_node('weld', validate_weld); graph.set_entry_point('material'); graph.add_edge('material', 'weld'); graph.add_edge('weld', END); graph = graph.compile()