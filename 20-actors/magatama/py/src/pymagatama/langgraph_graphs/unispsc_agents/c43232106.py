from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SoftwareState(TypedDict): vendor: str; license_type: str; compliant: bool; graph = StateGraph(SoftwareState); def validate_license(state: SoftwareState): state['compliant'] = state['license_type'] in ['Enterprise', 'Perpetual']; return state; graph.add_node('validate', validate_license); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()