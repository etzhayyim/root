from langgraph.graph import StateGraph, END; from typing import TypedDict; class ToolState(TypedDict): tool_specs: dict; validated: bool; def validate_hardness(state: ToolState): state['validated'] = state['tool_specs'].get('hardness', 0) > 45; return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_hardness); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()