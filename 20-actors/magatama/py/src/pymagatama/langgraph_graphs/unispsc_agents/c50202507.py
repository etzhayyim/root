from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class JuiceState(TypedDict): vendor_id: str; quality_score: float; compliance_docs: List[str]; def validate_quality(state: JuiceState): return {'quality_score': 1.0 if state['quality_score'] > 0.8 else 0.0}; def check_compliance(state: JuiceState): return {'compliance_docs': ['FDA', 'ISO22000']}; graph = StateGraph(JuiceState); graph.add_node('validate', validate_quality); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()