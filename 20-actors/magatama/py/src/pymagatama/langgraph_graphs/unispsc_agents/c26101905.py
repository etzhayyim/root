from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BlockState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state): state['validation_results'].append('Dimensional check passed'); return state; def check_material_density(state): state['validation_results'].append('Density test passed'); return state; graph = StateGraph(BlockState); graph.add_node('dim_check', validate_dimensions); graph.add_node('density_check', check_material_density); graph.set_entry_point('dim_check'); graph.add_edge('dim_check', 'density_check'); graph.add_edge('density_check', END); graph = graph.compile()