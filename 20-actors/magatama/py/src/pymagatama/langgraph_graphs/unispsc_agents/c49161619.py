from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StringSpecs(TypedDict): specs: dict; quality_check: bool; def validate_specs(state: StringSpecs): state['quality_check'] = 'gauge' in state['specs'] and state['specs']['gauge'] > 0; return state; graph = StateGraph(StringSpecs); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()