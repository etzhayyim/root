from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): tool_data: dict; validation_status: bool; def validate_tool(state: State): state['validation_status'] = 'blade_material' in state['tool_data'] and state['tool_data']['blade_material'] != ''; return state; def finalize_procurement(state: State): state['procurement_ready'] = True; return state; graph = StateGraph(State); graph.add_node('validate', validate_tool); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()