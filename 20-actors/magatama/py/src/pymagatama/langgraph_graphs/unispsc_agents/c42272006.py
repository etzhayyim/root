from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): device_id: str; compliance_checks: List[str]; approved: bool; def validate_sterility(state: State): state['compliance_checks'].append('Sterility_Verified'); return state; def check_regulatory(state: State): state['approved'] = True; return state; graph = StateGraph(State); graph.add_node('sterility', validate_sterility); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('sterility'); graph.add_edge('sterility', 'regulatory'); graph.add_edge('regulatory', END); app = graph.compile()