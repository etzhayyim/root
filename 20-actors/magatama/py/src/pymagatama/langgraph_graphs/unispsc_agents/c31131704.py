from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: ForgingState): state['validation_results'].append('Material verified'); return state; def validate_dimensions(state: ForgingState): state['validation_results'].append('Dimensions verified'); return state; graph = StateGraph(ForgingState); graph.add_node('validate_material', validate_material); graph.add_node('validate_dimensions', validate_dimensions); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'validate_dimensions'); graph.add_edge('validate_dimensions', END); graph = graph.compile()