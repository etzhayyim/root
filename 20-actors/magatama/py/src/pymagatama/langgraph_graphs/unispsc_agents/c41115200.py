from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadarState(TypedDict): specs: dict; compliance_check: bool; final_report: str; def validate_radar_specs(state: RadarState): return {'compliance_check': all(k in state['specs'] for k in ['frequency', 'power'])}; def export_control_check(state: RadarState): return {'final_report': 'Clear for export' if state['compliance_check'] else 'Requires ECCN verification'}; graph = StateGraph(RadarState); graph.add_node('validate', validate_radar_specs); graph.add_node('compliance', export_control_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()