from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DamState(TypedDict): status: str; safety_checks: List[str]; def structural_validation(state: DamState): return {state: {status: 'validated', safety_checks: ['geology', 'hydrology']}}; def environmental_review(state: DamState): return {status: 'approved'}; graph = StateGraph(DamState); graph.add_node('validate', structural_validation); graph.add_node('env_review', environmental_review); graph.set_entry_point('validate'); graph.add_edge('validate', 'env_review'); graph.add_edge('env_review', END); graph = graph.compile()