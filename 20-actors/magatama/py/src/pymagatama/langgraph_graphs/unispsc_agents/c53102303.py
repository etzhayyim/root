from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UnderwearState(TypedDict): specs: dict; validation_passed: bool; def validate_material(state: UnderwearState): material = state['specs'].get('fabric'); state['validation_passed'] = bool(material in ['Cotton', 'Polyester', 'Modal']); return state; def check_size(state: UnderwearState): state['validation_passed'] = state['validation_passed'] and (state['specs'].get('size') is not None); return state; graph = StateGraph(UnderwearState); graph.add_node('validate_material', validate_material); graph.add_node('check_size', check_size); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_size'); graph.add_edge('check_size', END); graph = graph.compile()