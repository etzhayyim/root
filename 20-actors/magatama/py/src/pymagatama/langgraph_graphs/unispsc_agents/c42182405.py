from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ScreeningState(TypedDict): data: dict; errors: Annotated[list, operator.add]; def validate_calibration(state: ScreeningState): state['data']['calibrated'] = True; return state; def check_compliance(state: ScreeningState): if 'iso_cert' not in state['data']: return {'errors': ['Missing ISO 13485 certification']}; return state; graph = StateGraph(ScreeningState); graph.add_node('validate', validate_calibration); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()