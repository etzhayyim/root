from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): specs: dict; validation_report: list; approved: bool; def validate_specs(state: DentalState): specs = state['specs']; issues = []; if specs.get('rpm', 0) < 5000: issues.append('Insufficient RPM'); return {'validation_report': issues, 'approved': len(issues) == 0}; graph = StateGraph(DentalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()