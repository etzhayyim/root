from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AdhesionState(TypedDict): chemical_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_viscosity(state: AdhesionState): return {'validation_results': ['viscosity_passed'] if state['chemical_data'].get('viscosity') else ['viscosity_failed']}; def check_compatibility(state: AdhesionState): return {'status': 'READY' if 'viscosity_passed' in state['validation_results'] else 'FAILED'}; graph = StateGraph(AdhesionState); graph.add_node('validate', validate_viscosity); graph.add_node('check', check_compatibility); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.set_entry_point('validate'); graph = graph.compile()