from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PunchState(TypedDict): capacity: int; material: str; validated: bool; def validate_capacity(state: PunchState): state['validated'] = state['capacity'] > 0; return {'validated': state['validated']}; def check_safety(state: PunchState): return {'validated': state['validated'] and state['material'] != 'restricted'}; graph = StateGraph(PunchState); graph.add_node('validate', validate_capacity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()