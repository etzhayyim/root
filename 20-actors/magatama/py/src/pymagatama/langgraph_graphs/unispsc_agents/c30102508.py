from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): spec: dict; verified: bool; errors: List[str]; def validate_titanium_spec(state: ProcState): spec = state['spec']; errors = []; if spec.get('grade') not in ['Grade 1', 'Grade 2', 'Grade 5']: errors.append('Invalid Grade'); return {'verified': len(errors) == 0, 'errors': errors}; graph = StateGraph(ProcState); graph.add_node('validate', validate_titanium_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()