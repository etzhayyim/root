from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DentalProcurement(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_grade(state: DentalProcurement): state['validated'] = all(c in state['certs'] for c in ['ISO_13485', 'CE_Medical']); return state; graph = StateGraph(DentalProcurement); graph.add_node('validate', validate_medical_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()