from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SecurityHardwareState(TypedDict): hardware_specs: dict; validation_results: List[str]; def validate_security_compliance(state: SecurityHardwareState): return {'validation_results': ['Compliance Verified'] if state['hardware_specs'].get('rating') == 'Grade 1' else ['Non-Compliant']}; def route_procurement(state: SecurityHardwareState): return 'end'; graph = StateGraph(SecurityHardwareState); graph.add_node('validate', validate_security_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()