from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BronzeState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_dimensions(state: BronzeState): state['validation_errors'] = [] if state['specs'].get('tolerance', 0.05) > 0.1 else ['Tolerance too tight']; return state; def check_composition(state: BronzeState): if 'alloy' not in state['specs']: state['validation_errors'].append('Missing alloy spec'); return state; builder = StateGraph(BronzeState); builder.add_node('validate', validate_dimensions); builder.add_node('check_mat', check_composition); builder.add_edge('validate', 'check_mat'); builder.add_edge('check_mat', END); builder.set_entry_point('validate'); graph = builder.compile()