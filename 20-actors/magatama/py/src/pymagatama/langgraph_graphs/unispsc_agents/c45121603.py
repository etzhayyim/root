from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LensState(TypedDict): lens_model: str; specs: dict; validation_errors: List[str]; def validate_optical_specs(state: LensState): errors = []; if 'mount_type' not in state['specs']: errors.append('Missing mount type'); return {'validation_errors': errors}; def check_export_control(state: LensState): return {'validation_errors': state.get('validation_errors', [])}; graph = StateGraph(LensState); graph.add_node('validate', validate_optical_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()