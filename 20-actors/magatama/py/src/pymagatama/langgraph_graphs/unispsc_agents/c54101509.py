from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class JewelryState(TypedDict): items: List[str]; validation_results: List[str]; status: str; def validate_purity(state: JewelryState): state['validation_results'].append('Purity check passed'); return state; def check_value(state: JewelryState): state['status'] = 'HIGH_VALUE_SECURITY_REQUIRED'; return state; graph = StateGraph(JewelryState); graph.add_node('purity', validate_purity); graph.add_node('security', check_value); graph.add_edge('purity', 'security'); graph.add_edge('security', END); graph.set_entry_point('purity'); graph = graph.compile()