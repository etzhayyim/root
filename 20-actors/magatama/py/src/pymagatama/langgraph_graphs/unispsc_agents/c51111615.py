from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GlutathioneState(TypedDict): purity: float; storage_temp: float; certificates: List[str]; approved: bool; def validate_quality(state: GlutathioneState): state['approved'] = state['purity'] >= 0.98 and state['storage_temp'] <= 25.0; return state; graph = StateGraph(GlutathioneState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()