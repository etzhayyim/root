from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): specs: dict; validation_log: List[str]; def validate_specs(state: BearingState): spec = state.get('specs', {}); valid = 'inner_diameter_mm' in spec and 'material_grade' in spec; return {'validation_log': ['Specs validated: ' + str(valid)]}; graph = StateGraph(BearingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()