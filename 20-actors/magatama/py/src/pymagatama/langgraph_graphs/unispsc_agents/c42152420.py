from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WaxState(TypedDict): wax_type: str; melting_point: float; compliant: bool; def check_compliance(state: WaxState): state['compliant'] = 120 <= state['melting_point'] <= 150; return state; graph = StateGraph(WaxState); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()