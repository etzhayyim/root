from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CatheterState(TypedDict): vendor_certs: List[str]; sterile_status: bool; compliance_passed: bool; def check_certification(state: CatheterState): state['compliance_passed'] = 'ISO 13485' in state['vendor_certs']; return state; def validate_sterility(state: CatheterState): return {'compliance_passed': state['compliance_passed'] and state['sterile_status']}; graph = StateGraph(CatheterState); graph.add_node('check_cert', check_certification); graph.add_node('validate_sterile', validate_sterility); graph.set_entry_point('check_cert'); graph.add_edge('check_cert', 'validate_sterile'); graph.add_edge('validate_sterile', END); graph.compile()