from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FloorPadState(TypedDict): pad_specs: dict; validation_report: List[str]; graph = StateGraph(FloorPadState); def validate_pad_specs(state: FloorPadState): results = []; specs = state.get('pad_specs', {}); if 'diameter' not in specs: results.append('Missing diameter'); return {'validation_report': results}; graph.add_node('validation', validate_pad_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()