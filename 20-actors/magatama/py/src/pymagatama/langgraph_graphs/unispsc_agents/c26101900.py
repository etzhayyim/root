from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EngineState(TypedDict): component_id: str; specs: dict; validation_passed: bool; def validate_specs(state: EngineState): state['validation_passed'] = all(k in state['specs'] for k in ['Material', 'Tolerance']); return state; def check_dual_use(state: EngineState): return 'dual-use-export-control' if state['specs'].get('application') == 'defense' else 'standard'; graph = StateGraph(EngineState); graph.add_node('validate', validate_specs); graph.add_node('check_export', check_dual_use); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_export'); graph.add_edge('check_export', END); compiled_graph = graph.compile()