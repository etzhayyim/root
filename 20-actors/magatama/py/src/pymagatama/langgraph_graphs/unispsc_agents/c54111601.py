from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClockState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: ClockState): state['validation_report'] = 'Valid' if state['specs'].get('precision') else 'Invalid'; return state; graph = StateGraph(ClockState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()