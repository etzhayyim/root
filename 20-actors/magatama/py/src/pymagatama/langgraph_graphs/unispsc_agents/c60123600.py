from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class GlitterState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: GlitterState): state['valid'] = 'particle_size_microns' in state['specs'] and 'non_toxicity_certification' in state['specs']; return state; builder = StateGraph(GlitterState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()