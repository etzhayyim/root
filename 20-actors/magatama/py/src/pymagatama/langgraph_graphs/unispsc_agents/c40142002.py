from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AirHoseState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(AirHoseState); def validate_pressure(state: AirHoseState): specs = state.get('specs', {}); min_pressure = 1.0; status = 'Pass' if specs.get('pressure', 0) >= min_pressure else 'Fail'; return {'validation_report': [f'Pressure validation: {status}']}; graph.add_node('validate_specs', validate_pressure); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', END); app = graph.compile()