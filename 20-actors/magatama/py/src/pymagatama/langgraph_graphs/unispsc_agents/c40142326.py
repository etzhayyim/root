from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeSpecs(TypedDict): specs: dict; errors: List[str]; def validate_spacer(state: PipeSpecs): specs = state['specs']; errors = []; if not specs.get('material_grade'): errors.append('Missing material grade'); if specs.get('load_bearing_capacity', 0) < 100: errors.append('Load capacity too low'); return {'errors': errors}; graph = StateGraph(PipeSpecs); graph.add_node('validate', validate_spacer); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()