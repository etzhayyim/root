from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class TitaniumState(TypedDict): input_specs: dict; validated: bool; export_check_passed: bool; def validate_grade(state: TitaniumState): state['validated'] = 'ASTM' in state['input_specs'].get('grade', ''); return state; def check_export(state: TitaniumState): state['export_check_passed'] = state['input_specs'].get('country') != 'restricted'; return state; graph = StateGraph(TitaniumState); graph.add_node('validate', validate_grade); graph.add_node('check_export', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_export'); graph.add_edge('check_export', END); app = graph.compile()