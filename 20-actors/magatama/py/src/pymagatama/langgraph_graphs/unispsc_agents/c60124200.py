from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProjectState(TypedDict): content: str; validated: bool; curriculum_match: float; graph = StateGraph(ProjectState); def validate_curriculum(state: ProjectState): return {'validated': True, 'curriculum_match': 0.95}; def finalize_project(state: ProjectState): return {'content': 'Approved: ' + state['content']}; graph.add_node('validate', validate_curriculum); graph.add_node('finalize', finalize_project); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()