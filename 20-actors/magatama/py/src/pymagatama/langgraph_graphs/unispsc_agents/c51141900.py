from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): medicine_name: str; compliance_check: bool; batch_verified: bool; def validate_compliance(state: DrugState): state['compliance_check'] = True; return state; def check_batch(state: DrugState): state['batch_verified'] = True; return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_compliance); graph.add_node('verify', check_batch); graph.set_entry_point('validate'); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); graph = graph.compile()