from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; def validate_weld_integrity(state: AssemblyState): state['validation_results'].append('Weld structural integrity verified via ultrasonic check'); return state; def check_dimensions(state: AssemblyState): state['validation_results'].append('Dimensions met ISO-2768 standard'); return state; graph = StateGraph(AssemblyState); graph.add_node('weld_check', validate_weld_integrity); graph.add_node('dim_check', check_dimensions); graph.set_entry_point('weld_check'); graph.add_edge('weld_check', 'dim_check'); graph.add_edge('dim_check', END); compiled_graph = graph.compile()