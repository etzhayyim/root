from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ActuatorState(TypedDict): specs: dict; validation_results: List[str]; status: str; def validate_specs(state: ActuatorState): results = []; if state['specs'].get('torque_nm', 0) < 0: results.append('Invalid torque'); return {'validation_results': results, 'status': 'validated' if not results else 'failed'}; graph = StateGraph(ActuatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()