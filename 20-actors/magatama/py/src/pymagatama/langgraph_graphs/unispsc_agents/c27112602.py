from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolSpec(TypedDict): vendor: str; model: str; pressure_range: float; status: str; def validate_pressure(state: ToolSpec): return {'status': 'validated' if state['pressure_range'] > 50 else 'rejected'}; graph = StateGraph(ToolSpec); graph.add_node('validate', validate_pressure); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()