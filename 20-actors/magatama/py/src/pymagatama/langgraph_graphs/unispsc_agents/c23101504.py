from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; validation_passed: bool; def check_specs(state: CompressorState): state['validation_passed'] = state['specs'].get('power') > 0 and state['specs'].get('pressure') > 0; return state; def route_step(state: CompressorState): return 'validate' if state.get('specs') else END; builder = StateGraph(CompressorState); builder.add_node('validate', check_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()