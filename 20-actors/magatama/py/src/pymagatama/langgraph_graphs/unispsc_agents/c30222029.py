from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BridgeState(TypedDict): specs: dict; validation_report: List[str]; approved: bool; def validate_specs(state: BridgeState): errors = []; if state['specs'].get('load_capacity', 0) < 5: errors.append('Load capacity below code requirement'); return {'validation_report': errors, 'approved': len(errors) == 0}; graph = StateGraph(BridgeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()