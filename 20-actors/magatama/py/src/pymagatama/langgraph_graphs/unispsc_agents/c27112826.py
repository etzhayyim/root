from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HoleSawState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: HoleSawState): errors = []; if state['specs'].get('cutting_diameter', 0) <= 0: errors.append('Invalid cutting diameter'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(HoleSawState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()