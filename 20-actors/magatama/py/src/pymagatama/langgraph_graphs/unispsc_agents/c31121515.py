from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_file: str; validated: bool; safety_check: bool; def validate_dimensions(state: State): return {'validated': True} if state['input_file'].endswith('.stp') else {'validated': False}; def check_lead_safety(state: State): return {'safety_check': True}; graph = StateGraph(State); graph.add_node('dim_check', validate_dimensions); graph.add_node('safety_protocol', check_lead_safety); graph.set_entry_point('dim_check'); graph.add_edge('dim_check', 'safety_protocol'); graph.add_edge('safety_protocol', END); graph = graph.compile()