from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BufferState(TypedDict): chemical_name: str; ph_target: float; validation_logs: List[str]; def validate_buffer(state: BufferState): status = 'Valid' if 0 <= state['ph_target'] <= 14 else 'Invalid'; return {'validation_logs': [f'Verification: {status} at pH {state['ph_target']}']}; graph = StateGraph(BufferState); graph.add_node('validate', validate_buffer); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()