from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpringState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_spring(state: SpringState): specs = state['specs']; errors = []; if specs.get('wire_diameter', 0) <= 0: errors.append('Invalid wire diameter'); state['valid'] = len(errors) == 0; state['error_log'] = errors; return state; graph = StateGraph(SpringState); graph.add_node('validate', validate_spring); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()