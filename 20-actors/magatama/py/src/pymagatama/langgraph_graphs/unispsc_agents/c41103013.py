from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RefrigeratorState(TypedDict): spec: dict; status: str; safety_checks: List[str]; def validate_specs(state: RefrigeratorState): certification = state['spec'].get('certification'); if not certification: return {'status': 'REJECTED'}; return {'status': 'VALIDATED', 'safety_checks': ['check_explosion_proof_rating', 'verify_temp_control']}; graph = StateGraph(RefrigeratorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()