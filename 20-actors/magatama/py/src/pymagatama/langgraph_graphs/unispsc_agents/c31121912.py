from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): input_specs: dict; validation: bool; output: str; def validate_dimensions(state: MoldState): return {'validation': True if 'tolerance' in state['input_specs'] else False}; def finalize_order(state: MoldState): return {'output': 'Validation successful, proceed to machining.'}; graph = StateGraph(MoldState); graph.add_node('validate', validate_dimensions); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()