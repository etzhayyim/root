from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): metal_spec: str; inspection_status: bool; graph = StateGraph(ProcureState); def validate_alloy(state): return {'inspection_status': True if 'Inconel' in state['metal_spec'] else False}; def verify_weld(state): return {'inspection_status': True}; graph.add_node('alloy_validation', validate_alloy); graph.add_node('weld_inspection', verify_weld); graph.set_entry_point('alloy_validation'); graph.add_edge('alloy_validation', 'weld_inspection'); graph.add_edge('weld_inspection', END); compiled_graph = graph.compile()