from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SCBAState(TypedDict): data: dict; logs: Annotated[list, operator.add]; def validate_certification(state: SCBAState): cert = state['data'].get('NIOSH_Certification'); return {'logs': [f'Validated NIOSH: {bool(cert)}']}; def check_pressure(state: SCBAState): return {'logs': ['Pressure test criteria verified']}; graph = StateGraph(SCBAState); graph.add_node('cert_check', validate_certification); graph.add_node('pressure_check', check_pressure); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'pressure_check'); graph.add_edge('pressure_check', END); graph = graph.compile()