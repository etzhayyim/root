from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ColumnState(TypedDict): column_id: str; specs: dict; validation_results: List[str]; def validate_specs(state: ColumnState): status = "Pass" if state['specs'].get('pressure_rating') else "Fail"; state['validation_results'].append(status); return state; def check_compliance(state: ColumnState): return "compliant" if state['validation_results'][0] == "Pass" else "non_compliant"; graph = StateGraph(ColumnState); graph.add_node("validate", validate_specs); graph.add_edge("validate", END); graph.set_entry_point("validate"); compile_graph = graph.compile()