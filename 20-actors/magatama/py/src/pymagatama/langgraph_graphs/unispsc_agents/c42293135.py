from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RetractorState(TypedDict): vendor_id: str; certification: str; spec_file: str; is_compliant: bool; def validate_specs(state: RetractorState): state['is_compliant'] = state['certification'] == 'ISO 13485' and state['spec_file'] is not None; return state; graph = StateGraph(RetractorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()