from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ViaductState(TypedDict): status: str; safety_checks: List[str]; def initialize_node(state): return {'status': 'planning', 'safety_checks': ['seismic_review']}; def construction_node(state): return {'status': 'under_construction', 'safety_checks': state['safety_checks'] + ['site_audit']}; graph = StateGraph(ViaductState); graph.add_node('init', initialize_node); graph.add_node('construct', construction_node); graph.add_edge('init', 'construct'); graph.add_edge('construct', END); graph.set_entry_point('init'); compiled_graph = graph.compile()