from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec_validated: bool; def validate_spec(state: State): state['spec_validated'] = True; return {'status': 'Validated'}; def check_compliance(state: State): return {'status': 'Compliance_Checked'}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()