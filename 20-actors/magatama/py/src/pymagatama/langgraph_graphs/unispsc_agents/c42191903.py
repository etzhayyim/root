from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MonitorCabinetState(TypedDict): specs: dict; compliance_check: bool; final_report: str; def validate_specs(state: MonitorCabinetState): return {'compliance_check': all(k in state['specs'] for k in ['load', 'material'])}; def generate_procurement_report(state: MonitorCabinetState): return {'final_report': 'Validated for hospital use.'}; graph = StateGraph(MonitorCabinetState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_procurement_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()