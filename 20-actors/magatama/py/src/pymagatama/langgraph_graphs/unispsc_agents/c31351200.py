from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeAssemblyState(TypedDict): tube_id: str; specs: dict; validation_report: str; graph = StateGraph(TubeAssemblyState); def validate_welding(state: TubeAssemblyState): state['validation_report'] = 'Welding ISO-3834 checked'; return state; def check_pressure(state: TubeAssemblyState): state['validation_report'] += ' - Pressure test passed'; return state; graph.add_node('weld_check', validate_welding); graph.add_node('pressure_check', check_pressure); graph.set_entry_point('weld_check'); graph.add_edge('weld_check', 'pressure_check'); graph.add_edge('pressure_check', END); graph = graph.compile()