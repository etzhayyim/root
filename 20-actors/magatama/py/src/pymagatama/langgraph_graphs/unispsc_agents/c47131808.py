from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GermicidalState(TypedDict): chemical_data: dict; compliance_docs: List[str]; approved: bool; def validate_composition(state: GermicidalState): return {'approved': state['chemical_data'].get('concentration') > 0.05}; def check_compliance(state: GermicidalState): return {'compliance_docs': ['SDS_VERIFIED', 'EPA_REG_VALID']}; graph = StateGraph(GermicidalState); graph.add_node('validate', validate_composition); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()