from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; from operator import add; class MaterialState(TypedDict): material_id: str; purity: float; status: str; logs: Annotated[Sequence[str], add]; def validate_purity(state: MaterialState) -> MaterialState: status = 'VALID' if state['purity'] >= 99.99 else 'REJECTED'; return {'status': status, 'logs': [f'Purity check result: {status}']}; def generate_spec(state: MaterialState) -> MaterialState: return {'logs': ['Spec document generated successfully']}; graph = StateGraph(MaterialState); graph.add_node('validate', validate_purity); graph.add_node('spec_gen', generate_spec); graph.add_edge('validate', 'spec_gen'); graph.add_edge('spec_gen', END); graph.set_entry_point('validate'); graph = graph.compile()