from langgraph.graph import StateGraph, END; from typing import TypedDict; class ChurchProcurementState(TypedDict): status: str; spec_docs: list; graph = StateGraph(ChurchProcurementState); def validate_specs(state): return {'status': 'VALIDATED'}; def execute_construction(state): return {'status': 'COMPLETED'}; graph.add_node('validate', validate_specs); graph.add_node('construct', execute_construction); graph.set_entry_point('validate'); graph.add_edge('validate', 'construct'); graph.add_edge('construct', END); graph.compile()