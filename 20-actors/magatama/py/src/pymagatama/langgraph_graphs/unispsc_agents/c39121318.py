from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input: str; specs: dict; valid: bool; def validate_specs(state: State): specs = state.get('specs', {}); rating = specs.get('nema_rating', ''); state['valid'] = rating in ['1', '3R', '4', '4X']; return state; builder = StateGraph(State); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()