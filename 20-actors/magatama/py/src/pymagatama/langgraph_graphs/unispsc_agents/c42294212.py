from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): instrument_set: dict; validation_report: str; def validate_spec(state: State): state['validation_report'] = 'Valid' if 'ISO' in state['instrument_set'].get('standard', '') else 'Invalid'; return state; def check_compliance(state: State): return 'approve' if state['validation_report'] == 'Valid' else 'reject'; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()