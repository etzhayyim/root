from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpacecraftState(TypedDict): vehicle_spec: dict; compliance_checked: bool; inspection_logs: List[str]; def validate_specs(state: SpacecraftState): state['compliance_checked'] = True; return {'compliance_checked': True}; def update_logs(state: SpacecraftState): state['inspection_logs'].append('ITEC_Audit_Passed'); return {'inspection_logs': state['inspection_logs']}; graph = StateGraph(SpacecraftState); graph.add_node('validate', validate_specs); graph.add_node('audit', update_logs); graph.set_entry_point('validate'); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); graph = graph.compile()