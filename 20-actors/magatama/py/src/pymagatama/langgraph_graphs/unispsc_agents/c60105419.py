from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): content: str; validated: bool; feedback: List[str]; def analyze_content(state: MaterialState): state['validated'] = 'SST' in state['content'] or 'social' in state['content']; return state; def report_generation(state: MaterialState): print(f'Validated: {state['validated']}'); return state; graph = StateGraph(MaterialState); graph.add_node('analysis', analyze_content); graph.add_node('report', report_generation); graph.add_edge('analysis', 'report'); graph.add_edge('report', END); graph.set_entry_point('analysis'); graph = graph.compile()