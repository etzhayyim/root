from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): medicine_name: str; compliance_docs: List[str]; quality_check: bool; def validate_pharma_spec(state: ProcState): state['quality_check'] = 'Pharmacopeia_Compliance' in state['compliance_docs']; return state; def check_storage(state: ProcState): return {'compliance_docs': state['compliance_docs'] + ['Temp_Storage_Verified']}; graph = StateGraph(ProcState); graph.add_node('validate', validate_pharma_spec); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph = graph.compile()