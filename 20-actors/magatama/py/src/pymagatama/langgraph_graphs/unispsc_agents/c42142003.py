from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): status: str; spec_validated: bool; def validate_spec(state: ProcureState): state['spec_validated'] = True; state['status'] = 'COMPLIANT'; return state; def finalize(state: ProcureState): state['status'] = 'READY'; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()