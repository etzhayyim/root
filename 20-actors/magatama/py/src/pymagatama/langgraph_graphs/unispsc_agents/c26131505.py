from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MarineState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: MarineState): state['compliance'] = 'IMO_certification' in state['specs'] and state['specs'].get('power_kW', 0) > 0; return state; def check_approval(state: MarineState): state['approved'] = state['compliance']; return state; graph = StateGraph(MarineState); graph.add_node('validate', validate_specs); graph.add_node('check', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()