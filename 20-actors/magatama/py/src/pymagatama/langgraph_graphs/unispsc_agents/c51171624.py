from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; analysis: dict; status: str; def validate_purity(state: State): purity = state['input_data'].get('purity', 0); state['analysis'] = {'valid': purity >= 99.0}; state['status'] = 'COMPLETED' if state['analysis']['valid'] else 'REJECTED'; return state; graph = StateGraph(State); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()