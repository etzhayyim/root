from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; purity: float; cold_chain_verified: bool; result: str; def validate_purity(state: PharmaState): return {'result': 'Validated'} if state['purity'] >= 99.0 else {'result': 'Rejected'}; def check_cold_chain(state: PharmaState): return {'result': 'Verified'} if state['cold_chain_verified'] else {'result': 'Failed'}; graph = StateGraph(PharmaState); graph.add_node('purity_check', validate_purity); graph.add_node('cold_chain', check_cold_chain); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'cold_chain'); graph.add_edge('cold_chain', END); graph = graph.compile()