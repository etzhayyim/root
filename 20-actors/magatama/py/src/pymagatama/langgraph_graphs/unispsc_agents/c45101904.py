from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EnlargerState(TypedDict): specs: dict; ready_for_procurement: bool; def validate_specs(state: EnlargerState): state['ready_for_procurement'] = 'lens_type' in state['specs'] and 'format' in state['specs']; return state; builder = StateGraph(EnlargerState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()