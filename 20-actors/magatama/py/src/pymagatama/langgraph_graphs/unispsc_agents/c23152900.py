from langgraph.graph import StateGraph, END; from typing import TypedDict; class RobotState(TypedDict): specs: dict; validation_status: bool; def validate_robot(state: RobotState): state['validation_status'] = all(k in state['specs'] for k in ['payload', 'reach']); return state; graph = StateGraph(RobotState); graph.add_node('validate', validate_robot); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()