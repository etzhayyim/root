from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; validation_status: bool; compliance_docs: List[str]; def validate_purity(state: PharmState): state['validation_status'] = True; return state; def verify_regulatory(state: PharmState): state['compliance_docs'].append('GMP_CERT'); return state; graph = StateGraph(PharmState); graph.add_node('validate_purity', validate_purity); graph.add_node('verify_regulatory', verify_regulatory); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'verify_regulatory'); graph.add_edge('verify_regulatory', END); graph = graph.compile()