from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class PulpIngestState(TypedDict): input_data: dict; quality_results: list; status: str; def validate_fiber_specs(state: PulpIngestState) -> PulpIngestState: state['quality_results'].append('fiber_length_verified'); return state; def check_sustainability(state: PulpIngestState) -> PulpIngestState: state['quality_results'].append('fsc_certification_confirmed'); return state; graph = StateGraph(PulpIngestState); graph.add_node('validate', validate_fiber_specs); graph.add_node('sustain', check_sustainability); graph.set_entry_point('validate'); graph.add_edge('validate', 'sustain'); graph.add_edge('sustain', END); graph = graph.compile()