from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[dict]; validation_results: List[bool]; def validate_drain_board(state: ProcurementState): return {'validation_results': [True]}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_drain_board); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()