from langgraph.graph import StateGraph, END; from typing import TypedDict; class LaserProcState(TypedDict): status: str; specs: dict; graph = StateGraph(LaserProcState); def validate_laser(state: LaserProcState): state['status'] = 'COMPLIANT' if state['specs'].get('laser_class') == 4 else 'NON-COMPLIANT'; return state; graph.add_node('validate', validate_laser); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()