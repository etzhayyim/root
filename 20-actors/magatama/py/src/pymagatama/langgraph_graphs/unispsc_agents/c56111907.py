from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolCartState(TypedDict): specs: dict; valid: bool; workflow: List[str]; def validate_specs(state: ToolCartState): state['valid'] = state['specs'].get('load_capacity', 0) > 0; return state; def route_inspection(state: ToolCartState): return 'inspection' if state['valid'] else END; graph = StateGraph(ToolCartState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()