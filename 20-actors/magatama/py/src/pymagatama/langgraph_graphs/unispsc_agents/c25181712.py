from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrailerState(TypedDict): trailer_specs: dict; validation_report: List[str]; def validate_capacity(state: TrailerState): if state['trailer_specs'].get('load_kg', 0) > 1000: state['validation_report'].append('Heavy Duty Certification Required'); return state; def check_compliance(state: TrailerState): if not state['trailer_specs'].get('cert'): state['validation_report'].append('Safety Compliance Missing'); return state; graph = StateGraph(TrailerState); graph.add_node('load_check', validate_capacity); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('load_check'); graph.add_edge('load_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()