from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class IonExchangeState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(IonExchangeState); def validate_specs(state: IonExchangeState): return {'validation_results': ['Pressure safety check passed', 'Resin compatibility verified']}; def check_compliance(state: IonExchangeState): return {'validation_results': state['validation_results'] + ['Standard ISO 9001 compliance validated']}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()