from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: ProcureState): hardness = state['specs'].get('hardness', 0); state['valid'] = hardness >= 58; state['error'] = '' if state['valid'] else 'Hardness below minimum requirement'; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()