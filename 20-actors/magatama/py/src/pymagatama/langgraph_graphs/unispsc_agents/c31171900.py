from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SprocketState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: SprocketState): specs = state['specs']; is_valid = all(key in specs for key in ['material', 'teeth', 'pitch']); return {'valid': is_valid, 'error': '' if is_valid else 'Missing required specs'}; def generate_report(state: SprocketState): return {'error': 'Report generated successfully'}; graph = StateGraph(SprocketState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()