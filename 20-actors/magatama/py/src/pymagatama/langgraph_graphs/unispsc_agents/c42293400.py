from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): device_id: str; compliance_docs: List[str]; inspection_passed: bool; def validate_compliance(state: SurgicalState): return {'compliance_docs': ['ISO13485', 'CE_MARK']}; def perform_inspection(state: SurgicalState): return {'inspection_passed': True}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_compliance); graph.add_node('inspect', perform_inspection); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()