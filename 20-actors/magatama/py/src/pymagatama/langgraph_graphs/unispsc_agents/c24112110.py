from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IBCState(TypedDict): container_specs: dict; validation_errors: List[str]; approved: bool; def validate_ibc(state: IBCState): errors = []; if state['container_specs'].get('capacity', 0) > 2000: errors.append('Capacity exceeds standard IBC limits'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(IBCState); graph.add_node('validate', validate_ibc); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()