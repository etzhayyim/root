from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalInstrumentState(TypedDict): instrument_ids: List[str]; qc_passed: bool; compliance_docs: dict; def validate_instrument(state): return {'qc_passed': True}; def check_compliance(state): return {'compliance_docs': {'iso_certified': True}}; graph = StateGraph(SurgicalInstrumentState); graph.add_node('validate', validate_instrument); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()