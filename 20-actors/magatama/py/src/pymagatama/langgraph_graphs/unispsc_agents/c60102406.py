from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MathToolState(TypedDict): items: List[str]; validated: bool; def validate_safety(state: MathToolState): return {'validated': all('non-toxic' in item for item in state['items'])}; graph = StateGraph(MathToolState); graph.add_node('safety_check', validate_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()