from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): content_type: str; validation_passed: bool; reviewer_notes: List[str]; def validate_material(state: AgentState): return {'validation_passed': True if state['content_type'] in ['video', 'print', 'digital'] else False}; def final_review(state: AgentState): return {'reviewer_notes': ['Approved for distribution'] if state['validation_passed'] else ['Rejected due to format mismatch']}; graph = StateGraph(AgentState); graph.add_node('validate', validate_material); graph.add_node('review', final_review); graph.add_edge('validate', 'review'); graph.add_edge('review', END); graph.set_entry_point('validate'); graph = graph.compile()