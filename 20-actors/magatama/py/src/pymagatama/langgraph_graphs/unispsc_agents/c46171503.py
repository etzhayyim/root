from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LockState(TypedDict): vendor_specs: dict; validation_logger: Annotated[list, operator.add]; status: str; def validate_grade(state: LockState): grade = state['vendor_specs'].get('ANSI_Grade'); return {'validation_logger': [f'Validating ANSI Grade {grade}']} if grade else {'status': 'REJECTED'}; def finalize_check(state: LockState): return {'status': 'COMPLIANT'}; graph = StateGraph(LockState); graph.add_node('validate', validate_grade); graph.add_node('finalize', finalize_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()