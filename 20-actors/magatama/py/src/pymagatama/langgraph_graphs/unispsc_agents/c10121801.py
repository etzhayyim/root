from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FertilizerState(TypedDict): n_ratio: float; p_ratio: float; k_ratio: float; is_organic: bool; inspection_passed: bool; def validate_composition(state: FertilizerState) -> FertilizerState: state['inspection_passed'] = (state['n_ratio'] + state['p_ratio'] + state['k_ratio'] >= 15.0); return state; def check_certification(state: FertilizerState) -> FertilizerState: return state; workflow = StateGraph(FertilizerState); workflow.add_node('validate', validate_composition); workflow.add_node('certify', check_certification); workflow.set_entry_point('validate'); workflow.add_edge('validate', 'certify'); workflow.add_edge('certify', END); graph = workflow.compile()