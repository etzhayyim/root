from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabHeaterState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_heater(state: LabHeaterState): specs = state['specs']; is_valid = specs.get('temp_max', 0) > 0 and 'safety_cert' in specs; return {'valid': is_valid, 'log': ['Validation complete']}; graph = StateGraph(LabHeaterState); graph.add_node('validate', validate_heater); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()