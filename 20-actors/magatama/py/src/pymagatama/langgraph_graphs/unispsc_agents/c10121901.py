from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class BreedingState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_spec(state: BreedingState): return {'validation_results': ['Spec check passed'], 'status': 'validated'}; def quarantine_check(state: BreedingState): return {'validation_results': ['Quarantine passed'], 'status': 'ready'}; builder = StateGraph(BreedingState); builder.add_node('validate', validate_spec); builder.add_node('quarantine', quarantine_check); builder.set_entry_point('validate'); builder.add_edge('validate', 'quarantine'); builder.add_edge('quarantine', END); graph = builder.compile()