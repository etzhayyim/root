from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_specs: dict; validation_results: List[str]; graph = StateGraph(ProcurementState); def validate_load(state: ProcurementState): state['validation_results'].append('Load capacity verified against standard') if state['vendor_specs'].get('load_kg', 0) > 5 else state['validation_results'].append('Load capacity insufficient'); return state; graph.add_node('validate_spec', validate_load); graph.set_entry_point('validate_spec'); graph.add_edge('validate_spec', END); graph = graph.compile()