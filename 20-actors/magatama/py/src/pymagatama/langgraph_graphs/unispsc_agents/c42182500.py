from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NasalDeviceState(TypedDict): device_id: str; calibration_status: bool; validation_errors: List[str]; def check_calibration(state: NasalDeviceState): return {'calibration_status': True} if state.get('device_id') else {'calibration_status': False}; def validate_specs(state: NasalDeviceState): return {'validation_errors': []} if state['calibration_status'] else {'validation_errors': ['Missing Calibration']}; graph = StateGraph(NasalDeviceState); graph.add_node('calibrate', check_calibration); graph.add_node('validate', validate_specs); graph.set_entry_point('calibrate'); graph.add_edge('calibrate', 'validate'); graph.add_edge('validate', END); graph = graph.compile()