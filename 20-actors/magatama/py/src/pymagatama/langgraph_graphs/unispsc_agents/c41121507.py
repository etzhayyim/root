from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PipetteState(TypedDict): device_id: str; specs: dict; validation_results: Annotated[list, operator.add]; def validate_specs(state: PipetteState): print(f'Validating specs for {state[device_id]}'); return {'validation_results': ['ISO 8655 check passed']}; def perform_calibration(state: PipetteState): print('Scheduling calibration routine'); return {'validation_results': ['Calibration protocol initiated']}; graph = StateGraph(PipetteState); graph.add_node('validate', validate_specs); graph.add_node('calibrate', perform_calibration); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); graph.set_entry_point('validate'); graph = graph.compile()