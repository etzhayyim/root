from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CreamerState(TypedDict): vendor_certs: List[str]; ingredient_check: bool; stock_status: str; def validate_cert(state: CreamerState): return {'ingredient_check': 'HACCP' in state['vendor_certs']}; def check_inventory(state: CreamerState): return {'stock_status': 'verified' if state['ingredient_check'] else 'rejected'}; graph = StateGraph(CreamerState); graph.add_node('cert_validation', validate_cert); graph.add_node('inventory_check', check_inventory); graph.add_edge('cert_validation', 'inventory_check'); graph.add_edge('inventory_check', END); graph.set_entry_point('cert_validation'); graph.compile()