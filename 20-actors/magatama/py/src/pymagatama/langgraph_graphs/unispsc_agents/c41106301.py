from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_data: dict; validation_errors: List[str]; def validate_purity(state: ReagentState): if state['reagent_data'].get('purity', 0) < 99.0: state['validation_errors'].append('Purity below 99%'); return state; def check_storage(state: ReagentState): if state['reagent_data'].get('temp') != '-20C': state['validation_errors'].append('Incorrect storage temp'); return state; graph = StateGraph(ReagentState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_storage', check_storage); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_storage'); graph.add_edge('check_storage', END); graph = graph.compile()