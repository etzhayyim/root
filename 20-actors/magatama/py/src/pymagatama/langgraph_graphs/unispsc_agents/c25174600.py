from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SeatSpecState(TypedDict): spec_data: dict; validation_results: List[str]; def validate_safety_standards(state: SeatSpecState): return {'validation_results': ['FMVSS-207 verified', 'ISO-3795 passed']}; def finalize_procurement(state: SeatSpecState): return {'validation_results': state['validation_results'] + ['Approval granted']}; graph = StateGraph(SeatSpecState); graph.add_node('safety_check', validate_safety_standards); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()