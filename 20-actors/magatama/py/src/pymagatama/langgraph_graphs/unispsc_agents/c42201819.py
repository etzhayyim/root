from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilmHangerState(TypedDict): items: List[dict]; validation_status: bool; inventory_check: bool; def validate_hanger_specs(state: FilmHangerState): state['validation_status'] = all('material' in item for item in state['items']); return state; def check_compatibility(state: FilmHangerState): state['inventory_check'] = True; return state; graph = StateGraph(FilmHangerState); graph.add_node('validate', validate_hanger_specs); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()