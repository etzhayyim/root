from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RoboticsState(TypedDict): specs: dict; validation_results: List[str]; def validate_motor_specs(state: RoboticsState): specs = state.get('specs', {}); results = []; if specs.get('torque', 0) < 0.1: results.append('Torque below threshold'); return {'validation_results': results}; graph = StateGraph(RoboticsState); graph.add_node('validate', validate_motor_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()