from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShredderProcessState(TypedDict): model_specs: dict; compliance_check: bool; graph = StateGraph(ShredderProcessState); def validate_specs(state): return {'compliance_check': state['model_specs'].get('security_level') == 'P-4'}; def hardware_check(state): return {'compliance_check': True}; graph.add_node('spec_validation', validate_specs); graph.add_node('safety_check', hardware_check); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()