from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): unit_id: str; specs: dict; approved: bool; def validate_specs(state: State): state['approved'] = 'sterilization_cert' in state['specs'] and state['specs']['flow_rate'] > 0; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()