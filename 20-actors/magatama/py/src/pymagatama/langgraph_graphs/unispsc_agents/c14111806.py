from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; class PaperProcureState(TypedDict): content: str; specs: dict; validated: bool; def validate_paper(state: PaperProcureState): state['validated'] = 'gsm_weight' in state['specs'] and state['specs']['gsm_weight'] >= 70; return state; graph = StateGraph(PaperProcureState); graph.add_node('validate', validate_paper); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()