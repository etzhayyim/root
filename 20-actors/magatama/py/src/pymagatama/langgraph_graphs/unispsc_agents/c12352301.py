from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalProcurementState(TypedDict): chemical_code: str; purity_check_passed: bool; risk_assessment: list[str]; def validate_purity(state: ChemicalProcurementState) -> dict: return {'purity_check_passed': True}; def assess_risk(state: ChemicalProcurementState) -> dict: return {'risk_assessment': ['dual-use-export-control', 'standard']}; builder = StateGraph(ChemicalProcurementState); builder.add_node('validate_purity', validate_purity); builder.add_node('assess_risk', assess_risk); builder.set_entry_point('validate_purity'); builder.add_edge('validate_purity', 'assess_risk'); builder.add_edge('assess_risk', END); graph = builder.compile()