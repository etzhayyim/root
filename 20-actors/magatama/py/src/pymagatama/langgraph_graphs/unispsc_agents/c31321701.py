from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: AssemblyState): state['valid'] = 'alloy' in state['specs'] and 'tensile' in state['specs']; state['log'].append('Validation complete'); return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()