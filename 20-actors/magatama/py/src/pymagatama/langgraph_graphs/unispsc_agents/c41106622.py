from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VectorState(TypedDict): vector_id: str; sequence_data: str; validation_passed: bool; def validate_sequence(state: VectorState): state['validation_passed'] = len(state['sequence_data']) > 0; return state; def check_compliance(state: VectorState): return 'compliance_check' if state['validation_passed'] else 'error'; graph = StateGraph(VectorState); graph.add_node('validate', validate_sequence); graph.add_node('compliance_check', lambda x: x); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()