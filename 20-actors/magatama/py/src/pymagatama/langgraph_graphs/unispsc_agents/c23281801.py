from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RackState(TypedDict): rack_id: str; specs: dict; verified: bool; def validate_specs(state: RackState): state['verified'] = all(k in state['specs'] for k in ['material', 'load']); return state; def route_verification(state: RackState): return 'verified' if state['verified'] else END; graph = StateGraph(RackState); graph.add_node('validate', validate_specs); graph.add_edge('__start__', 'validate'); graph.add_edge('validate', END); graph = graph.compile()