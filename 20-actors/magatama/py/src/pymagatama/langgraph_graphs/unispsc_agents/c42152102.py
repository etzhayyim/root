from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): model_id: str; spec_data: dict; approved: bool; def validate_specs(state: DentalState): state['approved'] = 'ISO13485' in state['spec_data'].get('certs', []); return state; def check_compliance(state: DentalState): return 'compliance_check' if state['approved'] else END; graph = StateGraph(DentalState); graph.add_node('validate', validate_specs); graph.add_node('compliance_check', lambda s: s); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()