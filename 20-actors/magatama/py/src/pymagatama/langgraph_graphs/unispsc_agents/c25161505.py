from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BikeState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: BikeState): errors = []; if not state['specs'].get('iso_safety_standards'): errors.append('Missing ISO safety certification'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(BikeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()