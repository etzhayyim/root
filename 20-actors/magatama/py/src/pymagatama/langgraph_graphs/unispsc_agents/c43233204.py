from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkSecurityState(TypedDict): vendor: str; security_specs: dict; validated: bool; def validate_security_config(state: NetworkSecurityState): state['validated'] = 'AES-256' in state['security_specs'].get('encryption', '') and state['security_specs'].get('protocol') == 'IPsec'; return state; graph = StateGraph(NetworkSecurityState); graph.add_node('validate_config', validate_security_config); graph.set_entry_point('validate_config'); graph.add_edge('validate_config', END); app = graph.compile()