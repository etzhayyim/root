from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SewingNeedleState(TypedDict): needle_specs: dict; validation_results: List[str]; graph = StateGraph(SewingNeedleState); def validate_specs(state: SewingNeedleState): return {'validation_results': ['Material check passed', 'Gauge standard verified']}; def check_compliance(state: SewingNeedleState): return {'validation_results': state['validation_results'] + ['Safety compliance confirmed']}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()