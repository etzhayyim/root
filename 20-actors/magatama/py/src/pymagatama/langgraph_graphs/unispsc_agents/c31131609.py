from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): document_id: str; compliance_checks: List[str]; approved: bool; def validate_beryllium_specs(state: ForgingState): state['compliance_checks'].append('Beryllium Handling Protocol Validated'); state['approved'] = True; return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_beryllium_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()