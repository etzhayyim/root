from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_name: str; purity: float; regulatory_status: bool; workflow_log: List[str]; def validate_purity(state: ProcState): state['workflow_log'].append('Validating purity levels'); return {'purity': 0.999} if state['purity'] >= 0.99 else {'purity': 0.0}; def check_compliance(state: ProcState): state['workflow_log'].append('Checking drug regulatory compliance'); return {'regulatory_status': True}; graph = StateGraph(ProcState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()