from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class WaterSoftenerState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: WaterSoftenerState): result = 'Valid' if state['specs'].get('pressure_rating') else 'Invalid'; return {'validation_results': [result], 'status': 'Validated'}; graph = StateGraph(WaterSoftenerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()