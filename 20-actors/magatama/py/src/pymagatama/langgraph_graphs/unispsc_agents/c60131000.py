from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InstrumentState(TypedDict): instrument_data: dict; validation_errors: List[str]; def validate_instrument(state: InstrumentState): errors = []; if not state['instrument_data'].get('keys'): errors.append('Missing key count'); return {'validation_errors': errors}; def finalize_procurement(state: InstrumentState): return {'instrument_data': state['instrument_data'], 'status': 'READY'}; graph = StateGraph(InstrumentState); graph.add_node('validate', validate_instrument); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()