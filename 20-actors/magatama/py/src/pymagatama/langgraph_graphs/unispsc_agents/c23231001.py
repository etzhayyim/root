from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): model_spec: str; validation_status: str; def validate_specs(state: ProcureState): return {'validation_status': 'passed' if 'power' in state['model_spec'] else 'failed'}; graph = StateGraph(ProcureState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()