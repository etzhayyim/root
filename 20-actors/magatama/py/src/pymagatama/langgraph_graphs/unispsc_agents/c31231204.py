from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BronzeState(TypedDict): metal_grade: str; dimensions: str; needs_inspection: bool; status: str; def validate_specs(state: BronzeState): return {'status': 'VALIDATED' if state['metal_grade'] else 'REJECTED'}; def perform_inspection(state: BronzeState): return {'status': 'INSPECTED' if state['needs_inspection'] else 'SKIPPED'}; graph = StateGraph(BronzeState); graph.add_node('validate', validate_specs); graph.add_node('inspect', perform_inspection); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()