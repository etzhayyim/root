from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: PumpState): specs = state['specs']; is_valid = all(k in specs for k in ['flow', 'pressure']); return {'valid': is_valid, 'error': '' if is_valid else 'Missing mandatory specs'}; builder = StateGraph(PumpState); builder.add_node('validator', validate_specs); builder.set_entry_point('validator'); builder.add_edge('validator', END); graph = builder.compile()