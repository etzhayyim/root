from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BookState(TypedDict): title: str; isbn: str; valid: bool; def validate_metadata(state: BookState): state['valid'] = len(state['isbn']) == 13; return state; graph = StateGraph(BookState); graph.add_node('validate', validate_metadata); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()