from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BlindProcurementState(TypedDict): specs: dict; validation_passed: bool; issues: List[str]; def validate_specs(state: BlindProcurementState): required = ['fire_retardancy', 'dimensions']; state['issues'] = [f for f in required if f not in state.get('specs', {})]; state['validation_passed'] = len(state['issues']) == 0; return state; graph = StateGraph(BlindProcurementState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()