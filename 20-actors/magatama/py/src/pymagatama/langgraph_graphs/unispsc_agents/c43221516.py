from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MonitorState(TypedDict): input_data: dict; validation_results: List[str]; status: str; def validate_specs(state: MonitorState): results = []; if 'voltage' not in state['input_data']: results.append('Missing voltage specs'); return {'validation_results': results, 'status': 'validating'}; def process_monitoring(state: MonitorState): return {'status': 'configured'}; graph = StateGraph(MonitorState); graph.add_node('validate', validate_specs); graph.add_node('process', process_monitoring); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()