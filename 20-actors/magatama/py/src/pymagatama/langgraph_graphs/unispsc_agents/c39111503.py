from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FixtureState(TypedDict): fixture_type: str; specs: dict; valid: bool; def validate_specs(state: FixtureState): state['valid'] = 'Voltage' in state['specs'] and 'IP rating' in state['specs']; return state; builder = StateGraph(FixtureState); builder.add_node('validation', validate_specs); builder.set_entry_point('validation'); builder.add_edge('validation', END); graph = builder.compile()