from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SwimwearState(TypedDict): specs: dict; compliance_report: str; approved: bool; def check_compliance(state: SwimwearState): state['approved'] = state['specs'].get('uv_protection', 0) >= 50; state['compliance_report'] = 'Approved' if state['approved'] else 'Rejected'; return state; graph = StateGraph(SwimwearState); graph.add_node('compliance', check_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', END); graph = graph.compile()