from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FerryState(TypedDict): vessel_id: str; compliance_docs: List[str]; inspection_status: bool; def check_imo_compliance(state: FerryState): state['compliance_docs'].append('IMO_VERIFIED'); state['inspection_status'] = True; return state; graph = StateGraph(FerryState); graph.add_node('verify', check_imo_compliance); graph.set_entry_point('verify'); graph.add_edge('verify', END); graph = graph.compile()