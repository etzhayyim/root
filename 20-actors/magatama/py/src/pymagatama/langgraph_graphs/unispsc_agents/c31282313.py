from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_spec: dict; validation_results: list; status: str; def validate_specs(state: ComponentState): cert = state['component_spec'].get('cert'); return {'validation_results': ['PASS'] if cert == 'AS9100' else ['FAIL'], 'status': 'VALIDATED'}; graph = StateGraph(ComponentState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()