from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TherapyState(TypedDict): device_id: str; compliance_checks: Annotated[list, operator.add]; status: str; def validate_medical_device(state: TherapyState): print(f'Validating medical device: {state["device_id"]}'); return {'compliance_checks': ['license_verified', 'iso_13485_verified'], 'status': 'validated'}; def finalize_procurement(state: TherapyState): print('Finalizing procurement documentation.'); return {'status': 'approved'}; graph = StateGraph(TherapyState); graph.add_node('validate', validate_medical_device); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()