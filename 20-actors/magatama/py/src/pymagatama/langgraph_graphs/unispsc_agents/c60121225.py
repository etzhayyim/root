from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WatercolorState(TypedDict): medium_data: dict; compliance_checks: List[str]; graph = StateGraph(WatercolorState); def validate_medium(state: WatercolorState): return {'compliance_checks': ['ASTM_D4236_Verified'] if state['medium_data'].get('toxicity_free') else ['Review_Warning']}; def finalize_spec(state: WatercolorState): return {'compliance_checks': state['compliance_checks'] + ['Specs_Finalized']}; graph.add_node('validate', validate_medium); graph.add_node('finalize', finalize_spec); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()