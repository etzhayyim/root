from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StethoscopeState(TypedDict): specs: dict; valid: bool; audit_log: List[str]; def validate_medical_specs(state: StethoscopeState): state['valid'] = 'material_composition' in state['specs'] and 'compliance_certification' in state['specs']; state['audit_log'].append('Validation completed'); return state; graph = StateGraph(StethoscopeState); graph.add_node('validate', validate_medical_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()