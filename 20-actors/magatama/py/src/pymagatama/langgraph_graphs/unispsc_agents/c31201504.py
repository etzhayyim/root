from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CarpetTapeState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: CarpetTapeState): s = state['specs']; errors = []; if s.get('adhesion', 0) < 10: errors.append('Adhesion insufficient'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(CarpetTapeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()