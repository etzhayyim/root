from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class NetworkState(TypedDict): equipment_id: str; spec_compliance: bool; validation_log: List[str]; def validate_hardware(state: NetworkState): state['validation_log'].append('Verifying GPRS compliance'); state['spec_compliance'] = True; return state; def check_export_controls(state: NetworkState): state['validation_log'].append('Checking dual-use export status'); return state; graph = StateGraph(NetworkState); graph.add_node('validate', validate_hardware); graph.add_node('export', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()