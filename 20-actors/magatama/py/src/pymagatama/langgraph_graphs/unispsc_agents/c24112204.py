from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PailState(TypedDict): bucket_id: str; gauge: float; un_rating: str; status: str; def validate_spec(state: PailState): return {'status': 'PASS' if state['gauge'] >= 0.5 and state['un_rating'] == 'certified' else 'FAIL'}; graph = StateGraph(PailState); graph.add_node('validation', validate_spec); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()