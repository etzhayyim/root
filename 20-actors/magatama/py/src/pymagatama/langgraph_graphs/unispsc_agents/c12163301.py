from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): input_data: dict; validation_errors: Annotated[Sequence[str], operator.add]; status: str; def validate_catalyst(state: CatalystState): errors = []; if state['input_data'].get('purity', 0) < 99.0: errors.append('Insufficient purity'); return {'validation_errors': errors, 'status': 'validated' if not errors else 'rejected'}; def process_catalyst(state: CatalystState): return {'status': 'processed'}; graph = StateGraph(CatalystState); graph.add_node('validate', validate_catalyst); graph.add_node('process', process_catalyst); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()