from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): component_specs: dict; validation_passed: bool; def check_specs(state: State): return {'validation_passed': 'material_grade' in state['component_specs']}; graph = StateGraph(State); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()