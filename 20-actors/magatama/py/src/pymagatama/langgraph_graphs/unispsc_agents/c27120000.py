from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class HydraulicState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: HydraulicState): return {'validation_results': ['Pressure standard met', 'Material check passed']}; def perform_export_check(state: HydraulicState): return {'validation_results': state['validation_results'] + ['Export control clearance obtained']}; graph = StateGraph(HydraulicState); graph.add_node('validate', validate_specs); graph.add_node('export_check', perform_export_check); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()