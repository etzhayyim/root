from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; purity: float; compliance_check: bool; def validate_integrity(state: DrugState): return {'compliance_check': state['purity'] > 0.99}; def audit_protocol(state: DrugState): return {'compliance_check': True}; graph = StateGraph(DrugState); graph.add_node('validate', validate_integrity); graph.add_node('audit', audit_protocol); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); graph.set_entry_point('validate'); graph = graph.compile()