from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeWrenchState(TypedDict): specs: dict; quality_passed: bool; inspection_logs: List[str]; def validate_specs(state: PipeWrenchState): state['quality_passed'] = state['specs'].get('hardness', 0) > 50; return state; def log_results(state: PipeWrenchState): state['inspection_logs'].append('Verification complete'); return state; graph = StateGraph(PipeWrenchState); graph.add_node('validate', validate_specs); graph.add_node('log', log_results); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph = graph.compile()