from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetyValveState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: SafetyValveState): s = state['specs']; is_valid = 'pressure_rating' in s and 'material' in s; return {'valid': is_valid, 'error_log': [] if is_valid else ['Missing critical specs']}; graph = StateGraph(SafetyValveState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()