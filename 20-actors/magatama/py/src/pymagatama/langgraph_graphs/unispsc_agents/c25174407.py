from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PedalState(TypedDict): pedal_specs: dict; validation_results: Annotated[list, operator.add]; def validate_specs(state: PedalState): results = ['Passed' if state['pedal_specs'].get('operating_force') > 0 else 'Failed']; return {'validation_results': results}; graph = StateGraph(PedalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()