from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MinneolaState(TypedDict): concentrate_id: str; quality_metrics: dict; inspection_status: bool; def validate_quality(state: MinneolaState): print('Validating Brix and Acidity...'); state['inspection_status'] = state['quality_metrics'].get('brix', 0) > 50; return state; def route_procurement(state: MinneolaState): return 'complete' if state['inspection_status'] else 'reject'; graph = StateGraph(MinneolaState); graph.add_node('validate', validate_quality); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()