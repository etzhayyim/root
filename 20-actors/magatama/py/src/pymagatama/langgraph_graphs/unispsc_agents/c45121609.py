from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CameraBracketState(TypedDict): bracket_spec: dict; validation_results: List[str]; graph = StateGraph(CameraBracketState); def validate_specs(state: CameraBracketState): result = []; if state['bracket_spec'].get('load_capacity') < 5: result.append('Insufficient load rating'); return {'validation_results': result}; def route_procurement(state: CameraBracketState): return 'end'; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()