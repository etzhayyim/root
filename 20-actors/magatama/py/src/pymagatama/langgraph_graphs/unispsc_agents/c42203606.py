from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TeleradState(TypedDict): device_specs: dict; compliance: bool; image_data: bytes; graph = StateGraph(TeleradState); def validate_dicom(state: TeleradState): state['compliance'] = True; return state; def check_regulatory(state: TeleradState): state['compliance'] = state.get('device_specs', {}).get('fda_cleared', False); return state; graph.add_node('validate', validate_dicom); graph.add_node('check_reg', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_reg'); graph.add_edge('check_reg', END); compiled_graph = graph.compile()