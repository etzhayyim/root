from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): items: List[str]; validation_status: bool; def validate_specs(state: SpecState): state['validation_status'] = all(len(i) > 0 for i in state['items']); return state; def check_medical_compliance(state: SpecState): return {'validation_status': True}; graph = StateGraph(SpecState); graph.add_node('validate', validate_specs); graph.add_node('medical_check', check_medical_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'medical_check'); graph.add_edge('medical_check', END); compiled_graph = graph.compile()