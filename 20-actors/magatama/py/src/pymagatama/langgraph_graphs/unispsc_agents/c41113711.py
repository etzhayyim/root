from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class NetworkAnalyzerState(TypedDict): specs: dict; validation_status: bool; compliance_report: str; def validate_specs(state: NetworkAnalyzerState): state['validation_status'] = 'frequency_range_ghz' in state['specs']; return state; def check_export_control(state: NetworkAnalyzerState): state['compliance_report'] = 'Export Controlled' if state['specs'].get('high_end') else 'Standard'; return state; graph = StateGraph(NetworkAnalyzerState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()