from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EngineSleeveState(TypedDict): specs: dict; validation_results: List[str]; def validate_sleeve_specs(state: EngineSleeveState): results = []; required = ['material', 'tolerance']; for field in required: if field not in state['specs']: results.append(f'Missing: {field}'); return {'validation_results': results}; graph = StateGraph(EngineSleeveState); graph.add_node('validate', validate_sleeve_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()