from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TuningForkCaseState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: TuningForkCaseState): errors = []; if state['specs'].get('material') not in ['ABS', 'Polypropylene']: errors.append('Invalid material'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(TuningForkCaseState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()