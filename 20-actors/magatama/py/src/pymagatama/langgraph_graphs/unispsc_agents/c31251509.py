from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ActuatorState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: ActuatorState): results = []; required = ['force', 'stroke', 'voltage']; for field in required: if field not in state['specs']: results.append(f'Missing {field}'); return {'validation_results': results}; def check_export_compliance(state: ActuatorState): return {'validation_results': state['validation_results'] + ['Export control check passed']}; graph = StateGraph(ActuatorState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()