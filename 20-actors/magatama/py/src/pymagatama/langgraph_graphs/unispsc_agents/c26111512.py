from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AxleState(TypedDict): axle_id: str; specifications: dict; validation_status: bool; def validate_axle(state: AxleState): state['validation_status'] = 'load' in state['specifications'] and 'material' in state['specifications']; return state; def check_export_control(state: AxleState): print(f'Checking export status for {state['axle_id']}'); return state; graph = StateGraph(AxleState); graph.add_node('validate', validate_axle); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()