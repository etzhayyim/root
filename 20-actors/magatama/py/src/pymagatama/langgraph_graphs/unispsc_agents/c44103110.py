from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PrintHeadState(TypedDict): model: str; specs: dict; verified: bool; def validate_compatibility(state: PrintHeadState): state['verified'] = state['model'].startswith('PH-'); return state; graph = StateGraph(PrintHeadState); graph.add_node('validate', validate_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()