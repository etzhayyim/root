from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ExhaustState(TypedDict): specs: dict; validation_log: list[str]; status: str; def validate_specs(state: ExhaustState): return {'validation_log': ['Specs compliance checked']}; def check_safety(state: ExhaustState): return {'status': 'READY' if state['specs'].get('explosion_proof') else 'REJECTED'}; graph = StateGraph(ExhaustState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); app = graph.compile()