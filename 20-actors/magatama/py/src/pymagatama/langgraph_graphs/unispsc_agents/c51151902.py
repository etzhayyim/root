from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): chemical_data: dict; valid: bool; def validate_purity(state: ProcState): state['valid'] = state['chemical_data'].get('purity', 0) > 98.0; return state; def check_regulations(state: ProcState): if state['valid']: print('Compliance check passed'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()