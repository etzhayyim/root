from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ContactState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_conductivity(state: ContactState): conductivity = state['specs'].get('conductivity'); state['valid'] = conductivity > 90.0 if conductivity else False; return state; def check_compliance(state: ContactState): state['error_log'] = [] if state['valid'] else ['Conductivity below threshold']; return state; graph = StateGraph(ContactState); graph.add_node('validate', validate_conductivity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()