from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RaspState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_spec(state: RaspState): return {'validation_results': ['Material check complete', 'Hardness check complete'], 'status': 'validated'}; def compile_graph(): graph = StateGraph(RaspState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()