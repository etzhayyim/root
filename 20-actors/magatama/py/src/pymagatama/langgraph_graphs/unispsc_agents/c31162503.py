from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BracesState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: BracesState): return {'validation_report': 'Validated structural integrity requirements.'}; def check_inventory(state: BracesState): return {'validation_report': 'Inventory verified.'}; graph = StateGraph(BracesState); graph.add_node('validate', validate_specs); graph.add_node('inventory', check_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph = graph.compile()