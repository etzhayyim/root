from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableState(TypedDict): specs: dict; valid: bool; cable_graph = StateGraph(CableState); def validate_specs(state: CableState): state['valid'] = all(k in state['specs'] for k in ['diameter_mm', 'conductor_material']); return state; cable_graph.add_node('validator', validate_specs); cable_graph.set_entry_point('validator'); cable_graph.add_edge('validator', END); graph = cable_graph.compile()