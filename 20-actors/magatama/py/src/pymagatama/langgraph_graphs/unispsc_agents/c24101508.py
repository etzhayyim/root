from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CreaperState(TypedDict): specs: dict; valid: bool; def validate_specs(state: CreaperState): state['valid'] = state['specs'].get('weight_capacity_kg', 0) > 100; return state; graph = StateGraph(CreaperState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()