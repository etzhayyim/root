from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SterilizationState(TypedDict): wrap_specs: dict; compliance_verified: bool; def validate_bfe(state: SterilizationState): state['compliance_verified'] = state['wrap_specs'].get('bfe', 0) > 99.9; return state; def check_compatibility(state: SterilizationState): return {'compliance_verified': state['wrap_specs'].get('method') in ['steam', 'eo_gas']}; graph = StateGraph(SterilizationState); graph.add_node('validate', validate_bfe); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()