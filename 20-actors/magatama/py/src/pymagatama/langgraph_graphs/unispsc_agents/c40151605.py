from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: CompressorState): errors = []; if state['specs'].get('power', 0) <= 0: errors.append('Invalid power'); return {'validation_results': errors, 'status': 'validated' if not errors else 'failed'}; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()