from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(ProcurementState); def validate_case_standards(state: ProcurementState): return {'validation_results': {'passed': True, 'compliance_check': 'ISO-13485'}}; def finalize_procurement(state: ProcurementState): return {'validation_results': {'status': 'approved'}}; graph.add_node('validate', validate_case_standards); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()