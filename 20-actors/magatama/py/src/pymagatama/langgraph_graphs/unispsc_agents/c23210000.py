from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input_spec: dict; validation_report: list; status: str; def validate_specs(state: ProcState): validated = True if 'iso_certification_standard' in state['input_spec'] else False; return {'validation_report': ['ISO validation passed'] if validated else ['ISO validation failed']}; def classify_risk(state: ProcState): return {'status': 'HIGH_PRIORITY' if state['input_spec'].get('dual_use') else 'STANDARD'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('risk_assessment', classify_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'risk_assessment'); graph.add_edge('risk_assessment', END); graph = graph.compile()