from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BarrierState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: BarrierState): errors = []; if state['specs'].get('barrier_arm_length_meters', 0) > 10: errors.append('Arm exceeds max safe operation length'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(BarrierState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()