from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ArrowState(TypedDict): specs: dict; compliance: bool; valid: bool; def validate_specs(state: ArrowState): state['valid'] = all(k in state['specs'] for k in ['spine', 'weight']); return state; def check_export(state: ArrowState): state['compliance'] = True; return state; graph = StateGraph(ArrowState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()