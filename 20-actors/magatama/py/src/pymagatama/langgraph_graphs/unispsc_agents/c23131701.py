from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; valid: bool; robot_type: str; def validate_specs(state: RobotState): state['valid'] = state['specs'].get('load', 0) > 0; return state; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()