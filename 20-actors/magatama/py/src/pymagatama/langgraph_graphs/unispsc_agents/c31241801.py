from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class FilterSpec(TypedDict): center_wavelength: float; bandwidth: float; validation_status: str; def validate_specs(state: FilterSpec): is_valid = state['center_wavelength'] > 0 and state['bandwidth'] > 0; state['validation_status'] = 'COMPLIANT' if is_valid else 'REJECTED'; return state; graph = StateGraph(FilterSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()