from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FreezerState(TypedDict): specs: dict; validation_results: List[str]; def validate_temperature_ctrl(state: FreezerState): state['validation_results'].append('Temp control verified'); return state; def check_regulatory_compliance(state: FreezerState): state['validation_results'].append('Health standard passed'); return state; graph = StateGraph(FreezerState); graph.add_node('validate_temp', validate_temperature_ctrl); graph.add_node('check_regs', check_regulatory_compliance); graph.set_entry_point('validate_temp'); graph.add_edge('validate_temp', 'check_regs'); graph.add_edge('check_regs', END); graph = graph.compile()