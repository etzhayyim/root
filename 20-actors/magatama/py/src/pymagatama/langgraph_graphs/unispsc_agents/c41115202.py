from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FeedHornState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_rf_specs(state: FeedHornState): state['valid'] = state['specs'].get('vswr', 2.0) <= 1.5; state['logs'].append('RF specs validated'); return state; def check_export_control(state: FeedHornState): state['logs'].append('Dual-use review initiated'); return state; graph = StateGraph(FeedHornState); graph.add_node('validate', validate_rf_specs); graph.add_node('export', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); app = graph.compile()