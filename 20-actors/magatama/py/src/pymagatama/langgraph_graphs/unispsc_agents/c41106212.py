from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MediaState(TypedDict): ingredients: List[str]; validation_status: bool; def validate_purity(state: MediaState): return {'validation_status': True} if all(['purity' in i for i in state['ingredients']]) else {'validation_status': False}; def final_approval(state: MediaState): return state; graph = StateGraph(MediaState); graph.add_node('validate', validate_purity); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()