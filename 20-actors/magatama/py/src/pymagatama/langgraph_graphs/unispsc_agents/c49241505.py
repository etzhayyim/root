from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlaygroundState(TypedDict): safety_docs: List[str]; inspection_status: str; compliance: bool; def validate_safety(state: PlaygroundState): state['compliance'] = all(doc in state['safety_docs'] for doc in ['EN1176', 'ASTM_F1487']); return state; def finalize_order(state: PlaygroundState): state['inspection_status'] = 'PENDING_ON_SITE_ASSEMBLY'; return state; graph = StateGraph(PlaygroundState); graph.add_node('validate', validate_safety); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()