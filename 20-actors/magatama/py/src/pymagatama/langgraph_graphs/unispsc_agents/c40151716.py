from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class PumpState(TypedDict): spec: dict; validation_report: list; graph = StateGraph(PumpState); def validate_specs(state: PumpState): spec = state.get('spec', {}); report = []; required_fields = ['material', 'pressure', 'flow_rate']; for field in required_fields: if field not in spec: report.append(f'Missing: {field}'); return {'validation_report': report}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()