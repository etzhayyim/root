from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoardSpecState(TypedDict): thickness: float; material: str; approved: bool; def validate_board(state: BoardSpecState): state['approved'] = state['thickness'] > 0.5; return state; graph = StateGraph(BoardSpecState); graph.add_node('validate', validate_board); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()