from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; hazard_verified: bool; compliance_docs: List[str]; def validate_purity(state: PharmState): state['purity'] = state['purity'] if state['purity'] >= 0.99 else 0.0; return state; def check_compliance(state: PharmState): state['hazard_verified'] = True; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()