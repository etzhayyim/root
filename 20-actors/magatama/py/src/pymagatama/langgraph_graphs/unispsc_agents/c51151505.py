from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_name: str; purities: List[float]; valid: bool; def validate_integrity(state: ProcState) -> ProcState: state['valid'] = all(p >= 99.0 for p in state['purities']); return state; def check_compliance(state: ProcState) -> ProcState: return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_integrity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()