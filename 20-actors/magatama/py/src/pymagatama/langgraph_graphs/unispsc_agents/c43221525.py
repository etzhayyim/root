from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class IntercomState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; def validate_intercom_specs(state: IntercomState): validation = True if 'PoE' in state['specs'] else False; return {'validation_results': ['PoE requirement checked: ' + str(validation)]}; def finalize_procurement(state: IntercomState): return {'validation_results': ['Workflow complete']}; graph = StateGraph(IntercomState); graph.add_node('validate', validate_intercom_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()