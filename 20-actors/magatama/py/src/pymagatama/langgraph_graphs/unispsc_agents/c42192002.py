from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TableAccessoryState(TypedDict): accessory_type: str; specs: dict; compliance_check: bool; def validate_specs(state: TableAccessoryState): state['compliance_check'] = 'medical_device_class' in state['specs'] and 'weight_capacity_kg' in state['specs']; return state; builder = StateGraph(TableAccessoryState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()