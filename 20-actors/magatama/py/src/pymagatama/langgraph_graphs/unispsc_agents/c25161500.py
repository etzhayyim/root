from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VehicleState(TypedDict): vehicle_type: str; specs: dict; validation_passed: bool; def validate_pedal_specs(state: VehicleState): state['validation_passed'] = 'brake_system' in state['specs'] and 'frame_material' in state['specs']; return state; def finalize_order(state: VehicleState): print('Order finalized for', state['vehicle_type']); return state; graph = StateGraph(VehicleState); graph.add_node('validate', validate_pedal_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()