from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_report: list; graph = StateGraph(State); def validate_electrical_specs(state: State): return {'validation_report': ['Spec checked: AWG and Voltage compliance']}; def check_regulatory(state: State): return {'validation_report': state['validation_report'] + ['Regulatory compliance: Passed']}; graph.add_node('specs', validate_electrical_specs); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('specs'); graph.add_edge('specs', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()