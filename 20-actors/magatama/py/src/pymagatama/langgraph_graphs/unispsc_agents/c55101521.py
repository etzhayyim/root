from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ManualState(TypedDict): content: str; validated: bool; errors: List[str]; def validate_manual(state: ManualState): state['validated'] = len(state['content']) > 100; return {'validated': state['validated']}; graph = StateGraph(ManualState); graph.add_node('validate', validate_manual); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()