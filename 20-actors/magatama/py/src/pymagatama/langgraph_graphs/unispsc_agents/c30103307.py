from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BilletState(TypedDict): billet_specs: dict; validation_results: List[str]; def validate_chemical_composition(state: BilletState): state['validation_results'].append('Chemistry Validated'); return state; def check_dimensional_accuracy(state: BilletState): state['validation_results'].append('Dimensions Validated'); return state; graph = StateGraph(BilletState); graph.add_node('chem_check', validate_chemical_composition); graph.add_node('dim_check', check_dimensional_accuracy); graph.set_entry_point('chem_check'); graph.add_edge('chem_check', 'dim_check'); graph.add_edge('dim_check', END); graph = graph.compile()