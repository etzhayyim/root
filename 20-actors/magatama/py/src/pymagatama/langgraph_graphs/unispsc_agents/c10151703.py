from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; import operator; class SoybeanState(TypedDict): metadata: Dict; quality_metrics: Dict; inspection_passed: bool; logs: Annotated[List[str], operator.add]; def validate_quality(state: SoybeanState): quality = state.get('quality_metrics', {}); passed = quality.get('moisture', 0) < 14 and quality.get('protein', 0) > 35; return {'inspection_passed': passed, 'logs': ['Quality checked']}; def certify_traceability(state: SoybeanState): return {'inspection_passed': state['inspection_passed'] and 'origin' in state['metadata'], 'logs': ['Traceability certified']}; graph = StateGraph(SoybeanState); graph.add_node('validate', validate_quality); graph.add_node('certify', certify_traceability); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()