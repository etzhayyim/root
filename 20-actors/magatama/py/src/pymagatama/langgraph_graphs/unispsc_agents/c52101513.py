from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CarpetProcureState(TypedDict): specs: dict; validation_passed: bool; report: str; def validate_specs(state: CarpetProcureState): state['validation_passed'] = all(k in state['specs'] for k in ['material', 'fire_rating']); return state; def finalize_review(state: CarpetProcureState): state['report'] = 'Validation complete' if state['validation_passed'] else 'Specs missing'; return state; graph = StateGraph(CarpetProcureState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_review); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()