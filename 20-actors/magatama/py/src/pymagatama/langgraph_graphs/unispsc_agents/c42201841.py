from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class XRayState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: XRayState): compliance = state['specs'].get('ISO_FDA') is not None; return {'validation_report': 'Compliant' if compliance else 'Rejected', 'approved': compliance}; graph = StateGraph(XRayState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()