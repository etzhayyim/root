from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TransducerState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(TransducerState); def validate_specs(state: TransducerState): report = []; missing = [f for f in ['pore_size', 'sterility'] if f not in state['specs']]; report.append('Valid' if not missing else f'Missing: {missing}'); return {'validation_report': report}; graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()