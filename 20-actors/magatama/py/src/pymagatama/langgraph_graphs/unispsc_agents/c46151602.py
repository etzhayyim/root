from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SecurityState(TypedDict): vendor_check: bool; spec_compliance: bool; export_check_passed: bool; def check_export_compliance(state: SecurityState): return { "export_check_passed": True }; def validate_specs(state: SecurityState): return { "spec_compliance": True }; graph = StateGraph(SecurityState); graph.add_node("export_review", check_export_compliance); graph.add_node("spec_validation", validate_specs); graph.set_entry_point("export_review"); graph.add_edge("export_review", "spec_validation"); graph.add_edge("spec_validation", END); graph = graph.compile()