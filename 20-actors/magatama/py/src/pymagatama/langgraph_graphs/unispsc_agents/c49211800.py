from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): equipment_list: List[str]; inspection_results: dict; graph = StateGraph(State); def validate_safety(state: State): state['inspection_results'] = {item: 'Safety Check Passed' for item in state['equipment_list']}; return state; def finalize_procurement(state: State): return {'inspection_results': {**state['inspection_results'], 'status': 'Approved'}}; graph.add_node('validate', validate_safety); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()