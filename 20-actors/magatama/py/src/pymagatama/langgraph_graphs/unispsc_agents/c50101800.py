from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProduceState(TypedDict): items: List[str]; inspection_results: dict; status: str; def validate_freshness(state: ProduceState): state['inspection_results'] = {'fresh': True}; return {'status': 'validated'}; def check_compliance(state: ProduceState): return {'status': 'approved'}; graph = StateGraph(ProduceState); graph.add_node('validate', validate_freshness); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()