from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_spec(state: PipeState): errors = []; if 'material' not in state['specs']: errors.append('Missing Material Grade'); return {'validation_errors': errors}; def compile_graph(): graph = StateGraph(PipeState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()