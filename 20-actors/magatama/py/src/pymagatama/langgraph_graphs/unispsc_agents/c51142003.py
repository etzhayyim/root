from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): assay: float; temp_log: list; valid: bool; def check_purity(state): return {'valid': state.get('assay', 0) > 98.0}; def verify_log(state): return {'valid': all(t < 25 for t in state.get('temp_log', []))}; graph = StateGraph(PharmaState); graph.add_node('check_purity', check_purity); graph.add_node('verify_log', verify_log); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'verify_log'); graph.add_edge('verify_log', END); graph = graph.compile()