from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): status: str; specs: dict; validation_errors: List[str]; def validate_dimensions(state: MoldState): state['status'] = 'DIMENSION_CHECK'; if state['specs'].get('tolerance') == 'standard': return 'valid'; return 'flag'; def run_material_analysis(state: MoldState): state['status'] = 'COMPOSITION_CHECK'; return 'complete'; graph = StateGraph(MoldState); graph.add_node('dim_check', validate_dimensions); graph.add_node('material_check', run_material_analysis); graph.set_entry_point('dim_check'); graph.add_edge('dim_check', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()