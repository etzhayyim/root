from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class APIState(TypedDict): active_ingredient: str; p_level: float; compliance_docs: List[str]; def validate_purity(state: APIState): return {'p_level': 99.9} if state['p_level'] < 99.0 else {'p_level': state['p_level']}; def check_compliance(state: APIState): return {'compliance_docs': ['GMP_CERT', 'COA']}; graph = StateGraph(APIState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_compliance'); graph.add_edge('check_compliance', END); graph = graph.compile()