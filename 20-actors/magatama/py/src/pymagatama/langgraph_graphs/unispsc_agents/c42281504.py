from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): specs: dict; valid: bool; def validate_sterilizer(state: State): state['valid'] = 50 <= state['specs'].get('temp', 0) <= 300; return state; graph = StateGraph(State); graph.add_node('validate', validate_sterilizer); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()