from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BomberState(TypedDict): aircraft_id: str; compliance_checks: List[str]; approved: bool; def validate_security(state: BomberState): state['compliance_checks'].append('security_cleared'); return state; def check_export_control(state: BomberState): state['compliance_checks'].append('export_verified'); state['approved'] = True; return state; graph = StateGraph(BomberState); graph.add_node('security', validate_security); graph.add_node('export', check_export_control); graph.add_edge('security', 'export'); graph.add_edge('export', END); graph.set_entry_point('security'); graph = graph.compile()