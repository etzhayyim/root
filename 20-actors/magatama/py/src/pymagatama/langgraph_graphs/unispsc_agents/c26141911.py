from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CalibratorState(TypedDict): device_specs: dict; compliance_status: bool; validation_log: List[str]; def validate_radiation_safety(state: CalibratorState): state['validation_log'].append('Checking shielding specs...'); return {'compliance_status': True}; def update_calibration_records(state: CalibratorState): state['validation_log'].append('Updating traceability matrix.'); return {'compliance_status': True}; graph = StateGraph(CalibratorState); graph.add_node('safety_check', validate_radiation_safety); graph.add_node('calibration_update', update_calibration_records); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'calibration_update'); graph.add_edge('calibration_update', END); graph = graph.compile()