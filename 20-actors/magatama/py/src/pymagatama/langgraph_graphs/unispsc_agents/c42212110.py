from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): device_specs: dict; validation_results: List[str]; def validate_ergonomics(state: State): results = state.get('validation_results', []); results.append('Ergonomics check passed'); return {'validation_results': results}; def check_compatibility(state: State): results = state.get('validation_results', []); results.append('Compatibility check passed'); return {'validation_results': results}; graph = StateGraph(State); graph.add_node('ergonomics', validate_ergonomics); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('ergonomics'); graph.add_edge('ergonomics', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()