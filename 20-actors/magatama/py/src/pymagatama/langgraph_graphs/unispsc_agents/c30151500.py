from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RoofingState(TypedDict): vendor_certs: List[str]; specs: dict; compliance_passed: bool; def validate_specs(state: RoofingState): state['compliance_passed'] = all(k in state['specs'] for k in ['fire_rating', 'waterproofing_grade']); return state; def check_certs(state: RoofingState): return {'compliance_passed': 'ISO_9001' in state['vendor_certs']}; graph = StateGraph(RoofingState); graph.add_node('validate', validate_specs); graph.add_node('check_certs', check_certs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_certs'); graph.add_edge('check_certs', END); graph = graph.compile()