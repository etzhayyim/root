from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; approved: bool; def validate_specs(state: ProcurementState): return {'approved': state['specs'].get('wattage', 0) < 500}; def finalize(state: ProcurementState): return {'approved': True}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()