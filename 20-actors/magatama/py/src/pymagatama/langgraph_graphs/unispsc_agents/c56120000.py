from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): items: List[str]; validation_results: List[str]; approved: bool; def validate_furniture(state: FurnitureState): results = ['Passed' if 'ISO' in item or 'ergonomic' in item else 'Flagged' for item in state['items']]; return {'validation_results': results, 'approved': all(r == 'Passed' for r in results)}; graph = StateGraph(FurnitureState); graph.add_node('validate', validate_furniture); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()