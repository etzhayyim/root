from langgraph.graph import StateGraph, END; from typing import TypedDict; class RobotState(TypedDict): spec: dict; validated: bool; state: str; def validate_specs(state: RobotState): state['validated'] = state['spec'].get('payload', 0) > 0; return {'validated': state['validated']}; def compile_logic(): graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_logic()