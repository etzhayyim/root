from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ApparelState(TypedDict): items: List[dict]; validation_results: List[bool]; def validate_garment(state: ApparelState): results = [all(item.get('material') != 'prohibited' for item in state['items'])]; return {'validation_results': results}; def generate_report(state: ApparelState): print(f'Processing {len(state['items'])} garments for procurement.'); return {'validation_results': [True]}; graph = StateGraph(ApparelState); graph.add_node('validate', validate_garment); graph.add_node('report', generate_report); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()