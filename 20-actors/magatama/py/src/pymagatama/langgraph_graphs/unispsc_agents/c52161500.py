from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AVState(TypedDict): specs: dict; validation_log: Annotated[list, operator.add]; status: str; def validate_specs(state: AVState): return {'validation_log': ['Checking signal compatibility and power specs'], 'status': 'VALIDATED'}; def finalize_procurement(state: AVState): return {'status': 'READY'}; graph = StateGraph(AVState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()