from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class XRayTubeState(TypedDict): tube_specs: dict; validation_report: List[str]; approved: bool; def validate_specs(state: XRayTubeState): specs = state['tube_specs']; errors = []; if specs.get('voltage', 0) > 150: errors.append('Voltage exceeds safety threshold'); return {'validation_report': errors, 'approved': len(errors) == 0}; graph = StateGraph(XRayTubeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()