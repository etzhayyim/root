from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FiltrationState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: FiltrationState): state['valid'] = 'pore_size_microns' in state['specs'] and 'sterility_certification' in state['specs']; return state; graph = StateGraph(FiltrationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()