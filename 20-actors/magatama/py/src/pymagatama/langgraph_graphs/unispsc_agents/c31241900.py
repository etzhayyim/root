from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class OpticalState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(OpticalState); def validate_optical_specs(state: OpticalState): return {'validation_results': ['Surface checked', 'Transmission verified']}; def compliance_check(state: OpticalState): return {'validation_results': state['validation_results'] + ['Export control cleared']}; graph.add_node('validate', validate_optical_specs); graph.add_node('compliance', compliance_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()