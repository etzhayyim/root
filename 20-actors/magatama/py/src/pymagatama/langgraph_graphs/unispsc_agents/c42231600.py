from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MedDeviceState(TypedDict): device_id: str; compliance_docs: List[str]; approved: bool; def validate_device(state: MedDeviceState): return {'approved': len(state['compliance_docs']) > 2}; def check_safety(state: MedDeviceState): return {'approved': state['approved'] and 'ISO_13485' in state['compliance_docs']}; graph = StateGraph(MedDeviceState); graph.add_node('validate', validate_device); graph.add_node('safety_check', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); compiled_graph = graph.compile()