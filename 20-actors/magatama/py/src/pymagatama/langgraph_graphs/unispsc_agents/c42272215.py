from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation: bool; output: dict; def validate_spec(state: State): state['validation'] = 'ISO13485' in state['input_data'].get('certifications', []) return state; def report_gen(state: State): state['output'] = {'status': 'Approved' if state['validation'] else 'Rejected'}; return state; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('report', report_gen); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()