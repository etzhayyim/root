from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class HelmetState(TypedDict): parts: List[str]; validated_specs: bool; graph = StateGraph(HelmetState); def validate_parts(state): return {'validated_specs': all(p.startswith('COMP') for p in state['parts'])}; graph.add_node('validation', validate_parts); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()