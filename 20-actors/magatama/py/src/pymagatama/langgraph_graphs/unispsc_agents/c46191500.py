from langgraph.graph import StateGraph, END; from typing import TypedDict; class FireSafetyState(TypedDict): vendor_certs: dict; compliance_check: bool; graph = StateGraph(FireSafetyState); def validate_certification(state: FireSafetyState): return {'compliance_check': 'ISO' in state['vendor_certs'].get('standards', [])}; def finalize_procurement(state: FireSafetyState): return {'compliance_check': True}; graph.add_node('validate', validate_certification); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()