from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmbalmingState(TypedDict): tube_specs: dict; compliance_check: bool; workflow_log: List[str]; def validate_specs(state: EmbalmingState): state['compliance_check'] = 'material_composition' in state['tube_specs']; state['workflow_log'].append('Validated medical grade material'); return state; def prepare_shipment(state: EmbalmingState): state['workflow_log'].append('Sterilization protocol engaged'); return state; graph = StateGraph(EmbalmingState); graph.add_node('validate', validate_specs); graph.add_node('ship', prepare_shipment); graph.add_edge('validate', 'ship'); graph.add_edge('ship', END); graph.set_entry_point('validate'); graph = graph.compile()