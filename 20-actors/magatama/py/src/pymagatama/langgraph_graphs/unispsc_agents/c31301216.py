from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_purity(state: ForgingState): state['valid'] = state['specs'].get('purity', 0) >= 99.9; state['logs'].append('Purity check complete'); return state; def validate_tolerances(state: ForgingState): state['valid'] = state['specs'].get('tolerance', 0.1) <= 0.05; state['logs'].append('Tolerance check complete'); return state; workflow = StateGraph(ForgingState); workflow.add_node('purity', validate_purity); workflow.add_node('tolerances', validate_tolerances); workflow.set_entry_point('purity'); workflow.add_edge('purity', 'tolerances'); workflow.add_edge('tolerances', END); graph = workflow.compile()