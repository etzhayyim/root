from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FilterSpec(TypedDict): status: str; specs: dict; graph = StateGraph(FilterSpec); def validate_filter(state: FilterSpec): return {'status': 'validated' if 'pore_size' in state['specs'] else 'error'}; graph.add_node('validate', validate_filter); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()