from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): item_data: dict; validation_report: list; def validate_spec(state: State): spec = state['item_data'].get('spec', {}); errors = []; if 'material' not in spec: errors.append('Missing material spec'); return {'validation_report': errors}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()