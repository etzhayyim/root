from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): material: str; dimensions: dict; quality_check: bool; def validate_casting_spec(state: CastingState): return {'quality_check': True} if state['material'] else {'quality_check': False}; graph = StateGraph(CastingState); graph.add_node('validate', validate_casting_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()