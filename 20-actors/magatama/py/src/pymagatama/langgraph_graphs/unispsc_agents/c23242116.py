from langgraph.graph import StateGraph, END; from typing import TypedDict; class TappingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: TappingState): state['valid'] = 'taper_standard' in state['specs'] and state['specs']['torque_capacity'] > 0; return state; graph = StateGraph(TappingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()