from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MirrorState(TypedDict): specs: dict; valid: bool; status: str; def validate_specs(state: MirrorState): state['valid'] = state['specs'].get('Ra_nm', 10) < 5; return state; def check_export(state: MirrorState): state['status'] = 'Export Controlled' if state['specs'].get('precision') == 'Ultra' else 'Standard'; return state; graph = StateGraph(MirrorState); graph.add_node('validate', validate_specs); graph.add_node('export', check_export); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()