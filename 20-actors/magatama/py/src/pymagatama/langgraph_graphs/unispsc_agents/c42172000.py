from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmergencyKitState(TypedDict): kit_items: List[str]; qc_passed: bool; compliance_status: bool; def validate_inventory(state: EmergencyKitState): state['qc_passed'] = len(state['kit_items']) > 0; return state; def check_regulatory(state: EmergencyKitState): state['compliance_status'] = True; return state; graph = StateGraph(EmergencyKitState); graph.add_node('inventory_check', validate_inventory); graph.add_node('compliance_check', check_regulatory); graph.set_entry_point('inventory_check'); graph.add_edge('inventory_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()