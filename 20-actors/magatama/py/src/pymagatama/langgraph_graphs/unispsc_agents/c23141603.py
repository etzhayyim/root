from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class WeldState(TypedDict): input_spec: dict; validation_results: dict; def validate_laser_spec(state: WeldState): state['validation_results'] = {'compliant': state['input_spec'].get('laser_class') == 'Class 4'}; return state; def route_approval(state: WeldState): return 'end' if state['validation_results']['compliant'] else 'reject'; graph = StateGraph(WeldState); graph.add_node('validate', validate_laser_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()