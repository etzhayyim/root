from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DoorState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: DoorState): state['compliance'] = state['specs'].get('fire_rating') != 'none'; return state; def finalize_order(state: DoorState): state['status'] = 'ready_for_procurement'; return state; graph = StateGraph(DoorState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()