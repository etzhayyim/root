from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): model_data: dict; validation_results: list; graph = StateGraph(GraphState); def validate_celesta_specs(state: GraphState): return {'validation_results': ['CAD check passed', 'Component verification pass']}; def finalize_procurement(state: GraphState): return {'model_data': {**state['model_data'], 'status': 'approved'}}; graph.add_node('validate', validate_celesta_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()