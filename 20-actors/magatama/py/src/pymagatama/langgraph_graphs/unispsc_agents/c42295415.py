from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalCoverState(TypedDict): status: str; specs: dict; compliance: bool; def validate_specs(state: SurgicalCoverState): state['compliance'] = 'ISO13485' in state['specs']; state['status'] = 'validated' if state['compliance'] else 'rejected'; return state; graph = StateGraph(SurgicalCoverState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()