from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec_data: dict; validation_status: bool; def validate_cal_certs(state: ProcState): state['validation_status'] = 'cert_id' in state['spec_data']; return state; def check_precision(state: ProcState): state['validation_status'] = state['spec_data'].get('accuracy', 0) <= 0.1; return state; graph = StateGraph(ProcState); graph.add_node('validate_certs', validate_cal_certs); graph.add_node('check_precision', check_precision); graph.set_entry_point('validate_certs'); graph.add_edge('validate_certs', 'check_precision'); graph.add_edge('check_precision', END); graph = graph.compile()