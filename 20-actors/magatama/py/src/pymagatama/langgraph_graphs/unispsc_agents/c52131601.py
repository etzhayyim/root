from langgraph.graph import StateGraph, END; from typing import TypedDict; class BlindState(TypedDict): specs: dict; valid: bool; def validate_specs(state: BlindState): state['valid'] = 'dimensions' in state['specs'] and 'safety_cert' in state['specs']; return state; graph = StateGraph(BlindState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()