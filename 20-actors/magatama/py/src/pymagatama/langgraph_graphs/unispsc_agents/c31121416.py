from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): metal_purity: float; specs_met: bool; inspection_logs: Annotated[list, operator.add]; def validate_purity(state: CastingState): return {'specs_met': state['metal_purity'] >= 0.99}; def record_log(state: CastingState): return {'inspection_logs': ['Purity verified', 'CAD specs confirmed']}; graph = StateGraph(CastingState); graph.add_node('validate', validate_purity); graph.add_node('log', record_log); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.set_entry_point('validate'); graph = graph.compile()