from langgraph.graph import StateGraph, END; from typing import TypedDict; class PrinterState(TypedDict): spec: dict; validated: bool; state = PrinterState(spec={}, validated=False); def validate_spec(state: dict): return {'validated': 'DPI_resolution_capability' in state['spec']}; graph = StateGraph(PrinterState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()