from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MagnetometerState(TypedDict): data: dict; validation_errors: List[str]; def validate_specs(state: MagnetometerState): specs = state['data'].get('specs', {}); errors = []; if 'range' not in specs: errors.append('Range is missing'); return {'validation_errors': errors}; graph = StateGraph(MagnetometerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()