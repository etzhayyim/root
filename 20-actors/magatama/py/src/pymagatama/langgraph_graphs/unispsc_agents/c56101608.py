from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BikeRackState(TypedDict): vendor_specs: dict; validation_report: list; status: str; def validate_specs(state: BikeRackState): results = []; # Logic to check material durability and compliance; state['validation_report'] = results; return state; def finalize_order(state: BikeRackState): state['status'] = 'PROCESSED'; return state; graph = StateGraph(BikeRackState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()