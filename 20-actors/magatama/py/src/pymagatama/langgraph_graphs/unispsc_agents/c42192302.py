from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LiftState(TypedDict): device_id: str; structural_integrity_passed: bool; safety_certifications: List[str]; def check_structural_integrity(state: LiftState): state['structural_integrity_passed'] = True; return state; def validate_compliance(state: LiftState): return 'valid' if 'ISO-10535' in state['safety_certifications'] else 'invalid'; graph = StateGraph(LiftState); graph.add_node('integrity_check', check_structural_integrity); graph.add_edge('integrity_check', END); graph.set_entry_point('integrity_check'); graph = graph.compile()