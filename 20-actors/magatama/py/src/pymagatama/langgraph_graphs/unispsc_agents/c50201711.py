from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TeaProcurementState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_tea(state: TeaProcurementState): return {'validated': all('ISO' in c for c in state.get('certs', []))}; graph = StateGraph(TeaProcurementState); graph.add_node('validate', validate_tea); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()