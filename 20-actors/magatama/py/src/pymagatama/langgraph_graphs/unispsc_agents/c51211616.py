from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AntiveninState(TypedDict): cold_chain_ok: bool; batch_verified: bool; steps: List[str]; def validate_cold_chain(state: AntiveninState): return {'cold_chain_ok': True, 'steps': state['steps'] + ['Cold chain validated']}; def verify_batch(state: AntiveninState): return {'batch_verified': True, 'steps': state['steps'] + ['Batch certification confirmed']}; graph = StateGraph(AntiveninState); graph.add_node('cold_chain', validate_cold_chain); graph.add_node('batch_verification', verify_batch); graph.set_entry_point('cold_chain'); graph.add_edge('cold_chain', 'batch_verification'); graph.add_edge('batch_verification', END); graph = graph.compile()