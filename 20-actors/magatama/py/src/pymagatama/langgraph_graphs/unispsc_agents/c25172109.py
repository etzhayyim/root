from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AirbagState(TypedDict): component_id: str; hazard_check: bool; compliance_docs: List[str]; def check_regulations(state: AirbagState): return {'hazard_check': True}; def validate_specs(state: AirbagState): return {'compliance_docs': ['ISO-16949', 'Safety-Data-Sheet']}; graph = StateGraph(AirbagState); graph.add_node('RegCheck', check_regulations); graph.add_node('SpecValidation', validate_specs); graph.set_entry_point('RegCheck'); graph.add_edge('RegCheck', 'SpecValidation'); graph.add_edge('SpecValidation', END); compiled_graph = graph.compile()