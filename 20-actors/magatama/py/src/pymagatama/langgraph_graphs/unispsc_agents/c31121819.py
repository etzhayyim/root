from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CeramicState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: CeramicState): state['valid'] = 'dimensional_accuracy_tolerance' in state['specs'] and 'porosity_test_results' in state['specs']; return state; graph = StateGraph(CeramicState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()