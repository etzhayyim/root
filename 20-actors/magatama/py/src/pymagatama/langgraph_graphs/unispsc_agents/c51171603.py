from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ItemState(TypedDict): item_name: str; grade: str; compliance_checked: bool; graph = StateGraph(ItemState); def validate_purity(state: ItemState): return {'compliance_checked': True} if state['grade'] == 'USP' else {'compliance_checked': False}; graph.add_node('verify', validate_purity); graph.set_entry_point('verify'); graph.add_edge('verify', END); graph = graph.compile()