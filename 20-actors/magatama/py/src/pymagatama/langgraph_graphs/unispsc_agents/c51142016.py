from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_status: bool; workflow_log: list; graph = StateGraph(State); def validate_pharma(state: State): state['validation_status'] = 'concentration' in state['input_data'] and 'lot_expiry' in state['input_data']; state['workflow_log'].append('Validation completed'); return state; def route_storage(state: State): return 'process_cold_chain' if state['validation_status'] else END; graph.add_node('validate', validate_pharma); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()