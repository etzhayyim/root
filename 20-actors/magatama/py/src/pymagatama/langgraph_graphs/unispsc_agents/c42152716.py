from langgraph.graph import StateGraph, END; from typing import TypedDict; class OrthoState(TypedDict): specs: dict; valid: bool; def validate_specs(state: OrthoState): state['valid'] = 'ISO 13485' in state['specs'].get('certifications', []) return state; graph = StateGraph(OrthoState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()