from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HeaterState(TypedDict): wattage: float; voltage: float; specs: dict; valid: bool; def validate_heater(state: HeaterState): state['valid'] = state['wattage'] > 0 and state['voltage'] > 0; return state; graph = StateGraph(HeaterState); graph.add_node('validate', validate_heater); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()