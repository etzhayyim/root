from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class DisposalState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: DisposalState): specs = state['specs']; is_valid = all([specs.get('motor_power_hp', 0) > 0, specs.get('noise_level_decibels') < 80]); return {'valid': is_valid, 'log': ['Validation complete'] if is_valid else ['Invalid specs']}; graph = StateGraph(DisposalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()