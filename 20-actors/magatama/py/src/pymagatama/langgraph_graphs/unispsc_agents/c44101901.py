from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input: str; status: str; def validate_machine(state: State): return {'status': 'validated' if 'endorsing' in state['input'] else 'rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_machine); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()