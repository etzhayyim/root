from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class APIState(TypedDict): ingredient: str; grade: str; compliance_checks: List[str]; def validate_api(state: APIState): state['compliance_checks'].append('GMP_Verified'); return state; def check_regulatory(state: APIState): state['compliance_checks'].append('FDA_EMA_Checked'); return state; graph = StateGraph(APIState); graph.add_node('validate', validate_api); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()