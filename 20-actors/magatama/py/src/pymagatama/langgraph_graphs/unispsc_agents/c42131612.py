from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): commodity: str; specs: dict; compliant: bool; def validate_gowns(state: State): specs = state.get('specs', {}); level = specs.get('AAMI_Level', 0); state['compliant'] = level >= 2; return state; graph = StateGraph(State); graph.add_node('validate', validate_gowns); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()