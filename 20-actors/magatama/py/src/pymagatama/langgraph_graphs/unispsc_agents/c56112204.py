from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WorksurfaceState(TypedDict): specs: dict; valid: bool; def validate_specs(state: WorksurfaceState): state['valid'] = 'load_bearing' in state['specs'] and state['specs']['load_bearing'] > 0; return state; graph = StateGraph(WorksurfaceState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()