from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; validation_errors: Annotated[list[str], operator.add]; status: str; def validate_specs(state: ProcureState): errors = []; if 'TSO_certification_number' not in state['specs']: errors.append('Missing TSO certification'); return {'validation_errors': errors, 'status': 'validated' if not errors else 'rejected'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()