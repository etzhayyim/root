from langgraph.graph import StateGraph, END; from typing import TypedDict; class AxleState(TypedDict): specs: dict; valid: bool; def val_specs(state: AxleState): state['valid'] = all(k in state['specs'] for k in ['material', 'hardness']); return state; graph = StateGraph(AxleState); graph.add_node('validate', val_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()