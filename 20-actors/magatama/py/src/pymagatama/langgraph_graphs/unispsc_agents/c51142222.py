from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine: str; license_valid: bool; storage_temp: float; qc_status: str; def validate_license(state): return {'license_valid': True} if 'license' in state else {'license_valid': False}; def check_storage(state): return {'qc_status': 'PASS'} if state['storage_temp'] < 25 else {'qc_status': 'FAIL'}; graph = StateGraph(PharmState); graph.add_node('validate', validate_license); graph.add_node('check_qc', check_storage); graph.add_edge('validate', 'check_qc'); graph.add_edge('check_qc', END); graph.set_entry_point('validate'); graph = graph.compile()