from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): material_data: dict; compliance_check: bool; risk_level: str; def validate_purity(state: AnalysisState): state['compliance_check'] = state['material_data'].get('purity', 0) > 99.9; return state; def assess_risk(state: AnalysisState): state['risk_level'] = 'high_security' if state['compliance_check'] else 'rejected'; return state; graph = StateGraph(AnalysisState); graph.add_node('validate', validate_purity); graph.add_node('risk', assess_risk); graph.add_edge('validate', 'risk'); graph.add_edge('risk', END); graph.set_entry_point('validate'); graph = graph.compile()