from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadiographyState(TypedDict): source_activity: float; regulatory_status: bool; validation_log: List[str]; def validate_source(state: RadiographyState): return {'validation_log': state['validation_log'] + ['Checking activity limits'] if state['source_activity'] < 1000 else ['Flagged for high activity']}; def check_license(state: RadiographyState): return {'regulatory_status': True}; graph = StateGraph(RadiographyState); graph.add_node('validate', validate_source); graph.add_node('license', check_license); graph.set_entry_point('validate'); graph.add_edge('validate', 'license'); graph.add_edge('license', END); compiled_graph = graph.compile()