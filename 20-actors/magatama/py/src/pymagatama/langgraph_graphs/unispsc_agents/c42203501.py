from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PacemakerState(TypedDict): device_id: str; compliance_docs: List[str]; status: str; def validate_regulatory(state): return {'status': 'CERTIFIED'} if 'ISO13485' in state['compliance_docs'] else {'status': 'REJECTED'}; def audit_traceability(state): return {'status': 'AUDIT_PASSED'}; graph = StateGraph(PacemakerState); graph.add_node('validate', validate_regulatory); graph.add_node('audit', audit_traceability); graph.set_entry_point('validate'); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); graph = graph.compile()