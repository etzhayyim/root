from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DriveState(TypedDict): input_specs: dict; validated: bool; analysis: str; def validate_specs(state: DriveState): specs = state['input_specs']; is_valid = all(key in specs for key in ['power', 'rpm_range']); return {'validated': is_valid, 'analysis': 'Specs valid' if is_valid else 'Missing parameters'}; def finish(state: DriveState): return {'analysis': 'Processing complete'}; graph = StateGraph(DriveState); graph.add_node('validate', validate_specs); graph.add_node('finalizer', finish); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalizer'); graph.add_edge('finalizer', END); app = graph.compile()