from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AlloyState(TypedDict): rod_specs: dict; validation_report: list; risk_score: int; def validate_alloy_composition(state: AlloyState): state['validation_report'].append('Composition check passed'); return state; def check_export_controls(state: AlloyState): state['risk_score'] = 5 if state['rod_specs'].get('is_dual_use') else 1; return state; graph = StateGraph(AlloyState); graph.add_node('validate', validate_alloy_composition); graph.add_node('export_check', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()