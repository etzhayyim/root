from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PagerState(TypedDict): equipment_id: str; specs: dict; valid: bool; def validate_specs(state: PagerState): state['valid'] = all(k in state['specs'] for k in ['frequency', 'power']); return state; graph = StateGraph(PagerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()