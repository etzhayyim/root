from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class WeldingMaskState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(WeldingMaskState); def validate_specs(state: WeldingMaskState): return {'validation_results': ['ANSI_Z87_Verified'] if state['input_data'].get('standard') == 'ANSI' else ['Standard_Mismatch']}; def check_safety(state: WeldingMaskState): return {'validation_results': ['Safety_Check_Passed']}; graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()