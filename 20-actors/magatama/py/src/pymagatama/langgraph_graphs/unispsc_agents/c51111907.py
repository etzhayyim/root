from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): commodity: str; purity: float; compliant: bool; def validate_quality(state: ProcureState): state['compliant'] = state['purity'] >= 99.0; return state; graph = StateGraph(ProcureState); graph.add_node('qc_check', validate_quality); graph.set_entry_point('qc_check'); graph.add_edge('qc_check', END); graph = graph.compile()