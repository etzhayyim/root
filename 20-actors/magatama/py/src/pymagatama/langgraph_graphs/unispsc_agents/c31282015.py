from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; specs: dict; valid: bool; def validate_specs(state: ComponentState): state['valid'] = 'Material Grade' in state['specs'] and 'Wall Thickness Tolerance' in state['specs']; return state; builder = StateGraph(ComponentState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()