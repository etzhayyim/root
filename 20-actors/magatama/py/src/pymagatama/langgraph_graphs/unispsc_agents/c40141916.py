from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DuctState(TypedDict): specs: dict; validation_passed: bool; def validate_duct(state: DuctState): state['validation_passed'] = all(k in state['specs'] for k in ['material', 'pressure']); return state; graph = StateGraph(DuctState); graph.add_node('validate', validate_duct); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()