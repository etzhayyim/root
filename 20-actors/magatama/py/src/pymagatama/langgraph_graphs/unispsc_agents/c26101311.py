from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MotorState(TypedDict): specs: dict; validation_passed: bool; motor_type: str; def validate_specs(state: MotorState): specs = state['specs']; is_valid = all(key in specs for key in ['voltage', 'rpm']); return {'validation_passed': is_valid}; def routing_logic(state: MotorState): return 'validate' if state.get('motor_type') == 'universal' else END; graph = StateGraph(MotorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()