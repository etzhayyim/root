from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_specs(state: CastingState): results = []; required = ['material', 'tolerance']; for field in required: results.append(f'Checked {field}') if field in state['specs'] else None; return {'validation_results': results, 'status': 'VALIDATED' if len(results) == 2 else 'REJECTED'}; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()