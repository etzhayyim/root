from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RecorderState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: RecorderState): errors = []; if state['specs'].get('channels', 0) <= 0: errors.append('Invalid channel count'); return {'validation_errors': errors, 'status': 'valid' if not errors else 'invalid'}; def process_procurement(state: RecorderState): return {'status': 'processed'}; graph = StateGraph(RecorderState); graph.add_node('validate', validate_specs); graph.add_node('process', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()