from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): data: dict; step: str; def validate_gmp(state: ProcureState): state['step'] = 'GMP_VALIDATION'; return state; def check_regulatory(state: ProcureState): state['step'] = 'REGULATORY_COMPLIANCE'; return state; workflow = StateGraph(ProcureState); workflow.add_node('validate', validate_gmp); workflow.add_node('compliance', check_regulatory); workflow.set_entry_point('validate'); workflow.add_edge('validate', 'compliance'); workflow.add_edge('compliance', END); graph = workflow.compile()