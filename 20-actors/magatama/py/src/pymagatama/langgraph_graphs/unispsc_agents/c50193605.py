from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FoodState(TypedDict): items: List[str]; qc_passed: bool; def check_quality(state: FoodState): print('Checking food safety standards...'); return {'qc_passed': True}; graph = StateGraph(FoodState); graph.add_node('qc_check', check_quality); graph.set_entry_point('qc_check'); graph.add_edge('qc_check', END); graph = graph.compile()