from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpValveState(TypedDict): valve_specs: dict; inspection_results: List[str]; validated: bool; def validate_specs(state: PumpValveState): pressure = state['valve_specs'].get('pressure', 0); state['validated'] = pressure > 0; return state; graph = StateGraph(PumpValveState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()