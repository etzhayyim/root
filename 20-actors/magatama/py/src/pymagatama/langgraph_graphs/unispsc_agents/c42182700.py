from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MeasurementState(TypedDict): device_type: str; is_calibrated: bool; accuracy_check: bool; workflow_steps: List[str]; def check_calibration(state: MeasurementState): state['is_calibrated'] = True; state['workflow_steps'].append('Calibration Verified'); return state; def validate_specs(state: MeasurementState): state['accuracy_check'] = True; state['workflow_steps'].append('Specification Validated'); return state; graph = StateGraph(MeasurementState); graph.add_node('calibrate', check_calibration); graph.add_node('validate', validate_specs); graph.set_entry_point('calibrate'); graph.add_edge('calibrate', 'validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()