from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): vendor_id: str; drug_batch: str; gmp_compliant: bool; temp_logs: List[float]; approved: bool; def validate_gmp(state: DrugState): state['gmp_compliant'] = True; return state; def check_storage(state: DrugState): state['approved'] = all(t < 25.0 for t in state['temp_logs']); return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_gmp); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph.compile()