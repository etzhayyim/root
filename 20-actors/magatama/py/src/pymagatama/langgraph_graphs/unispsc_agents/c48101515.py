from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_specs(state: ProcureState): state['compliance_status'] = 'NSF' in state['vendor_specs'].get('certifications', '') and state['vendor_specs'].get('voltage') == 220; state['workflow_log'].append('Validation completed'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()