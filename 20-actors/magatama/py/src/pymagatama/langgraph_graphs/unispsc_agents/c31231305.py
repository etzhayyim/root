from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): tubing_spec: dict; valid: bool; def validate_spec(state: State): specs = state.get('tubing_spec', {}); is_valid = 'purity_percentage' in specs and specs['purity_percentage'] >= 99.0; return {'valid': is_valid}; graph = StateGraph(State); graph.add_node('validation', validate_spec); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()