from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TramProcurementState(TypedDict): vehicle_specs: dict; compliance_checks: List[str]; approved: bool; def validate_specs(state: TramProcurementState): state['compliance_checks'].append('EN 15227 Safety Validated'); return state; def check_rail_compatibility(state: TramProcurementState): state['compliance_checks'].append('Gauge and Voltage Verified'); return state; graph = StateGraph(TramProcurementState); graph.add_node('validate', validate_specs); graph.add_node('check_rail', check_rail_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_rail'); graph.add_edge('check_rail', END); compiled_graph = graph.compile()