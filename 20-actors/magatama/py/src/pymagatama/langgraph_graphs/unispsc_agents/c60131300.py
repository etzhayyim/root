from langgraph.graph import StateGraph, END; from typing import TypedDict; class InstrumentState(TypedDict): instrument_data: dict; inspection_passed: bool; def validate_instrument(state: InstrumentState): return {'inspection_passed': 'serial_number' in state['instrument_data']}; def check_inventory(state: InstrumentState): return state; graph = StateGraph(InstrumentState); graph.add_node('validate', validate_instrument); graph.add_node('check_inv', check_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_inv'); graph.add_edge('check_inv', END); app = graph.compile()