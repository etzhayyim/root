from langgraph.graph import StateGraph, END; from typing import TypedDict; class EmesisBasinState(TypedDict): specs: dict; valid: bool; def validate_specs(state: EmesisBasinState): state['valid'] = 'material_composition' in state['specs'] and 'sterility_assurance_level' in state['specs']; return state; graph = StateGraph(EmesisBasinState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()