from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CrucibleState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(CrucibleState); def validate_thermal_specs(state: CrucibleState): state['validation_results'].append('Thermal Limit Validated'); return state; def check_chemical_inertness(state: CrucibleState): state['validation_results'].append('Chemical Inertness Verified'); return state; graph.add_node('thermal_check', validate_thermal_specs); graph.add_node('chemical_check', check_chemical_inertness); graph.set_entry_point('thermal_check'); graph.add_edge('thermal_check', 'chemical_check'); graph.add_edge('chemical_check', END); graph = graph.compile()