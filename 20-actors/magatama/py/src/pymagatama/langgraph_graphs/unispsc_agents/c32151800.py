from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SafetyDeviceState(TypedDict): device_id: str; specs: dict; validation_passed: bool; def validate_safety_rating(state: SafetyDeviceState): state['validation_passed'] = 'ISO13849' in state.get('specs', {}).get('standards', []); return state; def check_compliance(state: SafetyDeviceState): return 'compliance_check' if state['validation_passed'] else END; graph = StateGraph(SafetyDeviceState); graph.add_node('validate', validate_safety_rating); graph.add_node('compliance_check', lambda s: s); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()