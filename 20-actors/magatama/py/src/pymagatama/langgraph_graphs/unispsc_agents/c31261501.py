from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PlasticHousingState(TypedDict): dimensions: dict; material: str; specs: List[str]; validation_results: List[str]; def validate_material_specs(state: PlasticHousingState): return {'validation_results': ['Material passed UL94 check']}; def validate_dimensions(state: PlasticHousingState): return {'validation_results': ['Dimensions within tolerance']}; graph = StateGraph(PlasticHousingState); graph.add_node('material_check', validate_material_specs); graph.add_node('dim_check', validate_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dim_check'); graph.add_edge('dim_check', END); graph = graph.compile()