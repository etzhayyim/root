from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicroscopeState(TypedDict): specs: dict; validation_errors: List[str]; def validate_specs(state: MicroscopeState): errors = []; if state['specs'].get('magnification') is None: errors.append('Missing magnification'); return {'validation_errors': errors}; graph = StateGraph(MicroscopeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()