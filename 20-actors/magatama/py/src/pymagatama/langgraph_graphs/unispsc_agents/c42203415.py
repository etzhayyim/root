from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): status: str; spec_valid: bool; def validate_specs(state: ProcState): state['spec_valid'] = True; return {'status': 'VALIDATED'}; def finalize_procurement(state: ProcState): return {'status': 'COMPLETED'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()