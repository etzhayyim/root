from langgraph.graph import StateGraph, END; from typing import TypedDict; class LaserState(TypedDict): specs: dict; valid: bool; def validate_laser_specs(state: LaserState): state['valid'] = state['specs'].get('laser_power_class') in ['Class 4']; return state; def check_export_control(state: LaserState): state['valid'] = state['valid'] and state['specs'].get('export_license') is not None; return state; graph = StateGraph(LaserState); graph.add_node('validation', validate_laser_specs); graph.add_node('export', check_export_control); graph.set_entry_point('validation'); graph.add_edge('validation', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()