from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: CastState): if 'tin_alloy_grade' in state['specs']: state['validation_results'].append('Material grade verified'); return state; def validate_dimensions(state: CastState): if 'tolerance_mm' in state['specs']: state['validation_results'].append('Tolerance bounds checked'); return state; graph = StateGraph(CastState); graph.add_node('material_check', validate_material); graph.add_node('dimension_check', validate_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dimension_check'); graph.add_edge('dimension_check', END); graph = graph.compile()