from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MailerState(TypedDict): dimensions: dict; material: str; validated: bool; def validate_mailer(state: MailerState): print('Validating mailer specs...'); return {'validated': True}; graph = StateGraph(MailerState); graph.add_node('validate', validate_mailer); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()