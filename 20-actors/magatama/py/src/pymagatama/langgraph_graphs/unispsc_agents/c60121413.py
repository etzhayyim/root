from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(AgentState); def validate_specs(state: AgentState): validations = []; if not state['specs'].get('acid_free'): validations.append('Missing acid-free certification'); return {'validation_report': validations}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()