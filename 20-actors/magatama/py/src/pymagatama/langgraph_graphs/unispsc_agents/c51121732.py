from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine_name: str; validation_passed: bool; warnings: List[str]; def validate_batch(state: PharmState): state['validation_passed'] = True; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_batch); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()