from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CapillaryState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: CapillaryState): state['valid'] = state['specs'].get('burst_pressure', 0) > 1000; return state; graph = StateGraph(CapillaryState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()