from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class GoutDrugState(TypedDict): medication_name: str; validation_passed: bool; storage_temp: float; def validate_specs(state: GoutDrugState): state['validation_passed'] = state['storage_temp'] <= 25.0; return state; graph = StateGraph(GoutDrugState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()