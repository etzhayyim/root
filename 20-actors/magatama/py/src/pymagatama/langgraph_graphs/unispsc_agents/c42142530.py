from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DiagnosticNeedleState(TypedDict): needle_specs: dict; validation_results: List[str]; def validate_medical_specs(state: DiagnosticNeedleState): if state['needle_specs'].get('sterilization_method') == 'Gamma': state['validation_results'].append('Sterilization validated'); return state; def check_regulatory_compliance(state: DiagnosticNeedleState): if 'ISO 13485' in state['needle_specs']['certifications']: state['validation_results'].append('Regulatory pass'); return state; graph = StateGraph(DiagnosticNeedleState); graph.add_node('validate_specs', validate_medical_specs); graph.add_node('check_compliance', check_regulatory_compliance); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', 'check_compliance'); graph.add_edge('check_compliance', END); graph = graph.compile()