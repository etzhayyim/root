from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DentalState(TypedDict): organizer_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: DentalState): specs = state['organizer_specs']; errors = []; if not specs.get('autoclavable'): errors.append('Missing mandatory autoclavability rating'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(DentalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()