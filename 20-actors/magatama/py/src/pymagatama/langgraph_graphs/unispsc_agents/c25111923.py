from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OarlockState(TypedDict): oar_diameter: float; material: str; validated: bool; def check_specs(state: OarlockState): state['validated'] = state['oar_diameter'] > 0 and state['material'] in ['stainless_steel', 'brass']; return state; builder = StateGraph(OarlockState); builder.add_node('validation', check_specs); builder.set_entry_point('validation'); builder.add_edge('validation', END); graph = builder.compile()