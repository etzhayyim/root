from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalDeviceState(TypedDict): device_id: str; compliance_docs: List[str]; validation_results: dict; def validate_compliance(state: DentalDeviceState): state['validation_results'] = {'passed': all(doc in state['compliance_docs'] for doc in ['ISO_13485', 'Regulatory_Approval'])}; return state; def check_electrical_safety(state: DentalDeviceState): state['validation_results']['safety_check'] = 'certified'; return state; graph = StateGraph(DentalDeviceState); graph.add_node('compliance_check', validate_compliance); graph.add_node('safety_inspect', check_electrical_safety); graph.set_entry_point('compliance_check'); graph.add_edge('compliance_check', 'safety_inspect'); graph.add_edge('safety_inspect', END); graph = graph.compile()