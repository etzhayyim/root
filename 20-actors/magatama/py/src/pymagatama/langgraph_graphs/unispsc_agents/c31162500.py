from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): input_specs: dict; validated_specs: dict; inspection_required: bool; def validate_dimensions(state: ProcurementState): spec = state.get('input_specs', {}); state['validated_specs'] = {k: v for k, v in spec.items() if v is not None}; state['inspection_required'] = True; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()