from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): vendor_id: str; compliant: bool; batch_data: dict; graph = StateGraph(PharmaState); def validate_compliance(state: PharmaState): print('Checking DEA/Regulatory compliance...'); return {'compliant': True}; def check_storage(state: PharmaState): print('Verifying cold chain logs...'); return {'compliant': True}; graph.add_node('validate', validate_compliance); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); compile_graph = graph.compile()