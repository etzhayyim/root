from langgraph.graph import StateGraph, END; from typing import TypedDict; class ChairState(TypedDict): specs: dict; approval: bool; graph = StateGraph(ChairState); def validate_specs(state: ChairState): return {'approval': state['specs'].get('load_kg', 0) > 100}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()