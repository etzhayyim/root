from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class AgentState(TypedDict): messages: Annotated[list, add_messages]; analysis_result: str; def validate_reagent(state: AgentState): return {'analysis_result': 'VALIDATED_PURE'} if 'cas' in state else {'analysis_result': 'MISSING_CAS'}; def log_compliance(state: AgentState): return {'messages': ['Compliance check completed for CAS reagent.']}; graph = StateGraph(AgentState); graph.add_node('validate', validate_reagent); graph.add_node('log', log_compliance); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.set_entry_point('validate'); graph = graph.compile()