from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlannerState(TypedDict): planner_type: str; is_custom: bool; validation_errors: List[str]; def validate_planner(state: PlannerState): state['validation_errors'] = [] if not state['planner_type']: state['validation_errors'].append('Missing type') return state; workflow = StateGraph(PlannerState); workflow.add_node('validator', validate_planner); workflow.set_entry_point('validator'); workflow.add_edge('validator', END); graph = workflow.compile()