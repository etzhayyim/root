from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ThreadingTapState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ThreadingTapState): errors = []; required = ['material', 'thread_type', 'tolerance']; for field in required: if field not in state['specs']: errors.append(f'Missing {field}'); return {**state, 'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(ThreadingTapState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()