from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class VideoNetState(TypedDict): vendor_specs: dict; compliance_score: int; approved: bool; def validate_specs(state: VideoNetState): return {'approved': state['compliance_score'] > 80}; def finalize_procurement(state: VideoNetState): return {'approved': True}; graph = StateGraph(VideoNetState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()