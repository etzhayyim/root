from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: List[str]; validation_passed: bool; def validate_specs(state: State): state['validation_passed'] = 'Acoustic-Transparency-Rating' in state['specs']; return state; builder = StateGraph(State); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()