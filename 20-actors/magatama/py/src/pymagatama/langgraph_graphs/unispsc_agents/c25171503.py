from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LocomotiveWiperState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(LocomotiveWiperState); def validate_specs(state: LocomotiveWiperState): report = []; missing = [f for f in ['railway_standard_compliance', 'blade_length_mm'] if f not in state['specs']]; report.append('Validation failed' if missing else 'Validation passed'); return {'validation_report': report}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()