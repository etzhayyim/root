from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlasticFoilState(TypedDict): specs: dict; compliance_log: List[str]; approved: bool; def validate_specs(state: PlasticFoilState): thickness = state['specs'].get('thickness', 0); state['approved'] = thickness > 0; state['compliance_log'].append('Thickness validated') if thickness > 0 else state['compliance_log'].append('Thickness invalid'); return state; graph = StateGraph(PlasticFoilState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()