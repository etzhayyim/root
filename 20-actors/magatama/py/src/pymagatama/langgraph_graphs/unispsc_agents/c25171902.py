from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RailWheelState(TypedDict): specs: dict; validation_report: list[str]; def validate_wheel_specs(state: RailWheelState): if state['specs'].get('load_rating', 0) < 10000: return {'validation_report': ['Warning: Load rating below standard threshold.']}; return {'validation_report': ['Spec validation passed.']}; graph = StateGraph(RailWheelState); graph.add_node('validate', validate_wheel_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()