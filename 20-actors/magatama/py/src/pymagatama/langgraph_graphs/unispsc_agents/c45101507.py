from langgraph.graph import StateGraph, END; from typing import TypedDict; class PrintingPressState(TypedDict): specs: dict; validation: bool; def validate_specs(state: PrintingPressState): state['validation'] = 'Print Speed' in state['specs'] and 'Power' in state['specs']; return state; builder = StateGraph(PrintingPressState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()