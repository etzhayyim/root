from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlooringState(TypedDict): specs: dict; compliance_status: bool; def validate_specs(state: FlooringState): return {'compliance_status': 'F4' in state['specs'].get('formaldehyde_grade', '')}; def check_inventory(state: FlooringState): return {'compliance_status': state['compliance_status'] and state['specs'].get('stock_available', False)}; graph = StateGraph(FlooringState); graph.add_node('validate', validate_specs); graph.add_node('inventory', check_inventory); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()