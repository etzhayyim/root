from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FoodState(TypedDict): items: List[str]; qc_status: str; safety_checks: List[str]; def validate_quality(state: FoodState): return {'qc_status': 'passed' if all(check in state['safety_checks'] for check in ['HACCP', 'Temp-Control']) else 'failed'}; graph = StateGraph(FoodState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()