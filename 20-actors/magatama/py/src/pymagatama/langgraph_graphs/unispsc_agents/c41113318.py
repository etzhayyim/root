from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UraniumState(TypedDict): items: List[dict]; compliance_check: bool; export_approved: bool; def check_compliance(state: UraniumState): state['compliance_check'] = True; return state; def verify_export(state: UraniumState): state['export_approved'] = True; return state; graph = StateGraph(UraniumState); graph.add_node('compliance', check_compliance); graph.add_node('export', verify_export); graph.add_edge('compliance', 'export'); graph.add_edge('export', END); graph.set_entry_point('compliance'); graph = graph.compile()