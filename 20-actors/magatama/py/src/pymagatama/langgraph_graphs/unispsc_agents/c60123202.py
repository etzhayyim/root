from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SilkRibbonState(TypedDict): specs: dict; valid: bool; workflow_log: List[str]; def validate_specs(state: SilkRibbonState): specs = state['specs']; is_valid = all([specs.get('width'), specs.get('material') == '100% silk']); return {'valid': is_valid, 'workflow_log': ['Specs validated']}; def finalize_procurement(state: SilkRibbonState): return {'workflow_log': state['workflow_log'] + ['Procurement initiated']}; graph = StateGraph(SilkRibbonState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()