from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): doc_id: str; structural_integrity_passed: bool; export_license_verified: bool; def validate_specs(state: AssemblyState): return {'structural_integrity_passed': True} if state['doc_id'] else {'structural_integrity_passed': False}; def check_export(state: AssemblyState): return {'export_license_verified': True}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()