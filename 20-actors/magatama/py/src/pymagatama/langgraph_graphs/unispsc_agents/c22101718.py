from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RobotState(TypedDict): specs: dict; ready: bool; def validate_specs(state: RobotState): return {'ready': state['specs'].get('PayloadCapacity_kg', 0) > 0}; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()