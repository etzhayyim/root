from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PatientSupportState(TypedDict): specs: dict; validation_results: List[str]; def validate_safety(state: PatientSupportState): if state['specs'].get('load_capacity', 0) < 150: return {'validation_results': ['Capacity below standard']}; return {'validation_results': ['Safety compliant']}; def finalize_procurement(state: PatientSupportState): return {'validation_results': state['validation_results'] + ['Approved for procurement']}; graph = StateGraph(PatientSupportState); graph.add_node('validate', validate_safety); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()