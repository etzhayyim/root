from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ThermostatState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ThermostatState): errors = []; if 'range' not in state['specs']: errors.append('Range missing'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(ThermostatState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()