from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): cold_chain_verified: bool; batch_valid: bool; graph = StateGraph(PharmaState); def verify_cold_chain(state): return {'cold_chain_verified': True}; def validate_batch(state): return {'batch_valid': True}; graph.add_node('verify_cold_chain', verify_cold_chain); graph.add_node('validate_batch', validate_batch); graph.set_entry_point('verify_cold_chain'); graph.add_edge('verify_cold_chain', 'validate_batch'); graph.add_edge('validate_batch', END); graph.compile()