from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): device_id: str; quality_checks: List[str]; approved: bool; def check_sterility(state: SurgicalState): state['quality_checks'].append('sterility_verified'); return state; def validate_compliance(state: SurgicalState): state['approved'] = True; return state; graph = StateGraph(SurgicalState); graph.add_node('check_sterility', check_sterility); graph.add_node('validate_compliance', validate_compliance); graph.add_edge('check_sterility', 'validate_compliance'); graph.add_edge('validate_compliance', END); graph.set_entry_point('check_sterility'); graph = graph.compile()