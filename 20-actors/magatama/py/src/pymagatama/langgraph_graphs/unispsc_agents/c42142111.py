from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class IcePackState(TypedDict): specs: List[str]; validation_status: str; graph = StateGraph(IcePackState); def validate_material(state): state['validation_status'] = 'verified' if 'MSDS' in state['specs'] else 'failed'; return state; graph.add_node('material_check', validate_material); graph.set_entry_point('material_check'); graph.add_edge('material_check', END); graph = graph.compile()