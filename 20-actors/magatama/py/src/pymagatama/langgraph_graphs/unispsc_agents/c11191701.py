from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class SiliconProcurementState(TypedDict): wafer_specs: dict; compliance_status: list; graph = StateGraph(SiliconProcurementState); def validate_purity(state: SiliconProcurementState): return {'compliance_status': ['purity_check_passed']}; def initiate_export_review(state: SiliconProcurementState): return {'compliance_status': ['export_control_initiated']}; graph.add_node('validate_purity', validate_purity); graph.add_node('export_review', initiate_export_review); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()