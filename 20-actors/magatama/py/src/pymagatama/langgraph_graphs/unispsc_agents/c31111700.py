from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtrusionState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: ExtrusionState): state['validation_results'].append('Material chemistry verified'); return state; def validate_dimensions(state: ExtrusionState): state['validation_results'].append('Tolerance bounds checked'); return state; graph = StateGraph(ExtrusionState); graph.add_node('verify_material', validate_material); graph.add_node('verify_dims', validate_dimensions); graph.set_entry_point('verify_material'); graph.add_edge('verify_material', 'verify_dims'); graph.add_edge('verify_dims', END); graph = graph.compile()