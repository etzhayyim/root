from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): data: dict; logs: Annotated[list, operator.add]; def validate_specs(state: State): state['logs'].append('Validating mechanical specs'); return {'data': state['data']}; def check_safety(state: State): state['logs'].append('Checking safety compliance'); return {'data': state['data']}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()