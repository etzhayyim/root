from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PressureState(TypedDict): input_spec: dict; calibration_status: bool; compliance_check: bool; def validate_spec(state: PressureState): state['calibration_status'] = 'calibration_certificate' in state['input_spec'] return state; def check_compliance(state: PressureState): state['compliance_check'] = state['input_spec'].get('accuracy_class') is not None return state; graph = StateGraph(PressureState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()