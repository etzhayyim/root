from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShipProcurementState(TypedDict): vendor_clearance: bool; compliance_docs: List[str]; inspection_status: str; def verify_compliance(state: ShipProcurementState): return {'compliance_docs': ['ITAR_Certified', 'Naval_Registry']} if state['vendor_clearance'] else {'compliance_docs': []}; def finalize_procurement(state: ShipProcurementState): return {'inspection_status': 'COMPLETED'}; graph = StateGraph(ShipProcurementState); graph.add_node('verify', verify_compliance); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('verify'); graph.add_edge('verify', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()