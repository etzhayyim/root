from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; compliance_status: bool; def check_regulatory(state: PharmState): return {'compliance_status': state['input_data'].get('purity') > 99.0}; def validate_specs(state: PharmState): return {'validation_results': ['Purity check passed'] if state['compliance_status'] else ['Purity check failed']}; graph = StateGraph(PharmState); graph.add_node('regulatory', check_regulatory); graph.add_node('specs', validate_specs); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'specs'); graph.add_edge('specs', END); app = graph.compile()