from langgraph.graph import StateGraph, END; from typing import TypedDict; class InositolState(TypedDict): purity: float; status: str; spec_valid: bool; def check_purity(state: InositolState): state['spec_valid'] = state['purity'] >= 99.0; return {'status': 'Validated' if state['spec_valid'] else 'Rejected'}; graph = StateGraph(InositolState); graph.add_node('validate', check_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()