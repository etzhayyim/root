from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IncentiveState(TypedDict): items: List[str]; validated: bool; budget_check: bool; def validate_incentives(state: IncentiveState): return {'validated': all(len(i) > 0 for i in state['items'])}; def check_budget(state: IncentiveState): return {'budget_check': True}; graph = StateGraph(IncentiveState); graph.add_node('validate', validate_incentives); graph.add_node('budget', check_budget); graph.set_entry_point('validate'); graph.add_edge('validate', 'budget'); graph.add_edge('budget', END); app = graph.compile()