from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): commodity_code: str; quality_score: float; valid: bool; logs: Annotated[list, operator.add]; def validate_purity(state: ProcureState): return {'valid': state['quality_score'] > 0.99, 'logs': ['Purity validation completed']}; def check_export_control(state: ProcureState): return {'logs': ['Dual-use compliance checked']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_purity); graph.add_node('export', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()