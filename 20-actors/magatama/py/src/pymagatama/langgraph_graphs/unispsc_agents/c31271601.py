from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachinePartState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_dimensions(state: MachinePartState): state['validation_errors'] = [e for e in state['specs'].get('dims', []) if e < 0]; return state; def check_material(state: MachinePartState): state['approved'] = state['specs'].get('material') is not None; return state; graph = StateGraph(MachinePartState); graph.add_node('validate', validate_dimensions); graph.add_node('material_check', check_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()