from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): pump_specs: dict; validation_results: List[str]; def validate_medical_grade(state: PumpState): state['validation_results'].append('ISO13485_certified'); return state; def check_pressure_spec(state: PumpState): if state['pump_specs'].get('pressure', 0) > 100: state['validation_results'].append('HighPressure_Safety_Check_PASSED'); return state; graph = StateGraph(PumpState); graph.add_node('medical_validation', validate_medical_grade); graph.add_node('pressure_check', check_pressure_spec); graph.set_entry_point('medical_validation'); graph.add_edge('medical_validation', 'pressure_check'); graph.add_edge('pressure_check', END); graph = graph.compile()