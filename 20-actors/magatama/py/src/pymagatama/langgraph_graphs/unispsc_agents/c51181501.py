from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medicine: str; status: str; specs: List[str]; def validate_medicine(state: PharmaState): return {'status': 'validated' if state['medicine'] == 'Acarbose' else 'rejected'}; def check_compliance(state: PharmaState): return {'status': 'compliant' if 'USP' in state['specs'] else 'non-compliant'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_medicine); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()