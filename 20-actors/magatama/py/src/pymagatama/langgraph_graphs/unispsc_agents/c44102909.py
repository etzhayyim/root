from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CleaningState(TypedDict): drive_type: str; solvent_safety_verified: bool; validation_log: List[str]; def validate_compatibility(state): state['validation_log'].append('Checking drive compatibility'); return {'solvent_safety_verified': True}; def update_log(state): state['validation_log'].append('Processing complete'); return {}; graph = StateGraph(CleaningState); graph.add_node('compatibility', validate_compatibility); graph.add_node('finalize', update_log); graph.set_entry_point('compatibility'); graph.add_edge('compatibility', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()