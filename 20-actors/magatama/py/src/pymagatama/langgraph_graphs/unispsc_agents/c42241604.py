from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class VacuumState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(VacuumState); def validate_specs(state: VacuumState): print('Validating HEPA and airflow requirements'); return {'compliance_check': state['specs'].get('hepa_grade') == 'H13'}; def process_procurement(state: VacuumState): print('Processing order for medical vacuum equipment'); return state; graph.add_node('validate', validate_specs); graph.add_node('process', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()