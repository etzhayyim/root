from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): vendor_specs: dict; validation_results: List[str]; def validate_enclosure(state: SpecState): results = []; ip_rating = state['vendor_specs'].get('IP_rating', 0); if ip_rating < 54: results.append('Insufficient IP rating'); return {'validation_results': results}; graph = StateGraph(SpecState); graph.add_node('validate', validate_enclosure); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()