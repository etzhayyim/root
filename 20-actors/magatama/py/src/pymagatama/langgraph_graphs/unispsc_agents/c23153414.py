from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): data: dict; status: list[str]; graph = StateGraph(RobotState); def validate_specs(state: RobotState): return {'status': ['SPECS_VALIDATED']}; def check_export_compliance(state: RobotState): return {'status': ['COMPLIANCE_CHECKED']}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()