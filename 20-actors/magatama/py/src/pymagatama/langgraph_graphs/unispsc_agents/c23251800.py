from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolingState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: ToolingState): return {'validation_report': 'Specs verified for tolerance and material standard', 'approved': True}; graph = StateGraph(ToolingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()