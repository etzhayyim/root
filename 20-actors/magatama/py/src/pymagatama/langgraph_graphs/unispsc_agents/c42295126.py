from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): device_specs: dict; status: str; validation_errors: List[str]; def validate_spec(state: State): specs = state['device_specs']; errors = []; if 'sterilization_cert' not in specs: errors.append('Missing sterilization cert'); return {'validation_errors': errors, 'status': 'valid' if not errors else 'invalid'}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()