from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: list; status: str; def validate_integrity(state: AssemblyState): return {'validation_results': ['UT_Passed', 'Tensile_Check_OK']}; def check_compliance(state: AssemblyState): return {'status': 'Approved' if 'UT_Passed' in state['validation_results'] else 'Rejected'}; graph = StateGraph(AssemblyState); graph.add_node('integrity_check', validate_integrity); graph.add_node('compliance_review', check_compliance); graph.set_entry_point('integrity_check'); graph.add_edge('integrity_check', 'compliance_review'); graph.add_edge('compliance_review', END); graph = graph.compile()