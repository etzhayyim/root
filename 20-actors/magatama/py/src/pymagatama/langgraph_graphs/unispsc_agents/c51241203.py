from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_name: str; purity_level: float; compliance_docs: List[str]; def validate_purity(state: ChemicalState): return {'compliance_docs': state['compliance_docs'] + ['Purity Verified'] if state['purity_level'] >= 99.0 else ['Purity Rejected']}; def finalize_procurement(state: ChemicalState): return {'compliance_docs': state['compliance_docs'] + ['Procurement Approved']}; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_purity); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()