from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): grid_specs: dict; validation_errors: List[str]; approved: bool; def validate_grid(state: State): specs = state.get('grid_specs', {}); errors = []; if not specs.get('grid_dimensions'): errors.append('Missing dimensions'); if not specs.get('target_grade_level'): errors.append('Missing grade level'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(State); graph.add_node('validate', validate_grid); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()