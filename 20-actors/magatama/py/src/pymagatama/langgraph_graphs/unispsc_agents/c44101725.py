from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CabinetState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: CabinetState): load = state['specs'].get('load_capacity', 0); state['valid'] = load > 0; state['error'] = '' if state['valid'] else 'Invalid weight capacity'; return state; graph = StateGraph(CabinetState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()