from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IvRackState(TypedDict): vendor_id: str; specifications: dict; compliance_verified: bool; def validate_spec(state: IvRackState): state['compliance_verified'] = state['specifications'].get('load_capacity', 0) > 0; return state; def check_certification(state: IvRackState): state['compliance_verified'] = 'ISO13485' in state['specifications'].get('certs', []); return state; graph = StateGraph(IvRackState); graph.add_node('validate_spec', validate_spec); graph.add_node('check_cert', check_certification); graph.set_entry_point('validate_spec'); graph.add_edge('validate_spec', 'check_cert'); graph.add_edge('check_cert', END); app = graph.compile()