from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BioSampleState(TypedDict): sample_type: str; is_sterile: bool; temp_logs: List[float]; valid: bool; def check_sterility(state: BioSampleState): return {'valid': state['is_sterile']}; def validate_cold_chain(state: BioSampleState): return {'valid': all(t < -20 for t in state['temp_logs'])}; graph = StateGraph(BioSampleState); graph.add_node('check_sterility', check_sterility); graph.add_node('validate_cold_chain', validate_cold_chain); graph.set_entry_point('check_sterility'); graph.add_edge('check_sterility', 'validate_cold_chain'); graph.add_edge('validate_cold_chain', END); graph = graph.compile()