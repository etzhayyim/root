from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterSpec(TypedDict): model_no: str; efficiency: str; compliance: bool; def validate_spec(state: FilterSpec): return {'compliance': state['efficiency'] in ['N95', 'P100']} if state['model_no'] else {'compliance': False}; graph = StateGraph(FilterSpec); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()