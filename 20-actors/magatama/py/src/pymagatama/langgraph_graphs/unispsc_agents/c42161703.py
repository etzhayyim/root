from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_certs: List[str]; qc_passed: bool; specs: dict; def validate_cert(state: ProcureState): return {'qc_passed': 'ISO13485' in state['vendor_certs']}; def assembly_gate(state: ProcureState): return {'qc_passed': state['specs'].get('sterile', False)}; graph = StateGraph(ProcureState); graph.add_node('cert_check', validate_cert); graph.add_node('sterile_check', assembly_gate); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'sterile_check'); graph.add_edge('sterile_check', END); compiled_graph = graph.compile()