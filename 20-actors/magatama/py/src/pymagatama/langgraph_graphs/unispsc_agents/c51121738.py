from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_name: str; validation_passed: bool; risk_assessment: List[str]; def validate_chemical(state: ProcState): state['validation_passed'] = 'Deserpidine' in state['chemical_name']; return state; def assess_risk(state: ProcState): state['risk_assessment'] = ['regulated_health_product', 'dangerous_goods']; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_chemical); graph.add_node('risk', assess_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'risk'); graph.add_edge('risk', END); compiled_graph = graph.compile()