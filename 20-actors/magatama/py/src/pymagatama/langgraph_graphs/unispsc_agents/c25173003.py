from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RailLightState(TypedDict): spec: dict; validated: bool; errors: List[str]; def validate_spec(state: RailLightState): spec = state['spec']; errors = []; if spec.get('fire_rating') != 'EN45545': errors.append('Missing EN45545 certification'); return {'validated': len(errors) == 0, 'errors': errors}; graph = StateGraph(RailLightState); graph.add_node('validation', validate_spec); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph.compile()