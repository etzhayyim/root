from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CopperProcurementState(TypedDict): vendor_certs: dict; quality_check: bool; material_specs: dict; graph = StateGraph(CopperProcurementState); def validate_material(state): return {'quality_check': state['material_specs'].get('grade') == 'Certified-Copper'}; def verify_certs(state): return {'quality_check': 'ISO-9001' in state['vendor_certs'].get('standards', [])}; graph.add_node('validate', validate_material); graph.add_node('certify', verify_certs); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); compiled_graph = graph.compile()