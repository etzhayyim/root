from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ImportState(TypedDict): vendor_docs: List[str]; inspection_passed: bool; quality_score: float; def inspect_lot(state: ImportState): state['inspection_passed'] = 'certification' in state['vendor_docs']; return state; def validate_quality(state: ImportState): state['quality_score'] = 1.0 if state['inspection_passed'] else 0.0; return state; graph = StateGraph(ImportState); graph.add_node('inspection', inspect_lot); graph.add_node('quality_check', validate_quality); graph.set_entry_point('inspection'); graph.add_edge('inspection', 'quality_check'); graph.add_edge('quality_check', END); graph = graph.compile()