from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): chemical_name: str; compliance_docs: List[str]; approved: bool; def validate_purity(state: PharmaState): print('Validating purity...'); state['approved'] = 'GMP' in state['compliance_docs']; return state; builder = StateGraph(PharmaState); builder.add_node('validate', validate_purity); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()