from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurfboardState(TypedDict): board_type: str; specs: dict; approved: bool; def validate_specs(state: SurfboardState): return {'approved': 'length' in state['specs'] and 'material' in state['specs']}; graph = StateGraph(SurfboardState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()