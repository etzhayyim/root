from langgraph.graph import StateGraph, END; from typing import TypedDict; class ClockState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ClockState): state['valid'] = 'material_composition' in state['specs'] and 'accuracy_tolerance' in state['specs']; return state; builder = StateGraph(ClockState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()