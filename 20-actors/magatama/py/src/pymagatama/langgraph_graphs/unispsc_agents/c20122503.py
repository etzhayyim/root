from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): gripper_id: str; specs: dict; validation_status: bool; def validate_gripper(state: RobotState): print(f'Validating gripper {state[gripper_id]}'); return {'validation_status': True}; def process_assembly(state: RobotState): print('Processing assembly workflow'); return {'validation_status': True}; graph = StateGraph(RobotState); graph.add_node('validate', validate_gripper); graph.add_node('assemble', process_assembly); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()