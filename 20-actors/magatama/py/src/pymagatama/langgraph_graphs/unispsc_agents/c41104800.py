from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LabProcessState(TypedDict): input_specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_equipment(state: LabProcessState): specs = state['input_specs']; results = ['Validated thermal specs' if 'thermal' in specs else 'Thermal missing']; return {'validation_results': results, 'status': 'COMPLETE'}; def export_compliance_check(state: LabProcessState): return {'status': 'EXPORT_SCREENED'}; graph = StateGraph(LabProcessState); graph.add_node('validate', validate_equipment); graph.add_node('compliance', export_compliance_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.compile()