from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BookProcurementState(TypedDict): content_verified: bool; quality_passed: bool; order_list: List[str]; def verify_content(state: BookProcurementState): return {'content_verified': True}; def inspect_quality(state: BookProcurementState): return {'quality_passed': True}; graph = StateGraph(BookProcurementState); graph.add_node('verify', verify_content); graph.add_node('inspect', inspect_quality); graph.set_entry_point('verify'); graph.add_edge('verify', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()