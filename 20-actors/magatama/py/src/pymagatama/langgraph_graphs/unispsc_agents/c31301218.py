from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgeState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_metallurgy(state: ForgeState): state['validation_errors'] = [] if 'cert' not in state['specs'] else []; return state; def check_dimensions(state: ForgeState): return state; graph = StateGraph(ForgeState); graph.add_node('validate_metallurgy', validate_metallurgy); graph.add_node('check_dimensions', check_dimensions); graph.set_entry_point('validate_metallurgy'); graph.add_edge('validate_metallurgy', 'check_dimensions'); graph.add_edge('check_dimensions', END); graph = graph.compile()