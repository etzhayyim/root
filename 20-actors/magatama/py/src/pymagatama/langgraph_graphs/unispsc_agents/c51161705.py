from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): medicine_name: str; temp_log: List[float]; qc_passed: bool; def validate_temp(state: ProcureState): return {'qc_passed': all(15 <= t <= 25 for t in state['temp_log'])}; def check_purity(state: ProcureState): return {'qc_passed': True}; graph = StateGraph(ProcureState); graph.add_node('val_temp', validate_temp); graph.add_node('check_purity', check_purity); graph.set_entry_point('val_temp'); graph.add_edge('val_temp', 'check_purity'); graph.add_edge('check_purity', END); graph = graph.compile()