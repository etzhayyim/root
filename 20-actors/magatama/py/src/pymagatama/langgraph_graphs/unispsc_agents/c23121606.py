from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: CompressorState): results = []; if state['specs'].get('pressure', 0) > 1000: results.append('Needs pressure vessel certification'); return {'validation_results': results}; def finalize_order(state: CompressorState): return {'validation_results': state['validation_results'] + ['Order validated']}; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()