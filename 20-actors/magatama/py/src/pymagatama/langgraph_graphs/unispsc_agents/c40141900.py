from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DuctState(TypedDict): specs: dict; valid: bool; error: str; def validate_duct_spec(state: DuctState): required = ['material', 'pressure_rating']; state['valid'] = all(k in state['specs'] for k in required); return state; graph = StateGraph(DuctState); graph.add_node('validate', validate_duct_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()