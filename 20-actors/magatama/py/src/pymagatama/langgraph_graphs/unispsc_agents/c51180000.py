from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HormoneProcureState(TypedDict): medicine_code: str; quality_checks: List[str]; approved: bool; def validate_regulatory(state: HormoneProcureState): state['quality_checks'].append('GMP_Verified'); return state; def check_storage(state: HormoneProcureState): state['quality_checks'].append('Cold_Chain_Validated'); state['approved'] = True; return state; graph = StateGraph(HormoneProcureState); graph.add_node('verify_regulatory', validate_regulatory); graph.add_node('check_storage', check_storage); graph.set_entry_point('verify_regulatory'); graph.add_edge('verify_regulatory', 'check_storage'); graph.add_edge('check_storage', END); graph = graph.compile()