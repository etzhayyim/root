from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): fabric: str; quantity: int; specs_verified: bool; def validate_materials(state: State): state['specs_verified'] = state['fabric'] in ['silk', 'polyester', 'nylon']; return state; def check_stock(state: State): return 'stock_pass' if state['quantity'] > 0 else 'stock_fail'; graph = StateGraph(State); graph.add_node('validate', validate_materials); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()