from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class FurnaceState(TypedDict): specs: dict; compliance_status: bool; def validate_specs(state: FurnaceState): specs = state.get('specs', {}); return {'compliance_status': specs.get('max_temp', 0) > 0}; def generate_approval(state: FurnaceState): return {'compliance_status': True}; graph = StateGraph(FurnaceState); graph.add_node('validate', validate_specs); graph.add_node('approve', generate_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()