from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): material_spec: dict; compliance_check: bool; graph = StateGraph(State); def validate_specs(state: State): state['compliance_check'] = 'material_id' in state['material_spec'] and state['material_spec']['temp_rating'] > 200; return state; def check_export(state: State): return 'compliance_check' if state['compliance_check'] else END; graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate')