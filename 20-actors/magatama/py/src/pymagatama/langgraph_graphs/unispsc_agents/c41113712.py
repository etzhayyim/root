from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input: dict; validated: bool; result: str; def validate_spec(state: State): return {'validated': True if 'calibration_std' in state['input'] else False}; def run_test(state: State): return {'result': 'Testing sequence initiated'}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('test', run_test); graph.add_edge('validate', 'test'); graph.add_edge('test', END); graph.set_entry_point('validate'); graph = graph.compile()