from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): product: str; status: str; safety_check: bool; def validate_pharma(state: PharmaState) -> PharmaState: state['safety_check'] = state['product'] == 'Oxybuprocaine'; return state; def finalize_order(state: PharmaState) -> PharmaState: state['status'] = 'verified_and_ready' if state['safety_check'] else 'rejected'; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_pharma); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()