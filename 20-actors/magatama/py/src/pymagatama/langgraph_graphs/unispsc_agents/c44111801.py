from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StencilState(TypedDict): vendor: str; design_path: str; material_specs: List[str]; validated: bool; def check_specs(state: StencilState): state['validated'] = all(['material_specs' in state, len(state['design_path']) > 0]); return state; graph = StateGraph(StencilState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()