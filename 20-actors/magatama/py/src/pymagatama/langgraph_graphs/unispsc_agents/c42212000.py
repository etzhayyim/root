from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AidState(TypedDict): aid_type: str; is_compliant: bool; validation_log: List[str]; def check_compliance(state: AidState): return {'is_compliant': True, 'validation_log': ['Safety standards met']}; def route(state: AidState): return 'end'; graph = StateGraph(AidState); graph.add_node('verify', check_compliance); graph.set_entry_point('verify'); graph.add_edge('verify', END); graph.compile()