from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GantryState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: GantryState): results = []; if state['specs'].get('accuracy', 0) < 0.01: results.append('Passed Precision Test'); return {'validation_results': results, 'status': 'VALID'}; def approve_procurement(state: GantryState): return {'status': 'APPROVED'}; graph = StateGraph(GantryState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()