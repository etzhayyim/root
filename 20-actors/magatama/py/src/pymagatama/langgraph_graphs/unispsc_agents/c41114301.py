from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlowState(TypedDict): meter_id: str; data: dict; valid: bool; def validate_meter(state: FlowState): state['valid'] = 'accuracy' in state['data'] and state['data']['accuracy'] < 0.05; return state; graph = StateGraph(FlowState); graph.add_node('validate', validate_meter); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()