from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetyFlowState(TypedDict): spec_data: dict; approval_status: bool; validation_log: List[str]; def validate_safety_rating(state: SafetyFlowState): rating = state['spec_data'].get('SIL', 0); state['approval_status'] = rating >= 2; state['validation_log'].append(f'SIL rating check: {rating}'); return state; def final_approval(state: SafetyFlowState): return {'approval_status': state['approval_status']}; graph = StateGraph(SafetyFlowState); graph.add_node('safety_check', validate_safety_rating); graph.add_node('approval', final_approval); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'approval'); graph.add_edge('approval', END); graph = graph.compile()