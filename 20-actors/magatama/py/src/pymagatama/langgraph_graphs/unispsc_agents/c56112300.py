from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SeatingState(TypedDict): parts_list: List[str]; inspection_results: dict; status: str; def validate_spec(state: SeatingState) -> SeatingState: state['inspection_results'] = {'passed': True}; state['status'] = 'verified'; return state; def check_safety(state: SeatingState) -> SeatingState: state['status'] = 'safety_checked'; return state; graph = StateGraph(SeatingState); graph.add_node('validate', validate_spec); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compile_graph = graph.compile()