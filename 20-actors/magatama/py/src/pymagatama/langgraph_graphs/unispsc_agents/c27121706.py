from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_report: List[str]; def validate_specs(state: ProcurementState): spec = state['specs']; errors = []; if 'material' not in spec: errors.append('Missing material grade'); return {'validation_report': errors}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()