from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalAlloyState(TypedDict): alloy_data: dict; compliance_checks: List[str]; approved: bool; def validate_composition(state: DentalAlloyState): state["compliance_checks"].append("Composition matches ASTM F75"); return state; def check_certification(state: DentalAlloyState): state["approved"] = "ISO_13485" in state["alloy_data"].get("certs", []); return state; graph = StateGraph(DentalAlloyState); graph.add_node("validate", validate_composition); graph.add_node("certify", check_certification); graph.add_edge("validate", "certify"); graph.add_edge("certify", END); graph.set_entry_point("validate"); graph = graph.compile()