from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KiwiState(TypedDict): origin: str; brix: float; status: str; def validate_quality(state: KiwiState): return {'status': 'approved' if state['brix'] > 12.0 else 'rejected'}; graph = StateGraph(KiwiState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()