from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GeneratorState(TypedDict): input_specs: dict; validation_report: list[str]; approved: bool; def validate_hydraulic_specs(state: GeneratorState): specs = state['input_specs']; reports = []; validated = True; if specs.get('pressure', 0) > 350: reports.append('Pressure rating excessive'); validated = False; return {'validation_report': reports, 'approved': validated}; graph = StateGraph(GeneratorState); graph.add_node('validate', validate_hydraulic_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()