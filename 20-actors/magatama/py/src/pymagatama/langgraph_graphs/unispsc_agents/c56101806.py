from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; valid: bool; error: List[str]; def validate_specs(state: State): specs = state['specs']; errors = []; if 'safety_cert' not in specs: errors.append('Missing safety certification'); return {'valid': len(errors) == 0, 'error': errors}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()