from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipeState(TypedDict): doc_ids: List[str]; validation_results: dict; def validate_grade(state: PipeState): return {'validation_results': {'grade': 'Waspalloy-Verified'}}; def inspect_welds(state: PipeState): return {'validation_results': {'weld': 'Certified'}}; graph = StateGraph(PipeState); graph.add_node('grade_check', validate_grade); graph.add_node('weld_inspection', inspect_welds); graph.set_entry_point('grade_check'); graph.add_edge('grade_check', 'weld_inspection'); graph.add_edge('weld_inspection', END); graph = graph.compile()