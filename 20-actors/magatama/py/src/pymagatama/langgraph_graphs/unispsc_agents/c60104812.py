from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[str]; inspection_passed: bool; def validate_optics(state: State): return {'inspection_passed': len(state['items']) > 0}; def finalize_order(state: State): return {'inspection_passed': True}; graph = StateGraph(State); graph.add_node('validate', validate_optics); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()