from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RefrigVesselState(TypedDict): vessel_id: str; temp_range: float; status: str; def validate_specs(state: RefrigVesselState): return {'status': 'validated'} if state['temp_range'] < 0 else {'status': 'rejected'}; def check_compliance(state: RefrigVesselState): return {'status': 'compliant'}; graph = StateGraph(RefrigVesselState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()