from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KitState(TypedDict): kit_type: str; purity: float; cold_chain_required: bool; validation_passed: bool; def validate_kit(state: KitState): state['validation_passed'] = state['purity'] >= 0.95 and state['cold_chain_required'] == True; return state; graph = StateGraph(KitState); graph.add_node('validate', validate_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()