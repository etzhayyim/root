from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): progress: str; safety_checks: bool; def audit_safety(state: State): return {'safety_checks': True} if state.get('progress') == 'installed' else {'safety_checks': False}; def finish(state: State): return {'progress': 'certified'}; graph = StateGraph(State); graph.add_node('audit', audit_safety); graph.add_node('final', finish); graph.set_entry_point('audit'); graph.add_edge('audit', 'final'); graph.add_edge('final', END); compiled_graph = graph.compile()