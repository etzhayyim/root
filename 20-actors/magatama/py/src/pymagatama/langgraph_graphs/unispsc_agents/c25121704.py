from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RailState(TypedDict): component_id: str; specs: dict; verified: bool; def validate_specs(state: RailState): is_valid = all(key in state['specs'] for key in ['hardness', 'material']); return {'verified': is_valid}; def route_procurement(state: RailState): return 'approval' if state['verified'] else END; graph = StateGraph(RailState); graph.add_node('validate', validate_specs); graph.add_node('approval', lambda x: {'verified': True}); graph.add_edge('validate', 'approval'); graph.set_entry_point('validate'); graph.set_finish_point('approval'); compiled_graph = graph.compile()