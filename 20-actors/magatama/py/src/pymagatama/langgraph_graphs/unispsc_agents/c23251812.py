from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolingState(TypedDict): specs: dict; validation_report: List[str]; def validate_specs(state: ToolingState): specs = state['specs']; errors = []; if 'pitch' not in specs: errors.append('Pitch missing'); state['validation_report'] = errors; return state; graph = StateGraph(ToolingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()