from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WindowState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: WindowState): u_val = state['specs'].get('u_val', 10); state['valid'] = u_val < 3.0; state['error'] = None if state['valid'] else 'Poor insulation'; return state; graph = StateGraph(WindowState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()