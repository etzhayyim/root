from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PhonicsState(TypedDict): content: str; validated: bool; spec_compliance: List[str]; def validate_materials(state: PhonicsState): return {'validated': True, 'spec_compliance': ['ISO-8124-3']}; def generate_procurement_report(state: PhonicsState): return {'spec_compliance': state['spec_compliance'] + ['verified']}; graph = StateGraph(PhonicsState); graph.add_node('validate', validate_materials); graph.add_node('report', generate_procurement_report); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()