from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeState(TypedDict): tube_specs: dict; inspection_results: List[str]; validated: bool; def validate_specs(state: TubeState): state['validated'] = 'ASTM_Grade' in state['tube_specs']; return state; def run_inspection(state: TubeState): if state['validated']: state['inspection_results'].append('Ultrasonic Audit Complete'); return state; graph = StateGraph(TubeState); graph.add_node('validate', validate_specs); graph.add_node('inspect', run_inspection); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); compiled = graph.compile()