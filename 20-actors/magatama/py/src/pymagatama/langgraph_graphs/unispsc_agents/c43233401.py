from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoftwareState(TypedDict): software_spec: dict; validation_results: List[str]; def validate_compatibility(state): return {'validation_results': ['OS compliant']}; def check_security(state): return {'validation_results': ['Encryption verified']}; graph = StateGraph(SoftwareState); graph.add_node('validate', validate_compatibility); graph.add_node('secure', check_security); graph.add_edge('validate', 'secure'); graph.add_edge('secure', END); graph.set_entry_point('validate'); graph = graph.compile()