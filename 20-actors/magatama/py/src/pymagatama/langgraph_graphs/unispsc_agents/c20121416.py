from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class LinearBearingState(TypedDict): bearing_id: str; spec_data: dict; validation_results: list; status: str; def validate_bearing(state: LinearBearingState): spec = state['spec_data']; errors = []; if spec.get('load_capacity_n', 0) <= 0: errors.append('Invalid load capacity'); return {'validation_results': errors, 'status': 'validated' if not errors else 'failed'}; def compile_workflow(): workflow = StateGraph(LinearBearingState); workflow.add_node('validate', validate_bearing); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); return workflow.compile(); graph = compile_workflow()