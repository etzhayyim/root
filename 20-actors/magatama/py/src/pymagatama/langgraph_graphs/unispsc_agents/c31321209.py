from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_assembly(state: AssemblyState): state['valid'] = 'material' in state['specs'] and 'weld_quality' in state['specs']; state['logs'].append('Validation completed'); return state; builder = StateGraph(AssemblyState); builder.add_node('validator', validate_assembly); builder.set_entry_point('validator'); builder.add_edge('validator', END); graph = builder.compile()