from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalSpecState(TypedDict): abrasive_type: str; grit_level: int; compliant: bool; def validate_specs(state: DentalSpecState): state['compliant'] = state['grit_level'] > 0; return state; graph = StateGraph(DentalSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()