from langgraph.graph import StateGraph, END; from typing import TypedDict; class HublockState(TypedDict): specs: dict; valid: bool; def validate_specs(state: HublockState): state['valid'] = 'Material Grade' in state['specs'] and 'Hardness Rating' in state['specs']; return state; builder = StateGraph(HublockState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()