from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; purity: float; cold_chain_verified: bool; def validate_purity(state: PharmaState): return {'purity': state['purity'] >= 0.99}; def check_storage(state: PharmaState): return {'cold_chain_verified': True} if state['cold_chain_verified'] else {'cold_chain_verified': False}; graph = StateGraph(PharmaState); graph.add_node('purity_check', validate_purity); graph.add_node('storage_check', check_storage); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'storage_check'); graph.add_edge('storage_check', END); graph = graph.compile()