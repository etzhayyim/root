from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: BearingState): specs = state['specs']; required = ['bore_diameter', 'load_capacity']; state['valid'] = all(k in specs for k in required) and specs['bore_diameter'] > 0; return state; graph = StateGraph(BearingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()