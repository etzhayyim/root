from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AbacusState(TypedDict): specs: dict; valid: bool; def validate_specs(state: AbacusState): return {'valid': 'bead_count' in state['specs'] and 'material_composition' in state['specs']}; builder = StateGraph(AbacusState); builder.add_node('validate', validate_specs); builder.add_edge('validate', END); builder.set_entry_point('validate'); graph = builder.compile()