from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): purity_check: bool; trace_analysis: list[str]; risk_approved: bool; graph = StateGraph(CatalystState); def validate_purity(state: CatalystState): return {'purity_check': True}; def perform_trace_analysis(state: CatalystState): return {'trace_analysis': ['check_heavy_metals', 'check_residue']}; def approve_risk(state: CatalystState): return {'risk_approved': True}; graph.add_node('validate', validate_purity); graph.add_node('analysis', perform_trace_analysis); graph.add_node('approval', approve_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'analysis'); graph.add_edge('analysis', 'approval'); graph.add_edge('approval', END); graph = graph.compile()