from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity_percent: float; compliance_verified: bool; graph = StateGraph(PharmaState); def validate_purity(state: PharmaState): return {'compliance_verified': state['purity_percent'] >= 99.0}; def audit_documentation(state: PharmaState): return {'compliance_verified': True}; graph.add_node('validate', validate_purity); graph.add_node('audit', audit_documentation); graph.set_entry_point('validate'); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); graph = graph.compile()