from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ToolSpecState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_dimensions(state: ToolSpecState): return {'validation_results': ['Dimensions verified'], 'status': 'validating'}; def check_material_safety(state: ToolSpecState): return {'validation_results': ['Material safety confirmed'], 'status': 'completing'}; graph = StateGraph(ToolSpecState); graph.add_node('dimension_check', validate_dimensions); graph.add_node('material_check', check_material_safety); graph.set_entry_point('dimension_check'); graph.add_edge('dimension_check', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()