from langgraph.graph import StateGraph, END; class State(dict): pass; def validate_specs(state): return {'status': 'validated'}; def check_export_control(state): return {'clearance': True}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()