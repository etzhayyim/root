from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SpaState(TypedDict): vendor_id: str; specifications: dict; compliance_verified: bool; graph = StateGraph(SpaState); def validate_specs(state: SpaState): state['compliance_verified'] = 'Water Filtration Certification' in state['specifications']; return state; def finalize_procurement(state: SpaState): return state; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()