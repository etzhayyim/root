from langgraph.graph import StateGraph, END; from typing import TypedDict; class FilterPressState(TypedDict): specs: dict; valid: bool; def validate_specs(state: FilterPressState): state["valid"] = "pressure_rating" in state["specs"]; return state; graph = StateGraph(FilterPressState); graph.add_node("validate", validate_specs); graph.set_entry_point("validate"); graph.add_edge("validate", END); compiled_graph = graph.compile()