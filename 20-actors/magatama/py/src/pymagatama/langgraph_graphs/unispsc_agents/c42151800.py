from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): items: List[str]; qc_passed: bool; def validate_cert(state: DentalState): state['qc_passed'] = all(['ISO_13485' in item for item in state['items']]); return state; def route_procurement(state: DentalState): return 'qc' if state['qc_passed'] else END; graph = StateGraph(DentalState); graph.add_node('qc', validate_cert); graph.set_entry_point('qc'); graph.add_edge('qc', END); graph = graph.compile()