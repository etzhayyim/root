from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoftwareProcurementState(TypedDict): vendor: str; verifier_logs: List[str]; approved: bool; def validate_driver(state: SoftwareProcurementState): state['verifier_logs'].append('Verifying kernel-mode signature'); state['approved'] = True; return state; graph = StateGraph(SoftwareProcurementState); graph.add_node('validate', validate_driver); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()