from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChipperState(TypedDict): specs: dict; safety_check: bool; ready_for_procurement: bool; def validate_specs(state: ChipperState): safety = state['specs'].get('safety_features', False); return {'safety_check': safety}; def approval_node(state: ChipperState): return {'ready_for_procurement': state.get('safety_check', False)}; graph = StateGraph(ChipperState); graph.add_node('validate', validate_specs); graph.add_node('approval', approval_node); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph.set_entry_point('validate'); graph = graph.compile()