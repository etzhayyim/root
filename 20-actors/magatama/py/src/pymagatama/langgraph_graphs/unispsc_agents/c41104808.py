from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_check: bool; spec_compliance: bool; items: List[str]; graph = StateGraph(ProcState); def validate_specs(state: ProcState): state['spec_compliance'] = True; return state; def verify_compliance(state: ProcState): return 'certified' if state['spec_compliance'] else 'rejected'; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()