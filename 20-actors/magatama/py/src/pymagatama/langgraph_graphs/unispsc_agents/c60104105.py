from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TeachingAidState(TypedDict): specs: dict; valid: bool; workflow_steps: List[str]; def validate_specs(state: TeachingAidState): state['valid'] = 'anatomical_accuracy_certification' in state['specs']; return state; def finalize_workflow(state: TeachingAidState): state['workflow_steps'] = ['CAD_verification', 'Material_Safety_Check']; return state; graph = StateGraph(TeachingAidState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()