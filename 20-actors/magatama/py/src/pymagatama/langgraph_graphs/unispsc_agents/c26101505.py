from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SteamEngineState(TypedDict): specs: dict; compliance_status: bool; validation_log: List[str]; def validate_pressure_vessel(state: SteamEngineState): state['validation_log'].append('Checking pressure vessel compliance...'); state['compliance_status'] = True; return state; graph = StateGraph(SteamEngineState); graph.add_node('validate', validate_pressure_vessel); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()