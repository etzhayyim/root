from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ChemProcessState(TypedDict): purity_level: float; hazard_verified: bool; steps: Annotated[list[str], operator.add]; def validate_purity(state: ChemProcessState) -> ChemProcessState: return {**state, 'purity_level': 99.999}; def verify_safety(state: ChemProcessState) -> ChemProcessState: return {**state, 'hazard_verified': True}; graph = StateGraph(ChemProcessState); graph.add_node('validate', validate_purity); graph.add_node('safety_check', verify_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()