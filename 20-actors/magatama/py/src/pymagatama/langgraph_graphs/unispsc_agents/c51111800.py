from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HormoneProcessState(TypedDict): chemical_id: str; purity: float; storage_temp: float; status: str; def validate_purity(state: HormoneProcessState): state["status"] = "valid" if state["purity"] >= 0.99 else "rejected"; return state; def check_storage(state: HormoneProcessState): state["status"] = "compliant" if -20 <= state["storage_temp"] <= 5 else "hazardous"; return state; graph = StateGraph(HormoneProcessState); graph.add_node("validate", validate_purity); graph.add_node("storage", check_storage); graph.set_entry_point("validate"); graph.add_edge("validate", "storage"); graph.add_edge("storage", END); graph = graph.compile()