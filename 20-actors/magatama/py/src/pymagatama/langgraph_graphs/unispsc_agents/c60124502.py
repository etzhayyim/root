from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ResinState(TypedDict): resin_type: str; specs: dict; approved: bool; def validate_specs(state: ResinState): return {'approved': state['specs'].get('viscosity_cps', 0) > 0}; graph = StateGraph(ResinState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()