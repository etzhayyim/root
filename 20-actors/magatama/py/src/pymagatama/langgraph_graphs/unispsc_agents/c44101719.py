from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): model: str; part_id: str; status: str; def validate_compatibility(state: State): return {'status': 'validated'} if state.get('model') else {'status': 'error'}; def check_oem_stock(state: State): return {'status': 'checked'}; graph = StateGraph(State); graph.add_node('validate', validate_compatibility); graph.add_node('stock_check', check_oem_stock); graph.set_entry_point('validate'); graph.add_edge('validate', 'stock_check'); graph.add_edge('stock_check', END); graph = graph.compile()