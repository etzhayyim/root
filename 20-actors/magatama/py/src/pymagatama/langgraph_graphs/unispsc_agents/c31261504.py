from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HousingState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: HousingState): state['validation_results'].append('Dimensional check passed'); return state; def check_material(state: HousingState): state['validation_results'].append('Material certification verified'); return state; graph = StateGraph(HousingState); graph.add_node('validate_dim', validate_dimensions); graph.add_node('check_mat', check_material); graph.set_entry_point('validate_dim'); graph.add_edge('validate_dim', 'check_mat'); graph.add_edge('check_mat', END); graph = graph.compile()