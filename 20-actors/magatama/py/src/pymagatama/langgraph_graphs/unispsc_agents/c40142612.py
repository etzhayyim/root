from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AdapterState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_material(state: AdapterState): state['log'].append('Checking material compatibility'); state['valid'] = state['specs'].get('material') is not None; return state; def check_pressure(state: AdapterState): state['log'].append('Verifying pressure rating'); return state; graph = StateGraph(AdapterState); graph.add_node('material', validate_material); graph.add_node('pressure', check_pressure); graph.set_entry_point('material'); graph.add_edge('material', 'pressure'); graph.add_edge('pressure', END); compile_graph = graph.compile()