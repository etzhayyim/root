from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_spec: dict; validated: bool; analysis: str; def validate_specs(state: State): state['validated'] = 'max_cutting_capacity_mm' in state['input_spec']; return state; def analyze_safety(state: State): state['analysis'] = 'Safety check passed' if state['validated'] else 'Safety check failed'; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('safety', analyze_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()