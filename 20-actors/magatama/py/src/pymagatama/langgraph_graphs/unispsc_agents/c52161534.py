from langgraph.graph import StateGraph, END; from typing; import TypedDict; class ICProcurementState(TypedDict): component_id: str; spec_validation: bool; export_control_flag: bool; def validate_specs(state: ICProcurementState): return {'spec_validation': True if state['component_id'] else False}; def check_export_compliance(state: ICProcurementState): return {'export_control_flag': True}; graph = StateGraph(ICProcurementState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()