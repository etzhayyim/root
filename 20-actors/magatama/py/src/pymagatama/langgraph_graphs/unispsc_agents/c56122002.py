from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabStorageState(TypedDict): specs: dict; validation_results: List[str]; def validate_chemical_resistance(state: LabStorageState): state['validation_results'].append('Checked material compatibility'); return state; def check_fire_safety(state: LabStorageState): state['validation_results'].append('Verified fire-rating'); return state; graph = StateGraph(LabStorageState); graph.add_node('validate_chem', validate_chemical_resistance); graph.add_node('check_safety', check_fire_safety); graph.set_entry_point('validate_chem'); graph.add_edge('validate_chem', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()