from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_plastic(state: State): specs = state.get('specs', {}); is_valid = all(key in specs for key in ['material', 'tolerance']); return {'valid': is_valid, 'log': ['Validation complete'] if is_valid else ['Missing specs']}; graph = StateGraph(State); graph.add_node('validate', validate_plastic); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()