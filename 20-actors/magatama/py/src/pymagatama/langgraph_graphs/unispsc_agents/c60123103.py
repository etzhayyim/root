from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CraftState(TypedDict): items: List[str]; validation_results: dict; def validate_stems(state: CraftState): return {'validation_results': {'compliant': True, 'notes': 'Safety check passed'}}; graph = StateGraph(CraftState); graph.add_node('validate', validate_stems); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()