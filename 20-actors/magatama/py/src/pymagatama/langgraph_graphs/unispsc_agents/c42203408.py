from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_id: str; quality_clearance: bool; spec_fields: List[str]; def validate_medical_device(state: ProcState): return {'quality_clearance': True} if state['vendor_id'] else {'quality_clearance': False}; graph = StateGraph(ProcState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()