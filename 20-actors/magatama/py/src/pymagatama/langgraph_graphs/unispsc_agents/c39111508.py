from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TrackLightingState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state): state['compliance'] = all(k in state['specs'] for k in ['CRI', 'Voltage']); return state; def final_approval(state): state['approved'] = state['compliance']; return state; graph = StateGraph(TrackLightingState); graph.add_node('validate', validate_specs); graph.add_node('approval', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); app = graph.compile()