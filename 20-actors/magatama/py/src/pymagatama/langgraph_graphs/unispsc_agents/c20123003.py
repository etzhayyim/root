from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): load_capacity: float; material_specs: str; verification_status: bool; workflow_log: Annotated[Sequence[str], operator.add]; def validate_load(state: BearingState) -> BearingState: print('Validating Load'); return {'verification_status': state['load_capacity'] > 0, 'workflow_log': ['Load Validated']}; def check_material(state: BearingState) -> BearingState: print('Checking Material'); return {'workflow_log': ['Material Checked']}; graph = StateGraph(BearingState); graph.add_node('validate', validate_load); graph.add_node('material', check_material); graph.add_edge('validate', 'material'); graph.add_edge('material', END); graph.set_entry_point('validate'); graph = graph.compile()