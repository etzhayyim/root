from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; compliance_docs: List[str]; approved: bool; def validate_purity(state: PharmState): state['approved'] = state['purity'] >= 99.0; return state; def check_regulations(state: PharmState): state['compliance_docs'].append('ISO_GXP_CERT'); return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()