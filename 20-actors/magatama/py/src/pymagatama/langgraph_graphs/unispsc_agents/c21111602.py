from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class IrrigationValveState(TypedDict): valve_specs: dict; validation_results: List[str]; graph = StateGraph(IrrigationValveState); def validate_specs(state: IrrigationValveState): return {'validation_results': ['Pressure test passed', 'Material integrity verified']}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()