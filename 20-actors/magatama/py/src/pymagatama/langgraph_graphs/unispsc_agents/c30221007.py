from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TheaterProcurementState(TypedDict): vendor_docs: List[str]; safety_certs: List[str]; status: str; def validate_specs(state: TheaterProcurementState): return {'status': 'validated' if 'fire_rating' in state['safety_certs'] else 'rejected'}; def check_load_capacity(state: TheaterProcurementState): return {'status': 'inspected'}; graph = StateGraph(TheaterProcurementState); graph.add_node('validate', validate_specs); graph.add_node('check_load', check_load_capacity); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_load'); graph.add_edge('check_load', END); graph.compile()