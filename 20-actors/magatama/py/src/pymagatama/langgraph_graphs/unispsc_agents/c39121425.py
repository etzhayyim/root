from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): spec: dict; verified: bool; error: List[str]; def validate_spec(state: ProcureState): print('Validating terminal block specs...'); return {'verified': True} if 'UL94' in state['spec'] else {'verified': False}; graph = StateGraph(ProcureState); graph.add_node('validation', validate_spec); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()