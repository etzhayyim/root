from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): structure_data: dict; approval_status: bool; def validate_specs(state: AssemblyState): state['approval_status'] = 'material_grade' in state['structure_data'] and 'tensile' in state['structure_data']; return state; def flag_high_value(state: AssemblyState): return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('check_value', flag_high_value); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_value'); graph.add_edge('check_value', END); app = graph.compile()