from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_specs: dict; compliance_status: bool; validation_log: List[str]; def validate_kettle_spec(state: ProcurementState): state['validation_log'].append('Validating pressure rating and material grade'); state['compliance_status'] = True; return state; def check_export_controls(state: ProcurementState): state['validation_log'].append('Checking dual-use export compliance'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_kettle_spec); graph.add_node('export_check', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()