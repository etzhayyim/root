from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_id: str; device_specs: dict; compliance_verified: bool; def validate_specs(state: ProcureState): return {'compliance_verified': 'flow_rate' in state['device_specs']}; def finalize_procurement(state: ProcureState): return {'compliance_verified': True}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()