from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): content: dict; status: str; def validate_safety(state: State): state['status'] = 'COMPLIANT' if 'toxicity_report' in state['content'] else 'REJECTED'; return state; def check_durability(state: State): return state; graph = StateGraph(State); graph.add_node('validate', validate_safety); graph.add_node('durability', check_durability); graph.add_edge('validate', 'durability'); graph.add_edge('durability', END); graph.set_entry_point('validate'); graph = graph.compile()