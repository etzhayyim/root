from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SplintState(TypedDict): splint_id: str; specs: dict; valid: bool; def check_medical_compliance(state: SplintState): state['valid'] = 'ISO_13485_certification' in state['specs']; return state; def validate_pressure_specs(state: SplintState): state['valid'] = state.get('valid') and 'pressure_regulation_mechanism' in state['specs']; return state; graph = StateGraph(SplintState); graph.add_node('compliance', check_medical_compliance); graph.add_node('pressure', validate_pressure_specs); graph.add_edge('compliance', 'pressure'); graph.add_edge('pressure', END); graph.set_entry_point('compliance'); graph = graph.compile()