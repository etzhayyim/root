from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): item_id: str; specs: dict; verified: bool; def validate_specs(state: ProcurementState): specs = state.get('specs', {}); state['verified'] = all(k in specs for k in ['slot_capacity', 'material']); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()