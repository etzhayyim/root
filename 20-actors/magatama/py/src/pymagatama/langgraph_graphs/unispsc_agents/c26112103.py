from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BrakeState(TypedDict): input_spec: dict; validation_report: list; approved: bool; def validate_specs(state: BrakeState): report = []; if state['input_spec'].get('torque', 0) <= 0: report.append('Invalid Torque'); return {'validation_report': report, 'approved': False}; def final_decision(state: BrakeState): return {'approved': len(state['validation_report']) == 0}; graph = StateGraph(BrakeState); graph.add_node('validate', validate_specs); graph.add_node('decision', final_decision); graph.set_entry_point('validate'); graph.add_edge('validate', 'decision'); graph.add_edge('decision', END); app = graph.compile()