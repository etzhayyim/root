from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): material: str; strength_rating: float; approved: bool; def validate_tensile(state): return {'approved': state['strength_rating'] > 1000}; def routing_logic(state): return 'validate' if state['material'] == 'steel' else END; graph = StateGraph(ProcureState); graph.add_node('validate', validate_tensile); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()