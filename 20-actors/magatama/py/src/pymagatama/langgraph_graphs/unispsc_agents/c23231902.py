from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class LaserState(TypedDict): input_specs: dict; evaluation: str; graph = StateGraph(LaserState); def validate_specs(state: LaserState): return {'evaluation': 'Validated' if state['input_specs'].get('wattage') > 0 else 'Failed'}; def finalize_procurement(state: LaserState): return {'evaluation': 'Ready to Procure'}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()