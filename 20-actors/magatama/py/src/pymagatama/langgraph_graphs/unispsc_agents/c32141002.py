from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KlystronState(TypedDict): specs: dict; valid: bool; export_check_passed: bool; def validate_specs(state: KlystronState): state['valid'] = state['specs'].get('power_output', 0) > 0; return state; def check_export_controls(state: KlystronState): state['export_check_passed'] = state['specs'].get('is_dual_use', True); return state; graph = StateGraph(KlystronState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_controls); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()