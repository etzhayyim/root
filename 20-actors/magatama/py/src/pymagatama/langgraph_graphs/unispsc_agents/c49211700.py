from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BowlingState(TypedDict): items: List[str]; validated_items: List[str]; def validate_item(state: BowlingState): return {"validated_items": [item for item in state["items"] if "certified" in item.lower()]}; def check_procurement(state: BowlingState): return {"validated_items": state["validated_items"] + ["COMPLIANT"]}; graph = StateGraph(BowlingState); graph.add_node("validate", validate_item); graph.add_node("compliance", check_procurement); graph.add_edge("validate", "compliance"); graph.add_edge("compliance", END); graph.set_entry_point("validate"); graph = graph.compile()