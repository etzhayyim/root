from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DispensingState(TypedDict): device_id: str; accuracy_data: dict; compliance_status: bool; def validate_spec(state: DispensingState): state['compliance_status'] = True if state['accuracy_data'].get('tolerance') < 0.05 else False; return state; def check_regulations(state: DispensingState): print('Checking medical device compliance...'); return state; graph = StateGraph(DispensingState); graph.add_node('validate', validate_spec); graph.add_node('regulatory', check_regulations); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); app = graph.compile()