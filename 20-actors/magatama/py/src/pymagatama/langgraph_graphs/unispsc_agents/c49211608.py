from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GolfScopeState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: GolfScopeState): print('Validating range and optical specs...'); return {'compliance': True}; def update_approval(state: GolfScopeState): return {'approved': state['compliance']}; graph = StateGraph(GolfScopeState); graph.add_node('validate', validate_specs); graph.add_node('approve', update_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()