from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgMachineryState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(AgMachineryState); def validate_specs(state: AgMachineryState): return {'validation_results': ['Specs validated for safety compliance']}; def assess_logistics(state: AgMachineryState): return {'validation_results': ['Logistics plan approved for heavy equipment']}; graph.add_node('validate', validate_specs); graph.add_node('logistics', assess_logistics); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()