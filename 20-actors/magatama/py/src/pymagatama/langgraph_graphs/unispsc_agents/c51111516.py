from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ThiotepaState(TypedDict): vendor_certs: List[str]; supply_temp: float; validated: bool; def validate_vendor(state: ThiotepaState): return {'validated': 'GMP' in state['vendor_certs']}; def check_temp_compliance(state: ThiotepaState): return {'validated': state['supply_temp'] <= 2.0}; graph = StateGraph(ThiotepaState); graph.add_node('validate', validate_vendor); graph.add_node('cold_chain', check_temp_compliance); graph.add_edge('validate', 'cold_chain'); graph.add_edge('cold_chain', END); graph.set_entry_point('validate'); graph = graph.compile()