from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InfantFoodState(TypedDict): vendor_certs: List[str]; safety_check_passed: bool; shelf_life_days: int; def validate_compliance(state: InfantFoodState): return {'safety_check_passed': 'ISO 22000' in state['vendor_certs'] and state['shelf_life_days'] > 180}; graph = StateGraph(InfantFoodState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()