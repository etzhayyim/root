from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_data: dict; validation_results: List[str]; def validate_medical_compliance(state: KitState): results = []; if 'iso_cert' not in state['kit_data']: results.append('Missing ISO 13485'); if 'sterility_date' not in state['kit_data']: results.append('Invalid sterility date'); return {'validation_results': results}; def process_kit(state: KitState): print('Processing medical kit: ' + str(state['kit_data'])); return state; graph = StateGraph(KitState); graph.add_node('compliance', validate_medical_compliance); graph.add_node('process', process_kit); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'process'); graph.add_edge('process', END); app = graph.compile()