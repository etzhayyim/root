from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeState(TypedDict): tube_id: str; compliance: bool; test_results: List[float]; def validate_assembly(state: TubeState): return {'compliance': all(r > 0.95 for r in state['test_results'])}; def check_export(state: TubeState): return {'compliance': state['compliance']}; graph = StateGraph(TubeState); graph.add_node('validate', validate_assembly); graph.add_node('export_review', check_export); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph.set_entry_point('validate'); graph = graph.compile()