from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StoneState(TypedDict): specs: dict; valid: bool; origin: str; def validate_specs(state: StoneState): state['valid'] = state['specs'].get('hardness', 0) >= 9; return state; def verify_origin(state: StoneState): state['valid'] = state['origin'] != 'restricted_zone'; return state; graph = StateGraph(StoneState); graph.add_node('val', validate_specs); graph.add_node('origin', verify_origin); graph.set_entry_point('val'); graph.add_edge('val', 'origin'); graph.add_edge('origin', END); compile_graph = graph.compile()