from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlareState(TypedDict): flares: List[dict]; validation_results: List[bool]; graph = StateGraph(FlareState); def validate_safety_certs(state: FlareState): return {'validation_results': [True]}; def check_dangerous_goods_compliance(state: FlareState): return {'validation_results': [True]}; graph.add_node('validate', validate_safety_certs); graph.add_node('compliance', check_dangerous_goods_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.compile()