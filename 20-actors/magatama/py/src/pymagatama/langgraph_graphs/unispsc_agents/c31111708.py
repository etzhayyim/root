from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): vendor_docs: dict; specs_verified: bool; def validate_specs(state: ProcState): state['specs_verified'] = 'alloy_cert' in state['vendor_docs']; return state; def check_export_control(state: ProcState): return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()