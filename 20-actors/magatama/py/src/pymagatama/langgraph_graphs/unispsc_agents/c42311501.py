from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_clips(state: ProcurementState): state['validated'] = all(['Sterile' in cert for cert in state['certs']]); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_clips); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()