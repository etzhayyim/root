from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): specs: dict; valid: bool; quality_report: str; def validate_casting(state: CastState): state['valid'] = 'material_cert' in state['specs'] and 'tolerance' in state['specs']; return state; def finalize_order(state: CastState): state['quality_report'] = 'Certified' if state['valid'] else 'Rejected'; return state; graph = StateGraph(CastState); graph.add_node('validate', validate_casting); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()