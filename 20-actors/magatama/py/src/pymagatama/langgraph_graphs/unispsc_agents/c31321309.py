from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): items: List[dict]; status: str; def validate_specs(state: AssemblyState): return {'status': 'validated'} if all(['grade' in i for i in state['items']]) else {'status': 'rejected'}; def process_assembly(state: AssemblyState): return {'status': 'processed'}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('process', process_assembly); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()