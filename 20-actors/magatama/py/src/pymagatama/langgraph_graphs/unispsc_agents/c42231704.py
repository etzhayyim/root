from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeState(TypedDict): tube_type: str; specs: dict; compliant: bool; def validate_tube_specs(state: TubeState): return {'compliant': 'radiopaque' in state['specs'] and state['specs']['sterility'] == 'certified'}; graph = StateGraph(TubeState); graph.add_node('validate', validate_tube_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()