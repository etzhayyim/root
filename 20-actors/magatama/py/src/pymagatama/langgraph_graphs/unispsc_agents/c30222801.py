from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BarnState(TypedDict): specs: dict; compliance: bool; status: str; def validate_structure(state: BarnState): state['compliance'] = all(k in state['specs'] for k in ['load_bearing', 'material']); return {'status': 'validated' if state['compliance'] else 'failed'}; graph = StateGraph(BarnState); graph.add_node('validate', validate_structure); graph.add_edge('validate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()