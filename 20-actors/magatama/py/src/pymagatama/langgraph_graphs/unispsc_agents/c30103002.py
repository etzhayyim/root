from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ShaftState(TypedDict): dimensions: dict; material_cert: str; grade: str; validated: bool; def validate_specs(state: ShaftState): state['validated'] = state['grade'] in ['SUS304', 'SUS316'] and 'material_cert' in state; return state; graph = StateGraph(ShaftState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()