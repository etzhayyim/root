from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GCState(TypedDict): vendor_docs: dict; validated: bool; export_check: bool; def validate_spec(state: GCState): state['validated'] = 'column_type' in state['vendor_docs']; return state; def check_export(state: GCState): state['export_check'] = True; return state; graph = StateGraph(GCState); graph.add_node('validate', validate_spec); graph.add_node('export', check_export); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph.set_entry_point('validate'); graph = graph.compile()