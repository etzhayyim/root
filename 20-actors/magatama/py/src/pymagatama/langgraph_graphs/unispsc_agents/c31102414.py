from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): alloy_specs: dict; inspection_results: List[str]; approved: bool; def validate_specs(state: CastingState): state['approved'] = all(val > 0 for val in state['alloy_specs'].values()); return state; def run_inspection(state: CastingState): state['inspection_results'].append('X-Ray Passed'); return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.add_node('inspect', run_inspection); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()