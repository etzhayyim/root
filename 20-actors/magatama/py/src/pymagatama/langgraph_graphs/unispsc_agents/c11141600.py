from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalProcurementState(TypedDict): chemical_code: str; purity_check: bool; safety_compliance: bool; status: str; def validate_purity(state: ChemicalProcurementState): return {purity_check: True if state.get(purity_val, 0) > 99.0 else False}; def verify_compliance(state: ChemicalProcurementState): return {safety_compliance: True}; builder = StateGraph(ChemicalProcurementState); builder.add_node(validate_purity, validate_purity); builder.add_node(verify_compliance, verify_compliance); builder.set_entry_point(validate_purity); builder.add_edge(validate_purity, verify_compliance); builder.add_edge(verify_compliance, END); graph = builder.compile()