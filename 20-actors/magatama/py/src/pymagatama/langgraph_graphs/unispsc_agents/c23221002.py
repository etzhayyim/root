from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): robot_id: str; specs: dict; valid: bool; def validate_specs(state: RobotState) -> RobotState: payload = state['specs'].get('payload'); state['valid'] = payload > 0 and payload < 500; return state; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()