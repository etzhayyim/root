from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; purity: float; gmp_compliant: bool; valid: bool; def validator(state: PharmaState): state['valid'] = state['purity'] >= 99.0 and state['gmp_compliant']; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validator); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()