from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RobotState(TypedDict): specs: dict; validation: bool; graph = StateGraph(RobotState); def validate_robot(state: RobotState): state['validation'] = state['specs'].get('load', 0) > 0; return state; graph.add_node('validate', validate_robot); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()