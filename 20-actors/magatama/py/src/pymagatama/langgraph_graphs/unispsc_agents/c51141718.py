from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): ingredient: str; qc_passed: bool; storage_validated: bool; def check_purity(state: PharmaState): state['qc_passed'] = True; return state; def validate_storage(state: PharmaState): state['storage_validated'] = True; return state; graph = StateGraph(PharmaState); graph.add_node('qc_check', check_purity); graph.add_node('storage_check', validate_storage); graph.add_edge('qc_check', 'storage_check'); graph.add_edge('storage_check', END); graph.set_entry_point('qc_check'); graph = graph.compile()