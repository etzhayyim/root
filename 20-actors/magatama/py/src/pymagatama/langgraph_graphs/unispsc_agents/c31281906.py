from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; specs: dict; validation_passed: bool; def validate_copper_spec(state: ComponentState): state['validation_passed'] = 'tensile_strength' in state['specs'] and 'alloy_type' in state['specs']; return state; graph = StateGraph(ComponentState); graph.add_node('validate', validate_copper_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()