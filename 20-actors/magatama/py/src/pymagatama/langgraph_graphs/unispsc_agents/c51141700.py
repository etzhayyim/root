from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): medicine_code: str; temperature_ok: bool; doc_verified: bool; def check_temp(state: PharmaState): return {'temperature_ok': True}; def verify_docs(state: PharmaState): return {'doc_verified': True}; graph = StateGraph(PharmaState); graph.add_node('check_storage', check_temp); graph.add_node('verify_compliance', verify_docs); graph.set_entry_point('check_storage'); graph.add_edge('check_storage', 'verify_compliance'); graph.add_edge('verify_compliance', END); graph = graph.compile()