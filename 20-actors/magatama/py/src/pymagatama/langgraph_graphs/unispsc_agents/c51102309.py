from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): ingredient_name: str; purity: float; compliance_checks: List[str]; def validate_purity(state: ProcureState): return {'compliance_checks': state['compliance_checks'] + ['Purity Validation Passed'] if state['purity'] >= 99.0 else ['Purity Validation Failed']}; def verify_compliance(state: ProcureState): return {'compliance_checks': state['compliance_checks'] + ['Regulatory Audit Complete']}; graph = StateGraph(ProcureState); graph.add_node('validate_purity', validate_purity); graph.add_node('verify_compliance', verify_compliance); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'verify_compliance'); graph.add_edge('verify_compliance', END); graph = graph.compile()