from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class WickState(TypedDict): wick_type: str; specs: dict; validation_passed: bool; def check_safety_specs(state: WickState): state['validation_passed'] = 'lead_free' in state['specs'] and state['specs']['burn_rate'] < 0.5; return state; graph = StateGraph(WickState); graph.add_node('safety_check', check_safety_specs); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()