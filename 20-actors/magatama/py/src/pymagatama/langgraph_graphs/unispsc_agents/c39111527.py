from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SolarState(TypedDict): specs: dict; valid: bool;  def validate_specs(state: SolarState): state['valid'] = 'IP protection class' in state['specs'] and state['specs'].get('Lumen output', 0) > 0; return state; graph = StateGraph(SolarState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()