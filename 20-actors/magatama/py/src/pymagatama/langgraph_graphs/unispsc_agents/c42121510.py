from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class VetCaseState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_case(state: VetCaseState): error = []; if not state['specs'].get('sterilization_compatibility'): error.append('Missing sterilization data'); return {'validation_errors': error}; def check_approval(state: VetCaseState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(VetCaseState); graph.add_node('validate', validate_case); graph.add_node('approval', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph = graph.compile()