from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CTState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; compliance_status: bool; def validate_physics_spec(state: CTState): return { 'validation_results': ['Radiation shielding check passed'] }; def verify_logistics(state: CTState): return { 'compliance_status': True }; graph = StateGraph(CTState); graph.add_node('physics_check', validate_physics_spec); graph.add_node('logistics_check', verify_logistics); graph.set_entry_point('physics_check'); graph.add_edge('physics_check', 'logistics_check'); graph.add_edge('logistics_check', END); graph = graph.compile()