from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): vendor: str; specs: dict; compliant: bool; def validate_specs(state: State): state['compliant'] = 'iso_20345_rating' in state['specs']; return state; builder = StateGraph(State); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()