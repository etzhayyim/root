from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): device_id: str; compliance_check: bool; data_valid: bool; def check_medical_compliance(state: ProcState): return { "compliance_check": True }; def validate_sensor_data(state: ProcState): return { "data_valid": True }; graph = StateGraph(ProcState); graph.add_node("compliance", check_medical_compliance); graph.add_node("validation", validate_sensor_data); graph.set_entry_point("compliance"); graph.add_edge("compliance", "validation"); graph.add_edge("validation", END); app = graph.compile()