from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoneStimulatorState(TypedDict): data: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: BoneStimulatorState): errors = []; required = ['FDA_PMA_Certification', 'Frequency_Range']; for field in required: if field not in state['data']: errors.append(f'Missing {field}'); return {**state, 'validation_errors': errors}; def check_safety(state: BoneStimulatorState): state['approved'] = len(state['validation_errors']) == 0; return state; graph = StateGraph(BoneStimulatorState); graph.add_node('validate', validate_specs); graph.add_node('safety_check', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()