from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): tube_specs: dict; validation_report: List[str]; def validate_assembly(state: AssemblyState): results = []; if 'material' not in state['tube_specs']: results.append('Missing material grade'); return {'validation_report': results}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_assembly); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()