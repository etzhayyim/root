from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; purity: float; is_compliant: bool; def validate_purity(state: DrugState): state['is_compliant'] = state['purity'] >= 99.9; return state; def check_regulatory(state: DrugState): state['is_compliant'] = state['is_compliant'] and True; return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()