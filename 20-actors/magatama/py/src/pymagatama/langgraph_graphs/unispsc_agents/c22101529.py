from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserProcurementState(TypedDict): specs: dict; compliance_check: bool; export_control_flag: bool; def validate_specs(state: LaserProcurementState): print('Validating laser parameters...'); return {'compliance_check': True} if 'power_stability' in state['specs'] else {'compliance_check': False}; def check_export_laws(state: LaserProcurementState): is_dual = state['specs'].get('power', 0) > 1000; return {'export_control_flag': is_dual}; graph = StateGraph(LaserProcurementState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_laws); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()