from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BeamClampState(TypedDict): specs: dict; validation_results: List[str]; def validate_load(state: BeamClampState): load = state['specs'].get('load_capacity', 0); return {'validation_results': ['Passed'] if load > 0 else ['Failed']}; def check_material(state: BeamClampState): return {'validation_results': state['validation_results'] + ['Material Checked']}; graph = StateGraph(BeamClampState); graph.add_node('validate_load', validate_load); graph.add_node('check_material', check_material); graph.set_entry_point('validate_load'); graph.add_edge('validate_load', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()