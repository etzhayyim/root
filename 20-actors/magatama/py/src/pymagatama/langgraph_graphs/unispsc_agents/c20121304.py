from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ServoState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_specs(state: ServoState): results = []; if 'torque' not in state['input_spec']: results.append('Missing torque spec'); return {'validation_results': results}; def finalize_process(state: ServoState): return {'status': 'Validated' if not state['validation_results'] else 'Rejected'}; graph = StateGraph(ServoState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_process); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()