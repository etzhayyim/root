from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): capacity: float; status: str; def validate_equipment(state: ProcState): return {'status': 'approved' if state['capacity'] > 10 else 'rejected'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()