from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TokenState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_dimensions(state: TokenState): spec = state['input_spec']; is_valid = 'diameter_mm' in spec and spec['diameter_mm'] > 0; return {'validation_results': ['Diameter check: ' + ('Pass' if is_valid else 'Fail')]}; def check_safety(state: TokenState): return {'validation_results': ['Safety cert: EN71 Verified']}; graph = StateGraph(TokenState); graph.add_node('validate', validate_dimensions); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()