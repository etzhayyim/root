from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): model_number: str; compatibility_validated: bool; def validate_compatibility(state: ProcState): state['compatibility_validated'] = bool(state['model_number']); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()