from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BeamState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: BeamState): state['validation_report'] = 'Validating structural compliance' if 'Material Grade' in state['specs'] else 'Missing Material Grade'; return state; graph = StateGraph(BeamState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()