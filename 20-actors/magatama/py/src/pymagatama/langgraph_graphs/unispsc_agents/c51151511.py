from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; compliance_status: bool; def validate_purity(state: PharmState): return {'validation_results': ['Purity check passed'] if state['input_data'].get('purity', 0) > 99 else ['Purity check failed']}; def check_gmp(state: PharmState): return {'compliance_status': state['input_data'].get('gmp_verified', False)}; builder = StateGraph(PharmState); builder.add_node('validate', validate_purity); builder.add_node('compliance', check_gmp); builder.set_entry_point('validate'); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); graph = builder.compile()