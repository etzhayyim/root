from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DialSpecState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_dimensions(state: DialSpecState): return {'validation_results': ['Dimension check passed']}; def finalize_spec(state: DialSpecState): return {'status': 'COMPLETED'}; graph = StateGraph(DialSpecState); graph.add_node('validate', validate_dimensions); graph.add_node('format', finalize_spec); graph.set_entry_point('validate'); graph.add_edge('validate', 'format'); graph.add_edge('format', END); compiled_graph = graph.compile()