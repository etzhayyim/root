from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_specs(state: ProcState): validated = all(key in state['input_spec'] for key in ['Accuracy', 'InputType']); return {'validation_results': ['Spec check passed' if validated else 'Missing keys']}; def trigger_workflow(state: ProcState): return {'status': 'Verification Complete'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('workflow', trigger_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'workflow'); graph.add_edge('workflow', END); graph = graph.compile()