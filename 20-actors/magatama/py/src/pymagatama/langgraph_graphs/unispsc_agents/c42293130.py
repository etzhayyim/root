from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): spec_data: dict; validation_report: str; def validate_instrument(state: State): cert = state['spec_data'].get('iso_cert'); return {'validation_report': 'Passed' if cert else 'Failed'}; graph = StateGraph(State); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()