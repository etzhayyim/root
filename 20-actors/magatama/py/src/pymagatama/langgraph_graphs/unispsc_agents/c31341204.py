from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): doc_id: str; structural_specs: dict; rivets_ok: bool; graph = StateGraph(AssemblyState); def validate_materials(state: AssemblyState): state['structural_specs'] = {'material': 'Inconel 718'}; return state; def check_riveting_integrity(state: AssemblyState): state['rivets_ok'] = True; return state; graph.add_node('MaterialCheck', validate_materials); graph.add_node('RivetingCheck', check_riveting_integrity); graph.set_entry_point('MaterialCheck'); graph.add_edge('MaterialCheck', 'RivetingCheck'); graph.add_edge('RivetingCheck', END); graph = graph.compile()