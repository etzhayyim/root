from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BookProcurementState(TypedDict): title: str; isbn: str; verified: bool; workflow_steps: List[str]; def validate_metadata(state: BookProcurementState): state['verified'] = bool(state.get('isbn')); state['workflow_steps'].append('metadata_check'); return state; def check_academic_compliance(state: BookProcurementState): state['workflow_steps'].append('academic_review'); return state; graph = StateGraph(BookProcurementState); graph.add_node('validate', validate_metadata); graph.add_node('review', check_academic_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'review'); graph.add_edge('review', END); graph = graph.compile()