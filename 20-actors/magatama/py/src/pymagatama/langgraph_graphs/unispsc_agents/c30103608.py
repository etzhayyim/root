from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PoleState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: PoleState): length = state['specs'].get('length', 0); treatment = state['specs'].get('treatment', None); state['valid'] = length > 0 and treatment is not None; return state; graph = StateGraph(PoleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()