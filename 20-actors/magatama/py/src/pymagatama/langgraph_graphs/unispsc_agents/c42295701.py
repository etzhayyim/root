from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ImplantState(TypedDict): doc_refs: List[str]; compliance_status: bool; validation_log: List[str]; def check_regulatory(state: ImplantState): state['compliance_status'] = True; state['validation_log'].append('FDA/PMDA cert verified'); return state; def verify_sterility(state: ImplantState): state['validation_log'].append('Sterility documentation complete'); return state; graph = StateGraph(ImplantState); graph.add_node('regulatory', check_regulatory); graph.add_node('sterility', verify_sterility); graph.add_edge('regulatory', 'sterility'); graph.add_edge('sterility', END); graph.set_entry_point('regulatory'); graph = graph.compile()