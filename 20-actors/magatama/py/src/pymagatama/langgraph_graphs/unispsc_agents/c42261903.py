from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_type: str; inspection_passed: bool; compliance_logs: List[str]; def validate_forensic_specs(state: KitState): state['inspection_passed'] = True; state['compliance_logs'].append('Protocol validated'); return state; def check_biohazard_status(state: KitState): state['compliance_logs'].append('Biohazard safety checked'); return state; graph = StateGraph(KitState); graph.add_node('validate', validate_forensic_specs); graph.add_node('biohazard', check_biohazard_status); graph.add_edge('validate', 'biohazard'); graph.add_edge('biohazard', END); graph.set_entry_point('validate'); graph = graph.compile()