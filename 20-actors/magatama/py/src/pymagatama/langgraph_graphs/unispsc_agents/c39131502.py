from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): wire_spec: dict; validation_report: str; def validate_specs(state: State): state['validation_report'] = 'Validation Successful' if state['wire_spec'].get('shrink_ratio') else 'Missing Spec'; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()