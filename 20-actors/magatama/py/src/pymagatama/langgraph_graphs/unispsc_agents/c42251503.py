from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GameState(TypedDict): items: List[str]; validated: bool; compliance_check: bool; def validate_clinical(state: GameState): state['validated'] = True; return state; def check_privacy(state: GameState): state['compliance_check'] = True; return state; graph = StateGraph(GameState); graph.add_node('validate', validate_clinical); graph.add_node('compliance', check_privacy); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()