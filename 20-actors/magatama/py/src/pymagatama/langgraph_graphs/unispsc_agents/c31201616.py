from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AdhesiveState(TypedDict): adhesive_type: str; safety_checks: List[str]; inspection_passed: bool; def validate_sds(state: AdhesiveState): state['safety_checks'].append('SDS_Verified'); return {'inspection_passed': True}; def process_quality(state: AdhesiveState): return {'inspection_passed': True if 'SDS_Verified' in state['safety_checks'] else False}; graph = StateGraph(AdhesiveState); graph.add_node('validate_sds', validate_sds); graph.add_node('process_quality', process_quality); graph.set_entry_point('validate_sds'); graph.add_edge('validate_sds', 'process_quality'); graph.add_edge('process_quality', END); graph = graph.compile()