from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec: dict; valid: bool; def validate_spec(state: ProcState): return {'valid': 'UL_Certification' in state['spec'] and state['spec']['Material'] in ['Steel', 'PVC']}; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()