from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): messages: Annotated[Sequence[str], operator.add]; validation_passed: bool; def validate_naphazoline(state: State): return {'validation_passed': True}; def update_records(state: State): return {'messages': ['Procurement record stored for Naphazoline']}; graph = StateGraph(State); graph.add_node('validate', validate_naphazoline); graph.add_node('record', update_records); graph.set_entry_point('validate'); graph.add_edge('validate', 'record'); graph.add_edge('record', END); app = graph.compile()