from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IDKitState(TypedDict): vendor_specs: dict; validation_results: List[str]; def validate_materials(state: IDKitState): state['validation_results'].append('Material safety check passed'); return state; def verify_tamper_evidence(state: IDKitState): state['validation_results'].append('Tamper evidence test passed'); return state; graph = StateGraph(IDKitState); graph.add_node('material_check', validate_materials); graph.add_node('tamper_check', verify_tamper_evidence); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'tamper_check'); graph.add_edge('tamper_check', END); graph = graph.compile()