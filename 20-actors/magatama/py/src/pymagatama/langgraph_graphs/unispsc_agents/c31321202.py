from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; def validate_material_grade(state: AssemblyState): state['validation_results'].append('Grade verified'); return state; def validate_welding(state: AssemblyState): state['validation_results'].append('AWS compliance checked'); return state; graph = StateGraph(AssemblyState); graph.add_node('material', validate_material_grade); graph.add_node('welding', validate_welding); graph.set_entry_point('material'); graph.add_edge('material', 'welding'); graph.add_edge('welding', END); graph = graph.compile()