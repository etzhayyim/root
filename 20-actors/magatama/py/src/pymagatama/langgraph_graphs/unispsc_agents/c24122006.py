from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AppState(TypedDict): applicator_type: str; specs: dict; valid: bool; def check_specs(state: AppState): state["valid"] = "Material" in state["specs"] and "Diameter" in state["specs"]; return state; graph = StateGraph(AppState); graph.add_node("validate", check_specs); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()