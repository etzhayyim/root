from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EquipmentState(TypedDict): items: List[dict]; valid: bool; def validate_equipment(state: EquipmentState): state['valid'] = all('safety_cert' in item for item in state['items']); return state; graph = StateGraph(EquipmentState); graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()