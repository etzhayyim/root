from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RadiationState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_spec(state: RadiationState): import os; results = ['Spec valid'] if state['input_data'].get('mfd_date') else ['Spec invalid']; return {'validation_results': results}; def radiation_safety_check(state: RadiationState): return {'status': 'Safety Protocol Verified'}; graph = StateGraph(RadiationState); graph.add_node('validate', validate_spec); graph.add_node('safety', radiation_safety_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.compile()