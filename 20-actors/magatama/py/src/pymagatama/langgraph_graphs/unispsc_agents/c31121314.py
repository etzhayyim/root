from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): specs: dict; ready: bool; def validate_specs(state: CastState): return {'ready': 'Dimensional Tolerance' in state['specs']}; def approve(state: CastState): return {'ready': True}; graph = StateGraph(CastState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()