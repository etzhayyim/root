from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class RareEarthState(TypedDict): code: str; purity_check: bool; export_licensed: bool; logs: List[str]; def validate_purity(state: RareEarthState): return {"purity_check": True, "logs": state["logs"] + ["Purity verified"]}; def check_export_status(state: RareEarthState): return {"export_licensed": True, "logs": state["logs"] + ["Export license cleared"]}; graph = StateGraph(RareEarthState); graph.add_node("purity", validate_purity); graph.add_node("export", check_export_status); graph.set_entry_point("purity"); graph.add_edge("purity", "export"); graph.add_edge("export", END); compiled_graph = graph.compile()