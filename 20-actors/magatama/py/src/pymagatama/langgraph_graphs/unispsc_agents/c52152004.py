from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlateState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(PlateState); def validate_spec(state: PlateState): results = []; for item in state['items']: if 'food_grade' not in item: results.append(f'Missing food grade for {item.get('id')}'); return {'validation_results': results}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()