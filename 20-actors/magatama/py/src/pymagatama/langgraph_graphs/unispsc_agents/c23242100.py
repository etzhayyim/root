from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MachineAttachmentState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def validate_specs(state: MachineAttachmentState): return {'validation_results': ['Spec passed'] if state['specs'].get('tolerance') else ['Spec failed']}; def check_compliance(state: MachineAttachmentState): return {'approved': all(state['validation_results'])}; builder = StateGraph(MachineAttachmentState); builder.add_node('validate', validate_specs); builder.add_node('compliance', check_compliance); builder.set_entry_point('validate'); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); graph = builder.compile()