from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medicine_name: str; gmp_certified: bool; quality_check: bool; graph = StateGraph(PharmaState); def validate_gmp(state: PharmaState): return {'gmp_certified': True} if 'Carvedilol' in state['medicine_name'] else {'gmp_certified': False}; def run_qc(state: PharmaState): return {'quality_check': state.get('gmp_certified', False)}; graph.add_node('validate', validate_gmp); graph.add_node('qc', run_qc); graph.set_entry_point('validate'); graph.add_edge('validate', 'qc'); graph.add_edge('qc', END); compiled_graph = graph.compile()