from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WipeState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(WipeState); def validate_sterility(state: WipeState): state['validation_results'].append('Sterility check passed'); return state; def verify_regulatory(state: WipeState): state['validation_results'].append('FDA/PMDA compliance verified'); return state; graph.add_node('sterility', validate_sterility); graph.add_node('regulatory', verify_regulatory); graph.set_entry_point('sterility'); graph.add_edge('sterility', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()