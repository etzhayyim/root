from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ForgingState(TypedDict): metal_composition: str; dimensions: dict; verified: bool; def check_lead_safety(state: ForgingState): return {'verified': 'Pb' in state['metal_composition']}; def validate_specs(state: ForgingState): return {'verified': True} if state['dimensions']['precision'] > 0.01 else {'verified': False}; graph = StateGraph(ForgingState); graph.add_node('safety_check', check_lead_safety); graph.add_node('dimension_check', validate_specs); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'dimension_check'); graph.add_edge('dimension_check', END); compiled_graph = graph.compile()