from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LabTapeState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_spec(state: LabTapeState): errors = []; for item in state['items']: if 'temp_range' not in item: errors.append(f'Missing temp profile for {item.get('id')}'); return {'validation_errors': errors}; graph = StateGraph(LabTapeState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()