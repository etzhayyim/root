from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: CastingState): state['validation_results'].append('Material grade match confirmed'); return state; def check_geometry(state: CastingState): state['validation_results'].append('Dimensional tolerance verified'); return state; graph = StateGraph(CastingState); graph.add_node('material', validate_material); graph.add_node('geometry', check_geometry); graph.add_edge('material', 'geometry'); graph.add_edge('geometry', END); graph.set_entry_point('material'); graph = graph.compile()