from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoffeeAnalysisState(TypedDict): vendor_docs: List[str]; qc_passed: bool; def validate_quality(state: CoffeeAnalysisState): return {'qc_passed': all('cert' in doc.lower() for doc in state['vendor_docs'])}; graph = StateGraph(CoffeeAnalysisState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()