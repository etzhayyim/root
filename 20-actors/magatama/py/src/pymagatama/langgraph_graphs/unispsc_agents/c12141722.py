from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class FilmState(TypedDict): input_spec: dict; quality_results: list; status: str; graph = StateGraph(FilmState); def validate_purity(state: FilmState): return {'quality_results': ['purity_passed']}; def inspect_surface(state: FilmState): return {'quality_results': ['surface_passed']}; graph.add_node('purity', validate_purity); graph.add_node('surface', inspect_surface); graph.set_entry_point('purity'); graph.add_edge('purity', 'surface'); graph.add_edge('surface', END); graph = graph.compile()