from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity: float; compliance_check: bool; graph = StateGraph(PharmaState); def validate_purity(state: PharmaState): return {'compliance_check': state['purity'] >= 99.0}; def log_procurement(state: PharmaState): print(f'Processing {state["chemical_name"]} compliance'); return {}; graph.add_node('validate', validate_purity); graph.add_node('log', log_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.compile()