from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_passed: bool; report: str; def validate_assembly(state: AssemblyState): print('Validating non-metallic rivets...'); state['validation_passed'] = 'Material' in state['specs'] and 'RivetStrength' in state['specs']; return state; def finalize_report(state: AssemblyState): state['report'] = 'Validation Successful' if state['validation_passed'] else 'Validation Failed'; return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_assembly); graph.add_node('finalize', finalize_report); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()