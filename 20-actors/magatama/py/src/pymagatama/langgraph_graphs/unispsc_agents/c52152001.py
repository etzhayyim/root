from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PitcherState(TypedDict): items: List[dict]; validation_results: List[str]; def validate_materials(state: PitcherState): results = []; for item in state['items']: if 'food_grade' not in item: results.append(f'Missing food grade cert for {item.get('id')}'); return {'validation_results': results}; graph = StateGraph(PitcherState); graph.add_node('validate', validate_materials); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()