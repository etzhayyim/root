from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StapleState(TypedDict): items: List[dict]; validation_status: str; def validate_staples(state: StapleState): return {'validation_status': 'verified' if all(i.get('gauge') for i in state['items']) else 'rejected'}; graph = StateGraph(StapleState); graph.add_node('validation', validate_staples); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()