from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ScratchArtState(TypedDict): input_data: dict; approval_required: bool; def validate_quality(state: ScratchArtState): state['approval_required'] = state['input_data'].get('gsm', 0) < 150; return state; graph = StateGraph(ScratchArtState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()