from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: AssemblyState): state['valid'] = 'Dimensional Tolerance' in state['specs'] and 'Material Grade' in state['specs']; return state; def check_compliance(state: AssemblyState): return 'valid' if state['valid'] else 'error'; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()