from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class BearingProcurementState(TypedDict): messages: Annotated[list, add_messages]; spec_verified: bool; compliance_check: bool; def validate_spec(state: BearingProcurementState): return {'spec_verified': True}; def perform_compliance(state: BearingProcurementState): return {'compliance_check': True}; graph = StateGraph(BearingProcurementState); graph.add_node('validate', validate_spec); graph.add_node('compliance', perform_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()