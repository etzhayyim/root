from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WiperState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: WiperState): required = ['blade_length_mm', 'mounting_type']; missing = [f for f in required if f not in state['specs']]; state['validation_errors'] = missing; state['approved'] = len(missing) == 0; return state; graph = StateGraph(WiperState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()