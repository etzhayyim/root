from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class MiningState(TypedDict): input_spec: dict; validation_report: list[str]; approved: bool; def validate_drilling_spec(state: MiningState): report = []; if state['input_spec'].get('tensile_strength_mpa', 0) < 500: report.append('Low tensile strength'); return {'validation_report': report, 'approved': len(report) == 0}; graph = StateGraph(MiningState); graph.add_node('validate', validate_drilling_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()