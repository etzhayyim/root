from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalSpecState(TypedDict): device_id: str; specs: dict; validation_errors: List[str]; def validate_specs(state: SurgicalSpecState): if not state['specs'].get('ISO_13485'): state['validation_errors'].append('Missing ISO 13485 certification'); return state; def check_accuracy(state: SurgicalSpecState): if state['specs'].get('accuracy', 0) > 0.05: state['validation_errors'].append('Accuracy tolerance exceeds limits'); return state; graph = StateGraph(SurgicalSpecState); graph.add_node('validate', validate_specs); graph.add_node('accuracy_check', check_accuracy); graph.add_edge('validate', 'accuracy_check'); graph.add_edge('accuracy_check', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()