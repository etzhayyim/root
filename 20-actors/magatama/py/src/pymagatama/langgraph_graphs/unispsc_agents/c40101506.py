from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class VentilationState(TypedDict): specs: dict; validation_log: Annotated[list, operator.add]; status: str; def validate_specs(state: VentilationState): spec = state['specs']; log = [] if 'diameter' not in spec: log.append('Missing diameter') if 'material' not in spec: log.append('Missing material') return {'validation_log': log, 'status': 'VALIDATED' if not log else 'FAILED'}; graph = StateGraph(VentilationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()