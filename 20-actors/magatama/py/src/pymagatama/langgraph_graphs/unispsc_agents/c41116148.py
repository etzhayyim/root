from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlowCytometryState(TypedDict): items: List[str]; qc_passed: bool; validation_log: List[str]; def validate_reagent(state: FlowCytometryState): state['qc_passed'] = all(item.get('temp_stable', False) for item in state['items']); state['validation_log'].append('QC Check Completed'); return state; def prepare_shipment(state: FlowCytometryState): state['validation_log'].append('Cold Chain Prepared'); return state; graph = StateGraph(FlowCytometryState); graph.add_node('validate', validate_reagent); graph.add_node('ship', prepare_shipment); graph.add_edge('validate', 'ship'); graph.add_edge('ship', END); graph.set_entry_point('validate'); graph = graph.compile()