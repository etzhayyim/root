from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DialysisState(TypedDict): specs: dict; validation_results: List[str]; def validate_membrane(state: DialysisState): return {'validation_results': ['MWCO verified'] if 'mwco' in state['specs'] else ['Error: missing MWCO']}; def check_safety(state: DialysisState): return {'validation_results': state['validation_results'] + ['Sterility checked']}; graph = StateGraph(DialysisState); graph.add_node('validate', validate_membrane); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.compile()