from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcureState(TypedDict): items: List[str]; status: str; def validate_pharma(state: ProcureState): return {'status': 'Validated' if len(state['items']) > 0 else 'Error'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_pharma); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()