from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrainingState(TypedDict): aid_id: str; compliance: bool; approved: bool; def validate_security(state: TrainingState): print(f'Validating export controls for {state["aid_id"]}'); return {'compliance': True}; def finalize_approval(state: TrainingState): return {'approved': True}; graph = StateGraph(TrainingState); graph.add_node('security_check', validate_security); graph.add_node('approval', finalize_approval); graph.set_entry_point('security_check'); graph.add_edge('security_check', 'approval'); graph.add_edge('approval', END); compiled_graph = graph.compile()