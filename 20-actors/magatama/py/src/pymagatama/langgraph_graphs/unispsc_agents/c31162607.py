from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): vendor_specs: dict; validation_results: List[str]; def validate_load(state: ProcurementState): return {'validation_results': ['Load capacity verified']} if state['vendor_specs'].get('load_kn', 0) > 10 else {'validation_results': ['Load capacity insufficient']}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_load); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()