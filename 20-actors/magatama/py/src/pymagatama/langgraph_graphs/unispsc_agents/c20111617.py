from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ConnectorState(TypedDict): component_id: str; specs: dict; validation_log: Annotated[list[str], operator.add]; status: str; def validate_connector(state: ConnectorState): specs = state.get('specs', {}); errors = []; if specs.get('contact_resistance') > 50: errors.append('Resistance too high'); return {'validation_log': errors, 'status': 'FAILED' if errors else 'VALIDATED'}; def compile_graph(): graph = StateGraph(ConnectorState); graph.add_node('validate', validate_connector); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()