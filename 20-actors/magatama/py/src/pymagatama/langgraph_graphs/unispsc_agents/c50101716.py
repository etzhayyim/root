from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplyState(TypedDict): vendor: str; quality_score: float; certs: List[str]; def validate_pesticides(state: SupplyState): return {'quality_score': 0.95 if 'ISO22000' in state['certs'] else 0.5}; def check_origin(state: SupplyState): return {'vendor': state['vendor'] + '_verified'}; graph = StateGraph(SupplyState); graph.add_node('validate', validate_pesticides); graph.add_node('compliance', check_origin); graph.add_edge('compliance', 'validate'); graph.add_edge('validate', END); graph.set_entry_point('compliance'); graph = graph.compile()