from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WirewayState(TypedDict): specs: dict; valid: bool; validator_logs: List[str]; def validate_specs(state: WirewayState): specs = state['specs']; logs = []; valid = True; if 'rating' not in specs: logs.append('Missing NEMA/IP rating'); valid = False; return { 'valid': valid, 'validator_logs': logs }; graph = StateGraph(WirewayState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()