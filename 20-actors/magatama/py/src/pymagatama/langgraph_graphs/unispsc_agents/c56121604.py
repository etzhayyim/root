from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BeanBagState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; def validate_safety(state: BeanBagState): results = []; # Logic for checking fire-safety and choking hazard compliance; return {'validation_results': ['Safety compliant']}; def finalize_spec(state: BeanBagState): return {'validation_results': ['Specification ready for procurement']}; graph = StateGraph(BeanBagState); graph.add_node('validate', validate_safety); graph.add_node('finalize', finalize_spec); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()