from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MarkerState(TypedDict): marker_type: str; compliance_check: bool; target_surface: str; def validate_ink(state: MarkerState): return {'compliance_check': 'ASTM D-4236' in state.get('marker_type', '')}; graph = StateGraph(MarkerState); graph.add_node('validate', validate_ink); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()