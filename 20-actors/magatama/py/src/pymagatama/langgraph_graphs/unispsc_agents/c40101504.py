from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DamperState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(DamperState); def validate_specs(state: DamperState): report = []; r = state['specs'].get('fire_rating', 0); report.append('PASS' if r >= 60 else 'FAIL: Insufficient fire rating'); return {'validation_report': report}; graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()