from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): content: str; validated: bool; compliance_status: str; def validate_content(state: ProcState): state['validated'] = True if len(state['content']) > 10 else False; return state; def check_guidelines(state: ProcState): state['compliance_status'] = 'compliant' if state['validated'] else 'reject'; return state; builder = StateGraph(ProcState); builder.add_node('validate', validate_content); builder.add_node('compliance', check_guidelines); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); builder.set_entry_point('validate'); graph = builder.compile()