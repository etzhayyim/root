from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DispenserState(TypedDict): vendor_specs: dict; validation_results: dict; def validate_specs(state: DispenserState): return {'validation_results': {'is_compliant': state['vendor_specs'].get('capacity_ml', 0) > 0}}; def check_compliance(state: DispenserState): return {'validation_results': {'compliant': True}} if state['validation_results'].get('is_compliant') else {'validation_results': {'compliant': False}}; graph = StateGraph(DispenserState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()