from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LightingState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: LightingState): errors = []; if not state['specs'].get('ip_rating'): errors.append('Missing IP rating'); return {'validation_results': errors}; def check_compliance(state: LightingState): return 'compliance_passed' if not state['validation_results'] else 'compliance_failed'; graph = StateGraph(LightingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END)