from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LampCoverState(TypedDict): specs: dict; validation_results: List[str]; def validate_spec(state: LampCoverState): results = []; if state['specs'].get('heat_resistance', 0) < 100: results.append('Heat resistance below threshold'); return {'validation_results': results}; def generate_report(state: LampCoverState): return {'validation_results': state['validation_results'] + ['Report Generated']}; graph = StateGraph(LampCoverState); graph.add_node('validate', validate_spec); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()