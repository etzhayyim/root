from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): battery_type: str; specs: dict; compliant: bool; def check_compliance(state: State): state['compliant'] = 'UN 38.3' in state.get('specs', {}).get('certs', []); return state; graph = StateGraph(State); graph.add_node('validate', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()