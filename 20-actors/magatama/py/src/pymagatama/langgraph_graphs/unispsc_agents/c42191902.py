from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HospitalFurnitureState(TypedDict): furniture_specs: dict; validation_results: List[str]; def validate_specs(state: HospitalFurnitureState): valid = all([state['furniture_specs'].get('antimicrobial_rating') >= 99.9]); result = 'passed' if valid else 'failed'; return {'validation_results': [result]}; graph = StateGraph(HospitalFurnitureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()