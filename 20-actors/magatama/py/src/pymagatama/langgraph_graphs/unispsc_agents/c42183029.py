from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): data: dict; status: str; def validate_specs(state: State): return {'status': 'VALID' if 'calibration_cert' in state['data'] else 'INVALID'}; def check_regulatory(state: State): return {'status': 'REGULATORY_CLEARED'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()