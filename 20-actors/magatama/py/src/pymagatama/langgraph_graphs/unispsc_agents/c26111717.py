from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BatteryState(TypedDict): specs: dict; valid: bool; error: str; def validate_battery(state: BatteryState): required = ['voltage_rating_v', 'rohs_compliance']; state['valid'] = all(k in state['specs'] for k in required); return state; graph = StateGraph(BatteryState); graph.add_node('validate', validate_battery); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()