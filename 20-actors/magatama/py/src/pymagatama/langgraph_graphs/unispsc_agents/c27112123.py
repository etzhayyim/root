from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BenchDogState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_bench_dog(state: BenchDogState): state['valid'] = all(k in state['specs'] for k in ['diameter', 'material']); return state; graph = StateGraph(BenchDogState); graph.add_node('validate', validate_bench_dog); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()