from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ControlSystemState(TypedDict): vendor_specs: dict; validation_results: List[str]; approved: bool; def validate_specs(state: ControlSystemState): results = []; (results.append('Valid') if state['vendor_specs'].get('voltage') else results.append('Invalid')); return {'validation_results': results, 'approved': len(results) == 1}; graph = StateGraph(ControlSystemState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()