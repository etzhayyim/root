from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): commodity_id: str; specs: dict; validation_results: Annotated[Sequence[str], operator.add]; def validate_specs(state: ProcurementState): return {'validation_results': ['CAD model verified', 'Voltage tolerance within range']}; def generate_procurement_order(state: ProcurementState): return {'validation_results': ['Order generated for ID: ' + state['commodity_id']]}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('order', generate_procurement_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'order'); graph.add_edge('order', END); graph = graph.compile()