from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MannequinState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: MannequinState): errors = []; if not state['specs'].get('material'): errors.append('Material missing'); return {'validation_errors': errors}; def check_stability(state: MannequinState): approved = len(state['validation_errors']) == 0; return {'approved': approved}; graph = StateGraph(MannequinState); graph.add_node('validate', validate_specs); graph.add_node('stability', check_stability); graph.set_entry_point('validate'); graph.add_edge('validate', 'stability'); graph.add_edge('stability', END); graph = graph.compile()