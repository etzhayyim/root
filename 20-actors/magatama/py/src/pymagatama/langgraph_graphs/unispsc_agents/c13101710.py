from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_catalyst_purity(state: CatalystState): purity = state['input_data'].get('purity', 0); return {'validation_results': [f'Purity check: {purity}%'], 'status': 'validated' if purity > 99.9 else 'rejected'}; def process_catalytic_workflow(state: CatalystState): return {'validation_results': ['Workflow initialized for refining extraction'], 'status': 'processing'}; graph = StateGraph(CatalystState); graph.add_node('validate', validate_catalyst_purity); graph.add_node('process', process_catalytic_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()