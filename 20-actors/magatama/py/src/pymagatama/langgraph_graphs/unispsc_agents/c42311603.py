from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MedicalState(TypedDict): vendor_certs: List[str]; qc_passed: bool; quantity: int; def validate_compliance(state: MedicalState): return {'qc_passed': 'ISO 13485' in state['vendor_certs']}; def order_manager(state: MedicalState): print('Processing hemostatic agent procurement'); return {'qc_passed': True}; graph = StateGraph(MedicalState); graph.add_node('validate', validate_compliance); graph.add_node('process', order_manager); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.compile()