from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ThermalBrickState(TypedDict): specs: dict; validation_results: List[str]; def validate_thermal_specs(state: ThermalBrickState): results = []; if state['specs'].get('max_temp', 0) < 1000: results.append('Temperature rating insufficient'); return {'validation_results': results}; graph = StateGraph(ThermalBrickState); graph.add_node('validate', validate_thermal_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()