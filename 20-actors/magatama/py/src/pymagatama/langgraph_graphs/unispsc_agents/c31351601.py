from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): tube_specs: dict; validation_results: List[str]; def validate_cad(state: AssemblyState): return {'validation_results': ['CAD Geometry Verified']}; def check_pressure_spec(state: AssemblyState): return {'validation_results': state['validation_results'] + ['Pressure Rating Verified']}; graph = StateGraph(AssemblyState); graph.add_node('cad_check', validate_cad); graph.add_node('pressure_check', check_pressure_spec); graph.set_entry_point('cad_check'); graph.add_edge('cad_check', 'pressure_check'); graph.add_edge('pressure_check', END); graph = graph.compile()