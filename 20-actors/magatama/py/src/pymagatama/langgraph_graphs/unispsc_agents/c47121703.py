from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; valid: bool; def validate_urn(state: State): state['valid'] = 'fire_rating' in state['input_data'] and state['input_data']['capacity'] > 0; return state; graph = StateGraph(State); graph.add_node('validate', validate_urn); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()