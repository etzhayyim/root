from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineSpecState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: MachineSpecState): result = 'Valid' if 'material' in state['specs'] else 'Invalid'; return {'validation_results': [result]}; def check_export_control(state: MachineSpecState): return {'validation_results': state['validation_results'] + ['Export Check Passed']}; graph = StateGraph(MachineSpecState); graph.add_node('validate', validate_specs); graph.add_node('export', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()