from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ClampState(TypedDict): name: str; force_kn: float; status: str; def validate_force_capacity(state: ClampState) -> ClampState: state['status'] = 'verified' if state['force_kn'] > 0 else 'rejected'; return state; def finalize_specs(state: ClampState) -> ClampState: state['status'] = 'procurement_ready'; return state; graph = StateGraph(ClampState); graph.add_node('validate', validate_force_capacity); graph.add_node('finalize', finalize_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()