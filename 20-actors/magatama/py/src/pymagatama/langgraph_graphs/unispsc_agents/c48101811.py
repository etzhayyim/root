from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KitchenToolState(TypedDict): tool_specs: dict; validation_report: List[str]; graph = StateGraph(KitchenToolState); def validate_material(state: KitchenToolState): material = state['tool_specs'].get('material'); return {'validation_report': ['Material conforms to food safety' if material in ['stainless_steel', 'food_grade_silicone'] else 'Material check warning']}; graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()