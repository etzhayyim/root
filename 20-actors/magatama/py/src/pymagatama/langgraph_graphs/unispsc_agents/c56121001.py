from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BookCartState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: BookCartState): errors = []; if state['specs'].get('load_capacity_kg', 0) < 50: errors.append('Load capacity below safety threshold'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(BookCartState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()