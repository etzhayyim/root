from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): document: str; status: str; metadata: dict; def validate_gmp(state: PharmState): return {'status': 'VALID' if 'GMP' in state['metadata'] else 'INVALID'}; def check_expiry(state: PharmState): return {'status': 'EXPIRED' if state['metadata'].get('days_to_expiry', 0) < 90 else 'OK'}; graph = StateGraph(PharmState); graph.add_node('validate', validate_gmp); graph.add_node('expiry', check_expiry); graph.set_entry_point('validate'); graph.add_edge('validate', 'expiry'); graph.add_edge('expiry', END); graph = graph.compile()