from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): vendor_gmp: bool; material_purity: float; approved: bool; def check_gmp(state: ProcureState): return {'approved': state['vendor_gmp'] and state['material_purity'] >= 99.0}; graph = StateGraph(ProcureState); graph.add_node('verify', check_gmp); graph.set_entry_point('verify'); graph.add_edge('verify', END); graph = graph.compile()