from langgraph.graph import StateGraph, END; from typing import TypedDict; class AVState(TypedDict): specs: dict; valid: bool; def validate_specs(state: AVState): state['valid'] = 'Resolution' in state['specs'] and 'Interface Type' in state['specs']; return state; builder = StateGraph(AVState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()