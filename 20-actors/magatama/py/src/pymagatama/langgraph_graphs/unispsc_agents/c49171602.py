from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EquipmentState(TypedDict): items: List[str]; validated: bool; spec_compliance: bool; def validate_specs(state: EquipmentState): return {'validated': True}; def check_compliance(state: EquipmentState): return {'spec_compliance': all(item for item in state['items'])}; graph = StateGraph(EquipmentState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END);