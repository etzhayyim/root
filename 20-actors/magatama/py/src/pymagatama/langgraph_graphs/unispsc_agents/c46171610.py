from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CameraSpecState(TypedDict): specs: dict; security_check: bool; final_report: str; def validate_specs(state: CameraSpecState): return {'security_check': all(k in state['specs'] for k in ['resolution', 'encryption'])}; def finalize(state: CameraSpecState): return {'final_report': 'Validated hardware configuration.'}; graph = StateGraph(CameraSpecState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()