from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FreezerSpec(TypedDict): specs: dict; status: str; def validate_specs(state: FreezerSpec): temp = state['specs'].get('temp', 0); state['status'] = 'VALID' if temp <= -18 else 'INVALID'; return state; graph = StateGraph(FreezerSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()