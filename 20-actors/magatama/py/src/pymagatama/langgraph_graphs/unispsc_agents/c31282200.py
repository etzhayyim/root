from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): component_specs: dict; validation_results: List[str]; def validate_dimensions(state: GraphState): return {'validation_results': ['dimensions_checked']}; def inspect_surface(state: GraphState): return {'validation_results': ['surface_ok']}; graph = StateGraph(GraphState); graph.add_node('dim_check', validate_dimensions); graph.add_node('surf_check', inspect_surface); graph.add_edge('dim_check', 'surf_check'); graph.add_edge('surf_check', END); graph.set_entry_point('dim_check'); graph = graph.compile()