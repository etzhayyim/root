from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PolymerState(TypedDict): code: str; density: float; status: str; def validate_grade(state: PolymerState): return {'status': 'validated' if state.get('density', 0) > 900 else 'rejected'}; def check_compliance(state: PolymerState): return {'status': 'compliant'}; graph = StateGraph(PolymerState); graph.add_node('validate', validate_grade); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()