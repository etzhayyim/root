from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; def validate_specs(state: State): results = ['IP_validated', 'Voltage_checked']; return {'validation_results': results}; def update_inventory(state: State): return {'validation_results': ['Inventory_updated']}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('inventory', update_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph = graph.compile()