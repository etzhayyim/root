from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purities: List[float]; valid: bool; def validate_purity(state: PharmState): state['valid'] = all(p >= 98.0 for p in state['purities']); return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()