from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeSpecs(TypedDict): specs: dict; valid: bool; risk_level: str; def validate_specs(state: PipeSpecs): yield_strength = state['specs'].get('yield_strength', 0); state['valid'] = yield_strength >= 355; return state; def check_compliance(state: PipeSpecs): state['risk_level'] = 'HIGH' if state['valid'] else 'CRITICAL'; return state; graph = StateGraph(PipeSpecs); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()