from langgraph.graph import StateGraph, END; from typing import TypedDict; class StrapState(TypedDict): specs: dict; valid: bool; def validate_tensile(state: StrapState): state['valid'] = state['specs'].get('tensile', 0) > 500; return state; graph = StateGraph(StrapState); graph.add_node('validate', validate_tensile); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()