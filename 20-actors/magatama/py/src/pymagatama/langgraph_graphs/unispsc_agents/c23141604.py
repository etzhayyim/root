from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class PressState(TypedDict): input_data: dict; validation_results: Annotated[Sequence[str], operator.add]; safety_flag: bool; def validate_specs(state: PressState): specs = state['input_data']; results = []; safety = True; if specs.get('tonnage', 0) <= 0: results.append('Invalid Tonnage'); safety = False; return {'validation_results': results, 'safety_flag': safety}; def execute_workflow(state: PressState): return {'validation_results': ['Workflow Triggered']}; graph = StateGraph(PressState); graph.add_node('validate', validate_specs); graph.add_node('workflow', execute_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'workflow'); graph.add_edge('workflow', END); compile_graph = graph.compile()