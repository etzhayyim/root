from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class XRayState(TypedDict): radiation_compliance: bool; specs: dict; validation_report: str; def validate_specs(state: XRayState): return {'validation_report': 'Safety standards confirmed' if state['radiation_compliance'] else 'Failed safety audit'}; def execute_procurement(state: XRayState): return {'validation_report': 'Workflow complete'}; graph = StateGraph(XRayState); graph.add_node('validate', validate_specs); graph.add_node('execute', execute_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'execute'); graph.add_edge('execute', END); compiled_graph = graph.compile()