from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComforterState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ComforterState): errors = []; if 'fill_weight' not in state['specs']: errors.append('Missing fill weight'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; builder = StateGraph(ComforterState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()