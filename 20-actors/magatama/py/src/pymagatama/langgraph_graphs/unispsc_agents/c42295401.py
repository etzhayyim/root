from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): device_id: str; compliance_docs: List[str]; approved: bool; def validate_specs(state: State): state['approved'] = 'ISO_13485' in state['compliance_docs']; return state; def check_battery(state: State): return 'ready' if state['device_id'] else 'error'; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()