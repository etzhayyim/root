from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph; class RobotControllerState(TypedDict): controller_specs: dict; validation_results: list[str]; graph = StateGraph(RobotControllerState); def validate_controller(state: RobotControllerState): results = ['Voltage Range Verified', 'Protocol Compatibility Checked']; return {'validation_results': results}; graph.add_node('validate', validate_controller); graph.set_entry_point('validate'); graph.set_finish_point('validate'); compiled_graph = graph.compile()