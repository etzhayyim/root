from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MarketState(TypedDict): items: List[str]; validation_results: List[bool]; def validate_spec(state: MarketState): return {'validation_results': [True for i in state['items']]}; def log_procurement(state: MarketState): print('Processing market infrastructure procurement'); return state; graph = StateGraph(MarketState); graph.add_node('validate', validate_spec); graph.add_node('log', log_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.compile()