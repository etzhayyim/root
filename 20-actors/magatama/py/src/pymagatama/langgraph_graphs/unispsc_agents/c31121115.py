from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_machining(state: ProcState): state['valid'] = all(k in state['specs'] for k in ['material', 'tolerance']); state['log'].append('Validation complete'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_machining); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()