from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_api_standard(state: DrugState): status = 'PASS' if state['input_data'].get('purity', 0) >= 99.0 else 'FAIL'; return {'validation_results': [f'Purity Check: {status}'], 'status': status}; def check_gmp(state: DrugState): return {'validation_results': ['GMP: Verified']}; graph = StateGraph(DrugState); graph.add_node('validate_api', validate_api_standard); graph.add_node('check_gmp', check_gmp); graph.set_entry_point('validate_api'); graph.add_edge('validate_api', 'check_gmp'); graph.add_edge('check_gmp', END); graph = graph.compile()