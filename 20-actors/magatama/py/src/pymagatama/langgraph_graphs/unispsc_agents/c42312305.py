from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DebridementState(TypedDict): product_specs: dict; compliance_check: bool; graph = StateGraph(DebridementState); def validate_specs(state: DebridementState): state['compliance_check'] = 'enzyme_concentration' in state['product_specs'] and 'expiry_date_compliance' in state['product_specs']; return state; def check_safety(state: DebridementState): return 'approved' if state['compliance_check'] else 'rejected'; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()