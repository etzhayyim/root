from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CommodityState(TypedDict): vendor_specs: dict; compliance_check: bool; final_approval: bool; def validate_specs(state: CommodityState): state['compliance_check'] = 'Dimensional Tolerance' in state['vendor_specs']; return state; def approve_procurement(state: CommodityState): state['final_approval'] = state['compliance_check']; return state; graph = StateGraph(CommodityState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()