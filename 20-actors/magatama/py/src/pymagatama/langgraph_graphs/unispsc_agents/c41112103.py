from langgraph.graph import StateGraph, END; from typing import TypedDict; class SensorState(TypedDict): specs: dict; valid: bool; def validate_specs(state: SensorState): state['valid'] = 'fiber_length_m' in state['specs']; return state; builder = StateGraph(SensorState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()