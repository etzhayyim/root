from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): alloy_spec: str; dimensions: dict; quality_report: str; approved: bool; def validate_specs(state: CastState): state['approved'] = 'ASTM' in state['alloy_spec'] and state.get('quality_report') is not None; return state; graph = StateGraph(CastState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()