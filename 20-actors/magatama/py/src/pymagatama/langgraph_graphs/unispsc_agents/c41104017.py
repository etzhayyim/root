from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): items: List[str]; validation_status: bool; def validate_spec(state: SpecState): print('Validating sample holder specs...'); return {'validation_status': True}; def update_inventory(state: SpecState): print('Updating inventory for items...'); return {'items': state['items']}; graph = StateGraph(SpecState); graph.add_node('validate', validate_spec); graph.add_node('update', update_inventory); graph.add_edge('validate', 'update'); graph.add_edge('update', END); graph.set_entry_point('validate'); compile_graph = graph.compile()