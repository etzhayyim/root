from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabEquipmentState(TypedDict): equipment_type: str; specs: dict; verified: bool; def validate_specs(state: LabEquipmentState): state['verified'] = all(k in state['specs'] for k in ['Motor Power', 'Safety Rating']); return state; graph = StateGraph(LabEquipmentState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()