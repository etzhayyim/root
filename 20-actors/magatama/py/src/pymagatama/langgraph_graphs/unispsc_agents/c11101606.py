from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class MineralChemState(TypedDict): chemical_name: str; purity: float; safety_check: bool; validation_log: List[str]; def validate_purity(state: MineralChemState): return {purity: state[purity] >= 0.99, validation_log: state[validation_log] + [Purity check performed]}; def perform_safety_audit(state: MineralChemState): return {safety_check: True, validation_log: state[validation_log] + [Safety audit complete]}; graph = StateGraph(MineralChemState); graph.add_node(validate, validate_purity); graph.add_node(audit, perform_safety_audit); graph.set_entry_point(validate); graph.add_edge(validate, audit); graph.add_edge(audit, END); graph = graph.compile()