from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AerospaceState(TypedDict): data: dict; logs: Annotated[list, operator.add]; def validate_gauges(state: AerospaceState): return {'logs': ['Validating AS9100 compliance'], 'data': {**state['data'], 'status': 'valid'}}; def check_export_controls(state: AerospaceState): return {'logs': ['Checking ITAR/EAR export status']}; graph = StateGraph(AerospaceState); graph.add_node('validate', validate_gauges); graph.add_node('export', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph = graph.compile()