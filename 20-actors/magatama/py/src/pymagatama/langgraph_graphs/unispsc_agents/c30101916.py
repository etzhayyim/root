from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MetalState(TypedDict): metal_type: str; purity: float; weight: float; verified: bool; def validate_purity(state: MetalState): state['verified'] = state['purity'] >= 99.9; return state; def check_weight(state: MetalState): print(f'Verifying weight: {state['weight']}g'); return state; graph = StateGraph(MetalState); graph.add_node('validate', validate_purity); graph.add_node('weigh', check_weight); graph.set_entry_point('validate'); graph.add_edge('validate', 'weigh'); graph.add_edge('weigh', END); graph = graph.compile()