from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: ForgingState): state['valid'] = all(k in state['specs'] for k in ['material', 'tolerance']); return state; def check_compliance(state: ForgingState): state['logs'].append('Compliance checked against ISO standards'); return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()