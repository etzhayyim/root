from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplyState(TypedDict): concentrate_id: str; quality_score: float; needs_inspection: bool; def check_quality(state: SupplyState): return {'needs_inspection': state['quality_score'] < 0.9}; def route_procurement(state: SupplyState): return 'inspection' if state['needs_inspection'] else 'approve'; graph = StateGraph(SupplyState); graph.add_node('inspection', check_quality); graph.add_edge('inspection', END); graph.set_entry_point('inspection'); graph = graph.compile()