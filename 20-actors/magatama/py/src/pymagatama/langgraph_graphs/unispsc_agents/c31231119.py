from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ProcureState): state['valid'] = 'Material Grade' in state['specs'] and 'Outer Diameter Tolerance' in state['specs']; return state; def route_logic(state: ProcureState): return 'process' if state['valid'] else END; graph = StateGraph(ProcureState); graph.add_node('process', validate_specs); graph.set_entry_point('process'); graph.add_edge('process', END); graph = graph.compile()