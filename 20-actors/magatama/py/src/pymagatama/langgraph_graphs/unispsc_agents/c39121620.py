from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; valid: bool; def validate_surge_specs(state: State): specs = state['specs']; state['valid'] = specs.get('clamping_voltage', 0) > 0 and 'iec61000' in specs.get('standards', ''); return state; graph = StateGraph(State); graph.add_node('validation', validate_surge_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()