from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SuctionDeviceState(TypedDict): device_specs: dict; compliance_check: bool; graph = StateGraph(SuctionDeviceState); def validate_specs(state: SuctionDeviceState): return {'compliance_check': state['device_specs'].get('pressure', 0) > 0}; def check_certification(state: SuctionDeviceState): return {'compliance_check': 'ISO_13485' in state['device_specs'].get('certs', [])}; graph.add_node('validate', validate_specs); graph.add_node('certify', check_certification); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()