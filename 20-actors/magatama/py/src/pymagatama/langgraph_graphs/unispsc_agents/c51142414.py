from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): medicine: str; compliant: bool; batch_id: str; def validate_pharma(state: PharmaState) -> PharmaState: state['compliant'] = state['medicine'] == 'Sumatriptan' and len(state['batch_id']) > 0; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_pharma); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()