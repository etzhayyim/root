from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DriverState(TypedDict): vendor: str; version: str; validated: bool; errors: List[str]; def validate_driver(state: DriverState) -> DriverState: state['validated'] = state['version'].startswith('v4'); return state; graph = StateGraph(DriverState); graph.add_node('validate', validate_driver); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()