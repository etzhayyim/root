from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_name: str; purity: float; is_gmp_compliant: bool; validation_log: List[str]; def validate_purity(state: ProcureState): state['validation_log'].append('Checking purity levels'); return state if state['purity'] >= 99.0 else {'validation_log': state['validation_log'] + ['Low Purity']}; def check_compliance(state: ProcureState): state['validation_log'].append('Verifying GMP certification'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()