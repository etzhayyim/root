from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MedicalTapeState(TypedDict): tape_specs: dict; compliance_verified: bool; def validate_specs(state: MedicalTapeState): return {'compliance_verified': state['tape_specs'].get('sterilized', False)}; def check_regulatory(state: MedicalTapeState): print('Checking FDA/ISO regulatory compliance'); return {'compliance_verified': True}; graph = StateGraph(MedicalTapeState); graph.add_node('validate', validate_specs); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); app = graph.compile()