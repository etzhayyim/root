from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ImagingTrayState(TypedDict): tray_id: str; compliance_docs: List[str]; qc_passed: bool; def validate_compliance(state: ImagingTrayState): state['qc_passed'] = 'ISO_13485' in state['compliance_docs']; return state; def route_verification(state: ImagingTrayState): return 'valid' if state['qc_passed'] else 'failed'; graph = StateGraph(ImagingTrayState); graph.add_node('validate', validate_compliance); graph.add_conditional_edges('validate', route_verification, {'valid': END, 'failed': END}); graph.set_entry_point('validate'); graph = graph.compile()