from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class TapeRecState(TypedDict): input_data: dict; validation_logs: Annotated[list[str], operator.add]; status: str; def validate_specs(state: TapeRecState): print('Validating tape hardware specs...'); return {'validation_logs': ['Hardware verified'], 'status': 'READY'}; def compliance_check(state: TapeRecState): print('Checking export controls...'); return {'status': 'COMPLIANT'}; graph = StateGraph(TapeRecState); graph.add_node('validate', validate_specs); graph.add_node('compliance', compliance_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()