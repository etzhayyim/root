from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validation_results: dict; def validate_food_grade(state: ProcurementState): state['validation_results'] = {'compliance': 'pending'}; return state; graph = StateGraph(ProcurementState); graph.add_node('check_safety', validate_food_grade); graph.set_entry_point('check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()