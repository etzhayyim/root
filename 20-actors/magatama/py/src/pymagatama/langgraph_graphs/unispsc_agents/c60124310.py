from langgraph.graph import StateGraph, END; from typing import TypedDict; class PotterWheelState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PotterWheelState): state['valid'] = 'rotation_precision_tolerance' in state['specs'] and state['specs']['load_capacity_kg'] > 0; return state; graph = StateGraph(PotterWheelState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()