from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): chemical_data: dict; approval_status: bool; def validate_purity(state: State): status = state['chemical_data'].get('purity', 0) >= 99.0; return {'approval_status': status}; def check_regulatory(state: State): return {'approval_status': state['approval_status'] and state['chemical_data'].get('license_valid', False)}; graph = StateGraph(State); graph.add_node('validate_purity', validate_purity); graph.add_node('check_regulatory', check_regulatory); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_regulatory'); graph.add_edge('check_regulatory', END); compiled_graph = graph.compile()