from langgraph.graph import StateGraph, END; from typing import TypedDict; class CRTState(TypedDict): specs: dict; valid: bool; def validate_crt_specs(state: CRTState): specs = state['specs']; state['valid'] = 'anode_voltage' in specs and specs['anode_voltage'] > 0; return state; graph = StateGraph(CRTState); graph.add_node('validate', validate_crt_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()