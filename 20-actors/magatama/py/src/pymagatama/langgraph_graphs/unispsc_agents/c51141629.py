from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): vendor_certs: List[str]; purity_check: bool; compliant: bool; def validate_purity(state: DrugProcurementState): return {'purity_check': True} if 'GMP' in state['vendor_certs'] else {'purity_check': False}; def compliance_gate(state: DrugProcurementState): return 'valid' if state['purity_check'] else 'invalid'; graph = StateGraph(DrugProcurementState); graph.add_node('validate', validate_purity); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()