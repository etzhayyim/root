from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[dict]; approval_status: bool; def validate_range_specs(state: State): item = state['items'][0]; return {'approval_status': 'power_source' in item and 'safety_cert' in item}; graph = StateGraph(State); graph.add_node('validate', validate_range_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()