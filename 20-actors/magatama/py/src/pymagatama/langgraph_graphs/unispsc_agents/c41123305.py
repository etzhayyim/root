from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HistologyState(TypedDict): data: dict; validation_errors: List[str]; def validate_storage_spec(state: HistologyState): state['validation_errors'] = []; if 'drawer_capacity' not in state['data']: state['validation_errors'].append('Missing Capacity'); return state; def route_to_review(state: HistologyState): return 'end' if not state['validation_errors'] else 'end'; graph = StateGraph(HistologyState); graph.add_node('validate', validate_storage_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()