from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; valid: bool; flang_type: str; def validate_specs(state: State): state['valid'] = 'Material Grade' in state['specs'] and 'Pressure Rating' in state['specs']; return state; builder = StateGraph(State); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()