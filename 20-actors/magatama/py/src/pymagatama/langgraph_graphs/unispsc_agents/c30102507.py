from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): alloy_type: str; thickness: float; certs: List[str]; approved: bool; def validate_spec(state: GraphState): return {'approved': state['thickness'] > 0.5 and 'ASTM' in state['certs']}; def process_magnesium(state: GraphState): print('Processing specs for magnesium sheet'); return state; graph = StateGraph(GraphState); graph.add_node('validate', validate_spec); graph.add_node('process', process_magnesium); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()