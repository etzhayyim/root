from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[str]; inspection_results: dict; graph = StateGraph(State); def check_sterility(state: State): return {'inspection_results': {'sterile': True}}; def validate_batch(state: State): return {'inspection_results': {'valid_expiry': True}}; graph.add_node('check_sterility', check_sterility); graph.add_node('validate_batch', validate_batch); graph.set_entry_point('check_sterility'); graph.add_edge('check_sterility', 'validate_batch'); graph.add_edge('validate_batch', END); graph.compile()