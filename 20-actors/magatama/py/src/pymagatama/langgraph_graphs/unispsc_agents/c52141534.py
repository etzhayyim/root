from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): specs: dict; valid: bool; def check_safety(state: State): state['valid'] = state['specs'].get('Safety_Cert') is not None; return state; graph = StateGraph(State); graph.add_node('safety_check', check_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()