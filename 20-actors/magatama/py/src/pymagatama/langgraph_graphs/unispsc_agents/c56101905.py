from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PanelState(TypedDict): panel_specs: dict; status: str; def validate_specs(state: PanelState): state['status'] = 'validated' if 'material' in state['panel_specs'] else 'error'; return state; def final_check(state: PanelState): state['status'] = 'approved'; return state; graph = StateGraph(PanelState); graph.add_node('validate', validate_specs); graph.add_node('approve', final_check); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()