from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalInstrumentState(TypedDict): instrument_id: str; quality_score: float; compliance_verified: bool; def validate_instrument(state: SurgicalInstrumentState): print(f'Validating instrument {state["instrument_id"]}'); return {'quality_score': 1.0, 'compliance_verified': True}; def check_regulations(state: SurgicalInstrumentState): print('Checking FDA compliance'); return {'compliance_verified': True}; graph = StateGraph(SurgicalInstrumentState); graph.add_node('validate', validate_instrument); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()