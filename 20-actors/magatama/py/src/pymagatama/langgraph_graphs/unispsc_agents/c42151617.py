from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalDrillState(TypedDict): drill_specs: dict; validation_results: List[str]; def validate_spec(state: DentalDrillState): return {'validation_results': ['ISO 3823 compliant'] if state['drill_specs'].get('iso_standard') else ['Invalid standard']}; def finalize(state: DentalDrillState): return {'validation_results': state['validation_results'] + ['Approved']}; graph = StateGraph(DentalDrillState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()