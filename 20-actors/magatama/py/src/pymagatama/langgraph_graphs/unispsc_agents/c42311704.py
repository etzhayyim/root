from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DispenserState(TypedDict): model_id: str; specs: dict; validation_passed: bool; def validate_medical_spec(state: DispenserState): state['validation_passed'] = 'sterilization' in state['specs'] and 'material' in state['specs']; return state; def route_compliance(state: DispenserState): return 'valid' if state['validation_passed'] else 'invalid'; graph = StateGraph(DispenserState); graph.add_node('validate', validate_medical_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()