from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): chemical_data: dict; verification_logs: List[str]; def validate_octanoic_acid(state: AgentState): state['verification_logs'].append('Checking SDS and Hazard class'); return state; graph = StateGraph(AgentState); graph.add_node('validate', validate_octanoic_acid); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()