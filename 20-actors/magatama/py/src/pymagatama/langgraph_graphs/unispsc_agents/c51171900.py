from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medicine_name: str; compliance_checks: List[str]; approved: bool; def check_regulations(state: PharmaState): print(f'Validating {state["medicine_name"]} against GMP standards'); state['compliance_checks'].append('GMP_VALIDATED'); return state; def final_approval(state: PharmaState): state['approved'] = True; return state; graph = StateGraph(PharmaState); graph.add_node('check_regulations', check_regulations); graph.add_node('final_approval', final_approval); graph.set_entry_point('check_regulations'); graph.add_edge('check_regulations', 'final_approval'); graph.add_edge('final_approval', END); graph = graph.compile()