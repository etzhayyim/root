from langgraph.graph import StateGraph, END; from typing import TypedDict; class TitaniumState(TypedDict): spec: dict; validated: bool; status: str; def validate_spec(state: TitaniumState): state['validated'] = 'grade' in state['spec'] and 'dimensions' in state['spec']; state['status'] = 'Validated' if state['validated'] else 'Rejected'; return state; graph = StateGraph(TitaniumState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()