from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FolderState(TypedDict): data: dict; status: list[str]; graph = StateGraph(FolderState); def validate_folder_specs(state: FolderState): return {'status': ['specs_validated']}; def check_inventory(state: FolderState): return {'status': ['inventory_checked']}; graph.add_node('validate', validate_folder_specs); graph.add_node('inventory', check_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); compiled_graph = graph.compile()