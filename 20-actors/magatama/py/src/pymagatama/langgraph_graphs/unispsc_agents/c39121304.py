from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoxCoverState(TypedDict): dimensions: dict; specs: dict; valid: bool; def validate_specs(state: BoxCoverState): return {'valid': all(key in state['specs'] for key in ['IP Rating', 'Material Grade'])}; graph = StateGraph(BoxCoverState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()