from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): chemical_id: str; quality_passed: bool; compliance_checked: bool; def validate_api(state: ProcureState): state['quality_passed'] = state.get('chemical_id') is not None; return state; def check_regulations(state: ProcureState): state['compliance_checked'] = True; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_api); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()