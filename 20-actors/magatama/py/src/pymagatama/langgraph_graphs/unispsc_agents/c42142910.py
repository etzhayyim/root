from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ContactLensCaseState(TypedDict): items: List[str]; validation_status: bool; def validate_cleanliness(state: ContactLensCaseState): state['validation_status'] = True; return state; graph = StateGraph(ContactLensCaseState); graph.add_node('validate', validate_cleanliness); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()