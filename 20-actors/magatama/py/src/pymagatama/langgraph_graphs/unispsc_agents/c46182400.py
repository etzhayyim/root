from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DeconState(TypedDict): items: List[str]; validated: List[str]; status: str; def validate_decon_tool(state: DeconState): validated = [item for item in state['items'] if 'spec_compliant' in item]; return {'validated': validated, 'status': 'completed'}; graph = StateGraph(DeconState); graph.add_node('validate', validate_decon_tool); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()