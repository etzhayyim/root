from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FilterState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: FilterState): result = 'Valid' if state['specs'].get('pore_size') else 'Invalid'; return {'validation_results': [result]}; def check_compliance(state: FilterState): return {'validation_results': state['validation_results'] + ['Regulatory Compliance Verified']}; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()