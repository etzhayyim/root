from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class JuiceState(TypedDict): juice_type: str; quality_passed: bool; specs: dict; def validate_quality(state: JuiceState): state['quality_passed'] = state['specs'].get('brix', 0) > 11; return state; graph = StateGraph(JuiceState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()