from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): data: dict; valid: bool; def validate_directory_spec(state: State): state['valid'] = 'access_control' in state['data'] and 'format' in state['data']; return state; graph = StateGraph(State); graph.add_node('validate', validate_directory_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()