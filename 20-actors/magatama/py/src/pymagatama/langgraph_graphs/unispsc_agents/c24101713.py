from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ConveyorState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ConveyorState): errors = []; if state['specs'].get('load_capacity', 0) <= 0: errors.append('Invalid capacity'); return {'validation_errors': errors}; def check_safety(state: ConveyorState): approved = len(state['validation_errors']) == 0; return {'approved': approved}; graph = StateGraph(ConveyorState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()