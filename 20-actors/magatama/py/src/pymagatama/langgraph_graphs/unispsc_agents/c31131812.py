from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): material_spec: str; dimensions: dict; quality_certs: List[str]; approved: bool; def validate_spec(state: ForgingState): state['approved'] = 'ASTM_B348' in state['material_spec'] and len(state['quality_certs']) > 0; return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()