from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class WindowState(TypedDict): specs: dict; validation_errors: Annotated[list, operator.add]; status: str; def validate_specs(state: WindowState): specs = state['specs']; errors = []; if specs.get('u_factor', 1.0) > 0.5: errors.append('U-factor exceeds efficiency threshold'); return {'validation_errors': errors}; def check_compliance(state: WindowState): return {'status': 'approved' if not state['validation_errors'] else 'rejected'}; graph = StateGraph(WindowState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()