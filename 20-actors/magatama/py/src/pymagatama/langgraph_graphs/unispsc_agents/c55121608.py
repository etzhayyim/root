from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LabelState(TypedDict): spec: dict; is_valid: bool; def validate_barcode(state: LabelState): state['is_valid'] = 'barcode_format' in state['spec'] and state['spec']['barcode_format'] == 'GS1'; return state; graph = StateGraph(LabelState); graph.add_node('validate', validate_barcode); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()