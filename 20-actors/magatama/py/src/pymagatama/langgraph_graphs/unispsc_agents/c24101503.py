from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DollySpecState(TypedDict): specs: dict; ready_for_procurement: bool; def validate_specs(state: DollySpecState): is_valid = state['specs'].get('load_capacity_kg', 0) > 0; return {'ready_for_procurement': is_valid}; graph = StateGraph(DollySpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()