from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LabState(TypedDict): items: Annotated[list, operator.add]; validation_results: dict; def validate_specs(state: LabState): return {'validation_results': {'status': 'compliant'}}; def finalize_procurement(state: LabState): return {'status': 'approved'}; graph = StateGraph(LabState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()