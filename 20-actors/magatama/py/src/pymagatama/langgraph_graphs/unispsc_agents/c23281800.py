from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_specs: dict; validated: bool; analysis: str; def validate_specs(state: ProcureState): state['validated'] = 'material' in state['vendor_specs'] and 'heat_rating' in state['vendor_specs']; return state; def check_compliance(state: ProcureState): state['analysis'] = 'Compliant' if state['validated'] else 'Non-compliant'; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()