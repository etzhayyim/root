from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CapacitorState(TypedDict): specs: dict; valid: bool; task: str; def validate_specs(state: CapacitorState): return {'valid': 'Rated Voltage' in state['specs']}; def approval_step(state: CapacitorState): return {'task': 'Engineering Review'}; graph = StateGraph(CapacitorState); graph.add_node('validate', validate_specs); graph.add_node('approve', approval_step); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()