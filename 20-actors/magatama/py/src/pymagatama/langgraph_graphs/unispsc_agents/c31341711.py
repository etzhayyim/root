from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): model_id: str; specs: dict; validation_report: List[str]; graph = StateGraph(AssemblyState); def validate_material(state: AssemblyState): return {'validation_report': ['Material Grade Verified: Waspaloy']}; def inspect_assembly(state: AssemblyState): return {'validation_report': ['Torque specs checked']}; graph.add_node('material_check', validate_material); graph.add_node('assembly_check', inspect_assembly); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'assembly_check'); graph.add_edge('assembly_check', END); graph = graph.compile()