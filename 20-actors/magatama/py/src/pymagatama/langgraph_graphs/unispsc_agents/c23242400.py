from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): specs: dict; compliance_cleared: bool; graph = StateGraph(MachineState); def validate_specs(state: MachineState): return {'compliance_cleared': state['specs'].get('precision') is not None}; def export_check(state: MachineState): return {'compliance_cleared': state.get('compliance_cleared') and state['specs'].get('export_license')}; graph.add_node('validate', validate_specs); graph.add_node('export_review', export_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()