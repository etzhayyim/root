from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): material_spec: dict; validation_results: List[str]; graph = StateGraph(MaterialState); def validate_specs(state: MaterialState): results = []; if state['material_spec'].get('tensile_strength_mpa', 0) < 5: results.append('Failure: Tensile strength below standard'); return {'validation_results': results}; graph.add_node('spec_validation', validate_specs); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', END); compiled_graph = graph.compile()