from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TowelState(TypedDict): specs: dict; valid: bool; def validate_specs(state: TowelState): state['valid'] = 'gsm_weight_grams_per_sqm' in state['specs']; return state; def check_compliance(state: TowelState): return 'compliance_check' if state['valid'] else END; graph = StateGraph(TowelState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()