from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FoodImportState(TypedDict): vendor_id: str; compliance_docs: List[str]; inspection_passed: bool; def check_compliance(state: FoodImportState): state['compliance_docs'] = ['sps_certificate', 'lab_report']; return {'inspection_passed': True} if len(state['compliance_docs']) >= 2 else {'inspection_passed': False}; def finalize_order(state: FoodImportState): return {'inspection_passed': True}; graph = StateGraph(FoodImportState); graph.add_node('compliance', check_compliance); graph.add_node('finalizer', finalize_order); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'finalizer'); graph.add_edge('finalizer', END); compiled_graph = graph.compile()