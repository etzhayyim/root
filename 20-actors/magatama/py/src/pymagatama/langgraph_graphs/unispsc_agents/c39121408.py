from langgraph.graph import StateGraph, END; from typing import TypedDict; class ConnectorState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ConnectorState): state['valid'] = all(k in state['specs'] for k in ['voltage', 'material']); return state; graph = StateGraph(ConnectorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()