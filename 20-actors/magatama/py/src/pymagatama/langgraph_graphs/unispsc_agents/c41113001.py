from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): controller_specs: dict; validation_results: List[str]; def validate_specs(state: AnalysisState): results = []; if 'protocol' not in state['controller_specs']: results.append('Missing protocol'); return {'validation_results': results}; def hardware_check(state: AnalysisState): print('Performing digital calibration verification'); return {'validation_results': state['validation_results'] + ['Calibration Passed']}; graph = StateGraph(AnalysisState); graph.add_node('validate', validate_specs); graph.add_node('check', hardware_check); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()