from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LatheState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: LatheState): specs = state.get('specs', {}); errors = []; if not specs.get('motor_power_rating'): errors.append('Missing motor power'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(LatheState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()