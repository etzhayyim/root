from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrainingSpecState(TypedDict): items: List[str]; validation_results: dict; def validate_equipment(state: TrainingSpecState): return {'validation_results': {'status': 'approved'}}; graph = StateGraph(TrainingSpecState); graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()