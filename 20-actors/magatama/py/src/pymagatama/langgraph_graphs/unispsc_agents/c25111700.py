from langgraph.graph import StateGraph, END; from typing import TypedDict; class NavalState(TypedDict): vessel_id: str; compliance_check: bool; export_license: str; graph = StateGraph(NavalState); def validate_hull(state): return {'compliance_check': True}; def export_control(state): return {'export_license': 'ITAR_COMPLIANT'}; graph.add_node('hull_check', validate_hull); graph.add_node('export_review', export_control); graph.set_entry_point('hull_check'); graph.add_edge('hull_check', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()