from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): specs: dict; valid: bool; audit_log: List[str]; def validate_specs(state: AssemblyState): state['valid'] = 'Burst Pressure' in state['specs'] and 'Material Grade' in state['specs']; state['audit_log'].append('Validation completed'); return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()