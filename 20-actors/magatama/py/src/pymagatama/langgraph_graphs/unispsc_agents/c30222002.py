from langgraph.graph import StateGraph, END; from typing import TypedDict; class DockState(TypedDict): capacity: float; status: str; def validate_capacity(state: DockState): return {'status': 'validated' if state['capacity'] > 0 else 'error'}; graph = StateGraph(DockState); graph.add_node('validate', validate_capacity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()