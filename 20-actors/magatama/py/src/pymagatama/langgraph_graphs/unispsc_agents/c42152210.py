from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalGoldState(TypedDict): gold_purity: float; chemical_compliance: bool; material_spec: str; validated: bool; def check_purity(state: DentalGoldState): return {'validated': state['gold_purity'] >= 0.999}; def verify_compliance(state: DentalGoldState): return {'validated': state['chemical_compliance']}; graph = StateGraph(DentalGoldState); graph.add_node('check_purity', check_purity); graph.add_node('verify_compliance', verify_compliance); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'verify_compliance'); graph.add_edge('verify_compliance', END); graph = graph.compile()