from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_docs: List[str]; inspection_passed: bool; def validate_cert(state: ProcureState) -> ProcureState: state['inspection_passed'] = 'ISO13485' in state['vendor_docs']; return state; def route_step(state: ProcureState) -> str: return 'validate' if state['inspection_passed'] else 'END'; graph = StateGraph(ProcureState); graph.add_node('validate', validate_cert); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()