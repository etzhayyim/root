from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaunchState(TypedDict): vehicle_data: dict; compliance_status: bool; workflow_steps: List[str]; def validate_export(state: LaunchState): state['compliance_status'] = True; state['workflow_steps'].append('ITAR_Validation'); return state; def prepare_launch(state: LaunchState): state['workflow_steps'].append('Telemetry_Check'); return state; graph = StateGraph(LaunchState); graph.add_node('validate', validate_export); graph.add_node('prepare', prepare_launch); graph.set_entry_point('validate'); graph.add_edge('validate', 'prepare'); graph.add_edge('prepare', END); graph = graph.compile()