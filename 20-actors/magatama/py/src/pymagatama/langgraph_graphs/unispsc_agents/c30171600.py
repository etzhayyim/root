from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WindowState(TypedDict): specs: dict; validation_results: List[str]; def validate_u_value(state: WindowState): return {'validation_results': ['U-value certified']} if state['specs'].get('u_value') < 1.5 else {'validation_results': ['U-value failed']}; def check_safety(state: WindowState): return {'validation_results': state['validation_results'] + ['Safety glass verified']}; graph = StateGraph(WindowState); graph.add_node('validate_thermal', validate_u_value); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate_thermal'); graph.add_edge('validate_thermal', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()