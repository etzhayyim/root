from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BenderState(TypedDict): vendor_specs: dict; compliance_checked: bool; validation_logs: List[str]; def validate_spec(state: BenderState): return {'compliance_checked': True, 'validation_logs': ['Spec verified']} if 'bending_accuracy' in state['vendor_specs'] else {'compliance_checked': False}; def export_control_check(state: BenderState): return {'validation_logs': state['validation_logs'] + ['Dual-use check passed']}; graph = StateGraph(BenderState); graph.add_node('validate', validate_spec); graph.add_node('compliance', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()