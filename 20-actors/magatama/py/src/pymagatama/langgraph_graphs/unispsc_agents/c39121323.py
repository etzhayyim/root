from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): spec: dict; validated: bool; log: List[str]; def validate_specs(state: ProcureState): specs = state['spec']; is_valid = all(key in specs for key in ['ip_rating', 'material']); return {'validated': is_valid, 'log': ['Specs checked']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()