from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlowerStandState(TypedDict): spec: dict; validated: bool; errors: List[str]; def validate_specs(state: FlowerStandState): spec = state.get('spec', {}); errors = []; if spec.get('load_capacity_kg', 0) <= 0: errors.append('Invalid load capacity'); return {'validated': len(errors) == 0, 'errors': errors}; graph = StateGraph(FlowerStandState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()