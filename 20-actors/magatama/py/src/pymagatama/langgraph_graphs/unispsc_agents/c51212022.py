from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): spec: dict; validated: bool; def validate_quality(state: ProcureState): state['validated'] = state['spec'].get('purity_percentage', 0) > 95; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()