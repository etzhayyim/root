from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SwimwearState(TypedDict): fabric_spec: str; safety_cert: bool; approved: bool; def check_fabric(state: SwimwearState): state['approved'] = 'spandex' in state['fabric_spec'].lower(); return state; def check_safety(state: SwimwearState): state['approved'] = state['approved'] and state['safety_cert']; return state; graph = StateGraph(SwimwearState); graph.add_node('check_fabric', check_fabric); graph.add_node('check_safety', check_safety); graph.set_entry_point('check_fabric'); graph.add_edge('check_fabric', 'check_safety'); graph.add_edge('check_safety', END); compile_graph = graph.compile()