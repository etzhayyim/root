from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CleaningSupplyState(TypedDict): items: List[str]; validated: bool; next_step: str; def validate_supplies(state: CleaningSupplyState): return {'validated': all(len(i) > 0 for i in state['items'])}; def finalize_order(state: CleaningSupplyState): return {'next_step': 'READY_FOR_SHIPMENT'}; graph = StateGraph(CleaningSupplyState); graph.add_node('validate', validate_supplies); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()