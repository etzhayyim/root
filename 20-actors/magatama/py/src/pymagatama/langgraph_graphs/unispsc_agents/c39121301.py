from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input: str; validation_results: Annotated[list, operator.add]; def validate_enclosure(state: State): return {'validation_results': ['IP rating verified', 'Material thickness confirmed']}; def compile_summary(state: State): return {'input': 'Enclosure processed successfully'}; graph = StateGraph(State); graph.add_node('validate', validate_enclosure); graph.add_node('summary', compile_summary); graph.set_entry_point('validate'); graph.add_edge('validate', 'summary'); graph.add_edge('summary', END); compiled_graph = graph.compile()