from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolingState(TypedDict): tool_specs: dict; validation_results: List[str]; graph = StateGraph(ToolingState); def validate_specs(state: ToolingState): validation_results = []; if state['tool_specs'].get('angle') not in [60, 82, 90, 100, 120]: validation_results.append('Invalid angle'); return {'validation_results': validation_results}; def finalize_order(state: ToolingState): return {'validation_results': state['validation_results'] + ['Verified']}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()