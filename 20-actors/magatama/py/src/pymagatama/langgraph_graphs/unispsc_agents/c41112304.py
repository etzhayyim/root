from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MoistureMeterState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: MoistureMeterState): state['valid'] = all(k in state['specs'] for k in ['accuracy', 'range']); return state; graph = StateGraph(MoistureMeterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()