from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OpticalState(TypedDict): coating_specs: dict; validation_report: str; def validate_specs(state: OpticalState): if 'Refractive Index' not in state['coating_specs']: return {'validation_report': 'Invalid: Missing specs'}; return {'validation_report': 'Validated'}; def export_control_check(state: OpticalState): return {'validation_report': 'Export control cleared'}; graph = StateGraph(OpticalState); graph.add_node('validate', validate_specs); graph.add_node('export_check', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.compile()