from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class OvenProcurementState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(OvenProcurementState); def validate_thermal_specs(state: OvenProcurementState): specs = state.get('specs', {}); success = 'TempRange' in specs and 'Uniformity' in specs; return {'validation_report': ['Thermal specs valid' if success else 'Invalid specs']}; graph.add_node('validate', validate_thermal_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()