from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MCCState(TypedDict): voltage: int; breaker_type: str; specs_verified: bool; def validate_specs(state: MCCState): state['specs_verified'] = state['voltage'] >= 2300 and state['breaker_type'] != 'None'; return {'specs_verified': state['specs_verified']}; graph = StateGraph(MCCState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()