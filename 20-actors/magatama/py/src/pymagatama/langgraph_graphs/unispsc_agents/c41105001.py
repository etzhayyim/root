from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LabSeparatorState(TypedDict): separator_specs: dict; validation_logs: List[str]; def validate_rpm(state: LabSeparatorState): state['validation_logs'].append('Validating rotation speed.'); return state; def check_safety(state: LabSeparatorState): state['validation_logs'].append('Checking interlocks.'); return state; graph = StateGraph(LabSeparatorState); graph.add_node('validate', validate_rpm); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()