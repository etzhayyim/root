from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DispenserState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: DispenserState): specs = state['specs']; errors = []; if 'flow_rate_lph' not in specs: errors.append('Missing flow rate'); return {'validation_errors': errors}; def finalize_procurement(state: DispenserState): return {'status': 'READY_FOR_RFQ'}; graph = StateGraph(DispenserState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()