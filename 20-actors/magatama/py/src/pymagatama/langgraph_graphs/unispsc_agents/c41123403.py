from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DosingState(TypedDict): dropper_specs: dict; validation_results: List[str]; def validate_calibration(state: DosingState): return {'validation_results': ['Calibration verified']}; def check_material(state: DosingState): return {'validation_results': ['Material ISO compliant']}; graph = StateGraph(DosingState); graph.add_node('calibrate', validate_calibration); graph.add_node('material', check_material); graph.set_entry_point('calibrate'); graph.add_edge('calibrate', 'material'); graph.add_edge('material', END); graph = graph.compile()