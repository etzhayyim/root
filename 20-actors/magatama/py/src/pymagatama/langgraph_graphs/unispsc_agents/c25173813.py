from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TransmissionState(TypedDict): specs: dict; valid: bool; audit_log: List[str]; def validate_specs(state: TransmissionState): return {'valid': 'torque' in state['specs'] and 'gear_ratios' in state['specs']}; def document_audit(state: TransmissionState): print('Logging procurement audit...'); return {'audit_log': ['Specs Verified']}; graph = StateGraph(TransmissionState); graph.add_node('validate', validate_specs); graph.add_node('audit', document_audit); graph.set_entry_point('validate'); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); graph = graph.compile()