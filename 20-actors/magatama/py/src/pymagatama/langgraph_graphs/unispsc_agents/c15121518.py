from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class AlloyProcessingState(TypedDict): alloy_id: str; material_specs: dict; inspection_results: Annotated[Sequence[dict], operator.add]; status: str; def validate_alloy_specs(state: AlloyProcessingState): return {'status': 'validated'} if 'tensile_strength' in state['material_specs'] else {'status': 'rejected'}; def perform_inspection(state: AlloyProcessingState): return {'inspection_results': [{'pass': True, 'test': 'tensile_strength'}]}; graph = StateGraph(AlloyProcessingState); graph.add_node('validate', validate_alloy_specs); graph.add_node('inspect', perform_inspection); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()