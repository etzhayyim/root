from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicrotomeState(TypedDict): spec_data: dict; validation_results: List[str]; def validate_calibration(state: MicrotomeState): state['validation_results'].append('Calibration certificate verified'); return state; def check_compliance(state: MicrotomeState): state['validation_results'].append('ISO 13485 compliant'); return state; graph = StateGraph(MicrotomeState); graph.add_node('validate_calibration', validate_calibration); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_calibration'); graph.add_edge('validate_calibration', 'check_compliance'); graph.add_edge('check_compliance', END); compile_graph = graph.compile()