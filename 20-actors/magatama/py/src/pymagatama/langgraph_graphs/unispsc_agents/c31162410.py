from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PinState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PinState): state['valid'] = 'material' in state['specs'] and 'tolerance' in state['specs']; return state; graph = StateGraph(PinState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()