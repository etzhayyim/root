from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): device_specs: dict; validation_report: List[str]; graph = StateGraph(State); def validate_specs(state: State): report = []; dev = state.get('device_specs', {}); if not dev.get('ISO_Certification'): report.append('Missing ISO certification'); return {'validation_report': report}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()