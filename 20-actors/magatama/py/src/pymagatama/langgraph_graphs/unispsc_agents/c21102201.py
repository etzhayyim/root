from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PumpState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: PumpState): state['valid'] = 'flow_rate' in state['specs'] and 'pressure' in state['specs']; state['log'].append('Validation completed'); return state; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()