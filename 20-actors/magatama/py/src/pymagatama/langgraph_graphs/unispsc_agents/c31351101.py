from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeState(TypedDict): tube_specs: dict; validation_results: List[str]; def validate_welds(state: TubeState): return {'validation_results': ['Weld integrity verified']}; def check_materials(state: TubeState): return {'validation_results': ['Material cert compliant']}; graph = StateGraph(TubeState); graph.add_node('weld_check', validate_welds); graph.add_node('material_check', check_materials); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'weld_check'); graph.add_edge('weld_check', END); graph = graph.compile()