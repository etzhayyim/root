from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): component_id: str; specs: dict; validation_log: List[str]; status: str; def validate_metallurgy(state: AssemblyState): state['validation_log'].append('Inspecting Inconel material grade'); return state; def validate_welds(state: AssemblyState): state['validation_log'].append('Verifying UV weld penetration parameters'); return state; def finalize_assembly(state: AssemblyState): state['status'] = 'CERTIFIED'; return state; graph = StateGraph(AssemblyState); graph.add_node('metallurgy', validate_metallurgy); graph.add_node('welding', validate_welds); graph.add_node('certification', finalize_assembly); graph.set_entry_point('metallurgy'); graph.add_edge('metallurgy', 'welding'); graph.add_edge('welding', 'certification'); graph.add_edge('certification', END); graph = graph.compile()