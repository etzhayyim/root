from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ZincCastState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: ZincCastState): state['validation_results'].append('Dimensional check passed'); return state; def verify_material(state: ZincCastState): state['validation_results'].append('Alloy material verified'); return state; graph = StateGraph(ZincCastState); graph.add_node('validate', validate_dimensions); graph.add_node('material', verify_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'material'); graph.add_edge('material', END); graph = graph.compile()