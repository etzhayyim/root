from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WireRopeState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_clamp(state: WireRopeState): errors = []; if not state['specs'].get('load_rating'): errors.append('Missing SWL'); if not state['specs'].get('material_cert'): errors.append('Missing material cert'); return {'validation_errors': errors, 'approved': len(errors) == 0}; builder = StateGraph(WireRopeState); builder.add_node('validate', validate_clamp); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()