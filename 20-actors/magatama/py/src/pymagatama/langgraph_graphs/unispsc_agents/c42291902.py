from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): tube_specs: dict; validation_results: List[str]; graph = StateGraph(SurgicalState); def validate_spec(state: SurgicalState): return {'validation_results': ['Material ISO 10993 compliant']}; def secure_workflow(state: SurgicalState): return {'validation_results': ['Sterilization verified']}; graph.add_node('validate', validate_spec); graph.add_node('secure', secure_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'secure'); graph.add_edge('secure', END); app = graph.compile()