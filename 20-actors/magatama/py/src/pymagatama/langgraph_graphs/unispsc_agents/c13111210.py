from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MineralState(TypedDict): commodity_id: str; purity: float; origin: str; validated: bool; def validate_purity(state: MineralState): state['validated'] = state['purity'] >= 99.5; return state; def check_origin(state: MineralState): if state['origin'] in ['Japan', 'Australia', 'Canada']: return {'validated': True}; return {'validated': False}; graph = StateGraph(MineralState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_origin', check_origin); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_origin'); graph.add_edge('check_origin', END); graph = graph.compile();