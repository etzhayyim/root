from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RefillState(TypedDict): refill_specs: dict; validation_results: List[str]; def validate_refill(state: RefillState): specs = state['refill_specs']; results = []; if 'refill_type' not in specs: results.append('Missing refill_type'); state['validation_results'] = results; return state; graph = StateGraph(RefillState); graph.add_node('validate', validate_refill); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()