from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ApparelState(TypedDict): items: List[dict]; validation_results: dict; def validate_specifications(state: ApparelState): state['validation_results'] = {'passed': True}; return state; def check_compliance(state: ApparelState): state['validation_results']['compliance'] = 'verified'; return state; graph = StateGraph(ApparelState); graph.add_node('validate', validate_specifications); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()