from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class DeuteriumState(TypedDict): chemical_data: dict; validation_log: Annotated[Sequence[str], operator.add]; status: str; def validate_purity(state: DeuteriumState): purity = state['chemical_data'].get('purity', 0); return {'validation_log': ['Purity check passed' if purity > 99.9 else 'Purity failed'], 'status': 'validated' if purity > 99.9 else 'rejected'}; def export_control_check(state: DeuteriumState): return {'validation_log': ['Export control cleared for shipment']}; graph = StateGraph(DeuteriumState); graph.add_node('purity_check', validate_purity); graph.add_node('export_check', export_control_check); graph.add_edge('purity_check', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('purity_check'); graph = graph.compile()