from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class OphthalmicState(TypedDict): prism_specs: dict; validation_status: bool; def validate_prism(state: OphthalmicState): state['validation_status'] = state['prism_specs'].get('diopter', 0) > 0; return state; graph = StateGraph(OphthalmicState); graph.add_node('validate', validate_prism); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()