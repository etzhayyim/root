from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CouplingState(TypedDict): specs: dict; valid: bool; coupling_type: str; def validate_specs(state: CouplingState): state['valid'] = all(k in state['specs'] for k in ['material', 'pressure']); return state; def route_verification(state: CouplingState): return 'valid' if state['valid'] else END; graph = StateGraph(CouplingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()