from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FusistorState(TypedDict): component_details: dict; validation_passed: bool; logs: List[str]; def validate_electrical_specs(state: FusistorState): state['validation_passed'] = 'resistance' in state['component_details'] and 'current_limit' in state['component_details']; state['logs'].append('Specs validated'); return state; def check_compliance(state: FusistorState): state['logs'].append('Checking RoHS compliance'); return state; graph = StateGraph(FusistorState); graph.add_node('validate', validate_electrical_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()