from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: TubeState): specs = state['specs']; state['valid'] = 'pressure_rating' in specs and 'material_grade' in specs; return state; graph = StateGraph(TubeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()