from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CarboyState(TypedDict): capacity: float; material: str; chemical_compatibility: bool; validated: bool; def validate_spec(state: CarboyState): state['validated'] = state['capacity'] > 0 and state['chemical_compatibility']; return state; def check_material(state: CarboyState): return 'valid' if state['material'] in ['HDPE', 'PP'] else 'invalid'; graph = StateGraph(CarboyState); graph.add_node('validator', validate_spec); graph.add_edge('validator', END); graph.set_entry_point('validator'); compiled_graph = graph.compile()