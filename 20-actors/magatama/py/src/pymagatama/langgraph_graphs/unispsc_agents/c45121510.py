from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CameraProcurementState(TypedDict): camera_type: str; specs: dict; compliant: bool; def validate_specs(state: CameraProcurementState): state['compliant'] = 'ECCN' in state['specs']; return state; def route_procurement(state: CameraProcurementState): return 'validate' if state['compliant'] else END; graph = StateGraph(CameraProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()