from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CTComponentState(TypedDict): component_id: str; compliance_docs: List[str]; inspection_passed: bool; def validate_compliance(state: CTComponentState): return {'inspection_passed': len(state['compliance_docs']) > 2}; def run_sensor_test(state: CTComponentState): return {'inspection_passed': True}; graph = StateGraph(CTComponentState); graph.add_node('validate', validate_compliance); graph.add_node('sensor_test', run_sensor_test); graph.set_entry_point('validate'); graph.add_edge('validate', 'sensor_test'); graph.add_edge('sensor_test', END); app = graph.compile()