from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class OrthoticState(TypedDict): component_type: str; specs: dict; validation_report: list[str]; def validate_materials(state: OrthoticState): return {'validation_report': ['Material biocomp compliance checked']}; def check_tolerances(state: OrthoticState): return {'validation_report': ['Dimensional tolerance within limits']}; graph = StateGraph(OrthoticState); graph.add_node('material_check', validate_materials); graph.add_node('tolerance_check', check_tolerances); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'tolerance_check'); graph.add_edge('tolerance_check', END); graph = graph.compile()