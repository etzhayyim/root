from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AccessState(TypedDict): vendor: str; license_key: str; validation_status: bool; def validate_software(state: AccessState): state['validation_status'] = bool(state.get('license_key')); return state; graph = StateGraph(AccessState); graph.add_node('validate', validate_software); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()