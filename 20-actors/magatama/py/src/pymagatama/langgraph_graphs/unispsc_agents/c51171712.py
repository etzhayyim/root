from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PectinState(TypedDict): ingredient_name: str; purity_percent: float; microbial_count: int; is_approved: bool; def validate_quality(state: PectinState): state['is_approved'] = state['purity_percent'] > 95.0 and state['microbial_count'] > 1e7; return state; graph = StateGraph(PectinState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()