from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DentalSupplyState(TypedDict): supply_type: str; is_sterile: bool; certs: list; graph = StateGraph(DentalSupplyState); def validate_specs(state: DentalSupplyState): return {'is_sterile': True} if 'ISO13485' in state.get('certs', []) else {'is_sterile': False}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()