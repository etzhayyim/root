from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ServoProcureState(TypedDict): input_specs: dict; validation_errors: Annotated[Sequence[str], operator.add]; approval_status: bool; def validate_specs(state: ServoProcureState): errors = []; if not state['input_specs'].get('torque'): errors.append('Missing peak_torque'); return {'validation_errors': errors}; def check_compliance(state: ServoProcureState): status = len(state['validation_errors']) == 0; return {'approval_status': status}; graph = StateGraph(ServoProcureState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()