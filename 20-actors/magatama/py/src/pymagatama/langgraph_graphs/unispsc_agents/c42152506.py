from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalSpecState(TypedDict): material: str; sterility_assured: bool; compliance_docs: List[str]; def validate_medical_spec(state: DentalSpecState): return {'sterility_assured': state['material'] == 'Cotton' and len(state['compliance_docs']) > 0}; graph = StateGraph(DentalSpecState); graph.add_node('validate', validate_medical_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()