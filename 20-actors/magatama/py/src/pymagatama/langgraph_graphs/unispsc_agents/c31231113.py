from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class ZincState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_composition(state: ZincState): state['validation_errors'] = [] if 'grade' not in state['specs'] else []; return state; def check_precision(state: ZincState): return state; graph = StateGraph(ZincState); graph.add_node('validate', validate_composition); graph.add_node('precision', check_precision); graph.set_entry_point('validate'); graph.add_edge('validate', 'precision'); graph.add_edge('precision', END); app = graph.compile()