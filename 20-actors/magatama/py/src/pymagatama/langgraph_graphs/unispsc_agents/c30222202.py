from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SubstationState(TypedDict): capacity_kva: float; voltage_kv: float; status: str; def validate_substation(state: SubstationState): return 'valid' if state['capacity_kva'] > 0 else 'invalid'; graph = StateGraph(SubstationState); graph.add_node('validate', validate_substation); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()