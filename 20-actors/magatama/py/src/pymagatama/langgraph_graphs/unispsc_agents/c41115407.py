from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AASState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: AASState): errors = []; if state['specs'].get('detection_limit') > 10: errors.append('Detection limit exceeds tolerance'); return {'validation_errors': errors}; def review_compliance(state: AASState): state['approved'] = len(state['validation_errors']) == 0; return state; graph = StateGraph(AASState); graph.add_node('validate', validate_specs); graph.add_node('review', review_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'review'); graph.add_edge('review', END); graph = graph.compile()