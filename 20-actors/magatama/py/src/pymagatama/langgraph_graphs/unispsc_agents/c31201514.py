from langgraph.graph import StateGraph, END; from typing import TypedDict; class TapeState(TypedDict): thickness: float; width: float; length: float; valid: bool; def validate_tape(state: TapeState): state['valid'] = state['thickness'] >= 0.075 and state['width'] > 0; return state; graph = StateGraph(TapeState); graph.add_node('validate', validate_tape); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()