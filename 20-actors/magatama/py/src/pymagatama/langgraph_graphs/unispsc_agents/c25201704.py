from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CryptoState(TypedDict): input_spec: dict; compliance_passed: bool; logs: Annotated[list, operator.add]; def validate_export_control(state: CryptoState): status = state['input_spec'].get('ECCN') is not None; return {'compliance_passed': status, 'logs': ['ECCN checked']}; def run_crypto_check(state: CryptoState): return {'logs': ['Validation complete']}; graph = StateGraph(CryptoState); graph.add_node('validate', validate_export_control); graph.add_node('process', run_crypto_check); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()