from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetyState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(SafetyState); def validate_equipment(state: SafetyState): state['validation_results'] = {'passed': True, 'check_type': 'impact_test'}; return state; graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()