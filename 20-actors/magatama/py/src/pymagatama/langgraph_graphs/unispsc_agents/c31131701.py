from langgraph.graph import StateGraph, END; from typing import TypedDict; class ForgingState(TypedDict): alloy_type: str; specs_met: bool; def validate_alloy(state: ForgingState): return {'specs_met': state['alloy_type'] != 'unknown'}; graph = StateGraph(ForgingState); graph.add_node('validate', validate_alloy); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()