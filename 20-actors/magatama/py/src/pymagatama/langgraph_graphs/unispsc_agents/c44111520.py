from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurfaceProtectorState(TypedDict): vendor_specs: dict; compliance_check: bool; item_id: str; def validate_specs(state: SurfaceProtectorState): return {'compliance_check': 'Adhesive Strength Rating' in state['vendor_specs']}; def finalize_order(state: SurfaceProtectorState): return {'compliance_check': True}; graph = StateGraph(SurfaceProtectorState); graph.add_node('validation', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validation'); graph.add_edge('validation', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()