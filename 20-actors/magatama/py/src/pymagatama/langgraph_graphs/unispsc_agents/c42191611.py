from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class NurseSysState(TypedDict): vendor_specs: dict; compliance_check: bool; final_report: str; def validate_specs(state: NurseSysState): return {'compliance_check': all(k in state['vendor_specs'] for k in ['IP_Rating', 'Redundancy'])}; def generate_procurement_report(state: NurseSysState): return {'final_report': 'Validated system for hospital deployment'}; graph = StateGraph(NurseSysState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_procurement_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); app = graph.compile()