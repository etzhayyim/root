from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LensState(TypedDict): model_id: str; dimensions: dict; status: str; def validate_specs(state: LensState): state['status'] = 'verified' if state['dimensions']['diameter'] > 0 else 'failed'; return state; graph = StateGraph(LensState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()