from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CRRTState(TypedDict): items: List[str]; validation_results: dict; def validate_equipment(state: CRRTState): return {'validation_results': {'compliant': True}}; def check_regulatory(state: CRRTState): return {'validation_results': {'cleared': True}}; graph = StateGraph(CRRTState); graph.add_node('validate', validate_equipment); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); app = graph.compile()