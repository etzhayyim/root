from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_id: str; purity: float; compliance_certs: List[str]; validated: bool; def val_step(state: PharmState): state['validated'] = state['purity'] >= 99.0 and len(state['compliance_certs']) > 0; return state; graph = StateGraph(PharmState); graph.add_node('validate', val_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()