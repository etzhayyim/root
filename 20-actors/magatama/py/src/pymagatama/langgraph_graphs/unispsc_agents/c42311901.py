from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DrainageSpec(TypedDict): items: List[str]; validated: bool; graph = StateGraph(DrainageSpec); def validate_specs(state: DrainageSpec): state['validated'] = all('sterile' in item.lower() for item in state['items']); return state; def check_compliance(state: DrainageSpec): print('Checking ISO 13485 standards...'); return state; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()