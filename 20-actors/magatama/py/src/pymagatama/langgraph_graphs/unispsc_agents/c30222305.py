from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LibrarySpecState(TypedDict): specs: dict; validation_errors: List[str]; def validate_load(state: LibrarySpecState): return {'validation_errors': ['Load capacity insufficient'] if state['specs'].get('load', 0) < 50 else []}; def check_fire_rating(state: LibrarySpecState): return {'validation_errors': [] if state['specs'].get('fire_cert') else ['Missing fire cert']}; graph = StateGraph(LibrarySpecState); graph.add_node('validate_load', validate_load); graph.add_node('check_fire_rating', check_fire_rating); graph.set_entry_point('validate_load'); graph.add_edge('validate_load', 'check_fire_rating'); graph.add_edge('check_fire_rating', END); graph = graph.compile()