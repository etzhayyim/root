from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DigestionState(TypedDict): specs: dict; validation_errors: List[str]; validated: bool; def validate_specs(state: DigestionState): specs = state['specs']; errors = []; if 'max_temp' not in specs: errors.append('Missing Temperature Range'); state['validation_errors'] = errors; state['validated'] = len(errors) == 0; return state; graph = StateGraph(DigestionState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()