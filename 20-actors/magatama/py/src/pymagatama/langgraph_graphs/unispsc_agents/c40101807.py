from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SolarState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: SolarState): state['approved'] = state['specs'].get('efficiency', 0) > 0.7; return state; def generate_report(state: SolarState): state['validation_report'] = 'System meets standards' if state['approved'] else 'System failed safety criteria'; return state; graph = StateGraph(SolarState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()