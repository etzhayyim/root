from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IronPowderState(TypedDict): chemical_purity: float; particle_size_micron: float; is_approved: bool; def validate_spec(state: IronPowderState): state['is_approved'] = state['chemical_purity'] >= 99.0 and state['particle_size_micron'] < 150; return state; graph = StateGraph(IronPowderState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()