from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AutopsySpecState(TypedDict): input_data: dict; validation_report: list[str]; approved: bool; def validate_head_rest(state: AutopsySpecState): report = []; data = state['input_data']; if data.get('sterilization_compatible') != True: report.append('Invalid sterilization spec'); return {'validation_report': report, 'approved': len(report) == 0}; graph = StateGraph(AutopsySpecState); graph.add_node('validation', validate_head_rest); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()