from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GymSpecState(TypedDict): equipment_type: str; specs: dict; valid: bool; def validate_specs(state: GymSpecState): state['valid'] = 'FIG_certification_status' in state['specs'] and state['specs']['maximum_load_capacity_kg'] > 0; return state; graph = StateGraph(GymSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()