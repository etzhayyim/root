from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): ingredient_verified: bool; compliance_checked: bool; def verify_ingredients(state: State): return {'ingredient_verified': True}; def check_compliance(state: State): return {'compliance_checked': True}; graph = StateGraph(State); graph.add_node('verify', verify_ingredients); graph.add_node('compliance', check_compliance); graph.add_edge('verify', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('verify')