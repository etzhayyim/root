from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MoldState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: MoldState): state['valid'] = 'Material-Composition-Percentage' in state['specs']; return state; def check_compliance(state: MoldState): return 'valid' if state['valid'] else 'invalid'; graph = StateGraph(MoldState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()