from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: AssemblyState): state['valid'] = 'tensile_strength' in state['specs'] and state['specs']['tensile_strength'] > 1000; return state; def check_compliance(state: AssemblyState): state['logs'].append('Compliance verified'); return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()