from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_certs: List[str]; sterile_compliant: bool; graph = StateGraph(ProcureState); def check_sterility(state: ProcureState): return {'sterile_compliant': 'ISO11135' in state['vendor_certs']}; graph.add_node('verify_specs', check_sterility); graph.set_entry_point('verify_specs'); graph.add_edge('verify_specs', END); compiled_graph = graph.compile()