from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): chemical_name: str; purity: float; compliance_verified: bool; def check_purity(state: PharmaState): return {'compliance_verified': state['purity'] >= 99.0}; def validate_procurement(state: PharmaState): return {'compliance_verified': True}; graph = StateGraph(PharmaState); graph.add_node('verify_purity', check_purity); graph.add_node('compliance', validate_procurement); graph.set_entry_point('verify_purity'); graph.add_edge('verify_purity', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()