from langgraph.graph import StateGraph, END; from typing import TypedDict; class BrowserProcurementState(TypedDict): vendor_url: str; security_cert: bool; approved: bool; def check_browser_compliance(state: BrowserProcurementState): return {'approved': state['security_cert'] == True}; def finalize_procurement(state: BrowserProcurementState): return {'approved': True if state['approved'] else False}; graph = StateGraph(BrowserProcurementState); graph.add_node('compliance', check_browser_compliance); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()