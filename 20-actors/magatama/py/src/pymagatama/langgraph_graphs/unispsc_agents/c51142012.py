from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): chemical_data: dict; compliance_status: bool; def validate_purity(state: State): state['compliance_status'] = state['chemical_data'].get('purity', 0) >= 99.0; return state; def check_regulations(state: State): state['compliance_status'] = state['compliance_status'] and state['chemical_data'].get('has_coa', False); return state; graph = StateGraph(State); graph.add_node('validate_purity', validate_purity); graph.add_node('check_regulations', check_regulations); graph.add_edge('validate_purity', 'check_regulations'); graph.add_edge('check_regulations', END); graph.set_entry_point('validate_purity');