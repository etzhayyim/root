from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ToolSpecState(TypedDict): tool_data: dict; approval_status: bool; def validate_specs(state: ToolSpecState): return {'approval_status': 'DIN' in state['tool_data'].get('certs', [])}; def finalize_procurement(state: ToolSpecState): return {'approval_status': True}; graph = StateGraph(ToolSpecState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()