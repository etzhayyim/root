from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; concentration: float; regulatory_approved: bool; graph = StateGraph(DrugState); def validate_regulatory(state: DrugState): state['regulatory_approved'] = True; return state; graph.add_node('regulatory_check', validate_regulatory); graph.set_entry_point('regulatory_check'); graph.add_edge('regulatory_check', END); graph = graph.compile()