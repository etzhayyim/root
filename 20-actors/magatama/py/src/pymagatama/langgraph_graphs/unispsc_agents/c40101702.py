from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): vendor_specs: dict; compliance_check: bool; def validate_pressure_specs(state: GraphState): state['compliance_check'] = state['vendor_specs'].get('pressure', 0) > 0; return state; def check_certification(state: GraphState): state['compliance_check'] = state['compliance_check'] and 'ASME' in state['vendor_specs'].get('certs', []); return state; graph = StateGraph(GraphState); graph.add_node('validate_pressure', validate_pressure_specs); graph.add_node('check_certs', check_certification); graph.set_entry_point('validate_pressure'); graph.add_edge('validate_pressure', 'check_certs'); graph.add_edge('check_certs', END); graph = graph.compile()