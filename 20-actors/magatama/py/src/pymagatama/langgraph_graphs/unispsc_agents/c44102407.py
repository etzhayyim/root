from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class EmbosserState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(EmbosserState); def validate_specs(state: EmbosserState):   report = []   if 'tape_width' not in state['specs']: report.append('Missing mandatory tape width specification')   return {'validation_report': report}; def finalize_procurement(state: EmbosserState):   return {'validation_report': state['validation_report'] + ['Procurement specification finalized']}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()