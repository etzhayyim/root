from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class MetallurgicalState(TypedDict): chemical_data: dict; validation_results: list; status: str; graph = StateGraph(MetallurgicalState); def validate_purity(state: MetallurgicalState): purity = state.get('chemical_data', {}).get('purity', 0); return {'validation_results': ['Purity Verified' if purity > 99.9 else 'Purity Fail']}; def metallurgical_workflow(state: MetallurgicalState): return {'status': 'READY_FOR_REFINING'}; graph.add_node('validate', validate_purity); graph.add_node('process', metallurgical_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()