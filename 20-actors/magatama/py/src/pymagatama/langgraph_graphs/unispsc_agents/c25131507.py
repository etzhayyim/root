from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftState(TypedDict): aircraft_id: str; compliance: bool; inspections: List[str]; def validate_airworthiness(state: AircraftState): return {'compliance': True}; def perform_inspection(state: AircraftState): return {'inspections': ['structural_integrity', 'engine_check']}; graph = StateGraph(AircraftState); graph.add_node('validate', validate_airworthiness); graph.add_node('inspect', perform_inspection); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()