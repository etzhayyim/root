from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): ingredients: dict; compliance_docs: List[str]; approved: bool; def validate_purity(state: PharmaState): state['approved'] = state['ingredients'].get('kaolin_purity', 0) > 0.99; return state; def check_compliance(state: PharmaState): state['compliance_docs'] = ['COA', 'GMP_Cert']; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()