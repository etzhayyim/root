from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MineralState(TypedDict): commodity_code: str; purity_check: bool; compliance_status: bool; workflow_steps: Annotated[Sequence[str], operator.add]; def validate_purity(state: MineralState) -> MineralState: state['purity_check'] = True; state['workflow_steps'] = ['purity_verified']; return state; def check_compliance(state: MineralState) -> MineralState: state['compliance_status'] = True; state['workflow_steps'] = ['compliance_verified']; return state; graph = StateGraph(MineralState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_compliance'); graph.add_edge('check_compliance', END); graph = graph.compile()