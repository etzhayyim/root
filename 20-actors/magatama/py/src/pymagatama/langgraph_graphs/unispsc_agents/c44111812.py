from langgraph.graph import StateGraph, END; from typing import TypedDict; class DraftingKitState(TypedDict): kit_items: list; spec_compliant: bool; def validate_kit(state: DraftingKitState): return {'spec_compliant': all(item in state['kit_items'] for item in ['ruler', 'compass', 'pen'])}; graph = StateGraph(DraftingKitState); graph.add_node('validate', validate_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()