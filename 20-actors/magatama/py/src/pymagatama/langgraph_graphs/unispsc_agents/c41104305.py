from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InspissatorState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: InspissatorState): state['valid'] = 'Temperature uniformity' in state['specs'] and 'Sterilization validation' in state['specs']; state['log'].append('Validation completed'); return state; graph = StateGraph(InspissatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()