from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftPartsState(TypedDict): part_id: str; specifications: dict; validation_report: List[str]; def validate_heat_exchanger(state: AircraftPartsState): report = []; if 'AS9100' not in state['specifications']: report.append('Missing AS9100 certification'); return {'validation_report': report}; graph = StateGraph(AircraftPartsState); graph.add_node('validate', validate_heat_exchanger); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()