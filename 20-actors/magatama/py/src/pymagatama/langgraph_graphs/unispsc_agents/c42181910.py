from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KitState(TypedDict): kit_id: str; sterility_verified: bool; regulatory_compliance: bool; status: str; def verify_sterility(state: KitState): return {**state, 'sterility_verified': True} if 'id' in state else state; def check_compliance(state: KitState): return {**state, 'regulatory_compliance': True} if state['sterility_verified'] else state; graph = StateGraph(KitState); graph.add_node('verify', verify_sterility); graph.add_node('compliance', check_compliance); graph.add_edge('verify', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('verify'); graph = graph.compile()