from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class OximeterState(TypedDict): device_data: dict; validation_results: list; status: str; def validate_spec(state: OximeterState): results = []; certs = state['device_data'].get('certifications', []); if 'iso_80601' not in certs: results.append('Missing required ISO certification'); return {'validation_results': results, 'status': 'validated' if not results else 'rejected'}; graph = StateGraph(OximeterState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()