from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CardCageSpec(TypedDict): status: str; specs: List[str]; def validate_specs(state: CardCageSpec): return {'status': 'validated'} if len(state['specs']) > 3 else {'status': 'rejected'}; graph = StateGraph(CardCageSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()