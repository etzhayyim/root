from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): items: List[str]; validation_results: List[str]; def validate_specs(state: SpecState): results = ['Verified voltage' for i in state['items']]; return {'validation_results': results}; def finalize_procurement(state: SpecState): return {'validation_results': state['validation_results'] + ['Ready for bidding']}; graph = StateGraph(SpecState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()