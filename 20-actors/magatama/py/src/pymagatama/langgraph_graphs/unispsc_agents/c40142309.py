from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeSpecs(TypedDict): specs: List[str]; validated: bool; def validate_specs(state: PipeSpecs): return {'validated': 'Material Grade' in state['specs'] and 'Pressure Rating' in state['specs']}; graph = StateGraph(PipeSpecs); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()