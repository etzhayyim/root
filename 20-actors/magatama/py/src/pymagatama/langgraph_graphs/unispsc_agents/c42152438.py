from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RefractoryState(TypedDict): material_specs: dict; compliance_check: bool; material_data: str; def validate_specs(state: RefractoryState): if state['material_specs'].get('compressive_strength_mpa', 0) > 20: return {'compliance_check': True}; return {'compliance_check': False}; graph = StateGraph(RefractoryState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()