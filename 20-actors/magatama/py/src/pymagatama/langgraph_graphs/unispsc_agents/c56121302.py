from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class TransportState(TypedDict): specs: dict; validation_status: bool; workflow_log: list; def validate_specs(state: TransportState): state['validation_status'] = state['specs'].get('load_capacity', 0) > 0; return {'validation_status': state['validation_status']}; def route_processing(state: TransportState): return 'end' if state['validation_status'] else 'end'; graph = StateGraph(TransportState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()