from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InfusionState(TypedDict): vendor_id: str; sterility_docs: List[str]; qc_passed: bool; def validate_sterility(state: InfusionState): return {'qc_passed': len(state['sterility_docs']) > 0}; graph = StateGraph(InfusionState); graph.add_node('validate', validate_sterility); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()