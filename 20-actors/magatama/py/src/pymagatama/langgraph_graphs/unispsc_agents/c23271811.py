from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeldingFluxState(TypedDict): flux_type: str; composition: dict; is_compliant: bool; def validate_composition(state: WeldingFluxState): if 'fluoride' in state['composition']: return {'is_compliant': True}; return {'is_compliant': False}; def check_safety(state: WeldingFluxState): return {'is_compliant': state.get('is_compliant', False)}; graph = StateGraph(WeldingFluxState); graph.add_node('validate', validate_composition); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); app = graph.compile()