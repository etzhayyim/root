from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GlassSpec(TypedDict): specs: dict; quality_passed: bool; def validate_specs(state: GlassSpec): state['quality_passed'] = 'thickness_mm' in state['specs'] and 'safety_standard_compliance' in state['specs']; return state; builder = StateGraph(GlassSpec); builder.add_node('validator', validate_specs); builder.set_entry_point('validator'); builder.add_edge('validator', END); graph = builder.compile()