from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BerylliumState(TypedDict): items: List[str]; validation_results: dict; approval_required: bool; def validate_casting(state: BerylliumState): state['validation_results'] = {'purity_check': True, 'tolerance_check': 'Pass'}; return state; def check_export_compliance(state: BerylliumState): state['approval_required'] = True; return state; graph = StateGraph(BerylliumState); graph.add_node('validate', validate_casting); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()