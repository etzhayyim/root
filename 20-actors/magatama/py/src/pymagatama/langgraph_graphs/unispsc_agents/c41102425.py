from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcessorState(TypedDict): input_spec: dict; validation_results: list[str]; def validate_temp_range(state: ProcessorState): temp = state['input_spec'].get('temp_range', 0); status = 'Valid' if 30 <= temp <= 80 else 'Invalid'; return {'validation_results': [f'Temp check: {status}']}; def validate_safety(state: ProcessorState): return {'validation_results': ['Safety check: Pass']}; graph = StateGraph(ProcessorState); graph.add_node('temp_check', validate_temp_range); graph.add_node('safety_check', validate_safety); graph.set_entry_point('temp_check'); graph.add_edge('temp_check', 'safety_check'); graph.add_edge('safety_check', END); compiled_graph = graph.compile()