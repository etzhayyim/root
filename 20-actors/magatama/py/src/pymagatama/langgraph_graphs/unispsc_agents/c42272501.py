from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnesthesiaState(TypedDict): device_specs: dict; compliance_verified: bool; workflow_steps: List[str]; def validate_specs(state: AnesthesiaState): state['compliance_verified'] = 'ISO_13485' in state['device_specs']; return state; def route_workflow(state: AnesthesiaState): return 'complete' if state['compliance_verified'] else 'flag'; workflow = StateGraph(AnesthesiaState); workflow.add_node('validate', validate_specs); workflow.add_edge('validate', END); workflow.set_entry_point('validate'); graph = workflow.compile()