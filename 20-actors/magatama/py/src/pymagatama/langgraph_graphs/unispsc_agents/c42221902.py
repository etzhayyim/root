from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FlowState(TypedDict): device_specs: dict; compliance_status: bool; validation_log: List[str]; def validate_flow_specs(state: FlowState): is_valid = state['device_specs'].get('flow_accuracy') < 0.05; return {'compliance_status': is_valid, 'validation_log': ['Accuracy check passed'] if is_valid else ['Accuracy check failed']}; def generate_procurement_report(state: FlowState): return {'validation_log': state['validation_log'] + ['Report generated']}; graph = StateGraph(FlowState); graph.add_node('validate', validate_flow_specs); graph.add_node('report', generate_procurement_report); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()