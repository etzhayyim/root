from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; status: str; def validate_scraper_spec(state: State): return {'status': 'validated' if 'blade_width' in state['input_data'] else 'rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_scraper_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()