from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def check_compliance(state: ProcureState): state['validated'] = all(c in state.get('certs', []) for c in ['sterility', 'fda_approval']); return state; graph = StateGraph(ProcureState); graph.add_node('validate_docs', check_compliance); graph.set_entry_point('validate_docs'); graph.add_edge('validate_docs', END); graph = graph.compile()