from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class FitnessState(TypedDict): equipment_type: str; specs: dict; compliant: bool; def validate_equipment(state: FitnessState): state['compliant'] = state['specs'].get('weight_capacity', 0) > 100; return state; workflow = StateGraph(FitnessState); workflow.add_node('validate', validate_equipment); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()