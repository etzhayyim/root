from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): data: dict; plan: list; errors: Annotated[list, operator.add]; def validate_specs(state: GraphState): state['data']['validated'] = True; return state; def check_weld_integrity(state: GraphState): if 'weld_strength' not in state['data']: state['errors'].append('Missing weld strength data'); return state; graph = StateGraph(GraphState); graph.add_node('validate', validate_specs); graph.add_node('weld_check', check_weld_integrity); graph.set_entry_point('validate'); graph.add_edge('validate', 'weld_check'); graph.add_edge('weld_check', END); graph = graph.compile()