from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_specs: dict; validation_results: List[str]; def validate_safety(state: ToolState): if state['tool_specs'].get('Safety_Compliance'): return {'validation_results': ['Certified']}; return {'validation_results': ['Safety Check Failed']}; graph = StateGraph(ToolState); graph.add_node('safety_check', validate_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()