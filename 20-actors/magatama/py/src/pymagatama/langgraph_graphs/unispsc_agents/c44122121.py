from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClipState(TypedDict): clip_details: dict; validation_results: List[str]; graph = StateGraph(ClipState); def validate_material(state: ClipState): return {'validation_results': ['Material check passed']}; def check_mounting(state: ClipState): return {'validation_results': ['Mounting compatibility verified']}; graph.add_node('material', validate_material); graph.add_node('mounting', check_mounting); graph.set_entry_point('material'); graph.add_edge('material', 'mounting'); graph.add_edge('mounting', END); graph = graph.compile()