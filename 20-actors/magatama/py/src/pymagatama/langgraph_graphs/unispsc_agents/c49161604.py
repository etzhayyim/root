from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TennisBallState(TypedDict): vendor_id: str; quality_score: float; compliance_doc: List[str]; def validate_itf(state: TennisBallState): return {'quality_score': 1.0 if 'ITF_CERT' in state.get('compliance_doc', []) else 0.0}; def finalize_procurement(state: TennisBallState): return {'vendor_id': 'VERIFIED_OFFICIAL'}; graph = StateGraph(TennisBallState); graph.add_node('validate', validate_itf); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()