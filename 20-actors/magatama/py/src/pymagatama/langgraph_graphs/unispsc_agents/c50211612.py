from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): housing_specs: dict; status: str; def validate_housing(state: State): specs = state['housing_specs']; return {'status': 'passed' if specs.get('heat_resistance') > 150 else 'failed'}; graph = StateGraph(State); graph.add_node('validate', validate_housing); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()