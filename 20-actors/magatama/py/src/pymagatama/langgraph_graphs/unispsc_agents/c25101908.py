from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class QuadState(TypedDict): vehicle_data: dict; validation_results: List[str]; def check_specs(state: QuadState): specs = state['vehicle_data'].get('specs', {}); results = ['Pass' if 'engine_displacement' in specs else 'Fail']; return {'validation_results': results}; def update_inventory(state: QuadState): return {'validation_results': state['validation_results'] + ['Inventory Updated']}; graph = StateGraph(QuadState); graph.add_node('check', check_specs); graph.add_node('inventory', update_inventory); graph.set_entry_point('check'); graph.add_edge('check', 'inventory'); graph.add_edge('inventory', END); compiled_graph = graph.compile()