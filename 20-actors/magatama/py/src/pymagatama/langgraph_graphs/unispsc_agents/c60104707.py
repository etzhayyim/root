from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ManometerState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: ManometerState): specs = state['specs']; is_valid = all(key in specs for key in ['measurement_range', 'accuracy_class']); return {'validation_report': 'Valid' if is_valid else 'Missing specs', 'approved': is_valid}; graph = StateGraph(ManometerState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()