from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ContainerState(TypedDict): container_type: str; specs: dict; validation_report: List[str]; def validate_spec(state: ContainerState): report = []; if state['specs'].get('un_rating') == 'required' and not state['specs'].get('certification_id'): report.append('Missing UN certification'); return {'validation_report': report}; graph = StateGraph(ContainerState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()