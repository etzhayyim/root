from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EaselState(TypedDict): items: List[dict]; status: str; def validate_specs(state: EaselState): return {'status': 'validated'} if state['items'] else {'status': 'pending'}; graph = StateGraph(EaselState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()