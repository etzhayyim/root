from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class ProcessingState(TypedDict): metal_oxide_purity: float; validation_log: Annotated[list[str], operator.add]; status: str; def validate_purity(state: ProcessingState) -> ProcessingState: if state['metal_oxide_purity'] >= 99.9: return {'validation_log': ['Purity standard met'], 'status': 'PASS'}; return {'validation_log': ['Purity below threshold'], 'status': 'FAIL'}; def route_processing(state: ProcessingState) -> str: return 'pass' if state['status'] == 'PASS' else 'fail'; graph = StateGraph(ProcessingState); graph.add_node('validate', validate_purity); graph.add_edge('validate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()