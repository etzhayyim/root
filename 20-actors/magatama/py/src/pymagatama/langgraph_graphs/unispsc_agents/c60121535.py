from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EraserState(TypedDict): items: List[str]; validated_items: List[str]; def validate_material(state: EraserState): return { 'validated_items': [i for i in state['items'] if 'non-toxic' in i] }; graph = StateGraph(EraserState); graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()