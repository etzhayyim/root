from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PowerCordState(TypedDict): input_spec: dict; compliance_check: bool; final_report: str; def validate_specs(state: PowerCordState): specs = state['input_spec']; valid = all([k in specs for k in ['voltage_rating', 'safety_certification_ce_ul']]); return {'compliance_check': valid}; def generate_procurement_report(state: PowerCordState): report = f'Compliance report: {state['compliance_check']}'; return {'final_report': report}; graph = StateGraph(PowerCordState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_procurement_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()