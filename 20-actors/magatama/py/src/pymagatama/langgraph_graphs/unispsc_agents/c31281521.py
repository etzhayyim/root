from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: List[str]; validated: bool; def validate_specs(state: State): state['validated'] = all(len(s) > 0 for s in state['specs']); return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()