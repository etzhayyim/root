from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EquipmentState(TypedDict): items: List[str]; validated: bool; requirements_met: bool; def validate_equipment(state: EquipmentState): return {'validated': all(item for item in state['items'])}; def check_specs(state: EquipmentState): return {'requirements_met': True}; graph = StateGraph(EquipmentState); graph.add_node('validate', validate_equipment); graph.add_node('specs', check_specs); graph.add_edge('validate', 'specs'); graph.add_edge('specs', END); graph.set_entry_point('validate'); graph = graph.compile()