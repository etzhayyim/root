from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RubberProfileState(TypedDict): dimensions: dict; quality_certs: List[str]; approved: bool; def validate_dimensions(state: RubberProfileState): return {'approved': all(v > 0 for v in state['dimensions'].values())}; def check_certs(state: RubberProfileState): return {'approved': 'ISO9001' in state['quality_certs']}; graph = StateGraph(RubberProfileState); graph.add_node('validate', validate_dimensions); graph.add_node('certify', check_certs); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph.set_entry_point('validate'); graph = graph.compile()