from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): chemical_data: dict; approved: bool; def validate_purity(state: State): state['approved'] = state['chemical_data'].get('purity', 0) > 99.0; return state; def check_hazmat(state: State): return 'risk_flag' if state['chemical_data'].get('hazard_class') else 'standard'; graph = StateGraph(State); graph.add_node('validate', validate_purity); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()