from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): component_data: dict; validation_steps: List[str]; approved: bool; def validate_weld_spec(state: AssemblyState): state['validation_steps'].append('WELD_INTEGRITY_CHECK'); state['approved'] = True; return state; def export_compliance(state: AssemblyState): state['validation_steps'].append('ITAR_REVIEW'); return state; graph = StateGraph(AssemblyState); graph.add_node('weld_verify', validate_weld_spec); graph.add_node('compliance', export_compliance); graph.set_entry_point('weld_verify'); graph.add_edge('weld_verify', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()