from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): doc_id: str; weld_verified: bool; material_compliant: bool; def verify_weld(state: AssemblyState): print('Checking UV weld integrity...'); return {'weld_verified': True}; def check_material(state: AssemblyState): print('Validating copper purity...'); return {'material_compliant': True}; graph = StateGraph(AssemblyState); graph.add_node('verify_weld', verify_weld); graph.add_node('check_material', check_material); graph.set_entry_point('verify_weld'); graph.add_edge('verify_weld', 'check_material'); graph.add_edge('check_material', END); compiled_graph = graph.compile()