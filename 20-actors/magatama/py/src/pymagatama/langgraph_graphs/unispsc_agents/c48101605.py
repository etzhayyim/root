from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcessorState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ProcessorState): specs = state.get('specs', {}); errors = []; if specs.get('nsf_certified') != True: errors.append('NSF certification required'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(ProcessorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()