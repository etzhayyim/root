from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IVRState(TypedDict): hardware_specs: dict; compliance_check: bool; graph = StateGraph(IVRState); def validate_spec(state: IVRState): return {'compliance_check': True} if 'language' in state['hardware_specs'] else {'compliance_check': False}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()