from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): chemical_report: str; structural_integrity: bool; certification_passed: bool; def validate_chemistry(state: ForgingState): state['structural_integrity'] = 'Mg' in state['chemical_report']; return state; def verify_certs(state: ForgingState): state['certification_passed'] = True; return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_chemistry); graph.add_node('certify', verify_certs); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph.set_entry_point('validate'); graph = graph.compile()