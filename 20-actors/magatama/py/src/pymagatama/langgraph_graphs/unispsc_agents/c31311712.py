from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: PipeState): state['approved'] = 'Pressure Rating' in state['specs'] and state['specs']['Pressure Rating'] > 0; state['validation_report'] = 'Approved' if state['approved'] else 'Missing critical pressure parameters'; return state; graph = StateGraph(PipeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()