from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): specs: dict; validation_passed: bool; def check_specs(state: FastenerState): state['validation_passed'] = 'thread_pitch' in state['specs'] and 'material' in state['specs']; return state; builder = StateGraph(FastenerState); builder.add_node('validate', check_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()