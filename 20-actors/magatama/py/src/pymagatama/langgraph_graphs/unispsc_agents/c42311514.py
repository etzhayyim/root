from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validated: bool; state: str; def validate_compliance(state: State): state['validated'] = True; state['state'] = 'COMPLIANCE_CHECKED'; return state; def check_sterility(state: State): state['state'] = 'STERILITY_VERIFIED'; return state; builder = StateGraph(State); builder.add_node('compliance', validate_compliance); builder.add_node('sterility', check_sterility); builder.set_entry_point('compliance'); builder.add_edge('compliance', 'sterility'); builder.add_edge('sterility', END); graph = builder.compile()