from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CupState(TypedDict): items: List[dict]; approval: bool; def material_check(state: CupState): print('Checking material safety...'); state['approval'] = all(item.get('food_safe', True) for item in state['items']); return state; def compliance_check(state: CupState): print('Validating heat ratings...'); return {'approval': state['approval'] and all(i.get('heat_resistant') for i in state['items'])}; graph = StateGraph(CupState); graph.add_node('material', material_check); graph.add_node('compliance', compliance_check); graph.set_entry_point('material'); graph.add_edge('material', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()