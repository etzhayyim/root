from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: List[str]; validated: bool; def validate_specs(state: ProcState): state['validated'] = all(s in state['specs'] for s in ['Material', 'Diameter']); return state; builder = StateGraph(ProcState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()