from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class InsulationState(TypedDict): specs: dict; valid: bool; graph = StateGraph(InsulationState); def validate_thermal_props(state): state['valid'] = state['specs'].get('thermal_conductivity', 0) < 0.05; return state; graph.add_node('validate', validate_thermal_props); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()