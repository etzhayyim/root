from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TesterState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(TesterState); def validate_specs(state: TesterState): return {'validation_results': ['Voltage accuracy check passed', 'Current range verify success']}; def run_diagnostics(state: TesterState): return {'validation_results': state['validation_results'] + ['Thermal stability test complete']}; graph.add_node('validate_specs', validate_specs); graph.add_node('diagnostics', run_diagnostics); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', 'diagnostics'); graph.add_edge('diagnostics', END); app = graph.compile()