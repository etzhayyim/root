from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CTComponentState(TypedDict): component_id: str; specs: dict; compliance_checked: bool; final_report: str; def validate_specs(state: CTComponentState): return {'compliance_checked': True}; def generate_procurement_report(state: CTComponentState): return {'final_report': f'Validated {state[\'component_id\']} for procurement.'}; graph = StateGraph(CTComponentState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_procurement_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()