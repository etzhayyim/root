from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): data: dict; status: str; def validate_specs(state: State): state['status'] = 'validation_passed' if state['data'].get('purity', 0) >= 99.9 else 'validation_failed'; return state; def export_check(state: State): state['status'] = 'export_compliant'; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('export_review', export_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph = graph.compile()