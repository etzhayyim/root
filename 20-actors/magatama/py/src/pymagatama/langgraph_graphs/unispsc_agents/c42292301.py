from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class SurgicalInstrumentState(TypedDict): instrument_id: str; spec_docs: List[str]; validation_passed: bool; def validate_spec(state: SurgicalInstrumentState): state['validation_passed'] = len(state['spec_docs']) > 0; return state; graph = StateGraph(SurgicalInstrumentState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()