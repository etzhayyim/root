from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PipeState) -> PipeState: state['valid'] = all(key in state['specs'] for key in ['pressure', 'material']); return state; graph = StateGraph(PipeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()