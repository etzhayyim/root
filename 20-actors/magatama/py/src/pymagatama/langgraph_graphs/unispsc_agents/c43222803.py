from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DLCState(TypedDict): model_number: str; specs: dict; compliance_verified: bool; def validate_specs(state: DLCState): return {'compliance_verified': all(k in state['specs'] for k in ['mtbf', 'voltage'])}; def check_export(state: DLCState): return {'compliance_verified': True if state['specs'].get('export_licensed') else False}; graph = StateGraph(DLCState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()