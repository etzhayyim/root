from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PropellerState(TypedDict): propeller_specs: dict; validation_results: List[str]; def validate_materials(state: PropellerState): return {'validation_results': ['Material check passed']}; def validate_hydrodynamics(state: PropellerState): return {'validation_results': state['validation_results'] + ['Hydrodynamics verified']}; graph = StateGraph(PropellerState); graph.add_node('material_check', validate_materials); graph.add_node('hydro_check', validate_hydrodynamics); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'hydro_check'); graph.add_edge('hydro_check', END); graph = graph.compile()