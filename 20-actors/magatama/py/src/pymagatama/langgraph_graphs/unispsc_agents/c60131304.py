from langgraph.graph import StateGraph, END; from typing import TypedDict; class ViolinState(TypedDict): vendor_check: bool; spec_approval: bool; final_procurement_id: str; def validate_specs(state: ViolinState): return {'spec_approval': True}; def process_procurement(state: ViolinState): return {'final_procurement_id': 'V-VIO-2024-001'}; graph = StateGraph(ViolinState); graph.add_node('validate', validate_specs); graph.add_node('process', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()