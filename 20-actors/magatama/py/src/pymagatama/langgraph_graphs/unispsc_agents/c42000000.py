from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MedicalProcurementState(TypedDict): items: List[str]; compliance_check: bool; validation_log: List[str]; def validate_medical_device(state: MedicalProcurementState): state['validation_log'].append('Validating regulatory documentation...'); state['compliance_check'] = True; return state; def check_sterilization(state: MedicalProcurementState): state['validation_log'].append('Verifying sterilization specs...'); return state; graph = StateGraph(MedicalProcurementState); graph.add_node('validate', validate_medical_device); graph.add_node('sterilization', check_sterilization); graph.set_entry_point('validate'); graph.add_edge('validate', 'sterilization'); graph.add_edge('sterilization', END); compiled_graph = graph.compile()