from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DisintegratorState(TypedDict): vendor_specs: dict; approval_status: bool; validation_log: List[str]; def validate_specs(state: DisintegratorState): status = 'High' in state['vendor_specs'].get('SafetyStandard', '') or 'ISO' in state['vendor_specs'].get('Certification', ''); return {'approval_status': status, 'validation_log': ['Safety standards verified']}; def process_procurement(state: DisintegratorState): return {'validation_log': state['validation_log'] + ['Procurement workflow initiated']}; graph = StateGraph(DisintegratorState); graph.add_node('validate', validate_specs); graph.add_node('process', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()