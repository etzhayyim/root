from langgraph.graph import StateGraph, END; from typing import TypedDict; class StructureState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: StructureState): state['validation_passed'] = 'wind_load' in state['specs'] and 'fire_rating' in state['specs']; return state; def route_approval(state: StructureState): return 'approved' if state['validation_passed'] else 'rejected'; graph = StateGraph(StructureState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()