from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DieCastState(TypedDict): specs: dict; cad_file: str; validation_results: List[str]; graph = StateGraph(DieCastState); def validate_cad(state: DieCastState): return {'validation_results': ['CAD Geometry Validated', 'Tolerance Check Passed']}; def check_metallurgy(state: DieCastState): return {'validation_results': state['validation_results'] + ['Material Compliance Verified']}; graph.add_node('cad_validation', validate_cad); graph.add_node('material_compliance', check_metallurgy); graph.set_entry_point('cad_validation'); graph.add_edge('cad_validation', 'material_compliance'); graph.add_edge('material_compliance', END); compiled_graph = graph.compile()