from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProjectorState(TypedDict): projector_specs: dict; compliance_check: bool; graph = StateGraph(ProjectorState); def validate_lumen(state: ProjectorState): return {'compliance_check': state['projector_specs']['lumens'] > 2000}; def finalize_procurement(state: ProjectorState): return {'compliance_check': True}; graph.add_node('validate', validate_lumen); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()