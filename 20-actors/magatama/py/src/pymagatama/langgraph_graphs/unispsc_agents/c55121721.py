from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SignageState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SignageState): specs = state['specs']; errors = []; if 'Material' not in specs: errors.append('Missing Material'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(SignageState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()