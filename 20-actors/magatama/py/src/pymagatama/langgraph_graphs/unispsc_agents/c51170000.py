from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GIProcurementState(TypedDict): medicine_name: str; dosage: str; compliance_check: bool; storage_temp: str; def validate_compliance(state: GIProcurementState): return {'compliance_check': True} if 'GMP' in state.get('medicine_name', '') else {'compliance_check': False}; def archive_process(state: GIProcurementState): return {'status': 'processed'}; graph = StateGraph(GIProcurementState); graph.add_node('validate', validate_compliance); graph.add_node('archive', archive_process); graph.set_entry_point('validate'); graph.add_edge('validate', 'archive'); graph.add_edge('archive', END); compiled_graph = graph.compile()