from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; purity: float; is_compliant: bool; def validate_purity(state: DrugState): state['is_compliant'] = state['purity'] >= 99.0; return state; def check_gsmp(state: DrugState): print(f'Verifying GMP for {state["chemical_name"]}'); return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_gsmp); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()