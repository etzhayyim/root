from langgraph.graph import StateGraph, END; from typing import TypedDict; class SignalGenState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: SignalGenState): state['compliant'] = 'frequency_range' in state['specs'] and 'eccn' in state['specs']; return state; builder = StateGraph(SignalGenState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()