from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BackupState(TypedDict): data: dict; validation_checks: Annotated[list, operator.add]; def validate_compliance(state: BackupState): return {'validation_checks': ['Encryption-AES-256-Verified', 'RPO-SLA-Validated']}; def finalize_procurement(state: BackupState): return {'data': {'status': 'approved_for_bid'}}; graph = StateGraph(BackupState); graph.add_node('validate', validate_compliance); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()