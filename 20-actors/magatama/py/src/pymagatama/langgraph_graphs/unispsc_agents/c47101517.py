from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UVDisinfectionState(TypedDict): device_id: str; safety_check_passed: bool; intensity_level: float; def validate_safety(state: UVDisinfectionState): state['safety_check_passed'] = state['intensity_level'] > 0; return state; def check_compliance(state: UVDisinfectionState): return 'end' if state['safety_check_passed'] else 'end'; graph = StateGraph(UVDisinfectionState); graph.add_node('safety_validation', validate_safety); graph.add_node('compliance', check_compliance); graph.set_entry_point('safety_validation'); graph.add_edge('safety_validation', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()