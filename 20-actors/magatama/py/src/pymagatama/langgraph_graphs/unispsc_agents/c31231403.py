from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShimState(TypedDict): thickness: float; material: str; validated: bool; state = ShimState(thickness=0.0, material='SUS304', validated=False); def validate_shim(state: ShimState): state['validated'] = 0.001 <= state['thickness'] <= 1.0 and state['material'] != 'unknown'; return state; graph = StateGraph(ShimState); graph.add_node('validate', validate_shim); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()