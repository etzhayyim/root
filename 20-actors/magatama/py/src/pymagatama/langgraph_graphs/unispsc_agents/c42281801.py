from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_strip_spec(state: AgentState): data = state['input_data']; valid = 'accuracy' in data and 'range' in data; return {'validation_results': ['Spec Validated'] if valid else ['Spec Invalid'], 'status': 'COMPLETE'}; graph = StateGraph(AgentState); graph.add_node('validate', validate_strip_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()