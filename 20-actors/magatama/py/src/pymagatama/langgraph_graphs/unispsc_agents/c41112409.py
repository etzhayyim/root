from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScannerState(TypedDict): pressure_data: list; validation_status: bool; error_log: List[str]; def validate_scanner_spec(state: ScannerState): state['validation_status'] = True; return state; def export_control_check(state: ScannerState): return state; graph = StateGraph(ScannerState); graph.add_node('validate_specs', validate_scanner_spec); graph.add_node('export_check', export_control_check); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()