from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class MineralChemicalState(TypedDict): chemical_code: str; purity_check: bool; safety_audit: bool; approval_path: List[str]; def validate_purity(state: MineralChemicalState): return {'purity_check': True, 'approval_path': state['approval_path'] + ['purity_verified']}; def run_safety_audit(state: MineralChemicalState): return {'safety_audit': True, 'approval_path': state['approval_path'] + ['safety_cleared']}; graph = StateGraph(MineralChemicalState); graph.add_node('validate_purity', validate_purity); graph.add_node('run_safety_audit', run_safety_audit); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'run_safety_audit'); graph.add_edge('run_safety_audit', END); graph = graph.compile()