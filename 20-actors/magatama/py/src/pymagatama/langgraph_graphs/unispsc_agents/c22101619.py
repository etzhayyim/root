from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeldingMaterialState(TypedDict): material_specs: dict; validation_report: List[str]; def validate_material(state: WeldingMaterialState): specs = state['material_specs']; report = []; if specs.get('tensile_strength', 0) < 400: report.append('Low tensile strength detected'); return {'validation_report': report}; def finalize_procurement(state: WeldingMaterialState): return {'validation_report': state['validation_report'] + ['Procurement approved']}; graph = StateGraph(WeldingMaterialState); graph.add_node('validate', validate_material); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()