from langgraph.graph import StateGraph, END; from typing import TypedDict; class IndicatorState(TypedDict): input_specs: dict; validated: bool; log: list; def validate_specs(state: IndicatorState): return {"validated": all(k in state['input_specs'] for k in ['voltage', 'ip_rating', 'color'])}; graph = StateGraph(IndicatorState); graph.add_node("validation", validate_specs); graph.set_entry_point("validation"); graph.add_edge("validation", END); graph = graph.compile()