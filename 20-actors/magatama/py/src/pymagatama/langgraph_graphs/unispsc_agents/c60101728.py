from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ResourceState(TypedDict): resource_id: str; content_type: str; curriculum_match: bool; approved: bool; def validate_curriculum(state: ResourceState): print(f'Validating curriculum for {state["resource_id"]}'); return {"curriculum_match": True}; def finalize_approval(state: ResourceState): return {"approved": state["curriculum_match"]}; graph = StateGraph(ResourceState); graph.add_node("validate", validate_curriculum); graph.add_node("finalize", finalize_approval); graph.set_entry_point("validate"); graph.add_edge("validate", "finalize"); graph.add_edge("finalize", END); graph = graph.compile()