from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FruitState(TypedDict): fruit_type: str; quality_score: float; storage_path: str; def validate_freshness(state: FruitState): state['quality_score'] = 1.0; return state; def check_compliance(state: FruitState): return 'approved' if state['quality_score'] > 0.8 else 'rejected'; graph = StateGraph(FruitState); graph.add_node('validate', validate_freshness); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()