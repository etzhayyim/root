from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ConnectorState(TypedDict): connector_type: str; specs: dict; compliance: bool; def validate_specs(state: ConnectorState) -> ConnectorState: state['compliance'] = state['specs'].get('frequency_range_ghz', 0) > 0; return state; graph = StateGraph(ConnectorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()