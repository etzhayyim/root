from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RodState(TypedDict): rod_specs: dict; validation_errors: List[str]; def validate_specs(state: RodState): errors = []; if not state['rod_specs'].get('length'): errors.append('Missing length'); return {'validation_errors': errors}; graph = StateGraph(RodState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()