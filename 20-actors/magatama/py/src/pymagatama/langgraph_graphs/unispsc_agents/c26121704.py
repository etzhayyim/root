from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HarnessState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: HarnessState): state['validation_passed'] = all(k in state['specs'] for k in ['connector_type', 'insulation']); return state; def finalize_order(state: HarnessState): return {'status': 'processed'}; graph = StateGraph(HarnessState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()