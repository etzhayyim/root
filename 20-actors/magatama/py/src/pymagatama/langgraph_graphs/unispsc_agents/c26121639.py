from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: CableState): is_valid = 'Voltage Rating' in state['specs'] and 'AWG' in state['specs']; return {'approved': is_valid, 'validation_errors': [] if is_valid else ['Missing critical specs']}; graph = StateGraph(CableState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()