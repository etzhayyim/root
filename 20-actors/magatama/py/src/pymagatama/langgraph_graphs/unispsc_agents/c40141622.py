from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ValveState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: ValveState): specs = state['specs']; required = ['pressure_rating', 'material', 'connection_type']; state['valid'] = all(k in specs for k in required); state['error'] = '' if state['valid'] else 'Missing required specifications'; return state; graph = StateGraph(ValveState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()