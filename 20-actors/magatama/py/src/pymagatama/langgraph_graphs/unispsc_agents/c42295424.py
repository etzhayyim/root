from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PrepSupplyState(TypedDict): supply_id: str; sterility_verified: bool; compliance_docs: List[str]; graph = StateGraph(PrepSupplyState); def verify_sterility(state: PrepSupplyState): return {'sterility_verified': True}; def validate_compliance(state: PrepSupplyState): return {'compliance_docs': ['FDA_Class_I', 'ISO_13485']}; graph.add_node('verify', verify_sterility); graph.add_node('compliance', validate_compliance); graph.set_entry_point('verify'); graph.add_edge('verify', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()