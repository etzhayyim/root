from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): spec: dict; validated: bool; app_scope: str; def validate_spec(state: ProcureState): state['validated'] = 'Pressure Rating PSI' in state['spec'] and 'Material Grade' in state['spec']; return state; builder = StateGraph(ProcureState); builder.add_node('validate', validate_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()