from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlateState(TypedDict): specs: dict; valid: bool; workflow_log: List[str]; def validate_specs(state: PlateState): state['valid'] = len(state['specs']) >= 3; state['workflow_log'].append('Validation Complete'); return state; def prepare_shipping(state: PlateState): state['workflow_log'].append('Packing and Surface Inspection'); return state; graph = StateGraph(PlateState); graph.add_node('validate', validate_specs); graph.add_node('ship', prepare_shipping); graph.add_edge('validate', 'ship'); graph.add_edge('ship', END); graph.set_entry_point('validate'); graph = graph.compile()