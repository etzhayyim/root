from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): bat_specs: dict; compliance_status: bool; def validate_bat(state: State): specs = state['bat_specs']; return {'compliance_status': specs.get('certification') is not None and specs.get('weight') > 0}; def finalize_order(state: State): return {'compliance_status': True}; graph = StateGraph(State); graph.add_node('validate', validate_bat); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()