from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): items: List[str]; safety_verified: bool; compliance_docs: bool; def check_hazmat(state: KitState): state['safety_verified'] = all('reagent' in item for item in state['items']); return state; def verify_compliance(state: KitState): state['compliance_docs'] = True; return state; graph = StateGraph(KitState); graph.add_node('hazmat_check', check_hazmat); graph.add_node('compliance_check', verify_compliance); graph.set_entry_point('hazmat_check'); graph.add_edge('hazmat_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()