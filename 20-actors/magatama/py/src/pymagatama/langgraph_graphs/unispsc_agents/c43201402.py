from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): hardware_config: dict; status: str; validation_errors: Annotated[list, operator.add]; def validate_switch_spec(state: NetworkState): if state['hardware_config'].get('throughput') < 10: return {'validation_errors': ['Insufficient throughput']}; return {'status': 'validated'}; def deploy_config(state: NetworkState): return {'status': 'deployed'}; graph = StateGraph(NetworkState); graph.add_node('validate', validate_switch_spec); graph.add_node('deploy', deploy_config); graph.set_entry_point('validate'); graph.add_edge('validate', 'deploy'); graph.add_edge('deploy', END); compiled_graph = graph.compile()