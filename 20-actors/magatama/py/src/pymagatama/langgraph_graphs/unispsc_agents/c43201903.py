from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TapeLibraryState(TypedDict): model_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: TapeLibraryState): errors = []; if 'storage_capacity' not in state['model_specs']: errors.append('Missing capacity'); return {'validation_errors': errors}; def check_export(state: TapeLibraryState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(TapeLibraryState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()