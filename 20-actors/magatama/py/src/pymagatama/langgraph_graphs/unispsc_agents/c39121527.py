from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class EncoderState(TypedDict): resolution: int; signal: str; validated: bool; graph = StateGraph(EncoderState); def validate_specs(state: EncoderState): return {'validated': state['resolution'] > 0 and state['signal'] in ['TTL', 'HTL', 'SSI']}; graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()