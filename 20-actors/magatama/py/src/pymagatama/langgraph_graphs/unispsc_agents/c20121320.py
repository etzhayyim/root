from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class ShieldState(TypedDict): material_specs: dict; validation_results: List[str]; approved: bool; def validate_specs(state: ShieldState): results = []; if state['material_specs'].get('permeability', 0) > 50000: results.append('Permeability OK'); else: results.append('Permeability Low'); return {'validation_results': results}; def check_approval(state: ShieldState): return {'approved': len(state['validation_results']) > 0}; graph = StateGraph(ShieldState); graph.add_node('validate', validate_specs); graph.add_node('approve', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()