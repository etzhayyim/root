from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TapeProcurementState(TypedDict): specs: dict; validation_errors: Annotated[list, operator.add]; status: str; def validate_spindle_specs(state: TapeProcurementState): core_size = state['specs'].get('core_size'); errors = [] if core_size not in [1, 3] else []; return {'validation_errors': errors}; graph = StateGraph(TapeProcurementState); graph.add_node('validate', validate_spindle_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()