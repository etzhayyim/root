from langgraph.graph import StateGraph, END; from typing import TypedDict; class HarnessState(TypedDict): specs: dict; valid: bool; def validate_specs(state: HarnessState): state['valid'] = 'TSO_Certification' in state['specs'] and state['specs']['Tensile_Strength'] > 5000; return state; graph = StateGraph(HarnessState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()