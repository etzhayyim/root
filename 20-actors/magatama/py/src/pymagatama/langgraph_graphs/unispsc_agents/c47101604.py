from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoilerState(TypedDict): chemical_name: str; concentration: float; hazard_verified: bool; def validate_chemical(state: BoilerState): state['hazard_verified'] = state['concentration'] < 0.8; return state; def check_sds(state: BoilerState): return {'hazard_verified': True} if state['chemical_name'] else {'hazard_verified': False}; graph = StateGraph(BoilerState); graph.add_node('validate', validate_chemical); graph.add_node('sds_check', check_sds); graph.set_entry_point('validate'); graph.add_edge('validate', 'sds_check'); graph.add_edge('sds_check', END); graph = graph.compile()