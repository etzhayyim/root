from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): vendor_certs: list; specs_approved: bool; def check_compliance(state: ProcState): return {'specs_approved': 'ISO' in state['vendor_certs']}; def dispatch(state: ProcState): return 'Ready to ship' if state['specs_approved'] else 'Hold for audit'; graph = StateGraph(ProcState); graph.add_node('verify', check_compliance); graph.add_node('dispatch', dispatch); graph.set_entry_point('verify'); graph.add_edge('verify', 'dispatch'); graph.add_edge('dispatch', END); graph = graph.compile()