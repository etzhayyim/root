from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MotorState(TypedDict): specs: dict; validation_errors: Annotated[Sequence[str], operator.add]; status: str; def validate_specs(state: MotorState): specs = state['specs']; errors = []; if specs.get('torque_nm', 0) <= 0: errors.append('Invalid torque value'); return {'validation_errors': errors}; def compile_robot_graph(): workflow = StateGraph(MotorState); workflow.add_node('validate', validate_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); return workflow.compile(); graph = compile_robot_graph()