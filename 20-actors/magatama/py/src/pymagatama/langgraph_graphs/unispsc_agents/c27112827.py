from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): device_specs: dict; valid: bool; def validate_specs(state: ProcureState): state['valid'] = 'magnetic_density' in state['device_specs']; return state; def route_step(state: ProcureState): return 'validate' if state['valid'] else END; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()