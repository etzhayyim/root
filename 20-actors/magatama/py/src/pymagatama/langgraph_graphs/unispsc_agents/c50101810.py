from langgraph.graph import StateGraph, END; from typing import TypedDict; class PomeloState(TypedDict): fruit_type: str; quality_score: float; is_approved: bool; def check_quality(state: PomeloState): return {'is_approved': state['quality_score'] > 0.8}; graph = StateGraph(PomeloState); graph.add_node('check_quality', check_quality); graph.set_entry_point('check_quality'); graph.add_edge('check_quality', END); graph = graph.compile()