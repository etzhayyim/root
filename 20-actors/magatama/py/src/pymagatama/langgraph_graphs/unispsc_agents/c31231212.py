from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TitaniumState(TypedDict): material_spec: str; status: str; certification_valid: bool; def validate_grade(state: TitaniumState): state['certification_valid'] = 'Grade 5' in state['material_spec']; return {'status': 'validated' if state['certification_valid'] else 'rejected'}; def finalize_order(state: TitaniumState): return {'status': 'ready_for_shipment'}; graph = StateGraph(TitaniumState); graph.add_node('validate', validate_grade); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.set_entry_point('validate'); graph.add_edge('finalize', END); compiled_graph = graph.compile()