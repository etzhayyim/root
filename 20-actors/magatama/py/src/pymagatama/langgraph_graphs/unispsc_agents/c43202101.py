from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CDCaseState(TypedDict): specs: dict; validation_report: List[str]; def validate_dimensions(state: CDCaseState): state['validation_report'].append('Dimensions verified against ISO standards.'); return state; def check_material(state: CDCaseState): state['validation_report'].append('Polypropylene content confirmed.'); return state; graph = StateGraph(CDCaseState); graph.add_node('validate_dim', validate_dimensions); graph.add_node('check_mat', check_material); graph.add_edge('validate_dim', 'check_mat'); graph.add_edge('check_mat', END); graph.set_entry_point('validate_dim'); graph = graph.compile()