from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; specs: dict; status: str; def validate_specs(state: AssemblyState): print(f'Validating specs for {state['assembly_id']}'); return {'status': 'validated' if state['specs'] else 'failed'}; def perform_leak_test(state: AssemblyState): return {'status': 'leak_test_passed'}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('leak_test', perform_leak_test); graph.set_entry_point('validate'); graph.add_edge('validate', 'leak_test'); graph.add_edge('leak_test', END); compiled_graph = graph.compile()