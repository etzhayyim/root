from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TileState(TypedDict): items: List[str]; validated: bool; def validate_tiles(state: TileState): return {'validated': all(len(t) > 0 for t in state['items'])}; graph = StateGraph(TileState); graph.add_node('validate', validate_tiles); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()