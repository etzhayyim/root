from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensional(state: ForgingState): state['validation_results'].append('Dimensional check passed'); return state; def validate_material(state: ForgingState): state['validation_results'].append('MTR verified'); return state; workflow = StateGraph(ForgingState); workflow.add_node('dim', validate_dimensional); workflow.add_node('mat', validate_material); workflow.set_entry_point('dim'); workflow.add_edge('dim', 'mat'); workflow.add_edge('mat', END); graph = workflow.compile()