from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): spec_data: dict; status: str; def validate_specs(state: State): specs = state['spec_data']; state['status'] = 'approved' if specs.get('load_capacity_kg', 0) > 100 else 'rejected'; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()