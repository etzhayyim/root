from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; valid: bool; def validate_specs(state: State): state['valid'] = 'Alloy grade' in state['specs'] and state['specs']['Alloy grade'].startswith('C'); return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()