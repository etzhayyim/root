from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DNASeqState(TypedDict): device_specs: dict; validation_results: List[str]; def validate_device(state: DNASeqState): results = []; specs = state.get('device_specs', {}); if 'calibration_date' not in specs: results.append('Missing Calibration'); return {'validation_results': results}; def generate_report(state: DNASeqState): print('Generating procurement report'); return {'validation_results': state['validation_results'] + ['Report Generated']}; graph = StateGraph(DNASeqState); graph.add_node('validate', validate_device); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END);