from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_certs: List[str]; safety_pass: bool; system_specs: dict; graph = StateGraph(ProcureState); def validate_certs(state: ProcureState): state['safety_pass'] = 'ISO 13485' in state['vendor_certs']; return state; def check_thermal_specs(state: ProcureState): return {'safety_pass': state['system_specs'].get('temp_range') is not None}; graph.add_node('ValidateCerts', validate_certs); graph.add_node('CheckSpecs', check_thermal_specs); graph.set_entry_point('ValidateCerts'); graph.add_edge('ValidateCerts', 'CheckSpecs'); graph.add_edge('CheckSpecs', END); compile_graph = graph.compile()