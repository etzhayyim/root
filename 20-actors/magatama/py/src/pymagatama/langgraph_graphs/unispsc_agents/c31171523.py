from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AirBearingState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: AirBearingState): state['valid'] = state['specs'].get('load_capacity_n', 0) > 0; return state; graph = StateGraph(AirBearingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()