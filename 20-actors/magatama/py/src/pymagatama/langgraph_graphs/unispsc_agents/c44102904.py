from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SprayState(TypedDict): chemical_data: dict; safety_compliant: bool; def validate_safety(state: SprayState): state['safety_compliant'] = state['chemical_data'].get('flammable', False) == False; return state; def check_pressure(state: SprayState): print(f"Validating pressure for {state['chemical_data'].get('name')}"); return state; graph = StateGraph(SprayState); graph.add_node('safety', validate_safety); graph.add_node('pressure', check_pressure); graph.add_edge('safety', 'pressure'); graph.add_edge('pressure', END); graph.set_entry_point('safety'); compiled_graph = graph.compile()