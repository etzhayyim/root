from langgraph.graph import StateGraph, END; from typing import TypedDict; class ToolState(TypedDict): tool_specs: dict; validation_report: str; def validate_specs(state: ToolState): specs = state['tool_specs']; is_valid = 'material' in specs and 'torque_rating' in specs; return {'validation_report': 'Passed' if is_valid else 'Failed'}; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()