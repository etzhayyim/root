from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PostOpState(TypedDict): items: List[dict]; valid: bool; error: str; def validate_specs(state: PostOpState): state['valid'] = all('medical_device_registration_id' in item for item in state['items']); return state; graph = StateGraph(PostOpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()