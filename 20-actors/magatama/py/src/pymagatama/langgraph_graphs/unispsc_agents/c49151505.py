from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SnowboardState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: SnowboardState): if state['specs'].get('flex_rating', 0) < 1: state['validation_errors'].append('Invalid Flex'); return {'status': 'validated' if not state['validation_errors'] else 'failed'}; graph = StateGraph(SnowboardState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()