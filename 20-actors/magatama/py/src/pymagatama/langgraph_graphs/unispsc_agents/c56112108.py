from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): specs: dict; valid: bool; furniture_type: str; def validate_specs(state: FurnitureState): state['valid'] = 'dimensions' in state['specs'] and 'material' in state['specs']; return state; def finalize(state: FurnitureState): return {'result': 'approved' if state['valid'] else 'rejected'}; graph = StateGraph(FurnitureState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()