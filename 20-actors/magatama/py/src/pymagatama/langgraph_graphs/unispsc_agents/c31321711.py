from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; assembly_complete: bool; def validate_material(state: AssemblyState): return {'validation_results': ['Material Alloy Verified']}; def verify_torque(state: AssemblyState): return {'validation_results': ['Torque Check Passed']}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_material); graph.add_node('torque', verify_torque); graph.set_entry_point('validate'); graph.add_edge('validate', 'torque'); graph.add_edge('torque', END); graph = graph.compile()