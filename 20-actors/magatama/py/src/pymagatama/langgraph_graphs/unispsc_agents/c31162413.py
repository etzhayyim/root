from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): snap_spec: dict; validation_log: List[str]; def validate_dimensions(state: State): state['validation_log'].append('Dimensional check passed'); return state; def check_corrosion(state: State): state['validation_log'].append('Corrosion resistance verified'); return state; graph = StateGraph(State); graph.add_node('validate', validate_dimensions); graph.add_node('corrosion', check_corrosion); graph.set_entry_point('validate'); graph.add_edge('validate', 'corrosion'); graph.add_edge('corrosion', END); graph = graph.compile()