from langgraph.graph import StateGraph, END; from typing import TypedDict; class MopedState(TypedDict): specs: dict; valid: bool; def validate_specs(state: MopedState): valid = state['specs'].get('engine_displacement', 0) <= 50; return {'valid': valid}; graph = StateGraph(MopedState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()