from typing import TypedDict, Any; from langgraph.graph import StateGraph, END; class CrystallographyState(TypedDict): data: dict; status: str; def validate_specs(state: CrystallographyState): return {'status': 'validated' if 'source' in state['data'] else 'incomplete'}; def approve_export(state: CrystallographyState): return {'status': 'approved_for_export'}; graph = StateGraph(CrystallographyState); graph.add_node('validate', validate_specs); graph.add_node('export_review', approve_export); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph.set_entry_point('validate'); compile_graph = graph.compile()