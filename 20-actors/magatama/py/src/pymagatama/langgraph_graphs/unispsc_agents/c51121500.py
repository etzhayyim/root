from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): medicine_code: str; license_verified: bool; temp_req: str; validated: bool; def verify_regulatory(state: DrugProcurementState): state['license_verified'] = True; return state; def check_temp_controls(state: DrugProcurementState): state['validated'] = True; return state; graph = StateGraph(DrugProcurementState); graph.add_node('verify', verify_regulatory); graph.add_node('temp_check', check_temp_controls); graph.add_edge('verify', 'temp_check'); graph.add_edge('temp_check', END); graph.set_entry_point('verify'); graph = graph.compile()