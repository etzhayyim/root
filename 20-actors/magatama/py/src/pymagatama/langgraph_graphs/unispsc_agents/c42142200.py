from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HydroState(TypedDict): device_specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_specs(state: HydroState): state['compliance_status'] = 'temp_range' in state['device_specs'] and 'iso_cert' in state['device_specs']; return state; def finalize_order(state: HydroState): state['workflow_log'].append('Validation Complete'); return state; graph = StateGraph(HydroState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compile_graph = graph.compile()