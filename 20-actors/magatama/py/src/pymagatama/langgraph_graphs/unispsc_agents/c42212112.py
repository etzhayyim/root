from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): aids_list: List[str]; inspection_results: List[str]; approved: bool; def validate_ergonomics(state: ProcurementState): return {'inspection_results': ['Ergonomics Validated']}; def check_compliance(state: ProcurementState): return {'approved': True}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_ergonomics); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()