from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph; class GraphState(TypedDict): status: str; validation_results: Annotated[list, operator.add]; def validate_welds(state: GraphState): return {'validation_results': ['UV_Weld_Integrity_Confirmed']}; def compliance_check(state: GraphState): return {'validation_results': ['Export_Control_Clear']}; graph = StateGraph(GraphState); graph.add_node('validate', validate_welds); graph.add_node('compliance', compliance_check); graph.add_edge('validate', 'compliance'); graph.set_entry_point('validate'); graph.set_finish_point('compliance'); compile_graph = graph.compile()