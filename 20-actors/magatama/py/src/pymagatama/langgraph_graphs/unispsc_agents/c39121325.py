from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): specs: dict; valid: bool; def validate_enclosure(state: ProcState): state['valid'] = 'IP_Rating' in state['specs'] and state['specs']['IP_Rating'] >= 54; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_enclosure); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()