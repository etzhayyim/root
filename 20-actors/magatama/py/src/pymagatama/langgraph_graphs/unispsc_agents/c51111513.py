from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_docs: List[str]; qc_passed: bool; status: str; def validate_compliance(state: ProcureState): state['qc_passed'] = all('GMP' in doc for doc in state['vendor_docs']); return {'status': 'Validated' if state['qc_passed'] else 'Rejected'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()