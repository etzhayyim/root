from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; valid: bool; error: str; def validate_dimensions(state: ProcurementState): spec = state['specs']; state['valid'] = spec.get('width', 0) > 0 and spec.get('length', 0) > 0; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()