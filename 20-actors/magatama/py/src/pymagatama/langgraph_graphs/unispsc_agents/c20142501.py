from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class DrillState(TypedDict): messages: Annotated[list, add_messages]; spec_validated: bool; material_compliant: bool; def validate_drill_spec(state: DrillState): return {'spec_validated': True}; def check_material_safety(state: DrillState): return {'material_compliant': True}; graph = StateGraph(DrillState); graph.add_node('validate', validate_drill_spec); graph.add_node('material_check', check_material_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()