from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; valid: bool; material_grade: str; def validate_specs(state: ProcState): state['valid'] = state['material_grade'] in ['ASTM B69', 'Zamak 3']; return state; def route_procurement(state: ProcState): return 'process' if state['valid'] else END; graph = StateGraph(ProcState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()