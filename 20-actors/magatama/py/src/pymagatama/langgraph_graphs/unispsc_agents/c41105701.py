from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NucleicState(TypedDict): assay_data: str; validation_status: bool; requirements: List[str]; def validate_membrane(state: NucleicState): state['validation_status'] = 'immobilization' in state['assay_data'].lower(); return state; def check_storage(state: NucleicState): state['requirements'] = ['-20C storage', 'sterile handling']; return state; graph = StateGraph(NucleicState); graph.add_node('validate', validate_membrane); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); compiled_graph = graph.compile()