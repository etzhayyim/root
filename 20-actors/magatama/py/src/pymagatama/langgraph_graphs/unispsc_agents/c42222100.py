from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IvSystemState(TypedDict): device_specs: dict; compliance_check: bool; output: str; def validate_specs(state: IvSystemState): state['compliance_check'] = state['device_specs'].get('load_capacity', 0) > 50; return state; def finalize_order(state: IvSystemState): state['output'] = 'Validated' if state['compliance_check'] else 'Rejected'; return state; graph = StateGraph(IvSystemState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()