from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolSpecState(TypedDict): tool_code: str; specs: dict; valid: bool; def validate_specs(state: ToolSpecState): state['valid'] = all(key in state['specs'] for key in ['size', 'material']); return state; graph = StateGraph(ToolSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()