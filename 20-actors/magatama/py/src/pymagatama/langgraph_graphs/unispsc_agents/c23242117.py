from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): model_id: str; spec_check: bool; export_compliance: bool; def val_spec(state: ProcState): return {'spec_check': True} if state['model_id'] else {'spec_check': False}; def val_dual_use(state: ProcState): return {'export_compliance': True}; graph = StateGraph(ProcState); graph.add_node('spec_validation', val_spec); graph.add_node('export_control', val_dual_use); graph.add_edge('spec_validation', 'export_control'); graph.add_edge('export_control', END); graph.set_entry_point('spec_validation'); graph = graph.compile()