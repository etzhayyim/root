from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialysisState(TypedDict): unit_id: str; compliance_docs: List[str]; inspection_status: str; def validate_unit(state: DialysisState): return {'inspection_status': 'verified' if 'iso13485' in state['compliance_docs'] else 'failed'}; workflow = StateGraph(DialysisState); workflow.add_node('validate', validate_unit); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()