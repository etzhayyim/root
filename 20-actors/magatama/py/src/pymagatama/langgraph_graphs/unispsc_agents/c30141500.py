from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class InsulationState(TypedDict): specs: dict; valid: bool; def validate_thermal_specs(state: InsulationState): state['valid'] = state['specs'].get('thermal_conductivity_watt_per_mk', 0) < 0.05; return state; graph = StateGraph(InsulationState); graph.add_node('validate', validate_thermal_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()