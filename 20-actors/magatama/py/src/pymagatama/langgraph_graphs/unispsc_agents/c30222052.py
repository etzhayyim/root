from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_report: dict; status: str; def validate_specs(state: State): specs = state['input_data'].get('specs', {}); is_compliant = all(k in specs for k in ['slip_resistance_rating', 'material_composition']); return {'validation_report': {'compliant': is_compliant}, 'status': 'validated' if is_compliant else 'rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()