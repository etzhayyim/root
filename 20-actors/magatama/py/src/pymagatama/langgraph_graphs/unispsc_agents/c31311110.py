from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipeState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_titanium_spec(state: PipeState): grade = state['specs'].get('grade'); state['valid'] = grade == 'Grade 2' or grade == 'Grade 5'; return state; graph = StateGraph(PipeState); graph.add_node('validate', validate_titanium_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()