from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): pipe_specs: dict; validation_log: list; valid: bool; def validate_spec(state: PipeState): specs = state.get('pipe_specs', {}); is_valid = 'pressure_rating' in specs and 'material_grade' in specs; return {'valid': is_valid, 'validation_log': ['Specs checked']}; graph = StateGraph(PipeState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()