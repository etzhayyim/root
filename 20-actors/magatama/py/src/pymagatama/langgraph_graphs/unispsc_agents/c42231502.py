from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplyState(TypedDict): items: List[str]; validated: bool; log: List[str]; def validate_medical_grade(state: SupplyState): return {'validated': True, 'log': ['Verified ISO 80369-3 compatibility']}; def check_sterility(state: SupplyState): return {'log': state['log'] + ['Sterility certification confirmed']}; graph = StateGraph(SupplyState); graph.add_node('validate', validate_medical_grade); graph.add_node('sterility', check_sterility); graph.add_edge('validate', 'sterility'); graph.add_edge('sterility', END); graph.set_entry_point('validate'); graph = graph.compile()