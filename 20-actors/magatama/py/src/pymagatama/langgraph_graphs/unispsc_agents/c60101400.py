from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AwardState(TypedDict): items: List[str]; validated: bool; state: str; def validate_awards(state: AwardState): print('Validating material specifications and engraving accuracy.'); return {'validated': True, 'state': 'validated'}; def finalize_order(state: AwardState): print('Generating procurement order.'); return {'state': 'completed'}; graph = StateGraph(AwardState); graph.add_node('validate', validate_awards); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()