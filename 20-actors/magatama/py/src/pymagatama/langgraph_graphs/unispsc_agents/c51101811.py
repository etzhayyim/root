from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_data: dict; compliance_status: bool; def validate_purity(state: PharmState): state['compliance_status'] = state['chemical_data'].get('purity', 0) >= 99.0; return state; graph = StateGraph(PharmState); graph.add_node('validate_purity', validate_purity); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', END); graph = graph.compile()