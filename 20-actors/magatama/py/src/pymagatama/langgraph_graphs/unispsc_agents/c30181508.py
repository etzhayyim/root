from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PartitionState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: PartitionState): state['validation_results'].append('Material cert verified'); return state; def check_dimensions(state: PartitionState): state['validation_results'].append('Dimensional tolerance checked'); return state; graph = StateGraph(PartitionState); graph.add_node('material_check', validate_materials); graph.add_node('dim_check', check_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dim_check'); graph.add_edge('dim_check', END); graph = graph.compile()