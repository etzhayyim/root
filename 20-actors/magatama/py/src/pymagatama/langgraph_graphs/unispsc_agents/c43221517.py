from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input_data: dict; validation_report: list; status: str; def validate_specs(state: ProcState): validated = state['input_data'].get('specs') is not None; return {'validation_report': ['Specs provided'] if validated else ['Missing specs'], 'status': 'VALID' if validated else 'REJECTED'}; def process_integration(state: ProcState): return {'status': 'INTEGRATION_READY'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('integrate', process_integration); graph.add_edge('validate', 'integrate'); graph.add_edge('integrate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()