from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TransformerState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: TransformerState): state['compliance'] = 'K-factor' in state['specs'] and state['specs']['K-factor'] >= 4; return state; def final_decision(state: TransformerState): state['approved'] = state['compliance']; return state; graph = StateGraph(TransformerState); graph.add_node('validate', validate_specs); graph.add_node('decision', final_decision); graph.set_entry_point('validate'); graph.add_edge('validate', 'decision'); graph.add_edge('decision', END); app = graph.compile()