from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_specs: dict; inspection_results: list[str]; def validate_specs(state: ToolState): results = []; if 'material_composition' not in state['tool_specs']: results.append('Missing material spec'); return {'inspection_results': results}; def approve_procurement(state: ToolState): return {'inspection_results': state['inspection_results'] + ['Approved']}; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()