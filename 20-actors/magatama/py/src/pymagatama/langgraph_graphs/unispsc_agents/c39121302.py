from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class EnclosureState(TypedDict): spec: dict; validation_results: list[str]; graph = StateGraph(EnclosureState); def validate_specs(state: EnclosureState): results = []; ip_rating = state['spec'].get('ip_rating', 0); if ip_rating < 54: results.append('Insufficient IP rating for industrial environment'); return {'validation_results': results}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()