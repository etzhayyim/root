from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabelState(TypedDict): specs: dict; validation_passed: bool; def check_dimensions(state: LabelState): return {'validation_passed': 'length' in state['specs']}; def check_material(state: LabelState): return {'validation_passed': state['specs'].get('material') == 'synthetic'}; graph = StateGraph(LabelState); graph.add_node('check_dims', check_dimensions); graph.add_node('check_mat', check_material); graph.set_entry_point('check_dims'); graph.add_edge('check_dims', 'check_mat'); graph.add_edge('check_mat', END); graph = graph.compile()