from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipeState(TypedDict): material: str; weld_specs: dict; inspection_status: str; graph = StateGraph(PipeState); def validate_material(state: PipeState): state['inspection_status'] = 'verified' if state['material'] == 'Hastelloy X' else 'rejected'; return state; def check_welds(state: PipeState): return {'inspection_status': 'certified' if state['weld_specs'].get('uv_tested') else 'failed'}; graph.add_node('validate', validate_material); graph.add_node('weld_check', check_welds); graph.set_entry_point('validate'); graph.add_edge('validate', 'weld_check'); graph.add_edge('weld_check', END); graph = graph.compile()