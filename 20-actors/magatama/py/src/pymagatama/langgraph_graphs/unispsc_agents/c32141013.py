from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TriodeState(TypedDict): tube_type: str; specs: dict; verified: bool; def validate_specs(state: TriodeState): print('Validating electrical specs...'); state['verified'] = True; return state; def route_to_export(state: TriodeState): return 'export_control' if state['tube_type'] == 'military_grade' else 'procurement_end'; graph = StateGraph(TriodeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()