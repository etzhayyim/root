from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CalligraphyState(TypedDict): ink_specs: dict; validation_report: List[str]; def validate_viscosity(state: CalligraphyState): v = state['ink_specs'].get('viscosity'); state['validation_report'].append('Validating viscosity' if v else 'Missing viscosity'); return state; def check_archival(state: CalligraphyState): state['validation_report'].append('Checking archival certification'); return state; graph = StateGraph(CalligraphyState); graph.add_node('viscosity_check', validate_viscosity); graph.add_node('archival_check', check_archival); graph.set_entry_point('viscosity_check'); graph.add_edge('viscosity_check', 'archival_check'); graph.add_edge('archival_check', END); graph = graph.compile()