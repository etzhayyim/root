from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BPGraphState(TypedDict): device_id: str; compliance_docs: List[str]; inspection_status: str; def validate_cert(state: BPGraphState): state['inspection_status'] = 'Pending' if 'ISO13485' in state['compliance_docs'] else 'Rejected'; return state; def final_report(state: BPGraphState): state['inspection_status'] = 'Verified'; return state; graph = StateGraph(BPGraphState); graph.add_node('validate', validate_cert); graph.add_node('finalize', final_report); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()