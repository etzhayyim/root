from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_assembly(state: AssemblyState): state['valid'] = 'Weld Inspection Certificate' in state['specs'] and state['specs'].get('Material Grade') == 'Carbon Steel'; return state; graph = StateGraph(AssemblyState); graph.add_node('validator', validate_assembly); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()