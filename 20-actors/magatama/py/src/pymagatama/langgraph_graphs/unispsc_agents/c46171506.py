from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SafeProcurementState(TypedDict): specs: dict; validation_status: bool; def validate_specs(state: SafeProcurementState): status = "fire_rating" in state['specs'] and "lock_type" in state['specs']; return {"validation_status": status}; def finalize_procurement(state: SafeProcurementState): return {"validation_status": True}; graph = StateGraph(SafeProcurementState); graph.add_node("validate", validate_specs); graph.add_node("finalize", finalize_procurement); graph.set_entry_point("validate"); graph.add_edge("validate", "finalize"); graph.add_edge("finalize", END); graph = graph.compile()