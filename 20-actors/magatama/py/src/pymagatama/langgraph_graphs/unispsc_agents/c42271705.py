from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class OxygenState(TypedDict): connector_type: str; specs: dict; compliance: bool; graph = StateGraph(OxygenState); def validate_specs(state: OxygenState): return {'compliance': 'ISO-10993' in state['specs'].get('certifications', [])}; def finalize_procurement(state: OxygenState): return {'compliance': True}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()