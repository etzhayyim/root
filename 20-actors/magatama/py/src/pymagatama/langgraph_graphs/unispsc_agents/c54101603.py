from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RingProcurementState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: RingProcurementState): results = []; if 'material' not in state['specs']: results.append('Missing material'); return {'validation_results': results}; def update_workflow(state: RingProcurementState): return {'validation_results': state['validation_results'] + ['Workflow finalized']}; graph = StateGraph(RingProcurementState); graph.add_node('validate', validate_specs); graph.add_node('finalize', update_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()