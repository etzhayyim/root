from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ResistanceState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(ResistanceState); def validate_calibration(state: ResistanceState): state['validation_results'].append('Verifying ISO17025 certificate...'); return state; def check_accuracy(state: ResistanceState): state['validation_results'].append('Confirming accuracy vs range requirements.'); return state; graph.add_node('validate', validate_calibration); graph.add_node('check_range', check_accuracy); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_range'); graph.add_edge('check_range', END); compiled_graph = graph.compile()