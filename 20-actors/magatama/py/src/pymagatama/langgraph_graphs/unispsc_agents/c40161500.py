from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): data: dict; validation_errors: Annotated[list, operator.add]; def validate_specs(state: FilterState): errors = []; spec = state['data']; if spec.get('pressure_rating_bar', 0) <= 0: errors.append('Pressure rating invalid'); return {'validation_errors': errors}; def check_compliance(state: FilterState): return {'validation_errors': ['ISO compliance pending'] if state.get('data', {}).get('iso') is None else []}; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()