from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): data: dict; valid: bool; def validate_pharma(state: State): state['valid'] = 'batch_no' in state['data'] and 'gmp_cert' in state['data']; return state; graph = StateGraph(State); graph.add_node('validate', validate_pharma); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()