from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OilProcurementState(TypedDict): data: dict; steps: List[str]; def validate_quality(state: OilProcurementState): print('Validating fat content and peroxide levels...'); return {'steps': state['steps'] + ['quality_checked']}; def check_compliance(state: OilProcurementState): print('Checking HACCP and origin documentation...'); return {'steps': state['steps'] + ['compliance_verified']}; graph = StateGraph(OilProcurementState); graph.add_node('quality', validate_quality); graph.add_node('compliance', check_compliance); graph.set_entry_point('quality'); graph.add_edge('quality', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()