from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SmockState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SmockState): errors = []; if 'material' not in state['specs']: errors.append('Missing material'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(SmockState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()