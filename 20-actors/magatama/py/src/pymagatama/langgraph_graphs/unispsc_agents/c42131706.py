from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[dict]; validation_passed: bool; def validate_specs(state: ProcureState): state['validation_passed'] = all('sterility_assurance_level' in item for item in state['items']); return state; def route_procurement(state: ProcureState): return 'end' if state['validation_passed'] else 'retry'; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END);