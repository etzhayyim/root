from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DrinkwareState(TypedDict): items: list[str]; validation_results: Annotated[list[str], operator.add]; def validate_material(state: DrinkwareState): return {'validation_results': ['Material check passed']}; def validate_safety(state: DrinkwareState): return {'validation_results': ['Food safety standards met']}; graph = StateGraph(DrinkwareState); graph.add_node('material_check', validate_material); graph.add_node('safety_check', validate_safety); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()