from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ConveyorState(TypedDict): component_id: str; specifications: dict; validated: bool; def validate_specs(state: ConveyorState): state['validated'] = all(k in state['specifications'] for k in ['material', 'dimensions']); return state; graph = StateGraph(ConveyorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()