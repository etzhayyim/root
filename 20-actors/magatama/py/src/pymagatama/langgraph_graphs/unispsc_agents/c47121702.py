from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WasteState(TypedDict): capacity: int; material: str; compliant: bool; def validate_specs(state: WasteState): return {'compliant': state['capacity'] > 0 and state['material'] != ''}; def finalize_order(state: WasteState): return {'compliant': True}; graph = StateGraph(WasteState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()