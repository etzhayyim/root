from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AudioMediaState(TypedDict): media_id: str; format_valid: bool; license_verified: bool; def validate_format(state: AudioMediaState): state['format_valid'] = True; return state; def verify_license(state: AudioMediaState): state['license_verified'] = True; return state; graph = StateGraph(AudioMediaState); graph.add_node('validate', validate_format); graph.add_node('license', verify_license); graph.set_entry_point('validate'); graph.add_edge('validate', 'license'); graph.add_edge('license', END); graph = graph.compile()