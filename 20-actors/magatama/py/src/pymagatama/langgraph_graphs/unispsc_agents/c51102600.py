from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine_name: str; dosage: str; compliance_check: bool; workflow = StateGraph(PharmState); def validate_compliance(state: PharmState): return {'compliance_check': True if 'antibiotic' in state['medicine_name'].lower() else False}; def verify_storage(state: PharmState): print('Checking cold chain requirements...'); return state; workflow.add_node('compliance', validate_compliance); workflow.add_node('storage', verify_storage); workflow.set_entry_point('compliance'); workflow.add_edge('compliance', 'storage'); workflow.add_edge('storage', END); graph = workflow.compile()