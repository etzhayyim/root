from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgeState(TypedDict): specs: dict; validation_passed: bool; data: str; def validate_forging(state: ForgeState): print('Validating material grade and hardness...'); state['validation_passed'] = 'Alloy' in state['specs'] and 'Hardness' in state['specs']; return state; def check_quality_docs(state: ForgeState): print('Checking NDT and Mill Certs...'); return state; graph = StateGraph(ForgeState); graph.add_node('validate', validate_forging); graph.add_node('quality_check', check_quality_docs); graph.set_entry_point('validate'); graph.add_edge('validate', 'quality_check'); graph.add_edge('quality_check', END); graph = graph.compile()