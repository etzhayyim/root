from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CeilingPanState(TypedDict): input_spec: dict; validation_report: list[str]; status: str; def validate_dimensions(state: CeilingPanState): state['validation_report'].append('Checking dimensions'); return {'status': 'validated'}; def check_compliance(state: CeilingPanState): state['validation_report'].append('Checking UL compliance'); return {'status': 'compliant'}; graph = StateGraph(CeilingPanState); graph.add_node('dimension_check', validate_dimensions); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('dimension_check'); graph.add_edge('dimension_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()