from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MountGraphState(TypedDict): hardware_specs: dict; approval_status: bool; def validate_specs(state: MountGraphState): return {'approval_status': state['hardware_specs'].get('load_capacity_kg', 0) > 0}; graph = StateGraph(MountGraphState); graph.add_node('validate_specs', validate_specs); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', END); compiled_graph = graph.compile()