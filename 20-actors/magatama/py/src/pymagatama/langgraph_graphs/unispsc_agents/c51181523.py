from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine_name: str; validation_status: bool; compliance_docs: List[str]; def validate_pharmaceutical(state: PharmState): state['validation_status'] = True; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_pharmaceutical); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()