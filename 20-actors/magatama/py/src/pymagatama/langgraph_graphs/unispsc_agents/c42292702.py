from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec: dict; verified: bool; def validate_instrument(state: State): status = 'verified' if state['spec'].get('iso_13485') else 'rejected'; return {'status': status, 'verified': status == 'verified'}; graph = StateGraph(State); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()