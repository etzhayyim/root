from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BilliardChalkState(TypedDict): chalk_details: dict; validation_results: List[str]; graph = StateGraph(BilliardChalkState); def validate_purity(state: BilliardChalkState): return {'validation_results': ['Purity check passed']}; def check_supply_chain(state: BilliardChalkState): return {'validation_results': ['Supply chain verified']}; graph.add_node('validate', validate_purity); graph.add_node('supply_check', check_supply_chain); graph.set_entry_point('validate'); graph.add_edge('validate', 'supply_check'); graph.add_edge('supply_check', END); graph.compile()