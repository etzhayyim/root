from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validated: bool; results: List[str]; def validate_medical_device(state: State): return {'validated': all('ISO13485' in item for item in state['items'])}; def compile_system(): graph = StateGraph(State); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_system()