from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftSystemState(TypedDict): system_id: str; compliance_docs: List[str]; validated: bool; def validate_specs(state: AircraftSystemState): state['validated'] = all(doc in state['compliance_docs'] for doc in ['AS9100', 'DO-160']); return state; graph = StateGraph(AircraftSystemState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()