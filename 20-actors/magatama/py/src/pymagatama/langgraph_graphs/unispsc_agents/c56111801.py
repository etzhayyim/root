from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): component_id: str; specs: dict; validation_passed: bool; def validate_specs(state: ProcurementState): state['validation_passed'] = all(k in state['specs'] for k in ['voltage', 'certification', 'material']); return state; def route_procurement(state: ProcurementState): return 'validate' if state['component_id'] else END; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()