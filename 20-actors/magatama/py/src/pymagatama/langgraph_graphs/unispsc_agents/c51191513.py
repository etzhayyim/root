from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; safety_checks: List[str]; approved: bool; def validate_purity(state: DrugState): state['safety_checks'].append('Purity > 99.5% verified'); return state; def check_regulations(state: DrugState): state['approved'] = True; return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()