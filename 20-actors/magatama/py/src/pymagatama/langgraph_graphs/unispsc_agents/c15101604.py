from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): code: str; specs: dict; validation: bool; def validate_specs(state: ProcureState): return {'validation': all(key in state['specs'] for key in ['material_grade', 'tensile_strength_mpa'])}; def compile_procurement_graph(): builder = StateGraph(ProcureState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); return builder.compile(); graph = compile_procurement_graph()