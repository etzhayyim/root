from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SterilizationState(TypedDict): indicators: List[str]; validated: bool; certs: List[str]; def validate_indicators(state: SterilizationState): state['validated'] = all(c in state.get('certs', []) for c in ['ISO11140', 'ISO11138']); return state; def check_compliance(state: SterilizationState): return 'validated' if state['validated'] else 'rejected'; graph = StateGraph(SterilizationState); graph.add_node('validate', validate_indicators); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()