from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): device_id: str; specs: dict; compliance_checked: bool; def validate_specs(state: SurgicalState) -> dict: return {'compliance_checked': 'ISO 13485' in state['specs'].get('certs', [])}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()