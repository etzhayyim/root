from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class QuiltingState(TypedDict): content_type: str; is_verified: bool; steps: List[str]; def validate_materials(state: QuiltingState): return {'is_verified': True if state['content_type'] in ['book', 'video', 'pattern'] else False}; def compile_curriculum(state: QuiltingState): return {'steps': ['Pattern Audit', 'Technical Review', 'Safety Assessment']}; graph = StateGraph(QuiltingState); graph.add_node('validate', validate_materials); graph.add_node('compile', compile_curriculum); graph.set_entry_point('validate'); graph.add_edge('validate', 'compile'); graph.add_edge('compile', END); app = graph.compile()