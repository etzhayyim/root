from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DoorCloserState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: DoorCloserState): state['compliance'] = 'Fire rating' in state['specs'] and state['specs']['weight'] > 0; return state; def check_approval(state: DoorCloserState): state['approved'] = state['compliance']; return state; graph = StateGraph(DoorCloserState); graph.add_node('validate', validate_specs); graph.add_node('approval', check_approval); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph.set_entry_point('validate'); graph = graph.compile()