from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_specs: dict; status: str; def validate_specs(state: ToolState): print('Validating wire gauge and insulation'); return {'status': 'validated' if state['tool_specs']['gauge'] > 0 else 'rejected'}; def finalize_procurement(state: ToolState): print('Preparing purchase order'); return {'status': 'complete'}; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()