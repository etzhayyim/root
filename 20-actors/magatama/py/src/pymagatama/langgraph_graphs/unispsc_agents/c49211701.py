from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BowlingState(TypedDict): equipment_type: str; specs_verified: bool; def validate_specs(state: BowlingState) -> BowlingState: state['specs_verified'] = 'pinsetter' in state['equipment_type'].lower(); return state; def route_procurement(state: BowlingState) -> str: return 'processed' if state['specs_verified'] else 'review'; graph = StateGraph(BowlingState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()