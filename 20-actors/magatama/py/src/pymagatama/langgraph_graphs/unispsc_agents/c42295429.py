from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SuctionSpec(TypedDict): canister_id: str; volume: float; compliant: bool; status: str; def validate_spec(state: SuctionSpec): state['compliant'] = state['volume'] > 0; return {'status': 'validated' if state['compliant'] else 'failed'}; graph = StateGraph(SuctionSpec); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()