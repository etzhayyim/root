from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LeadProcessState(TypedDict): material_spec: dict; compliance_check: bool; export_control_flag: bool; def validate_quality(state: LeadProcessState): state['compliance_check'] = state['material_spec'].get('purity', 0) >= 99.9; return state; def hazard_screening(state: LeadProcessState): state['export_control_flag'] = state['material_spec'].get('weight', 0) > 500; return state; graph = StateGraph(LeadProcessState); graph.add_node('validate', validate_quality); graph.add_node('screen', hazard_screening); graph.set_entry_point('validate'); graph.add_edge('validate', 'screen'); graph.add_edge('screen', END); graph = graph.compile()