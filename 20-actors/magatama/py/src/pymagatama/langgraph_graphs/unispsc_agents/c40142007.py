from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HoseSpecState(TypedDict): specs: dict; validation_errors: List[str]; def validate_hose_specs(state: HoseSpecState): errors = []; if state['specs'].get('pressure_rating', 0) < 0: errors.append('Negative pressure rating'); return {'validation_errors': errors}; graph = StateGraph(HoseSpecState); graph.add_node('validate', validate_hose_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()