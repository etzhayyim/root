from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): specs: dict; valid: bool; def validate_tape_specs(state: ProcureState): state['valid'] = 'Adhesion strength' in state['specs'] and state['specs'].get('Temperature resistance range') is not None; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_tape_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()