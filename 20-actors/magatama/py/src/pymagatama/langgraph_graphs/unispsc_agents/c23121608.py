from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: RobotState): specs = state['specs']; errors = []; if specs.get('payload', 0) <= 0: errors.append('Invalid payload'); state['validation_errors'] = errors; state['status'] = 'validated' if not errors else 'failed'; return state; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()