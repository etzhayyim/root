from langgraph.graph import StateGraph, END; from typing import TypedDict; class CarbState(TypedDict): specs: dict; valid: bool; def validate_specs(state: CarbState): state['valid'] = 'flow_rate' in state['specs'] and state['specs']['flow_rate'] > 0; return state; graph = StateGraph(CarbState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()