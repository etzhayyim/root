from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BookProcurementState(TypedDict): content: str; validated: bool; metadata: dict; graph = StateGraph(BookProcurementState); def validate_content(state: BookProcurementState): return {'validated': 'Bible' in state.get('content', '')}; graph.add_node('validate', validate_content); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()