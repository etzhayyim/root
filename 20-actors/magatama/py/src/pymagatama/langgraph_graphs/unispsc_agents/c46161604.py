from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LifeVestState(TypedDict): vendor_certs: List[str]; safety_compliance: bool; status: str; def validate_cert(state: LifeVestState): return {'safety_compliance': 'ISO-12402' in state['vendor_certs']}; def assess_risk(state: LifeVestState): return {'status': 'APPROVED' if state['safety_compliance'] else 'REJECTED'}; graph = StateGraph(LifeVestState); graph.add_node('validate', validate_cert); graph.add_node('assess', assess_risk); graph.add_edge('validate', 'assess'); graph.add_edge('assess', END); graph.set_entry_point('validate'); graph = graph.compile()