from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AxleRepairState(TypedDict): part_number: str; specs_verified: bool; quality_check: bool; def validate_part(state: AxleRepairState): state['specs_verified'] = True; return state; def check_quality(state: AxleRepairState): state['quality_check'] = True; return state; graph = StateGraph(AxleRepairState); graph.add_node('validate', validate_part); graph.add_node('quality', check_quality); graph.add_edge('validate', 'quality'); graph.add_edge('quality', END); graph.set_entry_point('validate'); graph = graph.compile()