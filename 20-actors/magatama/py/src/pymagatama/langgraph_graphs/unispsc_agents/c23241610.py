from langgraph.graph import StateGraph, END; from typing import TypedDict; class LaserProcState(TypedDict): specs: dict; ready: bool; def validate_power(state): state['ready'] = state['specs'].get('power', 0) > 1000; return state; graph = StateGraph(LaserProcState); graph.add_node('validate', validate_power); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()