from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class QualitySpec(TypedDict): device_id: str; data_points: List[float]; validated: bool; def validate_physics_data(state: QualitySpec): state['validated'] = len(state['data_points']) > 5; return state; graph = StateGraph(QualitySpec); graph.add_node('validate', validate_physics_data); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()