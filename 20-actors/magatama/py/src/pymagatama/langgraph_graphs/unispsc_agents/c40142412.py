from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlangeState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: FlangeState): specs = state['specs']; is_valid = all(key in specs for key in ['grade', 'class', 'size']); return {'valid': is_valid, 'logs': ['Spec check passed' if is_valid else 'Spec check failed']}; def approve_flange(state: FlangeState): return {'logs': state['logs'] + ['Flange procurement approved']}; graph = StateGraph(FlangeState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_flange); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()