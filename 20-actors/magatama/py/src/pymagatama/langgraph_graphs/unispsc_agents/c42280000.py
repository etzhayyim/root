from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SterilizationState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_certs(state: SterilizationState): state['validated'] = all(c in state['certs'] for c in ['ISO_13485', 'CE_MDR']); return state; def check_compliance(state: SterilizationState): return 'validated' if state['validated'] else 'failed'; graph = StateGraph(SterilizationState); graph.add_node('validator', validate_certs); graph.add_edge('validator', END); graph.set_entry_point('validator'); graph = graph.compile()