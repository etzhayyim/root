from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PrintwheelState(TypedDict): model: str; compatibility_check: bool; validation_log: List[str]; def check_compatibility(state: PrintwheelState): state['compatibility_check'] = True; state['validation_log'].append('Model verified'); return state; def validate_specs(state: PrintwheelState): state['validation_log'].append('Specs validated'); return state; graph = StateGraph(PrintwheelState); graph.add_node('check_comp', check_compatibility); graph.add_node('val_specs', validate_specs); graph.set_entry_point('check_comp'); graph.add_edge('check_comp', 'val_specs'); graph.add_edge('val_specs', END); compiled_graph = graph.compile()