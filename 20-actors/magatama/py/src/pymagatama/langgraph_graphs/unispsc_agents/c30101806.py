from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; valid: bool; def validate_aluminum(state: ProcureState): state['valid'] = all(k in state['specs'] for k in ['alloy', 'dimensions']); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_aluminum); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()