from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicroAnalyzerState(TypedDict): vendor_docs: List[str]; inspection_results: dict; compliance_status: bool; def validate_spec(state: MicroAnalyzerState): state['compliance_status'] = 'ISO_13485' in state['vendor_docs']; return state; def check_calibration(state: MicroAnalyzerState): state['inspection_results'] = {'calibrated': True}; return state; graph = StateGraph(MicroAnalyzerState); graph.add_node('validate', validate_spec); graph.add_node('calibrate', check_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); compiled_graph = graph.compile()