from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_puzzle(state: ProcureState): return {'validated': all('EN71' in cert for cert in state['certs'])}; def process_order(state: ProcureState): return {'validated': True}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_puzzle); graph.add_node('process', process_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()