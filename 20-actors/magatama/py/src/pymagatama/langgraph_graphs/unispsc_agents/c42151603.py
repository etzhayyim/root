from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DentalToolState(TypedDict): tool_specs: dict; validation_results: list; graph = StateGraph(DentalToolState); def validate_material(state): return {'validation_results': ['Material check passed']}; def perform_quality_check(state): return {'validation_results': ['Quality inspection approved']}; graph.add_node('validate', validate_material); graph.add_node('inspect', perform_quality_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); compiled_graph = graph.compile()