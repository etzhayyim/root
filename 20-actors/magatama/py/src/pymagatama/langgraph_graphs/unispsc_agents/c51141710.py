from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; validation_status: bool; def check_compliance(state: PharmaState): print(f'Validating {state["chemical_name"]} for pharmaceutical standards.'); return {'validation_status': True}; graph = StateGraph(PharmaState); graph.add_node('compliance', check_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', END); 