from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProjectorState(TypedDict): model_specs: dict; compliance_check: bool; graph = StateGraph(ProjectorState); def validate_specs(state: ProjectorState): return {'compliance_check': state['model_specs'].get('brightness', 0) > 2000}; def check_warranty(state: ProjectorState): return {'compliance_check': state['model_specs'].get('warranty_years', 0) >= 3}; graph.add_node('validate', validate_specs); graph.add_node('warranty', check_warranty); graph.add_edge('validate', 'warranty'); graph.add_edge('warranty', END); graph.set_entry_point('validate'); graph = graph.compile()