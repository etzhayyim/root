from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): medicine: str; certs: list; compliance_check: bool; def validate_integrity(state: PharmaState): print(f'Validating storage specs for {state['medicine']}'); return {'compliance_check': True}; def check_certs(state: PharmaState): return {'compliance_check': 'GMP' in state['certs']}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_integrity); graph.add_node('certify', check_certs); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); compiled_graph = graph.compile()