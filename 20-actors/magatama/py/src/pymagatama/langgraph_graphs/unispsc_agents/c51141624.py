from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): assay_report: str; compliance_check: bool; def validate_purity(state: PharmaState): return {'compliance_check': True} if '99%' in state['assay_report'] else {'compliance_check': False}; def final_review(state: PharmaState): return {'compliance_check': state['compliance_check']}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('review', final_review); graph.set_entry_point('validate'); graph.add_edge('validate', 'review'); graph.add_edge('review', END); compiled_graph = graph.compile()