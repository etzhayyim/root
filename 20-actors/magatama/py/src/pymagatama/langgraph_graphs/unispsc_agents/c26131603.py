from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TrashRackState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: TrashRackState): specs = state['specs']; errors = []; if specs.get('bar_spacing', 0) <= 0: errors.append('Invalid bar spacing'); state['valid'] = len(errors) == 0; state['error_log'] = errors; return state; graph = StateGraph(TrashRackState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()