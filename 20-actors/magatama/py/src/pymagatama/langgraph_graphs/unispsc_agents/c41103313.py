from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DepthSensorState(TypedDict): meter_id: str; range_spec: float; pressure_rating: float; validation_status: bool; def validate_specs(state: DepthSensorState): if state['range_spec'] > 0 and state['pressure_rating'] > 0: state['validation_status'] = True; return state; def finalize_order(state: DepthSensorState): return {'status': 'approved'} if state['validation_status'] else {'status': 'rejected'}; graph = StateGraph(DepthSensorState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()