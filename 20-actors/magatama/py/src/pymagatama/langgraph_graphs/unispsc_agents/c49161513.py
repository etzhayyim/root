from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HockeyStickState(TypedDict): stick_specs: dict; compliance_status: bool; def validate_fih(state: HockeyStickState): state['compliance_status'] = state['stick_specs'].get('fih_certified', False); return state; def route_procurement(state: HockeyStickState): return 'approval' if state['compliance_status'] else 'reject'; graph = StateGraph(HockeyStickState); graph.add_node('validate', validate_fih); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()