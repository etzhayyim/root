from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): component_id: str; specs: dict; valid: bool; def val_step(state: ProcState): state['valid'] = len(state['specs']) > 3; return state; graph = StateGraph(ProcState); graph.add_node('val_step', val_step); graph.set_entry_point('val_step'); graph.add_edge('val_step', END); graph = graph.compile()