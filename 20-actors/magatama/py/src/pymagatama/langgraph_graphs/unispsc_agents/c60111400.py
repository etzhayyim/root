from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DecorState(TypedDict): items: List[str]; validated_specs: dict; status: str; def validate_decor_specs(state: DecorState): return {'status': 'validated'} if state.get('items') else {'status': 'rejected'}; def finalize_procurement(state: DecorState): return {'status': 'ready_for_purchase'}; graph = StateGraph(DecorState); graph.add_node('validate', validate_decor_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()