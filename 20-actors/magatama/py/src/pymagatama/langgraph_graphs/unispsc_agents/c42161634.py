from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialysisState(TypedDict): items: List[str]; qc_passed: bool; def validate_certifications(state: DialysisState): state['qc_passed'] = all(['ISO13485' in item or 'FDA' in item for item in state['items']]); return state; def route_final(state: DialysisState): return 'end' if state['qc_passed'] else 'flag_error'; graph = StateGraph(DialysisState); graph.add_node('validate', validate_certifications); graph.add_conditional_edges('validate', route_final, {'end': END, 'flag_error': END}); graph.set_entry_point('validate')