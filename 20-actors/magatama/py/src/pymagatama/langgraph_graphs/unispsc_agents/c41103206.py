from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabDetergentState(TypedDict): detergent_type: str; is_biodegradable: bool; safety_compliance: bool; def validate_specs(state: LabDetergentState): return {'safety_compliance': True} if state['detergent_type'] else {'safety_compliance': False}; graph = StateGraph(LabDetergentState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()