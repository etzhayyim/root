from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; def val_step(state: ProcureState): return {'validated': all(item for item in state['items'])}; graph = StateGraph(ProcureState); graph.add_node('validate', val_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()