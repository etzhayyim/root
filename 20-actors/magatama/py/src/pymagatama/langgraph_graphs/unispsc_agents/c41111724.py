from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SPMState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_spec(state: SPMState): print('Validating SPM technical specs...'); state['validation_results'].append('specs_verified'); return {'status': 'validated'}; def run_calibration_workflow(state: SPMState): print('Generating calibration procedures...'); return {'status': 'ready_for_deployment'}; graph = StateGraph(SPMState); graph.add_node('validate', validate_spec); graph.add_node('calibrate', run_calibration_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); app = graph.compile()