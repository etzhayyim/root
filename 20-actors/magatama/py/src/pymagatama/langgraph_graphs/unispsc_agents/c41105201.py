from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class StainerState(TypedDict): vendor_certs: List[str]; safety_compliance: bool; status: str; def validate_compliance(state: StainerState): return {'safety_compliance': len(state['vendor_certs']) >= 3}; def route_processing(state: StainerState): return 'complete' if state['safety_compliance'] else END; graph = StateGraph(StainerState); graph.add_node('validate', validate_compliance); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()