from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): cold_chain_compliant: bool; gmp_certified: bool; approved: bool; def check_thermal_history(state: PharmState): return {'cold_chain_compliant': True}; def verify_compliance(state: PharmState): return {'gmp_certified': True, 'approved': True}; graph = StateGraph(PharmState); graph.add_node('thermal_check', check_thermal_history); graph.add_node('regulatory_check', verify_compliance); graph.set_entry_point('thermal_check'); graph.add_edge('thermal_check', 'regulatory_check'); graph.add_edge('regulatory_check', END); graph.compile()