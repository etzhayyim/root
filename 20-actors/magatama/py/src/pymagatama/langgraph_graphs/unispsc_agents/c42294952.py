from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EndoState(TypedDict): device_id: str; specs: dict; validation_report: List[str]; def validate_specs(state: EndoState): state['validation_report'].append('Validating sterilization grade'); return state; def check_compatibility(state: EndoState): state['validation_report'].append('Checking endoscope channel fit'); return state; graph = StateGraph(EndoState); graph.add_node('validate', validate_specs); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()