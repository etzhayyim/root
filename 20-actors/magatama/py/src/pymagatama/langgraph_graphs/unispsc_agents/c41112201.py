from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CalorimeterState(TypedDict): specs: dict; validation_report: List[str]; def validate_specs(state: CalorimeterState): validation = []; specs = state.get('specs', {}); if 'range' not in specs: validation.append('Missing thermal range'); return {'validation_report': validation}; graph = StateGraph(CalorimeterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()