from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FilterState(TypedDict): specs: List[str]; validated: bool; error: str; def validate_specs(state: FilterState): return {'validated': all(f in state['specs'] for f in ['ISO 13485', 'Sterilization'])}; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()