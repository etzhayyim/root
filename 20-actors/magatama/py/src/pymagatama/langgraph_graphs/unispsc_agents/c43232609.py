from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LibraryState(TypedDict): vendor: str; requirements: List[str]; validated: bool; def validate_specs(state: LibraryState): state['validated'] = 'MARC21' in state['requirements']; return state; builder = StateGraph(LibraryState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()