from langgraph.graph import StateGraph, END; from typing import TypedDict; class WeldingState(TypedDict): rod_type: str; specs: dict; valid: bool; def validate_specs(state: WeldingState): state['valid'] = 'tensile_strength' in state['specs'] and 'material' in state['specs']; return state; graph = StateGraph(WeldingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()