from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): dimensions: dict; validation_report: list[str]; def validate_clamp(state: State): dims = state['dimensions']; errors = []; if dims.get('capacity', 0) <= 0: errors.append('Invalid capacity'); return {'validation_report': errors}; graph = StateGraph(State); graph.add_node('validate', validate_clamp); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()