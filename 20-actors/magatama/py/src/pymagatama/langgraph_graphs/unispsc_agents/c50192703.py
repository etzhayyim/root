from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MealState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_shelf_life(state: MealState): for item in state['items']: if item.get('days_to_expiry', 0) < 365: state['validation_errors'].append('Short shelf life'); return state; graph = StateGraph(MealState); graph.add_node('validate', validate_shelf_life); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()