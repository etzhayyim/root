from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SprocketState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: SprocketState): specs = state['specs']; is_valid = 'teeth' in specs and 'material' in specs; return {'valid': is_valid, 'error': '' if is_valid else 'Missing mandatory specs'}; def compile_graph(): builder = StateGraph(SprocketState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); return builder.compile(); graph = compile_graph()