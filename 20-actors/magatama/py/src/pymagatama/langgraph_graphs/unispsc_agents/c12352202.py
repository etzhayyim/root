from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class AluminumState(TypedDict): messages: Annotated[Sequence[str], add_messages]; purity: float; hazard_verified: bool; def validate_purity(state: AluminumState): return {'purity': 99.9}; def check_hazard(state: AluminumState): return {'hazard_verified': True}; graph = StateGraph(AluminumState); graph.add_node('validate', validate_purity); graph.add_node('safety_check', check_hazard); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()