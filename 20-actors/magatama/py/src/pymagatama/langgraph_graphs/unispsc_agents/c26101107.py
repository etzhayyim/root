from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MotorState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: MotorState): specs = state['specs']; errors = []; if specs.get('efficiency') not in ['IE3', 'IE4']: errors.append('Efficiency class below standard'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(MotorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()