from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipetteTipState(TypedDict): tip_type: str; is_sterile: bool; specs: dict; validation_report: List[str]; def validate_tip(state: PipetteTipState): report = []; if not state['specs'].get('dnase_rnase_free'): report.append('Missing purity cert'); return {'validation_report': report}; graph = StateGraph(PipetteTipState); graph.add_node('validate', validate_tip); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()