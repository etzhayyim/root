from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): instrument_id: str; compliance_docs: List[str]; validated: bool; def validate_instrument(state: SurgicalState): return {'validated': all(doc in ['FDA', 'ISO13485', 'CE'] for doc in state['compliance_docs'])}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()