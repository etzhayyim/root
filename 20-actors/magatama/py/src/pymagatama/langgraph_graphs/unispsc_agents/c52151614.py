from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoolingRackState(TypedDict): dimensions: dict; material: str; compliant: bool; def check_material(state: CoolingRackState): state['compliant'] = state['material'] == 'stainless_steel'; return state; def validate_specs(state: CoolingRackState): return 'compliant' if state['compliant'] else 'fail'; graph = StateGraph(CoolingRackState); graph.add_node('check_material', check_material); graph.add_node('validate_specs', validate_specs); graph.set_entry_point('check_material'); graph.add_edge('check_material', 'validate_specs'); graph.add_edge('validate_specs', END); compiled_graph = graph.compile()