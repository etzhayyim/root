from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalProcurementState(TypedDict): chemical_id: str; purity: float; hazard_level: int; compliance_checks: Annotated[Sequence[str], operator.add]; def validate_chemical_purity(state: ChemicalProcurementState): return {'compliance_checks': ['purity_verified'] if state['purity'] >= 99.0 else ['purity_failed']}; def check_hazards(state: ChemicalProcurementState): return {'compliance_checks': ['hazard_approved'] if state['hazard_level'] < 3 else ['hazard_restricted']}; graph = StateGraph(ChemicalProcurementState); graph.add_node('validate', validate_chemical_purity); graph.add_node('safety', check_hazards); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()