from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DNAKitState(TypedDict): components: List[str]; temp_check: bool; verified: bool; def validate_components(state: DNAKitState): return {'verified': all('reagent' in c for c in state['components'])}; def check_temp(state: DNAKitState): return {'temp_check': True}; graph = StateGraph(DNAKitState); graph.add_node('validate', validate_components); graph.add_node('temp_control', check_temp); graph.set_entry_point('validate'); graph.add_edge('validate', 'temp_control'); graph.add_edge('temp_control', END); graph = graph.compile()