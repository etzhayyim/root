from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftState(TypedDict): aircraft_id: str; compliance_checks: List[str]; approved: bool; def validate_specs(state: AircraftState): state['compliance_checks'].append('ITAR_CHECK'); return {'approved': True} if 'ITAR_CHECK' in state['compliance_checks'] else {'approved': False}; def export_audit(state: AircraftState): print(f'Auditing aircraft {state['aircraft_id']}'); return {'compliance_checks': state['compliance_checks'] + ['EXPORT_AUDIT']}; graph = StateGraph(AircraftState); graph.add_node('validate', validate_specs); graph.add_node('audit', export_audit); graph.set_entry_point('validate'); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); graph = graph.compile()