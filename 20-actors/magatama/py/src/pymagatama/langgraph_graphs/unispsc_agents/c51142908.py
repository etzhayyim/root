from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_data: dict; compliance_status: bool; workflow_log: list; graph = StateGraph(PharmaState); def validate_purity(state: PharmaState): state['compliance_status'] = state['chemical_data'].get('purity', 0) >= 99.0; return state; def verify_docs(state: PharmaState): state['workflow_log'].append('Documents verified'); return state; graph.add_node('validate', validate_purity); graph.add_node('docs', verify_docs); graph.set_entry_point('validate'); graph.add_edge('validate', 'docs'); graph.add_edge('docs', END); compiled_graph = graph.compile()