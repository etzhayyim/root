from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_data: dict; validation_results: Annotated[Sequence[str], operator.add]; safety_check_passed: bool; def validate_purity(state: ChemicalState): results = []; data = state['chemical_data']; if data.get('purity', 0) >= 99.9: results.append('Purity Verified'); return {'validation_results': results, 'safety_check_passed': True}; def hazmat_review(state: ChemicalState): return {'validation_results': ['Hazmat safety review completed'], 'safety_check_passed': state.get('safety_check_passed', False)}; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_purity); graph.add_node('hazmat', hazmat_review); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()