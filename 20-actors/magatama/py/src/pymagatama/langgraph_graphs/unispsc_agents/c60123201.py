from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaperRibbonState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_ribbon(state: PaperRibbonState): errors = []; if state['specs'].get('width', 0) <= 0: errors.append('Width must be positive'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(PaperRibbonState); graph.add_node('validate', validate_ribbon); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()