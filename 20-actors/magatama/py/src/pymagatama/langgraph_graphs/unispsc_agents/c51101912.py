from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_report: str; compliance_check: bool; def validate_purity(state: PharmaState): print('Validating purity...'); return {'compliance_check': True}; def inspect_storage(state: PharmaState): print('Checking storage parameters...'); return {'compliance_check': True}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('inspect', inspect_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()