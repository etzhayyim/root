from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PatientLightingState(TypedDict): specs: dict; compliant: bool; errors: List[str]; def validate_medical_standards(state: PatientLightingState): state['compliant'] = 'IEC 60601' in state['specs'].get('certifications', []) and state['specs'].get('lux', 0) > 300; return state; graph = StateGraph(PatientLightingState); graph.add_node('validate', validate_medical_standards); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()