from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabSupplyState(TypedDict): vendor: str; specs: dict; compliant: bool; def validate_specs(state: LabSupplyState): state['compliant'] = 'absorbency' in state['specs'] and state['specs']['absorbency'] > 500; return state; graph = StateGraph(LabSupplyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()