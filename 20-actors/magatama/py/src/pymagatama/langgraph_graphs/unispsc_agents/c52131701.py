from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class ValanceState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: ValanceState): material = state['specs'].get('fabric'); return {'validation_results': ['Material check passed'] if material else ['Validation failed: Missing material']}; def validate_fire_safety(state: ValanceState): rating = state['specs'].get('flame_retardancy'); return {'validation_results': state['validation_results'] + ['Fire rating verified']}; graph = StateGraph(ValanceState); graph.add_node('material_check', validate_materials); graph.add_node('fire_check', validate_fire_safety); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'fire_check'); graph.add_edge('fire_check', END); compiled_graph = graph.compile()