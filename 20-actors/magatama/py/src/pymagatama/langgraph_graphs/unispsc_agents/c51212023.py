from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): commodity: str; specs: dict; approved: bool; def validate_quality(state: ProcurementState) -> dict: status = state['specs'].get('brix_level', 0) > 10; return {'approved': status}; def route_procurement(state: ProcurementState) -> str: return 'approved' if state['approved'] else 'rejected'; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()