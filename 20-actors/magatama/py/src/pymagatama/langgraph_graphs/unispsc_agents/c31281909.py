from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input: str; validation_results: Annotated[list, operator.add]; status: str; def validate_lead_specs(state: ProcureState): return {'validation_results': ['Chemical purity check', 'Lead safety compliance confirmed'], 'status': 'Validated'}; def generate_compliance_docs(state: ProcureState): return {'status': 'Documentation Ready'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_lead_specs); graph.add_node('docs', generate_compliance_docs); graph.add_edge('validate', 'docs'); graph.add_edge('docs', END); graph.set_entry_point('validate'); graph = graph.compile()