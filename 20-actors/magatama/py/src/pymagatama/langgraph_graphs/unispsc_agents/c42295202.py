from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalToolState(TypedDict): tool_specs: dict; validation_results: List[str]; def validate_specs(state: SurgicalToolState): return {'validation_results': ['Compliance Verified'] if state['tool_specs'].get('iso_cert') else ['Compliance Failed']}; def route_procurement(state: SurgicalToolState): return 'complete'; graph = StateGraph(SurgicalToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()