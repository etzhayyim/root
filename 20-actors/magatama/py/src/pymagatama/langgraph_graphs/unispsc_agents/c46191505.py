from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FireAlarmState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: FireAlarmState): results = []; cert = state['specs'].get('certification'); if not cert: results.append('Missing mandatory certification'); return {'validation_results': results}; def generate_report(state: FireAlarmState): return {'validation_results': state['validation_results'] + ['Compliance report generated.']}; graph = StateGraph(FireAlarmState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()