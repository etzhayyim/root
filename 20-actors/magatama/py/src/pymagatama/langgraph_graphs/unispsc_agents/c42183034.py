from langgraph.graph import StateGraph, END; from typing import TypedDict; class VisionState(TypedDict): device_id: str; calib_data: dict; approved: bool; def validate_calibration(state: VisionState): print(f'Validating stereo unit {state[device_id]}'); return {'approved': True if state['calib_data'].get('accuracy') > 0.99 else False}; graph = StateGraph(VisionState); graph.add_node('validate', validate_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()