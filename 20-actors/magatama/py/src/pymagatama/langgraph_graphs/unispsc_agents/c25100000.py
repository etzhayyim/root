from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class VehicleState(TypedDict): vin: str; compliance_passed: bool; inspection_report: str; graph = StateGraph(VehicleState); def validate_vin(state: VehicleState): return {'compliance_passed': len(state['vin']) == 17}; def report_status(state: VehicleState): return {'inspection_report': 'Passed' if state['compliance_passed'] else 'Failed'}; graph.add_node('validate', validate_vin); graph.add_node('report', report_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()