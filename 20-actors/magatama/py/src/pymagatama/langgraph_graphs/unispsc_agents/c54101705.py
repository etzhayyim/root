from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SizerState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SizerState): errors = []; if state['specs'].get('measurement_range_mm', 0) < 10: errors.append('Range too small'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(SizerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()