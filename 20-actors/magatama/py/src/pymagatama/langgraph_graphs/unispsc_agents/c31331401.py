from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; compliance: bool; validated: bool; def validate_specs(state: AssemblyState): return {'validated': 'Alloy Grade' in state['specs'] and 'Weight' in state['specs']}; def check_compliance(state: AssemblyState): return {'compliance': state['validated']}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance_check'); graph.add_edge('compliance_check', END); graph.compile()