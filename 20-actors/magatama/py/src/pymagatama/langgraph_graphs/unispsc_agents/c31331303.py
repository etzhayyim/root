from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): part_specs: dict; validation_results: List[str]; def validate_weld(state: State): state['validation_results'].append('Weld integrity pass'); return state; def check_export_controls(state: State): state['validation_results'].append('Export regulatory check complete'); return state; graph = StateGraph(State); graph.add_node('weld_validation', validate_weld); graph.add_node('export_compliance', check_export_controls); graph.set_entry_point('weld_validation'); graph.add_edge('weld_validation', 'export_compliance'); graph.add_edge('export_compliance', END); graph.compile()