from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation: bool; graph = StateGraph(ProcurementState); def validate_bronze_specs(state): state['validation'] = 'yield_strength' in state['specs'] and 'tolerance' in state['specs']; return state; def determine_risk(state): return 'standard' if state['validation'] else 'flagged'; graph.add_node('validate', validate_bronze_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()