from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ParkingState(TypedDict): site_plans: str; requirements: List[str]; inspection_status: str; def validate_specs(state: ParkingState): return {'inspection_status': 'Validated'} if state['site_plans'] else {'inspection_status': 'Failed'}; def structural_review(state: ParkingState): return {'inspection_status': 'Safety Approved'}; graph = StateGraph(ParkingState); graph.add_node('validate', validate_specs); graph.add_node('review', structural_review); graph.add_edge('validate', 'review'); graph.add_edge('review', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()