from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; compliance_check: bool; def validate_tensile_strength(state: ForgingState): state['compliance_check'] = state['specs'].get('tensile_strength', 0) > 200; return state; def check_export_control(state: ForgingState): print('Checking dual-use compliance...'); return state; workflow = StateGraph(ForgingState); workflow.add_node('validate', validate_tensile_strength); workflow.add_node('export', check_export_control); workflow.set_entry_point('validate'); workflow.add_edge('validate', 'export'); workflow.add_edge('export', END); graph = workflow.compile()