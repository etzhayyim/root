from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BenchState(TypedDict): bench_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: BenchState): errors = []; if state['bench_specs'].get('load_capacity', 0) < 150: errors.append('Load capacity below minimum safety standard.'); return {'validation_errors': errors}; def determine_approval(state: BenchState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(BenchState); graph.add_node('validate', validate_specs); graph.add_node('approve', determine_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()