from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TunnelState(TypedDict): status: str; steps: List[str]; def validate_survey(state: TunnelState): return {'status': 'surveyed', 'steps': ['survey', 'validation']}; def perform_construction(state: TunnelState): return {'status': 'built', 'steps': state['steps'] + ['excavation', 'lining']}; graph = StateGraph(TunnelState); graph.add_node('survey', validate_survey); graph.add_node('construct', perform_construction); graph.set_entry_point('survey'); graph.add_edge('survey', 'construct'); graph.add_edge('construct', END); graph = graph.compile()