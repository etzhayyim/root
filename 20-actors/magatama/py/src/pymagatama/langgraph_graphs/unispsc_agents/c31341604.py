from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): assembly_id: str; materials_certified: bool; tolerance_pass: bool; heat_treatment_verified: bool; status: str; def validate_materials(state: AssemblyState): return {'materials_certified': True} if 'Inconel' in state['assembly_id'] else {'materials_certified': False}; def validate_tolerances(state: AssemblyState): return {'tolerance_pass': True}; def check_heat_treatment(state: AssemblyState): return {'heat_treatment_verified': True}; graph = StateGraph(AssemblyState); graph.add_node('material_check', validate_materials); graph.add_node('tolerance_check', validate_tolerances); graph.add_node('heat_check', check_heat_treatment); graph.add_edge('material_check', 'tolerance_check'); graph.add_edge('tolerance_check', 'heat_check'); graph.add_edge('heat_check', END); graph.set_entry_point('material_check'); graph = graph.compile()