from langgraph.graph import StateGraph, END; from typing import TypedDict; class ShieldingState(TypedDict): specs: dict; compliance_status: bool; def validate_specs(state: ShieldingState): state['compliance_status'] = 'mmPb' in state['specs'] and state['specs']['mmPb'] > 0; return state; graph = StateGraph(ShieldingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()