from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HotCellState(TypedDict): specs: dict; compliance_check: bool; assembly_steps: List[str]; def validate_shielding(state: HotCellState): state['compliance_check'] = state['specs'].get('lead_equiv', 0) > 10.0; return state; def plan_installation(state: HotCellState): state['assembly_steps'] = ['clean_frame', 'verify_seal', 'position_glass', 'pressure_test']; return state; graph = StateGraph(HotCellState); graph.add_node('validate', validate_shielding); graph.add_node('plan', plan_installation); graph.set_entry_point('validate'); graph.add_edge('validate', 'plan'); graph.add_edge('plan', END); graph = graph.compile()