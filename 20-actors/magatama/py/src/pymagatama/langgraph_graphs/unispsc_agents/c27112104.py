from langgraph.graph import StateGraph, END; from typing import TypedDict; class ToolSpecState(TypedDict): spec_data: dict; approved: bool; def validate_material(state: ToolSpecState): return {'approved': 'material' in state['spec_data']}; def check_safety(state: ToolSpecState): return {'approved': state['spec_data'].get('safety_certified', False)}; graph = StateGraph(ToolSpecState); graph.add_node('validate', validate_material); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()