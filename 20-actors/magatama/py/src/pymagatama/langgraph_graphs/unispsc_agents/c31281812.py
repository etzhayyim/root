from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): components: List[dict]; validation_results: dict; def validate_purity(state: State): state['validation_results'] = {'purity_check': True}; return state; def check_compliance(state: State): state['validation_results']['security_clearance'] = True; return state; graph = StateGraph(State); graph.add_node('validate_purity', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()