from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DieCastState(TypedDict): specs: dict; validation_report: list[str]; status: str; def validate_specs(state: DieCastState): report = []; if state['specs'].get('tensile_strength', 0) < 400: report.append('Low tensile strength'); return {'validation_report': report}; def assemble_graph(): graph = StateGraph(DieCastState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); return graph.compile(); graph = assemble_graph()