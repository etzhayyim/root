from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): specs: dict; quality_check: bool; graph = StateGraph(MoldState); def validate_specs(state: MoldState): return {'quality_check': 'tolerance' in state.get('specs', {})}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()