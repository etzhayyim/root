from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MarketState(TypedDict): vendor_certs: List[str]; safety_pass: bool; compliant_ingredients: bool; def check_compliance(state: MarketState): state['compliant_ingredients'] = 'FDA_compliance' in state['vendor_certs']; return state; def verify_safety(state: MarketState): state['safety_pass'] = state['compliant_ingredients']; return state; graph = StateGraph(MarketState); graph.add_node('check_compliance', check_compliance); graph.add_node('verify_safety', verify_safety); graph.set_entry_point('check_compliance'); graph.add_edge('check_compliance', 'verify_safety'); graph.add_edge('verify_safety', END); graph.compile()