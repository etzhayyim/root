from langgraph.graph import StateGraph, END; from typing import TypedDict; class PumpSpecState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PumpSpecState): state['valid'] = state['specs'].get('pressure', 0) > 0; return state; graph = StateGraph(PumpSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()