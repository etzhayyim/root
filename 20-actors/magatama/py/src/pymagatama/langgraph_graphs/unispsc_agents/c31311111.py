from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): alloy_type: str; welded: bool; pressure_test: bool; status: str; def validate_alloy(state: PipeState): return {'status': 'Validated' if state['alloy_type'] == 'Waspaloy' else 'Rejected'}; def check_weld(state: PipeState): return {'status': 'Weld_Certified' if state['welded'] else 'Weld_Failed'}; graph = StateGraph(PipeState); graph.add_node('validate', validate_alloy); graph.add_node('weld', check_weld); graph.set_entry_point('validate'); graph.add_edge('validate', 'weld'); graph.add_edge('weld', END); graph = graph.compile()