from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialysisState(TypedDict): component_id: str; safety_check: bool; compliance_docs: List[str]; def validate_sensor(state: DialysisState): state['safety_check'] = True; return state; graph = StateGraph(DialysisState); graph.add_node('validate', validate_sensor); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()