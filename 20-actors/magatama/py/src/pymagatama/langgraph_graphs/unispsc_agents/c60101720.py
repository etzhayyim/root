from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): content: str; validated: bool; def validate_postcard_spec(state: ProcState): return {'validated': len(state['content']) > 0}; graph = StateGraph(ProcState); graph.add_node('validate', validate_postcard_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()