from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HarnessState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: HarnessState): errors = []; if not state['specs'].get('temp_rating'): errors.append('Missing temp rating'); return {'validation_errors': errors, 'approved': len(errors) == 0}; def finalize_procurement(state: HarnessState): return {'approved': True}; graph = StateGraph(HarnessState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()