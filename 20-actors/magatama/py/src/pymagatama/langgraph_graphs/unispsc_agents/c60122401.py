from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class GlassState(TypedDict): fragments: list; valid_safety: bool; status: str; def validate_integrity(state: GlassState): return {'valid_safety': all(f.get('sharpness') < 5 for f in state['fragments'])}; def check_compliance(state: GlassState): return {'status': 'approved' if state['valid_safety'] else 'rejected'}; graph = StateGraph(GlassState); graph.add_node('validate', validate_integrity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()