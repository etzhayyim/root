from langgraph.graph import StateGraph, END; from typing import TypedDict; class StackerState(TypedDict): capacity: int; height: float; is_compliant: bool; def validate_specs(state: StackerState): return {'is_compliant': state['capacity'] > 0 and state['height'] > 0}; graph = StateGraph(StackerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()