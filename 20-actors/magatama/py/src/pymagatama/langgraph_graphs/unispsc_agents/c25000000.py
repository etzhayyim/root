from langgraph.graph import StateGraph, END; from typing import TypedDict; class VehicleProcurementState(TypedDict): vehicle_type: str; specs: dict; compliant: bool; def validate_specs(state: VehicleProcurementState): state['compliant'] = all(k in state['specs'] for k in ['engine', 'safety_rating']); return state; def check_export(state: VehicleProcurementState): if state['vehicle_type'] == 'military': state['compliant'] = False; return state; graph = StateGraph(VehicleProcurementState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()