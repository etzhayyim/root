from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input: str; validations: Annotated[list, operator.add]; result: str; def validate_specs(state: ProcState): return {'validations': ['spec_check_passed', 'compliance_verified']}; def finalize_proc(state: ProcState): return {'result': 'Procurement order ready for authorization'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_proc); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()