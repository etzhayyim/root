from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; components: List[str]; safety_approved: bool; def check_safety(state: KitState): state['safety_approved'] = True; return state; def validate_inventory(state: KitState): return {'components': ['verified']} if state.get('kit_id') else {'components': ['missing']}; graph = StateGraph(KitState); graph.add_node('safety', check_safety); graph.add_node('inventory', validate_inventory); graph.set_entry_point('safety'); graph.add_edge('safety', 'inventory'); graph.add_edge('inventory', END); graph = graph.compile()