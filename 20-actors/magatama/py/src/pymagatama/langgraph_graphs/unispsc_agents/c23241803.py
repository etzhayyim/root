from typing import TypedDict, Annotated, List; import operator; from langgraph.graph import StateGraph, END; class DrillState(TypedDict): specs: dict; validation_errors: Annotated[List[str], operator.add]; status: str; def validate_specs(state: DrillState): errors = []; if state['specs'].get('max_depth', 0) < 0: errors.append('Invalid depth'); return {'validation_errors': errors}; def check_export(state: DrillState): return {'status': 'EXPORT_REFERRED'} if state['specs'].get('is_dual_use') else {'status': 'READY'}; graph = StateGraph(DrillState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()