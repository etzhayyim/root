from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SupplyState(TypedDict): items: list; valid: bool; def validate_pad(state: SupplyState): state['valid'] = all('R-value' in item for item in state['items']); return state; graph = StateGraph(SupplyState); graph.add_node('validate', validate_pad); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()