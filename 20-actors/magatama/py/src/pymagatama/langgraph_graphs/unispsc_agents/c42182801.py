from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DiaperScaleState(TypedDict): scale_data: dict; validation_report: list; status: str; def validate_specs(state: DiaperScaleState): report = []; specs = state.get('scale_data', {}); if specs.get('accuracy') > 0.5: report.append('Accuracy too low'); return {'validation_report': report, 'status': 'validated' if not report else 'failed'}; graph = StateGraph(DiaperScaleState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()