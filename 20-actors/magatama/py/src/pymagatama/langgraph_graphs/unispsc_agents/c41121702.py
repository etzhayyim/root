from langgraph.graph import StateGraph, END; from typing import TypedDict; class TubeState(TypedDict): capacity: float; material: str; rcf_limit: int; is_valid: bool; def validate_specs(state: TubeState): state['is_valid'] = state['rcf_limit'] >= 10000; return state; graph = StateGraph(TubeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()