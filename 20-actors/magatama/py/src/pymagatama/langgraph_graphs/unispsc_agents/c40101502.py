from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExhausterState(TypedDict): specs: dict; compliance_status: bool; def validate_specs(state: ExhausterState): print('Validating CFM and Power...'); return {'compliance_status': state['specs'].get('power') > 0}; def check_safety(state: ExhausterState): print('Checking explosion proofing...'); return {'compliance_status': True}; graph = StateGraph(ExhausterState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()