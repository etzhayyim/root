from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StickerState(TypedDict): sticker_id: str; quality_passed: bool; specs: dict; def validate_stickers(state: StickerState): print(f'Validating stickers: {state[sticker_id]}'); return {'quality_passed': True}; def update_inventory(state: StickerState): print('Updating inventory based on batch specs'); return {'quality_passed': True}; graph = StateGraph(StickerState); graph.add_node('validate', validate_stickers); graph.add_node('inventory', update_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph.compile()