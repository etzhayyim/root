from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class NitrogenState(TypedDict): purity: float; pressure: float; validation_passed: bool; def validate_purity(state: NitrogenState): state['validation_passed'] = state['purity'] >= 99.999; return state; def check_pressure(state: NitrogenState): state['validation_passed'] = state['validation_passed'] and (10.0 <= state['pressure'] <= 20.0); return state; graph = StateGraph(NitrogenState); graph.add_node('validate', validate_purity); graph.add_node('pressure_check', check_pressure); graph.set_entry_point('validate'); graph.add_edge('validate', 'pressure_check'); graph.add_edge('pressure_check', END); graph = graph.compile()