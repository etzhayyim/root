from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ShieldingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(ShieldingState); def validate_attenuation(state: ShieldingState): state['validation_results'].append('Attenuation checked'); return state; def check_compliance(state: ShieldingState): state['validation_results'].append('Compliance verified'); return state; graph.add_node('validate', validate_attenuation); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()