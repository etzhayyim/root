from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FittingState(TypedDict): specs: dict; validation_report: list[str]; status: str; def validate_specs(state: FittingState): specs = state['specs']; errors = []; if specs.get('pressure_rating', 0) < 0: errors.append('Invalid Pressure'); return {'validation_report': errors, 'status': 'VALIDATED' if not errors else 'FAILED'}; graph = StateGraph(FittingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()