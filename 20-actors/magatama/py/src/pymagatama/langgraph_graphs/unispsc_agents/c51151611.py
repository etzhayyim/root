from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): chemical_name: str; purity: float; compliance_certs: List[str]; approved: bool; def check_quality(state: PharmaState): return {'approved': state['purity'] >= 99.5}; def logger(state: PharmaState): print(f'Processing {state["chemical_name"]}'); return {}; graph = StateGraph(PharmaState); graph.add_node('check', check_quality); graph.add_node('log', logger); graph.set_entry_point('log'); graph.add_edge('log', 'check'); graph.add_edge('check', END); graph = graph.compile()