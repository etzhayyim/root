from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RollingMachineState(TypedDict): specs: dict; validation_report: list; workflow_step: str; def validate_specs(state: RollingMachineState): state['validation_report'].append('Checking machine specs'); return state; def check_safety(state: RollingMachineState): state['validation_report'].append('Compliance verified'); return {'workflow_step': 'READY'}; graph = StateGraph(RollingMachineState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()