from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): ingredient: str; validated: bool; batch_id: str; def validate_purity(state: ProcState): return { 'validated': True }; def check_gmp(state: ProcState): return { 'validated': state['validated'] }; graph = StateGraph(ProcState); graph.add_node('purity', validate_purity); graph.add_node('gmp', check_gmp); graph.set_entry_point('purity'); graph.add_edge('purity', 'gmp'); graph.add_edge('gmp', END); graph = graph.compile()