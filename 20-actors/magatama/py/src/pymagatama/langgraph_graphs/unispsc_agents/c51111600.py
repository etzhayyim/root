from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): medicine_data: dict; compliance_checks: List[str]; graph = StateGraph(DrugState); def validate_regulatory(state: DrugState): return {'compliance_checks': state['compliance_checks'] + ['reg_vfy_done']}; def check_cold_chain(state: DrugState): return {'compliance_checks': state['compliance_checks'] + ['temp_profile_verified']}; graph.add_node('regulatory', validate_regulatory); graph.add_node('cold_chain', check_cold_chain); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'cold_chain'); graph.add_edge('cold_chain', END); graph = graph.compile()