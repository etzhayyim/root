from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): input_data: dict; analysis: str; is_approved: bool; def validate_grade(state: ProcureState): material = state['input_data'].get('material'); state['is_approved'] = material in ['stainless_steel', 'aluminum_coated']; return state; def check_safety(state: ProcureState): state['analysis'] = 'Safety check passed' if state['is_approved'] else 'Material non-compliant'; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_grade); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()