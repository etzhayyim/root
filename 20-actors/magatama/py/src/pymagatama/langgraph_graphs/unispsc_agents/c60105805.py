from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): items: List[str]; validated: bool; analysis_complete: bool; def validate_materials(state: AnalysisState): state['validated'] = all('color' in item.lower() for item in state['items']); return state; def finalize_workflow(state: AnalysisState): state['analysis_complete'] = True; return state; graph = StateGraph(AnalysisState); graph.add_node('validate', validate_materials); graph.add_node('finalize', finalize_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()