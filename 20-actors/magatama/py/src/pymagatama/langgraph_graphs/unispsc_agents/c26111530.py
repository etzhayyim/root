from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): spec: dict; validated: bool; audit_log: list; def validate_specs(state: ProcureState): print('Validating cam machining tolerance...'); state['validated'] = state['spec'].get('tolerance') < 0.005; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()