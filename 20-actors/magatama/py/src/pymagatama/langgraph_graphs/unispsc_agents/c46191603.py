from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FireEquipmentState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: FireEquipmentState): status = "Pass" if state["specs"].get("burst_pressure_rating", 0) > 20 else "Fail"; return {"validation_results": [status]}; graph = StateGraph(FireEquipmentState); graph.add_node("validate", validate_specs); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()