from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): content_title: str; age_group: str; compliance_check: bool; workflow: List[str]; def validate_content(state: MaterialState) -> MaterialState: state['compliance_check'] = True; return state; def finalize_material(state: MaterialState) -> MaterialState: state['workflow'].append('verified'); return state; graph = StateGraph(MaterialState); graph.add_node('validate', validate_content); graph.add_node('finalize', finalize_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()