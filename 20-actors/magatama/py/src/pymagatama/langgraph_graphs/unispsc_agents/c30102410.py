from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BrassRodState(TypedDict): rod_specs: dict; inspection_status: str; status: str; def validate_specs(state: BrassRodState): print('Validating alloy composition...'); return {'inspection_status': 'passed'}; def finalize_procurement(state: BrassRodState): print('Generating PO for rod order...'); return {'status': 'completed'}; graph = StateGraph(BrassRodState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()