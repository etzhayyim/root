from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DebridementProcurementState(TypedDict): vendor_docs: list[str]; validation_results: Annotated[list[str], operator.add]; compliance_status: str; def validate_certifications(state): return {'validation_results': ['ISO 13485 check passed', 'FDA PMA/510k verified']}; def check_biocompatibility(state): return {'validation_results': ['Biocompatibility test reports approved']}; graph = StateGraph(DebridementProcurementState); graph.add_node('validate_certs', validate_certifications); graph.add_node('check_bio', check_biocompatibility); graph.set_entry_point('validate_certs'); graph.add_edge('validate_certs', 'check_bio'); graph.add_edge('check_bio', END); graph = graph.compile()