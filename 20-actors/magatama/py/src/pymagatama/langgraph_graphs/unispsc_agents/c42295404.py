from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StentProcurementState(TypedDict): stent_id: str; compliance_docs: List[str]; approved: bool; def validate_stent_specs(state: StentProcurementState): print(f'Validating specs for {state[stent_id]}'); return {'approved': True}; def check_regulatory(state: StentProcurementState): print('Checking FDA/PMDA compliance'); return {'compliance_docs': ['ISO13485', 'CE_Mark']}; graph = StateGraph(StentProcurementState); graph.add_node('validate', validate_stent_specs); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()