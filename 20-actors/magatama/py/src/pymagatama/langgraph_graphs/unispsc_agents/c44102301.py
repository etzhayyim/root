from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BundlingState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: BundlingState): spec = state.get('specs', {}); state['valid'] = 'tension_setting' in spec and 'max_weight' in spec; return state; graph = StateGraph(BundlingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()