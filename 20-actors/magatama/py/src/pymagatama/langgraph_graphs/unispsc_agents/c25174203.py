from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BallJointState(TypedDict): dimensions: dict; material_specs: list; approved: bool; def validate_specs(state: BallJointState): return {'approved': all(k in state['dimensions'] for k in ['diameter', 'length'])}; def finish(state: BallJointState): return state; graph = StateGraph(BallJointState); graph.add_node('validate', validate_specs); graph.add_node('finish', finish); graph.set_entry_point('validate'); graph.add_edge('validate', 'finish'); graph.add_edge('finish', END); graph = graph.compile()