from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserWeldingState(TypedDict): vendor_specs: dict; compliance_check: bool; laser_safety_validated: bool; def validate_specs(state: LaserWeldingState): state['compliance_check'] = 'wavelength' in state['vendor_specs'] and 'power' in state['vendor_specs']; return state; def check_safety(state: LaserWeldingState): state['laser_safety_validated'] = state.get('compliance_check', False) and state['vendor_specs'].get('safety_class') == 'Class 4'; return state; graph = StateGraph(LaserWeldingState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()