from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class KitState(TypedDict): input_data: dict; compliance_checks: list; status: str; def validate_specs(state: KitState): state['compliance_checks'].append('ISO_MEDICAL_CHECK'); return {'status': 'VALIDATED'}; def finalize_kit(state: KitState): return {'status': 'READY_FOR_PROCUREMENT'}; graph = StateGraph(KitState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_kit); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()