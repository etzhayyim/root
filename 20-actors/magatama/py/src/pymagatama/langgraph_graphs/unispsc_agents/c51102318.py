from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): doc_id: str; content: str; validated: bool; compliance_flags: List[str]; def validate_purity(state: PharmaState): state['validated'] = True if '99%' in state['content'] else False; return state; graph = StateGraph(PharmaState); graph.add_node('purity_check', validate_purity); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', END); graph = graph.compile()