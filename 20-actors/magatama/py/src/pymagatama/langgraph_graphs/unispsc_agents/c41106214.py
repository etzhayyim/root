from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_data: dict; validation_errors: List[str]; graph = StateGraph(ReagentState); def validate_temp_profile(state: ReagentState): if state['reagent_data'].get('temp') not in ['-80C', '-20C']: state['validation_errors'].append('Invalid storage temp'); return state; def check_transformation_specs(state: ReagentState): if state['reagent_data'].get('eff', 0) < 1e6: state['validation_errors'].append('Efficiency below threshold'); return state; graph.add_node('validate_storage', validate_temp_profile); graph.add_node('validate_specs', check_transformation_specs); graph.set_entry_point('validate_storage'); graph.add_edge('validate_storage', 'validate_specs'); graph.add_edge('validate_specs', END); compiled_graph = graph.compile()