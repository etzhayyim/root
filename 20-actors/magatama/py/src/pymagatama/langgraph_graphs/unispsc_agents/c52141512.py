from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WaffleState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: WaffleState): state['compliant'] = state['specs'].get('wattage', 0) > 0 and 'cert' in state['specs']; return state; builder = StateGraph(WaffleState); builder.add_node('validator', validate_specs); builder.set_entry_point('validator'); builder.add_edge('validator', END); graph = builder.compile()