from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BasketballState(TypedDict): specs: dict; valid: bool;  def validate_specs(state: BasketballState): diameter = state['specs'].get('diameter'); weight = state['specs'].get('weight'); state['valid'] = 74.9 <= diameter <= 78.0 and 567 <= weight <= 650; return state; graph = StateGraph(BasketballState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()