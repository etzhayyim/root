from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PartState(TypedDict): part_specs: dict; validation_results: List[str]; def validate_dimensions(state: PartState): return {'validation_results': ['Dimension check passed']}; def perform_material_test(state: PartState): return {'validation_results': ['Material test passed']}; graph = StateGraph(PartState); graph.add_node('dimension_check', validate_dimensions); graph.add_node('material_test', perform_material_test); graph.set_entry_point('dimension_check'); graph.add_edge('dimension_check', 'material_test'); graph.add_edge('material_test', END); graph = graph.compile()