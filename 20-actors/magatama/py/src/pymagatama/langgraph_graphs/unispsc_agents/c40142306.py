from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeSaddleState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: PipeSaddleState): errs = []; if not state['specs'].get('pressure_rating'): errs.append('Missing pressure rating'); return {'validation_errors': errs, 'approved': len(errs) == 0}; graph = StateGraph(PipeSaddleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()