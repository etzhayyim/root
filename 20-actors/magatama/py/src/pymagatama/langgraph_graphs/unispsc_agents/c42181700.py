from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class EKGState(TypedDict): device_id: str; compliance_docs: list; is_calibrated: bool; def validate_compliance(state: EKGState): state['compliance_docs'].append('ISO_13485_Verified'); return state; def check_calibration(state: EKGState): state['is_calibrated'] = True; return state; graph = StateGraph(EKGState); graph.add_node('validate', validate_compliance); graph.add_node('check', check_calibration); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.set_entry_point('validate'); graph = graph.compile()