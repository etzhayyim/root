from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PackingMaterialState(TypedDict): input_spec: dict; validation_results: list; status: str; def validate_resilience(state: PackingMaterialState): res = state['input_spec'].get('puncture_resistance', 0) > 50; return {'validation_results': ['Resilience test passed' if res else 'Failed'], 'status': 'COMPLETED'}; graph = StateGraph(PackingMaterialState); graph.add_node('validate', validate_resilience); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()