from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validation_results: dict; def validate_specs(state: State): return {'validation_results': {'sterile': True, 'compliance': 'ISO-13485'}}; def check_expiry(state: State): return {'validation_results': {**state['validation_results'], 'expiry_check': 'passed'}}; graph = StateGraph(State); graph.add_node('val', validate_specs); graph.add_node('expiry', check_expiry); graph.set_entry_point('val'); graph.add_edge('val', 'expiry'); graph.add_edge('expiry', END); graph = graph.compile()