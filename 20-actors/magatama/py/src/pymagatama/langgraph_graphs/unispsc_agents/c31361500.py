from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; specs: dict; verified: bool; def check_adhesion(state: AssemblyState): state['verified'] = 'strength' in state['specs']; return state; def validate_optics(state: AssemblyState): state['verified'] = state['verified'] and state['specs'].get('transmission', 0) > 0.95; return state; graph = StateGraph(AssemblyState); graph.add_node('check_adhesion', check_adhesion); graph.add_node('validate_optics', validate_optics); graph.add_edge('check_adhesion', 'validate_optics'); graph.add_edge('validate_optics', END); graph.set_entry_point('check_adhesion'); graph = graph.compile()