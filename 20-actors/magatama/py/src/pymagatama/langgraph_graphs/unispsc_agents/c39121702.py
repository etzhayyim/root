from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableClipState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(CableClipState); def validate_specs(state: CableClipState): results = []; for item in state['items']: if 'material' not in item: results.append('Missing material'); state['validation_results'] = results; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()