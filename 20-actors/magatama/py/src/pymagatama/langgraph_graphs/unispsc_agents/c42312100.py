from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplyState(TypedDict): items: List[str]; validated: bool; log: List[str]; def validation_step(state: SupplyState): state['log'].append('Validating sterility records'); state['validated'] = True; return state; graph = StateGraph(SupplyState); graph.add_node('validate', validation_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()