from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtrusionState(TypedDict): alloy_type: str; specs_met: bool; material_cert: bool; def validate_specs(state: ExtrusionState): return {'specs_met': True} if state['alloy_type'] else {'specs_met': False}; def verify_origin(state: ExtrusionState): return {'material_cert': True}; graph = StateGraph(ExtrusionState); graph.add_node('validate', validate_specs); graph.add_node('verify', verify_origin); graph.set_entry_point('validate'); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); graph = graph.compile()