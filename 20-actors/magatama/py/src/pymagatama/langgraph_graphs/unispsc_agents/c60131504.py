from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TuningPinState(TypedDict): items: Annotated[list, operator.add]; validation_results: dict; def validate_dimensions(state: TuningPinState): return {'validation_results': {'dimensions': 'ok'}}; def check_material(state: TuningPinState): return {'validation_results': {'material': 'compliant'}}; graph = StateGraph(TuningPinState); graph.add_node('validate_dim', validate_dimensions); graph.add_node('check_mat', check_material); graph.set_entry_point('validate_dim'); graph.add_edge('validate_dim', 'check_mat'); graph.add_edge('check_mat', END); graph = graph.compile()