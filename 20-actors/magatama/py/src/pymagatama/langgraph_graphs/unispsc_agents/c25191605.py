from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpacecraftState(TypedDict): module_id: str; specs: dict; validation_report: List[str]; def validate_specs(state: SpacecraftState): state['validation_report'].append('Verifying ITAR/EAR compliance'); state['validation_report'].append('Checking structural integrity specs'); return state; def final_approval(state: SpacecraftState): state['validation_report'].append('Final mission readiness review complete'); return state; graph = StateGraph(SpacecraftState); graph.add_node('validate', validate_specs); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()