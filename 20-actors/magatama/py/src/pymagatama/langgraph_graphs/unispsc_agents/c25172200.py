from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): door_specs: dict; validation_passed: bool; def validate_dimensions(state: State): state['validation_passed'] = 'length' in state['door_specs'] and state['door_specs']['length'] > 0; return state; def check_quality(state: State): state['validation_passed'] = state['validation_passed'] and state['door_specs'].get('coating_grade') == 'OEM'; return state; graph = StateGraph(State); graph.add_node('validate', validate_dimensions); graph.add_node('quality_check', check_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'quality_check'); graph.add_edge('quality_check', END); graph = graph.compile()