from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShoppingBagState(TypedDict): specs: dict; validation_errors: List[str]; def validate_material(state: ShoppingBagState): state['validation_errors'].extend(['Invalid material'] if state['specs'].get('material') == 'unknown' else []); return state; def check_capacity(state: ShoppingBagState): state['validation_errors'].extend(['Capacity exceeded'] if state['specs'].get('weight', 0) > 20 else []); return state; graph = StateGraph(ShoppingBagState); graph.add_node('validate_material', validate_material); graph.add_node('check_capacity', check_capacity); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_capacity'); graph.add_edge('check_capacity', END); graph = graph.compile()