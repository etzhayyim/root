from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): specs: dict; verified: bool; workflow: List[str]; def validate_specs(state: MachineState): return {'verified': True} if 'power' in state['specs'] else {'verified': False}; def export_check(state: MachineState): return {'workflow': ['export_license_check']}; graph = StateGraph(MachineState); graph.add_node('validate', validate_specs); graph.add_node('export', export_check); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph.set_entry_point('validate'); compile_graph = graph.compile()