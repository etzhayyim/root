from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoagulationState(TypedDict): items: List[str]; validation_results: dict; def validate_lot(state: CoagulationState): return {'validation_results': {'lot_check': 'PASS'}}; def check_temp(state: CoagulationState): return {'validation_results': {'temp_check': 'COMPLIANT'}}; graph = StateGraph(CoagulationState); graph.add_node('validate_lot', validate_lot); graph.add_node('check_temp', check_temp); graph.set_entry_point('validate_lot'); graph.add_edge('validate_lot', 'check_temp'); graph.add_edge('check_temp', END); graph = graph.compile()