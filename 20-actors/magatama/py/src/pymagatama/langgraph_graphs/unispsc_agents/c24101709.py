from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ConveyorState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(ConveyorState); def validate_load(state: ConveyorState): state['validation_results'].append('Load capacity validated'); return state; def check_safety(state: ConveyorState): state['validation_results'].append('Safety compliance checked'); return state; graph.add_node('validate', validate_load); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()