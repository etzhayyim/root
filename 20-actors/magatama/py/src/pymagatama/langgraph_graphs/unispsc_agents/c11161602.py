from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class InorganicState(TypedDict): code: str; status: str; purity: float; hazard_cleared: bool; graph = StateGraph(InorganicState); def validate_purity(state: InorganicState) -> dict: return {'status': 'validated' if state['purity'] > 0.99 else 'rejected'}; def check_safety(state: InorganicState) -> dict: return {'hazard_cleared': True if state['status'] == 'validated' else False}; graph.add_node('purity_check', validate_purity); graph.add_node('safety_check', check_safety); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()