from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): component_type: str; specs: dict; verified: bool; def validate_specs(state: ProcState): state['verified'] = all(required in state['specs'] for required in ['pressure', 'material']); return state; def route_approval(state: ProcState): return 'complete' if state['verified'] else 'rework'; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_approval, {'complete': END, 'rework': 'validate'}); graph = graph.compile()