from langgraph.graph import StateGraph, END; from typing import TypedDict; class DryerState(TypedDict): specs: dict; approved: bool; def validate_specs(state: DryerState): state['approved'] = 'energy_efficiency_rating' in state['specs'] and 'power_source' in state['specs']; return state; def finalize_procurement(state: DryerState): return {'approved': state['approved']}; graph = StateGraph(DryerState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()