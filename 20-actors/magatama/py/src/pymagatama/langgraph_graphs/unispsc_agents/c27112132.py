from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClampState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(ClampState); def validate_specs(state: ClampState): results = []; if state['specs'].get('clamping_force', 0) <= 0: results.append('Invalid Clamping Force'); return {'validation_results': results}; def log_procurement(state: ClampState): print('Processing clamp procurement...'); return {}; graph.add_node('validate', validate_specs); graph.add_node('log', log_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); compiled_graph = graph.compile()