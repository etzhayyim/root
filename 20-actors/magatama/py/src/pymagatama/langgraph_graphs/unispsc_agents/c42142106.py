from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_specs(state: ProcState): state['validated'] = 'ISO_13485' in state['certs']; return state; def check_temp_range(state: ProcState): return {'validated': state['validated'] and True}; graph = StateGraph(ProcState); graph.add_node('validate', validate_medical_specs); graph.add_node('check_temp', check_temp_range); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_temp'); graph.add_edge('check_temp', END); graph.compile()