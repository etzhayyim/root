from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RollerProcurementState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: RollerProcurementState): specs = state.get('specs', {}); state['valid'] = 'power_rating_w' in specs and 'length_mm' in specs; return state; graph = StateGraph(RollerProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()