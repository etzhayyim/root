from langgraph.graph import StateGraph, END; from typing import TypedDict; class StovepipeState(TypedDict): wire_spec: dict; valid: bool; def validate_wire(state: StovepipeState): state['valid'] = state['wire_spec'].get('tensile_strength', 0) > 500; return state; graph = StateGraph(StovepipeState); graph.add_node('validate', validate_wire); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()