from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ControllerState(TypedDict): pressure_target: float; flow_rate: float; is_calibrated: bool; compliance_status: bool; def validate_specs(state: ControllerState): state['compliance_status'] = state['is_calibrated'] and state['pressure_target'] > 0; return state; graph = StateGraph(type=ControllerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()