from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(ProcState); def check_sterile_comp(state: ProcState): state['validation_results'] = {'autoclave_pass': True}; return state; def finalize_order(state: ProcState): print('Processing order for sterile instrument rolls'); return state; graph.add_node('validation', check_sterile_comp); graph.add_node('finalize', finalize_order); graph.set_entry_point('validation'); graph.add_edge('validation', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()