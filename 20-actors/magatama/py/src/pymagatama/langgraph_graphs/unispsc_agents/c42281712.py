from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input: str; validation: bool; def validate_ultrasonic_spec(state: State): return {'validation': True if 'kHz' in state['input'] else False}; graph = StateGraph(State); graph.add_node('validate', validate_ultrasonic_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()