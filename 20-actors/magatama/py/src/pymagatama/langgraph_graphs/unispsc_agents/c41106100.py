from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; validation_status: bool; temperature_logs: List[float]; def validate_storage(state: KitState): state['validation_status'] = all(t < -20 for t in state['temperature_logs']); return state; graph = StateGraph(KitState); graph.add_node('validate', validate_storage); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()