from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): product: str; validated: bool; docs: List[str]; def validate_purity(state: PharmaState): return {'validated': True}; def check_regulatory(state: PharmaState): return {'docs': ['GMP_CERT', 'FDA_APPROVAL']}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()