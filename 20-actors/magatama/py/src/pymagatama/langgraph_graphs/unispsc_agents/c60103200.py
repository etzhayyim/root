from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AlgebraState(TypedDict): content: str; validation_results: List[str]; def validate_materials(state: AlgebraState): result = 'Valid' if len(state['content']) > 10 else 'Invalid'; return {'validation_results': [result]}; graph = StateGraph(AlgebraState); graph.add_node('validator', validate_materials); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()