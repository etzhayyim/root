from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MannequinState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(MannequinState); def validate_specs(state: MannequinState): return {'validation_results': ['Fidelity_Check_Passed', 'Material_Safety_Verified']}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()