from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AlbuminState(TypedDict): batch_id: str; temp_logs: List[float]; is_certified: bool; approved: bool; def validate_temp(state: AlbuminState): return {'approved': all(2 <= t <= 8 for t in state['temp_logs'])}; def verify_cert(state: AlbuminState): return {'approved': state['is_certified']}; graph = StateGraph(AlbuminState); graph.add_node('validate_temp', validate_temp); graph.add_node('verify_cert', verify_cert); graph.set_entry_point('validate_temp'); graph.add_edge('validate_temp', 'verify_cert'); graph.add_edge('verify_cert', END); graph = graph.compile()