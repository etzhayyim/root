from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_data: dict; validation_errors: List[str]; graph = StateGraph(ReagentState); def validate_reagent(state: ReagentState): errors = []; if state['reagent_data'].get('expiry') is None: errors.append('Missing expiry'); return {'validation_errors': errors}; graph.add_node('validate', validate_reagent); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()