from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class PaperProcurementState(TypedDict): input_spec: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_paper_spec(state: PaperProcurementState): results = []; spec = state['input_spec']; if spec.get('gsm_rating', 0) < 50: results.append('Invalid GSM rating for industrial use'); return {'validation_results': results, 'status': 'VALIDATED' if not results else 'FAILED'}; builder = StateGraph(PaperProcurementState); builder.add_node('validate', validate_paper_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()