from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalProcState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_instruments(state: SurgicalProcState): print('Validating instrument counts...'); return {'validated': len(state['items']) > 0}; def check_compliance(state: SurgicalProcState): print('Verifying regulatory certs...'); return {'certs': ['ISO13485', 'FDA_510K']}; graph = StateGraph(SurgicalProcState); graph.add_node('validate', validate_instruments); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()