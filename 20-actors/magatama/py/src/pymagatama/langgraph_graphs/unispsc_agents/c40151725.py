from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): part_specs: dict; validation_status: bool; workflow_log: List[str]; def validate_specs(state: PumpState): state['validation_status'] = all(k in state['part_specs'] for k in ['material', 'pressure']); state['workflow_log'].append('Specs validated'); return state; def check_compatibility(state: PumpState): state['workflow_log'].append('Compatibility check complete'); return state; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()