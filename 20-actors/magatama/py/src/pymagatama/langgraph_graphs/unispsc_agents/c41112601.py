from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; sterility_verified: bool; expiry_date: str; status: str; def verify_sterility(state: KitState): return { "sterility_verified": True }; def check_expiry(state: KitState): return { "status": "valid" if state["expiry_date"] > "2024-01-01" else "expired" }; graph = StateGraph(KitState); graph.add_node("verify", verify_sterility); graph.add_node("check", check_expiry); graph.set_entry_point("verify"); graph.add_edge("verify", "check"); graph.add_edge("check", END); graph = graph.compile()