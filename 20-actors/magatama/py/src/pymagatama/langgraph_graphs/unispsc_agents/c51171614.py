from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): chemical_name: str; purity: float; compliant: bool; def validate_purity(state: State): state['compliant'] = state['purity'] >= 99.0; return state; def check_gmp(state: State): state['compliant'] = state['compliant'] and True; return state; graph = StateGraph(State); graph.add_node('validate', validate_purity); graph.add_node('gmp_check', check_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', 'gmp_check'); graph.add_edge('gmp_check', END); compiled_graph = graph.compile()