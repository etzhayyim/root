from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): cold_chain_verified: bool; gmp_certified: bool; batch_id: str; def verify_cold_chain(state: PharmState): return {'cold_chain_verified': True}; def validate_gmp(state: PharmState): return {'gmp_certified': True}; graph = StateGraph(PharmState); graph.add_node('verify_chain', verify_cold_chain); graph.add_node('validate_gmp', validate_gmp); graph.set_entry_point('verify_chain'); graph.add_edge('verify_chain', 'validate_gmp'); graph.add_edge('validate_gmp', END); compiled_graph = graph.compile()