from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserState(TypedDict): device_specs: dict; compliance_verified: bool; def validate_laser_specs(state: LaserState) -> dict: specs = state['device_specs']; is_compliant = 'Laser Class Rating' in specs and 'FDA/CE Certification Number' in specs; return {'compliance_verified': is_compliant}; graph = StateGraph(LaserState); graph.add_node('validate', validate_laser_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()