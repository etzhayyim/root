from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TherapyState(TypedDict): device_id: str; spectral_data: float; compliance_verified: bool; graph = StateGraph(TherapyState); def validate_spectral_output(state: TherapyState): return {'compliance_verified': state['spectral_data'] > 450}; def record_procurement(state: TherapyState): print(f'Processing {state["device_id"]}'); return state; graph.add_node('validate', validate_spectral_output); graph.add_node('record', record_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'record'); graph.add_edge('record', END); app = graph.compile()