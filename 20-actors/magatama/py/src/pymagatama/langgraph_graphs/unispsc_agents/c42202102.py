from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: List[str]; validated: bool; app_id: str; def validate_specs(state: ProcState): state['validated'] = 'sterile_certification' in state['specs']; return state; builder = StateGraph(ProcState); builder.add_node('validator', validate_specs); builder.set_entry_point('validator'); builder.add_edge('validator', END); graph = builder.compile()