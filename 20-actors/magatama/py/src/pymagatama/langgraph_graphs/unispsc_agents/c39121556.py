from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): spec: dict; validated: bool; graph = StateGraph(State); def validate_specs(state: State): state['validated'] = all(k in state['spec'] for k in ['voltage', 'ampere']); return state; def route(state: State): return 'process' if state['validated'] else END; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()