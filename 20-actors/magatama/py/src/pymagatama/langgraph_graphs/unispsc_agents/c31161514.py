from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcessState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_weld_screw(state: ProcessState): material = state['specs'].get('material'); state['valid'] = material is not None; state['log'].append('Validation completed'); return state; graph = StateGraph(ProcessState); graph.add_node('validate', validate_weld_screw); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()