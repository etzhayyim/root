from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CastingState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: CastingState): state['valid'] = 'Material Grade' in state['specs'] and 'Dimensional Tolerance' in state['specs']; state['logs'].append('Validation complete'); return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()