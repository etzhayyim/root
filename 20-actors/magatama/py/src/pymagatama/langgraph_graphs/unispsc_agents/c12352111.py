from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GasState(TypedDict): gas_code: str; purity: float; is_safe: bool; def validate_gas(state: GasState): state['is_safe'] = state['purity'] >= 0.999999; return state; def check_pressure(state: GasState): return {'is_safe': state['is_safe'] and True}; graph = StateGraph(GasState); graph.add_node('validate', validate_gas); graph.add_node('pressure', check_pressure); graph.set_entry_point('validate'); graph.add_edge('validate', 'pressure'); graph.add_edge('pressure', END); graph = graph.compile()