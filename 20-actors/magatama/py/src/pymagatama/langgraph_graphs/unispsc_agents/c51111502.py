from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; toxicity_level: float; compliance_met: bool; def validate_purity(state: PharmState): state['compliance_met'] = state['toxicity_level'] < 0.5; return state; graph = StateGraph(PharmState); graph.add_node('purity_check', validate_purity); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', END); graph.compile()