from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; purity_score: float; compliance_check: bool; def check_purity(state: PharmaState): return {'compliance_check': state['purity_score'] >= 99.0}; def finalize_procurement(state: PharmaState): return {'compliance_check': True}; graph = StateGraph(PharmaState); graph.add_node('verify_purity', check_purity); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('verify_purity'); graph.add_edge('verify_purity', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()