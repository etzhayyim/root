from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): species: str; freshness_score: float; certs: List[str]; approved: bool; def validate_plants(state: ProcurementState): state['approved'] = 'phytosanitary_certificate' in state['certs'] and state['freshness_score'] > 0.8; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_plants); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()