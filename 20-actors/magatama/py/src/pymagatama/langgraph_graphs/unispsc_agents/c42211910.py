from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BibState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: BibState): requirements = ['material', 'size']; state['valid'] = all(k in state['specs'] for k in requirements); return state; graph = StateGraph(BibState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()