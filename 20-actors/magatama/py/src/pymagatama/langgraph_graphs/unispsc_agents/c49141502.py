from langgraph.graph import StateGraph, END; from typing import TypedDict; class ScubaState(TypedDict): tank_specs: dict; inspection_passed: bool; def validate_tank(state: ScubaState): pressure = state['tank_specs'].get('pressure', 0); state['inspection_passed'] = pressure > 0; return state; graph = StateGraph(ScubaState); graph.add_node('validate', validate_tank); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()