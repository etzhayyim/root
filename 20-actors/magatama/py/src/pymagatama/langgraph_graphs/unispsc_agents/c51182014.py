from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): ingredients: List[str]; validation_results: dict; def validate_purity(state: PharmState): state['validation_results'] = {'purity_check': 'passed'}; return state; def check_regulations(state: PharmState): state['validation_results']['regulatory'] = 'verified'; return state; graph = StateGraph(PharmState); graph.add_node('purity_check', validate_purity); graph.add_node('regulatory_compliance', check_regulations); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'regulatory_compliance'); graph.add_edge('regulatory_compliance', END); graph = graph.compile()