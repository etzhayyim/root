from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SteeringState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: SteeringState): state['validation_report'] = 'Pass' if 'ISO26262' in state['specs'] else 'Fail'; return state; def check_approval(state: SteeringState): return 'approved' if state['validation_report'] == 'Pass' else 'rejected'; graph = StateGraph(SteeringState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', check_approval, {'approved': END, 'rejected': END}); graph.compile()