from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): vendor_id: str; quality_cert: bool; cold_chain_req: bool; approved: bool; def check_compliance(state: PharmaState): state['approved'] = state['quality_cert'] and state['cold_chain_req']; return state; builder = StateGraph(PharmaState); builder.add_node('compliance', check_compliance); builder.set_entry_point('compliance'); builder.add_edge('compliance', END); graph = builder.compile()