from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): data: dict; status: list[str]; def validate_purity(state: PharmState): purity = state['data'].get('purity', 0); return {'status': ['Purity Verified' if purity >= 99.0 else 'Purity Rejected']}; def check_gmp(state: PharmState): return {'status': ['GMP Certificate Validated']}; graph = StateGraph(PharmState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_gmp', check_gmp); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_gmp'); graph.add_edge('check_gmp', END); compiled_graph = graph.compile()