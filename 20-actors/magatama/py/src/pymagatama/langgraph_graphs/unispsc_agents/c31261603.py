from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SteelState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: SteelState): state['valid'] = 'Material Grade' in state['specs'] and 'Wall Thickness' in state['specs']; state['logs'].append('Validation complete'); return state; graph = StateGraph(SteelState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()