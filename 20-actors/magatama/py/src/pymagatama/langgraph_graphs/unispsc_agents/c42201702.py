from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UltrasoundState(TypedDict): device_id: str; compliance_status: bool; specs: dict; def validate_specs(state: UltrasoundState): return {'compliance_status': state['specs'].get('is_medical_grade', False)}; def finalize_report(state: UltrasoundState): return {'compliance_status': True}; graph = StateGraph(UltrasoundState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()