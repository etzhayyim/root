from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): flat_specs: dict; validated: bool; app_data: str; def validate_optical_flat(state: State): specs = state['flat_specs']; is_valid = specs.get('flatness_waves', 0) <= 0.05; return {'validated': is_valid}; def generate_report(state: State): return {'app_data': 'Inspection Report Generated'}; graph = StateGraph(State); graph.add_node('validate', validate_optical_flat); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()