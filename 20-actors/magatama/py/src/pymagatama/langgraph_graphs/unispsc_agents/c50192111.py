from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OrderState(TypedDict): items: List[str]; validated_certs: bool; inspection_passed: bool; def check_safety(state: OrderState): return {'validated_certs': True}; def verify_inspection(state: OrderState): return {'inspection_passed': True}; graph = StateGraph(OrderState); graph.add_node('safety_check', check_safety); graph.add_node('inspection_check', verify_inspection); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'inspection_check'); graph.add_edge('inspection_check', END); graph = graph.compile()