from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MosaicState(TypedDict): items: List[str]; validated_specs: bool; def validate_specs(state: MosaicState): return {'validated_specs': len(state['items']) > 0}; def finalize_order(state: MosaicState): return {'items': state['items']}; graph = StateGraph(MosaicState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()