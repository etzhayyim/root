from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ResorcinolState(TypedDict): chemical_specs: dict; compliance_check: bool; def validate_specs(state: ResorcinolState): state['compliance_check'] = state['chemical_specs'].get('purity', 0) >= 99.0; return state; def check_hazmat(state: ResorcinolState): return 'compliance_check' if state['compliance_check'] else END; graph = StateGraph(ResorcinolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()