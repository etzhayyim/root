from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; log: List[str]; def validate_antenna(state: ProcureState): state['validated'] = all('Frequency' in i for i in state['items']); state['log'].append('Validation completed'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_antenna); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()