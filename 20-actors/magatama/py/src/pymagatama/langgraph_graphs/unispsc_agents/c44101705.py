from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrayState(TypedDict): model: str; validated: bool; specs: dict; def validate_comp(state: TrayState): state['validated'] = 'Tray' in state['model']; return state; def check_specs(state: TrayState): state['specs'] = {'verified': True} if state['validated'] else {'verified': False}; return state; graph = StateGraph(TrayState); graph.add_node('validate', validate_comp); graph.add_node('specs', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'specs'); graph.add_edge('specs', END); graph = graph.compile()