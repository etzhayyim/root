from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BathProductState(TypedDict): product_name: str; specifications: dict; validation_passed: bool; def validate_ergonomics(state): print('Validating ergonomics...'); state['validation_passed'] = True; return state; def check_safety_certs(state): print('Checking safety certs...'); return state; graph = StateGraph(BathProductState); graph.add_node('validate', validate_ergonomics); graph.add_node('certify', check_safety_certs); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()