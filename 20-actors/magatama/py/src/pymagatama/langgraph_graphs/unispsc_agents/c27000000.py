from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): machinery_type: str; specs: dict; approved: bool; def validate_machinery(state: State): return {'approved': state['specs'].get('precision') == 'high'}; graph = StateGraph(State); graph.add_node('validate', validate_machinery); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()