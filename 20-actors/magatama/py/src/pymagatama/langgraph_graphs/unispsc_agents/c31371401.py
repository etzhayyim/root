from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SilicaState(TypedDict): specs: dict; valid: bool; def validate_specs(state: SilicaState): specs = state['specs']; state['valid'] = specs.get('temp', 0) > 1000; return state; graph = StateGraph(SilicaState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()