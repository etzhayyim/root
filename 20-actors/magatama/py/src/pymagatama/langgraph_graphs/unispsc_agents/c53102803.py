from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SwimwearState(TypedDict): specs: dict; compliant: bool; graph = StateGraph(SwimwearState); def validate_material(state: SwimwearState): state['compliant'] = 'spandex_content' in state['specs'] and state['specs']['spandex_content'] > 0; return state; graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()