from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InstrumentState(TypedDict): instrument_type: str; specs: dict; approved: bool; def validate_instrument(state: InstrumentState): state['approved'] = 'frequency_range' in state['specs'] and 'material' in state['specs']; return state; graph = StateGraph(InstrumentState); graph.add_node('validator', validate_instrument); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()