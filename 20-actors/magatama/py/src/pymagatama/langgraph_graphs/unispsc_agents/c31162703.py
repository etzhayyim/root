from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SliderState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SliderState): errors = []; if not state['specs'].get('load_capacity'): errors.append('Missing load capacity'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(SliderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()