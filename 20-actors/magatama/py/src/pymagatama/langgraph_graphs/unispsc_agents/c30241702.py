from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CanvasState(TypedDict): specs: dict; valid: bool; graph = StateGraph(CanvasState); def validate_specs(state: CanvasState): state['valid'] = all(k in state['specs'] for k in ['material', 'flame_rating']); return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile = graph.compile()