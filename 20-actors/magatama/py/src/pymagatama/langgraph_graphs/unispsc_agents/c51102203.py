from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_check: bool; compliance_status: str; quality_score: float; def validate_grade(state: ProcurementState): state['compliance_status'] = 'COMPLIANT' if state['quality_score'] >= 0.98 else 'NON-COMPLIANT'; return state; def check_vendor(state: ProcurementState): state['vendor_check'] = True; return state; graph = StateGraph(ProcurementState); graph.add_node('check_vendor', check_vendor); graph.add_node('validate', validate_grade); graph.set_entry_point('check_vendor'); graph.add_edge('check_vendor', 'validate'); graph.add_edge('validate', END); app = graph.compile()