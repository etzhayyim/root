from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TransparencyState(TypedDict): items: List[str]; validation_results: dict; def validate_specs(state: TransparencyState): state['validation_results'] = {item: 'Passed' for item in state['items']}; return state; def format_report(state: TransparencyState): return {'report': 'Validation complete: ' + str(state['validation_results'])}; graph = StateGraph(TransparencyState); graph.add_node('validate', validate_specs); graph.add_node('report', format_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()