from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CommodityState(TypedDict): code: str; purity: float; validated: bool; def validate_purity(state: CommodityState): return { 'validated': state['purity'] >= 99.9 }; def process_powder(state: CommodityState): return { 'code': 'PROCESSED_' + state['code'] }; graph = StateGraph(CommodityState); graph.add_node('validate', validate_purity); graph.add_node('process', process_powder); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()