from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabelState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(LabelState); def validate_specs(state: LabelState): state['validation_report'] = ['Adhesive check', 'Food safety review']; return state; def check_compliance(state: LabelState): return 'approved' if 'ISO' in state['specs'] else 'rejected'; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()