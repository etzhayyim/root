from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class AdapterState(TypedDict): model: str; compat_check: bool; output: str; def validate_mount(state: AdapterState): return {'compat_check': True, 'output': f'Validated mount for {state['model']}'}; def finalize_spec(state: AdapterState): return {'output': 'Specification confirmed'}; graph = StateGraph(AdapterState); graph.add_node('validate', validate_mount); graph.add_node('finalize', finalize_spec); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()