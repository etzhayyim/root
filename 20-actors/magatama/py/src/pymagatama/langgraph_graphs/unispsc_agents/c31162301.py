from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProfileState(TypedDict): dimensions: dict; material: str; validated: bool; def validate_specs(state: ProfileState): state['validated'] = all(k in state['dimensions'] for k in ['length', 'width', 'depth']); return state; graph = StateGraph(ProfileState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()