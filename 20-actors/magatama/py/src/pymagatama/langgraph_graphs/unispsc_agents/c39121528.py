from typing import TypedDict, Annotated, List; import operator; from langgraph.graph import StateGraph, END; class SensorState(TypedDict): specs: dict; validation_errors: Annotated[List[str], operator.add]; status: str; def validate_specs(state: SensorState): errors = []; if not state['specs'].get('ip_rating'): errors.append('Missing IP rating'); return {'validation_errors': errors, 'status': 'validating'}; def check_compliance(state: SensorState): return {'status': 'compliant' if not state['validation_errors'] else 'rejected'}; graph = StateGraph(SensorState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()