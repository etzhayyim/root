from langgraph.graph import StateGraph, END; from typing import TypedDict; class OrnipressinState(TypedDict): vendor_license: str; temp_log: list; status: str; def validate_compliance(state: OrnipressinState): return 'COMPLIANT' if state['vendor_license'] else 'INVALID'; def check_cold_chain(state: OrnipressinState): return 'VERIFIED' if all(t < 8 for t in state['temp_log']) else 'REJECTED'; graph = StateGraph(OrnipressinState); graph.add_node('compliance', validate_compliance); graph.add_node('cold_chain', check_cold_chain); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'cold_chain'); graph.add_edge('cold_chain', END); graph = graph.compile()