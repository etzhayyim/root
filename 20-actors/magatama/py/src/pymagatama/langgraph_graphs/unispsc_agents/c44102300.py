from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PackingMachineState(TypedDict): machine_id: str; specs: dict; validation_report: List[str]; def validate_specs(state: PackingMachineState): report = []; if state['specs'].get('safety_cert') is None: report.append('Missing safety certification'); return {'validation_report': report}; def machine_node(state: PackingMachineState): return {'validation_report': ['Machine registered in workflow']}; graph = StateGraph(PackingMachineState); graph.add_node('validate', validate_specs); graph.add_node('register', machine_node); graph.set_entry_point('validate'); graph.add_edge('validate', 'register'); graph.add_edge('register', END); app = graph.compile()