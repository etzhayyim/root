from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; valid: bool; status: str; def validate_specs(state: CastingState): state['valid'] = 'material_grade_specification' in state['specs']; return state; def final_approval(state: CastingState): state['status'] = 'Approved' if state['valid'] else 'Rejected'; return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.add_node('approval', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph = graph.compile()