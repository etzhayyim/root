from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class DSPProcState(TypedDict): signal_specs: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_dsp_specs(state: DSPProcState): spec = state['signal_specs']; res = 'PASS' if spec.get('clock_mhz', 0) > 100 else 'FAIL'; return {'validation_results': [f'Clock check: {res}'], 'status': 'validated'} if res == 'PASS' else {'status': 'rejected'}; def compile_graph(): graph = StateGraph(DSPProcState); graph.add_node('validate', validate_dsp_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()