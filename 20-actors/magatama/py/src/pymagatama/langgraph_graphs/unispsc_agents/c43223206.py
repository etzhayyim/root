from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): device_specs: dict; validation_passed: bool; def validate_specs(state: NetworkState): state['validation_passed'] = 'encryption' in state['device_specs'] and 'throughput' in state['device_specs']; return state; def route_step(state: NetworkState): return 'end' if state['validation_passed'] else END; graph = StateGraph(NetworkState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()