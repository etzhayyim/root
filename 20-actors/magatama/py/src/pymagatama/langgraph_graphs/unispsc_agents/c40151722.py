from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpPartState(TypedDict): part_numbers: List[str]; compatibility_verified: bool; quality_checks: List[str]; def verify_compatibility(state: PumpPartState): state['compatibility_verified'] = True; return state; def check_quality(state: PumpPartState): state['quality_checks'] = ['Material ASTM compliance', 'Dimensional tolerance verify']; return state; graph = StateGraph(PumpPartState); graph.add_node('verify', verify_compatibility); graph.add_node('quality', check_quality); graph.add_edge('verify', 'quality'); graph.add_edge('quality', END); graph.set_entry_point('verify'); compiled_graph = graph.compile()