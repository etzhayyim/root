from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ITProcurementState(TypedDict): specs: dict; validation_errors: Annotated[list, operator.add]; status: str; def validate_hardware(state: ITProcurementState): errors = []; reqs = ['CPU', 'RAM', 'Storage']; for r in reqs: if r not in state['specs']: errors.append(f'Missing {r}'); return {'validation_errors': errors}; graph = StateGraph(ITProcurementState); graph.add_node('validation', validate_hardware); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()