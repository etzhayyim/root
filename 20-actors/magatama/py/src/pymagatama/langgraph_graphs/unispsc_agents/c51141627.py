from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity_level: float; compliance_met: bool; def validate_purity(state: PharmState): return {'compliance_met': state['purity_level'] >= 99.0}; def check_regulatory(state: PharmState): return {'compliance_met': state['compliance_met']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.compile()