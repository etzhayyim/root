from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class YoYoState(TypedDict): specs: dict; quality_report: str; approved: bool; def validate_specs(state: YoYoState): return {'quality_report': 'Passed' if state['specs'].get('safety_cert') else 'Failed'}; def finalize_procurement(state: YoYoState): return {'approved': state['quality_report'] == 'Passed'}; graph = StateGraph(YoYoState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()