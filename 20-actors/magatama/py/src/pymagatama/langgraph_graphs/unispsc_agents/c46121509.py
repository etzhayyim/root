from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MissileWorkflow(TypedDict): status: str; certification_check: bool; export_compliance: bool; def validate_compliance(state: MissileWorkflow): state['export_compliance'] = True; return state; def check_certification(state: MissileWorkflow): state['certification_check'] = True; return state; graph = StateGraph(MissileWorkflow); graph.add_node('compliance', validate_compliance); graph.add_node('certification', check_certification); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'certification'); graph.add_edge('certification', END); graph = graph.compile()