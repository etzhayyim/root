from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class JewelryProcurementState(TypedDict): vendor_certs: List[str]; material_grade: str; appraisal_status: bool; approved: bool; def validate_authenticity(state: JewelryProcurementState): state['appraisal_status'] = 'GIA' in state['vendor_certs']; return state; def check_compliance(state: JewelryProcurementState): state['approved'] = state['appraisal_status'] and 'Conflict-Free' in state['vendor_certs']; return state; graph = StateGraph(JewelryProcurementState); graph.add_node('verify', validate_authenticity); graph.add_node('compliance', check_compliance); graph.add_edge('verify', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('verify'); graph = graph.compile()