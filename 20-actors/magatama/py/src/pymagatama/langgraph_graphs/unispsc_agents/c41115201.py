from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RadarState(TypedDict): vendor_specs: dict; security_clearance: bool; valid_tech_specs: bool; def validate_radar_params(state: RadarState): return {'valid_tech_specs': state['vendor_specs'].get('range', 0) > 0}; def check_export_controls(state: RadarState): return {'security_clearance': True}; graph = StateGraph(RadarState); graph.add_node('validate', validate_radar_params); graph.add_node('compliance', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()