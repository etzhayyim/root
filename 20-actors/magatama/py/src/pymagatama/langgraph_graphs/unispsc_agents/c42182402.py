from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AudiometryState(TypedDict): device_id: str; calibration_data: dict; compliance_verified: bool; def validate_calibration(state: AudiometryState): return {'compliance_verified': state['calibration_data'].get('accuracy', 0) > 0.95}; def route_to_maintenance(state: AudiometryState): return 'end' if state['compliance_verified'] else 'recalibrate'; graph = StateGraph(AudiometryState); graph.add_node('validate', validate_calibration); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()