from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_specs: dict; validation_results: List[str]; def validate_material(state: ProcurementState): state['validation_results'].append('Material grade verified'); return state; def inspect_welds(state: ProcurementState): state['validation_results'].append('Weld integrity confirmed'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_material); graph.add_node('inspect', inspect_welds); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()