from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DissectorState(TypedDict): vendor_certs: List[str]; inspection_passed: bool;  def validate_certs(state: DissectorState): return {'inspection_passed': 'ISO_13485' in state['vendor_certs']}; def inspect_quality(state: DissectorState): return {'inspection_passed': True}; graph = StateGraph(DissectorState); graph.add_node('validate', validate_certs); graph.add_node('inspect', inspect_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()