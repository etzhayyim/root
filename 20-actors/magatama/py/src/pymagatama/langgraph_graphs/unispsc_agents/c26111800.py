from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DriveState(TypedDict): specs: dict; validation_report: list[str]; def validate_specs(state: DriveState): return {'validation_report': ['Torque/Load compatibility check passed']}; def finalize(state: DriveState): return {'validation_report': state['validation_report'] + ['Approval workflow complete']}; graph = StateGraph(DriveState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()