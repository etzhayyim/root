from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WaspalloyState(TypedDict): assembly_id: str; quality_docs: List[str]; inspection_status: str; def validate_specs(state: WaspalloyState): return {'inspection_status': 'pending'} if not state['quality_docs'] else {'inspection_status': 'verified'}; def export_control_check(state: WaspalloyState): return {'inspection_status': 'flagged_ITAR'}; graph = StateGraph(WaspalloyState); graph.add_node('validation', validate_specs); graph.add_node('export', export_control_check); graph.set_entry_point('validation'); graph.add_edge('validation', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()