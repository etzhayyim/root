from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): metal_grade: str; specs: dict; valid: bool; def validate_specs(state: CastingState): state['valid'] = state['metal_grade'] in ['C83600', 'C90300']; return state; def finalize(state: CastingState): return {'result': 'certified' if state['valid'] else 'rejected'}; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); compile_graph = graph.compile()