from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlantState(TypedDict): specs: dict; validation_results: List[str]; def validate_well(state: PlantState): return {'validation_results': ['Pressure test passed']}; def perform_thermal_audit(state: PlantState): return {'validation_results': ['Thermal capacity within limits']}; graph = StateGraph(PlantState); graph.add_node('well_validation', validate_well); graph.add_node('thermal_audit', perform_thermal_audit); graph.set_entry_point('well_validation'); graph.add_edge('well_validation', 'thermal_audit'); graph.add_edge('thermal_audit', END); graph = graph.compile()