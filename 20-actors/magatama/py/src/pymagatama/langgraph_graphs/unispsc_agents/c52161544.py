from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EraserState(TypedDict): model_id: str; magnetic_field: float; valid: bool; def validate_erasure_specs(state: EraserState): state['valid'] = state['magnetic_field'] >= 5000; return state; graph = StateGraph(EraserState); graph.add_node('validate', validate_erasure_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()