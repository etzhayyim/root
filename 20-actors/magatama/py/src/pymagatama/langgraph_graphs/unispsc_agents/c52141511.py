from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class JuicerState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: JuicerState): state['compliance'] = state['specs'].get('power_w', 0) > 0 and 'food_safe' in state['specs']; return state; def check_approval(state: JuicerState): state['approved'] = state['compliance']; return state; graph = StateGraph(JuicerState); graph.add_node('validate', validate_specs); graph.add_node('approve', check_approval); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()