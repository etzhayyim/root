from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_spec(state: ProcurementState): spec = state['input_data'].get('spec', {}); is_valid = all(key in spec for key in ['Torque Rating', 'Material Grade']); return {'validation_results': ['Spec check passed' if is_valid else 'Missing fields'], 'status': 'validated' if is_valid else 'error'}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()