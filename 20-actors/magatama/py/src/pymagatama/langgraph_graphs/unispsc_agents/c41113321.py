from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PaintTesterState(TypedDict): input_data: dict; validation_results: dict; def validate_spec(state: PaintTesterState): state['validation_results'] = {'passed': True, 'reason': 'Compliance check passed'}; return state; def prepare_report(state: PaintTesterState): return {'validation_results': 'Report Generated'}; graph = StateGraph(PaintTesterState); graph.add_node('validate', validate_spec); graph.add_node('report', prepare_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()