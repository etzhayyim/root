from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RehabState(TypedDict): device_specs: dict; compliance_check: bool; output: str; def validate_specs(state: RehabState): state['compliance_check'] = 'weight_capacity_limit_kg' in state['device_specs']; return state; def finalize_procurement(state: RehabState): state['output'] = 'Validated' if state['compliance_check'] else 'Rejected'; return state; graph = StateGraph(RehabState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()