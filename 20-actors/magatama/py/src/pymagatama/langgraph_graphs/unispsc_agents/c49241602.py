from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec: dict; validated: bool; def validate_specs(state: State): state['validated'] = 'weight' in state['spec'] and 'diameter' in state['spec']; return state; def route_logic(state: State): return 'process' if state['validated'] else 'error'; graph = StateGraph(State); graph.add_node('process', validate_specs); graph.set_entry_point('process'); graph.add_edge('process', END); graph.compile()