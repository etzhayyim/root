from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BerylliumState(TypedDict): component_specs: dict; toxicity_check_passed: bool; export_license: bool; def validate_specs(state: BerylliumState): state['toxicity_check_passed'] = state['component_specs'].get('purity', 0) > 99.0; return state; def check_export(state: BerylliumState): state['export_license'] = True; return state; graph = StateGraph(BerylliumState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()