from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_pressure(state: PipeState): state['compliance'] = state['specs'].get('pressure_rating', 0) > 0; return state; def check_standards(state: PipeState): state['approved'] = state['specs'].get('cert') == 'ISO'; return state; graph = StateGraph(PipeState); graph.add_node('check_pressure', validate_pressure); graph.add_node('check_certs', check_standards); graph.add_edge('check_pressure', 'check_certs'); graph.add_edge('check_certs', END); graph.set_entry_point('check_pressure'); graph = graph.compile()