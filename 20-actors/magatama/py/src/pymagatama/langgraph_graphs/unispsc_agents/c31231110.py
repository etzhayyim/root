from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): material_grade: str; dimensions: dict; status: str; def validate_specs(state: ProcurementState): if state['material_grade'] in ['303', '304', '316']: return {'status': 'validated'}; return {'status': 'rejected'}; def check_compliance(state: ProcurementState): return {'status': 'compliant' if state.get('status') == 'validated' else 'non-compliant'}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()