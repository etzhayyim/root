from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StationeryState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_refill(state: StationeryState): errors = []; for item in state['items']: if 'size' not in item: errors.append(f'Missing size for {item.get('name', 'unknown')}'); return {'validation_errors': errors}; graph = StateGraph(StationeryState); graph.add_node('validate', validate_refill); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()