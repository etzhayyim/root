from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class TerminationState(TypedDict): specs: dict; valid: bool; def validate_specs(state: TerminationState): impedance = state['specs'].get('impedance_ohms'); state['valid'] = impedance in [50, 75]; return state; graph = StateGraph(TerminationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()