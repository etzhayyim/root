from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_items: List[str]; safety_verified: bool; compliance_status: str; def validate_components(state: KitState): return {'safety_verified': True}; def check_compliance(state: KitState): return {'compliance_status': 'Compliant'}; graph = StateGraph(KitState); graph.add_node('validate', validate_components); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()