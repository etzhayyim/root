from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FoodWarmerState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(FoodWarmerState); def validate_compliance(state: FoodWarmerState): return {'validation_results': ['Compliance Verified']}; def check_power_requirements(state: FoodWarmerState): return {'validation_results': ['Power Specs OK']}; graph.add_node('compliance', validate_compliance); graph.add_node('power', check_power_requirements); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'power'); graph.add_edge('power', END); graph = graph.compile()