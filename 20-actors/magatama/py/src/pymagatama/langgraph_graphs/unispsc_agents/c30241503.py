from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BailRingState(TypedDict): metal_grade: str; load_rating: float; certificates: List[str]; validated: bool; def check_material(state: BailRingState): return {'validated': state['metal_grade'] in ['Stainless Steel 316', 'Carbon Steel']} ; def check_load(state: BailRingState): return {'validated': state['load_rating'] > 0} ; graph = StateGraph(BailRingState); graph.add_node('check_material', check_material); graph.add_node('check_load', check_load); graph.set_entry_point('check_material'); graph.add_edge('check_material', 'check_load'); graph.add_edge('check_load', END); compiled_graph = graph.compile()