from langgraph.graph import StateGraph, END; from typing import TypedDict; class TapeState(TypedDict): vendor_specs: dict; validated: bool; def check_spec(state: TapeState): adhesive = state['vendor_specs'].get('adhesive_strength'); return {'validated': adhesive > 10}; graph = StateGraph(TapeState); graph.add_node('validate', check_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()