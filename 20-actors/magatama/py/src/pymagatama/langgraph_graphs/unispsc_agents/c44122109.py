from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): vendor_specs: dict; compliance_check: bool; graph = StateGraph(FastenerState); def validate_spec(state: FastenerState): return {'compliance_check': state['vendor_specs'].get('strength', 0) > 5.0}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()