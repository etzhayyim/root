from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class OpticalState(TypedDict): input_spec: str; validation_results: list; compliance_check: bool; def validate_optics(state: OpticalState): return {'validation_results': ['Surface quality: Pass', 'Transmission: 99.8%']}; def check_export(state: OpticalState): return {'compliance_check': True}; graph = StateGraph(OpticalState); graph.add_node('validate', validate_optics); graph.add_node('compliance', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()