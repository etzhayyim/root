from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class OrthoState(TypedDict): input_spec: dict; compliance_status: bool; workflow_log: Annotated[list, operator.add]; def validate_medical_grade(state: OrthoState): return { "compliance_status": state["input_spec"].get("certification") == "ISO-13485", "workflow_log": ["Validated medical compliance"] }; def finalize_report(state: OrthoState): return { "workflow_log": ["Generated procurement sign-off"] }; graph = StateGraph(OrthoState); graph.add_node("validate", validate_medical_grade); graph.add_node("finalize", finalize_report); graph.set_entry_point("validate"); graph.add_edge("validate", "finalize"); graph.add_edge("finalize", END); graph = graph.compile()