from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): input_data: dict; validation_results: List[str]; approved: bool; def val_sterile(state: ProcState): state['validation_results'].append('Sterility Verified'); return state; def val_mech(state: ProcState): state['validation_results'].append('Flow Accuracy Verified'); return state; graph = StateGraph(ProcState); graph.add_node('val_sterile', val_sterile); graph.add_node('val_mech', val_mech); graph.set_entry_point('val_sterile'); graph.add_edge('val_sterile', 'val_mech'); graph.add_edge('val_mech', END); graph = graph.compile()