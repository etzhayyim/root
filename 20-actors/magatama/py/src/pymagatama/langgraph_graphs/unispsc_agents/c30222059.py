from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SewageState(TypedDict): pump_specs: dict; validation_status: bool; def validate_specs(state: SewageState): specs = state['pump_specs']; status = all([specs.get('flow_rate'), specs.get('head_pressure')]); return {'validation_status': status}; def finalize_procurement(state: SewageState): return {'validation_status': True}; graph = StateGraph(SewageState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()