from langgraph.graph import StateGraph, END; from typing import TypedDict; class PumpState(TypedDict): specs: dict; valid: bool; def validate_specs(state: PumpState): specs = state.get('specs', {}); state['valid'] = 'flow' in specs and 'pressure' in specs; return state; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()