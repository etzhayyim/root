from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_name: str; toxicity_clearance: bool; hazard_assessment: str; graph = StateGraph(ProcureState); def check_toxicity(state: ProcureState): return {'toxicity_clearance': True, 'hazard_assessment': 'High toxicity substance detected'}; def validate_procurement(state: ProcureState): return {'hazard_assessment': 'Regulatory compliant' if state['toxicity_clearance'] else 'Rejected'}; graph.add_node('check', check_toxicity); graph.add_node('validate', validate_procurement); graph.set_entry_point('check'); graph.add_edge('check', 'validate'); graph.add_edge('validate', END); app = graph.compile()