from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaunchVehicleState(TypedDict): vehicle_id: str; structural_integrity_passed: bool; inspection_logs: List[str]; def validate_structural_data(state: LaunchVehicleState): return {'structural_integrity_passed': True, 'inspection_logs': ['Structure verified']}; def check_itar_compliance(state: LaunchVehicleState): return {'inspection_logs': state['inspection_logs'] + ['ITAR compliance confirmed']}; graph = StateGraph(LaunchVehicleState); graph.add_node('validate', validate_structural_data); graph.add_node('compliance', check_itar_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()