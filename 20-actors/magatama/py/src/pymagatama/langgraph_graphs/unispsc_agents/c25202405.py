from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class FuelState(TypedDict): doc_id: str; compliance_checks: list; status: str; def validate_specs(state: FuelState): state['compliance_checks'].append('RTCA_DO_160G_Verified'); return {'status': 'validated'}; def check_export_controls(state: FuelState): state['compliance_checks'].append('ITAR_Check_Complete'); return {'status': 'cleared'}; graph = StateGraph(FuelState); graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); compiled = graph.compile()