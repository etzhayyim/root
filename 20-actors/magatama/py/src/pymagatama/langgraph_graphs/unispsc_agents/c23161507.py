from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LatheSpecState(TypedDict): spec_data: dict; validation_report: list[str]; graph = StateGraph(LatheSpecState); def validate_specs(state: LatheSpecState): report = []; if state['spec_data'].get('positioning_accuracy', 0) > 0.05: report.append('Precision error'); return {'validation_report': report}; def check_export_control(state: LatheSpecState): return {'validation_report': state['validation_report'] + ['Dual-use check passed']}; graph.add_node('validate', validate_specs); graph.add_node('export', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); app = graph.compile()