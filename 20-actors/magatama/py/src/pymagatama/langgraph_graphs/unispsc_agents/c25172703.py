from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MarineSysState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(MarineSysState); def validate_specs(state: MarineSysState): report = []; missing = [f for f in ['pressure_rating', 'certification_class_society'] if f not in state['specs']]; report.append('Failed' if missing else 'Passed'); return {'validation_report': report}; graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); app = graph.compile()