from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CitrusState(TypedDict): fruit_type: str; quality_passed: bool; inspection_logs: List[str]; def validate_quality(state: CitrusState): return {'quality_passed': True, 'inspection_logs': ['Pesticide check clear', 'Microbial count within limits']}; def log_compliance(state: CitrusState): print('Compliance checked for shipment'); return {'inspection_logs': state['inspection_logs'] + ['Compliance recorded']}; graph = StateGraph(CitrusState); graph.add_node('validate', validate_quality); graph.add_node('report', log_compliance); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()