from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): instrument_data: dict; validation_results: List[str]; def validate_instrument(state: SurgicalState): validated = 'Valid' if state['instrument_data'].get('sterilization_grade') == 'Medical' else 'Invalid'; return {'validation_results': [validated]}; def check_compliance(state: SurgicalState): return {'validation_results': state['validation_results'] + ['ISO_13485_Verified']}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_instrument); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()