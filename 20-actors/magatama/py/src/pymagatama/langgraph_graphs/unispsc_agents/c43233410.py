from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DriverState(TypedDict): vendor: str; ver: str; is_signed: bool; status: str; def check_signature(state: DriverState) -> DriverState: state['is_signed'] = True; return state; def validate_compatibility(state: DriverState) -> DriverState: state['status'] = 'validated' if state['ver'] else 'failed'; return state; graph = StateGraph(DriverState); graph.add_node('verify', check_signature); graph.add_node('validate', validate_compatibility); graph.set_entry_point('verify'); graph.add_edge('verify', 'validate'); graph.add_edge('validate', END); app = graph.compile()