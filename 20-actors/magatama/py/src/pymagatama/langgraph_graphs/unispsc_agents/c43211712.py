from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_tablet_specs(state: GraphState): return {'validation_results': ['Spec check passed'], 'status': 'validated'}; def check_compatibility(state: GraphState): return {'validation_results': ['Compatibility confirmed'], 'status': 'complete'}; graph = StateGraph(GraphState); graph.add_node('validate', validate_tablet_specs); graph.add_node('compatibility', check_compatibility); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph.set_entry_point('validate'); graph = graph.compile()