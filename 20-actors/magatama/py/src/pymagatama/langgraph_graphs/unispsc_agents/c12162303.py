from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class State(TypedDict): purity: float; specs: dict; approved: bool; def validate_purity(state: State): return {'approved': state['purity'] >= 0.999}; graph = StateGraph(State); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()