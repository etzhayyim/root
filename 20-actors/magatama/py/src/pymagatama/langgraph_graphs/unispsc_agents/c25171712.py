from langgraph.graph import StateGraph, END; from typing import TypedDict; class BrakeState(TypedDict): specs: dict; valid: bool; def validate_specs(state: BrakeState): state['valid'] = 'Friction Material' in state['specs'] and 'Heat Resistance' in state['specs']; return state; graph = StateGraph(BrakeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()