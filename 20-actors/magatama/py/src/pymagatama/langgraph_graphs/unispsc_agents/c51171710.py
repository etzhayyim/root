from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): data: dict; logs: Annotated[list, operator.add]; def validate_purity(state: PharmState): return {'logs': ['Purity checked']} if state['data']['purity'] > 99.0 else {'logs': ['Purity low']}; def check_gmp(state: PharmState): return {'logs': ['GMP verified']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('gmp', check_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', 'gmp'); graph.add_edge('gmp', END); graph = graph.compile()