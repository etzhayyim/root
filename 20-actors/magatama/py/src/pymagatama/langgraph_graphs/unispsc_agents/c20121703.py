from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): component_id: str; specs: dict; validation_status: bool; def validate_specs(state: RobotState) -> RobotState: state['validation_status'] = 'payload_capacity' in state['specs'] and 'repeatability_accuracy' in state['specs']; return state; def check_compliance(state: RobotState) -> RobotState: state['validation_status'] = state['validation_status'] and (state['specs'].get('degree_of_freedom', 0) > 0); return state; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()