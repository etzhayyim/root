from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): purity_level: float; storage_temp: str; verified: bool; def validate_reagent(state: ReagentState): state['verified'] = state['purity_level'] >= 0.98 and state['storage_temp'] == '-20C'; return state; def route_storage(state: ReagentState): return 'complete' if state['verified'] else 'rejection'; graph = StateGraph(ReagentState); graph.add_node('validation', validate_reagent); graph.add_edge('validation', 'complete'); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()