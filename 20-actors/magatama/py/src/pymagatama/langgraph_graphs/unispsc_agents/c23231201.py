from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineSpec(TypedDict): model_id: str; precision: float; validation_passed: bool; def validate_machine(state: MachineSpec): state['validation_passed'] = state['precision'] < 0.01; return state; graph = StateGraph(MachineSpec); graph.add_node('validate', validate_machine); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()