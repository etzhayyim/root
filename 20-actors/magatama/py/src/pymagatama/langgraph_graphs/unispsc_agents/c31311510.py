from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipeState(TypedDict): assembly_id: str; specs: dict; validation_passed: bool; def validate_titanium_specs(state: PipeState): state['validation_passed'] = 'ASTM_Grade' in state['specs'] and state['specs']['pressure_rating'] > 0; return state; graph = StateGraph(PipeState); graph.add_node('validate', validate_titanium_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()