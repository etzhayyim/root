from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): ingredient: str; validated: bool; compliance_check: bool; def validate_purity(state: PharmState): return {'validated': True} if state['ingredient'] == 'Metoprolol' else {'validated': False}; def check_compliance(state: PharmState): return {'compliance_check': True}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()