from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TransportState(TypedDict): status: str; spec_data: dict; validation_errors: Annotated[list, operator.add]; def validate_specs(state: TransportState): errors = []; if 'leak_proof' not in state['spec_data']: errors.append('Leak-proof certification missing'); return {'validation_errors': errors}; def finalize_process(state: TransportState): return {'status': 'Approved' if not state['validation_errors'] else 'Rejected'}; graph = StateGraph(TransportState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_process); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()