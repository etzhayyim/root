from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SealantState(TypedDict): vendor: str; gtin: str; registration_verified: bool; content: List[str]; def verify_registration(state: SealantState): return {'registration_verified': state.get('gtin', '').startswith('084')}; def validate_specs(state: SealantState): return {'content': ['validated_spec_compliance']}; graph = StateGraph(SealantState); graph.add_node('verify', verify_registration); graph.add_node('specs', validate_specs); graph.set_entry_point('verify'); graph.add_edge('verify', 'specs'); graph.add_edge('specs', END); compiled_graph = graph.compile()