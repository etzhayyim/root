from typing import TypedDict, List; from langgraph.graph import StateGraph; class MotorState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_motor_specs(state: MotorState): specs = state['specs']; errors = []; required = ['power', 'voltage', 'speed']; for field in required: if field not in specs: errors.append(f'Missing {field}'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(MotorState); graph.add_node('validate', validate_motor_specs); graph.set_entry_point('validate'); graph.set_finish_point('validate'); graph = graph.compile()