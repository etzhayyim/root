from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ValveState(TypedDict): instrument_id: str; approval_status: bool; calibration_data: dict; tasks: List[str]; def validate_instrument(state: ValveState): return {'approval_status': True if state['instrument_id'].startswith('SV') else False}; def verify_calibration(state: ValveState): return {'tasks': ['verify_iso_compliance', 'check_calibration_date']}; graph = StateGraph(ValveState); graph.add_node('validate', validate_instrument); graph.add_node('calibrate', verify_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); compiled_graph = graph.compile()