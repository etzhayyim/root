from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KilnState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: KilnState): specs = state['specs']; errors = []; if specs.get('max_temp', 0) < 1200: errors.append('Temperature rating insufficient for industrial firing'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(KilnState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()