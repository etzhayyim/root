from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PomeFruitState(TypedDict): fruit_type: str; safety_checks: List[str]; inspection_passed: bool; def validate_safety(state: PomeFruitState): return {'inspection_passed': 'HACCP' in state['safety_checks']}; def compliance_check(state: PomeFruitState): print(f'Verifying safety for {state["fruit_type"]}'); return {'inspection_passed': True}; graph = StateGraph(PomeFruitState); graph.add_node('safety', validate_safety); graph.add_node('compliance', compliance_check); graph.add_edge('safety', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('safety'); graph = graph.compile()