from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): material_spec: str; validation_passed: bool; inspection_report: str; def validate_specs(state: ProcurementState): state['validation_passed'] = 'C1100' in state.get('material_spec', '') and 'mm' in state.get('material_spec', ''); return state; def generate_report(state: ProcurementState): state['inspection_report'] = 'Standard chemical and dimensional verification complete.' if state['validation_passed'] else 'Manual review required.'; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()