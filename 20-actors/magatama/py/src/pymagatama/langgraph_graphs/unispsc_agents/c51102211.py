from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity_data: float; qc_passed: bool; graph = StateGraph(PharmaState); def validate_purity(state: PharmaState): return {'qc_passed': state['purity_data'] >= 99.0}; graph.add_node('assay_check', validate_purity); graph.set_entry_point('assay_check'); graph.add_edge('assay_check', END); compiled_graph = graph.compile()