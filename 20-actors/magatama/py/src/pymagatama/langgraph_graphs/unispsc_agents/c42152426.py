from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalMaterialState(TypedDict): material_id: str; certification_valid: bool; shelf_life_check: bool; def validate_cert(state: DentalMaterialState): state['certification_valid'] = True; return state; def validate_shelf_life(state: DentalMaterialState): state['shelf_life_check'] = True; return state; graph = StateGraph(DentalMaterialState); graph.add_node('validate_cert', validate_cert); graph.add_node('validate_shelf_life', validate_shelf_life); graph.add_edge('validate_cert', 'validate_shelf_life'); graph.add_edge('validate_shelf_life', END); graph.set_entry_point('validate_cert'); graph = graph.compile()