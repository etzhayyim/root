from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InstrumentState(TypedDict): instrument_data: dict; validation_results: List[str]; graph = StateGraph(InstrumentState); def validate_material(state: InstrumentState): state['validation_results'].append('Material grade validated'); return state; def verify_regulatory(state: InstrumentState): state['validation_results'].append('ISO/FDA check confirmed'); return state; graph.add_node('validate_material', validate_material); graph.add_node('verify_regulatory', verify_regulatory); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'verify_regulatory'); graph.add_edge('verify_regulatory', END); compiled_graph = graph.compile()