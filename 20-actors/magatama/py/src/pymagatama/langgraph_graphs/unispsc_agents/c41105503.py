from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; ready: bool; validated: bool; def validate_specs(state: ProcState): state['validated'] = 'Voltage stability range' in state['specs'] and 'Recovery efficiency rate' in state['specs']; return state; def check_readiness(state: ProcState): state['ready'] = state['validated']; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('check', check_readiness); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); compiled_graph = graph.compile()