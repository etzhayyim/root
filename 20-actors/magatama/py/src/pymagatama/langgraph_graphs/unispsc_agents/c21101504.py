from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; valid: bool; engine_spec: str; def validate_specs(state: State): state['valid'] = 'engine_type' in state['input_data']; return state; def check_emissions(state: State): state['engine_spec'] = 'compliant' if state['input_data'].get('emission_rating') == 'Tier4' else 'non-compliant'; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('emissions', check_emissions); graph.set_entry_point('validate'); graph.add_edge('validate', 'emissions'); graph.add_edge('emissions', END); graph = graph.compile()