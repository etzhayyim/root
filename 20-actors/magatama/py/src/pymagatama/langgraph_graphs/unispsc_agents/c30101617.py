from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SpecState(TypedDict): specs: dict; validation_errors: List[str]; def validate_material(state: SpecState): errors = []; if state['specs'].get('moisture_content', 0) > 20: errors.append('Moisture content too high'); return {'validation_errors': errors}; graph = StateGraph(SpecState); graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()