from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class InsulationState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_specs(state: InsulationState): results = []; r_val = state['input_data'].get('r_value', 0); if r_val < 3.0: results.append('R-value below requirement'); return {'validation_results': results, 'status': 'validated' if not results else 'rejected'}; graph = StateGraph(InsulationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()