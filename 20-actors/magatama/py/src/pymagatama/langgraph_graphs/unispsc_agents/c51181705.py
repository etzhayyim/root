from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): chemical_name: str; purity_data: float; compliance_verified: bool; graph = StateGraph(PharmState); def check_purity(state: PharmState): return {'compliance_verified': state['purity_data'] >= 99.0}; def validate_regulations(state: PharmState): return {'compliance_verified': True}; graph.add_node('check_purity', check_purity); graph.add_node('validate_reg', validate_regulations); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'validate_reg'); graph.add_edge('validate_reg', END); app = graph.compile()