from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FoilState(TypedDict): specs: dict; validation_results: List[str]; def validate_foil(state: FoilState): results = []; required = ['material', 'thickness']; for key in required: if key not in state['specs']: results.append(f'Missing {key}'); return {'validation_results': results}; graph = StateGraph(FoilState); graph.add_node('validate', validate_foil); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()