from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BogieState(TypedDict): specs: dict; validation_log: List[str]; approved: bool; def validate_specs(state: BogieState): state['validation_log'].append('Verifying mechanical specs'); state['approved'] = all(k in state['specs'] for k in ['load_capacity', 'gauge_width']); return state; graph = StateGraph(BogieState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()