from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GlassState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_glass_spec(state: GlassState): specs = state['specs']; is_valid = all(key in specs for key in ['thermal_expansion', 'vacuum_rating']); return {'validation_report': 'Passed' if is_valid else 'Failed', 'approved': is_valid}; graph = StateGraph(GlassState); graph.add_node('validator', validate_glass_spec); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()