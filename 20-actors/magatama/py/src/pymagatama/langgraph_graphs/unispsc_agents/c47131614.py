from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_spec: dict; validated: bool; error: str; def validate_clamp(state: State): spec = state.get('input_spec', {}); return {'validated': 'diameter' in spec and 'material' in spec}; def process_procurement(state: State): return {'error': None} if state['validated'] else {'error': 'Missing required specs'}; graph = StateGraph(State); graph.add_node('validate', validate_clamp); graph.add_node('process', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); app = graph.compile()