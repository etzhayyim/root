from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DewateringState(TypedDict): specs: dict; validation_results: List[str]; def validate_flow_rate(state: DewateringState): return {'validation_results': ['Flow rate verified'] if 'flow_rate' in state['specs'] else ['Flow rate missing']}; def check_safety(state: DewateringState): return {'validation_results': state['validation_results'] + ['Safety check passed']}; graph = StateGraph(DewateringState); graph.add_node('validate', validate_flow_rate); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()