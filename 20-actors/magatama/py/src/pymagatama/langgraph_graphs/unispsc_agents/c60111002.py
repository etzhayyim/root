from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChartState(TypedDict): content: str; validated: bool; approved: bool; def check_quality(state: ChartState): return {'validated': len(state['content']) > 10}; def finalize_order(state: ChartState): return {'approved': state['validated']}; graph = StateGraph(ChartState); graph.add_node('check', check_quality); graph.add_node('finalize', finalize_order); graph.set_entry_point('check'); graph.add_edge('check', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()