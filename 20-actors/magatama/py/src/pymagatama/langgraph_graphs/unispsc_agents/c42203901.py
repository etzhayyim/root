from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DosimeterState(TypedDict): doc_id: str; data: dict; status: str; def validate_calib(state: DosimeterState): return {'status': 'validated' if 'cert' in state['data'] else 'rejected'}; def finalize_order(state: DosimeterState): return {'status': 'processed'}; graph = StateGraph(DosimeterState); graph.add_node('validate', validate_calib); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compile_graph = graph.compile()