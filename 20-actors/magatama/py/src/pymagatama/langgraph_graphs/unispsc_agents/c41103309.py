from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FlowAnalysisState(TypedDict): specs: dict; validation_results: List[str]; def validate_flow_specs(state: FlowAnalysisState): errors = []; if 'flow_rate' not in state['specs']: errors.append('Missing flow_rate'); return {'validation_results': errors}; def finalize_procurement(state: FlowAnalysisState): return {'validation_results': ['Approved']}; graph = StateGraph(FlowAnalysisState); graph.add_node('validate', validate_flow_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()