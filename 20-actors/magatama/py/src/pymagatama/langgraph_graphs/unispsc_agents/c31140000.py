from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MoldingState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(MoldingState); def validate_dimensions(state: MoldingState): return {'validation_report': ['Dimensions verified against engineering prints']}; def check_fire_rating(state: MoldingState): return {'validation_report': ['Fire rating compliance confirmed']}; graph.add_node('dimension_check', validate_dimensions); graph.add_node('fire_rating_check', check_fire_rating); graph.set_entry_point('dimension_check'); graph.add_edge('dimension_check', 'fire_rating_check'); graph.add_edge('fire_rating_check', END); graph = graph.compile()