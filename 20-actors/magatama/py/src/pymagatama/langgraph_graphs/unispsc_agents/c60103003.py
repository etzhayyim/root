from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BookState(TypedDict): content: str; is_aligned: bool; score: int; def validate_curriculum(state: BookState): state['is_aligned'] = 'curriculum' in state['content'].lower(); return state; def evaluate_quality(state: BookState): state['score'] = len(state['content']) > 1000 and 10 or 0; return state; graph = StateGraph(BookState); graph.add_node('validate', validate_curriculum); graph.add_node('evaluate', evaluate_quality); graph.add_edge('validate', 'evaluate'); graph.add_edge('evaluate', END); graph.set_entry_point('validate'); graph = graph.compile()