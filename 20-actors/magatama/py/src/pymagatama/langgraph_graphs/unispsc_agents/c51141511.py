from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_docs: List[str]; qc_verified: bool; compliance_status: str; def validate_docs(state: ProcureState): return {'qc_verified': True} if 'GMP' in state['vendor_docs'] else {'qc_verified': False}; def check_compliance(state: ProcureState): return {'compliance_status': 'CLEARED' if state['qc_verified'] else 'REJECTED'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_docs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()