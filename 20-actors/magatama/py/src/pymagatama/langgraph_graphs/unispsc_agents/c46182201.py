from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupportBeltState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SupportBeltState): errors = []; if not state['specs'].get('tensile_strength_rating'): errors.append('Missing tensile strength'); return {'validation_errors': errors}; def check_approval(state: SupportBeltState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(SupportBeltState); graph.add_node('validate', validate_specs); graph.add_node('approval', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); compiled_graph = graph.compile()