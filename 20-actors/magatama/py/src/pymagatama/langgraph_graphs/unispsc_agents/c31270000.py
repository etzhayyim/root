from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PartState(TypedDict): part_id: str; specs: dict; valid: bool; def validate_part(state: PartState): print(f'Validating specs for {state['part_id']}'); state['valid'] = True; return state; def export_check(state: PartState): print('Checking export control compliance'); return state; graph = StateGraph(PartState); graph.add_node('validate', validate_part); graph.add_node('export_check', export_check); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()