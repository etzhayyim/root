from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChopstickState(TypedDict): material: str; food_safe: bool; dimensions: List[float]; valid: bool; def validate_specs(state: ChopstickState): state['valid'] = state['food_safe'] and len(state['dimensions']) == 2; return state; graph = StateGraph(ChopstickState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()