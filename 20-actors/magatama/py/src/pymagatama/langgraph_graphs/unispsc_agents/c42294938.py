from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): input: str; validation_results: Annotated[list, operator.add]; status: str; def validate_spec(state: AgentState): state['validation_results'].append('Material validation passed'); return {'status': 'validated'}; def finalize_procurement(state: AgentState): return {'status': 'ready_for_order'}; graph = StateGraph(AgentState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()