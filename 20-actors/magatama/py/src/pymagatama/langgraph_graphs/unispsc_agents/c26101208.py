from langgraph.graph import StateGraph, END; from typing import TypedDict; class MotorState(TypedDict): specs: dict; validation: bool; graph = StateGraph(MotorState); def validate_specs(state: MotorState): return {'validation': all(k in state['specs'] for k in ['voltage', 'rpm', 'torque'])}; def classify_risk(state: MotorState): return {'validation': True} if state['validation'] else {'validation': False}; graph.add_node('validate', validate_specs); graph.add_node('risk_assessment', classify_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'risk_assessment'); graph.add_edge('risk_assessment', END); compiled_graph = graph.compile()