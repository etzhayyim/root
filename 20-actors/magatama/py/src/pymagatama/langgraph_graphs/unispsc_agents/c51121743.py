from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): ingredient_name: str; gmp_compliance: bool; purity_cert: bool; status: str; def validate_gmp(state: PharmState): return {'gmp_compliance': True} if state.get('gmp_compliance') else {'status': 'rejected'}; def validate_purity(state: PharmState): return {'purity_cert': True} if state.get('purity_cert') else {'status': 'pending_lab_test'}; graph = StateGraph(PharmState); graph.add_node('validate_gmp', validate_gmp); graph.add_node('validate_purity', validate_purity); graph.add_edge('validate_gmp', 'validate_purity'); graph.add_edge('validate_purity', END); graph.set_entry_point('validate_gmp'); graph = graph.compile()