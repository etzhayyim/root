from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: BearingState): return {"validation_report": "passed" if state["specs"].get("hardness") > 60 else "failed"}; graph = StateGraph(BearingState); graph.add_node("validator", validate_specs); graph.set_entry_point("validator"); graph.add_edge("validator", END); graph = graph.compile()