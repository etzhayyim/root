from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PurchaseState(TypedDict): vendor_id: str; item_specs: dict; validated: bool; def validate_specs(state: PurchaseState): state['validated'] = 'Connector Type' in state['item_specs']; return state; graph = StateGraph(PurchaseState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()