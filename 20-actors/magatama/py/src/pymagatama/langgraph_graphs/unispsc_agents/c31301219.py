from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: ForgingState): state['validation_results'].append('Material integrity validated'); return state; def validate_dimensions(state: ForgingState): state['validation_results'].append('Dimensional tolerances confirmed'); return state; workflow = StateGraph(ForgingState); workflow.add_node('material_check', validate_material); workflow.add_node('dimension_check', validate_dimensions); workflow.set_entry_point('material_check'); workflow.add_edge('material_check', 'dimension_check'); workflow.add_edge('dimension_check', END); graph = workflow.compile()