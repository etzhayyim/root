from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SwitchState(TypedDict): specs: dict; validation_report: list; status: str; def validate_specs(state: SwitchState): specs = state['specs']; errors = []; if specs.get('voltage', 0) < 0: errors.append('Invalid voltage'); return {'validation_report': errors, 'status': 'completed' if not errors else 'failed'}; graph = StateGraph(SwitchState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()