from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class OphthalmicScannerState(TypedDict): device_specs: dict; compliance_verified: bool; def validate_medical_standards(state: OphthalmicScannerState): return { 'compliance_verified': 'certificate_id' in state['device_specs'] }; def run_diagnostic_check(state: OphthalmicScannerState): return { 'compliance_verified': True }; graph = StateGraph(OphthalmicScannerState); graph.add_node('validate', validate_medical_standards); graph.add_node('check', run_diagnostic_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); compiled_graph = graph.compile()