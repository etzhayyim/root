from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: Annotated[list, operator.add]; status: str; def validate_cert(state: ProcureState): return {'status': 'validated'} if 'ISO13485' in state['items'] else {'status': 'rejected'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_cert); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()