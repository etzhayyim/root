from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableToolState(TypedDict): tools: List[str]; validation_passed: bool; def validate_specs(state: CableToolState): return {'validation_passed': len(state['tools']) > 0}; def process_order(state: CableToolState): return {'tools': [tool.upper() for tool in state['tools']]}; graph = StateGraph(CableToolState); graph.add_node('validate', validate_specs); graph.add_node('process', process_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()