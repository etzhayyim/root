from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(CastingState); def validate_specs(state: CastingState): state['validation_results'].append('Dimensional Check Passed'); return state; def check_compliance(state: CastingState): state['validation_results'].append('Material Compliance Verified'); return state; graph.add_node('validation', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validation'); graph.add_edge('validation', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()