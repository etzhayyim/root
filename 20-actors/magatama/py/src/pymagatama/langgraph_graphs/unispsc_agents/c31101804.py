from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): material_spec: str; dimensions: dict; quality_report: str; approved: bool; def validate_specs(state: CastState): state['approved'] = 'CF8M' in state['material_spec'] and 'NDT' in state['quality_report']; return state; builder = StateGraph(CastState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()