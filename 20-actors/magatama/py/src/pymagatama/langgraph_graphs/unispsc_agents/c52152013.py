from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): vendor_specs: dict; validated: bool; def check_food_safety(state: ProcureState): return {'validated': 'food_grade_cert' in state['vendor_specs']}; graph = StateGraph(ProcureState); graph.add_node('safety_check', check_food_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()