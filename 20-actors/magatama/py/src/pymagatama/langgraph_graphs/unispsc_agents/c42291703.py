from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): perforator_id: str; spec_validated: bool; sterilization_cert: str; def validate_spec(state: SurgicalState): state['spec_validated'] = 'ISO-13485' in state['sterilization_cert']; return state; def check_compliance(state: SurgicalState): print(f'Processing {state.perforator_id}'); return 'compliant' if state['spec_validated'] else 'rejected'; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END);