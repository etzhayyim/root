from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabelState(TypedDict): label_specs: dict; compliance_verified: bool; errors: List[str]; def validate_label(state: LabelState): state['compliance_verified'] = 'language_accuracy_certification' in state['label_specs']; return state; def check_compliance(state: LabelState): return 'valid' if state['compliance_verified'] else 'retry'; graph = StateGraph(LabelState); graph.add_node('validate', validate_label); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()