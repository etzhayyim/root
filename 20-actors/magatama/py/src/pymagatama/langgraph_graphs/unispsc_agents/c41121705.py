from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NMRState(TypedDict): tube_specs: dict; validation_results: List[str]; def validate_dimensions(state: NMRState): specs = state['tube_specs']; results = []; if specs.get('concentricity', 100) > 50: results.append('Concentricity out of spec'); return {'validation_results': results}; def generate_procurement_report(state: NMRState): return {'validation_results': state['validation_results'] + ['Report Generated']}; graph = StateGraph(NMRState); graph.add_node('validate', validate_dimensions); graph.add_node('report', generate_procurement_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()