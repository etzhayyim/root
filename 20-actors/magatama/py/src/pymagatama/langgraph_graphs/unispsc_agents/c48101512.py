from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GrillState(TypedDict): specs: dict; approved: bool; def validate_specs(state: GrillState): state['approved'] = 'BTU_rating' in state['specs'] and 'NSF_Certification' in state['specs']; return state; builder = StateGraph(GrillState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()