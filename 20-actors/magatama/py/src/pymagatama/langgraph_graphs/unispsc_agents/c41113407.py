from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec_data: dict; def validate_specs(state: State): return {'status': 'validated' if 'resolution' in state['spec_data'] else 'missing_data'}; def check_compliance(state: State): return {'status': 'compliant' if state['status'] == 'validated' else 'non-compliant'}; graph = StateGraph(State); graph.add_node('val', validate_specs); graph.add_node('comp', check_compliance); graph.set_entry_point('val'); graph.add_edge('val', 'comp'); graph.add_edge('comp', END); graph = graph.compile()