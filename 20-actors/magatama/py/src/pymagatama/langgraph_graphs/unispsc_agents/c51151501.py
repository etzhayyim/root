from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): chemical_name: str; gmp_certified: bool; quality_checks_passed: bool; def check_gmp(state: PharmaState): state['gmp_certified'] = True; return state; def validate_quality(state: PharmaState): state['quality_checks_passed'] = True; return state; graph = StateGraph(PharmaState); graph.add_node('certify', check_gmp); graph.add_node('assay', validate_quality); graph.set_entry_point('certify'); graph.add_edge('certify', 'assay'); graph.add_edge('assay', END); graph = graph.compile()