from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): content: str; validated: bool; compliance_report: str; def validate_purity(state: PharmState): state.update({'validated': True, 'compliance_report': 'Purity levels within USP parameters'}); return state; def check_regulatory(state: PharmState): state.update({'compliance_report': 'Regulation compliant for Miconazole nitrate'}); return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulatory); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()