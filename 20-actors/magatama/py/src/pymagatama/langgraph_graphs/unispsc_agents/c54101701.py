from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PicklingState(TypedDict): chemical_specs: dict; compliance_report: str; approved: bool; def validate_chemical_safety(state: PicklingState): state['approved'] = state['chemical_specs'].get('ph_level', 7) < 2; return state; def generate_report(state: PicklingState): state['compliance_report'] = 'Validation Complete' if state['approved'] else 'Hazard Breach'; return state; graph = StateGraph(PicklingState); graph.add_node('validate', validate_chemical_safety); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()