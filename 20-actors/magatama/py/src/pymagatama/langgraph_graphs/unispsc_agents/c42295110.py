from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_results: dict; graph = StateGraph(State); def validate_lighting_specs(state: State): state['validation_results'] = {'illuminance_ok': True}; return state; def check_regulatory_compliance(state: State): state['validation_results']['regulatory_ok'] = True; return state; graph.add_node('validate_specs', validate_lighting_specs); graph.add_node('check_regulations', check_regulatory_compliance); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', 'check_regulations'); graph.add_edge('check_regulations', END); graph.compile()