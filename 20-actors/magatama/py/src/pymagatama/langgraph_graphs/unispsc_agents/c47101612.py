from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): chemical_specs: dict; validated: bool; def validate_polyelec(state: ProcState): state['validated'] = all(k in state['chemical_specs'] for k in ['mw', 'ionic_type']); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_polyelec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()