from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: Annotated[list, operator.add]; validation_results: dict; graph = StateGraph(ProcureState); def validate_crusher(state): return {'validation_results': {'spec_compliant': True, 'safety_standard': 'ISO13485'}}; graph.add_node('validate', validate_crusher); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()