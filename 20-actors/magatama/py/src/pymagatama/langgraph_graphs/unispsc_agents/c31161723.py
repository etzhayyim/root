from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: Annotated[list, operator.add]; validation_status: bool; def validate_spec(state: ProcurementState): state['validation_status'] = all(['material' in item for item in state['items']]); return state; def check_compliance(state: ProcurementState): return 'compliant' if state['validation_status'] else 'rejected'; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_spec); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()