from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DieState(TypedDict): die_id: str; quality_score: float; is_approved: bool; workflow_steps: List[str]; def validate_die(state: DieState): state['workflow_steps'].append('Validating die geometry'); state['is_approved'] = state['quality_score'] > 0.95; return state; def export_compliance(state: DieState): state['workflow_steps'].append('Checking export controls'); return state; graph = StateGraph(DieState); graph.add_node('validate', validate_die); graph.add_node('compliance', export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()