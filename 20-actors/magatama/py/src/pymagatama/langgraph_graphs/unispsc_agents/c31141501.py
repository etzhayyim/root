from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): part_specs: dict; validation_report: list; status: str; def validate_specs(state: MoldState): state['validation_report'].append('Geometry checked'); return {'status': 'validated'}; def run_quality_check(state: MoldState): state['validation_report'].append('Material safety confirmed'); return {'status': 'approved'}; graph = StateGraph(MoldState); graph.add_node('validate', validate_specs); graph.add_node('qc', run_quality_check); graph.add_edge('validate', 'qc'); graph.add_edge('qc', END); graph.set_entry_point('validate'); graph = graph.compile()