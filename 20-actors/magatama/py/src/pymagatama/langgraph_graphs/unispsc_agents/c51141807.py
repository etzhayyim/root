from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): data: dict; valid: bool; def check_license(state: State): state['valid'] = 'license_number' in state['data'] and state['data']['license_number'] != ''; return state; def validate_inventory(state: State): return {'valid': state['data'].get('qty', 0) > 0}; graph = StateGraph(State); graph.add_node('check_license', check_license); graph.add_node('validate_inventory', validate_inventory); graph.set_entry_point('check_license'); graph.add_edge('check_license', 'validate_inventory'); graph.add_edge('validate_inventory', END); graph = graph.compile()