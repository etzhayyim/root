from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GumplantState(TypedDict): botanical_name: str; validation_passed: bool; quality_score: float; def identify_specie(state: GumplantState): state['validation_passed'] = state['botanical_name'] == 'Grindelia'; return state; def check_quality(state: GumplantState): state['quality_score'] = 1.0 if state['validation_passed'] else 0.0; return state; graph = StateGraph(GumplantState); graph.add_node('identify', identify_specie); graph.add_node('quality', check_quality); graph.add_edge('identify', 'quality'); graph.add_edge('quality', END); graph.set_entry_point('identify'); graph = graph.compile()