from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ThermometerState(TypedDict): data: dict; metadata: dict; validated: bool; def validate_spec(state: ThermometerState): return { 'validated': 'Accuracy_Range' in state['data'] }; def prepare_procurement(state: ThermometerState): return { 'metadata': {'status': 'Ready for Quote'} }; graph = StateGraph(ThermometerState); graph.add_node('validate', validate_spec); graph.add_node('process', prepare_procurement); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()