from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TruckProcurementState(TypedDict): truck_id: str; specs: dict; validation_passed: bool; def validate_specs(state: TruckProcurementState): state['validation_passed'] = 'load_capacity_tons' in state['specs'] and state['specs']['load_capacity_tons'] > 0; return state; def check_compliance(state: TruckProcurementState): return 'compliance_check' if state['validation_passed'] else END; graph = StateGraph(TruckProcurementState); graph.add_node('validate', validate_specs); graph.add_node('compliance_check', lambda s: s); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', check_compliance); graph.add_edge('compliance_check', END); graph = graph.compile()