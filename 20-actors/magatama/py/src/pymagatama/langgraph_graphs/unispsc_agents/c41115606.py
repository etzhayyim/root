from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ISEState(TypedDict): meter_specs: dict; validation_results: List[str]; def validate_meter(state: ISEState): results = []; specs = state.get('meter_specs', {}); if 'range' not in specs: results.append('Missing range criteria'); return {'validation_results': results}; graph = StateGraph(ISEState); graph.add_node('validate', validate_meter); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()