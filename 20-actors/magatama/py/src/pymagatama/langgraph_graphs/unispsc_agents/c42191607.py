from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; compliant: bool; def validate_curtain_standards(state: ProcurementState): state['compliant'] = all(item.get('fire_rated') for item in state['items']); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_curtain_standards); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()