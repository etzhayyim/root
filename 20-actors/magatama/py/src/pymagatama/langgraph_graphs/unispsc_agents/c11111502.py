from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from operator import add; class CommodityState(TypedDict): input_data: dict; analysis_results: list; status: str; def validate_purity(state: CommodityState): purity = state['input_data'].get('purity', 0); return {'status': 'PASSED' if purity >= 99.9 else 'FAILED'}; def check_compliance(state: CommodityState): return {'analysis_results': ['Compliance Check Completed']}; graph = StateGraph(CommodityState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()