from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EKGState(TypedDict): specs: dict; valid: bool; error: List[str]; def validate_specs(state: EKGState): state['valid'] = all(k in state['specs'] for k in ['iso_cert', 'material']); return state; graph = StateGraph(EKGState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()