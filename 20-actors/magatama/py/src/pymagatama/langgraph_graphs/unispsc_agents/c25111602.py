from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VesselState(TypedDict): vessel_id: str; specs: dict; compliant: bool; def validate_marine_specs(state: VesselState): state['compliant'] = state['specs'].get('pump_capacity') > 1000; return state; def route_inspection(state: VesselState): return 'approval' if state['compliant'] else 'reject'; graph = StateGraph(VesselState); graph.add_node('validate', validate_marine_specs); graph.add_conditional_edges('validate', route_inspection, {'approval': END, 'reject': END}); graph.set_entry_point('validate'); graph = graph.compile()