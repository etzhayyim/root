from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RescueCraftState(TypedDict): craft_specs: dict; inspection_logs: List[str]; approved: bool; def validate_specs(state: RescueCraftState): state['inspection_logs'].append('Checking hull integrity...'); state['approved'] = True if state['craft_specs'].get('buoyancy') else False; return state; graph = StateGraph(RescueCraftState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()