from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LibraryState(TypedDict): items: List[str]; validated: List[str]; def validator(state: LibraryState): validated = [item for item in state['items'] if 'ISBN' in item or 'DOI' in item]; return {'validated': validated}; graph = StateGraph(LibraryState); graph.add_node('validate', validator); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()