from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): hardware_spec: dict; validation_results: List[str]; def validate_durability(state: GraphState): state['validation_results'].append('Durability Check Passed'); return state; def check_compliance(state: GraphState): state['validation_results'].append('Compliance Check Passed'); return state; graph = StateGraph(GraphState); graph.add_node('durability', validate_durability); graph.add_node('compliance', check_compliance); graph.set_entry_point('durability'); graph.add_edge('durability', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()