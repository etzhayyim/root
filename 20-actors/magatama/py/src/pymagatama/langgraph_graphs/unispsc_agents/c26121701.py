from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WiringState(TypedDict): harness_specs: dict; validation_results: List[str]; graph = StateGraph(WiringState); def validate_continuity(state: WiringState): return {'validation_results': ['Continuity check passed']}; def check_insulation(state: WiringState): return {'validation_results': state['validation_results'] + ['Insulation resistance compliant']}; graph.add_node('continuity', validate_continuity); graph.add_node('insulation', check_insulation); graph.set_entry_point('continuity'); graph.add_edge('continuity', 'insulation'); graph.add_edge('insulation', END); graph = graph.compile()