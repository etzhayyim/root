from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClipNutState(TypedDict): n_type: str; specs: dict; approved: bool; def check_thickness(state: ClipNutState): state['approved'] = state['specs'].get('thickness', 0) > 0; return state; graph = StateGraph(ClipNutState); graph.add_node('validate_specs', check_thickness); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', END); graph = graph.compile()