from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): medicine: str; compliance_passed: bool; storage_validated: bool; def check_gmp(state: PharmState): return {'compliance_passed': True} if state['medicine'] == 'Famotidine' else {'compliance_passed': False}; def validate_storage(state: PharmState): return {'storage_validated': True}; graph = StateGraph(PharmState); graph.add_node('gmp_check', check_gmp); graph.add_node('storage_check', validate_storage); graph.set_entry_point('gmp_check'); graph.add_edge('gmp_check', 'storage_check'); graph.add_edge('storage_check', END); graph = graph.compile()