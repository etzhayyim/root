from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RiggingState(TypedDict): specs: dict; compliance: bool; valid: bool; def validate_specs(state: RiggingState): return {'valid': 'tensile-strength' in state['specs']}; def check_compliance(state: RiggingState): return {'compliance': state['valid'] and state['specs'].get('flammability-certification', False)}; graph = StateGraph(RiggingState); graph.add_node("validate", validate_specs); graph.add_node("compliance", check_compliance); graph.add_edge("validate", "compliance"); graph.add_edge("compliance", END); graph.set_entry_point("validate");