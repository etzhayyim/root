from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TinComponentState(TypedDict): dimensions: dict; material_spec: str; compliance_checked: bool; graph = StateGraph(TinComponentState); def validate_specs(state): return {'compliance_checked': state['material_spec'] == 'Sn-99.9'}; def check_dimensions(state): return {'compliance_checked': all(v < 0.05 for v in state['dimensions'].values())}; graph.add_node('validate', validate_specs); graph.add_node('check_dim', check_dimensions); graph.add_edge('validate', 'check_dim'); graph.add_edge('check_dim', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()