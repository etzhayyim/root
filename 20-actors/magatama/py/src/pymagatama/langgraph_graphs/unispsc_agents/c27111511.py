from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ToolSpecState(TypedDict): spec: dict; validation_errors: List[str]; def validate_cutters(state: ToolSpecState): errors = []; if state['spec'].get('insulation_rating', 0) < 1000: errors.append('Insufficient insulation for electrical work'); return {'validation_errors': errors}; graph = StateGraph(ToolSpecState); graph.add_node('validate', validate_cutters); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()