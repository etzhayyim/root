from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_data: dict; validation_results: Annotated[list, operator.add]; compliance_status: bool; def validate_purity(state: PharmState): purity = state['chemical_data'].get('purity', 0); return {'validation_results': ['Purity check passed'] if purity >= 99.0 else ['Purity check failed']}; def check_gmp(state: PharmState): gmp_cert = state['chemical_data'].get('gmp_cert', False); return {'compliance_status': gmp_cert}; graph = StateGraph(PharmState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_gmp', check_gmp); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_gmp'); graph.add_edge('check_gmp', END); graph = graph.compile()