from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ThermalState(TypedDict): specs: dict; validation_report: List[str]; def validate_thermal_specs(state: ThermalState): report = []; if state['specs'].get('temp_rating', 0) < 500: report.append('Low temperature rating'); return {'validation_report': report}; graph = StateGraph(ThermalState); graph.add_node('validate', validate_thermal_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()