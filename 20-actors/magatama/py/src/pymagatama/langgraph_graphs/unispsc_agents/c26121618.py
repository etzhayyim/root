from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableProcurementState(TypedDict): specs: dict; compliance_checked: bool; validation_log: List[str]; def validate_electrical_specs(state: CableProcurementState): state['validation_log'].append('Validating voltage and temp rating.'); state['compliance_checked'] = True; return state; graph = StateGraph(CableProcurementState); graph.add_node('validate', validate_electrical_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()