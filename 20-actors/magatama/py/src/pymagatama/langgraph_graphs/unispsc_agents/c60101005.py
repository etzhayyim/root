from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_items: List[str]; validation_passed: bool; def validate_kit(state: KitState): state['validation_passed'] = len(state['kit_items']) > 0; return state; graph = StateGraph(KitState); graph.add_node('validate', validate_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()