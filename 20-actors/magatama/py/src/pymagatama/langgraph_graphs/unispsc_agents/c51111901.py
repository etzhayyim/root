from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): status: str; activity: float; temp_log: List[float]; def validate_purity(state: ProcureState): return {'status': 'validated' if state['activity'] > 95.0 else 'rejected'}; graph = StateGraph(ProcureState); graph.add_node('check_activity', validate_purity); graph.set_entry_point('check_activity'); graph.add_edge('check_activity', END); graph = graph.compile()