from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TowelState(TypedDict): specs: dict; validation_report: List[str]; def validate_material(state: TowelState): state['validation_report'].append('Material verified for GSM compliance.'); return state; def check_towel_specs(state: TowelState): if state['specs'].get('gsm', 0) < 300: state['validation_report'].append('Quality below standard.'); return state; graph = StateGraph(TowelState); graph.add_node('validate_material', validate_material); graph.add_node('check_specs', check_towel_specs); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_specs'); graph.add_edge('check_specs', END); app = graph.compile()