from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_certs: List[str]; chemical_compliance: bool; safety_check: bool; def validate_compliance(state: ProcState): state['chemical_compliance'] = all(c in state['vendor_certs'] for c in ['FDA_Approval', 'ISO_13485']); return state; def verify_safety(state: ProcState): state['safety_check'] = state['chemical_compliance'] is True; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_compliance); graph.add_node('safety', verify_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()