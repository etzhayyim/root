from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoreState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_density(state: CoreState): state['valid'] = state['specs'].get('density', 0) > 0; return state; def check_compliance(state: CoreState): state['logs'].append('Export control check performed'); return state; graph = StateGraph(CoreState); graph.add_node('validate', validate_density); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()