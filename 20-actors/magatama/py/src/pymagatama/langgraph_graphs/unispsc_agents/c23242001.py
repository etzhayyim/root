from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): specs: dict; validation_passed: bool; def validate_machine_specs(state: MachineState): force = state['specs'].get('force', 0); state['validation_passed'] = force > 0; return state; graph = StateGraph(MachineState); graph.add_node('validate', validate_machine_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()