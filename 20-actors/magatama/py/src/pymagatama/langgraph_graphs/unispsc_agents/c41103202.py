from langgraph.graph import StateGraph, END; from typing import TypedDict; class LabWasherState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: LabWasherState): state['validation_passed'] = all(k in state['specs'] for k in ['chemical_resistance', 'water_flow']); return state; graph = StateGraph(LabWasherState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()