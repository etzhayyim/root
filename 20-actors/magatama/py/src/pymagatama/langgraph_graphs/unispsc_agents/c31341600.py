from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): assembly_specs: dict; validation_results: List[str]; def validate_bonding(state: AssemblyState): results = []; if 'bonding_method' not in state['assembly_specs']: results.append('Missing bonding method'); return {'validation_results': results}; def approve_assembly(state: AssemblyState): return {'validation_results': state['validation_results'] + ['Approved']}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_bonding); graph.add_node('approve', approve_assembly); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compile_graph = graph.compile()