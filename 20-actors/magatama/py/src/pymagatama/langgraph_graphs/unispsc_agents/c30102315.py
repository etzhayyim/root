from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlasticProfileState(TypedDict): profile_specs: dict; validation_results: List[str]; def validate_material(state: PlasticProfileState): state['validation_results'].append('Material verified'); return state; def check_dimensions(state: PlasticProfileState): state['validation_results'].append('Dimensions within tolerance'); return state; workflow = StateGraph(PlasticProfileState); workflow.add_node('material_check', validate_material); workflow.add_node('dimension_check', check_dimensions); workflow.set_entry_point('material_check'); workflow.add_edge('material_check', 'dimension_check'); workflow.add_edge('dimension_check', END); graph = workflow.compile()