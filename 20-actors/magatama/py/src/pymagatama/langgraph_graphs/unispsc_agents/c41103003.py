from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CryostatState(TypedDict): vendor_specs: dict; validation_passed: bool; def validate_specs(state: CryostatState): specs = state['vendor_specs']; min_temp = specs.get('min_temp', 0); state['validation_passed'] = min_temp <= 4.2; return state; graph = StateGraph(CryostatState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()