from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_id: str; quality_checks: Annotated[Sequence[str], operator.add]; safety_approved: bool; graph = StateGraph(ReagentState); def validate_purity(state: ReagentState): return { 'quality_checks': ['purity_verified'] }; def check_safety_compliance(state: ReagentState): return { 'safety_approved': True }; graph.add_node('purity_check', validate_purity); graph.add_node('safety_check', check_safety_compliance); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()