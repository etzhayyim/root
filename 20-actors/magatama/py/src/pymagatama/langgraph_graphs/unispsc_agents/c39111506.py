from langgraph.graph import StateGraph, END; from typing import TypedDict; class ChandelierState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ChandelierState): state['valid'] = 'SafetyCertification' in state['specs'] and state['specs']['WeightCapacity'] > 0; return state; graph = StateGraph(ChandelierState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()