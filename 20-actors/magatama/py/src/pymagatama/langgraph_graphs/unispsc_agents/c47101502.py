from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): data: dict; valid: bool; def validate_equipment(state: State): specs = state['data'].get('specs', {}); state['valid'] = specs.get('removal_efficiency', 0) > 95; return state; graph = StateGraph(State); graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()