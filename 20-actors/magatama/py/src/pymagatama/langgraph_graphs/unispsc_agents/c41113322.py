from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): data: dict; valid: bool; def validate_specs(state: AnalysisState): state['valid'] = all(k in state['data'] for k in ['range', 'sensor_type']); return state; graph = StateGraph(AnalysisState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()