from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PawlState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: PawlState): state['validation_results'].append('Dimensional check passed'); state['status'] = 'VALIDATED'; return state; def check_material(state: PawlState): state['validation_results'].append('Material cert verified'); return state; graph = StateGraph(PawlState); graph.add_node('validate_specs', validate_specs); graph.add_node('check_material', check_material); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()