from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_instruments(state: SurgicalState): validated = all(cert in state['certs'] for cert in ['ISO13485', 'CE-Mark']); return {'validated': validated}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_instruments); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()