from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_name: str; specifications: dict; valid: bool; def val_check(state: ToolState): state['valid'] = state['specifications'].get('weight_in_grams', 0) > 0; return state; graph = StateGraph(ToolState); graph.add_node('validate', val_check); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()