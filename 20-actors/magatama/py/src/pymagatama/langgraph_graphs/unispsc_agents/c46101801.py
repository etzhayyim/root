from langgraph.graph import StateGraph, END; from typing import TypedDict; class GunCaseState(TypedDict): model: str; compliance: bool; status: str; def validate_case(state: GunCaseState): state['compliance'] = state['model'] is not None; return {'status': 'validated'} if state['compliance'] else {'status': 'rejected'}; graph = StateGraph(GunCaseState); graph.add_node('validator', validate_case); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()