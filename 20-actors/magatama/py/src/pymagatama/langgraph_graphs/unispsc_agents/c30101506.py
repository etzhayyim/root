from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): specs: dict; valid: bool; def validate_specs(state: MaterialState): state['valid'] = 'Alloy Grade' in state['specs'] and 'Dimensional Tolerance' in state['specs']; return state; builder = StateGraph(MaterialState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()