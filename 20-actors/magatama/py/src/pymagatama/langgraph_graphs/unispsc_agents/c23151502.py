from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcessState(TypedDict): cutting_specs: dict; compliance_check: bool; graph = StateGraph(ProcessState); def validate_specs(state: ProcessState): state['compliance_check'] = state['cutting_specs'].get('power') > 1000; return state; def check_export(state: ProcessState): state['compliance_check'] = state['compliance_check'] and not state['cutting_specs'].get('is_restricted', True); return state; graph.add_node('validate', validate_specs); graph.add_node('export', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); app = graph.compile()