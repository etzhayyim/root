from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): specs: dict; valid: bool; def validate_switch(state: State): specs = state['specs']; state['valid'] = 'switching_temperature_range' in specs and 'contact_rating_voltage_current' in specs; return state; graph = StateGraph(State); graph.add_node('validate', validate_switch); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()