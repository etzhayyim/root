from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PedestalState(TypedDict): dimensions: dict; material: str; locking_mechanism: bool; approved: bool; def validate_specs(state: PedestalState): state['approved'] = all([state['dimensions'], state['material']]) return state; def check_locking(state: PedestalState): state['locking_mechanism'] = True; return state; graph = StateGraph(PedestalState); graph.add_node('validate', validate_specs); graph.add_node('security_check', check_locking); graph.set_entry_point('validate'); graph.add_edge('validate', 'security_check'); graph.add_edge('security_check', END); graph = graph.compile()