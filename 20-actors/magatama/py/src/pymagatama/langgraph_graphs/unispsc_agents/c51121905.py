from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): data: dict; status: str; def validate_regulatory(state: PharmState): state['status'] = 'VALIDATED' if 'registration' in state['data'] else 'REJECTED'; return state; def check_purity(state: PharmState): state['status'] = 'READY' if state['data'].get('purity', 0) > 99.0 else 'FAILED'; return state; graph = StateGraph(PharmState); graph.add_node('regulatory', validate_regulatory); graph.add_node('purity', check_purity); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'purity'); graph.add_edge('purity', END); graph = graph.compile()