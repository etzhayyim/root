from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LCDProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def check_specs(state: LCDProcurementState): specs = state.get('specs', {}); errors = []; if 'resolution' not in specs: errors.append('Resolution missing'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(LCDProcurementState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()