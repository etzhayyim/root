from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CleatState(TypedDict): specs: dict; valid: bool; error: str; def validate_load_capacity(state: CleatState): load = state['specs'].get('load_rating', 0); state['valid'] = load > 0; state['error'] = '' if state['valid'] else 'Invalid rating'; return state; graph = StateGraph(CleatState); graph.add_node('validate', validate_load_capacity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()