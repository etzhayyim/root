from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AmalgamState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(AmalgamState); def validate_spec(state: AmalgamState): return {'validation_results': ['ISO 7494-1 compliant'] if state['specs'].get('iso_cert') else ['Manual review needed']}; def check_material(state: AmalgamState): return {'validation_results': state['validation_results'] + ['Material Check Passed']}; graph.add_node('validate_spec', validate_spec); graph.add_node('check_material', check_material); graph.set_entry_point('validate_spec'); graph.add_edge('validate_spec', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()