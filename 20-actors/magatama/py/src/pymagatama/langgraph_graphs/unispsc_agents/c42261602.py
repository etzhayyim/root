from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BodyBagState(TypedDict): specs: dict; validation_passed: bool; report: str; def validate_specs(state: BodyBagState): state['validation_passed'] = state['specs'].get('load_capacity', 0) > 100; return state; graph = StateGraph(BodyBagState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()