from langgraph.graph import StateGraph, END; from typing import TypedDict; class GloveState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(GloveState); def validate_aql(state: GloveState): return {'compliance_check': state['specs'].get('aql_rating', 0) <= 2.5}; def finalize(state: GloveState): return state; graph.add_node('validate', validate_aql); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()