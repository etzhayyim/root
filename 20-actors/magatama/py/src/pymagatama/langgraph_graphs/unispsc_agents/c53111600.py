from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShoeProcurementState(TypedDict): items: List[str]; validation_results: dict; def validate_specs(state: ShoeProcurementState): return {'validation_results': {'passed': all(item for item in state['items'])}}; graph = StateGraph(ShoeProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()