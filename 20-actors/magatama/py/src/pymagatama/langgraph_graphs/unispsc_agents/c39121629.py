from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FuseBlockState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: FuseBlockState): specs = state['specs']; errors = []; if not specs.get('voltage', 0) > 0: errors.append('Invalid voltage'); if not specs.get('current', 0) > 0: errors.append('Invalid current'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(FuseBlockState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()