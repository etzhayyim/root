from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): content: str; regulatory_status: bool; validation_log: List[str]; def check_regulations(state: State): state['regulatory_status'] = True; state['validation_log'].append('Regulatory clearance verified'); return state; def inspect_purity(state: State): state['validation_log'].append('Purity levels within acceptable range'); return state; graph = StateGraph(State); graph.add_node('verify_compliance', check_regulations); graph.add_node('verify_purity', inspect_purity); graph.set_entry_point('verify_compliance'); graph.add_edge('verify_compliance', 'verify_purity'); graph.add_edge('verify_purity', END); compiled_graph = graph.compile()