from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PhenytoinState(TypedDict): status: str; purity: float; compliance: bool; def validate_quality(state: PhenytoinState): if state['purity'] >= 99.9: return {'status': 'approved'}; return {'status': 'rejected'}; def check_reg(state: PhenytoinState): return {'compliance': True}; graph = StateGraph(PhenytoinState); graph.add_node('quality', validate_quality); graph.add_node('regulatory', check_reg); graph.set_entry_point('quality'); graph.add_edge('quality', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()