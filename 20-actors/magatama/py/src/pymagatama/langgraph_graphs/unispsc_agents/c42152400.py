from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): material_specs: dict; compliance_verified: bool; def validate_regulatory_compliance(state: DentalState): state['compliance_verified'] = 'FDA_registration_number' in state['material_specs']; return state; def check_biocompatibility(state: DentalState): return state; graph = StateGraph(DentalState); graph.add_node('validate', validate_regulatory_compliance); graph.add_node('check_bio', check_biocompatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_bio'); graph.add_edge('check_bio', END); graph = graph.compile()