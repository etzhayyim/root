from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TriageState(TypedDict): tags_data: dict; compliance_score: int; def validate_spec(state: TriageState): state['compliance_score'] = 100 if 'iso_compliance_code' in state['tags_data'] else 0; return state; graph = StateGraph(TriageState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()