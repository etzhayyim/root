from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DistributionBoxState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: DistributionBoxState): state['validation_errors'] = []; if state['specs'].get('IP_Rating', 0) < 54: state['validation_errors'].append('Insufficient protection rating'); return state; def check_approval(state: DistributionBoxState): state['approved'] = len(state['validation_errors']) == 0; return state; graph = StateGraph(DistributionBoxState); graph.add_node('validate', validate_specs); graph.add_node('approve', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()