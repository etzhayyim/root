from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(ProcurementState); def validate_medical_grade(state: ProcurementState): state['validation_results'].append('Verifying ISO 13485 and sterilization'); return state; def check_compliance(state: ProcurementState): state['validation_results'].append('Compliance verified'); return state; graph.add_node('validate', validate_medical_grade); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()