from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): status: str; spec_check: bool; def validate_spec(state: ProcureState): return {'spec_check': True} if state['status'] == 'ready' else {'spec_check': False}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()