from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; compliance_check: bool; storage_temp: float; graph = StateGraph(PharmaState); def validate_license(state): return {'compliance_check': True}; def check_storage(state): return {'storage_temp': 20.0}; graph.add_node('validate', validate_license); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); compiled_graph = graph.compile()