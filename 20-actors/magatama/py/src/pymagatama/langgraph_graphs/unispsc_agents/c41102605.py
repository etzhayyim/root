from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AerationState(TypedDict): specs: dict; validation_report: list; status: str; def validate_specs(state: AerationState): specs = state['specs']; errs = []; if specs.get('power_consumption_watts', 0) > 1000: errs.append('High power consumption'); return {'validation_report': errs, 'status': 'validated' if not errs else 'flagged'}; workflow = StateGraph(AerationState); workflow.add_node('validate', validate_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()