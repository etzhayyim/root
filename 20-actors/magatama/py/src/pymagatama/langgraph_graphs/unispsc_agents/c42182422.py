from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): device_id: str; compliance_docs: List[str]; inspection_passed: bool; def validate_specs(state: State): state['inspection_passed'] = 'ISO13485' in state['compliance_docs'] and 'pressure_test' in state['compliance_docs']; return state; builder = StateGraph(State); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()