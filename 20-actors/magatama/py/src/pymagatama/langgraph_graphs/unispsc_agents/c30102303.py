from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProfileState(TypedDict): profile_specs: dict; is_valid: bool; def validate_specs(state: ProfileState): grade = state['profile_specs'].get('grade'); state['is_valid'] = grade in ['SS400', 'SM490']; return state; def check_compliance(state: ProfileState): return 'compliance_check' if state['is_valid'] else END; graph = StateGraph(ProfileState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()