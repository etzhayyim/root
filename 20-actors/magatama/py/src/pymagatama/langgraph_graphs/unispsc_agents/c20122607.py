from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ValveState(TypedDict): valve_id: str; spec: dict; compliance_passed: bool; def check_valve_compliance(state: ValveState): state['compliance_passed'] = 'pressure_rating_mpa' in state['spec'] and 'material_specification' in state['spec']; return state; def validate_manufacturing(state: ValveState): return 'compliance_passed' if state['compliance_passed'] else END; graph = StateGraph(ValveState); graph.add_node('check_compliance', check_valve_compliance); graph.add_node('validate', validate_manufacturing); graph.set_entry_point('check_compliance'); graph.add_edge('check_compliance', 'validate'); graph.add_edge('validate', END); graph = graph.compile()