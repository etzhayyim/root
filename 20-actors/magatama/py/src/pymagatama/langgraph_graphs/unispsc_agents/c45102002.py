from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): status: str; specs: dict; verified: bool; def check_mechanical(state: MachineState): return {'verified': True, 'status': 'inspected'}; def assess_risks(state: MachineState): return {'status': 'risk_assessed'}; graph = StateGraph(MachineState); graph.add_node('check', check_mechanical); graph.add_node('risk', assess_risks); graph.set_entry_point('check'); graph.add_edge('check', 'risk'); graph.add_edge('risk', END); compiled_graph = graph.compile()