from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PrefabState(TypedDict): vendor_docs: List[str]; qc_passed: bool; code_compliant: bool; def check_docs(state: PrefabState): return {'qc_passed': len(state['vendor_docs']) > 0}; def validate_compliance(state: PrefabState): return {'code_compliant': True}; graph = StateGraph(PrefabState); graph.add_node('check_docs', check_docs); graph.add_node('compliance', validate_compliance); graph.set_entry_point('check_docs'); graph.add_edge('check_docs', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()