from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class NetworkState(TypedDict): hardware_specs: dict; compliance_passed: bool; network_role: str; def validate_hw(state: NetworkState): print('Validating 2.5G HW...'); state['compliance_passed'] = True; return state; graph = StateGraph(NetworkState); graph.add_node('validate', validate_hw); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()