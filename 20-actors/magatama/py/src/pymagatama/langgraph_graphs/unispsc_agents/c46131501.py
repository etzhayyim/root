from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RocketState(TypedDict): rocket_specs: dict; validation_status: bool; export_license: str; def validate_physics(state: RocketState): state['validation_status'] = True if state['rocket_specs'].get('stages', 0) > 1 else False; return state; def check_export_compliance(state: RocketState): state['export_license'] = 'REQUIRED' if state['validation_status'] else 'N/A'; return state; graph = StateGraph(RocketState); graph.add_node('validate', validate_physics); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()