from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class QAState(TypedDict): data: dict; report: str; status: str; def validate_calib(state: QAState): state['status'] = 'Valid' if state['data'].get('cert_date') else 'Invalid'; return state; def generate_report(state: QAState): state['report'] = 'Performance metrics archived.'; return state; graph = StateGraph(QAState); graph.add_node('validate', validate_calib); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()