from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeSpecs(TypedDict): tube_type: str; dimensions: dict; compliance: bool; status: str; def validate_specs(state: TubeSpecs) -> TubeSpecs: state['status'] = 'VALIDATED' if state['dimensions'].get('od') > 0 else 'INVALID'; return state; def check_compliance(state: TubeSpecs) -> TubeSpecs: state['compliance'] = True; return state; graph = StateGraph(TubeSpecs); graph.add_node('validate', validate_specs); graph.add_node('compliance_check', check_compliance); graph.add_edge('validate', 'compliance_check'); graph.add_edge('compliance_check', END); graph.set_entry_point('validate'); graph = graph.compile()