from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IronStripState(TypedDict): specs: dict; validation_results: List[str]; def validate_material_specs(state: IronStripState): results = []; required_fields = ['tensile_strength', 'chemical_comp']; for field in required_fields: if field not in state['specs']: results.append(f'Missing {field}'); state['validation_results'] = results; return state; graph = StateGraph(IronStripState); graph.add_node('validate', validate_material_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()