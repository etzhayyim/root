from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlantState(TypedDict): species_info: dict; quarantine_status: bool; inspection_report: str; def validate_biological_safety(state: PlantState) -> PlantState: state['quarantine_status'] = True; return state; def inspection_step(state: PlantState) -> PlantState: state['inspection_report'] = 'Pass'; return state; graph = StateGraph(PlantState); graph.add_node('validate', validate_biological_safety); graph.add_node('inspect', inspection_step); graph.set_entry_point('validate'); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph = graph.compile()