from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrillingPartState(TypedDict): part_id: str; specs: dict; valid: bool; def validate_specs(state: DrillingPartState): state['valid'] = 'material' in state['specs'] and 'strength' in state['specs']; return state; def check_compliance(state: DrillingPartState): return 'approved' if state['valid'] else 'flagged'; graph = StateGraph(DrillingPartState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()