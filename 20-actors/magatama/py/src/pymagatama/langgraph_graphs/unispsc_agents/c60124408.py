from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MetalProcurementState(TypedDict): composition: str; purity: float; status: str; def validate_composition(state: MetalProcurementState): return {'status': 'validated' if state['purity'] >= 0.95 else 'rejected'}; def check_specs(state: MetalProcurementState): return {'status': 'approved' if state['status'] == 'validated' else 'manual_review'}; graph = StateGraph(MetalProcurementState); graph.add_node('validate', validate_composition); graph.add_node('check', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()