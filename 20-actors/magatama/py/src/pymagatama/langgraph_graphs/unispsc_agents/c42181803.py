from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OximeterState(TypedDict): sensor_data: dict; validation_errors: List[str]; class Validator: @staticmethod def check_biocompatibility(state: OximeterState): if 'iso_10993' not in state['sensor_data']: state['validation_errors'].append('Missing ISO 10993'); return state graph = StateGraph(OximeterState); graph.add_node('validate', Validator.check_biocompatibility); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()