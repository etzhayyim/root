from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): content: str; validated: bool; compliance: List[str]; def validate_content(state: MaterialState): return {'validated': True, 'compliance': ['medical_review_passed']}; def finalize_materials(state: MaterialState): return {'compliance': state['compliance'] + ['approved_for_distribution']}; graph = StateGraph(MaterialState); graph.add_node('validate', validate_content); graph.add_node('finalize', finalize_materials); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()