from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SignageState(TypedDict): items: List[dict]; validation_results: List[bool]; def validate_material(state: SignageState): return { "validation_results": [item.get("material") is not None for item in state["items"]] }; graph = StateGraph(SignageState); graph.add_node("validate", validate_material); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()