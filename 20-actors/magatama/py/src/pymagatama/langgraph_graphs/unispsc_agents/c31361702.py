from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: AssemblyState): print("Validating carbon steel specs..."); state['valid'] = 'material_grade' in state['specs']; return state; def route_logic(state: AssemblyState): return 'end' if state['valid'] else 'error'; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()