from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CartState(TypedDict): specs: dict; valid: bool;  def validate_load_capacity(state: CartState): state['valid'] = state['specs'].get('load_capacity_kg', 0) > 0; return state;  def check_stability(state: CartState): print('Checking wheelbase stability requirements...'); return state;  graph = StateGraph(CartState); graph.add_node('validate', validate_load_capacity); graph.add_node('stability', check_stability); graph.set_entry_point('validate'); graph.add_edge('validate', 'stability'); graph.add_edge('stability', END); app = graph.compile()