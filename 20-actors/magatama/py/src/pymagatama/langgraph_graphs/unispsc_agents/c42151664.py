from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalDiscState(TypedDict): specs: dict; validation_results: List[str]; def validate_disc_specs(state: DentalDiscState): rpm = state['specs'].get('rpm', 0); return {'validation_results': ['RPM Valid' if rpm < 30000 else 'RPM Exceeds Safety Limit']}; graph = StateGraph(DentalDiscState); graph.add_node('validate', validate_disc_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()