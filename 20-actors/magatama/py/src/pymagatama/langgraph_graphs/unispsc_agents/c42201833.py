from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcureState(TypedDict): specs: dict; compliance: bool; approved: bool; def check_compliance(state: ProcureState): return {'compliance': state['specs'].get('radiation_shielding') == 'Lead-Equivalence-Certified'}; def validate_mechanics(state: ProcureState): return {'approved': state['compliance'] and state['specs'].get('cycle_test', 0) > 100000}; graph = StateGraph(ProcureState); graph.add_node('check', check_compliance); graph.add_node('validate', validate_mechanics); graph.add_edge('check', 'validate'); graph.add_edge('validate', END); graph.set_entry_point('check'); graph = graph.compile()