from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): tube_specs: dict; validation_results: List[str]; def validate_dimensions(state: AssemblyState): state['validation_results'].append('DIM_CHECK_PASS'); return state; def check_compliance(state: AssemblyState): state['validation_results'].append('COMPLIANCE_CERT_VERIFIED'); return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_dimensions); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()