from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PovidoneState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(PovidoneState); def validate_quality(state: PovidoneState): status = 'verified' if state['specs'].get('purity') > 99.0 else 'rejected'; return {'validation_results': [f'Purity check: {status}']}; graph.add_node('quality_check', validate_quality); graph.set_entry_point('quality_check'); graph.add_edge('quality_check', END); graph = graph.compile()