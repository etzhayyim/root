from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): chemical_batch_id: str; purity_level: float; status: str; safety_checked: bool; def validate_safety(state: CatalystState) -> dict: return {'safety_checked': state['purity_level'] > 0.95}; def update_inventory(state: CatalystState) -> dict: return {'status': 'processed'}; graph = StateGraph(CatalystState); graph.add_node('safety_check', validate_safety); graph.add_node('inventory', update_inventory); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'inventory'); graph.add_edge('inventory', END); graph = graph.compile()