from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StructureState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: StructureState): state['validation_passed'] = all(k in state['specs'] for k in ['material', 'torque']); return state; def finalize_order(state: StructureState): return {'status': 'processed'}; graph = StateGraph(StructureState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()