from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): showcase_config: dict; validated: bool; app = StateGraph(State); def validate_specs(state: State): state['validated'] = all(key in state['showcase_config'] for key in ['dimensions_mm', 'glass_type']); return state; def finalize_order(state: State): return {'validated': True}; app.add_node('validate', validate_specs); app.add_node('finalize', finalize_order); app.set_entry_point('validate'); app.add_edge('validate', 'finalize'); app.add_edge('finalize', END); graph = app.compile()