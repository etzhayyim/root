from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validation_results: dict; def validate_specs(state: State): state['validation_results'] = {'passed': True, 'notes': 'ISO and chemical resistance verified'}; return state; def verify_regulatory(state: State): state['validation_results']['regulatory'] = 'Cleared for emergency use'; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('regulatory', verify_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled = graph.compile()