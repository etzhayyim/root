from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ProcureState): state['valid'] = 'Material Grade' in state['specs'] and 'Tensile Strength' in state['specs']; return state; def finalize(state: ProcureState): return {'finalized': True}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()