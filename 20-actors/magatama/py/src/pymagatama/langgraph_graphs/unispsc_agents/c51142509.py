from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class APIProcurementState(TypedDict): ingredient: str; purity: float; compliance: bool; approved: bool; def validate_purity(state): return {'approved': state['purity'] >= 99.9}; def check_compliance(state): return {'compliance': True}; graph = StateGraph(APIProcurementState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()