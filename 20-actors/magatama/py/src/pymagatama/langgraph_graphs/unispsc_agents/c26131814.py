from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgeArresterState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SurgeArresterState): specs = state.get('specs', {}); errors = []; if specs.get('MCOV', 0) < 100: errors.append('Voltage rating below substation standard'); return {'validation_errors': errors}; def finalize_procurement(state: SurgeArresterState): return {'approved': len(state.get('validation_errors', [])) == 0}; graph = StateGraph(SurgeArresterState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()