from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SprayerState(TypedDict): specs: dict; compliance_check: bool; ready_for_procurement: bool; def validate_specs(state: SprayerState): state['compliance_check'] = 'Operating pressure' in state['specs'] and state['specs']['Operating pressure'] > 0; return state; graph = StateGraph(SprayerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()