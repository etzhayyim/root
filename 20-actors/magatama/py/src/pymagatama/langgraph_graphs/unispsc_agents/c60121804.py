from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InkState(TypedDict): ink_type: str; specs: dict; valid: bool; def validate_specs(state: InkState): state['valid'] = 'OEKO-TEX' in state['specs'].get('certs', []) and state['specs'].get('viscosity') > 0; return state; graph = StateGraph(InkState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()