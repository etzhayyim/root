from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; compliance_check: bool; def validate_purity(state: PharmState): return {'compliance_check': state['purity'] >= 99.0}; def approve_procurement(state: PharmState): print(f'Procurement approved for {state["chemical_name"]}'); return {'compliance_check': True}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()