from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_data: dict; compliance_checks: List[str]; approved: bool; def validate_compliance(state: ProcureState): state['compliance_checks'].append('Regulatory ID Verified'); state['approved'] = True; return state; graph = StateGraph(ProcureState); graph.add_node('verify', validate_compliance); graph.set_entry_point('verify'); graph.add_edge('verify', END); compiled_graph = graph.compile()