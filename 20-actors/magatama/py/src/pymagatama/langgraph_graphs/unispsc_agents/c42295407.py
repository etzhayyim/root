from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MaskState(TypedDict): vendor: str; certification_valid: bool; bfe_rating: float; approved: bool; def validate_certification(state: MaskState): state['certification_valid'] = True if state.get('certification') == 'ISO13485' else False; return state; def check_bfe(state: MaskState): state['approved'] = state['bfe_rating'] >= 95.0; return state; graph = StateGraph(MaskState); graph.add_node('validate', validate_certification); graph.add_node('check_bfe', check_bfe); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_bfe'); graph.add_edge('check_bfe', END); graph = graph.compile()