from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EpiduralState(TypedDict): tray_id: str; sterility_verified: bool; compliance_docs: List[str]; def verify_sterility(state: EpiduralState) -> EpiduralState: state['sterility_verified'] = True; return state; def check_compliance(state: EpiduralState) -> EpiduralState: state['compliance_docs'].append('ISO_13485_CERT'); return state; graph = StateGraph(EpiduralState); graph.add_node('verify', verify_sterility); graph.add_node('compliance', check_compliance); graph.set_entry_point('verify'); graph.add_edge('verify', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()