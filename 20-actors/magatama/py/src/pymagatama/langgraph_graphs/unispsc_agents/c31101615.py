from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): content: str; specs: dict; valid: bool; def validate_lead_castings(state: State): state['valid'] = all(k in state['specs'] for k in ['purity', 'dimensions']); return state; graph = StateGraph(State); graph.add_node('validate', validate_lead_castings); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()