from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): cold_chain_verified: bool; inventory_data: dict; documentation: List[str]; def verify_cold_chain(state: PharmaState): state['cold_chain_verified'] = True if state.get('temp_log') else False; return state; def validate_docs(state: PharmaState): state['documentation'].append('Cert_Verified'); return state; graph = StateGraph(PharmaState); graph.add_node('verify_chain', verify_cold_chain); graph.add_node('validate_docs', validate_docs); graph.add_edge('verify_chain', 'validate_docs'); graph.add_edge('validate_docs', END); graph.set_entry_point('verify_chain'); app = graph.compile()