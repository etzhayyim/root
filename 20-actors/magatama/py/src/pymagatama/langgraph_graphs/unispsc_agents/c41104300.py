from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FermenterState(TypedDict): vendor: str; specs: dict; validation_status: bool; graph = StateGraph(FermenterState); def validate_specs(state: FermenterState): return {'validation_status': 'capacity' in state['specs'] and 'material' in state['specs']}; def check_dual_use(state: FermenterState): return {'validation_status': True}; graph.add_node('val', validate_specs); graph.add_node('export', check_dual_use); graph.set_entry_point('val'); graph.add_edge('val', 'export'); graph.add_edge('export', END); app = graph.compile()