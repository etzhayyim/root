from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MedicalKitState(TypedDict): kit_data: dict; compliance_checked: bool; status: str; def validate_certifications(state: MedicalKitState): state['compliance_checked'] = 'ISO13485_Cert' in state['kit_data']; return state; def check_regulatory_status(state: MedicalKitState): state['status'] = 'APPROVED' if state['compliance_checked'] else 'REJECTED'; return state; graph = StateGraph(MedicalKitState); graph.add_node('validate', validate_certifications); graph.add_node('regulatory', check_regulatory_status); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); graph = graph.compile()