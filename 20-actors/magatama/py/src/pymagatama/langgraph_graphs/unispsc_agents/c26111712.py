from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BatteryState(TypedDict): capacity: float; voltage: float; safety_cert: bool; approved: bool; def validate_specs(state: BatteryState): state['approved'] = state['capacity'] > 0 and state['safety_cert']; return state; builder = StateGraph(BatteryState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()