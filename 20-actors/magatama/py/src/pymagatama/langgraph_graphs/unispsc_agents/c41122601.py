from langgraph.graph import StateGraph, END; from typing import TypedDict; class SlideState(TypedDict): specs: dict; validation_status: bool; def validate_slides(state: SlideState): state['validation_status'] = 'dimensions' in state['specs'] and 'material' in state['specs']; return state; builder = StateGraph(SlideState); builder.add_node('validate', validate_slides); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()