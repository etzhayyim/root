from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HotCellState(TypedDict): device_id: str; integrity_check: bool; export_control_cleared: bool; graph = StateGraph(HotCellState); def check_integrity(state: HotCellState): return {'integrity_check': True}; def verify_export(state: HotCellState): return {'export_control_cleared': True}; graph.add_node('integrity', check_integrity); graph.add_node('export', verify_export); graph.add_edge('integrity', 'export'); graph.add_edge('export', END); graph.set_entry_point('integrity'); compiled_graph = graph.compile()