from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OzoneState(TypedDict): ozone_output: float; safety_check: bool; status: str; def validate_specs(state: OzoneState): return {'status': 'validated' if state['ozone_output'] > 0 else 'error'}; def check_safety(state: OzoneState): return {'safety_check': True}; graph = StateGraph(OzoneState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()