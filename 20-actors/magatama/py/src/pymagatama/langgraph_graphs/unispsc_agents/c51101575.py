from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class VialState(TypedDict): vial_id: str; quality_metrics: Dict[str, float]; workflow_steps: List[str]; def validate_vial(state: VialState): state['workflow_steps'].append('VALIDATING_DIMENSIONS'); return state; def sterilization_check(state: VialState): state['workflow_steps'].append('VERIFYING_STERILITY_REPORT'); return state; graph = StateGraph(VialState); graph.add_node('validate', validate_vial); graph.add_node('sterilize', sterilization_check); graph.add_edge('validate', 'sterilize'); graph.add_edge('sterilize', END); graph.set_entry_point('validate'); graph = graph.compile()