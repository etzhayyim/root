from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ALSProcurementState(TypedDict): drug_name: str; compliance_docs: List[str]; verified: bool; def validate_regulatory(state: ALSProcurementState): print(f'Validating: {state[drug_name]}'); state['verified'] = True; return state; def cold_chain_audit(state: ALSProcurementState): print('Auditing cold chain requirements...'); return state; graph = StateGraph(ALSProcurementState); graph.add_node('regulatory', validate_regulatory); graph.add_node('cold_chain', cold_chain_audit); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'cold_chain'); graph.add_edge('cold_chain', END); graph = graph.compile()