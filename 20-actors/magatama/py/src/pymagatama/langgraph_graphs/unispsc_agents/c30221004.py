from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ShopBuildState(TypedDict): site_specs: str; compliance_docs: List[str]; inspection_status: str; def validate_engineering(state: ShopBuildState): return {'inspection_status': 'validated'} if 'structural_stamp' in state['compliance_docs'] else {'inspection_status': 'rejected'}; graph = StateGraph(ShopBuildState); graph.add_node('validate_eng', validate_engineering); graph.set_entry_point('validate_eng'); graph.add_edge('validate_eng', END); graph = graph.compile()