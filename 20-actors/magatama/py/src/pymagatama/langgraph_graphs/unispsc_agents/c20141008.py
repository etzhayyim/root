from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MiningState(TypedDict): drill_specs: dict; validation_results: List[str]; def validate_material(state: MiningState): return {'validation_results': ['Material check passed']}; def perform_quality_check(state: MiningState): return {'validation_results': ['Hardness test compliant']}; graph = StateGraph(MiningState); graph.add_node('material_validation', validate_material); graph.add_node('quality_assurance', perform_quality_check); graph.set_entry_point('material_validation'); graph.add_edge('material_validation', 'quality_assurance'); graph.add_edge('quality_assurance', END); compiled_graph = graph.compile()