from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): metal_type: str; purity_level: float; weight: float; is_verified: bool; def validate_purity(state: CastingState): state['is_verified'] = state['purity_level'] >= 0.999; return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()