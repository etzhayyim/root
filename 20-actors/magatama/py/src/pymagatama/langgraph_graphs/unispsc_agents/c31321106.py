from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): spec: dict; validated: bool; app_data: str; def validate_spec(state: ProcureState): return {'validated': 'Material Composition' in state['spec']}; def route_procurement(state: ProcureState): return 'end' if state['validated'] else 'error'; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()