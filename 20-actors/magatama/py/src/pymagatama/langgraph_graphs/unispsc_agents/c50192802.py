from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FoodProcurementState(TypedDict): items: List[str]; inspection_passed: bool; temp_logs: List[float]; graph = StateGraph(FoodProcurementState); def validate_temp(state: FoodProcurementState): return {'inspection_passed': all(t <= -18.0 for t in state['temp_logs'])}; def finalize_order(state: FoodProcurementState): return {'inspection_passed': state['inspection_passed']}; graph.add_node('validate', validate_temp); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()