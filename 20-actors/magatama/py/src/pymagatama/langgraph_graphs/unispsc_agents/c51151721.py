from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; purity: float; compliant: bool; def validate_purity(state: DrugState): state['compliant'] = state['purity'] >= 99.0; return state; def check_gmp(state: DrugState): print('Checking GMP status...'); return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('gmp', check_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', 'gmp'); graph.add_edge('gmp', END); compiled_graph = graph.compile()