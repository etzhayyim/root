from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UroState(TypedDict): device_id: str; certification: bool; status: str; def validate_cert(state: UroState): return {'status': 'approved' if state['certification'] else 'rejected'}; def process_dilator(state: UroState): return {'status': 'processed'}; graph = StateGraph(UroState); graph.add_node('validate', validate_cert); graph.add_node('process', process_dilator); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()