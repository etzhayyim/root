from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class WasteState(TypedDict): input_data: str; safety_check: bool; compliance_status: str; def validate_regulatory_code(state: WasteState): return {'safety_check': True, 'compliance_status': 'IAEA-Validated'}; def verify_containment(state: WasteState): return {'compliance_status': 'Containment-Verified'}; graph = StateGraph(WasteState); graph.add_node('regulatory_check', validate_regulatory_code); graph.add_node('containment_audit', verify_containment); graph.set_entry_point('regulatory_check'); graph.add_edge('regulatory_check', 'containment_audit'); graph.add_edge('containment_audit', END); graph = graph.compile()