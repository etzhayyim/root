from langgraph.graph import StateGraph, END; from typing import TypedDict; class TankState(TypedDict): capacity: float; material: str; validated: bool; def validate_spec(state: TankState): state['validated'] = state['capacity'] > 0 and bool(state['material']); return state; def check_compliance(state: TankState): print('Checking compliance...'); return 'validated' if state['validated'] else 'failed'; graph = StateGraph(TankState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()