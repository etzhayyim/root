from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): chemical_name: str; validation_passed: bool; logs: List[str]; def validate_pharmaceutical(state: PharmaState): print(f'Validating {state[chemical_name]}'); return {'validation_passed': True, 'logs': ['Purity verified', 'Storage specs met']}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_pharmaceutical); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()