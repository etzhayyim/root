from langgraph.graph import StateGraph, END; from typing import TypedDict; class ShowerCapState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: ShowerCapState): state['compliant'] = 'water_resistance' in state['specs'] and state['specs']['water_resistance'] > 80; return state; graph = StateGraph(ShowerCapState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()