from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RefuelingState(TypedDict): specs: dict; checks: List[str]; approved: bool; def validate_specs(state: RefuelingState): specs = state['specs']; checks = []; is_ok = True; if 'explosion_proof_rating' not in specs: is_ok = False; checks.append('Missing explosion rating'); return {'checks': checks, 'approved': is_ok}; builder = StateGraph(RefuelingState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()