from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): unit_id: str; specs_verified: bool; def validate_optics(state: ProcureState): state[specs_verified] = True; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_optics); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()