from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class OphthalmicState(TypedDict): lens_specs: dict; validation_report: list; status: str; def validate_optical_params(state: OphthalmicState): print('Validating diopter and refractive index...'); return {'validation_report': ['Optical verification passed']}; def check_regulatory_compliance(state: OphthalmicState): print('Checking ISO 13485...'); return {'status': 'COMPLIANT'}; graph = StateGraph(OphthalmicState); graph.add_node('validate', validate_optical_params); graph.add_node('compliance', check_regulatory_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()