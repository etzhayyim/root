from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProbeState(TypedDict): input_data: dict; validation_score: float; approved: bool; def validate_probe_spec(state: ProbeState): state['validation_score'] = 1.0 if 'precision' in state['input_data'] else 0.0; return state; def check_compliance(state: ProbeState): state['approved'] = state['validation_score'] > 0.9; return state; graph = StateGraph(ProbeState); graph.add_node('validate', validate_probe_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()