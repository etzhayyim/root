from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExcavationState(TypedDict): site_id: str; depth_requirements: float; hazard_log: List[str]; def validate_site(state: ExcavationState): print(f'Validating site: {state["site_id"]}'); return {'hazard_log': ['Safety Clearance Checked']}; def execute_dig(state: ExcavationState): print(f'Commencing dig to {state["depth_requirements"]}m'); return {'hazard_log': state['hazard_log'] + ['Dig Initiated']}; graph = StateGraph(ExcavationState); graph.add_node('validate', validate_site); graph.add_node('operate', execute_dig); graph.set_entry_point('validate'); graph.add_edge('validate', 'operate'); graph.add_edge('operate', END); compiled_graph = graph.compile()