from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class CarbonFiberState(TypedDict): tensile_strength: float; modulus: float; validation_passed: bool; processing_log: Annotated[list, add_messages]; def validate_specs(state: CarbonFiberState): state['validation_passed'] = state['tensile_strength'] > 3000.0 and state['modulus'] > 200.0; return {'validation_passed': state['validation_passed'], 'processing_log': ['Specs validated']}; def run_workflow(state: CarbonFiberState): return {'processing_log': ['Material ready for structural integration']}; graph = StateGraph(CarbonFiberState); graph.add_node('validate', validate_specs); graph.add_node('process', run_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()