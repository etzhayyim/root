from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): status: str; spec_data: dict; approved: bool; def validate_medical_grade(state: State): state['approved'] = 'ISO_13485' in state['spec_data']; return state; def check_regulatory(state: State): state['status'] = 'COMPLIANT' if state['approved'] else 'REJECTED'; return state; graph = StateGraph(State); graph.add_node('validate', validate_medical_grade); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()