from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): input_data: dict; validation_errors: Annotated[Sequence[str], operator.add]; compliance_status: bool; def validate_chemical_specs(state: ProcurementState): errors = []; if not state['input_data'].get('cas_number'): errors.append('Missing CAS number'); return {'validation_errors': errors}; def check_compliance(state: ProcurementState): return {'compliance_status': len(state['validation_errors']) == 0}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_chemical_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()