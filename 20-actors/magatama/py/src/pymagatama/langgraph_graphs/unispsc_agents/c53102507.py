from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HangerState(TypedDict): specs: dict; valid: bool; def validate_hanger(state: HangerState): state['valid'] = state['specs'].get('load_capacity', 0) > 0; return state; graph = StateGraph(HangerState); graph.add_node('validate', validate_hanger); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()