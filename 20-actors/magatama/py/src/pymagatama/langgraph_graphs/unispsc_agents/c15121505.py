from typing import TypedDict, Annotated, List, Union; from langgraph.graph import StateGraph, END; class AcidProcurementState(TypedDict): chemical_name: str; purity: float; hazard_verified: bool; compliance_checks: List[str]; def validate_purity(state: AcidProcurementState) -> AcidProcurementState: state['compliance_checks'].append('Purity > 98%'); state['hazard_verified'] = True; return state; def final_approval(state: AcidProcurementState) -> AcidProcurementState: state['compliance_checks'].append('Safety Protocol Cleared'); return state; graph = StateGraph(AcidProcurementState); graph.add_node('validate', validate_purity); graph.add_node('approve', final_approval); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); app = graph.compile()