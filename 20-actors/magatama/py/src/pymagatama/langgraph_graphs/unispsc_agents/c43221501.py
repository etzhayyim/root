from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): config: dict; approval: bool; graph = StateGraph(State); def validate_config(state: State): return {'approval': 'IVR_script' in state['config']}; def process_workflow(state: State): print('Deploying IVR sequences'); return {'approval': True}; graph.add_node('validate', validate_config); graph.add_node('deploy', process_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'deploy'); graph.add_edge('deploy', END); compiled_graph = graph.compile()