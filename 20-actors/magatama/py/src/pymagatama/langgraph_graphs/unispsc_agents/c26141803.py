from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShieldingState(TypedDict): specs: dict; compliance_status: bool; validation_log: List[str]; def validate_shielding(state: ShieldingState): state['compliance_status'] = state['specs'].get('lead_eq', 0) > 50; state['validation_log'].append('Shielding thickness verified.'); return state; def check_regulatory(state: ShieldingState): state['validation_log'].append('Export control check performed.'); return state; graph = StateGraph(ShieldingState); graph.add_node('validate', validate_shielding); graph.add_node('compliance', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.compile()