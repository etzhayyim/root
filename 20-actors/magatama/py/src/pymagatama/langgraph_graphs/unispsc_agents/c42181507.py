from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MedicalDeviceState(TypedDict): device_id: str; quality_checks: List[str]; approved: bool; def validate_optics(state: MedicalDeviceState): state['quality_checks'].append('optical_validation'); return state; def check_regulatory(state: MedicalDeviceState): state['approved'] = True; return state; graph = StateGraph(MedicalDeviceState); graph.add_node('validate', validate_optics); graph.add_node('compliance', check_regulatory); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()