from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_specs(state: AgentState): material = state['input_data'].get('material', ''); volume = state['input_data'].get('volume', 0); results = ['Validated' if material == 'medical-grade-silicone' and 0 < volume <= 500 else 'Invalid']; return {'validation_results': results, 'status': 'COMPLETE'}; graph = StateGraph(AgentState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()