from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FuseState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(FuseState); def validate_electrical_specs(state: FuseState): rating = state['specs'].get('rated_current', 0); return {'validation_report': [f'Current rating {rating}A validated for circuit safety']} if rating > 0 else {'validation_report': ['Invalid current rating']}; graph.add_node('validate', validate_electrical_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()