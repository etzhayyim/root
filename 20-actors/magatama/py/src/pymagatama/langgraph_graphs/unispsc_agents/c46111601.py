from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; payload: dict; graph = StateGraph(State); def validate_export_license(state): return {'status': 'license_verified'}; def run_ballistic_check(state): return {'status': 'ballistics_compliant'}; graph.add_node('validate_license', validate_export_license); graph.add_node('run_ballistic_check', run_ballistic_check); graph.set_entry_point('validate_license'); graph.add_edge('validate_license', 'run_ballistic_check'); graph.add_edge('run_ballistic_check', END); compiled_graph = graph.compile()