from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: FastenerState): state['valid'] = 'Material Composition' in state['specs'] and 'Tensile Strength' in state['specs']; return state; goal = StateGraph(FastenerState); goal.add_node('validate', validate_specs); goal.set_entry_point('validate'); goal.add_edge('validate', END); graph = goal.compile()