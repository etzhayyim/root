from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CameraState(TypedDict): input_data: dict; validation_report: list; status: str; def validate_camera_specs(state: CameraState): state['validation_report'].append('Validating optical sensor and print mechanism'); return {'status': 'validated'}; def check_film_stock(state: CameraState): state['validation_report'].append('Checking compatible film unit availability'); return {'status': 'ready'}; graph = StateGraph(CameraState); graph.add_node('validate', validate_camera_specs); graph.add_node('inventory', check_film_stock); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); compiled_graph = graph.compile()