from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class RobotToolingState(TypedDict): tooling_id: str; specs: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_tooling_specs(state: RobotToolingState): return {'validation_results': ['CAD model verified', 'Safety rating checked'], 'status': 'validated'}; def finalize_procurement(state: RobotToolingState): return {'status': 'ready_to_order'}; graph = StateGraph(RobotToolingState); graph.add_node('validate', validate_tooling_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()