from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeToolState(TypedDict): tool_specs: dict; validation_status: bool; workflow_steps: List[str]; def validate_specs(state: PipeToolState): specs = state['tool_specs']; is_valid = 'diameter' in specs and 'angle' in specs; return {'validation_status': is_valid}; def route_workflow(state: PipeToolState): return 'complete' if state['validation_status'] else 'error'; graph = StateGraph(PipeToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_workflow, {'complete': END, 'error': END}); graph = graph.compile()