from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AttapulgiteState(TypedDict): chemical_purity: float; moisture: float; needs_refinement: bool; graph = StateGraph(AttapulgiteState); def validate_specs(state: AttapulgiteState): return {'needs_refinement': state['chemical_purity'] < 95.0}; def check_moisture(state: AttapulgiteState): return state; graph.add_node('validate', validate_specs); graph.add_node('check_moisture', check_moisture); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_moisture'); graph.add_edge('check_moisture', END); app = graph.compile()