from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input: str; validated: bool; approved: bool; def check_gmp(state: State): return {'validated': True}; def verify_regulatory(state: State): return {'approved': True}; graph = StateGraph(State); graph.add_node('gmp_check', check_gmp); graph.add_node('reg_audit', verify_regulatory); graph.set_entry_point('gmp_check'); graph.add_edge('gmp_check', 'reg_audit'); graph.add_edge('reg_audit', END); compiled_graph = graph.compile()