from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): markings: List[str]; specification: dict; is_verified: bool; def validate_spec(state: State): state['is_verified'] = 'Non-Toxic' in state['specification'].get('certifications', []) and 'Erasability' in state['specification']; return state; builder = StateGraph(State); builder.add_node('validate', validate_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()