from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): purity_check: bool; safety_compliant: bool; validation_log: Annotated[Sequence[str], operator.add]; def validate_purity(state: ChemicalState): return {'purity_check': True, 'validation_log': ['Purity verified against spec']}; def check_safety_protocols(state: ChemicalState): return {'safety_compliant': True, 'validation_log': ['Safety protocols validated']}; graph = StateGraph(ChemicalState); graph.add_node('purity', validate_purity); graph.add_node('safety', check_safety_protocols); graph.set_entry_point('purity'); graph.add_edge('purity', 'safety'); graph.add_edge('safety', END); graph = graph.compile()