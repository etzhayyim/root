from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): commodity: str; compliance_verified: bool; steps: List[str]; def verify_compliance(state: ProcurementState): state['compliance_verified'] = True; state['steps'].append('Compliance checks passed'); return state; def validate_shipment(state: ProcurementState): state['steps'].append('Shipment handling protocol initialized'); return state; graph = StateGraph(ProcurementState); graph.add_node('verify', verify_compliance); graph.add_node('validate', validate_shipment); graph.set_entry_point('verify'); graph.add_edge('verify', 'validate'); graph.add_edge('validate', END); app = graph.compile()