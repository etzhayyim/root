from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): product: str; spec_validated: bool; route: str; def validate_compliance(state: ProcurementState): return {'spec_validated': True, 'route': 'procure'}; def finalize(state: ProcurementState): return {'route': 'completed'}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_compliance); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()