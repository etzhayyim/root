from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HousingState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(HousingState); def validate_specs(state: HousingState): issues = []; if 'ip_rating' not in state['specs']: issues.append('Missing mandatory IP rating'); return {'validation_report': issues}; def route_task(state: HousingState): return 'complete' if not state['validation_report'] else END; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()