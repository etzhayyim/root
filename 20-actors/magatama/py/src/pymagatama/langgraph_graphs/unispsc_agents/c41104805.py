from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EvaporatorState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: EvaporatorState): specs = state['specs']; errors = []; if 'min_temp' not in specs: errors.append('Missing min_temp'); state['validation_errors'] = errors; state['status'] = 'invalid' if errors else 'validated'; return state; graph = StateGraph(EvaporatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()