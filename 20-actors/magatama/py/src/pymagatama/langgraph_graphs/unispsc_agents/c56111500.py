from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OfficeProcurementState(TypedDict): workstation_specs: dict; compliance_check: bool; final_report: str; def validate_specs(state: OfficeProcurementState): print('Validating workstation specs...'); return {'compliance_check': True}; def approve_order(state: OfficeProcurementState): print('Workflow complete.'); return {'final_report': 'Approved'}; graph = StateGraph(OfficeProcurementState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()