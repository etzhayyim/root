from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PatchPanelState(TypedDict): port_count: int; shielding: bool; specs_valid: bool; graph = StateGraph(PatchPanelState); def validate_specs(state: PatchPanelState): return {'specs_valid': state['port_count'] in [24, 48]}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()