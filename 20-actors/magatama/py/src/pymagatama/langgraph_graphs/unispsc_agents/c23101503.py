from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BroachingState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: BroachingState): if state['specs'].get('force', 0) <= 0: state['validation_errors'].append('Invalid Force'); return {'approved': False}; def check_export_compliance(state: BroachingState): return {'approved': True}; graph = StateGraph(BroachingState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()