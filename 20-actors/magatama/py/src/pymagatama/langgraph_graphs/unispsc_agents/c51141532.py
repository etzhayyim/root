from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; concentration: float; compliance_check: bool; metadata: dict; def validate_purity(state: PharmState): state['compliance_check'] = state['concentration'] >= 99.5; return state; def check_storage(state: PharmState): state['metadata'] = {'temp_control': '2-8C'}; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph = graph.compile()