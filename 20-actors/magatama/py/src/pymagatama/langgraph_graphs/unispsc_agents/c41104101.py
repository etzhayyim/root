from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecimenMailerState(TypedDict): mailer_type: str; integrity_check: bool; compliance_docs: List[str]; def validate_specimen_container(state: SpecimenMailerState): return {'integrity_check': True if state['mailer_type'] in ['plastic', 'cushioned'] else False}; def verify_compliance(state: SpecimenMailerState): return {'compliance_docs': ['ISO_13485', 'CE_IVD']}; graph = StateGraph(SpecimenMailerState); graph.add_node('validate', validate_specimen_container); graph.add_node('compliance', verify_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()