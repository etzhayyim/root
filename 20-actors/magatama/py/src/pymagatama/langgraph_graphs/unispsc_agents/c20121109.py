from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class AerospaceFastenerState(TypedDict): fastener_spec: dict; validation_logs: Annotated[Sequence[str], operator.add]; compliance_status: bool; def validate_fastener_spec(state: AerospaceFastenerState): log = f'Validating fastener grade {state["fastener_spec"].get("grade")}'; return {"validation_logs": [log], "compliance_status": True}; def finalize_procurement(state: AerospaceFastenerState): return {"validation_logs": ["Finalizing order"]}; graph = StateGraph(AerospaceFastenerState); graph.add_node("validate", validate_fastener_spec); graph.add_node("finalize", finalize_procurement); graph.set_entry_point("validate"); graph.add_edge("validate", "finalize"); graph.add_edge("finalize", END); graph = graph.compile()