from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MediaState(TypedDict): media_list: List[str]; validated_items: List[str]; def validate_media(state: MediaState): return {'validated_items': [item for item in state['media_list'] if item.endswith(('.cd', '.tape'))]}; def check_licensing(state: MediaState): return {'validated_items': [item + '_licensed' for item in state['validated_items']]}; graph = StateGraph(MediaState); graph.add_node('validate', validate_media); graph.add_node('license', check_licensing); graph.add_edge('validate', 'license'); graph.add_edge('license', END); graph.set_entry_point('validate');