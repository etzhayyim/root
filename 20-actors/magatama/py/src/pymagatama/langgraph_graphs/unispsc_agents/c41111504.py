from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_report: list; graph = StateGraph(State); def validate_spec(state: State): report = []; if state['input_data'].get('accuracy') is None: report.append('Missing accuracy field'); return {'validation_report': report}; def route_procurement(state: State): return 'process' if not state['validation_report'] else END; graph.add_node('validate', validate_spec); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()