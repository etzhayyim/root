from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): antigen_data: dict; validation_report: List[str]; def validate_purity(state: AgentState): purity = state['antigen_data'].get('purity_index', 0); return {'validation_report': ['Purity verified'] if purity > 0.95 else ['Purity failure']}; def archive_lot(state: AgentState): return {'validation_report': state['validation_report'] + ['Lot archived in secure system']}; graph = StateGraph(AgentState); graph.add_node('validate', validate_purity); graph.add_node('archive', archive_lot); graph.set_entry_point('validate'); graph.add_edge('validate', 'archive'); graph.add_edge('archive', END); compiled_graph = graph.compile()