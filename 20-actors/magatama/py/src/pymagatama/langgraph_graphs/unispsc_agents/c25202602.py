from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AircraftPartsState(TypedDict): status: str; spec_check: bool; export_control_clearance: bool; def validate_specs(state: AircraftPartsState): state['spec_check'] = True; return state; def check_export_laws(state: AircraftPartsState): state['export_control_clearance'] = True; return state; graph = StateGraph(AircraftPartsState); graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export_laws); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph = graph.compile()