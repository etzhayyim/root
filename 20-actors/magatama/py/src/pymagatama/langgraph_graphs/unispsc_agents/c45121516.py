from langgraph.graph import StateGraph, END; from typing import TypedDict; class CameraState(TypedDict): model_specs: dict; is_compliant: bool; def validate_specs(state: CameraState): state['is_compliant'] = 'ECCN' in state['model_specs'] and state['model_specs']['resolution'] >= 1080; return state; graph = StateGraph(CameraState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()