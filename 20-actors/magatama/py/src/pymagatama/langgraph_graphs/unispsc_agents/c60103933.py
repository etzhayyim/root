from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SpecimenState(TypedDict): specimen_id: str; biohazard_cleared: bool; storage_temp_valid: bool; ethics_verified: bool; def check_ethics(state: SpecimenState): state['ethics_verified'] = True; return state; def validate_conditions(state: SpecimenState): state['storage_temp_valid'] = True; return state; graph = StateGraph(SpecimenState); graph.add_node('verify_ethics', check_ethics); graph.add_node('check_storage', validate_conditions); graph.set_entry_point('verify_ethics'); graph.add_edge('verify_ethics', 'check_storage'); graph.add_edge('check_storage', END); graph = graph.compile()