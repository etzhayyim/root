from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialysisFilterState(TypedDict): filter_id: str; specs: dict; validation_status: str; def validate_specs(state: DialysisFilterState): is_valid = 'sterilization_date' in state['specs'] and state['specs'].get('pore_size') is not None; state['validation_status'] = 'approved' if is_valid else 'rejected'; return state; graph = StateGraph(DialysisFilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()