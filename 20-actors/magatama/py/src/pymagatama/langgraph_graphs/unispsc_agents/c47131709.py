from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DispenserState(TypedDict): vendor: str; model: str; compliance_passed: bool; specs: dict; graph = StateGraph(DispenserState); def validate_specs(state: DispenserState): state['compliance_passed'] = 'dimensions' in state['specs'] and 'material_composition' in state['specs']; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()