from langgraph.graph import StateGraph, END; from typing import TypedDict; class BearingState(TypedDict): specs: dict; valid: bool; def check_specs(state: BearingState): state['valid'] = 'precision_grade' in state['specs'] and state['specs']['outer_diameter_mm'] > 0; return state; graph = StateGraph(BearingState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()