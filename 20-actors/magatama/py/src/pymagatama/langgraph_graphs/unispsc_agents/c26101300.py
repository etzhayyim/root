from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MotorState(TypedDict): input_specs: dict; validation_results: list[str]; approved: bool; def validate_specs(state: MotorState): specs = state['input_specs']; results = []; if 'pressure' not in specs: results.append('Missing pressure rating'); return {'validation_results': results, 'approved': len(results) == 0}; graph = StateGraph(MotorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()