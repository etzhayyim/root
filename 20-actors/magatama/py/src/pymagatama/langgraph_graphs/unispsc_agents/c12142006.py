from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class ChemicalState(TypedDict): messages: Annotated[Sequence[str], add_messages]; viscosity: float; is_compliant: bool; def validate_viscosity(state: ChemicalState): return { 'is_compliant': state['viscosity'] > 10.0 }; def compile_workflow(): builder = StateGraph(ChemicalState); builder.add_node('validate', validate_viscosity); builder.set_entry_point('validate'); builder.add_edge('validate', END); return builder.compile(); graph = compile_workflow()