from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DockLightState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: DockLightState): specs = state['specs']; results = []; if specs.get('ip_rating', 0) < 65: results.append('Insufficient IP rating'); return {'validation_results': results, 'status': 'validated' if not results else 'rejected'}; graph = StateGraph(DockLightState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()