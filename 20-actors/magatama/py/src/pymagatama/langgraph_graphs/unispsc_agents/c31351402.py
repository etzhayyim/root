from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): tube_specs: dict; inspection_status: str; validation_errors: List[str]; def validate_specs(state: AssemblyState): print('Validating carbon steel specs...'); return {'inspection_status': 'COMPLIANT' if state['tube_specs'].get('pressure_rating') > 1000 else 'FAILED'}; def check_export_controls(state: AssemblyState): print('Checking dual-use export compliance...'); return {'validation_errors': ['Export License Required'] if state['tube_specs'].get('is_international') else []}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()