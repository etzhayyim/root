from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): doc_id: str; specs: dict; valid: bool; def validate_specs(state: ProcureState): state['valid'] = all(k in state['specs'] for k in ['material', 'sterilization']); return state; def check_reg(state: ProcureState): print('Checking medical compliance...'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_reg); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()