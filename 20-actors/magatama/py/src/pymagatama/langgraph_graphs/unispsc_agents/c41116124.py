from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HistologyState(TypedDict): reagent_data: dict; validation_errors: List[str]; approved: bool; def validate_reagent(state: HistologyState): data = state['reagent_data']; errors = []; if 'msds' not in data: errors.append('Missing MSDS'); if data.get('temp') not in ['2-8C', 'RT']: errors.append('Invalid temp'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(HistologyState); graph.add_node('validate', validate_reagent); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()