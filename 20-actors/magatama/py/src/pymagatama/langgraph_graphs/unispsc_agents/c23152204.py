from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BearingState(TypedDict): specs: dict; validation_log: List[str]; approved: bool; def validate_specs(state: BearingState): specs = state['specs']; log = []; is_valid = True; if specs.get('load_rating', 0) <= 0: log.append('Invalid load rating'); is_valid = False; return {'validation_log': log, 'approved': is_valid}; graph = StateGraph(BearingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()