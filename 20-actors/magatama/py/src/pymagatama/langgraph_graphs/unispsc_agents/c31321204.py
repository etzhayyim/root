from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): material: str; specs: dict; ready: bool; def validate_specs(state: ProcureState): state['ready'] = 'ASTMB' in state['specs'].get('ASTM_spec_compliance', '') return state; def check_export(state: ProcureState): print('Export control review triggered for Inconel.'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph.set_entry_point('validate'); graph = graph.compile()