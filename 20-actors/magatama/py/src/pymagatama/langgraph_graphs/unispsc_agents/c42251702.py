from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReharampState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(ReharampState); def validate_load(state: ReharampState): capacity = state['specs'].get('load_kg', 0); return {'validation_results': ['Pass' if capacity >= 150 else 'Fail']}; def check_safety(state: ReharampState): return {'validation_results': ['Safety Verified']}; graph.add_node('validate_load', validate_load); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate_load'); graph.add_edge('validate_load', 'check_safety'); graph.add_edge('check_safety', END); compiled_graph = graph.compile()