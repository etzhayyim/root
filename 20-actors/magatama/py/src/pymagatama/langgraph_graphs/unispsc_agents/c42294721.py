from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AorticPunchState(TypedDict): punch_specs: dict; validation_results: List[str]; def validate_medical_grade(state: AorticPunchState): return {'validation_results': ['Grade Verified']} if state['punch_specs'].get('is_sterile') else {'validation_results': ['Failed']}; def check_diameter(state: AorticPunchState): return {'validation_results': state['validation_results'] + ['Diameter OK']}; graph = StateGraph(AorticPunchState); graph.add_node('verify_material', validate_medical_grade); graph.add_node('check_size', check_diameter); graph.set_entry_point('verify_material'); graph.add_edge('verify_material', 'check_size'); graph.add_edge('check_size', END); graph = graph.compile()