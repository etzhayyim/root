from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; specs: dict; validation_log: List[str]; def validate_welds(state: AssemblyState): state['validation_log'].append('Checking NDT results...'); return state; def check_dimensions(state: AssemblyState): state['validation_log'].append('Verifying geometric tolerances...'); return state; graph = StateGraph(AssemblyState); graph.add_node('validate_welds', validate_welds); graph.add_node('check_dims', check_dimensions); graph.set_entry_point('validate_welds'); graph.add_edge('validate_welds', 'check_dims'); graph.add_edge('check_dims', END); graph = graph.compile()