from langgraph.graph import StateGraph, END; from typing import TypedDict; class AirFittingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: AirFittingState): state['valid'] = 'pressure_rating' in state['specs'] and state['specs']['pressure_rating'] > 0; return state; graph = StateGraph(AirFittingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()