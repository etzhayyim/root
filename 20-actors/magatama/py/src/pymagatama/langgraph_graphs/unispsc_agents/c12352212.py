from typing import TypedDict, Annotated, List; import operator; from langgraph.graph import StateGraph, END; class ChemicalProcessState(TypedDict): chemical_id: str; purity: float; hazard_level: int; validation_logs: Annotated[List[str], operator.add]; def validate_purity(state: ChemicalProcessState): return {'validation_logs': ['Purity check passed'] if state['purity'] >= 99.0 else ['Purity check failed']}; def assess_safety(state: ChemicalProcessState): return {'validation_logs': ['Safety protocols validated']}; graph = StateGraph(ChemicalProcessState); graph.add_node('validate', validate_purity); graph.add_node('safety', assess_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()