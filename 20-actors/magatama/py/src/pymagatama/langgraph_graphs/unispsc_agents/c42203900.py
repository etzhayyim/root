from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadiationState(TypedDict): device_id: str; compliance_docs: List[str]; calibration_status: bool; graph = StateGraph(RadiationState); def validate_calibration(state: RadiationState): return {'calibration_status': True} if 'cal_cert' in state['compliance_docs'] else {'calibration_status': False}; def check_compliance(state: RadiationState): return {'compliance_docs': state['compliance_docs'] + ['regulatory_verified']}; graph.add_node('validate', validate_calibration); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()