from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class HeatExchangerState(TypedDict): thermal_load: float; pressure_rating: float; design_validated: bool; def validate_design(state: HeatExchangerState): state['design_validated'] = state['pressure_rating'] > 150.0; return state; graph = StateGraph(HeatExchangerState); graph.add_node('validation', validate_design); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()