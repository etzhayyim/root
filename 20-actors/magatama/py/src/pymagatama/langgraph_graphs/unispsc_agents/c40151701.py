from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): specs: dict; validation_results: list; status: str; def validate_dimensions(state: PumpState): return {'validation_results': ['Dimension check passed']}; def check_material(state: PumpState): return {'validation_results': ['Material cert verified']}; graph = StateGraph(PumpState); graph.add_node('dim', validate_dimensions); graph.add_node('mat', check_material); graph.set_entry_point('dim'); graph.add_edge('dim', 'mat'); graph.add_edge('mat', END); graph = graph.compile()