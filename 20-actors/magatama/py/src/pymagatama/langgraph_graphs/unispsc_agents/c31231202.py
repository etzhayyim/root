from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BerylliumState(TypedDict): chemical_purity: float; dimension_check: bool; export_compliance: bool; status: str; def validate_purity(state: BerylliumState): state['status'] = 'COMPLIANT' if state['chemical_purity'] >= 0.99 else 'REJECTED'; return state; def verify_export(state: BerylliumState): state['export_compliance'] = True; return state; graph = StateGraph(BerylliumState); graph.add_node('validate_purity', validate_purity); graph.add_node('verify_export', verify_export); graph.add_edge('validate_purity', 'verify_export'); graph.add_edge('verify_export', END); graph.set_entry_point('validate_purity'); graph_app = graph.compile()