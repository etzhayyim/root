from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): specs: dict; compliance_checked: bool; status: str; def validate_specs(state: MachineState): state['compliance_checked'] = all(k in state['specs'] for k in ['voltage', 'tolerance']); return state; def check_export_control(state: MachineState): state['status'] = 'Flagged for Export Review' if state.get('is_dual_use') else 'Approved'; return state; graph = StateGraph(MachineState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()