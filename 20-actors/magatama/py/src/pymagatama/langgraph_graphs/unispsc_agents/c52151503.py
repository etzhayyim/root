from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlatwareState(TypedDict): items: List[dict]; compliance_status: bool; def validate_spec(state: FlatwareState): state['compliance_status'] = all(item.get('food_grade') == True for item in state['items']); return {'compliance_status': state['compliance_status']}; graph = StateGraph(FlatwareState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()