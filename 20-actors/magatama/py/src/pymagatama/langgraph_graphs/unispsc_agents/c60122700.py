from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaintState(TypedDict): items: List[str]; validation_results: List[str]; def validate_sds(state: PaintState): return {'validation_results': ['SDS_Checked']}; def verify_compliance(state: PaintState): return {'validation_results': state['validation_results'] + ['Voc_Limit_Approved']}; graph = StateGraph(PaintState); graph.add_node('sds_check', validate_sds); graph.add_node('compliance_check', verify_compliance); graph.set_entry_point('sds_check'); graph.add_edge('sds_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()