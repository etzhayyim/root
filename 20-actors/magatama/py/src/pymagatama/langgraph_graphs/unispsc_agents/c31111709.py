from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): alloy_spec: str; compliance_check: bool; graph = StateGraph(ProcureState); def validate_material(state): state['compliance_check'] = state['alloy_spec'] != ''; return state; def check_export_control(state): return state; graph.add_node('validate', validate_material); graph.add_node('export_review', check_export_control); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph.set_entry_point('validate'); graph = graph.compile()