from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): input_data: dict; verification_logs: Annotated[list, operator.add]; status: str; def validate_purity(state: PharmState): purity = state['input_data'].get('purity', 0); status = 'PASSED' if purity >= 99.0 else 'FAILED'; return {'verification_logs': [f'Purity check: {status}'], 'status': status}; def check_compliance(state: PharmState): return {'verification_logs': ['Compliance verified against USP/EP standards']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()