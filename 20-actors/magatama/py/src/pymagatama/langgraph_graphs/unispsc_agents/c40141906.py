from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DuctState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(DuctState); def validate_thickness(state: DuctState): state['validation_results'].append('Thickness verified'); return state; def check_corrosion(state: DuctState): state['validation_results'].append('Coating check passed'); return state; graph.add_node('validate_thickness', validate_thickness); graph.add_node('check_corrosion', check_corrosion); graph.set_entry_point('validate_thickness'); graph.add_edge('validate_thickness', 'check_corrosion'); graph.add_edge('check_corrosion', END); compiled_graph = graph.compile()