from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): purity: float; gmp_certified: bool; qc_passed: bool; state: str; graph = StateGraph(PharmaState); def validate_purity(state: PharmaState): return {'qc_passed': state['purity'] >= 99.0}; def check_compliance(state: PharmaState): return {'state': 'COMPLIANT' if state['gmp_certified'] and state['qc_passed'] else 'REJECTED'}; graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()