from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): input: str; validation_results: Annotated[list, operator.add]; status: str; def validate_purity(state: AgentState): return {'validation_results': ['Purity check: Pass'] if '98%' in state['input'].lower() else ['Purity check: Fail']}; def check_storage(state: AgentState): return {'validation_results': ['Temp log: Verified']}; graph = StateGraph(AgentState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_storage', check_storage); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_storage'); graph.add_edge('check_storage', END); graph = graph.compile()