from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CPMProcurementState(TypedDict): device_specs: dict; compliance_docs: list; validation_status: str; def validate_specs(state): return {'validation_status': 'verified' if 'FDA_approval' in state['compliance_docs'] else 'pending'}; def check_safety(state): return {'validation_status': 'safety_cleared'}; graph = StateGraph(CPMProcurementState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); app = graph.compile()