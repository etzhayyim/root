from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): specs: dict; valid: bool; def validate_specs(state: FastenerState): state['valid'] = 'material_grade' in state['specs'] and state['specs']['tensile_strength_mpa'] > 0; return state; graph = StateGraph(FastenerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()