from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TinAngleState(TypedDict): material_specs: dict; validation_results: List[str]; graph = StateGraph(TinAngleState); def validate_purity(state: TinAngleState): return {'validation_results': ['Purity check passed']}; def check_dimension(state: TinAngleState): return {'validation_results': ['Dimensions compliant']}; graph.add_node('purity', validate_purity); graph.add_node('dimension', check_dimension); graph.set_entry_point('purity'); graph.add_edge('purity', 'dimension'); graph.add_edge('dimension', END); graph = graph.compile()