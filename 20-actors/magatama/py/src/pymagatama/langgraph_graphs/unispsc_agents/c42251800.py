from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RehabState(TypedDict): equipment_id: str; compliance_docs: List[str]; inspection_passed: bool; def validate_specs(state: RehabState): state['inspection_passed'] = len(state['compliance_docs']) >= 3; return state; def check_compliance(state: RehabState): return 'valid' if state['inspection_passed'] else 'invalid'; graph = StateGraph(RehabState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()