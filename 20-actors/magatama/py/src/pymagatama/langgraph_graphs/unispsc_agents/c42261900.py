from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForensicState(TypedDict): items: List[str]; qc_passed: bool; data_verified: bool; def validate_forensic_spec(state: ForensicState): state['qc_passed'] = all(item for item in state['items']); return state; def verify_chain_of_custody(state: ForensicState): state['data_verified'] = True; return state; graph = StateGraph(ForensicState); graph.add_node('validate', validate_forensic_spec); graph.add_node('verify', verify_chain_of_custody); graph.set_entry_point('validate'); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); graph = graph.compile()