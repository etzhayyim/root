from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SecurityWireState(TypedDict): wire_specs: dict; validation_results: List[str]; graph = StateGraph(SecurityWireState); def validate_tensile_strength(state: SecurityWireState): specs = state['wire_specs']; return {'validation_results': ['Tensile strength check passed'] if specs.get('tensile', 0) > 1000 else ['Tensile strength insufficient']}; def check_compliance(state: SecurityWireState): return {'validation_results': state['validation_results'] + ['Compliance verified']}; graph.add_node('validate', validate_tensile_strength); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()