from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SpecimenState(TypedDict): specimen_id: str; integrity_status: bool; validation_log: List[str]; def validate_specimen_container(state: SpecimenState): state['validation_log'].append('Checking seal integrity'); state['integrity_status'] = True; return state; def check_compliance(state: SpecimenState): state['validation_log'].append('Verifying ISO 10993 compliance'); return state; graph = StateGraph(SpecimenState); graph.add_node('validate', validate_specimen_container); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()