from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; purity: float; compliance_met: bool; def check_quality(state: PharmaState): return {'compliance_met': state['purity'] >= 99.9}; def finalize_procurement(state: PharmaState): return {'compliance_met': True}; graph = StateGraph(PharmaState); graph.add_node('quality_check', check_quality); graph.add_node('finalizer', finalize_procurement); graph.set_entry_point('quality_check'); graph.add_conditional_edges('quality_check', lambda x: 'finalizer' if x['compliance_met'] else END, {'finalizer': 'finalizer'}); graph.add_edge('finalizer', END); graph = graph.compile()