from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OpticsState(TypedDict): items: List[str]; validated: bool; errors: List[str]; def validate_optics(state: OpticsState): validated = all('spec_check' in item.lower() for item in state['items']); return {'validated': validated}; graph = StateGraph(OpticsState); graph.add_node('validate', validate_optics); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()