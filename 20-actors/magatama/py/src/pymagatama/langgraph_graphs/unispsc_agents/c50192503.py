from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OrderState(TypedDict): ingredients: List[str]; inspection_passed: bool; def validate_freshness(state: OrderState): return {'inspection_passed': True}; def check_safety(state: OrderState): print('Checking HACCP compliance'); return {}; graph = StateGraph(OrderState); graph.add_node('validate', validate_freshness); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()