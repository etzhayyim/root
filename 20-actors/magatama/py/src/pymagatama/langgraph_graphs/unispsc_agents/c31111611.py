from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_certs: List[str]; material_purity: float; specs_met: bool; def validate_purity(state: ProcState): return { 'specs_met': state['material_purity'] >= 0.99 }; def check_certification(state: ProcState): return { 'specs_met': 'ISO-9001' in state['vendor_certs'] }; graph = StateGraph(ProcState); graph.add_node('validate_material', validate_purity); graph.add_node('cert_check', check_certification); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'cert_check'); graph.add_edge('cert_check', END); compiled_graph = graph.compile()