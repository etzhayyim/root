from langgraph.graph import StateGraph, END; from typing import TypedDict; class LaserState(TypedDict): spec: dict; valid: bool; def validate_specs(state: LaserState): state['valid'] = state['spec'].get('power', 0) > 0; return state; graph = StateGraph(LaserState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()