from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: str; validation_status: str; def validate_tablet_specs(state: State): return {'validation_status': 'verified' if 'composition' in state['input_data'] else 'failed'}; def process_logistics(state: State): return {'validation_status': 'shipped'}; graph = StateGraph(State); graph.add_node('validate', validate_tablet_specs); graph.add_node('ship', process_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'ship'); graph.add_edge('ship', END); graph = graph.compile()