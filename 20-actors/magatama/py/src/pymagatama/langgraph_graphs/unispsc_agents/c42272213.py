from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CPAPState(TypedDict): device_id: str; compliance_docs: List[str]; approved: bool; def validate_medical_compliance(state: CPAPState): state['approved'] = all(doc in state['compliance_docs'] for doc in ['ISO_10993', 'FDA_510k']); return state; graph = StateGraph(CPAPState); graph.add_node('validate', validate_medical_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()