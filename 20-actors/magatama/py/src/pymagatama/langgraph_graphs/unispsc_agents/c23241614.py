from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MillingState(TypedDict): specs: dict; validation_results: List[str]; def validate_milling_specs(state: MillingState): results = []; specs = state.get('specs', {}); if specs.get('diameter', 0) <= 0: results.append('Invalid Diameter'); return {'validation_results': results}; graph = StateGraph(MillingState); graph.add_node('validate', validate_milling_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()