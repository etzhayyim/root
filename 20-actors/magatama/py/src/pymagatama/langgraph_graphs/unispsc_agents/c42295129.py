from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DispenserState(TypedDict): vendor_docs: dict; compliance_check: bool; final_approval: bool; def validate_specs(state: DispenserState): return {'compliance_check': all(k in state['vendor_docs'] for k in ['ISO13485', 'IEC60601'])}; def approve_procurement(state: DispenserState): return {'final_approval': state['compliance_check']}; graph = StateGraph(DispenserState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()