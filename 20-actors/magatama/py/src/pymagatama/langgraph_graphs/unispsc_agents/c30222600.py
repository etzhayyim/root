from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(State); def validate_safety_certs(state: State): return {'validation_results': {'cert_check': 'passed'}}; def check_structural_integrity(state: State): return {'validation_results': {'integrity_check': 'passed'}}; graph.add_node('safety_check', validate_safety_certs); graph.add_node('structural_check', check_structural_integrity); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'structural_check'); graph.add_edge('structural_check', END); compiled_graph = graph.compile()