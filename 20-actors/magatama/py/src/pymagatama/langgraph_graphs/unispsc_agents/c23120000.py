from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RobotState(TypedDict): specs: dict; valid: bool; robot_graph = StateGraph(RobotState); def validate_specs(state: RobotState): state['valid'] = 'ECCN' in state['specs'] and 'MTBF' in state['specs']; return state; robot_graph.add_node('validator', validate_specs); robot_graph.set_entry_point('validator'); robot_graph.add_edge('validator', END); graph = robot_graph.compile()