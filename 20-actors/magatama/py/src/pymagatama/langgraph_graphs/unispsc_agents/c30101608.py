from langgraph.graph import StateGraph, END; from typing import TypedDict; class TitaniumState(TypedDict): spec: dict; validated: bool; chain_of_custody: str; def validate_grade(state: TitaniumState): state['validated'] = state['spec'].get('grade') in ['Grade 5', 'Grade 23']; return state; def verify_cert(state: TitaniumState): print('Checking MTR...'); return state; graph = StateGraph(TitaniumState); graph.add_node('validate', validate_grade); graph.add_node('cert_check', verify_cert); graph.set_entry_point('validate'); graph.add_edge('validate', 'cert_check'); graph.add_edge('cert_check', END); graph = graph.compile()