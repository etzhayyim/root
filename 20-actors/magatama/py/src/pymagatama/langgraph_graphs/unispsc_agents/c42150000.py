from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DentalProcureState(TypedDict): items: List[str]; validated: bool; compliance_status: str; def validate_equipment(state: DentalProcureState): return {'validated': True, 'compliance_status': 'Certified'}; def check_regulatory(state: DentalProcureState): return {'compliance_status': 'FDA_Cleared'}; graph = StateGraph(DentalProcureState); graph.add_node('validate', validate_equipment); graph.add_node('compliance', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()