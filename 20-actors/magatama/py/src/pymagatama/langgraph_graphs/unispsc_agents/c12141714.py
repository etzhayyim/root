from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; import operator; class ResinProcurementState(TypedDict): chemical_data: dict; validation_results: Annotated[Sequence[str], operator.add]; compliance_status: bool; def validate_chemical_spec(state: ResinProcurementState): return {'validation_results': ['Chemical spec verified against MSDS'], 'compliance_status': True}; def assess_export_control(state: ResinProcurementState): return {'validation_results': ['Export control check passed']}; graph = StateGraph(ResinProcurementState); graph.add_node('validate', validate_chemical_spec); graph.add_node('export', assess_export_control); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph.set_entry_point('validate'); app = graph.compile()