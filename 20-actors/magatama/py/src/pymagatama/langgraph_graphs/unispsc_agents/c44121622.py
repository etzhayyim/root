from langgraph.graph import StateGraph, END; from typing import TypedDict; class MoistenerState(TypedDict): specs: dict; validation_passed: bool; def check_specs(state: MoistenerState): state['validation_passed'] = 'liquid_capacity' in state['specs']; return state; builder = StateGraph(MoistenerState); builder.add_node('check', check_specs); builder.set_entry_point('check'); builder.add_edge('check', END); graph = builder.compile()