from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetyRailState(TypedDict): items: List[dict]; validation_errors: List[str]; approved: bool; def validate_compliance(state: SafetyRailState): errors = []; for item in state['items']: if item.get('load_capacity', 0) < 140: errors.append(f'Safety deficiency: load capacity below 140kg'); return {'validation_errors': errors}; def finalize_procurement(state: SafetyRailState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(SafetyRailState); graph.add_node('validate', validate_compliance); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()