from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_procurement(state: ProcState): return {'validated': all(c in state['certs'] for c in ['ISO13485', 'FDA_510K'])}; graph = StateGraph(ProcState); graph.add_node('validate', validate_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()