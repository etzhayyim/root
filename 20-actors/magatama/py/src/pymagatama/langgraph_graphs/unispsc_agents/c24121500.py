from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PackagingState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_packaging(state: PackagingState): errors = []; for item in state['items']: if 'dimensions' not in item: errors.append('Missing dimensions'); state['validation_errors'] = errors; return state; graph = StateGraph(PackagingState); graph.add_node('validate', validate_packaging); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()