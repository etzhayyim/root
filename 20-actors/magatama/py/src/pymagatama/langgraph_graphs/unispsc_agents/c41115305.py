from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_report: str; def validate_specs(state: State): specs = state['input_data']; valid = 'Accuracy_Class_Percentage' in specs and 'Frequency_Range_Hz' in specs; return {'validation_report': 'Passed' if valid else 'Failed'}; def log_procurement(state: State): return {'validation_report': 'Logged to ERP'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('log', log_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph = graph.compile()