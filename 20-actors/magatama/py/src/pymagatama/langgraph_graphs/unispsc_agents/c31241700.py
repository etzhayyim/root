from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MirrorState(TypedDict): specs: dict; valid: bool; def validate_specs(state: MirrorState): state['valid'] = 'Optical' in state['specs'] and 'Dimensions' in state['specs']; return state; builder = StateGraph(MirrorState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()