from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MedicalDeviceState(TypedDict): device_id: str; compliance_docs: List[str]; validated: bool; def check_compliance(state: MedicalDeviceState): state['validated'] = all(doc in state['compliance_docs'] for doc in ['iso_certs', 'fda_clearance']); return state; graph = StateGraph(MedicalDeviceState); graph.add_node('compliance', check_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', END); 