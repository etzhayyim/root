from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SolarBlanketState(TypedDict): dimensions: dict; material_spec: str; order_status: str; def validate_specs(state: SolarBlanketState) -> dict: return {'order_status': 'validated' if 'thickness' in state['material_spec'] else 'pending'}; def process_order(state: SolarBlanketState) -> dict: return {'order_status': 'processed'}; graph = StateGraph(SolarBlanketState); graph.add_node('validate', validate_specs); graph.add_node('process', process_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()