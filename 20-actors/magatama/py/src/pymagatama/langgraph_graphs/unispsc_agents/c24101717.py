from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ConveyorState(TypedDict): idler_specs: dict; validation_report: List[str]; def validate_load(state: ConveyorState): report = []; load = state['idler_specs'].get('load', 0); if load > 5000: report.append('Oversized load detected'); return {'validation_report': report}; def assemble_graph(): builder = StateGraph(ConveyorState); builder.add_node('validate', validate_load); builder.set_entry_point('validate'); builder.add_edge('validate', END); return builder.compile(); graph = assemble_graph()