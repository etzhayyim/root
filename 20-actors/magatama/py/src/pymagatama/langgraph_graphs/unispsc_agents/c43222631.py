from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class WifiSpecState(TypedDict): vendor_data: dict; compliance_check: bool; graph = StateGraph(WifiSpecState); def validate_compliance(state: WifiSpecState): return {'compliance_check': state['vendor_data'].get('regulatory_cert') is not None}; def route_procurement(state: WifiSpecState): return 'end' if state['compliance_check'] else 'end'; graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()