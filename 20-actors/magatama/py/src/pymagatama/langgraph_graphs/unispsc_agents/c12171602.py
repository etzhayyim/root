from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class PolymerState(TypedDict): code: str; specs: Dict; validation_status: bool; workflow_log: List[str]; def validate_purity(state: PolymerState): state['validation_status'] = state['specs'].get('purity_percentage', 0) > 99.9; state['workflow_log'].append('Purity check completed'); return state; def prepare_shipping(state: PolymerState): state['workflow_log'].append('Dangerous goods labeling applied'); return state; graph = StateGraph(PolymerState); graph.add_node('validate_purity', validate_purity); graph.add_node('prepare_shipping', prepare_shipping); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'prepare_shipping'); graph.add_edge('prepare_shipping', END); compiled_graph = graph.compile()