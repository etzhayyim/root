from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BainMarieState(TypedDict): specs: dict; ready_for_procurement: bool; def validate_specs(state: BainMarieState): is_valid = 'power' in state['specs'] and 'capacity' in state['specs']; return {'ready_for_procurement': is_valid}; graph = StateGraph(BainMarieState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()