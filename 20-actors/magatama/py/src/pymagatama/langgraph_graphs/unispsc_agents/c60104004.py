from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KitState(TypedDict): kit_id: str; temp_req: str; validated: bool; state = KitState(kit_id='null', temp_req='-20C', validated=False); def validate_kit(state: KitState): state['validated'] = True; return state; graph = StateGraph(KitState); graph.add_node('validate', validate_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()