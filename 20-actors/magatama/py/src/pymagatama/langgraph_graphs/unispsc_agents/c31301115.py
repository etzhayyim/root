from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): dimensions: dict; material_content: float; is_verified: bool; def validate_lead_forging(state: ForgingState): state['is_verified'] = state['material_content'] >= 0.99; return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_lead_forging); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()