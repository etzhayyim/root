from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): chemical_data: dict; validation_errors: List[str]; approved: bool; def check_purity(state: State): state['approved'] = state['chemical_data'].get('purity', 0) > 98; return state; graph = StateGraph(State); graph.add_node('check_purity', check_purity); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', END); graph = graph.compile()