from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[str]; status: str; def validate_spec(state: State): return {'status': 'validated' if 'Regulatory-cert' in state['items'] else 'rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()