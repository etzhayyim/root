from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): instrument_data: dict; validation_errors: List[str]; def validate_instrument(state: GraphState): errors = []; if 'accuracy' not in state['instrument_data']: errors.append('Missing accuracy metric'); return {'validation_errors': errors}; def check_export_control(state: GraphState): return {'validation_errors': state['validation_errors'] + ['Dual-use screening complete']}; graph = StateGraph(GraphState); graph.add_node('validate', validate_instrument); graph.add_node('export_check', check_export_control); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()