from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): nrr_rating: int; compliance: bool; approved: bool; def check_nrr(state: State): return {'approved': state['nrr_rating'] >= 20 and state['compliance']}; graph = StateGraph(State); graph.add_node('validate', check_nrr); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()