from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validated_specs: dict; compliance_status: bool; def validate_ppe(state: ProcurementState): state['compliance_status'] = all(spec in state['validated_specs'] for spec in ['BFE', 'PFE']) return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_ppe); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()