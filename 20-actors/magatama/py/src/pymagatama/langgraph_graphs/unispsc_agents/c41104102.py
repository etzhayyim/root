from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LancetState(TypedDict): sterility_checked: bool; device_specs: dict; compliance_verified: bool; def check_sterility(state: LancetState): state['sterility_checked'] = True; return state; def verify_compliance(state: LancetState): state['compliance_verified'] = state.get('device_specs', {}).get('iso_cert', False); return state; graph = StateGraph(LancetState); graph.add_node('sterility', check_sterility); graph.add_node('compliance', verify_compliance); graph.set_entry_point('sterility'); graph.add_edge('sterility', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()