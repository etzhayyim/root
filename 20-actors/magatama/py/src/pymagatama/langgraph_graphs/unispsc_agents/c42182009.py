from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): spec_data: dict; validation_errors: List[str]; def validate_medical_spec(state: SpecState): errors = []; if not state['spec_data'].get('sterilization_method'): errors.append('Missing sterilization data'); if not state['spec_data'].get('compatibility_model'): errors.append('Compatibility unspecified'); return {'validation_errors': errors}; graph = StateGraph(SpecState); graph.add_node('validate', validate_medical_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()