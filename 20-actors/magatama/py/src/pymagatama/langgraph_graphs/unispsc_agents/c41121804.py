from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlaskProcurementState(TypedDict): flask_type: str; specs: dict; validation_passed: bool; def validate_specs(state: FlaskProcurementState): state['validation_passed'] = 'material' in state['specs'] and 'capacity' in state['specs']; return state; def route_procurement(state: FlaskProcurementState): return 'validate' if state['flask_type'] else END; graph = StateGraph(FlaskProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()