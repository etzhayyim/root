from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BootState(TypedDict): specs: dict; valid: bool; errors: List[str]; def validate_boot_specs(state: BootState): errors = []; if 'material' not in state['specs']: errors.append('Missing material'); return {'valid': len(errors) == 0, 'errors': errors}; graph = StateGraph(BootState); graph.add_node('validate', validate_boot_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()