from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EndoscopeState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(EndoscopeState); def validate_compliance(state: EndoscopeState): return {'validation_results': {'compliant': True}}; def check_sterility(state: EndoscopeState): return {'validation_results': {'sterile': True}}; graph.add_node('compliance', validate_compliance); graph.add_node('sterility', check_sterility); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'sterility'); graph.add_edge('sterility', END); compiled_graph = graph.compile()