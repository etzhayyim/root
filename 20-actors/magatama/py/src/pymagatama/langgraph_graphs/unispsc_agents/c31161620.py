from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class BoltState(TypedDict): specs: dict; valid: bool; def validate_grade(state: BoltState): state['valid'] = 'Grade' in state['specs'] and float(state['specs']['Grade']) >= 8.8; return state; graph = StateGraph(BoltState); graph.add_node('validate', validate_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()