from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DialysisState(TypedDict): data: dict; logs: Annotated[list, operator.add]; def validate_specs(state: DialysisState): return {'logs': ['Specs validated for blood oxygen apparatus']}; def compliance_check(state: DialysisState): return {'logs': ['Compliance verified']}; graph = StateGraph(DialysisState); graph.add_node('validate', validate_specs); graph.add_node('compliance', compliance_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()