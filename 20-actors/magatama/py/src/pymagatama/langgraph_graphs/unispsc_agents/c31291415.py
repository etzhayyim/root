from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_docs: List[str]; inspection_passed: bool; certified: bool; def metal_extrusion_validation(state: ProcurementState): return {'inspection_passed': all('ISO' in doc for doc in state.get('vendor_docs', []))}; def certification_check(state: ProcurementState): return {'certified': True}; graph = StateGraph(ProcurementState); graph.add_node('validate_metal', metal_extrusion_validation); graph.add_node('verify_certs', certification_check); graph.add_edge('validate_metal', 'verify_certs'); graph.add_edge('verify_certs', END); graph.set_entry_point('validate_metal'); graph = graph.compile()