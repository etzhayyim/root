from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OpticState(TypedDict): specs: dict; validation_results: List[str]; def validate_laser_spec(state: OpticState): results = []; if state['specs'].get('laser_damage_threshold', 0) < 5: results.append('Low damage threshold'); return {'validation_results': results}; graph = StateGraph(OpticState); graph.add_node('validate', validate_laser_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()