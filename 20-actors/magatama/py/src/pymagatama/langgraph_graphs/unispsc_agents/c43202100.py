from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validated_items: List[str]; def validate_accessory(state: State): return {'validated_items': [item for item in state['items'] if 'compliant' in item]}; graph = StateGraph(State); graph.add_node('validate', validate_accessory); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()