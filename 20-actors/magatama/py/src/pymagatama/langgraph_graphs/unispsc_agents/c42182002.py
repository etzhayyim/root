from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; qc_passed: bool; proc_status: str; def validate_medical_device(state: ProcState): state['qc_passed'] = all(item.startswith('CERT-') for item in state['items']); state['proc_status'] = 'COMPLETED' if state['qc_passed'] else 'REQUIRED_REVIEW'; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()