from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MetalPartState(TypedDict): component_id: str; specs: dict; validation: bool; graph = StateGraph(MetalPartState); def validate_spec(state: MetalPartState): state['validation'] = all(k in state['specs'] for k in ['density', 'material']); return state; def check_compliance(state: MetalPartState): print(f'Processing part {state['component_id']}'); return state; graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()