from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AntidoteState(TypedDict): medicine_id: str; batch_id: str; expiry_date: str; is_verified: bool; def validate_product(state: AntidoteState): state['is_verified'] = bool(state['expiry_date'] and state['batch_id']); return state; graph = StateGraph(AntidoteState); graph.add_node('validate', validate_product); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()