from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WindowState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: WindowState): specs = state['specs']; errors = []; if specs.get('u_factor', 0) > 0.5: errors.append('U-Factor exceeds energy standards.'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(WindowState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()