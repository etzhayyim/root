from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RodProcurementState(TypedDict): alloy_type: str; dimensions: dict; has_mtr: bool; approved: bool; def validate_alloy(state: RodProcurementState): state['approved'] = state['alloy_type'] in ['6061-T6', '7075-T6']; return state; def check_compliance(state: RodProcurementState): if state['has_mtr']: print('Documentation sufficient'); else: print('Missing MTR'); return state; graph = StateGraph(RodProcurementState); graph.add_node('validate', validate_alloy); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()