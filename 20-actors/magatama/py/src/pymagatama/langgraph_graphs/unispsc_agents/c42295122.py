from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TourniquetState(TypedDict): specs: dict; valid: bool; def validate_specs(state: TourniquetState): state['valid'] = all(key in state['specs'] for key in ['pressure_range', 'sterilization']); return state; graph = StateGraph(TourniquetState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()