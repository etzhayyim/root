from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): vendor_check: bool; purity_verified: bool; storage_validated: bool; results: List[str]; def check_vendor(state): return {'vendor_check': True}; def verify_specs(state): return {'purity_verified': True}; def validate_storage(state): return {'storage_validated': True}; graph = StateGraph(ReagentState); graph.add_node('vendor', check_vendor); graph.add_node('purity', verify_specs); graph.add_node('storage', validate_storage); graph.set_entry_point('vendor'); graph.add_edge('vendor', 'purity'); graph.add_edge('purity', 'storage'); graph.add_edge('storage', END); graph = graph.compile()