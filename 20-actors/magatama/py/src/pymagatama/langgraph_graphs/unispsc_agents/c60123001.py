from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShapeState(TypedDict): model_ref: str; dims: List[float]; validation_status: bool; def validate_dimensions(state: ShapeState): state['validation_status'] = all(d > 0 for d in state['dims']); return state; def check_material_safety(state: ShapeState): state['validation_status'] = state['validation_status'] and True; return state; graph = StateGraph(ShapeState); graph.add_node('validate', validate_dimensions); graph.add_node('safety', check_material_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()