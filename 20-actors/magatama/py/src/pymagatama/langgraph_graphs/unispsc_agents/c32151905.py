from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): component_id: str; specs: dict; valid: bool; def validate_specs(state: State): state['valid'] = 'IP_rating' in state['specs'] and 'voltage_rating' in state['specs']; return state; builder = StateGraph(State); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()