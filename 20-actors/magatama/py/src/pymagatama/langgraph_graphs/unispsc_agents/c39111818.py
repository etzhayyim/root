from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReflectorState(TypedDict): reflector_specs: dict; validation_report: str; def validate_specs(state: ReflectorState): return {'validation_report': 'Compliant' if state['reflector_specs'].get('reflectivity') > 0.9 else 'Failed'}; graph = StateGraph(ReflectorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()