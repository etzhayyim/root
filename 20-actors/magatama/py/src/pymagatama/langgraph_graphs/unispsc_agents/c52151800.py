from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CookwareState(TypedDict): vendor_id: str; materials: List[str]; safety_certs: List[str]; compliant: bool; def validate_specs(state: CookwareState): state['compliant'] = 'FDA' in state['safety_certs'] and 'PFOA-free' in state['materials']; return state; builder = StateGraph(CookwareState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()