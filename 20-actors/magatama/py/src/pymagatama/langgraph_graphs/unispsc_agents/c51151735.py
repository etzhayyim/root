from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): procurement_flow: List[str]; license_verified: bool; def check_license(state: ProcState): state['license_verified'] = True; return {'procurement_flow': state['procurement_flow'] + ['License Verified']}; def validate_compliance(state: ProcState): return {'procurement_flow': state['procurement_flow'] + ['Compliance Validated']}; graph = StateGraph(ProcState); graph.add_node('verify', check_license); graph.add_node('validate', validate_compliance); graph.set_entry_point('verify'); graph.add_edge('verify', 'validate'); graph.add_edge('validate', END); app = graph.compile()