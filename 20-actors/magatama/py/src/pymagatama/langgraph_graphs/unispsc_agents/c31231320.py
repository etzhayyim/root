from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MetalState(TypedDict): metal_type: str; purity: float; validated: bool; def validate_purity(state: MetalState): state['validated'] = state['purity'] >= 99.9; return state; graph = StateGraph(MetalState); graph.add_node('purity_check', validate_purity); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', END); graph = graph.compile()