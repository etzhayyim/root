from langgraph.graph import StateGraph, END; from pydantic import BaseModel; from typing import List; class ScraperState(BaseModel): item_id: str; specifications: dict; approval_status: bool = False; def validate_spec(state: ScraperState): state.approval_status = all(key in state.specifications for key in ['blade_material', 'blade_width_mm']); return state; graph = StateGraph(ScraperState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()