from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_id: str; purity_check: bool; regulatory_approval: bool; graph = StateGraph(PharmState); def check_purity(state: PharmState): return {'purity_check': True}; def verify_regulations(state: PharmState): return {'regulatory_approval': True}; graph.add_node('purity', check_purity); graph.add_node('compliance', verify_regulations); graph.set_entry_point('purity'); graph.add_edge('purity', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()