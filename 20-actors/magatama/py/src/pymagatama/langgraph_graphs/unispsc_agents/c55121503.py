from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TagState(TypedDict): tags: List[str]; validated: bool; log: List[str]; def validate_tag(state: TagState): state['validated'] = all(len(t) > 3 for t in state['tags']); state['log'].append('Validation completed'); return state; graph = StateGraph(TagState); graph.add_node('validate', validate_tag); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()