from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: List[str]; validated: bool; export_check: bool; def check_export(state: CastingState): return {'export_check': True}; def validate_specs(state: CastingState): return {'validated': len(state['specs']) > 0}; graph = StateGraph(CastingState); graph.add_node('export', check_export); graph.add_node('validate', validate_specs); graph.set_entry_point('export'); graph.add_edge('export', 'validate'); graph.add_edge('validate', END); app = graph.compile()