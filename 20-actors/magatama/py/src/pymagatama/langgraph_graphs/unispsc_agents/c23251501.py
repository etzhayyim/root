from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineSpec(TypedDict): model: str; force: int; validated: bool; def validate_press_specs(state: MachineSpec): state['validated'] = state['force'] > 0; return state; graph = StateGraph(MachineSpec); graph.add_node('validate', validate_press_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()