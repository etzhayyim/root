from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EyewashState(TypedDict): spec_data: dict; validation_errors: List[str]; approved: bool; def validate_spec(state: EyewashState): compliance = state['spec_data'].get('ansi_compliant', False); return {'approved': compliance, 'validation_errors': [] if compliance else ['Non-compliant with ANSI Z358.1']}; graph = StateGraph(EyewashState); graph.add_node('validator', validate_spec); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()