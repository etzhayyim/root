from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): equipment_type: str; specs_verified: bool; safety_check: bool; def validate_equipment(state: ProcureState): return {'specs_verified': True if state['equipment_type'] else False}; def safety_audit(state: ProcureState): return {'safety_check': True}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_equipment); graph.add_node('safety', safety_audit); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()