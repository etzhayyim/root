from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoffeeMakerState(TypedDict): specs: dict; compliance: bool; validated: bool; def validate_specs(state: CoffeeMakerState): state['compliance'] = 'voltage' in state['specs'] and 'safety_cert' in state['specs']; return state; def check_approval(state: CoffeeMakerState): state['validated'] = state['compliance']; return state; graph = StateGraph(CoffeeMakerState); graph.add_node('validate', validate_specs); graph.add_node('approve', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()