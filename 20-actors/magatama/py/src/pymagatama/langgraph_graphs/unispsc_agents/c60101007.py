from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MathKitState(TypedDict): kit_id: str; compliance_check: bool; assembly_parts: List[str]; def validate_kit(state: MathKitState): state['compliance_check'] = True; return state; def verify_parts(state: MathKitState): state['assembly_parts'] = ['multiplication_table', 'tiles', 'manual']; return state; graph = StateGraph(MathKitState); graph.add_node('validate', validate_kit); graph.add_node('verify', verify_parts); graph.set_entry_point('validate'); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); graph = graph.compile()