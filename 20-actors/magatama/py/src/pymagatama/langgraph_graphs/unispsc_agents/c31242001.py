from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): items: Annotated[list, operator.add]; validation_results: dict; def validate_specs(state: GraphState): return {'validation_results': {'status': 'pending', 'checks': ['material', 'coating', 'damage_threshold']}}; def check_export_compliance(state: GraphState): return {'validation_results': {'status': 'compliant', 'export_control': 'dual-use-verified'}}; graph = StateGraph(GraphState); graph.add_node('validate_specs', validate_specs); graph.add_node('export_compliance', check_export_compliance); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', 'export_compliance'); graph.add_edge('export_compliance', END); graph = graph.compile()