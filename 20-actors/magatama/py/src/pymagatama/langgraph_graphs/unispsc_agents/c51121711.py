from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class MinoxidilState(TypedDict): chemical_purity: float; gmp_compliant: bool; validation_status: str; def validate_purity(state: MinoxidilState): return {'validation_status': 'PASS' if state['chemical_purity'] >= 99.0 else 'FAIL'}; def check_gmp(state: MinoxidilState): return {'validation_status': 'PASS' if state['gmp_compliant'] else 'FAIL'}; graph = StateGraph(MinoxidilState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_gmp', check_gmp); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_gmp'); graph.add_edge('check_gmp', END); graph = graph.compile()