from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BagState(TypedDict): specs: dict; validation_results: List[str]; def validate_durability(state: BagState): return {'validation_results': ['Material check passed']}; def validate_dimensions(state: BagState): return {'validation_results': ['Dimension check passed']}; graph = StateGraph(BagState); graph.add_node('durability', validate_durability); graph.add_node('dimensions', validate_dimensions); graph.add_edge('durability', 'dimensions'); graph.add_edge('dimensions', END); graph.set_entry_point('durability'); graph = graph.compile()