from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): material: str; specs: dict; approved: bool; def validate_specs(state: ForgingState): state['approved'] = state['specs'].get('tensile_strength', 0) > 500; return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()