from langgraph.graph import StateGraph, END; from typing import TypedDict; class MissileProcurementState(TypedDict): status: str; safety_clearance: bool; export_license: bool; def check_compliance(state: MissileProcurementState): return {'status': 'COMPLIANT' if state['safety_clearance'] and state['export_license'] else 'BLOCKED'}; graph = StateGraph(MissileProcurementState); graph.add_node('verify', check_compliance); graph.set_entry_point('verify'); graph.add_edge('verify', END); graph = graph.compile()