from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): doc_id: str; alloy_spec: str; validation_passed: bool; def validate_casting(state: CastState): state['validation_passed'] = 'Zn' in state['alloy_spec'] and len(state['alloy_spec']) > 2; return state; graph = StateGraph(CastState); graph.add_node('validate', validate_casting); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()