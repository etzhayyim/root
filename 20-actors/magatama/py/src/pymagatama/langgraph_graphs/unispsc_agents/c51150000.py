from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine_name: str; compliance_check: bool; cold_chain_required: bool; def check_regulatory(state: PharmState): state['compliance_check'] = True; return state; def verify_storage(state: PharmState): state['cold_chain_required'] = True; return state; graph = StateGraph(PharmState); graph.add_node('regulatory', check_regulatory); graph.add_node('storage', verify_storage); graph.set_entry_point('regulatory'); graph.add_edge('regulatory', 'storage'); graph.add_edge('storage', END); graph = graph.compile()