from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AdsorbentState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_spec(state: AdsorbentState): specs = state['input_data'].get('specs', {}); is_valid = 'purity_percentage' in specs and 'particle_size_mesh' in specs; return {'validation_results': ['spec_validated' if is_valid else 'spec_failed'], 'status': 'valid' if is_valid else 'invalid'}; def process_adsorbent(state: AdsorbentState): return {'status': 'processed'}; graph = StateGraph(AdsorbentState); graph.add_node('validate', validate_spec); graph.add_node('process', process_adsorbent); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()