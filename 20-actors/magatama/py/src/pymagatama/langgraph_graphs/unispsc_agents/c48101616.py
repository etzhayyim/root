from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): spec_data: dict; approved: bool; def validate_safety(state: ProcureState): return {'approved': state['spec_data'].get('NSF_certified', False)}; graph = StateGraph(ProcureState); graph.add_node('safety_check', validate_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()