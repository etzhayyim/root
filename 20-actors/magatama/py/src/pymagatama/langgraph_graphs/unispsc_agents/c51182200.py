from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): medicine_id: str; approval_status: bool; temperature_logs: List[float]; audit_log: List[str]; def validate_cold_chain(state: DrugProcurementState): if any(t > 8.0 for t in state['temperature_logs']): state['audit_log'].append('Cold chain failure'); return {'approval_status': False}; return {'approval_status': True}; graph = StateGraph(DrugProcurementState); graph.add_node('validate', validate_cold_chain); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()