from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class ToolState(TypedDict): input_spec: str; validation_report: list; status: str; def validate_specs(state: ToolState): report = ['Accuracy check passed' if 'tolerance' in state['input_spec'].lower() else 'Missing tolerance']; return {'validation_report': report, 'status': 'validated'}; def compile_procurement_graph(): graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_procurement_graph()