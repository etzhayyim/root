from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BallastState(TypedDict): specs: dict; compliance_status: bool; def validate_specs(state: BallastState): required = ['voltage', 'current']; missing = [f for f in required if f not in state['specs']]; state['compliance_status'] = len(missing) == 0; return state; graph = StateGraph(BallastState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()