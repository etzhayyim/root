from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RecorderState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: RecorderState): errors = []; if state['specs'].get('battery_life_hours', 0) < 5: errors.append('Insufficient battery'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(RecorderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()