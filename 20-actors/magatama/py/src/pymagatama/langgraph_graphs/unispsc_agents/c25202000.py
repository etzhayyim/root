from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AircraftPowerState(TypedDict): data: dict; validation_errors: Annotated[list, operator.add]; def validate_specs(state: AircraftPowerState): errors = []; if 'voltage_regulation' not in state['data']: errors.append('Missing voltage regulation spec'); return {'validation_errors': errors}; def check_compliance(state: AircraftPowerState): return {'validation_errors': ['Pending AS9100 audit'] if not state['data'].get('certified') else []}; graph = StateGraph(AircraftPowerState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()