from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; def validate_welds(state: AssemblyState): print('Checking weld integrity...'); return {'validation_results': ['Weld Structural Pass']}; def validate_specs(state: AssemblyState): print('Checking dimensions...'); return {'validation_results': ['Dimensions Pass']}; graph = StateGraph(AssemblyState); graph.add_node('weld_check', validate_welds); graph.add_node('dim_check', validate_specs); graph.add_edge('weld_check', 'dim_check'); graph.add_edge('dim_check', END); graph.set_entry_point('weld_check'); compiled_graph = graph.compile()