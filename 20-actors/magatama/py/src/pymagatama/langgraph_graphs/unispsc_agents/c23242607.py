from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FinishingState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: FinishingState): return {'validation_results': ['Surface roughness within tolerance' if state['specs'].get('ra') < 0.1 else 'Fail']}; def initialize_graph(): graph = StateGraph(FinishingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = initialize_graph()