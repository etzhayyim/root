from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): pump_specs: dict; validation_results: List[str]; graph = StateGraph(PumpState); def validate_specs(state: PumpState): return {"validation_results": ["Specs valid" if state['pump_specs'].get('flow_rate') else "Missing flow rate"]}; def route_procurement(state: PumpState): return "END"; graph.add_node("validate", validate_specs); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()