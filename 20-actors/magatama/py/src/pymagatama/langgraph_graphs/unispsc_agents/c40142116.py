from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): material: str; pressure_rating: float; compliant: bool; graph = StateGraph(PipeState); def validate_spec(state: PipeState): state['compliant'] = state['pressure_rating'] > 10.0; return state; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()