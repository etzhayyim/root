from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InspectionState(TypedDict): gage_specs: dict; status: str; def validate_dimensions(state: InspectionState): tolerance = state['gage_specs'].get('tolerance', 0.001); return {'status': 'validated'} if tolerance <= 0.005 else {'status': 'rejected'}; graph = StateGraph(InspectionState); graph.add_node('validate', validate_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()