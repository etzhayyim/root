from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DialysisMonitorState(TypedDict): monitor_id: str; calibration_valid: bool; specs: dict; ready_for_deployment: bool; def validate_specs(state: DialysisMonitorState): state['ready_for_deployment'] = state['calibration_valid'] and 'ISO_13485' in state['specs'].get('certs', []); return {'ready_for_deployment': state['ready_for_deployment']}; graph = StateGraph(DialysisMonitorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()