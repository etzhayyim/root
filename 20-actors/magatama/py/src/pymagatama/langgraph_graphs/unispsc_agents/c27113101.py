from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FishTapeState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: FishTapeState): results = []; if state['specs'].get('length', 0) <= 0: results.append('Invalid length'); return {'validation_results': results}; def check_material(state: FishTapeState): return {'validation_results': state['validation_results'] + ['Material verification complete']}; graph = StateGraph(FishTapeState); graph.add_node('validate', validate_specs); graph.add_node('material_check', check_material); graph.add_edge('validate', 'material_check'); graph.add_edge('material_check', END); graph.set_entry_point('validate'); graph = graph.compile()