from langgraph.graph import StateGraph, END; from typing import TypedDict; class OfficeState(TypedDict): specs: dict; validation_status: bool; def validate_specs(state: OfficeState): return {'validation_status': all(key in state['specs'] for key in ['dimensions', 'fire_rating'])}; graph = StateGraph(OfficeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()