from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProjectState(TypedDict): items: List[str]; validated_specs: List[str]; status: str; def validate_materials(state: ProjectState): validated = [item for item in state['items'] if 'non-toxic' in item]; return {'validated_specs': validated, 'status': 'validated'}; def compile_project_graph(): graph = StateGraph(ProjectState); graph.add_node('validate', validate_materials); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_project_graph()