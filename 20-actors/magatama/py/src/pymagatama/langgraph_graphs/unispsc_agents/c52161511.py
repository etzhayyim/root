from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadioState(TypedDict): radio_specs: dict; validation_report: str; approved: bool; def tech_spec_validator(state: RadioState): specs = state['radio_specs']; is_valid = all(key in specs for key in ['frequency', 'power']); return {'validation_report': 'Validated' if is_valid else 'Incomplete', 'approved': is_valid}; def compliance_check(state: RadioState): return {'approved': state['approved'] and 'certification' in state['radio_specs']}; graph = StateGraph(RadioState); graph.add_node('spec_validator', tech_spec_validator); graph.add_node('compliance', compliance_check); graph.add_edge('spec_validator', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('spec_validator'); graph = graph.compile()