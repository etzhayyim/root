from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): items: List[dict]; validation_status: bool; workflow_log: List[str]; def validate_tools(state: ToolState): state['validation_status'] = all('material' in item for item in state['items']); state['workflow_log'].append('Validation completed'); return state; def finalize_order(state: ToolState): state['workflow_log'].append('Order finalized'); return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_tools); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()