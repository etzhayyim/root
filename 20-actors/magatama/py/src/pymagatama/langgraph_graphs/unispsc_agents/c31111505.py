from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): metal_spec: dict; validity: bool; graph = StateGraph(State); def validate_spec(state: State): state['validity'] = 'Alloy Grade' in state['metal_spec'] and state['metal_spec']['Alloy Grade'] != ''; return state; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()