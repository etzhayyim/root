from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FuserState(TypedDict): model_number: str; verified: bool; def validate_compatibility(state: FuserState): return {'verified': state['model_number'].startswith('LJ-')}; def finalize_order(state: FuserState): return {'verified': True}; graph = StateGraph(FuserState); graph.add_node('validate', validate_compatibility); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()