from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GlasswareState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(GlasswareState); def validate_thermal_specs(state: GlasswareState): state['validation_report'].append('Thermal shock rating checked.'); return state; def check_vacuum_integrity(state: GlasswareState): state['validation_report'].append('Pressure capacity verified.'); return state; graph.add_node('thermal_val', validate_thermal_specs); graph.add_node('vacuum_val', check_vacuum_integrity); graph.set_entry_point('thermal_val'); graph.add_edge('thermal_val', 'vacuum_val'); graph.add_edge('vacuum_val', END); graph = graph.compile()