from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): reel_specs: dict; validation_report: str; def validate_reels(state: State): specs = state.get('reel_specs', {}); status = 'Valid' if 'ISO_11607' in specs else 'Invalid'; return {'validation_report': f'Device sterilization validation: {status}'}; graph = StateGraph(State); graph.add_node('validate', validate_reels); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()