from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_name: str; validation_passed: bool; inventory_check: bool; def validate_chemical(state: ChemicalState): state['validation_passed'] = state['chemical_name'] == 'Ammonium molybdate'; return state; def check_inventory(state: ChemicalState): state['inventory_check'] = True; return state; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_chemical); graph.add_node('inventory', check_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); compiled_graph = graph.compile()