from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PumpState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def check_pressure_spec(state: PumpState): state['approved'] = state['specs'].get('pressure_rating', 0) > 40; return state; graph = StateGraph(PumpState); graph.add_node('validate', check_pressure_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()