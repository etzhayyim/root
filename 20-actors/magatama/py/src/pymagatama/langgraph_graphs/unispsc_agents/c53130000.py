from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ItemState(TypedDict): items: List[str]; status: str; def validate_ingredients(state: ItemState) -> ItemState: return {'status': 'validated'} if all(i for i in state['items']) else {'status': 'failed'}; def check_compliance(state: ItemState) -> ItemState: return {'status': 'compliant'}; graph = StateGraph(ItemState); graph.add_node('validate', validate_ingredients); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()