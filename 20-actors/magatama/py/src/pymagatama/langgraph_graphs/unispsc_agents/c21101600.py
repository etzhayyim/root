from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: MachineState): errors = []; if state['specs'].get('power_rating_kw', 0) <= 0: errors.append('Invalid power rating'); return {'validation_errors': errors}; def check_approval(state: MachineState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(MachineState); graph.add_node('validate', validate_specs); graph.add_node('approval', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph.compile()