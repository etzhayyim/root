from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SutureState(TypedDict): needle_type: str; specs: dict; compliance_check: bool; def validate_specs(state: SutureState): return {'compliance_check': state['specs'].get('sterilization') == 'EO'}; def approve_procurement(state: SutureState): return {'compliance_check': True}; graph = StateGraph(SutureState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()