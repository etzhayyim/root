from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ETTState(TypedDict): tube_id: str; compliance: bool; status: str; def validate_iso_standards(state: ETTState): state['compliance'] = True; state['status'] = 'Validated'; return state; def check_sterilization(state: ETTState): state['status'] = 'Certified'; return state; graph = StateGraph(ETTState); graph.add_node('validate', validate_iso_standards); graph.add_node('certify', check_sterilization); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()