from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DiodeState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: DiodeState): state['valid'] = state['specs'].get('Vf', 0) < 1.0; return state; graph = StateGraph(DiodeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()