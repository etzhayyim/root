from langgraph.graph import StateGraph, END; from typing import TypedDict; class MetronomeState(TypedDict): specs: dict; valid: bool; def validate_specs(state: MetronomeState): state['valid'] = state['specs'].get('bpm', 0) > 0; return state; graph = StateGraph(MetronomeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()