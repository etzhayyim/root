from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; status: str; def validate_specs(state: State): state['status'] = 'VALIDATED' if 'laser_power' in state['input_data'] else 'FAILED'; return state; def check_compliance(state: State): state['status'] = 'COMPLIANT' if state['status'] == 'VALIDATED' else 'REJECTED'; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()