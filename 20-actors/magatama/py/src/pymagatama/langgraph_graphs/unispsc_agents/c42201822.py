from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class XRayProcurementState(TypedDict): vendor_certs: List[str]; safety_specs: dict; approved: bool; def validate_compliance(state: XRayProcurementState): return {'approved': 'ISO13485' in state['vendor_certs'] and 'lead_equiv' in state['safety_specs']}; graph = StateGraph(XRayProcurementState); graph.add_node('compliance', validate_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()