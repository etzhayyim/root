from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): status: str; purity: float; license_checked: bool; def check_license(state): state['license_checked'] = True; return state; def validate_purity(state): state['status'] = 'COMPLIANT' if state['purity'] > 0.95 else 'REJECTED'; return state; graph = StateGraph(ProcState); graph.add_node('check_license', check_license); graph.add_node('validate_purity', validate_purity); graph.add_edge('check_license', 'validate_purity'); graph.add_edge('validate_purity', END); graph.set_entry_point('check_license'); graph = graph.compile()