from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): vendor_docs: dict; status: str; spec_validation: bool; def check_gmp(state: PharmState): return {'spec_validation': 'GMP_Cert' in state['vendor_docs']}; def finalize(state: PharmState): return {'status': 'Approved' if state['spec_validation'] else 'Rejected'}; graph = StateGraph(PharmState); graph.add_node('verify_gmp', check_gmp); graph.add_node('finalize', finalize); graph.set_entry_point('verify_gmp'); graph.add_edge('verify_gmp', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()