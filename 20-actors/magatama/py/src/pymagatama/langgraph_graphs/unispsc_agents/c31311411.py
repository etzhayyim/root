from langgraph.graph import StateGraph, END; from typing import TypedDict; class PipelineState(TypedDict): material_cert: str; weld_audit: bool; approved: bool; def validate_material(state: PipelineState): return {'material_cert': 'Validated'}; def audit_weld(state: PipelineState): return {'weld_audit': True, 'approved': True}; graph = StateGraph(PipelineState); graph.add_node('material', validate_material); graph.add_node('weld', audit_weld); graph.set_entry_point('material'); graph.add_edge('material', 'weld'); graph.add_edge('weld', END); graph = graph.compile()