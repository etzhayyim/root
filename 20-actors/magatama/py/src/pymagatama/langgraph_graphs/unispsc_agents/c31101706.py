from langgraph.graph import StateGraph, END; from typing import TypedDict; class CastingState(TypedDict): specs: dict; valid: bool;  def validate_casting(state: CastingState): state['valid'] = 'alloy' in state['specs'] and 'tolerance' in state['specs']; return state; builder = StateGraph(CastingState); builder.add_node('validate', validate_casting); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()