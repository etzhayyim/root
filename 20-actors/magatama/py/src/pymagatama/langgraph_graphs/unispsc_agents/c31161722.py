from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UnionNutState(TypedDict): specs: dict; compliance_check: bool; validation_log: List[str]; def validate_specs(state: UnionNutState): specs = state['specs']; log = []; compliant = True; if 'thread_standard' not in specs: log.append('Missing thread spec'); compliant = False; if 'material' not in specs: log.append('Missing material spec'); compliant = False; return {'compliance_check': compliant, 'validation_log': log}; graph = StateGraph(UnionNutState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()