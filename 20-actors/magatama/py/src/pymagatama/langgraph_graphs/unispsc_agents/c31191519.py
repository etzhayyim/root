from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AbrasiveSpec(TypedDict): abrasive_type: str; grit: int; rpm_limit: int; audit_status: str; def validate_specs(state: AbrasiveSpec): return {'audit_status': 'PASSED' if state['rpm_limit'] > 0 else 'FAILED'}; graph = StateGraph(AbrasiveSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()