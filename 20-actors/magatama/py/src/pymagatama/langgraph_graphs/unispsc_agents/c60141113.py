from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LotteryState(TypedDict): items: List[str]; compliant: bool; audit_result: str; def validate_compliance(state: LotteryState): state['compliant'] = True if 'license' in state['items'] else False; return state; def run_audit(state: LotteryState): state['audit_result'] = 'PASSED' if state['compliant'] else 'FAILED'; return state; graph = StateGraph(LotteryState); graph.add_node('validate', validate_compliance); graph.add_node('audit', run_audit); graph.set_entry_point('validate'); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); graph = graph.compile()