from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserProcState(TypedDict): spec: dict; valid: bool; log: List[str]; def validate_laser_spec(state: LaserProcState): spec = state['spec']; is_valid = all(k in spec for k in ['laser_class', 'power']) and spec['power'] > 0; return {'valid': is_valid, 'log': ['Spec valid' if is_valid else 'Spec incomplete']}; graph = StateGraph(LaserProcState); graph.add_node('validate', validate_laser_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()