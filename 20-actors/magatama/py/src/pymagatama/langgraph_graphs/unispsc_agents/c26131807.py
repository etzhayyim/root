from langgraph.graph import StateGraph, END; from typing import TypedDict; class TurbineState(TypedDict): panel_id: str; spec_valid: bool; risk_level: str; def validate_specs(state: TurbineState): state['spec_valid'] = True; return state; def assess_risk(state: TurbineState): state['risk_level'] = 'high'; return state; graph = StateGraph(TurbineState); graph.add_node('validate', validate_specs); graph.add_node('risk_assessment', assess_risk); graph.add_edge('validate', 'risk_assessment'); graph.add_edge('risk_assessment', END); graph.set_entry_point('validate'); graph = graph.compile()