from typing import TypedDict, Annotated, List, Union; from langgraph.graph import StateGraph, END; class CommodityState(TypedDict): code: str; specs: dict; validation_status: bool; workflow_log: List[str]; def validate_purity(state: CommodityState): state['validation_status'] = state['specs'].get('purity_percentage', 0) > 99.0; state['workflow_log'].append('Purity validation complete'); return state; def check_export_compliance(state: CommodityState): state['workflow_log'].append('Checking dual-use compliance'); return state; graph = StateGraph(CommodityState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_export_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()