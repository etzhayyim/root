from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): vendor_id: str; compliance_docs: List[str]; validated: bool; def validate_oss(state: NetworkState): state['validated'] = len(state['compliance_docs']) > 0; return state; graph = StateGraph(NetworkState); graph.add_node('validate', validate_oss); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()