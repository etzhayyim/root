from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): medicine_name: str; validation_status: bool; compliance_docs: List[str]; def validate_pharmaceutical(state: ProcurementState): state['validation_status'] = 'clomiphene' in state['medicine_name'].lower(); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_pharmaceutical); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()