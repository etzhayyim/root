from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SilverWireState(TypedDict): wire_specs: dict; inspection_status: bool; alerts: List[str]; def validate_purity(state: SilverWireState): if state['wire_specs'].get('purity', 0) < 99.9: state['alerts'].append('Purity below 99.9%'); return {'inspection_status': False}; return {'inspection_status': True}; graph = StateGraph(SilverWireState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()