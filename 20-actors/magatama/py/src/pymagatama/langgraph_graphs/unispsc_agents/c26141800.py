from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HotCellState(TypedDict): vendor_docs: List[str]; safety_verified: bool; compliance_status: str; def validate_integrity(state: HotCellState): return {'safety_verified': True, 'compliance_status': 'Verified'}; def run_compliance_check(state: HotCellState): return {'compliance_status': 'Export-Control-Cleared'}; graph = StateGraph(HotCellState); graph.add_node('integrity', validate_integrity); graph.add_node('compliance', run_compliance_check); graph.set_entry_point('integrity'); graph.add_edge('integrity', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()