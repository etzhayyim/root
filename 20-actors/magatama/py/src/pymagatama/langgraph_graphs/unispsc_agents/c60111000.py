from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PosterState(TypedDict): content_title: str; dimensions: str; material: str; verified: bool; def validate_poster(state: PosterState): print(f'Validating poster: {state[content_title]}'); return {'verified': True}; graph = StateGraph(PosterState); graph.add_node('validate', validate_poster); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()