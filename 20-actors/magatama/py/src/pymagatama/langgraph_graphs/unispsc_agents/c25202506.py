from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftDoorState(TypedDict): door_id: str; structural_integrity_verified: bool; export_license_cleared: bool; graph = StateGraph(AircraftDoorState); def validate_structure(state: AircraftDoorState): return {'structural_integrity_verified': True}; def check_export_compliance(state: AircraftDoorState): return {'export_license_cleared': True}; graph.add_node('validate', validate_structure); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()