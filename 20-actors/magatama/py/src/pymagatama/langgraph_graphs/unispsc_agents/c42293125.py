from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): device_id: str; compliance_verified: bool; def validate_device(state: State): state['compliance_verified'] = True; return state; graph = StateGraph(State); graph.add_node('validate', validate_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()