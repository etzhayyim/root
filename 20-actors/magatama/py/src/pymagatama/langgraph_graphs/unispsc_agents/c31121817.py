from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_ceramic_specs(state: State): status = 'PASS' if state['specs'].get('thermal_expansion', 0) < 5.0 else 'FAIL'; return {'validation_report': f'Constraint Check: {status}', 'approved': status == 'PASS'}; graph = StateGraph(State); graph.add_node('validate', validate_ceramic_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()