from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphicState(TypedDict): content: str; specs: dict; approved: bool; def validate_specs(state: GraphicState): state['approved'] = 'dimensions' in state['specs'] and 'material' in state['specs']; return state; def route_approval(state: GraphicState): return 'approved' if state['approved'] else END; graph = StateGraph(GraphicState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_conditional_edges('validator', route_approval, {'approved': END}); graph.add_edge('validator', END); graph = graph.compile()