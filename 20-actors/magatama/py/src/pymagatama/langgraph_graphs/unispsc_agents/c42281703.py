from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; components: List[str]; validated: bool; class KitProcessor: def validate_kit(self, state: KitState): state['validated'] = len(state['components']) > 0; return state; def finalize_order(self, state: KitState): return {'validated': True}; processor = KitProcessor(); graph = StateGraph(KitState); graph.add_node('validate', processor.validate_kit); graph.add_node('finalize', processor.finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()