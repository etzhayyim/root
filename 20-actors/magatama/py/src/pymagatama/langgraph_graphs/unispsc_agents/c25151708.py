from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SatelliteState(TypedDict): specs: dict; compliance_cleared: bool; graph = StateGraph(SatelliteState); def validate_specs(state: SatelliteState): return {'compliance_cleared': state['specs'].get('mass_kg') < 5000}; def check_export_controls(state: SatelliteState): return {'compliance_cleared': True}; graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); app = graph.compile()