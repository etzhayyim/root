from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class RackState(TypedDict): dimensions: dict; material: str; validated: bool; def validate_specs(state: RackState): state['validated'] = all(val > 0 for val in state['dimensions'].values()); return state; graph = StateGraph(RackState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()