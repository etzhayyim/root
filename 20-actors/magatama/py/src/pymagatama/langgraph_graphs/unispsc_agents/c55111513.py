from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class EduContentState(TypedDict): content_url: str; is_accessible: bool; license_validated: bool; def validate_accessibility(state: EduContentState): return {'is_accessible': True} if 'compliant' in state['content_url'] else {'is_accessible': False}; def check_licensing(state: EduContentState): return {'license_validated': True}; graph = StateGraph(EduContentState); graph.add_node('accessibility_check', validate_accessibility); graph.add_node('license_check', check_licensing); graph.set_entry_point('accessibility_check'); graph.add_edge('accessibility_check', 'license_check'); graph.add_edge('license_check', END); graph = graph.compile()