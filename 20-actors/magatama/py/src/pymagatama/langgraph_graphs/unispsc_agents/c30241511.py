from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StakeProcurementState(TypedDict): specs: dict; validation_passed: bool; report: str; def validate_stake_specs(state: StakeProcurementState): state['validation_passed'] = 'Material Grade' in state['specs'] and 'Tensile Strength' in state['specs']; return state; def finalize_procurement(state: StakeProcurementState): state['report'] = 'Validation successful' if state['validation_passed'] else 'Specs Incomplete'; return state; graph = StateGraph(StakeProcurementState); graph.add_node('validate', validate_stake_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()