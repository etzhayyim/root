from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MessKitState(TypedDict): vendor_certs: dict; mil_spec_verified: bool; material_spec: str; status: str; def validate_specs(state: MessKitState): state['mil_spec_verified'] = 'MIL-SPEC' in state['material_spec']; return state; def check_compliance(state: MessKitState): if not state['mil_spec_verified']: state['status'] = 'FLAGGED_FOR_REVIEW'; return state; state['status'] = 'APPROVED'; return state; graph = StateGraph(MessKitState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()