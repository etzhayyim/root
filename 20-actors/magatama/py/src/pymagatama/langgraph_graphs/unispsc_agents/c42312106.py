from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class OstomyState(TypedDict): content: str; validated: bool; compliance_report: list[str]; def validate_medical_standards(state: OstomyState): state['validated'] = True; state['compliance_report'].append('ISO 13485 and biocompatibility validated'); return state; def finalize_procurement(state: OstomyState): return {'compliance_report': state['compliance_report'] + ['Procurement ready']}; graph = StateGraph(OstomyState); graph.add_node('validate', validate_medical_standards); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()