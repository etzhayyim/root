from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NarcoticState(TypedDict): vendor_id: str; license_valid: bool; quantity: float; compliance_check: bool; def verify_vendor(state: NarcoticState): state['license_valid'] = True; return state; def check_compliance(state: NarcoticState): state['compliance_check'] = state['license_valid'] and state['quantity'] > 0; return state; graph = StateGraph(NarcoticState); graph.add_node('verify', verify_vendor); graph.add_node('compliance', check_compliance); graph.set_entry_point('verify'); graph.add_edge('verify', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()