from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClutchProcureState(TypedDict): vendor_specs: dict; inspection_status: str; graph = StateGraph(ClutchProcureState); def validate_pressure(state: ClutchProcureState): return {'inspection_status': 'validated'} if state['vendor_specs'].get('pressure_psi', 0) > 0 else {'inspection_status': 'failed'}; def audit_materials(state: ClutchProcureState): return {'inspection_status': 'audited'}; graph.add_node('validate', validate_pressure); graph.add_node('audit', audit_materials); graph.set_entry_point('validate'); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); graph = graph.compile()