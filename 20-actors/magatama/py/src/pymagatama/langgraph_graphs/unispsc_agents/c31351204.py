from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeSpecState(TypedDict): specs: List[str]; validation_status: str; def validate_inconel_specs(state: TubeSpecState): return {'validation_status': 'passed' if 'Material Grade Certificate' in state['specs'] else 'failed'}; graph = StateGraph(TubeSpecState); graph.add_node('validator', validate_inconel_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()