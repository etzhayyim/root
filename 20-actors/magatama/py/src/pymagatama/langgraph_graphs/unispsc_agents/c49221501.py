from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScoreboardState(TypedDict): vendor_specs: dict; validation_results: list; graph = StateGraph(ScoreboardState); def validate_electrical(state: ScoreboardState): return {'validation_results': ['Electrical Safety Verified']}; def check_weather_rating(state: ScoreboardState): return {'validation_results': ['IP-65 Compliant']}; graph.add_node('elec_check', validate_electrical); graph.add_node('weather_check', check_weather_rating); graph.set_entry_point('elec_check'); graph.add_edge('elec_check', 'weather_check'); graph.add_edge('weather_check', END); graph = graph.compile()