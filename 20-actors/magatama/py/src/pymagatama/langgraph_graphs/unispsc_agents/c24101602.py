from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HoistState(TypedDict): hoist_id: str; specs: dict; valid: bool; def validate_specs(state: HoistState): capacity = state['specs'].get('capacity', 0); state['valid'] = capacity > 0; return state; def route_verification(state: HoistState): return 'valid' if state['valid'] else END; graph = StateGraph(HoistState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()