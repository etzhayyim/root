from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PencilState(TypedDict): items: List[str]; validated: bool; def validate_pencils(state: PencilState): validated = all(len(item) > 0 for item in state['items']); return {'validated': validated}; def finalize_procurement(state: PencilState): return {'status': 'processed'}; graph = StateGraph(PencilState); graph.add_node('validate', validate_pencils); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()