from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): signal_type: str; integrity_check: bool; graph = StateGraph(GraphState); def validate_input_config(state: GraphState): return {'integrity_check': state.get('signal_type') in ['4-20mA', '0-10V']}; def route_to_controller(state: GraphState): return 'end'; graph.add_node('validate', validate_input_config); graph.add_node('route', route_to_controller); graph.set_entry_point('validate'); graph.add_edge('validate', 'route'); graph.add_edge('route', END); compiled_graph = graph.compile()