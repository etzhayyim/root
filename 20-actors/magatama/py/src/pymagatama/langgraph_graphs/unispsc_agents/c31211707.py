from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VarnishState(TypedDict): specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_voc(state: VarnishState): state['compliance_status'] = state['specs'].get('voc', 0) < 50; state['workflow_log'].append('VOC Validation Complete'); return state; def check_hazmat(state: VarnishState): state['workflow_log'].append('HazMat screening passed'); return state; graph = StateGraph(VarnishState); graph.add_node('validate_voc', validate_voc); graph.add_node('hazmat_check', check_hazmat); graph.set_entry_point('validate_voc'); graph.add_edge('validate_voc', 'hazmat_check'); graph.add_edge('hazmat_check', END); graph = graph.compile()