from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[dict]; validation_status: bool; def validate_decorations(state: ProcState): state['validation_status'] = all('material' in item and 'dimensions' in item for item in state['items']); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_decorations); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()