from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DispenserState(TypedDict): specs: dict; validation_errors: Annotated[list, operator.add]; status: str; def validate_specs(state: DispenserState): results = []; target_viscosity = state['specs'].get('viscosity'); if not target_viscosity: results.append('Missing viscosity spec'); return {'validation_errors': results, 'status': 'validated' if not results else 'error'}; graph = StateGraph(DispenserState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()