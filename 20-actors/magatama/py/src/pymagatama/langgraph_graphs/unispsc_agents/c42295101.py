from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BasinState(TypedDict): specs: dict; validation_log: List[str]; approved: bool; def val_material(state: BasinState): state['validation_log'].append('Checking material grade...'); return state; def val_certs(state: BasinState): state['validation_log'].append('Verifying medical certs...'); state['approved'] = True; return state; graph = StateGraph(BasinState); graph.add_node('val_material', val_material); graph.add_node('val_certs', val_certs); graph.set_entry_point('val_material'); graph.add_edge('val_material', 'val_certs'); graph.add_edge('val_certs', END); graph = graph.compile()