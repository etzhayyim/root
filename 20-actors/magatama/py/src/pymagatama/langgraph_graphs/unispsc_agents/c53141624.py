from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_report: list[str]; graph = StateGraph(State); def validate_materials(state: State): state['validation_report'].append('Verified material compliance'); return state; def check_needle_safety(state: State): state['validation_report'].append('Checked needle standards'); return state; graph.add_node('validate', validate_materials); graph.add_node('safety', check_needle_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()