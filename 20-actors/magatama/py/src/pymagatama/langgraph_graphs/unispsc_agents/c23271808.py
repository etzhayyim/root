from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FluxState(TypedDict): flux_type: str; safety_compliant: bool; validation_log: List[str]; def check_compliance(state: FluxState): state['safety_compliant'] = state['flux_type'] in ['No-Clean', 'RMA']; state['validation_log'].append('Compliance checked.'); return state; def finalize_procurement(state: FluxState): state['validation_log'].append('Procurement approved.'); return state; graph = StateGraph(FluxState); graph.add_node('compliance', check_compliance); graph.add_node('finalize', finalize_procurement); graph.add_edge('compliance', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('compliance'); graph = graph.compile()