from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_data: dict; validation_results: list[str]; status: str; def validate_purity(state: ChemicalState): result = 'Purity OK' if state['chemical_data'].get('purity', 0) > 99.0 else 'Purity Fail'; return {'validation_results': [result], 'status': 'validated'}; def check_storage(state: ChemicalState): return {'status': 'storage_verified'}; graph = StateGraph(ChemicalState); graph.add_node('purity_check', validate_purity); graph.add_node('storage_check', check_storage); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'storage_check'); graph.add_edge('storage_check', END); graph = graph.compile()