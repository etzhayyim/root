from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class RobotGearState(TypedDict): input_data: dict; validation_results: Annotated[list[str], operator.add]; status: str; def validate_specs(state: RobotGearState): results = []; data = state.get('input_data', {}); if data.get('backlash') and data['backlash'] > 0.05: results.append('Backlash exceeds threshold'); return {'validation_results': results, 'status': 'validated' if not results else 'error'}; def build_graph(): graph = StateGraph(RobotGearState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = build_graph()