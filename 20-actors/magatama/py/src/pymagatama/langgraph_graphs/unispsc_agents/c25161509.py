from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class BikeSpecs(TypedDict): model_info: str; safety_compliant: bool; graph = StateGraph(BikeSpecs); def validate_safety(state: BikeSpecs): return {'safety_compliant': 'CPSC' in state['model_info']}; def finalize_procurement(state: BikeSpecs): return state; graph.add_node('validate', validate_safety); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compile_graph = graph.compile()