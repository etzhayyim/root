from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TowbarState(TypedDict): model_id: str; structural_specs: dict; certification_check: bool; workflow_steps: List[str]; def validate_specs(state: TowbarState): state['workflow_steps'].append('Validating material integrity'); return {'certification_check': True}; def finalize_approval(state: TowbarState): state['workflow_steps'].append('Finalizing GSE certification document'); return {'workflow_steps': state['workflow_steps']}; graph = StateGraph(TowbarState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()