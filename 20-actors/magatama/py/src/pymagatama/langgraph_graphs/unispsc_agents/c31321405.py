from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): data: dict; checks: List[str]; graph = StateGraph(AssemblyState); def validate_welds(state: AssemblyState): return {'checks': state.get('checks', []) + ['weld_inspection_complete']}; def verify_material(state: AssemblyState): return {'checks': state.get('checks', []) + ['material_cert_verified']}; graph.add_node('weld_check', validate_welds); graph.add_node('material_check', verify_material); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'weld_check'); graph.add_edge('weld_check', END); graph = graph.compile()