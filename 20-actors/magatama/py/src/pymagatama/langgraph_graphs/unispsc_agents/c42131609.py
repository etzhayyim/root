from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShoeCoverState(TypedDict): specs: dict; validation_passed: bool; def check_specs(state: ShoeCoverState): state['validation_passed'] = 'sterility_level' in state['specs'] and state['specs']['fluid_resistance_rating'] > 0; return state; graph = StateGraph(ShoeCoverState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()