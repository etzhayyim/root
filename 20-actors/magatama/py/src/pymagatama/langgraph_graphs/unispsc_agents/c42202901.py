from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class XrayState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: XrayState): state['compliance'] = state['specs'].get('kVp', 0) < 150; return {'compliance': state['compliance']}; def log_deployment(state: XrayState): print('Processing deployment for X-ray unit'); return {'status': 'READY'}; graph = StateGraph(XrayState); graph.add_node('validate', validate_specs); graph.add_node('log', log_deployment); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph = graph.compile()