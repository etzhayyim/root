from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; status: str; def validate_spec(state: State): return {'status': 'validated'} if 'encryption_standard' in state['input_data'] else {'status': 'invalid'}; def process_locking_unit(state: State): return {'status': 'processed'}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('process', process_locking_unit); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()