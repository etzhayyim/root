from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(State); def validate_compliance(state: State): return {'validation_results': {'sterile': True, 'expiry_check': 'passed'}}; def check_regulatory(state: State): return {'validation_results': {'fda_status': 'cleared'}}; graph.add_node('compliance', validate_compliance); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()