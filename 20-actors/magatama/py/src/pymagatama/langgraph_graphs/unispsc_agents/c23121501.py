from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CompressorState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: CompressorState): state['validation_errors'] = []; if state['specs'].get('pressure', 0) > 100: state['validation_errors'].append('Pressure exceeds safety limit'); return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()