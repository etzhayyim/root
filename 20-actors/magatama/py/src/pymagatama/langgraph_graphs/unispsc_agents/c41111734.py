from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StageState(TypedDict): model: str; specs: dict; valid: bool; def validate_specs(state: StageState): state['valid'] = 'travel_range_mm' in state['specs'] and state['specs']['travel_range_mm'] > 0; return state; graph = StateGraph(StageState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()