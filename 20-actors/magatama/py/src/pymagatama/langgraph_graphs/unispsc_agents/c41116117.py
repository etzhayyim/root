from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): items: List[str]; validation_passed: bool; def validate_reagent(state: AgentState): return {'validation_passed': all('purity' in item.lower() for item in state['items'])}; def log_compliance(state: AgentState): print('Compliance checked for environmental reagents'); return state; graph = StateGraph(AgentState); graph.add_node('validate', validate_reagent); graph.add_node('log', log_compliance); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()