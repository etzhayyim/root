from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeldingState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: WeldingState): state['approved'] = all(k in state['specs'] for k in ['amps', 'duty_cycle']); return state; def check_export(state: WeldingState): state['validation_report'] = 'Dual-use check passed' if state['approved'] else 'Missing technical specs'; return state; graph = StateGraph(WeldingState); graph.add_node('validate', validate_specs); graph.add_node('export', check_export); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph.set_entry_point('validate'); graph = graph.compile()