from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RescueSignalState(TypedDict): signal_type: str; is_verified: bool; compliance_docs: List[str]; def validate_dye_spec(state: RescueSignalState): return {'is_verified': 'SOLAS' in state['compliance_docs']}; def check_safety(state: RescueSignalState): return {'is_verified': state['is_verified'] and state['signal_type'] == 'fluorescein'}; graph = StateGraph(RescueSignalState); graph.add_node('validation', validate_dye_spec); graph.add_node('safety_check', check_safety); graph.set_entry_point('validation'); graph.add_edge('validation', 'safety_check'); graph.add_edge('safety_check', END); compiled_graph = graph.compile()