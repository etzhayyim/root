from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; status: str; def validate_specs(state: State): return {'status': 'validated' if 'voltage' in state['input_data'] else 'error'}; def check_safety(state: State): return {'status': 'safety_cleared'}; builder = StateGraph(State); builder.add_node('validate', validate_specs); builder.add_node('safety', check_safety); builder.add_edge('validate', 'safety'); builder.add_edge('safety', END); builder.set_entry_point('validate'); graph = builder.compile()