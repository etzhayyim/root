from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EngineState(TypedDict): engine_id: str; compliance_docs: List[str]; inspection_status: str; graph = StateGraph(EngineState); def validate_docs(state: EngineState): return {'inspection_status': 'DOCUMENT_VERIFIED'} if len(state['compliance_docs']) > 2 else {'inspection_status': 'REJECTED'}; def perform_spec_check(state: EngineState): return {'inspection_status': 'APPROVED'}; graph.add_node('validate', validate_docs); graph.add_node('check_specs', perform_spec_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_specs'); graph.add_edge('check_specs', END); app = graph.compile()