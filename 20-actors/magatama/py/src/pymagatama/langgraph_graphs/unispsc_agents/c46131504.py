from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RocketState(TypedDict): status: str; spec_data: dict; compliance: bool; def validate_specs(state: RocketState): return {'status': 'validated' if 'thrust' in state['spec_data'] else 'error'}; def check_compliance(state: RocketState): return {'compliance': True}; graph = StateGraph(RocketState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()