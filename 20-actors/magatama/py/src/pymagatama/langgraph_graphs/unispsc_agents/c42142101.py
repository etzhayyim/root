from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class TherapyCoverState(TypedDict): input_spec: dict; compliance_status: bool; validation_log: list; def validate_materials(state: TherapyCoverState): state['validation_log'].append('Checking material biocompatibility...'); state['compliance_status'] = True; return state; def check_thermal_specs(state: TherapyCoverState): state['validation_log'].append('Verifying thermal insulation...'); return state; graph = StateGraph(TherapyCoverState); graph.add_node('material_check', validate_materials); graph.add_node('thermal_check', check_thermal_specs); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'thermal_check'); graph.add_edge('thermal_check', END); compiled_graph = graph.compile()