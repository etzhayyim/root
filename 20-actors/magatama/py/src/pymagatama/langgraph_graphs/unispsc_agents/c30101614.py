from langgraph.graph import StateGraph, END; from typing import TypedDict; class LeadState(TypedDict): purity: float; hazard_cleared: bool; graph = StateGraph(LeadState); def validate_purity(state: LeadState): return {'hazard_cleared': state['purity'] >= 99.9}; graph.add_node('validation', validate_purity); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()