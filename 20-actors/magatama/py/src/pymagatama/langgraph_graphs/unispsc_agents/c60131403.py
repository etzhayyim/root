from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TambourineState(TypedDict): specs: dict; valid: bool; def validate_specs(state: TambourineState): state['valid'] = 'material_composition' in state['specs'] and 'jingle_material' in state['specs']; return state; builder = StateGraph(TambourineState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()