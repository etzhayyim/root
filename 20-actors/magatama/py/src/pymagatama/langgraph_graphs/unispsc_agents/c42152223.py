from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): unit_id: str; specs: dict; validation_passed: bool; def validate_specs(state: DentalState): state['validation_passed'] = all(k in state['specs'] for k in ['iso_cert', 'power_output']); return state; def check_compliance(state: DentalState): print(f'Checking compliance for {state['unit_id']}'); return {'validation_passed': True}; graph = StateGraph(DentalState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()