from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): content: str; specs: dict; valid: bool; def validate_specs(state: State): state['valid'] = all(k in state['specs'] for k in ['capacity', 'sterility']); return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()