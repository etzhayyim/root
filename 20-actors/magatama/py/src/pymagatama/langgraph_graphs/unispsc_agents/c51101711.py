from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DrugProcureState(TypedDict): vendor_compliance: bool; material_spec: dict; approved: bool; def validate_spec(state: DrugProcureState): state['approved'] = all(k in state['material_spec'] for k in ['purity', 'cert_id']); return state; def check_compliance(state: DrugProcureState): state['vendor_compliance'] = True; return state; graph = StateGraph(DrugProcureState); graph.add_node('compliance', check_compliance); graph.add_node('validation', validate_spec); graph.add_edge('compliance', 'validation'); graph.add_edge('validation', END); graph.set_entry_point('compliance'); graph = graph.compile()