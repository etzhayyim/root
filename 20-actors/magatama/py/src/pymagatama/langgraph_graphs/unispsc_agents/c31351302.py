from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_errors: List[str]; validated: bool; def check_materials(state: AssemblyState): state['validation_errors'] = [] if 'grade' not in state['specs'] else []; return state; def validate_rivets(state: AssemblyState): print('Validating rivet pattern torque and spacing.'); state['validated'] = True; return state; graph = StateGraph(AssemblyState); graph.add_node('material_check', check_materials); graph.add_node('rivet_validation', validate_rivets); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'rivet_validation'); graph.add_edge('rivet_validation', END); graph = graph.compile()