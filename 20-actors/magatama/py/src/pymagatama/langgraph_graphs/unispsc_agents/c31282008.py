from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): component_id: str; dims: dict; certified: bool; graph = StateGraph(State); def validate_specs(state: State): state['certified'] = state.get('dims', {}).get('thickness', 0) > 0; return state; def final_check(state: State): return 'approved' if state['certified'] else 'rejected'; graph.add_node('validate', validate_specs); graph.add_node('check', final_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()