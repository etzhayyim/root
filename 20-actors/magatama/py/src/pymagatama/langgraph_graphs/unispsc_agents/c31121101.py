from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DieCastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: DieCastingState): state['validation_results'].append('Dimensional check passed'); return state; def check_porosity(state: DieCastingState): state['validation_results'].append('X-Ray porosity check cleared'); return state; graph = StateGraph(DieCastingState); graph.add_node('dimension_check', validate_dimensions); graph.add_node('porosity_check', check_porosity); graph.add_edge('dimension_check', 'porosity_check'); graph.add_edge('porosity_check', END); graph.set_entry_point('dimension_check'); graph = graph.compile()