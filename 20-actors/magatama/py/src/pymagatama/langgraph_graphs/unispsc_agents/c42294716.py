from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PerfusionState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: PerfusionState): state['valid'] = all(key in state['specs'] for key in ['sterilization', 'material']); state['logs'].append('Validation complete.'); return state; graph = StateGraph(PerfusionState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()