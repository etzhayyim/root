from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; flags: List[str]; def validate_chemical_safety(state: ProcureState): print('Validating SDS compliance...'); state['flags'].append('SDS_VERIFIED'); return state; def check_bonding_specs(state: ProcureState): print('Checking spec criteria...'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_chemical_safety); graph.add_node('specs', check_bonding_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'specs'); graph.add_edge('specs', END); app = graph.compile()