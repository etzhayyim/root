from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShotPeeningState(TypedDict): specs: dict; validation_results: List[str]; def validate_nozzle_specs(state: ShotPeeningState): state['validation_results'].append('Pressure check passed'); return state; def check_compliance(state: ShotPeeningState): state['validation_results'].append('Dual-use compliance verified'); return state; graph = StateGraph(ShotPeeningState); graph.add_node('validate', validate_nozzle_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()