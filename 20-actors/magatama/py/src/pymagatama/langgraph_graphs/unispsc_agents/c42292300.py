from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalToolState(TypedDict): tool_specs: dict; compliance_verified: bool; def validate_instrument(state: SurgicalToolState): state['compliance_verified'] = 'ISO-13485' in state['tool_specs'].get('certs', []); return state; graph = StateGraph(SurgicalToolState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()