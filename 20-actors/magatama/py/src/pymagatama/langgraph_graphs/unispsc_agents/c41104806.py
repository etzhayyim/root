from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): input_spec: dict; validated: bool; analysis_result: str; def validate_specs(state: ProcState): state['validated'] = 'solvent_compatibility_list' in state['input_spec']; return state; def check_compliance(state: ProcState): state['analysis_result'] = 'Approved' if state['validated'] else 'Rejected'; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()