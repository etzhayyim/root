from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class UrethralSoundState(TypedDict): vendor_certs: List[str]; material_spec: str; validated: bool; def validate_instrument(state: UrethralSoundState): state['validated'] = 'ISO-13485' in state['vendor_certs'] and state['material_spec'] == 'medical-grade-steel'; return state; graph = StateGraph(UrethralSoundState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()