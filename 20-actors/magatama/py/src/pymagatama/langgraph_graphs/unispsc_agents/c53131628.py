from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShampooState(TypedDict): ingredients: List[str]; safety_approved: bool; compliant_packaging: bool; def validate_ingredients(state: ShampooState): state['safety_approved'] = 'toxic_substance' not in state['ingredients']; return state; def check_compliance(state: ShampooState): state['compliant_packaging'] = True; return state; graph = StateGraph(ShampooState); graph.add_node('validate', validate_ingredients); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()