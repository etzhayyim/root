from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MotionProcurementState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; compliance_status: bool; def check_export_controls(state: MotionProcurementState): return {'validation_results': ['Export Compliance Verified']}; def validate_technical_specs(state: MotionProcurementState): return {'compliance_status': True}; graph = StateGraph(MotionProcurementState); graph.add_node('export_check', check_export_controls); graph.add_node('spec_validation', validate_technical_specs); graph.set_entry_point('export_check'); graph.add_edge('export_check', 'spec_validation'); graph.add_edge('spec_validation', END); compiled_graph = graph.compile()