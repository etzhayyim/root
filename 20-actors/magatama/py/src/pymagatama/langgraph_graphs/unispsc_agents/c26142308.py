from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShieldingState(TypedDict): specs: dict; validation_results: List[str]; def validate_density(state: ShieldingState): if state['specs'].get('density') < 11.34: return {'validation_results': ['Density below standard']}; return {'validation_results': ['Density approved']}; def check_hazmat(state: ShieldingState): return {'validation_results': state['validation_results'] + ['Hazmat shipping compliance confirmed']}; graph = StateGraph(ShieldingState); graph.add_node('validate_density', validate_density); graph.add_node('hazmat_check', check_hazmat); graph.set_entry_point('validate_density'); graph.add_edge('validate_density', 'hazmat_check'); graph.add_edge('hazmat_check', END); graph = graph.compile()