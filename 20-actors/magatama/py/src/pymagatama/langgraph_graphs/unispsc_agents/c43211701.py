from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScannerState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ScannerState): errors = []; if 'ip_rating' not in state['specs']: errors.append('Missing IP rating'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(ScannerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()