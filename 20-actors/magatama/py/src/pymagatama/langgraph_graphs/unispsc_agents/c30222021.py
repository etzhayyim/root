from langgraph.graph import StateGraph, END; from typing import TypedDict; class MarinaState(TypedDict): site_specs: dict; status: str; def validate_specs(state: MarinaState): state['status'] = 'VALIDATED' if 'depth' in state['site_specs'] else 'PENDING'; return state; def construction_workflow(state: MarinaState): return {'status': 'READY_FOR_COMMISSION'}; graph = StateGraph(MarinaState); graph.add_node('validate', validate_specs); graph.add_node('build', construction_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'build'); graph.add_edge('build', END); app = graph.compile()