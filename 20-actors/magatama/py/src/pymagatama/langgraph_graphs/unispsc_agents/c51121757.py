from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): input_data: dict; validation_results: List[str]; approved: bool; def validate_quality(state: PharmState): state['validation_results'].append('GMP_Check_Passed'); state['validation_results'].append('Purity_Specs_Validated'); return {'validation_results': state['validation_results']}; def regulatory_check(state: PharmState): state['approved'] = True; return {'approved': True}; graph = StateGraph(PharmState); graph.add_node('validate', validate_quality); graph.add_node('regulatory', regulatory_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()