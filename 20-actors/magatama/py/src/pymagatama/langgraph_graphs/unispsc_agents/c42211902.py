from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CookingAidState(TypedDict): device_specs: dict; compliance_verified: bool; def validate_specs(state: CookingAidState) -> CookingAidState: state['compliance_verified'] = 'accessibility_certification' in state['device_specs']; return state; def check_ergonomics(state: CookingAidState) -> CookingAidState: return state; graph = StateGraph(CookingAidState); graph.add_node('validate', validate_specs); graph.add_node('ergonomics', check_ergonomics); graph.set_entry_point('validate'); graph.add_edge('validate', 'ergonomics'); graph.add_edge('ergonomics', END); graph = graph.compile()