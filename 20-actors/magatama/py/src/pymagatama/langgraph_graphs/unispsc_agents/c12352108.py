from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_id: str; purity: float; compliance_ok: bool; audit_log: List[str]; def validate_chemical(state: ChemicalState): state['compliance_ok'] = state['purity'] >= 99.0; state['audit_log'].append('Purity check completed.'); return state; def flag_hazard(state: ChemicalState): state['audit_log'].append('Hazard assessment flagged for dual-use control.'); return state; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_chemical); graph.add_node('flag_hazard', flag_hazard); graph.add_edge('validate', 'flag_hazard'); graph.add_edge('flag_hazard', END); graph.set_entry_point('validate'); graph = graph.compile()