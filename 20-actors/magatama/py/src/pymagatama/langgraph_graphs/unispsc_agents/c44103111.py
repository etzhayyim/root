from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class InkRollState(TypedDict): data: dict; validation_errors: List[str]; def validate_spec(state: InkRollState): errors = []; if 'Compatibility_Model_Numbers' not in state['data']: errors.append('Missing compatibility'); return {'validation_errors': errors}; graph = StateGraph(InkRollState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()