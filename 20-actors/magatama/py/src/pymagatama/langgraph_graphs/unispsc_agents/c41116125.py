from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HistologyState(TypedDict): items: List[str]; validation_results: dict; workflow_status: str; def validate_qc_lot(state: HistologyState): state['validation_results'] = {'lot_verified': True}; state['workflow_status'] = 'validation_complete'; return state; def prepare_shipping(state: HistologyState): state['workflow_status'] = 'shipping_prep'; return state; graph = StateGraph(HistologyState); graph.add_node('validate', validate_qc_lot); graph.add_node('ship', prepare_shipping); graph.set_entry_point('validate'); graph.add_edge('validate', 'ship'); graph.add_edge('ship', END); graph = graph.compile()