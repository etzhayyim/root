from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HoseState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_hose_specs(state: HoseState) -> HoseState: errors = []; if state['specs'].get('pressure_rating_bar', 0) <= 0: errors.append('Invalid pressure rating'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(HoseState); graph.add_node('validate', validate_hose_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()