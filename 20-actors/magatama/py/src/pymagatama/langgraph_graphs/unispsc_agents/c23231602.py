from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_report: list[str]; graph = StateGraph(State); def validate_specs(state: State): specs = state['input_data'].get('specs', {}); report = []; required = ['Material', 'Hardness']; for r in required: if r not in specs: report.append(f'Missing {r}'); return {'validation_report': report}; def finalize_procurement(state: State): return {'validation_report': state['validation_report'] + ['Passed']}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()