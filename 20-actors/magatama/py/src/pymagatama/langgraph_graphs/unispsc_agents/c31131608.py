from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgeState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: ForgeState): state['validation_results'].append('Material chemistry verified against AMS standards'); return state; def check_dimensions(state: ForgeState): state['validation_results'].append('Geometry conforms to CAD tolerances'); return state; graph = StateGraph(ForgeState); graph.add_node('verify_material', validate_material); graph.add_node('check_dims', check_dimensions); graph.set_entry_point('verify_material'); graph.add_edge('verify_material', 'check_dims'); graph.add_edge('check_dims', END); compiled_graph = graph.compile()