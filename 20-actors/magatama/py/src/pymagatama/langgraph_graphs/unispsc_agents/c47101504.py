from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BacterialState(TypedDict): specs: dict; compliance_status: bool; def validate_specs(state: BacterialState): state['compliance_status'] = 'filtration_efficiency_rating' in state['specs']; return state; def check_safety(state: BacterialState): return state; graph = StateGraph(BacterialState); graph.add_node('validate', validate_specs); graph.add_node('safety_check', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()