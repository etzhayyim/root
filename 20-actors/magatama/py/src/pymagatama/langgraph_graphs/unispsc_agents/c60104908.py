from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: ProcState): state['compliant'] = 'Field Strength Output' in state['specs'] and 'EMC' in state['specs']; return state; builder = StateGraph(ProcState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()