from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec: dict; valid: bool; def validate_stone(state: ProcState): state['valid'] = state['spec'].get('grit_size') is not None and state['spec'].get('max_rpm_rating') > 0; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_stone); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()