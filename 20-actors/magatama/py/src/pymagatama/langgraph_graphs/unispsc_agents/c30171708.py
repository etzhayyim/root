from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GlassState(TypedDict): dims: List[float]; thickness: float; specs: dict; valid: bool; def validate_glass(state: GlassState): state['valid'] = state['thickness'] > 0 and all(d > 0 for d in state['dims']); return state; graph = StateGraph(GlassState); graph.add_node('validate', validate_glass); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()