from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProduceState(TypedDict): origin: str; brix: float; status: str; def validate_quality(state: ProduceState): status = 'Approved' if state['brix'] > 10 else 'Rejected'; return {'status': status}; def handle_logistics(state: ProduceState): return {'status': state['status'] + '_Dispatched'}; graph = StateGraph(ProduceState); graph.add_node('validate', validate_quality); graph.add_node('logistics', handle_logistics); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()