from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TractorState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: TractorState): if state['specs'].get('power', 0) < 50: state.setdefault('validation_errors', []).append('Minimum power requirement not met'); return {'approved': False}; return {'approved': True}; graph = StateGraph(TractorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()