from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HemoglobinState(TypedDict): data: dict; validation_errors: List[str]; approved: bool; def validate_photometer_specs(state: HemoglobinState) -> HemoglobinState: specs = state['data'].get('specs', {}); errors = []; if specs.get('wavelength_accuracy', 0) > 2.0: errors.append('Wavelength accuracy exceeds limit'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(HemoglobinState); graph.add_node('validate', validate_photometer_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()