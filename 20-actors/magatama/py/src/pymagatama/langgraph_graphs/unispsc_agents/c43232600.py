from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SoftwareState(TypedDict): vendor: str; requirements: str; compliance_check: bool; graph = StateGraph(SoftwareState); def validate_software(state: SoftwareState): return {'compliance_check': True}; graph.add_node('validate', validate_software); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()