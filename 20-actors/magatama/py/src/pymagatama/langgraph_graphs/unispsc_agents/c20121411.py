from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_spec(state: FastenerState): result = 'PASS' if 'material_grade' in state['input_spec'] else 'FAIL'; return {'validation_results': [f'Material check: {result}'], 'status': result}; def process_fastener(state: FastenerState): if state['status'] == 'PASS': return {'status': 'READY_FOR_PROCUREMENT'}; return {'status': 'REJECTED'}; graph = StateGraph(FastenerState); graph.add_node('validate', validate_spec); graph.add_node('process', process_fastener); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()