from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input_data: dict; compliance_checks: Annotated[list, operator.add]; status: str; def validate_purity(state: ProcureState): state['compliance_checks'].append('Purity > 99% passed'); return {'status': 'validated'}; def check_regulatory(state: ProcureState): state['compliance_checks'].append('Regulatory clearance verified'); return {'status': 'approved'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_purity); graph.add_node('regulatory', check_regulatory); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); app = graph.compile()