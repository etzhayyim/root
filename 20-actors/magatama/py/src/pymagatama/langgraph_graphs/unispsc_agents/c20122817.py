from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): bearing_code: str; specs: dict; valid: bool; def validate_specs(state: BearingState): state['valid'] = all(k in state['specs'] for k in ['load_rating_kn', 'material_specification']); return state; def finalize(state: BearingState): return state; graph = StateGraph(BearingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()