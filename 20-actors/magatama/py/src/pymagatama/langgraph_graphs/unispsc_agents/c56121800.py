from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; inspection_required: bool; graph = StateGraph(ProcState); def validate_spec(state: ProcState): return {'inspection_required': True if len(state['items']) > 0 else False}; graph.add_node('validation', validate_spec); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()