from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; specs: dict; validation_passed: bool; def validate_hastelloy_specs(state: AssemblyState): state['validation_passed'] = 'Hastelloy X' in state['specs'].get('material', '') and 'rivet_integrity' in state['specs']; return state; builder = StateGraph(AssemblyState); builder.add_node('validation', validate_hastelloy_specs); builder.set_entry_point('validation'); builder.add_edge('validation', END); graph = builder.compile()