from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): items: List[str]; validated: bool; next_step: str; def validate_specs(state: SupplyState): return {'validated': all(item for item in state['items'])}; def route_procurement(state: SupplyState): return 'complete' if state['validated'] else 'review'; graph = StateGraph(SupplyState); graph.add_node('validation', validate_specs); graph.add_conditional_edges('validation', route_procurement, {'complete': END, 'review': END}); graph.set_entry_point('validation'); graph = graph.compile()