from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): batch_id: str; quality_score: float; requirements: List[str]; def validate_purity(state: PharmaState): print(f'Validating purity for {state["batch_id"]}'); return {'quality_score': 0.95}; def verify_compliance(state: PharmaState): print('Checking GMP status'); return {'requirements': ['GMP_CERTIFIED']}; graph = StateGraph(PharmaState); graph.add_node('purity', validate_purity); graph.add_node('compliance', verify_compliance); graph.set_entry_point('purity'); graph.add_edge('purity', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()