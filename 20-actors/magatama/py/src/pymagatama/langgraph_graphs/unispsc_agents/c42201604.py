from langgraph.graph import StateGraph, END; from typing import TypedDict; class MRIState(TypedDict): phantom_id: str; qc_passed: bool; data: dict; def validate_phantom(state: MRIState): return {'qc_passed': True if state['data'].get('signal_noise_ratio') > 0.0 else False}; graph = StateGraph(MRIState); graph.add_node('validate', validate_phantom); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()