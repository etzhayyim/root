from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; valid: bool; def validation_step(state: State): state['valid'] = 'spec' in state['input_data']; return state; graph = StateGraph(State); graph.add_node('validate', validation_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()