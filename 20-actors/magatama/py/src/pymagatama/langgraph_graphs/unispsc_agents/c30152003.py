from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FencingState(TypedDict): vendor_docs: List[str]; qc_passed: bool; specs: dict; def validate_integrity(state: FencingState): return {'qc_passed': 'certification_c' in state['vendor_docs']}; def assess_durability(state: FencingState): pass; graph = StateGraph(FencingState); graph.add_node('validate', validate_integrity); graph.add_node('assess', assess_durability); graph.set_entry_point('validate'); graph.add_edge('validate', 'assess'); graph.add_edge('assess', END); graph = graph.compile()