from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DeviceState(TypedDict): device_id: str; compliance_checked: bool; inspection_result: str; graph = StateGraph(DeviceState); def validate_compliance(state: DeviceState): return {'compliance_checked': True}; def perform_inspection(state: DeviceState): return {'inspection_result': 'PASS'}; graph.add_node('compliance', validate_compliance); graph.add_node('inspection', perform_inspection); graph.add_edge('compliance', 'inspection'); graph.add_edge('inspection', END); graph.set_entry_point('compliance'); compiled_graph = graph.compile()