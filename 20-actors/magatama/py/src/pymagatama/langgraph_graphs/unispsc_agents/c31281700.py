from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeldingState(TypedDict): component_specs: dict; inspection_results: List[str]; approved: bool; def validate_weld_spec(state: WeldingState): print('Validating weld specifications...'); return {'approved': True if state['component_specs'].get('material') else False}; def run_ndt_simulation(state: WeldingState): print('Running NDT simulation...'); return {'inspection_results': ['X-ray PASSED', 'Dye-penetrant PASSED']}; graph = StateGraph(WeldingState); graph.add_node('validate', validate_weld_spec); graph.add_node('ndt', run_ndt_simulation); graph.set_entry_point('validate'); graph.add_edge('validate', 'ndt'); graph.add_edge('ndt', END); graph = graph.compile()