from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HandCleanerState(TypedDict): chemical_data: dict; compliance_status: bool; def validate_msds(state: HandCleanerState): state['compliance_status'] = 'MSDS' in state['chemical_data']; return state; def check_hazmat(state: HandCleanerState): print('Checking hazardous ingredients...'); return state; graph = StateGraph(HandCleanerState); graph.add_node('validate', validate_msds); graph.add_node('hazmat_check', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat_check'); graph.add_edge('hazmat_check', END); graph = graph.compile()