from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BlockProcurementState(TypedDict): blocks: List[dict]; validation_passed: bool; def validate_quality(state: BlockProcurementState): print('Validating material safety...'); return {'validation_passed': True}; def update_inventory(state: BlockProcurementState): print('Inventory updated.'); return {}; graph = StateGraph(BlockProcurementState); graph.add_node('validate', validate_quality); graph.add_node('inventory', update_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph = graph.compile()