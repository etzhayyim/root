from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): clip_type: str; quantity: int; verified: bool; def validate_clip(state: ProcurementState): state['verified'] = state['quantity'] > 0 and state['clip_type'] in ['plastic', 'metal']; return state; builder = StateGraph(ProcurementState); builder.add_node('validate', validate_clip); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()