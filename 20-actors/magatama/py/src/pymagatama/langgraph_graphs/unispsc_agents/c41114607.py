from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TestingState(TypedDict): specs: dict; ready: bool; validated: bool; def check_specs(state: TestingState): return {'ready': 'Calibration Certificate' in state['specs']}; def run_validation(state: TestingState): return {'validated': True}; graph = StateGraph(TestingState); graph.add_node('check', check_specs); graph.add_node('validate', run_validation); graph.set_entry_point('check'); graph.add_edge('check', 'validate'); graph.add_edge('validate', END); app = graph.compile()