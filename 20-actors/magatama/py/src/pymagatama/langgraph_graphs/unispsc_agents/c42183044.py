from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: str; validation_status: bool; workflow_steps: Annotated[list, operator.add]; def validate_medical_device(state: State): return {'validation_status': True, 'workflow_steps': ['Check calibration certificate', 'Assess clinical regulatory compliance']}; def finalize_procurement(state: State): return {'workflow_steps': ['Generate PO', 'Schedule medical device inspection']}; graph = StateGraph(State); graph.add_node('validate', validate_medical_device); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compile_graph = graph.compile()