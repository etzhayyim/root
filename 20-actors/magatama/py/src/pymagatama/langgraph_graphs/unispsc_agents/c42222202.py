from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): device_specs: dict; compliance_status: bool; def validate_specs(state: ProcState): state['compliance_status'] = all(k in state['device_specs'] for k in ['iso_standard', 'sterility_audit']); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()