from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WharfState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(WharfState); def validate_specs(state: WharfState): return {'compliance_check': all(k in state['specs'] for k in ['load', 'corrosion'])}; def engineering_review(state: WharfState): return {'compliance_check': True}; graph.add_node('validation', validate_specs); graph.add_node('review', engineering_review); graph.set_entry_point('validation'); graph.add_edge('validation', 'review'); graph.add_edge('review', END); graph = graph.compile()