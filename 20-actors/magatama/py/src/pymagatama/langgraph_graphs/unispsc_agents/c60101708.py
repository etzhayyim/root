from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChartState(TypedDict): content: str; validated: bool; specs: List[str]; def validate_specs(state: ChartState): state['validated'] = len(state['specs']) >= 3; return state; graph = StateGraph(ChartState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()