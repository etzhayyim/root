from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LavageState(TypedDict): specs: dict; validation_results: List[str]; def validate_medical_specs(state: LavageState): results = []; required = ['ISO13485', 'PressureLimit']; for r in required: results.append('Passed' if r in state['specs'] else 'Failed'); return {'validation_results': results}; workflow = StateGraph(LavageState); workflow.add_node('validate', validate_medical_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()