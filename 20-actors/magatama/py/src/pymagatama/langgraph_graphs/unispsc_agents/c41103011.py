from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class RefrigeratorState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: RefrigeratorState): is_valid = 'temp_range' in state['specs'] and state['specs']['temp_range'] != ''; return {'validation_report': 'Pass' if is_valid else 'Fail'}; def finalize_order(state: RefrigeratorState): return {'validation_report': state['validation_report'] + '_finalized'}; graph = StateGraph(RefrigeratorState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()