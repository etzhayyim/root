from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpringState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_spring(state: SpringState): errors = []; if state['specs'].get('free_length', 0) <= 0: errors.append('Invalid length'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(SpringState); graph.add_node('validate', validate_spring); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()