from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: RobotState): specs = state['specs']; if specs.get('load_capacity_kg', 0) > 0: state['valid'] = True; else: state['valid'] = False; state['error'] = 'Invalid load capacity'; return state; graph = StateGraph(RobotState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()