from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_name: str; validation_status: bool; specs: List[str]; def validate_drill(state: ToolState): state['validation_status'] = True if len(state['specs']) > 0 else False; return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_drill); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()