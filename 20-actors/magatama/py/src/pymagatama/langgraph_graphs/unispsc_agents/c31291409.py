from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtrusionState(TypedDict): component_id: str; alloy_specs: dict; inspection_status: str; def validate_alloy(state: ExtrusionState): return {'inspection_status': 'COMPLIANT' if state.get('alloy_specs') else 'FAILED'}; def cad_check(state: ExtrusionState): return {'inspection_status': 'CAD_VERIFIED'}; graph = StateGraph(ExtrusionState); graph.add_node('alloy_check', validate_alloy); graph.add_node('cad_validation', cad_check); graph.set_entry_point('alloy_check'); graph.add_edge('alloy_check', 'cad_validation'); graph.add_edge('cad_validation', END); graph = graph.compile()