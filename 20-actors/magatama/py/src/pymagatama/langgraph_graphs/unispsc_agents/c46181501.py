from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ApronState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: ApronState) -> ApronState: state['validation_results'].append('Material verified against ISO requirements.'); return state; def check_compliance(state: ApronState) -> ApronState: state['validation_results'].append('Safety compliance confirmed.'); return state; graph = StateGraph(ApronState); graph.add_node('validate_materials', validate_materials); graph.add_node('check_compliance', check_compliance); graph.add_edge('validate_materials', 'check_compliance'); graph.add_edge('check_compliance', END); graph.set_entry_point('validate_materials'); graph = graph.compile()