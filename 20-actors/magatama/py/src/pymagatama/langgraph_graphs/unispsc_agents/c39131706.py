from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ConduitState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(ConduitState); def validate_material(state: ConduitState): return {'validation_results': state.get('validation_results', []) + ['material_verified']}; def check_code_compliance(state: ConduitState): return {'validation_results': state.get('validation_results', []) + ['code_compliant']}; graph.add_node('material_check', validate_material); graph.add_node('code_check', check_code_compliance); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'code_check'); graph.add_edge('code_check', END); graph = graph.compile()