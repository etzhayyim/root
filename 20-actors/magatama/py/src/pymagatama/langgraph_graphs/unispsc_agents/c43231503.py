from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_id: str; compliance_check: bool; workflow_steps: List[str]; def validate_vendor(state: ProcurementState): state['compliance_check'] = True; return state; def map_workflow(state: ProcurementState): state['workflow_steps'] = ['security_audit', 'api_config', 'vendor_onboarding']; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_vendor); graph.add_node('map', map_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'map'); graph.add_edge('map', END); compiled_graph = graph.compile()