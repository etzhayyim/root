from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RefrigeratorState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: RefrigeratorState): if state['specs'].get('temperature_range_celsius', 0) < -80: state['status'] = 'SPECIAL_INQUIRY'; return state; state['status'] = 'VALIDATED'; return state; graph = StateGraph(RefrigeratorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()