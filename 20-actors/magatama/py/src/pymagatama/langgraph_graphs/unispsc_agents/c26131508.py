from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlantState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: PlantState): errors = []; if state['specs'].get('pressure', 0) > 200: errors.append('Pressure exceeds safety threshold'); return {'validation_errors': errors}; def finalize_procurement(state: PlantState): return {'status': 'READY_FOR_BIDDING' if not state['validation_errors'] else 'REJECTED'}; graph = StateGraph(PlantState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()