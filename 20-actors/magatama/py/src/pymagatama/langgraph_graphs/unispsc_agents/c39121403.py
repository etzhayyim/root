from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PluginState(TypedDict): nema_code: str; voltage: int; is_valid: bool; def validate_plug(state: PluginState): state['is_valid'] = state['voltage'] >= 110 and len(state['nema_code']) > 0; return state; graph = StateGraph(PluginState); graph.add_node('validate', validate_plug); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()