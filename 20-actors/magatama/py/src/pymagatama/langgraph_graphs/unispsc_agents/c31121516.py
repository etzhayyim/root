from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CastingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(CastingState); def validate_tolerances(state: CastingState): state['validation_results'].append('Tolerances verified via CAD check'); return state; def check_material_cert(state: CastingState): state['validation_results'].append('Material certification validated'); return state; graph.add_node('tolerance_check', validate_tolerances); graph.add_node('material_check', check_material_cert); graph.set_entry_point('tolerance_check'); graph.add_edge('tolerance_check', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()