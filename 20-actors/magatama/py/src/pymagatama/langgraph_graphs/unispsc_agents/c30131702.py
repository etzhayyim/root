from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StoneSpecState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: StoneSpecState): specs = state['specs']; errors = []; if specs.get('water_absorption', 0) > 0.5: errors.append('Water absorption exceeds safety limit'); return {'validation_errors': errors}; def check_approval(state: StoneSpecState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(StoneSpecState); graph.add_node('validate', validate_specs); graph.add_node('approval', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph = graph.compile()