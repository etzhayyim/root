from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcessState(TypedDict): solids_data: dict; validation_errors: List[str]; graph = StateGraph(ProcessState); def validate_purity(state: ProcessState): return {'validation_errors': ['Low purity'] if state['solids_data'].get('purity', 0) < 95 else []}; graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()