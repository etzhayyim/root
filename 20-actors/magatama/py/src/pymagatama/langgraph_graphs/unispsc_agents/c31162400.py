from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FastenerState(TypedDict): specs: dict; validation_report: str; def validate_fastener(state: FastenerState): if 'material' in state['specs']: return {'validation_report': 'Material check passed'}; return {'validation_report': 'Missing material specs'}; graph = StateGraph(FastenerState); graph.add_node('validate', validate_fastener); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()