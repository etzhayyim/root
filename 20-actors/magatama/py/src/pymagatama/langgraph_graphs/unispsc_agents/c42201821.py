from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_certs: List[str]; technical_specs: dict; approved: bool; def validate_specs(state: ProcState): state['approved'] = 'ISO13485' in state['vendor_certs'] and 'translucency_rating' in state['technical_specs']; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()