from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TensionerState(TypedDict): specs: dict; validation_results: List[str]; def validate_tensioner(state: TensionerState): results = []; if state['specs'].get('load_capacity') < 1000: results.append('Load capacity below minimum standard.'); return {'validation_results': results}; graph = StateGraph(TensionerState); graph.add_node('validate', validate_tensioner); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()