from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): spec: dict; validated: bool; def check_specs(state: ProcureState): state['validated'] = 'Pressure Rating' in state['spec'] and 'Material Grade' in state['spec']; return state; builder = StateGraph(ProcureState); builder.add_node('validator', check_specs); builder.set_entry_point('validator'); builder.add_edge('validator', END); graph = builder.compile()