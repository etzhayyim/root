from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MathToolState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(MathToolState); def validate_spec(state: MathToolState): results = []; for item in state['items']: if 'fraction_face' in item and item['fraction_face']: results.append('Correct'); else: results.append('Invalid'); return {'validation_results': results}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()