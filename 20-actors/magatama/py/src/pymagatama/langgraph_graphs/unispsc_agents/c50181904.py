from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FoodState(TypedDict): items: List[str]; inspection_passed: bool; def check_quality(state: FoodState): return {'inspection_passed': True} if 'croutons' in state['items'] else {'inspection_passed': False}; graph = StateGraph(FoodState); graph.add_node('inspect', check_quality); graph.set_entry_point('inspect'); graph.add_edge('inspect', END); graph.compile()