from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): equipment_specs: dict; validation_results: List[str]; def validate_specs(state: State): results = []; specs = state.get('equipment_specs', {}); if specs.get('power_source') not in ['petrol', 'electric']: results.append('Invalid power source'); return {'validation_results': results}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()