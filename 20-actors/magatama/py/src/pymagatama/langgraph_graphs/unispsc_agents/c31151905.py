from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StrappingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(StrappingState); def validate_specs(state: StrappingState): results = []; required = ['tensile_strength', 'material']; results = [f'Missing {r}' for r in required if r not in state['specs']]; return {'validation_results': results}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()