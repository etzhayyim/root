from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): data: dict; status: list[str]; graph = StateGraph(PharmState); def validate_compliance(state: PharmState): state['status'].append('Compliance Checked'); return state; def verify_purity(state: PharmState): state['status'].append('Purity Verified'); return state; graph.add_node('compliance', validate_compliance); graph.add_node('purity', verify_purity); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'purity'); graph.add_edge('purity', END); graph = graph.compile()