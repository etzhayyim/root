from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IgnitionState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def check_specs(state: IgnitionState): state['validation_results'].append('Voltage and compatibility confirmed.'); return {'validation_results': state['validation_results']}; def finalize_procurement(state: IgnitionState): state['approved'] = True; return state; graph = StateGraph(IgnitionState); graph.add_node('validate', check_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()