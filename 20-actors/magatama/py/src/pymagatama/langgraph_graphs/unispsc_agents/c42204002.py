from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShieldingState(TypedDict): spec: dict; validation_results: List[str]; def validate_radiation_rating(state: ShieldingState): rating = state['spec'].get('lead_equivalent_mmPb', 0); return {'validation_results': ['Valid' if rating >= 0.5 else 'Invalid']} if rating else {'validation_results': ['Missing data']}; graph = StateGraph(ShieldingState); graph.add_node('validate', validate_radiation_rating); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()