from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class IntraocularGasState(TypedDict): gas_type: str; purity_check: bool; compliance_docs: list; def validate_purity(state: IntraocularGasState): return {'purity_check': state.get('purity_percentage', 0) >= 99.9}; def check_compliance(state: IntraocularGasState): return {'compliance_docs': ['ISO13485', 'CE_Mark', 'FDA_Approval']}; graph = StateGraph(IntraocularGasState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()