from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NeonLampState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: NeonLampState): results = []; if state['specs'].get('voltage', 0) > 500: results.append('Needs high voltage insulator check'); return {'validation_results': results}; def finalize_procurement(state: NeonLampState): return {'validation_results': state['validation_results'] + ['Standard compliant']}; graph = StateGraph(NeonLampState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()