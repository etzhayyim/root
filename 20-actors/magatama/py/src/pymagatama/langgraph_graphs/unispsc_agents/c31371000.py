from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CeramicState(TypedDict): specs: dict; validation_passed: bool; report: str; def validate_fiber_specs(state: CeramicState): state['validation_passed'] = state['specs'].get('temp_rating', 0) > 1000; return state; def generate_safety_report(state: CeramicState): state['report'] = 'Validation complete' if state['validation_passed'] else 'Safety violation detected'; return state; graph = StateGraph(CeramicState); graph.add_node('validate', validate_fiber_specs); graph.add_node('report', generate_safety_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); app = graph.compile()