from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): pressure_target: float; flow_rate: float; is_valid: bool; def val_step(state: CompressorState): state['is_valid'] = state['pressure_target'] > 0 and state['flow_rate'] > 0; return state; graph = StateGraph(CompressorState); graph.add_node('validation', val_step); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()