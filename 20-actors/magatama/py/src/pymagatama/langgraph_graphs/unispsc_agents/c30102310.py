from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BrassState(TypedDict): dimensions: dict; alloy_specs: str; compliance_check: bool; def validate_dimensions(state: BrassState): state['compliance_check'] = all(v > 0 for v in state['dimensions'].values()); return state; def verify_material(state: BrassState): state['compliance_check'] = state['alloy_specs'] in ['C2801', 'C3604']; return state; graph = StateGraph(BrassState); graph.add_node('validate_dims', validate_dimensions); graph.add_node('verify_mat', verify_material); graph.set_entry_point('validate_dims'); graph.add_edge('validate_dims', 'verify_mat'); graph.add_edge('verify_mat', END); graph = graph.compile()