from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ContractState(TypedDict): vendor: str; license_terms: dict; is_valid: bool; def validate_license(state: ContractState): state['is_valid'] = 'duration' in state['license_terms'] and 'seats' in state['license_terms']; return state; def provision_access(state: ContractState): print(f'Provisioning access for {state['vendor']}'); return state; graph = StateGraph(ContractState); graph.add_node('validate', validate_license); graph.add_node('provision', provision_access); graph.set_entry_point('validate'); graph.add_edge('validate', 'provision'); graph.add_edge('provision', END); app = graph.compile()