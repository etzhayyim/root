from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WarmState(TypedDict): specs: dict; compliance_check: bool; target_temp: float; graph = StateGraph(WarmState); def validate_temp(state: WarmState): return {'compliance_check': 30 <= state['target_temp'] <= 50}; def finalize_procurement(state: WarmState): return {'compliance_check': True}; graph.add_node('validate', validate_temp); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()