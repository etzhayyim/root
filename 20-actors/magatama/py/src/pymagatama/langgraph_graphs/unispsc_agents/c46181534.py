from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class WorkflowState(TypedDict): wristband_spec: dict; validation_status: bool; def validate_specs(state: WorkflowState): state['validation_status'] = 'material' in state['wristband_spec'] and 'tensile_strength' in state['wristband_spec']; return state; def check_compliance(state: WorkflowState): return 'approved' if state['validation_status'] else 'rejected'; graph = StateGraph(WorkflowState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()