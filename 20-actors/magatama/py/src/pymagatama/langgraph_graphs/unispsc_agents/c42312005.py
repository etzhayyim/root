from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_certs: dict; adhesive_specs: dict; approved: bool; def validate_compliance(state: ProcState): state['approved'] = 'ISO10993' in state['vendor_certs'].get('certifications', []) and state['adhesive_specs'].get('shelf_life', 0) > 6; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()