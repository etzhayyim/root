from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LearningResourceState(TypedDict): resource_type: str; target_audience: str; content_assets: List[str]; validation_status: bool; def validate_content(state: LearningResourceState): state['validation_status'] = len(state['content_assets']) > 0; return state; def format_curriculum(state: LearningResourceState): return state; graph = StateGraph(LearningResourceState); graph.add_node('validate', validate_content); graph.add_node('format', format_curriculum); graph.set_entry_point('validate'); graph.add_edge('validate', 'format'); graph.add_edge('format', END); graph = graph.compile()