from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GoboState(TypedDict): specs: dict; valid: bool; def validate_specs(state: GoboState): state['valid'] = all(k in state['specs'] for k in ['outer_diameter', 'model']); return state; graph = StateGraph(GoboState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()