from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_material_data(state: CastingState): state['validation_results'].append('Material certification verified'); return state; def check_tolerance(state: CastingState): state['validation_results'].append('Dimensional tolerance audit passed'); return state; graph = StateGraph(CastingState); graph.add_node('material_check', validate_material_data); graph.add_node('dimension_audit', check_tolerance); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dimension_audit'); graph.add_edge('dimension_audit', END); graph = graph.compile()