from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PumpPackingState(TypedDict): specs: dict; validation_report: List[str]; def validate_specs(state: PumpPackingState): errors = [] if not state['specs'].get('pressure_rating'): errors.append('Missing pressure rating'); return {'validation_report': errors}; graph = StateGraph(PumpPackingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()