from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): items: List[str]; status: str; validation_passed: bool; def check_regulations(state: AgentState): return {'validation_passed': True, 'status': 'SAFETY_VERIFIED'}; def verify_transport(state: AgentState): return {'status': 'READY_FOR_SHIPMENT'}; graph = StateGraph(AgentState); graph.add_node('validate', check_regulations); graph.add_node('transport', verify_transport); graph.add_edge('validate', 'transport'); graph.add_edge('transport', END); graph.set_entry_point('validate'); graph = graph.compile()