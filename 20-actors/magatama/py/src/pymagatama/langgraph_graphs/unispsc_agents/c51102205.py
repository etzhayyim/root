from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): chemical_data: dict; compliance_status: bool; workflow_log: Annotated[list, operator.add]; def validate_chemical(state: ProcurementState): is_valid = state['chemical_data'].get('purity', 0) >= 99.0; return {'compliance_status': is_valid, 'workflow_log': ['Purity validation completed']}; def finalize_procurement(state: ProcurementState): return {'workflow_log': ['Procurement finalized']}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_chemical); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()