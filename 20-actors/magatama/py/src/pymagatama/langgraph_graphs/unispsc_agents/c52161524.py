from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadioState(TypedDict): frequency_range: str; is_compliant: bool; radio_certifications: List[str]; def validate_radio_specs(state: RadioState) -> RadioState: state['is_compliant'] = 'FCC_CE' in state['radio_certifications'] and state['frequency_range'] != ''; return state; def route_approval(state: RadioState) -> str: return 'approved' if state['is_compliant'] else 'rejected'; graph = StateGraph(RadioState); graph.add_node('validate', validate_radio_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()