from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IrrigationState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: IrrigationState): state['compliance'] = 'ISO_Standard' in state['specs']; return state; def final_decision(state: IrrigationState): state['approved'] = state['compliance']; return state; graph = StateGraph(IrrigationState); graph.add_node('validate', validate_specs); graph.add_node('decision', final_decision); graph.set_entry_point('validate'); graph.add_edge('validate', 'decision'); graph.add_edge('decision', END); app = graph.compile()