from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WasteProcessState(TypedDict): facility_id: str; compliance_docs: List[str]; safety_status: bool; workflow = StateGraph(WasteProcessState); def validate_compliance(state: WasteProcessState): return {'safety_status': len(state['compliance_docs']) >= 3}; def run_simulation(state: WasteProcessState): return {'safety_status': True}; workflow.add_node('validate', validate_compliance); workflow.add_node('simulate', run_simulation); workflow.set_entry_point('validate'); workflow.add_edge('validate', 'simulate'); workflow.add_edge('simulate', END); graph = workflow.compile()