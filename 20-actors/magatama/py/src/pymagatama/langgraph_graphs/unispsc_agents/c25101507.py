from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VehicleProcureState(TypedDict): vehicle_details: dict; spec_compliance: bool; approval_status: str; def validate_specs(state: VehicleProcureState): return {'spec_compliance': all(key in state['vehicle_details'] for key in ['gvwr', 'emission', 'safety'])}; def finalize_procurement(state: VehicleProcureState): return {'approval_status': 'COMPLETED' if state['spec_compliance'] else 'REJECTED'}; graph = StateGraph(VehicleProcureState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()