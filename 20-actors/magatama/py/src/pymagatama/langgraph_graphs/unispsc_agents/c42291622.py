from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AdenotomeState(TypedDict): status: str; specs: dict; compliance_checked: bool; def validate_specs(state: AdenotomeState): state['compliance_checked'] = 'iso_13485_certification' in state['specs']; return state; def route_approval(state: AdenotomeState): return 'approved' if state['compliance_checked'] else 'rejected'; graph = StateGraph(AdenotomeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_approval, {'approved': END, 'rejected': END}); graph.compile()