from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrimmerState(TypedDict): model_specs: dict; compliance_check: bool; output: str; def validate_specs(state: TrimmerState): state['compliance_check'] = 'motor_power_spec' in state['model_specs']; return state; def finalize_order(state: TrimmerState): state['output'] = 'Validated' if state['compliance_check'] else 'Rejected'; return state; graph = StateGraph(TrimmerState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()