from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LaserProcurementState(TypedDict): specs: dict; validation_passed: bool; risk_level: str; def validate_laser_specs(state: LaserProcurementState): print('Validating laser safety class and wattage...'); state['validation_passed'] = state['specs'].get('laser_class_rating') == 'Class 4'; return state; def export_control_check(state: LaserProcurementState): print('Checking dual-use restrictions...'); state['risk_level'] = 'HIGH' if state['specs'].get('wattage', 0) > 5000 else 'MEDIUM'; return state; graph = StateGraph(LaserProcurementState); graph.add_node('validate', validate_laser_specs); graph.add_node('export_check', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()