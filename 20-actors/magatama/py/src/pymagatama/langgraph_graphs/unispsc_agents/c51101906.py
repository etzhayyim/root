from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): medicine: str; status: str; certification: bool; def validate_drugs(state: ProcureState): return {'status': 'validated' if state['certification'] else 'rejected'}; def check_cold_chain(state: ProcureState): return {'status': 'compliant_storage'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_drugs); graph.add_node('storage_check', check_cold_chain); graph.add_edge('validate', 'storage_check'); graph.add_edge('storage_check', END); graph.set_entry_point('validate'); graph = graph.compile()