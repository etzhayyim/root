from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; test_type: str; safety_clearance: bool; metrics: dict; def validate_safety(state: KitState): return { 'safety_clearance': True } if state.get('kit_id') else { 'safety_clearance': False }; def process_results(state: KitState): return { 'metrics': {'status': 'validated'} }; graph = StateGraph(KitState); graph.add_node('validate', validate_safety); graph.add_node('results', process_results); graph.set_entry_point('validate'); graph.add_edge('validate', 'results'); graph.add_edge('results', END); graph = graph.compile()