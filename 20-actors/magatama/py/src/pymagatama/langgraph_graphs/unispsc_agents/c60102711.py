from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TangramState(TypedDict): item_id: str; specifications: dict; validation_score: float; def validate_curriculum(state: TangramState): state['validation_score'] = 1.0 if 'curriculum' in state['specifications'] else 0.5; return state; graph = StateGraph(TangramState); graph.add_node('validate', validate_curriculum); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()