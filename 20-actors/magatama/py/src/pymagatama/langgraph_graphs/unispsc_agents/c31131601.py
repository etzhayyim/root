from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): material_spec: str; dimensions: dict; inspection_results: List[str]; graph = StateGraph(ForgingState); def validate_material(state: ForgingState): return {'inspection_results': ['Material check passed']}; def perform_dimensional_check(state: ForgingState): return {'inspection_results': ['Dimensions compliant']}; graph.add_node('MaterialValidation', validate_material); graph.add_node('DimensionalInspection', perform_dimensional_check); graph.set_entry_point('MaterialValidation'); graph.add_edge('MaterialValidation', 'DimensionalInspection'); graph.add_edge('DimensionalInspection', END); graph = graph.compile()