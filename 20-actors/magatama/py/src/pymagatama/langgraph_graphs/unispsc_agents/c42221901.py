from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FlowmeterState(TypedDict): flow_rate: float; compliance_check: bool; metadata: dict; def validate_specs(state: FlowmeterState): state['compliance_check'] = state['flow_rate'] > 0; return state; def finalize_order(state: FlowmeterState): return {'metadata': {'status': 'validated'}}; graph = StateGraph(FlowmeterState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()