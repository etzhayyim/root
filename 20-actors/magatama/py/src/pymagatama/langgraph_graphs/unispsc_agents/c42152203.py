from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalBurnerState(TypedDict): burner_model: str; safety_checks: List[str]; inspection_status: str; def validate_specs(state: DentalBurnerState): return {'inspection_status': 'passed' if 'Gas_Shutoff' in state['safety_checks'] else 'failed'}; def build_graph(): graph = StateGraph(DentalBurnerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = build_graph()