from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: list; status: str; def validate_cleaner(state: State): results = []; if 'diameter' not in state['input_data']: results.append('Missing diameter'); return {'validation_results': results, 'status': 'validated' if not results else 'error'}; graph = StateGraph(State); graph.add_node('validate', validate_cleaner); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()