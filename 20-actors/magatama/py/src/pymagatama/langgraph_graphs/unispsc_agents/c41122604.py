from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HemocytometerState(TypedDict): specs: dict; validation_report: List[str]; class HemocytometerProcessor: def validate_specs(self, state: HemocytometerState): state['validation_report'].append('Validating chamber depth'); return state; def check_compliance(self, state: HemocytometerState): state['validation_report'].append('Checking ISO compliance'); return state; graph = StateGraph(HemocytometerState); processor = HemocytometerProcessor(); graph.add_node('validate', processor.validate_specs); graph.add_node('compliance', processor.check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()