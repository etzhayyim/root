from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; log: List[str]; def validate_machine(state: ProcureState): state['validated'] = all(len(i) > 5 for i in state['items']); state['log'].append('Validation complete'); return state; def export_compliance(state: ProcureState): state['log'].append('Checking dual-use export rules'); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_machine); graph.add_node('compliance', export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()