from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): chemical_name: str; validation_passed: bool; compliance_status: List[str]; def validate_purity(state: AgentState): state['validation_passed'] = True; state['compliance_status'].append('Purity Verified'); return state; def check_regulatory(state: AgentState): state['compliance_status'].append('Regulatory Approved'); return state; graph = StateGraph(AgentState); graph.add_node('validate', validate_purity); graph.add_node('regulatory', check_regulatory); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); graph = graph.compile()