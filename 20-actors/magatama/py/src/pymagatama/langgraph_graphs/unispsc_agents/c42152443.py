from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CementState(TypedDict): chemical_composition: str; setting_time: float; inspection_passed: bool; def validate_cement(state: CementState): state['inspection_passed'] = state['setting_time'] < 10.0; return state; graph = StateGraph(CementState); graph.add_node('validate', validate_cement); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()