from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OfficeSupplyState(TypedDict): items: List[str]; validation_passed: bool; def validate_supplies(state: OfficeSupplyState): return {'validation_passed': all(len(i) > 0 for i in state.get('items', []))}; def finalize_order(state: OfficeSupplyState): return {'items': state['items']}; graph = StateGraph(OfficeSupplyState); graph.add_node('validate', validate_supplies); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()