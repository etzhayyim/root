from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation: bool; results: List[str]; def validate_specs(state: CastingState): state['validation'] = all(k in state['specs'] for k in ['material', 'tolerance']); return state; def run_workflow(state: CastingState): state['results'] = ['Dimensional check passed', 'Ultrasonic scan completed']; return state; builder = StateGraph(CastingState); builder.add_node('validate', validate_specs); builder.add_node('process', run_workflow); builder.set_entry_point('validate'); builder.add_edge('validate', 'process'); builder.add_edge('process', END); graph = builder.compile()