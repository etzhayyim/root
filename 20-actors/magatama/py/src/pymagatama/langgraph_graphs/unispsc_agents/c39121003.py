from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TransformerState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(TransformerState); def validate_specs(state: TransformerState): val = state['specs'].get('accuracy_class'); return {'validation_results': ['Success'] if val else ['Missing Accuracy Class']}; def route_procurement(state: TransformerState): return 'complete' if 'Success' in state['validation_results'] else END; graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()