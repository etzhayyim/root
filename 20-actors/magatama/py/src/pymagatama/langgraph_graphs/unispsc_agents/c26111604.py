from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GeneratorState(TypedDict): specs: dict; validation_results: List[str]; def validate_flow_rate(state: GeneratorState): return {'validation_results': ['Flow rate within ISO standard'] if state['specs'].get('flow_rate') > 0 else ['Flow rate error']}; def check_safety(state: GeneratorState): return {'validation_results': state['validation_results'] + ['Safety compliance verified']}; graph = StateGraph(GeneratorState); graph.add_node('validate', validate_flow_rate); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()