from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LightStandState(TypedDict): spec: dict; validated: bool; error: str; def validate_specs(state: LightStandState): specs = state.get('spec', {}); is_valid = specs.get('max_load', 0) > 0 and 'height' in specs; return {'validated': is_valid, 'error': '' if is_valid else 'Invalid spec parameters'}; graph = StateGraph(LightStandState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()