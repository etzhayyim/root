from langgraph.graph import StateGraph, END; from typing import TypedDict; class TracheaState(TypedDict): tube_specs: dict; compliance_status: bool; def validate_specs(state: TracheaState): state['compliance_status'] = 'ISO 5366' in state['tube_specs'].get('standards', []); return state; def finalize_order(state: TracheaState): return {'result': 'Ready' if state['compliance_status'] else 'Failed'}; graph = StateGraph(TracheaState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()