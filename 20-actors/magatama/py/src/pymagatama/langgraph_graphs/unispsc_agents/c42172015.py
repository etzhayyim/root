from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class KitState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_standards(state: KitState): state['validated'] = all(c in state['certs'] for c in ['ISO13485', 'FDA_510k']); return state; graph = StateGraph(KitState); graph.add_node('validate', validate_medical_standards); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()