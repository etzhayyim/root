from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CerealState(TypedDict): items: List[str]; inspection_results: dict; graph = StateGraph(CerealState); def validate_safety(state: CerealState): return {'inspection_results': {'status': 'passed', 'compliance': 'HACCP'}}; def process_procurement(state: CerealState): return {'items': [i.upper() for i in state['items']]}; graph.add_node('safety_check', validate_safety); graph.add_node('process', process_procurement); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'process'); graph.add_edge('process', END); graph = graph.compile()