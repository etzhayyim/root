from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): spec_data: dict; validation_results: dict; graph = StateGraph(FilterState); def validate_specs(state: FilterState): target = state['spec_data']; errors = []; if 'OD' not in target: errors.append('Missing Optical Density'); return {'validation_results': {'valid': len(errors) == 0, 'errors': errors}}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()