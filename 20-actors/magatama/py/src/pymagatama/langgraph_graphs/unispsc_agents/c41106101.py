from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; temp_log: List[float]; is_expired: bool; def validate_temp(state: KitState): return {"is_valid": all(t < 8.0 for t in state["temp_log"])}; def check_expiry(state: KitState): return {"status": "blocked" if state["is_expired"] else "approved"}; graph = StateGraph(KitState); graph.add_node("temp_check", validate_temp); graph.add_node("expiry_check", check_expiry); graph.set_entry_point("temp_check"); graph.add_edge("temp_check", "expiry_check"); graph.add_edge("expiry_check", END); graph = graph.compile()