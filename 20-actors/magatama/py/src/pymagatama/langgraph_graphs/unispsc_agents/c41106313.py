from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; quality_passed: bool; storage_requirements: str; def validate_reagent_specs(state: KitState): return {"quality_passed": True if state.get("storage_requirements") == "-20C" else False}; workflow = StateGraph(KitState); workflow.add_node("validate", validate_reagent_specs); workflow.set_entry_point("validate"); workflow.add_edge("validate", END); graph = workflow.compile()