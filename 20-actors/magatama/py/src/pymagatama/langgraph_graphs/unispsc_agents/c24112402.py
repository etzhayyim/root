from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CabinetState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_compliance(state: CabinetState): import re; errors = []; required = ['fire_rating', 'material']; for field in required: if field not in state['specs']: errors.append(f'Missing {field}'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(CabinetState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()