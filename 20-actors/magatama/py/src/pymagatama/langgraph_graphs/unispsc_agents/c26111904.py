from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClutchState(TypedDict): specs: dict; valid: bool; clutch_graph = StateGraph(ClutchState); def validate_torque(state: ClutchState): state['valid'] = state['specs'].get('torque', 0) > 0; return state; def check_friction(state: ClutchState): return {'valid': state['valid'] and 'friction' in state['specs']}; clutch_graph.add_node('validate_torque', validate_torque); clutch_graph.add_node('check_friction', check_friction); clutch_graph.set_entry_point('validate_torque'); clutch_graph.add_edge('validate_torque', 'check_friction'); clutch_graph.add_edge('check_friction', END); graph = clutch_graph.compile()