from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EtoposideState(TypedDict): chemical_name: str; purity: float; cold_chain_verified: bool; history: List[str]; def validate_purity(state: EtoposideState): return {'purity': 0.999 if state['purity'] >= 0.99 else 0.0}; def check_cold_chain(state: EtoposideState): return {'cold_chain_verified': True}; graph = StateGraph(EtoposideState); graph.add_node('validate', validate_purity); graph.add_node('cold_chain', check_cold_chain); graph.set_entry_point('validate'); graph.add_edge('validate', 'cold_chain'); graph.add_edge('cold_chain', END); compile_graph = graph.compile()