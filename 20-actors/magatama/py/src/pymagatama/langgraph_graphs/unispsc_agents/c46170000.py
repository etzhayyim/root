from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SecurityState(TypedDict): surveillance_units: List[str]; compliance_check: bool; final_report: str; def validate_security_compliance(state: SecurityState): state['compliance_check'] = True; return {'compliance_check': True}; def generate_procurement_report(state: SecurityState): state['final_report'] = 'Validation complete'; return {'final_report': 'Validated'}; graph = StateGraph(SecurityState); graph.add_node('validate', validate_security_compliance); graph.add_node('report', generate_procurement_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()