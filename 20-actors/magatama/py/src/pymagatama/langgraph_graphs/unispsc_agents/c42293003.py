from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; approval_required: bool; def validate_spec(state: State): state['approval_required'] = 'ISO_13485' not in state['input_data']; return state; def route_step(state: State): return 'approval' if state['approval_required'] else 'complete'; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('approval', lambda s: s); graph.add_edge('validate', 'approval'); graph.add_conditional_edges('validate', route_step, {'approval': 'approval', 'complete': END}); graph.set_entry_point('validate'); graph = graph.compile()