from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_docs: list; specs: dict; validation_results: Annotated[list, operator.add]; def validate_cylinder_specs(state: ProcureState): print('Validating master cylinder pressure specs...'); return {'validation_results': ['Pressure test passed']}; def finalize_order(state: ProcureState): return {'validation_results': ['Order approved']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_cylinder_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()