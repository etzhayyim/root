from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): paint_specs: dict; compliance_status: bool; def validate_specs(state: State): state['compliance_status'] = state['paint_specs'].get('VOC_content', 100) < 250; return state; def check_hazmat(state: State): print('Running HazMat check'); return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()