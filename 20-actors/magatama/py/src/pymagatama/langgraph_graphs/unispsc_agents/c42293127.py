from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RetractorState(TypedDict): input_specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_material(state: RetractorState): result = 'Pass' if state['input_specs'].get('material') == 'ASTM F138' else 'Fail'; return {'validation_results': [f'Material check: {result}']}; def evaluate_design(state: RetractorState): return {'validation_results': ['Design geometry verified']}; graph = StateGraph(RetractorState); graph.add_node('material_check', validate_material); graph.add_node('design_check', evaluate_design); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'design_check'); graph.add_edge('design_check', END); graph = graph.compile()