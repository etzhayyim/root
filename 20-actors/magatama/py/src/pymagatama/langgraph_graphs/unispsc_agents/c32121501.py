from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CapacitorState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: CapacitorState): state['validation_passed'] = 'rated_voltage_v' in state['specs'] and state['specs']['rated_voltage_v'] > 0; return state; graph = StateGraph(CapacitorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()