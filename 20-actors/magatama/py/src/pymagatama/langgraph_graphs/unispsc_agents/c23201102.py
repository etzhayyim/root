from langgraph.graph import StateGraph, END; from typing import TypedDict; class DumpTruckState(TypedDict): truck_id: str; specs_verified: bool; compliance_passed: bool; def validate_specs(state: DumpTruckState): return {'specs_verified': True} if state['truck_id'] else {'specs_verified': False}; def check_compliance(state: DumpTruckState): return {'compliance_passed': True} if state['specs_verified'] else {'compliance_passed': False}; graph = StateGraph(DumpTruckState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()