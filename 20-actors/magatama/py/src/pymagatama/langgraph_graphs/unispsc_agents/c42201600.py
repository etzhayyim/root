from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class MRIState(TypedDict): site_specs: dict; compliance_status: bool; def validate_site(state: MRIState): return {'compliance_status': 'tesla' in state['site_specs']}; def check_security(state: MRIState): print('Checking export controls...'); return {'compliance_status': True}; graph = StateGraph(MRIState); graph.add_node('validate', validate_site); graph.add_node('security', check_security); graph.set_entry_point('validate'); graph.add_edge('validate', 'security'); graph.add_edge('security', END); compiled_graph = graph.compile()