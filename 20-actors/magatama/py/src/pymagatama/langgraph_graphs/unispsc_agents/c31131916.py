from typing import TypedDict,List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): dimensions: dict; material_cert: bool; qc_passed: bool; def validate_specs(state: ForgingState): return {'qc_passed': all([state['material_cert'], state['dimensions']])}; def build_graph(): graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = build_graph()