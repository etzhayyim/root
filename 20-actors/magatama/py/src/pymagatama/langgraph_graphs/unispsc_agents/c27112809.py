from langgraph.graph import StateGraph, END; from typing import TypedDict; class ToolHolderState(TypedDict): spec: dict; valid: bool; def validate_specs(state: ToolHolderState): specs = state.get('spec', {}); state['valid'] = 'taper' in specs and 'rpm' in specs; return state; graph = StateGraph(ToolHolderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()