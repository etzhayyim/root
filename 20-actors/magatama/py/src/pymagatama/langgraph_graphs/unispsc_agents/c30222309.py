from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FacilityState(TypedDict): facility_id: str; safety_specs: List[str]; validated: bool; def validate_specs(state: FacilityState): state['validated'] = all(s in ['ISO_9001', 'OSHA'] for s in state['safety_specs']); return state; graph = StateGraph(FacilityState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()