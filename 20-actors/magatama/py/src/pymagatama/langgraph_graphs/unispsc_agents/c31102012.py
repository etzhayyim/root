from langgraph.graph import StateGraph, END; from typing import TypedDict; class CastingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: CastingState): state['valid'] = all(key in state['specs'] for key in ['tensile', 'alloy']); return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()