from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; structural_data: dict; validation_passed: bool; def check_structural_integrity(state: AssemblyState): state['validation_passed'] = 'tensile_strength' in state['structural_data'] and state['structural_data']['tensile_strength'] > 500; return state; def route_to_qa(state: AssemblyState): return 'qa_check' if state['validation_passed'] else END; graph = StateGraph(AssemblyState); graph.add_node('validate', check_structural_integrity); graph.add_node('qa_check', lambda s: s); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_to_qa); graph.add_edge('qa_check', END); graph.compile()