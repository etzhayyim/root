from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): status: str; spec: dict; graph = StateGraph(ProcState); def validate_medical_grade(state: ProcState): return {'status': 'validated' if 'iso_cert' in state['spec'] else 'rejected'}; graph.add_node('validate', validate_medical_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()