from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): equipment_id: str; compliance_check: bool; routing_table_ready: bool; def check_standards(state: GraphState): state['compliance_check'] = True; return state; def configure_routing(state: GraphState): state['routing_table_ready'] = True; return state; graph = StateGraph(GraphState); graph.add_node('standards', check_standards); graph.add_node('routing', configure_routing); graph.set_entry_point('standards'); graph.add_edge('standards', 'routing'); graph.add_edge('routing', END); app = graph.compile()