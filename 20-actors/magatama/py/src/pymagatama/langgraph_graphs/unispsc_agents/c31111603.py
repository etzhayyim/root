from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_alloy(state: ProcurementState): spec = state['input_data'].get('alloy_grade'); result = 'Pass' if spec in ['C36000', 'C38000'] else 'Fail'; state['validation_results'].append(f'Alloy check: {result}'); return state; def validate_dimensions(state: ProcurementState): state['validation_results'].append('Dimensional verification against drawing: Pending'); return state; graph = StateGraph(ProcurementState); graph.add_node('val_alloy', validate_alloy); graph.add_node('val_dims', validate_dimensions); graph.set_entry_point('val_alloy'); graph.add_edge('val_alloy', 'val_dims'); graph.add_edge('val_dims', END); graph = graph.compile()