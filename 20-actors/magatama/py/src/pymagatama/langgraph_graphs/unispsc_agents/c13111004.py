from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MineralState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; final_report: dict; def validate_purity(state: MineralState): result = 'PASS' if state['input_data'].get('purity', 0) >= 99.0 else 'FAIL'; return {'validation_results': [f'Purity check: {result}']}; def generate_procurement_report(state: MineralState): return {'final_report': {'status': 'READY', 'validated': True}}; graph = StateGraph(MineralState); graph.add_node('validate', validate_purity); graph.add_node('report', generate_procurement_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()