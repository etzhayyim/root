from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): data: dict; signals: Annotated[list, operator.add]; def validate_met_specs(state: ProcureState): return {'signals': ['spec_validated'] if 'accuracy' in state['data'] else ['spec_missing']}; def check_export_control(state: ProcureState): return {'signals': ['export_license_required']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_met_specs); graph.add_node('export', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph = graph.compile()