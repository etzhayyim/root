from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): vendor_specs: dict; is_verified: bool; errors: List[str]; def validate_specs(state: PumpState): state['errors'] = [] if 'pressure_rating' not in state['vendor_specs'] else []; state['is_verified'] = len(state['errors']) == 0; return state; graph_builder = StateGraph(PumpState); graph_builder.add_node('validate', validate_specs); graph_builder.set_entry_point('validate'); graph_builder.add_edge('validate', END); graph = graph_builder.compile()