from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ZincRodState(TypedDict): rod_specs: dict; validation_results: List[str]; graph = StateGraph(ZincRodState); def validate_purity(state: ZincRodState): state['validation_results'].append('Purity check completed'); return state; def check_dimensions(state: ZincRodState): state['validation_results'].append('Dimensions verified within tolerance'); return state; graph.add_node('purity_check', validate_purity); graph.add_node('dimension_check', check_dimensions); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'dimension_check'); graph.add_edge('dimension_check', END); compiled_graph = graph.compile()