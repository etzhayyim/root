from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): capacity: int; engine_type: str; safety_compliant: bool; graph = StateGraph(State); def validate_capacity(state: State): return {'safety_compliant': state['capacity'] > 0}; def verify_regulatory(state: State): return {'safety_compliant': True if state['engine_type'] in ['Electric', 'Gasoline'] else False}; graph.add_node('validate', validate_capacity); graph.add_node('regulatory', verify_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); app = graph.compile()