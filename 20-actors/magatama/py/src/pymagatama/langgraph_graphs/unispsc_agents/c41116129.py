from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; validation_status: bool; inventory_temp: float; def validate_kit(state: KitState): print('Validating Microbiology Kit...'); return {'validation_status': True}; def check_storage(state: KitState): print(f'Checking shelf life for {state["kit_id"]}...'); return {'inventory_temp': 4.0}; graph = StateGraph(KitState); graph.add_node('validate', validate_kit); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); compiled_graph = graph.compile()