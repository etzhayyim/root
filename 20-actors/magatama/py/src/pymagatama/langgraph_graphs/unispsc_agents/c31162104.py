from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnchorState(TypedDict): anchor_specs: dict; validation_results: List[str]; def validate_specs(state: AnchorState): results = []; specs = state['anchor_specs']; if specs.get('tensile_strength', 0) < 500: results.append('Insufficient strength'); return {'validation_results': results}; graph = StateGraph(AnchorState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); compiled_graph = graph.compile()