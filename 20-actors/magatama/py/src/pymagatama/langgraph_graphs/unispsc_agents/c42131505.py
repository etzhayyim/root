from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_certs: List[str]; material_compliance: bool; status: str; def validate_certifications(state: ProcurementState): state['material_compliance'] = 'ISO10993' in state['vendor_certs']; state['status'] = 'COMPLIANT' if state['material_compliance'] else 'REJECTED'; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_certifications); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()