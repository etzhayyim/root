from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): medicine_name: str; validation_status: bool; compliance_docs: List[str]; def validate_drug_specs(state: DrugProcurementState): return {'validation_status': True}; def verify_regulatory_approval(state: DrugProcurementState): return {'compliance_docs': ['FDA', 'EMA']}; workflow = StateGraph(DrugProcurementState); workflow.add_node('validate', validate_drug_specs); workflow.add_node('verify', verify_regulatory_approval); workflow.add_edge('validate', 'verify'); workflow.add_edge('verify', END); workflow.set_entry_point('validate'); graph = workflow.compile()