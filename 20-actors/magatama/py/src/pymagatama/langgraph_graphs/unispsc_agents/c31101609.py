from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BerylliumState(TypedDict): status: str; spec_check: bool; export_license: bool; def validate_spec(state: BerylliumState): state['spec_check'] = True; return state; def check_export_control(state: BerylliumState): state['export_license'] = True; return state; graph = StateGraph(BerylliumState); graph.add_node('validate_spec', validate_spec); graph.add_node('export_control', check_export_control); graph.set_entry_point('validate_spec'); graph.add_edge('validate_spec', 'export_control'); graph.add_edge('export_control', END); graph = graph.compile()