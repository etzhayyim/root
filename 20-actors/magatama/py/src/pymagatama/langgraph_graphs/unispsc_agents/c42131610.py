from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): supplier_cert: str; specs_met: bool; def validate_specs(state: ProcState): return {'specs_met': (state['supplier_cert'] == 'ISO-13485')}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()