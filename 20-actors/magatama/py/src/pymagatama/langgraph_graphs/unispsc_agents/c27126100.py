from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): pressure_rating: float; specs: List[str]; validated: bool; def validate_pressure(state: ToolState): state['validated'] = state['pressure_rating'] > 0; return state; def check_specs(state: ToolState): state['specs'].append('Pressure Tested'); return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_pressure); graph.add_node('check', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()