from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical_name: str; purity_check: bool; compliance_doc: str; graph = StateGraph(PharmaState); def validate_purity(state: PharmaState): return {'purity_check': True} if state.get('purity_percentage', 0) > 99.0 else {'purity_check': False}; def check_compliance(state: PharmaState): return {'compliance_doc': 'VALIDATED'}; graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END);