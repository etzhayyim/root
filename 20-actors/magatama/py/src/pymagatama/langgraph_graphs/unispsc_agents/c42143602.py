from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RestraintState(TypedDict): vendor_certs: List[str]; safety_compliance: bool; is_valid: bool; def check_compliance(state: RestraintState): return {'is_valid': 'ISO13485' in state['vendor_certs'] and state['safety_compliance']}; builder = StateGraph(RestraintState); builder.add_node('compliance', check_compliance); builder.set_entry_point('compliance'); builder.add_edge('compliance', END); graph = builder.compile()