from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MetalState(TypedDict): metal_code: str; purity: float; status: str; logs: Annotated[list, operator.add]; def validate_purity(state: MetalState): return {status: 'validated' if state['purity'] >= 99.9 else 'rejected', logs: ['Purity check completed']}; def check_compliance(state: MetalState): return {status: 'compliant' if state['status'] == 'validated' else 'flagged', logs: ['Compliance check finished']}; graph = StateGraph(MetalState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()