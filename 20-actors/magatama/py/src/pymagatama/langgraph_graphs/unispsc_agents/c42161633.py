from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialyzerState(TypedDict): specs: dict; compliance_check: bool; error: str; def validate_membranes(state: DialyzerState): state['compliance_check'] = state['specs'].get('biocompatibility') == 'ISO 10993'; return state; def finalize_procurement(state: DialyzerState): return {'compliance_check': True} if state['compliance_check'] else {'error': 'Failed compliance'}; graph = StateGraph(DialyzerState); graph.add_node('validate', validate_membranes); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compile = graph.compile()