from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FilterNeedleState(TypedDict): spec: dict; valid: bool; error: str; def validate_specs(state: FilterNeedleState) -> FilterNeedleState: pore_size = state['spec'].get('pore_size', 0); state['valid'] = 0.2 <= pore_size <= 5.0; return state; graph = StateGraph(FilterNeedleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()