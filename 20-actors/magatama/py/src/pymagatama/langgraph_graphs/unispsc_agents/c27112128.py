from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolSpec(TypedDict): vendor: str; hrc_rating: int; compliance: bool; graph = StateGraph(ToolSpec); def validate_spec(state: ToolSpec): return {'compliance': state.get('hrc_rating', 0) >= 45}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()