from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; specs: dict; valid: bool; def validate_specs(state: AssemblyState): print(f'Validating specs for {state[assembly_id]}'); state[valid] = 'Pressure Rating PSI' in state[specs] and 'Material Alloy' in state[specs]; return state; def route_logic(state: AssemblyState): return 'valid' if state[valid] else END; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()