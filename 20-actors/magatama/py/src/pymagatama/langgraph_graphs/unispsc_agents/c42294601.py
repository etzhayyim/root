from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validation_results: dict; def validate_medical_device(state: ProcureState): return {'validation_results': {'ISO_check': True, 'sterile_check': True}}; def finalize_procurement(state: ProcureState): return {'validation_results': {'status': 'Approved'}}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_medical_device); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()