from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RocketKitState(TypedDict): kit_spec: dict; verification_passed: bool; safety_clearance: bool; def validate_specs(state: RocketKitState): state['verification_passed'] = 'engine_class_rating' in state['kit_spec']; return state; def check_safety_compliance(state: RocketKitState): state['safety_clearance'] = state.get('verification_passed', False) and 'fire_code_compliance' in state['kit_spec']; return state; graph = StateGraph(RocketKitState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety_compliance); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()