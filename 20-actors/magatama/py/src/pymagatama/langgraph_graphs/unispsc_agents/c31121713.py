from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ZincCastState(TypedDict): specs: dict; validation_results: List[str]; def validate_tolerance(state: ZincCastState): state['validation_results'].append('Tolerance checked'); return state; def check_material(state: ZincCastState): state['validation_results'].append('Alloy content verified'); return state; graph = StateGraph(ZincCastState); graph.add_node('tolerance', validate_tolerance); graph.add_node('material', check_material); graph.set_entry_point('tolerance'); graph.add_edge('tolerance', 'material'); graph.add_edge('material', END); graph = graph.compile()