from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableState(TypedDict): panel_specs: dict; validation_results: List[str]; def validate_panel_spec(state: CableState): specs = state['panel_specs']; errors = []; if 'rack_u' not in specs: errors.append('Missing rack unit height'); return {'validation_results': errors}; def finalize_assembly(state: CableState): return {'validation_results': state['validation_results'] + ['Assembly ready for shipment']}; graph = StateGraph(CableState); graph.add_node('validate', validate_panel_spec); graph.add_node('finalize', finalize_assembly); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()