from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaceuticalState(TypedDict): items: List[str]; validation_status: bool; def validate_batch(state: PharmaceuticalState): state['validation_status'] = all(['GMP' in item or 'Safety' in item for item in state['items']]); return state; graph = StateGraph(PharmaceuticalState); graph.add_node('validate', validate_batch); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph_app = graph.compile()