from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SecurityState(TypedDict): firearm_id: str; compliance_docs: List[str]; inspection_status: str; def validate_compliance(state: SecurityState): return {'inspection_status': 'verified' if len(state['compliance_docs']) > 2 else 'pending'}; def secure_registry_update(state: SecurityState): return {'inspection_status': 'registered'}; graph = StateGraph(SecurityState); graph.add_node('validate', validate_compliance); graph.add_node('registry', secure_registry_update); graph.set_entry_point('validate'); graph.add_edge('validate', 'registry'); graph.add_edge('registry', END); graph = graph.compile()