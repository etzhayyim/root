from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PerfusionState(TypedDict): device_id: str; quality_checklist: List[str]; approved: bool; def validate_device(state: PerfusionState): return {'approved': 'sterile_cert' in state['quality_checklist']}; def assemble_workflow(): graph = StateGraph(PerfusionState); graph.add_node('validate', validate_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = assemble_workflow()