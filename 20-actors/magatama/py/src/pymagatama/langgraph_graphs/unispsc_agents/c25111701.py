from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SubmarineState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_hull(state: SubmarineState): state['validation_errors'].append('Hull integrity check passed') if 'material' in state['specs'] else state['validation_errors'].append('Missing material spec'); return state; def check_compliance(state: SubmarineState): is_safe = state['specs'].get('depth_rating', 0) > 0; state['approved'] = is_safe; return state; graph = StateGraph(SubmarineState); graph.add_node('validate_hull', validate_hull); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate_hull'); graph.add_edge('validate_hull', 'compliance'); graph.add_edge('compliance', END); submarine_graph = graph.compile()