from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class OphthalmicState(TypedDict): items: List[dict]; approval_level: str; def validate_sterile_spec(state: OphthalmicState): return {'approval_level': 'CLINICAL_REVIEW'} if all(i.get('sterile') for i in state['items']) else {'approval_level': 'REJECTED'}; graph = StateGraph(OphthalmicState); graph.add_node('validate', validate_sterile_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()