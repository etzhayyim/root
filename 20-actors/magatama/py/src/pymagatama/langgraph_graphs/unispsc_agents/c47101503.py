from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): specs: dict; validation_report: List[str]; def validate_carbon_media(state: FilterState): state['validation_report'].append('Validating iodine number and pressure drop.'); return state; def check_compliance(state: FilterState): state['validation_report'].append('Checking industrial filtration standards.'); return state; graph = StateGraph(FilterState); graph.add_node('validate', validate_carbon_media); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); 