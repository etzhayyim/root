from langgraph.graph import StateGraph, END; from typing import TypedDict; class InsertState(TypedDict): spec: dict; valid: bool; def validate_spec(state: InsertState): state['valid'] = 'material_grade' in state['spec'] and 'tolerance' in state['spec']; return state; graph = StateGraph(InsertState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()