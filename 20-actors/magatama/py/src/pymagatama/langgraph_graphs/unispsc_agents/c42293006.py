from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; verification_status: bool; def validate_specs(state: State): state['verification_status'] = all(k in state['input_data'] for k in ['material', 'accuracy', 'sterility']); return state; def check_compliance(state: State): if state['verification_status']: print('Validation Successful'); return 'approved'; return 'rejected'; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()