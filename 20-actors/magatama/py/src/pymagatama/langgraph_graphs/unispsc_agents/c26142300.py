from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadiationState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_shielding(state: RadiationState): state['compliance'] = state['specs'].get('radiation_attenuation') > 0.99; return {'compliance': state['compliance']}; def export_control_check(state: RadiationState): state['approved'] = 'restricted' not in state['specs'].get('destination', ''); return {'approved': state['approved']}; graph = StateGraph(RadiationState); graph.add_node('validate', validate_shielding); graph.add_node('export_check', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()