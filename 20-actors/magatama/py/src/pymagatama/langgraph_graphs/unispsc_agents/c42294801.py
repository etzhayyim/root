from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_specs(state: State): return {'validation_results': ['ISO 8600 check passed'], 'status': 'validating'}; def check_compliance(state: State): return {'status': 'compliant'}; builder = StateGraph(State); builder.add_node('validate', validate_specs); builder.add_node('compliance', check_compliance); builder.set_entry_point('validate'); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); graph = builder.compile()