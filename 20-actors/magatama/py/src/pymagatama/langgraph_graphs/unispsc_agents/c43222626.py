from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ModemState(TypedDict): vendor_specs: dict; validation_report: list; status: str; def validate_docsis(state: ModemState): return {'validation_report': ['DOCSIS 3.1 verification successful']}; def check_isp_compat(state: ModemState): return {'validation_report': ['ISP compatibility confirmed']}; graph = StateGraph(ModemState); graph.add_node('validate', validate_docsis); graph.add_node('compatibility', check_isp_compat); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph.set_entry_point('validate'); graph = graph.compile()