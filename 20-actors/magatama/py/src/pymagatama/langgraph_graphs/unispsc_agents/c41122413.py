from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabClampState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_clamp_specs(state: LabClampState): errors = []; for item in state['items']: if not item.get('material'): errors.append(f'Missing material for {item["id"]}'); return {'validation_errors': errors}; def finalize_procurement(state: LabClampState): return {'status': 'ready_for_rfq'}; graph = StateGraph(LabClampState); graph.add_node('validate', validate_clamp_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()