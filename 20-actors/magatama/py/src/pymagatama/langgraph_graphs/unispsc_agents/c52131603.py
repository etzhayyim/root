from langgraph.graph import StateGraph, END; from typing import TypedDict; class ShutterState(TypedDict): dimensions: dict; specs: dict; valid: bool; def validate_specs(state: ShutterState): return {'valid': all([state['specs'].get('fire_resistant'), state['dimensions']])}; graph = StateGraph(ShutterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()