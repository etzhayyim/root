from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ModelCaseState(TypedDict): model_specs: dict; validation_report: str; def validate_case(state: ModelCaseState): if 'material' in state['model_specs']: return {'validation_report': 'Validated against ISO dental standards'}; return {'validation_report': 'Insufficient material data'}; graph = StateGraph(ModelCaseState); graph.add_node('validate', validate_case); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()