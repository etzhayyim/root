from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StationState(TypedDict): station_id: str; structural_check: bool; safety_approved: bool; def node_validate_structure(state: StationState): return {'structural_check': True}; def node_check_safety(state: StationState): return {'safety_approved': True}; graph = StateGraph(StationState); graph.add_node('validate_structure', node_validate_structure); graph.add_node('check_safety', node_check_safety); graph.set_entry_point('validate_structure'); graph.add_edge('validate_structure', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()