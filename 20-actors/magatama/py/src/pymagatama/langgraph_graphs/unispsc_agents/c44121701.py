from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PenProcurementState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def validate_specs(state: PenProcurementState): results = []; if state['specs'].get('tip_diameter', 0) <= 0: results.append('Invalid tip diameter'); return {'validation_results': results, 'approved': len(results) == 0}; graph = StateGraph(PenProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()