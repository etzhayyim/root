from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): chemical_data: dict; valid: bool; def validate_api(state: State): state['valid'] = 'purity' in state['chemical_data'] and state['chemical_data']['purity'] >= 99.0; return state; graph = StateGraph(State); graph.add_node('validate', validate_api); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()