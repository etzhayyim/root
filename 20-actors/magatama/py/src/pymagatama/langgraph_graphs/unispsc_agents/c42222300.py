from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TransfusionState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_device(state: TransfusionState): state['validated'] = all(['sterile_certification' in state['certs'], 'iso_13485_compliance' in state['certs']]); return state; graph = StateGraph(TransfusionState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()