from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IBIState(TypedDict): vendor_docs: List[str]; compliance_scores: dict; approved: bool; def validate_specs(state: IBIState): return {'compliance_scores': {'bacnet': 100, 'cyber': 90}}; def approve_installation(state: IBIState): return {'approved': state['compliance_scores']['bacnet'] > 95}; graph = StateGraph(IBIState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_installation); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()