from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShimState(TypedDict): thickness: float; material: str; validated: bool; def validate_specs(state: ShimState): state['validated'] = 0.001 <= state['thickness'] <= 5.0 and state['material'] != ''; return state; graph = StateGraph(ShimState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()