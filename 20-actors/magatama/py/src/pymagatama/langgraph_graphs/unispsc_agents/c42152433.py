from typing import TypedDict,List; from langgraph.graph import StateGraph, END; class DentalFilmState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(DentalFilmState); def validate_specs(state: DentalFilmState): return {'validation_report': ['Verified ISO compatibility', 'Checked storage requirements']}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()