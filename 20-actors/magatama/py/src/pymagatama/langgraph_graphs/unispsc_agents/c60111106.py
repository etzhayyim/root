from langgraph.graph import StateGraph, END; from typing import TypedDict; class BoardState(TypedDict): dimensions: str; material: str; validated: bool; def validate_board(state: BoardState): state['validated'] = all([state.get('dimensions'), state.get('material')]); return state; graph = StateGraph(BoardState); graph.add_node('validate', validate_board); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()