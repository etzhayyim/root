from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LightingState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: LightingState): specs = state['specs']; errors = []; if specs.get('ip_rating', 0) < 54: errors.append('Insufficient IP rating for outdoor use'); return {'valid': len(errors) == 0, 'error_log': errors}; graph = StateGraph(LightingState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()