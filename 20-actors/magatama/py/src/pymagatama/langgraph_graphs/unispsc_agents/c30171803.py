from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; valid: bool; error: str; def validate_skylight(state: State): specs = state['specs']; required = ['Diameter', 'Thermal Transmittance']; state['valid'] = all(field in specs for field in required); return state; graph = StateGraph(State); graph.add_node('validate', validate_skylight); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()