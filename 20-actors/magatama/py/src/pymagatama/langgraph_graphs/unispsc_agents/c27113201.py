from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolKitState(TypedDict): items: List[str]; validated: bool; defect_rate: float; def validate_kit(state: ToolKitState): return {'validated': len(state.get('items', [])) > 0}; def check_inventory(state: ToolKitState): return {'defect_rate': 0.0}; graph = StateGraph(ToolKitState); graph.add_node('validate', validate_kit); graph.add_node('inventory', check_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph = graph.compile()