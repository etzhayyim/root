from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_soy_crayons(state: ProcurementState): state['validated'] = all('AP-non-toxic' in c for c in state['certs']); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_soy_crayons); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()