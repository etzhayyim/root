from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; valid: bool; result: str; def validate_instrument(state: State): return { "valid": "precision" in state["input_data"], "result": "Validated" if "precision" in state["input_data"] else "Rejected" }; graph = StateGraph(State); graph.add_node("validate", validate_instrument); graph.set_entry_point("validate"); graph.add_edge("validate", END); compiled_graph = graph.compile()