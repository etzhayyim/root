from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgeState(TypedDict): specs: dict; validation_results: list; status: str; def validate_metallurgy(state: ForgeState): state['validation_results'].append('Metallurgy test pass'); return state; def validate_geometry(state: ForgeState): state['validation_results'].append('Geometric tolerance pass'); return state; graph = StateGraph(ForgeState); graph.add_node('metallurgy', validate_metallurgy); graph.add_node('geometry', validate_geometry); graph.set_entry_point('metallurgy'); graph.add_edge('metallurgy', 'geometry'); graph.add_edge('geometry', END); graph = graph.compile()