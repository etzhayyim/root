from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): chemical_data: dict; compliance_status: bool; def validate_purity(state: ProcurementState): state['compliance_status'] = state['chemical_data'].get('purity', 0) >= 99.0; return state; def check_hazmat(state: ProcurementState): return {'compliance_status': 'safe' if state['chemical_data'].get('heavy_metals', 10) < 5 else 'flagged'}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_purity); graph.add_node('safety_check', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()