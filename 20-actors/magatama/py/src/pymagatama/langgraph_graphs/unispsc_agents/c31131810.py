from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgeState(TypedDict): alloy_type: str; tolerances: dict; status: str; def validate_specs(state: ForgeState): return {'status': 'validated' if state['alloy_type'] == '7075' else 'rejected'}; graph = StateGraph(ForgeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()