from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): status: str; spec_validated: bool; def validate_plate_specs(state: ProcureState): state['spec_validated'] = True; return {'status': 'validated'}; def finalize_procurement(state: ProcureState): return {'status': 'ready_to_order'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_plate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()