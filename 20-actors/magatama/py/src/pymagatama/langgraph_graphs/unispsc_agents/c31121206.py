from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): geometry_file: str; material_grade: str; tolerance: float; validated: bool; def validate_cad(state: CastState): state['validated'] = state['tolerance'] < 0.05; return state; def check_compliance(state: CastState): return 'valid' if state['validated'] else 'reject'; graph = StateGraph(CastState); graph.add_node('validate', validate_cad); graph.add_conditional_edges('validate', check_compliance, {'valid': END, 'reject': END}); graph.set_entry_point('validate'); graph = graph.compile()