from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrapState(TypedDict): trap_type: str; is_validated: bool; results: List[str]; def validate_trap(state: TrapState): return {'is_validated': state['trap_type'] == 'sticky'}; def process_data(state: TrapState): return {'results': ['Validated', 'Ready for deployment']}; graph = StateGraph(TrapState); graph.add_node('validate', validate_trap); graph.add_node('process', process_data); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()