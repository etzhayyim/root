from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_drill_specs(state: State): specs = state['specs']; state['valid'] = specs.get('motor_power', 0) > 200 and specs.get('depth', 0) > 0; state['log'].append('Validation complete'); return state; graph = StateGraph(State); graph.add_node('validate', validate_drill_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()