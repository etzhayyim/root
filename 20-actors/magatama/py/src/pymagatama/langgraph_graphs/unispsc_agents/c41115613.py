from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SalinityGraphState(TypedDict): input_data: dict; validation_results: List[str]; approved: bool; def validate_specs(state: SalinityGraphState): specs = state['input_data'].get('specs', {}); results = []; results.append('Range Verified') if 'range' in specs else results.append('Missing Range'); return {'validation_results': results, 'approved': len(results) == 1}; graph = StateGraph(SalinityGraphState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()