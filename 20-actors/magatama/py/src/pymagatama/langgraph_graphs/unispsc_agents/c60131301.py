from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class HarpsichordState(TypedDict): input: str; inspection_required: bool; routing_path: str; def validate_instrument(state: HarpsichordState): print('Validating wood and mechanism...'); return {'inspection_required': True}; def route_logistics(state: HarpsichordState): return {'routing_path': 'Specialized-Fragile-Transit'}; graph = StateGraph(HarpsichordState); graph.add_node('validate', validate_instrument); graph.add_node('route', route_logistics); graph.add_edge('validate', 'route'); graph.add_edge('route', END); graph.set_entry_point('validate'); graph = graph.compile()