from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CleaningAgentState(TypedDict): chemical_report: str; safety_compliance: bool; status: str; def validate_sds(state: CleaningAgentState): return {'safety_compliance': True} if 'compliant' in state.get('chemical_report', '').lower() else {'safety_compliance': False}; def approve_batch(state: CleaningAgentState): return {'status': 'Approved' if state['safety_compliance'] else 'Rejected'}; graph = StateGraph(CleaningAgentState); graph.add_node('validate', validate_sds); graph.add_node('approve', approve_batch); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()