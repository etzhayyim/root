from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AircraftFurnishSpec(TypedDict): specs: dict; compliance_status: bool; def validate_airworthiness(state: AircraftFurnishSpec): state['compliance_status'] = 'FAA-PMA' in state['specs'].get('certs', []); return state; graph = StateGraph(AircraftFurnishSpec); graph.add_node('validation', validate_airworthiness); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()