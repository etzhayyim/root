from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validation_results: dict; def validate_device(state: ProcState): return {"validation_results": {"is_iso_compliant": True}}; def process_order(state: ProcState): return {"items": [i.upper() for i in state["items"]], "validation_results": {"status": "verified"}}; graph = StateGraph(ProcState); graph.add_node("validate", validate_device); graph.add_node("process", process_order); graph.set_entry_point("validate"); graph.add_edge("validate", "process"); graph.add_edge("process", END); graph = graph.compile()