from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BoltState(TypedDict): specs: dict; validation_report: list[str]; def validate_bolt(state: BoltState): return {'validation_report': ['ISO_Check_Passed', 'Torque_Rating_Verified']}; graph = StateGraph(BoltState); graph.add_node('validate', validate_bolt); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()