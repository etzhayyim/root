from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ScaleState(TypedDict): specs: dict; compliance_check: bool; calibration_status: bool; def validate_specs(state: ScaleState): state['compliance_check'] = state['specs'].get('class') == 'Class I' or state['specs'].get('class') == 'Class II'; return state; def verify_calibration(state: ScaleState): state['calibration_status'] = 'cert' in state['specs']; return state; graph = StateGraph(ScaleState); graph.add_node('validate', validate_specs); graph.add_node('calibrate', verify_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); compiled_graph = graph.compile()