from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): medicine: str; compliance_docs: List[str]; validated: bool; def check_gmp(state: DrugProcurementState): state['validated'] = 'GMP_cert' in state['compliance_docs']; return state; def finalize(state: DrugProcurementState): return {'validated': state['validated']}; graph = StateGraph(DrugProcurementState); graph.add_node('check_gmp', check_gmp); graph.add_node('finalize', finalize); graph.set_entry_point('check_gmp'); graph.add_edge('check_gmp', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()