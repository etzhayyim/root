from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; validation_report: List[str]; def validate_specs(state: ProcState): validated = 'material' in state['specs'] and 'dimensions' in state['specs']; return {'validation_report': ['Passed' if validated else 'Missing technical requirements']}; def finalize_procurement(state: ProcState): return {'validation_report': state['validation_report'] + ['Procurement initiated']}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()