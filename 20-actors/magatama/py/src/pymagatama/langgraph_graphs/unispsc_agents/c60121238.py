from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PaintPotState(TypedDict): capacity: int; material: str; validated: bool; def validate_specs(state: PaintPotState): return {'validated': state['capacity'] > 0 and state['material'] != ''}; graph = StateGraph(PaintPotState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()