from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class GarmentBagState(TypedDict): specs: dict; valid: bool; def validate_specs(state: GarmentBagState): state['valid'] = 'material_composition' in state['specs'] and 'dimensions_cm' in state['specs']; return state; graph = StateGraph(GarmentBagState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()