from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_device(state: ProcState): state['validated'] = all(['ISO_13485' in c for c in state['certs']]); return state; graph_builder = StateGraph(ProcState); graph_builder.add_node('validate', validate_medical_device); graph_builder.set_entry_point('validate'); graph_builder.add_edge('validate', END); graph = graph_builder.compile()