from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_certs: List[str]; safety_verified: bool; output: str; def validate_compliance(state: ProcState): state['safety_verified'] = 'ISO13485' in state['vendor_certs']; return {'safety_verified': state['safety_verified']}; def generate_report(state: ProcState): return {'output': 'Compliance confirmed' if state['safety_verified'] else 'Rejected'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_compliance); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()