from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): input: str; validation_results: Annotated[list, operator.add]; def validate_specs(state: AgentState): return {'validation_results': ['Material check passed', 'Flow rate verification passed']}; def route_task(state: AgentState): return 'process'; graph = StateGraph(AgentState); graph.add_node('process', validate_specs); graph.set_entry_point('process'); graph.add_edge('process', END); graph = graph.compile()