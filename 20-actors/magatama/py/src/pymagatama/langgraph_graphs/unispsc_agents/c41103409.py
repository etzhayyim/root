from langgraph.graph import StateGraph, END; from typing import TypedDict; class EnvChamberState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: EnvChamberState): print('Validating chamber specs...'); return {'validation_report': 'Specs verified'} if state['specs'].get('temp_range') else {'validation_report': 'Invalid'}; def finalize_order(state: EnvChamberState): return {'validation_report': 'Order ready for procurement'}; graph = StateGraph(EnvChamberState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()