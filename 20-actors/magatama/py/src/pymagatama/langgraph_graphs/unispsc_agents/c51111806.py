from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_purity(state: AgentState): purity = state['input_data'].get('purity', 0); return {'validation_results': [f'Purity check: {purity}%']} if purity >= 99 else {'validation_results': ['Purity failed']}; def check_hazmat(state: AgentState): return {'status': 'HAZMAT_PROTOCOL_READY'}; graph = StateGraph(AgentState); graph.add_node('validate', validate_purity); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()