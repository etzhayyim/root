from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class HeaderState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(HeaderState); def validate_spec(state: HeaderState): return {'validation_results': ['Material compliance checked', 'Dimension consistency verified']}; def approve_order(state: HeaderState): return {'validation_results': state['validation_results'] + ['Approved']}; graph.add_node('validate', validate_spec); graph.add_node('approve', approve_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.compile()