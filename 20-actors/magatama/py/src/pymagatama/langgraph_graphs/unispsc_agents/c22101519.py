from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserState(TypedDict): specs: dict; ready_for_procurement: bool; def validate_specs(state: LaserState): has_fields = all(k in state['specs'] for k in ['laser_power_class', 'safety_certification_iso']); return {'ready_for_procurement': has_fields}; def approve_procurement(state: LaserState): return {'ready_for_procurement': True}; graph = StateGraph(LaserState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()