from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): device_id: str; specs: dict; validation_passed: bool; def validate_changer(state: GraphState): state['validation_passed'] = 'format' in state['specs'] and 'capacity' in state['specs']; return state; def route_logic(state: GraphState): return 'valid' if state['validation_passed'] else END; graph = StateGraph(GraphState); graph.add_node('validate', validate_changer); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()