from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FilterState(TypedDict): spec_data: dict; compliance_status: bool; validation_log: List[str]; def validate_specs(state: FilterState): spec = state['spec_data']; errors = []; if spec.get('pressure_rating', 0) < 1000: errors.append('Pressure rating below safety threshold'); return {'compliance_status': len(errors) == 0, 'validation_log': errors}; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()