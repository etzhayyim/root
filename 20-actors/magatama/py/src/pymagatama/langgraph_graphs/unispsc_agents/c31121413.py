from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): dimensions: dict; material_cert: bool; inspection_result: str; graph = StateGraph(CastingState); def validate_specs(state: CastingState): return {'inspection_result': 'PASS' if state['material_cert'] else 'FAIL'}; def check_dimensions(state: CastingState): return {'inspection_result': 'PASS' if state['dimensions']['tolerance'] < 0.05 else 'FAIL'}; graph.add_node('validate', validate_specs); graph.add_node('check_dim', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_dim'); graph.add_edge('check_dim', END); app = graph.compile()