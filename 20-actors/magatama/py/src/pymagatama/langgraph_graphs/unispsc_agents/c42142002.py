from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WorkflowState(TypedDict): items: List[str]; safety_check: bool; def validate_knife(state: WorkflowState): return {'safety_check': True}; def update_inventory(state: WorkflowState): return {'items': ['knife_verified']}; graph = StateGraph(WorkflowState); graph.add_node('validate', validate_knife); graph.add_node('update', update_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'update'); graph.add_edge('update', END); compiled_graph = graph.compile()