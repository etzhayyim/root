from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GalleryState(TypedDict): gallery_id: str; compliance_docs: List[str]; inspection_status: str; def validate_compliance(state: GalleryState): state['inspection_status'] = 'verified' if len(state['compliance_docs']) > 2 else 'pending'; return state; def finalize_contract(state: GalleryState): return {'inspection_status': 'finalized'}; graph = StateGraph(GalleryState); graph.add_node('validate', validate_compliance); graph.add_node('finalize', finalize_contract); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()