from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CleaningSupplyState(TypedDict): items: List[str]; hazard_flag: bool; compliance_status: bool; def check_sds(state: CleaningSupplyState): return {'hazard_flag': any('chemical' in i for i in state['items'])}; def validate_compliance(state: CleaningSupplyState): return {'compliance_status': True}; graph = StateGraph(CleaningSupplyState); graph.add_node('sds_check', check_sds); graph.add_node('compliance_val', validate_compliance); graph.add_edge('sds_check', 'compliance_val'); graph.add_edge('compliance_val', END); graph.set_entry_point('sds_check'); graph = graph.compile()