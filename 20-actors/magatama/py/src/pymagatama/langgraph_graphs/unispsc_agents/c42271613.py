from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MonitorState(TypedDict): device_id: str; compliance_docs: List[str]; score: float; def validate_compliance(state: MonitorState): return {'score': 1.0 if len(state['compliance_docs']) > 2 else 0.0}; def check_threshold(state: MonitorState): return 'end' if state['score'] >= 1.0 else 'end'; graph = StateGraph(MonitorState); graph.add_node('validate', validate_compliance); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()