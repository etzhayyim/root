from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): enzyme_type: str; purity_check: bool; storage_validated: bool; steps: List[str]; def validate_enzyme(state: ProcurementState): state['purity_check'] = True; state['steps'].append('Purity Verification'); return state; def check_environment(state: ProcurementState): state['storage_validated'] = True; state['steps'].append('Cold Chain Check'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_enzyme); graph.add_node('storage', check_environment); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph.set_entry_point('validate'); app = graph.compile()