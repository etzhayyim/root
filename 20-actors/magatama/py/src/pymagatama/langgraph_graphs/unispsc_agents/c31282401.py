from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): component_type: str; specs: dict; approved: bool; def validate_physics(state: ProcureState): state['approved'] = 'explosive_formed' in state['specs'].get('method', '') and state['specs'].get('strength', 0) > 500; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_physics); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()