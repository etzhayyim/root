from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PatientClothingState(TypedDict): items: List[dict]; validation_results: dict; def validate_spec(state: PatientClothingState): return {'validation_results': {'compliant': True}}; def check_compliance(state: PatientClothingState): return 'valid' if state['validation_results']['compliant'] else 'flagged'; graph = StateGraph(PatientClothingState); graph.add_node('validate', validate_spec); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()