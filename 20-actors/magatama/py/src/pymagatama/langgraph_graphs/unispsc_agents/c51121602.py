from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): status: str; batch_data: dict; validation_errors: List[str]; def validate_pharmacy_spec(state: PharmaState): batch = state['batch_data']; errors = []; if not batch.get('gmp_cert'): errors.append('Missing GMP cert'); if not batch.get('temp_log'): errors.append('Missing temp logs'); return {'validation_errors': errors, 'status': 'validated' if not errors else 'rejected'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_pharmacy_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()