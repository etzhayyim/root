from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ExamTableState(TypedDict): specs: dict; compliance: bool; assessment: str; def validate_spec(state: ExamTableState): state['compliance'] = 'ISO-13485' in state['specs'] and state['specs']['weight_rating'] > 150; return state; def finalize_assessment(state: ExamTableState): state['assessment'] = 'Approved' if state['compliance'] else 'Rejected'; return state; graph = StateGraph(ExamTableState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_assessment); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()