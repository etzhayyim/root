from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class MaterialState(TypedDict): purity: float; particle_size: float; compliant: bool; history: Annotated[list, operator.add]; def validate_purity(state: MaterialState): return {'compliant': state['purity'] >= 99.999}; def validate_size(state: MaterialState): return {'compliant': state['particle_size'] <= 50.0}; graph = StateGraph(MaterialState); graph.add_node('purity_check', validate_purity); graph.add_node('size_check', validate_size); graph.add_edge('purity_check', 'size_check'); graph.add_edge('size_check', END); graph.set_entry_point('purity_check'); graph = graph.compile()