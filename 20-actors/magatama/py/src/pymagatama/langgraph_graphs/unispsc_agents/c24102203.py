from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SealState(TypedDict): input_spec: dict; validation_report: list[str]; def validate_specs(state: SealState): report = []; if state['input_spec'].get('power') is None: report.append('Missing power specs'); return {'validation_report': report}; def route_step(state: SealState): return 'end' if not state['validation_report'] else 'end'; graph = StateGraph(SealState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()