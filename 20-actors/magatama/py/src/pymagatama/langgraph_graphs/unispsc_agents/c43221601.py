from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): specs: dict; valid: bool; def validate_specs(state: GraphState): state['valid'] = state['specs'].get('insertion_loss_db', 0) < 1.0; return state; graph = StateGraph(GraphState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()