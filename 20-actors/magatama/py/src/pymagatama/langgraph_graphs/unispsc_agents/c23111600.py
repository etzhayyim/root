from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CleaningEquipmentState(TypedDict): specs: dict; compliance_verified: bool; def validate_specs(state: CleaningEquipmentState): return {'compliance_verified': state['specs'].get('operating_pressure_bar', 0) > 0}; graph = StateGraph(CleaningEquipmentState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()