from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BeanbagState(TypedDict): vendor_specs: dict; compliance_check: bool; final_approval: bool; def validate_specs(state: BeanbagState): state['compliance_check'] = 'fire_safety' in state['vendor_specs'] and state['vendor_specs']['fire_safety'] >= 'CA-117'; return state; def approve_procurement(state: BeanbagState): state['final_approval'] = state['compliance_check']; return state; graph = StateGraph(BeanbagState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()