from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; compliance_status: bool; approved: bool; def validate_specs(state: ProcurementState): specs = state['specs']; is_valid = specs.get('weight_capacity', 0) >= 120 and specs.get('material_safe', False); return {'compliance_status': is_valid}; def final_approval(state: ProcurementState): return {'approved': state['compliance_status']}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('approve', final_approval); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()