from langgraph.graph import StateGraph, END; from typing import TypedDict; class VehicleState(TypedDict): vehicle_data: dict; validation_passed: bool; def validate_specs(state: VehicleState): state['validation_passed'] = 'safety_rating' in state['vehicle_data'] and state['vehicle_data']['fuel_type'] in ['gas', 'electric']; return state; def finalize_order(state: VehicleState): return {'validation_passed': True}; graph = StateGraph(VehicleState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()