from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaundryState(TypedDict): items: List[str]; validated: bool; next_step: str; def validate_laundry_basket(state: LaundryState): return {'validated': True, 'next_step': 'route_procurement'}; def route_procurement(state: LaundryState): return {'next_step': 'END'}; graph = StateGraph(LaundryState); graph.add_node('validate', validate_laundry_basket); graph.add_node('route', route_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'route'); graph.add_edge('route', END); graph = graph.compile()