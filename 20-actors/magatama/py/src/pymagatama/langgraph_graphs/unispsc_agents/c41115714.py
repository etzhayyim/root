from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AutosamplerState(TypedDict): input_specs: dict; validation_results: List[str]; status: str; def validate_specs(state: AutosamplerState): print('Validating autosampler parameters...'); state['validation_results'] = ['Precision Check Passed', 'Vial Compatibility Confirmed']; state['status'] = 'VALIDATED'; return state; def finalize_order(state: AutosamplerState): state['status'] = 'READY_FOR_PROCUREMENT'; return state; graph = StateGraph(AutosamplerState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()