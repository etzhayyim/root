from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): dimensions: dict; material_spec: str; status: str; def validate_dimensions(state: State): status = 'pass' if all(k in state['dimensions'] for k in ['width', 'height', 'length']) else 'fail'; return {'status': status}; def verify_material(state: State): return {'status': 'verified' if state['material_spec'] else 'pending'}; graph = StateGraph(State); graph.add_node('validate', validate_dimensions); graph.add_node('verify', verify_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); graph = graph.compile()