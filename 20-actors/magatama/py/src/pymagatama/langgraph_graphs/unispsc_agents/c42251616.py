from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): device_specs: dict; compliance_check: bool; def validate_medical_grade(state: ProcureState): return {'compliance_check': state['device_specs'].get('cert_id') is not None}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_medical_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()