from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; status: str; def validate_specs(state: State): return {'status': 'validated' if 'version' in state['input_data'] else 'rejected'}; def finalize_procurement(state: State): return {'status': 'finalized'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()