from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: Annotated[list, operator.add]; validation_results: list; graph = StateGraph(ProcureState); def validate_flange(state): return {'validation_results': ['Structural integrity check passed', 'Material spec confirmed']}; def route_to_qa(state): return 'qa_check'; graph.add_node('validate', validate_flange); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()