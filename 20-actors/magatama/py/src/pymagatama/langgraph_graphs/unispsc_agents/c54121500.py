from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GemState(TypedDict): gem_data: dict; compliance_check: bool; verified: bool; def check_compliance(state: GemState): return {'compliance_check': 'GIA' in state['gem_data'].get('certs', [])}; def verify_gem(state: GemState): return {'verified': state.get('compliance_check', False)}; graph = StateGraph(GemState); graph.add_node('compliance', check_compliance); graph.add_node('verification', verify_gem); graph.add_edge('compliance', 'verification'); graph.add_edge('verification', END); graph.set_entry_point('compliance'); graph = graph.compile()