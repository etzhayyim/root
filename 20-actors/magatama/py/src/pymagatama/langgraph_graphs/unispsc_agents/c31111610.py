from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PlasticState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: PlasticState): state['valid'] = 'material' in state['specs'] and 'tolerance' in state['specs']; return state; def route_flow(state: PlasticState): return 'validate' if not state.get('valid') else END; graph = StateGraph(PlasticState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()