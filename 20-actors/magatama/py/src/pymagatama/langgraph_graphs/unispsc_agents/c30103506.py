from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class HoneycombState(TypedDict): material_spec: dict; validation_report: list; status: str; def validate_alloy(state: HoneycombState): alloy = state['material_spec'].get('grade'); valid = alloy in ['304', '321', 'Inconel']; return {'status': 'PASS' if valid else 'FAIL'}; def check_density(state: HoneycombState): density = state['material_spec'].get('density', 0); return {'status': 'PASS' if density > 50 else 'FAIL_DENSITY'}; graph = StateGraph(HoneycombState); graph.add_node('validate_alloy', validate_alloy); graph.add_node('check_density', check_density); graph.set_entry_point('validate_alloy'); graph.add_edge('validate_alloy', 'check_density'); graph.add_edge('check_density', END); compiled_graph = graph.compile()