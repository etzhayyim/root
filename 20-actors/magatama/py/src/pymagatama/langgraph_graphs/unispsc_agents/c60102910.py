from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoinBankState(TypedDict): vendor_id: str; specs: dict; validation_results: List[str]; def validate_materials(state: CoinBankState): material = state['specs'].get('material', ''); return {'validation_results': ['passed' if material else 'failed']}; def check_security(state: CoinBankState): mechanism = state['specs'].get('lock_type', 'none'); return {'validation_results': state['validation_results'] + [f'lock_{mechanism}']}; graph = StateGraph(CoinBankState); graph.add_node('validate', validate_materials); graph.add_node('security', check_security); graph.add_edge('validate', 'security'); graph.add_edge('security', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()