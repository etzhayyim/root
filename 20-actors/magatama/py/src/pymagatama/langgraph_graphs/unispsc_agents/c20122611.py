from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class FastenerState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: FastenerState): results = []; if 'material_grade' not in state['specs']: results.append('Missing material grade'); return {'validation_results': results, 'status': 'validated' if not results else 'error'}; def finalize_procurement(state: FastenerState): return {'status': 'approved'}; graph = StateGraph(FastenerState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); compile_graph = graph.compile()