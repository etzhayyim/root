from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): spec: dict; validated: bool; def validate_equipment(state: ProcureState): state['validated'] = 'mesh_size' in state['spec']; return state; builder = StateGraph(ProcureState); builder.add_node('validate', validate_equipment); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()