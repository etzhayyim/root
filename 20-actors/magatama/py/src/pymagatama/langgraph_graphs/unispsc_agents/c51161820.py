from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): chemical_name: str; validation_passed: bool; safety_data: dict; def validate_chemical(state: ProcurementState): state['validation_passed'] = 'flash_point' in state['safety_data'] and state['safety_data']['purity'] > 0.98; return state; def check_regulations(state: ProcurementState): state['safety_data']['compliant'] = True; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_chemical); graph.add_node('regs', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'regs'); graph.add_edge('regs', END); graph = graph.compile()