from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CatheterState(TypedDict): device_id: str; compliance_docs: List[str]; approved: bool; def check_compliance(state: CatheterState): state['approved'] = all(doc in state['compliance_docs'] for doc in ['ISO13485', 'CE_MARK']); return state; graph = StateGraph(CatheterState); graph.add_node('compliance_check', check_compliance); graph.set_entry_point('compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()