from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VehicleState(TypedDict): vin: str; specs: dict; compliance_status: bool; def validate_vin(state: VehicleState): return {'compliance_status': len(state['vin']) == 17}; def check_emissions(state: VehicleState): return {'compliance_status': state['specs'].get('euro_norm', 0) >= 6}; graph = StateGraph(VehicleState); graph.add_node('validate', validate_vin); graph.add_node('emissions', check_emissions); graph.add_edge('validate', 'emissions'); graph.add_edge('emissions', END); graph.set_entry_point('validate'); graph = graph.compile()