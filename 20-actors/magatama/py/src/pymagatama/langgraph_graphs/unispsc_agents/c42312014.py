from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): kit_components: List[str]; validation_results: dict; graph = StateGraph(State); def validate_sterile_integrity(state: State): return {'validation_results': {'sterile_check': 'PASS'}}; def check_regulatory_compliance(state: State): return {'validation_results': {'regulation_check': 'VERIFIED'}}; graph.add_node('validate_kit', validate_sterile_integrity); graph.add_node('check_regs', check_regulatory_compliance); graph.set_entry_point('validate_kit'); graph.add_edge('validate_kit', 'check_regs'); graph.add_edge('check_regs', END); app = graph.compile()