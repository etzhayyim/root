from langgraph.graph import StateGraph, END; from typing import TypedDict; class SensorState(TypedDict): sensor_data: dict; valid: bool; def val_sensor(state: SensorState): state['valid'] = state['sensor_data'].get('sensitivity', 0) > 0; return state; graph = StateGraph(SensorState); graph.add_node('validate', val_sensor); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()