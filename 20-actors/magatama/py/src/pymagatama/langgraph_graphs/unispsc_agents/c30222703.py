from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class WarehouseState(TypedDict): vendor_certs: dict; load_requirements: dict; validation_errors: Annotated[list, operator.add]; def validate_capacity(state: WarehouseState): errors = [] if state['load_requirements'].get('weight_limit') > 5000: errors.append('Structural reinforcement required for specified weight'); return {'validation_errors': errors}; def check_compliance(state: WarehouseState): return {'validation_errors': ['Missing safety cert' if not state['vendor_certs'].get('osha') else None]}; graph = StateGraph(WarehouseState); graph.add_node('validate', validate_capacity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()