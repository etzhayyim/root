from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftState(TypedDict): specs: dict; compliance: bool; review_required: bool; def validate_specs(state: AircraftState): state['compliance'] = 'RTCA-DO-160G' in state['specs'].get('certifications', []); return state; def check_export(state: AircraftState): state['review_required'] = True; return state; graph = StateGraph(AircraftState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()