from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalGuideState(TypedDict): guide_id: str; cad_file_path: str; validation_status: bool; workflow_steps: List[str]; def validate_cad(state: SurgicalGuideState) -> dict: print(f'Validating CAD file for {state["guide_id"]}'); return {'validation_status': True, 'workflow_steps': ['CAD_VALIDATED']}; def perform_compliance_check(state: SurgicalGuideState) -> dict: print('Checking ISO 10993 compliance'); return {'workflow_steps': state['workflow_steps'] + ['COMPLIANCE_PASSED']}; graph = StateGraph(SurgicalGuideState); graph.add_node('validate', validate_cad); graph.add_node('compliance', perform_compliance_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()