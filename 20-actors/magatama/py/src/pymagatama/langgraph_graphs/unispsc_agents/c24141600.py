from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PackingState(TypedDict): items: list; quality_pass: bool; def validate_cushioning(state: PackingState): state['quality_pass'] = all(item.get('thickness') > 10 for item in state['items']); return state; graph = StateGraph(PackingState); graph.add_node('validate', validate_cushioning); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()