from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ActuatorState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: ActuatorState): errors = []; if state['specs'].get('load_capacity', 0) <= 0: errors.append('Invalid load capacity'); return {'validation_results': errors}; def check_compliance(state: ActuatorState): return {'validation_results': state['validation_results'] + ['Compliance checked']}; graph = StateGraph(ActuatorState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()