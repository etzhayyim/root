from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TireProcurementState(TypedDict): tire_specs: dict; compliance_check: bool; verified_data: dict; def validate_specs(state: TireProcurementState): return {'compliance_check': all(k in state['tire_specs'] for k in ['size', 'load_index'])}; def finalize_order(state: TireProcurementState): return {'verified_data': {'status': 'READY', 'specs': state['tire_specs']}}; graph = StateGraph(TireProcurementState); graph.add_node('validation', validate_specs); graph.add_node('finalization', finalize_order); graph.set_entry_point('validation'); graph.add_edge('validation', 'finalization'); graph.add_edge('finalization', END); graph = graph.compile()