from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RivetState(TypedDict): assembly_id: str; structural_specs: dict; validated: bool; def validate_assembly(state: RivetState): state['validated'] = all(k in state['structural_specs'] for k in ['tensile', 'alloy']); return state; graph = StateGraph(RivetState); graph.add_node('validation', validate_assembly); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()