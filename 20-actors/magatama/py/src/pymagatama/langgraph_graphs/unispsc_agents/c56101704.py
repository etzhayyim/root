from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TableBaseState(TypedDict): vendor_specs: dict; compliance_check: bool; final_report: str; def validate_specs(state: TableBaseState): state['compliance_check'] = state['vendor_specs'].get('load_kg', 0) > 50; return state; def generate_report(state: TableBaseState): state['final_report'] = 'COMPLIANT' if state['compliance_check'] else 'NON-COMPLIANT'; return state; graph = StateGraph(TableBaseState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()