from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaintState(TypedDict): items: List[str]; validated: bool; def check_safety(state: PaintState): state['validated'] = True; return state; graph = StateGraph(PaintState); graph.add_node('safety_check', check_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()