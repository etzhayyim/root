from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_check: str; compliance_audit: str; approval_status: str; graph = StateGraph(ProcState); def validate_medical_device(state: ProcState): return {'compliance_audit': 'PASSED' if state['vendor_check'] == 'certified' else 'FAILED'}; def finalize_procurement(state: ProcState): return {'approval_status': 'APPROVED' if state['compliance_audit'] == 'PASSED' else 'REJECTED'}; graph.add_node('validate', validate_medical_device); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()