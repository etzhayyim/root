from langgraph.graph import StateGraph, END; from typing import TypedDict; class VetEquipmentState(TypedDict): data: dict; status: str; def validate_specs(state: VetEquipmentState): return {'status': 'validated'} if 'flow_rate' in state['data'] else {'status': 'rejected'}; def check_compliance(state: VetEquipmentState): return {'status': 'compliant'}; graph = StateGraph(VetEquipmentState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()