from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ToyState(TypedDict): safety_certs: List[str]; inspection_passed: bool; def validate_safety(state: ToyState): state['inspection_passed'] = 'ASTM_F963' in state['safety_certs']; return state; def finalize_procurement(state: ToyState): return {'status': 'approved' if state['inspection_passed'] else 'rejected'}; graph = StateGraph(ToyState); graph.add_node('validate', validate_safety); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()