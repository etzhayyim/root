from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; validation_report: str; def validate_instrument(state: State): specs = state['specs']; is_valid = 'calibration_certificate' in specs and specs.get('accuracy_class'); return {'validation_report': 'Pass' if is_valid else 'Fail'}; graph = StateGraph(State); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()