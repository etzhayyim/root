from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TobaccoState(TypedDict): tobacco_type: str; nicotine_level: float; compliance_verified: bool; history: List[str]; def validate_nicotine(state: TobaccoState): return { 'compliance_verified': state['nicotine_level'] < 5.0 }; def approve_shipment(state: TobaccoState): return { 'history': state['history'] + ['Shipment Approved'] }; graph = StateGraph(TobaccoState); graph.add_node('validate', validate_nicotine); graph.add_node('approve', approve_shipment); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()