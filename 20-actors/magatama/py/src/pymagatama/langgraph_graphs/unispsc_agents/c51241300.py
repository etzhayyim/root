from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class AstringentState(TypedDict): chemical_report: dict; compliance_verified: bool; def validate_composition(state: AstringentState): state['compliance_verified'] = 'active_ingredients' in state['chemical_report']; return state; def finalize_specs(state: AstringentState): return {'result': 'approved' if state['compliance_verified'] else 'rejected'}; graph = StateGraph(AstringentState); graph.add_node('validate', validate_composition); graph.add_node('finalize', finalize_specs); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()