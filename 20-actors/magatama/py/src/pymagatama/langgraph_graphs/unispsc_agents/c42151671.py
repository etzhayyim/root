from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): items: List[str]; validated: bool; record_id: str; def validate_dental_specs(state: DentalState): state['validated'] = True; return state; graph = StateGraph(DentalState); graph.add_node('validate', validate_dental_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()