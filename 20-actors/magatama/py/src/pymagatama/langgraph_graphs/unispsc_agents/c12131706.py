from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): catalyst_code: str; validation_steps: Annotated[Sequence[str], operator.add]; status: str; def validate_catalyst_data(state: CatalystState): return {'validation_steps': ['Check Purity', 'Verify MSDA'], 'status': 'validated'}; def compute_catalyst_workflow(state: CatalystState): return {'status': 'processed'}; graph = StateGraph(CatalystState); graph.add_node('validate', validate_catalyst_data); graph.add_node('process', compute_catalyst_workflow); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()