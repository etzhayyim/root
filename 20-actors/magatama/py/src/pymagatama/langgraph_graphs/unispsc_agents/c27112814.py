from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolingState(TypedDict): bits_spec: dict; validation_results: List[str]; graph = StateGraph(ToolingState); def validate_spec(state: ToolingState): return {'validation_results': ['Dimension Check', 'Hardness Test']}; def approve_order(state: ToolingState): return {'validation_results': state['validation_results'] + ['Approved']}; graph.add_node('validate', validate_spec); graph.add_node('approve', approve_order); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()