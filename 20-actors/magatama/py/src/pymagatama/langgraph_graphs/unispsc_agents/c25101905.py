from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ATVState(TypedDict): vehicle_type: str; specs: dict; approved: bool; def validate_atv(state: ATVState): specs = state['specs']; state['approved'] = specs.get('climbing_angle', 0) > 25; return state; graph = StateGraph(ATVState); graph.add_node('validate', validate_atv); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()