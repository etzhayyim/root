from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ApplianceState(TypedDict): items: List[str]; qc_passed: bool; def validate_specs(state: ApplianceState): return {'qc_passed': all(item for item in state['items'])}; def approve_procurement(state: ApplianceState): return {'qc_passed': True}; graph = StateGraph(ApplianceState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()