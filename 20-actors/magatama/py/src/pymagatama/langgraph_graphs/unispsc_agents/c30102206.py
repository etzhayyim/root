from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AluminumProcurement(TypedDict): alloy: str; thickness: float; certs: List[str]; validated: bool; def validate_specs(state: AluminumProcurement): return {'validated': bool(state['alloy'])}; def check_certs(state: AluminumProcurement): return {'validated': 'MTR' in state['certs']}; graph = StateGraph(AluminumProcurement); graph.add_node('validate', validate_specs); graph.add_node('verify_certs', check_certs); graph.set_entry_point('validate'); graph.add_edge('validate', 'verify_certs'); graph.add_edge('verify_certs', END); app = graph.compile()