from langgraph.graph import StateGraph, END; from typing import TypedDict; class MistEliminatorState(TypedDict): spec: dict; valid: bool; error: str; def validate_specs(state: MistEliminatorState): spec = state['spec']; state['valid'] = 'efficiency' in spec and 'material' in spec; return state; graph = StateGraph(MistEliminatorState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()