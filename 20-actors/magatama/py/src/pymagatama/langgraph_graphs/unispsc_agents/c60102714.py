from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TangramState(TypedDict): dimensions: dict; material: str; quality_passed: bool; def validate_dimensions(state: TangramState): state['quality_passed'] = all(val > 0 for val in state['dimensions'].values()); return state; def check_material(state: TangramState): if state['material'] == 'non-toxic': return {'quality_passed': True}; return {'quality_passed': False}; graph = StateGraph(TangramState); graph.add_node('validate', validate_dimensions); graph.add_node('material_check', check_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()