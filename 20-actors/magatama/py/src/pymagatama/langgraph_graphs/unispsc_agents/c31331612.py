from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; assembly_passed: bool; def validate_weld_integrity(state: AssemblyState): state['validation_results'].append('NDT performed'); state['assembly_passed'] = state['specs'].get('tensile_strength', 0) > 200; return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_weld_integrity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()