from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class FacsimileState(TypedDict): model_id: str; ribbon_type: str; is_compatible: bool; def validate_compatibility(state: FacsimileState): state['is_compatible'] = state['model_id'].startswith('FX-') and state['ribbon_type'] in ['thermal', 'carbon']; return {'is_compatible': state['is_compatible']}; workflow = StateGraph(FacsimileState); workflow.add_node('validate', validate_compatibility); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()