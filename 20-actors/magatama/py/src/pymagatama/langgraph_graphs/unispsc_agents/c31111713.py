from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): input_data: dict; validation_results: List[str]; graph = StateGraph(State); def validate_material(state: State): return {'validation_results': ['Grade verified']}; def check_tolerances(state: State): return {'validation_results': ['Tolerances within spec']}; graph.add_node('verify_material', validate_material); graph.add_node('check_dims', check_tolerances); graph.set_entry_point('verify_material'); graph.add_edge('verify_material', 'check_dims'); graph.add_edge('check_dims', END); graph = graph.compile()