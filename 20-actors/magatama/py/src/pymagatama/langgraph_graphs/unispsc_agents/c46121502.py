from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MissileState(TypedDict): vendor_id: str; clearance_level: int; compliance_status: bool; components: List[str]; def validate_export_ctrl(state: MissileState): return {'compliance_status': state['clearance_level'] >= 5}; def route_procurement(state: MissileState): return 'process' if state['compliance_status'] else 'reject'; graph = StateGraph(MissileState); graph.add_node('export_check', validate_export_ctrl); graph.add_conditional_edges('export_check', route_procurement, {'process': 'process', 'reject': END}); graph.add_node('process', lambda s: s); graph.set_entry_point('export_check'); graph.add_edge('process', END); graph = graph.compile()