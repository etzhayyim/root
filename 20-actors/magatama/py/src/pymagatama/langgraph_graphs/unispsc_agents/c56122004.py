from langgraph.graph import StateGraph, END; from typing import TypedDict; class SinkProcurementState(TypedDict): specs: dict; validation: bool; def validate_dimensions(state: SinkProcurementState): return {'validation': 'depth' in state['specs'] and 'width' in state['specs']}; def approve_order(state: SinkProcurementState): return {'validation': True}; graph = StateGraph(SinkProcurementState); graph.add_node('validate', validate_dimensions); graph.add_node('approve', approve_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()