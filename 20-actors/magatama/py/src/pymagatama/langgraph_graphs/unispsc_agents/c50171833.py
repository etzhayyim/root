from langgraph.graph import StateGraph, END; from typing import TypedDict; class FoodState(TypedDict): content: str; validated: bool; shelf_life: int; def validate_product(state: FoodState): return {'validated': state.get('shelf_life', 0) > 0}; def check_safety(state: FoodState): return {'validated': state['validated'] and True}; graph = StateGraph(FoodState); graph.add_node('validate', validate_product); graph.add_node('safety', check_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()