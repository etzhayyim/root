from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): cold_chain_verified: bool; expiration_checked: bool; supply_chain_docs: List[str]; def verify_cold_chain(state: State): state['cold_chain_verified'] = True; return state; def validate_docs(state: State): state['expiration_checked'] = True; return state; graph = StateGraph(State); graph.add_node('verify_cold_chain', verify_cold_chain); graph.add_node('validate_docs', validate_docs); graph.set_entry_point('verify_cold_chain'); graph.add_edge('verify_cold_chain', 'validate_docs'); graph.add_edge('validate_docs', END); graph = graph.compile()