from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LiveAnimalState(TypedDict): animal_data: dict; health_cert: str; status: str; def validate_health(state: LiveAnimalState): return {'status': 'HEALTH_VERIFIED' if state['health_cert'] else 'HEALTH_PENDING'}; def verify_logistics(state: LiveAnimalState): return {'status': 'LOGISTICS_READY'}; graph = StateGraph(LiveAnimalState); graph.add_node('validate', validate_health); graph.add_node('logistics', verify_logistics); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()