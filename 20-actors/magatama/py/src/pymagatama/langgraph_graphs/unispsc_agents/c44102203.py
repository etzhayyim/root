from langgraph.graph import StateGraph, END; from typing import TypedDict; class ScannerState(TypedDict): model: str; compat_verified: bool; result: str; def verify_compat(state: ScannerState): state['compat_verified'] = True if state['model'] else False; return state; def finalize_order(state: ScannerState): state['result'] = 'Validated' if state['compat_verified'] else 'Rejected'; return state; graph = StateGraph(ScannerState); graph.add_node('verify', verify_compat); graph.add_node('finalize', finalize_order); graph.set_entry_point('verify'); graph.add_edge('verify', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()