from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CastState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(CastState); def validate_dimensions(state: CastState): return {'validation_results': ['Dimensions Verified']}; def check_material_integrity(state: CastState): return {'validation_results': ['Metallurgical Analysis Passed']}; graph.add_node('validate_dims', validate_dimensions); graph.add_node('check_material', check_material_integrity); graph.set_entry_point('validate_dims'); graph.add_edge('validate_dims', 'check_material'); graph.add_edge('check_material', END); graph.compile()