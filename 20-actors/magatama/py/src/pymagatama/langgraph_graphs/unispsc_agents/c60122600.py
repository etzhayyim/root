from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MosaicState(TypedDict): material: str; dimensions: dict; quality_check: bool; workflow: List[str]; def material_validator(state): state['workflow'].append('Material validation passed'); return state; def dimension_check(state): state['workflow'].append('Dimensions verified'); state['quality_check'] = True; return state; graph = StateGraph(MosaicState); graph.add_node('validate_material', material_validator); graph.add_node('check_dims', dimension_check); graph.add_edge('validate_material', 'check_dims'); graph.add_edge('check_dims', END); graph.set_entry_point('validate_material'); graph = graph.compile()