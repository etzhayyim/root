from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalState(TypedDict): instrument_data: dict; validation_results: List[str]; def validate_material(state: SurgicalState): state['validation_results'].append('Material Checked'); return state; def check_sterilization(state: SurgicalState): state['validation_results'].append('Sterilization Specs Verified'); return state; graph = StateGraph(SurgicalState); graph.add_node('validate_material', validate_material); graph.add_node('check_sterilization', check_sterilization); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_sterilization'); graph.add_edge('check_sterilization', END); graph = graph.compile()