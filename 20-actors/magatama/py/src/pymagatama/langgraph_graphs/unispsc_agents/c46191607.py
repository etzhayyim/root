from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list[str], operator.add]; def validate_standards(state: State): cert = state['input_data'].get('certification'); result = 'Valid' if cert == 'NIOSH' else 'Invalid'; return {'validation_results': [f'Compliance check: {result}']}; def check_safety_features(state: State): return {'validation_results': ['Pressure gauge and alarm verified']}; graph = StateGraph(State); graph.add_node('cert_check', validate_standards); graph.add_node('safety_check', check_safety_features); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()