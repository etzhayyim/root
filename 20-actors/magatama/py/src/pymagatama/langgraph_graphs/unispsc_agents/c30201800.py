from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReliefStructureState(TypedDict): structure_specs: dict; status: str; safety_flags: List[str]; def validate_specs(state: ReliefStructureState): return {'status': 'validated' if 'wind_load' in state['structure_specs'] else 'requires_review'}; def check_compliance(state: ReliefStructureState): return {'safety_flags': ['fire_rating_verified']}; graph = StateGraph(ReliefStructureState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()