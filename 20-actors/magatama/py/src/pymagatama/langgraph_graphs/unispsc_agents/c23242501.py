from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: MachineState): if not state['specs'].get('precision_tolerance'): state['validation_errors'].append('Missing tolerance'); return {'approved': len(state['validation_errors']) == 0}; def export_control_check(state: MachineState): return {'approved': state.get('approved', False)}; graph = StateGraph(MachineState); graph.add_node('validate', validate_specs); graph.add_node('export_check', export_control_check); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()