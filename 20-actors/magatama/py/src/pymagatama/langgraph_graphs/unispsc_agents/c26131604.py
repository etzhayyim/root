from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScreenState(TypedDict): screen_type: str; specs: dict; ready: bool; def validate_specs(state: ScreenState): state['ready'] = 'mounting_mechanism' in state['specs'] and state['specs']['ip_rating'] >= 54; return state; def finalize_order(state: ScreenState): print(f'Processing order for {state['screen_type']}'); return {'ready': True}; graph = StateGraph(ScreenState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()