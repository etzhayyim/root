from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetySpecs(TypedDict): specs: List[str]; compliant: bool; def validate_specs(state: SafetySpecs): state['compliant'] = 'ISO_20345' in state['specs']; return state; builder = StateGraph(SafetySpecs); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()