from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BatteryState(TypedDict): capacity: int; voltage: float; compliant: bool; def validate_specs(state: BatteryState): state['compliant'] = state['capacity'] > 1000 and state['voltage'] > 1.0; return state; graph = StateGraph(BatteryState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()