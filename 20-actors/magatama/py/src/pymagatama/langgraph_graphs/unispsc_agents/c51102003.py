from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): medicine: str; quality_docs: List[str]; approved: bool; def check_gmp(state: PharmaState): return {'approved': 'GMP_Cert' in state['quality_docs']}; graph = StateGraph(PharmaState); graph.add_node('validate_gmp', check_gmp); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', END); compiled_graph = graph.compile()