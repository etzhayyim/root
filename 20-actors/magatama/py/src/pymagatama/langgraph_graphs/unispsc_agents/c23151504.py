from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: MachineState): s = state['specs']; state['valid'] = s.get('motor_power_kw', 0) > 0 and 'control_system' in s; state['log'].append('Validation complete'); return state; graph = StateGraph(MachineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()