from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PositioningState(TypedDict): device_specs: dict; validation_results: List[str]; def validate_medical_specs(state: PositioningState): results = []; if 'ISO_13485' not in state['device_specs']: results.append('Missing ISO 13485'); return {'validation_results': results}; def check_compliance(state: PositioningState): print('Checking regulatory compliance...'); return {'validation_results': state['validation_results'] + ['Compliance Verified']}; graph = StateGraph(PositioningState); graph.add_node('validate', validate_medical_specs); graph.add_node('check', check_compliance); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.set_entry_point('validate'); graph = graph.compile()