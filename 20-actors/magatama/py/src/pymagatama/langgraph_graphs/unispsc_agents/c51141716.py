from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_gmp(state: PharmaState): state['validation_results'].append('GMP_Verified'); return state; def check_storage(state: PharmaState): state['validation_results'].append('Cold_Chain_Validated'); return state; graph = StateGraph(PharmaState); graph.add_node('validate_gmp', validate_gmp); graph.add_node('check_storage', check_storage); graph.add_edge('validate_gmp', 'check_storage'); graph.add_edge('check_storage', END); graph.set_entry_point('validate_gmp'); graph = graph.compile()