from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OphthalmicState(TypedDict): items: List[str]; validated: bool; cert_ok: bool; def validate_instrument(state: OphthalmicState): print('Validating surgical equipment compliance...'); state['validated'] = True; return state; def check_cert(state: OphthalmicState): state['cert_ok'] = True; return state; graph = StateGraph(OphthalmicState); graph.add_node('validate', validate_instrument); graph.add_node('check_cert', check_cert); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_cert'); graph.add_edge('check_cert', END); graph = graph.compile()