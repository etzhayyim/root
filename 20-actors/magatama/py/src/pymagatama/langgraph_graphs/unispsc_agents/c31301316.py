from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DieState(TypedDict): specs: dict; validation_results: List[str]; def validate_purity(state: DieState): return {'validation_results': state['validation_results'] + ['Purity Verified']}; def check_geometry(state: DieState): return {'validation_results': state['validation_results'] + ['Geometry OK']}; graph = StateGraph(DieState); graph.add_node('verify_material', validate_purity); graph.add_node('check_specs', check_geometry); graph.set_entry_point('verify_material'); graph.add_edge('verify_material', 'check_specs'); graph.add_edge('check_specs', END); compiled_graph = graph.compile()