from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): chemical: str; purity: float; status: str; def validate_purity(state: PharmaState): return {'status': 'approved' if state['purity'] >= 99.0 else 'rejected'}; def report_status(state: PharmaState): return {'status': 'processed'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('report', report_status); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()