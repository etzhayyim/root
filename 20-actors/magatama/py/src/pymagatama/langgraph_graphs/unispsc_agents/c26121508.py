from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WireState(TypedDict): wire_type: str; specs: dict; compliant: bool; def validate_specs(state: WireState): state['compliant'] = all(k in state['specs'] for k in ['temp', 'voltage', 'awg']); return state; graph = StateGraph(WireState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()