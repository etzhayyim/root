from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MedicalDocState(TypedDict): doc_type: str; is_compliant: bool; validation_log: List[str]; def validate_medical_doc(state: MedicalDocState): state['is_compliant'] = state['doc_type'] in ['chart', 'report', 'prescription']; state['validation_log'].append('Validation completed'); return state; graph = StateGraph(MedicalDocState); graph.add_node('validate', validate_medical_doc); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()