from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_frame_specs(state: ProcurementState): weight_limit = state['specs'].get('weight_limit', 0); is_valid = weight_limit >= 120; return {'validation_results': [f'Weight limit {weight_limit}kg approved: {is_valid}'], 'status': 'validated' if is_valid else 'rejected'}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_frame_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()