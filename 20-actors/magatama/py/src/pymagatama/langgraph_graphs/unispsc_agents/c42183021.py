from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class OphthalmicState(TypedDict): data: dict; analysis: str; workflow_steps: Annotated[Sequence[str], operator.add]; def validate_device(state: OphthalmicState): return {'analysis': 'Validated calibration standards', 'workflow_steps': ['CertCheck', 'ImagingTest']}; def generate_report(state: OphthalmicState): return {'analysis': 'Clinical report generated', 'workflow_steps': ['DataExport']}; graph = StateGraph(OphthalmicState); graph.add_node('validate', validate_device); graph.add_node('export', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()