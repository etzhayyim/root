from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: MoldState): tolerance = state['specs'].get('dimensional_tolerance_mm', 0.05); return {'validation_results': ['Dimension check passed'] if tolerance <= 0.1 else ['Dimension check failed']}; def check_material(state: MoldState): return {'validation_results': state['validation_results'] + ['Material stress test complete']}; graph = StateGraph(MoldState); graph.add_node('validate_dim', validate_dimensions); graph.add_node('stress_test', check_material); graph.add_edge('validate_dim', 'stress_test'); graph.add_edge('stress_test', END); graph.set_entry_point('validate_dim'); graph = graph.compile()