from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): metal_type: str; tolerances: dict; specs_verified: bool; graph = StateGraph(CastingState); def validate_brass_specs(state: CastingState): state['specs_verified'] = state['metal_type'] == 'brass' and all(v < 0.05 for v in state['tolerances'].values()); return state; graph.add_node('validate', validate_brass_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()