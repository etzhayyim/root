from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): struct_id: str; compliance_checks: List[str]; approved: bool; def validate_structure(state: State): state['compliance_checks'].append('Safety Code Verified'); state['approved'] = True; return state; graph = StateGraph(State); graph.add_node('validate', validate_structure); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()