from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ProcurementState): spec = state['specs']; errors = []; if 'alloy' not in spec: errors.append('Missing alloy composition'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()