from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GroundingState(TypedDict): vendor_specs: dict; compliance_verified: bool; def validate_specs(state: GroundingState): specs = state.get('vendor_specs', {}); conductivity = specs.get('conductivity', 0); is_valid = conductivity >= 95.0; return {'compliance_verified': is_valid}; graph = StateGraph(GroundingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()