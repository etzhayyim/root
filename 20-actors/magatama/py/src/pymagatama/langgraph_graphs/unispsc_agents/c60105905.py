from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): content_data: str; validation_results: Annotated[list, operator.add]; def validate_content(state: ProcureState): return {'validation_results': ['Compliance Verified']}; def route_procurement(state: ProcureState): return 'end'; graph = StateGraph(ProcureState); graph.add_node('validate', validate_content); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()