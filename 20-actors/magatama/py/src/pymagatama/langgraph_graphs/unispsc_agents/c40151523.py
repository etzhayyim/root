from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: PumpState): specs = state['specs']; errors = []; if specs.get('pressure', 0) > 100: errors.append('Pressure rating exceeds standard safety thresholds.'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()