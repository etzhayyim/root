from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LabelState(TypedDict): vendor_id: str; label_specs: dict; compliance_check: bool; def validate_specs(state: LabelState): print('Validating material and core size...'); state['compliance_check'] = True; return state; graph = StateGraph(LabelState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()