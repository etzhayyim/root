from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class StampState(TypedDict): items: List[str]; validation_logs: List[str]; def validate_supplies(state: StampState): return {'validation_logs': ['Checking SDS compliance', 'Verifying ink compatibility']}; def finalize_procurement(state: StampState): return {'validation_logs': state['validation_logs'] + ['Procurement request finalized']}; graph = StateGraph(StampState); graph.add_node('validate', validate_supplies); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()