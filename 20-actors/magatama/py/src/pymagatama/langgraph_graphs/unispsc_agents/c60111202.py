from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): border_specs: List[str]; validation_status: str; def validate_border_pack(state: GraphState): return {'validation_status': 'verified' if state['border_specs'] else 'failed'}; graph = StateGraph(GraphState); graph.add_node('validate', validate_border_pack); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()