from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MedicalSoftwareState(TypedDict): vendor_docs: List[str]; regulatory_status: bool; validation_passed: bool; def check_regulations(state: MedicalSoftwareState): return {'regulatory_status': 'FDA_Cleared' in state['vendor_docs']}; def validate_integration(state: MedicalSoftwareState): return {'validation_passed': True}; graph = StateGraph(MedicalSoftwareState); graph.add_node('regulatory_check', check_regulations); graph.add_node('integration_validation', validate_integration); graph.set_entry_point('regulatory_check'); graph.add_edge('regulatory_check', 'integration_validation'); graph.add_edge('integration_validation', END); graph = graph.compile()