from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; validation_report: List[str]; approved: bool; def validate_specs(state: CompressorState): errors = []; if state['specs'].get('iso_purity') != 'Class 0': errors.append('Insufficient air purity'); return {'validation_report': errors, 'approved': len(errors) == 0}; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()