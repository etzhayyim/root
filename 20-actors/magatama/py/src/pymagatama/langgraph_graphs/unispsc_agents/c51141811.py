from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; validation_status: bool; regulatory_check: bool; def check_license(state): return {'regulatory_check': True}; def validate_purity(state): return {'validation_status': state.get('purity_percentage', 0) >= 99.0}; graph = StateGraph(PharmState); graph.add_node('verify_license', check_license); graph.add_node('check_quality', validate_purity); graph.set_entry_point('verify_license'); graph.add_edge('verify_license', 'check_quality'); graph.add_edge('check_quality', END); graph = graph.compile()