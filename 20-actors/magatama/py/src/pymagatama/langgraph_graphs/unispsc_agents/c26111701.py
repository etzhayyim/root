from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BatteryState(TypedDict): specs: dict; compliance_checks: List[str]; approved: bool; def validate_specs(state: BatteryState): required = ['UN38.3', 'Voltage']; state['compliance_checks'] = [s for s in required if s in state['specs']]; state['approved'] = len(state['compliance_checks']) == len(required); return state; graph = StateGraph(BatteryState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()