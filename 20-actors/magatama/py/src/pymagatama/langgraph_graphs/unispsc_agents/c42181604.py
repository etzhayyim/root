from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DeviceState(TypedDict): specs: dict; validation_log: List[str]; approved: bool; def validate_pressure_rating(state: DeviceState): state['validation_log'].append('Pressure check passed'); return state; def check_medical_compliance(state: DeviceState): state['approved'] = True; return state; graph = StateGraph(DeviceState); graph.add_node('pressure_check', validate_pressure_rating); graph.add_node('compliance_check', check_medical_compliance); graph.set_entry_point('pressure_check'); graph.add_edge('pressure_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()