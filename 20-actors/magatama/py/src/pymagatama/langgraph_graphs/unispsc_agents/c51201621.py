from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VaccineState(TypedDict): cold_chain_verified: bool; inventory_status: str; audit_logs: List[str]; def verify_storage(state: VaccineState): return { 'cold_chain_verified': True }; def update_inventory(state: VaccineState): return { 'inventory_status': 'SHIPPED' }; graph = StateGraph(VaccineState); graph.add_node('verify', verify_storage); graph.add_node('ship', update_inventory); graph.add_edge('verify', 'ship'); graph.add_edge('ship', END); graph.set_entry_point('verify'); graph = graph.compile()