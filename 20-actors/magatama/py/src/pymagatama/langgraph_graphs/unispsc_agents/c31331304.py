from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class InconelState(TypedDict): specs: dict; validation_report: List[str]; assembly_status: str; def validate_weld_integrity(state: InconelState): state['validation_report'].append('Weld integrity verified'); return {'assembly_status': 'Validated'}; def check_export_compliance(state: InconelState): state['validation_report'].append('Export compliance verified'); return {'assembly_status': 'Cleared'}; graph = StateGraph(InconelState); graph.add_node('weld_check', validate_weld_integrity); graph.add_node('export_check', check_export_compliance); graph.add_edge('weld_check', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('weld_check'); graph = graph.compile()