from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): chemical_name: str; purity: float; compliance_docs: List[str]; approved: bool; def validate_purity(state: PharmaState): state['approved'] = state['purity'] >= 99.5; return state; def check_compliance(state: PharmaState): state['approved'] = 'USP' in state['compliance_docs'] and state['approved']; return state; graph = StateGraph(PharmaState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_compliance'); graph.add_edge('check_compliance', END); graph = graph.compile()