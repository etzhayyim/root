from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): items: List[str]; validated: bool; def validate_dental_tool(state: DentalState): state['validated'] = all(['material' in item.lower() for item in state['items']]); return state; def check_compliance(state: DentalState): print('Checking ISO 13485 compliance...'); return state; graph = StateGraph(DentalState); graph.add_node('validate', validate_dental_tool); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()