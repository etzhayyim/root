from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PurchaseState(TypedDict): vendor_id: str; item_spec: dict; risk_score: int; approved: bool; def validate_spec(state): state['approved'] = 'weight_capacity' in state['item_spec']; return state; def assess_risk(state): state['risk_score'] = 1 if state['approved'] else 5; return state; graph = StateGraph(PurchaseState); graph.add_node('validate', validate_spec); graph.add_node('risk', assess_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'risk'); graph.add_edge('risk', END); graph = graph.compile()