from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): component_id: str; specs: dict; validation_results: Annotated[list, operator.add]; def validate_dimensions(state: ProcState): return {'validation_results': ['Dimensional check passed']}; def perform_nondestructive_test(state: ProcState): return {'validation_results': ['NDT inspection complete']}; graph = StateGraph(ProcState); graph.add_node('val_dims', validate_dimensions); graph.add_node('ndt_check', perform_nondestructive_test); graph.set_entry_point('val_dims'); graph.add_edge('val_dims', 'ndt_check'); graph.add_edge('ndt_check', END); graph = graph.compile()