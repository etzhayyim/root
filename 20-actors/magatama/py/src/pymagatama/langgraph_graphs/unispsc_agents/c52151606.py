from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): item: str; dimensions: dict; safety_compliant: bool; graph = StateGraph(ProcurementState); def validate_material(state: ProcurementState): return {'safety_compliant': True}; def update_dimensions(state: ProcurementState): return {'dimensions': {'length': 0, 'width': 0}}; graph.add_node('validate_material', validate_material); graph.add_node('update_dimensions', update_dimensions); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'update_dimensions'); graph.add_edge('update_dimensions', END); graph = graph.compile()