from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EvidenceState(TypedDict): items: List[str]; validated: bool; def validate_lifter(state: EvidenceState): state['validated'] = all('lifter' in item.lower() for item in state['items']); return state; def finalize(state: EvidenceState): return {'validated': state['validated']}; graph = StateGraph(EvidenceState); graph.add_node('validate_spec', validate_lifter); graph.add_node('finalizer', finalize); graph.set_entry_point('validate_spec'); graph.add_edge('validate_spec', 'finalizer'); graph.add_edge('finalizer', END); graph = graph.compile()