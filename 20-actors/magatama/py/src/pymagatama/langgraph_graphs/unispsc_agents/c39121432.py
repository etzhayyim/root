from langgraph.graph import StateGraph, END; from typing import TypedDict; class TerminalState(TypedDict): specs: dict; validation_passed: bool; def validate_terminal_specs(state: TerminalState): valid = all(k in state['specs'] for k in ['amp_rating', 'material']); return {'validation_passed': valid}; graph = StateGraph(TerminalState); graph.add_node('validate', validate_terminal_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()