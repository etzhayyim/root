from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicroscopeState(TypedDict): vendor_specs: dict; validation_report: List[str]; graph = StateGraph(MicroscopeState); def validate_optical_standards(state: MicroscopeState): return {'validation_report': ['Optical mount verified', 'Sensor compatibility checked']}; def check_export_controls(state: MicroscopeState): return {'validation_report': ['Export control screening completed']}; graph.add_node('validate_optics', validate_optical_standards); graph.add_node('check_export', check_export_controls); graph.set_entry_point('validate_optics'); graph.add_edge('validate_optics', 'check_export'); graph.add_edge('check_export', END); compile_graph = graph.compile()