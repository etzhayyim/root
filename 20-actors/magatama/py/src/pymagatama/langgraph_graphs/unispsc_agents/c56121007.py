from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TableState(TypedDict): vendor_specs: dict; inspection_status: str; approved: bool; def validate_specs(state: TableState): specs = state['vendor_specs']; valid = all([specs.get('load_capacity', 0) > 200, specs.get('fire_rated', False)]); return {'inspection_status': 'passed' if valid else 'failed', 'approved': valid}; graph = StateGraph(TableState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()