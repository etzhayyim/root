from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CapState(TypedDict): items: List[dict]; inspection_passed: bool; def material_check(state: CapState): state['inspection_passed'] = all(i.get('material') != 'prohibited' for i in state['items']); return state; def embroidery_check(state: CapState): return state; graph = StateGraph(CapState); graph.add_node('material_check', material_check); graph.add_node('embroidery_check', embroidery_check); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'embroidery_check'); graph.add_edge('embroidery_check', END); graph = graph.compile()