from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ServerState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: ServerState): errors = []; if not state['specs'].get('cpu'): errors.append('Missing CPU'); return {'validation_errors': errors, 'status': 'validated' if not errors else 'failed'}; graph = StateGraph(ServerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiler = graph.compile()