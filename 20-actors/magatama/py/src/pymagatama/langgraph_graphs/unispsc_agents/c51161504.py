from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): content: str; validation_flags: Annotated[list, operator.add]; def validate_api(state: DrugState): return {'validation_flags': ['GMP_CHECKED', 'PURITY_VERIFIED']}; def check_safety(state: DrugState): return {'validation_flags': ['SDS_ATTACHED']}; graph = StateGraph(DrugState); graph.add_node('validate_api', validate_api); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate_api'); graph.add_edge('validate_api', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()