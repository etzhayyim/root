from langgraph.graph import StateGraph; from typing import TypedDict, List; class PaintState(TypedDict): vendor_certs: List[str]; voc_compliant: bool; spec_validated: bool; def validate_paint_specs(state: PaintState): state['spec_validated'] = 'ISO-12944' in state['vendor_certs'] and state['voc_compliant']; return state; graph = StateGraph(PaintState); graph.add_node('validate', validate_paint_specs); graph.set_entry_point('validate'); graph.set_finish_point('validate');