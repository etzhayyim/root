from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotControlState(TypedDict): controller_id: str; specs: dict; valid: bool; def validate_specs(state: RobotControlState): state['valid'] = 'processor' in state['specs'] and 'protocol' in state['specs']; return state; def check_export_controls(state: RobotControlState): if state.get('dual_use'): print('Flagging for trade compliance review'); return state; graph = StateGraph(RobotControlState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_controls); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()