from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): status: str; purity: float; certs: list; graph = StateGraph(ProcState); def validate_purity(state: ProcState): return { "status": "validated" if state['purity'] >= 99.9 else "rejected" }; def check_certs(state: ProcState): return { "status": "approved" if "GMP" in state['certs'] else "rejected" }; graph.add_node("validate", validate_purity); graph.add_node("compliance", check_certs); graph.set_entry_point("validate"); graph.add_edge("validate", "compliance"); graph.add_edge("compliance", END); graph = graph.compile()