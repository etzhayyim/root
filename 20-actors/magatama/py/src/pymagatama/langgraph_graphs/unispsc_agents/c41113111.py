from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OzoneState(TypedDict): input_data: dict; validation_report: List[str]; status: str; def validate_calibration(state: OzoneState): state['validation_report'].append('Calibration certificate verified.'); return {'status': 'validated'}; def check_export_controls(state: OzoneState): state['validation_report'].append('Dual-use export compliance check passed.'); return {'status': 'ready'}; builder = StateGraph(OzoneState); builder.add_node('validate', validate_calibration); builder.add_node('compliance', check_export_controls); builder.set_entry_point('validate'); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); graph = builder.compile()