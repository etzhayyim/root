from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MatRackState(TypedDict): dimensions: dict; safety_compliant: bool; storage_capacity: int; def validate_dimensions(state: MatRackState): return {'safety_compliant': state['dimensions'].get('width', 0) < 200}; def calculate_capacity(state: MatRackState): return {'storage_capacity': state['dimensions'].get('depth', 0) // 10}; graph = StateGraph(MatRackState); graph.add_node('validator', validate_dimensions); graph.add_node('calculator', calculate_capacity); graph.set_entry_point('validator'); graph.add_edge('validator', 'calculator'); graph.add_edge('calculator', END); compiled_graph = graph.compile()