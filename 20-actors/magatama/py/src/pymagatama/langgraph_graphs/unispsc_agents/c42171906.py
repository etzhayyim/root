from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IntubationState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_grade(state: IntubationState): certs = state.get('certs', []); return {'validated': 'ISO_13485' in certs}; def assembly_workflow(state: IntubationState): return {'items': ['Airway Case', 'Laryngoscope', 'ETT']}; graph = StateGraph(IntubationState); graph.add_node('validate', validate_medical_grade); graph.add_node('assemble', assembly_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph = graph.compile()