from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BerryState(TypedDict): vendor: str; quality_score: float; needs_refrigeration: bool; inspector_notes: str; def validate_quality(state: BerryState): return {'quality_score': state['quality_score'] + 0.1 if state['needs_refrigeration'] else 0.0}; def finalize_order(state: BerryState): return {'inspector_notes': 'Verified for freshness'}; graph = StateGraph(BerryState); graph.add_node('validate', validate_quality); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()