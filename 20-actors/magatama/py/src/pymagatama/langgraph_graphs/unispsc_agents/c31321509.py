from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): vendor_docs: dict; quality_check: bool; assembly_specs: dict; def validate_specs(state: AssemblyState): print('Validating weld and material specs...'); return {'quality_check': True} if 'material' in state['assembly_specs'] else {'quality_check': False}; def approve_procurement(state: AssemblyState): return {'status': 'Approved' if state['quality_check'] else 'Rejected'}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()