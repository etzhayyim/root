from langgraph.graph import StateGraph, END; from typing import TypedDict; class FoodState(TypedDict): product: str; status: str; def validate_product(state: FoodState): return {'status': 'validated'} if state['product'] else {'status': 'error'}; def finalize_order(state: FoodState): return {'status': 'complete'}; graph = StateGraph(FoodState); graph.add_node('validate', validate_product); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()