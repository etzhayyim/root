from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PalletState(TypedDict): dimensions: dict; material: str; compliant: bool; next_step: str; def validate_pallet(state: PalletState): state['compliant'] = state['dimensions']['load_limit'] > 0 and state['material'] in ['wood', 'plastic', 'metal']; return {'next_step': 'approve' if state['compliant'] else 'reject'}; def route_step(state: PalletState): return state['next_step']; graph = StateGraph(PalletState); graph.add_node('validate', validate_pallet); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()