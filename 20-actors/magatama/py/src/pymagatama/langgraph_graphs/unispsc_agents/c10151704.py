from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class FeedState(TypedDict): code: str; specs: dict; validation_status: str; next_step: str; def validate_feed_specs(state: FeedState) -> FeedState: state['validation_status'] = 'COMPLIANT' if 'composition_analysis_report' in state['specs'] else 'PENDING'; return state; def route_procurement(state: FeedState) -> str: return 'VALIDATE' if state['validation_status'] == 'PENDING' else END; graph = StateGraph(FeedState); graph.add_node('validate', validate_feed_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()