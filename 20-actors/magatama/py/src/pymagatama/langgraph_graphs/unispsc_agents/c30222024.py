from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FerryTerminalState(TypedDict): site_specs: str; compliance_checks: List[str]; approved: bool; def validate_engineering(state: FerryTerminalState): return {'compliance_checks': ['structural_load_test', 'marine_access_check'], 'approved': True}; graph = StateGraph(FerryTerminalState); graph.add_node('engineering_review', validate_engineering); graph.add_edge('engineering_review', END); graph.set_entry_point('engineering_review'); graph = graph.compile()