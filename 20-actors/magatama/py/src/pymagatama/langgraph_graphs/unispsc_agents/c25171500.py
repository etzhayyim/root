from langgraph.graph import StateGraph, END; from typing import TypedDict; class WiperState(TypedDict): specs: dict; valid: bool; def validate_specs(state: WiperState): state['valid'] = 'Blade Length' in state['specs'] and 'Performance Standard' in state['specs']; return state; graph = StateGraph(WiperState); graph.add_node('val', validate_specs); graph.set_entry_point('val'); graph.add_edge('val', END); compiled_graph = graph.compile()