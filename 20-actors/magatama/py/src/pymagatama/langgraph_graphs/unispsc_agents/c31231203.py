from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): material_spec: dict; qa_result: bool; def validate_grade(state: State): state['qa_result'] = 'C3604' in state['material_spec'].get('grade', ''); return state; def check_dimensions(state: State): return {'qa_result': state['material_spec'].get('thickness', 0) > 0}; graph = StateGraph(State); graph.add_node('validate_grade', validate_grade); graph.add_node('check_dimensions', check_dimensions); graph.set_entry_point('validate_grade'); graph.add_edge('validate_grade', 'check_dimensions'); graph.add_edge('check_dimensions', END); compiled_graph = graph.compile()