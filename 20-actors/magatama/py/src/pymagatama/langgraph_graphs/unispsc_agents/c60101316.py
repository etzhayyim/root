from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlashCardState(TypedDict): dimensions: dict; material: str; status: str; def validate_specs(state: FlashCardState): return {'status': 'validated'} if state['dimensions'] else {'status': 'pending'}; graph = StateGraph(FlashCardState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()