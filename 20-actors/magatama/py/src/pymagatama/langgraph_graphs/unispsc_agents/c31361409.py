from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): vendor_specs: dict; validation_passed: bool; report: str; def validate_welds(state: AssemblyState): state['validation_passed'] = 'ASME' in state['vendor_specs'].get('standards', []); return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_welds); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()