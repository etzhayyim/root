from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): model_specs: dict; is_sterile: bool; validation_passed: bool; def check_medical_grade(state: ProcurementState): state['validation_passed'] = state['is_sterile'] and 'standard_iso' in state['model_specs']; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', check_medical_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()