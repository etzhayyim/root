from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity: float; is_compliant: bool; def validate_purity(state: PharmaState): state['is_compliant'] = state['purity'] >= 99.9; return state; def check_regulations(state: PharmaState): print(f'Checking {state['chemical_name']} against FDA/EMA standards'); return state; builder = StateGraph(PharmaState); builder.add_node('validate', validate_purity); builder.add_node('compliance', check_regulations); builder.set_entry_point('validate'); builder.add_edge('validate', 'compliance'); builder.add_edge('compliance', END); graph = builder.compile()