from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; structural_integrity_score: float; compliance_checks: List[str]; graph = StateGraph(AssemblyState); def validate_integrity(state: AssemblyState): return {'structural_integrity_score': 0.95}; def verify_compliance(state: AssemblyState): return {'compliance_checks': ['AMS-T-9046', 'AS9100']}; graph.add_node('integrity', validate_integrity); graph.add_node('compliance', verify_compliance); graph.set_entry_point('integrity'); graph.add_edge('integrity', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()