from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GermaniumState(TypedDict): purity_check: bool; material_spec: dict; compliance_approved: bool; def validate_purity(state: GermaniumState): return {'purity_check': state.get('material_spec', {}).get('purity', 0) >= 0.99999}; def export_control_check(state: GermaniumState): return {'compliance_approved': True}; graph = StateGraph(GermaniumState); graph.add_node('validate_purity', validate_purity); graph.add_node('export_control_check', export_control_check); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'export_control_check'); graph.add_edge('export_control_check', END); graph = graph.compile()