from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlateState(TypedDict): specs: dict; valid: bool; output: str; def validate_plate(state: PlateState): state['valid'] = 'thickness' in state['specs'] and 'grade' in state['specs']; return state; drawer = StateGraph(PlateState); drawer.add_node('validate', validate_plate); drawer.set_entry_point('validate'); drawer.add_edge('validate', END); graph = drawer.compile()