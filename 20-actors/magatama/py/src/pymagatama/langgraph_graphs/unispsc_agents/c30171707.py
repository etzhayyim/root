from langgraph.graph import StateGraph, END; from typing import TypedDict; class GlassState(TypedDict): specs: dict; valid: bool; def validate_glass_specs(state: GlassState): specs = state.get('specs', {}); state['valid'] = 'impact_certification_code' in specs and specs.get('thickness', 0) > 0; return state; graph = StateGraph(GlassState); graph.add_node('validate', validate_glass_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()