from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ForgingState(TypedDict): dimensions: dict; material_specs: dict; validated: bool; def validate_specs(state: ForgingState): state['validated'] = all([state['dimensions'].get('od'), state['material_specs'].get('grade')]); return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()