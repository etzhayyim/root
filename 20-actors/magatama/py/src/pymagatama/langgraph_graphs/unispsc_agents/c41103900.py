from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CentrifugeState(TypedDict): specs: dict; validation_errors: List[str]; def validate_specs(state: CentrifugeState): errors = []; if state['specs'].get('speed', 0) <= 0: errors.append('Invalid RPM'); return {'validation_errors': errors}; graph = StateGraph(CentrifugeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()