from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MasonryState(TypedDict): nails_specs: dict; validation_report: str; def validate_specs(state: MasonryState): return {'validation_report': 'Passed' if state['nails_specs'].get('hardness') > 50 else 'Failed'}; graph = StateGraph(MasonryState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()