from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class WeldingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: WeldingState): state['valid'] = 'Power Output' in state['specs'] and state['specs']['Power Output'] > 0; return state; graph = StateGraph(WeldingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()