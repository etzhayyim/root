from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): chemical_specs: dict; is_compliant: bool; def validate_chemistry(state: State): state['is_compliant'] = state['chemical_specs'].get('ph_level') is not None; return state; graph = StateGraph(State); graph.add_node('validate', validate_chemistry); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()