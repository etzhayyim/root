from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_results: dict; graph = StateGraph(State); def validate_specs(state: State): return {'validation_results': {'status': 'pass' if 'capacity' in state['input_data'] else 'fail'}}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()