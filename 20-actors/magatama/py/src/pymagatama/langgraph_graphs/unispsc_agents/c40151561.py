from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): pump_specs: dict; validation_report: list[str]; def validate_pump(state: PumpState): specs = state['pump_specs']; report = []; if specs.get('pressure', 0) > 500: report.append('High pressure alert: Safety valve specification required.'); return {'validation_report': report}; graph = StateGraph(PumpState); graph.add_node('validation', validate_pump); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()