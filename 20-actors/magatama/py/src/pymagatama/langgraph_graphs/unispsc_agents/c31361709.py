from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; structural_integrity: bool; compliance: bool; def validate_dimensions(state: AssemblyState): state['structural_integrity'] = True; return state; def check_compliance(state: AssemblyState): state['compliance'] = True; return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_dimensions); graph.add_node('compliant', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliant'); graph.add_edge('compliant', END); app = graph.compile()