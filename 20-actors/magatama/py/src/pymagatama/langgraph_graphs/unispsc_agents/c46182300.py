from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetyState(TypedDict): equipment_ids: List[str]; inspection_reports: dict; valid_certs: bool; def validate_safety_certs(state: SafetyState): return {'valid_certs': all(state['inspection_reports'].values())}; def log_risk(state: SafetyState): print(f'Processing safety equipment: {state["equipment_ids"]}'); return {}; graph = StateGraph(SafetyState); graph.add_node('validate', validate_safety_certs); graph.add_node('log', log_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph = graph.compile()