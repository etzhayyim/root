from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): material: str; diameter_range: str; is_compliant: bool; def validate_specs(state: State): state['is_compliant'] = state['material'] in ['Stainless Steel', 'Zinc-Plated Steel'] and float(state['diameter_range'].split('-')[0]) > 0; return state; graph = StateGraph(State); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()