from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FreezerState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: FreezerState): specs = state['specs']; is_valid = all(key in specs for key in ['TemperatureRangeCelsius', 'RefrigerantType']); return {'valid': is_valid, 'error': '' if is_valid else 'Missing required specs'}; graph = StateGraph(FreezerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()