from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProjectorState(TypedDict): specs: dict; valid: bool; def tech_eval(state: ProjectorState): state['valid'] = 'technology' in state['specs'] and state['specs']['technology'] == 'digital-film'; return state; def compliance_check(state: ProjectorState): state['valid'] = state['valid'] and state['specs'].get('cert_id') is not None; return state; graph = StateGraph(ProjectorState); graph.add_node('tech_eval', tech_eval); graph.add_node('compliance_check', compliance_check); graph.add_edge('tech_eval', 'compliance_check'); graph.add_edge('compliance_check', END); graph.set_entry_point('tech_eval'); graph = graph.compile()