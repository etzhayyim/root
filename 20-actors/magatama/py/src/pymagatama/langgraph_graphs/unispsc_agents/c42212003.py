from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; approval: bool; graph = StateGraph(State); def validate_specs(state: State) -> dict: return {"approval": all(k in state["input_data"] for k in ["material", "torque"])}; graph.add_node("validate", validate_specs); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()