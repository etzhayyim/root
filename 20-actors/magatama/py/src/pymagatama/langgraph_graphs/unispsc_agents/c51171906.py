from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medicine_name: str; gmp_certified: bool; quality_report: str; approved: bool; def validate_gmp(state: PharmaState): state['approved'] = state['gmp_certified'] and len(state['quality_report']) > 0; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()