from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_license: str; purity_check: bool; compliance_status: bool; def verify_regulatory_compliance(state: ProcureState): return {'compliance_status': state['vendor_license'] != ''}; def validate_purity(state: ProcureState): return {'purity_check': True}; graph = StateGraph(ProcureState); graph.add_node('verify', verify_regulatory_compliance); graph.add_node('assay', validate_purity); graph.add_edge('verify', 'assay'); graph.add_edge('assay', END); graph.set_entry_point('verify'); graph = graph.compile()