from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SterilizationState(TypedDict): bag_specs: dict; compliance_verified: bool; def check_compliance(state: SterilizationState): state['compliance_verified'] = 'ISO11607' in state['bag_specs'].get('certifications', []); return state; def validate_specs(state: SterilizationState): return {'compliance_verified': state.get('compliance_verified', False)}; graph = StateGraph(SterilizationState); graph.add_node('check_compliance', check_compliance); graph.add_node('validate_specs', validate_specs); graph.set_entry_point('check_compliance'); graph.add_edge('check_compliance', 'validate_specs'); graph.add_edge('validate_specs', END); compiled_graph = graph.compile()