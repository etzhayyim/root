from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class JigState(TypedDict): specs: dict; validation_report: str; graph = StateGraph(JigState); def validate_tolerance(state: JigState): state['validation_report'] = 'Tolerance check: PASSED' if state['specs'].get('tolerance') < 0.01 else 'FAILED'; return state; def check_materials(state: JigState): state['validation_report'] += ' | Material verification complete.'; return state; graph.add_node('validate', validate_tolerance); graph.add_node('material_check', check_materials); graph.set_entry_point('validate'); graph.add_edge('validate', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()