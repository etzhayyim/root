from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EducationState(TypedDict): content: str; validated: bool; feedback: List[str]; def validation_step(state: EducationState): is_valid = 'birth defects' in state['content'].lower(); return {'validated': is_valid, 'feedback': ['Passed medical review'] if is_valid else ['Requires medical verification']}; graph = StateGraph(EducationState); graph.add_node('validate', validation_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()