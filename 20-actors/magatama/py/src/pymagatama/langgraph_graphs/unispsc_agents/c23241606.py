from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): payload: float; status: str; def validate_robotics_spec(state: RobotState): return {'status': 'validated'} if state['payload'] > 0 else {'status': 'invalid'}; graph = StateGraph(RobotState); graph.add_node('validate', validate_robotics_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()