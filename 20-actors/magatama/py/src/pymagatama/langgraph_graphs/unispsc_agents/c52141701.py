from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DentalDeviceState(TypedDict): device_info: dict; validation_results: Annotated[list, operator.add]; def validate_specs(state: DentalDeviceState): result = 'Valid' if state['device_info'].get('ipx_rating', 0) >= 7 else 'Invalid'; return {'validation_results': [result]}; graph = StateGraph(DentalDeviceState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()