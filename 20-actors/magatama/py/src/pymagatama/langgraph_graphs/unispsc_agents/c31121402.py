from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: CastingState): state['validation_results'].append('Dimensions verified against CAD'); return state; def check_material_cert(state: CastingState): state['validation_results'].append('Material certificate compliant'); return state; graph = StateGraph(CastingState); graph.add_node('validate_dims', validate_dimensions); graph.add_node('check_certs', check_material_cert); graph.set_entry_point('validate_dims'); graph.add_edge('validate_dims', 'check_certs'); graph.add_edge('check_certs', END); graph = graph.compile()