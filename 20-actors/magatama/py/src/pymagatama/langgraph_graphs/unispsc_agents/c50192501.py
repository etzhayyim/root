from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SandwichState(TypedDict): items: List[dict]; validation_status: bool; delivery_ready: bool; def validate_compliance(state: SandwichState): state['validation_status'] = all('temp' in item and 'expiry' in item for item in state['items']); return state; def check_logistics(state: SandwichState): state['delivery_ready'] = state['validation_status'] and len(state['items']) > 0; return state; graph = StateGraph(SandwichState); graph.add_node('validate', validate_compliance); graph.add_node('logistics', check_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); compiled_graph = graph.compile()