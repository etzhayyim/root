from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): device_id: str; raw_data: dict; calibration_status: bool; validation_log: List[str]; def check_calibration(state: AnalysisState): return {'calibration_status': True} if 'cal_data' in state['raw_data'] else {'calibration_status': False}; def validate_emissions(state: AnalysisState): return {'validation_log': ['Compliance check passed']} if state['calibration_status'] else {'validation_log': ['Calibration failed. Stop process.']}; graph = StateGraph(AnalysisState); graph.add_node('check', check_calibration); graph.add_node('validate', validate_emissions); graph.add_edge('check', 'validate'); graph.add_edge('validate', END); graph.set_entry_point('check'); graph = graph.compile()