from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HydroGenState(TypedDict): capacity_mw: float; voltage_kv: float; status: str; def validate_specs(state: HydroGenState): state['status'] = 'VALIDATED' if state['capacity_mw'] > 0 else 'INVALID'; return state; def check_export_compliance(state: HydroGenState): state['status'] = 'COMPLIANT' if state['status'] == 'VALIDATED' else 'ERROR'; return state; graph = StateGraph(HydroGenState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()