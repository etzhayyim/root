from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; valid: bool; error: str; def validate_instrument(state: State): specs = state.get('specs', {}); state['valid'] = 'calibration_certificate' in specs; return state; graph = StateGraph(State); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()