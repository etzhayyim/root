from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BassinetState(TypedDict): model_specs: dict; compliance_check: bool; def validate_safety(state: BassinetState): state['compliance_check'] = state['model_specs'].get('safety_cert') == 'ASTM_F2194'; return state; def finalize_procurement(state: BassinetState): return {'finalized': True}; graph = StateGraph(BassinetState); graph.add_node('validate', validate_safety); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()