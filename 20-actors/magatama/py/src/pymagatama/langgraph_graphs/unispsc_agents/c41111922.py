from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LampSensorState(TypedDict): sensor_id: str; specs: dict; validation_passed: bool; def validate_specs(state: LampSensorState): state['validation_passed'] = 'Input Voltage' in state['specs'] and 'Sensitivity' in state['specs']; return state; def route_to_procure(state: LampSensorState): return 'procure' if state['validation_passed'] else 'revise'; graph = StateGraph(LampSensorState); graph.add_node('validate', validate_specs); graph.add_node('procure', lambda x: x); graph.add_edge('validate', 'procure'); graph.set_entry_point('validate'); graph.set_finish_point('procure'); compiled_graph = graph.compile()