from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CargoState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_specs(state: CargoState): return {'validation_results': ['Specs Verified'], 'status': 'VALIDATED'} if state['specs'].get('load_capacity') else {'status': 'REJECTED'}; graph = StateGraph(CargoState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()