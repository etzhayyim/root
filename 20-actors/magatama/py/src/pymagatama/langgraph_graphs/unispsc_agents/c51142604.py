from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PemolineState(TypedDict): chemical_data: dict; verification_status: bool; compliance_docs: List[str]; graph = StateGraph(PemolineState); def validate_purity(state: PemolineState): state['verification_status'] = state['chemical_data'].get('purity', 0) >= 99.0; return state; graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()