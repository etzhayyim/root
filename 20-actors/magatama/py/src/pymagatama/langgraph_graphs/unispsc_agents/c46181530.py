from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FingerCotState(TypedDict): specs: dict; validation_passed: bool; def check_quality(state: FingerCotState): state['validation_passed'] = state['specs'].get('aql') <= 1.5; return state; graph = StateGraph(FingerCotState); graph.add_node('quality_check', check_quality); graph.set_entry_point('quality_check'); graph.add_edge('quality_check', END); graph = graph.compile()