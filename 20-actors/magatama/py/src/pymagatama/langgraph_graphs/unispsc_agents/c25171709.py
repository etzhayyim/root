from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BrakeState(TypedDict): specs: dict; validation_results: List[str]; def validate_thermal_specs(state: BrakeState): if state['specs'].get('cooling_capacity', 0) > 0: return {'validation_results': ['Thermal capacity OK']}; return {'validation_results': ['Thermal capacity insufficient']}; def run_quality_check(state: BrakeState): return {'validation_results': state['validation_results'] + ['Mechanical Seal Passed']}; graph = StateGraph(BrakeState); graph.add_node('thermal_check', validate_thermal_specs); graph.add_node('seal_check', run_quality_check); graph.set_entry_point('thermal_check'); graph.add_edge('thermal_check', 'seal_check'); graph.add_edge('seal_check', END); graph = graph.compile()