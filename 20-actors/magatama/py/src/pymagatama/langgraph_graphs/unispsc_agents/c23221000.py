from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineSpecState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_specs(state: MachineSpecState): state['valid'] = state['specs'].get('accuracy', 0) < 5; state['logs'].append('Accuracy validated'); return state; def check_export_control(state: MachineSpecState): state['logs'].append('Checking dual-use compliance'); return state; graph = StateGraph(MachineSpecState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()