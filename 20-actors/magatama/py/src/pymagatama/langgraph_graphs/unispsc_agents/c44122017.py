from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FolderState(TypedDict): items: List[dict]; valid: bool; def validation_step(state: FolderState): state['valid'] = all('load_capacity' in i for i in state['items']); return state; graph = StateGraph(FolderState); graph.add_node('validate', validation_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()