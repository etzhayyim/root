from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DocetaxelState(TypedDict): vendor_certs: List[str]; supply_chain_temp: float; validated: bool; def check_gmp(state: DocetaxelState): state['validated'] = 'GMP' in state['vendor_certs']; return state; def check_temp(state: DocetaxelState): if state['supply_chain_temp'] > 8.0: state['validated'] = False; return state; graph = StateGraph(DocetaxelState); graph.add_node('verify_gmp', check_gmp); graph.add_node('verify_temp', check_temp); graph.set_entry_point('verify_gmp'); graph.add_edge('verify_gmp', 'verify_temp'); graph.add_edge('verify_temp', END); compile_graph = graph.compile()