from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CentrifugeState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: CentrifugeState): if state['specs'].get('temp_range') is None: state['validation_errors'].append('Missing temp_range'); return state; def check_compliance(state: CentrifugeState): state['status'] = 'COMPLIANT' if not state['validation_errors'] else 'REJECTED'; return state; graph = StateGraph(CentrifugeState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()