from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FlashPointState(TypedDict): spec_data: dict; validation_errors: List[str]; approved: bool; def validate_instrument(state: FlashPointState): state['validation_errors'] = []; if 'astm_compliance' not in state['spec_data']: state['validation_errors'].append('Missing ASTM compliance info'); state['approved'] = len(state['validation_errors']) == 0; return state; graph = StateGraph(FlashPointState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()