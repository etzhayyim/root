from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: FilterState): spec = state['specs']; is_valid = 'pore_size' in spec and spec['pore_size'] > 0; return {'valid': is_valid, 'log': ['Validation complete'] if is_valid else ['Invalid specs']}; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()