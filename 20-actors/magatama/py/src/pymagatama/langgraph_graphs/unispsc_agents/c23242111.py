from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LatheState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: LatheState): state['valid'] = 'indexing_accuracy' in state['specs'] and state['specs']['indexing_accuracy'] > 0; return state; graph = StateGraph(LatheState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()