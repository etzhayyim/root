from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TransformerState(TypedDict): spec: dict; validation_errors: List[str]; approved: bool; def validate_transformer(state: TransformerState): spec = state['spec']; errors = []; if spec.get('power_rating', 0) <= 0: errors.append('Rating must be positive'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(TransformerState); graph.add_node('validate', validate_transformer); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()