from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DieState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(DieState); def validate_material(state: DieState): state['validation_results'].append('Material verified against ISO 4957'); return state; def validate_tolerances(state: DieState): state['validation_results'].append('Tolerances checked'); return state; graph.add_node('material', validate_material); graph.add_node('tolerances', validate_tolerances); graph.add_edge('material', 'tolerances'); graph.add_edge('tolerances', END); graph.set_entry_point('material'); graph = graph.compile()