from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AccumulatorState(TypedDict): vendor_certs: List[str]; qc_status: str; approved: bool; def validate_certs(state: AccumulatorState): return {'approved': 'AS9100' in state['vendor_certs']}; def inspect_pressure(state: AccumulatorState): return {'qc_status': 'passed' if state['approved'] else 'failed'}; graph = StateGraph(AccumulatorState); graph.add_node('cert_check', validate_certs); graph.add_node('pressure_test', inspect_pressure); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'pressure_test'); graph.add_edge('pressure_test', END); app = graph.compile()