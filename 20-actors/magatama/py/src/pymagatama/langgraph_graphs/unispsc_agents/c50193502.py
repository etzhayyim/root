from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PearProcurementState(TypedDict): data: dict; status: str; def validate_compliance(state: PearProcurementState): state['status'] = 'COMPLIANT' if state['data'].get('cert') else 'REJECTED'; return state; def check_perishables(state: PearProcurementState): state['status'] = 'EXPIRED' if state['data'].get('days_to_expiry', 0) < 30 else state['status']; return state; graph = StateGraph(PearProcurementState); graph.add_node('validation', validate_compliance); graph.add_node('expiry_check', check_perishables); graph.set_entry_point('validation'); graph.add_edge('validation', 'expiry_check'); graph.add_edge('expiry_check', END); graph = graph.compile()