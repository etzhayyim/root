from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): status: str; specs: dict; audit_trail: List[str]; def validate_specs(state: MachineState): return {"audit_trail": state["audit_trail"] + ["Validating mechanical specs"]}; def run_safety_check(state: MachineState): return {"status": "Ready for Operation"}; graph = StateGraph(MachineState); graph.add_node("validate", validate_specs); graph.add_node("safety", run_safety_check); graph.set_entry_point("validate"); graph.add_edge("validate", "safety"); graph.add_edge("safety", END); app = graph.compile()