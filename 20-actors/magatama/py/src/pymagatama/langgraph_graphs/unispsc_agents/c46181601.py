from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetyState(TypedDict): specs: dict; compliant: bool; errors: List[str]; def validate_specs(state: SafetyState): state['compliant'] = all(k in state['specs'] for k in ['fire_rating', 'material']); return state; def check_compliance(state: SafetyState): return 'valid' if state['compliant'] else 'invalid'; graph = StateGraph(SafetyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END);