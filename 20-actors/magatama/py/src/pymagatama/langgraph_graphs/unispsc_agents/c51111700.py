from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugProcurementState(TypedDict): chemical_data: dict; verification_results: List[str]; approved: bool; def validate_quality(state: DrugProcurementState): return {'verification_results': ['Quality checked']}; def safety_audit(state: DrugProcurementState): return {'approved': True}; graph = StateGraph(DrugProcurementState); graph.add_node('quality_check', validate_quality); graph.add_node('safety_audit', safety_audit); graph.set_entry_point('quality_check'); graph.add_edge('quality_check', 'safety_audit'); graph.add_edge('safety_audit', END); graph = graph.compile()