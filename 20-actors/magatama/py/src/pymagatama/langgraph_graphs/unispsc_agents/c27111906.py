from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): tool_id: str; specs: dict; valid: bool; def validate_specs(state: ToolState): state['valid'] = all(k in state['specs'] for k in ['material', 'width']); return state; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()