from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): component_id: str; specs: dict; validation_passed: bool; def validate_specs(state: State): state['validation_passed'] = all(k in state['specs'] for k in ['material', 'dimensions']); return state; def check_safety_critical(state: State): return 'pass' if state['validation_passed'] else 'fail'; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()