from langgraph.graph import StateGraph, END; from typing import TypedDict; class KitState(TypedDict): items: list; valid: bool; def validate_tools(state: KitState): state['valid'] = len(state['items']) > 0; return state; graph = StateGraph(KitState); graph.add_node('validate', validate_tools); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()