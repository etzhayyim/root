from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InkState(TypedDict): ink_type: str; specs: dict; valid: bool; def validate_spec(state: InkState): state['valid'] = 'viscosity_cps' in state['specs'] and 'ph_level' in state['specs']; return state; builder = StateGraph(InkState); builder.add_node('validate', validate_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()