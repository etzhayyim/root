from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): data: dict; report: str; def validate_instrument(state: State): return {'report': 'Validation complete: Range spec confirmed.'}; def finalize_procurement(state: State): return {'report': 'Procurement documentation generated.'}; graph = StateGraph(State); graph.add_node('validate', validate_instrument); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()