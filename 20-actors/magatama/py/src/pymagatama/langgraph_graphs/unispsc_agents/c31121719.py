from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(ProcurementState); def validate_machining_specs(state: ProcurementState): return {'validation_results': ['Material check passed', 'Tolerance check passed']}; def finalize_procurement(state: ProcurementState): return {'validation_results': ['Final procurement approved']}; graph.add_node('validate', validate_machining_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()