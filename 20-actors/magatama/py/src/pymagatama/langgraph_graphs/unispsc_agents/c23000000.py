from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): machinery_specs: dict; validation_results: List[str]; def validate_specs(state: State): results = []; if 'voltage' not in state['machinery_specs']: results.append('Missing voltage'); return {'validation_results': results}; def approve_procurement(state: State): return {'validation_results': state['validation_results'] + ['Approved']}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()