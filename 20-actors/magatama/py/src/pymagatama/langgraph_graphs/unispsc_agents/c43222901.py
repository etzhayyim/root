from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PowerConditionerState(TypedDict): specs: dict; compliance_check: bool; graph = StateGraph(PowerConditionerState); def validate_specs(state: PowerConditionerState): return {'compliance_check': state['specs'].get('voltage_rating') is not None}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()