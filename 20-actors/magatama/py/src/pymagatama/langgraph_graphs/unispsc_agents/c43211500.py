from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ComputerSpecState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ComputerSpecState): errors = []; if not state['specs'].get('processor'): errors.append('Missing Processor'); return {'validation_errors': errors}; def check_compliance(state: ComputerSpecState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(ComputerSpecState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()