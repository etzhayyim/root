from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; compliance_flag: bool; def validate_specs(state: State): results = []; is_compliant = True; if 'Sterilization Method' not in state['input_spec']: results.append('Missing Sterilization'); is_compliant = False; return {'validation_results': results, 'compliance_flag': is_compliant}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()