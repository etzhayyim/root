from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StockState(TypedDict): alloy_type: str; specs: dict; valid: bool; def validate_specs(state: StockState): state['valid'] = 'tensile_strength' in state['specs'] and 'material_grade' in state['specs']; return state; builder = StateGraph(StockState); builder.add_node('validator', validate_specs); builder.set_entry_point('validator'); builder.add_edge('validator', END); graph = builder.compile()