from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BarbecueState(TypedDict): specs: dict; valid: bool; def validate_specs(state: BarbecueState): state['valid'] = all(k in state['specs'] for k in ['Fuel_Type', 'Safety_Certification']); return state; graph = StateGraph(BarbecueState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()