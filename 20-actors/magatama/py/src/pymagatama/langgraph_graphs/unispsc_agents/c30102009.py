from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CopperFoilState(TypedDict): specs: dict; validation_log: List[str]; approved: bool; def validate_purity(state: CopperFoilState): state['validation_log'].append('Checking purity...'); return {'approved': state['specs'].get('purity', 0) >= 99.9}; def check_thickness(state: CopperFoilState): state['validation_log'].append('Verifying thickness...'); return {'approved': state['specs'].get('thickness', 0) > 0}; graph = StateGraph(CopperFoilState); graph.add_node('purity', validate_purity); graph.add_node('thickness', check_thickness); graph.set_entry_point('purity'); graph.add_edge('purity', 'thickness'); graph.add_edge('thickness', END); graph = graph.compile()