from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CompressionState(TypedDict): input_files: List[str]; compression_ratio: float; status: str; def validate_data(state: CompressionState): state['status'] = 'validation_complete'; return state; def run_compression(state: CompressionState): state['compression_ratio'] = 0.5; state['status'] = 'processed'; return state; graph = StateGraph(CompressionState); graph.add_node('validate', validate_data); graph.add_node('compress', run_compression); graph.set_entry_point('validate'); graph.add_edge('validate', 'compress'); graph.add_edge('compress', END); graph = graph.compile()