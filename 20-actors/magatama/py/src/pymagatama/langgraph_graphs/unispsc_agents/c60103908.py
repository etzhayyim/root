from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlantState(TypedDict): species: str; health_status: str; transit_specs: dict; validated: bool; def validate_plants(state: PlantState): state['validated'] = 'phytosanitary' in state['transit_specs'] and state['health_status'] == 'healthy'; return state; graph = StateGraph(PlantState); graph.add_node('validator', validate_plants); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()