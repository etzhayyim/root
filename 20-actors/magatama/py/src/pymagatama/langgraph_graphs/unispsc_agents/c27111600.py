from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolingState(TypedDict): specs: List[str]; validation_results: List[str]; status: str; def validate_specs(state: ToolingState): return {'validation_results': ['Specs verified against ISO standards']}; def check_export(state: ToolingState): return {'status': 'EXPORT_CONTROL_CHECK_PASSED'}; graph = StateGraph(ToolingState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_export); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()