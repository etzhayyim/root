from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CMSState(TypedDict): content_requirements: dict; compliance_check: bool; workflow: list; def validate_cms(state: CMSState): state['compliance_check'] = True; return state; def init_workflow(state: CMSState): state['workflow'] = ['API_Validation', 'Deployment_Sandbox', 'Security_Scan']; return state; graph = StateGraph(CMSState); graph.add_node('validate', validate_cms); graph.add_node('init', init_workflow); graph.set_entry_point('init'); graph.add_edge('init', 'validate'); graph.add_edge('validate', END); graph = graph.compile()