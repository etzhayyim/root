from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_id: str; purity_check: bool; gmp_compliant: bool; status: str; def validate_purity(state: PharmaState) -> PharmaState: state['purity_check'] = True; return state; def check_gmp(state: PharmaState) -> PharmaState: state['gmp_compliant'] = True; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('certify', check_gmp); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()