from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WatchCaseState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(WatchCaseState); def validate_material(state): state['validation_results'].append('Material verified'); return state; def check_dimensions(state): state['validation_results'].append('Dimensions within tolerance'); return state; graph.add_node('material_check', validate_material); graph.add_node('dimension_check', check_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dimension_check'); graph.add_edge('dimension_check', END); graph = graph.compile()