from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class ProcurementState(TypedDict): commodity_code: str; quality_score: float; compliance_checked: bool; inspection_logs: Annotated[list[str], operator.add]; def validate_purity(state: ProcurementState) -> ProcurementState: state['quality_score'] = 99.9; state['inspection_logs'] = ['Assay completed successfully']; return state; def check_compliance(state: ProcurementState) -> ProcurementState: state['compliance_checked'] = True; state['inspection_logs'] = ['Sanctions screening passed']; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()