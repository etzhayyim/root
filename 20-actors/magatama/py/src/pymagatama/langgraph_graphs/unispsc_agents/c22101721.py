from langgraph.graph import StateGraph, END; from typing import TypedDict; class LaserState(TypedDict): spec: dict; verified: bool; def validate_laser(state: LaserState): return {'verified': state['spec'].get('power_rating', 0) > 0}; graph = StateGraph(LaserState); graph.add_node('validate', validate_laser); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()