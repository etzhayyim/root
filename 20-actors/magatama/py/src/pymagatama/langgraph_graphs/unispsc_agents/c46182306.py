from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SafetyHarnessState(TypedDict): specs: dict; compliance_check: bool; final_report: str; def validate_specs(state: SafetyHarnessState): state['compliance_check'] = state['specs'].get('ISO_Standard_Compliance') == True; return state; def generate_report(state: SafetyHarnessState): state['final_report'] = 'Compliance verified' if state['compliance_check'] else 'Compliance failed'; return state; graph = StateGraph(SafetyHarnessState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()