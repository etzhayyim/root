from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): input_data: dict; validation_logs: Annotated[Sequence[str], operator.add]; compliance_status: bool; def validate_api_specs(state: PharmState): logs = []; status = True; if state['input_data'].get('purity', 0) < 99.0: logs.append('Purity check failed'); status = False; return {'validation_logs': logs, 'compliance_status': status}; def regulatory_check(state: PharmState): return {'validation_logs': ['Regulatory clearance verified']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_api_specs); graph.add_node('regulatory', regulatory_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()