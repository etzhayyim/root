from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CanvasState(TypedDict): content: str; validated: bool; inspection_score: int; def validate_canvas(state: CanvasState): score = 100 if 'cotton' in state['content'].lower() or 'linen' in state['content'].lower() else 50; return {'validated': score > 80, 'inspection_score': score}; graph = StateGraph(CanvasState); graph.add_node('validate', validate_canvas); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()