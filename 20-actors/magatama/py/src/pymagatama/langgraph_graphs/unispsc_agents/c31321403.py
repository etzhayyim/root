from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): input_specs: dict; validated: bool; compliance_report: str; def validate_specs(state: AssemblyState): state['validated'] = 'material_grade' in state['input_specs']; return state; def check_compliance(state: AssemblyState): state['compliance_report'] = 'Certified' if state['validated'] else 'Rejected'; return state; graph = StateGraph(AssemblyState); graph.add_node('validation', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validation', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validation'); graph = graph.compile()