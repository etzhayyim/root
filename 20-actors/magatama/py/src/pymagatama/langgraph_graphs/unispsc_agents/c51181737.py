from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; gmp_certified: bool; temp_logs: List[float]; valid: bool; def validate_gmp(state: PharmState): state['valid'] = state['gmp_certified'] and min(state['temp_logs']) >= 15; return state; graph = StateGraph(PharmState); graph.add_node('validation', validate_gmp); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()