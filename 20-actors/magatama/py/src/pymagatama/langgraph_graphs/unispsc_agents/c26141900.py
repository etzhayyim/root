from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NucleonicState(TypedDict): device_id: str; safety_clearance: bool; radiation_data: dict; status: str; def validate_safety(state: NucleonicState): return {'safety_clearance': True} if state.get('radiation_data') else {'safety_clearance': False}; def compliance_check(state: NucleonicState): return {'status': 'COMPLIANT' if state['safety_clearance'] else 'FAILED'}; graph = StateGraph(NucleonicState); graph.add_node('validate', validate_safety); graph.add_node('compliance', compliance_check); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()