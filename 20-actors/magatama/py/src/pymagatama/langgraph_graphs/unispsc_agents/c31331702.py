from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; valid: bool; logs: List[str]; def validate_structural_integrity(state: AssemblyState): state['valid'] = 'tensile_strength_mpa' in state['specs'] and state['specs']['tensile_strength_mpa'] > 400; state['logs'].append('Integrity check complete'); return state; def route_verification(state: AssemblyState): return 'complete' if state['valid'] else END; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_structural_integrity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()