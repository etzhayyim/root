from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FacilityState(TypedDict): specs: dict; validation_report: List[str]; def validate_medical_standards(state: FacilityState): report = []; if 'HVAC_spec' not in state['specs']: report.append('Missing HVAC Sterilization Spec'); return {'validation_report': report}; def check_compliance(state: FacilityState): return {'validation_report': state['validation_report'] + ['Compliance Verified']}; graph = StateGraph(FacilityState); graph.add_node('validate', validate_medical_standards); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()