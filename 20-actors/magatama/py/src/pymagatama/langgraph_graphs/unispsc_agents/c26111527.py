from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MotionControlState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: MotionControlState) -> MotionControlState: schema = ['Axis Count', 'Safety Compliance']; results = [f'Missing {s}' for s in schema if s not in state['specs']]; return {**state, 'validation_results': results}; graph = StateGraph(MotionControlState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()