from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): status: str; spec_data: dict; validation_logs: List[str]; def validate_weld_integrity(state: AssemblyState): print('Checking ultrasonic weld pattern consistency...'); state['validation_logs'].append('Weld integrity pass'); return {'status': 'validated'}; def finalize_assembly(state: AssemblyState): state['status'] = 'ready_for_procurement'; return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_weld_integrity); graph.add_node('finalize', finalize_assembly); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()