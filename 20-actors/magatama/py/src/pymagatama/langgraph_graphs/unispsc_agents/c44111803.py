from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CompassState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_specs(state: CompassState): errors = []; for item in state['items']: if not item.get('material'): errors.append('Missing material spec'); return {'validation_errors': errors}; def finalize_procurement(state: CompassState): return {'items': state['items'], 'status': 'READY'}; graph = StateGraph(CompassState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()