from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): animal_type: str; restraint_type: str; compliance_check: bool; def validate_spec(state: ProcState): state['compliance_check'] = True if state['animal_type'] and state['restraint_type'] else False; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()