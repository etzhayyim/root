from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): data: dict; specs: List[str]; validated: bool; def validate_specs(state: State): state['validated'] = all(k in state['data'] for k in state['specs']); return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()