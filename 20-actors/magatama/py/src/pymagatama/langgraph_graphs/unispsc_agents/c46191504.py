from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): sensor_data: dict; validation_report: str; def validate_calib(state: State): state['validation_report'] = 'Valid' if state['sensor_data'].get('calibrated') else 'Invalid'; return state; graph = StateGraph(State); graph.add_node('validate', validate_calib); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()