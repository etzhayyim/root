from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CallSequencerState(TypedDict): sequencer_specs: dict; validation_errors: List[str]; def validate_sequencer_config(state: CallSequencerState): specs = state.get('sequencer_specs', {}); errors = []; if not specs.get('voip_protocol'): errors.append('Missing VOIP Protocol'); return {'validation_errors': errors}; def finalize_config(state: CallSequencerState): print('Configuration finalized'); return {'validation_errors': []}; graph = StateGraph(CallSequencerState); graph.add_node('validate', validate_sequencer_config); graph.add_node('finalize', finalize_config); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()