from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CabinetSpec(TypedDict): dimensions: dict; cooling_req: str; validated: bool; def validate_spec(state: CabinetSpec): state['validated'] = 'cooling_req' in state and 'dimensions' in state; return state; graph = StateGraph(CabinetSpec); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()