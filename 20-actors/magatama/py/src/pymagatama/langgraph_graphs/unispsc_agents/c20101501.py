from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class HousingState(TypedDict): dimensions: dict; material_specs: dict; validation_logs: Annotated[Sequence[str], operator.add]; def validate_dimensions(state: HousingState): return {"validation_logs": ["Validating dimensional tolerances against CAD specs"]}; def material_integrity_check(state: HousingState): return {"validation_logs": ["Verifying material composition and tensile strength"]}; graph = StateGraph(HousingState); graph.add_node("validate", validate_dimensions); graph.add_node("material", material_integrity_check); graph.add_edge("validate", "material"); graph.add_edge("material", END); graph.set_entry_point("validate"); graph = graph.compile()