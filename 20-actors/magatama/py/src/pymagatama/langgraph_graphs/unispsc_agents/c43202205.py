from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KeyboardState(TypedDict): specs: dict; valid: bool; def validate_keycap(state: KeyboardState): compatibility = state['specs'].get('switch_type'); state['valid'] = compatibility in ['CherryMX', 'Topre', 'Kailh']; return state; builder = StateGraph(KeyboardState); builder.add_node('validate', validate_keycap); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()