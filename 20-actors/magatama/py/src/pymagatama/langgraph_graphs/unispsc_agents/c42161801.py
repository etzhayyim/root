from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): device_id: str; registry_data: dict; compliance_status: bool; def validate_device(state: State): state['compliance_status'] = 'cert' in state['registry_data']; return state; def check_risk(state: State): return 'high_risk' if state['compliance_status'] is False else 'ready'; graph = StateGraph(State); graph.add_node('validate', validate_device); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()