from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KjeldahlState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: KjeldahlState): state['compliance'] = 'heating_temperature_range_celsius' in state['specs']; return state; def check_safety(state: KjeldahlState): state['status'] = 'Safety Verified' if state['compliance'] else 'Failed'; return state; graph = StateGraph(KjeldahlState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()