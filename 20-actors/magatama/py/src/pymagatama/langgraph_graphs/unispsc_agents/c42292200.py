from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): device_specs: dict; compliance_verified: bool; def validate_specs(state: SurgicalState): return {'compliance_verified': 'fda_id' in state['device_specs']}; def check_sterility(state: SurgicalState): return {'compliance_verified': state['device_specs'].get('sterile', False)}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_specs); graph.add_node('sterility', check_sterility); graph.set_entry_point('validate'); graph.add_edge('validate', 'sterility'); graph.add_edge('sterility', END); graph = graph.compile()