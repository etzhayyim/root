from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): reagent_id: str; quality_passed: bool; storage_conditions: str; def validate_reagent(state: KitState): return {"quality_passed": state.get("storage_conditions") == "2-8C"}; def finalize_log(state: KitState): print(f"Processing complete for {state['reagent_id']}"); return state; graph = StateGraph(KitState); graph.add_node("validate", validate_reagent); graph.add_node("finalize", finalize_log); graph.set_entry_point("validate"); graph.add_edge("validate", "finalize"); graph.add_edge("finalize", END); graph = graph.compile()