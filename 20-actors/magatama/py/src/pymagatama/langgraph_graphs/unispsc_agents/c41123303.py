from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CabinetState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: CabinetState): errors = []; if not state['specs'].get('capacity'): errors.append('Capacity missing'); return {'validation_errors': errors}; def check_regulatory(state: CabinetState): return {'status': 'approved' if not state['validation_errors'] else 'rejected'}; graph = StateGraph(CabinetState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()