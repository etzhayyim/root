from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LocomotiveState(TypedDict): specs: dict; compliance: bool; validation_log: list; graph = StateGraph(LocomotiveState); def validate_specs(state: LocomotiveState): state['validation_log'].append('Validating Tractive Effort and UIC compliance'); state['compliance'] = True if 'Tractive Effort Capacity' in state['specs'] else False; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()