from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CastingState(TypedDict): specs: dict; quality_report: str; approved: bool; def validate_metallurgy(state: CastingState): state['approved'] = 'Cu' in state['specs'].get('chemical_composition', '') return state; def check_precision(state: CastingState): return state; graph = StateGraph(CastingState); graph.add_node('validate', validate_metallurgy); graph.add_node('precision', check_precision); graph.set_entry_point('validate'); graph.add_edge('validate', 'precision'); graph.add_edge('precision', END); graph.compile()