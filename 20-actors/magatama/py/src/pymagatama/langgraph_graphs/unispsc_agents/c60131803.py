from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ProcureState): state['valid'] = 'weight_specification' in state['specs'] and 'material_composition' in state['specs']; return state; builder = StateGraph(ProcureState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()