from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DosimetryState(TypedDict): phantom_id: str; compliance_passed: bool; specs: List[str]; def validate_specs(state: DosimetryState): return {'compliance_passed': 'Radiation calibration certificate' in state['specs']}; def check_safety_protocols(state: DosimetryState): return {'compliance_passed': state['compliance_passed'] and True}; graph = StateGraph(DosimetryState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety_protocols); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()