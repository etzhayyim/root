from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): housing_specs: dict; inspection_results: List[str]; def validate_specs(state: State): return {'inspection_results': ['compliant'] if state['housing_specs'].get('ip_rating', 0) >= 65 else ['non-compliant']}; def check_material(state: State): return {'inspection_results': state['inspection_results'] + ['material_verified']}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.add_node('material', check_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'material'); graph.add_edge('material', END); graph = graph.compile()