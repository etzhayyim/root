from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: FilterState): state['compliance'] = state['specs'].get('hepa_grade') == 'H14'; return state; def check_airflow(state: FilterState): state['status'] = 'Validated' if state['specs'].get('airflow', 0) > 500 else 'Insufficient'; return state; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.add_node('airflow_check', check_airflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'airflow_check'); graph.add_edge('airflow_check', END); graph = graph.compile()