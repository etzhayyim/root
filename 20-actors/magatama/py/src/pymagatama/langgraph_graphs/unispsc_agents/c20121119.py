from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MotorState(TypedDict): specs: dict; validation_logs: Annotated[list, operator.add]; compliance_status: bool; def validate_motor_spec(state: MotorState): specs = state['specs']; logs = []; status = True; if specs.get('torque_nm', 0) <= 0: logs.append('Invalid torque'); status = False; return {'validation_logs': logs, 'compliance_status': status}; def compile_graph(): graph = StateGraph(MotorState); graph.add_node('validate', validate_motor_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()