from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TransformerSpecs(TypedDict): input_voltage: float; output_voltage: float; power_rating: float; compliance: List[str]; def validate_specs(state: TransformerSpecs): if state['input_voltage'] <= 0 or state['output_voltage'] <= 0: return 'invalid'; return 'valid'; graph = StateGraph(TransformerSpecs); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()