from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): dimensions: dict; quality_certs: list; status: str; def validate_specs(state: ForgingState): return {'status': 'validated' if 'iso9001' in state['quality_certs'] else 'rejected'}; def check_material(state: ForgingState): return {'status': 'approved' if 'titanium_grade5' in state['dimensions'] else 'failed'}; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.add_node('check_mat', check_material); graph.add_edge('validate', 'check_mat'); graph.add_edge('check_mat', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()