from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class LubricantState(TypedDict): input_data: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_sds(state: LubricantState): return {'validation_results': ['SDS verified'], 'status': 'validated'} if 'sds_id' in state['input_data'] else {'status': 'rejected'}; def process_logistics(state: LubricantState): return {'validation_results': ['Logistics scheduled']}; graph = StateGraph(LubricantState); graph.add_node('validate', validate_sds); graph.add_node('logistics', process_logistics); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph.set_entry_point('validate'); graph = graph.compile()