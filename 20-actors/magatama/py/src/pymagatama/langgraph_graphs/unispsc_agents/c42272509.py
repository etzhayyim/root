from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_result: bool; graph = StateGraph(State); def validate_calibration_standards(state: State): state['validation_result'] = 'standard_iso' in state['input_data']; return state; def check_compliance(state: State): return 'compliance_check'; graph.add_node('validate', validate_calibration_standards); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()