from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; analysis_result: str; def validate_purity(state: State): return {'analysis_result': 'Purity Verified' if state['input_data'].get('purity', 0) > 99.0 else 'Purity Rejected'}; def check_compliance(state: State): return {'analysis_result': 'Compliance Active' if 'gmp_cert' in state['input_data'] else 'Compliance Missing'}; graph = StateGraph(State); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()