from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SiCState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_purity(state: SiCState): return {"validation_results": ["Purity check passed" if state["input_spec"].get("purity", 0) > 99.5 else "Purity failed"]}; def process_grading(state: SiCState): return {"status": "Grading complete"}; graph = StateGraph(SiCState); graph.add_node("validate", validate_purity); graph.add_node("grade", process_grading); graph.add_edge("validate", "grade"); graph.add_edge("grade", END); graph.set_entry_point("validate"); graph = graph.compile()