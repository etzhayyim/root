from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): commodity_code: str; spec_validated: bool; export_check_passed: bool; def validate_export(state: ProcState): return {'export_check_passed': True}; def validate_specs(state: ProcState): return {'spec_validated': True}; graph = StateGraph(ProcState); graph.add_node('export_check', validate_export); graph.add_node('spec_check', validate_specs); graph.add_edge('export_check', 'spec_check'); graph.add_edge('spec_check', END); graph.set_entry_point('export_check'); graph = graph.compile()