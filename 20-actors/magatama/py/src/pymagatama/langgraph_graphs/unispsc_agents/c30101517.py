from langgraph.graph import StateGraph, END; from typing import TypedDict; class MetalState(TypedDict): metal_type: str; purity: float; weight: float; verified: bool; def validate_purity(state: MetalState): state['verified'] = state['purity'] >= 0.999; return state; def check_high_value(state: MetalState): return 'review' if state['weight'] > 500 else 'proceed'; graph = StateGraph(MetalState); graph.add_node('validate', validate_purity); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()