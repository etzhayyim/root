from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ClaspState(TypedDict): specs: dict; validation_results: list; graph = StateGraph(ClaspState); def validate_material(state: ClaspState): return {'validation_results': ['Material check passed']}; def check_tensile_strength(state: ClaspState): return {'validation_results': ['Tensile test passed']}; graph.add_node('material_check', validate_material); graph.add_node('tensile_check', check_tensile_strength); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'tensile_check'); graph.add_edge('tensile_check', END); compiled_graph = graph.compile()