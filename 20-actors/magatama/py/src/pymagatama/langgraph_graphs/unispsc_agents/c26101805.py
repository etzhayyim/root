from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_components: List[str]; compatibility_score: float; validated: bool; def check_compatibility(state: KitState): state['validated'] = len(state['kit_components']) > 0; return state; def validate_parts(state: KitState): state['compatibility_score'] = 1.0 if state['validated'] else 0.0; return state; graph = StateGraph(KitState); graph.add_node('check_comp', check_compatibility); graph.add_node('validate', validate_parts); graph.set_entry_point('check_comp'); graph.add_edge('check_comp', 'validate'); graph.add_edge('validate', END); App = graph.compile()