from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PCRState(TypedDict): status: str; spec_validation: bool; items: List[str]; def validate_purity(state: PCRState): return {'spec_validation': True}; def update_inventory(state: PCRState): return {'status': 'optimized'}; graph = StateGraph(PCRState); graph.add_node('validate', validate_purity); graph.add_node('update', update_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'update'); graph.add_edge('update', END); graph.compile()