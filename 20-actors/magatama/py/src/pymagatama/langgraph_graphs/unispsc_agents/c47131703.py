from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): input_data: dict; validation_errors: List[str]; approved: bool; def validate_spec(state: State): errors = []; spec = state['input_data']; if 'capacity_liters' not in spec: errors.append('Missing capacity'); if spec.get('material_composition') not in ['ABS', 'Stainless Steel']: errors.append('Unsupported material'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()