from langgraph.graph import StateGraph, END; from typing import TypedDict; class SignalState(TypedDict): status: str; safety_cert: bool; def check_safety(state: SignalState): return {'status': 'verified' if state['safety_cert'] else 'failed'}; def build_graph(): graph = StateGraph(SignalState); graph.add_node('safety_check', check_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); return graph.compile(); graph = build_graph()