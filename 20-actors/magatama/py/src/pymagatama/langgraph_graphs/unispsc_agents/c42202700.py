from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadiotherapyState(TypedDict): vendor_id: str; compliance_docs: List[str]; validation_result: bool; def validate_compliance(state: RadiotherapyState): state['validation_result'] = len(state['compliance_docs']) >= 3; return state; def check_export_control(state: RadiotherapyState): return state; workflow = StateGraph(RadiotherapyState); workflow.add_node('validate', validate_compliance); workflow.add_node('export_check', check_export_control); workflow.set_entry_point('validate'); workflow.add_edge('validate', 'export_check'); workflow.add_edge('export_check', END); graph = workflow.compile()