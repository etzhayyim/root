from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class HeaterState(TypedDict): wattage: int; safety_certified: bool; approved: bool; def validate_specs(state: HeaterState): state['approved'] = state['wattage'] > 0 and state['safety_certified']; return state; builder = StateGraph(HeaterState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()