from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class EyeAccessoryState(TypedDict): vendor_certs: dict; device_specs: dict; approved: bool; def validate_medical_device(state: EyeAccessoryState): iso_cert = state['vendor_certs'].get('ISO-13485'); return {'approved': iso_cert is True}; graph = StateGraph(EyeAccessoryState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()