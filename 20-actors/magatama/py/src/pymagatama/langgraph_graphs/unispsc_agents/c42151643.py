from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DentalSpecState(TypedDict): specs: dict; validation_errors: List[str]; def validate_tool(state: DentalSpecState): errors = []; if 'Material Grade' not in state['specs']: errors.append('Missing Material Grade'); return {'validation_errors': errors}; graph = StateGraph(DentalSpecState); graph.add_node('validate', validate_tool); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()