from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GasState(TypedDict): chemical_purity: float; safety_compliance: bool; verified_chain: bool; processing_log: Annotated[list, operator.add]; def validate_purity(state: GasState): return {verified_chain: state['chemical_purity'] >= 99.999}; def safety_audit(state: GasState): return {safety_compliance: True}; graph = StateGraph(GasState); graph.add_node('validate', validate_purity); graph.add_node('safety', safety_audit); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()