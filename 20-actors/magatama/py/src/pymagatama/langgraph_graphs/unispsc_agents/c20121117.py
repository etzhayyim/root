from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class MotorState(TypedDict): specs: Dict; validation_results: List[str]; def validate_torque(state: MotorState): state['validation_results'].append('Torque verified') if state['specs'].get('torque_nm', 0) > 0 else state['validation_results'].append('Torque invalid'); return state; def check_compliance(state: MotorState): state['validation_results'].append('Export compliance checked'); return state; graph = StateGraph(MotorState); graph.add_node('validate', validate_torque); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()