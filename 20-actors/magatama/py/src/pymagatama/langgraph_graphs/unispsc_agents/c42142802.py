from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): data: dict; logs: Annotated[list, operator.add]; graph = StateGraph(State); def validate_medical_compliance(state: State): state['logs'] = ['Checking regulatory compliance']; return state; def check_compression_specs(state: State): state['logs'] = ['Verifying mmHg ratings']; return state; graph.add_node('validate', validate_medical_compliance); graph.add_node('specs', check_compression_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'specs'); graph.add_edge('specs', END); compiled_graph = graph.compile()