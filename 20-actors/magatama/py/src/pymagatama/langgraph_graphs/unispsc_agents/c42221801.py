from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_id: str; quality_docs: List[str]; approved: bool; def validate_docs(state: ProcurementState): state['approved'] = 'iso_13485' in state['quality_docs']; return state; def check_quality(state: ProcurementState): return 'approved' if state['approved'] else 'rejected'; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_docs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()