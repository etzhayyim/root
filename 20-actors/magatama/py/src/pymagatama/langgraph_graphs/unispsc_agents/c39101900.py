from langgraph.graph import StateGraph, END; from typing import TypedDict; class BallastState(TypedDict): specs: dict; valid: bool; def validate_specs(state: BallastState): state['valid'] = all(k in state['specs'] for k in ['wattage', 'voltage']); return state; graph = StateGraph(BallastState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()