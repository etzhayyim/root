from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MotorBaseState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(MotorBaseState); def validate_dimensions(state: MotorBaseState): return {'validation_results': ['Dimensions compliant']}; def inspect_material(state: MotorBaseState): return {'validation_results': ['Material cert verified']}; graph.add_node('validate', validate_dimensions); graph.add_node('inspect', inspect_material); graph.add_edge('validate', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('validate'); graph = graph.compile()