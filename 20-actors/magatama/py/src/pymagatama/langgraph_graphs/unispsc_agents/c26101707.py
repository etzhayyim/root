from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): component_name: str; specs: dict; ready: bool; def val_specs(state: ProcureState): return {'ready': all(k in state['specs'] for k in ['material', 'tolerance'])}; graph = StateGraph(ProcureState); graph.add_node('validate', val_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()