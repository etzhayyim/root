from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RestraintState(TypedDict): specs: dict; valid: bool; audit_log: List[str]; def validate_specs(state: RestraintState): state['valid'] = 'tensile_strength' in state['specs'] and state['specs']['tensile_strength'] > 500; return state; def check_compliance(state: RestraintState): state['audit_log'].append('ISO-13485 check complete'); return state; graph = StateGraph(RestraintState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()