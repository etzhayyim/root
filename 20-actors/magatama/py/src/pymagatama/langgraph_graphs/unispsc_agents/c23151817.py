from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeldingState(TypedDict): input_data: dict; validation_results: List[str]; approved: bool; def validate_specs(state: WeldingState): specs = state['input_data']; errors = []; if specs.get('power_output_kw', 0) <= 0: errors.append('Invalid power output'); return {'validation_results': errors, 'approved': len(errors) == 0}; graph = StateGraph(WeldingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()