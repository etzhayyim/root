from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class ForgingState(TypedDict): messages: Annotated[list, add_messages]; compliance_status: bool; def validate_lead_content(state: ForgingState): return {'compliance_status': True}; def check_machining_specs(state: ForgingState): return {'compliance_status': True}; graph = StateGraph(ForgingState); graph.add_node('validate', validate_lead_content); graph.add_node('check_specs', check_machining_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_specs'); graph.add_edge('check_specs', END); graph = graph.compile()