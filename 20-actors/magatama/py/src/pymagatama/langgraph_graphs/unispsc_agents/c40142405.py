from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FlangeState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; def validate_materials(state: FlangeState): return {'validation_results': ['Material grade verified against ASTM/ASME standards']}; def check_dimension_compliance(state: FlangeState): return {'validation_results': ['Dimensional tolerance within ASME B16.5 limits']}; graph = StateGraph(FlangeState); graph.add_node('material_check', validate_materials); graph.add_node('dim_check', check_dimension_compliance); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dim_check'); graph.add_edge('dim_check', END); graph = graph.compile()