from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DentalSpecState(TypedDict):;    sku_code: str;    compliance_status: bool;    sterilization_validated: bool; def validate_medical_cert(state: DentalSpecState):;    state['compliance_status'] = True;    return state; def check_sterilization_rating(state: DentalSpecState):;    state['sterilization_validated'] = True;    return state; graph = StateGraph(DentalSpecState); graph.add_node('ValidateCert', validate_medical_cert); graph.add_node('CheckSterile', check_sterilization_rating); graph.set_entry_point('ValidateCert'); graph.add_edge('ValidateCert', 'CheckSterile'); graph.add_edge('CheckSterile', END); graph = graph.compile()