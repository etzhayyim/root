from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): part_specs: dict; validation_passed: bool; def validate_forging(state: ForgingState): state['validation_passed'] = 'Material Grade' in state['part_specs'] and 'Dimensional Tolerance' in state['part_specs']; return state; builder = StateGraph(ForgingState); builder.add_node('validate', validate_forging); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()