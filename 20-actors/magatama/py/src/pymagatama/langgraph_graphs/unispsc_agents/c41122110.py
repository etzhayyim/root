from langgraph.graph import StateGraph, END; from typing import TypedDict; class DispenserState(TypedDict): input: str; validated: bool; status: str; def validate_spec(state: DispenserState): state['validated'] = True; state['status'] = 'COMPLIANT'; return state; graph = StateGraph(DispenserState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()