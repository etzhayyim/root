from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BlottingState(TypedDict): membrane_type: str; pore_size: float; valid: bool; def validate_membrane(state: BlottingState): state['valid'] = state['pore_size'] in [0.2, 0.45]; return {'valid': state['valid']}; graph = StateGraph(BlottingState); graph.add_node('validation', validate_membrane); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()