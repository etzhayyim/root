from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): device_id: str; compliance_check: bool; validation_log: List[str]; def validate_medical_device(state: ProcState): state['validation_log'].append('Validating ISO/FDA status'); state['compliance_check'] = True; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()