from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): apparel_specs: dict; compliance_status: bool; def validate_specs(state: State): specs = state['apparel_specs']; state['compliance_status'] = 'chemical_permeation_rating' in specs and specs['chemical_permeation_rating'] >= 4; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()