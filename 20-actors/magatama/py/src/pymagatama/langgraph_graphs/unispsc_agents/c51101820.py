from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity: float; compliant: bool; def validate_purity(state: PharmaState): state['compliant'] = state['purity'] >= 99.0; return state; def check_regulations(state: PharmaState): return {'compliant': state['compliant'] and True}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('check', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()