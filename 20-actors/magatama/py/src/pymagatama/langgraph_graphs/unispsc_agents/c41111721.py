from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TEMState(TypedDict): specs: dict; compliance_check: bool; export_licensed: bool; def validate_specs(state: TEMState): state['compliance_check'] = state['specs'].get('voltage', 0) > 0; return state; def check_export(state: TEMState): state['export_licensed'] = state['specs'].get('eccn') is not None; return state; graph = StateGraph(TEMState); graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()