from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): device_type: str; is_compliant: bool; def validate_device(state: ProcState): state["is_compliant"] = True if state["device_type"] in ["ergonomic", "orthopedic"] else False; return state; graph = StateGraph(ProcState); graph.add_node("validate", validate_device); graph.set_entry_point("validate"); graph.add_edge("validate", END); compiled_graph = graph.compile()