from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): component_id: str; specs: dict; validation_passed: bool; def check_specs(state: ProcureState): state['validation_passed'] = 'tensile_strength_mpa' in state['specs'] and state['specs']['tensile_strength_mpa'] > 400; return state; graph = StateGraph(ProcureState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()