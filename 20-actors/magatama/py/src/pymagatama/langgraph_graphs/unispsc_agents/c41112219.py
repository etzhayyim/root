from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProbeState(TypedDict): probe_specs: dict; validation_passed: bool; error_log: List[str]; def validate_specs(state: ProbeState): specs = state['probe_specs']; errors = []; if specs.get('temp_range', 0) > 1200: errors.append('Range exceeds standard sensor limits'); return {'validation_passed': len(errors) == 0, 'error_log': errors}; graph = StateGraph(ProbeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()