from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(CableState); def validate_impedance(state: CableState): impedance = state['specs'].get('impedance'); return {'validation_results': ['Pass' if impedance == 50 or impedance == 75 else 'Fail']}; def check_compliance(state: CableState): return {'validation_results': state['validation_results'] + ['Material Compliance Verified']}; graph.add_node('validate', validate_impedance); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()