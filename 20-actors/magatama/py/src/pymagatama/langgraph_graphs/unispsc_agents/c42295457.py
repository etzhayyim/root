from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SupplyState(TypedDict): vendor_id: str; item_specs: dict; compliance_verified: bool; def validate_specs(state: SupplyState): specs = state['item_specs']; state['compliance_verified'] = all(key in specs for key in ['sterility', 'material', 'fluid_resistance']); return state; graph = StateGraph(SupplyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()