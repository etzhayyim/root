from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BushingState(TypedDict): specs: dict; validation_passed: bool; issues: List[str]; def validate_specs(state: BushingState): tolerance = state['specs'].get('tolerance', 0.0); passed = tolerance <= 0.05; return {'validation_passed': passed, 'issues': [] if passed else ['Tolerance exceeds limit']}; def finalize(state: BushingState): return {'validation_passed': state['validation_passed']}; graph = StateGraph(BushingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()