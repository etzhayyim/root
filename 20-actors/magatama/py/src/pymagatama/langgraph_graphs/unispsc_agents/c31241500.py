from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class LensState(TypedDict): specs: dict; validation_results: list; status: str; def validate_optical_specs(state: LensState): issues = []; specs = state.get('specs', {}); if 'surface_quality' not in specs: issues.append('Missing scratch-dig'); state['validation_results'] = issues; state['status'] = 'COMPLETED' if not issues else 'FAILED'; return state; graph = StateGraph(LensState); graph.add_node('validate', validate_optical_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()