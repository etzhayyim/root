from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): code: str; compliance_check: bool; material_spec: dict; workflow_log: List[str]; def validate_chemical(state: ChemicalState): state['compliance_check'] = 'cas_number' in state['material_spec'] and state['material_spec']['purity_percentage'] > 99.0; state['workflow_log'].append('Chemical integrity validated'); return state; def prepare_shipping(state: ChemicalState): state['workflow_log'].append('Dangerous goods protocols initiated'); return state; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_chemical); graph.add_node('shipping', prepare_shipping); graph.add_edge('validate', 'shipping'); graph.add_edge('shipping', END); graph.set_entry_point('validate'); app = graph.compile()