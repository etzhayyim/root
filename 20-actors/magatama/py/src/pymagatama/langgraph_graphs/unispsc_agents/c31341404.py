from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; alloy_specs: dict; weld_tests: List[str]; approved: bool; def validate_specs(state: AssemblyState): return {'approved': 'weld_integrity' in state['weld_tests'] and state['alloy_specs'].get('grade') == 'Inconel-718'}; def export_control_check(state: AssemblyState): return {'approved': False} if state.get('export_restricted') else {'approved': True}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('export_check', export_control_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()