from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph; class FuseState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(FuseState); def validate_electrical_spec(state: FuseState): rating = state['specs'].get('current_rating'); return {'validation_report': [f'Verified current capacity: {rating}A']} if rating else {'validation_report': ['Error: Missing current rating']}; def conduct_compliance_check(state: FuseState): return {'validation_report': state['validation_report'] + ['Passed RoHS/UL inspection']}; graph.add_node('validate', validate_electrical_spec); graph.add_node('compliance', conduct_compliance_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.set_finish_point('compliance'); graph = graph.compile()