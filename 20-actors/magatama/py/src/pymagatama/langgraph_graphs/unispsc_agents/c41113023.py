from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GelProcessState(TypedDict): input_specs: dict; validation_results: list; is_approved: bool; def validate_equipment(state: GelProcessState): specs = state['input_specs']; val = 'validated' if 'Voltage_Range' in specs else 'error'; return {'validation_results': [val]}; def check_compliance(state: GelProcessState): return {'is_approved': 'error' not in state['validation_results']}; graph = StateGraph(GelProcessState); graph.add_node('validate', validate_equipment); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); app = graph.compile()