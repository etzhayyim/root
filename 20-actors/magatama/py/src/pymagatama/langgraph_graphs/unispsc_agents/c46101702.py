from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftAmmoState(TypedDict): ammo_load_reqs: dict; compliance_status: bool; validation_log: List[str]; def validate_system_specs(state: AircraftAmmoState): state['compliance_status'] = 'MIL-STD' in state['ammo_load_reqs'].get('standards', []); state['validation_log'].append('System compliance checked'); return state; def hazard_assessment(state: AircraftAmmoState): state['validation_log'].append('Hazard check completed for ordnance handling'); return state; graph = StateGraph(AircraftAmmoState); graph.add_node('validate', validate_system_specs); graph.add_node('hazard', hazard_assessment); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazard'); graph.add_edge('hazard', END); graph = graph.compile()