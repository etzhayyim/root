from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EKGState(TypedDict): device_id: str; test_results: dict; validated: bool; def validate_electrode_performance(state: EKGState): state['validated'] = state['test_results'].get('impedance', 1000) < 500; return state; def report_results(state: EKGState): print(f'Device {state['device_id']} validation: {state['validated']}'); return state; graph = StateGraph(EKGState); graph.add_node('validate', validate_electrode_performance); graph.add_node('report', report_results); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()