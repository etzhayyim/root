from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicroscopeProcurementState(TypedDict): vendor_docs: List[str]; compliance_checks: List[str]; final_approval: bool; def validate_specs(state: MicroscopeProcurementState): print('Validating laser safety and optical resolution'); return {'compliance_checks': ['LASER_SAFETY_PASS']}; def check_export(state: MicroscopeProcurementState): print('Checking dual-use export control status'); return {'compliance_checks': ['EXPORT_CLEARED']}; graph = StateGraph(MicroscopeProcurementState); graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()