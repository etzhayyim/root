from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ConveyorState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: ConveyorState): spec = state['specs']; is_valid = 'material' in spec and 'tensile' in spec; return {'valid': is_valid, 'error_log': [] if is_valid else ['Missing mandatory specs']}; graph = StateGraph(ConveyorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()