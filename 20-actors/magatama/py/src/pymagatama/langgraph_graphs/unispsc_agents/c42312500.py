from langgraph.graph import StateGraph, END; from typing import TypedDict; class WoundSupplyState(TypedDict): supply_id: str; quality_score: int; is_sterile: bool; graph = StateGraph(WoundSupplyState); def validate_sterility(state: WoundSupplyState): return {'is_sterile': True}; def assess_quality(state: WoundSupplyState): return {'quality_score': 95}; graph.add_node('validate', validate_sterility); graph.add_node('assess', assess_quality); graph.add_edge('validate', 'assess'); graph.add_edge('assess', END); graph.set_entry_point('validate'); app = graph.compile()