from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): sensor_data: dict; validation_results: list; status: str; def validate_sensor_spec(state: RobotState): res = state['sensor_data'].get('range', 0) > 0; return {'validation_results': [f'Range valid: {res}'], 'status': 'validated' if res else 'failed'}; def build_graph(): graph = StateGraph(RobotState); graph.add_node('validate', validate_sensor_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = build_graph()