from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmergencyLightingState(TypedDict): items: List[str]; validation_results: List[str]; def validate_spec(state: EmergencyLightingState): return {'validation_results': ['Spec compliance verified']}; graph = StateGraph(EmergencyLightingState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()