from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input_spec: str; validation_report: list[str]; approved: bool; def validate_injection_gun(state: ProcureState): validated = 'ISO13485' in state['input_spec'] and 'Pressure_Rating' in state['input_spec']; return {'validation_report': ['Spec checked against ISO standards'], 'approved': validated}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_injection_gun); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()