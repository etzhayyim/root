from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class VenturiState(TypedDict): specs: dict; validation_errors: List[str]; status: str; def validate_specs(state: VenturiState): if state['specs'].get('pressure_rating', 0) <= 0: state['validation_errors'].append('Invalid pressure'); return {'status': 'invalid'}; return {'status': 'validated'}; graph = StateGraph(VenturiState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()