from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CastState(TypedDict): input_specs: dict; validation_report: list; status: str; def validate_casting(state: CastState): state['validation_report'].append('Geometry check complete'); return {'status': 'processed'}; def quality_gate(state: CastState): return 'end' if state['status'] == 'processed' else 'error'; graph = StateGraph(CastState); graph.add_node('validate', validate_casting); graph.add_edge('validate', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()