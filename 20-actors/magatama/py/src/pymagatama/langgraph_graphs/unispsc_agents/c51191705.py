from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): chemical_name: str; purity: float; compliance_certs: List[str]; approved: bool; def validate_purity(state: ProcurementState): state['approved'] = state['purity'] >= 99.9; return state; def check_certs(state: ProcurementState): state['approved'] = state['approved'] and len(state['compliance_certs']) >= 2; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_certs); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()