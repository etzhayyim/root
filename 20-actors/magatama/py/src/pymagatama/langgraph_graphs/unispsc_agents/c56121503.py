from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(FurnitureState); def validate_specs(state: FurnitureState): report = []; if not state['specs'].get('fire_retardant'): report.append('Missing fire safety cert'); return {'validation_report': report}; def finalize_order(state: FurnitureState): return {'validation_report': state['validation_report'] + ['Order ready']}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()