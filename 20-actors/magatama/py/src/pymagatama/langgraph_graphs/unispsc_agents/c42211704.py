from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input_data: dict; validation_report: list; status: str; def validate_specs(state: ProcureState): state['validation_report'].append('Checking material safety'); return {'status': 'validated'} if state['input_data'].get('material') else {'status': 'failed'}; def report_gen(state: ProcureState): return {'status': 'finalized'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('finalize', report_gen); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()