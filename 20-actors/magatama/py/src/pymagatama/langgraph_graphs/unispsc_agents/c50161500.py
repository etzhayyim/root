from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; inspection_passed: bool; def val_quality(state: ProcurementState): return {'inspection_passed': True}; def route_stock(state: ProcurementState): return 'add_to_inventory'; graph = StateGraph(ProcurementState); graph.add_node('validate', val_quality); graph.add_node('stock', route_stock); graph.set_entry_point('validate'); graph.add_edge('validate', 'stock'); graph.add_edge('stock', END); compiled_graph = graph.compile()