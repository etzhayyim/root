from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DocMgmtState(TypedDict): doc_metadata: dict; validation_errors: List[str]; def validate_security_compliance(state: DocMgmtState): state['validation_errors'] = [] if 'encryption' not in state['doc_metadata'] else []; return state; def check_workflow_logic(state: DocMgmtState): return state; builder = StateGraph(DocMgmtState); builder.add_node('validate', validate_security_compliance); builder.add_node('check', check_workflow_logic); builder.set_entry_point('validate'); builder.add_edge('validate', 'check'); builder.add_edge('check', END); graph = builder.compile()