from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): device_specs: dict; compliance_check: bool; vendor_approved: bool; def validate_specs(state: ProcurementState): return {'compliance_check': state['device_specs'].get('temp_range', 0) >= 37}; def verify_vendor(state: ProcurementState): return {'vendor_approved': True}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('verify', verify_vendor); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); graph.set_entry_point('validate'); graph = graph.compile()