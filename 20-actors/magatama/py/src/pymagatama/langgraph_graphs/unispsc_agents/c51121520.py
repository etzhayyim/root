from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): status: str; spec: dict; valid: bool; def validate_pharma(state: ProcState): state['valid'] = state['spec'].get('Purity_Level', 0) >= 99.0; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_pharma); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()