from langgraph.graph import StateGraph, END; from typing import TypedDict; class ToasterState(TypedDict): specs: dict; compliant: bool; def validate_specs(state: ToasterState): state['compliant'] = 'wattage_rating' in state['specs'] and 'safety_certification_pse' in state['specs']; return state; builder = StateGraph(ToasterState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()