from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): chemical_purity: float; specs_met: bool; material_certs: List[str]; def validate_purity(state: ForgingState): return {'specs_met': state['chemical_purity'] >= 99.9}; def check_certs(state: ForgingState): return {'specs_met': len(state['material_certs']) > 0}; graph = StateGraph(ForgingState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_certs', check_certs); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_certs'); graph.add_edge('check_certs', END); graph = graph.compile()