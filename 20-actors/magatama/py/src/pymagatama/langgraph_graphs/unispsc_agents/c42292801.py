from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OphthalmicState(TypedDict): device_id: str; compliance_docs: List[str]; inspection_passed: bool; def validate_compliance(state: OphthalmicState): return {'inspection_passed': len(state['compliance_docs']) >= 3}; def route_step(state: OphthalmicState): return 'validate' if state['device_id'] else END; graph = StateGraph(OphthalmicState); graph.add_node('validate', validate_compliance); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()