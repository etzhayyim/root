from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CoolingUnitState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: CoolingUnitState): state['valid'] = state['specs'].get('cooling_capacity', 0) > 0; state['log'].append('Validation completed'); return state; graph = StateGraph(CoolingUnitState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()