from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): dimensions: dict; material_specs: str; certified: bool; def validate_dimensions(state: ForgingState): state['certified'] = all(val > 0 for val in state['dimensions'].values()); return state; def check_material(state: ForgingState): return {'certified': 'Zinc-Alloy' in state['material_specs']}; graph = StateGraph(ForgingState); graph.add_node('validate', validate_dimensions); graph.add_node('material_check', check_material); graph.set_entry_point('validate'); graph.add_edge('validate', 'material_check'); graph.add_edge('material_check', END); graph = graph.compile()