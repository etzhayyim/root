from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CameraRingState(TypedDict): ring_specs: dict; validation_results: List[str]; graph = StateGraph(CameraRingState); def validate_thread_pitch(state: CameraRingState): state['validation_results'].append('Thread pitch verified'); return state; def check_material_grade(state: CameraRingState): state['validation_results'].append('Material grade confirmed'); return state; graph.add_node('validate_thread', validate_thread_pitch); graph.add_node('check_material', check_material_grade); graph.set_entry_point('validate_thread'); graph.add_edge('validate_thread', 'check_material'); graph.add_edge('check_material', END); app = graph.compile()