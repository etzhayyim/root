from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TriacState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: TriacState): errors = []; required = ['voltage', 'current', 'package']; for r in required: if r not in state['specs']: errors.append(f'Missing {r}'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(TriacState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()