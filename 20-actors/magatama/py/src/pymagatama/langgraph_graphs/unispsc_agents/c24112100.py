from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ContainerState(TypedDict): container_type: str; capacity: float; compliance_check: bool; graph = StateGraph(ContainerState); def validate_specs(state: ContainerState): return {'compliance_check': state['capacity'] > 0}; def check_hazmat(state: ContainerState): return {'compliance_check': True} if 'metal' in state['container_type'] else {'compliance_check': False}; graph.add_node('validate', validate_specs); graph.add_node('hazmat_check', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat_check'); graph.add_edge('hazmat_check', END); compiled_graph = graph.compile()