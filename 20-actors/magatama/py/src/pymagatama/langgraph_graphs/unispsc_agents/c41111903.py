from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MetalDetectorState(TypedDict): vendor_certs: List[str]; calibration_status: bool; compliance_check_passed: bool; def validate_cert(state: MetalDetectorState): return {'compliance_check_passed': 'ISO-9001' in state['vendor_certs']}; def assess_risk(state: MetalDetectorState): return {'compliance_check_passed': state['calibration_status'] and state['compliance_check_passed']}; graph = StateGraph(MetalDetectorState); graph.add_node('validate', validate_cert); graph.add_node('risk_assessment', assess_risk); graph.set_entry_point('validate'); graph.add_edge('validate', 'risk_assessment'); graph.add_edge('risk_assessment', END); graph = graph.compile()