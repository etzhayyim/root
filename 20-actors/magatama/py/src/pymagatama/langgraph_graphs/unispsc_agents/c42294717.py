from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VADState(TypedDict): device_specs: dict; compliance_docs: List[str]; approved: bool; def validate_specs(state: VADState): return {'approved': all(k in state['device_specs'] for k in ['ISO', 'Biocompatibility'])}; graph = StateGraph(VADState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()