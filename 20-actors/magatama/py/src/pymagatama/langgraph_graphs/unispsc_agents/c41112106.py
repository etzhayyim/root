from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: ProcState): specs = state.get('specs', {}); state['valid'] = 'accuracy_class' in specs and 'output_signal_type' in specs; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()