from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): mesh_density: float; material: str; validated: bool; def validate_mesh(state: SpecState): state['validated'] = state['mesh_density'] > 0 and len(state['material']) > 0; return state; graph = StateGraph(SpecState); graph.add_node('validate', validate_mesh); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()