from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VanityKitState(TypedDict): items: List[str]; inspection_passed: bool; compliance_docs: List[str]; def validate_kit(state: VanityKitState): state['inspection_passed'] = len(state['items']) >= 3; return state; def check_compliance(state: VanityKitState): state['compliance_docs'] = ['ISO22716'] if state['inspection_passed'] else []; return state; graph = StateGraph(VanityKitState); graph.add_node('validate', validate_kit); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()