from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TankerState(TypedDict): specs: dict; compliance_status: bool; def validate_specs(state: TankerState) -> TankerState: state['compliance_status'] = 'pressure_rating' in state['specs']; return state; def route_inspection(state: TankerState) -> str: return 'pass' if state['compliance_status'] else 'fail'; graph = StateGraph(TankerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()