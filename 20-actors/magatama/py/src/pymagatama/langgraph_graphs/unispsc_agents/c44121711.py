from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MarkerProcurementState(TypedDict): vendor_specs: dict; compliance_check: bool; final_approval: bool; def validate_specs(state: MarkerProcurementState): print('Validating ink and tip specs...'); return {'compliance_check': True}; def approve_procurement(state: MarkerProcurementState): print('Finalizing order...'); return {'final_approval': True}; graph = StateGraph(MarkerProcurementState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()