from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoatProcurementState(TypedDict): vessel_id: str; compliance_checks: List[str]; approved: bool; def validate_hull(state: BoatProcurementState): state['compliance_checks'].append('Hull integrity validated'); return state; def check_regulations(state: BoatProcurementState): state['compliance_checks'].append('Maritime regulations verified'); state['approved'] = True; return state; graph = StateGraph(BoatProcurementState); graph.add_node('validate_hull', validate_hull); graph.add_node('check_regulations', check_regulations); graph.set_entry_point('validate_hull'); graph.add_edge('validate_hull', 'check_regulations'); graph.add_edge('check_regulations', END); compile_graph = graph.compile()