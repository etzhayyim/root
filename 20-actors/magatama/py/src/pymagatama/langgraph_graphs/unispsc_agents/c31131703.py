from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_results: List[str]; def validate_material_grade(state: ForgingState): state['validation_results'].append('Material grade compliant'); return state; def check_dimensions(state: ForgingState): state['validation_results'].append('Dimensions within tolerance'); return state; graph = StateGraph(ForgingState); graph.add_node('material_check', validate_material_grade); graph.add_node('dimensional_check', check_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dimensional_check'); graph.add_edge('dimensional_check', END); graph = graph.compile()