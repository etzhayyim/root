from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class MedicalVacState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: MedicalVacState): state['compliance'] = 'ISO_7396' in state['specs'] and state['specs']['capacity'] > 0; return {'status': 'validated' if state['compliance'] else 'rejected'}; graph = StateGraph(MedicalVacState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()