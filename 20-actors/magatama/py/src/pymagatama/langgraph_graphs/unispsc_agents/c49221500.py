from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validated: bool; def validate_spec(state: State): return {'validated': all(len(i) > 0 for i in state['items'])}; def finalize_procurement(state: State): return {'validated': True}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate');