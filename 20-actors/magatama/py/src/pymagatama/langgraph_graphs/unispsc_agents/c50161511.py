from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChocolateState(TypedDict): items: List[dict]; status: str; def validate_temp_control(state: ChocolateState): print('Validating thermal logs...'); state['status'] = 'verified'; return state; def check_expiry(state: ChocolateState): print('Checking shelf-life compliance...'); return state; graph = StateGraph(ChocolateState); graph.add_node('thermal_check', validate_temp_control); graph.add_node('expiry_check', check_expiry); graph.set_entry_point('thermal_check'); graph.add_edge('thermal_check', 'expiry_check'); graph.add_edge('expiry_check', END); graph = graph.compile()