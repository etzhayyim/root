from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BanjoState(TypedDict): specs: dict; quality_check: bool; graph = StateGraph(BanjoState); def validate_specs(state: BanjoState): state['quality_check'] = 'tonering_material' in state['specs'] and 'number_of_strings' in state['specs']; return state; def check_compliance(state: BanjoState): return 'approved' if state['quality_check'] else 'rejected'; graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()