from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): material_check: bool; weld_verified: bool; compliance_ok: bool; def check_material(state: ProcState): return {'material_check': True}; def verify_welds(state: ProcState): return {'weld_verified': True}; def validate_compliance(state: ProcState): return {'compliance_ok': True}; graph = StateGraph(ProcState); graph.add_node('material', check_material); graph.add_node('welds', verify_welds); graph.add_node('compliance', validate_compliance); graph.set_entry_point('material'); graph.add_edge('material', 'welds'); graph.add_edge('welds', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()