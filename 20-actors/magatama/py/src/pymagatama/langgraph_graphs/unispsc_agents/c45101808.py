from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BindingState(TypedDict): specs: dict; ready_for_binding: bool; from_node: str; def validate_specs(state: BindingState): state['ready_for_binding'] = state['specs'].get('binding_capacity_pages', 0) > 0; return state; graph = StateGraph(BindingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()