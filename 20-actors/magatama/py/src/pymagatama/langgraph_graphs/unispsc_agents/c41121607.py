from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipetteState(TypedDict): vendor_certs: List[str]; volume: int; sterile: bool; approved: bool; def validate_specs(state: PipetteState): state['approved'] = 'iso-13485' in state['vendor_certs'] and state['sterile'] is True; return state; graph = StateGraph(PipetteState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()