from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CameraState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: CameraState): spec = state['specs']; errors = []; if spec.get('resolution', 0) < 1080: errors.append('Resolution below minimum standard'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(CameraState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()