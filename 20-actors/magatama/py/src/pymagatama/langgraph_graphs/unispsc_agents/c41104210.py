from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; approval_status: bool; def validate_specs(state: State): return {'approval_status': state['input_data'].get('rpm') > 0}; def check_safety(state: State): return {'approval_status': state['input_data'].get('temp_limit') < 300}; graph_builder = StateGraph(State); graph_builder.add_node('validate', validate_specs); graph_builder.add_node('safety', check_safety); graph_builder.add_edge('validate', 'safety'); graph_builder.add_edge('safety', END); graph_builder.set_entry_point('validate'); graph = graph_builder.compile()