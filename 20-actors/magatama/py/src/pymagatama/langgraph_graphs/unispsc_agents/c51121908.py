from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DigitalisState(TypedDict): ingredient: str; purity: float; compliant: bool; graph = StateGraph(DigitalisState); def validate_purity(state: DigitalisState): return {'compliant': state['purity'] >= 98.0}; def check_regulations(state: DigitalisState): return {'compliant': True}; graph.add_node('validate_purity', validate_purity); graph.add_node('check_regulations', check_regulations); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_regulations'); graph.add_edge('check_regulations', END); graph = graph.compile()