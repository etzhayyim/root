from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DieProcurementState(TypedDict): die_id: str; spec_validated: bool; material_verified: bool; def validate_spec(state: DieProcurementState): print('Validating die tolerance...'); return {'spec_validated': True}; def check_material(state: DieProcurementState): print('Checking material heat treatment...'); return {'material_verified': True}; graph = StateGraph(DieProcurementState); graph.add_node('validate', validate_spec); graph.add_node('material', check_material); graph.add_edge('validate', 'material'); graph.add_edge('material', END); graph.set_entry_point('validate'); graph = graph.compile()