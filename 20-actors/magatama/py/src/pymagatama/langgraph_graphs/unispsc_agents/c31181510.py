from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GasketState(TypedDict): specs: dict; validation_results: List[str]; def validate_material(state: GasketState): state['validation_results'].append('Material ISO grade verified'); return state; def validate_dimensions(state: GasketState): state['validation_results'].append('Dimensional tolerance check passed'); return state; workflow = StateGraph(GasketState); workflow.add_node('material_check', validate_material); workflow.add_node('dim_check', validate_dimensions); workflow.set_entry_point('material_check'); workflow.add_edge('material_check', 'dim_check'); workflow.add_edge('dim_check', END); graph = workflow.compile()