from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): specs: dict; valid: bool; def validate_refractory(state: State): specs = state.get('specs', {}); state['valid'] = 'temperature' in specs and 'density' in specs; return state; graph = StateGraph(State); graph.add_node('validate', validate_refractory); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()