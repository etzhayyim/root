from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): model: str; specs: dict; approved: bool; def validate_specs(state: ProcState): state['approved'] = 'xray_transparency' in state['specs']; return state; builder = StateGraph(ProcState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()