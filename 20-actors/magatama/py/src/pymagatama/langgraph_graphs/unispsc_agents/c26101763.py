from langgraph.graph import StateGraph, END; from typing import TypedDict; class FreezePlugState(TypedDict): specs: dict; valid: bool; def check_specs(state: FreezePlugState): state['valid'] = 'diameter' in state['specs'] and 'material' in state['specs']; return state; builder = StateGraph(FreezePlugState); builder.add_node('validate', check_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()