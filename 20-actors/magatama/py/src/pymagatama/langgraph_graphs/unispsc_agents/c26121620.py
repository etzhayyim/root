from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: CableState): specs = state['specs']; state['valid'] = 'connector_type_a' in specs and 'voltage_rating' in specs; return state; graph = StateGraph(CableState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()