from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class RubberProcureState(TypedDict): input_spec: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_purity(state: RubberProcureState): return {'validation_results': ['Purity check: Pass'] if state['input_spec'].get('purity', 0) > 95 else ['Purity check: Fail']} def check_safety(state: RubberProcureState): return {'status': 'Approved' if 'Fail' not in state['validation_results'] else 'Rejected'} graph = StateGraph(RubberProcureState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()