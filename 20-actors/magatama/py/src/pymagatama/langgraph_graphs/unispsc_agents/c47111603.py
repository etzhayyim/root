from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SteamMachineState(TypedDict): model_id: str; specs: dict; compliant: bool; def validate_pressure(state: SteamMachineState): state['compliant'] = state['specs'].get('pressure', 0) > 0; return state; graph = StateGraph(SteamMachineState); graph.add_node('validate', validate_pressure); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()