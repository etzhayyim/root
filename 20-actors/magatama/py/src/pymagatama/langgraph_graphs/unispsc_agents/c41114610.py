from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FoundryState(TypedDict): vendor_specs: dict; validation_results: List[str]; graph = StateGraph(FoundryState); def validate_equipment(state: FoundryState): state['validation_results'].append('Equipment integrity checked'); return state; def check_compliance(state: FoundryState): state['validation_results'].append('ISO-9001 and safety compliance verified'); return state; graph.add_node('validate', validate_equipment); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()