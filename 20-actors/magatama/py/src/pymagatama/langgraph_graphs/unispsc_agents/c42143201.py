from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpermKitState(TypedDict): vendor_docs: dict; compliance_check: bool; final_report: str; def validate_docs(state: SpermKitState): state['compliance_check'] = 'ISO13485' in state['vendor_docs'].get('certs', []); return state; def finalize_order(state: SpermKitState): state['final_report'] = 'Validation Complete' if state['compliance_check'] else 'Validation Failed'; return state; graph = StateGraph(SpermKitState); graph.add_node('validate', validate_docs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()