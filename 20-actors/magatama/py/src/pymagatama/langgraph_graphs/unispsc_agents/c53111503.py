from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BootProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_materials(state: BootProcurementState): state['validation_errors'] = [] if 'material_composition' not in state['specs'] else []; return state; def check_safety(state: BootProcurementState): if 'toxic_substance_test_certificate' not in state['specs']: state['validation_errors'].append('Missing safety cert'); return state; graph = StateGraph(BootProcurementState); graph.add_node('validate_materials', validate_materials); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate_materials'); graph.add_edge('validate_materials', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()