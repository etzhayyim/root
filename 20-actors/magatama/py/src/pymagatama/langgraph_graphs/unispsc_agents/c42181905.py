from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CableState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: CableState): specs = state['specs']; required = ['connector_type', 'biocompatibility_iso10993']; is_valid = all(key in specs for key in required); return {'valid': is_valid, 'error': '' if is_valid else 'Missing mandatory specs'}; graph = StateGraph(CableState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()