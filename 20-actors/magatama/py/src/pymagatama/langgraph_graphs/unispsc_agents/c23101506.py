from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): robot_id: str; specs_verified: bool; export_check_passed: bool; def validate_specs(state: RobotState): return {'specs_verified': True}; def check_export_controls(state: RobotState): return {'export_check_passed': True}; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export_controls); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()