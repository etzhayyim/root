from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: RobotState): results = []; if state['specs'].get('payload', 0) > 1000: results.append('Heavy duty check passed'); return {'validation_results': results}; def assemble_workflow(state: RobotState): return {'validation_results': state['validation_results'] + ['Workflow Configured']}; graph = StateGraph(RobotState); graph.add_node('validate', validate_specs); graph.add_node('assemble', assemble_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph = graph.compile()