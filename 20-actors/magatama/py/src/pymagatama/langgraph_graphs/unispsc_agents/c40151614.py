from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CompressorState(TypedDict): specs: dict; compliance: bool; valid: bool; def validate_specs(state: CompressorState): state['valid'] = 'Pressure Ratio' in state['specs'] and float(state['specs']['Pressure Ratio']) > 0; return state; def check_compliance(state: CompressorState): state['compliance'] = True; return state; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()