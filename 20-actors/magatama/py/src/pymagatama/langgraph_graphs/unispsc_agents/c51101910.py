from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; coa_provided: bool; purity: float; status: str; def validate_coa(state: PharmaState): return {'status': 'validated' if state['coa_provided'] else 'rejected'}; def check_purity(state: PharmaState): return {'status': 'approved' if state['purity'] >= 99.0 else 'flagged'}; graph = StateGraph(PharmaState); graph.add_node('validate_coa', validate_coa); graph.add_node('check_purity', check_purity); graph.add_edge('validate_coa', 'check_purity'); graph.add_edge('check_purity', END); graph.set_entry_point('validate_coa'); graph = graph.compile()