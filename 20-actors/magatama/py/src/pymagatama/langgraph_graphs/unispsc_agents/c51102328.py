from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity_data: float; compliance_verified: bool; def validate_purity(state: PharmState): state['compliance_verified'] = state['purity_data'] >= 99.0; return state; def check_regulations(state: PharmState): return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()