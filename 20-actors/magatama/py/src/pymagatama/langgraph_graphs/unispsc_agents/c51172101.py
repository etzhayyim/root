from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): procurement_data: dict; compliance_checks: Annotated[list, operator.add]; def validate_quality(state: PharmState): return {'compliance_checks': ['GMP_VERIFIED'] if state['procurement_data'].get('gmp') else ['FAILED']}; def update_records(state: PharmState): return {'compliance_checks': ['RECORD_UPDATED']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_quality); graph.add_node('finalize', update_records); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()