from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TableSkirtState(TypedDict): fabric_spec: str; fire_safety_cert: bool; dimensions: dict; status: str; def validate_specs(state: TableSkirtState): if not state.get('fire_safety_cert'): return {'status': 'REJECTED_FIRE_SAFETY'}; return {'status': 'VALIDATED'}; def finalize(state: TableSkirtState): return {'status': 'APPROVED'}; graph = StateGraph(TableSkirtState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()