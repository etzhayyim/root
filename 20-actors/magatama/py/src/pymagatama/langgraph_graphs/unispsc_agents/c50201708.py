from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CoffeeState(TypedDict): items: List[str]; inspection_passed: bool; def check_quality(state: CoffeeState): return {'inspection_passed': True} if state['items'] else {'inspection_passed': False}; def validate_supplies(state: CoffeeState): return {'inspection_passed': True}; graph = StateGraph(CoffeeState); graph.add_node('quality_check', check_quality); graph.add_node('supply_validation', validate_supplies); graph.add_edge('quality_check', 'supply_validation'); graph.add_edge('supply_validation', END); graph.set_entry_point('quality_check'); graph = graph.compile()