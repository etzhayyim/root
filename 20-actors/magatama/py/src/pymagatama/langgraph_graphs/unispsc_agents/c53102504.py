from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GloveState(TypedDict): glove_type: str; compliance_certs: List[str]; approved: bool; def validate_spec(state: GloveState): state['approved'] = 'ISO' in state['compliance_certs']; return {'approved': state['approved']}; graph = StateGraph(GloveState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()