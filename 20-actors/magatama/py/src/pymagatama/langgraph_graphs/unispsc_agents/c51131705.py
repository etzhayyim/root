from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purities: List[float]; documentation_verified: bool; def check_quality(state: PharmState): return {'documentation_verified': all([q >= 99.0 for q in state['purities']])}; def validate_compliance(state: PharmState): return {'documentation_verified': state['documentation_verified']}; graph = StateGraph(PharmState); graph.add_node('quality_check', check_quality); graph.add_node('compliance_check', validate_compliance); graph.set_entry_point('quality_check'); graph.add_edge('quality_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()