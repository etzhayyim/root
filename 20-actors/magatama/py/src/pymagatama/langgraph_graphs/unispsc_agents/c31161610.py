from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EyeBoltState(TypedDict): specs: dict; validation_passed: bool; warnings: List[str]; def validate_specs(state: EyeBoltState): is_valid = state['specs'].get('WLL', 0) > 0 and 'Thread' in state['specs']; return {'validation_passed': is_valid}; def route_by_validation(state: EyeBoltState): return 'approval' if state['validation_passed'] else 'reject'; graph = StateGraph(EyeBoltState); graph.add_node('validate', validate_specs); graph.add_edge('validate', 'approval'); graph.set_entry_point('validate'); graph = graph.compile()