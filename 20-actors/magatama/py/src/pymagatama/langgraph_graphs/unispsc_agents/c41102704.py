from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DiffractionState(TypedDict): device_id: str; specs: dict; compliance_cleared: bool; def validate_specs(state: DiffractionState): state['compliance_cleared'] = 'radiation_safety_certification' in state['specs'] return state; def check_export_control(state: DiffractionState): return state; graph = StateGraph(DiffractionState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()