from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProtocolState(TypedDict): items: Annotated[Sequence[dict], operator.add]; status: str; def validate_specs(state: ProtocolState): print('Validating equipment safety standards...'); return {'status': 'validated'}; def finalize_procurement(state: ProtocolState): print('Processing stationary bike order...'); return {'status': 'processed'}; graph = StateGraph(ProtocolState); graph.add_node('validate', validate_specs); graph.add_node('procure', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'procure'); graph.add_edge('procure', END); compiled_graph = graph.compile()