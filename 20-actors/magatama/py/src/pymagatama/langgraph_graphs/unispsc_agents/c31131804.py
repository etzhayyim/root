from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): dimensions: dict; material_specs: dict; approved: bool; def validate_specs(state): return {'approved': all(state['material_specs'].values())}; def check_dimensions(state): return {'approved': state['dimensions'].get('tolerance', 0) < 0.05}; graph = StateGraph(ForgingState); graph.add_node('validate', validate_specs); graph.add_node('check', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()