from langgraph.graph import StateGraph, END; from typing import TypedDict; class SkivingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: SkivingState): state['valid'] = all(k in state['specs'] for k in ['material', 'hardness']); return state; graph = StateGraph(SkivingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()