from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ENTProcessorState(TypedDict): device_id: str; compliance_docs: List[str]; approved: bool; def validate_med_specs(state: ENTProcessorState): print('Validating device specs against ISO 13485...'); state['approved'] = True; return state; def check_regulatory(state: ENTProcessorState): print('Verifying FDA/PMDA registration...'); return state; graph = StateGraph(ENTProcessorState); graph.add_node('validate', validate_med_specs); graph.add_node('regulatory', check_regulatory); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); graph = graph.compile()