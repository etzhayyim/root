from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_forging(state: ForgingState): tolerance = state['specs'].get('tolerance', 0.05); return {'validation_report': 'Passed' if tolerance <= 0.05 else 'Failed', 'approved': tolerance <= 0.05}; graph = StateGraph(ForgingState); graph.add_node('validate', validate_forging); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()