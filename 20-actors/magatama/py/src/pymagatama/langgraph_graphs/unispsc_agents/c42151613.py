from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: DentalState): state['compliance'] = 'ISO' in state['specs'].get('standard', ''); return state; def final_decision(state: DentalState): state['approved'] = state['compliance']; return state; graph = StateGraph(DentalState); graph.add_node('validation', validate_specs); graph.add_node('decision', final_decision); graph.set_entry_point('validation'); graph.add_edge('validation', 'decision'); graph.add_edge('decision', END); compiled_graph = graph.compile()