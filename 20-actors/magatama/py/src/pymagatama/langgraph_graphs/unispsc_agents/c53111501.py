from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BootState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_specs(state: BootState): errors = []; for item in state['items']: if 'size' not in item: errors.append('Missing size'); state['validation_errors'] = errors; return state; graph = StateGraph(BootState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()