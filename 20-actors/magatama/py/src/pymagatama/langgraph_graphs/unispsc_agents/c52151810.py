from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor: str; specs: dict; approved: bool; def validate_specs(state: ProcureState): state['approved'] = 'food_grade' in state['specs'].get('certifications', []) and state['specs'].get('wattage') < 1500; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()