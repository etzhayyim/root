from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; purity: float; compliant: bool; def validate_purity(state: DrugState): state['compliant'] = state['purity'] >= 0.99; return state; def check_regulations(state: DrugState): print(f'Checking {state['chemical_name']} against FDA/EMA guidelines'); return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('regulations', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulations'); graph.add_edge('regulations', END); graph = graph.compile()