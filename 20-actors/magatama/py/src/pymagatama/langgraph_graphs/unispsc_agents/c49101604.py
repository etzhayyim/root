from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StampState(TypedDict): stamp_id: str; condition: str; verified: bool; def validate_stamp(state: StampState): state['verified'] = (state['condition'] in ['Mint', 'Used-Fine']); return state; graph = StateGraph(StampState); graph.add_node('validate', validate_stamp); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()