from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_status: bool; workflow_log: list; graph = StateGraph(State); def validate_equipment(state: State): state['validation_status'] = 'RadiationSafetyCert' in state['input_data']; return state; def check_compliance(state: State): state['workflow_log'].append('Checking clinical compliance'); return state; graph.add_node('Validate', validate_equipment); graph.add_node('Compliance', check_compliance); graph.set_entry_point('Validate'); graph.add_edge('Validate', 'Compliance'); graph.add_edge('Compliance', END); compiled_graph = graph.compile()