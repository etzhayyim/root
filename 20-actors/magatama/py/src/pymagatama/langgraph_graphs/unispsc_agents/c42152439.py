from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentureState(TypedDict): vendor_certs: List[str]; material_specs: dict; approved: bool; def validate_specs(state: DentureState): state['approved'] = 'ISO_10993' in state['vendor_certs'] and state['material_specs']['toxicity'] == 'none'; return state; graph = StateGraph(DentureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()