from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LoadBinderState(TypedDict): binder_type: str; wll: float; chain_size: str; valid: bool; def validate_specs(state: LoadBinderState) -> LoadBinderState: state['valid'] = state['wll'] > 0 and state['chain_size'] != ''; return state; graph = StateGraph(LoadBinderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()