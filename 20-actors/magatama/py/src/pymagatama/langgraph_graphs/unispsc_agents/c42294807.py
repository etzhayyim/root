from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): doc_id: str; quality_score: float; compliance_check: bool; def validate_instrument(state: ProcState): state['compliance_check'] = True; return state; def check_quality(state: ProcState): state['quality_score'] = 1.0; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_instrument); graph.add_node('quality', check_quality); graph.add_edge('validate', 'quality'); graph.add_edge('quality', END); graph.set_entry_point('validate'); graph = graph.compile()