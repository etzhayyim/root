from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LeafSpringState(TypedDict): specs: dict; validation_results: List[str]; def validate_load(state): return {'validation_results': ['Load capacity within range'] if state['specs'].get('load') > 0 else ['Load error']}; def check_safety(state): return {'validation_results': state['validation_results'] + ['Safety compliance confirmed']}; graph = StateGraph(LeafSpringState); graph.add_node('validate', validate_load); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()