from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RepairTapeState(TypedDict): spec: dict; validation_report: List[str]; def validate_tape_specs(state: RepairTapeState): errors = []; if not state['spec'].get('pressure_rating'): errors.append('Missing pressure rating'); return {'validation_report': errors}; graph = StateGraph(RepairTapeState); graph.add_node('validate', validate_tape_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()