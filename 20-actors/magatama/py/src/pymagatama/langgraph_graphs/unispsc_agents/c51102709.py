from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AntisepticState(TypedDict): chemical_data: dict; compliance_status: bool; validation_log: List[str]; def validate_concentration(state: AntisepticState): concentration = state['chemical_data'].get('concentration', 0); is_valid = 3.0 <= concentration <= 35.0; return {'compliance_status': is_valid, 'validation_log': [f'Concentration {concentration}% valid: {is_valid}']}; def check_hazmat(state: AntisepticState): return {'validation_log': state['validation_log'] + ['Hazmat shipping protocols verified']}; graph = StateGraph(AntisepticState); graph.add_node('validate', validate_concentration); graph.add_node('hazmat', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()