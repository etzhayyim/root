from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LensPaperState(TypedDict): items: List[str]; validation_results: List[str]; def validate_material(state: LensPaperState): state['validation_results'].append('Lint-free verified'); return state; def check_stock(state: LensPaperState): state['validation_results'].append('Inventory levels sufficient'); return state; graph = StateGraph(LensPaperState); graph.add_node('validate', validate_material); graph.add_node('stock', check_stock); graph.set_entry_point('validate'); graph.add_edge('validate', 'stock'); graph.add_edge('stock', END); graph = graph.compile()