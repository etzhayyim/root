from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SurgicalToolState(TypedDict): items: List[str]; validation_results: List[str]; def validate_material(state: SurgicalToolState): return {'validation_results': ['Material check passed: Surgical Grade Stainless Steel']}; def check_certification(state: SurgicalToolState): return {'validation_results': ['ISO 13485 Certification verified']}; graph = StateGraph(SurgicalToolState); graph.add_node('material_check', validate_material); graph.add_node('cert_check', check_certification); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'cert_check'); graph.add_edge('cert_check', END); compiled_graph = graph.compile()