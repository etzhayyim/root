from langgraph.graph import StateGraph, END; from typing import TypedDict; class DosingSpec(TypedDict): accuracy_verified: bool; material_safe: bool; graph = StateGraph(DosingSpec); def validate_accuracy(state: DosingSpec): return {"accuracy_verified": True}; def validate_material(state: DosingSpec): return {"material_safe": True}; graph.add_node("accuracy", validate_accuracy); graph.add_node("material", validate_material); graph.set_entry_point("accuracy"); graph.add_edge("accuracy", "material"); graph.add_edge("material", END); graph.compile()