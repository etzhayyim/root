from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GearState(TypedDict): tool_specs: dict; validation_results: List[str]; graph = StateGraph(GearState); def validate_specs(state: GearState): return {'validation_results': ['Specs compliant'] if 'module' in state['tool_specs'] else ['Missing module']}; def check_export(state: GearState): return {'validation_results': state['validation_results'] + ['Export control checked']}; graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); compiled_graph = graph.compile()