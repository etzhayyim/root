from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VehicleState(TypedDict): vehicle_id: str; specs: dict; compliant: bool; def validate_specs(state: VehicleState): state['compliant'] = 'emission_standard_compliance' in state['specs'] and 'vin' in state['specs']; return state; def route_step(state: VehicleState): return 'validate' if state.get('vehicle_id') else END; graph = StateGraph(VehicleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()