from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PumpPartState(TypedDict): part_details: dict; validation_report: list[str]; graph = StateGraph(PumpPartState); def validate_spec(state: PumpPartState): return {'validation_report': ['Spec compliance verified']}; def check_compatibility(state: PumpPartState): return {'validation_report': ['Compatibility confirmed']}; graph.add_node('validate', validate_spec); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()