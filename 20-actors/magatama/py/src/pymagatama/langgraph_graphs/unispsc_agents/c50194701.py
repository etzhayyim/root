from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): content: str; quality_score: float; approved: bool; def check_safety(state: State): state['approved'] = state['quality_score'] > 0.9; return state; graph = StateGraph(State); graph.add_node('safety_check', check_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()