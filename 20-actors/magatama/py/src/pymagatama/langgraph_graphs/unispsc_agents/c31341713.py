from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(AssemblyState); def validate_spec(state: AssemblyState): return {'validation_results': ['Material check complete', 'Torque spec verified']}; def assemble_parts(state: AssemblyState): return {'validation_results': state['validation_results'] + ['Assembly staged']}; graph.add_node('validate', validate_spec); graph.add_node('assemble', assemble_parts); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); compiled_graph = graph.compile()