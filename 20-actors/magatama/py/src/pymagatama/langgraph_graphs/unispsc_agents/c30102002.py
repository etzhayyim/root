from langgraph.graph import StateGraph, END; from typing import TypedDict; class AlloyState(TypedDict): alloy_type: str; specs: dict; compliance: bool; def validate_spec(state: AlloyState): state['compliance'] = 'ASTM' in state['specs'].get('standard', '') and state['specs'].get('thickness', 0) > 0; return state; graph = StateGraph(AlloyState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()