from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(ProcState); def validate_welds(state: ProcState): state['validation_results'] = {'weld_integrity': 'pending', 'material_compliance': 'checked'}; return state; def check_export(state: ProcState): state['validation_results']['export_control'] = 'flagged_for_review'; return state; graph.add_node('validate_welds', validate_welds); graph.add_node('check_export', check_export); graph.set_entry_point('validate_welds'); graph.add_edge('validate_welds', 'check_export'); graph.add_edge('check_export', END); app = graph.compile()