from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BinderState(TypedDict): specs: dict; valid: bool; def validate_specs(state: BinderState): state['valid'] = len(state.get('specs', {})) >= 3; return state; graph = StateGraph(BinderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()