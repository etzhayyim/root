from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class HFState(TypedDict): chemical_data: dict; validation_logs: Annotated[Sequence[str], operator.add]; status: str; def validate_purity(state: HFState): purity = state['chemical_data'].get('purity', 0); return {'validation_logs': [f'Purity check: {purity}%'], 'status': 'PASS' if purity > 99.9 else 'FAIL'}; def check_regulations(state: HFState): return {'validation_logs': ['Checking export control compliance...'], 'status': 'COMPLIANT'}; graph = StateGraph(HFState); graph.add_node('purity_check', validate_purity); graph.add_node('regulatory_check', check_regulations); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'regulatory_check'); graph.add_edge('regulatory_check', END); graph = graph.compile()