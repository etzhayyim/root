from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RoofingState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: RoofingState): errors = []; if state['specs'].get('fire_rating') != 'Class A': errors.append('Fire rating does not meet Class A requirements'); return {'validation_errors': errors}; def finalize_approval(state: RoofingState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(RoofingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()