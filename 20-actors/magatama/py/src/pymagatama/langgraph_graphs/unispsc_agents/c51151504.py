from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): chemical_name: str; gmp_certified: bool; temp_logs: List[float]; approved: bool; def validate_gmp(state: ProcurementState) -> dict: return {'approved': state['gmp_certified']}; def check_storage(state: ProcurementState) -> dict: return {'approved': all(t < 25.0 for t in state['temp_logs'])}; graph = StateGraph(ProcurementState); graph.add_node('validate_gmp', validate_gmp); graph.add_node('check_storage', check_storage); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', 'check_storage'); graph.add_edge('check_storage', END); graph = graph.compile()