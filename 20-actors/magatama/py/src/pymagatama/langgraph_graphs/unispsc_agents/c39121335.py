from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CabinetState(TypedDict): cabinet_id: str; specs: dict; validation_passed: bool; def validate_specs(state: CabinetState): state['validation_passed'] = all(k in state['specs'] for k in ['IP_rating', 'material']); return state; graph = StateGraph(CabinetState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()