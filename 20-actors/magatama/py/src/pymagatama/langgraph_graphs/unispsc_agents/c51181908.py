from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_id: str; purity: float; conformant: bool; def check_quality(state: PharmaState): state['conformant'] = state['purity'] >= 99.0; return state; def validate_gmp(state: PharmaState): return {'conformant': True} if state['conformant'] else {'conformant': False}; graph = StateGraph(PharmaState); graph.add_node('quality_check', check_quality); graph.add_node('gmp_check', validate_gmp); graph.add_edge('quality_check', 'gmp_check'); graph.add_edge('gmp_check', END); graph.set_entry_point('quality_check'); graph = graph.compile()