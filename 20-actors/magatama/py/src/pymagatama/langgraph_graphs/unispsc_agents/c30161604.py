from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CeilingSpec(TypedDict): specs: List[str]; validated: bool; def validate_specs(state: CeilingSpec): state['validated'] = all(len(s) > 0 for s in state['specs']); return state; graph = StateGraph(CeilingSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()