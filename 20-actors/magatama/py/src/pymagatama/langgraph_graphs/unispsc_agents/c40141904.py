from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DuctProcurementState(TypedDict): specs: dict; validation_results: list; status: str; def validate_material(state: DuctProcurementState): 'Validates alloy specs against industry standards'; return {'validation_results': ['Alloy grade verified']}; def check_compliance(state: DuctProcurementState): 'Checks fire and pressure compliance'; return {'status': 'Approved'}; graph = StateGraph(DuctProcurementState); graph.add_node('validate', validate_material); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()