from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): container_type: str; is_sterile: bool; specs: List[str]; def validate_medical_grade(state: SpecState): return {'specs': state['specs'] + ['Verified Medical Grade']} if state['is_sterile'] else {'specs': state['specs'] + ['Non-sterile, manual review']}; graph = StateGraph(SpecState); graph.add_node('validate', validate_medical_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()