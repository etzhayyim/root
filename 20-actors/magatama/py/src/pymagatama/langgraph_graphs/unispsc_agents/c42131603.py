from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; valid: bool; audit_log: List[str]; def validate_medical_garment(state: ProcurementState): spec = state['specs']; is_valid = 'ISO' in spec and 'fabric' in spec; state['valid'] = is_valid; state['audit_log'].append('Medical sanitation compliance checked'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_medical_garment); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()