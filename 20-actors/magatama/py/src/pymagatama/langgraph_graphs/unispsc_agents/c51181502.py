from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; purity: float; compliant: bool; def validate_purity(state: DrugState): state['compliant'] = state['purity'] >= 99.0; return state; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()