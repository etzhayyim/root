from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class MailSorterState(TypedDict): dimensions: dict; material: str; inspection_passed: bool; def validate_specs(state: MailSorterState): state['inspection_passed'] = all([state['dimensions'].get('width', 0) > 0, state['material'] != '']); return state; def check_safety(state: MailSorterState): return 'compliant' if state['inspection_passed'] else 'flag_for_review'; graph = StateGraph(MailSorterState); graph.add_node('validate', validate_specs); graph.add_node('safety_check', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()