from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TileState(TypedDict): tiles: List[dict]; validation_results: List[str]; def validate_nrc(state: TileState): return {'validation_results': ['NRC >= 0.70 compliant']}; def check_fire_rating(state: TileState): return {'validation_results': ['Fire rating Class A confirmed']}; graph = StateGraph(TileState); graph.add_node('nrc_check', validate_nrc); graph.add_node('fire_check', check_fire_rating); graph.set_entry_point('nrc_check'); graph.add_edge('nrc_check', 'fire_check'); graph.add_edge('fire_check', END); graph = graph.compile()