from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DraperyState(TypedDict): specs: dict; validation_report: List[str]; def validate_specs(state: DraperyState): specs = state.get('specs', {}); report = []; required = ['flame_rating', 'dimensions']; for key in required: if key not in specs: report.append(f'Missing {key}'); return {'validation_report': report}; graph = StateGraph(DraperyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()