from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): vendor_id: str; specifications: List[str]; validation_result: bool; def validate_specs(state: ProcurementState): state['validation_result'] = all(len(s) > 0 for s in state['specifications']); return state; def route_procurement(state: ProcurementState): return 'end' if state['validation_result'] else 'end'; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()