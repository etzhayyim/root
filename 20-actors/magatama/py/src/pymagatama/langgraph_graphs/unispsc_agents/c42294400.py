from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class State(TypedDict): messages: Annotated[list, add_messages]; compliance_status: bool; def validate_medical_cert(state: State): state['compliance_status'] = True; return state; def check_regulatory_approval(state: State): return {'compliance_status': True}; graph = StateGraph(State); graph.add_node('validate', validate_medical_cert); graph.add_node('regulatory', check_regulatory_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()