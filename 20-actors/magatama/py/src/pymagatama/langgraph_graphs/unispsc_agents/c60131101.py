from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class TrumpetState(TypedDict): vendor_specs: dict; quality_check: bool; graph = StateGraph(TrumpetState); def validate_specs(state: TrumpetState): return {quality_check: state['vendor_specs'].get('pitch_accuracy') == 'A+'}; def approve(state: TrumpetState): return {'quality_check': True}; graph.add_node('validation', validate_specs); graph.add_node('approval', approve); graph.set_entry_point('validation'); graph.add_edge('validation', 'approval'); graph.add_edge('approval', END); app = graph.compile()