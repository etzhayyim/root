from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RTDState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_rtd(state: RTDState): errors = []; if state['specs'].get('accuracy_class') not in ['A', 'AA', 'B']: errors.append('Invalid DIN accuracy class'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(RTDState); graph.add_node('validate', validate_rtd); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()