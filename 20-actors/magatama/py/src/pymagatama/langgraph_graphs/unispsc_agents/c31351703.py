from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_metallurgy(state: AssemblyState): return {'validation_results': ['Material Alloy X verified']}; def perform_weld_inspection(state: AssemblyState): return {'validation_results': ['Ultrasonic test compliant']}; graph = StateGraph(AssemblyState); graph.add_node('material_check', validate_metallurgy); graph.add_node('weld_audit', perform_weld_inspection); graph.add_edge('material_check', 'weld_audit'); graph.add_edge('weld_audit', END); graph.set_entry_point('material_check'); graph = graph.compile()