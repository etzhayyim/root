from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_id: str; purity: float; compliance_check: bool; graph = StateGraph(ReagentState); def validate_purity(state: ReagentState): return {'compliance_check': state['purity'] >= 0.99}; def log_risk(state: ReagentState): print(f'Processing reagent {state['reagent_id']}'); return state; graph.add_node('validate', validate_purity); graph.add_node('log', log_risk); graph.set_entry_point('log'); graph.add_edge('log', 'validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()