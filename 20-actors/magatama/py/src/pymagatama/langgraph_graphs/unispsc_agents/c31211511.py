from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaintProcurementState(TypedDict): specs: dict; compliance_status: bool; approved: bool; def validate_specs(state): state['compliance_status'] = state['specs'].get('VOC_content', 0) < 450; return state; def check_hazmat(state): state['approved'] = state['compliance_status']; return state; graph = StateGraph(PaintProcurementState); graph.add_node('validate', validate_specs); graph.add_node('hazmat_check', check_hazmat); graph.set_entry_point('validate'); graph.add_edge('validate', 'hazmat_check'); graph.add_edge('hazmat_check', END); compiled_graph = graph.compile()