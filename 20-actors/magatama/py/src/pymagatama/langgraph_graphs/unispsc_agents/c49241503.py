from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PlaygroundState(TypedDict): safety_docs: str; inspection_status: str; graph = StateGraph(PlaygroundState); def validate_safety(state: PlaygroundState): return {'inspection_status': 'verified' if state['safety_docs'] else 'pending'}; graph.add_node('safety_check', validate_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()