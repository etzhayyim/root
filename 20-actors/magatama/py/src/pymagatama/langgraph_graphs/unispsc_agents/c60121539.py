from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): input_data: dict; status: str; graph = StateGraph(State); def validate_msds(state: State): return {'status': 'MSDS_VERIFIED'} if 'msds' in state['input_data'] else {'status': 'MSDS_REQUIRED'}; def check_flammability(state: State): return {'status': 'SAFETY_CLEARED'}; graph.add_node('validate_msds', validate_msds); graph.add_node('check_flammability', check_flammability); graph.set_entry_point('validate_msds'); graph.add_edge('validate_msds', 'check_flammability'); graph.add_edge('check_flammability', END); graph = graph.compile()