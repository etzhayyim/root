from langgraph.graph import StateGraph, END; from typing import TypedDict; class AwardState(TypedDict): design_data: dict; validation_status: bool; def validate_design(state: AwardState): state['validation_status'] = 'engraving_text' in state['design_data']; return state; def finalize_order(state: AwardState): return {'validation_status': True}; graph = StateGraph(AwardState); graph.add_node('validate', validate_design); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()