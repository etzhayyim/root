from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class KettleState(TypedDict): specs: dict; compliance_score: float; approved: bool; def validation_node(state: KettleState): state['compliance_score'] = 1.0 if 'UL' in state['specs'].get('cert', '') else 0.0; state['approved'] = state['compliance_score'] >= 1.0; return state; graph = StateGraph(KettleState); graph.add_node('validate', validation_node); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()