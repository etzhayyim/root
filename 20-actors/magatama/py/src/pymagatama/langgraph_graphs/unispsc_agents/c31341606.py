from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: AssemblyState): results = []; if 'tensile_strength_mpa' not in state['specs']: results.append('Missing tensile strength'); return {'validation_results': results, 'status': 'validated' if not results else 'error'}; workflow = StateGraph(AssemblyState); workflow.add_node('validate', validate_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()