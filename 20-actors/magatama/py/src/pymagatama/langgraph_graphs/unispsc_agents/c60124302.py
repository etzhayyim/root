from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClayProcurementState(TypedDict): clay_type: str; specs: dict; validation_passed: bool; def validate_specs(state: ClayProcurementState): print(f'Validating specs: {state["specs"]}'); state['validation_passed'] = all(k in state['specs'] for k in ['firing_temperature_range_celsius', 'moisture_content_percentage']); return state; graph = StateGraph(ClayProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()