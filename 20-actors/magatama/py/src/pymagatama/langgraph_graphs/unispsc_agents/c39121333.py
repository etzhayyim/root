from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoxState(TypedDict): box_type: str; specs: dict; compliant: bool; def validate_specs(state: BoxState): state['compliant'] = 'fire_rating' in state['specs'] and state['specs']['fire_rating'] >= 60; return state; graph = StateGraph(BoxState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()