from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MgOHState(TypedDict): chemical_purity: float; moisture_content: float; compliance_certs: List[str]; approved: bool; def validate_purity(state: MgOHState): state['approved'] = state['chemical_purity'] >= 99.0; return state; def check_moisture(state: MgOHState): state['approved'] = state['approved'] and (state['moisture_content'] <= 0.5); return state; graph = StateGraph(MgOHState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_moisture', check_moisture); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_moisture'); graph.add_edge('check_moisture', END); graph = graph.compile()