from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; verified: bool; status: str; def validate_licensing(state: ProcState): return { 'verified': True, 'status': 'License Verified' }; def check_compliance(state: ProcState): return { 'status': 'Compliance Confirmed' }; graph = StateGraph(ProcState); graph.add_node('verify_license', validate_licensing); graph.add_node('check_compliance', check_compliance); graph.set_entry_point('verify_license'); graph.add_edge('verify_license', 'check_compliance'); graph.add_edge('check_compliance', END); graph = graph.compile()