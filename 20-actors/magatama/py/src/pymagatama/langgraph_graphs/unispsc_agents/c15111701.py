from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class AviationFuelState(TypedDict): fuel_type: str; purity_check: bool; safety_compliance: bool; status: str; def validate_fuel_quality(state: AviationFuelState) -> AviationFuelState: state['purity_check'] = True; return state; def check_regulatory_compliance(state: AviationFuelState) -> AviationFuelState: state['safety_compliance'] = True; return state; graph = StateGraph(AviationFuelState); graph.add_node('validate_purity', validate_fuel_quality); graph.add_node('check_safety', check_regulatory_compliance); graph.add_edge('validate_purity', 'check_safety'); graph.add_edge('check_safety', END); graph.set_entry_point('validate_purity'); compiled_graph = graph.compile()