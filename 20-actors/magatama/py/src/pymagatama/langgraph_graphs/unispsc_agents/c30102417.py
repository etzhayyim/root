from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NickelProcurementState(TypedDict): specs: dict; compliance_check: bool; material_grade: str; def validate_grade(state: NickelProcurementState): state['compliance_check'] = state['material_grade'] in ['N02200', 'N02201']; return state; def verify_mtr(state: NickelProcurementState): print('Verifying Mill Test Report compliance...'); return state; graph = StateGraph(NickelProcurementState); graph.add_node('validate', validate_grade); graph.add_node('verify', verify_mtr); graph.set_entry_point('validate'); graph.add_edge('validate', 'verify'); graph.add_edge('verify', END); graph = graph.compile()