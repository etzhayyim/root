from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FilterState(TypedDict): specs: dict; validation: bool; graph = StateGraph(FilterState); def validate_specs(state: FilterState): return {'validation': 'micron_rating' in state['specs']}; def check_compliance(state: FilterState): return {'validation': True if state['validation'] else False}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()