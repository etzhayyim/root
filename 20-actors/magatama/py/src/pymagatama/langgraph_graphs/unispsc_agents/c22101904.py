from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserState(TypedDict): specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_specs(state: LaserState): state['compliance_status'] = 'laser_class_rating' in state['specs']; state['workflow_log'].append('Validation completed'); return state; graph = StateGraph(LaserState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()