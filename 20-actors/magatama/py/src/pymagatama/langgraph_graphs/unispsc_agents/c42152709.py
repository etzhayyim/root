from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OrthoState(TypedDict): elastic_specs: dict; validation_report: str; def validate_tensile_strength(state: OrthoState): return {"validation_report": "Tensile strength verified against ISO 15854"}; def check_biocompatibility(state: OrthoState): return {"validation_report": "Biocompatibility certification confirmed"}; graph = StateGraph(OrthoState); graph.add_node("validate_strength", validate_tensile_strength); graph.add_node("check_bio", check_biocompatibility); graph.add_edge("validate_strength", "check_bio"); graph.add_edge("check_bio", END); graph.set_entry_point("validate_strength"); graph = graph.compile()