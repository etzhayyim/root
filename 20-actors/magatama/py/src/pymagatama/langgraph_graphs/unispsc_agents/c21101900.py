from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SprayState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_specs(state: SprayState): results = []; capacity = state['input_data'].get('TankCapacity', 0); if capacity <= 0: results.append('Invalid capacity'); return {'validation_results': results, 'status': 'validated'}; def compile_graph(): graph = StateGraph(SprayState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); return graph.compile(); graph = compile_graph()