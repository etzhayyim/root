from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): status: str; validation_logs: Annotated[list, operator.add]; def validate_medical_device(state: ProcState): return {'validation_logs': ['Checking FDA Compliance', 'Verifying Radiation Safety Protocols']}; def finalize_procurement(state: ProcState): return {'status': 'READY_FOR_PURCHASE'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_medical_device); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()