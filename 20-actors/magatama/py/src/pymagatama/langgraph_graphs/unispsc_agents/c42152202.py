from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: DentalState): state['valid'] = 'pressure_range_psi' in state['specs'] and state['specs']['pressure_range_psi'] > 0; return state; graph = StateGraph(DentalState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()