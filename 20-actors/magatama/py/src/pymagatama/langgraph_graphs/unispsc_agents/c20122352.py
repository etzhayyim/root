from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): items: Annotated[Sequence[dict], operator.add]; status: str; validation_errors: list; def validate_spec(state: BearingState): return {'status': 'validated' if all('load_rating' in i for i in state['items']) else 'invalid'}; def finalize_procurement(state: BearingState): return {'status': 'approved'}; graph = StateGraph(BearingState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()