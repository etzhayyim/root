from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(State); def validate_spec(state: State): return {'validation_results': ['Compatibility check passed']}; def check_compliance(state: State): return {'validation_results': ['ISO 13485 verified']}; graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); 