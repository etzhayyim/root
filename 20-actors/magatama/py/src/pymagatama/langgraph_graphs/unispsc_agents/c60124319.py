from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ModelCompoundState(TypedDict): compound_type: str; safety_compliant: bool; validation_log: List[str]; def validate_quality(state: ModelCompoundState): state['validation_log'].append('Checking material safety...'); state['safety_compliant'] = True; return state; graph = StateGraph(ModelCompoundState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()