from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HerbState(TypedDict): vendor_docs: List[str]; inspection_passed: bool; def validate_quality(state: HerbState): return {'inspection_passed': len(state['vendor_docs']) >= 3}; def final_check(state: HerbState): return {'inspection_passed': True}; graph = StateGraph(HerbState); graph.add_node('validate', validate_quality); graph.add_node('approve', final_check); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); graph = graph.compile()