from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StretcherState(TypedDict): vendor_specs: dict; compliance_report: List[str]; approved: bool; def validate_specs(state: StretcherState): compliance = []; specs = state['vendor_specs']; if specs.get('load_capacity', 0) < 150: compliance.append('Load capacity below standard requirement'); if not specs.get('iso_certified'): compliance.append('Missing ISO certification'); return {'compliance_report': compliance, 'approved': len(compliance) == 0}; graph = StateGraph(StretcherState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()