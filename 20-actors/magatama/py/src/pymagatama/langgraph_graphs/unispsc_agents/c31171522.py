from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: BearingState): results = []; if state['specs'].get('max_rpm', 0) > 50000: results.append('Needs export control review'); return {'validation_results': results}; def check_compatibility(state: BearingState): return {'validation_results': state['validation_results'] + ['CAD integration verified']}; graph = StateGraph(BearingState); graph.add_node('validate', validate_specs); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()