from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): vendor_id: str; item_specs: dict; inspection_required: bool; approved: bool; def validate_specs(state: ProcurementState): return {'inspection_required': state['item_specs'].get('durable', False)}; def finalize_procurement(state: ProcurementState): return {'approved': True}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('approve', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()