from langgraph.graph import StateGraph, END; from typing import TypedDict; class DentalState(TypedDict): abrasive_type: str; grit_size: float; approved: bool; def validate_spec(state: DentalState): return 'approved' if state['grit_size'] < 50 else 'rejected'; builder = StateGraph(DentalState); builder.add_node('validate', validate_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()