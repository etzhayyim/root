from langgraph.graph import StateGraph, END; from typing import TypedDict; class GoldFoilState(TypedDict): status: str; spec_data: dict; validation_score: float; def validate_spec(state: GoldFoilState): state['validation_score'] = 1.0 if 'ISO' in state['spec_data'] else 0.0; return state; def finalize_order(state: GoldFoilState): state['status'] = 'COMPLETED'; return state; graph = StateGraph(GoldFoilState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()