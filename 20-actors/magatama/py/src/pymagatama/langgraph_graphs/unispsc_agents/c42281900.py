from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SterilizationState(TypedDict): items: List[dict]; compliance_checks: List[str]; def validate_iso11607(state: SterilizationState): state['compliance_checks'].append('ISO 11607-1 Verified'); return state; def check_barrier_integrity(state: SterilizationState): state['compliance_checks'].append('Microbial Barrier Tested'); return state; graph = StateGraph(SterilizationState); graph.add_node('validate', validate_iso11607); graph.add_node('integrity_check', check_barrier_integrity); graph.set_entry_point('validate'); graph.add_edge('validate', 'integrity_check'); graph.add_edge('integrity_check', END); graph = graph.compile()