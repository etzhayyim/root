from langgraph.graph import StateGraph, END; from typing import TypedDict; class JewelryState(TypedDict): certification: str; value: float; compliant: bool; def validate_jewelry(state: JewelryState): state['compliant'] = state['certification'] != '' and state['value'] > 0; return state; graph = StateGraph(JewelryState); graph.add_node('validate', validate_jewelry); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()