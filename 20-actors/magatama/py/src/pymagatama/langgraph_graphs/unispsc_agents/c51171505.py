from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SimethiconeState(TypedDict): vendor_docs: List[str]; validation_results: dict; compliance_status: bool; def validate_coa(state: SimethiconeState): return {'validation_results': {'coa_checked': True}, 'compliance_status': True}; def check_regulatory(state: SimethiconeState): return {'compliance_status': True}; graph = StateGraph(SimethiconeState); graph.add_node('validate_coa', validate_coa); graph.add_node('regulatory_check', check_regulatory); graph.set_entry_point('validate_coa'); graph.add_edge('validate_coa', 'regulatory_check'); graph.add_edge('regulatory_check', END); graph = graph.compile()