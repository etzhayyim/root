from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class RefractoryState(TypedDict): specs: dict; validation_report: list[str]; graph = StateGraph(RefractoryState); def validate_thermal_specs(state: RefractoryState): validation_result = 'Valid' if state['specs'].get('refractoriness') > 1500 else 'Invalid'; return {'validation_report': [validation_result]}; graph.add_node('validate', validate_thermal_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()