from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PivotState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: PivotState): specs = state['specs']; is_valid = all(key in specs for key in ['material', 'diameter', 'tolerance']); return {'valid': is_valid, 'error': '' if is_valid else 'Missing mandatory specifications'}; def compile_graph(): graph = StateGraph(PivotState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()