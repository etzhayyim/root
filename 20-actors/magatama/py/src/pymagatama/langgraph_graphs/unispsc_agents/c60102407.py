from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; validated: bool; state_code: str; def validate_educational_specs(state: State): state['validated'] = all(item for item in state['items']); return state; graph = StateGraph(State); graph.add_node('spec_validation', validate_educational_specs); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', END); graph = graph.compile()