from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class NailDryerState(TypedDict): specs: dict; validation_results: List[str]; def validate_uv_specs(state: NailDryerState): results = []; lamp = state['specs'].get('lamp_type'); wavelength = state['specs'].get('wavelength'); if lamp == 'UV' and not (360 <= wavelength <= 400): results.append('Invalid UV wavelength.'); return {'validation_results': results}; graph = StateGraph(NailDryerState); graph.add_node('validate', validate_uv_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()