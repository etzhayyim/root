from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): inventory_check: bool; schema_valid: bool; def validate_furniture(state: State): state['schema_valid'] = True; return state; def check_specs(state: State): state['inventory_check'] = True; return state; graph = StateGraph(State); graph.add_node('validate', validate_furniture); graph.add_node('check', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph = graph.compile()