from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ClutchState(TypedDict): specs: dict; validation_log: Annotated[list, operator.add]; status: str; def validate_specs(state: ClutchState): passed = state['specs'].get('pressure', 0) > 0; return {'validation_log': ['Pressure validated'] if passed else ['Pressure invalid'], 'status': 'VALID' if passed else 'REJECTED'}; graph = StateGraph(ClutchState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()