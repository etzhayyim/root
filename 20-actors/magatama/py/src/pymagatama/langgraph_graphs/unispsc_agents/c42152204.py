from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): spec: dict; valid: bool; error: str; def validate_specs(state: ProcState): specs = state.get('spec', {}); is_valid = all(key in specs for key in ['voltage', 'iso_cert']); return {'valid': is_valid, 'error': '' if is_valid else 'Missing mandatory certs'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()