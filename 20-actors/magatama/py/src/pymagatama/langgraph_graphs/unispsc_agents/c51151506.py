from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): reagent_data: dict; validation_passed: bool; def validate_reagent(state: ProcState): return {'validation_passed': 'CAS_Number' in state['reagent_data']}; def check_temp(state: ProcState): return {'validation_passed': state['reagent_data'].get('temp') == 'minus_80C'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_reagent); graph.add_node('cold_chain', check_temp); graph.add_edge('validate', 'cold_chain'); graph.add_edge('cold_chain', END); graph.set_entry_point('validate'); graph = graph.compile()