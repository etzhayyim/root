from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): items: List[str]; validated: bool; def validate_spec(state: SupplyState): return {'validated': len(state['items']) > 0}; graph = StateGraph(SupplyState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()