from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HoseState(TypedDict): specs: dict; valid: bool; error: str; def validate_hose_specs(state: HoseState): if 'pressure' not in state['specs']: return {'valid': False, 'error': 'Missing Pressure Spec'}; return {'valid': True}; graph = StateGraph(HoseState); graph.add_node('validate', validate_hose_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()