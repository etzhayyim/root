from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): component_specs: dict; validation_report: str; def validate_specs(state: State): return {'validation_report': 'Validated against aerospace standards'} if 'material' in state['component_specs'] else {'validation_report': 'Invalid'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()