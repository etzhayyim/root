from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TractorState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: TractorState): specs = state['specs']; errors = []; if specs.get('horsepower', 0) < 50: errors.append('Insufficient horsepower'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(TractorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()