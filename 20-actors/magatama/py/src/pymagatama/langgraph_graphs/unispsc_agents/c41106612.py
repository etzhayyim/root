from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExpressionVectorState(TypedDict): vector_id: str; quality_metrics: dict; compliant: bool; def validate_sequence(state: ExpressionVectorState): state['compliant'] = 'sequence_ok' in state['quality_metrics']; return state; def check_biosafety(state: ExpressionVectorState): return state; graph = StateGraph(ExpressionVectorState); graph.add_node('validate', validate_sequence); graph.add_node('biosafety', check_biosafety); graph.set_entry_point('validate'); graph.add_edge('validate', 'biosafety'); graph.add_edge('biosafety', END); compiled_graph = graph.compile()