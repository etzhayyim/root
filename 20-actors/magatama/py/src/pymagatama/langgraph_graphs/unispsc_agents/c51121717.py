from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmaState(TypedDict): chemical: str; purity: float; compliance: bool; approved: bool; def check_purity(state: PharmaState): return {'compliance': state['purity'] >= 99.0}; def validate_reg(state: PharmaState): return {'approved': state['compliance']}; graph = StateGraph(PharmaState); graph.add_node('check_purity', check_purity); graph.add_node('validate_reg', validate_reg); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'validate_reg'); graph.add_edge('validate_reg', END); graph = graph.compile()