from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): specs: dict; validation_passed: bool; def validate_die_specs(state: ToolState): specs = state['specs']; passed = all(k in specs for k in ['hardness', 'material', 'tolerance']); return {'validation_passed': passed}; graph = StateGraph(ToolState); graph.add_node('validate', validate_die_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()