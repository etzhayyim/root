from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): alloy_code: str; spec_data: dict; validation_report: str; def validate_magnesium_specs(state: State): spec = state['spec_data']; is_valid = 'grade' in spec and spec['grade'].startswith('AZ'); return {'validation_report': 'Passed' if is_valid else 'Failed'}; def export_control_check(state: State): return {'validation_report': 'Requires License' if state['alloy_code'] == 'D-USE' else 'Cleared'}; graph = StateGraph(State); graph.add_node('validate', validate_magnesium_specs); graph.add_node('export_check', export_control_check); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()