from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IMRTState(TypedDict): unit_id: str; compliance_check: bool; safety_validation: bool; def validate_physics(state: IMRTState): return {'compliance_check': True}; def safety_protocol(state: IMRTState): return {'safety_validation': True}; graph = StateGraph(IMRTState); graph.add_node('physics_check', validate_physics); graph.add_node('safety_check', safety_protocol); graph.set_entry_point('physics_check'); graph.add_edge('physics_check', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()