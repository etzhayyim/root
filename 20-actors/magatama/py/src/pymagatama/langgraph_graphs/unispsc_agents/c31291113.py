from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ExtrusionState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: ExtrusionState): import re; req = state['specs'].get('pressure_rating', 0); state['validation_report'] = 'Passed' if req > 5000 else 'Failed'; state['approved'] = req > 5000; return state; graph = StateGraph(ExtrusionState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()