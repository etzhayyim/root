from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BatchTubState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: BatchTubState): errors = []; if state['specs'].get('dimensions') == '0': errors.append('Dimensions missing'); return {'validation_errors': errors}; def check_safety(state: BatchTubState): return {'approved': len(state.get('validation_errors', [])) == 0}; graph = StateGraph(BatchTubState); graph.add_node('validate', validate_specs); graph.add_node('safety_check', check_safety); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph.set_entry_point('validate'); graph = graph.compile()