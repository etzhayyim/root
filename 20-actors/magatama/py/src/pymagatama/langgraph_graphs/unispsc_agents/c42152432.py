from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DentalMaterialState(TypedDict): material_name: str; specifications: dict; compliance_status: bool; def validate_specs(state: DentalMaterialState): required = ['ISO15912', 'SettingExpansion']; missing = [f for f in required if f not in state['specifications']]; state['compliance_status'] = len(missing) == 0; return state; graph = StateGraph(DentalMaterialState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()