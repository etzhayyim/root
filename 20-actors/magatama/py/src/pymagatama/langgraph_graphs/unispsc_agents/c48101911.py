from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_specs: List[str]; validated: bool; next_step: str; def validate_specs(state: ProcurementState): return {'validated': all(s in state['vendor_specs'] for s in ['Material Composition', 'Food-Grade Certification'])}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()