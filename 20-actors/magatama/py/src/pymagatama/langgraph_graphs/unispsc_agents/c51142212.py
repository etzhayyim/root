from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; purity: float; license_valid: bool; def check_compliance(state: State): return {'status': 'compliant' if state['license_valid'] and state['purity'] > 0.99 else 'rejected'}; graph = StateGraph(State); graph.add_node('compliance', check_compliance); graph.set_entry_point('compliance'); graph.add_edge('compliance', END); graph = graph.compile()