from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; quality_passed: bool; temp_logs: List[float]; def validate_temp(state: KitState): return {'quality_passed': all(t < 8.0 for t in state['temp_logs'])}; graph = StateGraph(KitState); graph.add_node('validate', validate_temp); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()