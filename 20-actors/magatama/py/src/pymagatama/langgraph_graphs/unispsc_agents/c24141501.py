from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_report: str; graph = StateGraph(State); def validate_film_specs(state: State): specs = state['input_data']; required = ['thickness', 'width', 'tensile_strength']; valid = all(k in specs for k in required); return {'validation_report': 'Passed' if valid else 'Failed'}; graph.add_node('validate', validate_film_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()