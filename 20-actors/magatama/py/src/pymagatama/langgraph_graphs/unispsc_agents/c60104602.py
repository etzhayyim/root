from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): model_specs: List[str]; validation_passed: bool; def validate_specs(state: State): state['validation_passed'] = len(state['model_specs']) > 0; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()