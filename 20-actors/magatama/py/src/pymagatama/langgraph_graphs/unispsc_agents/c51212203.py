from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): batch_id: str; purity: float; compliance_check: bool; def validate_purity(state: PharmState): return {'compliance_check': state['purity'] >= 99.0}; def finalize_procurement(state: PharmState): return {'compliance_check': True}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()