from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class EndoscopeState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(EndoscopeState); def validate_medical_compliance(state: EndoscopeState): return {'validation_results': ['ISO_13485_certified']}; def check_sterilization_specs(state: EndoscopeState): return {'validation_results': ['autoclave_compatible']}; graph.add_node('compliance', validate_medical_compliance); graph.add_node('sterilization', check_sterilization_specs); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'sterilization'); graph.add_edge('sterilization', END); graph = graph.compile()