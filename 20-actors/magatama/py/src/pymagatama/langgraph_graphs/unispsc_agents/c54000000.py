from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class JewelryState(TypedDict): content: dict; compliance_checked: bool; appraisal_verified: bool; def validate_authenticity(state: JewelryState): state['compliance_checked'] = True; return state; def verify_appraisal(state: JewelryState): state['appraisal_verified'] = True; return state; graph = StateGraph(JewelryState); graph.add_node('verify', validate_authenticity); graph.add_node('appraise', verify_appraisal); graph.set_entry_point('verify'); graph.add_edge('verify', 'appraise'); graph.add_edge('appraise', END); graph = graph.compile()