from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FruitState(TypedDict): items: List[str]; validated_items: List[dict]; status: str; def validate_shelf_life(state): return {'validated_items': [{'item': i, 'status': 'pass'} for i in state['items']]}; graph = StateGraph(FruitState); graph.add_node('validate', validate_shelf_life); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()