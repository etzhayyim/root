from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProbeState(TypedDict): items: Annotated[list, operator.add]; compliance_status: bool; def validate_probe(state: ProbeState): state['compliance_status'] = all(item.get('sterile') for item in state['items']); return state; def check_quality(state: ProbeState): return {'compliance_status': state['compliance_status']}; graph = StateGraph(ProbeState); graph.add_node('validate', validate_probe); graph.add_node('qc', check_quality); graph.add_edge('validate', 'qc'); graph.add_edge('qc', END); graph.set_entry_point('validate'); graph = graph.compile()