from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicroscopeState(TypedDict): hardware_specs: dict; compliance_check: bool; def validate_compatibility(state: MicroscopeState) -> MicroscopeState: state['compliance_check'] = 'MountType' in state['hardware_specs']; return state; def final_approval(state: MicroscopeState) -> MicroscopeState: return state; graph = StateGraph(MicroscopeState); graph.add_node('validate', validate_compatibility); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()