from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ImpressionState(TypedDict): material_type: str; specs: dict; compliant: bool; def validate_dental_material(state: ImpressionState): state['compliant'] = 'ISO_1563_compliance' in state['specs'] and state['specs'].get('shelf_life_expiration') > '2025-01-01'; return state; graph = StateGraph(ImpressionState); graph.add_node('validate', validate_dental_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()