from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialysisState(TypedDict): strap_specs: dict; validation_results: List[str]; def validate_material(state: DialysisState): return {'validation_results': ['Biocompatibility check passed'] if state['strap_specs'].get('is_biocompatible') else ['Biocompatibility check failed']}; def check_strength(state: DialysisState): return {'validation_results': state['validation_results'] + ['Tensile strength verified']}; graph = StateGraph(DialysisState); graph.add_node('validate_material', validate_material); graph.add_node('check_strength', check_strength); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_strength'); graph.add_edge('check_strength', END); app = graph.compile()