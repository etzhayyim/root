from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): items: List[dict]; validation_results: dict; def validate_spec(state: FurnitureState): state['validation_results'] = {'passed': all(item.get('weight_capacity') > 0 for item in state['items'])}; return state; def finalize_order(state: FurnitureState): return {'status': 'approved' if state['validation_results']['passed'] else 'rejected'}; graph = StateGraph(FurnitureState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()