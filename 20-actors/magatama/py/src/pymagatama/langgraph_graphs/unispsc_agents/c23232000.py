from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LatheState(TypedDict): model_id: str; specifications: dict; compliance_check: bool; def validate_specs(state: LatheState): state['compliance_check'] = state['specifications'].get('motor_power_kw', 0) > 0; return state; def finalize_order(state: LatheState): return {'finalized': True}; graph = StateGraph(LatheState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()