from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): device_id: str; compliance_docs: List[str]; approved: bool; def validate_cert(state: FilterState): state['approved'] = 'ISO13485' in state['compliance_docs']; return state; def check_risk(state: FilterState): return 'approved' if state['approved'] else 'rejected'; graph = StateGraph(FilterState); graph.add_node('validate', validate_cert); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()