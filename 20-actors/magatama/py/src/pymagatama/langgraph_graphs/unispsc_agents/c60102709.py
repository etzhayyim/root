from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; validated: bool; def check_safety(state: ProcState): return {'validated': True} if 'safety_cert' in state['items'] else {'validated': False}; graph = StateGraph(ProcState); graph.add_node('safety_check', check_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()