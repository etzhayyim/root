from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): active_ingredient: str; p_level: float; compliance_docs: List[str]; def validate_purity(state: PharmaState): return {'p_level': 99.9} if state['p_level'] >= 99.0 else {'p_level': 0.0}; def check_compliance(state: PharmaState): return {'compliance_docs': ['FDA', 'GMP']}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()