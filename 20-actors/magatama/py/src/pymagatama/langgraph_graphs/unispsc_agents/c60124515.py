from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToyState(TypedDict): name: str; age_rating: int; safety_cleared: bool; def safety_check(state: ToyState): state['safety_cleared'] = state['age_rating'] >= 3; return state; def compliance_check(state: ToyState): return {'safety_cleared': state.get('safety_cleared', False)}; graph = StateGraph(ToyState); graph.add_node('safety', safety_check); graph.add_node('compliance', compliance_check); graph.set_entry_point('safety'); graph.add_edge('safety', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()