from langgraph.graph import StateGraph, END; from typing import TypedDict; class SpeakerState(TypedDict): specs: dict; valid: bool; def val_specs(state: SpeakerState): state['valid'] = all(k in state['specs'] for k in ['power', 'impedance']); return state; graph = StateGraph(SpeakerState); graph.add_node('validate', val_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()