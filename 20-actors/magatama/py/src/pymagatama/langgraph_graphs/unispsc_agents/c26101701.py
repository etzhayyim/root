from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AircraftPartState(TypedDict): part_id: str; compliance: bool; inspection_logs: List[str]; def validate_part(state: AircraftPartState): return {'compliance': True, 'inspection_logs': ['ISO9001: Verified', 'AS9100: Verified']}; def route_part(state: AircraftPartState): return 'end' if state['compliance'] else END; graph = StateGraph(AircraftPartState); graph.add_node('validate', validate_part); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()