from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlaskState(TypedDict): capacity: float; material: str; validated: bool; results: List[str]; def validate_specs(state: FlaskState): validated = state.get('material') == 'Borosilicate' and state.get('capacity') > 0; return {'validated': validated, 'results': ['Safety test passed'] if validated else ['Safety test failed']}; graph = StateGraph(FlaskState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()