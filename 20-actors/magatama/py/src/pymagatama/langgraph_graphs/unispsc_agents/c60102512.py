from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NumeracyDocState(TypedDict): doc_content: str; educational_standards: List[str]; approved: bool; def validate_content(state: NumeracyDocState): state['approved'] = 'math' in state['doc_content'].lower() and len(state['educational_standards']) > 0; return state; graph = StateGraph(NumeracyDocState); graph.add_node('validate', validate_content); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()