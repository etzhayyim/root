from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ElementProcessState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_purity(state: ElementProcessState): return {'validation_results': ['Purity spectral analysis complete']}; def certify_origin(state: ElementProcessState): return {'validation_results': ['Origin certification verified']}; graph = StateGraph(ElementProcessState); graph.add_node('validate', validate_purity); graph.add_node('certify', certify_origin); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); compiled_graph = graph.compile()