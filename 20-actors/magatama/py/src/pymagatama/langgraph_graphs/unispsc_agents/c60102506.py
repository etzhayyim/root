from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EduState(TypedDict): content: str; validated: bool; meta: dict; def validate_content(state: EduState) -> EduState: state['validated'] = 'subtraction' in state['content'].lower(); return state; def format_output(state: EduState) -> EduState: state['meta'] = {'status': 'ready_for_print'}; return state; graph = StateGraph(EduState); graph.add_node('validate', validate_content); graph.add_node('format', format_output); graph.add_edge('validate', 'format'); graph.add_edge('format', END); graph.set_entry_point('validate'); graph = graph.compile()