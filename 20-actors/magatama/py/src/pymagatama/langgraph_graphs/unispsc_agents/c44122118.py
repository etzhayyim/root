from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FastenerState(TypedDict): vendor_specs: dict; compliance_status: str; graph = StateGraph(FastenerState); def validate_specs(state: FastenerState): return {'compliance_status': 'PASS' if state['vendor_specs'].get('tensile_strength', 0) > 50 else 'FAIL'}; def route_step(state: FastenerState): return 'validate' if state.get('vendor_specs') else END; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()