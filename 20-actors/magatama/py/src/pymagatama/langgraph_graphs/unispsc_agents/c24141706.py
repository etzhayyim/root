from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpoolState(TypedDict): spool_id: str; specs: dict; validation_passed: bool; def validate_specs(state: SpoolState): state['validation_passed'] = all(k in state['specs'] for k in ['flange', 'bore']); return state; def route_procurement(state: SpoolState): return 'end' if state['validation_passed'] else 'end'; graph = StateGraph(SpoolState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()