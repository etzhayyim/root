from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): reagent_data: dict; validation_results: List[str]; def validate_reagent(state: AnalysisState): results = []; data = state.get('reagent_data', {}); if not data.get('lot_number'): results.append('Missing Lot Number'); if data.get('temp_range', 0) > 8: results.append('Cold Chain Violation'); return {'validation_results': results}; graph = StateGraph(AnalysisState); graph.add_node('validate', validate_reagent); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()