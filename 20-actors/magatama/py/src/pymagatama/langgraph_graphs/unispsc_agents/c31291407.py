from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcessState(TypedDict): content: str; hazards: list[str]; validation_passed: bool; def validate_lead(state: ProcessState): state['validation_passed'] = 'lead' in state['content'].lower() and 'safety_cert' in state['content']; return state; def check_hazards(state: ProcessState): state['hazards'] = ['toxicity_risk', 'heavy_metal_regulation']; return state; graph = StateGraph(ProcessState); graph.add_node('validate', validate_lead); graph.add_node('check_hazards', check_hazards); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_hazards'); graph.add_edge('check_hazards', END); graph = graph.compile()