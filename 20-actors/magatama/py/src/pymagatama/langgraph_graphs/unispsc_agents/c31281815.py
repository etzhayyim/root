from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): component_id: str; specs: dict; approved: bool; def validate_spec(state: ProcureState): state['approved'] = all(k in state['specs'] for k in ['material', 'tolerance']); return state; def check_compliance(state: ProcureState): if state['approved']: state['final_status'] = 'COMPLIANT'; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()