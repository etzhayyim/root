from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[dict]; status: str; def validate_specs(state: State): for item in state['items']: item['verified'] = 'post_length' in item and 'material' in item; return {'status': 'validated'}; def assemble_graph(): graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = assemble_graph()