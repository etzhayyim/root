from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; validation_passed: bool; logs: List[str]; def validate_purity(state: PharmState): state['logs'].append('Verifying GMP purity levels...'); state['validation_passed'] = True; return state; def check_compliance(state: PharmState): state['logs'].append('Checking regulatory compliance...'); return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()