from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WatersportState(TypedDict): equipment_list: List[str]; safety_verified: bool; def validate_equipment(state: WatersportState): return {'safety_verified': all('ISO' in item for item in state.get('equipment_list', []))}; def route_procurement(state: WatersportState): return 'end' if state['safety_verified'] else 'review'; graph = StateGraph(WatersportState); graph.add_node('validate', validate_equipment); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()