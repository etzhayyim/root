from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EKGState(TypedDict): device_id: str; compliance_docs: List[str]; inspection_passed: bool; def check_compliance(state: EKGState): return {'inspection_passed': len(state['compliance_docs']) > 2}; def finalize_record(state: EKGState): return {'inspection_passed': True}; graph = StateGraph(EKGState); graph.add_node('validate', check_compliance); graph.add_node('finalize', finalize_record); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()