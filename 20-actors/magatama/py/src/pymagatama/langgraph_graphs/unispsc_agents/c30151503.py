from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RoofingState(TypedDict): specs: dict; validation: bool; def validate_specs(state: RoofingState): state['validation'] = all(k in state['specs'] for k in ['tensile_strength', 'material']); return state; def check_compliance(state: RoofingState): state['validation'] = state['validation'] and state['specs'].get('fire_rating') == 'A'; return state; graph = StateGraph(RoofingState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()