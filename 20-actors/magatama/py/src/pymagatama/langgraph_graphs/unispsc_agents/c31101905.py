from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IronCastState(TypedDict): part_id: str; specs: dict; valid: bool; def validate_specs(state: IronCastState): state['valid'] = 'material' in state['specs'] and state['specs']['material'] != ''; return state; def check_dimensions(state: IronCastState): print('Checking dimensional tolerance...'); return state; graph = StateGraph(IronCastState); graph.add_node('validate', validate_specs); graph.add_node('check_dims', check_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_dims'); graph.add_edge('check_dims', END); graph = graph.compile()