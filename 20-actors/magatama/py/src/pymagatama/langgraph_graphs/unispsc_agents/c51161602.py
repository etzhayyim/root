from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; validated: bool; def check_purity(state: PharmState): return {'validated': state['purity'] >= 99.0}; def finalize_procurement(state: PharmState): print(f'Processing {state["chemical_name"]}. Validation: {state["validated"]}'); return state; graph = StateGraph(PharmState); graph.add_node('validate', check_purity); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()