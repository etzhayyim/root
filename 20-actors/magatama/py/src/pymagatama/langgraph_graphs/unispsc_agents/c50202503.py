from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): commodity: str; specs: dict; approved: bool; def validate_specs(state: ProcurementState): state['approved'] = 'brix_degree' in state['specs'] and 'microbiological_test_report' in state['specs']; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()