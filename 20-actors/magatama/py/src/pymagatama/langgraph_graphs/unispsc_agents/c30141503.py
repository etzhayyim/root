from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InsulationState(TypedDict): specs: dict; valid: bool; def validate_specs(state: InsulationState): state['valid'] = state['specs'].get('fire_rating') == 'A1'; return state; graph = StateGraph(InsulationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()