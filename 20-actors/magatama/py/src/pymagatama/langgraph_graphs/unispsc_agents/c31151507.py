from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_tensile_strength(state: ProcurementState): strength = state['specs'].get('tensile_strength', 0); if strength < 50: state['validation_errors'].append('Strength below industrial threshold'); return {'approved': False}; return {'approved': True}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_tensile_strength); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()