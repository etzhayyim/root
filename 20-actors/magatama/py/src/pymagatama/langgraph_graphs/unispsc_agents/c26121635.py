from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RequisitionState(TypedDict): specs: dict; valid: bool; workflow_log: List[str]; def validate_specs(state: RequisitionState): specs = state['specs']; state['valid'] = 'Cable Capacity' in specs and 'IP Rating' in specs; state['workflow_log'].append('Validation completed'); return state; graph = StateGraph(RequisitionState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()