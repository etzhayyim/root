from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DeskState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(DeskState); def validate_specs(state: DeskState): results = []; for item in state['items']: if 'material' not in item: results.append(f'Missing material for {item.get("name")}'); state['validation_results'] = results; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()