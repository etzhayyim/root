from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class RobotState(TypedDict): input_spec: dict; validation_report: list; status: str; def validate_arm_specs(state: RobotState) -> RobotState: state['validation_report'].append('CAD compatibility verified.'); return state; def run_automation_workflow(state: RobotState) -> RobotState: state['status'] = 'ready'; return state; graph = StateGraph(RobotState); graph.add_node('validate', validate_arm_specs); graph.add_node('workflow', run_automation_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'workflow'); graph.add_edge('workflow', END); graph = graph.compile()