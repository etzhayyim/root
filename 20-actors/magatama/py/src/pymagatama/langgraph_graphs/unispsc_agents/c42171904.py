from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtricationState(TypedDict): items: List[str]; validated: bool; def validate_specs(state: ExtricationState): return {'validated': len(state['items']) > 0}; graph = StateGraph(ExtricationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()