from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): metal_grade: str; weld_cert: bool; inspection_passed: bool; def check_material(state: GraphState): return {'inspection_passed': state['metal_grade'] == 'Grade 5'}; def validate_welds(state: GraphState): return {'inspection_passed': state['weld_cert']}; graph = StateGraph(GraphState); graph.add_node('check_material', check_material); graph.add_node('validate_welds', validate_welds); graph.set_entry_point('check_material'); graph.add_edge('check_material', 'validate_welds'); graph.add_edge('validate_welds', END); graph = graph.compile()