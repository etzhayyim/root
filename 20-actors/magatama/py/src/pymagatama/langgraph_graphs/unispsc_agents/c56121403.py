from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TableSpecState(TypedDict): specs: dict; validation_errors: List[str]; def validate_load_capacity(state: TableSpecState): if state['specs'].get('load', 0) < 50: state['validation_errors'].append('Load capacity too low'); return state; def validate_casters(state: TableSpecState): if not state['specs'].get('locking_mechanism', False): state['validation_errors'].append('Locking mechanism required'); return state; graph = StateGraph(TableSpecState); graph.add_node('check_load', validate_load_capacity); graph.add_node('check_casters', validate_casters); graph.set_entry_point('check_load'); graph.add_edge('check_load', 'check_casters'); graph.add_edge('check_casters', END); graph = graph.compile()