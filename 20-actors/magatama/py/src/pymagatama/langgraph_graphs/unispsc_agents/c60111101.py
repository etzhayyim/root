from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BoardState(TypedDict): board_specs: dict; validation_errors: Annotated[list, operator.add]; def validate_specs(state: BoardState): errors = []; if not state['board_specs'].get('dimensions'): errors.append('Dimension missing'); return {'validation_errors': errors}; def finalize_procurement(state: BoardState): return {'validation_errors': []}; graph = StateGraph(BoardState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()