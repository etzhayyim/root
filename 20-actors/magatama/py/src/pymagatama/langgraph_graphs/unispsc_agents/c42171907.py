from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validation_status: bool; def validate_specs(state: ProcureState): state['validation_status'] = 'material' in state['items'] and 'sterilization' in state['items']; return state; builder = StateGraph(ProcureState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()