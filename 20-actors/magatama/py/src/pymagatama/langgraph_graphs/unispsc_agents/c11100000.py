from typing import TypedDict, Annotated, Sequence; from langgraph.graph import StateGraph, END; class MineralState(TypedDict): purity_level: float; impurities: list[str]; origin: str; compliance_verified: bool; def check_purity(state: MineralState): state['compliance_verified'] = state['purity_level'] > 99.5; return state; def verify_origin(state: MineralState): state['compliance_verified'] = state['origin'] != 'restricted_zone'; return state; graph = StateGraph(MineralState); graph.add_node('check_purity', check_purity); graph.add_node('verify_origin', verify_origin); graph.add_edge('check_purity', 'verify_origin'); graph.add_edge('verify_origin', END); graph.set_entry_point('check_purity'); graph = graph.compile()