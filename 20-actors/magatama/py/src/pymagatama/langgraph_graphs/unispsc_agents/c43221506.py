from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): device_id: str; specs: dict; valid: bool; def validate_specs(state: GraphState): state['valid'] = all(k in state['specs'] for k in ['Resolution', 'Encryption']); return state; graph = StateGraph(GraphState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()