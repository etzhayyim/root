from langgraph.graph import StateGraph, END; from typing import TypedDict; class ChalkState(TypedDict): chalk_type: str; is_dustless: bool; quantity: int; approved: bool; def validate_chalk(state: ChalkState): return {"approved": state.get("is_dustless", False)}; graph = StateGraph(ChalkState); graph.add_node("validate", validate_chalk); graph.set_entry_point("validate"); graph.add_edge("validate", END); graph = graph.compile()