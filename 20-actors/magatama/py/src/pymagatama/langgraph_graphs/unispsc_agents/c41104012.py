from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_id: str; specs: dict; validation_status: bool; def validate_specs(state: ProcState): state['validation_status'] = all(k in state['specs'] for k in ['material', 'capacity']); return state; def check_compliance(state: ProcState): return 'compliance_check_passed' if state['validation_status'] else 'manual_review'; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END);