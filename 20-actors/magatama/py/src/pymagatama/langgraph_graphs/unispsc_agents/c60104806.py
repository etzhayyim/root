from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): data: dict; status: str; def validate_physics_spec(state: State): state['status'] = 'validation_passed' if state['data'].get('frequency_hz', 0) > 0 else 'validation_failed'; return state; graph = StateGraph(State); graph.add_node('validate', validate_physics_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()