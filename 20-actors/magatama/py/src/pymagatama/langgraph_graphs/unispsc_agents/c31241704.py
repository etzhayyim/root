from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MirrorState(TypedDict): specs: dict; validation_errors: List[str]; def validate_optical_spec(state: MirrorState): specs = state.get('specs', {}); errors = []; if 'flatness' not in specs: errors.append('Missing surface flatness criteria'); return {'validation_errors': errors}; graph = StateGraph(MirrorState); graph.add_node('validate', validate_optical_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()