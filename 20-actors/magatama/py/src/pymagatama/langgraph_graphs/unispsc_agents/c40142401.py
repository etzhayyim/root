from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AmmoniaState(TypedDict): flange_specs: dict; validation_errors: List[str]; approved: bool; def check_pressure(state: AmmoniaState): rating = state['flange_specs'].get('pressure'); state['approved'] = rating > 150; return state; graph = StateGraph(AmmoniaState); graph.add_node('pressure_check', check_pressure); graph.set_entry_point('pressure_check'); graph.add_edge('pressure_check', END); graph = graph.compile()