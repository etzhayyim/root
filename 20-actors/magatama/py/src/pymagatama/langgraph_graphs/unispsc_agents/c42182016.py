from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScopeState(TypedDict): device_id: str; compliance_checks: List[str]; approved: bool; def validate_nasopharyngoscope(state: ScopeState): state['compliance_checks'].append('ISO_13485_VERIFIED'); state['approved'] = True; return state; graph = StateGraph(StateGraph); graph.add_node('validate', validate_nasopharyngoscope); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()