from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; validation_report: list; graph = StateGraph(State); def validate_storage_specs(state: State): specs = state['input_data']; errors = []; required = ['material', 'capacity']; for field in required: if field not in specs: errors.append(f'Missing {field}'); return {'validation_report': errors}; graph.add_node('validation', validate_storage_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()