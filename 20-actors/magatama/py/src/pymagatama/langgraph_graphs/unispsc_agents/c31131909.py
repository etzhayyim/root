from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; valid: bool; audit_log: List[str]; def validate_purity(state: ForgingState): state['valid'] = state['specs'].get('purity', 0) >= 0.999; return state; def check_export_control(state: ForgingState): state['audit_log'].append('Export control check initiated'); return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_purity); graph.add_node('export', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); graph = graph.compile()