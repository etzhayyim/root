from langgraph.graph import StateGraph, END; from typing import TypedDict; class SailOrderState(TypedDict): spec: dict; validated: bool; def validate_specs(state: SailOrderState): state['validated'] = 'material' in state['spec'] and 'dimensions' in state['spec']; return {'validated': state['validated']}; graph = StateGraph(SailOrderState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()