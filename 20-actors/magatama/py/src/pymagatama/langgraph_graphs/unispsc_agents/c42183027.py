from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_spec(state: State): specs = state['specs']; results = []; if specs.get('color_standard') != 'ISO-8597': results.append('Color mismatch'); return {'validation_results': results, 'status': 'validated' if not results else 'error'}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()