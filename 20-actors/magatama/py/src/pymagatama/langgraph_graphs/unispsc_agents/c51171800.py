from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): medicine_name: str; dosage: str; compliance_check: bool; batch_id: str; def validate_compliance(state: State): state['compliance_check'] = True if len(state['batch_id']) > 5 else False; return state; def route_approval(state: State): return 'approve' if state['compliance_check'] else 'reject'; graph = StateGraph(State); graph.add_node('validate', validate_compliance); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()