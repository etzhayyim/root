from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): items: List[str]; validated: bool; defect_rate: float; graph = StateGraph(SupplyState); def validate_beard_covers(state: SupplyState): state['validated'] = all(item.startswith('sterile') for item in state['items']); return state; def check_quality(state: SupplyState): state['defect_rate'] = 0.01; return state; graph.add_node('validate', validate_beard_covers); graph.add_node('quality_check', check_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'quality_check'); graph.add_edge('quality_check', END); graph.compile()