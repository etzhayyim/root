from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_laser(state: LaserState): state['valid'] = state['specs'].get('power', 0) > 0; state['log'].append('Validation complete'); return state; workflow = StateGraph(LaserState); workflow.add_node('validate', validate_laser); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()