from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input_data: str; validation_results: Annotated[list, operator.add]; status: str; def validate_instrument(state: ProcureState): return {'validation_results': ['Calibration check passed'], 'status': 'VALIDATED'}; def generate_contract(state: ProcureState): return {'status': 'COMPLETED'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_instrument); graph.add_node('generate', generate_contract); graph.set_entry_point('validate'); graph.add_edge('validate', 'generate'); graph.add_edge('generate', END); compiled_graph = graph.compile()