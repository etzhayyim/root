from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): seal_data: dict; valid: bool; def validate_seal(state: State): state['valid'] = 'material' in state['seal_data'] and 'pressure' in state['seal_data']; return {'valid': state['valid']}; graph = StateGraph(State); graph.add_node('validate', validate_seal); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()