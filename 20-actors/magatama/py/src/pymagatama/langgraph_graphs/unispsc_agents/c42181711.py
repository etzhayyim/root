from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EKGState(TypedDict): device_id: str; compliance_docs: List[str]; status: str; def validate_compliance(state: EKGState): return {'status': 'validated' if 'ISO_13485' in state['compliance_docs'] else 'failed'}; def process_transmission(state: EKGState): return {'status': 'ready_for_deployment'}; graph = StateGraph(EKGState); graph.add_node('validate', validate_compliance); graph.add_node('process', process_transmission); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); app = graph.compile()