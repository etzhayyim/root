from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): rod_specs: dict; status: str; def validate_grade(state: State): state['status'] = 'verified' if 'Material Grade' in state['rod_specs'] else 'failed'; return state; def check_compliance(state: State): state['status'] = 'approved' if state['status'] == 'verified' else 'rejected'; return state; graph = StateGraph(State); graph.add_node('validate', validate_grade); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()