from langgraph.graph import StateGraph, END; from typing import TypedDict; class DuctSpecState(TypedDict): material: str; pressure: int; valid: bool; def validate_duct(state: DuctSpecState): state['valid'] = state['pressure'] > 0 and state['material'] == 'steel'; return state; graph = StateGraph(DuctSpecState); graph.add_node('validate', validate_duct); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()