from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input: str; spec_validated: bool; def check_pressure(state: State): state['spec_validated'] = 'pressure' in state['input'].lower(); return state; graph = StateGraph(State); graph.add_node('check', check_pressure); graph.set_entry_point('check'); graph.add_edge('check', END); graph.compile()