from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): code: str; compliance_passed: bool; data: dict; def validate_compliance(state: ProcState): state['compliance_passed'] = 'GMP_Certification' in state['data']; return state; def check_purity(state: ProcState): state['compliance_passed'] = state['compliance_passed'] and (float(state['data'].get('Purity_Percentage', 0)) >= 99.0); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_compliance); graph.add_node('purity', check_purity); graph.add_edge('validate', 'purity'); graph.add_edge('purity', END); graph.set_entry_point('validate'); graph = graph.compile()