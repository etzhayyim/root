from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolBagState(TypedDict): items: List[dict]; valid: bool; def validate_specs(state: ToolBagState): state['valid'] = all('load_capacity' in item for item in state['items']); return state; def categorize(state: ToolBagState): return 'validated' if state['valid'] else 'rejected'; graph = StateGraph(ToolBagState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()