from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): unit_dimensions: dict; material_type: str; inspection_passed: bool; def validate_dimensions(state: State): return {'inspection_passed': state['unit_dimensions'].get('depth', 0) > 100}; def check_material(state: State): return {'inspection_passed': state['material_type'] in ['Granite', 'Marble']}; graph = StateGraph(State); graph.add_node('validate_dim', validate_dimensions); graph.add_node('check_mat', check_material); graph.set_entry_point('validate_dim'); graph.add_edge('validate_dim', 'check_mat'); graph.add_edge('check_mat', END); graph.compile()