from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): items: List[str]; safety_verified: bool; records: List[dict]; def validate_safety(state: State): return {'safety_verified': all('cert' in item.lower() for item in state['items'])}; def compile_procurement_graph(): builder = StateGraph(State); builder.add_node('validate', validate_safety); builder.add_edge('validate', END); builder.set_entry_point('validate'); return builder.compile(); graph = compile_procurement_graph()