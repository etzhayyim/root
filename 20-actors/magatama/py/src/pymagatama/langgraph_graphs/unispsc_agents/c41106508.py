from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcessState(TypedDict): kit_id: str; validation_status: bool; workflow_steps: List[str]; def validate_cells(state: ProcessState): state['validation_status'] = True; state['workflow_steps'].append('cell_line_check'); return state; def prepare_reagents(state: ProcessState): state['workflow_steps'].append('reagent_lot_verification'); return state; graph = StateGraph(ProcessState); graph.add_node('validate', validate_cells); graph.add_node('prep', prepare_reagents); graph.set_entry_point('validate'); graph.add_edge('validate', 'prep'); graph.add_edge('prep', END); graph = graph.compile()