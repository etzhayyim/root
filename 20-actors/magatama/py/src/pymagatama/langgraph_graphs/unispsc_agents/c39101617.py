from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LampState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_lamp(state: LampState): state['compliance'] = 'wattage_rating' in state['specs'] and 'mercury_content_mg' in state['specs']; state['approved'] = state['compliance']; return state; graph = StateGraph(LampState); graph.add_node('validate', validate_lamp); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()