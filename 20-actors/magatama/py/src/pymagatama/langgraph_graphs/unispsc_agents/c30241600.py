from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: list; status: str; def validate_engineering_specs(state: State): specs = state['input_data'].get('specs', {}); results = ['Load Check OK' if specs.get('load') > 500 else 'Load Check Fail']; return {'validation_results': results}; def update_status(state: State): return {'status': 'Validated' if all('OK' in r for r in state['validation_results']) else 'Rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_engineering_specs); graph.add_node('status', update_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'status'); graph.add_edge('status', END); compiled_graph = graph.compile()