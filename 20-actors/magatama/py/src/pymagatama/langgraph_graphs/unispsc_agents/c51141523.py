from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): input: str; validation_status: str; workflow_summary: str; def validate_pharma(state: AgentState): is_valid = 'purity' in state['input'].lower(); return {'validation_status': 'PASS' if is_valid else 'FAIL'}; def route_compliance(state: AgentState): return 'compliance' if state['validation_status'] == 'PASS' else END; graph = StateGraph(AgentState); graph.add_node('validate', validate_pharma); graph.add_node('compliance', lambda s: {'workflow_summary': 'Compliance check passed'}); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_compliance); graph.add_edge('compliance', END); graph = graph.compile()