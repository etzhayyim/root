from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): vendor: str; specs: dict; compliance: bool; graph = StateGraph(FurnitureState); def validate_specs(state: FurnitureState): return {'compliance': 'dimensions' in state['specs']}; def finalize_order(state: FurnitureState): return {'compliance': True}; graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()