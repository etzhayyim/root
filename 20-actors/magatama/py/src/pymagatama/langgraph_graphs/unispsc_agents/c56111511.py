from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OfficeProcureState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(OfficeProcureState); def validate_specs(state: OfficeProcureState): return {'validation_results': ['Specs Verified']}; def check_inventory(state: OfficeProcureState): return {'validation_results': ['Inventory Checked']}; graph.add_node('Validate', validate_specs); graph.add_node('Inventory', check_inventory); graph.set_entry_point('Validate'); graph.add_edge('Validate', 'Inventory'); graph.add_edge('Inventory', END); compiled_graph = graph.compile()