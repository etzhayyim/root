from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BalanceState(TypedDict): capacity: float; is_calibrated: bool; status: str; def validate_specs(state: BalanceState): return {'status': 'Validated' if state['capacity'] > 0 and state['is_calibrated'] else 'Failed'}; def finalize(state: BalanceState): return {'status': 'Ready for Procurement'}; graph = StateGraph(BalanceState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()