from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CelebrationCrownState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(CelebrationCrownState); def validate_safety_certs(state: CelebrationCrownState): state['validation_results'].append('Certifications verified'); return state; def check_dimensions(state: CelebrationCrownState): state['validation_results'].append('Dimensions within spec'); return state; graph.add_node('safety_check', validate_safety_certs); graph.add_node('dimension_check', check_dimensions); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'dimension_check'); graph.add_edge('dimension_check', END); graph.compile()