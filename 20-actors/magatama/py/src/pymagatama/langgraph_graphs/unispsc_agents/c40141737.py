from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DiaphragmState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; def validate_material(state: DiaphragmState): return {'validation_results': ['Material test passed']} if 'material' in state['specs'] else {'validation_results': ['Material missing']}; def validate_pressure(state: DiaphragmState): return {'validation_results': ['Pressure test passed']} if 'pressure' in state['specs'] else {'validation_results': ['Pressure missing']}; graph = StateGraph(DiaphragmState); graph.add_node('material_check', validate_material); graph.add_node('pressure_check', validate_pressure); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'pressure_check'); graph.add_edge('pressure_check', END); graph = graph.compile()