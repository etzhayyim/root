from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class mRNAState(TypedDict): kit_name: str; purity_check: bool; storage_validated: bool; def validate_purity(state: mRNAState): state['purity_check'] = True; return state; def validate_storage(state: mRNAState): state['storage_validated'] = True; return state; graph = StateGraph(mRNAState); graph.add_node('validate_purity', validate_purity); graph.add_node('validate_storage', validate_storage); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'validate_storage'); graph.add_edge('validate_storage', END); graph = graph.compile()