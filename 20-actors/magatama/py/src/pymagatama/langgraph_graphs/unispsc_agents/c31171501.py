from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): spec: dict; is_valid: bool; validation_log: List[str]; def validate_bearing(state: BearingState): state['is_valid'] = 'Load Rating' in state['spec'] and 'Material Grade' in state['spec']; state['validation_log'] = ['Validated specs'] if state['is_valid'] else ['Missing specs']; return state; builder = StateGraph(BearingState); builder.add_node('validate', validate_bearing); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()