from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WindowSpec(TypedDict): dimensions: dict; material: str; validated: bool; def validate_specs(state: WindowSpec): state['validated'] = all([state.get('dimensions'), state.get('material')]); return state; graph = StateGraph(WindowSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()