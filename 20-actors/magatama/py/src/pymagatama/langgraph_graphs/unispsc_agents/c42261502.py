from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AutopsyToolState(TypedDict): specs: dict; compliance_checked: bool; inspection_report: str; def validate_specs(state: AutopsyToolState): state['compliance_checked'] = 'ISO 7153-1' in state['specs'].get('standards', []); return state; def generate_report(state: AutopsyToolState): state['inspection_report'] = 'Validation complete' if state['compliance_checked'] else 'Fail'; return state; graph = StateGraph(AutopsyToolState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph.set_entry_point('validate'); graph = graph.compile()