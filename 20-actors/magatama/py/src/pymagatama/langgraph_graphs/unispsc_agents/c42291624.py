from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class MeniscotomeState(TypedDict): device_id: str; sterilization_status: bool; quality_check: bool; def validate_sterilization(state: MeniscotomeState) -> MeniscotomeState: state['sterilization_status'] = True; return state; def perform_qc(state: MeniscotomeState) -> MeniscotomeState: state['quality_check'] = True; return state; graph = StateGraph(MeniscotomeState); graph.add_node('validate', validate_sterilization); graph.add_node('qc', perform_qc); graph.add_edge('validate', 'qc'); graph.add_edge('qc', END); graph.set_entry_point('validate'); graph = graph.compile()