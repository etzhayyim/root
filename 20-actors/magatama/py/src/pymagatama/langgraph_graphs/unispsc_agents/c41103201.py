from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WasherState(TypedDict): washer_specs: dict; validation_results: List[str]; def validate_material(state: WasherState): material = state['washer_specs'].get('material'); status = 'Valid' if material in ['PTFE', 'Hastelloy', '316L'] else 'Invalid'; return {'validation_results': [f'Material check: {status}']}; def compliance_check(state: WasherState): return {'validation_results': state['validation_results'] + ['Compliance verified']}; graph = StateGraph(WasherState); graph.add_node('validate_material', validate_material); graph.add_node('compliance', compliance_check); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()