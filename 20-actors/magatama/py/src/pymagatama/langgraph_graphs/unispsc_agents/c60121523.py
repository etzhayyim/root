from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MarkerState(TypedDict): vendor_specs: dict; validation_report: list[str]; graph = StateGraph(MarkerState); def validate_ink_safety(state: MarkerState): return {'validation_report': ['Safety check passed']} if 'safety_cert' in state['vendor_specs'] else {'validation_report': ['Missing safety cert']}; graph.add_node('validate_safety', validate_ink_safety); graph.set_entry_point('validate_safety'); graph.add_edge('validate_safety', END); graph = graph.compile()