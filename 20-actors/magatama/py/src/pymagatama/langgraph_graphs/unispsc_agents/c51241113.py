from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; validation_passed: bool; storage_requirements: List[str]; def validate_purity(state: PharmState): state['validation_passed'] = True; return state; def check_compliance(state: PharmState): state['storage_requirements'] = ['2-8 degrees C', 'Light-protected']; return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()