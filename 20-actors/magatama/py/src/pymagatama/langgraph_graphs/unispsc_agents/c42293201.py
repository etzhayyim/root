from langgraph.graph import StateGraph, END; from typing import TypedDict; class SurgicalState(TypedDict): device_specs: dict; compliance_check: bool; graph = StateGraph(SurgicalState); def validate_medical_specs(state: SurgicalState): state['compliance_check'] = all(k in state['device_specs'] for k in ['material', 'sterility']); return state; def check_regulatory_approval(state: SurgicalState): return {'compliance_check': True} if state['compliance_check'] else {'compliance_check': False}; graph.add_node('validate', validate_medical_specs); graph.add_node('regulatory', check_regulatory_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()