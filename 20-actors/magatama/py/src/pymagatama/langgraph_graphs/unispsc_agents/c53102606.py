from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BathRobeState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: BathRobeState): state['approved'] = all(k in state['specs'] for k in ['gsm', 'material']); return state; graph = StateGraph(BathRobeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()