from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): data: dict; regulatory_compliant: bool; def check_regulatory_compliance(state: State): state['regulatory_compliant'] = 'GMP_cert' in state['data']; return state; def validate_fluoride_content(state: State): state['data']['content_validated'] = state['data'].get('ppm', 0) > 0; return state; graph = StateGraph(State); graph.add_node('regulatory_check', check_regulatory_compliance); graph.add_node('content_validation', validate_fluoride_content); graph.set_entry_point('regulatory_check'); graph.add_edge('regulatory_check', 'content_validation'); graph.add_edge('content_validation', END); graph = graph.compile()