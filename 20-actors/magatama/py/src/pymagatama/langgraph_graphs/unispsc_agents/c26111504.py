from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BeltState(TypedDict): belt_specs: dict; validation_results: List[str]; def validate_belt_specs(state: BeltState): results = []; if state['belt_specs'].get('tensile_strength', 0) < 500: results.append('Tensile strength too low'); return {'validation_results': results}; def generate_report(state: BeltState): print('Compliance output generated'); return {'validation_results': state['validation_results'] + ['Report Generated']}; graph = StateGraph(BeltState); graph.add_node('validate', validate_belt_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()