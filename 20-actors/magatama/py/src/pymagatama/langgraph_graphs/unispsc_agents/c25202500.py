from langgraph.graph import StateGraph, END; from typing import TypedDict; class AircraftState(TypedDict): doc_id: str; compliance_check: bool; ready: bool; def validate_specs(state: AircraftState): state['compliance_check'] = True; return state; def finalize_order(state: AircraftState): state['ready'] = True; return state; graph = StateGraph(AircraftState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()