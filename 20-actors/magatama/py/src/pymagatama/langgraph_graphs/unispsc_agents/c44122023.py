from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): content: str; validated: bool; def validate_board(state: GraphState): return {'validated': 'archboard' in state['content'].lower()}; graph = StateGraph(GraphState); graph.add_node('validate', validate_board); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()