from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): chemical_specs: dict; validation_results: List[str]; approved: bool; def validate_compliance(state: ProcurementState): state['validation_results'] = ['Safety Data Verification', 'Registration Check']; state['approved'] = True; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()