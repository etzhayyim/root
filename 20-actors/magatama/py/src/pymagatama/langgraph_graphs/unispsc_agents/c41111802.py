from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InspectionState(TypedDict): equipment_type: str; specs: dict; compliant: bool; def check_compliance(state: InspectionState): state['compliant'] = state['specs'].get('standard') == 'ASTM E1417'; return state; def validate_hazmat(state: InspectionState): state['compliant'] = state['compliant'] and state['specs'].get('flammability') == 'low'; return state; graph = StateGraph(InspectionState); graph.add_node('compliance', check_compliance); graph.add_node('hazmat', validate_hazmat); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'hazmat'); graph.add_edge('hazmat', END); graph = graph.compile()