from langgraph.graph import StateGraph, END; from typing import TypedDict; class BulbState(TypedDict): bulb_spec: dict; is_compliant: bool; def check_medical_grade(state: BulbState): state['is_compliant'] = 'iso_certified' in state['bulb_spec']; return state; def validate_specs(state: BulbState): return 'compliant' if state['is_compliant'] else 'reject'; graph = StateGraph(BulbState); graph.add_node('validate', check_medical_grade); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()