from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SauceState(TypedDict): sauce_type: str; ingredients: List[str]; safety_verified: bool; def validate_ingredients(state: SauceState): state['safety_verified'] = 'prohibited_additive' not in state['ingredients']; return state; def finalize_procurement(state: SauceState): return {'status': 'approved' if state['safety_verified'] else 'rejected'}; graph = StateGraph(SauceState); graph.add_node('validate', validate_ingredients); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()