from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SteeringState(TypedDict): specs: List[str]; validated: bool; graph = StateGraph(SteeringState); def validate_specs(state: SteeringState): state['validated'] = all(len(s) > 0 for s in state['specs']); return state; def check_compliance(state: SteeringState): print('Compliance check initiated'); return state; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compile_graph = graph.compile()