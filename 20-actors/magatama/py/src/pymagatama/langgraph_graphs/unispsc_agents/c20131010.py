from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): component_id: str; specs: dict; validation_status: str; audit_logs: Annotated[Sequence[str], operator.add]; def validate_robot_component(state: RobotState) -> RobotState: payload = state['specs'].get('payload', 0); if payload > 500: return {**state, 'validation_status': 'REQUIRES_EXPERT_REVIEW'}; return {**state, 'validation_status': 'PASS'}; def finalize_procurement(state: RobotState) -> RobotState: return {**state, 'audit_logs': ['Component verified and approved']}; graph = StateGraph(RobotState); graph.add_node('validate', validate_robot_component); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()