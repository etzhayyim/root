from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProbeState(TypedDict): spec: dict; approved: bool; def validate_specs(state: ProbeState): state['approved'] = 'sterilization_method' in state['spec'] and state['spec']['probe_diameter_mm'] > 0; return state; graph = StateGraph(ProbeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()