from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity: float; ready_for_shipping: bool; def validate_purity(state: PharmState): state['ready_for_shipping'] = state['purity'] >= 99.0; return state; def check_gs1(state: PharmState): print(f'Validating GS1 compliance for {state['chemical_name']}'); return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_gs1); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()