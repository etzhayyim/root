from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BatchState(TypedDict): items: List[dict]; status: str; def validate_quality(state: BatchState): for item in state['items']: if item.get('brix', 0) < 10: item['status'] = 'rejected' else: item['status'] = 'approved'; return state; graph = StateGraph(BatchState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()