from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class TapeSpecState(TypedDict): tape_type: str; is_sterile: bool; specs_valid: bool; def validate_specs(state: TapeSpecState): state['specs_valid'] = state['is_sterile'] and 'latex-free' in state['tape_type'].lower(); return state; graph = StateGraph(TapeSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()