from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity_check: bool; regulatory_compliance: bool; def check_purity(state: PharmState): return {'purity_check': True}; def verify_compliance(state: PharmState): return {'regulatory_compliance': True}; graph = StateGraph(PharmState); graph.add_node('purity', check_purity); graph.add_node('regulatory', verify_compliance); graph.set_entry_point('purity'); graph.add_edge('purity', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()