from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): chemical_name: str; purity: float; compliant: bool; def check_purity(state: ProcState): state['compliant'] = state['purity'] >= 99.0; return state; def validate_msds(state: ProcState): state['compliant'] = state['compliant'] and True; return state; graph = StateGraph(ProcState); graph.add_node('check_purity', check_purity); graph.add_node('validate_msds', validate_msds); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'validate_msds'); graph.add_edge('validate_msds', END); graph = graph.compile()