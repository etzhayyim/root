from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FreezerState(TypedDict): safety_check: bool; specs: dict; status: str; def validate_specs(state: FreezerState): return {'safety_check': state['specs'].get('explosion_proof', False)}; def finalize_procurement(state: FreezerState): return {'status': 'Approved' if state['safety_check'] else 'Rejected'}; graph = StateGraph(FreezerState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()