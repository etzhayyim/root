from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BacteriaSuppliesState(TypedDict): supply_type: str; is_sterile: bool; validation_log: List[str]; def validate_supplies(state: BacteriaSuppliesState): return {'validation_log': state['validation_log'] + ['Checking sterility and purity standards']}; def check_storage(state: BacteriaSuppliesState): return {'validation_log': state['validation_log'] + ['Verifying cold chain compliance']}; graph = StateGraph(BacteriaSuppliesState); graph.add_node('validate', validate_supplies); graph.add_node('storage', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); app = graph.compile()