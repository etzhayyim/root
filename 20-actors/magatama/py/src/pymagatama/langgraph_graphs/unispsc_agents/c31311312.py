from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_spec(state: PipeState): state['valid'] = 'pressure_rating' in state['specs'] and 'material_grade' in state['specs']; return state; builder = StateGraph(PipeState); builder.add_node('validate', validate_spec); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()