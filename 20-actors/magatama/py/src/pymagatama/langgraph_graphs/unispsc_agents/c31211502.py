from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaintState(TypedDict): vendor_specs: dict; compliance_check: bool; needs_lab_test: bool; def validate_paint(state: PaintState): voc = state['vendor_specs'].get('voc_content', 0); return {'compliance_check': voc < 250, 'needs_lab_test': voc > 100}; def routing_logic(state: PaintState): return 'lab_test' if state['needs_lab_test'] else END; graph = StateGraph(PaintState); graph.add_node('validate', validate_paint); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()