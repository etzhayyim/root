from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CleanerState(TypedDict): chemical_report: str; safety_compliance: bool; approval_required: bool; def validate_sds(state: CleanerState): return {'safety_compliance': 'SDS' in state['chemical_report']}; def check_hazard(state: CleanerState): return {'approval_required': True}; graph = StateGraph(CleanerState); graph.add_node('validate_sds', validate_sds); graph.add_node('check_hazard', check_hazard); graph.set_entry_point('validate_sds'); graph.add_edge('validate_sds', 'check_hazard'); graph.add_edge('check_hazard', END); compiled_graph = graph.compile()