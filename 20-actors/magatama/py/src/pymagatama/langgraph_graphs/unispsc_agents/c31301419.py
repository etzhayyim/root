from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): material_spec: str; dimensions: dict; validation_passed: bool; def check_specs(state: ForgingState): state['validation_passed'] = bool(state.get('material_spec') and state.get('dimensions')); return state; def route_logic(state: ForgingState): return 'complete' if state['validation_passed'] else 'flag_error'; graph = StateGraph(ForgingState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()