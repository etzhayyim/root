from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): material_spec: str; certification: List[str]; approved: bool; def validate_metallurgy(state: ProcState): state['approved'] = 'Hastelloy X' in state['material_spec'] and len(state['certification']) > 0; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_metallurgy); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()