from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TattooState(TypedDict): design_file: str; safety_cert: bool; approved: bool; def check_dermatology(state: TattooState): return {'approved': state['safety_cert']}; graph = StateGraph(TattooState); graph.add_node('verify_safety', check_dermatology); graph.set_entry_point('verify_safety'); graph.add_edge('verify_safety', END); app = graph.compile()