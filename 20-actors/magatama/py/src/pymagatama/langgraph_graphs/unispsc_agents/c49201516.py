from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class FitnessEquipmentState(TypedDict): equipment_type: str; specs: dict; validation_passed: bool; def validate_equipment(state: FitnessEquipmentState): state['validation_passed'] = 'weight_capacity' in state['specs'] and 'safety_cert' in state['specs']; return state; graph = StateGraph(FitnessEquipmentState); graph.add_node('validate', validate_equipment); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()