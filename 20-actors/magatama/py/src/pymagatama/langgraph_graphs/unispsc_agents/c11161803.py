from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): chemical_spec: dict; validation_results: Annotated[Sequence[str], operator.add]; def validate_purity(state: AgentState): return {'validation_results': ['Purity validated']}; def check_safety(state: AgentState): return {'validation_results': ['Safety protocols met']}; graph = StateGraph(AgentState); graph.add_node('purity', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('purity'); graph.add_edge('purity', 'safety'); graph.add_edge('safety', END); graph = graph.compile()