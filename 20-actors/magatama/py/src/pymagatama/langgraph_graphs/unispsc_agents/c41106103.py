from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DNAKitState(TypedDict): kit_id: str; quality_metrics: dict; compliant: bool; def validate_dna_kit(state: DNAKitState): state['compliant'] = state['quality_metrics'].get('purity_score', 0) > 0.95; return state; graph = StateGraph(DNAKitState); graph.add_node('validate', validate_dna_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()