from langgraph.graph import StateGraph, END; from typing import TypedDict; class RiotGearState(TypedDict): vendor_certs: list; safety_compliant: bool; graph = StateGraph(RiotGearState); def validate_compliance(state: RiotGearState): return {'safety_compliant': 'NIJ-0104.02' in state['vendor_certs']}; def route_procurement(state: RiotGearState): return 'approval' if state['safety_compliant'] else 'reject'; graph.add_node('validate', validate_compliance); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()