from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MissileSpecState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: MissileSpecState): errs = []; required = ['itar_compliance', 'payload_limit']; for r in required: if r not in state['specs']: errs.append(f'Missing {r}'); return {'validation_errors': errs, 'approved': len(errs) == 0}; graph = StateGraph(MissileSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()