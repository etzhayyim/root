from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PastaState(TypedDict): pasta_type: str; is_compliant: bool; inspection_result: str; def validate_specs(state: PastaState): return {'is_compliant': state.get('pasta_type') in ['dry', 'fresh']}; def check_safety(state: PastaState): return {'inspection_result': 'passed' if state.get('is_compliant') else 'failed'}; graph = StateGraph(PastaState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()