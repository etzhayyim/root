from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): tube_specs: dict; validation_results: List[str]; def validate_weld(state: AssemblyState): results = []; if state['tube_specs'].get('pressure_rating', 0) < 500: results.append('Pressure rating below threshold'); return {'validation_results': results}; def verify_compliance(state: AssemblyState): return {'validation_results': state['validation_results'] + ['ISO_compliance_verified']}; graph = StateGraph(AssemblyState); graph.add_node('weld_check', validate_weld); graph.add_node('compliance', verify_compliance); graph.set_entry_point('weld_check'); graph.add_edge('weld_check', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()