from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SealState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: SealState): state['valid'] = 'Pressure Rating PSI' in state['specs'] and state['specs']['Pressure Rating PSI'] > 0; state['log'].append('Validation complete'); return state; graph = StateGraph(SealState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()