from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeSpec(TypedDict): tube_id: str; weld_cert: bool; pressure_test: float; def validate_weld(state: TubeSpec) -> dict: return {"weld_cert": True} if state["pressure_test"] > 1000 else {"weld_cert": False}; def finalize_assembly(state: TubeSpec) -> dict: return {"status": "APPROVED" if state["weld_cert"] else "REJECTED"}; graph = StateGraph(TubeSpec); graph.add_node("validate", validate_weld); graph.add_node("finalize", finalize_assembly); graph.set_entry_point("validate"); graph.add_edge("validate", "finalize"); graph.add_edge("finalize", END); graph = graph.compile()