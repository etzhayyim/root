from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): specs: dict; compliance_verified: bool; def validate_iso_cert(state: SurgicalState): state['compliance_verified'] = 'ISO_13485' in state['specs']; return state; def check_material(state: SurgicalState): return {'compliance_verified': state['specs'].get('material') == 'medical_grade'}; graph = StateGraph(SurgicalState); graph.add_node('validate_iso', validate_iso_cert); graph.add_node('check_material', check_material); graph.set_entry_point('validate_iso'); graph.add_edge('validate_iso', 'check_material'); graph.add_edge('check_material', END); graph = graph.compile()