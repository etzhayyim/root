from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): ink_type: str; model_match: bool; volume: int; graph = StateGraph(ProcurementState); def validate_compatibility(state: ProcurementState): state['model_match'] = len(state['ink_type']) > 0; return state; graph.add_node('validate', validate_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()