from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProtocolState(TypedDict): input_data: dict; validation_errors: Annotated[list[str], operator.add]; status: str; def validate_specs(state: ProtocolState): errors = []; specs = state['input_data'].get('specs', {}); if 'max_weight' not in specs: errors.append('Missing mandatory weight capacity'); return {'validation_errors': errors}; def finalize_procurement(state: ProtocolState): return {'status': 'READY_FOR_ORDER' if not state['validation_errors'] else 'REJECTED'}; graph = StateGraph(ProtocolState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()