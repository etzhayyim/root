from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): batch_id: str; purity: float; compliant: bool; def validate_purity(state: PharmState): return {'compliant': state['purity'] >= 99.0}; def check_safety(state: PharmState): return {'compliant': state['compliant'] and True}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()