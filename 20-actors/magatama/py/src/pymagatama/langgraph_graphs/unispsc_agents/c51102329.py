from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class APIState(TypedDict): content: str; validation_status: bool; compliance_report: str; def validate_api(state: APIState): return {'validation_status': 'purity' in state['content'].lower(), 'compliance_report': 'GMP verified' if state['validation_status'] else 'Pending'}; graph = StateGraph(APIState); graph.add_node('validate', validate_api); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()