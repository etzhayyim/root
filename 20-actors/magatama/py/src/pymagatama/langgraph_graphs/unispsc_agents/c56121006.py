from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BenchProcurementState(TypedDict): vendor_specs: dict; compliance_check: bool; final_approval: bool; def validate_specs(state: BenchProcurementState): return {'compliance_check': state['vendor_specs'].get('fire_rating') == 'compliant'}; def approve_workflow(state: BenchProcurementState): return {'final_approval': True}; graph = StateGraph(BenchProcurementState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()