from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_report: List[str]; approved: bool; def validate_poncho(state: ProcurementState): spec = state['specs']; errors = []; if spec.get('waterproof_rating', 0) < 5000: errors.append('Insufficient waterproof rating'); return {'validation_report': errors, 'approved': len(errors) == 0}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_poncho); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()