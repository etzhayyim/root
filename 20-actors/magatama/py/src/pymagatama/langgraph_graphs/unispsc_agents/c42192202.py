from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GurneyState(TypedDict): specs: dict; compliance_status: bool; def validate_specs(state: GurneyState): is_valid = state['specs'].get('weight_capacity_kg', 0) > 150; return {'compliance_status': is_valid}; def route_procurement(state: GurneyState): return 'compliance_check' if state['compliance_status'] else END; graph = StateGraph(GurneyState); graph.add_node('compliance_check', validate_specs); graph.set_entry_point('compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()