from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MeasurementState(TypedDict): source_type: str; is_certified: bool; calibration_valid: bool; workflow_log: List[str]; def check_radiation_source(state): return {'workflow_log': state['workflow_log'] + ['Validating radioactive source credentials...']}; def validate_safety_compliance(state): return {'workflow_log': state['workflow_log'] + ['Verifying safety interlocks and licensing...']}; graph = StateGraph(MeasurementState); graph.add_node('source_check', check_radiation_source); graph.add_node('safety_check', validate_safety_compliance); graph.add_edge('source_check', 'safety_check'); graph.add_edge('safety_check', END); graph.set_entry_point('source_check'); graph = graph.compile()