from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ThimbleState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: ThimbleState): required = ['material', 'diameter']; valid = all(k in state['specs'] for k in required); return {'valid': valid, 'error_log': [] if valid else ['Missing technical specs']}; graph = StateGraph(ThimbleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()