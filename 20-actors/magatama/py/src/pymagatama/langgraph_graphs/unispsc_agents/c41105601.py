from langgraph.graph import StateGraph, END; from typing import TypedDict; class SequencingState(TypedDict): kit_type: str; temperature_check: bool; validation_status: bool; def validate_reagent(state: SequencingState): state['validation_status'] = state['temperature_check'] and (state['kit_type'] in ['DNA', 'RNA']); return state; graph = StateGraph(SequencingState); graph.add_node('validate', validate_reagent); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()