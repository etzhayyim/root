from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class LearningResourceState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_spec(state: LearningResourceState): state['validation_results'].append('Certification verified'); return {'status': 'validated'}; def compile_workflow(): graph = StateGraph(LearningResourceState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_workflow()