from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BathLiftState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: BathLiftState): specs = state['specs']; is_valid = specs.get('weight_capacity', 0) > 0 and 'ip_rating' in specs; return {'valid': is_valid, 'error': '' if is_valid else 'Invalid specs'}; def finalize_check(state: BathLiftState): return {'result': 'Ready for procurement'}; graph = StateGraph(BathLiftState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_check); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()