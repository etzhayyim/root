from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def analyze_specs(state: CastingState): state['validation_results'].append('Verifying material grade and dimensional tolerance...'); return state; def check_compliance(state: CastingState): state['validation_results'].append('Checking export compliance...'); return state; graph = StateGraph(CastingState); graph.add_node('analyzer', analyze_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('analyzer'); graph.add_edge('analyzer', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()