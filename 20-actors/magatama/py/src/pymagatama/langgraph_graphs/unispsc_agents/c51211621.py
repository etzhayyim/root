from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PralidoximeState(TypedDict): chemical_data: dict; validation_report: list; status: str; def validate_chem(state: PralidoximeState): print('Validating purity and purity certificates...'); return {'validation_report': ['Purity verified'], 'status': 'Validated'}; def check_compliance(state: PralidoximeState): print('Checking DEA/chemical control compliance...'); return {'status': 'Compliant'}; graph = StateGraph(PralidoximeState); graph.add_node('validate', validate_chem); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()