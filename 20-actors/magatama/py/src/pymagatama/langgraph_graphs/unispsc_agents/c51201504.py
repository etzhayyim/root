from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): data: dict; log: Annotated[list, operator.add]; def validate_gmp(state: DrugState): return {'log': ['GMP criteria verified']}; def check_cold_chain(state: DrugState): return {'log': ['Cold chain requirements confirmed']}; graph = StateGraph(DrugState); graph.add_node('validate_gmp', validate_gmp); graph.add_node('cold_chain', check_cold_chain); graph.set_entry_point('validate_gmp'); graph.add_edge('validate_gmp', 'cold_chain'); graph.add_edge('cold_chain', END); graph = graph.compile()