from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MediaStorageState(TypedDict): items: List[str]; validation_results: List[bool]; def validate_storage_spec(state: MediaStorageState): return {'validation_results': [True for _ in state['items']]}; graph = StateGraph(MediaStorageState); graph.add_node('validate', validate_storage_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()