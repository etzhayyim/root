from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): chemical_data: dict; validation_report: list; def validate_hazmat(state: State): report = []; data = state['chemical_data']; if data.get('hazmat_class') != '2.1': report.append('Hazard classification mismatch'); return {'validation_report': report}; def check_purity(state: State): report = state['validation_report']; if state['chemical_data'].get('purity', 0) < 99.0: report.append('Low purity level'); return {'validation_report': report}; graph = StateGraph(State); graph.add_node('validate_hazmat', validate_hazmat); graph.add_node('check_purity', check_purity); graph.set_entry_point('validate_hazmat'); graph.add_edge('validate_hazmat', 'check_purity'); graph.add_edge('check_purity', END); graph = graph.compile()