from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): code: str; specs: dict; approved: bool; def validate_specs(state: ProcureState): state['approved'] = state['specs'].get('load_capacity', 0) > 500; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()