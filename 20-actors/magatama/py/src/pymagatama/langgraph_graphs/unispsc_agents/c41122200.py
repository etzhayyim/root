from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CrucibleState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(CrucibleState); def validate_temp_rating(state: CrucibleState): if state['specs'].get('max_temp', 0) < 1000: return {'validation_report': ['Low heat threshold for industrial crucible']}; return {'validation_report': ['Temp rating verified']}; graph.add_node('validate', validate_temp_rating); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()