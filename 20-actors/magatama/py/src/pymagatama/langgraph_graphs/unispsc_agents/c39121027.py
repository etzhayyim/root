from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TransformerState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: TransformerState): errors = []; if not state['specs'].get('power_rating'): errors.append('Missing Power Rating'); return {'validation_errors': errors}; def check_safety(state: TransformerState): status = len(state['validation_errors']) == 0; return {'approved': status}; graph = StateGraph(TransformerState); graph.add_node('validate', validate_specs); graph.add_node('safety_check', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()