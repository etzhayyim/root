from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GrainState(TypedDict): grain_type: str; moisture: float; quality_passed: bool; def check_quality(state: GrainState): return {'quality_passed': state['moisture'] < 14.0}; def finalize_order(state: GrainState): return {'quality_passed': True}; graph = StateGraph(GrainState); graph.add_node('check', check_quality); graph.add_node('finalize', finalize_order); graph.set_entry_point('check'); graph.add_edge('check', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()