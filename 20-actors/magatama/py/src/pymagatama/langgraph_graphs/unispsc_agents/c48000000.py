from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ServiceState(TypedDict): items: Annotated[list, operator.add]; compliance_score: int; def validate_equipment(state: ServiceState): print('Validating machine specs...'); state['compliance_score'] = 100; return state; graph = StateGraph(ServiceState); graph.add_node('validate_specs', validate_equipment); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', END); graph = graph.compile()