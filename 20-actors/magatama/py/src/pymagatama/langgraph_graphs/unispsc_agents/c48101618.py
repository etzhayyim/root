from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DishPartState(TypedDict): part_id: str; specs: dict; verified: bool; def validate_specs(state: DishPartState): state['verified'] = all(k in state['specs'] for k in ['material', 'temp_rating']); return state; def check_compatibility(state: DishPartState): state['verified'] = state['verified'] and state.get('part_id', '').startswith('OEM-'); return state; graph = StateGraph(DishPartState); graph.add_node('validate', validate_specs); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()