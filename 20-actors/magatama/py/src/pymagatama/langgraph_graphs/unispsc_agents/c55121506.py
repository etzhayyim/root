from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PriceTagState(TypedDict): tags: List[str]; validated: bool; def validate_specs(state: PriceTagState): state['validated'] = all(len(t) > 0 for t in state['tags']); return state; graph = StateGraph(PriceTagState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()