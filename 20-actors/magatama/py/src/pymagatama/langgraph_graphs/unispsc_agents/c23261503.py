from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class LOMState(TypedDict): model_specs: dict; export_compliance: bool; machine_id: str; def validate_compliance(state: LOMState): state['export_compliance'] = True; return state; def check_specs(state: LOMState): state['model_specs']['validated'] = True; return state; graph = StateGraph(LOMState); graph.add_node('compliance', validate_compliance); graph.add_node('specs', check_specs); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'specs'); graph.add_edge('specs', END); compiled_graph = graph.compile()