from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): instrument_id: str; compliance_checked: bool; def validate_spec(state: State): return {'compliance_checked': True}; def update_records(state: State): return {'instrument_id': state['instrument_id']}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('record', update_records); graph.set_entry_point('validate'); graph.add_edge('validate', 'record'); graph.add_edge('record', END); app = graph.compile()