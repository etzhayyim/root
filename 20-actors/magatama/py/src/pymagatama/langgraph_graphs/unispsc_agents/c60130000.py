from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class InstrumentState(TypedDict): instrument_data: dict; verification_status: str; def validate_instrument(state: InstrumentState): return {'verification_status': 'verified' if 'material' in state['instrument_data'] else 'rejected'}; def check_compliance(state: InstrumentState): return {'verification_status': 'compliant' if state['verification_status'] == 'verified' else 'non-compliant'}; graph = StateGraph(InstrumentState); graph.add_node('validate', validate_instrument); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()