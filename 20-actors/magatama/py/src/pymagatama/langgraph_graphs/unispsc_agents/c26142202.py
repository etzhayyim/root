from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AnalysisState(TypedDict): data: dict; safety_check_passed: bool; compliance_docs: List[str]; def validate_safety(state: AnalysisState): state['safety_check_passed'] = 'Radiation_Shielding' in state['data']; return state; def check_compliance(state: AnalysisState): state['compliance_docs'].append('IAEA_Standard_Verified'); return state; graph = StateGraph(AnalysisState); graph.add_node('validate', validate_safety); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()