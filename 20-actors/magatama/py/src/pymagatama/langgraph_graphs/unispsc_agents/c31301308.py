from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: ForgingState): state['validation_results'].append('Material Grade Validated'); return state; def validate_geometry(state: ForgingState): state['validation_results'].append('Geometry/Tolerance Check Passed'); return state; graph = StateGraph(ForgingState); graph.add_node('material_check', validate_materials); graph.add_node('geometry_check', validate_geometry); graph.add_edge('material_check', 'geometry_check'); graph.add_edge('geometry_check', END); graph.set_entry_point('material_check'); compile_graph = graph.compile()