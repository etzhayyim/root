from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: CastingState): state['validation_results'].append('Material chemistry verified'); return state; def check_dimensions(state: CastingState): state['validation_results'].append('Tolerance check passed'); return state; graph = StateGraph(CastingState); graph.add_node('MaterialValidation', validate_materials); graph.add_node('DimensionalCheck', check_dimensions); graph.set_entry_point('MaterialValidation'); graph.add_edge('MaterialValidation', 'DimensionalCheck'); graph.add_edge('DimensionalCheck', END); graph = graph.compile()