from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class TideClockState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: TideClockState): errors = []; if state['specs'].get('water_resistance', 0) < 5: errors.append('Insufficient water resistance'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(TideClockState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()