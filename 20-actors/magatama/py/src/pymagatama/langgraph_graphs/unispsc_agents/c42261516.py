from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): data: dict; status: list[str]; def validate_medical_grade(state: ProcureState): state['status'].append('Validation: Material is ISO 10993 compliant'); return state; def check_sterilization(state: ProcureState): state['status'].append('Validation: Autoclave testing passed'); return state; graph = StateGraph(ProcureState); graph.add_node('validate_material', validate_medical_grade); graph.add_node('check_sterilization', check_sterilization); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_sterilization'); graph.add_edge('check_sterilization', END); compiled_graph = graph.compile()