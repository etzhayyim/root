from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SlipRingState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SlipRingState): errors = []; if state['specs'].get('voltage', 0) > 600: errors.append('High voltage audit required'); return {'validation_errors': errors}; def check_approval(state: SlipRingState): return 'approved' if not state['validation_errors'] else 'rejected'; graph = StateGraph(SlipRingState); graph.add_node('validator', validate_specs); graph.add_edge('validator', END); graph.set_entry_point('validator'); graph = graph.compile()