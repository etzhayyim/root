from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): data: dict; status: str; def validate_dimensions(state: State): state['status'] = 'COMPLIANT' if state['data'].get('width') > 0 else 'INVALID'; return state; graph = StateGraph(State); graph.add_node('validate', validate_dimensions); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()