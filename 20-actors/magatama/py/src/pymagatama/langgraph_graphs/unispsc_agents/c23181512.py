from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; valid: bool; error: str; def validate_actuator(state: State): spec = state['input_data']; return {'valid': all(k in spec for k in ['load', 'stroke']), 'error': 'Missing specs' if not all(k in spec for k in ['load', 'stroke']) else ''}; graph = StateGraph(State); graph.add_node('validate', validate_actuator); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()