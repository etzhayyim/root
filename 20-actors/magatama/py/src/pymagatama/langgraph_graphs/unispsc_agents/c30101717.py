from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ConcreteState(TypedDict): specs: dict; valid: bool;  def validate_beam(state: ConcreteState): specs = state['specs']; state['valid'] = specs.get('compressive_strength_mpa', 0) >= 30; return state; graph = StateGraph(ConcreteState); graph.add_node('validate', validate_beam); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()