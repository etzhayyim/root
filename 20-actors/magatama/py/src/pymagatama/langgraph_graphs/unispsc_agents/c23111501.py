from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class HydraulicSpecState(TypedDict): spec: dict; validated: bool; def validate_specs(state: HydraulicSpecState): state['validated'] = state['spec'].get('pressure_mpa', 0) > 0; return state; graph = StateGraph(HydraulicSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()