from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): input_spec: dict; validation_report: list; status: str; def validate_bath_specs(state: GraphState): spec = state['input_spec']; errors = []; if spec.get('temp_range') is None: errors.append('Temperature range missing'); return {'validation_report': errors, 'status': 'valid' if not errors else 'invalid'}; graph = StateGraph(GraphState); graph.add_node('validate', validate_bath_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()