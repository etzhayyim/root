from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SlaveCylinderState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(SlaveCylinderState); def validate_specs(state: SlaveCylinderState): results = []; if state['specs'].get('bore_diameter', 0) <= 0: results.append('Invalid bore diameter'); return {'validation_results': results}; def check_compatibility(state: SlaveCylinderState): return {'validation_results': state['validation_results'] + ['Compatibility check passed']}; graph.add_node('validate', validate_specs); graph.add_node('compatibility', check_compatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph = graph.compile()