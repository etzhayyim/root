from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GaitState(TypedDict): device_type: str; specs: dict; compliance_verified: bool; def validate_specs(state: GaitState): return {'compliance_verified': state['specs'].get('load_capacity_kg', 0) > 0}; def check_health_reg(state: GaitState): return {'compliance_verified': state['specs'].get('ISO_13485_certification', False)}; graph = StateGraph(GaitState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_health_reg); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()