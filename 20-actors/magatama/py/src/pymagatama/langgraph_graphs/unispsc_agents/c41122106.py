from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class InoculatorState(TypedDict): items: Annotated[list, operator.add]; status: str; def validate_specs(state: InoculatorState): return {'status': 'READY' if all(i.get('sterile') for i in state['items']) else 'REJECTED'}; def quality_check(state: InoculatorState): return {'status': 'PASSED'}; graph = StateGraph(InoculatorState); graph.add_node('validate', validate_specs); graph.add_node('quality', quality_check); graph.add_edge('validate', 'quality'); graph.add_edge('quality', END); graph.set_entry_point('validate'); graph = graph.compile()