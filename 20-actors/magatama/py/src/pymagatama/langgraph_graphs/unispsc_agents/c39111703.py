from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: ProcurementState): specs = state['specs']; errors = []; if specs.get('IP_rating', 0) < 65: errors.append('Insufficient IP rating for storm use'); return {'validation_errors': errors}; def finalize_procurement(state: ProcurementState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()