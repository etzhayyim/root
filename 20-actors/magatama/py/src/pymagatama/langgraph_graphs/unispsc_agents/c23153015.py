from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class MotorState(TypedDict): specs: dict; valid: bool; error: str; def validate_motor(state: MotorState): specs = state['specs']; state['valid'] = 'rated_power_kw' in specs and 'voltage_rating' in specs; return state; graph = StateGraph(MotorState); graph.add_node('validate', validate_motor); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()