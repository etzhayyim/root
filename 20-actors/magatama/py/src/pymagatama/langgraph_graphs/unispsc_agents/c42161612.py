from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialyzerState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def validate_specs(state: DialyzerState): results = []; required = ['Sterilization Method', 'ISO 10993']; for field in required: if field not in state['specs']: results.append(f'Missing: {field}'); return {'validation_results': results, 'approved': len(results) == 0}; graph = StateGraph(DialyzerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()