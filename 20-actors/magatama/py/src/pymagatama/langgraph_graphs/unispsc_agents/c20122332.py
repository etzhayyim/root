from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input_spec: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_unit(state: ProcureState): return {'validation_results': [f'Check_Accuracy_{state["input_spec"].get("accuracy", 0)}', 'Check_Material_Cert_Valid']}; def finalize_procurement(state: ProcureState): return {'status': 'READY_FOR_RFQ'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_unit); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()