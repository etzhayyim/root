from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): tool_data: dict; validation_status: str; def validate_spec(state: State): state['validation_status'] = 'verified' if 'material' in state['tool_data'] else 'incomplete'; return state; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()