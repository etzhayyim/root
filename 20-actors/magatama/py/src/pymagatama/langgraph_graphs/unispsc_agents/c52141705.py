from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): model_id: str; parts_list: List[str]; validation_results: dict; graph = StateGraph(State); def validate_compatibility(state: State): state['validation_results'] = {'passed': True}; return state; def check_stock(state: State): return state; graph.add_node('validate', validate_compatibility); graph.add_node('check_inventory', check_stock); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_inventory'); graph.add_edge('check_inventory', END); graph = graph.compile()