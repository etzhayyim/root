from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BerylliumState(TypedDict): component_id: str; purity_level: float; export_verified: bool; compliance_status: List[str]; def validate_export(state: BerylliumState): state['export_verified'] = True; return state; def check_purity(state: BerylliumState): state['compliance_status'].append('Purity Verified' if state['purity_level'] > 0.99 else 'Failed'); return state; graph = StateGraph(BerylliumState); graph.add_node('export', validate_export); graph.add_node('purity', check_purity); graph.set_entry_point('export'); graph.add_edge('export', 'purity'); graph.add_edge('purity', END); graph = graph.compile()