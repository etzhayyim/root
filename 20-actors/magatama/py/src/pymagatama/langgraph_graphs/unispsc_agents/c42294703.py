from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): commodity: str; compliance_passed: bool; def validate_medical_device(state: State): return {'compliance_passed': True} if '42294703' in state['commodity'] else {'compliance_passed': False}; graph = StateGraph(State); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()