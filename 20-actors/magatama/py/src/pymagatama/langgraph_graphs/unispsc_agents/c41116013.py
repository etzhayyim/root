from langgraph.graph import StateGraph, END; from typing import TypedDict; class ReagentState(TypedDict): reagent_id: str; safety_check: bool; compliance_status: str; def validate_reagent(state: ReagentState): return {'safety_check': True, 'compliance_status': 'verified'} if state['reagent_id'] else {'safety_check': False, 'compliance_status': 'failed'}; def process_documentation(state: ReagentState): return {'compliance_status': 'certified_for_use'}; graph = StateGraph(ReagentState); graph.add_node('validate', validate_reagent); graph.add_node('certify', process_documentation); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); graph = graph.compile()