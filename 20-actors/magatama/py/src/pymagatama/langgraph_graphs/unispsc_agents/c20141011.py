from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): component_specs: dict; validation_report: List[str]; def validate_load(state: State): specs = state['component_specs']; status = 'Pass' if specs.get('load', 0) > 0 else 'Fail'; return {'validation_report': [f'Load validation: {status}']}; graph = StateGraph(State); graph.add_node('validate', validate_load); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()