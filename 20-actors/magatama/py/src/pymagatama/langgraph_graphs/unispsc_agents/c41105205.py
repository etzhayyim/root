from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MicroslideState(TypedDict): progress: Annotated[list, operator.add]; status: str; def validate_spec(state: MicroslideState): return {'progress': ['spec_validated']}; def execute_assembly(state: MicroslideState): return {'progress': ['assembly_completed']}; graph = StateGraph(MicroslideState); graph.add_node('validate', validate_spec); graph.add_node('assemble', execute_assembly); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph.compile()