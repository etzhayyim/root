from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): device_type: str; safety_checks: List[str]; approved: bool; def validate_specs(state: GraphState): state['safety_checks'].append('ISO-13485-checked'); return {'approved': True} if 'ISO-13485-checked' in state['safety_checks'] else {'approved': False}; graph = StateGraph(GraphState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()