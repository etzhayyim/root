from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): spec: dict; validated: bool; state = State(spec={}, validated=False); def validate_thermal_specs(state: State): state['validated'] = state['spec'].get('temperature_rating', 0) > 500; return state; graph = StateGraph(State); graph.add_node('validate', validate_thermal_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()