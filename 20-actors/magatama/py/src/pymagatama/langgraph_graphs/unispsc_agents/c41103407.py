from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): specs: dict; validation_report: str; def validate_chamber_specs(state: AgentState): specs = state.get('specs', {}); status = 'Valid' if all(k in specs for k in ['material', 'o2_level']) else 'Invalid'; return {'validation_report': status}; graph = StateGraph(AgentState); graph.add_node('validate', validate_chamber_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()