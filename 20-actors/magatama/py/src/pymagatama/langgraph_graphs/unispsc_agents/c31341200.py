from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; spec_data: dict; verification_status: bool; def validate_rivet_spec(state: AssemblyState): state['verification_status'] = 'Tensile Strength' in state['spec_data'] and 'Rivet Specification' in state['spec_data']; return state; def final_report(state: AssemblyState): print(f'Processing {state.get('assembly_id')}: Status={state['verification_status']}'); return state; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_rivet_spec); graph.add_node('report', final_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled = graph.compile()