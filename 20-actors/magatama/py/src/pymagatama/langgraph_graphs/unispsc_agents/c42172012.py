from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VentriculostomyState(TypedDict): kit_id: str; compliance_docs: List[str]; approved: bool; def validate_compliance(state: VentriculostomyState): state['approved'] = all(d in state['compliance_docs'] for d in ['ISO10993', 'SterilityCert']); return state; def route_approval(state: VentriculostomyState): return 'approved' if state['approved'] else 'rejected'; graph = StateGraph(VentriculostomyState); graph.add_node('validate', validate_compliance); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()