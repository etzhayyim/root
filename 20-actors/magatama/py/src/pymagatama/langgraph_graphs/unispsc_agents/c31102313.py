from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: CastingState): state['validation_results'].append('DIMENSION_CHECK_PASS'); return state; def check_material_composition(state: CastingState): state['validation_results'].append('MATERIAL_VERIFIED'); return state; graph = StateGraph(CastingState); graph.add_node('validate_dims', validate_dimensions); graph.add_node('verify_material', check_material_composition); graph.set_entry_point('validate_dims'); graph.add_edge('validate_dims', 'verify_material'); graph.add_edge('verify_material', END); graph = graph.compile()