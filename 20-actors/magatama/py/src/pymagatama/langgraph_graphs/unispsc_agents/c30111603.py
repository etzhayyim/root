from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_spec: dict; qa_check: bool; logistics_ready: bool; def validate_specs(state: ProcureState): return {'qa_check': state['chemical_spec'].get('strength', 0) > 2.5}; def check_logistics(state: ProcureState): return {'logistics_ready': True}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.add_node('logistics', check_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph.compile()