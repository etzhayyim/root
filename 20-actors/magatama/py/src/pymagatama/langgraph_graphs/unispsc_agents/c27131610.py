from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): model_ref: str; components: List[str]; verified: bool; def validate_kit(state: KitState): state['verified'] = bool(state['model_ref']) and len(state['components']) > 0; return state; graph = StateGraph(KitState); graph.add_node('validate', validate_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()