from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): tile_specs: dict; validation_results: List[str]; def validate_materials(state: AgentState): state['validation_results'].append('Material safety checked'); return state; def check_compliance(state: AgentState): state['validation_results'].append('ASTM F963 compliance verified'); return state; graph = StateGraph(AgentState); graph.add_node('validate', validate_materials); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()