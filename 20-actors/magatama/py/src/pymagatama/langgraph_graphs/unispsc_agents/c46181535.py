from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SupplyState(TypedDict): items: List[str]; validated: bool; spec: dict; def validate_specs(state: SupplyState): state['validated'] = all(k in state['spec'] for k in ['material', 'size']); return state; graph = StateGraph(SupplyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()