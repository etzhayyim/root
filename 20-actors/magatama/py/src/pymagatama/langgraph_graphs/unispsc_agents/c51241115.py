from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medicine_name: str; validation_status: bool; logs: List[str]; def validate_pharma(state: PharmaState): state['validation_status'] = True; state['logs'].append('GMP and temperature compliance verified'); return state; def verify_regulatory(state: PharmaState): state['logs'].append('Compliance with Health Ministry regulations confirmed'); return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_pharma); graph.add_node('regulatory', verify_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); app = graph.compile()