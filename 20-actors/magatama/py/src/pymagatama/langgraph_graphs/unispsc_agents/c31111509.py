from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtrusionState(TypedDict): alloy_code: str; specs: dict; compliance_check: bool; def validate_alloy(state: ExtrusionState): return {'compliance_check': state['alloy_code'] in ['6061', '6063', '7075']}; def check_specs(state: ExtrusionState): return {'compliance_check': all(k in state['specs'] for k in ['tensile_strength', 'elongation'])}; graph = StateGraph(ExtrusionState); graph.add_node('validate', validate_alloy); graph.add_node('specs', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'specs'); graph.add_edge('specs', END); app = graph.compile()