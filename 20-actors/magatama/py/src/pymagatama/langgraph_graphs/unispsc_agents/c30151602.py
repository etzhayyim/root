from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FlashingState(TypedDict): dimensions: dict; specs: dict; approved: bool; def check_specs(state: FlashingState): state['approved'] = all(k in state['specs'] for k in ['gauge', 'material']); return state; graph = StateGraph(FlashingState); graph.add_node('validation', check_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()