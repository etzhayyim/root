from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class APIState(TypedDict): ingredient_name: str; purity: float; compliance_passed: bool; def validate_purity(state: APIState): state['compliance_passed'] = state['purity'] >= 99.0; return state; def check_regulations(state: APIState): print(f'Checking compliance for {state['ingredient_name']}'); return state; graph = StateGraph(APIState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()