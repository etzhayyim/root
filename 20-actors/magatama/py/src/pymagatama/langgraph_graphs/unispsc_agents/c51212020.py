from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CatnipState(TypedDict): vendor_certs: List[str]; qc_passed: bool; moisture_level: float; def check_purity(state: CatnipState): state['qc_passed'] = 'ISO-cert' in state['vendor_certs'] and state['moisture_level'] < 12.0; return state; def validate_packaging(state: CatnipState): state['qc_passed'] = state['qc_passed'] and True; return state; graph = StateGraph(CatnipState); graph.add_node('check_purity', check_purity); graph.add_node('validate_packaging', validate_packaging); graph.add_edge('check_purity', 'validate_packaging'); graph.add_edge('validate_packaging', END); graph.set_entry_point('check_purity'); graph = graph.compile()