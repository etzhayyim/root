from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpliceSpecState(TypedDict): specs: dict; valid: bool; error_log: List[str]; def validate_specs(state: SpliceSpecState): specs = state['specs']; errors = []; required = ['material', 'thickness', 'tensile_strength']; for field in required: if field not in specs: errors.append(f'Missing {field}'); return {'valid': len(errors) == 0, 'error_log': errors}; graph = StateGraph(SpliceSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()