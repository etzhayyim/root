from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class FlagStandState(TypedDict): base_material: str; weight_kg: float; validated: bool; def validate_spec(state: FlagStandState): state['validated'] = state['weight_kg'] >= 2.0; return state; graph = StateGraph(FlagStandState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()