from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): wedge_specs: dict; compliance_verified: bool; def validate_specs(state: DentalState): return {'compliance_verified': 'biocompatibility_certification' in state['wedge_specs']}; def finalize(state: DentalState): print('Compliance checked. Proceeding with procurement.'); return state; graph = StateGraph(DentalState); graph.add_node('validate', validate_specs); graph.add_node('process', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()