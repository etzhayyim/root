from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AirbrushState(TypedDict): spec: dict; validated: bool; next_step: str; def validate_specs(state: AirbrushState): state['validated'] = state['spec'].get('nozzle_size_mm', 0) > 0; state['next_step'] = 'completed' if state['validated'] else 'error'; return state; graph = StateGraph(AirbrushState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()