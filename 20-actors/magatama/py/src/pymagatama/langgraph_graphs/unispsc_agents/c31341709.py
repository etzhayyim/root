from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_report: List[str]; graph = StateGraph(AssemblyState); def validate_specs(state: AssemblyState): return {'validation_report': ['Material grade checked', 'Bolt torque specs verified']}; def assemble_structure(state: AssemblyState): return {'validation_report': state['validation_report'] + ['Assembly sequence defined']}; graph.add_node('validate', validate_specs); graph.add_node('assemble', assemble_structure); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph = graph.compile()