from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): enclosure_specs: dict; validation_results: List[str]; graph = StateGraph(State); def validate_specs(state: State): results = []; specs = state.get('enclosure_specs', {}); if 'IP_rating' not in specs: results.append('Missing IP rating'); return {'validation_results': results}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()