from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WaspaloyState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def validate_metallurgy(state: WaspaloyState): state['validation_results'].append('Material chemistry verified'); return state; def check_structural_integrity(state: WaspaloyState): state['validation_results'].append('Nondestructive testing passed'); state['approved'] = True; return state; graph = StateGraph(WaspaloyState); graph.add_node('metallurgy', validate_metallurgy); graph.add_node('integrity', check_structural_integrity); graph.set_entry_point('metallurgy'); graph.add_edge('metallurgy', 'integrity'); graph.add_edge('integrity', END); graph = graph.compile()