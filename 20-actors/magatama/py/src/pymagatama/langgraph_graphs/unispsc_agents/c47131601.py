from langgraph.graph import StateGraph, END; from typing import TypedDict; class CleaningToolState(TypedDict): input_data: str; validation_status: bool; def check_durability(state: CleaningToolState): state['validation_status'] = 'durable' in state['input_data'].lower(); return state; graph = StateGraph(CleaningToolState); graph.add_node('validate', check_durability); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()