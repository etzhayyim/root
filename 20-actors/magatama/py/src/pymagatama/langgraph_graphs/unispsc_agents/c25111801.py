from langgraph.graph import StateGraph, END; from typing import TypedDict; class SailboatState(TypedDict): specs: dict; compliance_verified: bool; def validate_specs(state: SailboatState): state['compliance_verified'] = 'hull_material_certification' in state['specs']; return state; def finalize_workflow(state: SailboatState): return state; graph = StateGraph(SailboatState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()