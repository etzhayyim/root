from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class FramingState(TypedDict): items: Annotated[list, operator.add]; validation_log: list; def validate_driver_compatibility(state: FramingState): print('Checking tool-point compatibility...'); return {'validation_log': ['Compatibility check passed']}; def finalize_order(state: FramingState): return {'validation_log': ['Order processed']}; graph = StateGraph(FramingState); graph.add_node('validate', validate_driver_compatibility); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()