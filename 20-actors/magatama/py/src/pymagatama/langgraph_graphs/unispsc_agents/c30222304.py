from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LectureTheaterState(TypedDict): seating_layout: str; av_specs: List[str]; validated: bool; def validate_layout(state: LectureTheaterState) -> LectureTheaterState: state['validated'] = 'tier' in state['seating_layout'].lower(); return state; def check_compliance(state: LectureTheaterState) -> LectureTheaterState: print(f'Checking AV: {state["av_specs"]}'); return state; graph = StateGraph(LectureTheaterState); graph.add_node('validate', validate_layout); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()