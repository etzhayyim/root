from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): specs: dict; valid: bool; def check_specs(state: ProcState): return {'valid': 'ISO' in state['specs'].get('compliance', '')}; graph = StateGraph(ProcState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()