from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class RefiningState(TypedDict): reagent_data: dict; quality_status: str; next_step: str; def validate_reagent(state: RefiningState): data = state['reagent_data']; status = 'PASSED' if data.get('purity', 0) > 98.5 else 'FAILED'; return {'quality_status': status}; def process_refining_workflow(state: RefiningState): if state['quality_status'] == 'PASSED': return {'next_step': 'INITIATE_INDUSTRIAL_APPLICATION'}; return {'next_step': 'QUARANTINE'}; graph = StateGraph(RefiningState); graph.add_node('validate', validate_reagent); graph.add_node('workflow', process_refining_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'workflow'); graph.add_edge('workflow', END); graph = graph.compile()