from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BarometerState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_sensor(state: BarometerState): data = state['input_data']; res = [True if 'accuracy' in data and 'range' in data else False]; return {'validation_results': res, 'status': 'validated' if all(res) else 'failed'}; def compile_graph(): graph = StateGraph(BarometerState); graph.add_node('validate', validate_sensor); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()