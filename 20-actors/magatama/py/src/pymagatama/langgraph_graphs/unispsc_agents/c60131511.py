from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MuteProcurementState(TypedDict): instrument_type: str; material: str; specs_verified: bool; def validate_mute_specs(state: MuteProcurementState) -> MuteProcurementState: state['specs_verified'] = state['instrument_type'] != '' and state['material'] != ''; return state; graph = StateGraph(MuteProcurementState); graph.add_node('validate', validate_mute_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()