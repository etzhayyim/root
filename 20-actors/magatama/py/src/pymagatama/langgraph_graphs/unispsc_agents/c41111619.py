from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GaugeState(TypedDict): device_id: str; dimension_spec: float; tolerance: float; validated: bool; state_log: List[str]; def validate_gauge(state: GaugeState): print(f'Validating gauge {state["device_id"]}'); state['validated'] = True; return state; graph = StateGraph(GaugeState); graph.add_node('validate', validate_gauge); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()