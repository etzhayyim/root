from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): chemical_data: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_purity(state: CatalystState): purity = state['chemical_data'].get('purity', 0); return {'validation_results': ['Purity Check: OK' if purity >= 99.0 else 'Purity Check: FAILED'], 'status': 'valid' if purity >= 99.0 else 'rejected'}; def process_catalyst(state: CatalystState): return {'status': 'processed'}; graph = StateGraph(CatalystState); graph.add_node('validate', validate_purity); graph.add_node('process', process_catalyst); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()