from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validation_status: bool; def validate_aid(state: ProcureState): return {'validation_status': all('ergonomic' in item.lower() for item in state['items'])}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_aid); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()