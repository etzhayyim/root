from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): specs: dict; approved: bool; def validate_specs(state: ProcState): state['approved'] = all(k in state['specs'] for k in ['safety_cert', 'durability']); return 'approved' if state['approved'] else 'rejected'; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()