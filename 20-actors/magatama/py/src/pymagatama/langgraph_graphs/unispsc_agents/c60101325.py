from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FlashcardState(TypedDict): content_verified: bool; compliance_checked: bool; cards: List[dict]; def verify_content(state: FlashcardState): return {'content_verified': True}; def check_compliance(state: FlashcardState): return {'compliance_checked': True}; graph = StateGraph(FlashcardState); graph.add_node('verify', verify_content); graph.add_node('compliance', check_compliance); graph.set_entry_point('verify'); graph.add_edge('verify', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()