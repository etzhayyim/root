from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SupplyState(TypedDict): fabric_data: dict; validation_report: list[str]; def validate_material(state: SupplyState): material = state['fabric_data'].get('material'); report = ['Safety check passed'] if material else ['Validation failed']; return {'validation_report': report}; def finalize_order(state: SupplyState): return {'validation_report': state['validation_report'] + ['Order ready for processing']}; graph = StateGraph(SupplyState); graph.add_node('validate', validate_material); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()