from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DockSpecState(TypedDict): specs: dict; validation_errors: list; graph = StateGraph(DockSpecState); def validate_load(state: DockSpecState): state['validation_errors'] = ['Load exceed 20T' ] if state['specs'].get('load', 0) > 20000 else []; return state; graph.add_node('validate', validate_load); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()