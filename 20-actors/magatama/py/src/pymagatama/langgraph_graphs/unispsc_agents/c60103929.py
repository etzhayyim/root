from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SpecimenState(TypedDict): specimen_id: str; quality_passed: bool; inspection_logs: Annotated[list, operator.add]; def validate_biological_integrity(state: SpecimenState): return {'quality_passed': True, 'inspection_logs': ['Integrity check passed']}; def finalize_procurement(state: SpecimenState): return {'inspection_logs': ['Procurement finalized']}; graph = StateGraph(SpecimenState); graph.add_node('validate', validate_biological_integrity); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()