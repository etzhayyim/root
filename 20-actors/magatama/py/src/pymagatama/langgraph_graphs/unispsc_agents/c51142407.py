from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_id: str; purity_check: bool; compliance_status: bool; def check_purity_level(state: DrugState): state['purity_check'] = True; return state; def verify_regulatory_compliance(state: DrugState): state['compliance_status'] = True; return state; graph = StateGraph(DrugState); graph.add_node('verify_purity', check_purity_level); graph.add_node('compliance_check', verify_regulatory_compliance); graph.set_entry_point('verify_purity'); graph.add_edge('verify_purity', 'compliance_check'); graph.add_edge('compliance_check', END); compiled_graph = graph.compile()