from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ApronState(TypedDict): specs: dict; approval_status: bool; def validate_specs(state: ApronState): return {'approval_status': 'material' in state['specs']}; graph = StateGraph(ApronState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()