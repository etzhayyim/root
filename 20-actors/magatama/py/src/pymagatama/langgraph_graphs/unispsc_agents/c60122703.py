from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[str]; status: str; def validate_tools(state: State): return {'status': 'validated'}; def approve_procurement(state: State): return {'status': 'approved'}; graph = StateGraph(State); graph.add_node('validate', validate_tools); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()