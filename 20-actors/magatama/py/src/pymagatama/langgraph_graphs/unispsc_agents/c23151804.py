from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): specs: dict; validation_report: List[str]; validated: bool; def validate_pressure_vessel(state: ProcState): state['validation_report'].append('Verifying ASME/PED compliance...'); state['validated'] = True; return state; def check_dual_use(state: ProcState): state['validation_report'].append('Checking export license requirements...'); return state; graph = StateGraph(ProcState); graph.add_node('validate_vessel', validate_pressure_vessel); graph.add_node('export_check', check_dual_use); graph.set_entry_point('validate_vessel'); graph.add_edge('validate_vessel', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()