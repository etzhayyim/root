from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class DoorState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: DoorState): errors = []; if not state['specs'].get('fire_rating'): errors.append('Missing fire rating'); return {'validation_errors': errors}; def check_safety(state: DoorState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(DoorState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()