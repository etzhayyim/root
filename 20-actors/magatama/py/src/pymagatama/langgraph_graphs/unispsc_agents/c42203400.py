from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_device(state: ProcureState): state['validated'] = all(c in state.get('certs', []) for c in ['ISO13485', 'RegulatoryClearance']); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()