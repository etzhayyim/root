from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): data: dict; status: str; def validate_spec(state: FurnitureState): state['status'] = 'validated' if 'weight_capacity' in state['data'] else 'incomplete'; return state; def check_safety(state: FurnitureState): return 'compliant' if state['data'].get('fire_safety') == 'yes' else 'rejected'; graph = StateGraph(FurnitureState); graph.add_node('validate', validate_spec); graph.add_node('check_safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_safety'); graph.add_edge('check_safety', END); graph = graph.compile()