from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): material_id: str; specs: Dict; validation_status: bool; workflow_log: List[str]; def validate_alloy(state: MaterialState): state['workflow_log'].append('Validating alloy composition...'); state['validation_status'] = True; return state; def check_durability(state: MaterialState): state['workflow_log'].append('Checking tensile strength...'); return state; graph = StateGraph(MaterialState); graph.add_node('validate_alloy', validate_alloy); graph.add_node('check_durability', check_durability); graph.set_entry_point('validate_alloy'); graph.add_edge('validate_alloy', 'check_durability'); graph.add_edge('check_durability', END); graph = graph.compile()