from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: list[str]; validation_logs: Annotated[list[str], operator.add]; def validate_specs(state: ProcState): return {'validation_logs': ['Material verification passed', 'Dimension calibration complete']}; def approve_order(state: ProcState): return {'validation_logs': ['Order authorized for procurement']}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()