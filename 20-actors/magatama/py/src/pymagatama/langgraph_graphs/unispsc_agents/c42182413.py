from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TuningForkState(TypedDict): specs: List[str]; validated: bool; results: str; def validate_specs(state: TuningForkState): return {'validated': 'calibration_certificate' in state['specs']}; def finalize_process(state: TuningForkState): return {'results': 'Validation Complete'}; graph = StateGraph(TuningForkState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_process); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()