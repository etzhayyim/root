from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PipeState(TypedDict): material_specs: dict; inspection_results: List[str]; approved: bool; def validate_metallurgy(state: PipeState): return {'approved': state['material_specs'].get('grade') in ['625', '718']}; def perform_nde(state: PipeState): return {'inspection_results': ['Ultrasonic Test Passed', 'Visual Inspection Passed']}; graph = StateGraph(PipeState); graph.add_node('validate', validate_metallurgy); graph.add_node('nde', perform_nde); graph.add_edge('validate', 'nde'); graph.add_edge('nde', END); graph.set_entry_point('validate'); graph = graph.compile()