from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): input_spec: dict; validation_report: list[str]; approval_status: bool; def validate_medical_device(state: ProcureState): report = []; certs = state['input_spec'].get('certifications', []); if 'ISO 13485' not in certs: report.append('Missing ISO 13485'); return {'validation_report': report, 'approval_status': len(report) == 0}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()