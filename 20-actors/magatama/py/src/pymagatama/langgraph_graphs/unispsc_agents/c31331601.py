from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): n_parts: int; weld_quality_pass: bool; spec_docs: list[str]; def validate_welds(state: AssemblyState): return {'weld_quality_pass': True} if len(state['spec_docs']) >= 3 else {'weld_quality_pass': False}; def assemble_graph(): graph = StateGraph(AssemblyState); graph.add_node('validate', validate_welds); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = assemble_graph()