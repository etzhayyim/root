from langgraph.graph import StateGraph, END; from typing import TypedDict; class MetalState(TypedDict): metal_type: str; weight: float; purity: float; approved: bool; def validate_purity(state: MetalState): state['approved'] = state['purity'] >= 0.999; return state; def check_value(state: MetalState): print(f'Processing {state["metal_type"]} forging'); return state; graph = StateGraph(MetalState); graph.add_node('validate', validate_purity); graph.add_node('check', check_value); graph.set_entry_point('check'); graph.add_edge('check', 'validate'); graph.add_edge('validate', END); graph = graph.compile()