from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StethoscopeState(TypedDict): items: List[dict]; valid: bool; criteria: dict; def validate_specs(state: StethoscopeState): state['valid'] = all(item.get('medical_cert') for item in state['items']); return state; graph = StateGraph(StethoscopeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()