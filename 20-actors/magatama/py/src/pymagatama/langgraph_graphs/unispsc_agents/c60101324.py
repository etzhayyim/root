from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): content: str; validated: bool; def check_safety(state: State): return {'validated': 'non-toxic' in state.get('content', '').lower()}; graph = StateGraph(State); graph.add_node('safety_check', check_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()