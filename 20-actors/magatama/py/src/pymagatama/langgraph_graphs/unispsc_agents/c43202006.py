from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MOState(TypedDict): capacity: int; verified: bool; log: List[str]; def validate_disk(state: MOState): state['verified'] = state['capacity'] > 0; state['log'].append('Validation complete'); return state; graph = StateGraph(MOState); graph.add_node('validate', validate_disk); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()