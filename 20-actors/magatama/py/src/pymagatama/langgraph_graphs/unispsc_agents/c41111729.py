from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicroscopeSpec(TypedDict): specs: dict; errors: List[str]; def validate_optics(state: MicroscopeSpec): optics = state['specs']; errors = []; if optics.get('magnification') <= 0: errors.append('Mag must be positive'); return {'errors': errors}; graph = StateGraph(MicroscopeSpec); graph.add_node('validate', validate_optics); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()