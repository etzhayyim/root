from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtrusionState(TypedDict): data: dict; specs: List[str]; validated: bool; def validate_specs(state: ExtrusionState): state['validated'] = all(k in state['data'] for k in state['specs']); return state; def check_quality(state: ExtrusionState): print('Checking dimensional accuracy...'); return state; graph = StateGraph(ExtrusionState); graph.add_node('validate', validate_specs); graph.add_node('qc', check_quality); graph.add_edge('validate', 'qc'); graph.add_edge('qc', END); graph.set_entry_point('validate'); graph = graph.compile()