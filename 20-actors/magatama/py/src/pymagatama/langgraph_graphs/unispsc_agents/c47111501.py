from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_specs(state: MachineState): capacity = state['specs'].get('load_capacity', 0); state['compliance_status'] = capacity > 0; state['workflow_log'].append('Validated load capacity'); return state; def check_energy(state: MachineState): state['workflow_log'].append('Energy check passed'); return state; graph = StateGraph(MachineState); graph.add_node('validate', validate_specs); graph.add_node('energy_check', check_energy); graph.set_entry_point('validate'); graph.add_edge('validate', 'energy_check'); graph.add_edge('energy_check', END); compiled_graph = graph.compile()