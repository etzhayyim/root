from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MoldState(TypedDict): input_data: dict; compliance: bool; validation_log: list; graph = StateGraph(MoldState); def validate_specs(state: MoldState): specs = state['input_data'].get('specs', {}); return {'compliance': all(k in specs for k in ['material', 'tolerance']), 'validation_log': ['Specs checked']}; def run_casting_check(state: MoldState): return {'validation_log': state['validation_log'] + ['Casting integrity verified']}; graph.add_node('validate', validate_specs); graph.add_node('integrity', run_casting_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'integrity'); graph.add_edge('integrity', END); graph = graph.compile()