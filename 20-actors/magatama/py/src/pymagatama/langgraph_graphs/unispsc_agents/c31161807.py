from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WasherState(TypedDict): specs: dict; valid: bool; def validate_specs(state: WasherState): state['valid'] = 'material' in state['specs'] and 'size' in state['specs']; return state; builder = StateGraph(WasherState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()