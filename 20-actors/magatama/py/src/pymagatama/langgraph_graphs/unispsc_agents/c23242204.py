from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LappingState(TypedDict): vendor_specs: dict; validation_results: List[str]; def validate_specs(state: LappingState): result = 'PASS' if state['vendor_specs'].get('precision') > 0.001 else 'FAIL'; return {'validation_results': [result]}; def check_export_control(state: LappingState): return {'validation_results': state['validation_results'] + ['Export Control Review Required']}; graph = StateGraph(LappingState); graph.add_node('validate', validate_specs); graph.add_node('export_review', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); compiled_graph = graph.compile()