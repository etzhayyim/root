from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ImagingProcurementState(TypedDict): vendor_docs: dict; compliance_status: bool; workflow_steps: Annotated[list, operator.add]; def validate_imaging_specs(state: ImagingProcurementState): return {'compliance_status': True, 'workflow_steps': ['Check Radiation Certs', 'Verify FDA Clearance']}; def process_imaging_order(state: ImagingProcurementState): return {'workflow_steps': ['Procurement Approved']}; graph = StateGraph(ImagingProcurementState); graph.add_node('validate', validate_imaging_specs); graph.add_node('process', process_imaging_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()