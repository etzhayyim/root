from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IngotsState(TypedDict): composition_data: dict; validation_passed: bool; def validate_composition(state: IngotsState): state['validation_passed'] = 'Fe' in state['composition_data'] and state['composition_data']['Fe'] > 90; return state; def route_next(state: IngotsState): return 'process' if state['validation_passed'] else END; graph = StateGraph(IngotsState); graph.add_node('validate', validate_composition); graph.add_node('process', lambda s: s); graph.set_entry_point('validate'); graph.add_conditional_edges('validate', route_next); graph.add_edge('process', END); graph = graph.compile()