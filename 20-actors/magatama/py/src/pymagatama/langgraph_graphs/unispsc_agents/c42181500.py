from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DiagnosticState(TypedDict): items: List[dict]; validated: bool; log: List[str]; def validation_node(state: DiagnosticState): state['validated'] = all('cert' in item for item in state['items']); return state; def reporting_node(state: DiagnosticState): state['log'].append('Validation complete'); return state; graph = StateGraph(DiagnosticState); graph.add_node('validate', validation_node); graph.add_node('report', reporting_node); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()