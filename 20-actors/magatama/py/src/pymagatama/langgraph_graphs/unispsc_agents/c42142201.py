from langgraph.graph import StateGraph, END; from typing import TypedDict; class HydroState(TypedDict): specs: dict; valid: bool; def validation_step(state: HydroState): state['valid'] = all(k in state['specs'] for k in ['material', 'temp_range']); return state; graph = StateGraph(HydroState); graph.add_node('validate', validation_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()