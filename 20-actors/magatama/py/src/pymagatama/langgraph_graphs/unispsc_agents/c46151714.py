from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EvidenceCabinetState(TypedDict): cabinet_spec: dict; validation_results: List[str]; graph = StateGraph(EvidenceCabinetState); def validate_airflow(state: EvidenceCabinetState): state['validation_results'].append('Airflow checked'); return state; def validate_filtration(state: EvidenceCabinetState): state['validation_results'].append('HEPA filter verified'); return state; graph.add_node('validate_airflow', validate_airflow); graph.add_node('validate_filtration', validate_filtration); graph.set_entry_point('validate_airflow'); graph.add_edge('validate_airflow', 'validate_filtration'); graph.add_edge('validate_filtration', END); graph = graph.compile()