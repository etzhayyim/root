from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: CastingState): results = []; if state['specs'].get('tolerance', 0) > 0.05: results.append('Tolerance out of range'); return {'validation_results': results}; def process_order(state: CastingState): return {'validation_results': state['validation_results'] + ['CAD verification complete']}; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.add_node('process', process_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()