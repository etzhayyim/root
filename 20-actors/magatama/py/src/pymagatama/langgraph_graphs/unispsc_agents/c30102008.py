from langgraph.graph import StateGraph, END; from typing import TypedDict; class TitaniumState(TypedDict): specs: dict; valid: bool; def validate_specs(state: TitaniumState): state['valid'] = 'ASTM_Standard' in state['specs'] and state['specs']['Thickness_Tolerance'] < 0.01; return state; graph = StateGraph(TitaniumState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()