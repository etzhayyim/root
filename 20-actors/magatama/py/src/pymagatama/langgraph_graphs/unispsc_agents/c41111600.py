from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MeasurementState(TypedDict): specs: dict; valid: bool; error: str; def validate_spec(state: MeasurementState): s = state['specs']; state['valid'] = 'accuracy_tolerance' in s and 'calibration_standard' in s; return state; graph = StateGraph(MeasurementState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()