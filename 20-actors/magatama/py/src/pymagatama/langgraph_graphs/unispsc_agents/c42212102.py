from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AccessibilityState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_accessibility(state: AccessibilityState): state['validation_errors'] = [e for item in state['items'] if 'tactile_feedback' not in item]; return {'validation_errors': state['validation_errors']}; graph = StateGraph(AccessibilityState); graph.add_node('validate', validate_accessibility); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()