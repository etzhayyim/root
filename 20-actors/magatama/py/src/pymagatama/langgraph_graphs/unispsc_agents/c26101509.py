from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HydroNode(TypedDict): status: str; spec_data: dict; validations: List[str]; def validate_spec(state: HydroNode): state['validations'] = ['Power Check', 'Material Integrity Check']; return state; def final_approval(state: HydroNode): state['status'] = 'Approved'; return state; graph = StateGraph(HydroNode); graph.add_node('validate', validate_spec); graph.add_node('approve', final_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()