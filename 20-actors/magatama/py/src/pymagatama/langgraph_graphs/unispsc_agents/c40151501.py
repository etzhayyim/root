from langgraph.graph import StateGraph, END; from typing import TypedDict; class AirPumpState(TypedDict): pump_specs: dict; validated: bool; def check_specs(state: AirPumpState): specs = state['pump_specs']; validated = 'Flow Rate' in specs and 'Max Pressure' in specs; return {'validated': validated}; graph = StateGraph(AirPumpState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()