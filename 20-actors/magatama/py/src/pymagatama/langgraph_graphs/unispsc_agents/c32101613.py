from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TTLState(TypedDict): component_id: str; spec_data: dict; verification_result: bool; def validate_spec(state: TTLState): specs = state['spec_data']; is_valid = all(key in specs for key in ['logic_family_series', 'pin_count']); return {'verification_result': is_valid}; def routing_logic(state: TTLState): return 'process' if state['verification_result'] else 'error'; graph = StateGraph(TTLState); graph.add_node('validate', validate_spec); graph.add_node('process', lambda x: x); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()