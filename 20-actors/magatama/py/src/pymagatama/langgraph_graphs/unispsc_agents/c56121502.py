from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChairSpec(TypedDict): specs: List[str]; validated: bool; payload: dict; def validate_specs(state: ChairSpec): return {'validated': all(k in state['payload'] for k in ['material', 'load_capacity'])}; graph = StateGraph(ChairSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()