from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): particle_data: dict; validation_results: List[str]; def validate_spec(state: State): state['validation_results'].append('Size range within spec'); return state; def check_calibration(state: State): state['validation_results'].append('Calibration cert valid'); return state; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.add_node('calibrate', check_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); app = graph.compile()