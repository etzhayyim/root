from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_docs: List[str]; compliance_status: bool; def validate_docs(state: ProcState): return {'compliance_status': all('ISO' in doc for doc in state['vendor_docs'])}; def compliance_check(state: ProcState): return 'approved' if state['compliance_status'] else 'rejected'; graph = StateGraph(ProcState); graph.add_node('validate', validate_docs); graph.add_node('check', compliance_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'check'); graph.add_edge('check', END); compiled_graph = graph.compile()