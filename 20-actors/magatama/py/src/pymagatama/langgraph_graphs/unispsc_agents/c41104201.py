from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_id: str; purity_rating: float; compliance_verified: bool; def validate_reagent(state: ReagentState): state['compliance_verified'] = state['purity_rating'] >= 99.9; return state; graph = StateGraph(ReagentState); graph.add_node('validate', validate_reagent); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()