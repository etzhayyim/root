from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProduceState(TypedDict): fruit_type: str; quality_score: float; compliance_checks: List[str]; def validate_freshness(state: ProduceState): state['compliance_checks'].append('temp_log_verified'); return {'quality_score': 0.95}; def check_phytosanitary(state: ProduceState): state['compliance_checks'].append('phytosanitary_cleared'); return {'compliance_checks': state['compliance_checks']}; graph = StateGraph(ProduceState); graph.add_node('validate', validate_freshness); graph.add_node('quarantine', check_phytosanitary); graph.set_entry_point('validate'); graph.add_edge('validate', 'quarantine'); graph.add_edge('quarantine', END); graph = graph.compile()