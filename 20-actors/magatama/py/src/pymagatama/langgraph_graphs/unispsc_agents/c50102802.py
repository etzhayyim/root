from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CherryState(TypedDict): cold_chain_log: List[float]; safety_cert: bool; approved: bool; def validate_temp(state: CherryState): is_valid = all(t <= -18.0 for t in state['cold_chain_log']); return {'approved': is_valid and state['safety_cert']}; graph = StateGraph(CherryState); graph.add_node('validate', validate_temp); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()