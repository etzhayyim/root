from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_docs: List[str]; qc_passed: bool; proc_status: str; def validate_sds(state: ProcState): state['qc_passed'] = 'SDS' in state['vendor_docs']; return {'proc_status': 'Validated' if state['qc_passed'] else 'Rejected'}; graph = StateGraph(ProcState); graph.add_node('validate_sds', validate_sds); graph.set_entry_point('validate_sds'); graph.add_edge('validate_sds', END); graph = graph.compile()