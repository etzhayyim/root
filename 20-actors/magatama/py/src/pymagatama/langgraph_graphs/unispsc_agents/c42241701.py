from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class OrthoState(TypedDict): device_name: str; specifications: dict; compliance_verified: bool; def check_compliance(state: OrthoState): state['compliance_verified'] = 'ISO13485' in state['specifications'] and 'Class' in state['specifications']; return state; def validate_specs(state: OrthoState): return 'valid' if state['compliance_verified'] else 'invalid'; graph = StateGraph(OrthoState); graph.add_node('verify', check_compliance); graph.add_edge('verify', END); graph.set_entry_point('verify'); graph = graph.compile()