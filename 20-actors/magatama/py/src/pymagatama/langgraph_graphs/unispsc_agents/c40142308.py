from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeSpecState(TypedDict): spec: dict; validation_errors: List[str]; graph = StateGraph(PipeSpecState); def validate_spec(state: PipeSpecState): errors = []; if state['spec'].get('pressure_rating', 0) <= 0: errors.append('Invalid pressure rating'); return {'validation_errors': errors}; graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()