from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CopperProcurementState(TypedDict): component_specs: dict; validation_results: List[str]; def validate_thickness(state: CopperProcurementState): state['validation_results'].append('Thickness verified'); return state; def check_surface_finish(state: CopperProcurementState): state['validation_results'].append('Surface within tolerance'); return state; graph = StateGraph(CopperProcurementState); graph.add_node('thickness_check', validate_thickness); graph.add_node('surface_check', check_surface_finish); graph.set_entry_point('thickness_check'); graph.add_edge('thickness_check', 'surface_check'); graph.add_edge('surface_check', END); graph = graph.compile()