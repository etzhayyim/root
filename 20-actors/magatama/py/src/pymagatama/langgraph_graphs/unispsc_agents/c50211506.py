from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SnuffState(TypedDict): tobacco_data: dict; compliance_checks: list; graph = StateGraph(SnuffState); def validate_nicotine_level(state: SnuffState): state['compliance_checks'].append('nicotine_valid') if state['tobacco_data'].get('nicotine_level', 0) < 50 else None; return state; def check_import_regulations(state: SnuffState): state['compliance_checks'].append('import_cleared'); return state; graph.add_node('validate', validate_nicotine_level); graph.add_node('compliance', check_import_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()