from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): material_data: dict; validation_passed: bool; def validate_material(state: State): state['validation_passed'] = 'lot_number' in state['material_data'] and 'expiry' in state['material_data']; return state; def check_compliance(state: State): return 'next_step' if state['validation_passed'] else END; graph = StateGraph(State); graph.add_node('validate', validate_material); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END);