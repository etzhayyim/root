from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class AngleState(TypedDict): specs: dict; valid: bool; def validate_specs(state: AngleState): specs = state['specs']; is_valid = all(key in specs for key in ['grade', 'dimensions']); return {'valid': is_valid}; graph = StateGraph(AngleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()