from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): chemical_name: str; validation_status: bool; compliance_docs: List[str]; def validate_chemical(state: ProcurementState): state['validation_status'] = 'Diethylstilbestrol' in state['chemical_name']; return state; def check_hazmat(state: ProcurementState): state['compliance_docs'].append('MSDS_REVIEWED'); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_chemical); graph.add_node('hazmat', check_hazmat); graph.add_edge('validate', 'hazmat'); graph.add_edge('hazmat', END); graph.set_entry_point('validate'); graph = graph.compile()