from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LockoutState(TypedDict): device_specs: dict; validation_passed: bool; warnings: List[str]; def validate_dimensions(state: LockoutState): state['validation_passed'] = 'max_dim' in state['device_specs']; return state; def check_compliance(state: LockoutState): if not state['validation_passed']: state['warnings'].append('Dimensions missing'); return state; graph = StateGraph(LockoutState); graph.add_node('validate', validate_dimensions); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()