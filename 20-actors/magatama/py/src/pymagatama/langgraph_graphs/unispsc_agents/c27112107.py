from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ToolState(TypedDict): tool_name: str; specs: dict; approved: bool; def validation_step(state: ToolState): state['approved'] = state['specs'].get('hardness_hrc', 0) >= 45; return state; graph = StateGraph(ToolState); graph.add_node('validate', validation_step); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()