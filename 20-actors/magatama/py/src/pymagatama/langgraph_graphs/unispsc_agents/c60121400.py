from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FramingState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: FramingState): errors = []; if 'dimensions' not in state['specs']: errors.append('Missing dimensions'); return {'validation_errors': errors}; def finalize_order(state: FramingState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(FramingState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()