from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PryBarState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(PryBarState); def validate_material(state: PryBarState): state['validation_results'].append('Material grade confirmed.'); return state; def validate_load(state: PryBarState): state['validation_results'].append('Load capacity verified.'); return state; graph.add_node('material_check', validate_material); graph.add_node('load_check', validate_load); graph.add_edge('material_check', 'load_check'); graph.add_edge('load_check', END); graph.set_entry_point('material_check'); compiled_graph = graph.compile()