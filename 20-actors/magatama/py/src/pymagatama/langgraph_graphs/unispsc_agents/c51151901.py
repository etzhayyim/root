from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): lot: str; expiry: str; temp_log: List[float]; is_compliant: bool; def validate_pharma(state: PharmaState) -> PharmaState: state['is_compliant'] = (len(state['temp_log']) > 0 and all(2.0 <= t <= 8.0 for t in state['temp_log'])); return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_pharma); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()