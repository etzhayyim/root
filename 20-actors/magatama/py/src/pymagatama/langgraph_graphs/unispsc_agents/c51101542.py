from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_id: str; quality_checks: List[str]; status: str; def validate_reagent(state: ReagentState): if 'purity_verified' not in state['quality_checks']: return {'status': 'pending_qa'}; return {'status': 'approved'}; def route_reagent(state: ReagentState): if state['status'] == 'approved': return 'dispatch'; return 'qa_audit'; graph = StateGraph(ReagentState); graph.add_node('validate', validate_reagent); graph.add_conditional_edges('validate', route_reagent, {'dispatch': END, 'qa_audit': END}); graph.set_entry_point('validate'); compile_graph = graph.compile()