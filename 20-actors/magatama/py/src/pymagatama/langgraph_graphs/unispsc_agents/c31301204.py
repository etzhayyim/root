from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): vendor_specs: dict; inspection_passed: bool; compliance_code: str; def validate_dimensions(state: ForgingState): state['inspection_passed'] = state['vendor_specs'].get('tolerance') <= 0.05; return state; def check_export_control(state: ForgingState): state['compliance_code'] = 'LEVEL_2_RESTRICTED' if state['vendor_specs'].get('alloy_type') == 'nickel-based' else 'STANDARD'; return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_dimensions); graph.add_node('export_check', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()