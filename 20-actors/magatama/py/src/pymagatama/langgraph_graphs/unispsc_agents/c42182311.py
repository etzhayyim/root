from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NeuroState(TypedDict): sensor_data: dict; validation_report: List[str]; def validate_sensor_specs(state: NeuroState): report = []; "Check frequency response"; if state['sensor_data'].get('sampling_rate', 0) < 1000: report.append('Low sampling rate'); return {'validation_report': report}; graph = StateGraph(NeuroState); graph.add_node('validate', validate_sensor_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()