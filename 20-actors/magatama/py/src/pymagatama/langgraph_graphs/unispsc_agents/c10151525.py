from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class ProcessingState(TypedDict): commodity_code: str; specs: Dict[str, any]; validation_logs: Annotated[List[str], add_messages]; def validate_machinery_spec(state: ProcessingState): logs = [f"Validating specs for {state['commodity_code']}"]; return {'validation_logs': logs}; def check_safety_standards(state: ProcessingState): return {'validation_logs': ["Safety standards verified against ISO"]}; graph = StateGraph(ProcessingState); graph.add_node("validate", validate_machinery_spec); graph.add_node("safety", check_safety_standards); graph.add_edge("validate", "safety"); graph.add_edge("safety", END); graph.set_entry_point("validate"); graph = graph.compile()