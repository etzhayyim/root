from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitchenApplianceState(TypedDict): appliance_id: str; specs: dict; validation_status: bool; workflow_log: List[str]; def validate_specs(state: KitchenApplianceState): status = all(key in state['specs'] for key in ['capacity', 'power_source']); return {'validation_status': status, 'workflow_log': ['Specs checked']}; def approve_procurement(state: KitchenApplianceState): return {'workflow_log': ['Approved for production']}; graph = StateGraph(KitchenApplianceState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()