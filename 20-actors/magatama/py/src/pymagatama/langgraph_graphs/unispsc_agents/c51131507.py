from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): cold_chain_verified: bool; lot_valid: bool; temperature_logs: List[float]; def verify_cold_chain(state: PharmaState) -> PharmaState: state['cold_chain_verified'] = all(2.0 <= t <= 8.0 for t in state['temperature_logs']); return state; def validate_lot(state: PharmaState) -> PharmaState: state['lot_valid'] = True; return state; graph = StateGraph(PharmaState); graph.add_node('verify_chain', verify_cold_chain); graph.add_node('validate_lot', validate_lot); graph.set_entry_point('verify_chain'); graph.add_edge('verify_chain', 'validate_lot'); graph.add_edge('validate_lot', END); graph = graph.compile()