from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DialysisState(TypedDict): unit_id: str; specs: dict; validation_status: bool; def validate_specs(state: DialysisState): state['validation_status'] = 'Flow rate' in state['specs'] and 'ISO_cert' in state['specs']; return state; def route_approval(state: DialysisState): return 'approved' if state['validation_status'] else 'rejected'; graph = StateGraph(DialysisState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END);