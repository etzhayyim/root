from langgraph.graph import StateGraph, END; from typing import TypedDict; class PLCState(TypedDict): spec_data: dict; validation_passed: bool; def validate_specs(state: PLCState): state['validation_passed'] = 'voltage_rating' in state['spec_data'] and 'ip_rating' in state['spec_data']; return state; graph = StateGraph(PLCState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()