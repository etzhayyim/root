from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): commodity: str; compliance_checks: List[str]; approved: bool; def check_regulations(state: ProcureState): state['compliance_checks'].append('License Verified'); return {'approved': True} if 'License Verified' in state['compliance_checks'] else {'approved': False}; graph = StateGraph(ProcureState); graph.add_node('regulatory_scrutiny', check_regulations); graph.set_entry_point('regulatory_scrutiny'); graph.add_edge('regulatory_scrutiny', END); graph = graph.compile()