from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_data: dict; verification_status: str; def validate_purity(state: PharmState): status = "pass" if state["chemical_data"].get("purity", 0) >= 99.0 else "fail"; return {"verification_status": status}; def check_regulatory(state: PharmState): return {"verification_status": "compliant" if "gmp" in state["chemical_data"] else "flagged"}; graph = StateGraph(PharmState); graph.add_node("purity_check", validate_purity); graph.add_node("reg_check", check_regulatory); graph.set_entry_point("purity_check"); graph.add_edge("purity_check", "reg_check"); graph.add_edge("reg_check", END); graph = graph.compile()