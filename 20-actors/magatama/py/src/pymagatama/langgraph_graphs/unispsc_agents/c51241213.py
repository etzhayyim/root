from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): vendor_certs: List[str]; safety_approved: bool; procurement_complete: bool; def validate_pharma(state: ProcState): state['safety_approved'] = 'GMP' in state['vendor_certs']; return state; def finalize_procurement(state: ProcState): state['procurement_complete'] = state['safety_approved']; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_pharma); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()