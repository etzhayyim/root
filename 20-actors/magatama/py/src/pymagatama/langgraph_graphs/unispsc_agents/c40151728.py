from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): pump_model: str; kit_contents: List[str]; verified: bool; def validate_kit(state: PumpState): state['verified'] = len(state['kit_contents']) > 0; return state; graph = StateGraph(PumpState); graph.add_node('validate', validate_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()