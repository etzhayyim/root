from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class HeaterState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: HeaterState): errors = []; if state['specs'].get('wattage', 0) <= 0: errors.append('Invalid wattage'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(HeaterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()