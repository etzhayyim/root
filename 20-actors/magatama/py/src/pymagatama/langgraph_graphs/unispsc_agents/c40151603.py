from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_specs(state: CompressorState): state['compliance_status'] = 'maximum_discharge_pressure' in state['specs'] and 'gas_compatibility' in state['specs']; state['workflow_log'].append('Validation completed'); return state; def route_approval(state: CompressorState): return 'approved' if state['compliance_status'] else 'rejected'; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()