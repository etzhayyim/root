from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MarkerState(TypedDict): tags: List[str]; validated: bool; def check_compliance(state: MarkerState): state['validated'] = 'regulated-health-product' in state['tags']; return state; def validate_markers(state: MarkerState): return 'validated' if state['validated'] else 'rejected'; graph = StateGraph(MarkerState); graph.add_node('check_compliance', check_compliance); graph.add_edge('check_compliance', END); graph.set_entry_point('check_compliance'); graph = graph.compile()