from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RefractoryState(TypedDict): material_specs: dict; inspection_status: bool; flow_test_result: float; graph = StateGraph(RefractoryState); def validate_specs(state: RefractoryState): return {'inspection_status': state['material_specs'].get('Al2O3', 0) > 40}; def check_flow_properties(state: RefractoryState): return {'flow_test_result': state['material_specs'].get('flow_value', 150)}; graph.add_node('validate', validate_specs); graph.add_node('flow_analysis', check_flow_properties); graph.set_entry_point('validate'); graph.add_edge('validate', 'flow_analysis'); graph.add_edge('flow_analysis', END); graph = graph.compile()