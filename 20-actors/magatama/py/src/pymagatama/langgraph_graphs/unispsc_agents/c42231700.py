from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeState(TypedDict): tube_specs: dict; validation_results: List[str]; def validate_medical_compliance(state: TubeState): results = []; if 'ISO_13485' not in state['tube_specs'].get('certs', []): results.append('Missing ISO 13485'); return {'validation_results': results}; def check_physical_specs(state: TubeState): return {'validation_results': ['Specs Verified']}; graph = StateGraph(TubeState); graph.add_node('compliance', validate_medical_compliance); graph.add_node('physical', check_physical_specs); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'physical'); graph.add_edge('physical', END); graph = graph.compile()