from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; specs: dict; valid: bool; def validate_specs(state: AssemblyState): state['valid'] = 'Material specification' in state['specs'] and 'Tensile strength' in state['specs']; return state; def check_compliance(state: AssemblyState): return 'compliant' if state['valid'] else 'non-compliant'; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()