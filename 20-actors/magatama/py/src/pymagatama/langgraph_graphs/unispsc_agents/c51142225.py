from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NarcoticState(TypedDict): n_code: str; license_verified: bool; safety_check: bool; def verify_regulatory(state: NarcoticState): return {'license_verified': True}; def safety_audit(state: NarcoticState): return {'safety_check': True}; graph = StateGraph(NarcoticState); graph.add_node('verify', verify_regulatory); graph.add_node('audit', safety_audit); graph.add_edge('verify', 'audit'); graph.add_edge('audit', END); graph.set_entry_point('verify'); graph = graph.compile()