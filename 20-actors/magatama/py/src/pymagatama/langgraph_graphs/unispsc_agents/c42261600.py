from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AutopsySupplyState(TypedDict): items: List[str]; validated: bool; biohazard_cleared: bool; def validate_equipment(state: AutopsySupplyState): return {'validated': all(len(i) > 0 for i in state['items'])}; def check_safety(state: AutopsySupplyState): return {'biohazard_cleared': True}; graph = StateGraph(AutopsySupplyState); graph.add_node('validate', validate_equipment); graph.add_node('safety_check', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()