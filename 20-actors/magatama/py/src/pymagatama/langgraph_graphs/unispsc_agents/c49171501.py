from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GymnasticState(TypedDict): equipment_specs: dict; compliance_passed: bool; inspection_report: str; def validate_equipment(state: GymnasticState): state['compliance_passed'] = state['equipment_specs'].get('tensile_strength', 0) > 2000; return state; def generate_report(state: GymnasticState): state['inspection_report'] = 'Standard compliance met.' if state['compliance_passed'] else 'Compliance failed.'; return state; graph = StateGraph(GymnasticState); graph.add_node('validate', validate_equipment); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); graph = graph.compile()