from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FerruleState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: FerruleState): state['valid'] = 'Material Grade' in state['specs'] and 'Pressure Rating PSI' in state['specs']; return state; graph = StateGraph(FerruleState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()