from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PLCState(TypedDict): specs: dict; validation_results: list; status: str; def validate_specs(state: PLCState): state['validation_results'].append('Voltage check complete'); state['status'] = 'VALIDATED'; return state; graph = StateGraph(PLCState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()