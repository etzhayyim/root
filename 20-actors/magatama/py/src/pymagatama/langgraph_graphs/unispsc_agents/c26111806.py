from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): pulley_specs: dict; validated: bool; def validate_specs(state: State): state['validated'] = 'material' in state['pulley_specs'] and 'diameter' in state['pulley_specs']; return state; builder = StateGraph(State); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()