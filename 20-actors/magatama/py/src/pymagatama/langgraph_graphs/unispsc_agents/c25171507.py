from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WiperState(TypedDict): specs: dict; validation_results: List[str]; def validate_specs(state: WiperState): result = []; if state['specs'].get('blade_length_mm', 0) < 100: result.append('Error: Blade length too short'); return {'validation_results': result}; def check_compliance(state: WiperState): return {'validation_results': state['validation_results'] + ['Compliance verified']}; graph = StateGraph(WiperState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()