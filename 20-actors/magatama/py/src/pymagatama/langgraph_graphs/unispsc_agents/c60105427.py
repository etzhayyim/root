from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): content: str; validated: bool; compliance_status: List[str]; def validate_legal_content(state: ProcureState): state['validated'] = 'jurisdiction' in state['content'].lower(); return state; def check_compliance(state: ProcureState): state['compliance_status'] = ['PASSED'] if state['validated'] else ['FAILED_LEGAL_CHECK']; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_legal_content); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()