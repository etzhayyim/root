from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_id: str; temp_verified: bool; quality_passed: bool; logs: List[str]; def verify_cold_chain(state: ReagentState): state['temp_verified'] = True; state['logs'].append('Cold chain validated'); return state; def validate_quality(state: ReagentState): state['quality_passed'] = True; state['logs'].append('QC standards met'); return state; graph = StateGraph(ReagentState); graph.add_node('verify_cold_chain', verify_cold_chain); graph.add_node('validate_quality', validate_quality); graph.add_edge('verify_cold_chain', 'validate_quality'); graph.add_edge('validate_quality', END); graph.set_entry_point('verify_cold_chain'); graph = graph.compile()