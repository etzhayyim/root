from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TouchpadState(TypedDict): specs: dict; valid: bool;  def validate_specs(state: TouchpadState): return {'valid': 'DPI' in state['specs'] and 'Interface' in state['specs']}; def logger(state: TouchpadState): print(f'Processing touchpad: {state}'); return {}; graph = StateGraph(TouchpadState); graph.add_node('validate', validate_specs); graph.add_node('log', logger); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph.set_entry_point('validate'); app = graph.compile()