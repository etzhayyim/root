from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EndoscopicState(TypedDict): items: List[dict]; valid: bool; certification: str; def validate_instrument(state: EndoscopicState): state['valid'] = all('cert' in item and item['cert'] == 'ISO 13485' for item in state['items']); return state; graph = StateGraph(EndoscopicState); graph.add_node('validate', validate_instrument); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()