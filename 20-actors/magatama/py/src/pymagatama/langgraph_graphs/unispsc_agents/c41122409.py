from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LabToolState(TypedDict): tool_specs: dict; validation_passed: bool; state = LabToolState(tool_specs={}, validation_passed=False); def validate_specs(state: LabToolState): state['validation_passed'] = 'Material Grade' in state['tool_specs']; return state; graph = StateGraph(LabToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()