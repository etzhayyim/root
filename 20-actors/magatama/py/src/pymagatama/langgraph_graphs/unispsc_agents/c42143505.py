from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RepairKitState(TypedDict): components: List[str]; inspection_passed: bool; def check_compliance(state: RepairKitState): return {'inspection_passed': len(state['components']) > 0}; def finalize_procurement(state: RepairKitState): return {'status': 'approved'}; graph = StateGraph(RepairKitState); graph.add_node('validate', check_compliance); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()