from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): device_id: str; compliance_checks: List[str]; approved: bool; def val_device(state: State): state['compliance_checks'].append('ISO13485_Verified'); state['approved'] = True; return state; graph = StateGraph(State); graph.add_node('validate', val_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()