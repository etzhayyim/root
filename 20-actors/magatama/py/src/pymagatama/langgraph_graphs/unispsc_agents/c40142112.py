from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; valid: bool; risk_level: str; def validate_lead(state: PipeState): state['valid'] = state['specs'].get('purity', 0) >= 99.9; return state; def check_compliance(state: PipeState): state['risk_level'] = 'high_regulatory_review'; return state; graph = StateGraph(PipeState); graph.add_node('validate', validate_lead); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()