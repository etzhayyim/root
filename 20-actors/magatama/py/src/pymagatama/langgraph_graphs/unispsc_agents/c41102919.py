from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SolventState(TypedDict): specs: dict; compliance_check: bool; ready: bool; def validate_specs(state: SolventState): state['compliance_check'] = 'Explosion-proof Rating' in state['specs']; return state; def check_readiness(state: SolventState): state['ready'] = state['compliance_check']; return state; graph = StateGraph(SolventState); graph.add_node('validate', validate_specs); graph.add_node('readiness', check_readiness); graph.set_entry_point('validate'); graph.add_edge('validate', 'readiness'); graph.add_edge('readiness', END); graph = graph.compile()