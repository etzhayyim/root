from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): vendor_docs: List[str]; qc_status: str; def validate_pharma_cert(state: GraphState): return {'qc_status': 'verified'} if 'COA' in state['vendor_docs'] else {'qc_status': 'failed'}; def check_regulatory(state: GraphState): return {'qc_status': 'compliance_checked'}; graph = StateGraph(GraphState); graph.add_node('val_cert', validate_pharma_cert); graph.add_node('reg_check', check_regulatory); graph.set_entry_point('val_cert'); graph.add_edge('val_cert', 'reg_check'); graph.add_edge('reg_check', END); graph = graph.compile()