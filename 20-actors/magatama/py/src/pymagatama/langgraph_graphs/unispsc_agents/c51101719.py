from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_name: str; purity: float; compliant: bool; steps: List[str]; def validate_purity(state: ChemicalState): state['compliant'] = state['purity'] >= 99.0; state['steps'].append('Purity Validation'); return state; def verify_regulatory(state: ChemicalState): state['steps'].append('Regulatory Check'); return state; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_purity); graph.add_node('regulatory', verify_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph = graph.compile()