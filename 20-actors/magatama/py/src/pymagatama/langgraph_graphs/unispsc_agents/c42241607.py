from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): spec: dict; validated: bool; pipeline_status: str; def validate_spec(state: ProcureState): return {'validated': state['spec'].get('weight_capacity', 0) > 0}; def finalize(state: ProcureState): return {'pipeline_status': 'READY_FOR_RFQ'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.compile()