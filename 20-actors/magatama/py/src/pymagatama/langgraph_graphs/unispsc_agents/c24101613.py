from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PulleyState(TypedDict): specs: dict; validation_report: str; def validate_specs(state: PulleyState): return {'validation_report': 'Passed' if state['specs'].get('load_capacity') else 'Failed'}; def finalize_procurement(state: PulleyState): return {'validation_report': 'Order ready for dispatch'}; graph = StateGraph(PulleyState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()