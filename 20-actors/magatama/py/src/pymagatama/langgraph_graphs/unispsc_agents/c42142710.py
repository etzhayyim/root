from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; specs: dict; def validate_specs(state: State): return {'status': 'validated'} if 'sterilization_method' in state['specs'] else {'status': 'rejected'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()