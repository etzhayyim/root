from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtrusionState(TypedDict): metal_type: str; purity: float; specs: dict; approved: bool; def validate_purity(state: ExtrusionState): state['approved'] = state['purity'] >= 99.9; return state; def check_export_control(state: ExtrusionState): return state; workflow = StateGraph(ExtrusionState); workflow.add_node('validate', validate_purity); workflow.add_node('export_check', check_export_control); workflow.set_entry_point('validate'); workflow.add_edge('validate', 'export_check'); workflow.add_edge('export_check', END); graph = workflow.compile()