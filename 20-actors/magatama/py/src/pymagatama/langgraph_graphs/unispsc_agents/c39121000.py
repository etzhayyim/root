from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PowerProcurementState(TypedDict): specs: dict; compliance_verified: bool; def check_specs(state: PowerProcurementState): print('Validating power conditioning specs...'); state['compliance_verified'] = True; return state; graph = StateGraph(PowerProcurementState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()