from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PhotoProcState(TypedDict): content: str; license_verified: bool; metadata: dict; def verify_metadata(state: PhotoProcState): return {'license_verified': 'license' in state['metadata']}; def validate_quality(state: PhotoProcState): return {'content': 'validated_' + state['content']}; graph = StateGraph(PhotoProcState); graph.add_node('verify', verify_metadata); graph.add_node('validate', validate_quality); graph.set_entry_point('verify'); graph.add_edge('verify', 'validate'); graph.add_edge('validate', END); compiled = graph.compile()