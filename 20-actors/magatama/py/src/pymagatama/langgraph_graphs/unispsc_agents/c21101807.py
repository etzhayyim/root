from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): pump_specs: dict; inspection_results: list; status: str; def validate_specs(state: PumpState): specs = state.get('pump_specs', {}); status = 'approved' if 'flow_rate' in specs and 'head_pressure' in specs else 'rejected'; return {'status': status}; def finalize_procurement(state: PumpState): return {'status': 'processed'}; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compile_graph = graph.compile()