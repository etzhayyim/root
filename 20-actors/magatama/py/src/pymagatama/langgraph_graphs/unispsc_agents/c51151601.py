from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; concentration: float; is_gmp_certified: bool; approved: bool; def validate_purity(state: DrugState): return {'approved': state['is_gmp_certified'] and state['concentration'] > 0}; def finalize_procurement(state: DrugState): return {'approved': state['approved']}; graph = StateGraph(DrugState); graph.add_node('validate', validate_purity); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()