from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeldingGraphState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: WeldingGraphState): spec = state['specs']; state['compliance'] = spec.get('iso_cert', False) and spec.get('reach', 0) > 0; return state; graph = StateGraph(WeldingGraphState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()