from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AnalyzerState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def check_specs(state: AnalyzerState): errors = ["Invalid Range" for val in [state['specs'].get('range')] if not val]; return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(AnalyzerState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()