from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): material_spec: str; dimensions: dict; quality_cert: bool; approved: bool; def validate_specs(state: ForgingState): state['approved'] = state['quality_cert'] and 'HRC' in state['material_spec']; return state; builder = StateGraph(ForgingState); builder.add_node('validate_specs', validate_specs); builder.set_entry_point('validate_specs'); builder.add_edge('validate_specs', END); graph = builder.compile()