from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): specs: List[str]; validated: bool; defect_check: bool; def validate_specs(state: ProcurementState): state['validated'] = len(state['specs']) > 0; return state; def check_defects(state: ProcurementState): state['defect_check'] = True; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('defect_audit', check_defects); graph.add_edge('validate', 'defect_audit'); graph.add_edge('defect_audit', END); graph.set_entry_point('validate'); graph = graph.compile()