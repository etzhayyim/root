from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): purity_level: float; compliance_met: bool; processing_steps: Annotated[list, operator.add]; def validate_purity(state: MaterialState): return {'compliance_met': state['purity_level'] >= 99.9}; def finalize_log(state: MaterialState): return {'processing_steps': ['Validation complete', 'Certificate generated']}; graph = StateGraph(MaterialState); graph.add_node('validate', validate_purity); graph.add_node('finalize', finalize_log); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()