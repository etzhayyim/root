from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoardState(TypedDict): items: List[dict]; validation_errors: List[str]; def validate_specs(state: BoardState): for item in state['items']: if 'dimensions' not in item: state['validation_errors'].append('Missing dimensions'); return state; def classify_mounting(state: BoardState): return state; graph = StateGraph(BoardState); graph.add_node('validate', validate_specs); graph.add_node('classify', classify_mounting); graph.set_entry_point('validate'); graph.add_edge('validate', 'classify'); graph.add_edge('classify', END); run = graph.compile()