from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NarcoticState(TypedDict): data: dict; compliance_checked: bool; document_verified: bool; def check_compliance(state: NarcoticState): state['compliance_checked'] = True; return state; def verify_docs(state: NarcoticState): state['document_verified'] = True; return state; graph = StateGraph(NarcoticState); graph.add_node('compliance', check_compliance); graph.add_node('verification', verify_docs); graph.set_entry_point('compliance'); graph.add_edge('compliance', 'verification'); graph.add_edge('verification', END); graph = graph.compile()