from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SpecState(TypedDict): material_specs: dict; validation_results: List[str]; def validate_material_compliance(state: SpecState): return {'validation_results': ['Compliance Verified']}; def check_dimensional_accuracy(state: SpecState): return {'validation_results': state['validation_results'] + ['Dimensional Checked']}; graph = StateGraph(SpecState); graph.add_node('material_check', validate_material_compliance); graph.add_node('dimensional_check', check_dimensional_accuracy); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dimensional_check'); graph.add_edge('dimensional_check', END); graph = graph.compile()