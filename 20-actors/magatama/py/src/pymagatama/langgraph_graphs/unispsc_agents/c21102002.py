from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SortingMachineState(TypedDict): input_specs: dict; validation_results: List[str]; approved: bool; def validate_specs(state: SortingMachineState): results = []; if state['input_specs'].get('throughput') < 5: results.append('Low capacity'); return {'validation_results': results}; def check_approval(state: SortingMachineState): return 'approved' if not state['validation_results'] else 'manual_review'; graph = StateGraph(SortingMachineState); graph.add_node('validate', validate_specs); graph.add_node('approval', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); compiled_graph = graph.compile()