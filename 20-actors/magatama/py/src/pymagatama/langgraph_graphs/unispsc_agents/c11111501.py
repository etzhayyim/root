from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_id: str; purity: float; hazard_level: int; compliance_status: bool; workflow_log: Annotated[Sequence[str], operator.add]; def validate_purity(state: ChemicalState): return {'compliance_status': state['purity'] >= 0.99, 'workflow_log': ['Purity validated']}; def check_hazard(state: ChemicalState): return {'workflow_log': ['Hazard check completed']}; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_purity); graph.add_node('hazard_check', check_hazard); graph.add_edge('validate', 'hazard_check'); graph.add_edge('hazard_check', END); graph.set_entry_point('validate'); graph = graph.compile()