from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SRAMState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SRAMState): errors = []; if state['specs'].get('access_time_ns', 0) > 100: errors.append('Access time too slow'); return {'validation_errors': errors}; def finalize(state: SRAMState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(SRAMState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()