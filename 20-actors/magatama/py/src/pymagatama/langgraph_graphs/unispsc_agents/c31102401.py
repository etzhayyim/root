from langgraph.graph import StateGraph, END; from typing import TypedDict; class CastingState(TypedDict): alloy_spec: str; dimensions: dict; quality_status: bool; def validate_specs(state: CastingState): return {'quality_status': len(state['alloy_spec']) > 0}; def perform_ndt(state: CastingState): return {'quality_status': True if state['quality_status'] else False}; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.add_node('ndt', perform_ndt); graph.set_entry_point('validate'); graph.add_edge('validate', 'ndt'); graph.add_edge('ndt', END); compile_graph = graph.compile()