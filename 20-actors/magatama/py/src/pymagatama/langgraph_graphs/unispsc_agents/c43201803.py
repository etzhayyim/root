from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HDDState(TypedDict): model_number: str; capacity: float; status: str; def validate_specs(state: HDDState): return {'status': 'validated' if state['capacity'] > 0 else 'rejected'}; def check_compliance(state: HDDState): return {'status': 'compliant'}; graph = StateGraph(HDDState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()