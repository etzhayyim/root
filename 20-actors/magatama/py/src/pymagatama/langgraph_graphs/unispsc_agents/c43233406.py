from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DriverState(TypedDict): vendor: str; version: str; checksum: str; validated: bool; def validate_checksum(state: DriverState): state['validated'] = state['checksum'].startswith('sha256:'); return state; graph = StateGraph(DriverState); graph.add_node('validate', validate_checksum); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()