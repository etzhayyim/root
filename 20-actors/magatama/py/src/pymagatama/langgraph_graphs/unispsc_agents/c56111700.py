from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FurnitureState(TypedDict): items: List[dict]; validation_results: List[str]; graph = StateGraph(FurnitureState); def validate_specs(state: FurnitureState): return {'validation_results': ['Specs Verified']}; def route_procurement(state: FurnitureState): return 'complete'; graph.add_node('validate', validate_specs); graph.add_edge('validate', END); graph.set_entry_point('validate'); app = graph.compile()