from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CraftState(TypedDict): items: List[str]; validated_items: List[dict]; compliance_check: bool; def validate_craft_safety(state: CraftState): return {'validated_items': [{'item': i, 'certified': True} for i in state['items']], 'compliance_check': True}; def update_inventory(state: CraftState): return {'compliance_check': True}; graph = StateGraph(CraftState); graph.add_node('validate', validate_craft_safety); graph.add_node('inventory', update_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph = graph.compile()