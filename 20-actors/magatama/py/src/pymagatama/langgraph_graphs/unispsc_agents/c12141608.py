from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CatalystState(TypedDict): chemical_batch_id: str; purity_check: bool; active_status: bool; workflow_log: Annotated[Sequence[str], operator.add]; def validate_purity(state: CatalystState): return {'purity_check': True, 'workflow_log': ['Purity verified at 99.9%']}; def check_activity(state: CatalystState): return {'active_status': True, 'workflow_log': ['Catalytic activity confirmed']}; graph = StateGraph(CatalystState); graph.add_node('purity', validate_purity); graph.add_node('activity', check_activity); graph.set_entry_point('purity'); graph.add_edge('purity', 'activity'); graph.add_edge('activity', END); compiled_graph = graph.compile()