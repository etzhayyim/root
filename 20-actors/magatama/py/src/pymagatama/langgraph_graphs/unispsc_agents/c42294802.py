from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): status: str; spec_data: dict; graph = StateGraph(State); def validate_specs(state: State): return {'status': 'validated' if 'sterile_certification' in state['spec_data'] else 'error'}; def check_regulatory(state: State): return {'status': 'compliant' if state['status'] == 'validated' else 'non-compliant'}; graph.add_node('validate', validate_specs); graph.add_node('regulatory', check_regulatory); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); app = graph.compile()