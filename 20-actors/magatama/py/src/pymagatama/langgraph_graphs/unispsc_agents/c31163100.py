from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ConnectorState(TypedDict): connector_specs: dict; validation_results: List[str]; graph = StateGraph(ConnectorState); def validate_specs(state: ConnectorState): issues = []; specs = state['connector_specs']; if 'voltage' not in specs: issues.append('Voltage rating missing'); return {'validation_results': issues}; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()