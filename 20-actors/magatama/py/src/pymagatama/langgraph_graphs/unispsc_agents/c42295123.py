from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalDeviceState(TypedDict): device_type: str; specs: dict; compliance_status: bool; def validate_medical_device(state: SurgicalDeviceState): if state['device_type'] in ['suction', 'aspirator']: state['compliance_status'] = True; return state; graph = StateGraph(SurgicalDeviceState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()