from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; processed_steps: Annotated[Sequence[str], operator.add]; status: str; def validate_purity(state: State): return {'processed_steps': ['purity_check_performed'], 'status': 'validated'}; def compute_refinement(state: State): return {'processed_steps': ['refinement_calculated'], 'status': 'ready_for_production'}; graph = StateGraph(State); graph.add_node('purity', validate_purity); graph.add_node('refinement', compute_refinement); graph.add_edge('purity', 'refinement'); graph.add_edge('refinement', END); graph.set_entry_point('purity'); graph = graph.compile()