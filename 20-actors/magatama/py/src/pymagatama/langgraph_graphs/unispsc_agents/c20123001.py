from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class RobotState(TypedDict): messages: Annotated[Sequence[str], operator.add]; status: str; def validate_arm(state: RobotState): return {'status': 'validated'} if 'payload' in state['messages'] else {'status': 'failed'}; def compile_graph(): graph = StateGraph(RobotState); graph.add_node('validate', validate_arm); graph.set_entry_point('validate'); graph.add_edge('validate', END); return graph.compile(); graph = compile_graph()