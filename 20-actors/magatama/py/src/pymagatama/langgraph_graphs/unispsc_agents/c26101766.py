from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GovernorState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: GovernorState): state['valid'] = 'Input Voltage Range' in state['specs']; return state; def route(state: GovernorState): return 'end' if state['valid'] else 'error'; graph = StateGraph(GovernorState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph.compile()