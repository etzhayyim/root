from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; import operator; class GraphState(TypedDict): input_data: dict; validation_results: list; status: str; def validate_node(state: GraphState): return {'validation_results': ['standard_compliance_check_passed'], 'status': 'validated'}; def process_node(state: GraphState): return {'status': 'processed'}; builder = StateGraph(GraphState); builder.add_node('validate', validate_node); builder.add_node('process', process_node); builder.add_edge('validate', 'process'); builder.add_edge('process', END); builder.set_entry_point('validate'); graph = builder.compile()