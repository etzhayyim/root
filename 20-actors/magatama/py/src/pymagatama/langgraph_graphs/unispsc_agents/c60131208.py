from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KazooProcurementState(TypedDict): vendor_id: str; quality_score: float; test_results: List[str]; def validate_material(state): return {'test_results': state['test_results'] + ['Material Checked']}; def acoustic_check(state): return {'quality_score': 0.95}; graph = StateGraph(KazooProcurementState); graph.add_node('material_validation', validate_material); graph.add_node('acoustic_inspection', acoustic_check); graph.add_edge('material_validation', 'acoustic_inspection'); graph.add_edge('acoustic_inspection', END); graph.set_entry_point('material_validation'); graph = graph.compile()