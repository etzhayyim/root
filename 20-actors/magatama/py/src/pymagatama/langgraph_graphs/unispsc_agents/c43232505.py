from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoftwareState(TypedDict): vendor_id: str; license_model: str; compatibility_check: bool; graph = StateGraph(SoftwareState); def validate_lms(state: SoftwareState): return {'compatibility_check': True}; def finalize_procurement(state: SoftwareState): return {'vendor_id': 'PROV_001'}; graph.add_node('lms_validation', validate_lms); graph.add_node('finalization', finalize_procurement); graph.set_entry_point('lms_validation'); graph.add_edge('lms_validation', 'finalization'); graph.add_edge('finalization', END); compiled_graph = graph.compile()