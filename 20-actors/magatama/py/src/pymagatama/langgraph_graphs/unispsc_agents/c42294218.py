from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ENTProcurementState(TypedDict): instrument_set_id: str; compliance_docs: List[str]; inspection_status: bool; def validate_certifications(state: ENTProcurementState): print(f'Validating ISO docs for {state["instrument_set_id"]}'); return {'inspection_status': True}; def update_inventory(state: ENTProcurementState): print('Updating medical device asset registry'); return {'inspection_status': True}; graph = StateGraph(ENTProcurementState); graph.add_node('validate', validate_certifications); graph.add_node('inventory', update_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph = graph.compile()