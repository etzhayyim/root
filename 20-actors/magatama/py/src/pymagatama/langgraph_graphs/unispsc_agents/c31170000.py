from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; validation_errors: Annotated[list, operator.add]; status: str; def validate_specs(state: ProcureState): errors = []; spec = state.get('specs', {}); if 'load_rating' not in spec: errors.append('Missing load rating'); if 'material_grade' not in spec: errors.append('Missing material standard'); return {'validation_errors': errors, 'status': 'validated' if not errors else 'failed'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()