from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InsulationState(TypedDict): vendor_specs: dict; compliance_verified: bool; defects: List[str]; def validate_specs(state: InsulationState): thermal = state['vendor_specs'].get('thermal_conductivity', 0); state['compliance_verified'] = thermal < 0.05; return state; def check_safety(state: InsulationState): if state['vendor_specs'].get('fire_rating') != 'Class A': state['defects'].append('Fire rating inadequate'); return state; graph = StateGraph(InsulationState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()