from langgraph.graph import StateGraph, END; from typing import TypedDict; class LoadFrameState(TypedDict): specs: dict; valid: bool; def validate_specs(state: LoadFrameState): return {'valid': state['specs'].get('max_load', 0) > 0}; graph = StateGraph(LoadFrameState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()