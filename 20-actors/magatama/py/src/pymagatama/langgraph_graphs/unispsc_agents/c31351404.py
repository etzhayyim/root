from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): vendor_certs: dict; cad_file: str; compliance_status: bool; def validate_materials(state: ProcState): state['compliance_status'] = 'AS9100' in state.get('vendor_certs', {}); return state; def check_pressure_specs(state: ProcState): return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_materials); graph.add_node('pressure_check', check_pressure_specs); graph.set_entry_point('validate'); graph.add_edge('validate', 'pressure_check'); graph.add_edge('pressure_check', END); compiled_graph = graph.compile()