from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MonochromatorState(TypedDict): specs: dict; validation_report: list[str]; status: str; def validate_specs(state: MonochromatorState): specs = state['specs']; errors = []; if specs.get('wavelength_range_nm', 0) <= 0: errors.append('Invalid wavelength range'); return {'validation_report': errors, 'status': 'validated' if not errors else 'rejected'}; graph = StateGraph(MonochromatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()