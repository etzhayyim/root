from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AutopsySpecState(TypedDict): vendor_docs: List[str]; validation_results: dict; compliance_check: bool; def validate_instrument_specs(state: AutopsySpecState): return {'validation_results': {'grade': 'passed', 'specs': 'verified'}}; def check_regulatory_compliance(state: AutopsySpecState): return {'compliance_check': True}; graph = StateGraph(AutopsySpecState); graph.add_node('validate', validate_instrument_specs); graph.add_node('compliance', check_regulatory_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()