from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): port_specs: dict; validation_log: Annotated[list, operator.add]; def validate_electrical_specs(state: GraphState): validated = all(key in state['port_specs'] for key in ['voltage_rating', 'connector_type']); return { 'validation_log': ['Specs valid' if validated else 'Specs incomplete'] }; def check_compliance(state: GraphState): return { 'validation_log': ['Regulatory compliance checked'] }; graph = StateGraph(GraphState); graph.add_node('validate', validate_electrical_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()