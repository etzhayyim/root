from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChartState(TypedDict): data: dict; validation_errors: List[str]; def validate_chart(state: ChartState): return {'validation_errors': ['Missing Metadata'] if not state['data'].get('crs') else []}; graph = StateGraph(ChartState); graph.add_node('validate', validate_chart); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()