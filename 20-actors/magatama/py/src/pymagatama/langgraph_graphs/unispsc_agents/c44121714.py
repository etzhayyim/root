from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GripProcurementState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: GripProcurementState): state['validation_results'].append('Material safety check passed'); return state; def check_ergonomics(state: GripProcurementState): state['validation_results'].append('Ergonomic compliance verified'); return state; graph = StateGraph(GripProcurementState); graph.add_node('material_check', validate_materials); graph.add_node('ergonomic_check', check_ergonomics); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'ergonomic_check'); graph.add_edge('ergonomic_check', END); graph = graph.compile()