from langgraph.graph import StateGraph, END; from typing import TypedDict; class FreezerState(TypedDict): specs: dict; valid: bool; def validate_specs(state: FreezerState): specs = state['specs']; state['valid'] = 'temperature_range_celsius' in specs and specs['capacity_liters'] > 0; return state; graph = StateGraph(FreezerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()