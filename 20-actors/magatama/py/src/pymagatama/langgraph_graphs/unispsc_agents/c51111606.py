from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): chemical_name: str; purity: float; validated: bool; class Workflow: @staticmethod def validate_purity(state: PharmaState): state['validated'] = state['purity'] >= 99.0; return state; graph = StateGraph(PharmaState); graph.add_node('validate', Workflow.validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()