from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoltState(TypedDict): specs: dict; validation_passed: bool; def validate_specs(state: BoltState): state['validation_passed'] = all(k in state['specs'] for k in ['material', 'size', 'standard']); return state; def check_compliance(state: BoltState): print('Checking bolt compliance...'); return state; graph = StateGraph(BoltState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); bolt_graph = graph.compile()