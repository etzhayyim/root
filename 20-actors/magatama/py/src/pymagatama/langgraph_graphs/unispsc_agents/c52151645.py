from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitchenToolState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(KitchenToolState); def validate_material(state: KitchenToolState): state['validation_results'].append('Material verified safe'); return state; def check_durability(state: KitchenToolState): state['validation_results'].append('Durability tests passed'); return state; graph.add_node('validate_material', validate_material); graph.add_node('check_durability', check_durability); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_durability'); graph.add_edge('check_durability', END); graph.compile()