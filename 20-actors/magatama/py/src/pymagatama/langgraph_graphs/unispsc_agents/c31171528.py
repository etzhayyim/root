from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BearingState(TypedDict): spec_data: dict; approval_status: bool; validation_log: List[str]; def validate_bearing(state: BearingState): spec = state['spec_data']; errors = []; if spec.get('load_rating', 0) <= 0: errors.append('Invalid Load Rating'); state['approval_status'] = len(errors) == 0; state['validation_log'] = errors; return state; graph = StateGraph(BearingState); graph.add_node('validate', validate_bearing); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()