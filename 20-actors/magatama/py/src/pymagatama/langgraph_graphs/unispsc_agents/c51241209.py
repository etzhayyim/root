from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): chemical_data: dict; compliance_checks: Annotated[Sequence[str], operator.add]; status: str; def validate_hydroquinone(state: ProcureState): print('Validating Hydroquinone safety protocols...'); return {'compliance_checks': ['MSDS', 'Hazmat Handling'], 'status': 'validated'}; def finalize_procurement(state: ProcureState): return {'status': 'ready_for_order'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_hydroquinone); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()