from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DryCleaningState(TypedDict): specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_specs(state: DryCleaningState): state['compliance_status'] = 'VOC_certified' in state['specs']; state['workflow_log'].append('Validating technical specifications'); return state; def check_safety(state: DryCleaningState): state['workflow_log'].append('Checking solvent safety protocols'); return state; graph = StateGraph(DryCleaningState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()