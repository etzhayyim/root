from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class SoftwareState(TypedDict): content: str; validated: bool; compliance_report: str; def validate_reference(state: SoftwareState): is_valid = 'version' in state['content'] and 'license' in state['content']; return {'validated': is_valid, 'compliance_report': 'Pass' if is_valid else 'Schema missing'}; graph = StateGraph(SoftwareState); graph.add_node('validate', validate_reference); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()