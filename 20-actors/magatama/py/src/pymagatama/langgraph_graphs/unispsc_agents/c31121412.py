from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(CastingState); def validate_dimensions(state): return {'validation_results': ['Dimension check passed']}; def certify_material(state): return {'validation_results': ['Material test passed']}; graph.add_node('check_dims', validate_dimensions); graph.add_node('certify', certify_material); graph.set_entry_point('check_dims'); graph.add_edge('check_dims', 'certify'); graph.add_edge('certify', END); graph = graph.compile()