from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): items: List[str]; qc_passed: bool; def validate_spec(state: DentalState): state['qc_passed'] = all(['material' in item.lower() for item in state['items']]); return state; def check_regulations(state: DentalState): print('Checking PMDA/FDA compliance...'); return state; graph = StateGraph(DentalState); graph.add_node('validate', validate_spec); graph.add_node('compliance', check_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()