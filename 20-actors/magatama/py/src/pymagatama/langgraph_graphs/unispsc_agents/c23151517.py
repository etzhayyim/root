from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeldingState(TypedDict): vendor_id: str; quality_metrics: List[str]; approved: bool; def validate_torch(state: WeldingState): state['approved'] = True if 'ISO_9001' in state['quality_metrics'] else False; return state; graph = StateGraph(WeldingState); graph.add_node('validate', validate_torch); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()