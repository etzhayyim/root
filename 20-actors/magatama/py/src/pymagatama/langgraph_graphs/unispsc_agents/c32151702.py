from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ControllerState(TypedDict): interface_type: str; is_compatible: bool; compliance_docs: List[str]; def check_compatibility(state: ControllerState): state['is_compatible'] = state['interface_type'] in ['PCIe', 'USB', 'Ethernet']; return state; def validate_compliance(state: ControllerState): state['compliance_docs'] = ['ISO9001', 'FCC']; return state; graph = StateGraph(ControllerState); graph.add_node('check', check_compatibility); graph.add_node('validate', validate_compliance); graph.set_entry_point('check'); graph.add_edge('check', 'validate'); graph.add_edge('validate', END); app = graph.compile()