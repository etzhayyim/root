from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DoorFrameState(TypedDict): specs: dict; validation_results: List[str]; def validate_dimensions(state: DoorFrameState): state['validation_results'].append('Dimensional check passed'); return state; def check_fire_rating(state: DoorFrameState): state['validation_results'].append('Fire rating verified'); return state; graph = StateGraph(DoorFrameState); graph.add_node('validate_dim', validate_dimensions); graph.add_node('check_fire', check_fire_rating); graph.set_entry_point('validate_dim'); graph.add_edge('validate_dim', 'check_fire'); graph.add_edge('check_fire', END); graph = graph.compile()