from langgraph.graph import StateGraph, END; from typing import TypedDict; class SonometerState(TypedDict): specs: dict; valid: bool; def validate_specs(state: SonometerState): state['valid'] = all(k in state['specs'] for k in ['accuracy_class', 'measurement_range_db']); return state; graph = StateGraph(SonometerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()