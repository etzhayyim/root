from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ToastState(TypedDict): spec: dict; validated: bool; def validate_spec(state: ToastState): state['validated'] = 'voltage' in state['spec'] and 'capacity' in state['spec']; return state; builder = StateGraph(ToastState); builder.add_node('validator', validate_spec); builder.set_entry_point('validator'); builder.add_edge('validator', END); graph = builder.compile()