from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; def validate_materials(state: AssemblyState): return {'validation_results': ['Material check passed']}; def perform_structural_audit(state: AssemblyState): return {'validation_results': ['Audit performed']}; graph = StateGraph(AssemblyState); graph.add_node('material_check', validate_materials); graph.add_node('structural_audit', perform_structural_audit); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'structural_audit'); graph.add_edge('structural_audit', END); graph = graph.compile()