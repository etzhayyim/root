from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): chemical_report: str; dimensional_check: bool; export_license: bool; graph = StateGraph(ForgingState); def validate_chemistry(state): return {'chemical_report': 'Verified'} if state.get('chemical_report') else {'chemical_report': 'Pending'}; def check_compliance(state): return {'export_license': True} if 'ITAR' in str(state) else {'export_license': False}; graph.add_node('chemistry', validate_chemistry); graph.add_node('compliance', check_compliance); graph.set_entry_point('chemistry'); graph.add_edge('chemistry', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()