from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastState(TypedDict): specs: dict; validation_passed: bool; logs: List[str]; def validate_dimensions(state: CastState): state['validation_passed'] = all(val > 0 for val in state['specs'].values()); return state; def check_composition(state: CastState): state['logs'].append('Verifying Cu purity...'); return state; graph = StateGraph(CastState); graph.add_node('validate', validate_dimensions); graph.add_node('analyze', check_composition); graph.set_entry_point('analyze'); graph.add_edge('analyze', 'validate'); graph.add_edge('validate', END); graph = graph.compile()