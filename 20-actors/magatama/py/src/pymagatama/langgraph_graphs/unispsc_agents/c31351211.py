from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeAssemblyState(TypedDict): tube_id: str; specs: dict; validation_passed: bool; warnings: List[str]; def validate_material(state: TubeAssemblyState) -> TubeAssemblyState: state['validation_passed'] = state['specs'].get('material') == 'Waspalloy'; return state; def check_welding_docs(state: TubeAssemblyState) -> TubeAssemblyState: if 'wps' not in state['specs']: state['warnings'].append('Missing Welding Procedure Specification'); return state; graph = StateGraph(TubeAssemblyState); graph.add_node('validate', validate_material); graph.add_node('compliance', check_welding_docs); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()