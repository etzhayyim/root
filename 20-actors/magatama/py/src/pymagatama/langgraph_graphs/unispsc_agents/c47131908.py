from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; compliant: bool; def val_absorb(state: ProcureState): return {'compliant': all(['msds' in i.lower() for i in state['items']])}; graph = StateGraph(ProcureState); graph.add_node('validate', val_absorb); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()