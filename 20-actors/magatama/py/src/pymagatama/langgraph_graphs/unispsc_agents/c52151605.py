from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): manual_check_req: bool; is_electric: bool; def check_specs(state: ProcState): return 'electric_safety_check' if state['is_electric'] else 'mechanical_qc_check'; graph = StateGraph(ProcState); graph.add_node('mechanical_qc_check', lambda x: {'manual_check_req': False}); graph.add_node('electric_safety_check', lambda x: {'manual_check_req': True}); graph.set_entry_point('mechanical_qc_check'); graph.add_edge('mechanical_qc_check', END); graph.add_edge('electric_safety_check', END); app = graph.compile()