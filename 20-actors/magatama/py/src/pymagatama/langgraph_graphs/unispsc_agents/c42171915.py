from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EMTBagState(TypedDict): bag_specs: dict; validation_errors: List[str]; def validate_bag(state: EMTBagState): errors = []; if state['bag_specs'].get('durability') != 'high': errors.append('Insufficient durability rating'); return {'validation_errors': errors}; graph = StateGraph(EMTBagState); graph.add_node('validate_specs', validate_bag); graph.set_entry_point('validate_specs'); graph.add_edge('validate_specs', END); graph = graph.compile()