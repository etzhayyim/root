from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EggProcurementState(TypedDict): items: List[str]; qc_passed: bool; temp_logs: List[float]; def validate_temperature(state: EggProcurementState): return {'qc_passed': all(t < 5.0 for t in state['temp_logs'])}; def finalize_procurement(state: EggProcurementState): return {'items': state['items']}; graph = StateGraph(EggProcurementState); graph.add_node('validate', validate_temperature); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()