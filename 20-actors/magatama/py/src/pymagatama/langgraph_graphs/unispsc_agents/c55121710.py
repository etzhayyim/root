from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrafficSignState(TypedDict): sign_specs: dict; compliance_checked: bool; validation_log: List[str]; def validate_sign_specs(state: TrafficSignState): print('Validating traffic sign dimensions and reflectivity'); return {'compliance_checked': True}; def update_log(state: TrafficSignState): return {'validation_log': ['Specs validated against regulatory standards']}; graph = StateGraph(TrafficSignState); graph.add_node('validate', validate_sign_specs); graph.add_node('log', update_log); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); graph = graph.compile()