from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class JuiceState(TypedDict): vendor: str; quality_score: float; compliance_docs: List[str]; approved: bool; def check_quality(state: JuiceState): return {'approved': state['quality_score'] > 85 and 'HACCP' in state['compliance_docs']}; graph = StateGraph(JuiceState); graph.add_node('qc', check_quality); graph.set_entry_point('qc'); graph.add_edge('qc', END); graph.compile()