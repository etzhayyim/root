from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class LacingState(TypedDict): spec: dict; validated: bool; from pydantic import BaseModel, Field; class CordSpec(BaseModel): tensile_strength: float; material: str; def validate_spec(state: LacingState): state['validated'] = state['spec'].get('tensile_strength', 0) > 10; return state; graph = StateGraph(LacingState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()