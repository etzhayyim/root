from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): assay_value: float; status: str; def validate_purity(state: PharmaState): return {'status': 'passed' if state['assay_value'] >= 99.0 else 'failed'}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()