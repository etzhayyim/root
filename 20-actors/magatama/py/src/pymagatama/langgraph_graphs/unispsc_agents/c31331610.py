from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class TitaniumState(TypedDict): specs: dict; validation_results: List[str]; approved: bool; def validate_specs(state: TitaniumState): state['validation_results'].append('Checking material grade and WPQR certification'); return state; def check_quality(state: TitaniumState): state['approved'] = True if 'WPQR' in state['specs'] else False; return state; graph = StateGraph(TitaniumState); graph.add_node('validate', validate_specs); graph.add_node('qc', check_quality); graph.add_edge('validate', 'qc'); graph.add_edge('qc', END); graph.set_entry_point('validate'); graph = graph.compile()