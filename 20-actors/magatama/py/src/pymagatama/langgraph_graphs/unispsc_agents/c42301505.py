from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class AgentState(TypedDict): tasks: Annotated[list, operator.add]; validation_results: dict; graph = StateGraph(AgentState); def validate_spec(state: AgentState): return {'validation_results': {'is_medical_grade': True}}; def process_procurement(state: AgentState): return {'tasks': ['verify_material_spec', 'check_disinfectant_rating']}; graph.add_node('validate', validate_spec); graph.add_node('process', process_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); app = graph.compile()