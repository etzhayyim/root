from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class InterlockState(TypedDict): vendor_certs: List[str]; safety_rating: str; status: str; def validate_compliance(state: InterlockState): return { "status": "valid" if "ISO_13849" in state["vendor_certs"] else "rejected" }; def finalize_procurement(state: InterlockState): return { "status": "approved" }; graph = StateGraph(InterlockState); graph.add_node("validate", validate_compliance); graph.add_node("finalize", finalize_procurement); graph.set_entry_point("validate"); graph.add_edge("validate", "finalize"); graph.add_edge("finalize", END); app = graph.compile()