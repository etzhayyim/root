from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class FlowState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: FlowState): state['compliance'] = 'PressureRating' in state['specs'] and 'MaterialType' in state['specs']; return state; def check_export(state: FlowState): state['approved'] = state['compliance'] and state['specs'].get('MaterialType') != 'Restricted'; return state; graph = StateGraph(FlowState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()