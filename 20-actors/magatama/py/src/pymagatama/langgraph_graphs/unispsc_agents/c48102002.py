from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class BoothState(TypedDict): inputs: dict; validation_errors: Annotated[list, operator.add]; status: str; def validate_booth(state: BoothState): errs = []; specs = state['inputs'].get('specs', {}); if not specs.get('fire_rating'): errs.append('Missing fire rating'); return {'validation_errors': errs, 'status': 'validated' if not errs else 'failed'}; graph = StateGraph(BoothState); graph.add_node('validate', validate_booth); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()