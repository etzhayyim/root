from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: Annotated[list, operator.add]; compliance_flag: bool; def validate_spec(state: ProcState): state['compliance_flag'] = all(item.get('cert') for item in state['items']); return state; def finalize_order(state: ProcState): print('Processing slide stainer compliance and inventory check.'); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()