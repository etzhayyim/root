from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LensState(TypedDict): lens_specs: dict; validation_results: List[str]; def validate_optical(state: LensState): issues = [] if state['lens_specs'].get('resolution', 0) < 1000: issues.append('Low resolution'); return {'validation_results': issues}; def check_sterilization(state: LensState): return {'validation_results': state['validation_results'] + ['CertVerified']}; graph = StateGraph(LensState); graph.add_node('optic_check', validate_optical); graph.add_node('sterility_check', check_sterilization); graph.set_entry_point('optic_check'); graph.add_edge('optic_check', 'sterility_check'); graph.add_edge('sterility_check', END); graph = graph.compile()