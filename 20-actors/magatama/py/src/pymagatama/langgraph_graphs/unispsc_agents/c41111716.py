from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BorescopeState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: BorescopeState): state['approved'] = state['specs'].get('ip_rating', 0) >= 67; return state; graph = StateGraph(BorescopeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()