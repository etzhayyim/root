from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_plate_specs(state: AssemblyState): errs = []; if state['specs'].get('tensile_strength', 0) < 400: errs.append('Insufficient tensile strength'); return {'validation_errors': errs, 'approved': len(errs) == 0}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_plate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()