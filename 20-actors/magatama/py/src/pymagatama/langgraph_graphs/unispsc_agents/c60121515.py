from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PastelState(TypedDict): content: str; validated: bool; analysis: str; def validate_materials(state: PastelState): return {'validated': 'non-toxic' in state['content'].lower()}; def finalize_report(state: PastelState): return {'analysis': 'Procurement criteria met for oil-based pastels.'}; graph = StateGraph(PastelState); graph.add_node('validate', validate_materials); graph.add_node('finalize', finalize_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()