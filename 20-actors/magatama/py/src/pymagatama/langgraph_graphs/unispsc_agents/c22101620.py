from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserProcState(TypedDict): input_specs: dict; validation_report: str; approved: bool; def validate_specs(state: LaserProcState): if state['input_specs'].get('laser_power_watts', 0) > 1000: return {'validation_report': 'High power, check export controls', 'approved': True}; return {'validation_report': 'Standard operation', 'approved': True}; graph = StateGraph(LaserProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()