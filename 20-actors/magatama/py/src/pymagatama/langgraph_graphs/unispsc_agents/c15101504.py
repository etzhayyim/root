from typing import TypedDict, Annotated, List, Dict; from langgraph.graph import StateGraph, END; class IronOreState(TypedDict): chemical_specs: Dict; density_check: bool; compliance_status: str; def validate_purity(state: IronOreState): purity = state['chemical_specs'].get('Fe_content', 0); return {'density_check': purity > 65.0}; def check_compliance(state: IronOreState): return {'compliance_status': 'APPROVED' if state['density_check'] else 'REJECTED'}; graph = StateGraph(IronOreState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()