from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; valid: bool; engine_config: str; def validate_specs(state: PipeState): state['valid'] = state['specs'].get('diameter') > 0; return state; def configure_engine(state: PipeState): state['engine_config'] = 'High-Speed-Rotating-Head'; return state; graph = StateGraph(PipeState); graph.add_node('validate', validate_specs); graph.add_node('configure', configure_engine); graph.set_entry_point('validate'); graph.add_edge('validate', 'configure'); graph.add_edge('configure', END); graph = graph.compile()