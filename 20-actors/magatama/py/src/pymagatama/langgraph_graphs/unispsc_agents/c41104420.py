from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IncubationState(TypedDict): vendor_docs: List[str]; inspection_passed: bool; safety_compliance: bool; def validate_specs(state: IncubationState): return {'inspection_passed': True} if 'calibration_cert' in state['vendor_docs'] else {'inspection_passed': False}; def check_safety(state: IncubationState): return {'safety_compliance': True}; graph = StateGraph(IncubationState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()