from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GrapeState(TypedDict): harvest_date: str; quality_score: float; storage_temp: float; approved: bool; def check_quality(state: GrapeState): state['approved'] = state['quality_score'] > 0.8; return state; def check_temp(state: GrapeState): state['approved'] = state['storage_temp'] < 5.0; return state; graph = StateGraph(GrapeState); graph.add_node('quality', check_quality); graph.add_node('temp', check_temp); graph.set_entry_point('quality'); graph.add_edge('quality', 'temp'); graph.add_edge('temp', END); graph = graph.compile()