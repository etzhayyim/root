from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): status: str; spec_validation: bool; def check_certification(state: ProcurementState): state['spec_validation'] = True; return state; def finalize_order(state: ProcurementState): state['status'] = 'ready_for_review'; return state; graph = StateGraph(ProcurementState); graph.add_node('cert_check', check_certification); graph.add_node('finalizer', finalize_order); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'finalizer'); graph.add_edge('finalizer', END); compiled_graph = graph.compile()