from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_data: dict; compliance_checks: List[str]; inspection_status: str; def validate_purity(state: PharmState): state['compliance_checks'].append('Purity Verified'); return {'inspection_status': 'COMPLIANT'} if state['chemical_data']['purity'] > 0.99 else {'inspection_status': 'REJECTED'}; def verify_gmp(state: PharmState): state['compliance_checks'].append('GMP Verified'); return state; graph = StateGraph(PharmState); graph.add_node('purity_check', validate_purity); graph.add_node('gmp_audit', verify_gmp); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'gmp_audit'); graph.add_edge('gmp_audit', END); graph = graph.compile()