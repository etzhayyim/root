from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CondimentCounterState(TypedDict): vendor_specs: dict; compliance_verified: bool; validation_log: List[str]; def validate_specs(state: CondimentCounterState): state['validation_log'].append('Validating material grade and NSF status.'); state['compliance_verified'] = True if 'NSF_certified' in state['vendor_specs'] else False; return state; graph = StateGraph(CondimentCounterState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()