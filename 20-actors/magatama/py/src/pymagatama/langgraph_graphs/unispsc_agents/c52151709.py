from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): items: List[str]; validated_items: List[str]; status: str; def validate_cutlery(state: ProcurementState): validated = [item for item in state['items'] if 'food_grade' in item]; return {'validated_items': validated, 'status': 'validated' if validated else 'failed'}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_cutlery); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()