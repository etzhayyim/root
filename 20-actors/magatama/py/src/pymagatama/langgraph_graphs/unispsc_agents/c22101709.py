from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToolState(TypedDict): specs: List[str]; validated: bool; def validate_specs(state: ToolState): return {'validated': all(len(s) > 0 for s in state['specs'])}; graph = StateGraph(ToolState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()