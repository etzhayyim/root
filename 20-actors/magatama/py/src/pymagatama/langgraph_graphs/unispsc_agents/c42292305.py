from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): device_id: str; compliance_checks: List[str]; approved: bool; def validate_instrument(state: State): state['compliance_checks'].append('ISO_13485_VERIFIED'); state['approved'] = True; return state; builder = StateGraph(State); builder.add_node('validate', validate_instrument); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()