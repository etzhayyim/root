from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): needle_gauge: str; is_sterile: bool; compliance: List[str]; def validate_specs(state: State): return {'compliance': ['ISO-7864-Standard'] if state['is_sterile'] else ['Rejected']}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()