from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MedicalChairState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: MedicalChairState): state['compliance'] = state['specs'].get('weight_capacity', 0) > 100; return state; def finalize_order(state: MedicalChairState): state['approved'] = state['compliance']; return state; graph = StateGraph(MedicalChairState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()