from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ConnectorState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_connector(state: ConnectorState): specs = state['specs']; errors = []; if specs.get('ip_rating', 0) < 65: errors.append('Insufficient IP rating'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(ConnectorState); graph.add_node('validate', validate_connector); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()