from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): item: str; specs: dict; approved: bool; def validate_specs(state: ProcureState): state['approved'] = 'Material composition' in state['specs']; return state; def route_step(state: ProcureState): return 'validate' if state['specs'] else END; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()