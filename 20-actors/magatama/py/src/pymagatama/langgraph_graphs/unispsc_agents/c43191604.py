from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class TapeDriveState(TypedDict): drive_id: str; capacity: float; status: str; logs: Annotated[list, operator.add]; def validate_drive(state: TapeDriveState) -> TapeDriveState: return {'status': 'validated' if state['capacity'] > 0 else 'failed', 'logs': ['validation_complete']}; def check_compatibility(state: TapeDriveState) -> TapeDriveState: return {'status': 'compatible', 'logs': ['compatibility_checked']}; graph = StateGraph(TapeDriveState); graph.add_node('validate', validate_drive); graph.add_node('compatibility', check_compatibility); graph.add_edge('validate', 'compatibility'); graph.add_edge('compatibility', END); graph.set_entry_point('validate'); graph = graph.compile()