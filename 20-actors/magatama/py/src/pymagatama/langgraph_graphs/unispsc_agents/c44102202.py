from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ScannerState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_spec(state: ScannerState):   specs = state['input_data'].get('specs', {})   results = ['Passed' if specs.get('speed') > 0 else 'Failed']   return {'validation_results': results}; def update_status(state: ScannerState):   return {'status': 'Validated' if 'Passed' in state['validation_results'] else 'Error'}; graph = StateGraph(ScannerState); graph.add_node('validate', validate_spec); graph.add_node('status', update_status); graph.set_entry_point('validate'); graph.add_edge('validate', 'status'); graph.add_edge('status', END); graph = graph.compile()