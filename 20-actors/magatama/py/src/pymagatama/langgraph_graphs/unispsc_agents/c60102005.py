from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BookState(TypedDict): title: str; content_check: bool; compliance_status: str; def validate_content(state: BookState): state['content_check'] = 'educational' in state['title'].lower(); return {'content_check': state['content_check']}; def check_compliance(state: BookState): state['compliance_status'] = 'APPROVED' if state['content_check'] else 'REJECTED'; return {'compliance_status': state['compliance_status']}; graph = StateGraph(BookState); graph.add_node('validate', validate_content); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()