from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExamState(TypedDict): card_id: str; verified: bool; clinical_compliance: bool; def validate_card_integrity(state: ExamState): return {'verified': True}; def check_clinical_compliance(state: ExamState): return {'clinical_compliance': True}; graph = StateGraph(ExamState); graph.add_node('validate', validate_card_integrity); graph.add_node('compliance', check_clinical_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()