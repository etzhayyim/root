from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class FluidProcurementState(TypedDict): fluid_id: str; quality_checks: Annotated[Sequence[str], operator.add]; safety_cleared: bool; def validate_spec(state: FluidProcurementState): print(f'Validating specs for {state["fluid_id"]}'); return {'quality_checks': ['viscosity_passed', 'thermal_stable_passed']}; def assess_safety(state: FluidProcurementState): return {'safety_cleared': True}; graph = StateGraph(FluidProcurementState); graph.add_node('validate', validate_spec); graph.add_node('safety', assess_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph = graph.compile()