from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): commodity_code: str; spec_data: dict; approved: bool; def validate_ingot_specs(state: ProcurementState): state['approved'] = 'PurityLevel' in state['spec_data'] and state['spec_data']['PurityLevel'] >= 99.0; return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_ingot_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()