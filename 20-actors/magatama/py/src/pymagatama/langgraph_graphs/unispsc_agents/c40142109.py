from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PipeState(TypedDict): specs: dict; valid: bool;  def validate_pipe(state: PipeState): state['valid'] = state['specs'].get('compressive_strength_mpa', 0) >= 30; return state; graph = StateGraph(PipeState); graph.add_node('validate', validate_pipe); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()