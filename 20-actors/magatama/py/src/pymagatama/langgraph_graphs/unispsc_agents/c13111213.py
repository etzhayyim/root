from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class LubricantState(TypedDict): input_data: dict; validation_results: Annotated[Sequence[str], operator.add]; status: str; def validate_sds(state: LubricantState): return {validation_results: ['SDS verified']}; def check_storage(state: LubricantState): return {status: 'COMPLIANT_FOR_STORAGE'}; builder = StateGraph(LubricantState); builder.add_node('sds_check', validate_sds); builder.add_node('storage_check', check_storage); builder.set_entry_point('sds_check'); builder.add_edge('sds_check', 'storage_check'); builder.add_edge('storage_check', END); graph = builder.compile()