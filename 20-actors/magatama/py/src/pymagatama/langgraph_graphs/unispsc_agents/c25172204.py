from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TruckDoorState(TypedDict): door_id: str; specifications: dict; validation_status: bool; error_log: List[str]; def validate_door_specs(state: TruckDoorState): valid = all(k in state['specifications'] for k in ['gauge', 'material']); return {'validation_status': valid, 'error_log': [] if valid else ['Missing specs']}; def assembly_workflow(state: TruckDoorState): return {'validation_status': True}; graph = StateGraph(TruckDoorState); graph.add_node('validate', validate_door_specs); graph.add_node('assemble', assembly_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); compiled_graph = graph.compile()