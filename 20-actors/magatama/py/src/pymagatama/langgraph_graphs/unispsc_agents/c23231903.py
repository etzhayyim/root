from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): machine_id: str; status: str; needs_calibration: bool; def validate_spec(state: MachineState): return {'status': 'validated' if state['machine_id'] else 'error'}; def check_calibration(state: MachineState): return {'needs_calibration': True if state['status'] == 'validated' else False}; graph = StateGraph(MachineState); graph.add_node('validate', validate_spec); graph.add_node('calibrate', check_calibration); graph.set_entry_point('validate'); graph.add_edge('validate', 'calibrate'); graph.add_edge('calibrate', END); compiled_graph = graph.compile()