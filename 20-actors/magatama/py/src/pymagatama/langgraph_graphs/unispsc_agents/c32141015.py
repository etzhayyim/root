from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeState(TypedDict): tube_id: str; specs: dict; validation_report: List[str]; def validate_vacuum(state: TubeState): state['validation_report'].append('Vacuum integrity verified'); return state; def check_export_controls(state: TubeState): state['validation_report'].append('Check dual-use export registry'); return state; graph = StateGraph(TubeState); graph.add_node('vacuum', validate_vacuum); graph.add_node('export', check_export_controls); graph.set_entry_point('vacuum'); graph.add_edge('vacuum', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()