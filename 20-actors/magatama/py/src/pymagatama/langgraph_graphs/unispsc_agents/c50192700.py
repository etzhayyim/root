from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MealState(TypedDict): items: List[str]; inspection_passed: bool; def validate_safety(state: MealState): state['inspection_passed'] = all(item.get('cert_valid', False) for item in state['items']); return state; def log_expiry(state: MealState): print('Checking expiration dates for batch'); return state; graph = StateGraph(MealState); graph.add_node('validate', validate_safety); graph.add_node('log', log_expiry); graph.set_entry_point('validate'); graph.add_edge('validate', 'log'); graph.add_edge('log', END); compiled_graph = graph.compile()