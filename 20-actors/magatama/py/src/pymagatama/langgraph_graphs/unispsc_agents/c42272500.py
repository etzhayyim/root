from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class AnesthesiaState(TypedDict): device_id: str; compliance_docs: List[str]; qc_passed: bool; def check_compliance(state: 'AnesthesiaState'): return {'qc_passed': all(['ISO_13485' in doc for doc in state['compliance_docs']])}; def workflow(state: 'AnesthesiaState'): return 'check_compliance'; graph = StateGraph(AnesthesiaState); graph.add_node('compliance_check', check_compliance); graph.add_edge('compliance_check', END); graph.set_entry_point('compliance_check'); graph = graph.compile()