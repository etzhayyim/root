from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): data: dict; status: str; def validate_vector_data(state: ProcState) -> ProcState: state['status'] = 'VALIDATED' if 'vector_payload' in state['data'] else 'FAILED'; return state; def render_sequence(state: ProcState) -> ProcState: state['status'] = 'RENDERED'; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_vector_data); graph.add_node('render', render_sequence); graph.set_entry_point('validate'); graph.add_edge('validate', 'render'); graph.add_edge('render', END); graph = graph.compile()