from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_data: dict; compliance_status: bool; workflow_steps: List[str]; def validate_api(state: PharmState): state['compliance_status'] = 'GMP' in state['chemical_data'].get('certifications', []) and state['chemical_data'].get('purity', 0) >= 99.0; state['workflow_steps'].append('VALIDATE_CERT'); return state; def check_regulatory(state: PharmState): state['workflow_steps'].append('FDA_REVIEW'); return state; graph = StateGraph(PharmState); graph.add_node('validate', validate_api); graph.add_node('regulatory', check_regulatory); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); graph.set_entry_point('validate'); graph = graph.compile()