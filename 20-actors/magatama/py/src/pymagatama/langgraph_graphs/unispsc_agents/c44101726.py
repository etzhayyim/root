from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): model: str; version: str; validated: bool; def validate_compatibility(state): return {"validated": state.get("model") in ["LaserJet", "OfficeJet"]}; def issue_license(state): return {"validated": True}; graph = StateGraph(GraphState); graph.add_node("validate", validate_compatibility); graph.add_node("issue", issue_license); graph.set_entry_point("validate"); graph.add_edge("validate", "issue"); graph.add_edge("issue", END); graph = graph.compile()