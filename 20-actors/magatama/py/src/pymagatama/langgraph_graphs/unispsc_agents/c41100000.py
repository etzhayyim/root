from langgraph.graph import StateGraph, END; from typing import TypedDict; class LabEquipmentState(TypedDict): vendor_specs: dict; compliance_check: bool; graph = StateGraph(LabEquipmentState); def validate_calibration(state): state['compliance_check'] = 'ISO_cert' in state['vendor_specs']; return state; graph.add_node('calibrate', validate_calibration); graph.set_entry_point('calibrate'); graph.add_edge('calibrate', END); app = graph.compile()