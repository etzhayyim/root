from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): vendor_id: str; compliance_docs: List[str]; inspection_passed: bool; def validate_api(state: PharmaState): return {'inspection_passed': len(state['compliance_docs']) >= 3}; def audit_workflow(state: PharmaState): return {'inspection_passed': True}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_api); graph.add_node('audit', audit_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'audit'); graph.add_edge('audit', END); graph = graph.compile()