from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class InjectorState(TypedDict): specs: dict; validation_log: List[str]; graph = StateGraph(InjectorState); def validate_specs(state: InjectorState): state['validation_log'].append('Checking pressure limits'); return {'validation_log': state['validation_log']}; def check_compliance(state: InjectorState): state['validation_log'].append('Verifying ISO 13485'); return {'validation_log': state['validation_log']}; graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()