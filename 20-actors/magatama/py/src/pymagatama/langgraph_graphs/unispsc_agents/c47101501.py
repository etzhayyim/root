from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ProcessState(TypedDict): input_specs: dict; validated_data: dict; status: str; def validate_specs(state: ProcessState): state['status'] = 'VALIDATING'; if 'flow_rate' in state['input_specs']: state['validated_data'] = {'spec_check': 'PASS'}; return state; def run_workflow(state: ProcessState): state['status'] = 'COMPLETED'; return state; graph = StateGraph(ProcessState); graph.add_node('validate', validate_specs); graph.add_node('compute', run_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'compute'); graph.add_edge('compute', END); graph = graph.compile()