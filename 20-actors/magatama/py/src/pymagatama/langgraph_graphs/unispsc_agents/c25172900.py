from langgraph.graph import StateGraph, END; from typing import TypedDict; class LightingState(TypedDict): spec: dict; compliance: bool; validation_log: list; def validate_compliance(state: LightingState): state['compliance'] = state['spec'].get('E-mark') != None; return state; graph = StateGraph(LightingState); graph.add_node('validate', validate_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()