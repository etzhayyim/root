from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ClusterState(TypedDict): cluster_id: str; specifications: dict; validation_report: str; graph = StateGraph(ClusterState); def validate_specs(state: ClusterState): state['validation_report'] = 'Validating voltage and protocol compliance...'; return state; def check_safety(state: ClusterState): state['validation_report'] += ' Safety compliance checks passed.'; return state; graph.add_node('validator', validate_specs); graph.add_node('safety_check', check_safety); graph.set_entry_point('validator'); graph.add_edge('validator', 'safety_check'); graph.add_edge('safety_check', END); compiled_graph = graph.compile()