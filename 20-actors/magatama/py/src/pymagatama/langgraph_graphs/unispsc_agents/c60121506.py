from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MarkerState(TypedDict): vendor_id: str; ink_safety_data: str; compliance: bool; def validate_ink(state: MarkerState): return {'compliance': 'RoHS' in state['ink_safety_data']}; def finalize_order(state: MarkerState): return {'compliance': True}; graph = StateGraph(MarkerState); graph.add_node('validate', validate_ink); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()