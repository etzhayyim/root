from langgraph.graph import StateGraph, END; from typing import TypedDict; class SurgicalProcurementState(TypedDict): vendor_id: str; device_specs: dict; compliance_verified: bool; graph = StateGraph(SurgicalProcurementState); def validate_specifications(state): state['compliance_verified'] = all(k in state['device_specs'] for k in ['material', 'sterilization']); return state; def check_regulatory(state): state['compliance_verified'] = state.get('compliance_verified') and state['device_specs'].get('regulatory_status') == 'approved'; return state; graph.add_node('validate', validate_specifications); graph.add_node('regulatory', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()