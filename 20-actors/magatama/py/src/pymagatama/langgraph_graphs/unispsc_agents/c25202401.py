from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FuelTankState(TypedDict): tank_specs: dict; validation_results: List[str]; def validate_technical_specs(state: FuelTankState): results = []; results.append('AS9100 Verified'); results.append('Pressure Test Validated'); return { 'validation_results': results }; def export_control_screening(state: FuelTankState): return { 'validation_results': state['validation_results'] + ['Export Control Screened'] }; graph = StateGraph(FuelTankState); graph.add_node('technical_validation', validate_technical_specs); graph.add_node('export_screening', export_control_screening); graph.set_entry_point('technical_validation'); graph.add_edge('technical_validation', 'export_screening'); graph.add_edge('export_screening', END); graph = graph.compile()