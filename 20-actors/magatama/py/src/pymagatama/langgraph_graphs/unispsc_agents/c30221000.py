from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StructureState(TypedDict): vendor: str; design_specs: dict; compliance_passed: bool; struct_graph = StateGraph(StructureState); def validate_engineering(state: StructureState): state['compliance_passed'] = 'load_limit' in state['design_specs']; return state; def final_approval(state: StructureState): return state; struct_graph.add_node('validate', validate_engineering); struct_graph.add_node('approve', final_approval); struct_graph.set_entry_point('validate'); struct_graph.add_edge('validate', 'approve'); struct_graph.add_edge('approve', END); graph = struct_graph.compile()