from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): ingredient: str; temp_compliant: bool; gmp_verified: bool; def check_storage(state): return {'temp_compliant': True}; def verify_gmp(state): return {'gmp_verified': True}; graph = StateGraph(PharmaState); graph.add_node('storage_check', check_storage); graph.add_node('gmp_check', verify_gmp); graph.set_entry_point('storage_check'); graph.add_edge('storage_check', 'gmp_check'); graph.add_edge('gmp_check', END); graph = graph.compile()