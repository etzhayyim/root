from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CosmeticProcessState(TypedDict): items: List[str]; qc_passed: bool; compliance_docs: List[str]; def validate_ingredients(state: CosmeticProcessState): return {'qc_passed': all('banned' not in item for item in state['items'])}; def check_compliance(state: CosmeticProcessState): return {'compliance_docs': ['FDA', 'ISO22716']}; graph = StateGraph(CosmeticProcessState); graph.add_node('validate', validate_ingredients); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()