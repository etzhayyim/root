from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class RobotProcurementState(TypedDict): payload: float; reach: float; dpr_compliance: bool; verified_specs: bool; graph = StateGraph(RobotProcurementState); def validate_payload(state): return {'verified_specs': state['payload'] > 0}; def check_export_control(state): return {'dpr_compliance': True}; graph.add_node('validate', validate_payload); graph.add_node('export', check_export_control); graph.set_entry_point('validate'); graph.add_edge('validate', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()