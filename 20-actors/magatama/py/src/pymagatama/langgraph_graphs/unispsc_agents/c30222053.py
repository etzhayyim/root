from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class State(TypedDict): status: str; safety_checks: bool; compliance_docs: bool; def check_structural_integrity(state: State): return {'safety_checks': True} if state.get('status') == 'verified' else {'safety_checks': False}; def verify_compliance(state: State): return {'compliance_docs': True}; graph = StateGraph(State); graph.add_node('integrity_check', check_structural_integrity); graph.add_node('compliance_check', verify_compliance); graph.set_entry_point('integrity_check'); graph.add_edge('integrity_check', 'compliance_check'); graph.add_edge('compliance_check', END); graph = graph.compile()