from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class StorageState(TypedDict): vendor_specs: dict; compliance_passed: bool; inspector_report: str; def validate_specs(state: StorageState): specs = state['vendor_specs']; is_valid = all(key in specs for key in ['load_capacity', 'dimensions']); return {'compliance_passed': is_valid}; def routing(state: StorageState): return 'valid' if state['compliance_passed'] else 'error'; graph = StateGraph(StorageState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()