from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; validation_results: List[str]; def validate_material_grade(state: AssemblyState): state['validation_results'].append('Grade Verified'); return state; def validate_weld_integrity(state: AssemblyState): state['validation_results'].append('Weld Inspected'); return state; graph = StateGraph(AssemblyState); graph.add_node('material', validate_material_grade); graph.add_node('weld', validate_weld_integrity); graph.set_entry_point('material'); graph.add_edge('material', 'weld'); graph.add_edge('weld', END); graph.compile()