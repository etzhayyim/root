from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PacifierState(TypedDict): vendor_docs: dict; compliance_passed: bool; inspection_report: str; def validate_safety(state: PacifierState): return {'compliance_passed': 'BPA_free' in state['vendor_docs'] and 'ASTM_F963' in state['vendor_docs']}; def final_review(state: PacifierState): return {'inspection_report': 'Passed' if state['compliance_passed'] else 'Failed'}; graph = StateGraph(PacifierState); graph.add_node('validate', validate_safety); graph.add_node('review', final_review); graph.add_edge('validate', 'review'); graph.add_edge('review', END); graph.set_entry_point('validate')