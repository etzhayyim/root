from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PumpState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: PumpState): specs = state['specs']; errors = []; if specs.get('flow_rate', 0) <= 0: errors.append('Flow rate must be positive.'); if not specs.get('chem_resistant'): errors.append('Chemical resistance missing.'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(PumpState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph_app = graph.compile()