from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SecurityState(TypedDict): vendor: str; security_cert: bool; vulnerability_scan: bool; graph = StateGraph(SecurityState); def check_cert(state): state['security_cert'] = True; return state; def run_scan(state): state['vulnerability_scan'] = True; return state; graph.add_node('cert_check', check_cert); graph.add_node('vuln_scan', run_scan); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'vuln_scan'); graph.add_edge('vuln_scan', END); app = graph.compile()