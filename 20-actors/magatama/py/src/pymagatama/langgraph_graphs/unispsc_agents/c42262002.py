from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class EmbalmingGraphState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_tube_specs(state: EmbalmingGraphState): status = 'Valid' if state['specs'].get('sterilization') == 'EO' else 'Invalid'; return {'validation_results': [status], 'status': status}; graph = StateGraph(EmbalmingGraphState); graph.add_node('validate', validate_tube_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()