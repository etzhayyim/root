from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SteelState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: SteelState): state['valid'] = 'grade' in state['specs'] and 'thickness' in state['specs']; state['log'].append('Validation complete'); return state; graph = StateGraph(SteelState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()