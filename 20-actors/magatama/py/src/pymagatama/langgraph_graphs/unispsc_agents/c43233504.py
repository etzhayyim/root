from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChatSoftwareState(TypedDict): vendor_url: str; security_cert: bool; compliance_status: str; def validate_security(state: ChatSoftwareState): return {'security_cert': True if 'ISO' in state['compliance_status'] else False}; def check_gateway(state: ChatSoftwareState): return {'compliance_status': 'Verified' if state['security_cert'] else 'Rejected'}; graph = StateGraph(ChatSoftwareState); graph.add_node('validate', validate_security); graph.add_node('compliance', check_gateway); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); app = graph.compile()