from langgraph.graph import StateGraph, END; from typing import TypedDict; class OrganBathState(TypedDict): specs: dict; compliance: bool; status: str; def validate_specs(state: OrganBathState): return {'compliance': state['specs'].get('temp_range') != None}; def approve_order(state: OrganBathState): return {'status': 'APPROVED' if state['compliance'] else 'REJECTED'}; graph = StateGraph(OrganBathState); graph.add_node('validate', validate_specs); graph.add_node('approve', approve_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()