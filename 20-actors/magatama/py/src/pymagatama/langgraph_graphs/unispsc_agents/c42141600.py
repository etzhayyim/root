from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validation_results: dict; def validate_medical_grade(state: ProcurementState): return {'validation_results': {'compliant': all(['sterilized' in i.lower() for i in state['items']])}}; def finalize_procurement(state: ProcurementState): return {'validation_results': {'status': 'approved'}}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_medical_grade); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()