from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EraserState(TypedDict): input_data: dict; validation_results: List[str]; def validate_material(state: EraserState): state['validation_results'].append('Material Safety Data Sheet (MSDS) verified'); return state; def check_quality(state: EraserState): state['validation_results'].append('Adsorption test passed'); return state; graph = StateGraph(EraserState); graph.add_node('validate', validate_material); graph.add_node('quality_check', check_quality); graph.set_entry_point('validate'); graph.add_edge('validate', 'quality_check'); graph.add_edge('quality_check', END); graph = graph.compile()