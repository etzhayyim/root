from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: Annotated[Sequence[str], operator.add]; status: str; def validate_cert(state: ProcureState): return {'status': 'CERTIFIED' if 'ISO' in state['items'] else 'PENDING'}; def finalize_procurement(state: ProcureState): return {'status': 'READY'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_cert); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()