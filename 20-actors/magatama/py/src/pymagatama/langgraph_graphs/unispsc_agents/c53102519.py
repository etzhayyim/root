from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GorgetSpec(TypedDict): vendor_specs: dict; compliance_status: bool; def validate_material(state: GorgetSpec): state['compliance_status'] = 'steel' in state['vendor_specs'].get('material', '').lower(); return state; def check_historical_data(state: GorgetSpec): return state; graph = StateGraph(GorgetSpec); graph.add_node('validate_material', validate_material); graph.add_node('check_history', check_historical_data); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', 'check_history'); graph.add_edge('check_history', END); compiled_graph = graph.compile()