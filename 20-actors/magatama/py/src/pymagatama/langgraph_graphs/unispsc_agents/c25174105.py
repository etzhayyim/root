from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RackState(TypedDict): model: str; load_capacity: float; safety_passed: bool; def validate_specs(state: RackState): return {'safety_passed': state['load_capacity'] > 0 and state['model'] != ''}; def finalize_procurement(state: RackState): return {'safety_passed': True}; graph = StateGraph(RackState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()