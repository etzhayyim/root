from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): items: List[str]; qc_passed: bool; temp_verified: bool; def check_quality(state: PharmState): return {'qc_passed': True}; def verify_storage(state: PharmState): return {'temp_verified': True}; graph = StateGraph(PharmState); graph.add_node('qc', check_quality); graph.add_node('storage', verify_storage); graph.set_entry_point('qc'); graph.add_edge('qc', 'storage'); graph.add_edge('storage', END); graph = graph.compile()