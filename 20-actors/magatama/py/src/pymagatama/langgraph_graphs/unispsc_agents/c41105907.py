from typing import TypedDict, Annotated, List; from langgraph.graph import StateGraph, END; class LibraryState(TypedDict): vendor_data: dict; validation_passed: bool; compliance_risk: str; def validate_lib(state: LibraryState): state['validation_passed'] = 'cert' in state['vendor_data']; return state; def check_compliance(state: LibraryState): state['compliance_risk'] = 'high' if state['vendor_data'].get('dual_use') else 'low'; return state; graph = StateGraph(LibraryState); graph.add_node('validate', validate_lib); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()