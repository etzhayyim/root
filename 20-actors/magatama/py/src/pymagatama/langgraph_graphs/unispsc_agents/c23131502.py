from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CuttingMachineState(TypedDict): specs: dict; validation_report: dict; status: str; def validate_specs(state: CuttingMachineState): state['validation_report'] = {'passed': True} if 'power' in state['specs'] else {'passed': False}; return state; def route_step(state: CuttingMachineState): return 'validate' if not state.get('status') else END; graph = StateGraph(CuttingMachineState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()