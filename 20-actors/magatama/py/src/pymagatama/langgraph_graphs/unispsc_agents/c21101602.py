from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class TractorState(TypedDict): vendor_id: str; specs: dict; status: str; def validate_specs(state: TractorState): state['status'] = 'validation_passed' if state['specs'].get('power') else 'validation_failed'; return state; def check_compliance(state: TractorState): state['status'] = 'compliant' if state['status'] == 'validation_passed' else 'non_compliant'; return state; graph = StateGraph(TractorState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); app = graph.compile()