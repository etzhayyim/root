from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlayEquipmentState(TypedDict): equipment_type: str; is_compliant: bool; report: str; def validate_safety(state: PlayEquipmentState): return {'is_compliant': state.get('equipment_type') != 'archaic_unsafe', 'report': 'Verified against ASTM standards'}; def compile_graph(): graph = StateGraph(PlayEquipmentState); graph.add_node('safety_check', validate_safety); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', END); return graph.compile(); graph = compile_graph()