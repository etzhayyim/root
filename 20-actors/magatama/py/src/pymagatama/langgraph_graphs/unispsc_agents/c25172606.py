from langgraph.graph import StateGraph, END; from typing import TypedDict; class HoodState(TypedDict): hood_specs: dict; validation_passed: bool; def check_specs(state: HoodState): return {'validation_passed': all(k in state['hood_specs'] for k in ['material', 'dimensions'])}; def finalize(state: HoodState): return {'validation_passed': True}; graph = StateGraph(HoodState); graph.add_node('validate', check_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()