from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class RobotState(TypedDict): controller_model: str; spec_validated: bool; error_logs: List[str]; def validate_controller(state: RobotState): state['spec_validated'] = 'ISO' in state['controller_model']; return state; builder = StateGraph(RobotState); builder.add_node('validate', validate_controller); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()