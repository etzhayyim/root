from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): items: List[str]; verified: bool; errors: List[str]; def validate_kit(state: KitState): missing = [i for i in state['items'] if not i]; return {'verified': len(missing) == 0, 'errors': missing}; graph = StateGraph(KitState); graph.add_node('validate', validate_kit); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()