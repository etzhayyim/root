from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ExtrusionState(TypedDict): dimensions: dict; material_cert: bool; approved: bool; def validate_dimensions(state: ExtrusionState): state['approved'] = all(v > 0 for v in state['dimensions'].values()); return state; def check_material(state: ExtrusionState): return {'material_cert': True}; graph = StateGraph(ExtrusionState); graph.add_node('validate', validate_dimensions); graph.add_node('cert_check', check_material); graph.set_entry_point('cert_check'); graph.add_edge('cert_check', 'validate'); graph.add_edge('validate', END); app = graph.compile()