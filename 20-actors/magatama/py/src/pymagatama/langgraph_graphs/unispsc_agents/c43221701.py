from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BroadcastState(TypedDict): specs: dict; validation_report: str; graph = StateGraph(BroadcastState); def validate_specs(state: BroadcastState): state['validation_report'] = 'Validating technical standards for core TV equipment' if 'frequency_range' in state['specs'] else 'Missing critical frequency specs'; return state; graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()