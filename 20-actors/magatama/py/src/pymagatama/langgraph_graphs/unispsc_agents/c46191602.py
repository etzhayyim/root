from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SprinklerState(TypedDict): specs: dict; compliance: bool; graph = StateGraph(SprinklerState); def validate_specs(state: SprinklerState): return {'compliance': 'UL-listing' in state['specs']}; def notify_approval(state: SprinklerState): print('System ready for installation'); return state; graph.add_node('validate', validate_specs); graph.add_node('approve', notify_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()