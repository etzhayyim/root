from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class IdlerState(TypedDict): specs: dict; validation_report: list[str]; def validate_dimensions(state: IdlerState): report = []; if not state['specs'].get('diameter'): report.append('Missing diameter'); return {'validation_report': report}; def check_material(state: IdlerState): return {'validation_report': ['Material certified']} if state['specs'].get('material') else {}; graph = StateGraph(IdlerState); graph.add_node('validate_dims', validate_dimensions); graph.add_node('check_mat', check_material); graph.set_entry_point('validate_dims'); graph.add_edge('validate_dims', 'check_mat'); graph.add_edge('check_mat', END); compiled_graph = graph.compile()