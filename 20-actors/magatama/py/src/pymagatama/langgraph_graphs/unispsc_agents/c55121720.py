from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmblemState(TypedDict): material: str; adhesive_type: str; is_compliant: bool; def check_material_safety(state: EmblemState): state['is_compliant'] = state['material'] != 'asbestos'; return state; def validate_design(state: EmblemState): return state; graph = StateGraph(EmblemState); graph.add_node('safety_check', check_material_safety); graph.add_node('design_val', validate_design); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'design_val'); graph.add_edge('design_val', END); graph = graph.compile()