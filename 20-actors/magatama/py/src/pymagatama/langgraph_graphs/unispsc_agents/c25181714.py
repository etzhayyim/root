from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TrailerState(TypedDict): trailer_specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: TrailerState): errors = []; if state['trailer_specs'].get('load_capacity', 0) <= 0: errors.append('Invalid load capacity'); return {'validation_errors': errors}; def check_compliance(state: TrailerState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(TrailerState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()