from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): component_id: str; specs: dict; validation_passed: bool; def validate_specs(state: ComponentState): state['validation_passed'] = 'Interface' in state['specs'] and 'MTBF' in state['specs']; return state; def route_logic(state: ComponentState): return 'end' if state['validation_passed'] else 'end'; graph = StateGraph(ComponentState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()