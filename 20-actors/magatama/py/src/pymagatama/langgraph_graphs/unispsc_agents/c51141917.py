from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): vendor_id: str; compliance: bool; status: str; def validate_cert(state: ProcureState): state['compliance'] = True; return state; def finalize(state: ProcureState): state['status'] = 'APPROVED'; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_cert); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()