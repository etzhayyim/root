from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class CardHolderState(TypedDict): specs: dict; validation_report: str; approved: bool; def validate_specs(state: CardHolderState): state['approved'] = 'ergonomic_rating' in state['specs']; return state; def generate_report(state: CardHolderState): state['validation_report'] = 'Compliance Verified' if state['approved'] else 'Missing Spec'; return state; graph = StateGraph(CardHolderState); graph.add_node('validate', validate_specs); graph.add_node('report', generate_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compile_graph = graph.compile()