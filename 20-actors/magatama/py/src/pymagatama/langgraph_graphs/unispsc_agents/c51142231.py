from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): chemical_name: str; validation_passed: bool; compliance_docs: List[str]; def validate_compliance(state: State): state['validation_passed'] = 'license_id' in state['compliance_docs'] and 'coa' in state['compliance_docs']; return state; def check_storage_reqs(state: State): return state; workflow = StateGraph(State); workflow.add_node('validate', validate_compliance); workflow.add_node('storage', check_storage_reqs); workflow.set_entry_point('validate'); workflow.add_edge('validate', 'storage'); workflow.add_edge('storage', END); graph = workflow.compile()