from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): chemical_name: str; purity_percent: float; is_gmp_certified: bool; approved: bool; def validate_purity(state: PharmState): return {'approved': state['purity_percent'] >= 99.0 and state['is_gmp_certified']}; graph = StateGraph(PharmState); graph.add_node('validate', validate_purity); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()