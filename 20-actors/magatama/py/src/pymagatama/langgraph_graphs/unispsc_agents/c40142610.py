from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TubeSpecState(TypedDict): specs: dict; validation_errors: List[str]; def validate_specs(state: TubeSpecState): errors = []; if state['specs'].get('pressure_rating', 0) <= 0: errors.append('Invalid pressure rating'); return {'validation_errors': errors}; graph = StateGraph(TubeSpecState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()