from langgraph.graph import StateGraph, END; from typing import TypedDict; class TubeState(TypedDict): specs: dict; valid: bool; def validate_specs(state: TubeState): state['valid'] = 'Pressure Tolerance' in state['specs'] and state['specs']['Pressure Tolerance'] > 0; return state; graph = StateGraph(TubeState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()