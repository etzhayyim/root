from langgraph.graph import StateGraph, END; from typing import TypedDict; class CurtainState(TypedDict): hardware_specs: dict; validated: bool; def check_specs(state: CurtainState): state['validated'] = all(k in state['hardware_specs'] for k in ['material', 'diameter']); return state; graph = StateGraph(CurtainState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()