from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrumLifterState(TypedDict): capacity: float; lift_height: float; verified: bool; def validate_specs(state: DrumLifterState): state['verified'] = state['capacity'] > 0 and state['lift_height'] > 0; return state; graph = StateGraph(DrumLifterState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()