from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_certs: List[str]; material_specs: dict; approved: bool; def validate_compliance(state: ProcurementState): state['approved'] = 'ISO13485' in state['vendor_certs'] and 'fluid_barrier' in state['material_specs']; return state; builder = StateGraph(ProcurementState); builder.add_node('validate', validate_compliance); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()