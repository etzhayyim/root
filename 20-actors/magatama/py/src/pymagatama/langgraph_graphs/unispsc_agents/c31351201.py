from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): tube_specs: dict; validation_results: List[str]; def validate_welds(state: AssemblyState): state['validation_results'].append('X-Ray Inspection Passed'); return state; def verify_material(state: AssemblyState): state['validation_results'].append('Alloy Composition Verified'); return state; graph = StateGraph(AssemblyState); graph.add_node('weld_check', validate_welds); graph.add_node('material_check', verify_material); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'weld_check'); graph.add_edge('weld_check', END); graph = graph.compile()