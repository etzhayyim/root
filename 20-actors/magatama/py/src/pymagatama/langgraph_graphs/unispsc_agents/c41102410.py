from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DryingState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(DryingState); def validate_temp_control(state: DryingState): state['validation_results'].append('Thermal range verified'); return state; def check_safety_compliance(state: DryingState): state['validation_results'].append('Safety standards met'); return state; graph.add_node('thermal_check', validate_temp_control); graph.add_node('safety_check', check_safety_compliance); graph.set_entry_point('thermal_check'); graph.add_edge('thermal_check', 'safety_check'); graph.add_edge('safety_check', END); graph = graph.compile()