from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HuntingState(TypedDict): items: List[str]; flags: List[str]; verified: bool; def check_regulations(state: HuntingState): state['flags'] = ['arms-or-security-review']; return {'verified': True}; def validate_specs(state: HuntingState): return {'verified': True}; graph = StateGraph(HuntingState); graph.add_node('regulatory_compliance', check_regulations); graph.add_node('spec_validation', validate_specs); graph.set_entry_point('regulatory_compliance'); graph.add_edge('regulatory_compliance', 'spec_validation'); graph.add_edge('spec_validation', END); graph = graph.compile()