from langgraph.graph import StateGraph, END; from typing import TypedDict; class AmbulanceState(TypedDict): spec: dict; verified: bool; def check_medical_standard(state: AmbulanceState): state['verified'] = 'ISO-9001' in state['spec'].get('certs', []) return state; graph = StateGraph(AmbulanceState); graph.add_node('validate', check_medical_standard); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()