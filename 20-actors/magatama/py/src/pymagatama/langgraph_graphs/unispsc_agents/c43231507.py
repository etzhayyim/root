from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProjectSoftwareState(TypedDict): vendor: str; license_type: str; is_compliant: bool; def validate_compliance(state: ProjectSoftwareState): state['is_compliant'] = state['license_type'] in ['SaaS', 'On-Premise']; return state; def check_vendor(state: ProjectSoftwareState): state['vendor'] = state['vendor'].upper(); return state; graph = StateGraph(ProjectSoftwareState); graph.add_node('validate', validate_compliance); graph.add_node('check_vendor', check_vendor); graph.set_entry_point('check_vendor'); graph.add_edge('check_vendor', 'validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()