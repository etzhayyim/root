from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class VideoState(TypedDict): vendor: str; license_type: str; compliance_checked: bool; def check_compliance(state: VideoState): state['compliance_checked'] = True; return state; graph = StateGraph(VideoState); graph.add_node('check', check_compliance); graph.set_entry_point('check'); graph.add_edge('check', END); graph = graph.compile()