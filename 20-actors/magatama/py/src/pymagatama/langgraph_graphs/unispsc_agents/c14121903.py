from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ContainerState(TypedDict): container_id: str; specs: dict; validation_status: bool; workflow_log: Annotated[Sequence[str], operator.add]; def validate_specs(state: ContainerState): is_valid = 'un_approval_number' in state['specs'] and state['specs']['max_capacity_liters'] > 0; return {'validation_status': is_valid, 'workflow_log': ['Specs validated for industrial standards.']}; def execute_safety_check(state: ContainerState): return {'validation_status': state['validation_status'] and True, 'workflow_log': ['Safety protocols and leak tests completed.']}; graph = StateGraph(ContainerState); graph.add_node('validate', validate_specs); graph.add_node('safety_check', execute_safety_check); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()