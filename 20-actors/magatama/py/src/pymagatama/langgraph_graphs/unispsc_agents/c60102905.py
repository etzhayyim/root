from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; valid: bool;  def validate_magnetic_specs(state: State): return {'valid': state['input_data'].get('coercivity', 0) > 300}; def check_security(state: State): return {'valid': state['valid'] and state['input_data'].get('is_certified', False)}; graph = StateGraph(State); graph.add_node('validate', validate_magnetic_specs); graph.add_node('security', check_security); graph.set_entry_point('validate'); graph.add_edge('validate', 'security'); graph.add_edge('security', END); app = graph.compile()