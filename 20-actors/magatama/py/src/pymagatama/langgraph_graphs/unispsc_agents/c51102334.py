from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): medicine_name: str; validation_status: bool; compliance_docs: List[str]; def check_compliance(state: PharmaState): state['validation_status'] = True; return state; graph = StateGraph(PharmaState); graph.add_node('verify', check_compliance); graph.set_entry_point('verify'); graph.add_edge('verify', END); compiled_graph = graph.compile()