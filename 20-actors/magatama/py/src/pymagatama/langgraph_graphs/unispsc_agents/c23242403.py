from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MachineState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def val_specs(state: MachineState): errors = []; if not state['specs'].get('spindle_speed_rpm'): errors.append('Missing mandatory spindle speed'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(MachineState); graph.add_node('validate', val_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()