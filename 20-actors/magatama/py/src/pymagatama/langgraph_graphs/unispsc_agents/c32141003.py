from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MagnetronState(TypedDict): specs: dict; validation_report: list; risk_score: int; graph = StateGraph(MagnetronState); def validate_specs(state: MagnetronState): return {'validation_report': ['Spec validation passed']}; def perform_export_check(state: MagnetronState): return {'risk_score': 5}; graph.add_node('validate', validate_specs); graph.add_node('export_control', perform_export_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_control'); graph.add_edge('export_control', END); graph.compile()