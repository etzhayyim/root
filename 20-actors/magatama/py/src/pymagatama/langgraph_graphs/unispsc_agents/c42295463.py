from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): specs: dict; valid: bool;  def validate_spec(state: State): state['valid'] = 'ISO-13485-Certification' in state['specs'] and state['specs']['Material-Biocompatibility'] == 'Class-VI'; return state; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()