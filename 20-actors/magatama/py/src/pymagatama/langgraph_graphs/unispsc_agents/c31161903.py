from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SpringState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: SpringState): required = ['material', 'spring_rate']; state['valid'] = all(k in state['specs'] for k in required); return state; graph = StateGraph(SpringState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()