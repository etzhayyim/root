from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class IngredientState(TypedDict): ingredient_name: str; purity_check: bool; gmp_verified: bool; final_status: str; def check_purity(state: IngredientState): return {'purity_check': True}; def verify_gmp(state: IngredientState): return {'gmp_verified': True}; def finalize_procurement(state: IngredientState): return {'final_status': 'APPROVED' if state['purity_check'] and state['gmp_verified'] else 'REJECTED'}; graph = StateGraph(IngredientState); graph.add_node('check_purity', check_purity); graph.add_node('verify_gmp', verify_gmp); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('check_purity'); graph.add_edge('check_purity', 'verify_gmp'); graph.add_edge('verify_gmp', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()