from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class BerylliumState(TypedDict): order_id: str; material_specs: dict; export_compliance: bool; approval_status: str; def validate_specs(state: BerylliumState): state['approval_status'] = 'Pending'; return state; def check_export_regulations(state: BerylliumState): state['export_compliance'] = True; return state; graph = StateGraph(BerylliumState); graph.add_node('validate', validate_specs); graph.add_node('export_check', check_export_regulations); graph.set_entry_point('validate'); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()