from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AbrasiveState(TypedDict): status: str; spec_data: dict; approval: bool; def validate_mesh_spec(state: AbrasiveState): state['status'] = 'valid' if 'grit_size' in state['spec_data'] else 'invalid'; return state; def check_safety(state: AbrasiveState): state['approval'] = True; return state; graph = StateGraph(AbrasiveState); graph.add_node('validate', validate_mesh_spec); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()