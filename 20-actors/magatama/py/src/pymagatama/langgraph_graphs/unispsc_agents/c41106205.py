from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ReagentState(TypedDict): reagent_data: dict; validation_results: List[str]; def validate_reagent(state: ReagentState): if 'purity' in state['reagent_data'] and state['reagent_data']['purity'] > 98: return {'validation_results': ['Purity Verified']}; return {'validation_results': ['Purity Check Failed']}; def approve_procurement(state: ReagentState): return {'validation_results': ['Procurement Approved']}; graph = StateGraph(ReagentState); graph.add_node('validate', validate_reagent); graph.add_node('approve', approve_procurement); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()