from langgraph.graph import StateGraph, END; from typing import TypedDict; class PharmaState(TypedDict): batch_id: str; quality_passed: bool; temp_logs: list; def validate_batch(state: PharmaState): return {'quality_passed': True} if 'batch_id' in state else {'quality_passed': False}; def check_storage(state: PharmaState): return {'quality_passed': all(t < 25 for t in state['temp_logs'])}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_batch); graph.add_node('storage_check', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'storage_check'); graph.add_edge('storage_check', END); graph = graph.compile()