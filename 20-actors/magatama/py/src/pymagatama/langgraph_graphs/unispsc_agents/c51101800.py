from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): vendor_license: str; regulatory_compliance: bool; temperature_logs: List[float]; def validate_license(state): return {'regulatory_compliance': len(state['vendor_license']) > 10}; def check_temp(state): return {'regulatory_compliance': all(t < 25.0 for t in state['temperature_logs'])}; graph = StateGraph(DrugState); graph.add_node('validate_license', validate_license); graph.add_node('check_temp', check_temp); graph.set_entry_point('validate_license'); graph.add_edge('validate_license', 'check_temp'); graph.add_edge('check_temp', END); graph = graph.compile()