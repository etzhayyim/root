from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class PaperProcurementState(TypedDict): code: str; specs: dict; approved: bool; def validate_specs(state: PaperProcurementState): state['approved'] = 'gsm_weight' in state['specs'] and 'paper_grade' in state['specs']; return state; def finalize_order(state: PaperProcurementState): return {'approved': True}; graph = StateGraph(PaperProcurementState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()