from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): items: List[dict]; status: str; def validate_specs(state: State): return {'status': 'validated' if all('Adjustment Range' in i for i in state['items']) else 'invalid'}; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()