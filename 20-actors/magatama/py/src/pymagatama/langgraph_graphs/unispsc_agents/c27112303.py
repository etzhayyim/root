from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): scribing_data: dict; quality_status: str; def validate_hardness(state: State): hardness = state['scribing_data'].get('hardness', 0); state['quality_status'] = 'PASS' if hardness >= 55 else 'FAIL'; return state; def finalize_order(state: State): return {'quality_status': 'APPROVED'} if state['quality_status'] == 'PASS' else {'quality_status': 'REJECTED'}; graph = StateGraph(State); graph.add_node('validate', validate_hardness); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()