from langgraph.graph import StateGraph, END; from typing import TypedDict; class RimState(TypedDict): specs: dict; valid: bool; def validate_specs(state: RimState): state['valid'] = 'LoadRating' in state['specs']; return state; builder = StateGraph(RimState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()