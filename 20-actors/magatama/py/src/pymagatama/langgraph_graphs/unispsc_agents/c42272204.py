from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class VentilatorState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: VentilatorState): if 'Medical_Device_License_Number' in state['specs']: return {'valid': True}; return {'valid': False}; graph = StateGraph(VentilatorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()