from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class LubricantState(TypedDict): code: str; specs: dict; validation_results: Annotated[Sequence[str], operator.add]; def validate_viscosity(state: LubricantState): return {'validation_results': ['Viscosity within range'] if state['specs'].get('viscosity_grade_iso') else ['Invalid Viscosity']}; def check_safety(state: LubricantState): return {'validation_results': ['MSDS Verified'] if state['specs'].get('msds_compliant') else ['Missing MSDS']}; graph = StateGraph(LubricantState); graph.add_node('validate', validate_viscosity); graph.add_node('safety', check_safety); graph.set_entry_point('validate'); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()