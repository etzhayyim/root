from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): product_name: str; temp_range: str; is_verified: bool; def validate_cold_chain(state: PharmaState): return {'is_verified': '2-8C' in state['temp_range']}; def record_batch(state: PharmaState): print(f'Logging batch for {state['product_name']}'); return {'is_verified': True}; graph = StateGraph(PharmaState); graph.add_node('validate', validate_cold_chain); graph.add_node('record', record_batch); graph.set_entry_point('validate'); graph.add_edge('validate', 'record'); graph.add_edge('record', END); app = graph.compile()