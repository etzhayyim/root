from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class LaserProcState(TypedDict): specs: dict; compliance_check: bool; ready_for_procurement: bool; def validate_specs(state: LaserProcState): state['compliance_check'] = state['specs'].get('laser_class') in ['Class 4']; return state; def verify_regulatory(state: LaserProcState): state['ready_for_procurement'] = state['compliance_check'] and 'export_license' in state['specs']; return state; graph = StateGraph(LaserProcState); graph.add_node('validate', validate_specs); graph.add_node('regulatory', verify_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'regulatory'); graph.add_edge('regulatory', END); compiled_graph = graph.compile()