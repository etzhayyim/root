from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SolderingState(TypedDict): specs: dict; validation_report: list; status: str; def validate_specs(state: SolderingState): if state['specs'].get('power_rating', 0) > 0: return {'validation_report': ['Specs OK'], 'status': 'READY'} return {'validation_report': ['Power rating missing'], 'status': 'FAILED'}; graph = StateGraph(SolderingState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()