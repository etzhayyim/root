from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DuctProcurementState(TypedDict): items: List[str]; validated: bool; defect_logs: List[str]; def validate_bronze_specs(state: DuctProcurementState): print('Validating bronze duct standards...'); return {'validated': True}; graph = StateGraph(DuctProcurementState); graph.add_node('validation', validate_bronze_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); app = graph.compile()