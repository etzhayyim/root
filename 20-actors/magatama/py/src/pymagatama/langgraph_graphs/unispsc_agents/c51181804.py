from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmaState(TypedDict): status: str; specs: dict; valid: bool; def validate_purity(state: PharmaState): state['valid'] = state['specs'].get('purity', 0) >= 99.0; return state; def check_regulations(state: PharmaState): state['status'] = 'COMPLIANT' if state['valid'] else 'REJECTED'; return state; graph = StateGraph(PharmaState); graph.add_node('validate', validate_purity); graph.add_node('regulate', check_regulations); graph.add_edge('validate', 'regulate'); graph.add_edge('regulate', END); graph.set_entry_point('validate'); compile_graph = graph.compile()