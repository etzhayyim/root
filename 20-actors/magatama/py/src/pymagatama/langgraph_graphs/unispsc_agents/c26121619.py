from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableState(TypedDict): specs: dict; validation_results: List[str]; graph = StateGraph(CableState); def validate_thermal(state: CableState): temp = state['specs'].get('temp', 0); state['validation_results'].append('Pass' if temp > 150 else 'Fail'); return state; def check_compliance(state: CableState): state['validation_results'].append('Certification verified'); return state; graph.add_node('thermal_check', validate_thermal); graph.add_node('compliance', check_compliance); graph.set_entry_point('thermal_check'); graph.add_edge('thermal_check', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()