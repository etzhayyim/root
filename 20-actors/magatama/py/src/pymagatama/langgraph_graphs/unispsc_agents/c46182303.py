from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): harness_id: str; compliance_checks: List[str]; approved: bool; def validate_specs(state: GraphState): compliance = []; if 'OSHA' in state['harness_id']: compliance.append('compliance_pass'); return {'compliance_checks': compliance}; def finalize_approval(state: GraphState): return {'approved': len(state['compliance_checks']) > 0}; graph = StateGraph(GraphState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); compiled_graph = graph.compile()