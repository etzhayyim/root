from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MaterialState(TypedDict): specs: dict; valid: bool; def validate_specs(state: MaterialState): state['valid'] = 'Material Composition' in state['specs'] and state['specs']['Heat Resistance Rating'] > 100; return state; graph = StateGraph(MaterialState); graph.add_node('validator', validate_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()