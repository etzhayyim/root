from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): input_data: dict; approval: bool; error: str; def validate_grade(state): return {'approval': state['input_data'].get('purity') >= 99.0}; def check_safety(state): return {'error': 'None' if state['input_data'].get('temp') < 25 else 'Temperature violation'}; graph = StateGraph(State); graph.add_node('validation', validate_grade); graph.add_node('safety', check_safety); graph.set_entry_point('validation'); graph.add_edge('validation', 'safety'); graph.add_edge('safety', END); graph = graph.compile()