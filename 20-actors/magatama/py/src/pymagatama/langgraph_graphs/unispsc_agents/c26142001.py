from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GammaSourceState(TypedDict): source_id: str; activity: float; compliance_certs: List[str]; approved: bool; def validate_integrity(state: GammaSourceState): return {'approved': len(state['compliance_certs']) >= 3}; def check_export_license(state: GammaSourceState): return {'approved': state['activity'] < 1000}; graph = StateGraph(GammaSourceState); graph.add_node('integrity', validate_integrity); graph.add_node('export', check_export_license); graph.set_entry_point('integrity'); graph.add_edge('integrity', 'export'); graph.add_edge('export', END); compiled_graph = graph.compile()