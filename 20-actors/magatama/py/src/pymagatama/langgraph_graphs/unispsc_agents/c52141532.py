from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ProcState): state['valid'] = all(['voltage' in state['specs'], 'cert' in state['specs']]); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()