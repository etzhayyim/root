from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(GraphState); def validate_safety(state: GraphState): return {'validation_results': ['Safety Interlock Checked']}; def validate_calibration(state: GraphState): return {'validation_results': ['Calibration Pulse Verified']}; graph.add_node('safety', validate_safety); graph.add_node('calibration', validate_calibration); graph.set_entry_point('safety'); graph.add_edge('safety', 'calibration'); graph.add_edge('calibration', END); graph = graph.compile()