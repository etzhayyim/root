from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EndoscopyProcurementState(TypedDict): vendor_certs: List[str]; qc_passed: bool; specs: dict; def validate_cert(state: EndoscopyProcurementState): return {'qc_passed': 'ISO 13485' in state['vendor_certs']}; def process_workflow(state: EndoscopyProcurementState): return state; graph = StateGraph(EndoscopyProcurementState); graph.add_node('validate', validate_cert); graph.add_node('process', process_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); app = graph.compile()