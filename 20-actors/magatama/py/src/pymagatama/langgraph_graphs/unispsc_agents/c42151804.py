from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalSpecState(TypedDict): items: List[str]; validation_results: dict; def validate_instrument(state: DentalSpecState): state['validation_results'] = {'sterilization_check': True, 'material_check': True}; return state; def check_compliance(state: DentalSpecState): return 'compliant' if state['validation_results']['sterilization_check'] else 'non_compliant'; graph = StateGraph(DentalSpecState); graph.add_node('validate', validate_instrument); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()