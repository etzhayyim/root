from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DentalState(TypedDict): serial_number: str; qc_passed: bool; specs: dict; def validate_instrument(state: DentalState): state['qc_passed'] = 'ISO13485' in state['specs']; return state; def check_quality(state: DentalState): return 'passed' if state['qc_passed'] else 'failed'; graph = StateGraph(DentalState); graph.add_node('validate', validate_instrument); graph.add_conditional_edges('validate', check_quality, {'passed': END, 'failed': END}); graph.set_entry_point('validate'); graph = graph.compile()