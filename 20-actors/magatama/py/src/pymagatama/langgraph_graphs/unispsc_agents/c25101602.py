from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TowTruckState(TypedDict): vehicle_specs: dict; validation_results: List[str]; def validate_specs(state: TowTruckState): is_valid = state['vehicle_specs'].get('towing_capacity', 0) > 0; return {'validation_results': ['Specs Valid' if is_valid else 'Specs Invalid']}; def finalize_procurement(state: TowTruckState): return {'validation_results': state['validation_results'] + ['Process Finished']}; graph = StateGraph(TowTruckState); graph.add_node('validate', validate_specs); graph.add_node('finalize', finalize_procurement); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()