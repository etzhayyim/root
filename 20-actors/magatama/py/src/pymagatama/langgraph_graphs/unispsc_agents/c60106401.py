from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): items: List[str]; validated: bool; def check_components(state: KitState): return {'validated': len(state['items']) > 0}; def validate_safety(state: KitState): return {'validated': state['validated'] and True}; graph = StateGraph(KitState); graph.add_node('check', check_components); graph.add_node('safety', validate_safety); graph.set_entry_point('check'); graph.add_edge('check', 'safety'); graph.add_edge('safety', END); compiled_graph = graph.compile()