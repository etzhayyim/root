from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ToyState(TypedDict): items: List[str]; inspection_results: dict; def validate_safety(state: ToyState): return {'inspection_results': {'passed': True, 'certs': ['ASTM', 'EN71']}}; def check_material(state: ToyState): return {'inspection_results': {'material_safe': True}}; graph = StateGraph(ToyState); graph.add_node('safety_check', validate_safety); graph.add_node('material_audit', check_material); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'material_audit'); graph.add_edge('material_audit', END); graph = graph.compile()