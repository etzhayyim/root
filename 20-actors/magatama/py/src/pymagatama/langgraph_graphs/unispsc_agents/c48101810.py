from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitchenwareState(TypedDict): items: List[dict]; validation_results: List[bool]; def validate_spec(state: KitchenwareState): state['validation_results'] = [all(k in item for k in ['material', 'size']) for item in state['items']]; return state; graph = StateGraph(KitchenwareState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()