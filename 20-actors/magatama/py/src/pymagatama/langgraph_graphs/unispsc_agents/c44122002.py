from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): specs: dict; valid: bool; def validate_specs(state: ProcureState): thickness = state['specs'].get('thickness', 0); state['valid'] = thickness >= 50; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()