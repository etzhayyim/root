from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TentProcurementState(TypedDict): specs: dict; compliance_checked: bool; approved: bool; def validate_specs(state: TentProcurementState): return {'compliance_checked': all(k in state['specs'] for k in ['flame_cert', 'wind_rating'])}; def check_approval(state: TentProcurementState): return {'approved': state['compliance_checked']}; graph = StateGraph(TentProcurementState); graph.add_node('validate', validate_specs); graph.add_node('approval', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); graph = graph.compile()