from langgraph.graph import StateGraph, END; from typing import TypedDict; class GraphiteSpec(TypedDict): specs: dict; status: str; def validate_specs(state: GraphiteSpec): return {'status': 'validated' if 'purity' in state['specs'] else 'error'}; graph = StateGraph(GraphiteSpec); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()