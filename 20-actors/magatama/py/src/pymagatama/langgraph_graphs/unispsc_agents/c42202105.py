from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BrachyState(TypedDict): unit_id: str; safety_certs: List[str]; validated: bool; def validate_unit(state: BrachyState): state['validated'] = len(state['safety_certs']) >= 3; return state; graph = StateGraph(BrachyState); graph.add_node('validate', validate_unit); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()