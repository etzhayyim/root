from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PaperSheetState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_material(state: PaperSheetState): state['valid'] = state['specs'].get('ph') == 7.0; return state; graph = StateGraph(PaperSheetState); graph.add_node('validate', validate_material); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()