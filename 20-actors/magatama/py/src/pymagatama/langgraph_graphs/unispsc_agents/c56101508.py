from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MattressState(TypedDict): vendor_id: str; specs: dict; compliance_check: bool; graph = StateGraph(MattressState); def validate_specs(state: MattressState): print('Validating mattress dimensions and fire safety...'); return {'compliance_check': True}; def check_certification(state: MattressState): print('Checking Oeko-Tex and safety certifications...'); return {'compliance_check': True}; graph.add_node('validate', validate_specs); graph.add_node('certify', check_certification); graph.set_entry_point('validate'); graph.add_edge('validate', 'certify'); graph.add_edge('certify', END); compiled_graph = graph.compile()