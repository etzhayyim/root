from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PlateSpecState(TypedDict): specs: dict; validation_report: str; graph = StateGraph(PlateSpecState); def validate_dimensions(state: PlateSpecState): return {'validation_report': 'Dimensional tolerance verified.'}; def check_material_grade(state: PlateSpecState): return {'validation_report': 'Material metallurgy validated.'}; graph.add_node('dim_check', validate_dimensions); graph.add_node('mat_check', check_material_grade); graph.set_entry_point('dim_check'); graph.add_edge('dim_check', 'mat_check'); graph.add_edge('mat_check', END); compiled_graph = graph.compile()