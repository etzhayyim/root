from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FilterState(TypedDict): spec: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: FilterState): if state['spec'].get('pressure_rating_bar', 0) <= 0: state['validation_errors'].append('Invalid pressure rating'); return {'approved': False}; def check_compliance(state: FilterState): return {'approved': True}; graph = StateGraph(FilterState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()