from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BJTState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_electrical(state: BJTState): state['log'].append('Validating electrical ratings...'); return {'valid': True}; def check_compliance(state: BJTState): state['log'].append('Checking RoHS compliance...'); return {'valid': True}; graph = StateGraph(BJTState); graph.add_node('electrical', validate_electrical); graph.add_node('compliance', check_compliance); graph.set_entry_point('electrical'); graph.add_edge('electrical', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()