from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WireMarkerState(TypedDict): marker_type: str; specs: dict; valid: bool; def validate_spec(state: WireMarkerState): state['valid'] = 'material_composition' in state['specs']; return state; graph = StateGraph(WireMarkerState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()