from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): items: List[str]; validated: bool; log: List[str]; def validate_components(state: KitState): state['validated'] = all(['cleaner' in state['items'], 'cloth' in state['items']]); state['log'].append('Validation complete'); return state; graph = StateGraph(KitState); graph.add_node('validate', validate_components); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()