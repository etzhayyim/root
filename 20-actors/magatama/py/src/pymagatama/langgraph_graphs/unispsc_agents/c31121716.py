from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CastingState(TypedDict): metal_type: str; purity: float; specs_verified: bool; def validate_specs(state: CastingState): return {'specs_verified': state['purity'] >= 0.999}; def check_lead_time(state: CastingState): return {'specs_verified': True}; graph = StateGraph(CastingState); graph.add_node('validate', validate_specs); graph.add_node('logistics', check_lead_time); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); graph.set_entry_point('validate'); graph = graph.compile()