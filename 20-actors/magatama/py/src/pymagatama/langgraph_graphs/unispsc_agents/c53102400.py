from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HosieryState(TypedDict): specs: dict; compliance_passed: bool; def validate_specs(state: HosieryState): state['compliance_passed'] = 'fiber' in state['specs'] and 'denier' in state['specs']; return state; def check_compliance(state: HosieryState): return 'passed' if state['compliance_passed'] else 'failed'; graph = StateGraph(HosieryState); graph.add_node('validation', validate_specs); graph.add_edge('validation', END); graph.set_entry_point('validation'); graph = graph.compile()