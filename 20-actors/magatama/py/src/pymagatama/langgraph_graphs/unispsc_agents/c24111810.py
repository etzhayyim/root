from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class WaterTankState(TypedDict): capacity: int; material: str; certification: bool; valid: bool; def check_specs(state: WaterTankState): state['valid'] = state['capacity'] > 0 and state['certification'] == True; return state; graph = StateGraph(WaterTankState); graph.add_node('validate', check_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()