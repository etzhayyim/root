from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validation_results: dict; def validate_lead_casting(state: ProcurementState): return {'validation_results': {'density_check': True, 'compliance_check': 'ISO-Lead-Std'}}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_lead_casting); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()