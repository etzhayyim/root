from langgraph.graph import StateGraph, END; from typing import TypedDict; class MiningState(TypedDict): vendor_data: dict; specs: dict; approved: bool; def validate_specs(state: MiningState): state['approved'] = state['specs'].get('load_capacity', 0) > 0; return state; graph = StateGraph(MiningState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()