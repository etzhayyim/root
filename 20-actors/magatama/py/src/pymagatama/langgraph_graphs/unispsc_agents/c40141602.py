from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class ValveState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: ValveState): specs = state['specs']; is_valid = all(k in specs for k in ['pressure', 'material']); return {'valid': is_valid, 'error': '' if is_valid else 'Missing specs'}; graph = StateGraph(ValveState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()