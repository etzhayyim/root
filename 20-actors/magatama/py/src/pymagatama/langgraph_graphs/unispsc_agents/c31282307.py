from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcessState(TypedDict): component_id: str; specs: dict; validation_passed: bool; def validate_specs(state: ProcessState): state['validation_passed'] = all(k in state['specs'] for k in ['material', 'tolerance']); return state; def check_compliance(state: ProcessState): if state['validation_passed']: print('Compliance check passed'); return 'end'; return 'end'; graph = StateGraph(ProcessState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()