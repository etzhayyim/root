from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CableState(TypedDict): specs: dict; validation_errors: List[str]; def validate_impedance(state: CableState): impedance = state['specs'].get('impedance'); if impedance != 75: state['validation_errors'].append('Impedance must be 75 ohms'); return state; def validate_shielding(state: CableState): if state['specs'].get('shielding', 0) < 90: state['validation_errors'].append('Shielding below threshold'); return state; graph = StateGraph(CableState); graph.add_node('impedance_check', validate_impedance); graph.add_node('shielding_check', validate_shielding); graph.set_entry_point('impedance_check'); graph.add_edge('impedance_check', 'shielding_check'); graph.add_edge('shielding_check', END); graph = graph.compile()