from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CalibrationState(TypedDict): data: dict; validated: bool; def validate_specs(state: CalibrationState): print('Validating calibration specs...'); state['validated'] = True; return state; graph = StateGraph(CalibrationState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()