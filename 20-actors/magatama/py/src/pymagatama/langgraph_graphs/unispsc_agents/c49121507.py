from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MattressState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: MattressState): errors = []; if 'weight_capacity' not in state['specs']: errors.append('Missing weight capacity'); return {'validation_errors': errors}; def check_approval(state: MattressState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(MattressState); graph.add_node('validate', validate_specs); graph.add_node('approval', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approval'); graph.add_edge('approval', END); app = graph.compile()