from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_specs(state: AssemblyState): print('Validating pressure and material specs...'); return {'valid': True, 'log': ['Specs pass ISO standards']}; def check_inventory(state: AssemblyState): print('Checking stock levels...'); return {'log': state['log'] + ['Inventory sufficient']}; graph = StateGraph(AssemblyState); graph.add_node('validate', validate_specs); graph.add_node('inventory', check_inventory); graph.set_entry_point('validate'); graph.add_edge('validate', 'inventory'); graph.add_edge('inventory', END); graph = graph.compile()