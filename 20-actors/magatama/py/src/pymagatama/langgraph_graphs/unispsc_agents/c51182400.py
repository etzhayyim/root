from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): chemical_name: str; purity: float; compliance_docs: List[str]; approved: bool; def validate_quality(state: State): state['approved'] = state['purity'] >= 99.9 and len(state['compliance_docs']) > 0; return state; graph = StateGraph(State); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()