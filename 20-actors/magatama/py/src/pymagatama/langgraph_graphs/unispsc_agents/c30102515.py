from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; validation: list[str]; def validate_plastic(state: State): specs = state['specs']; logs = []; if 'thickness' not in specs: logs.append('Missing thickness'); return {'validation': logs}; builder = StateGraph(State); builder.add_node('validate', validate_plastic); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()