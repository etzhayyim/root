from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class KitState(TypedDict): kit_id: str; safety_clearance: bool; logistics_checked: bool; def validate_safety(state: KitState): return { 'safety_clearance': True }; def verify_logistics(state: KitState): return { 'logistics_checked': True }; graph = StateGraph(KitState); graph.add_node('validate_safety', validate_safety); graph.add_node('verify_logistics', verify_logistics); graph.set_entry_point('validate_safety'); graph.add_edge('validate_safety', 'verify_logistics'); graph.add_edge('verify_logistics', END); graph = graph.compile()