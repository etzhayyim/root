from langgraph.graph import StateGraph, END; from typing import TypedDict; class GemState(TypedDict): vendor_certs: bool; inspection_passed: bool; valuation_data: dict; def verify_certification(state: GemState): return {'vendor_certs': True} if 'GIA' in str(state) else {'vendor_certs': False}; def inspect_quality(state: GemState): return {'inspection_passed': True}; graph = StateGraph(GemState); graph.add_node('verify', verify_certification); graph.add_node('inspect', inspect_quality); graph.add_edge('verify', 'inspect'); graph.add_edge('inspect', END); graph.set_entry_point('verify'); graph = graph.compile()