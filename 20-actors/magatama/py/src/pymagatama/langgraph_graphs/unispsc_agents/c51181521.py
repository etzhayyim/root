from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): vendor_id: str; quality_cert: bool; inspection_passed: bool; def check_gmp(state: PharmState): state['quality_cert'] = True; return state; def validate_shipment(state: PharmState): state['inspection_passed'] = True; return state; graph = StateGraph(PharmState); graph.add_node('verify_gmp', check_gmp); graph.add_node('inspection', validate_shipment); graph.add_edge('verify_gmp', 'inspection'); graph.add_edge('inspection', END); graph.set_entry_point('verify_gmp'); graph = graph.compile()