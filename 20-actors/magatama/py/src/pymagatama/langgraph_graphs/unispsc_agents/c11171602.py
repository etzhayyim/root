from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class ChemicalState(TypedDict): chemical_batch_id: str; purity_level: float; certification_status: bool; workflow_log: Annotated[Sequence[str], operator.add]; def validate_chemical_integrity(state: ChemicalState): if state['purity_level'] < 0.99: return {'workflow_log': ['Invalid purity level']} return {'workflow_log': ['Purity validated successfully']}; def process_supply_chain(state: ChemicalState): return {'workflow_log': ['Chain of custody verified']}; graph = StateGraph(ChemicalState); graph.add_node('validate', validate_chemical_integrity); graph.add_node('supply_chain', process_supply_chain); graph.add_edge('validate', 'supply_chain'); graph.add_edge('supply_chain', END); graph.set_entry_point('validate'); graph = graph.compile()