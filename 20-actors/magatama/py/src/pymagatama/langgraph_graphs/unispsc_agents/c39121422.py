from langgraph.graph import StateGraph, END; from typing import TypedDict; class CouplingState(TypedDict): specs: dict; valid: bool; def validate_specs(state: CouplingState): state['valid'] = 'torque' in state['specs'] and 'material' in state['specs']; return state; builder = StateGraph(CouplingState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()