from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): items: List[str]; validated: bool; record_id: str; def validate_medical_device(state: ProcureState): state['validated'] = all(item.startswith('ISO') for item in state['items']); return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_medical_device); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()