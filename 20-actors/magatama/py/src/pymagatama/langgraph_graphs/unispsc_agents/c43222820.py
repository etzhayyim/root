from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): items: List[str]; status: str; def validate_specs(state: ProcState): return {'status': 'validated'} if len(state['items']) > 0 else {'status': 'failed'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()