from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class HSAState(TypedDict): model_id: str; qc_passed: bool; inspector_id: str; def validate_hsa_spec(state: HSAState): print(f'Validating specs for {state[model_id]}'); return {'qc_passed': True} if state['inspector_id'] else {'qc_passed': False}; def assemble_check(state: HSAState): return 'complete'; graph = StateGraph(HSAState); graph.add_node('validate', validate_hsa_spec); graph.add_node('assemble', assemble_check); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph = graph.compile()