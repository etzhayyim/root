from langgraph.graph import StateGraph, END; from typing import TypedDict; class MeprobamateState(TypedDict): purity_check: bool; safety_compliance: bool; graph = StateGraph(MeprobamateState); def validate_purity(state: MeprobamateState): return {'purity_check': True}; def check_regulatory(state: MeprobamateState): return {'safety_compliance': True}; graph.add_node('validate', validate_purity); graph.add_node('compliance', check_regulatory); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); compiled_graph = graph.compile()