from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): spec_data: dict; compliance_status: bool; def validate_manifold(state: ProcState): state['compliance_status'] = all(k in state['spec_data'] for k in ['pressure', 'material', 'sealing']); return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_manifold); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()