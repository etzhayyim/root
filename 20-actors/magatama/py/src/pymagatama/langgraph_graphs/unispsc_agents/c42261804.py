from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class MorgueFridgeState(TypedDict): capacity: int; target_temp: float; compliance_certs: list; status: str; def validate_specs(state: MorgueFridgeState): return {'status': 'validated' if state['capacity'] > 0 else 'failed'}; def process_logistics(state: MorgueFridgeState): return {'status': 'logistics_ready'}; graph = StateGraph(MorgueFridgeState); graph.add_node('validate', validate_specs); graph.add_node('logistics', process_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); compiled_graph = graph.compile()