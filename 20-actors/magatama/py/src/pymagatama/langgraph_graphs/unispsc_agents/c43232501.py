from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SoftwareState(TypedDict): vendor_name: str; languages: List[str]; license_valid: bool; def validate_software(state: SoftwareState): return {'license_valid': True if state['vendor_name'] else False}; def check_languages(state: SoftwareState): return {'languages': state.get('languages', ['en'])}; graph = StateGraph(SoftwareState); graph.add_node('validate', validate_software); graph.add_node('check_lang', check_languages); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_lang'); graph.add_edge('check_lang', END); compiled_graph = graph.compile()