from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TransportState(TypedDict): equipment_specs: dict; compliance_status: bool; workflow_log: List[str]; def validate_spec(state: TransportState): state['compliance_status'] = 'load_bearing_capacity' in state['equipment_specs']; state['workflow_log'].append('Validation completed'); return state; def finalize_procurement(state: TransportState): state['workflow_log'].append('Procurement finalized'); return state; graph = StateGraph(TransportState); graph.add_node('validate', validate_spec); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()