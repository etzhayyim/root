from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WoundCareState(TypedDict): items: List[dict]; approval_status: bool; def validate_specs(state: WoundCareState): state['approval_status'] = all('sterilization_method' in item for item in state['items']); return state; def check_compliance(state: WoundCareState): return 'approved' if state['approval_status'] else 'rejected'; graph = StateGraph(WoundCareState); graph.add_node('validate', validate_specs); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END);