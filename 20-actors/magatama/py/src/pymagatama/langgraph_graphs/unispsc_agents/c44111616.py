from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class CheckSeparatorState(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; status: str; def validate_dimensions(state: CheckSeparatorState): specs = state['input_data'].get('specs', {}); score = 1 if specs.get('thickness') else 0; return {'validation_results': [f'Dimension Check Status: {score}']}; def generate_procurement_report(state: CheckSeparatorState): return {'status': 'READY_FOR_RFQ'}; graph = StateGraph(CheckSeparatorState); graph.add_node('validate', validate_dimensions); graph.add_node('report', generate_procurement_report); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()