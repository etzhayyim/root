from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class NetworkState(TypedDict): vendor: str; requirements: List[str]; validation_passed: bool; def validate_vendor(state: NetworkState): state['validation_passed'] = 'compliance' in state['vendor'].lower(); return state; def security_check(state: NetworkState): return state; graph = StateGraph(NetworkState); graph.add_node('validate', validate_vendor); graph.add_node('security', security_check); graph.add_edge('validate', 'security'); graph.add_edge('security', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()