from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class EraserState(TypedDict): items: List[dict]; validation_results: List[bool]; def validate_material(state: EraserState): result = all('PVC' in item.get('material', '') for item in state['items']); return {'validation_results': [result]}; graph = StateGraph(EraserState); graph.add_node('validate_material', validate_material); graph.set_entry_point('validate_material'); graph.add_edge('validate_material', END); graph = graph.compile()