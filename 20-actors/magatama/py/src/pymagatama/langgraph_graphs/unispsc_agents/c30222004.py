from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GarageState(TypedDict): facility_id: str; structural_specs: dict; compliance: bool; def validate_specs(state: GarageState): return {'compliance': 'load_capacity' in state['structural_specs']}; def finalize_planning(state: GarageState): return {'compliance': True}; graph = StateGraph(GarageState); graph.add_node('validate', validate_specs); graph.add_node('plan', finalize_planning); graph.add_edge('validate', 'plan'); graph.add_edge('plan', END); graph.set_entry_point('validate'); graph = graph.compile()