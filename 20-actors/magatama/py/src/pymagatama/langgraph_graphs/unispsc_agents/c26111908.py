from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class State(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: State): specs = state['specs']; required = ['torque_capacity_nm', 'bore_diameter_mm']; state['valid'] = all(k in specs for k in required) and specs['bore_diameter_mm'] > 0; return state; graph = StateGraph(State); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()