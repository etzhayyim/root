from langgraph.graph import StateGraph, END; from typing import TypedDict; class VialState(TypedDict): vial_type: str; specs: dict; valid: bool; def validate_vial(state: VialState): state['valid'] = state['specs'].get('material') in ['borosilicate', 'pp']; return state; builder = StateGraph(VialState); builder.add_node('validate', validate_vial); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()