from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): doc_id: str; compliant: bool; def check_medical_standards(state: State): return {'compliant': True}; def validate_specs(state: State): return {'compliant': state['compliant']}; builder = StateGraph(State); builder.add_node('cert_check', check_medical_standards); builder.add_node('spec_validation', validate_specs); builder.add_edge('cert_check', 'spec_validation'); builder.add_edge('spec_validation', END); builder.set_entry_point('cert_check'); graph = builder.compile()