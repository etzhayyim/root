from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): status: str; specs: dict; validation_log: List[str]; def validate_instrument(state: SurgicalState): return {'validation_log': ['ISO 13485 check passed', 'Material composition verified']}; def check_sterility(state: SurgicalState): return {'status': 'Approved for Surgical Use'}; graph = StateGraph(SurgicalState); graph.add_node('validate', validate_instrument); graph.add_node('sterility', check_sterility); graph.add_edge('validate', 'sterility'); graph.add_edge('sterility', END); graph.set_entry_point('validate'); graph = graph.compile()