from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): safety_approved: bool; age_appropriate: bool; material_compliant: bool; def check_safety(state: State): return {'safety_approved': True}; def validate_specs(state: State): return {'age_appropriate': True, 'material_compliant': True}; graph = StateGraph(State); graph.add_node('safety_check', check_safety); graph.add_node('spec_validation', validate_specs); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'spec_validation'); graph.add_edge('spec_validation', END); compiled_graph = graph.compile()