from langgraph.graph import StateGraph, END; from typing import TypedDict; class GloveState(TypedDict): specs: dict; valid: bool; def check_specs(state: GloveState): return {'valid': 'material_composition' in state['specs']}; def finalize(state: GloveState): return {'valid': state['valid']}; graph = StateGraph(GloveState); graph.add_node('validate', check_specs); graph.add_node('finalize', finalize); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); app = graph.compile()