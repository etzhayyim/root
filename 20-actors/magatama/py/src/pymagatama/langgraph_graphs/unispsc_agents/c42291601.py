from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class LaserScalpelState(TypedDict): specs: dict; compliance_checked: bool; graph = StateGraph(LaserScalpelState); def validate_laser_specs(state: LaserScalpelState): state['compliance_checked'] = 'ISO_13485_Certification' in state['specs'] and state['specs']['Laser_Class_Rating'] <= 4; return state; graph.add_node('validate', validate_laser_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()