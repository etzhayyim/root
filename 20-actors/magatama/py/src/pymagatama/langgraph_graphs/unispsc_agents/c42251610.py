from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_results: Annotated[list, operator.add]; graph = StateGraph(State); def validate_medical_grade(state: State): return {'validation_results': ['ISO 13485 checked']}; def check_safety_spec(state: State): return {'validation_results': ['Burst resistance verified']}; graph.add_node('med_check', validate_medical_grade); graph.add_node('safety_check', check_safety_spec); graph.set_entry_point('med_check'); graph.add_edge('med_check', 'safety_check'); graph.add_edge('safety_check', END); compiled_graph = graph.compile()