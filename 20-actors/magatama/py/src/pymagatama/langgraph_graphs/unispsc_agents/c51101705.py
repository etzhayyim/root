from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PraziquantelState(TypedDict): chemical_specs: dict; compliance_status: bool; def validate_purity(state: PraziquantelState): purity = state['chemical_specs'].get('purity', 0); return {'compliance_status': purity >= 99.0}; def check_gmp(state: PraziquantelState): return {'compliance_status': state['compliance_status'] and state['chemical_specs'].get('gmp', False)}; graph = StateGraph(PraziquantelState); graph.add_node('validate_purity', validate_purity); graph.add_node('check_gmp', check_gmp); graph.set_entry_point('validate_purity'); graph.add_edge('validate_purity', 'check_gmp'); graph.add_edge('check_gmp', END); graph = graph.compile()