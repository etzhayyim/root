from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class CatalystProcessState(TypedDict): chemical_data: dict; validation_results: Annotated[Sequence[str], operator.add]; compliance_status: bool; def validate_chemical_purity(state: CatalystProcessState): purity = state['chemical_data'].get('purity', 0); status = purity >= 99.9; return {'validation_results': [f'Purity check: {purity}%'], 'compliance_status': status}; def check_export_controls(state: CatalystProcessState): return {'validation_results': ['Export control cleared']}; graph = StateGraph(CatalystProcessState); graph.add_node('purity_check', validate_chemical_purity); graph.add_node('export_check', check_export_controls); graph.set_entry_point('purity_check'); graph.add_edge('purity_check', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()