from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BoilerState(TypedDict): boiler_type: str; specs: dict; validation_passed: bool; def validate_specs(state: BoilerState): state['validation_passed'] = 'efficiency' in state['specs'] and 'safety_certs' in state['specs']; return state; def route_logic(state: BoilerState): return 'validate' if state['boiler_type'] == 'propane' else END; graph = StateGraph(BoilerState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()