from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class HeaterState(TypedDict): specs: dict; validation_results: List[str]; def val_specs(state: HeaterState): state['validation_results'].append('Checking Temp Rating...') if state['specs'].get('temp', 0) > 500 else None; return state; graph = StateGraph(HeaterState); graph.add_node('validate', val_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()