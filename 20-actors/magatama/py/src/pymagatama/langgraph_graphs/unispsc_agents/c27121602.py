from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): specs: dict; valid: bool; def validate_cylinder(state: ProcState): pressure = state['specs'].get('pressure', 0); state['valid'] = pressure > 0; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_cylinder); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()