from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): data: dict; status: str; def validate_specs(state: GraphState): return {'status': 'validated' if 'capacity' in state['data'] else 'invalid'}; def route_to_procure(state: GraphState): return 'end'; graph = StateGraph(GraphState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()