from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PaintState(TypedDict): paper_specs: dict; inspection_status: str; def validate_paper(state: PaintState): weight = state['paper_specs'].get('gsm', 0); return {'inspection_status': 'PASS' if weight >= 300 else 'FAIL'}; def check_quality(state: PaintState): pass; graph = StateGraph(PaintState); graph.add_node('validate', validate_paper); graph.add_node('check', check_quality); graph.add_edge('validate', 'check'); graph.add_edge('check', END); graph.set_entry_point('validate'); graph = graph.compile()