from langgraph.graph import StateGraph, END; from typing import TypedDict; class SprayKitState(TypedDict): specs: dict; valid: bool; def validate_specs(state: SprayKitState): state['valid'] = 'working_pressure_psi' in state['specs'] and state['specs']['working_pressure_psi'] > 0; return state; graph = StateGraph(SprayKitState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); compiled_graph = graph.compile()