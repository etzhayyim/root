from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class LightingState(TypedDict): messages: Annotated[list, add_messages]; spec_data: dict; approval_status: bool; def validate_specs(state: LightingState): spec = state.get('spec_data', {}); return {'approval_status': 'beam_angle' in spec and 'ip_rating' in spec}; def process_workflow(state: LightingState): return {'messages': ['Specs validated, proceeding to procurement routing']}; graph = StateGraph(LightingState); graph.add_node('validate', validate_specs); graph.add_node('process', process_workflow); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()