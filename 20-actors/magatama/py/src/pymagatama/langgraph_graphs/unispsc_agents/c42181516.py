from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class EMGState(TypedDict): device_specs: dict; compliance_status: bool; workflow_log: Annotated[Sequence[str], operator.add]; def validate_specs(state: EMGState): return {'compliance_status': 'ISO13485' in state['device_specs'].get('certs', []), 'workflow_log': ['Validated specifications']}; def check_safety(state: EMGState): return {'workflow_log': ['Safety audit complete']}; graph = StateGraph(EMGState); graph.add_node('validate', validate_specs); graph.add_node('safety', check_safety); graph.add_edge('validate', 'safety'); graph.add_edge('safety', END); graph.set_entry_point('validate'); graph = graph.compile()