from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class WaterBagState(TypedDict): capacity: float; material_grade: str; status: str; def validate_specs(state: WaterBagState) -> WaterBagState: return {'status': 'approved' if state['capacity'] > 0 and state['material_grade'] == 'food-safe' else 'rejected'}; graph = StateGraph(WaterBagState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()