from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class OphthalmicState(TypedDict): device_info: dict; validation_errors: Annotated[Sequence[str], operator.add]; def validate_compliance(state: OphthalmicState): return {'validation_errors': ['Pending regulatory check'] if not state['device_info'].get('iso_cert') else []}; def finalize_procurement(state: OphthalmicState): return {'device_info': {**state['device_info'], 'status': 'APPROVED'}}; graph = StateGraph(OphthalmicState); graph.add_node('validate', validate_compliance); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate')