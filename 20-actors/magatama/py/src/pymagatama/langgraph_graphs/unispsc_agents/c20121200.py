from typing import TypedDict, Annotated, Sequence; import operator; from langgraph.graph import StateGraph, END; class MotorProcurementState(TypedDict): input_spec: dict; validation_results: Annotated[list[str], operator.add]; motor_verified: bool; graph = StateGraph(MotorProcurementState); def validate_electrical_specs(state: MotorProcurementState): results = []; if 'voltage' not in state['input_spec']: results.append('Missing voltage'); return {'validation_results': results, 'motor_verified': False}; def check_industrial_safety(state: MotorProcurementState): verified = len(state['validation_results']) == 0; return {'motor_verified': verified}; graph.add_node('validate', validate_electrical_specs); graph.add_node('safety_check', check_industrial_safety); graph.add_edge('validate', 'safety_check'); graph.add_edge('safety_check', END); graph.set_entry_point('validate'); graph = graph.compile()