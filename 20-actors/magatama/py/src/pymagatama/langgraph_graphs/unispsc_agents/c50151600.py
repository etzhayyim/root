from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PurchaseState(TypedDict): vendor_certs: List[str]; qc_passed: bool; storage_req: str; def validate_cert(state: PurchaseState): state['qc_passed'] = 'ISO22000' in state['vendor_certs']; return state; def check_storage(state: PurchaseState): state['storage_req'] = 'cold_chain' if state['qc_passed'] else 'rejected'; return state; graph = StateGraph(PurchaseState); graph.add_node('validate', validate_cert); graph.add_node('check_store', check_storage); graph.set_entry_point('validate'); graph.add_edge('validate', 'check_store'); graph.add_edge('check_store', END); graph = graph.compile()