from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SignageState(TypedDict): specs: dict; compliance: bool; approved: bool; def validate_specs(state: SignageState): return {'compliance': 'material_spec' in state['specs'] and 'dimensions_mm' in state['specs']}; def approval_node(state: SignageState): return {'approved': state['compliance']}; graph = StateGraph(SignageState); graph.add_node('validate', validate_specs); graph.add_node('approve', approval_node); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()