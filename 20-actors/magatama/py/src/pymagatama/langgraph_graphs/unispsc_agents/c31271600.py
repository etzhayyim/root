from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class PartState(TypedDict): specs: dict; validation_results: Annotated[list, operator.add]; compliant: bool; def validate_dimensions(state: PartState): return {'validation_results': ['CAD dimension check passed'], 'compliant': True}; def check_material(state: PartState): return {'validation_results': ['Material cert verified']}; graph = StateGraph(PartState); graph.add_node('dim_check', validate_dimensions); graph.add_node('mat_check', check_material); graph.add_edge('dim_check', 'mat_check'); graph.add_edge('mat_check', END); graph.set_entry_point('dim_check'); graph = graph.compile()