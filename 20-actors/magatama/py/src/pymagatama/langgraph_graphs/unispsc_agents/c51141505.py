from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class DrugState(TypedDict): chemical_name: str; purity_percent: float; compliance_docs: List[str]; def validate_drug(state: DrugState): return {'compliance_docs': ['passed_pharmaceutical_audit'] if state['purity_percent'] >= 99.0 else ['failed_audit']}; graph = StateGraph(DrugState); graph.add_node('validate', validate_drug); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()