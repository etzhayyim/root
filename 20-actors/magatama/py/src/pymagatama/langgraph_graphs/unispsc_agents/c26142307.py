from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RadiationState(TypedDict): shielding_specs: dict; compliance_check: bool; final_approval: str; def validate_shielding(state: RadiationState): state['compliance_check'] = state['shielding_specs'].get('thickness', 0) >= 50; return state; def finalize_order(state: RadiationState): state['final_approval'] = 'APPROVED' if state['compliance_check'] else 'REJECTED'; return state; graph = StateGraph(RadiationState); graph.add_node('validate', validate_shielding); graph.add_node('finalize', finalize_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph = graph.compile()