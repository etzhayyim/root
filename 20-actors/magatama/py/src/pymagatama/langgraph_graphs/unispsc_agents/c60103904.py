from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class FungiState(TypedDict): strain_id: str; viability_check: bool; compliance_status: bool; def validate_strain(state: FungiState): print(f'Validating {state["strain_id"]}'); return {'compliance_status': True}; def check_storage(state: FungiState): print('Verifying temp logs'); return {'viability_check': True}; graph = StateGraph(FungiState); graph.add_node('validate', validate_strain); graph.add_node('storage', check_storage); graph.add_edge('validate', 'storage'); graph.add_edge('storage', END); graph.set_entry_point('validate'); compiled_graph = graph.compile()