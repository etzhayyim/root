from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class WeldingSpecState(TypedDict): spec: dict; validated: bool; def validate_spec(state: WeldingSpecState): voltage = state['spec'].get('voltage', 0); state['validated'] = voltage >= 200; return state; graph = StateGraph(WeldingSpecState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()