from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GearState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: GearState): required = ['module', 'material_grade', 'precision_grade_jis']; state['valid'] = all(k in state['specs'] for k in required); return state; graph = StateGraph(GearState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()