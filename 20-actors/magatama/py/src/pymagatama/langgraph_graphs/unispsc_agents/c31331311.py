from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): component_id: str; structural_integrity_score: float; inspection_reports: List[str]; approved: bool; def validate_weld_integrity(state: AssemblyState): print(f'Validating sonic weld for {state[component_id]}'); state[structural_integrity_score] = 0.98; return state; def check_export_controls(state: AssemblyState): state[approved] = True; return state; graph = StateGraph(AssemblyState); graph.add_node('validate_weld', validate_weld_integrity); graph.add_node('export_check', check_export_controls); graph.set_entry_point('validate_weld'); graph.add_edge('validate_weld', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()