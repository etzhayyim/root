from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): items: List[str]; validated: bool; certs: List[str]; def validate_medical_grade(state: ProcurementState): state.update({'validated': all(['ISO13485' in c for c in state['certs']])}); return state; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_medical_grade); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph.compile()