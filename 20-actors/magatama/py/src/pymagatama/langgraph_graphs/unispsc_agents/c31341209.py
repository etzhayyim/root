from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AssemblyState(TypedDict): assembly_id: str; specs: dict; validation_status: bool; def check_rivet_integrity(state: AssemblyState): state['validation_status'] = True; return state; graph = StateGraph(AssemblyState); graph.add_node('validate_rivets', check_rivet_integrity); graph.set_entry_point('validate_rivets'); graph.add_edge('validate_rivets', END); graph = graph.compile()