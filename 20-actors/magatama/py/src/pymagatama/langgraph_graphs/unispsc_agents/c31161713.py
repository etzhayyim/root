from langgraph.graph import StateGraph, END; from typing import TypedDict; class NutState(TypedDict): spec: dict; is_valid: bool; def validate_specs(state: NutState): state['is_valid'] = 'thread' in state['spec'] and 'pressure' in state['spec']; return state; builder = StateGraph(NutState); builder.add_node('validate', validate_specs); builder.set_entry_point('validate'); builder.add_edge('validate', END); graph = builder.compile()