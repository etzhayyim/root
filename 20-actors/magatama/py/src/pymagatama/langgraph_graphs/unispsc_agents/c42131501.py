from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BibState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_bib_specs(state: BibState): errors = []; if not state['specs'].get('absorbency_rate'): errors.append('Missing absorbency rate'); if state['specs'].get('latex_content') == 'yes': errors.append('Latex content restricted'); return {'validation_errors': errors, 'approved': len(errors) == 0}; graph = StateGraph(BibState); graph.add_node('validator', validate_bib_specs); graph.set_entry_point('validator'); graph.add_edge('validator', END); graph = graph.compile()