from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class AcousticState(TypedDict): specs: dict; compliance: bool; issues: List[str]; def validate_acoustic_specs(state: AcousticState): stc = state['specs'].get('stc', 0); state['compliance'] = stc >= 30; return state; graph = StateGraph(AcousticState); graph.add_node('validate', validate_acoustic_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()