from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class State(TypedDict): input_data: dict; validation_report: list; def validate_spec(state: State): spec = state['input_data']; errors = []; if not spec.get('material_composition'): errors.append('Missing material'); return {'validation_report': errors}; graph = StateGraph(State); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()