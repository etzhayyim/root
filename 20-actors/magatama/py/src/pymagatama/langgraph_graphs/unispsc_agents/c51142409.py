from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): chemical_data: dict; compliance_checked: bool; def validate_isometheptene(state: ProcState): return {'compliance_checked': state['chemical_data'].get('purity', 0) >= 99.0}; def approve_order(state: ProcState): return {'compliance_checked': True}; graph = StateGraph(ProcState); graph.add_node('validate', validate_isometheptene); graph.add_node('approve', approve_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); graph = graph.compile()