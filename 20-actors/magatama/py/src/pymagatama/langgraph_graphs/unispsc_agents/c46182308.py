from langgraph.graph import StateGraph, END; from typing import TypedDict; class State(TypedDict): grips_data: dict; approved: bool; def validate_grip(state: State): state['approved'] = state['grips_data'].get('load_capacity', 0) > 500; return state; graph = StateGraph(State); graph.add_node('validate', validate_grip); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()