from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class CordState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_cord(state: CordState): errors = []; if state['specs'].get('amperage_rating', 0) < 10: errors.append('Insufficient amperage'); state['validation_errors'] = errors; state['approved'] = len(errors) == 0; return state; graph = StateGraph(CordState); graph.add_node('validate', validate_cord); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()