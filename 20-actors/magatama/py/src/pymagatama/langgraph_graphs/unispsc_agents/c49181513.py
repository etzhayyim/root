from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class BilliardState(TypedDict): specs: dict; valid: bool; def validate_specs(state: BilliardState): state['valid'] = 'hardness' in state['specs'] and 'diameter' in state['specs']; return state; graph = StateGraph(BilliardState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()