from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeldState(TypedDict): specs: dict; safety_check: bool; approved: bool; def validate_specs(state: WeldState): state['safety_check'] = all(k in state['specs'] for k in ['pressure_rating', 'certification']); return state; def final_decision(state: WeldState): state['approved'] = state['safety_check']; return state; graph = StateGraph(WeldState); graph.add_node('validate', validate_specs); graph.add_node('decision', final_decision); graph.set_entry_point('validate'); graph.add_edge('validate', 'decision'); graph.add_edge('decision', END); compile_graph = graph.compile()