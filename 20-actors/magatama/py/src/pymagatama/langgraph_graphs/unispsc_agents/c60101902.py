from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class State(TypedDict): blocks: List[str]; validated: bool; def validate_blocks(state: State) -> State: state['validated'] = all(len(b) == 1 for b in state['blocks']); return state; graph = StateGraph(State); graph.add_node('validate', validate_blocks); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()