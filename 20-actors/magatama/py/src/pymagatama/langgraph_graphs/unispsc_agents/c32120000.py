from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ComponentState(TypedDict): components: list; status: str; def validate_specs(state): return {'status': 'validated'} if all('rohs' in c for c in state['components']) else {'status': 'rejected'}; def process_logistics(state): return {'status': 'ready_for_shipment'}; graph = StateGraph(ComponentState); graph.add_node('validate', validate_specs); graph.add_node('logistics', process_logistics); graph.set_entry_point('validate'); graph.add_edge('validate', 'logistics'); graph.add_edge('logistics', END); compiled_graph = graph.compile()