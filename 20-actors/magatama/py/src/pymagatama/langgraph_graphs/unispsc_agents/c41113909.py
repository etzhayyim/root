from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SoilSampleState(TypedDict): specs: dict; validation_errors: List[str]; approved: bool; def validate_specs(state: SoilSampleState): error = [] if 'diameter' not in state['specs'] else None; return {'validation_errors': [error] if error else []}; def check_approval(state: SoilSampleState): return {'approved': len(state['validation_errors']) == 0}; graph = StateGraph(SoilSampleState); graph.add_node('validate', validate_specs); graph.add_node('approve', check_approval); graph.set_entry_point('validate'); graph.add_edge('validate', 'approve'); graph.add_edge('approve', END); compiled_graph = graph.compile()