from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): vendor_certs: List[str]; safety_compliance: bool; status: str; def validate_spec(state: ProcureState): return {'status': 'validated' if all(c in state['vendor_certs'] for c in ['ISO-13485', 'CE']) else 'rejected'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()