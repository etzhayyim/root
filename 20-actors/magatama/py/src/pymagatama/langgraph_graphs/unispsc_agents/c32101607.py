from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ICState(TypedDict): specs: dict; compliance: bool; ic_type: str; graph = StateGraph(ICState); def validate_specs(state: ICState): return {'compliance': 'Capacity' in state['specs']}; def export_check(state: ICState): return {'compliance': state.get('ic_type') != 'military_grade'}; graph.add_node('validate', validate_specs); graph.add_node('export_review', export_check); graph.add_edge('validate', 'export_review'); graph.add_edge('export_review', END); graph.set_entry_point('validate'); app = graph.compile()