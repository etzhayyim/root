from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class SurgicalState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(SurgicalState); def validate_sterility(state: SurgicalState): return {'validation_results': {'sterile': True}}; def check_aami_level(state: SurgicalState): return {'validation_results': {'level': 'AAMI-3'}}; graph.add_node('check_sterility', validate_sterility); graph.add_node('check_barrier', check_aami_level); graph.set_entry_point('check_sterility'); graph.add_edge('check_sterility', 'check_barrier'); graph.add_edge('check_barrier', END); graph.compile()