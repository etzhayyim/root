from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GumboroState(TypedDict): cold_chain_log: List[float]; safety_check: bool; def validate_cold_chain(state): return {'safety_check': all(2 <= temp <= 8 for temp in state['cold_chain_log'])}; def process_vaccine(state): return {'safety_check': True} if state['safety_check'] else {'safety_check': False}; graph = StateGraph(GumboroState); graph.add_node('validate', validate_cold_chain); graph.add_node('process', process_vaccine); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph.set_entry_point('validate'); graph = graph.compile()