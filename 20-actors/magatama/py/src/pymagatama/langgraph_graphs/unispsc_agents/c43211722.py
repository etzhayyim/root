from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ScannerState(TypedDict): card_image: str; metadata: dict; validated: bool; def validate_scan(state: ScannerState): state['validated'] = True if 'ocr_text' in state['metadata'] else False; return state; graph = StateGraph(ScannerState); graph.add_node('validate', validate_scan); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()