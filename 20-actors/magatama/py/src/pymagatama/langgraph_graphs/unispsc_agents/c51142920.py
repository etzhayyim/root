from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcaineState(TypedDict): chemical_data: str; validation_status: bool; compliance_docs: List[str]; def validate_purity(state: ProcaineState): state['validation_status'] = True if 'purity' in state['chemical_data'] else False; return state; def check_compliance(state: ProcaineState): state['compliance_docs'] = ['GMP', 'SDS']; return state; graph = StateGraph(ProcaineState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()