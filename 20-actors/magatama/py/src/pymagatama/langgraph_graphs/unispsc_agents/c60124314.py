from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcureState(TypedDict): status: str; specs: dict; def validate_quality(state: ProcureState): print('Validating material safety...'); return {'status': 'validated'}; def finalize_procurement(state: ProcureState): print('Generating PO...'); return {'status': 'completed'}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_quality); graph.add_node('finalize', finalize_procurement); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate'); graph = graph.compile()