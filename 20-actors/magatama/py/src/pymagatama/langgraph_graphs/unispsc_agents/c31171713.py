from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GearState(TypedDict): specs: dict; validation: bool; errors: List[str]; def validate_specs(state: GearState): hrc = state['specs'].get('hardness', 0); is_valid = 58 <= hrc <= 62; return {'validation': is_valid, 'errors': [] if is_valid else ['Hardness out of spec']}; def assemble_process(state: GearState): return {'validation': True}; graph = StateGraph(GearState); graph.add_node('validate', validate_specs); graph.add_node('assemble', assemble_process); graph.set_entry_point('validate'); graph.add_edge('validate', 'assemble'); graph.add_edge('assemble', END); graph = graph.compile()