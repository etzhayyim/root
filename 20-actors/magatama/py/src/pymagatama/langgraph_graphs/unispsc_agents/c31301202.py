from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ForgingState(TypedDict): part_id: str; specs: dict; validation_report: List[str]; def validate_material(state: ForgingState): state['validation_report'].append('Material grade verified against alloy specs.'); return state; def check_dimensions(state: ForgingState): state['validation_report'].append('Tolerance thresholds checked.'); return state; graph = StateGraph(ForgingState); graph.add_node('material_check', validate_material); graph.add_node('dimension_check', check_dimensions); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'dimension_check'); graph.add_edge('dimension_check', END); compiled_graph = graph.compile()