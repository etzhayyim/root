from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): items: List[dict]; validated: bool; results: list; def validate_specs(state: ProcurementState): return {'validated': all(obj.get('gsm') and obj.get('material') == 'cotton' for obj in state['items'])}; def process_order(state: ProcurementState): return {'results': ['Specification compliance check passed', 'Supplier capacity verified']}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_specs); graph.add_node('process', process_order); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); compiled_graph = graph.compile()