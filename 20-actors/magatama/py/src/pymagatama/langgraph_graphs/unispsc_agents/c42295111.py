from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PositioningSpecState(TypedDict): device_specs: dict; compliance_checks: List[str]; approved: bool; def validate_specs(state: PositioningSpecState): specs = state.get('device_specs', {}); checks = []; checks.append('ISO_13485') if specs.get('iso_cert') else None; checks.append('Biocompatible') if specs.get('bio_test') else None; return {'compliance_checks': checks, 'approved': len(checks) == 2}; workflow = StateGraph(PositioningSpecState); workflow.add_node('validate', validate_specs); workflow.set_entry_point('validate'); workflow.add_edge('validate', END); graph = workflow.compile()