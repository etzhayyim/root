from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): specs: dict; ready: bool; def validate_specs(state: ProcState): state['ready'] = 'Material Grade' in state['specs'] and 'Welding Certification' in state['specs']; return {'ready': state['ready']}; graph = StateGraph(ProcState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()