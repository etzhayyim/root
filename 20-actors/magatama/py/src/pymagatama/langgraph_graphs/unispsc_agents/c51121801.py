from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): item: str; quality_checks: List[str]; approved: bool; def validate_quality(state: ProcureState): state['quality_checks'].append('pharmacopeia_check'); state['approved'] = True; return state; graph = StateGraph(ProcureState); graph.add_node('validate', validate_quality); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()