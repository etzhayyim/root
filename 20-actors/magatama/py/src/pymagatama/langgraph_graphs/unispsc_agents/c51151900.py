from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): medicine_name: str; compliance_check: bool; batch_id: str; graph = StateGraph(PharmState); def validate_batch(state: PharmState): return {'compliance_check': True if state['batch_id'] else False}; def route_procurement(state: PharmState): return 'end'; graph.add_node('validate', validate_batch); graph.add_edge('validate', END); graph.set_entry_point('validate'); graph = graph.compile()