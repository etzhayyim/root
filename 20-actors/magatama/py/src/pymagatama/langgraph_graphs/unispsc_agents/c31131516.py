from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ForgingState(TypedDict): metal_purity: float; dimensions: dict; verified: bool; def validate_spec(state: ForgingState): state['verified'] = state['metal_purity'] >= 99.9; return state; graph = StateGraph(ForgingState); graph.add_node('validate', validate_spec); graph.set_entry_point('validate'); graph.add_edge('validate', END); App = graph.compile()