from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class PharmState(TypedDict): medicine_name: str; compliance_checks: List[str]; validated: bool; def check_gmp(state: PharmState): state['compliance_checks'].append('GMP_Verified'); return {'validated': True}; def validate_cold_chain(state: PharmState): state['compliance_checks'].append('ColdChain_Confirmed'); return {'validated': True}; graph = StateGraph(PharmState); graph.add_node('gmp_check', check_gmp); graph.add_node('cold_chain', validate_cold_chain); graph.set_entry_point('gmp_check'); graph.add_edge('gmp_check', 'cold_chain'); graph.add_edge('cold_chain', END); graph = graph.compile()