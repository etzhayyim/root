from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): items: List[str]; validation_results: dict; graph = StateGraph(ProcurementState); def validate_specs(state: ProcurementState): return {'validation_results': {'compliance_checked': True}}; graph.add_node('spec_validation', validate_specs); graph.set_entry_point('spec_validation'); graph.add_edge('spec_validation', END); graph = graph.compile()