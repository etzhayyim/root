from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class TapeSpec(TypedDict): width: float; length: float; adhesive_type: str; compliant: bool; def check_tape_specs(state: TapeSpec): state['compliant'] = state['width'] > 0 and state['length'] > 0; return state; graph = StateGraph(TapeSpec); graph.add_node('validate', check_tape_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()