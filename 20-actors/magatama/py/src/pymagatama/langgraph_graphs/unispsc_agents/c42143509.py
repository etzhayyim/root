from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EarmoldState(TypedDict): adhesive_type: str; is_biocompatible: bool; specs: dict; validation_log: List[str]; def validate_adhesive(state: EarmoldState): logs = []; valid = True; if not state['is_biocompatible']: logs.append('Error: Non-biocompatible material rejected.'); valid = False; return {'validation_log': logs}; def final_check(state: EarmoldState): return {'validation_log': state['validation_log'] + ['Certification confirmed.']}; graph = StateGraph(EarmoldState); graph.add_node('validate', validate_adhesive); graph.add_node('finalize', final_check); graph.add_edge('validate', 'finalize'); graph.add_edge('finalize', END); graph.set_entry_point('validate');