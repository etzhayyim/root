from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class PharmState(TypedDict): ingredients: List[str]; qc_passed: bool; certs: List[str]; def validate_gmp(state: PharmState): state['qc_passed'] = 'GMP' in state['certs']; return state; builder = StateGraph(PharmState); builder.add_node('qc_check', validate_gmp); builder.set_entry_point('qc_check'); builder.add_edge('qc_check', END); graph = builder.compile()