from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class OrificeState(TypedDict): specs: dict; valid: bool; error: str; def validate_plate(state: OrificeState): state['valid'] = state['specs'].get('bore_diameter') > 0; return state; graph = StateGraph(OrificeState); graph.add_node('validate', validate_plate); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()