from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class GraphState(TypedDict): device_type: str; is_calibrated: bool; compliance_status: List[str]; def check_compliance(state: GraphState): return {'compliance_status': ['EMC', 'ISO17025']}; def validate_device(state: GraphState): return {'is_calibrated': True}; graph = StateGraph(GraphState); graph.add_node('validate', validate_device); graph.add_node('compliance', check_compliance); graph.set_entry_point('validate'); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph = graph.compile()