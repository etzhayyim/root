from langgraph.graph import StateGraph, END; from typing import TypedDict; class PaperMoldState(TypedDict): mold_specs: dict; inspection_status: bool; def validate_mold(state: PaperMoldState): state['inspection_status'] = 'material' in state['mold_specs'] and state['mold_specs']['mesh_count'] > 0; return state; graph = StateGraph(PaperMoldState); graph.add_node('validate', validate_mold); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()