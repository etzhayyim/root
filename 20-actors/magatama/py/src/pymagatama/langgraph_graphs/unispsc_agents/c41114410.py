from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class WeatherStationState(TypedDict): station_id: str; sensor_data: dict; calibration_status: bool; workflow_log: List[str]; def validate_calibration(state: WeatherStationState): state['calibration_status'] = True; state['workflow_log'].append('Calibration verified'); return state; def process_sensors(state: WeatherStationState): state['workflow_log'].append('Sensor data processing complete'); return state; graph = StateGraph(WeatherStationState); graph.add_node('validate', validate_calibration); graph.add_node('process', process_sensors); graph.set_entry_point('validate'); graph.add_edge('validate', 'process'); graph.add_edge('process', END); graph = graph.compile()