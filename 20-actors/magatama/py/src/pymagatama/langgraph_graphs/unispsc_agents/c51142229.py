from langgraph.graph import StateGraph, END; from typing import TypedDict; class ProcState(TypedDict): input_data: str; validation_status: bool; def validate_narcotic_control(state: ProcState): state['validation_status'] = True if 'license' in state['input_data'] else False; return state; graph = StateGraph(ProcState); graph.add_node('validate', validate_narcotic_control); graph.set_entry_point('validate'); graph.add_edge('validate', END); compile_graph = graph.compile()