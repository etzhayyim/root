from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ShieldingState(TypedDict): specs: dict; compliant: bool; workflow_steps: List[str]; def validate_shielding(state: ShieldingState): state['compliant'] = state['specs'].get('lead_equivalence_mmPb', 0) >= 0.5; state['workflow_steps'].append('Validation completed'); return state; graph = StateGraph(ShieldingState); graph.add_node('validate', validate_shielding); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()