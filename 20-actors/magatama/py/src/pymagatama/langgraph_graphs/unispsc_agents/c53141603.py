from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ThimbleState(TypedDict): dimensions: dict; material: str; verified: bool; def validate_specs(state: ThimbleState): return {'verified': state.get('material') in ['steel', 'brass', 'plastic']}; graph = StateGraph(ThimbleState); graph.add_node('validation', validate_specs); graph.set_entry_point('validation'); graph.add_edge('validation', END); graph = graph.compile()