from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; class CompressorState(TypedDict): specs: dict; valid: bool; error: str; def validate_specs(state: CompressorState): s = state['specs']; state['valid'] = 'Refrigerant_Type' in s and 'Cooling_Capacity_kW' in s; return state; graph = StateGraph(CompressorState); graph.add_node('validate', validate_specs); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()