from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class EmbroideryState(TypedDict): hoop_id: str; quality_passed: bool; specs: dict; def validate_hoop(state: EmbroideryState): return {'quality_passed': state['specs'].get('material_type') is not None}; def route_action(state: EmbroideryState): return 'validate' if not state.get('quality_passed') else END; graph = StateGraph(EmbroideryState); graph.add_node('validate', validate_hoop); graph.set_entry_point('validate'); graph.add_edge('validate', END); app = graph.compile()