from langgraph.graph import StateGraph, END; from typing import TypedDict, List; class ProcurementState(TypedDict): vendor: str; specs: dict; compliance_cleared: bool; graph = StateGraph(ProcurementState); def validate_beryllium_safety(state: ProcurementState): return {'compliance_cleared': state.get('specs', {}).get('hazmat_verified', False)}; def check_export_controls(state: ProcurementState): return {'compliance_cleared': True}; graph.add_node('safety_check', validate_beryllium_safety); graph.add_node('export_check', check_export_controls); graph.set_entry_point('safety_check'); graph.add_edge('safety_check', 'export_check'); graph.add_edge('export_check', END); graph = graph.compile()