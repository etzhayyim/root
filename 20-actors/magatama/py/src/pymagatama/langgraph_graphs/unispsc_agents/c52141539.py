from typing import TypedDict, Annotated; from langgraph.graph import StateGraph, END; from langgraph.graph.message import add_messages; class TeaMakerState(TypedDict): specs: dict; validation_report: list; graph = StateGraph(TeaMakerState); def validate_electrical(state: TeaMakerState): state['validation_report'].append('Electrical compliance checked'); return state; def validate_food_safety(state: TeaMakerState): state['validation_report'].append('FDA/RoHS compliance checked'); return state; graph.add_node('elec_check', validate_electrical); graph.add_node('food_check', validate_food_safety); graph.set_entry_point('elec_check'); graph.add_edge('elec_check', 'food_check'); graph.add_edge('food_check', END); graph = graph.compile()