from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class SputteringState(TypedDict): target_spec: dict; validation_results: List[str]; def validate_purity(state: SputteringState): purity = state['target_spec'].get('purity', 0); result = 'Pass' if purity >= 99.99 else 'Fail: Purity too low'; return {'validation_results': [result]}; def check_export(state: SputteringState): return {'validation_results': state['validation_results'] + ['Export License Required']}; graph = StateGraph(SputteringState); graph.add_node('validate', validate_purity); graph.add_node('export_check', check_export); graph.add_edge('validate', 'export_check'); graph.add_edge('export_check', END); graph.set_entry_point('validate'); graph = graph.compile()