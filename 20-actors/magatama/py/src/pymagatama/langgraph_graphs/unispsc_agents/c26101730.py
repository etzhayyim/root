from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class RockerShaftState(TypedDict): specs: dict; validation_results: List[str]; def validate_material_specs(state: RockerShaftState): hardness = state['specs'].get('hardness', 0); return {'validation_results': ['PASSED'] if hardness > 50 else ['FAILED_HARDNESS']}; def check_tolerances(state: RockerShaftState): return {'validation_results': state['validation_results'] + ['TOLERANCE_VERIFIED']}; graph = StateGraph(RockerShaftState); graph.add_node('material_check', validate_material_specs); graph.add_node('tolerance_check', check_tolerances); graph.set_entry_point('material_check'); graph.add_edge('material_check', 'tolerance_check'); graph.add_edge('tolerance_check', END); graph = graph.compile()