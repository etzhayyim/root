from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class MicroChemState(TypedDict): tools: List[str]; validated: bool; results: str; def validate_tools(state: MicroChemState): return {'validated': all(t == 'approved' for t in state['tools'])}; def report_results(state: MicroChemState): return {'results': 'Validation Complete' if state['validated'] else 'Discrepancy Detected'}; graph = StateGraph(MicroChemState); graph.add_node('validate', validate_tools); graph.add_node('report', report_results); graph.set_entry_point('validate'); graph.add_edge('validate', 'report'); graph.add_edge('report', END); compiled_graph = graph.compile()