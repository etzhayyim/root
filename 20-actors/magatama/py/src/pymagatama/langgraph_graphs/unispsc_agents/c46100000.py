from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): weapon_type: str; compliance_docs: List[str]; approved: bool; def validate_compliance(state: ProcurementState): state['approved'] = 'end_user_certificate' in state['compliance_docs'] and 'export_license' in state['compliance_docs']; return state; builder = StateGraph(ProcurementState); builder.add_node('compliance_check', validate_compliance); builder.set_entry_point('compliance_check'); builder.add_edge('compliance_check', END); graph = builder.compile()