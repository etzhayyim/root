from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ChannelState(TypedDict): specs: dict; valid: bool; log: List[str]; def validate_plastic_channel(state: ChannelState): s = state['specs']; state['valid'] = s.get('material') in ['PVC', 'HDPE'] and s.get('thickness', 0) > 0; state['log'].append('Validation complete'); return state; graph = StateGraph(ChannelState); graph.add_node('validate', validate_plastic_channel); graph.set_entry_point('validate'); graph.add_edge('validate', END); graph = graph.compile()