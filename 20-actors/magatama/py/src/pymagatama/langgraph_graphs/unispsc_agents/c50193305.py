from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcState(TypedDict): fruit_data: dict; quality_status: str; def validate_purity(state: ProcState): return {'quality_status': 'PASS' if state['fruit_data'].get('ph', 0) < 3.0 else 'FAIL'}; def check_compliance(state: ProcState): return {'quality_status': 'COMPLIANT' if state['quality_status'] == 'PASS' else 'REJECTED'}; graph = StateGraph(ProcState); graph.add_node('validate', validate_purity); graph.add_node('compliance', check_compliance); graph.add_edge('validate', 'compliance'); graph.add_edge('compliance', END); graph.set_entry_point('validate'); graph = graph.compile()