from typing import TypedDict, Annotated; import operator; from langgraph.graph import StateGraph, END; class ProcureState(TypedDict): needle_specs: dict; validation_results: Annotated[list, operator.add]; def validate_medical_grade(state: ProcureState): return {'validation_results': ['ISO-13485-Checked', 'Biocompatibility-Certified']}; def check_sterility(state: ProcureState): return {'validation_results': ['Sterilization-Verified']}; graph = StateGraph(ProcureState); graph.add_node('validate', validate_medical_grade); graph.add_node('sterility', check_sterility); graph.set_entry_point('validate'); graph.add_edge('validate', 'sterility'); graph.add_edge('sterility', END); graph = graph.compile()