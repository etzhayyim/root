import operator from typing import TypedDict, Annotated, List from langgraph.graph import StateGraph, END class GraphState(TypedDict): items: List[str] compliance_checked: bool export_permitted: bool def validate_compliance(state: GraphState): return {'compliance_checked': True} def check_dual_use(state: GraphState): return {'export_permitted': True} graph = StateGraph(GraphState) graph.add_node('validate', validate_compliance) graph.add_node('export_check', check_dual_use) graph.set_entry_point('validate') graph.add_edge('validate', 'export_check') graph.add_edge('export_check', END) graph = graph.compile()