from typing import TypedDict, List; from langgraph.graph import StateGraph, END; class ProcurementState(TypedDict): vendor_docs: List[str]; qc_passed: bool; def validate_docs(state: ProcurementState): return {'qc_passed': 'ISO_13485' in state['vendor_docs']}; def check_biocompatibility(state: ProcurementState): return {'qc_passed': state['qc_passed'] and 'ISO_10993' in state['vendor_docs']}; graph = StateGraph(ProcurementState); graph.add_node('validate', validate_docs); graph.add_node('biocomp', check_biocompatibility); graph.set_entry_point('validate'); graph.add_edge('validate', 'biocomp'); graph.add_edge('biocomp', END); graph = graph.compile()