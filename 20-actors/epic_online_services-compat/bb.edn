{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([epic_online_services.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'epic_online_services.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
