;; silicon 珪 — actor manifest
;; Ternary silicon-fab orchestration (litho/wafer/test/packaging … 8-process scope)
;; baien ternary ASIC (iwakura inference / fuigo train) fab-side owner
;; ADR-2605242500 + 2605242545 (fab charter) · ADR-2606021139 (split out of tsukuru namespace)
{:actor/id        "silicon"
 :actor/glyph     "珪"
 :actor/tier      :tier-b
 :actor/domain    "silicon.etzhayyim.com"
 :actor/purpose   "First-party ternary silicon-fab orchestration — 8 fab 工程 (litho/deposition/etch/implant/CMP/metrology/test/packaging) の自社装置 RTL/CAD/robotics 設計 + Pregel cell orchestration. baien ternary ASIC (iwakura 推論 / fuigo 訓練) の fab-side owner. Split out of the tsukuru namespace per ADR-2606021139 so tsukuru = B2B ordering only. silicon-as-asset は inalienable (land-trust 類比, ADR-2605242500). Apache 2.0 + Charter Rider §2(a)(c) 兵器/監視転用ガード適用. Murakumo-only, kotoba-EAVT-native."
 :actor/status    :r0
 :actor/adr       "2605242500"
 :actor/related   ["2605242545" "2606021139" "2605242515" "2605242700"]

 :actor/charter-rider
 {:rider/version "2.0"
  :rider/applied true}

 :actor/gates
 [{:gate/id "G1"  :gate/name "weapons-surveillance-guard" :gate/rule "silicon design = 兵器転用 / 監視転用 禁止 (Charter Rider §2(a)(c)); dual-use review (silenForceReview) 必須"}
  {:gate/id "G2"  :gate/name "inalienable-asset"          :gate/rule "religious-corp 所有 silicon は inalienable — transfer/sale/burn 禁止 (land-trust 類比, ADR-2605242500)"}
  {:gate/id "G3"  :gate/name "open-reference-design"      :gate/rule "fab equipment reference design は Apache 2.0 + Charter Rider で公開 (Transparent Force R&D, ADR-2605192315)"}
  {:gate/id "G5"  :gate/name "murakumo-only"              :gate/rule "工程設計/最適化の inference は KotobaLLM 127.0.0.1:4000; 外部 LLM 禁止 (ADR-2605215000)"}
  {:gate/id "G6"  :gate/name "kotoba-eavt-native"         :gate/rule "wafer lot / equipment telemetry / process step は kotoba Datom; RisingWave/SQL を canonical にしない"}
  {:gate/id "G7"  :gate/name "no-server-key"              :gate/rule "attestation 署名は member/operator 鍵; platform private key 非保持 (ADR-2605231525)"}
  {:gate/id "G8"  :gate/name "lot-traceability"           :gate/rule "wafer lot / chip test / packaging の全段 attestation を kotoba Datom に lossless 記録; silent truncation 禁止"}
  {:gate/id "G11" :gate/name "outward-gated"              :gate/rule "実 fab 装置の発注/建設 (Phase 4 自家 fab) は本 actor 射程外 + Council ゲート; R0 は設計のみ"}]

 ;; 8-process scope per ADR-2605242545; 4 cells implemented (R0), 4 pending (depo/etch/implant/CMP)
 :actor/cells
 [{:cell/id "mask_lithography" :cell/kind :langgraph :cell/runtime :wasm   :cell/status :r0}
  {:cell/id "wafer_processing" :cell/kind :langgraph :cell/runtime :wasm   :cell/status :r0}
  {:cell/id "chiptest"         :cell/kind :langgraph :cell/runtime :wasm   :cell/status :r0}
  {:cell/id "packaging"        :cell/kind :langgraph :cell/runtime :wasm   :cell/status :r0}
  {:cell/id "deposition"       :cell/kind :langgraph :cell/runtime :wasm   :cell/status :pending}
  {:cell/id "etch"             :cell/kind :langgraph :cell/runtime :wasm   :cell/status :pending}
  {:cell/id "implant"          :cell/kind :langgraph :cell/runtime :wasm   :cell/status :pending}
  {:cell/id "cmp"              :cell/kind :langgraph :cell/runtime :wasm   :cell/status :pending}
  {:cell/id "lot_ledger"       :cell/kind :datalog   :cell/runtime :kotoba :cell/status :r0}]

 ;; lexicons already exist under 00-contracts/lexicons/com/etzhayyim/silicon/
 :actor/lex
 [{:lex/id "chipManufacturingAttestation"}
  {:lex/id "waferLotAttestation"}
  {:lex/id "fabEquipmentTelemetry"}
  {:lex/id "silenForceReview"}]}
