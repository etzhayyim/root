;; watatsumi 綿津見 — class_certification_binder cell (terminal)
;; Bind all prior records into kotoba-datomic-anchored audit certification
{:cell/id "class_certification_binder"
 :cell/glyph "📋"
 :cell/kind :langgraph
 :cell/runtime :wasm
 :cell/entry "py/agent.py"
 :cell/handler "handle_class_certification_binder"
 :cell/doc "Terminal: Aggregate all prior stage records (hull → assembly → weld → system → join → pressure → trial + emissions) into one kotoba-datomic-anchored audit binder. Council 5-of-7 Safe attestation required for all new craft classes (silenSubmersibleReview). Output = classCertificationRecord (DNV-RU-UWT / ABS Underwater Vehicles / NK equivalent)."
 :cell/state-graph
 {:nodes [:collect-records :validate-completeness :anchor-kotoba-datomic :record-certification]
  :entry :collect-records
  :edges [[:collect-records :validate-completeness]
          [:validate-completeness :anchor-kotoba-datomic]
          [:anchor-kotoba-datomic :record-certification]
          [:record-certification :end]]}
 :cell/llm
 {:provider :murakumo
  :endpoint "127.0.0.1:4000"
  :model "gemma3:4b"
  :charter-rider true}
 :cell/gates ["G2" "G15" "G16"]
 :cell/kotoba
 {:reads [:hull-ring/attestation :section/attestation :weld-inspection/pass :system/attestation :section-join/attestation :pressure-test/pass :sea-trial/attestation :emissions-audit/compliant]
  :writes [:class-cert/id :class-cert/craft-did :class-cert/kotoba-datomic-cid :class-cert/all-stages-complete]}}
