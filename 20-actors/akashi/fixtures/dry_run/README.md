# dry_run fixture

Golden output for `adapters/dry_run_fixtures.py`.

This fixture is a regression guard for local parsing only. It does not represent
live collection, platform authorization, or kotoba writes.
