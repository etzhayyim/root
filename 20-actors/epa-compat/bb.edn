{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([epa.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'epa.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
