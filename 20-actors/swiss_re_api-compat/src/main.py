"""
Py Kotodama WASM entrypoint for the Swiss_re_api clean-room actor.

Clean-room, API-shaped CRUD over a Datomic-backed Kotoba schema.
Generated by 70-tools/deepen_actors.py (ADR 260607 deepening phase).
No proprietary code or credentials; resource shapes only.
"""
from kotodama import Runtime
from kotoba import load_schema
from datomic import DatomicClient
import uuid
import datetime

schema = load_schema("../schema/swiss_re_api.kotoba")
db = DatomicClient.connect()
app = Runtime("swiss_re_api-compat")


def now():
    return datetime.datetime.utcnow().isoformat()


def new_id(prefix):
    return f"{prefix}_" + uuid.uuid4().hex[:16]


def _persist(entity, rec):
    """Transact a record into Datomic as namespaced EAVT facts."""
    facts = {}
    for k, v in rec.items():
        facts[f"swiss_re_api.{entity}/{k}"] = v
    db.transact([facts])
    return rec


def _query(entity, eid=None):
    """Read records of an entity from Datomic (optionally by id)."""
    pattern = {"entity": f"swiss_re_api.{entity}"}
    if eid is not None:
        pattern["id"] = eid
    return db.query(pattern)


def _require(data, fields):
    missing = [f for f in fields if not data.get(f)]
    if missing:
        return {"error": {"message": "Missing required fields: " + ", ".join(missing)}}
    return None


@app.route("/v1/accounts", methods=["POST"])
def create_account(request):
    """Create a Account."""
    data = request.json or request.form or {}
    err = _require(data, ['type', 'balance'])
    if err:
        return err, 400
    rec = {"id": new_id("swissrea_acc")}
    rec["holderId"] = data.get('holderId')
    rec["type"] = data.get('type')
    rec["balance"] = float(data.get('balance') or 0)
    rec["currency"] = data.get('currency')
    rec["status"] = data.get('status')
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Account", rec)
    return rec, 201

@app.route("/v1/accounts", methods=["GET"])
def list_accounts(request):
    """List Accounts."""
    items = _query("Account")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/accounts/<eid>", methods=["GET"])
def get_account(request, eid):
    """Retrieve a Account by id."""
    rows = _query("Account", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/accounts/<eid>", methods=["POST", "PATCH"])
def update_account(request, eid):
    """Update a Account."""
    rows = _query("Account", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Account", rec)
    return rec, 200

@app.route("/v1/accounts/<eid>", methods=["DELETE"])
def delete_account(request, eid):
    """Delete a Account."""
    rows = _query("Account", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"swiss_re_api.Account", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/loans", methods=["POST"])
def create_loan(request):
    """Create a Loan."""
    data = request.json or request.form or {}
    err = _require(data, ['principal', 'rate'])
    if err:
        return err, 400
    rec = {"id": new_id("swissrea_loa")}
    rec["borrowerId"] = data.get('borrowerId')
    rec["principal"] = float(data.get('principal') or 0)
    rec["rate"] = float(data.get('rate') or 0)
    rec["termMonths"] = int(data.get('termMonths') or 0)
    rec["status"] = data.get('status')
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Loan", rec)
    return rec, 201

@app.route("/v1/loans", methods=["GET"])
def list_loans(request):
    """List Loans."""
    items = _query("Loan")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/loans/<eid>", methods=["GET"])
def get_loan(request, eid):
    """Retrieve a Loan by id."""
    rows = _query("Loan", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/loans/<eid>", methods=["POST", "PATCH"])
def update_loan(request, eid):
    """Update a Loan."""
    rows = _query("Loan", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Loan", rec)
    return rec, 200

@app.route("/v1/loans/<eid>", methods=["DELETE"])
def delete_loan(request, eid):
    """Delete a Loan."""
    rows = _query("Loan", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"swiss_re_api.Loan", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/policies", methods=["POST"])
def create_policy(request):
    """Create a Policy."""
    data = request.json or request.form or {}
    err = _require(data, ['type', 'premium'])
    if err:
        return err, 400
    rec = {"id": new_id("swissrea_pol")}
    rec["holderId"] = data.get('holderId')
    rec["type"] = data.get('type')
    rec["premium"] = float(data.get('premium') or 0)
    rec["coverage"] = float(data.get('coverage') or 0)
    rec["status"] = data.get('status')
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Policy", rec)
    return rec, 201

@app.route("/v1/policies", methods=["GET"])
def list_policies(request):
    """List Policies."""
    items = _query("Policy")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/policies/<eid>", methods=["GET"])
def get_policy(request, eid):
    """Retrieve a Policy by id."""
    rows = _query("Policy", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/policies/<eid>", methods=["POST", "PATCH"])
def update_policy(request, eid):
    """Update a Policy."""
    rows = _query("Policy", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Policy", rec)
    return rec, 200

@app.route("/v1/policies/<eid>", methods=["DELETE"])
def delete_policy(request, eid):
    """Delete a Policy."""
    rows = _query("Policy", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"swiss_re_api.Policy", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/claims", methods=["POST"])
def create_claim(request):
    """Create a Claim."""
    data = request.json or request.form or {}
    err = _require(data, ['amount', 'status'])
    if err:
        return err, 400
    rec = {"id": new_id("swissrea_cla")}
    rec["policyId"] = data.get('policyId')
    rec["amount"] = float(data.get('amount') or 0)
    rec["status"] = data.get('status')
    rec["filedAt"] = data.get('filedAt')
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Claim", rec)
    return rec, 201

@app.route("/v1/claims", methods=["GET"])
def list_claims(request):
    """List Claims."""
    items = _query("Claim")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/claims/<eid>", methods=["GET"])
def get_claim(request, eid):
    """Retrieve a Claim by id."""
    rows = _query("Claim", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/claims/<eid>", methods=["POST", "PATCH"])
def update_claim(request, eid):
    """Update a Claim."""
    rows = _query("Claim", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Claim", rec)
    return rec, 200

@app.route("/v1/claims/<eid>", methods=["DELETE"])
def delete_claim(request, eid):
    """Delete a Claim."""
    rows = _query("Claim", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"swiss_re_api.Claim", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/transactions", methods=["POST"])
def create_transaction(request):
    """Create a Transaction."""
    data = request.json or request.form or {}
    err = _require(data, ['amount', 'type'])
    if err:
        return err, 400
    rec = {"id": new_id("swissrea_tra")}
    rec["accountId"] = data.get('accountId')
    rec["amount"] = float(data.get('amount') or 0)
    rec["type"] = data.get('type')
    rec["postedAt"] = data.get('postedAt')
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Transaction", rec)
    return rec, 201

@app.route("/v1/transactions", methods=["GET"])
def list_transactions(request):
    """List Transactions."""
    items = _query("Transaction")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/transactions/<eid>", methods=["GET"])
def get_transaction(request, eid):
    """Retrieve a Transaction by id."""
    rows = _query("Transaction", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/transactions/<eid>", methods=["POST", "PATCH"])
def update_transaction(request, eid):
    """Update a Transaction."""
    rows = _query("Transaction", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Transaction", rec)
    return rec, 200

@app.route("/v1/transactions/<eid>", methods=["DELETE"])
def delete_transaction(request, eid):
    """Delete a Transaction."""
    rows = _query("Transaction", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"swiss_re_api.Transaction", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/customers", methods=["POST"])
def create_customer(request):
    """Create a Customer."""
    data = request.json or request.form or {}
    err = _require(data, ['name', 'kycStatus'])
    if err:
        return err, 400
    rec = {"id": new_id("swissrea_cus")}
    rec["name"] = data.get('name')
    rec["taxId"] = data.get('taxId')
    rec["kycStatus"] = data.get('kycStatus')
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Customer", rec)
    return rec, 201

@app.route("/v1/customers", methods=["GET"])
def list_customers(request):
    """List Customers."""
    items = _query("Customer")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/customers/<eid>", methods=["GET"])
def get_customer(request, eid):
    """Retrieve a Customer by id."""
    rows = _query("Customer", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/customers/<eid>", methods=["POST", "PATCH"])
def update_customer(request, eid):
    """Update a Customer."""
    rows = _query("Customer", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Customer", rec)
    return rec, 200

@app.route("/v1/customers/<eid>", methods=["DELETE"])
def delete_customer(request, eid):
    """Delete a Customer."""
    rows = _query("Customer", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"swiss_re_api.Customer", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/healthz", methods=["GET"])
def healthz(request):
    return {"status": "ok", "actor": "swiss_re_api-compat", "entities": ['Account', 'Loan', 'Policy', 'Claim', 'Transaction', 'Customer']}, 200


if __name__ == "__main__":
    app.start()
