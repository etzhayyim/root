{
  "name": "@etzhayyim/mst-projector",
  "version": "0.1.0",
  "description": "Phase 3 indexed materialized views for rw-free actors (subscribes to PDS firehose, maintains LanceDB + DuckDB indexes)",
  "type": "module",
  "main": "src/index.ts",
  "private": true,
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@etzhayyim/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
