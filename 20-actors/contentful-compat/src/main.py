"""
Py Kotodama WASM entrypoint for the Contentful clean-room actor.

Clean-room, API-shaped CRUD over a Datomic-backed Kotoba schema.
Generated by 70-tools/deepen_actors.py (ADR 260607 deepening phase).
No proprietary code or credentials; resource shapes only.
"""
from kotodama import Runtime
from kotoba import load_schema
from datomic import DatomicClient
import uuid
import datetime

schema = load_schema("../schema/contentful.kotoba")
db = DatomicClient.connect()
app = Runtime("contentful-compat")


def now():
    return datetime.datetime.utcnow().isoformat()


def new_id(prefix):
    return f"{prefix}_" + uuid.uuid4().hex[:16]


def _persist(entity, rec):
    """Transact a record into Datomic as namespaced EAVT facts."""
    facts = {}
    for k, v in rec.items():
        facts[f"contentful.{entity}/{k}"] = v
    db.transact([facts])
    return rec


def _query(entity, eid=None):
    """Read records of an entity from Datomic (optionally by id)."""
    pattern = {"entity": f"contentful.{entity}"}
    if eid is not None:
        pattern["id"] = eid
    return db.query(pattern)


def _require(data, fields):
    missing = [f for f in fields if not data.get(f)]
    if missing:
        return {"error": {"message": "Missing required fields: " + ", ".join(missing)}}
    return None


@app.route("/v1/contenttypes", methods=["POST"])
def create_content_type(request):
    """Create a ContentType."""
    data = request.json or request.form or {}
    err = _require(data, ['name', 'description'])
    if err:
        return err, 400
    rec = {"id": new_id("contentf_con")}
    rec["name"] = data.get('name')
    rec["description"] = data.get('description')
    rec["displayField"] = data.get('displayField')
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("ContentType", rec)
    return rec, 201

@app.route("/v1/contenttypes", methods=["GET"])
def list_content_types(request):
    """List ContentTypes."""
    items = _query("ContentType")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/contenttypes/<eid>", methods=["GET"])
def get_content_type(request, eid):
    """Retrieve a ContentType by id."""
    rows = _query("ContentType", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/contenttypes/<eid>", methods=["POST", "PATCH"])
def update_content_type(request, eid):
    """Update a ContentType."""
    rows = _query("ContentType", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("ContentType", rec)
    return rec, 200

@app.route("/v1/contenttypes/<eid>", methods=["DELETE"])
def delete_content_type(request, eid):
    """Delete a ContentType."""
    rows = _query("ContentType", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"contentful.ContentType", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/spaces", methods=["POST"])
def create_space(request):
    """Create a Space."""
    data = request.json or request.form or {}
    err = _require(data, ['name'])
    if err:
        return err, 400
    rec = {"id": new_id("contentf_spa")}
    rec["name"] = data.get('name')
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Space", rec)
    return rec, 201

@app.route("/v1/spaces", methods=["GET"])
def list_spaces(request):
    """List Spaces."""
    items = _query("Space")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/spaces/<eid>", methods=["GET"])
def get_space(request, eid):
    """Retrieve a Space by id."""
    rows = _query("Space", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/spaces/<eid>", methods=["POST", "PATCH"])
def update_space(request, eid):
    """Update a Space."""
    rows = _query("Space", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Space", rec)
    return rec, 200

@app.route("/v1/spaces/<eid>", methods=["DELETE"])
def delete_space(request, eid):
    """Delete a Space."""
    rows = _query("Space", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"contentful.Space", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/environments", methods=["POST"])
def create_environment(request):
    """Create a Environment."""
    data = request.json or request.form or {}
    err = _require(data, ['name'])
    if err:
        return err, 400
    rec = {"id": new_id("contentf_env")}
    rec["name"] = data.get('name')
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Environment", rec)
    return rec, 201

@app.route("/v1/environments", methods=["GET"])
def list_environments(request):
    """List Environments."""
    items = _query("Environment")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/environments/<eid>", methods=["GET"])
def get_environment(request, eid):
    """Retrieve a Environment by id."""
    rows = _query("Environment", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/environments/<eid>", methods=["POST", "PATCH"])
def update_environment(request, eid):
    """Update a Environment."""
    rows = _query("Environment", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Environment", rec)
    return rec, 200

@app.route("/v1/environments/<eid>", methods=["DELETE"])
def delete_environment(request, eid):
    """Delete a Environment."""
    rows = _query("Environment", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"contentful.Environment", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/locales", methods=["POST"])
def create_locale(request):
    """Create a Locale."""
    data = request.json or request.form or {}
    err = _require(data, ['name', 'code'])
    if err:
        return err, 400
    rec = {"id": new_id("contentf_loc")}
    rec["name"] = data.get('name')
    rec["code"] = data.get('code')
    rec["fallbackCode"] = data.get('fallbackCode')
    rec["default"] = bool(data.get('default'))
    rec["optional"] = bool(data.get('optional'))
    rec["contentDeliveryApi"] = bool(data.get('contentDeliveryApi'))
    rec["contentManagementApi"] = bool(data.get('contentManagementApi'))
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Locale", rec)
    return rec, 201

@app.route("/v1/locales", methods=["GET"])
def list_locales(request):
    """List Locales."""
    items = _query("Locale")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/locales/<eid>", methods=["GET"])
def get_locale(request, eid):
    """Retrieve a Locale by id."""
    rows = _query("Locale", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/locales/<eid>", methods=["POST", "PATCH"])
def update_locale(request, eid):
    """Update a Locale."""
    rows = _query("Locale", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Locale", rec)
    return rec, 200

@app.route("/v1/locales/<eid>", methods=["DELETE"])
def delete_locale(request, eid):
    """Delete a Locale."""
    rows = _query("Locale", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"contentful.Locale", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/apikeys", methods=["POST"])
def create_api_key(request):
    """Create a ApiKey."""
    data = request.json or request.form or {}
    err = _require(data, ['name', 'description'])
    if err:
        return err, 400
    rec = {"id": new_id("contentf_api")}
    rec["name"] = data.get('name')
    rec["description"] = data.get('description')
    rec["accessToken"] = data.get('accessToken')
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("ApiKey", rec)
    return rec, 201

@app.route("/v1/apikeys", methods=["GET"])
def list_api_keys(request):
    """List ApiKeys."""
    items = _query("ApiKey")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/apikeys/<eid>", methods=["GET"])
def get_api_key(request, eid):
    """Retrieve a ApiKey by id."""
    rows = _query("ApiKey", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/apikeys/<eid>", methods=["POST", "PATCH"])
def update_api_key(request, eid):
    """Update a ApiKey."""
    rows = _query("ApiKey", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("ApiKey", rec)
    return rec, 200

@app.route("/v1/apikeys/<eid>", methods=["DELETE"])
def delete_api_key(request, eid):
    """Delete a ApiKey."""
    rows = _query("ApiKey", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"contentful.ApiKey", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/v1/webhooks", methods=["POST"])
def create_webhook(request):
    """Create a Webhook."""
    data = request.json or request.form or {}
    err = _require(data, ['name', 'url'])
    if err:
        return err, 400
    rec = {"id": new_id("contentf_web")}
    rec["name"] = data.get('name')
    rec["url"] = data.get('url')
    rec["httpBasicUsername"] = data.get('httpBasicUsername')
    rec["active"] = bool(data.get('active'))
    rec["createdAt"] = now()
    rec["updatedAt"] = rec["createdAt"]
    _persist("Webhook", rec)
    return rec, 201

@app.route("/v1/webhooks", methods=["GET"])
def list_webhooks(request):
    """List Webhooks."""
    items = _query("Webhook")
    return {"object": "list", "data": items, "count": len(items)}, 200

@app.route("/v1/webhooks/<eid>", methods=["GET"])
def get_webhook(request, eid):
    """Retrieve a Webhook by id."""
    rows = _query("Webhook", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    return rows[0], 200

@app.route("/v1/webhooks/<eid>", methods=["POST", "PATCH"])
def update_webhook(request, eid):
    """Update a Webhook."""
    rows = _query("Webhook", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    data = request.json or request.form or {}
    rec = rows[0]
    for k, v in data.items():
        if k in rec and k not in ("id", "createdAt"):
            rec[k] = v
    rec["updatedAt"] = now()
    _persist("Webhook", rec)
    return rec, 200

@app.route("/v1/webhooks/<eid>", methods=["DELETE"])
def delete_webhook(request, eid):
    """Delete a Webhook."""
    rows = _query("Webhook", eid)
    if not rows:
        return {"error": {"message": "Not found"}}, 404
    db.retract({"entity": f"contentful.Webhook", "id": eid})
    return {"id": eid, "deleted": True}, 200

@app.route("/healthz", methods=["GET"])
def healthz(request):
    return {"status": "ok", "actor": "contentful-compat", "entities": ['ContentType', 'Space', 'Environment', 'Locale', 'ApiKey', 'Webhook']}, 200


if __name__ == "__main__":
    app.start()
