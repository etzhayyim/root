"""nusa fiber_provenance (幣) cell — R0 coded reference.

The G1 enforcement point #1-of-cells: turns a cultivar into a fibre-use provenance
record ONLY after the THC-class screen passes. A psychoactive/high-THC input raises
ValueError — there is no code path that produces a provenance record for it.
.solve() raises until Council activation (ADR-2606039800).
"""
