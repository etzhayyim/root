;; junkan 循環 — clj-native actor manifest (kotoba-Datom-native sibling of manifest.jsonld)
;; ANALYSIS-ONLY societal-systems-dynamics observer. ADR-2605290927.
;; This manifest describes the clj-native governance-asymmetry substrate (added
;; 2026-06-21); the AT-Proto lexicon/cell manifest stays in manifest.jsonld.
{:actor/id        "junkan"
 :actor/glyph     "循"
 :actor/tier      :tier-b
 :actor/domain    "junkan.etzhayyim.com"
 :actor/kind      :societal-systems-dynamics-observer
 :actor/purpose   "ANALYSIS-ONLY system-dynamics observer of society. Reads which feedback loops are spinning 好循環/悪循環 + Meadows leverage candidates from passive, public, AGGREGATE data — and may ONLY look, never touch (G4, no outward channel; enforced by absence). The clj-native governance-asymmetry substrate applies this to one question: which concrete laws/institutions/doctrines/values worldwide widen or narrow the structural asymmetry between citizens (国民) and the state (政府), recording who established each (誰が), the circumstances (経緯), and the stakeholders (関係者). Everything is a disclosed HYPOTHESIS (G5), aggregate-only (G6), a resilience/leverage MAP and never a target-list (G7)."
 :actor/status    :r0
 :actor/adr       "2605290927"
 :actor/related   ["2605192100" "2605262130" "2605312345" "2605262400" "2605240200" "2605264000" "2605241500"]

 :actor/charter-rider {:rider/version "3.5" :rider/applied true}

 :actor/substrate
 {:ontology "20-actors/junkan/kotoba/ontology.junkan-gov.edn"
  :seed     "20-actors/junkan/kotoba/seed.governance-asymmetry.edn"
  :dataset  "80-data/junkan-governance/"
  :methods  ["junkan_edn" "analyze" "query" "validate" "kotoba" "autorun"]}

 :actor/gates
 [{:gate/id "G4" :gate/name "analysis-only-no-actuation" :gate/rule "No outward channel — output is append-only findings + a local ledger; no post/mention/email/tx. Enforced by ABSENCE of any dispatch path; :junkan/actuate + :junkan/dispatch unrepresentable (test-enforced)"}
  {:gate/id "G5" :gate/name "no-causal-overclaim" :gate/rule "Every loop/regime is a HYPOTHESIS (:junkan/hypothesis :true); correlation/structural-sign only, never proven causation; :junkan.gov.loop/proven-cause unrepresentable"}
  {:gate/id "G6" :gate/name "aggregate-only-no-individual" :gate/rule "Instruments + institutional/public-historical enactors only; no private-individual modeling, no PII; :junkan.gov.instr/person unrepresentable"}
  {:gate/id "G7" :gate/name "sober-non-eschatological-map" :gate/rule "Vicious findings stated soberly; a resilience/leverage MAP, never a target-list or country ranking-to-shame (§1.13 + §1.15)"}
  {:gate/id "G9" :gate/name "append-only-immutable" :gate/rule "Findings are append-only content-addressed datoms on a tamper-evident commit-DAG; nothing retracted"}
  {:gate/id "G11" :gate/name "candidates-not-directives" :gate/rule "Meadows leverage points are CANDIDATES with uncertainty (:prescription? false); no directive issued; :junkan.gov/prescription unrepresentable"}
  {:gate/id "G13" :gate/name "council-internal-default" :gate/rule "Default audience = Council/internal; surfacing beyond is performed by ossekai/kataribe on junkan's behalf, never by junkan (preserves G4)"}]

 :actor/non-goals
 [{:n/id "N1" :n/rule "Never intervenes/actuates (not ossekai — no nudge/post/mention/email/tx)"}
  {:n/id "N3" :n/rule "Not a forecaster / prediction-market (describes present regime; no point-forecasts-as-fact)"}
  {:n/id "N4" :n/rule "Not a policy-prescriber / lobby (candidates with uncertainty only)"}
  {:n/id "N6" :n/rule "Not a state-intel function (§1.12 routing-around)"}
  {:n/id "N9" :n/rule "Not individual profiling / social-scoring"}]

 :actor/composes-with
 {:danjo "public-accountability discrepancy observations feed instrument data"
  :keizu "government power-relations KG grounds enactor/stakeholder edges"
  :kanae "renders fund-flow visualization of economic-capture instruments"
  :ooyake "government structural atlas grounds institution instruments"
  :kosatsu "competing crime/sanction designations ground coercion instruments"
  :ossekai "may publish a junkan finding ON junkan's behalf (G13), never junkan"}}
