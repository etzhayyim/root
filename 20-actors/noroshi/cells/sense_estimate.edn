{:cell/id "sense_estimate"
 :cell/glyph "烽"
 :cell/face :isac
 :cell/kind :langgraph
 :cell/runtime :wasm
 :cell/node "benjamin"
 :cell/doc "Recover a target's range + radial velocity from an isacWaveform via the 2-D OFDM-radar periodogram (methods/isac_sim.py). The target is an OBJECT — :target-class :object only; no :person value exists (G4/N2; sensing is not surveillance, a target is not a person — watari G4). Honest aliasing beyond R_max / the Doppler interval (N4). Emits a senseEstimate Datom. R0 scaffold (.solve() raises)."
 :cell/entry "cells/sense_estimate/cell.py"
 :cell/handler "SenseEstimateCell"
 :cell/state-graph
 {:nodes [:reciprocal-grid :periodogram :peak-pick :emit] :entry :reciprocal-grid
  :edges [[:reciprocal-grid :periodogram] [:periodogram :peak-pick] [:peak-pick :emit] [:emit :end]]}
 :cell/gates ["G4" "G9" "G10"]
 :cell/kotoba {:reads [:wave/id :wave/range-res-m :wave/vel-res-mps] :writes [:sense/id :sense/target-class :sense/range-m :sense/velocity-mps]}}
