{:cell/id "reliability_qual"
 :cell/glyph "烽"
 :cell/face :packaging
 :cell/kind :langgraph
 :cell/runtime :wasm
 :cell/node "manasseh"
 :cell/coded true
 :cell/doc "Plan a packaged photonic-module reliability qualification using the Telcordia GR-468 test SHAPE only (G1 open standard, names as neutral vocabulary): thermal cycling, damp heat, mechanical shock, fibre pull. Murakumo-only narration (G6). Emits qual-plan Datoms; live environmental testing is G8-gated (no chamber at R0). R0 scaffold (.solve() raises). Coded (this maturity pass): select-suite/stress-plan/acceptance/emit are real, tested Clojure (cells/reliability_qual/{cell,state_machine}.cljc) calling a genuine PASS/FAIL engine (methods/reliability-qual, representative GR-468-SHAPE thresholds, G10) over caller-supplied results — only .solve() itself stays Council-gated; no live chamber exists at any activation level short of a real qualification-lab integration."
 :cell/entry "cells/reliability_qual/cell.cljc"
 :cell/handler "noroshi.cells.reliability-qual.cell/solve"
 :cell/state-graph
 {:nodes [:select-suite :stress-plan :acceptance :emit] :entry :select-suite
  :edges [[:select-suite :stress-plan] [:stress-plan :acceptance] [:acceptance :emit] [:emit :end]]}
 :cell/llm {:provider :murakumo :endpoint "127.0.0.1:4000" :model "gemma3:4b" :charter-rider true}
 :cell/gates ["G1" "G6" "G8" "G9" "G10"]
 :cell/kotoba {:reads [:pdev/id] :writes [:qual/id :qual/suite :qual/acceptance :qual/dry-run :qual/representative]}}
