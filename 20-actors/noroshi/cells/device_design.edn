{:cell/id "device_design"
 :cell/glyph "烽"
 :cell/face :chip
 :cell/kind :langgraph
 :cell/runtime :wasm
 :cell/node "naphtali"
 :cell/doc "Generative photonic-device design: NL intent → Murakumo (G6) → an open-EPDA ModelOp plan (GDSFactory/Meep/KLayout SHAPE only, G1) for a silicon-photonic comms component (modulator / grating-coupler / CPO module). Force class is :civilian-comms by construction (G3/N1). Emits a photonicDevice Datom. R0 scaffold (.solve() raises); live tapeout is G8-gated."
 :cell/entry "cells/device_design/cell.py"
 :cell/handler "DeviceDesignCell"
 :cell/state-graph
 {:nodes [:intent :civilian-gate :epda-plan :emit] :entry :intent
  :edges [[:intent :civilian-gate] [:civilian-gate :epda-plan] [:epda-plan :emit] [:emit :end]]}
 :cell/llm {:provider :murakumo :endpoint "127.0.0.1:4000" :model "gemma3:4b" :charter-rider true}
 :cell/gates ["G1" "G3" "G6" "G9" "G10"]
 :cell/kotoba {:reads [] :writes [:pdev/id :pdev/kind :pdev/force-class :pdev/eda]}}
