[package]
name = "hoge-shannon-jco"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { version = "0.36", features = ["realloc"] }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
