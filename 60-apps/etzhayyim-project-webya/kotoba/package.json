{
  "name": "@etzhayyim/webya-kotoba",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "kotoba impl of webya (AI homepage generation for 企業・士業, Consensys c-split). PLAINTEXT: site catalog + template + page + generation-job ops timeline + domain (public DNS proof tokens). E2E (kotoba, ADR-2605181100): client contact PII + 士業 per-person regulated disclosure credentials via sdk.encryptedWrite/Read (read-cap = owner DID + recipients). STAYS etzhayyim via consent-capability: LLM/LangGraph site-generation inference + CF custom-hostname provisioning call + CF_API_TOKEN custody. Founder directive 2026-06-03: maximal migration.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": { "@etzhayyim/sdk": "workspace:*" },
  "devDependencies": { "@etzhayyim/sdk-mock": "workspace:*", "typescript": "^5.6.0", "vitest": "^4.1.0" }
}
