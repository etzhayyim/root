{
  "name": "@etzhayyim/newsletter-kotoba",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "kotoba reference implementation of newsletter (public newsletter-issue archive: issue + section) using @etzhayyim/sdk. Registry on AT PDS (replaces RW). (c) mixed split: public published-issue archive migrates; subscriber list (email PII) + Resend batch delivery + LangGraph LLM generation + sponsor/ad slot stay etzhayyim. Substrate posture per ADR-2605172000.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": { "@etzhayyim/sdk": "workspace:*" },
  "devDependencies": { "@etzhayyim/sdk-mock": "workspace:*", "typescript": "^5.6.0", "vitest": "^4.1.0" }
}
