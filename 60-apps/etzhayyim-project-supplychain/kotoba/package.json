{
  "name": "@etzhayyim/supplychain-kotoba",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "kotoba reference implementation of supplychain (supply-graph: material/assembly/supplier nodes + dependency edges + risk) using @etzhayyim/sdk. Registry on AT PDS (replaces vertex_jukyu_supply_node / edge_jukyu_supply_dependency). Substrate posture per ADR-2605172000.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@etzhayyim/sdk": "workspace:*"
  },
  "devDependencies": {
    "@etzhayyim/sdk-mock": "workspace:*",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0"
  }
}
