{
  "name": "cyber-drill-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build:assets": "cd ../svelte && pnpm run build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "gen-key": "node scripts/gen-key.mjs",
    "kv:list": "wrangler kv key list --binding=DRILL_KEYS --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250520.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.86.0"
  }
}
