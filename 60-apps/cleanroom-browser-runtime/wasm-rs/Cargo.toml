[package]
name = "cleanroom-actor-raw"
version = "0.1.0"
edition = "2021"
description = "Compact Rust clean-room actor → raw single-block WASM (browser-local tier, ADR 260607 / 2606014500)"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
strip = true
codegen-units = 1
