{
  "name": "@etzhayyim/resource-provider-rw-free",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "RW-free impl of resource-provider (Consensys c-split). Public marketplace catalog (resourceListing) + aggregate stats (contributionStat) plaintext; provider PII (providerProfile), contribution content (contributionEntry), reward ledger + balances (rewardLedgerEntry/rewardBalance) sealed via kotoba E2E (sdk.encryptedWrite/Read, ADR-2605181100). GPU/LLM inference, quality-validation compute, raw-credential custody, and the fiat MoR / payout settlement rail stay etzhayyim via consent-capability.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": { "@etzhayyim/sdk": "workspace:*" },
  "devDependencies": { "@etzhayyim/sdk-mock": "workspace:*", "typescript": "^5.6.0", "vitest": "^4.1.0" }
}
