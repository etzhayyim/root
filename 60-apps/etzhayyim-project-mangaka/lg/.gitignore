# Python build / cache
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Local venvs (smoke / dev)
.venv*/

# langgraph-cli local-dev runtime artefacts
.langgraph_api/

# uv lockfiles (pin via Dockerfile, not committed)
uv.lock
