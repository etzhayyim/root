{
  "name": "@etzhayyim/meet-kotoba",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "kotoba impl of meet (Consensys c-split). Google Meet + recorder migration: public meeting catalog + media-chunk pointer metadata plaintext (sdk.write/read, FK recordingChunk→meeting via exists()); recorder session / participant / transcript PII + private content sealed via kotoba E2E (sdk.encryptedWrite/Read, ADR-2605181100). MLX whisper transcription inference + recorder-bot join/leave enforcement + OAuth/consent credential custody + media-byte archive stay etzhayyim via consent-capability. AT-Lexicon no-float: transcript confidence carried as integer pct 0-100.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": { "@etzhayyim/sdk": "workspace:*" },
  "devDependencies": { "@etzhayyim/sdk-mock": "workspace:*", "typescript": "^5.6.0", "vitest": "^4.1.0" }
}
