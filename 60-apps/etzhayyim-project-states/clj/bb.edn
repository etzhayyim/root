;; Scoped bb.edn for the etzhayyim-project-states clj port (ADR-2606280030).
;; Pure-stdlib data tooling — cheshire + clojure.test are built into babashka,
;; so no external deps. Run tests with:  bb test   (or)  bb run_tests.clj
{:paths ["src" "resources" "test"]
 :tasks
 {test {:doc "Run the clojure.test suite under babashka"
        :task (load-file "run_tests.clj")}}}
