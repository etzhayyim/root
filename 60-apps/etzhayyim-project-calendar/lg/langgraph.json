{
  "$comment": "lg-calendar — LangGraph Server config. Google Calendar v3 + Microsoft Graph compatible calendar API over kotoba datomic (ADR-2606010500). The primary surface is the FastAPI XRPC server (lg_calendar.server:app); graphs below are for langgraph dev / health.",
  "dependencies": ["./lg_calendar"],
  "graphs": {
    "health": "./lg_calendar/graphs/health.py:GRAPH"
  },
  "env": ".env",
  "python_version": "3.11",
  "auth": {
    "$comment": "x-api-key (LG_CALENDAR_API_KEY env), optional. Internal-trust header from the atproto actor-worker pipethrough is the edge boundary."
  }
}
