# maps RunPod Serverless endpoint deps. ADR-2604271800.
#
# Phase 1 (scaffold, deterministic stub): just `runpod`. Cold-start
# stays under 2s, no GPU needed, contract verifiable end-to-end.
#
# Phase 2 (real models): uncomment the geospatial + torch stack and
# rebuild the image. RunPod base images for `pytorch:2.4-cuda12.4`
# already ship torch + numpy, so the incremental footprint is only
# rasterio/rio-tiler + the model weights downloaded on first invoke.

runpod>=1.7.0

# --- Phase 2 (real inference) ---
# torch>=2.4.0
# torchvision>=0.19.0
# rasterio>=1.3.10
# rio-tiler>=6.6.0
# numpy>=1.26.0
# pillow>=10.4.0
# huggingface_hub>=0.24.0
# torchgeo>=0.6.0
