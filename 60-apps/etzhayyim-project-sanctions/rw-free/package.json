{
  "name": "@etzhayyim/sanctions-rw-free",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "RW-free reference implementation of sanctions (public consolidated sanctions-LIST reference: OFAC SDN/CONS + EU + UN + JP-MOF + UK-OFSI + AU-DFAT + CA-OSFI entries + per-list refresh tracking) using @etzhayyim/sdk. Registry on AT PDS (replaces RW). (c) mixed split: lists are open-data; screenEntity screening + match audit (customer PII custody + AML liability) stay etzhayyim via consent-capability. Substrate posture per ADR-2605172000.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": { "@etzhayyim/sdk": "workspace:*" },
  "devDependencies": { "@etzhayyim/sdk-mock": "workspace:*", "typescript": "^5.6.0", "vitest": "^4.1.0" }
}
