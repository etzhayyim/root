#!/bin/zsh
set -euo pipefail

set -a
source "{{ ansible_facts.env.HOME }}/.etzhayyim/executor.env"
set +a

exec "{{ ansible_facts.env.HOME }}/executor-venv/bin/uvicorn" \
  app:app \
  --app-dir "{{ ansible_facts.env.HOME }}/.etzhayyim/executor" \
  --host 0.0.0.0 \
  --port "{{ executor_port }}"
