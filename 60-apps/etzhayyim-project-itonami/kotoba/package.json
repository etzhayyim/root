{
  "name": "@etzhayyim/itonami-kotoba",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "kotoba reference implementation of itonami (aircraft engine lifecycle SIMULATION: engine designs + assembly + procurement + test results) using @etzhayyim/sdk. Registry on AT PDS. Engineering simulation/digital-twin data; no PII, real settlement, or regulatory liability. Substrate posture per ADR-2605172000.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": { "@etzhayyim/sdk": "workspace:*" },
  "devDependencies": { "@etzhayyim/sdk-mock": "workspace:*", "typescript": "^5.6.0", "vitest": "^4.1.0" }
}
