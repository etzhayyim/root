{
  "name": "@etzhayyim/os-messaging-rw-free",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "RW-free impl of os-messaging (Consensys c-split, maximal migration). PLAINTEXT public crawl catalog (openChannel, scraperRun) + kotoba-E2E private control-plane/content (bridge, openMessage) sealed via sdk.encryptedWrite/Read (ADR-2605181100). Platform credential custody + webhook send/receive relay + crawl/scrape compute EXECUTION stay etzhayyim via consent-capability.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": { "@etzhayyim/sdk": "workspace:*" },
  "devDependencies": { "@etzhayyim/sdk-mock": "workspace:*", "typescript": "^5.6.0", "vitest": "^4.1.0" }
}
