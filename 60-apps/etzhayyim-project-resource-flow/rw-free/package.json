{
  "name": "@etzhayyim/resource-flow-rw-free",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "RW-free reference implementation of resource-flow (public 2次ソース resource-flow visualization data: emitter registry + flow edges + anomaly records) using @etzhayyim/sdk. Registry on AT PDS (replaces RW). (c) mixed split: the public externally-authored flow data migrates; the anomaly-detection algorithm + sankey-MV aggregation compute stays etzhayyim via consent-capability. Substrate posture per ADR-2605172000 + ADR-0028.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": { "@etzhayyim/sdk": "workspace:*" },
  "devDependencies": { "@etzhayyim/sdk-mock": "workspace:*", "typescript": "^5.6.0", "vitest": "^4.1.0" }
}
