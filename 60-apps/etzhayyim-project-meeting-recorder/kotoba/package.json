{
  "name": "@etzhayyim/meeting-recorder-kotoba",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "kotoba impl of meeting-recorder (Consensys c-split, WAVE 2). Public provider catalog plaintext + session/recordingChunk/transcriptSegment per-person bodies sealed via kotoba E2E (sdk.encryptedWrite/Read, ADR-2605181100). Recorder-bot join/capture execution, GPU/MLX whisper inference, B2 media-blob custody, and consentToken custody stay etzhayyim via consent-capability.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": { "@etzhayyim/sdk": "workspace:*" },
  "devDependencies": { "@etzhayyim/sdk-mock": "workspace:*", "typescript": "^5.6.0", "vitest": "^4.1.0" }
}
