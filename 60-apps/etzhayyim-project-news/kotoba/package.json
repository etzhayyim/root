{
  "name": "@etzhayyim/news-kotoba",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "kotoba reference implementation of news (public news-aggregation catalog: RSS/news source + ingested article) using @etzhayyim/sdk. Registry on AT PDS (replaces RW). (c) mixed split: public news-media open-data migrates; the wRPC pipeline's quality-evaluation + translation LLM compute stays etzhayyim. Substrate posture per ADR-2605172000.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": { "@etzhayyim/sdk": "workspace:*" },
  "devDependencies": { "@etzhayyim/sdk-mock": "workspace:*", "typescript": "^5.6.0", "vitest": "^4.1.0" }
}
