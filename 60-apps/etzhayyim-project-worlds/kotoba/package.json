{
  "name": "@etzhayyim/worlds-kotoba",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "kotoba reference implementation of worlds (virtual-worlds authoring: scene → asset + portal) using @etzhayyim/sdk. Registry on AT PDS (replaces RW). (a) content-authoring product (webpage/kami/pptx cluster); first-party user-authored scenes/assets/portals + public published-scene directory. No generation compute (contrast voxelforge). Substrate posture per ADR-2605172000.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": { "@etzhayyim/sdk": "workspace:*" },
  "devDependencies": { "@etzhayyim/sdk-mock": "workspace:*", "typescript": "^5.6.0", "vitest": "^4.1.0" }
}
