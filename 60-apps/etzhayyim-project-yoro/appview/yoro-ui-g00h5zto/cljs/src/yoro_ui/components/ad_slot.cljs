(ns yoro-ui.components.ad-slot
  "Port of svelte/src/lib/components/AdSlot.svelte.
   removed: §2(c) ad-tech (adsense, exoclick, medianet) — see CHARTER-RIDER.md.
   Renders nothing; kept so callers ported from svelte keep compiling.")

(defn ad-slot [& _]
  nil)
