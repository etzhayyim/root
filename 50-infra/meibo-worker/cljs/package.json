{
  "name": "meibo-worker-cljs",
  "version": "0.1.0",
  "private": true,
  "description": "ClojureScript core for the meibo Cloudflare Worker (compiled to ../cljs-out/worker_core.js)",
  "scripts": {
    "build": "npx shadow-cljs release worker",
    "watch": "npx shadow-cljs watch worker"
  },
  "devDependencies": {
    "shadow-cljs": "^2.28.3"
  }
}
