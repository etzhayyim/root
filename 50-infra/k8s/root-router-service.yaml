apiVersion: v1
kind: Service
metadata:
  name: etzhayyim-root-router-svc
  namespace: clean-room-system
spec:
  type: LoadBalancer
  selector:
    app: root-router
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
