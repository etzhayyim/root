---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mitama-udf
resources:
  - deployment.yaml
# Secret is NOT listed here intentionally — create it imperatively
# from local creds (1Password / Keychain) before `kubectl apply -k`.
# See secret.yaml.example for the create-secret one-liner.
images:
  - name: ghcr.io/etzhayyim/lg-yatabase
    newTag: 0.0.6-amd64
