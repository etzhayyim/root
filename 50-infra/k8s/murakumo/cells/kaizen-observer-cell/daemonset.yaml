apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: kaizen-observer-cell
  namespace: etzhayyim-cells
  labels:
    app.kubernetes.io/name: kaizen-observer-cell
    app.kubernetes.io/component: religious-corp-cell
    app.kubernetes.io/part-of: etzhayyim-religious-corp
    etzhayyim.com/cell-name: KaizenObserverCell
    etzhayyim.com/leader-node: levi
  annotations:
    etzhayyim.com/leader-node: levi
    etzhayyim.com/leader-hostname: levinomac-mini.local
    etzhayyim.com/trigger: cron
    etzhayyim.com/healthz-port: '13070'
    etzhayyim.com/cron: '*/10 * * * *'
    etzhayyim.com/adr: 2605240200,2605240100,2605240000,2605232345
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: kaizen-observer-cell
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kaizen-observer-cell
        app.kubernetes.io/component: religious-corp-cell
        app.kubernetes.io/part-of: etzhayyim-religious-corp
        etzhayyim.com/cell-name: KaizenObserverCell
        etzhayyim.com/leader-node: levi
    spec:
      serviceAccountName: kaizen-observer-cell
      nodeSelector:
        kubernetes.io/hostname: levinomac-mini.local
      imagePullSecrets:
        - name: ghcr-pull
      containers:
        - name: cell
          image: ghcr.io/etzhayyim/kotodama:main
          imagePullPolicy: Always
          command:
            - python
            - -m
            - kotodama.cell_runner_main
            - --node
            - levi
            - --cell-only
            - KaizenObserverCell
          env:
            - name: ETZ_SUBSTRATE
              value: kotoba-datomic
            - name: ETZ_CHECKPOINTER_SOCKET
              value: /run/etzhayyim/checkpointer.sock
            - name: ETZ_CELL_NAME
              value: KaizenObserverCell
            - name: ETZHAYYIM_LOG_DIR
              value: /tmp/etzhayyim/log
            - name: ETZ_HEALTHZ_BIND
              value: 0.0.0.0
            - name: FLEET_TOML
              value: /etc/etzhayyim/fleet.toml
          ports:
            - name: runner-healthz
              containerPort: 13000
              protocol: TCP
            - name: cell-healthz
              containerPort: 13070
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: runner-healthz
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: runner-healthz
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: fleet-config
              mountPath: /etc/etzhayyim
              readOnly: true
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 128Mi
        - name: fleet-config
          configMap:
            name: fleet-config
            items:
              - key: fleet.toml
                path: fleet.toml
