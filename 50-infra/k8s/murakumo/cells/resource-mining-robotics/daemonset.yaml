apiVersion: apps/v1
kind: DaemonSet
metadata:
  labels:
    app.kubernetes.io/name: resource-mining-robotics
    etzhayyim.com/cell-name: ResourceMiningRoboticsCell
    etzhayyim.com/leader-node: gad
  name: resource-mining-robotics
  namespace: etzhayyim-cells
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: resource-mining-robotics
  template:
    metadata:
      labels:
        app.kubernetes.io/name: resource-mining-robotics
        etzhayyim.com/cell-name: ResourceMiningRoboticsCell
        etzhayyim.com/leader-node: gad
    spec:
      containers:
      - command:
        - python
        - -m
        - kotodama.cell_runner_main
        - --node
        - gad
        - --cell-only
        - ResourceMiningRoboticsCell
        env:
        - name: ETZ_SUBSTRATE
          value: kotoba-datomic
        - name: ETZ_CELL_NAME
          value: ResourceMiningRoboticsCell
        - name: FLEET_TOML
          value: /etc/etzhayyim/fleet.toml
        - name: COUNCIL_FLEET_ATTESTATION_TX_HASH
          value: '0xabcdef1234567890'
        image: ghcr.io/etzhayyim/kotodama:main
        name: cell
        volumeMounts:
        - mountPath: /etc/etzhayyim
          name: fleet-config
          readOnly: true
      nodeSelector:
        kubernetes.io/hostname: gadnomac-mini.local
      volumes:
      - configMap:
          name: fleet-config
        name: fleet-config
