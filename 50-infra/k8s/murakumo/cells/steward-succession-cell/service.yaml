apiVersion: v1
kind: Service
metadata:
  name: steward-succession-cell
  namespace: etzhayyim-cells
  labels:
    app.kubernetes.io/name: steward-succession-cell
    app.kubernetes.io/component: religious-corp-cell
    app.kubernetes.io/part-of: etzhayyim-religious-corp
    etzhayyim.com/cell-name: StewardSuccessionCell
    etzhayyim.com/leader-node: judah
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: steward-succession-cell
  ports:
    - name: runner-healthz
      port: 13000
      targetPort: runner-healthz
      protocol: TCP
    - name: cell-healthz
      port: 13007
      targetPort: cell-healthz
      protocol: TCP
