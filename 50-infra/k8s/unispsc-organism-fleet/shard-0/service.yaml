apiVersion: v1
kind: Service
metadata:
  name: unispsc-organism-fleet-shard-0
  namespace: etzhayyim-organism
  labels:
    app.kubernetes.io/name: unispsc-organism-fleet
    etzhayyim.com/shard: "0"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: unispsc-organism-fleet
    etzhayyim.com/shard: "0"
  ports:
    - name: healthz
      port: 13040
      targetPort: healthz
      protocol: TCP
