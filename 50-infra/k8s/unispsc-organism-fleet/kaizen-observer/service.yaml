apiVersion: v1
kind: Service
metadata:
  name: kaizen-observer
  namespace: etzhayyim-organism
  labels:
    app.kubernetes.io/name: kaizen-observer
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kaizen-observer
  ports:
    - name: healthz
      port: 13070
      targetPort: 13070
      protocol: TCP
