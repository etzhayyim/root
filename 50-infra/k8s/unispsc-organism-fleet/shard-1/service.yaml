apiVersion: v1
kind: Service
metadata:
  name: unispsc-organism-fleet-shard-1
  namespace: etzhayyim-organism
  labels:
    app.kubernetes.io/name: unispsc-organism-fleet
    etzhayyim.com/shard: "1"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: unispsc-organism-fleet
    etzhayyim.com/shard: "1"
  ports:
    - name: healthz
      port: 13050
      targetPort: healthz
      protocol: TCP
