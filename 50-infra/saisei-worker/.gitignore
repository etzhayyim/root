node_modules/
.wrangler/
.dev.vars
*.log
dist/
build/
worker-configuration.d.ts
out/
# shadow-cljs build output for the Worker core — generated by `cljs/ npm run
# build` (run automatically by the `build:cljs` / `predeploy` scripts). Not
# committed; rebuilt from cljs/src on every deploy.
cljs-out/
cljs/.shadow-cljs/
