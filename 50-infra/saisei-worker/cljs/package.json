{
  "name": "saisei-worker-cljs",
  "version": "0.1.0",
  "private": true,
  "description": "ClojureScript core for the saisei Cloudflare Worker (compiled to ../cljs-out/worker_core.js)",
  "scripts": {
    "build": "amu compile --target wasm32-browser worker",
    "watch": "amu compile --target wasm32-browser worker",
    "gen-data": "kbb --backend sci ../../../70-tools/scripts/saisei-worker/gen-data.cljk"
  },
  "devDependencies": {
    "shadow-cljs": "^3.4.11"
  }
}
