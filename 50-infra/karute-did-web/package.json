{
  "name": "karute-did-web",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker serving did:web:karute.etzhayyim.com DID Document + reverse-proxying the karute SuperApp static bundle and XRPC requests. Per ADR-2605231900.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260501.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
