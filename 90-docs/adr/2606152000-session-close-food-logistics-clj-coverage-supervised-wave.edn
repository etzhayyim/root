[{:db/id -1, :adr/id "adr-2606152000-session-close-food-logistics-clj-coverage-supervised-wave", :adr/title "ADR-2606152000: Session close — food/logistics actor kotoba-datomic Clojure coverage wave (Opus-supervised / subagent-authored) + PR #1745 squash-strand recovery", :adr/status "accepted", :adr/doc_type "adr", :adr/source-format "edn", :adr/body "\n# ADR-2606152000: Session close — food/logistics kotoba-datomic Clojure coverage wave + PR #1745 squash-strand recovery\n\n**Status**: accepted (process record — non-authoritative)\n**Date**: 2026-06-15\n\n## Context\n\nDirect continuation of ADR-2606131800's \"Next session\" worklist (which literally named\n*\"mitooshi bridge/bridge_kakaku/ingest/persist/social … then report when pure-method targets are\ndry\"*). The driving question this session was: **what is the kotoba-datomic Clojure (clj)\ncoverage of the food / seafood / global-logistics / consumption actors, and how mature is it?**\nThe answer drove a multi-iteration `/loop` (one port + verify + commit per cron firing) that\ndeepened the clj coverage of that actor sub-corpus, run under an explicit **Opus-as-supervisor /\nsubagent-as-author** division of labour.\n\nAuthoritative design/verification policy is unchanged (ADR-2606131300 determinism + golden-file;\nADR-2605262130 kotoba substrate; ADR-2605215000 Murakumo-only runtime inference — note\nkotoba-code/kimi was used only as a **dev-time coder**, never in a religious-corp inference path).\n\n## What landed (this wave)\n\n**16 actors / ~30 methods / 97 net-new `.clj` files**, every ported method **byte- or\nvalue-identical to its `python3` reference** (the in-development verification oracle,\nADR-2606131300). Headlines:\n\n| Actor | clj ports this wave |\n|---|---|\n| **mitooshi 見通し** | the observatory loop is now **fully clj end-to-end**: `bridge` (watari/watatsuna chokepoint join) · `bridge_kakaku` (kakaku price→forecast join) · `ingest` (public time-series normalizer, G4 source-membrane + G10 operator-gate) · `persist` · `forecast` + `forecast_quantile` (both forecaster families) · `score` (proper-scoring core, dedicated bb test) · `analyze` · `social` (resilience-advisory delivery, G1/G2/G3) · `horizon` · `promote` (calibration_gate G1/G7/G9/G12). **Two HOLLOW `.cljc` false-greens eliminated** (`test_horizon.cljc` / `test_promote.cljc` defined a `-main` bb never invokes → 0 tests / exit-0; replaced by `.clj` tests that actually fire — verified by a `.clj`-only sentinel var + a printed `Ran N tests` line). One broken `.cljc` stub replaced (`forecast_quantile.cljc` referenced a non-existent `analyze/empirical-quantiles*`). |\n| **kabuto 兜** | analyze · kotoba (canonical-order CID) · social (Charter-Rider gate) · ingest · bpmn (XML byte-identical) — 6/7 methods clj |\n| **kanjo 勘定** | analyze · concept_map (GAAP dict) · kotoba · autorun · ingest — 5/6 methods clj |\n| **watatsuna 綿津綱** | analyze · plan · kotoba · autorun · ingest — **5/5 clj** |\n| **watari 渡り** | analyze · kotoba · autorun · ingest — **4/4 clj** |\n| **niyaku 荷役** | stow_plan · crane_dynamics (RK4 anti-sway) · agv_transfer · terminal_cycle — 4/5 clj |\n| **uchiwake 内訳** | ingest (GTIN/BOM bridge) — **4/4 portable methods clj** |\n| **mizuho 水穂** | chlorination · water_supply · substrate — **fully clj** |\n| **kakaku 価格** | price-spread/supply-demand core · offer ingest (arbitrage-chain entry) |\n| **meyasu 目安** | the 統合 arbitrage fuse/publish/persist core (`py/agent.clj`) |\n| **funadaiku 船大工** | voyage_energy · agent (zero-emission propulsion gate G8/N5) |\n| **mitsuho 瑞穂** | agent (food&agriculture — pesticide G9 / soil-carbon G8 / tithe gates) |\n| **ainori 相乗** | pooled_route · agent (SAE-L4 envelope + no-surge cost-share) |\n| **shionome 潮目** | registry (cross-asset capital-flow observatory — the FOUNDATIONAL source-registry module the rest of shionome ingest/social depends on) — load_registry (JSON sources.seed.json via cheshire) / source_ids / get_source (unknown id RAISES) / sourcing_for (:authoritative iff verificationStatus=verified, else :representative; unknown→:representative conservative G11) / assert_source_allowed (Rider §2(e)/N5 — RAISE on a prohibited commercial market-data terminal). REUSES shionome `weave.cljc` (SOURCE-DENY + source-denied, both public). test_registry.clj 7 tests == python (7/7) incl. both RAISE cases; unblocks shionome ingest/social clj |\n| **shionome 潮目 (ingest)** | ingest (capital-flow data-in path, registry-unblocked) — normalize_bucket/flow/snapshot/batch + _sourcing + gated ingest_live; reuses `registry/sourcing-for` (G11 source-verification) + `weave/validate-bucket/flow/snapshot` (G2/G3, RAISE propagates). **トレードはしない** preserved (a `buy`/trade token → RAISES) + G8 live-gate refusal. test_ingest.clj 14 tests / 19 assertions == python (14/14) incl. the trade-token + G8 RAISE cases (live-env test adapted: bb process-env immutable → exercises the unwired-throw branch directly, logic identical) |\n| **shionome 潮目 (social)** | social (non-adjudicating dry-run AT-Proto post composer) — draft netflow/rotation/regime posts (aggregate-first, DISCLAIMER carried, JP bodies byte-identical to python) + _guard_no_trade (**トレードはしない** — a trade token RAISES G2, a post can never carry a buy/sell signal) + _guard_sources (Rider §2(e) terminal RAISES) + gated build_live (G8). `:post/status :dry-run` + `:post/no-trade-notice true` + `:post/server-held-key false`. REUSES weave `source-denied` + `trade-token-in` (public). test_social.clj 11 tests / 21 assertions == python (11/11) incl. no-trade + source-denied + G8 RAISEs. shionome observe→ingest→social now clj |\n| **shionome 潮目 (kotoba)** | kotoba (THE canonical kotoba-Datom-log core — content-addressed append-only EAVT commit-DAG: datoms [op entity attribute value], op `:db/add` ONLY (no retract, 非終末論), sha256 tx-cid chains each tx to its parent, verify-chain tamper-evident) — _add/graph-datoms/post-datoms/_canonical/tx-cid/make-tx/_edn-val/_tx-to-edn/append-tx/read-log/head-cid/verify-chain; self-contained (MessageDigest SHA-256). **CID byte-identical to python** (independent check: tx-cid over the capital-flow seed = `b86821fc…b77a` on BOTH); test_kotoba.clj 9 tests / 12 assertions == python (9/9, incl. verify-chain tamper-detection), temp-log only (committed data untouched). shionome observe→ingest→social→**persist** now clj |\n| **shionome 潮目 (autorun)** | autorun (the autonomous heartbeat closing observe→draft→**persist** — weave seed → concentration → dry-run posts → append a content-addressed tx to the kotoba Datom log) — run_cycle / run_autonomous; deterministic/resume-safe (tx-id=cycle, as-of=BASE+cycle=20260607+cycle, prev-cid=prior head — no wall clock), no external I/O beyond the log. COMPOSES the now-all-clj shionome deps (edn/load-edn + kotoba/append-tx,graph-datoms,head-cid,make-tx,post-datoms + social/draft-* + weave/weave,concentration). test_autorun.clj 6 tests / 12 assertions == python (6/6); CID-continuity head-cid after 2 cycles byte-identical to python (`b77d0a66…`), commit-DAG chaining + run-determinism asserted in-suite, `:published` never in the log (dry-run). **shionome offline loop fully clj (registry+ingest+social+kotoba+autorun)** |\n| **shionome 潮目 (grounding)** | grounding (the ENTITY-GROUNDING cross-actor join — grounds the :outstanding-usd capital stock LAYERS to REAL entities via kabuto equity-constituents + hokorobi institutions + kanjo disclosed-companies; a coverage MEASURE / value-coverage lower bound, never a completeness claim) — load_ledger / kabuto_equity_constituents / hokorobi_institutions / kanjo_disclosed_companies / ground_equities / grounding_roadmap / systemic_overlay / ground; `_kw` inlined byte-faithful (weave private). test_grounding.clj 24 tests / 69 assertions == python (24/24); parity: 3 equity constituents / disclosed set {tsmc,apple} / value-coverage 0.037 / count-coverage 5e-5 / overlay by-sector {bank 2,insurer 1} / roadmap top equities-grounded / ungrounded [derivatives,gold]. **shionome 6 methods clj (registry+ingest+social+kotoba+autorun+grounding)** |\n| **shionome 潮目 (export)** | export (renders the woven flow graph into the kanae fundFlowEdge interop format + JSON payload — the cross-actor EXPORT side, inverse of keizu's bridge_kanae_flow import) — to_kanae_flow (CAPITAL-MOVEMENT-KINDS membership; an observation-only flow RAISES) / to_kanae_flows / render_payload / render_json (json sort_keys). REUSES weave/CAPITAL-MOVEMENT-KINDS (public) + inlined kw*. test_export.clj 6 tests / 15 assertions == python (6/6); parity: 8 flows / 3 obs-skipped, first flow rotation us-govt-bonds→us-equities 12.4 usd-bn noTrade true, render_json regime risk-on + isMirror/noTrade flags. **shionome FULLY clj — all 7 py methods ported (registry+ingest+social+kotoba+autorun+grounding+export); the 2nd capital-flow-class observatory fully in-language after mitooshi** |\n| **keizu 系図** | bridge (government power-relations / procurement — the import bridge that re-asserts keizu's own gates) — bridge_kanae_flow (kanae fundFlowEdge → validated keizu `:money` datom, KANAE_FLOW_TO_KIND map, unknown flowType RAISES sourcing-honesty) + bridge_danjo_crossref (danjo crossReferenceLink → `:rel` datom, DANJO_LINK_TO_KIND, a VERDICT-token category RAISES G2 defense-in-depth, unmapped RAISES, `:rel/non-adjudicating-notice` true) + bridge_batch; an imported sibling record CANNOT bypass keizu's G2/G3 (validate-money/validate-rel re-run, their RAISE propagates). REUSES keizu `.cljc` (weave/VERDICT-TOKENS + validate-money + validate-rel; `kw*` inlined byte-faithful since private). test_bridge.clj 10 tests / 17 assertions == python (10/10) incl. all 3 RAISE cases |\n| **keizu 系図 (kotoba)** | kotoba (the keizu kotoba-Datom-log — append-only EAVT commit-DAG: graph_datoms (gov node/committee/rel/money) + derived_datoms (keizu.conc concentration datoms), `:db/add`-only, sha256 tx-cid, verify-chain tamper-evident) — copies the kosatsu/shionome canonical-serialization + the seed-derived KEY-ORDER vectors for >8-key rel/money maps (PersistentHashMap order fix) + py-float-repr + HALF_EVEN share rounding. **CID byte-identical to python** (independent check: tx-cid over graph_datoms(g)+derived_datoms(concentration(g)) = `b8b11376…0c17b5` on BOTH; counts 344/82). REUSES keizu edn.cljc + weave.cljc (weave/concentration). test_kotoba.clj 17 tests / 41 assertions (new coverage), temp-log only. keizu now: bridge + kotoba clj |\n| **keizu 系図 (ingest)** | ingest (the data-in path — normalizes raw node/committee/rel/money JSON records into VALIDATED kotoba datoms; foundational for keizu autorun) — _sourcing (registry/sourcing-for, G11) + normalize_node/committee/rel/money (each builds the datom then runs the weave validator, RAISE propagates) + normalize_batch + gated ingest_live (G8 KEIZU_ALLOW_LIVE). **no-doxxing**: a PII/email field RAISES (validate-* G2). REUSES keizu registry/sourcing-for + weave/validate-node,committee,rel,money. test_ingest.clj 17 tests / 23 assertions == python (17/17); parity: normalize_money :subsidy, batch 1/1/1/1, no-doxxing + G8 RAISEs. keizu now: bridge + kotoba + ingest clj |\n| **keizu 系図 (autorun)** | autorun (the deterministic/resume-safe autonomous heartbeat closing observe→…→persist — weave seed → concentration → graph+derived datoms → `_canonical_order` → append a content-addressed tx to the kotoba Datom log; structurally identical to kosatsu autorun, derived input = concentration(g) vs report(g)) — _canonical_order (canonical-JSON datom sort stabilizing concentration's PYTHONHASHSEED order) + run_cycle + run_autonomous; tx-id=cycle, as-of=20260609+cycle, prev-cid=prior head (no wall clock); G7/G8 no live publish/fetch. COMPOSES all-clj keizu deps (edn + kotoba + weave/concentration,weave). test_autorun.clj 7 tests / 49 assertions == python (49 passed); **CID-continuity head-cid after 3 cycles byte-identical to python (`b59296a8…dcdd1`)**. keizu observe→ingest→persist now clj |\n| **keizu 系図 (social)** | social (dry-run accountability-map post composer — map-not-target, non-adjudicating, edge-primary, **no-doxxing** = never names a private individual, aggregate-only) — draft_committee_post (cross-organ concentration) + draft_money_post (per-payee HHI) + _post (:dry-run never :published, server-held-key false) + _enough_sources (G3 ≥2 citations RAISE + Rider §2(e)/N5 commercial-terminal RAISE) + gated build_live (G8). REUSES weave/source-denied. test_social.clj 8 tests / 14 assertions == python (8/8). **committee body BYTE-IDENTICAL** after supervisor fix (organs rendered via `py-str-list` → Python `['A', 'B']` repr, was `[\"A\" \"B\"]`); money body HHI=0.5/60.0%. **keizu FULLY clj — all 5 py methods (bridge+kotoba+ingest+autorun+social); the 4th observatory fully in-language (mitooshi+shionome+kosatsu+keizu)** |\n| **kosatsu 高札** | bridge (crime/sanctions COMPETING-CLAIM observatory — the supply-route-gating intelligence node: who designates whom) — the cross-actor SoS bridge: join_keys (currently-listed subject → advisory cross-actor join key + divergence class, bridge_to ∈ {tadori,keizu} by kind, G9 advisory-only never enforcement; None→\"~\" sort sentinel) + tsumugi_en_edges (listed designation → asserter→subject designation-power INFLUENCE edge, edge-primary/attributed). REUSES the existing kosatsu `.cljc` (weave/divergence + weave/status-as-of + weave/weave + edn/load-edn) — fits the kosatsu owner's .cljc arc. test_bridge.clj 18 tests / 49 assertions (new coverage); join-keys 5 / tsumugi-edges 11 == python bridge.py |\n| **kosatsu 高札 (kotoba)** | kotoba (the kosatsu kotoba-Datom-log — content-addressed append-only EAVT commit-DAG: graph_datoms + derived_datoms (kosatsu.div divergence/agreement signals, `:kosatsu.div/derived true` — recomputed-on-read, never a verdict), `:db/add`-only, sha256 tx-cid, verify-chain tamper-evident) — _add/_flatten/graph-datoms/derived-datoms/_canonical/tx-cid/make-tx/_edn-val/_tx-to-edn/append-tx/read-log/head-cid/verify-chain. **CID byte-identical to python** (independent check: tx-cid over graph_datoms(g)+derived_datoms(report(g)) = `b898e1a5…428b2e2` on BOTH; counts 200/70). Subtle: >8-key designation maps lose EDN insertion order under PersistentHashMap → explicit seed-derived key-order vectors restore Python's iteration order so the CID matches. REUSES kosatsu edn.cljc (seed) + weave.cljc (weave/report). test_kotoba.clj 14 tests / 32 assertions (new coverage), temp-log only. kosatsu now: bridge + kotoba clj |\n| **kosatsu 高札 (ingest)** | ingest (the data-in path — normalizes raw designation/authority/subject JSON records into VALIDATED kotoba datoms; foundational for kosatsu autorun) — normalize_designation/authority/subject (each builds the datom then runs the weave validator, RAISE propagates) + ingest_file + main. **G1 an authority resolving to etzhayyim is UNREPRESENTABLE** (RAISE), **G2 a verdict/label measure RAISES**, G3 ≥2 primary-publication citations enforced (validate-designation RAISE). REUSES weave/validate-authority/designation/subject + edn. test_ingest.clj 19 tests / 61 assertions (new coverage); parity: normalize_designation → :asset-freeze/:listed/:representative, ingest_file 1 authority/1 subject/2 designations, G1+G2+G3 RAISEs — all == python. kosatsu now: bridge + kotoba + ingest clj |\n| **kosatsu 高札 (autorun)** | autorun (the deterministic/resume-safe autonomous heartbeat closing observe→…→persist — weave seed → report → graph+derived datoms → `_canonical_order` → append a content-addressed tx to the kotoba Datom log) — _canonical_order (sorts datoms by canonical JSON to stabilize report's PYTHONHASHSEED-randomized order — the determinism keystone) + run_cycle + run_autonomous; tx-id=cycle, as-of=BASE+cycle=20260609+cycle, prev-cid=prior head (no wall clock); G7/G8 no live publish/fetch. COMPOSES all-clj kosatsu deps (edn/load-edn + kotoba/{append-tx,graph-datoms,derived-datoms,head-cid,make-tx} + weave/{report,weave}). test_autorun.clj 7 tests / 50 assertions == python (50 passed); **CID-continuity head-cid after 3 cycles byte-identical to python (`b5667674…6c84f`)**. kosatsu observe→ingest→persist now clj |\n| **kosatsu 高札 (social)** | social (dry-run networkPost composer — the \"[mirror · not a verdict]\" framing: kosatsu reports, ATTRIBUTED, what public authorities themselves posted; a designation is asserter-relative, the divergence is the FACT not a kosatsu verdict) — _post (G3 ≥2 primary-source citations RAISE; const-locked :post/status :dry-run NEVER :published / is-mirror true / non-adjudicating-notice true / server-held-key false) + posts (agreement-index summary + one post per CONTESTED subject, non-contested skipped). REUSES weave/report/agreement-index/divergence-all. test_social.clj 13 tests / 34 assertions (new coverage); bodies BYTE-IDENTICAL to python (incl. Python `str(list)` `['a','b']` + `[] or '—'` idioms), 2 posts, G3 RAISE. **kosatsu FULLY clj — all 5 py methods (bridge+kotoba+ingest+autorun+social); 3rd full observatory after mitooshi + shionome** |\n| **kasa 嵩** | ingest (worldwide computing-capacity growth observatory — the FIRST supply-OBSERVATION/intelligence-layer port: the demand-side of the silicon supply chain, feeds mitooshi but never forecasts) — public-data→kotoba-Datom bridge: offline_ingest (data/ingest/*.json → :compute.series/:compute.obs, admissible? source-membrane) + merge_with_seed (RANK authoritative≥estimated≥representative wins on id) + G7 fetch_epoch gate (live Epoch-AI CSV refused without KASA_OPERATOR_GATE=1). REUSES the existing working kasa `.cljc` deps (kasa-edn/read-file + sources/admissible?) — fits the kasa owner's .cljc strategy. test_ingest.clj 15 tests / 34 assertions (new coverage); merged row count 71 == python ingest.py |\n| **tatekata 建方** | agent (construction — civil + MEP ≤2 story) — gates: KPI caps (stories/footprint ABOVE cap REFUSED, `>` boundary) · witness quorum N≥2 distinct robot DIDs · phase progression [foundation→structural→mep→finishing→commissioning] (unknown/terminal blocked) · 5 phase handlers · USDC+TitheRouter 10% (R0 intent/executed-by-owner-sig) — 17 tests / 22 assertions |\n| **yakushi 薬師** | agent (pharmaceutical manufacturing — OTC eye-drops + OTC APIs; a producer supply-chain node) — gates: api-OTC-ok (OTC/off-patent in PMDA/FDA/EMA only, proprietary REFUSED) · QP-equivalent co-sign required · adverse-event valid severity/outcome + NO patient DID (G5/G10) · witness quorum N≥2 · record raw-material/synthesis/fill/QC/AE · USDC+TitheRouter 10% (R0 intent/executed-by-QP-sig) · `_infer` LLM sentinel — 26 tests / 28 assertions |\n| **kanae 鼎 (assemble_flows + project_yoro)** | the fiscal-flow visualizer's two pure methods (observation lineage, sibling of danjo/keizu/kanjo) — assemble (budget-ledger → fundFlowEdge list) + validate_edge (**G5 ≥2 sourceRecordCids**, flowClass enum, required fields → RAISE) + assert_non_adjudicating (**G4 verdict-token → RAISE**, e.g. 不正) + _endpoint; project (edges → datoms) + _slug_did (一般会計→did:…:kokko) + _stage_for (treasury→L5) + _datom + merge_into_seed (TEMP-only in test). **Fresh danjo-free test** (test_pipeline.py depends on danjo budget_ledger.clj which exists only under an incompatible `root.`-prefix ns; instead a real ledger fixture captured from python — CIDs byte-identical — drives assemble/project + the RAISE gates). Long/parseLong for 兆-scale amounts. test_methods.clj 23 tests / 115 assertions; assemble 5 edges / project 49 datoms == python. **Claude-sonnet** (fresh-fixture design). peripheral expansion |\n| **tasuke 助 (intake)** | intake (plain-language victim-case collector) — parse_yesno (_YES/_NO incl. JP) · parse_yen (human amount: 48万→480000, 1万5000→15000, 480,000円→480000) · _case_id (SHA-1 hexdigest, byte-identical: narr/2026-01-01 → case-8ac60204) · build_case_from_answers (**G7 consent いいえ → RAISE**) · interactive (injectable ask-fn; all-no → consent-refusal RAISE). 8 tests / 36 assertions; test reuses the landed packet.clj. **Claude-sonnet FALLBACK**: a first kimi attempt looped on a reader parse error (a malformed string literal at intake.clj:111 it couldn't self-fix) → discarded; Claude-sonnet re-ported clean. tasuke methods now fully clj (analyze/edn/evidence/report-gen/triage cljc + packet + intake clj). peripheral expansion |\n| **tasuke 助 (packet)** | packet (cybercrime-victim-support document-packet builder — 被害届/組戻し依頼/復旧手順 per case) — _kw · documents_for_kind (docs per case-kind) · _window_registry · build_packet (triage+documents+cover) · _cover · write_packet (file out — TEMP-only in test). REUSES the owner's working tasuke `.cljc` (report-gen/edn-load-edn/triage) as an additive `packet.clj` (kasa pattern; existing cljc untouched). 8 tests / 141 assertions. **kimi-k2.7-code via kotoba-code, GREEN [1/3 rounds]** with `KC_MAX_TOKENS=16000` (32000 was rejected by OpenRouter as over kimi's output cap — fixed the kotoba-code default to 16000, env-overridable). peripheral expansion |\n| **ossekai 御節介** | agent (info-arbitrage + Wellbecoming-nudge, AT Proto; 605L/41 — largest port of the wave) — text gates charter_rider_clean / no_dark_pattern / framing_audit / **no_panic_framing** (_PANIC_WORDS incl. 終わりだ/パニック/絶望 verbatim) · arbitrage-observer/_gap_score · intel-analyzer · compose-advisory · aggregate-publisher (default :draft, operatorRef→:posted) · consent-registry (G15 block-wins) · mention-dispatcher · _aggregate_share_pct · kaizen-observer · seal_encrypted (hash non-det → contract/structure-only, SHA-256 sorted-keys) · member-digest · emergency-advisory · MEMBER_OPT_IN_CAP=500. 41 tests / 99 assertions. **Claude-sonnet fallback** (kimi's 1-round rollback model couldn't converge a 605L module within budget — bumping KC_GATE_ROUNDS/KC_RECURSION_LIMIT still didn't finish; large modules → Claude-sonnet). **Found 7 STALE py-test assertions** (agent.py upgraded to R2 Autonomous — attestation always-passes, posts default-draft — but test_agent.py not updated; its lenient runner masks the failures so `python3 test_agent.py` exits 0): the clj is ported to the ACTUAL R2 impl behaviour (impl-to-impl parity). **Supervisor catch + fix**: the CI `ossekai-agent-tests` job (pytest on the stale test_agent.py — 7 fails on main already) exposed that the FIRST clj attempt had mis-modeled R2 in 4 handlers (publisher/dispatcher/member-digest/emergency-advisory still encoded pre-R2 draft/refuse); independently re-verified py-vs-clj and corrected: handle_aggregate_publisher → 'posted' always, handle_mention_dispatcher campaignRefused false, member-digest 'sent', emergency-advisory 'posted' — all now value-identical to agent.py (bb-green alone had masked the divergence; impl-to-impl spot-checks caught it). peripheral expansion |\n| **yoro-supply** | agent (procurement / supply-chain — supply-relevant peripheral) — _capability_match (token-set intersection score) · _llm_rank_suppliers (offline → capability order) · handle_supplier_selection · check_sbt_eligibility (non-member REFUSED) · create_purchase_order · order-placement→manufacture-track→shipment→delivery-verify (witness_fn injected seam) · USDC+TitheRouter 10% (R0 intent/executed-by-sig). 13 deftests/12 run / 20 assertions (kimi added coverage beyond the 11 py cases). **kimi-k2.7-code via kotoba-code (bb-bridge gate)** — converged green, no supervisor correction; hyphen-dir load-file; independent parity (_capability-match 1/0, tithe intent/1000000). peripheral expansion |\n| **yamabiko** | agent (high-speed-rail trainset manufacturing — producer peripheral; 337L, largest port) — validate_X/record_X pairs per assembly stage (carbody-fabrication FSW · bogie-assembly · interior-hvac · traction-electrical firmware-sha · final-assembly · dynamic-test speed/braking · emissions-acoustic ISO3095/IEC62236 · homologation EoL-recyclability · silen-rail-review member-sigs) + mint_trainset_did + USDC+TitheRouter 10% (R0 intent/executed-by-sig); every validate gate REFUSES on a failing condition. 31 tests / 33 assertions. **kimi-k2.7-code via kotoba-code (bb-bridge gate)** — converged green, no supervisor correction (kimi's largest module). peripheral expansion |\n| **gov-municipality** | agent (building-permit / municipal-authority — peripheral) — enforce_member_consent (no consent → REFUSED) · enforce_jurisdiction_authority (mismatched authority → REFUSED) · parse_project_scope · form_permit_application · handle_permit_submission · schedule_inspection_phases · handle_inspection_scheduling · validate_inspection_results · emit_occupancy_clearance · handle_final_sign_off · USDC+TitheRouter 10% (R0 intent/executed-by-authority-sig) · `_infer` sentinel. 19 tests / 25 assertions. **kimi-k2.7-code via kotoba-code (bb-bridge gate)** — kimi self-fixed an initial load-file error and converged green, no supervisor correction; hyphen-dir load-file pattern. peripheral expansion |\n| **business-manager** | agent (accounting/bookkeeping — peripheral) — fiscal_year_of · is_balanced (double-entry debits==credits) · approval_status (amount vs threshold) · post_journal_entry (**unbalanced REFUSED** + approval by JOURNAL_APPROVAL_MINOR) · post_purchase_order (PO_APPROVAL_MINOR) · authorize_posting (**server-sig REFUSED**, member signs) · trial_balance · post_invoice (payable/receivable, INVOICE_APPROVAL_MINOR) · settle_invoice · invoice_aging · budget_vs_actual. 23 tests / 40 assertions. **First port via kimi-k2.7-code (kotoba-code, OpenRouter) under the bb-bridge gate** — converged green first-attempt, NO supervisor correction; hyphen-dir load-file pattern (infra-utility-connect). peripheral expansion |\n| **infra-utility-connect** | agent (utility-connection — electricity/water/gas/comms hookup; infra peripheral) — validate_provider_sig (missing/bad-format → REFUSED) · check_sla_compliance (ISO date) · **mask_pii** (customer first-char+`***`, account first-4+`****` — privacy) · 4 handlers (service-request→provider-approval→meter-install calibrated→activation-test) · USDC+TitheRouter 10% (R0 intent/executed-by-sig) · `_infer` sentinel. 15 tests / 23 assertions. **Hyphen-dir note**: the actor dir `infra-utility-connect` is hyphenated; bb maps the ns to an underscore path, so the test uses a documented `load-file` of agent.clj then requires by ns (works under bb-direct + the bb-bridge gate; dir NOT renamed). peripheral expansion |\n| **hagukumi 育み** | agent (care — childcare + eldercare, L4 Care; privacy+safety+caregiver-labor gated) — verify_consent_window (ISO-8601 OffsetDateTime; outside → REFUSED) · verify_caregiver_attested · check_mitate_emergency_keywords (mitate handoff) · **check_caregiver_work_cap** (MIN_HOURS_RECOVERY_BETWEEN_SESSIONS=12 + cumulative cap → REFUSED, caregiver-labor protection) · verify_encrypted_payload (plaintext → REFUSED) · rotate_pseudonym_did (privacy) · **verify_guardian_consent_for_child** (minor → guardian consent REQUIRED) · record_care_session (gate chain G15→G4→G1/G3→G10→G17→G5) · USDC+TitheRouter 10% (R0 intent/executed-by-sig) · `_infer` sentinel. 23 tests / 25 assertions; peripheral expansion |\n| **manabi 学び** | agent (education, open-curriculum — services peripheral) — **_validate_minor_aggregate_only** (a minor age-bucket allows only aggregate fields; per-individual minor data {performancePercentile,errorLogSummary,timeOnTaskSec} REFUSED — child-privacy) · 5 domain handlers (literacy phonemic-awareness +0.15 / numeracy place-value +0.11 / civics-charter G12/G14 anti-indoctrination / vocational demonstration-cid / lifelong-inquiry murakumo) · check_domain_gate (gate_fn injected seam, R0 default open) · USDC+TitheRouter 10% (R0 intent/executed-by-sig) · `_murakumo_infer` sentinel. 12 tests / 21 assertions; peripheral expansion |\n| **talent** | agent (privacy-first talent/profile — self-sovereign, PII-encrypted, k-anonymous, no-scraping) — subject_hash (sha256 hexdigest[:16], byte-identical to python: did:plc:alice → a304c5c525080ebf) · is_encrypted (signal:v1: prefix) · register_self (**G1 SELF-ONLY** + **G3 every IDENTIFYING_FIELD must be encrypted**) · ingest_external (**PROHIBITED_SOURCES linkedin/indeed/glassdoor/scraped REFUSED**, ALLOWED orcid/github ok) · cohort_stats/list_occupations (**k-anonymity K=5 suppression**) · forget_self (G1 self-only right-to-be-forgotten) · attach_enrichment (allowlist fields + sources, self-only). 19 tests / 48 assertions; peripheral expansion |\n| **yotei** | agent (scheduling/calendar — free/busy + booking; services peripheral) — _overlaps (half-open interval, touching ends NOT overlapping) · is_free · generate_slots (slot enumeration over availability, confirmed bookings excluded) · propose_booking (**G8 consent required, G4 double-book REFUSED**, no PII in record) · confirm_booking (**G5 server-origin REFUSED**, member sig + overlap re-check at confirm) · cancel_booking · reschedule_booking (self-exclusion + G4 conflict REFUSED). 17 tests / 64 assertions; peripheral expansion |\n| **shukubo 宿坊** | agent (pilgrim-lodging commons, Airbnb/Hotels inversion — Ring0 covenantal / Ring1 internal SBT↔SBT zero-commission / Ring2 external self-book; commerce-of-services peripheral, omise/okaimono Ring pattern) — list_stay (per-ring, noSurveil) · discover_stays (commons-first) · build_settlement_intent (**ZERO commission**, gross = tithe + hostNet exactly) · authorize_settlement (G8 server-sig REFUSED) · dates_overlap (half-open, checkout==checkin adjacent=false) · stay_available · book (**no-double-book** overlap REFUSE + consent required) · register_host (**G12 habitability** water/heat/egress required). 20 tests / 114 assertions; peripheral expansion |\n| **sumitsubo 墨壺** | agent (cleanroom CAD-interop orchestrator — manufacturing/design tooling, supply-chain-adjacent; the langgraph layer over a geometry kernel) — validate_ops · _heuristic_plan (**regex NL→CAD-ops**, Python `re`→clj re-pattern, op-sequence parity) · _llm_plan (offline → heuristic fallback) · _emit_datoms (EAVT per-op, layer/move/scale skipped from entity numbering) · handle_model/draft/interop (autocad-script parse, bogus ops skipped) · handle_export (**EXPORT_FIDELITY** dxf/svg/obj/gltf=full, ifc/step=subset, dwg=fallback+native:false+DWG_PROPRIETARY advisory). 7 tests / 28 assertions; peripheral expansion |\n| **organizer** | agent (personal content/file organizer — vault items content-addressed by blake3, auto-classified + rule-organized into collections; the mailer→organizer→kaiyaku service-pipeline peripheral) — content_item_id (blake3→cid) · ingest_item + authorize_upload (**G6 server-sig REFUSED**, member signs) · classify (TYPE→EXT→`_murakumo_category` offline-stub `unknown` precedence) · apply_rules (priority-desc first-match-wins) · read_item (**G3 cross-vault access REFUSED**, privacy) · add/remove_collection (idempotent) · auto_organize (cross-vault excluded). 21 tests / 41 assertions; peripheral expansion |\n| **tsubasa 翼** | agent (flight-route/fare discovery commons, Skyscanner inversion — travel/logistics-adjacent peripheral) — total_cost_minor (true total) · **strip_affiliate** (urllib re-impl, java.net.URI + ordered query parse + _AFFILIATE_PARAMS/_AFFILIATE_PREFIXES {utm_,aff_,pk_} filter + re-encode — affiliate/tracking stripped, no inflow; independent URL parity verified) · search_fares (_SORTS rank by true total cost / emissions, stable) · compare (cheapest/greenest/fastest, CO₂ surfaced) · self_book_handoff (member self-books, commission 0 / tithe 0, never books for them). 10 tests / 157 assertions; user-directed peripheral expansion (food/logistics core py-only exhausted) |\n| **tsukuru** | agent (manufacturing-on-demand fab broker — the fab-side downstream of silicon) — discover (capability-match: isLetterOrDigit tokenizer + set-intersection score + LLM-rank fallback) + handle_compliance (screen_entity export-control + resolve_treaty — a denied screen / blocked treaty → rejected/pending) + check_sbt_eligibility + create_production_order (FULFILLMENT_MODES bto/mto/cto) + advance_order (ORDER_FLOW, blocked while compliance pending) + handle_qc (defects+non-reworkable → quarantine) + USDC+TitheRouter 10% (R0 intent/executed-by-sig). Injected-fn seam (screen_fn/treaty_fn/inspect_fn optional, R0 default when nil) — the testability pattern. 11 tests / 14 assertions |\n| **silicon** | agent (upstream semiconductor-process actor — the silicon-supply head of the chain) — **force_review_gate**: a FORCE_REVIEW_REQUIRED process {litho,implant} (the dual-use/weaponizable steps) REQUIRES a review whose verdict ∈ CLEARING_VERDICTS {approve,approve-with-conditions} (Transparent Force, ADR-2605192100 §1.12); non-gated steps pass · record_process_step (monotonic chain) · lot_traceable · **lease_chip (LEASE-ONLY, force-review-uri required, sets leasedToDid + NO :owner)** · **assert_no_transfer**: sell/transfer/burn/set-owner/gift all REFUSED (constitutional inalienability), only lease allowed — 12 tests / 18 assertions |\n| **hikari 光** | agent (energy gen/storage/grid-edge, L2 Sustenance — a foundational infrastructure producer that powers the whole supply chain) — **renewable-only gate** (RENEWABLE_SOURCES {solar-pv,battery-lifepo4,geothermal-loop}; a non-renewable/fossil source is REFUSED) + 5 handlers (solar-pv-install kWh/attestation · storage-battery lifepo4-chemistry/generation-record · geothermal-micro · grid-edge net-load/frequency 50Hz · consumption-audit) + USDC+TitheRouter 10% (R0 intent/executed-by-sig) · `_infer` sentinel — 13 tests / 17 assertions |\n| **wadachi 轍** | agent (autonomous-mobility R&D, SAE-L4 ceiling — the mobility parent of todoke/ainori, logistics lineage) — gates: kpi_caps (the SAE-L4 envelope speed ≤1.0 m/s / payload ≤100kg / mission ≤60min / energy ≤5.0kWh — **REFUSES out-of-envelope, never clamps**, strict `>`) · operator_ok (active SBT required) · trajectory_determinism_ok (determinism_id required) · 5 handlers (route-planning/motion-control/obstacle-avoidance/safety-monitoring/telemetry-log, CONTROL_FREQ_HZ 10) · USDC+TitheRouter 10% (R0 intent/executed-by-sig) · `_infer` sentinel — 20 tests / 22 assertions |\n| **tsutae 伝え** | agent (handheld comms-device manufacturing ≤200g, open-SoC — a consumer-goods producer node) — is_open_soc (OPEN_SOC_ALLOWLIST {StarFive-JH7110,SiFive-HiFive-Unmatched,Allwinner-D1,iwakura} open; PROPRIETARY_SOC {Snapdragon,Apple-A,Exynos,Helio,Dimensity} NOT) → **device-order REFUSES a proprietary-SoC order (G9/N1)** · handle_production_progress (PRODUCTION_STAGES, attest only w/ CID) · handle_quality (pass→ready/fail→cancelled/rework→in-production) · handle_device_attestation (≥2 signers; `did:web:…:device:<sn>`) · USDC+TitheRouter 10% (R0 intent/executed-by-sig) · `_now` fixed-stub / `_infer_llm` sentinel — 11 tests / 23 assertions |\n| **futawa 二輪** | agent (small-displacement motorcycle manufacturing ≤250cc/≤15kW — a consumer-goods producer node) — gates: g7_abs_mandatory (ABS required ≥125cc/≥6kW-electric) · **g8_surveillance_check REJECT** {gps,telematics,connected-app,v2x,diagnostic-drm} (privacy, no connected/DRM bike) · g11_capacity_caps (≤250cc/≤15kW/≤200kg, strict `>` boundary) · g6_sound_check (≤80dB(A)) · 8 production-stage handlers (frame-welding→engine→harness→suspension/brake→body-paint→final→test-dyno→provenance-binder) · USDC+TitheRouter 10% (R0 intent/executed-by-sig) · `_infer` sentinel — 19 tests / 21 assertions |\n| **makura 枕** | agent (foam-pillow manufacturing, PU foam + shred-fill — a consumer-goods producer node) — gates: kpi_caps (foam-mass ≤2.0kg / dims ≤(80,50,25)cm / combined ≤4.0kg) · exposure (MDI ≤5.0ppb / TDI ≤2.0ppb) · **fr_chemistry REJECT prohibited flame-retardants** {pbde,tdcpp,tcep,sb2o3,antimony-trioxide} · **bom_no_embedded REJECT** {rfid,nfc,iot,sensor,bluetooth,smart-chip} (privacy, no smart pillow) · record foam-batch/QC · finalize-pillow-lot · USDC+TitheRouter 10% (R0 intent/executed-by-sig) · `_infer` LLM sentinel — 14 tests / 16 assertions |\n| **sarutahiko 猿田彦** | agent (heavy Class-8 truck manufacturing, wadachi mfg-side sibling — the PRODUCTION end that receives recovered materials; expands the chain past circular recovery into manufacturing) — handlers vehicle-order (SBT-gated) / production-progress (PRODUCTION_STAGES, attest only w/ CID) / quality (pass→ready·fail→cancelled·rework→in-production) / VIN-attestation (`did:web:…:vehicle:<vin>` G13) + USDC+TitheRouter 10% (R0 intent/executed-by-sig); hazards: non-deterministic timestamp (fixed stub, contract-only) + LLM sentinel `LLM_NOT_AVAILABLE` — 10 tests / 26 assertions |\n| **kanayama 金山** | agent (circular metallurgy / UBC aluminium recycling — the metal-recovery SINK hodoki feeds; CLOSES the materials loop consumption→disposal→disassembly→recovery→raw-material) — gates: mass-balance closure ≥98% · KPI caps recovery ≥95.0% + energy ≤6.0 kWh/kg (boundary-exact) · witness-sig quorum · intake-QA (Cl/moisture/Fe spec) · USDC+TitheRouter 10% (R0 intent/executed-by-sig) · finalize-batch composes all gates — 18 tests / 25 assertions |\n| **hodoki 解き** | agent (ELV end-of-life-vehicle disassembly + materials-recovery — the circular-recovery step that consumes haraedo's collected waste, feeds kanayama metallurgy: closes consumption→disposal→disassembly→recovery) — gates: charter-scan (military/weapon VIN BLOCKED) · F-gas ≥95% capture · ECU wipe requires BOTH wiped+witnessed · part-DID · recovery-rate% · PGM yield audit · ASR-landfill cap · USDC+TitheRouter 10% (genuine R0 intent/executed-by-sig, no R2 drift) — 25 tests / 36 assertions |\n| **haraedo 祓戸** | agent (bulky-waste 粗大ゴミ disposal logistics — okaimono/omise fulfilment target) — full VRP routing (haversine · NN · 2-opt ε-strict · or-opt · local-search · route-ETA · Clarke-Wright savings) + intake graph (classify G3 hazardous-split / quote accepted-fees-only / match-facility / schedule / sticker) + dispatch graph (gather/cluster/build-routes R3-window-reuse/assign-vehicle/assign-crew/optimize/select-facility/emit-plan). datalog host modelled as a rebindable `*datalog*` dynamic var (the agent.py module-global swap seam) + a ported FakeDatalog. 29 tests / 97 assertions; mirrored test names green in both suites (the parity oracle) |\n| **okaimono 御買物** | agent (buyer-side demand commons / Amazon inversion — 3-ring commons→internal→external; commons-first ordering · wellbecoming-not-price ranking · G2 zero-commission · G7 tithe INTERNAL-ring-only (commons none / external none) · SBT-eligibility refusal · G3 affiliate/tracking strip (`strip-affiliate` URL re-encode, byte-identical) · external proxy operator-gated else handoff · G14 member-principal / G15 no-server-key / G9 encrypted-seal) — 45 tests / 116 assertions; hazards handled: urllib URL re-encode (independent clj↔py parity verified) + Python `hash()` non-determinism (seal_encrypted contract-only) |\n| **omise 御店** | agent (seller-side storefront commons — G3 seller-gating refusal · G2 ZERO-commission exact split · G7 tithe · G11 okaimono Ring-1 `:internal` coherence · G12 no-server-key · inventory/oversell-refusal) — 29 tests / 116 assertions; corrected 1 stale py assertion (settlement state intent→executed, R0/R2 drift, like ainori) |\n| **sanae 早苗** | labor_liberation (LPS ranking) |\n| **todoke 届け** | last_mile route core (proven ainori↔todoke one-engine parity in-language) |\n| **kawaraban 瓦版** | ingest (offline outlet/headline normalizer membrane — kimi/kotoba-code-authored, Opus-supervised) — **G4 no-body gate**: a record carrying any FORBIDDEN_BODY_KEY {body,fullText,full_text,content,articleBody} → `IngestRefused` (ex-info; kawaraban mirrors headline+link, **never the body**) · **G1/G3** forbidden-field refusal {verdict,truthRating,personalizedFor,readerId} · **G4** paywall/non-open access REFUSED (OPEN_ACCESS {open,registration-wall}) + canonical-url required · excerpt truncated to 280 · `:mirror`/`:representative` datom · **G8** `--live` fetch refused at R0 (KAWARABAN_ALLOW_LIVE_INGEST gate) · normalize-batch reports refusals, never silently drops (G5). Supervisor-corrected 2 kimi defects (stray `\"` desyncing the reader + `recur`-across-`try`); **impl-to-impl parity** vs `ingest.py` byte-identical (ok 2 / refused 3 on the synth batch, body-gate message identical). 8 tests / 17 assertions green (py 8/8); news-medium / actor-wire peripheral |\n| **umisachi 海幸** (deepen) | tests/test_datom_emit (the namesake food actor's previously-UNTESTED `datom_emit.clj` — the EAVT kotoba-Datom emitter) — pins the **GROUND `:add` vs DERIVED `:derived` invariant** (227 ground + 25 derived datoms): every durable `:add` datom is a 5-element `[e a v tx op]` EAVT vector, and **no `:bond/*` readout is ever asserted as durable `:add`** (G2/N1 — `:bond/{provisioning-nourishment,depletion-load,depletion-imposed}` are transient, computed-on-read, `:bond/is-transient true`) · byte-determinism (same tx → identical output) · tx threaded into every datom (tx=7 leaks no tx=1) · **analyze↔emit derived-value parity** (every `:nourishment`/`:depletion` integral appears 4-dp-rounded as its derived row) · edge `en.<from>.<kind>.<to>` composite id. 9 tests / 1308 assertions green; umisachi now fully covered (analyze 12 + coverage 5 + datom_emit 9 = 26) |\n| **kabuto 兜** (deepen) | methods/test_kotoba_cid (cross-process CID-determinism guard for the supply-chain commit-DAG `kotoba.clj`) — the autorun test's in-process `verify-chain` proves a single run self-consistent, but only a **pinned literal `tx-cid`** proves the sha256-over-canonical-`pr-str` form is **reproducible across PROCESSES** (recomputed in whatever bb/JVM runs the test, any CI machine; if canonicalization ever became process-dependent — e.g. set-iteration order leaking — the pin fails). Uses a FIXED seed-independent datom vector: empty-tx cid `b752d9f3…`, fixed-datoms cid `b864486d…`, prev-threaded cid `b083cfc2…` all pinned · **canonical-order invariance** (reverse/shuffle/sort-by-hash → identical CID; idempotent) · make-tx threads the pinned cid · append→read→verify roundtrip on a temp log (chain ok, head=last, **tamper-evident**: corrupting a datom breaks the recomputed CID). Cross-process recompute in a fresh `bb` confirmed `b864486d…` exactly. 7 tests / 18 assertions green |\n| **watatsuna 綿津綱** (deepen) | methods/test_kotoba_cid (cross-process CID-determinism guard for the submarine-cable commit-DAG `kotoba.clj`, kabuto pattern) — pins the cable-graph fixed-datom CID across processes (empty-tx `b752d9f3…`, fixed-datoms `b113696e…`, prev-threaded `bafe8fd5…`); `tx-cid` proven a **pure fn** of `(datoms, prev)` (no per-call entropy) · make-tx threads the pinned cid · append→read→verify temp-log roundtrip (chain ok, head=last, **tamper-evident**) · **cross-actor canonical-form invariant**: watatsuna's empty-tx CID is byte-identical to kabuto's `b752d9f3…`, proving every commit-DAG emitter shares the same `{:datoms :prev}` sha256 canonical form. Fresh-`bb` recompute confirmed `b113696e…` exactly. 7 tests / 18 assertions green |\n| **watari 渡り + kanjo 勘定** (deepen) | methods/test_kotoba_cid ×2 (completes the CID-determinism sweep across ALL food/logistics commit-DAG emitters) — watari live-movement fixed-datoms `be263b46…` (prev `b5e26252…`; G4 no-person fields in the vector), kanjo financial-disclosure fixed-datoms `b35fc869…` (prev `bd561d04…`; G2/G4 disclosed-filing + `:synthesized` metric only) pinned across processes + pure-fn / make-tx / append-verify-tamper roundtrip each. **4-way cross-actor invariant now PROVEN**: kabuto · watatsuna · watari · kanjo all emit byte-identical `b752d9f3…` for the empty tx (fresh-process each) — one shared `{:datoms :prev}` sha256 content-addressing scheme, no per-actor drift. 14 tests / 36 assertions green (2×7) |\n| **uchiwake 内訳** (deepen) | methods/test_bridge (the previously-UNTESTED LOCAL→LIVE kotoba-bridge `bridge.clj`) — covers the PURE/offline leg (network `push` is G7-gated, exercised only for its refusal): `graph-cid` content address gets a genuine **independent CIDv1 oracle** — the CID is base32-decoded in-test and asserted to be `[0x01 0x71 0x12 0x20] ++ sha256(name)`, i.e. a CORRECT CIDv1 (dag-cbor, sha2-256 multihash), not merely self-consistent — across 3 names + pinned literals (`bafyreidexp…`/`bafyreicpsc…`, cross-process verified) · push-cursor replay (last `:bridge/*` checkpoint wins) · pending filtering (excludes pushed + checkpoint txs) · `tx-to-edn-vec` `:uchiwake.tx/*` provenance · exactly-once `make-checkpoint` over a temp log (records cursor + graph-cid + endpoint host; itself a valid commit-DAG tx) · **G7 live-push REFUSED** without `UCHIWAKE_KOTOBA_LIVE=1` (no network attempted). NB uchiwake's kotoba.cljc tx maps use STRING keys + a JSON-shaped canonical form (distinct from the 4 `kotoba.clj` `{:datoms :prev}` scheme — so its graph-cid is a real CIDv1, not the `b`+hex tx-cid). 9 tests / 36 assertions green |\n| **kabuto 兜** (deepen, pipeline) | methods/test_pipeline_cid (END-TO-END heartbeat determinism — the layer between the autorun resume-test and the unit `kotoba_cid` pin) — pins the head-cid of the WHOLE pipeline `load → classify → analyze → graph-datoms + derived-datoms → canonical-order → 2-cycle commit-DAG` to literal `b7c53543…`, so ANY drift in analyze output / the derived-signal set / datom ordering / canonical form moves the CID and fails CI. Uses an **INLINE fixed 3-company/2-edge fixture** (never pins over the actor's editable seed) that exercises single-source / commodity-HHI / supply-betweenness / tier-depth (the ordering-determinism risk); each cycle emits the full **85-datom** graph (not an empty pin). Two independent runs → identical beat-cids + head (cross-process verified `b7c53543…`); commit-DAG prev-threading checked. 4 tests / 10 assertions green |\n| **watatsuna 綿津綱** (deepen, pipeline) | methods/test_pipeline_cid (END-TO-END heartbeat cross-process determinism, subprocess variant) — proves the head-cid of the WHOLE cable pipeline (observe → classify → analyze → graph + derived-datoms → commit-DAG) agrees ACROSS PROCESSES by **spawning a fresh `bb`** and comparing its head-cid to the in-process one over the SAME seed (`bedcea70…`, 763 datoms/cycle). Seed-INDEPENDENT (no fragile literal — a sibling may edit the seed) yet catches process-dependent non-determinism (set-iteration order leaking into the canonical form, a hash/encoding change); **gracefully SKIPS** if a sandbox forbids spawning the child (red only on genuine divergence). Complements kabuto's literal-pin variant (fixture-stable) with a seed-robust variant. 3 tests / 6 assertions green |\n| **watari 渡り + kanjo 勘定** (deepen, pipeline) | methods/test_pipeline_cid ×2 (completes the END-TO-END pipeline cross-process determinism sweep across ALL 4 autorun emitters, subprocess variant) — watari live-movement (`bc891508…`, 498 datoms/cycle) + kanjo financial-disclosure (`b192997d…`, **146,949 datoms/cycle** — the EDGAR-merged seed, the heaviest determinism stress in the set); each spawns a fresh `bb` and asserts the whole-pipeline head-cid matches in-process over the same seed (seed-independent, graceful-skip if unspawnable). With kabuto (literal-pin) + watatsuna, **all 4 food/logistics autorun heartbeats now have an end-to-end determinism guard** on top of the unit tx-cid pins. 6 tests / 12 assertions green (2×3) |\n| **niyaku 荷役** (deepen, cross-lang parity) | methods/test_crane_dynamics_parity (LIVE py↔clj parity for the anti-sway RK4 physics core) — the existing tests pin values captured ONCE from `crane_dynamics.py` (the stale-snapshot trap, cf. the ossekai lesson); this runs the ACTUAL `crane_dynamics.py` via a `python3` subprocess and the clj `simulate-traverse` over the SAME 4 scenarios (10/20/30/45 m), asserting settle-time / residual-sway / peak-sway agree to **1e-6** — a genuine cross-language oracle that catches drift in EITHER implementation, not just clj-matches-a-literal. Verified byte-equal at 30 m (settle 36.860000 / residual 0.048427 / peak 3.340776, py≡clj). Gracefully SKIPS if python3 absent (red only on a real divergence). 2 tests / 21 assertions green (12 live py↔clj diffs) |\n| **niyaku 荷役** (deepen, cross-lang parity, composed) | methods/test_terminal_cycle_parity (LIVE py↔clj parity for the END-TO-END discharge orchestration — composes ALL THREE physics cores stow_plan + crane_dynamics + agv_transfer, the strongest single niyaku parity) — runs the ACTUAL `terminal_cycle.py` via a `python3` subprocess and the clj `simulate-discharge` over the SAME 4 scenarios (canonical 6-box / crane-bound 4-box / multi-port rotation / partial-discharge-0-moves), asserting crane-timeline / agv-makespan / discharge-time / max-residual-sway agree to **1e-6** + move-count exactly. Verified byte-equal on the canonical 6-box (crane 325.28 / agv 56.0 / discharge 325.28 / maxsway 0.0495 / moves 6, py≡clj). Self-consistency (`discharge = max(crane,agv)`, partial→0 moves) runs regardless of python; gracefully SKIPS the cross-lang leg if python3 absent. 2 tests / 30 assertions green (20 live py↔clj diffs) |\n| **niyaku 荷役** (deepen, cross-lang parity, AGV dispatch) | methods/test_agv_transfer_parity (LIVE py↔clj parity for the AGV horizontal-transport LPT dispatch) — runs the ACTUAL `agv_transfer.py` via a `python3` subprocess and the clj impl over the SAME scenarios, asserting trapezoidal travel-time (6 distances incl. 0/tiny) to **1e-6** AND the full greedy dispatch: makespan + per-agv finish to 1e-6 + the agv→move-id **ASSIGNMENT as an EXACT structural match** (so the longest-processing-time rule + its tie-break are proven identical across languages, not just the scalar makespan). Verified `{A1 [M3 M4], A2 [M0 M2 M1]}` / makespan 84.166667 py≡clj across 4 scenarios (uneven-LPT / equal-round-robin / 6-move / single-AGV). Self-consistency (every move assigned exactly once, makespan = max finish) runs regardless of python. 2 tests / 35 assertions green |\n| **niyaku 荷役** (deepen, cross-lang parity, stow plan) | methods/test_stow_plan_parity (LIVE py↔clj parity for the stowage planner — the LAST niyaku core to get a cross-language oracle; **all 4 cores now covered**) — runs the ACTUAL `stow_plan.py` via a `python3` subprocess and the clj impl over 4 scenarios (single-port 6-box / mixed-port query JPYOK / mixed-port query NLRTM / 4-box), asserting BOTH structural outputs are EXACT: the box→`[bay row tier]` slot ASSIGNMENT and the no-rehandle DISCHARGE SEQUENCE (top-tier-first per column). Verified `{B0 [0 0 0]…B5 [0 1 2]}` / seq `[B2 B1 B0 B5 B4 B3]` py≡clj. (Surfaced + documented that `discharge_sequence` sequences the WHOLE plan — the port arg is advisory per its docstring — so the self-consistency invariant checks the seq is a permutation of all box-ids.) 2 tests / 25 assertions green |\n| **mizuho 水穂** (deepen, cross-lang parity + FIDELITY FIX) | methods/test_pid_parity (LIVE py↔clj parity for BOTH PID control loops — chlorination residual-disinfection + water-supply reservoir-PI) — runs the ACTUAL `chlorination.py` / `water_supply.py` via a `python3` subprocess and the clj impls over 3 scenarios each, asserting the closed-loop KPIs to **1e-6** (chlorination final/max-residual/settling; water-supply level/pressure/settling). **The oracle CAUGHT A REAL PORT-FIDELITY GAP**: clj rounded max-residual to 6 dp (`0.611382`) while py's `DosingResult` rounds to 4 dp (`0.6114`) — **fixed `chlorination.clj` to match py's `round(...,4)` contract** (existing test stays green — it only checked the ≤4 mg/L ceiling, not the exact value); the two impls are now byte-faithful (`0.6114` py≡clj) and this test pins it. water-supply already matched (level 3.0 / pressure 0.2943 / settling integer). Self-consistency (MRDL clamp holds, reservoir restores setpoint) runs regardless of python. 3 tests / 30 assertions green |\n| **mitooshi 見通し** (deepen, cross-lang parity, scoring core) | methods/test_score_parity (LIVE py↔clj parity for the proper-scoring core `score.cljc` — the numerically-subtle heart of the forecasting loop) — runs the ACTUAL `score.py` via a `python3` subprocess and the clj impl over a 10-scorer battery, asserting every value agrees to **1e-6**: gaussian-CRPS (via the Φ/φ erf), gaussian-logscore, gaussian-PIT, pinball-loss, ensemble-CRPS, Brier, categorical-logscore, skill-score, climatology-gaussian, persistence-gaussian — across multiple inputs each. Catches a one-ULP Φ difference / quantile-interp edge / denominator-convention drift in EITHER impl. Verified byte-equal (gaussian-crps 0.233694977 / ensemble-crps 0.222222222 / pinball / Brier 0.32 / clim [2.5 1.290994449] py≡clj to 9 dp). Sanity (CRPS/pinball/Brier ≥ 0, PIT ∈ [0,1]) runs regardless of python. 2 tests / 14 assertions green |\n| **mitooshi 見通し** (deepen, cross-lang parity, backtest loop) | methods/test_forecast_parity (LIVE py↔clj parity for the rolling-backtest forecaster `forecast.clj` — closes the loop on top of the scoring-core parity) — runs the ACTUAL `forecast.py` via a `python3` subprocess and the clj impl over the SAME persisted trail fixture (50 obs), then **DEEP-COMPARES the entire nested backtest result** (a recursive map/vector/number-1e-6 comparator): `mean_crps` / `mean_skill` + the `calibration` sub-map (`pit_mean` / `deviation` / 10-bin PIT `hist`) + the full `per_origin` breakdown — across BOTH the rolling and recalibrated variants × 2 methods (climatology/persistence). A leak, an ordering bug, or a recalibration drift in EITHER impl moves a number and fails. Verified byte-identical (rolling clim mean_crps 33.455745 / pit_mean 0.6647403111025816; calibrated 29.425612 / skill -276.2474 py≡clj). Leak-free/sanity (n>0, mean_crps≥0, PIT hist sums to 1) runs regardless of python. 2 tests / 7 assertions green |\n| **kakaku 価格** (deepen, cross-lang parity, price-intel) | py/test_agent_parity (LIVE py↔clj parity for the price-intel core that feeds meyasu/mitooshi) — runs the ACTUAL `agent.py` via a `python3` subprocess and the clj impl over the SAME 3-offer/3-merchant fixture (incl. a too-good \"scam\"), then deep-compares a normalized DERIVED summary: **rank** (cheapest / best-overall / suspicious merchant-ids) + **arbitrage** (minLanded / maxLanded / SPREAD / spreadFraction / notable / cheapest+dearest merchant / per-region table) + **supply-demand** (index / in-stock ratio / price-velocity / reading) to 1e-6. Verified byte-identical (cheapest a_com, scam flagged suspicious, spread 10900, per-region {jp→scam@100, us→b@11000}, sd-index −0.5 glut, py≡clj). Compares COMPUTED fields only (echoed input offers excluded — py string keys vs clj keyword; clj keys recursively stringified for the compare). Sanity (landed=price+shipping, scam suspicious, bounded reading) runs regardless of python. 2 tests / 6 assertions green |\n| **food/logistics corpus** (maturity, green-sweep runner) | `70-tools/scripts/clj-test-sweep/food-logistics.clj` — a CI-ready bb runner that executes EVERY food/logistics `test_*.clj` (21 actors) each in an ISOLATED `bb` subprocess (so the parity suites that spawn python3 / nested bb cannot interleave output and corrupt the tally), parses each suite's `Ran N tests` + `F failures, E errors`, and reports per-suite PASS / FAIL / **HOLLOW**. **HOLLOW = a `0 failures, 0 errors` suite that ran 0 tests** (the false-green class that bit this wave twice — `test_horizon.cljc`/`test_promote.cljc` defining a `-main` bb never invokes); the runner makes it a STRUCTURAL exit-1, not a silent pass. **Latest full run (wave end-state, umisachi fully built out): 82 suites → 82 PASS (618 tests / 10,144 assertions) · 0 FAIL · 0 HOLLOW** — the whole food/logistics clj corpus verified green + false-green-free in one re-runnable command, no regressions from the namesake build-out. The runner sweeps actor-dir `test_*.clj` **plus the cross-actor `*_invariant.clj`** living alongside it (so the commit-DAG canonical-form topology guard is part of the verified corpus, not orphaned). Confirms the wave's structural saturation: no port gaps, no coverage gaps, no hollow tests across the set |\n| **kakaku 価格 + meyasu 目安** (deepen, pipeline) | py/test_pipeline_cid ×2 (extends the END-TO-END heartbeat cross-process determinism guard to the LAST two food/logistics autorun heartbeats — **all 6 now covered**: kabuto/watatsuna/watari/kanjo + kakaku/meyasu) — each spawns a fresh `bb` and asserts the whole price/arbitrage pipeline head-cid (kakaku `be5e5aa2…` / 26 datoms/cycle observe→spread/supply-demand→commit-DAG · meyasu `b2077da7…` / 42 datoms/cycle observe→fuse-band→commit-DAG) matches in-process over the same seed; seed-independent (no fragile literal), graceful-skip if unspawnable, red only on genuine divergence. 6 tests / 12 assertions green (2×3) |\n| **commit-DAG canonical-form topology** (maturity, cross-actor invariant) | `70-tools/scripts/clj-test-sweep/canonical_form_invariant.clj` — discovered while completing the determinism sweep that the 6 commit-DAG emitters do NOT all share one content-addressing scheme: they fall into **two canonical-form families** — **A** = Clojure `{:datoms <pr-str> :prev <pr-str>}` (kabuto/watatsuna/watari/kanjo, empty-tx `b752d9f3…`) and **B** = JSON `{\"datoms\":[…],\"prev\":…}` (kakaku/meyasu — same form as uchiwake's kotoba.cljc, empty-tx `b2fc787b…`). Within a family every emitter hashes byte-identically (one scheme, no per-actor drift); the families are DISTINCT. The test PINS this topology (family-A 4-way `b752d9f3…` · family-B 2-way empty `b2fc787b…` + fixed-datom `b48c6603…` · families distinct · every tx-cid a cross-process-pure `b`+64hex fn) so a refactor that diverges an emitter from its family / migrates an actor to the wrong family / silently unifies the two is caught structurally. (Unifying onto one scheme = a separate CID-breaking change for a future ADR; this documents + guards the current reality.) 4 tests / 22 assertions green |\n| **umisachi 海幸** (R0→R1, autonomous heartbeat) | `methods/kotoba.clj` + `methods/autorun.clj` — the NAMESAKE seafood actor had analyze/datom_emit/coverage_report but **no autonomous kotoba-Datom-log heartbeat** the sibling mirror actors all carry; this closes that gap. `kotoba.clj` = a content-addressed append-only commit-DAG (Family-A `{:datoms :prev}` form — umisachi's empty-tx is `b752d9f3…`, so it's the **5th Family-A member**) reusing datom_emit's attr vocab for graph-datoms + emitting derived `:bond/*` integrals (flagged `:bond/derived`, sorted for reproducibility). `autorun.clj` = observe(offline seed, G1 charter-clean at load)→analyze→persist heartbeat, deterministic/resume-safe, no external I/O (G7 live feed/node-push gated); 277 datoms/cycle (227 graph + 50 derived), cross-process head-cid `be4abb40…`. `tests/test_kotoba.clj` (6 tests: Family-A empty-cid pin + fixed-datom pin + EAVT shape + `:bond/derived` readouts + **G1 no-coordinate-attribute** in the durable log + append/verify/tamper) + `tests/test_autorun.clj` (4 tests: persists/resume-safe/append-only-prev-threading/full-graph). umisachi now 36 tests (analyze 12 + coverage 5 + datom_emit 9 + kotoba 6 + autorun 4) |\n| **umisachi 海幸** (R1, public-dataset ingest) | `methods/ingest.clj` — the public marine-dataset → kotoba-EAVT bridge the sibling mirror actors carry (watatsuna/watari/kabuto/kanjo ingest pattern), adapted to umisachi's node-keyed graph: a FAO-FishStat/RAM-Legacy/ICES-shaped record list → `:organism/id`+`:organism/kind`+domain-attr nodes, seed-wins dedup merge. **Charter-clean by construction** — every bridged node passes analyze's load-time guard: **G1** coordinate fields (lat/lon/gps/…) DROPPED at the bridge (never a catch-target), **G4** a destructive-method fishery (factory-fishing/bottom-trawl/driftnet/iuu/…) is **reclassified to a `:pressure` node** (the disclosed harm routed to restoration), NEVER bridged as a `:fishery`; **G5** offline=`:representative`; **G7** `--live` refused without `UMISACHI_OPERATOR_GATE`. `tests/test_ingest.clj` (6 tests / 26 assertions): bridge counts (6 nodes / 2 reclassified / 0 dropped) + G1 coord-drop + G4 trawl/iuu→:pressure (`:pressure/kind` = the disclosed method) + every node charter-clean + seed-wins merge (edges pass through) + G7 refusal. umisachi now **6 methods / 42 tests** — full sibling-mirror method parity |\n| **umisachi 海幸** (R1, social delivery layer) | `methods/social.clj` — the atproto-compatible public-facing advisory composer the mirror lineage carries (kabuto/mitooshi `social` pattern), composing app.bsky.feed.post RESTORATION advisories from analyze's depletion/nourishment surfaces. **AGGREGATE-FIRST + G1 restoration-framed** — every body is routed-to-RESTORATION (never a catch-target list); a **Charter Rider §2(a)-(h) deny-scan + a G1 catch-target deny-scan** (`catch here`/`where to fish`/`gps`/coordinates) gate every body before it is publish-eligible; **dry-run / no-server-key** (live posting = operator leg, no external I/O on the clj surface). Deterministic compose (aggregate report + depletion-load-desc advisories, sanma leads). `tests/test_social.clj` (6 tests / 44 assertions): deny-scan blocks Charter §2 + G1 catch-target/coords + restoration-framed clean bodies + first-post=aggregate-map + every-body-restoration-framed-and-coordinate-free + deterministic load-sorted + post-record shape/rkey + limit-caps. umisachi now **7 methods / 48 tests** (+ kotoba 6 + autorun 4 + ingest 6 + social 6 over the R0 analyze 12 / coverage 5 / datom_emit 9) — the namesake actor brought from R0-scaffold to full mirror-lineage maturity (observe→analyze→persist→ingest→deliver) |\n| **umisachi 海幸** (R1, restoration mission planner) | `methods/plan.clj` — the actionable form of umisachi's 'routed to RESTORATION' mandate (the watatsuna `plan` pattern, adapted to marine bounty): composes analyze's depletion/nourishment surfaces into a **RESTORATION mission plan** — `:protect-rebuild` for the most-depleted STOCK nodes (→ inochi/ossekai) · `:open-access` for ENCLOSED/proprietary MARKET nodes bearing depletion (→ okaimono/ossekai; the `:commons` market already-open is correctly excluded) · `:route-to-accountability` for the top-5 depletion-OUT 取-holders (→ danjo/tsumugi/kabuto power-mirror). **CONSTITUTIONAL (G1/N1): every recommendation is PROTECT/REBUILD/OPEN/ROUTE-TO-ACCOUNTABILITY — there is NO catch/harvest/interdiction kind by construction** (a restoration map, never a catch-target list); recommend-only, sibling-actor + operator gated. 14 recs (8 protect / 1 open / 5 account; sanma + industrial-overfishing lead). `tests/test_plan.clj` (6 tests / 58 assertions): counts/kinds + **only-restoration-kinds-by-construction** + protect-targets-stocks/open-targets-enclosed-markets-only + accountability-routes-power-mirror + deterministic + renderers state the G1 invariant. umisachi now **8 methods / 54 tests** — full observe→analyze→persist→ingest→deliver→**plan** pipeline |\n| **umisachi 海幸** (R1, end-to-end integration) | `tests/test_integration.clj` — the unit suites prove each method in isolation; this proves they **COMPOSE** as one pipeline: ingest a fixture (a new stock + a factory-fishing fishery) → `merge-graph` onto the seed → `load-graph`/`analyze` the merged graph → `plan` + `social` → persist the merged graph datoms to a temp commit-DAG → `verify-chain`. Catches integration bugs no unit test sees (ingest output analyze can't consume, plan/social disagreeing with the analyze surface, a persisted graph that won't verify) and confirms the charter gates hold on the **MERGED** (not just seed) graph: the new stock flows ingest→analyze (33 nodes), the factory-fishing fishery is **G4-reclassified to `:pressure`** (never `:fishery`) in the merged graph, plan stays restoration-only, every post Charter+G1-clean, the persisted merged graph verifies, and the whole pipeline is deterministic end-to-end (analyze surfaces + plan ids + persisted CID identical across runs). 4 tests / 14 assertions. **umisachi: 9 suites / 58 tests** — full pipeline + composition verified |\n| **mitsuho 瑞穂** (deepen, cross-lang parity + FIDELITY FIX) | py/test_agent_parity (LIVE py↔clj parity for the food/agriculture agent — the snapshot `test_agent.clj` only checks clj-vs-itself; this runs the ACTUAL `agent.py` via a `python3` subprocess and DEEP-COMPARES the FULL handler outputs over the SAME states across the constitutional gates: G9 prohibited-pesticide REJECT, G7 patented-seed REJECT, G8 soil-carbon→council-review, G3/G4 10%-tithe settlement). **The oracle CAUGHT A SECOND REAL PORT-FIDELITY GAP** (after mizuho's rounding): the `rejection_reason` MESSAGE diverged — py renders the pesticide list as Python repr `['neonicotinoid…']` (single quotes), clj as a Clojure vector `[\"neonicotinoid…\"]` (double quotes); the headline decision matched so the snapshot test never saw it. **Fixed `agent.clj` validate-pesticides to match py's f-string list repr** (single quotes, `, ` sep) — the reason string is now byte-identical; existing 11-test snapshot stays green (it never pinned the exact string). 2 tests / 10 assertions (5 crop-plan full outputs + 2 harvest + parcel + 2 settlement, all deep-equal py≡clj); clj-gates-fire sanity runs regardless of python |\n| **funadaiku 船大工** (deepen, cross-lang parity) | py/test_agent_parity (LIVE py↔clj parity for the zero-emission cargo-ship agent — the agent-shaped seam after mitsuho) — runs the ACTUAL `agent.py` via a `python3` subprocess and the clj impl over the SAME inputs, DEEP-COMPARING FULL outputs across the **defining gate** `gate-zero-emission-propulsion` (G8: fossil/diesel/gas REFUSED — \"fossil propulsion prohibited per G8 (defining gate)\" · missing-component refusal · G9 non-green-H₂ refusal · verified pass, all 4 branches + reason strings) + structured handlers (steel-block-fabrication / weld-ndt pass+fail, `_infer` sentinel \"LLM_NOT_AVAILABLE\" deterministic both sides) + G3/G4 10%-tithe settlement (intent + buyer-sig→executed). **Byte-faithful — NO divergence** (funadaiku's port is clean, unlike mitsuho's reason-string); the live oracle now guards it against future drift. 2 tests / 10 assertions (5 gate + steel + 2 weld + 2 settlement deep-equal py≡clj); clj-defining-gate sanity runs regardless of python |\n| **ainori 相乗** (deepen, cross-lang parity) | py/test_agent_parity (LIVE IMPL-to-impl py↔clj parity for the pooled-mobility agent — the agent-shaped seam after funadaiku) — the existing snapshot's stale test_agent.PY still expects settlement `intent` where the IMPLs now agree on `executed`; this compares the ACTUAL py impl vs clj impl over the SAME inputs, DEEP-COMPARING FULL outputs of the deterministic functions: the **SAE-L4 safety envelope** (G3 REFUSAL-not-clamp — over-speed/SAE>4/out-of-ODD/unknown-zone, 4 branches + reason strings) + **no-surge cost-share** (G2, higher occupancy → lower share) + **10%-tithe settlement** (G4 driverWage 0 / serverHeldKey false / executed) + **member-signed authorization** (G5: member→signed/signatureRef, server→refused). `match_pool` excluded — its matchId embeds Python's process-nondeterministic `hash()` (the documented contract-only class), un-portable. **Byte-faithful — NO divergence** (the impls agree on `executed`; the 'intent' mismatch was only the stale py TEST, confirming the impl-to-impl method over snapshot, the ossekai lesson). 2 tests / 12 assertions (4 envelope + 3 cost-share + settlement + 2 auth deep-equal py≡clj); clj-gates sanity runs regardless of python |\n| **todoke 届け** (deepen, cross-lang parity + FIDELITY FIX) | methods/test_last_mile_parity (LIVE py↔clj parity for the last-mile route core — NN+2-opt sequencing under the SAE-L4 sidewalk-ODD envelope) — runs the ACTUAL `last_mile.py` via a `python3` subprocess and the clj impl over the SAME stops, comparing the optimized SEQUENCE (`[\"s0\" \"s4\" \"s1\" \"s2\" \"s3\"]`) + path length (10.650281539872886, 1e-9) + the three G7 envelope REFUSAL messages + the labour-liberation sizing scalars. **The oracle CAUGHT A THIRD REAL PORT-FIDELITY GAP** (after mizuho rounding + mitsuho pesticide-list): the out-of-ODD refusal rendered the zone differently — py `zone 'road'` (Python `{s.zone!r}` repr, single quotes), clj `zone \"road\"` (pr-str double quotes); the headline refusal matched so the snapshot never saw it. **Fixed `last_mile.clj` check-envelope to single-quote the zone** matching py's `!r`; the message is now byte-identical, existing 9-test suite stays green. 2 tests / 11 assertions (sequence + length + 3 refusal messages + 2 scalars deep-equal py≡clj); clj-route sanity runs regardless of python |\n| **sanae 早苗** (deepen, cross-lang parity) | methods/test_labor_liberation_parity (LIVE py↔clj parity for the Liberation-Priority-Score ranking) — runs the ACTUAL `labor_liberation.py` via a `python3` subprocess and the clj impl, comparing the FULL LPS ranked seed (every sector name + score to 1e-9, incl the **N1 charter-gate that zeroes MINING to LPS 0**) + freed-labour-hours + displacement-cohort sizing. The ranked seed exercises `lps()` over all 10 sectors, so a drift in the score formula or the N1 gate moves a number and fails. **Byte-faithful — NO divergence** (sanae 10.751… / kiyome 10.615… / … / MINING 0.0, freed 4.2e11, cohort 210M, all py≡clj). 2 tests / 18 assertions (10-sector ranking deep-equal + freed + cohort); clj-ranking+N1-gate sanity runs regardless of python |\n| **hodoki 解き** (deepen, cross-lang parity + FIDELITY FIX) | py/test_agent_parity (LIVE py↔clj parity for the ELV-disassembly agent) — found by a TARGETED grep-hunt for the recurring divergence class (`pr-str` of a collection in a message). Runs the ACTUAL `agent.py` via a `python3` subprocess and the clj impl, DEEP-COMPARING FULL outputs across G5 charter-scan (military/weapon REFUSED) / G6 F-gas ≥95% / G8 ECU-wipe (wiped+witnessed) / PGM-yield / G3/G4 tithe settlement. **The hunt CAUGHT A FOURTH REAL PORT-FIDELITY GAP — the worst formatting divergence yet** (both quote AND separator): the charter-scan reason rendered the matched-pattern list as py `['military', 'weapon', 'armored']` (Python list repr) vs clj `[\"military\" \"weapon\" \"armored\"]` (pr-str: double quotes + space-only sep). **Fixed `agent.clj` scan-charter-compliance to match py's list repr** (single quotes, `, ` sep); byte-identical now, existing 25-test suite stays green. 2 tests / 10 assertions (scan 2 + fgas 3 + ecu 3 + pgm 2 + settle 2 deep-equal py≡clj); clj-gates sanity runs regardless of python. **4th defect of the parity sweep — all four the same message-format class (py repr vs clj pr-str), all invisible to headline-decision snapshots** |\n\nThe **supply-chain arbitrage chain is now clj in-language**: `kakaku` (price/supply-demand\nobserve) → `mitooshi` (bridge_kakaku → forecast distribution) → `meyasu` (统合 fuse). And the\ncross-actor chokepoint join (`watari` live transit + `watatsuna` cable load → `mitooshi`\nforecast) is clj on both sides.\n\n**Tooling**: `70-tools/kotoba-bb-bridge/run-bb-tests.sh` — a babashka actor test-sweep wired as a\n**kotoba-code test-gate** (aggregates per-suite results; emits the gate's `0 failures, 0 errors`\nphrase ONLY when every suite is green; an `n==0` guard + per-suite green-phrase suppression close\nthe false-green hole). This let the `kotoba-code` agentic coder (kimi) drive a port under a real\nbb gate (used for `mitooshi/synthesize`, supervisor-corrected to green).\n\n## Method (Opus supervisor / subagent author)\n\nPer-iteration loop: Opus (1) picks the next target from the food/logistics py-only set, (2)\ninspects deps + the py test, (3) delegates the port to a **Claude-sonnet subagent** (bb-direct)\nwith the house idiom + the exact gate list, then (4) **independently verifies** — `bb` green +\n`python3` parity + the bb-bridge gate + a clean `git status` (own paths only, no `out/`/`data/`/\n`.cpcache`) — and (5) commits scoped to its own paths. Claude-sonnet subagents converged green\nwith **no supervisor correction on 17 consecutive ports**; the one kimi/kotoba-code port needed a\n2-line supervisor fix (missing `clojure.string` require + a `:series/*` fold guard).\n\nReusable idiom (same as ADR-2606131800): ns-from-path (dashes↔underscores), own/shared minimal\nEDN reader (keywords kept as `\":ns/name\"` strings to mirror the Python dict keys), constitutional\ngates ported 1:1 and test-enforced, `(/ (Math/round (* x 1e_n)) 1e_n)` for Python-`round` parity,\n`quot` for `//`, and the bb run-guard\n`(when (= *file* (System/getProperty \"babashka.file\")) (… (run-tests …) (System/exit …)))`.\n\n## PR #1745 squash-strand recovery (the honest landing note)\n\nThis wave was authored on `worktree-umisachi-seafood-clj` and pushed to **PR #1745**, per the\nstanding `/loop` directive. **PR #1745 was squash-merged after only its first commit (the umisachi\nseed)**, then auto-closed; the subsequent **47 commits accumulated on the already-merged branch**,\nlanding nothing further on `main` (the branch drifted to 47-ahead / 152-behind). The work was\nnever at risk — it stayed durable on the pushed remote branch — but it needed a fresh PR to land.\n\nRecovery (this ADR's PR): a clean landing branch was cut from `origin/main`; the **97 truly-new\n`.clj` files** (verified absent from `main` — pure additions, `git diff --cached` shows `97 A`,\nzero M/D, so no sibling agent's work is clobbered) plus this ADR + README row were brought over.\n\nDeliberately **excluded** from the landing PR (left as a follow-up reconciliation, the durable\ncopies remain on the stranded branch):\n\n- **3 uchiwake files** (`methods/openfoodfacts.clj`, `methods/crosscheck.clj`,\n  `methods/test_crosscheck.clj`) — a different version of each already exists on `main` (a parallel\n  uchiwake worktree landed them with different content). Not overwritten; reconcile separately.\n- The **per-commit `CLAUDE.md` roster annotations** — the branch's `CLAUDE.md` is 152 commits\n  behind a heavily-diverged `main`, so re-applying 16 roster-row appends would conflict; **this ADR\n  + its README index row are the durable record of the wave** (the same call ADR-2606131800 made\n  when `deps.toml`/`CLAUDE.md` churn blocked registration).\n\n## Boundaries (NOT ported this wave)\n\n- **niyaku `isaac_sway_sim.py`** — clean-room Cartpole on `kotodama.nv_compat`; nv_compat host\n  dependency, deferred.\n- **I/O-coupled legs** (`*/transact.py` urllib live-transact, `mitooshi` live fetch) — network /\n  kotoba-engine bound; the gates they guard are already ported + test-enforced in the pure layer.\n- **`kakaku_edn.py`** — a hand-rolled Python EDN parser; obviated by native `clojure.edn`, no\n  consumer; not worth a port.\n\n## Status at close\n\n- **Stranded**: 47 commits on `worktree-umisachi-seafood-clj` (PR #1745 already merged; branch is\n  the durable copy of the full wave incl. the CLAUDE.md roster prose + the 3 uchiwake variants).\n- **Landed via this ADR's PR**: 97 net-new clj files (pure additions) + this ADR + README row.\n- **Follow-ups**: (a) reconcile the 3 differing uchiwake clj files vs `main`; (b) optionally\n  re-apply the CLAUDE.md roster annotations onto current `main`; (c) **rotate the OpenRouter API\n  key** (Keychain `service=openrouter`) — it was briefly echoed to a bash log earlier in the arc;\n  kotoba-code/kimi was held off in favour of Claude-sonnet subagents until rotation.\n- **Next pure-method targets** (when resumed): niyaku `isaac_sway_sim` (needs nv_compat),\n  then the food/logistics py-only set is effectively dry.\n\n## Registry note\n\nAs in ADR-2606131800, `deps.toml` is not git-tracked in the working tree at close, so the\n`[[adrs]]`/`[[modules]]` rows could not be PR'd; this ADR + the ADR README index row are the\ndurable record. Add the rows if/when `deps.toml` is restored to tracking.\n", :adr/path "90-docs/adr/2606152000-session-close-food-logistics-clj-coverage-supervised-wave.edn", :adr/source-repo "root", :adr/topic "session-close-food-logistics-clj-coverage-supervised-wave", :adr/last_verified "2026-06-15", :adr/date "2026-06-15", :adr/authoritative false, :adr/related ["adr-2606074200-umisachi-seafood-kg-mirror" "adr-2605215000-etzhayyim-inference-murakumo-only-no-runpod"]}]
