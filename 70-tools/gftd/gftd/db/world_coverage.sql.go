// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: world_coverage.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countActors = `-- name: CountActors :one
SELECT COUNT(DISTINCT vertex_id)::bigint AS cnt FROM vertex_actor
`

func (q *Queries) CountActors(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countActors)
	var cnt int64
	err := row.Scan(&cnt)
	return cnt, err
}

const countApps = `-- name: CountApps :one
SELECT COUNT(DISTINCT vertex_id)::bigint AS cnt FROM vertex_app
`

func (q *Queries) CountApps(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countApps)
	var cnt int64
	err := row.Scan(&cnt)
	return cnt, err
}

const countDids = `-- name: CountDids :one
SELECT COUNT(DISTINCT vertex_id)::bigint AS cnt FROM vertex_did
`

func (q *Queries) CountDids(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countDids)
	var cnt int64
	err := row.Scan(&cnt)
	return cnt, err
}

const countPosts = `-- name: CountPosts :one
SELECT 0::bigint AS cnt
`

func (q *Queries) CountPosts(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countPosts)
	var cnt int64
	err := row.Scan(&cnt)
	return cnt, err
}

const countProfiles = `-- name: CountProfiles :one
SELECT COUNT(DISTINCT vertex_id)::bigint AS cnt FROM vertex_profile_fragment
`

func (q *Queries) CountProfiles(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countProfiles)
	var cnt int64
	err := row.Scan(&cnt)
	return cnt, err
}

const countProfilesByRepo = `-- name: CountProfilesByRepo :one
SELECT COUNT(DISTINCT vertex_id)::bigint AS cnt FROM vertex_profile_fragment WHERE repo = $1
`

func (q *Queries) CountProfilesByRepo(ctx context.Context, repo pgtype.Text) (int64, error) {
	row := q.db.QueryRow(ctx, countProfilesByRepo, repo)
	var cnt int64
	err := row.Scan(&cnt)
	return cnt, err
}

const countProfilesByRepoGrouped = `-- name: CountProfilesByRepoGrouped :many
SELECT repo, COUNT(DISTINCT vertex_id)::bigint AS cnt
FROM vertex_profile_fragment
WHERE repo IS NOT NULL
GROUP BY repo
LIMIT 10000
`

type CountProfilesByRepoGroupedRow struct {
	Repo pgtype.Text
	Cnt  int64
}

func (q *Queries) CountProfilesByRepoGrouped(ctx context.Context) ([]CountProfilesByRepoGroupedRow, error) {
	rows, err := q.db.Query(ctx, countProfilesByRepoGrouped)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []CountProfilesByRepoGroupedRow{}
	for rows.Next() {
		var i CountProfilesByRepoGroupedRow
		if err := rows.Scan(&i.Repo, &i.Cnt); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listApps = `-- name: ListApps :many
SELECT vertex_id, repo, display_name FROM vertex_app LIMIT 2000
`

type ListAppsRow struct {
	VertexID    pgtype.Text
	Repo        pgtype.Text
	DisplayName pgtype.Text
}

func (q *Queries) ListApps(ctx context.Context) ([]ListAppsRow, error) {
	rows, err := q.db.Query(ctx, listApps)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListAppsRow{}
	for rows.Next() {
		var i ListAppsRow
		if err := rows.Scan(&i.VertexID, &i.Repo, &i.DisplayName); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
