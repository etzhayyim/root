{
  "name": "etzhayyim-gws-oauth-test-island",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Isolated test island for the google-workspace-oauth template helpers (b64url, KEK envelope AES-GCM, JWT decode, OAuth URL).",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}
