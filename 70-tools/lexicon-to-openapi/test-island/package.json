{
  "name": "etzhayyim-lexicon-to-openapi-test-island",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Isolated test island for @etzhayyim/lexicon-to-openapi. Outside the pnpm workspace glob so it carries its own lock; does not touch the root pnpm-lock.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}
