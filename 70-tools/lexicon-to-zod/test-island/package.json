{
  "name": "etzhayyim-lexicon-to-zod-test-island",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Isolated test island for @etzhayyim/lexicon-to-zod. Outside the pnpm workspace glob so it carries its own lock; does not touch the root pnpm-lock.",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}
