;; kotoba mesh readiness registry — generated by `bb actor:mesh --survey`
;; ADR-2606230001 — actor-to-kotoba-mesh pipeline
;;
;; :mesh-ready    — cells present and ALL pass static analysis (0 blockers in cells)
;; :cells-clean   — cells present and clean, but methods/*.cljc have some blockers
;; :needs-rewrite — cells present but cells themselves have compile blockers
;; :no-cells      — has methods/*.cljc but no cells/ (observatory on-kse pattern)
;; :scaffold-only — no cljc yet (py-only or stub)
;;
;; Blocker count = lines containing any COMPILE-BLOCKER pattern across all .cljc files.
;; The safe-rewrite guide for each blocker type is in:
;;   40-engine/kotoba/crates/kotoba-clj/tests/himawari_compile_test.rs
;;   40-engine/kotoba/crates/kotoba-clj/tests/actor_mesh_compile_test.rs (ADR-2606230001)

[
 ;; ── MESH-READY (cells present, 0 compile blockers anywhere) ─────────────────
 {:actor "yamabiko"          :cljc 16 :cells 9  :blocker-lines 0   :status :mesh-ready
  :trigger :on-http :note "rail car manufacturing — 9 cells all clean; kotoba.app.edn generated"}
 {:actor "gov_municipality"  :cljc 9  :cells 3  :blocker-lines 0   :status :mesh-ready
  :trigger :on-http :note "permit workflow — 3 cells clean; kotoba.app.edn generated"}
 {:actor "sarutahiko"        :cljc 22 :cells 9  :blocker-lines 2   :status :mesh-ready
  :trigger :on-http :note "heavy truck mfg — 2 method-level blocker lines, cells themselves clean"}

 ;; ── CELLS-CLEAN (cells are clean, some method-level blockers) ────────────────
 {:actor "kanayama"          :cljc 22 :cells 9  :blocker-lines 4   :status :cells-clean
  :trigger :on-http :note "8/9 cells clean; mass_balance_binder has java.math.BigDecimal in round2 fn — fix: integer arithmetic; kotoba.app.edn generated for 8 cells"}
 {:actor "watatsumi"         :cljc 22 :cells 9  :blocker-lines 3   :status :cells-clean
  :trigger :on-http :note "civilian submersible — cells clean, method-level blockers"}
 {:actor "wadachi"           :cljc 13 :cells 5  :blocker-lines 1   :status :cells-clean
  :trigger :on-http :note "autonomous mobility R&D — 1 method-level blocker, cells clean"}
 {:actor "tsutae"            :cljc 19 :cells 8  :blocker-lines 4   :status :cells-clean
  :trigger :on-http :note "handheld comms device — cells clean, 4 method blockers"}

 ;; ── NEEDS-REWRITE (cells present but cells have compile blockers) ────────────
 {:actor "himawari"          :cljc 16 :cells 7  :blocker-lines 94  :status :needs-rewrite
  :trigger :on-http :note "solar PV mfg — cells have blockers; safe-rewrite patterns proven in himawari_compile_test.rs (PR #2313)"}
 {:actor "funadaiku"         :cljc 18 :cells 9  :blocker-lines 22  :status :needs-rewrite
  :trigger :on-http :note "zero-emission cargo ship building — cells have blockers"}
 {:actor "kamado"            :cljc 25 :cells 6  :blocker-lines 85  :status :needs-rewrite
  :trigger :on-http :note "carbon refining + fossil decommission — cells have blockers"}
 {:actor "magatama"          :cljc 12 :cells 10 :blocker-lines 23  :status :needs-rewrite
  :trigger :on-http :note "geological survey — cells have blockers"}
 {:actor "hagukumi"          :cljc 10 :cells 5  :blocker-lines 15  :status :needs-rewrite
  :trigger :on-http :note "care actor (childcare/eldercare) — cells have blockers"}
 {:actor "hikari"            :cljc 22 :cells 5  :blocker-lines 44  :status :needs-rewrite
  :trigger :on-http :note "energy grid actor — cells have blockers"}
 {:actor "niyaku"            :cljc 20 :cells 9  :blocker-lines 40  :status :needs-rewrite
  :trigger :on-http :note "port cargo handling — cells have blockers"}
 {:actor "ake"               :cljc 23 :cells 5  :blocker-lines 48  :status :needs-rewrite
  :trigger :on-http :note "community-edit membrane — cells have blockers"}
 {:actor "kawaraban"         :cljc 13 :cells 5  :blocker-lines 41  :status :needs-rewrite
  :trigger :on-http :note "news medium — cells have blockers"}
 {:actor "tasuke"            :cljc 24 :cells 5  :blocker-lines 65  :status :needs-rewrite
  :trigger :on-http :note "cybercrime victim support — cells have blockers"}
 {:actor "keizu"             :cljc 32 :cells 5  :blocker-lines 94  :status :needs-rewrite
  :trigger :on-kse  :note "government power-relations KG — cells have blockers"}
 {:actor "mitooshi"          :cljc 39 :cells 5  :blocker-lines 116 :status :needs-rewrite
  :trigger :on-http :note "probabilistic forecasting — cells have blockers"}
 {:actor "tedai"             :cljc 13 :cells 5  :blocker-lines 91  :status :needs-rewrite
  :trigger :on-http :note "member computer-operation — cells have blockers"}
 {:actor "fuchi"             :cljc 27 :cells 5  :blocker-lines 99  :status :needs-rewrite
  :trigger :on-http :note "maintainer sustenance allocator — cells have blockers"}
 {:actor "karakuri"          :cljc 20 :cells 5  :blocker-lines 81  :status :needs-rewrite
  :trigger :on-http :note "web-service-to-CLI — cells have blockers"}
 {:actor "shionome"          :cljc 34 :cells 5  :blocker-lines 92  :status :needs-rewrite
  :trigger :on-kse  :note "cross-asset capital-flow observatory — cells have blockers; T1 Rust wasm already deployed (ADR-2606111330)"}
 {:actor "silicon"           :cljc 17 :cells 4  :blocker-lines 40  :status :needs-rewrite
  :trigger :on-http :note "ternary silicon supply — cells have blockers"}
 {:actor "noroshi"           :cljc 31 :cells 2  :blocker-lines 148 :status :needs-rewrite
  :trigger :on-http :note "photonics-electronics — cells have blockers"}
 {:actor "nusa"              :cljc 9  :cells 2  :blocker-lines 36  :status :needs-rewrite
  :trigger :on-http :note "hemp heritage — cells have blockers"}
 {:actor "hotaru"            :cljc 10 :cells 5  :blocker-lines 65  :status :needs-rewrite
  :trigger :on-http :note "III-V/InP substrate — cells have blockers"}
 {:actor "tatekata"          :cljc 14 :cells 4  :blocker-lines 6   :status :needs-rewrite
  :trigger :on-http :note "construction — 6 blocker lines in cells"}
 {:actor "kosatsu"           :cljc 21 :cells 1  :blocker-lines 75  :status :needs-rewrite
  :trigger :on-kse  :note "sanctions observatory — cell has blockers"}
 {:actor "sanae"             :cljc 7  :cells 1  :blocker-lines 12  :status :needs-rewrite
  :trigger :on-http :note "field agriculture robotics — cell has blockers"}
 {:actor "kiyome"            :cljc 3  :cells 1  :blocker-lines 5   :status :needs-rewrite
  :trigger :on-http :note "cleaning robotics — cell has blockers"}
 {:actor "hataori"           :cljc 3  :cells 1  :blocker-lines 4   :status :needs-rewrite
  :trigger :on-http :note "garment robotics — cell has blockers"}
 {:actor "mizuho"            :cljc 9  :cells 1  :blocker-lines 25  :status :needs-rewrite
  :trigger :on-http :note "water/sanitation — cell has blockers"}
 {:actor "suji"              :cljc 20 :cells 2  :blocker-lines 49  :status :needs-rewrite
  :trigger :on-http :note "musculoskeletal biomechanics sim — cells have blockers"}
 {:actor "yadori"            :cljc 8  :cells 2  :blocker-lines 24  :status :needs-rewrite
  :trigger :on-http :note "DNS/domain acquisition — cells have blockers"}
 {:actor "tazuna"            :cljc 5  :cells 1  :blocker-lines 10  :status :needs-rewrite
  :trigger :on-http :note "remote robotics fleet — cell has blockers"}
 {:actor "ooyake"            :cljc 5  :cells 2  :blocker-lines 5   :status :needs-rewrite
  :trigger :on-kse  :note "government atlas — cells have blockers"}
 {:actor "todoke"            :cljc 7  :cells 2  :blocker-lines 28  :status :needs-rewrite
  :trigger :on-http :note "last-mile delivery — cells have blockers"}

 ;; ── NO-CELLS (methods/*.cljc but no cells; on-kse/on-tick trigger pattern) ───
 {:actor "inochi"    :cljc 12 :cells 0 :blocker-lines 35  :status :no-cells
  :trigger :on-kse :note "biosphere KG — kotoba.app.edn already present; methods have blockers"}
 {:actor "kafun"     :cljc 14 :cells 0 :blocker-lines 34  :status :no-cells
  :trigger :on-kse :note "forest remediation — methods have blockers (Math/round in round3)"}
 {:actor "busshi"    :cljc 12 :cells 0 :blocker-lines 20  :status :no-cells
  :trigger :on-kse :note "commodity KG — methods have blockers"}
 {:actor "tatara"    :cljc 19 :cells 0 :blocker-lines 29  :status :no-cells
  :trigger :on-kse :note "manufacturing-plant geographic KG — kotoba.app.edn already present"}
 {:actor "kabuto"    :cljc 16 :cells 0 :blocker-lines 47  :status :no-cells
  :trigger :on-kse :note "supply-chain KG — methods have blockers"}
 {:actor "kanjo"     :cljc 9  :cells 0 :blocker-lines 33  :status :no-cells
  :trigger :on-kse :note "financial disclosure KG — methods have blockers"}
 {:actor "tsumugi"   :cljc 36 :cells 0 :blocker-lines 184 :status :no-cells
  :trigger :on-kse :note "Engi KG intel weaver — methods have blockers"}
 {:actor "kaname"    :cljc 29 :cells 0 :blocker-lines 103 :status :no-cells
  :trigger :on-tick :note "SoS leverage-point synthesizer — methods have blockers"}
 {:actor "ibuki"     :cljc 51 :cells 0 :blocker-lines 148 :status :no-cells
  :trigger :on-tick :note "organism autonomy — methods have blockers"}
 {:actor "mimamori"  :cljc 15 :cells 0 :blocker-lines 32  :status :no-cells
  :trigger :on-tick :note "covenant keeping membrane — methods have blockers"}
 {:actor "ugachi"    :cljc 13 :cells 0 :blocker-lines 19  :status :no-cells
  :trigger :on-kse :note "extraction risk gate — methods have blockers"}
 {:actor "funamori"  :cljc 9  :cells 0 :blocker-lines 18  :status :no-cells
  :trigger :on-kse :note "salinity-gradient power — methods have blockers"}
 {:actor "jinushi"   :cljc 43 :cells 0 :blocker-lines 56  :status :no-cells
  :trigger :on-kse :note "land ownership KG — kotoba.app.edn already present"}
 {:actor "hokorobi"  :cljc 12 :cells 0 :blocker-lines 38  :status :no-cells
  :trigger :on-kse :note "systemic finance-risk observatory — methods have blockers"}
 {:actor "hoshimori" :cljc 14 :cells 0 :blocker-lines 39  :status :no-cells
  :trigger :on-kse :note "orbital stewardship mirror — methods have blockers"}
 {:actor "watatsuna" :cljc 12 :cells 0 :blocker-lines 43  :status :no-cells
  :trigger :on-kse :note "submarine cable KG — methods have blockers"}
 {:actor "chie"      :cljc 22 :cells 0 :blocker-lines 38  :status :no-cells
  :trigger :on-kse :note "AI domain intelligence — methods have blockers"}
 {:actor "asobi"     :cljc 12 :cells 0 :blocker-lines 31  :status :no-cells
  :trigger :on-kse :note "freed-time/play KG — methods have blockers; kotoba.app.edn already present"}
 {:actor "rasen"     :cljc 17 :cells 0 :blocker-lines 68  :status :no-cells
  :trigger :on-kse :note "public-genetics KG — methods have blockers"}
 {:actor "shiori"    :cljc 10 :cells 0 :blocker-lines 30  :status :no-cells
  :trigger :on-kse :note "Wellbecoming detractor observatory — methods have blockers"}
 {:actor "tokigusuri" :cljc 9 :cells 0 :blocker-lines 36  :status :no-cells
  :trigger :on-kse :note "pharma patent-cliff — methods have blockers"}
 {:actor "danjo"     :cljc 7  :cells 0 :blocker-lines 39  :status :no-cells
  :trigger :on-kse :note "public-accountability oversight — methods have blockers"}
 {:actor "hakoniwa"  :cljc 15 :cells 0 :blocker-lines 70  :status :no-cells
  :trigger :on-tick :note "forward-simulation observatory — methods have blockers"}
 {:actor "tate"      :cljc 18 :cells 0 :blocker-lines 181 :status :no-cells
  :trigger :on-kse :note "citizen legal-defense concierge — observatory pattern; methods use str/includes? for clause/notice matching (181 blocker-lines). Mesh contribution = the host-emitted ANONYMIZED coverage digest (methods/coverage_publish.cljc, content-addressed CIDv1, G1 member-data-free — never member docs); atproto profile did:web:etzhayyim.com:actor:tate. cells .solve() is R0 scaffold (no Pregel cells to compile)."}
]
