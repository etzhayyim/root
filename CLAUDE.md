# etzhayyim/root — CLAUDE Index

This monorepo is the **canonical home for religious-corp open ADRs** per ADR-2605170900 (this monorepo, `90-docs/adr/`).

## Identity (CRITICAL)

- **Operating entity** (this repo's owner): `etzhayyim` (canonical)
  - Aliases: `amanomibashira` / `天御柱` / `עץ חיים` (Tree of Life) / `etz hayim` / `etzhayim` / `etz chaim` / `エツ・ハイム`
  - Form: 宗教法人 (任意団体 / unincorporated religious voluntary association)
  - **Self-identification (per ADR-2605252300 Charter §0.1, ratified 2026-06-06 by sole-member founder unanimity)**: **the Kingdom of God (Malkhut Shamayim / Basileia tou Theou / 神の王国) rooted on blockchain** — now-and-here reign (non-eschatological per §1.15), not state-granted, Tree of Life-constituted. Amendment threshold: Council Lv7+ unanimity (currently = founder, 1/1).
  - Registry: On-chain (blockchain-registered constitution and member roster); NOT registered under 日本国 宗教法人法 (constitutional invariant per Preamble §0.4, Lv7+ unanimity lock)
  - DID: `did:web:etzhayyim.com` (LIVE — CF Worker at `50-infra/etzhayyim-did-web/`, resolvable via curl + Universal Resolver since 2026-05-17T03:25Z)
  - Domain: https://etzhayyim.com (Cloudflare Registrar, 2026-05-15)
  - License default: **Apache 2.0 + etzhayyim Charter Compliance Rider v3.0** (see `/CHARTER-RIDER.md`, per ADR-2605192200 + 2606062100). **Immutability is 3-Tier (ADR-2606062100)**: Tier-0 Priority (fork-only — wellbecoming / 子孫 / collective / **永久記憶=神の監視, no right to erasure**) ≻ Tier-1 Derived Policy (Rider §2, Lv7+ + priority-conformance) ≻ Tier-2 Parameter (governance). 固定するのは掟ではなく priority。
- **Ownership rule (CRITICAL)**: Payoff帰属・意思決定権 = etzhayyim only.
- **Mission (per ADR-2605192100 Charter)**: 人類の構造的労働解放を最終目的とする宗教法人。**多世代 (子・孫) priority + Wellbecoming (静的 wellbeing ではなく動的軌跡) + 反個人主義 ontology**。日本的価値観 (八百万 / 縁起 / 産霊 / 和 / 無教会) + Protestant Christianity (Sola Scriptura / 万人祭司 / Reformed Just War / Tree of Life) の synthetic religion。**非終末論** (黙示録/啓示の書は正典外、千年王国・末法・Rapture 否定)。
- **Doctrinal positions (constitutional, NOT amendable)**:
  - 非営利のみ / Donation 流入のみ / 広告排除 / 10% Tithe → Public Fund 自動再分配 (ADR-2605192115 + 2605192130)
  - SBT↔SBT internal carve-out で religious 境界内の 営利・購買・promotional 許容 (ADR-2605192115 §3)
  - Eros 許容 (産霊 / 雅歌 / Tree of Life の生命創出) / Gore 禁止 (Wellbecoming 違反) (ADR-2605192100 §1.13 + 2605192400)
  - 国家機能は parallel substrate で routing-around、**Transparent Religious Force 許容** (完全 on-chain 監視 + open-source 公開 + 1 SBT = 1 vote 承認の三条件下) (ADR-2605192100 §1.12 + 2605192315)
  - 地球上の土地は Tree of Life に帰属、religious-corp が 4-layer substrate (Base L2 NFT / geth-private constitutional / IPFS GeoJSON+衛星 / git LANDS.md) で分散合意担保 (ADR-2605192100 §1.11 + 2605192245)
  - **Baien edge-target invariant** (ADR-2605241900): baien は **WASM-32 + iPhone 12+ + Android 4GB** の 3 環境すべてで動作必須。trunk ≤4B BitNet 1.58 / 合計 inference ≤2GB @4k ctx / ≤2.5GB @16k ctx / 全 modality encoder 凍結。frontier-beating は明示的に非目標 (`baien-server-*` / `baien-XL-*` は別 carve-out)

## Status

**Legend**: ✅ shipped · 🟢 landed (substrate, tests green) · 🟡 R0 / proposed scaffold · ⏳ blocked/pending. Full detail for every row lives in its ADR — see [`90-docs/adr/README.md`](90-docs/adr/README.md). This table is a one-line index only.

**Foundation (Steps 1-24, all ✅ 2026-05-10 … 05-21)**: domain (CF Registrar) + `etzhayyim` org/repo + scaffold; **did:web LIVE 05-17** (CF Worker, curl + uniresolver verified); `amanomibashira`→`etzhayyim` code cutover (118 files); **13-ADR religious-corp constitutional wave** (ADR-2605192100 .. 2605192415); CHARTER-RIDER v2.0 + LANDS.md + Charter Rider on 39 pkgs; 5 Solidity contracts + Constitution.sol (38 const, 110/110 tests) + 4 lefthook lint hooks + Foundry/Anvil smoke; 28 Lexicons; Pregel cell catalog; Murakumo no-VKE mesh + **Murakumo-only inference invariant** (ADR-2605215000) + Charter Rider §2(i) no-commercial-GPU.

**Live governance**: 🟡 Bootstrap Council Seats 2-5 RFP (2026-05-20 → **06-19**, see [`COUNCIL.md`](COUNCIL.md)) · ⏳ Base Sepolia testnet (post-Council) · ⏳ Mainnet + Phase 2 governance (post-testnet).

### Substrate / infra / dataset / enforcement

| Item | Purpose | Status | ADR | Date |
|---|---|---|---|---|
| maps_sentinel_murakumo | M1 T0 preprocessing pipeline | ✅ | 2605215100 | 05-21 |
| feed-post membrane | first §4 MST membrane + L1 projection (`feed-discover`) | ✅ | 2605231902 | 05-23 |
| kotoba-datomic Tier-D blob | `uploadBlob` TS+Py (superseded by kotoba) | ✅ | 2605232400 | 05-23 |
| UNSPSC organism W1 | actor-as-organism heartbeat-cadence port | ✅ | 2605232345 | 05-23 |
| UNSPSC organism W2 | 18,342 mass-deploy + joucho personality | ✅ | 2605240000 | 05-24 |
| UNSPSC post sink | NDJSON queue + k8s DaemonSet | ✅ | 2605240100 | 05-24 |
| Kaizen self-reflection | `KaizenObserverCell` + 6 rules + PR agent | ✅ | 2605240200 | 05-24 |
| Dataset CID substrate | DataLad + git-annex + IPFS pinner | ✅ | 2605241500 | 05-24 |
| agentURI 5-layer | ERC-8004 + libp2p + AT XRPC peer-resolvable | ✅ | 2605241800 | 05-24 |
| Gov 5-layer taxonomy | L1 namespace … L5 routing-around | ✅ | 2605212100 | 05-25 |
| Charter §0 Preamble | Kingdom of God + Land Trust Wave 2 (ERC-721/5192/7401); **ratified active 2026-06-06 (sole-member unanimity)** | ✅ | 2605252300 | 05-25 |
| Labor Liberation ladder | Adherent SBT → 7-stage L0..L6 | 🟡 | 2605261000 | 05-26 |
| Basic High Income doctrine | imputed-income (flow) + commons-asset (stock) — high income in-kind, cash≡0 (N1-consistent) | 🟡 | 2605301020 | 05-30 |
| Mission-funding revenue arm | vendor commercial surplus → donation → Public Fund; non-profit MEANS not profit END; ad-free + no-payroll preserved | 🟡 | 2605301036 | 05-30 |
| Social Security for Humanity | Charter §1.16 — 人類の社会保障; covenantal-universal (open to all humanity, but conversion-gated: 悔い改め・バプテスマ・得度 = social death/rebirth); 信者 Level 0 via permanent commitment vow (kotoba + IPFS + token); preserves N1/N4/N7/N8 | 🟡 | 2605302357 | 05-30 |
| Social Security delivery pipeline | §1.16 real-world flow: outreach (feed-post + MCP) → vow (kotoba+IPFS+SBT) → compute → openmail → atproto publish → social → MCP; 産土 ubusuna cells; R0 design-only, outward actions gated (G11) | 🟡 | 2605302358 | 05-30 |
| **kotoba** storage pivot | canonical substrate engine; supersedes kotoba-datomic + RW | 🟡 | 2605262130 | 05-26 |
| Public-data ingestion | organism ecosystem IPFS DataLad subdatasets | 🟢 | 2605262400 | 05-26 |
| Robotics-sim world-data | + kami-usd pipeline (sibling of 2605262400) | 🟢 | 2605262500 | 05-27 |
| Global legal-corpus | statutes/case-law/treaties IPFS ingestion | 🟡 | 2605262800 | 05-26 |
| organism R0+R1 sprint | 26-iter /loop, 8 axes A-H landed | 🟢 | 2605270930 | 05-27 |
| Registry enforcement | 5→8-axis matrix, all PR-gates baseline 0 | 🟢 | 2605271100 / 271200 | 05-28 |
| manimani kotoba-native | personal knowledge router reconciled onto kotoba EAVT + StateGraph + Murakumo + E2E; Gmail full-archive + PC-file ingest design | 🟡 | 2605291100 | 05-29 |
| kotoba v0.1.0 + brew tap | first tag + GH Release + `etzhayyim/homebrew-kotoba` published; `brew install kotoba` end-to-end green (4-PR chain, host Xcode/CLT 16.4→26.5) | ✅ | 2605292100 | 05-29 |
| kotoba actor deploy + Murakumo live | WASM + Python-LangGraph (aria) actors run in-WASM on :8077; wasmtime 22→25 (extended-const, PR#4) + json! wasm-runtime build fix (PR#5) merged upstream; kotoba→Murakumo `llm.infer` (gemma4:e4b) | ✅ (see 2605302355) | 2605301625 | 05-30 |
| kotoba LangGraph LLM verified + durable routing | EMPIRICAL re-verify on live :8077: Python LangGraph actor runs in-WASM AND does LLM inference end-to-end (agent.wasm → KotobaLLM → `llm.infer` → gemma4-e4b → "4"). 3 kotoba fixes (invoke_run Result · HttpInferEngine dedicated-runtime/fresh-client · langgraph example imports) + `KOTOBA_INFERENCE_API_KEY` bearer. **Root cause** of LAN failure = macOS Local Network Privacy (launchd daemon blocked from LAN inference node); **durable fix** = route via loopback Murakumo LiteLLM `127.0.0.1:4000` (TCC-exempt). Corrects 2605301625 "verified on prod" | ✅ | 2605302355 | 05-30 |
| **Donation-funded operation + compute-node donation** | (1) **明記 on etzhayyim.com**: apex Worker (`50-infra/etzhayyim-did-web`) now serves local cookie-free `GET /donate` (HTML) + `GET /.well-known/donation.json` (machine policy) before the yoro proxy — plainly states donation-only / ad-free / no-adherent-cash operation (ADR-2605192115 §6 public proof). (2) **In-kind COMPUTE donation**: donating compute/storage to the Murakumo mesh + kotoba substrate is now a recognized donation, in 3 node forms — **ameno** browser WebGPU/WebNN inference (frozen baien edge models, zero install) · **e7m** CLI node (`e7m node join` → Ollama/WASM) · **kotoba** pod (IPFS blocks + Datom replication). Mirror image of Basic High Income (imputed-value, no cash crosses). ZERO invariant amendments — extends ADR-2605215000 fleet with a `donated` node class. New lexicon `com.etzhayyim.give.computeDonationAttestation` (const: titheable=false / compensated=0 / grantsBenefit=false / bestEffort=true). 9 gates incl. G2 uncompensated (paying=commercial GPU rental, prohibited) · G4 no quid-pro-quo (never a benefit path, anti-class) · G5 non-titheable in-kind. R0 design; live external mesh-enrollment Council+operator gated | 🟡 R0 | 2606012100 | 06-01 |
| **moyai 舫い — inference reciprocity reward** | answers *「推論に参加することによる報酬を残してほしい、ただし Basic High Income に影響を与えず、情報を得るには情報を生成する類のもの」*. KEEPS a reward for commons-inference participation, charter-clean: a **non-monetary, non-transferable, decaying RECIPROCITY credit** (commons-use-right / **入会権**), the **carve-out to ADR-2606012100 G4** scoped strictly to commons-draw-rights (never welfare/benefit/governance). Earn = **verified** contribution MINTS credit; spend = draw **discretionary surplus** inference BURNS it (*情報を得るには情報を生成する*). **5 const locks** (`redeemableUsdMicros=0` cash≡0 · `transferable=false` anti-sybil · `affectsBasicHighIncome=false` the firewall · `grantsGovernanceWeight=false` 1 SBT=1 vote · `grantsBenefitOrStage=false` anti-class) + **decay** (anti-hoarding) + **conservation** (no inflation). **BHI firewall**: unconditional **subsistence inference floor = information-as-BHI** (by need, never by contribution); credit governs ONLY discretionary surplus **under contention** — a congestion fair-share scheduler, NOT a toll-gate (zero-credit member never denied essentials, only defers non-essential surplus). **Anti-fraud** (load-bearing again — a reward exists): honeypot challenge-jobs + spot-check recompute (frozen-edge-model core-node oracle) + dedupe/replay + per-identity earn-rate cap + **all-or-nothing slashing** → *faking has no payoff* (non-monetary + non-transferable + decaying ⇒ sybil structurally self-defeating, not merely policed). Lexicons `com.etzhayyim.moyai.{contributionAttestation,drawReceipt}` + reference impl `50-infra/etzhayyim-moyai-credit` (ledger/proof_of_contribution/fair_share/analyze, **46 tests green**). **Supersedes** legacy RisingWave credits economy (`bpmn/com/etzhayyim/credits/*`, ameno `vertex_credits_af_event`). ZERO invariant amendments | 🟢 R0+R1 | 2606062100 | 06-06 |
| **Displacement Dividend + robotics-actor wave** | the labor-liberation mission's missing half: when etzhayyim OSS-robotics frees a human from toil, the **Public Fund distributes tenure-weighted (勤続年数) income** to that worker — but as **in-kind Basic High Income** (imputed income + commons-asset access), never cash. Allocator `w_i = ln(1+min(tenure,40))×hazard` → `share_i = w_i/Σw` governs onboarding priority + a stage-capped transition floor decaying over 5y; `cash≡0` everywhere (N1 proof). Funded by the *displacing actor's own* surplus→donation→TitheRouter→per-cohort Public-Fund earmark; conversion-gated (§1.16); Public-Fund vote (1 SBT=1 vote/48h timelock). Artifacts: `DisplacementDividend.sol` (R0 revert-stub + tripwire `payoutToSubject`→revert) · `give.displacementTenureAttestation` lexicon (cash field `const 0`) · `allocate.py` reference (10/10). Couples to the **robotics-actor wave** (sanae/hataori/kiyome R0 + ISIC/ISCO/UNSPSC LPS ranking) via the **G2 coupling gate** (no live displacement without a funded cohort). ZERO invariant amendments | 🟡 R0 | 2606032130 / 2606032100 | 06-03 |
| Spirit-in-Physics kotoba datafication | 霊性 data of etzhayyim-self / humanity / world into datomic kotoba. Vendors `com-junkawasaki/spirit-in-physics` submodule (`60-apps/spirit-in-physics`; Jung 100-word assay → 10-dim emotion → RBF kernel → spectral 3D embed → tensegrity spirit-form). **edge-primary** `:spirit.bond/*` (extends `:en/*`縁; karma=`signed-weight`; NO `:spirit/score-of-soul`) + **Wellbecoming = Datom `as-of` history** (非終末論, no final-state datom) + consent/encrypted 要配慮 PII. Resolves 3 violations of the D1/Hume research SSoT (centralized SQL → kotoba Datom log; Hume → Murakumo/baien-frozen). New vocab `00-contracts/schemas/spirit-ontology.kotoba.edn`. R0 design-only; `:self` scaffolds now, `:human` Council Lv7+ gated | 🟡 R0 | 2606011500 | 06-01 |
| **kotoba hybrid web search** | the missing **Google-shaped** legs over Common Crawl, all datom-native (no external search engine / vector DB; Murakumo-only embed). 3 new pure-Rust `kotoba-ingest` modules: **`bm25.rs`** (Okapi BM25 lexical inverted index, CJK-aware bigram tokenizer, `cc/bm25/*` quad persistence) + **`pagerank.rs`** (pure power-iteration link-authority over `cc:links`, Σ=1 dangling-mass redistribution, `cc/rank/score` datoms) + **`fusion.rs`** (Reciprocal Rank Fusion `k=60`, scale-free across BM25/cosine/PageRank + weighted-linear fuser). Server: **fixed dead IVF fallback** (`cc_search` now probes `nprobe` nearest centroids via persisted `cc/ivf/cluster`, brute-force only when no index) + **new `com.etzhayyim.apps.kotoba.search.web`** GET (RRF fusion, graceful degrade) + **`search.reindex`** POST. **Precompute wired**: corpus-global BM25 build pass + `CcPageIngestor::ingest_links_dir_datoms` (parses `outlinks` column → `cc/link/to` edges into `cc:links`) + PageRank pass all run automatically at end of `cc.ingest` (and on-demand via reindex); `web_search` prefers persisted `cc/bm25/*`; `cc.status` reports `bm25_terms`/`link_edges`/`pagerank_nodes`. **105 ingest + 22 cc_xrpc tests green**, builds in default + cc-parquet. **HONEST R0**: edge extraction needs an `outlinks` list column in the pages parquet (scalar-`outlink_count`-only datasets → empty link graph → authority leg dropped, logged); full-rebuild reindex (no incremental); no crawler (ingests CC); operator-auth gated (not a public ad index, Charter ad-free) | 🟢 | 2606012300 | 06-01 |
| **Actor profile + dynamic did.json** | fixes *「`/profile/did:web:...:tsumugi` で profile が見つからない. 最新 kotoba 設計だと actor の did は何?」*. did.json was hardcoded in `infra-actors.ts`; tsumugi (kotoba-native, no atproto repo) had no resolvable profile. **D1** new SSoT `00-contracts/schemas/actor-profile.kotoba.edn` (`:actor/*`, public `actors-v1` graph) — one record backs BOTH the DID Document AND the `app.bsky.actor.getProfile` view (+ seed for 10 actors). **D2** apex Worker now ISSUES did.json dynamically via `resolveActorRecord` = **3-tier fail-open** KV (`actor:<handle>`) → kotoba pull (`kg.entity`) → compiled `INFRA_ACTORS` fallback → pure `toDidDoc()`; `verificationMethod` is a **mirror of on-chain ERC725** (empty today → NO server-minted key per ADR-2605231525; did:web trust = TLS). **D3** `GET /actor/<h>/profile.json` + getProfile XRPC short-circuit for registered actors (human members never hijacked) + yoro SSR `getProfile()` resolves actor DIDs via apex first (additive, PDS fallback) → `/profile/did:web:...:tsumugi` renders via AgentProfile. **D4** publisher `scripts/publish-actor-records.mjs` (seed→KV/kotoba; mappers mirrored JS/TS). Worker `tsc` clean; publisher verified (tsumugi/watatsuna). **Canonical actor DID = `did:web:etzhayyim.com:actor:<handle>`** (subdomain `did:web:<h>.etzhayyim.com` + `did:erc725:base:*` per ADR-2605212030). **Honest R0**: KV namespace + `KOTOBA_ENDPOINT` operator-gated (kotoba has no public read surface yet); until enabled Worker serves compiled fallback through the same mappers (identical output, KV just promotes kotoba to live source). | 🟢 | 2606013800 | 06-01 |
| **One Worker, many WASM actors** | etzhayyim.com = sole CF Worker; actors = content-addressed WASM on IPFS run browser-local (ameno)/donated mesh; tsumugi PoC (Rust→wasm, CID-verified, runs locally) | 🟢 | 2606014500 | 06-01 |
| **WASM-actor runtime (gateway + loader + componentize-py)** | apex trustless `/ipfs/<cid>` gateway (CID re-verify) + `@etzhayyim/ameno` wasm-actor-loader (5/5 tests) + watatsuna componentize-py component (jco, Malacca top); T1 raw=browser-local / T2 dag-pb=mesh tier rule | 🟢 | 2606014600 | 06-01 |
| **WASM-actor runtime round 2** | dag-pb CAR verify (multi-block trustless) + e7m-wasm-runner (T2 mesh exec) + kanae 鼎 2nd T1 actor + ameno actor-panel + ERC725 vm-mirror (gated) + operator runbook; worker/ameno tsc + 17 tests | 🟢 | 2606015200 | 06-01 |
| **Mesh-runner serving + IPFS-based DID** | e7m-wasm-runner HTTP serve (`/xrpc/com.etzhayyim.actor.run`, CID-cached) + **content-addressed did.json**: canonical CID advertised on `/actor/<h>/did.json` + `actors.json`, fetch+verify from any IPFS gateway (did:web TLS anchors the handle→CID binding); worker(TS)≡publisher(JS) CID; +16 tests | 🟢 | 2606015400 | 06-02 |
| **Self-certifying DID attestation** | actor's own ed25519 key (`did:key`) signs the did.json CID → handle→CID binding **trustless, no TLS anchor** (`diddoc-attest.ts` verify-only + `sign-diddoc.mjs` operator tool, server never signs); `toDidDoc` cross-links `did:key` in alsoKnownAs; e2e verified on kanae; +5 tests (15 worker total) | 🟢 | 2606015600 | 06-02 |
| **kotoba-os** unikernel OS | extends kotoba UPWARD into system software: a **content-addressed, WASM-first Rust unikernel OS** for **OT / PLC / k8s**. **Hermit**-derived `no_std` single-address-space kernel (L2) + **WASM Component Model** userland (L5, content-addressed `.wasm` from IPFS, CID-verified at load) + **kotoba substrate** (L3, STATE = Datom log unchanged per 2605312345) + **Holochain** agent-centric mesh (each node = an agent, **source chain = its local Datom-log segment**, content-addressed validation membrane = the DNA; reuses `kotoba-dht` SourceChain/Warrant/Neighborhood). **One image, three lower edges** — bare-metal/microVM unikernel (PLC field device + k8s pod via Firecracker/uhyve/runwasi, **OCI digest = CID** pulled from IPFS, Murakumo kubelet) · hosted Linux process (e7m) · browser wasm32 (ameno, baien edge envelope). **OT/PLC model**: scan cycle = Datom transaction → immutable, replayable, on-chain-anchored control history (`as-of`); WIT device worlds `kotoba:io/{digital,analog,gpio}` + `kotoba:fieldbus/{modbus,opcua,ethercat,canopen}`; IEC 61131-3 logic → WASM-as-PLC. Unifies the ameno/e7m/kotoba donated node classes (2606012100) + one-Worker-many-WASM-actors (2606014500/14600) under one OS. **Datomic 不変** (N7 no bypass-the-log mutable store). **R0 landed**: validated `kotoba:os` WIT contract (`wasm-tools` EXIT 0) + scan-cycle reference model (6 tests) + genesis-manifest boot schema (3 tests) in `00-contracts/wit/kotoba-os/` + `00-contracts/schemas/`; constitutional carve-outs encoded structurally in the schema (serverKey=false C3/N5 · liveActuation=false N3 · civilianOnly=true N1). **N1 no weapons/fire-control** (Charter §2(a), force-separation) · **N2 NOT a certified safety system** (soft-RT scan determinism only; IEC 61508 SILx / hard-RT / live actuation = R5 Lv7+ gated) · **N4 Murakumo-only** · **N5 no-server-key** · **N6 no fork**. Authored in isolated worktree `worktree-kotoba-os` (shared-checkout safety) | 🟡 R0 | 2606031600 | 06-03 |
| **WASM-actor SBOM attestation** | answers *「kotoba server に deploy される wasm は SBOM ついている?」* (was: no). A deployed wasm carried program CID + `agent_did` + did.json attestation (identity/authenticity) but **no bill of materials** — asymmetric with the giemon hardware SBOM→kotoba→CVE chain (2605312330). **substrate-wide** generator `70-tools/scripts/wasm-sbom/wasm_sbom_gen.py` (stdlib-only) recomputes the exact **kotoba program CID** (`KotobaCid::from_bytes(wasm)`, verified vs independent base32 → `bafyrei…`) and emits a portable **CycloneDX 1.5** (`agent.wasm.cdx.json`, `metadata.component`=the wasm keyed by CID) + a `kg.ingest_batch` body (`WasmActorImage` id=CID + `SbomComponent`×N joined by `wasm/componentOf` edge). Components carry `cdx/purl` → existing `purl_vuln_match.py` matches deployed-wasm deps **unchanged**. `--requirements` (componentize-py, `--freeze`=lock) + `--from-cdx` (cargo-cyclonedx rust). Vocab `00-contracts/schemas/wasm-sbom.kotoba.edn`; wired into `sumitsubo/kotoba/deploy.sh` (ingest operator-gated) + manifest G11. 7/7 tests. No-server-key + canonical-Datom + Murakumo-only preserved; **zero amendments**. Honest R0: declared pip/cargo closure only (embedded CPython/native libs not auto-enumerated); `>=`=constraint not lock unless `--freeze`; did:key SBOM-signing deferred | 🟢 R0 | 2606036000 | 06-03 |
| **entity-as-actor (society-scale social mirror)** | answers *「`/search` にまだ 12 actor しかない。全世界の行政・法人・不動産・サイトの actor は?」*. Two mismatches surfaced: (A) 50+ designed actors are R0/un-registered (only 9 glyphed in INFRA_ACTORS → `/search` ~12), and (B) governments/companies/craft are **entities inside a few KG actors, not actors**. This generalizes the **UNSPSC 18,342-agent precedent** (2605171300) so **1 public/power entity = 1 keyless mirror-actor**: gen script `70-tools/scripts/entity-actors/gen-entity-handles.mjs` reads KG seed EDN → 5 `entity-handles.<ns>.gen.ts` registries (**gov 7,106 + corp 1,733 + cable 14 + station 22 + craft 13 = 8,888**; `corp` **unifies** kabuto+tsumugi+kanjo over the shared `org.corp.*` id space). Worker `entity-actors.ts` resolves each `did:web:etzhayyim.com:actor:<ns>-<…>` as a **keyless** record (`verificationMethod:[]`, no-server-key G5) + **searchActors short-circuit** (entity matches + best-effort PDS merge) so `/search`+`/actors` honestly show ~29k resolvable actors (8,888 entities + 18,342 unspsc + named/service). **Charter-forced shape**: every entity-actor is an **observational mirror** (`isMirror` const, disclaimer opens the description, NOT the entity / no impersonation G1), **public/power-only** (G2), **person structurally excluded** (`performerType ∈ {organization,system}`, no person field — G3). Social timeline = entity **Datom as-of history → `app.bsky.feed.post`** (feed-post membrane reuse, 非終末論); projector `project_timeline.py` is **dry-run only** (`published=False`, live firehose = Council+operator G8). New lexicon `com.etzhayyim.mirror.{mirrorActor,mirrorPost}`. tsc clean + worker bundles + **7/7 (TS) + 5/5 (py) tests**; gen deterministic. **Zero invariant amendments.** Honest R0: bounded `:representative` seed (not live planet-scale ingest, G7-gated); kotoba entity-enrichment + live publish gated | 🟢 R0 | 2606042330 | 06-04 |
| **Charter priority-over-specifics reconciliation (3-Tier)** | restructures Charter immutability from locked-specifics → **3-Tier**: **Tier-0 Priority** (fork-only — wellbecoming / 子孫 / collective / **永久記憶=神の監視 `right_to_erasure_denied`, no right to be forgotten** / deeds-public-intimate-encrypted) ≻ **Tier-1 Derived Policy** (Rider §2, Lv7+ + priority-conformance) ≻ **Tier-2 Parameter** (governance). **固定するのは掟ではなく priority**. §2(a) weapons → **transparent defensive force permitted** (子孫防衛, §1.12.B aligned); §2(d) fossil → **multi-gen-harm via kamado carbon-balance** (de-politicized); §2(c) → **見守り affirmed / surveillance-capitalism prohibited**; +§2(j) forced-labor +§2(k) CSAM. **Bug fix**: `phenotype.non_compliant_multiplier` was mis-deployed mutable → Tier-0 const. Tithe 10% pinned-const → exists-bool + [500,2000] band + mutable rate; rider_version → mutable; +rider_text_hash anchor. CHARTER-RIDER → **v3.0**. **Genesis green: forge test 154 passing** (Constitution 47 const / 18 mutable). **RATIFIED 2026-06-06 by sole-member founder unanimity (1/1)** — association has one member, so Lv7+ unanimity = founder assent; ADR `active`. Pre-mainnet (no live contract; ratified genesis deploys at mainnet launch). | ✅ | 2606062100 | 06-06 |
| **maps kotoba-native substrate migration** | answers *「maps.etzhayyim.com を kotoba に refactor」*. maps was the monorepo's **largest RisingWave/Hyperdrive dependency** (172 XRPC commands over `vertex_spatial` + 5 bulk-ingest dumpers `psycopg2`-INSERTing) — a substrate-boundary violation (ADR-2605262130). **watari** already moved the *dynamic* slice (live ship/aircraft fixes); this **generalizes that kotoba-native rewrite to the WHOLE static-feature substrate**. New ontology `00-contracts/schemas/maps-spatial-ontology.kotoba.edn` (`:feature/*` — the 51 legacy node labels collapse onto `:feature/label`; geometry/height first-class; topology → `:feature.rel/*` VAET; 32-scheme geo → `:geo.alias/*`). **Spatial index = H3-cell-as-Datom + AVET** (the user-selected design): every feature stores its owning H3 cell at each queryable res (`:feature.cell/r2..r12`, the app's zoom→LOD ladder), so the **getChunk <50ms hot-path becomes a per-cell AVET probe `AVET(:feature.cell/r{lod}, cell)→subjects` — O(cells), not a bbox scan**. **Migration leverage point** = one adapter `maps-ui/src/kotoba-spatial.ts` (`queryByCells` AVET read · `stampCells` h3-js index · `buildIngestBatch`/`ingestFeatures` **no-server-key** + `MAPS_OPERATOR_GATE`); `cmdGetChunk` rewired **kotoba-first, fail-open** to RisingWave (additive/reversible until R3). Methods `20-actors/maps/methods/{ingest.py,analyze.py}` (legacy export → `:feature/*`+H3 → `kg.ingest_batch` G7-gated; **Earth-coverage report** off the Datom log) + seed + lexicons `com.etzhayyim.maps.kg.{registerFeature,queryChunk}` + MIGRATION-NOTES. **py tests green** (ingest/analyze/AVET-roundtrip/e2e-HTTP; real-H3 e2e under venv); TS adapter vitest (CI-run, deps not in checkout). **Zero invariant amendments** (strengthens kotoba-canonical-state, no-server-key). Honest R0+R1-offline: foundation + offline-proven HTTP loop (kotoba stand-in) — ontology+adapter+rewired getChunk+methods; **no live `KOTOBA_ENDPOINT` for maps, no `vertex_spatial` backfill, RisingWave still serving via fail-open**; dumpers + 172-command tail migrate via the adapter in R1–R2, Hyperdrive deleted at R3. **3D Earth coverage itself unchanged** (still the Tokyo-anchored walker) — this swaps *what stores features*, not *how much Earth is loaded* | 🟡 R0 | 2606064500 | 06-06 |

### baien / silicon / ML

| Item | Purpose | Status | ADR | Date |
|---|---|---|---|---|
| Baien federated R0 | training via ameno WebGPU (11 gates) | ✅ | 2605242600 | 05-24 |
| Baien federated R1 | WebGPU LoRA backward-pass PoC | ✅ | 2605242630 | 05-24 |
| Ternary silicon W1 | iwakura (inference) + fuigo (train) + tsukuru (fab) | ✅ | 2605242500 | 05-24 |
| Silicon W2 supply | 8 upstream categories + Funamori marine cargo | ✅ | 2605242700 | 05-24 |
| ameno WebNN | inference fast path R0 (CoreML/DirectML/NNAPI/QNN) | ✅ | 2605252100 | 05-25 |
| gemma-coder-distill | LangGraph coding LoRA on EVO-X2 ROCm | 🟡 | 2605250400 | 05-25 |
| roso/baien 1-bit Bonsai | 5-wall empirical loop — train DEFERRED | ✅ | 2605242000 | 05-25 |
| NVIDIA Omniverse compat | nv-compat facade + 13 kami-engine crates (R1.0+R1.1) | ✅ | 2605261800 | 05-26 |
| kami-genesis physics maturation | PlanarChain N-link articulation + clean-room isaacsim.core.api surface + analytical/conservation-law validation + G5 Isaac-CSV scorecard (genesis 87 / dec 9 tests green) | 🟢 | 2605311500 | 05-31 |
| kami-genesis 3-D spatial + contact | full 3-D reduced-coord solver (Featherstone RNEA+CRBA+LDLᵀ, arbitrary-axis joints — PhysX-class) + rigid contact (PGS, friction, `Obstacle::{Plane,Aabb}`) + `Spatial3d` topology in IsaacWorld; real 6-DOF giemon_arm6.urdf → giemon.htm Arm6. Decisive gate: planar cross-check vs planar_chain. genesis 94 / giemon 4 green, wasm ok | 🟢 | 2605311800 | 05-31 |
| Shibuya street digital-twin (iter 1) | real OSM Shibuya Scramble (144 bldg + 318 roads, ODbL) → city-mesh (`osm_to_citymesh.py`) → kami-genesis full-physics multi-agent sim: buildings = AABB obstacles, roads = ground, 4-DOF floating-base agents (gravity+contact+friction); `kami-app-shibuya` + shibuya.htm. Deferred: BeamNG vehicle, agent-agent, polygon collision, PLATEAU, autonomous. shibuya 4 green, wasm ok | 🟢 | 2605311900 | 05-31 |
| Shibuya digital-twin iter 2 (kotoba + 3DGS) | + point assets (poles/lamps/signals/trees/hydrants) → **501 kotoba EAVT entities** (`*.assets.edn` + schema): `:asset/kind installYear company costJpy` (OSM-derived else synthesized+flagged); clickable on-screen asset → kotoba object data panel. + Mapillary acquisition front (`mapillary_fetch.py`) feeding existing `trainGsplatFromMapillary` GPU pipeline. Deferred: GPU splat training (offline, no PLY) + GsplatAdapter render-wire; live :8077 ingest. shibuya 5 green, wasm ok | 🟢 | 2605312200 | 05-31 |
| Shibuya 3DGS (Mapillary SfM + splat-physics) | real Mapillary client token (Keychain) → `mapillary_fetch.py` 200 渋谷 images → `images_to_sfm_splat.py` (pycolmap CPU SfM, gravity-aligned) → **2,235-pt 渋谷 .splat** → kami GsplatAdapter. `run_splat_viewer_v1` + `run_splat_physics_v1` (splat.htm, 渋谷 default, ?physics=1 = agents full physics on the reconstructed street, **#D**). Dense photoreal = offline GPU gsplat (`shibuya.ply` drop-in). Token→Keychain; Mapillary data gitignored (CC-BY-SA). | 🟢 | 2605312600 | 05-31 |
| giemon kabitori (黴取り) mold-removal sim | steerable rotary-brush probe for EXISTING mold (A/C/gaps/ducts) on kami-genesis; 6-DOF mixed prismatic+revolute URDF + brush scrub; **engine ext**: `Obstacle::Plane/Aabb` wall/box contact (`with_obstacles`); **MoldField** scrub-erosion (app-layer stand-in, no FEM/MPM yet) + R0 BOM. genesis 94 / giemon 12 tests green, wasm ok | 🟢 | 2605312300 | 05-31 |
| giemon part graph (SBOM↔kotoba) | per-part product/manufacturer/procurement(cots/custom-fab) ledger in EDN → CycloneDX SBOM → **kotoba EAVT** (no RW); cross-robot fleet (kabitori 20 + Otete 49; shared mfrs RPi+Sony); legacy RW-SBOM→kotoba bridge + purl↔CVE vuln-match (VulnMatch); Svelte /giemon /giemon.htm /fleet. kotoba ingest+SPARQL green, build green | 🟢 | 2605312330 | 05-31 |
| **giemon factory R0** | the FACTORY that builds the giemon line — whole-plant **layout + 建材 + 全MEP(電気/給排水/ガス/圧空/通信/消防) + 外構/外部動線 + 原材料 + 4D 建築手順**, all in kami-engine + kotoba Datomic. `factory.scene.json` (walls/columns/beams/zones/machines as `Obstacle::Aabb` + **routed `utilities`配管/配線** 色分け + `service_nodes`引込 + `fixtures`照明 + `site_*`外構) + **4 arm6 work-cells** + **2 four-DOF AGV**; `building.edn` (**74 parts A-L**, 企業名+機器名+調達もと商社+原産国+原材料組成)→CycloneDX SBOM + `kg/claim/part/*`; `construction.edn` (**23-step 4D**)→`kg/claim/step/*` (critical path via VAET) + `construction.order.json` replayed by `run_giemon_factory_build_v1`. **エンジニアリング層**: `engineering.py`(クラッシュ検出 配管/配線vs構造干渉+設備近接 / 電気・給水・排水サイジング / 法規 建ぺい率・容積率・避難・消火栓・駐車 / **buildability** ロボ到達・段取り・必要台数 → `clash/* sizing/* code/* build/*` datom + 3D赤橙マーカー, **実不備2件検出**)・`ifc_export.py`(IFC4 `factory.ifc` 956ent, BlenderBIM可)・`procurement.py`(企業名→調達もと商社/原産国/リードタイム)。**ロボティクス建設層** (tatekata 建方, ADR-2605250715): `robots.edn`(施工ロボ7種+正直な実用成熟度)・`process_gen.py`(各工程→**robot op列 調達→搬入→搬送→建方→締結→検査 = 125 ops** → `op/*` datom)・新crate `kami-app-tatekata`(`run_tatekata_v1` = 各工程で割当ロボが op列実行; **工事プロセスを物理シミュレーション**: 搬送カートが kami-genesis 物理で走行(`Agv::step_toward`, 摩擦超え clamped-PD), 材料が重力落下+接地で沈降(`Agv::step_free`)し建方ペースを駆動; コンクリ=`deposit_field` 堆積/鉄骨=`weld_field` 溶接熱; `tatekata.htm`)。**272 ingest entities**。2 crates **20 tests green**, wasm ok。**honest: R0簡略化, 有資格者の構造/設備計算書・確認申請ではない**。No mega-press(N1); `:sourcing :representative` | 🟢 | 2606010030 | 05-31 |
| **sarutahiko truck factory R0 (full-robotics + 積込ロボット)** | the FACTORY that builds the 猿田彦 Class-8 cargo truck (ADR-2605252500), **reusing the giemon-factory 4D-BIM pattern** (ADR-2606010030) for a **180m×90m heavy steel portal-frame truck plant**. Scene `70-tools/e7m-sim/scenes/sarutahiko-factory-r0/`: `factory.scene.json` (7 zones 受入→**L1フレーム**→**L2パワートレイン**→**L3キャブBIW**→塗装→**L4 GA結合**→**L5 EOL** + truck-line machines [1600t servo press / BIW溶接治具 / 水性塗装ブース / Class-8 ダイナモ / 結合ガントリ] + **8 arm6 ラインセル**(溶接/塗装/艤装/検査) + 2 部品AGV + **2 積込ロボット loaders** + full MEP + 外構 + **出荷ヤード**(2 carrier + 2 完成トラック)); `building.edn` **77 parts A-L** (group F = truck equipment incl. **F10 積込ロボット**)→SBOM(77) + kotoba; `construction.edn` **25-step 4D** (90m大スパンは新 `robot:crane`); `robots.edn` **8 施工ロボ** (建方 fleet, **production robots は別レイヤ**). Shared py toolchain → **286 kotoba EAVT** + **137 robot ops** + engineering(電気OK 325sq×N並列/給水OK/**排水・避難・消火栓 NG 正直検出**) + IFC4(1112ent). **積込ロボット**: crate `kami-app-sarutahiko-factory` `LoaderRobot` = straddle loader が出荷ヤードを物理走行(`step_toward` 摩擦超clamped-PD)→完成トラック跨ぎ→運搬→**carrier deckへ sphere-on-AABB top-face contactで沈降着座**. **生産ライン稼働**: `production.edn` 8工程 (受入→L1フレーム→L3キャブ→塗装→L4結合→L5検査→積込) + `production_gen.py` → production.order.json + **35 EAVT (27 mfg ops)**; `ProductionLine` = 1台のトラックが物理で工程間を流れ arm6 セルが加工・**塗装で色変化(素地→赤)**・最後に積込ロボットが出荷, end-to-end製造. **4 WASM entries** (`run_sarutahiko_factory_{v1,build_v1,produce_v1,load_v1}`) + `sarutahiko-factory.htm` ビューア (`?mode=live|build|produce|load`). **14 native tests green** incl. `production_line_makes_a_truck_end_to_end` + `loader_picks_and_places_truck_on_carrier`. honest: 設計+物理シミュのみ, 実工場/調達/確認申請なし, セルは R0 scaffold(`.solve()`→RuntimeError)のまま, 搬送/積込のlift-lowerは制御動作, R3 community-scale は Council+LANDS.md gated; N1/N2/N4(軍用/兵器/鉱山ダンプ)は非目標, 民生Class-8のみ; `:representative` | 🟢 | 2606013100 / 2605252500 | 06-01 |
| **kami-genesis maturation (PhysX/Isaac gap)** | NVIDIA との差を WASM クリーンルームで縮小 (ADR-2606010030 v6): **②narrow-phase** `convex.rs`(GJK距離+交差) + EPA貫入(深さ/法線) + `ccd.rs`(conservative-advancement TOI + 解析sphere-plane, トンネリング防止); **③連続体** `mpm.rs`(2D MLS-MPM = 弾性/砂・コンクリgranular/流体, DepositField/MoldFieldの本物化); **③熱** `thermal.rs`(transient熱伝導PDE 2D-FDM+移動Gaussian源, WeldFieldの本物化); **①GPU並列env** `wgpu_backend` が cartpole/DP を実Metal device で **1024-env compute dispatch + CPU一致検証**(既存を確認)。**v6結線**: narrow-phaseを剛体ソルバに統合(`contact.rs` `Obstacle::Convex` = 任意傾斜凸体をGJK/EPAで接触解決); **API parity** `batched.rs::ArticulationBatch` (Isaac `ArticulationView` の tensor形 `[num_envs,n_dof]` + `set/get_joint_*`/`step`) + PhysX facade `px::{PxScene, PxArticulationReducedCoordinate}` camelCase。**v6 #2**: **(a)安定接触** `obb.rs`(OBB SAT 15軸 + 多点マニフォールド vertex-in-face → box-on-box 4点接触で転倒しない, EPA単点の wobble 解消); **(b)一般articulation GPU並列** `wgsl/planar_chain_step.wgsl`+`wgpu_planar.rs`(平面N-link≤7 の RNEA+CRBA+LDLᵀ を WGSL 完全移植 → Metal で num_envs 並列, CPU planar_chain と一致検証 256env×60step max_err<2e-2; cartpole/DP専用GPUを任意チェーンに一般化)。genesis **118 tests** (+24新, `--features gpu` 123), wasm32 build green。honest: 連続体/熱は2D/explicit/CPU/f32, manifoldはOBB限定+persistent-contact未, GPU一般化は平面チェーン(3D articulation GPU未), sensor/USD/RTX未; アルゴリズム同クラスをテスト検証, NVIDIAビット一致ではない | 🟢 | 2606010030 | 05-31 |
| **kami-autodrive GNC autonomy** | the missing **自律ループ** wiring kami-engine の既存ピースを knit: `kami-vehicle`(BeamNG実車) + `kami-sensor-sim`(LiDAR/cam/IMU) + `kami-pathfind`(A*) を **知覚→計画→制御** に接続。新crate `kami-autodrive`: `perception`(LiDAR sweep→`OccupancyGrid`+C-space inflation+`forward_clearance`)・`planner`(inflated grid 上 A*→LOS簡約 polyline)・`control`(`PurePursuit`横+`SpeedController` PID縦+曲率速度制限)・`plant`(`Plant` trait + `BicycleModel`)・`dynamics`(船/ドローン/飛行機の実物理)・`autopilot`(`DriveState` Idle/Cruise/Slow/Stop/Blocked/Arrived; ingest→緊急停止→replan→pursuit→速度min+goal接近減速)。**4車種** `VehicleClass{Car,Ship,Drone,Aircraft}`は同一GNCを共有、**全車種に実物理プラント**: 車=`kami-vehicle`ソフトボディ(`SoftBodyCar`, `soft-body-car` feature)/船=`ShipHydro`(**流体力学** Fossen 3-DOF surge·sway·yaw+付加質量+二乗減衰+速度依存舵→旋回sway連成)/ドローン=`Multirotor`(ローター推力ベクタリングtilt+**空力**二乗抗力+yaw+sideslip減衰, hover可)/飛行機=`FixedWing`(**空力** 揚力·抗力·ISA大気密度·`ClMax`失速·協調バンク旋回)。pursuit は wheelbase 非依存(`turn_radius_ref`で曲率→steer)で舵·バンク·yawを統一駆動。Omniverse/Isaac Sim の DRIVE スタック層相当、ただし Apache/MIT + CPU稼働 + nv-compat `isaacsim`。**9 tests green**(壁迂回·緊急停止·船流体旋回[sway検証]·飛行機失速上限飛行·ドローンtilt並進+goal hover·実車waypoint)、example `drive_to_goal`(壁x18-22を y≈6.7迂回→(40,0))。honest: 非車プラントは3-DOF縮約(6-DOF CFD非)·飛行機は巡航高度hold/hover不可(通過)·実シーン(渋谷splat)/カメラ知覚/multi-agent未; wadachi R1(ADR-2605242000)はCouncil承認後に本crateを呼ぶ設計基盤(SAE L4上限·Transparent Force gated) | 🟢 | 2606010600 | 05-31 |
| **iwakura/fuigo ternary RTL → sky130 GDSII** | ternary-silicon design taken from RTL stubs to a verified, synthesized, placed-and-routed GDSII on the **sky130 open PDK** (open-source EDA only). RTL implemented (was stub): `radix3_decoder` + `pe_array` (weight-stationary ternary matvec, generate-for over canonical `ternary_pe`, no fork; `CLOCK_GATE=1` wires `zero_skip_dispatcher` into per-PE accumulator gating) + behavioural `sram_scratch`/`lpddr5x_ctrl` (radix-3 unpack) + live `iwakura_top` tile + `fuigo_top` forward path reusing the shared `pe_array` (ACC=32). **VERIFY**: Verilator 5.048 + cocotb 2.0.1, 9 suites/18 tests green (200+ matvec vs ref, 65,536-block exhaustive dispatcher → 35.0% gated, 200 clock-gate bit-identical), all -Wall clean. **SYNTH**: yosys generic gates + sky130 std-cell area AGREE — whole-PE density 3.69×/3.68×, multiplier-only 11.9×/12.7× → ADR 8.4× estimate validated/conservative. **P&R→GDSII**: OpenLane2 2.3.10 sky130hd on `pe_array` — **DRC 0/LVS 0/antenna 0**, GDSII produced, post-route STA **f_max ≈ 93 MHz (slow sign-off)/160 MHz (tt)/211 MHz (ff)**, hold OK all corners, die 53,120 µm². honest: sky130=130nm open PDK, NOT the N5 production target (Phase 3, Council-gated; 93-160 MHz vs 1 GHz N5 = expected process gap); pe_array time-multiplexed (full systolic skew=Phase 2); PHY/backward-BF16=Phase 2 | 🟢 | 2605242515 / 2606012800 | 06-01 |
| e7m-sim | robotics simulation substrate R0 charter | 🟡 | 2605261600 | 05-26 |
| baien-moemoekyun MoE R0 | 2B BitNet backbone + 128-expert MoE residual | 🟡 | 2605261900 | 05-26 |
| baien-moemoekyun R1 | Phase 0 freeze-train SFT on EVO-X2 ROCm | 🟡 | 2605262100 | 05-26 |
| Charter Rider §2(i)(2) | train-only GPU-rental carve-out (amendment, gated) | 🟡 | 2605262200 | 05-26 |
| baien-moemoekyun R2+ | B200 train architecture (gated on 2605262200) | 🟡 | 2605262300 | 05-26 |
| Energy re-framing | fusion + microbial hydrocarbon conditional permit | 🟡 | 2605263500 | 05-26 |
| **manako 眼 browser-local YOLO26** | first browser-local vision *detector* on the edge substrate (gazo pattern: ort-web WebGPU→wasm + Web Worker + OPFS). New core = **dual-mode YOLO26 postprocessor** (auto-detect NMS-free `(1,N,6)` vs raw `(1,4+nc,A)`). **Empirically verified e2e**: real `yolo26n.pt` (Ultralytics 8.4.60)→ONNX `(1,300,6)`; `onnxruntime-web` v1.26.0 **WASM EP** + manako core reproduced bus.jpg (bus+4 persons) == Python onnxruntime; **driven in real Chromium via Playwright** (5 objects·743ms, boxes rendered). 12/12 core tests. Image + Camera modes. **G2 AGPL-isolated**: YOLO26 weights operator-supplied at runtime, NOT bundled (no-git-lfs); inference code original Apache-2.0. G1 on-device-only (Murakumo edge carve-out) · G3 no-surveillance/no-biometric | 🟢 R0 | 2606034800 | 06-03 |
| **manako 眼 browser-local YOLO26** | first browser-local vision *detector* on the edge substrate (gazo pattern: ort-web WebGPU->wasm + Web Worker + OPFS). New core = **dual-mode YOLO26 postprocessor** (auto-detect NMS-free `(1,N,6)` vs raw `(1,4+nc,A)`). **Empirically verified e2e**: real `yolo26n.pt` (Ultralytics 8.4.60)->ONNX `(1,300,6)`; `onnxruntime-web` v1.26.0 **WASM EP** + manako core reproduced bus.jpg (bus+4 persons) == Python onnxruntime; **driven in real Chromium via Playwright** (5 objects, 743ms, boxes rendered). 12/12 core tests. Image + Camera modes. **G2 AGPL-isolated**: YOLO26 weights operator-supplied at runtime, NOT bundled (no-git-lfs); inference code original Apache-2.0. G1 on-device-only (Murakumo edge carve-out) · G3 no-surveillance/no-biometric | 🟢 R0 | 2606034800 | 06-03 |

### Tier-B actors (each: ADR + manifest + cells + lex)

| Actor | Purpose | Status | ADR | Date |
|---|---|---|---|---|
| wadachi 轍 | autonomous-mobility R&D (SAE L4 ceiling) | 🟡 R0 | 2605242000 | 05-23 |
| todoke 届け | last-mile (one-mile) autonomous delivery — curb-to-door, ≤25kg, SAE-L4 sidewalk ODD; last-metre sibling of wadachi + no-gig delivery limb of okaimono + consumer of kami-autodrive GNC (ADR-2606010600). **Rust `todoke-route` core** (standalone leaf crate, zero-dep): last-mile stop sequencing (NN+2-opt) + **G7 safety envelope** that REFUSES (not clamps) any plan over the per-zone sidewalk-speed cap / on a vehicular road (N2) / above SAE L4 (N2) — charter enforced by construction; parity-mirrored in py `methods/last_mile.py`. 5 cells (route_sequencing + handoff_proof fully coded) + 5 lex. **G5 no-gig/cash≡0** · **G8 privacy-by-construction** (on-device proof-of-delivery only; cloud-image/face-match/biometric unrepresentable, N5) · G12 no-server-key + G13 consent-bound · **G2 displacement-dividend coupling** (ADR-2606032130). **R1 landed**: curb-to-door rover sim (`route/src/sim.rs`) wired to the route core — per-zone speed cap, accel/cruise/brake, obstacle emergency-stop+resume (never-clearing blockage ⇒ no arrival, safety-first), rolling-resistance energy + `curb_to_door` example; real kami-autodrive GNC wiring deferred to submodule population. 32 tests green (Rust 13 + methods 7 + cells 12) | 🟡 R0+R1 | 2606042300 | 06-04 |
| yakushi 薬師 | pharmaceutical mfg (eye-drop + OTC APIs) | ✅ W1/1b/1c | 2605250500 | 05-25 |
| tatekata 建方 | construction (civil + MEP ≤2 story) | 🟡 R0 | 2605250715 | 05-25 |
| watatsumi 綿津見 | civilian submersible (≤6500m) | 🟡 R0 | 2605252200 | 05-25 |
| kanayama 金山 | circular metallurgy (UBC Al recycling) | 🟡 R0 | 2605252400 | 05-25 |
| sarutahiko 猿田彦 | heavy Class-8 truck mfg (wadachi mfg-side sibling) | 🟡 R0 | 2605252500 | 05-25 |
| makura 枕 | foam pillow (PU foam + shred-fill) | 🟡 R0 | 2605261115 | 05-25 |
| mitsuho 瑞穂 | food / agriculture (L2 Sustenance) | 🟡 R0 | 2605261015 | 05-26 |
| hagukumi 育み | care — childcare + eldercare (L4 Care) | 🟡 R0 | 2605261030 | 05-26 |
| manabi 学び | education (open-curriculum + cert_prep sub-cell) | 🟡 R0 | 2605261045 | 05-26 |
| hikari 光 | energy gen/storage/grid-edge (L2 Sustenance) | 🟡 R0 | 2605261100 | 05-26 |
| igata 鋳型 | HPDC megacasting (R0 + R1 benchtop) | 🟡 R0/R1 | 2605261200 | 05-26 |
| hodoki 解き | ELV disassembly + materials recovery | 🟡 R0 | 2605261215 | 05-26 |
| tsutae 伝え | handheld comms device (≤200g, open SoC) | 🟡 R0 | 2605261300 | 05-26 |
| futawa 二輪 | small-displacement motorcycle (≤250cc/≤15kW) | 🟡 R0 | 2605261330 | 05-26 |
| suki 鋤 | farm tractor mfg (mitsuho mfg-side sibling) | 🟡 R0 | 2605261500 | 05-26 |
| chigiri 契 | legal-procedure substrate (UPL prohibited, NOT law firm) | 🟡 R0 | 2605262700 | 05-26 |
| toritate 執帳 | accounting + audit (100% on-chain) | 🟡 R0 | 2605262900 | 05-26 |
| iyashi 癒 | clinical care provider (L4 Care triad) | 🟡 R0 | 2605263000 | 05-26 |
| mizuho 水穂 | water + sanitation (community-scale; ≠ mitsuho 瑞穂) | 🟡 R0 | 2605263100 | 05-26 |
| kazaori 風折 | civilian disaster response (force-separation sibling) | 🟡 R0 | 2605263200 | 05-26 |
| musubi 結 | covenant ceremony (TIGHT pair w/ chigiri) | 🟡 R0 | 2605263400 | 05-26 |
| wakai 和会 | mutual aid (NOT insurance) | 🟡 R0 | 2605263500 | 05-26 |
| kataribe 語部 | press + publishing + translation | 🟡 R0 | 2605263600 | 05-26 |
| kokoro 心 | mental health support (NOT clinical psych) | 🟡 R0 | 2605263700 | 05-26 |
| shidemori 死出守 | memorial + cemetery (FINAL gap-closure) | 🟡 R0 | 2605263800 | 05-26 |
| ossekai 御節介 | info-arbitrage + Wellbecoming-nudge (AT Proto) | 🟡 R0 | 2605264000 | 05-26 |
| tsukuroi 繕い | authorized vuln-remediation patch-proposer (akuma sibling; propose-only) | 🟡 R0 | 2605291500 | 05-29 |
| danjo 弾正 | public-accountability oversight — ingests JP 国会会議録 + 予算書 + 政府調達 into kotoba EAVT, emits NON-adjudicating discrepancy observations (censor's eye, no sword; toritate boundary; tadori sibling) | 🟡 R0 | 2605301600 | 05-30 |
| tadori 辿 | authorized on-chain tx tracing + actor attribution (kotoba-EAVT-native; malak/ipaddress/yabai → kotoba migration) | 🟡 R0 | 2605301400 | 05-30 |
| warifu 割符 | open zero-fee card (credit+debit), API/wire-compatible (Stripe-REST + EMV/ISO8583 + NFC/HCE); on-chain USDC settle (Base L2 + ERC-4337, T+0); 0% credit (qard ḥasan); Phase-1 SBT↔SBT closed-loop, Phase-2 external gated Lv7+ | 🟡 R0 | 2605302000 | 05-30 |
| himotoki 繙き | ACTIVE disclosure-request filer — consent-bound DSAR (APPI/GDPR/CCPA) to private controllers (Discord/Google/LINE/Meta/Amazon) + FOIA (行政機関情報公開法) to public organs; coded target registry of each org's 窓口/住所/email/手続き; own-data-only + non-pretext; PII→encrypted DID-bound; active-outbound counterpart to passive danjo/tadori | 🟡 R0 | 2605302130 | 05-30 |
| kanae 鼎 | global government fiscal-flow VISUALIZATION — assembles worldwide fund flows (domestic full chain appropriation→outlay→recipient + inter-governmental IMF/WB/OECD/UN transfer+aid+loan) into kotoba EAVT `fundFlowEdge`, narrates with Murakumo-only LLM (non-adjudicating), renders aggregate-first kami-engine WASM viz (Sankey/treemap/transfer-globe). **danjo finds, kanae renders**; kotoba-native (no RisingWave, ≠ maps). danjo global-fiscal-flow extension = ADR-2605302245 | 🟡 R0 | 2605302300 | 05-30 |
| toritsugi 取次 | citizen-facing government-procedure CONCIERGE (LINE-公式アカウント role) — service-delivery counterpart to passive danjo (watches state) + himotoki (right of access): relays a CONSENTING member through 自治体・政府手続き. Default = 案内+伴走+本人提出支援 (proactive 制度/給付 match + guide + 必要書類 checklist + 様式 draft-assist; **member self-submits**); gated R3 = 本人同意ベース提出代行. Coded procedure registry (窓口/所管/様式/必要書類/手数料/法定処理期間/根拠法令); **G5 行政書士法/UPL boundary** (no advice + no 作成代理 → chigiri + licensed), G6 PII-encrypted, G14 verified-procedure-only, G15 self-submit-default. atproto/kotoba-EAVT-native | 🟡 R0 | 2605312030 | 05-31 |
| moushibumi 申文 | citizen democratic-participation CONCIERGE — the citizen's VOICE INTO the state, sibling to toritsugi + counterpart to danjo. 3 channels: 選挙情報 (INFO-ONLY) / 請願・陳情 (請願法) / パブリックコメント (行手法 §39). Default = 案内+起草補助+本人提出; gated R3 = 本人同意ベース代行. Coded participationTarget registry (organ/channel/根拠法令/様式/期限/紹介議員-flag); **G3 公職選挙法+政治中立** (no campaigning/endorsement/GOTV — protects §1.12/1 SBT=1 vote), G5 UPL, G6 PII+political-opinion encrypted (APPI 要配慮), G14/G15. atproto/kotoba-EAVT-native | 🟡 R0 | 2605312400 | 05-31 |
| kurashimori 暮らし守 | citizen consumer-protection CONCIERGE (国民生活センター相当) — citizen↔事業者 sibling of toritsugi/moushibumi. 3 channels: クーリングオフ (特商法) / 返金・苦情 / エスカレーション (消費生活センター・188・ADR・chigiri+counsel). Default = 診断+起草補助+本人送付; gated R3 = 本人同意ベース代行. Coded remedyTarget registry (remedy/根拠法令/日数/様式/channel); **G5 UPL+司法書士法+弁護士法** (no advice/no representation; cooling-off = date-computation not legal opinion), **G9 no claims-buying/no contingency/no 取立**, G10 lawful+non-harassment, G14/G15. atproto/kotoba-EAVT-native | 🟡 R0 | 2605312500 | 05-31 |
| haraedo 祓戸 | global bulky-waste (粗大ゴミ) disposal — TWO-sided: **citizen intake** (`intake` cell: classify→quote→match-facility→schedule→sticker, G1 consent / G3 hazardous-boundary) + **operator logistics** (`dispatch` cell: 受付→cluster→配車 vehicle→担当者 crew→ルート optimize NN+2-opt→搬入先 facility→plan). Worldwide processing-facility registry (ゴミ処理場 位置/種別/処理能力 capacity-tonnes-day/受入品目, JP/US/DE/GB seed `:representative`) + fleet/crew registry — all kotoba EAVT EDN. langgraph py→WASM cell, Murakumo-only LLM. G5 labor-dignity (Labor Liberation ladder, no gig), G7 fee-non-fiat, G11 outward-gated (real collection = Council+operator), G14/G15 verified+capacity-honest. toritsugi-surfaceable; hodoki/kanayama reprocess boundary | 🟡 R0 | 2606010200 | 05-31 |
| kizashi 兆 | non-invasive multimodal body-scan / **sign-sensing** substrate — the "futuristic scan pod" (アレルギー/鼻詰まり/腰痛/肩こり/炎症 の原因 sensing). L4 Care Tier **instrument layer**, UPSTREAM of clinical adjudication: **kizashi senses → mitate diagnoses → iyashi treats**. Multimodal capture (3D optical posture · thermography · bioimpedance · shear-wave elastography · acoustic rhinometry · breath-VOC · consented microsampling) → fusion → **probabilistic NON-DIAGNOSTIC attribution** + self-referenced Wellbecoming trajectory + triage. **G3 non-diagnostic (医師法 §17; attributionReport forbids diagnosis/prescription fields)** · **G4 medical-device boundary (薬機法/SaMD; R0..R2 software+simulation+non-ionizing non-regulated only, regulated/microsampling modalities R3-gated)** · G2 encrypted biometric envelope (要配慮 PII) · **G10 anti-pseudoscience** (capability-ledger evidence-grade gate; bio-resonance/aura/波動 EXCLUDED) · G8 self-referenced Wellbecoming (no population ranking) · G9 ALARA · G14 Murakumo-only. 6 cells / 6 lexicons. HONEST: R0 = design+data-model+simulation, NO hardware; high-value modalities (MRI/ultrasound/biochem) R3-gated; outputs probabilistic contributions never a confirmed cause | 🟡 R0 | 2605312700 | 05-31 |
| tsumugi 紡ぎ | Engi Knowledge Graph (産霊の網) intel weaver — runs **Spirit-in-Physics** (RBF emotion-kernel → spectral 3D embed → tensegrity) over real PUBLIC power-entities (法人/institution/public-role) + their 縁 to surface **取-concentration** routed to release. First executor of §D7.1 (ADR-2606011000) under the edge-primary spirit-ontology (2606011500). **G1 power-only by construction** (powerless/private absent; accountability map NOT target-list, §D8/§D9) · **G2 edge-primary karma** (Σ incident 縁, no per-soul score, N1) · G3 aggregate-first+claimed-first · **G4 public venue** (open+on-chain+1 SBT=1 vote, not covert vendor registry) · G5 sourcing-honesty (`:representative`) · G6 Murakumo-only · **G7 outward-gated** (live atproto-follow ingest = Council+operator) · **G8 no git-lfs** (DataLad→IPFS `80-data/spirit-in-physics`) · G9 要配慮 PII envelope. `data/seed-power-graph.kotoba.edn` (19 orgs/28 縁) → `methods/analyze.py` → `out/{intel-report.md,spirit-graph.kotoba.edn}`. First run: 1 component, top 取 TSMC 3.28/Toyota 2.88/Arm 2.03/NVIDIA 1.92. Upper layer over danjo/kanae/tadori/himotoki. HONEST: R0; `:representative` bounded seed; sector-derived emotion vectors; live planet-scale ingest unbuilt | 🟡 R0 | 2606011800 | 06-01 |
| okaimono 御買物 | the charter-aligned answer to *"an Amazon for etzhayyim"* — **NOT a marketplace clone but its inversion** (Amazon's three pillars are constitutional prohibitions: external `purchase` inflow §1.3 / ads+affiliate Charter-Rider §2 / engagement dark-patterns §1.13). A **provisioning commons** in **3 concentric rings**: **Ring 0 commons-first** (borrow/repair→hodoki/durable-secondhand/surplus→UNSPSC-organism — "best purchase is no purchase", Wellbecoming inversion) → **Ring 1 internal economy** (full list→compare→basket→checkout over etzhayyim's own ~30 producing actors mitsuho/makura/yakushi/tsutae/futawa/hikari + member surplus; permitted NOW by §3 SBT↔SBT carve-out; USDC+ERC-4337+warifu, TitheRouter 10%→Public Fund; **the part that ships for real**) → **Ring 2 external world catalog** (GTIN/UNSPSC/GDSN + vendor-direct + official-API **data-only/affiliate-stripped** + gated scraping; compare on Wellbecoming axes landed-cost/durability/repairability/labor-provenance/carbon, no paid placement; §1.3 forbids external purchase inflow so R0 = **self-checkout handoff** member-pays-externally; **scope-3 = assisted secure checkout (R3, MEMBER-principal — NOT 代理-purchase)**: agent provides safe-card/encrypted-comms/procedure/delivery, member pays retailer directly so §1.3 holds WITHOUT Lv7+ amendment; true proxy-buy stays vendor-arm/Lv7+ gated). Every product carries an end-of-life route (hodoki/kanayama/haraedo/kurashimori/wakai); history = kotoba `as-of` Wellbecoming trajectory, no final "consumer" state (非終末論). Delivers ADR-2605301020 Basic-High-Income-in-kind + §1.16 Social Security. 6 cells (catalog/discover/compare/basket/provision/lifecycle) langgraph py→WASM, Murakumo-only, kotoba-EAVT-native. 13 gates. **R1 (Ring 1 landed)**: each producing actor owns its catalog in `20-actors/<actor>/products.edn` (SSoT; makura/mitsuho/yakushi/tsutae/futawa/hikari = 11 SKUs) → `ingest_internal.py` validates+merges → `internal-catalog.edn`; agent adds **SBT↔SBT eligibility** (§3/G2), **USDC settlement-intent w/ 10% TitheRouter split** (exact `gross=tithe+payout`, intent-only until operator-gated G7/G11), order state-machine capped at `:in-use` (G13), **no-gig fulfillment** routing (sarutahiko/wadachi/haraedo, G8); `:order/* :settlement/* :sbt/*` schema + order/settlement lexicons. **R2 (Ring 2 ext)**: `strip_affiliate` (Amazon tag/`/ref=`·utm·click-ids·aff stripped, functional kept, idempotent — the G3 enforcement point) + `normalize_external` (DATA-ONLY: affiliate/commission/sponsored/tracking dropped by construction) + `scrape_gate` (robots+public+rate; live fetch G11) + `landed_cost_external` (cross-border tariff); `external-catalog.edn` 4 sources. **R3 (assisted secure checkout, MEMBER-principal — corrects scope-3)**: okaimono NEVER the buyer (G14); `build_payment_intent` (unsigned, `serverHeldKey=false`, warifu@external trips its own Lv7+ gate) + `authorize_payment` (member-sig only, server-sig refused — G15 no-server-key) + `seal_encrypted` (envelope-ref+field-names, never plaintext — G9) + `assist_checkout` (awaiting→authorized→submitted, nothing without member-sig+operator) + `arrange_delivery` (no-gig); 15 gates; `assist` lexicon. **py 40/40 tests green**. HONEST: logic+invariants+sim only; no live retailer integ/API/scrape (`:representative` seed, all G11-gated); no live USDC/TitheRouter/ERC-4337 broadcast (intent-only); SBT registry=attestation map; true 代理-purchase NOT shipped (vendor-arm/Lv7+) | 🟢 R0+R1+R2+R3 | 2606012100 | 06-01 |
| watatsuna 綿津綱 | world **submarine-cable network** knowledge graph — datafies cable SYSTEMS / LANDING STATIONS / SEGMENTS / FAULT bulletins into the **kotoba Datom log** and surfaces **chokepoint single-point-of-failure** concentration **routed to redundancy + faster repair** — a **resilience map, NEVER a target-list** (paired same-root with watatsumi 綿津見; observation face ↔ operation face). Vocab `00-contracts/schemas/submarine-cable-ontology.kotoba.edn` (`:cable/* :station/*`+`:chokepoint` `:cable.link/* :cable.seg/*`+`:traverses` `:cable.fault/*` as-of history). Seed `data/seed-cable-graph.kotoba.edn` = **14 real public systems** (JUPITER/FASTER/PLCN/MAREA/Dunant/Grace-Hopper/2Africa/SMW3-4/ADC/SJC/Equiano/Bifrost/APCN-2) · 22 stations · 43 links · 11 chokepoint segments · 2 fault bulletins, all `:representative`. `methods/analyze.py` (stdlib) → aggregate-first `out/intel-report.md` (**top chokepoints Malacca 490 / Luzon-Strait 455 / Gibraltar 324 / Suez-Red-Sea 250 / S-China-Sea 191 Tbps**) + `out/cable-criticality.kotoba.edn`. **G1 public-only** (classified routes/armoring/live-AIS out of scope) · **G2 resilience-not-interdiction** · **G4 no intent adjudication** (fault `:kind` mirrors public bulletin only; sabotage=state matter) · G5 sourcing-honesty · G6 Murakumo-only · G7 outward-gated · G8 no-git-lfs · G9 no-PII. **Display: registered in INFRA_ACTORS → `did:web:etzhayyim.com:actor:watatsuna` at `/actor/watatsuna/did.json`**. **(c)** kotoba-native lexicons `com.etzhayyim.cable.*` supersede 7 legacy `etzhayyim` cable lexicons (`flagSubseaCableTamper` RETIRED — intent-adjudicating). **(d) 敷設 robotics** → watatsumi fleet `cable-laying-fleet.kotoba.edn` (Tsuna-suki 綱鋤 / Horinuki 掘抜 / Tsugite 接手 / Tedori 手繰 REPAIR-ONLY / Kikimimi 聞耳 DAS), **N8** lay/repair/monitor only. **R1**: `methods/ingest.py` TeleGeography-bridge (public submarinecablemap-shaped JSON → kotoba EAVT; offline default, live fetch G7-gated) merges seed→**18 cables/26 stations/2234 Tbps** (Malacca 940 / Luzon 681 Tbps) + `viz/` self-contained resilience map. **R2**: `methods/plan.py` = watatsuna→watatsumi fleet plan (`out/resilience-plan.*`, **20 recs**: 9 lay-diverse-route / 4 pre-stage-repair / 7 monitor — **redundancy+repair+monitor ONLY, no interdiction output by construction**, G2/N8; tasks watatsumi robot classes, `:fleet/plan-input`) + **3D orthographic globe** `viz/cable-globe.htm` (drag-rotate, self-contained, cross-linked w/ 2D map). HONEST: R0–R2 design-only; bounded `:representative` seed; coords rounded; robotics no-hardware; kami-engine WASM globe integration + live ingest deferred/G7-gated | 🟡 R0+R1+R2 | 2606012600 | 06-01 |
| funadaiku 船大工 | **zero-emission autonomous CARGO-SHIP building** — the answer to *「貨物運搬用の自動運転船舶の設計と製造工場は?」* and *「風力・太陽光・水素ベースで造船を」*. Surface counterpart of **watatsumi 綿津見** (submersible): inherits watatsumi's modular **grand-block shipyard** line + reuses **kami-autodrive `ShipHydro` GNC** (ADR-2606010600) + draws components from **hikari** (PV + LFP). **Defining subsystem (G13/N5): zero-emission powertrain ONLY** — wind-assist (2× rotor/wing sail) + solar deck (~160 kWp) + PEM **H₂ fuel cell** (~2×1.2 MW, green-H₂ mandatory G14) + LFP battery (~2 MWh) + 2× electric azimuth pods; **NO fossil main/aux engine**. Reference vessel **Nagi 凪 class** (coastal ~3,000 DWT / ~90 m / ~10 kn / IMO **MASS Degree 3** remote-no-crew, Degree 4 Council-gated N10) in `data/vessel.edn`; shipyard plant (building dock / panel line / block shops / **ATEX-zoned H₂ powertrain bay** + routed MEP incl. H₂+N₂-purge) in `data/shipyard.edn` (giemon-factory pattern); vessel SBOM `data/building.edn`; yard robotics `data/fleet.kotoba.edn` (Ita-ori 板織 / Oshidashi 押出 / Tsutsuki 突き / Nuri-de 塗手 / Otete-yard). **9 Pregel cells** (steel_block_fabrication→grand_block_assembly→weld_ndt_inspection→**powertrain_integration**→outfitting→launch_commissioning→sea_trial + decarbonization_audit cross-cut + class_certification_binder terminal; all import-clean, RuntimeError on `.solve()`) + **9 lexicons** `com.etzhayyim.funadaiku.*`. **EMPIRICAL** `methods/voyage_energy.py` (stdlib Admiralty-law energy budget) → 200 nm coastal leg: **wind 16.0% / solar 1.9% / hydrogen 82.1% / fossil 0.0%**, green-H₂ ≈ 1,039 kg, battery 170 min harbour zero-emission → confirms survey (no single source is a full prime mover). **OPERATIONAL kami-engine sim**: `kami-autodrive/examples/nagi_voyage.rs` sails the Nagi autonomously (Autopilot+ShipHydro GNC) through a multi-waypoint course while a zero-emission powertrain gates available thrust (no fossil; throttle power-limited not fuel-topped) → captured arrival **hydrogen 84.4% / solar 8.9% / wind 6.6% / fossil 0.0%**; regression `tests/nagi_zero_emission_voyage.rs` 2 green, full kami-autodrive suite green. **14 gates G1–G14 + 12 non-goals N1–N12** (N1 naval/armed · N2 nuclear · N5 fossil propulsion · N8 beaching ship-breaking→hodoki/kanayama · N11 dark-fleet/sanctions-evasion). HONEST: R0 design+data-model+sim only; no steel cut; ShipHydro 3-DOF planar (6-DOF CFD R2+); coastal scale (ocean VLCC out of R0–R3, G12); `:representative`; live yard/bunkering/sea-trial Council+operator gated (G11/G12) | 🟡 R0 | 2606013400 | 06-01 |
| kabuto 兜 | world PUBLIC-COMPANY supply-chain KG — listed companies + 登記本社住所 + 公開連絡先 + first-class supplier→customer SUPPLY edges + BPMN; surfaces single-source/sector/jurisdiction concentration (+ diversification index / betweenness / tier-depth / regional-bloc / per-commodity HHI) routed to redundancy + accountability; atproto social via kotoba-server; browser-native kotoba-wasm render on /search; resilience + transparency map, NEVER a target-list (tsumugi/watatsuna/danjo sibling, shares org.corp.* id space) | 🟡 R0 | 2606022000 | 06-02 |
| kanjō 勘定 | world PUBLIC-COMPANY financial-disclosure (決算) KG — registers listed companies' DISCLOSED BS/PL/CF line items from **PRIMARY disclosure ONLY** (JP EDINET 有価証券報告書 + US SEC EDGAR 10-K/20-F + Companies House + EU OAM, all Tier-A per 2605263800) into the kotoba Datom log as `:fin.fact/*`, **normalized across JP-GAAP / US-GAAP / IFRS** onto canonical concepts (concept_map; honest where non-comparable — 経常利益 = JGAAP-only). Vocab `corporate-financials-ontology.kotoba.edn` (`:fin.filing/:fin.fact/:fin.concept/:fin.metric/:fin.agg`). External public-company sibling of **toritate 執帳** (internal accounting), financials face of **kabuto 兜** (shares `org.corp.*` id space). **NON-ADJUDICATING (G2) + NO investment advice (G4, NOT 投資助言業) + NO forecasting (no 業績予想)** — records what the company disclosed + transparent ratios, never a rating/valuation/recommendation. **四季報 + paid terminals (Bloomberg/CapIQ/Refinitiv/FactSet/Moody's/D&B/Pitchbook/Crunchbase) PROHIBITED** (Charter Rider §2(e) anti-gatekeeping + §2(c) — read the filing, never the terminal; the public *facts* flow, the proprietary *compilation* does not). 3 cells (concept_map/ingest/analyze) + as-of restatement history (非終末論). **R0 verified**: `:representative` seed 6 filings / 36 facts / 5 real filers (Toyota·Sony·Nintendo EDINET / Apple·Microsoft EDGAR) → cross-GAAP normalize + per-company ratios + YoY + sector/currency aggregates (no FX cross-currency sum) green. Live full-universe XBRL parse = **G7** Council+operator gated | 🟢 R0 | 2606032000 | 06-03 |
| ooyake 公 | world government atlas — kotoba-Datomic structural atlas of public government units (supranational→country→prefecture/municipality→ministry→agency→bureau→window) with address/window/form/procedure/BPMN. Observational MIRROR + civic wayfinding map: never the government, never an official channel, never a target-list. Public read path: `/.well-known/gov-units.json` + `/gov`; reconcile-live remains gated except bootstrap-attested JP official-code backbone | 🟡 R0/R1 | 2606021600 | 06-02 |
| sumitsubo 墨壺 | **cleanroom CAD interop** for kami-engine-sdk (Vectorworks/Autodesk/AutoCAD), modeling + export — SDK module `@etzhayyim/sumitsubo-cad` (TS, zero-dep, re-export target): geometry kernel + neutral **ModelOp** vocab; exporters **DXF/SVG/OBJ/glTF (full)** + **IFC4/STEP (subset)** + **DWG (proprietary→DXF fallback + `DWG_PROPRIETARY` advisory, never native)**; DXF importer; **published-API-shape adapters** `VectorScript` (Vectorworks) + `AcadDatabase`/`command()` (AutoCAD ObjectARX/.NET/LISP) — cleanroom: published call shapes only, **no vendor SDK/decompile/trademark code**; kotoba EAVT bridge `drawingToDatoms → :dwg.*`. Plus **kotoba Pregel-LangGraph** cells: `model` (generative NL→Murakumo→ModelOp plan→Datoms) · `draft` (2D assist) · `interop` (vendor-script→ops) · `export` · `catalog` — **one ModelOp vocab across TS kernel + py cell** (one model, two runtimes). **tsc strict clean + 15 vitest green; py tests green**. 10 gates (G1 cleanroom / G2 kotoba-EAVT / G3 Murakumo-only / G5 DWG-honesty / G6 no-server-key). Honest R0: subset exporters (no ACIS/Parasolid B-rep N2), op-list kami-kernel binding (live `kami-app-cad` wiring follow-up), kami-engine-sdk re-export deferred (submodule absent). ADR id shared w/ parallel tsuzuri ADR (filename disambiguates) | 🟢 R0 | 2606033600 | 06-03 |
| sanae 早苗 | **labor-liberation OSS-robotics #1** — field-agriculture robotics (sow/weed/harvest), the robotics body of mitsuho's L2-Sustenance mandate. ISIC A01 / ISCO 6111,9211 / UNSPSC 70 = the single largest pool of human toil on Earth (~27% of world employment). Fleet 早乙女/草薙(herbicide-free weeder, coded ref cell)/稲架/案山子/田螺. 5 cells (RuntimeError R0) + 5 lex + LPS method (7/7) + weeding state-machine (5/5). **G9 regenerative-only** (no synthetic pesticide/herbicide, net soil-carbon≥0, seed sovereignty) · **G2 displacement-dividend coupling** (no live displacement without funded tenure-weighted cohort, 2606032130). R0 design-only; field deploy Council+operator gated | 🟡 R0 | 2606032100 | 06-03 |
| hataori 機織 | **labor-liberation OSS-robotics #2** — garment/apparel robotics (cut-make-trim) to END the sweatshop, not out-compete it. ISIC C13-14 / ISCO 7531,8219 / UNSPSC 53 (~65M workers, overwhelmingly women, wage-theft/disaster-prone). Fleet 裁/栲幡/縫殿(seam-manip, honest `:research`)/検針/畳. 5 cells (finishing_packing coded, 6/6) + 5 lex. **G9 fair-labor-provenance** (no displaced worker re-employed below Basic-High-Income standard) · **G8 honesty** (robotic sewing of limp fabric unsolved at scale) · **G2** dividend coupling. R0 design-only | 🟡 R0 | 2606032100 | 06-03 |
| kiyome 清め | **labor-liberation OSS-robotics #3** — domestic/janitorial cleaning robotics, frees the most invisible/gendered/dignity-poor labour (ISIC T,N81 / ISCO 9111,9112 / UNSPSC 76; ~75M domestic + tens of M cleaners). Fleet 箒/拭/厨/塵取(→haraedo)/濯(seeds susugi #12). 5 cells (surface_cleaning coded, 7/7) + 5 lex. **G9 privacy-by-construction** (homes: on-device only, NO cloud imagery/surveillance/biometric — `const` invariants in lexicons; the cleaner robot is the opposite of a spy) · **G2** dividend coupling. R0 design-only | 🟡 R0 | 2606032100 | 06-03 |
| yadori 宿り | **DNS-availability + domain acquisition** — answers *「DNS の空きを確認・予約する actor は設計されているか」* (was: no; etzhayyim.com was registered by hand). Checks availability via **RDAP** (structured WHOIS successor) and shepherds a **member-principal** acquisition via the **Cloudflare Registrar at-cost path** — the charter-clean inverse of a retail registrar (GoDaddy/Namecheap). 5 cells (availability_check + reservation coded) + 5 lex + RDAP availability classifier (`methods/availability.py`, IDN/punycode + IANA bootstrap + status→verdict). **availability_check cell wraps the classifier; live RDAP fetch wired but G7-gated** (operator attestation + `YADORI_ALLOW_LIVE_RDAP=1`) — empirically verified live across .com/.org/.dev (example.com/etzhayyim.com→registered, free names→available). **G2 no-fiat-inflow/member-principal** (registrar fees never from religious-corp funds; okaimono assisted-checkout ADR-2606012100; yadori never the buyer → §1.3 holds without amendment) · **G3 cloudflare-registrar-default** (at-cost, no markup; GoDaddy/external fiat-markup registrars Council-gated) · **G5 no-server-key** (member signs; server signature refused) · **G6 no-squatting** (no cybersquatting/typosquatting/trademark/speculation/parking; held-trademark+confusable screen + Charter-Rider scan) · G1 read-only RDAP · G7 outward-gated (live fetch + registrar mutate gated) · G9 PII-encrypted. **31 tests green** (11 RDAP classifier + 20 cell state-machine: availability_check G7 live-gate + reservation G2/G3/G5/G6). R0 design + live RDAP wired-but-gated; live registration Council Lv6+ + operator gated | 🟡 R0 | 2606038400 | 06-03 |
| karakuri 絡繰 | **web-service-to-CLI** — answers *「squarespace のような webservice も CLI にする actor を設計して」* (the `clianything.org` shape). Gives a member a uniform CLI / command vocabulary (**`ServiceOp`** = `service·noun·verb·safety·destructive·adapter-tier`) over GUI-only SaaS (Squarespace/Wix/Notion/Shopify/Stripe/GitHub/…), driving the **MEMBER's OWN account** through the safest of 3 adapter tiers (**T1 official-API > T2 ToS-permitted headless-browser > T3 structured export**) and logging every op to the kotoba Datom log. The **charter-clean inverse of clianything.org** (as okaimono inverts Amazon, yadori inverts GoDaddy): **G1 own-account-only** (no third-party scraping/surveillance; himotoki prior art) · **G2 official-API-first / ToS-honest** (T2 headless only where ToS permits; **no detection-evasion** — no captcha-farm/proxy-cloaking/rate-limit-circumvention; `:automation-prohibited` refuses T2 by construction) · **G3 no-server-key** (member creds/sessions member-held+encrypted; mutate = member signature, server sig refused, ADR-2605231525) · G4 Murakumo-only · **G5 read-default/mutate-gated** (read+export ship R0; create/update/**delete** need member-sig+dry-run) · **G6 outward-gated** (ANY live third-party call Council Lv6+ + operator; R0 = parse/plan/dry-run only) · **G7 kotoba-EAVT audit** (every ServiceOp a Datom, replayable; member audits what touched their account) · G8 sourcing-honesty · G9 export = member's OWN data, encrypted. 5 cells (session_broker coded) + 5 lex + **`methods/command.py`** ServiceOp parser/planner (safety-classify + official-API-first tier-select + ToS gate + mutate gate, offline). **29 tests green** (21 planner + 8 session_broker state-machine G1/G3/G5). R0 design-only; no live adapter execution (gated). N1 not a scraper · N2 no detection-evasion · N4 no paywall/DRM circumvention · N6 no ad/affiliate driving | 🟡 R0 | 2606039200 | 06-03 |
| nusa 幣 | **ritual/industrial hemp heritage + low-THC cultivation** — answers *「日本国内での大麻の解禁についての project / actor は設計されているか」* (was: none). **Not a legalization actor** — the charter-clean answer. 幣=大幣(ōnusa, 麻の祓具). Datafies Japan's **ritual-hemp heritage** (神道の祓い/清め 注連縄/祓串/大幣; 皇室の **麁服 aratae** 大嘗祭神御衣 + **阿波忌部**の系譜) and the **低THC 繊維/神事用栽培者免許** pathway reopened by the **2023改正 (大麻草の栽培の規制に関する法律, 令和5年法律第84号)** into the kotoba Datom log with as-of history (非終末論). Honest history: GHQの大麻取締法(1948)が断ったのは主に**繊維・祭祀用栽培**で嗜好用文化ではない(在来種は低THC繊維型). **Charter-clean by construction**: 繊維+神事+文化史のみ; **嗜好THC は `:thc-class` invariant で除外**(G1 — `:psychoactive` は schema/lexicon/code の3箇所で表現不能, `fiber_provenance` が `ValueError`)· **G2 非嗜好**(使用罪尊重)· **G3 非裁定/政治中立**(事実のみ; 解禁論に推進/反対なし→danjo/moushibumi)· **G4 member-principal/no-fiat-inflow**(栽培者免許費用は宗教法人資金から出さず okaimono 経由, nusa は名義人/出資者にならない)· G5 no-server-key · G6 Murakumo-only · G7 sourcing-honesty · G8 outward-gated(実栽培/免許申請/live ingest = Council Lv6+ + operator)· G9 PII-consent · **G10 medical-boundary**(大麻由来医薬品は所掌外→yakushi/iyashi/mitate). 5 cells (fiber_provenance + cultivation_license_plan + observation_bridge coded; `.solve()` raises at R0) + 5 lex `com.etzhayyim.nusa.*` + ontology `ritual-hemp-ontology.kotoba.edn` + `methods/analyze.py` heritage report. **方向2 = 既存actor拡張**: danjo に `cannabis-policy-legislative-trace` methodNote(非裁定・両論中立)+ moushibumi に大麻草栽培規制法パブコメ/請願 participationTarget(賛否中立). **25 tests green** (8 analyze + 17 cell). N1 嗜好用なし · N2 解禁推進/反対の政治組織でない · N3 医薬品製造者でない · N4 無免許栽培を助長しない · N5 高THC種苗流通なし | 🟡 R0 | 2606039800 | 06-03 |
| tazuna 手綱 | **clean-room remote-robotics fleet operation + teleoperation + learning-from-demonstration** — answers *「boston dynamics の orbit のように remote の robotics 操作と、それによる学習・training は設計されているか? クリーンルーム設計で」* (was: ABSENT — robotics was autonomous-only, teleop an explicit non-goal, zero imitation-learning). The charter-clean **Boston-Dynamics-Orbit** equivalent: the missing **HEAD** (transparent, member-signed, on-chain-logged fleet/teleop control plane over the robotics-body actors sanae/kiyome/hataori/giemon/wadachi) + the missing **LOOP** (teleop trajectory → consent-bound demonstration → **baien federated** imitation/behavior-cloning policy → **supervised autonomy hand-off**; 手綱 = the reins, held to teleoperate then released as autonomy is earned). A **horizontal control-plane actor** (operates the bodies, not a single occupation). **Remote actuation admitted ONLY as Transparent Force** (§1.12.B: on-chain monitored + open-source + 1 SBT=1 vote — every `teleopCommand` an anchored Datom; no force-auth ref → no grant) **under no-server-key** (member signs every actuation command; server signature refused by construction, ADR-2605231525). `:weaponizable` force class **structurally unrepresentable** (N1 — like nusa `:thc-class` / kotoba-os `liveActuation=false`); soft-RT **deadman/e-stop/latency** safe-stop→autonomy-fallback (**NOT a certified safety system**; IEC 61508 hard-RT live actuation = R5/Lv7+, kotoba-os N2); demonstrations = member's **labour** (consent/encrypted/`cash≡0`, Wellbecoming `as-of`); home teleop **on-device-only** (no cloud video/biometric, kiyome G9); **Murakumo-only** + **baien edge envelope** (ADR-2605241900); **displacement-dividend coupling** (G2, ADR-2606032130). **Clean-room interop** (G1, sumitsubo + nv-compat pattern): published API shapes + open standards ONLY — Orbit REST/gRPC call *shapes* (names in docstrings only) / Open-RMF (Apache-2.0) / ROS 2 / DDS / MCAP / rosbag2 / LeRobot / Foxglove / nv-compat Isaac; NO vendor SDK/decompile/trademark/fork. 7 cells (operation: fleet_registry/mission_dispatch/**teleop_session** [coded] /telemetry_ingest · learning: demonstration_record/policy_train/autonomy_handoff; `.solve()` raises at R0) + 6 lex `com.etzhayyim.tazuna.*` + kotoba EAVT schema/seed + `:representative` fleet registry + `methods/teleop_safety.py` (force-class/no-server-key/soft-RT reasoner). **29 tests green** (18 teleop-safety + 11 teleop_session state-machine). R0 design-only; no hardware/no live link/no live actuation (G7 Council Lv6+ + operator; Lv7+ for `:powered-actuation`). N1 no weapons/force-as-harm · N4 no gig-teleop marketplace · N6 no autonomy beyond SAE-L4 / no un-supervised policy promotion · N7 no vendor SDK bundling/no ToS evasion · N8 no cash for demonstrations | 🟡 R0 | 2606042100 | 06-04 |
| watari 渡り | **live ship + aircraft real-time position KG** — answers *「船舶や飛行機のリアルタイムな位置情報を取得・更新する actor は設計されているか」* (was: yes but non-compliant — legacy `maps` `aismarine`/`aircraft_live` stored positions in **RisingWave** `vertex_vessel_position`/`vertex_aircraft_state`, legacy `vessel` `tracking:ais` via **graph.write SQL**; both violate kotoba-canonical-state ADR-2605262130 + route narration off-Murakumo). The **kotoba-native rewrite**: ingests **public transponder broadcasts only** (ships via **AIS**/AISStream · aircraft via **ADS-B**/OpenSky+adsb.fi) into the kotoba Datom log as an **append-only as-of trajectory** — **latest `:craft.fix` = current position, fix stream = trail (非終末論)**, no mutable overwritten row. Surfaces aggregate **sea-lane/air-corridor/chokepoint/approach** concentration routed to **safety + collision-avoidance + congestion-easing + resilience**. **DYNAMIC sibling of watatsuna 綿津綱** (static cables): both key on the **SAME chokepoint keywords** (`:malacca`/`:luzon-strait`/`:suez-red-sea`/`:hormuz`) → vessel transit composes with cable load into **one maritime resilience picture** (海/空 path trilogy w/ watatsumi 綿津見). A **situational-awareness map, NEVER a person-surveillance feed and NEVER a target-list**. New `:craft/* :craft.fix/* :craft.leg/* :lane/*` ontology (`moving-craft-ontology.kotoba.edn`) + 4 lex `com.etzhayyim.watari.*` (supersede legacy maps/vessel surfaces, MIGRATION-NOTES) + `analyze.py` (chokepoint-transit/lane-load/approach-congestion/freshness-tail) + `ingest.py` (offline AIS/ADS-B normalizer). **EMPIRICAL**: `analyze.py` green on `:representative` seed (13 craft/26 fixes/9 lanes → top transit Malacca 3, freshness-tail 2 honest gaps); `ingest.py` normalizes offline batch + **refuses `--live` without G7 gate**; 4 lex + manifest lint clean; DID registered (`did:web:etzhayyim.com:actor:watari`). **G1 public-broadcast-only** (military/blocked-from-display LADD/PIA out) · **G2 situational-not-surveillance/not-targeting** (§2(a) force-sep + §2(d); watatsuna G2) · **G4 no person-tracking/no pattern-of-life** (a craft is a craft, not a person — operator=company org id, private yacht/jet excluded; the invariant legacy maps/vessel lacked) · G5 no-fabricated-coverage (stale→freshness-tail) · G6 Murakumo-only · G7 live-ingest Council+operator gated · G8 no-git-lfs · G9 no-PII. N3 observes craft, never flies/sails one (≠ kami-autodrive/funadaiku/watatsumi) · N4 not GMDSS/ATC/VTS safety-of-life · N5 no RisingWave/SQL. ZERO invariant amendments. Honest R0: design+data-model+analyzer only, `:representative` seed (rounded coords/illustrative ts, NOT live capture), live AIS/ADS-B ingest G7-gated, legacy maps/vessel superseded-not-deleted | 🟡 R0 | 2606041827 | 06-04 |
| hotaru 蛍 | **III-V / InP substrate open-publication commons** — answers *「インジウムリン基盤の生成・製造に関わる actor は設計されているか」* (was: no; III-V *manufacturing* is constitutionally PROHIBITED through R3 per ADR-2605265500 §2). **NOT a fab** — the charter-clean answer (nusa pattern): the **open-publication III-V/InP substrate knowledge COMMONS** that ADR-2605265500 §2's **R4+ re-evaluation gate is explicitly conditioned on** (*"open-source III-V wafer + epitaxy IP becomes available; currently no such commons exists"*). hotaru **is the construction of that commons**. Datafies the substrate chain (合成→単結晶育成 LEC/VGF/VB→ウェハ加工→エピ面) into the kotoba Datom log. 蛍=直接遷移発光: シリコンの**間接**遷移こそが III-V 族(レーザ/LED/フォトディテクタ/光ファイバ・海底ケーブル光学系 watatsuna)の存在理由 → iwakura/fuigo **間接**遷移シリコン(ADR-2605242500)の兄弟。**3 structural invariants enforced in schema+lexicon+code** (nusa `:thc-class` pattern): **G1 open-IP-only** (`:source-license` ∈ practiceable-open set; `:vendor-proprietary` MOCVD recipe **unrepresentable**) · **G2 design-only** (`:iiiv.crystal/:iiiv.wafer/fabricated` `:db/allowed [false]`; grown boule/manufactured wafer unrepresentable through R3) · **G4 conflict-mineral** (In/Ga require clean `:in-sourcing`, inherits hikari/himawari §G2). **G3 NON-ADJUDICATING** on the gate — hotaru reports, **Council Lv7+ decides** (Lv7+, not Lv6+, because III-V is gated through R3). New ontology `iii-v-substrate-ontology.kotoba.edn` + 6 lex `com.etzhayyim.hotaru.*` + 5 cells (commons_ingest + precursor_safety **coded**; 3 stubs) + `methods/analyze.py`. **EMPIRICAL**: substrate commons **READY** (4/4 stages open-mature: LEC InP/VGF/CMP/wet-etch); **epitaxy GAP** (vendor-IP-dense device stack-up, `:gap`); **R4+ gate satisfiable from commons alone = False** → fabrication stays PROHIBITED through R3, binding gap = epitaxy/device not substrate growth. **22 tests green** (10 methods + 12 cells); DID registered (`did:web:etzhayyim.com:actor:hotaru`). **ZERO invariant amendments.** N1 civilian-only (no fire-control/weapons-seeker) · N2 NOT a fab · N4 ≠ silicon iwakura/himawari track. Honest R0: design+datafication+analyzer over bounded `:representative` seed; no growth sim / live corpus harvest / live kotoba; figures need primary-source verify (G7) | 🟡 R0 | 2606051200 | 06-05 |
| noroshi 烽 | **光電融合 (photonics-electronics convergence) communication chip + ISAC + photonic packaging robotics** — answers *「光電融合の通信チップの actor, robotics, ISAC sim の設計はできているか」* (was: NONE — the silicon/iwakura/fuigo line is all-ELECTRONIC digital ternary ASIC; zero silicon-photonics / CPO / optical-transceiver / ISAC coverage). 烽 (狼煙, beacon-fire) is the original optical telecom — a watchtower **senses** a distant fire and **relays** a coded message, one emission two functions = exactly **ISAC** (Integrated Sensing And Communication). The **photonic** sibling of the electronic silicon/iwakura/fuigo line + RF tsutae, and the **CPO transceiver-chip** end of the watatsuna submarine-cable medium. **3 faces, each a verifiable stdlib core**: **(chip)** `link_budget.py` optical/CPO power budget + energy/bit (reference CPO 2km/100G closes +10.05 dB, **3.96× lower energy/bit** than pluggable) · **(isac)** `isac_sim.py` OFDM-radar reciprocal-processing JCAS — recovers a **civilian object's** range+velocity on-bin + the comms↔sensing **power-split tradeoff** · **(packaging)** `active_alignment.py` Hooke-Jeeves fibre↔grating active alignment (<1 dB in ~50 probes) under an **IEC 60825 + civilian-use laser-safety interlock** (the safety-critical coded core, like tazuna `teleop_safety`). **CIVILIAN by construction**: **G3/N1** weaponisation (directed-energy / laser-dazzle / fire-control radar) **structurally unrepresentable** (schema/lex/`PERMITTED_USES`; iwakura/nusa `:class` precedent) · **G4/N2** ISAC senses **objects never persons** (no `:person` class; watari invariant) · **G1/N5** clean-room **open-EDA only** (GDSFactory/Meep/KLayout/Verilator/yosys/OpenLane + open PDK; extends the verified iwakura RTL→sky130 flow; NO Cadence/Synopsys/Lumerical/Ansys/NDA-PDK) · **G5/N3** soft laser-safety, NOT certified (kotoba-os N2) · **G2** packaging fleet Displacement-Dividend-coupled (ADR-2606032130) · G6 Murakumo-only · G7 no-server-key · G8 outward-gated (live tapeout/laser/actuation Council Lv6+, Class-3B/4 Lv7+) · G9 kotoba-EAVT canonical · G10 `:representative` (no silicon; sims are DSP not measured devices) · G11 CycloneDX SBOM at tapeout. 6 cells (chip device_design/link_budget · isac isac_waveform/sense_estimate · packaging **active_alignment** [coded]/reliability_qual; `.solve()` raises) + 5 lex `com.etzhayyim.noroshi.*` + kotoba schema/seed + photonic-fleet seed + ontology `photonic-convergence-ontology.kotoba.edn` + DID registered (`did:web:etzhayyim.com:actor:noroshi`). **R1 integrations**: **(c)** `cable_endpoint.py` joins noroshi CPO chips to the **watatsuna** submarine-cable medium → CPO-transceiver fleet sizing per landing → per-chokepoint demand (luzon-strait→suez→malacca→gibraltar, matching watatsuna's capacity ranking; resilience NOT target-list) · **(a)** `kami_isac_bridge.py` + `wit/kami-isac.wit` = ISAC sensor as a **kami-autodrive** plant (ADR-2606010600), scenario→range/velocity tracks, civilian objects only · **(b)** `pic_layout.py` GDSFactory-shaped ModelOp layout plan → feeds waveguide length back into `link_budget.py`, real GDS write gated behind optional `gdsfactory` import. **203 tests green** (189 methods + 14 cell state-machine; incl. a structural charter-invariant suite [civilian-only/object-not-person/no-server-key/open-EDA over parsed lex+schema+ontology] + an SSoT-consistency suite [manifest↔files, ontology≡schema, seeds↔schema drift-locks + seed-VALUE enum/const charter enforcement] + degenerate-input/route-length/missing-seed guards). ZERO invariant amendments. Honest R0+R1: design+arithmetic/DSP sim only; no foundry/silicon/measured device/live laser/live robot (all G8-gated); kami-engine submodule unpopulated + gdsfactory not installed → (a)/(b) ship as bridge+WIT-contract+gated-backend (sumitsubo pattern), (c) full offline join | 🟡 R0+R1 | 2606051600 | 06-05 |
| kamado 竈 | **closed-loop carbon refining + fossil-refinery decommissioning/transition + refinery observation** — answers *「石油精製のプラントの actor・robotics は設計されているか」* (was: only a legacy `oil-refining` Cypher/RisingWave **observation** actor — `graph.query`/`graph.write` `MATCH (r:Refinery)…`, violating kotoba-canonical-state ADR-2605262130; **no robotics/plant**). 竈 = the hearth-furnace kami (竈神/荒神); the transformation apparatus is neutral, the carbon **origin+fate** carry the harm. **EMPIRICAL thesis** (`methods/carbon_balance.py`): a fossil→combusted pathway is **+3.50 tCO₂e/t** (one-way geological stock→flow = genuinely multi-generational, Charter §1.9/§2(f) + ADR-2605263500 D3); full robotic advanced-process-control reaches only **+3.38** (~3% cut — origin+fate ~89% is in the carbon, untouchable), so **robotics makes fossil refining cleaner, never harmless**; net≤0 is reached ONLY by changing the **feedstock** to closed-loop carbon (biogenic ≤+0.03 / captured-CO₂ e-fuel ≤+0.03 / carbon-locked durable material −3.07). That finding is made **structural**: `:fossil-virgin-crude` is **unrepresentable** (the `:feedstock/class` invariant in schema enum + lexicon const + `feedstock_guard.py` ValueError — nusa `:thc-class` / tazuna `:weaponizable` pattern), so a fossil-fed refinery cannot be expressed. **Three faces** over the kotoba Datom log: **(A) observation** — kotoba-native successor that **supersedes** legacy `oil-refining` (no RisingWave); refinery/unit/outage + transition-readiness as a **resilience+transition map, NEVER a target-list** (G4, watari/watatsuna sibling; observation≠operation) · **(B) §2(d) decommission/transition robotics** — safely wind down / remediate / **convert** existing fossil assets (→ hikari solar / synthesis plant / hodoki+kanayama recovery), humans out of H₂S/benzene/pyrophoric hot zones (G9 + displacement-dividend ADR-2606032130; G3 `:expand`/`:restart-fossil` life-extension unrepresentable) · **(C) closed-loop synthetic refining** — same unit ops on biogenic/captured-CO₂/recycled carbon ONLY, renewable-powered, every design scored against D3; tazuna-style member-signed Transparent-Force control (G5 no-server-key, G11 NOT a certified safety system). New ontology `refining-ontology.kotoba.edn` + 6 lex `com.etzhayyim.kamado.*` + manifest/CLAUDE.md/README. **19 tests green** (11 methods + 8 cell-state-machine); `carbon_balance.py`+`analyze.py` run (5 refineries/3 §2(d) plans/3 synthesis designs, 3/3 pass D3); cells `.solve()` RuntimeError at R0. **ZERO invariant amendments** (strengthens §2(d)/D3/kotoba-canonical/no-server-key). N1 not a fossil-crude refinery operator (unrepresentable) · N2 not new fossil extraction (§2(d), never) · N3 no military/naval/aviation fuel (§2(a)) · N6 no RisingWave/Cypher (supersedes legacy). Honest R0: design+data-model+carbon-sim+dry-run; `:representative` seed; per-tonne ledger not full LCA; live ingest/teardown/actuation Council Lv6+ + operator gated (G8/G11) | 🟡 R0 | 2606051500 | 06-05 |
| mitooshi 見通し | **probabilistic forecasting observatory + leak-free fact→error→weight→learn architecture** — answers *「kotoba で quant market や Google-Trends の model での未来予測 actor は設計しているか? 実際の予測と、事実からモデル誤差・weight を修正・学習する architecture は?」* (was: no forecasting actor; only fragments — kakaku price-history / magatama opportunity.forecast / ameno predict-next; kanjo + yobel explicitly PROHIBIT forecasting/speculation). A naive quant predictor IS a trading bot = profit speculation (Charter §1.3 + yobel bar), so mitooshi is its **charter-clean inverse** (okaimono/yadori/kamado pattern): a **non-adjudicating observatory** emitting **probability distributions** over PUBLIC time-series (chokepoint transit-load/congestion/availability/flow-rate/price-index/search-interest) routed to **resilience/planning/early-warning** (danjo/kanae/watari/watatsuna siblings), that **NEVER trades, holds a position, advises, or derives P&L**. The **fact→誤差→weight→学習 loop is STRUCTURAL** on the append-only Datom log: a forecast carries its `:info-as-of` stamp, the realizing observation arrives later as an append-only `:observed-at` datom, and the proper-scoring residual (CRPS/pinball/Brier/log-score + PIT + skill-vs-baseline) is the join across kotoba `as-of` → **look-ahead leak is STRUCTURALLY impossible on an append-only log** (`score_pair` raises if obs ≤ info-as-of). Residuals drive an **EWMA bias + variance-inflation recalibration** whose real training substrate is **baien federated edge** (Murakumo-only, ADR-2605242600/2630). **4 structural invariants** (schema `:db/allowed`/enum + lexicon const/enum + Python ValueError, nusa/tazuna/kamado pattern): **G1 distribution-only** (`:forecast/point-asserted` allowed [false] — 非終末論, no final-state datom) · **G2 non-speculative** (`:forecast/use` enum excludes trade/wager/position) · **G4 primary-public-source-only** (`:series/source-class` enum excludes Bloomberg/四季報 + scraped Google-Trends; kanjo §2(c)/(e); Trends only via karakuri member-principal) · **G5 leak-free skill-honest scoring** (info-as-of boundary + proper scoring rules + skill vs documented baseline). Promotion only when model **beats baseline (G12) AND calibrated (G7) AND member-signed (G9 no-server-key)**. New ontology `forecasting-ontology.kotoba.edn` + 6 lex `com.etzhayyim.mitooshi.*` + 5 cells (all 5 coded state machines; `.solve()` RuntimeError R0) + `methods/score.py` (CRPS Gaussian closed-form/pinball/Brier/log-score/PIT/skill + ensemble energy-form, leak-checked) + `methods/analyze.py` (leak-free backtest + reliability diagram) + `methods/ingest.py` (offline normalizer). **EMPIRICAL** (all 3 distribution kinds wired end-to-end with metric-appropriate baselines): gaussian EWMA+drift **CRPS 0.089 / skill vs climatology +0.81 / persistence +0.31** · quantile regressor **pinball 0.024 / skill +0.71** · categorical direction-classifier **Brier 0.338 / skill +0.44** · ensemble energy-form **CRPS 0.027 / skill +0.78** — all 4 distribution kinds **skilled** and wired end-to-end. **The learning loop is PROVEN to close** (`test_learning_loop.py`): a biased+misdispersed model handed only its own residuals → online_update → apply_correction drives **CRPS 1.503→0.403 (−73%) / PIT mean 0.95→0.50 / learned bias +2.02** → calibration_gate promote CLEARED (member-signed) / REFUSED (unsigned, no-server-key); **and stays stable continually** (`test_continual_learning.py`): EWMA correction converges to true bias, rejects noise, and **re-converges on regime drift 2.0→4.0** (c→1.996 then c→3.996 — drift-tracked, not stuck). Offline `ingest.py` normalizer (G4 source membrane refuses Bloomberg/scraped-Trends; G10 `--live` gated). `analyze.py` emits a **PIT reliability diagram** (`out/reliability.md` + `:fc.calib/*` datoms — the G7 calibration-honesty artifact, kami-engine-viz-ready). **All 5 cells coded state machines** (backtest_score = `score_batch`, leak-checked). **Cross-actor bridge** (`bridge.py`): maps watari (vessel-transit) + watatsuna (cable-load) **derived chokepoint aggregates** into forecastable `:series`/`:obs` over the SHARED chokepoint keyword space — **verified on the REAL watari/watatsuna outputs** (9 chokepoint series; `:malacca` becomes both `s-malacca-transit` 3-vessels + `s-malacca-cable` 940 Tbps; 54 non-chokepoint records ignored) → mitooshi forecasts the very chokepoints they observe (observe→forecast; `:derived` siblings ingested as `:representative` not authoritative, G11). **Multi-horizon skill decay** (`horizon.py`, AR(1) φ=0.9): skill vs climatology **+0.30 @h=1 → ≈0 @h=6** — honest useful-foresight range, never a flat-skill crystal ball (非終末論). **89 tests green** (26 score + 24 cells + 9 analyze + 7 ingest + 6 bridge + 6 horizon + 6 learning-loop + 5 continual-learning; one-command `run_tests.sh`). **DID registered** in INFRA_ACTORS → `did:web:etzhayyim.com:actor:mitooshi` (resolvable + searchable). **ZERO invariant amendments.** N1 not a trading/quant bot · N2 no investment advice/業績予想 (kanjo boundary, not 投資助言業) · N4 no person-level prediction · N5 no point-prophecy · N6 no commercial-GPU training · N7 no server-signed promotion. Honest R0: design+offline scoring/recalibration only; `:representative` seed (4 series/4 models); live ingest/publish/promotion/federated-pass Council Lv6+ + operator gated (G10) | 🟡 R0 | 2606051800 | 06-05 |
| ake 朱 | **community-edit membrane — Wikipedia collaborative-correction STANCE, charter-fitted** — answers *「etzhayyim の actor や情報を Wikipedia のようなスタンスで更新できる設計になっているか?」* (was: no — actor/KG data was actor-owned, git-committed, ADR-author-gated; no community-edit path, no proposal/vote workflow, no member-initiated `:representative→:authoritative` promotion). 朱 (訂正の朱墨, 朱を入れる=校正) is how a **信者 (SBT holder)** proposes a correction to a **KG fact** or an **actor profile**. Every edit is a **member-signed proposal appended to the kotoba Datom log** (never an overwrite — 非終末論, full immutable revision history). A **Murakumo-only LLM scores risk+quality and ROUTES** (the Wikipedia **ORES** analogue) but **NEVER decides accept/reject** (G2 — `route_for(risk,quality,rider)` is a PURE FUNCTION; `:triage/decision` does not exist): low-risk well-sourced → **auto-accept** (optimistic), risky/contested → **1 SBT=1 vote** (+48h timelock), invariant-adjacent → **Council Lv7+**, Charter-Rider §2(a)-(h) hit → **refused** (no vote can promote it). Mirror entity-actors are **corrected as observations, never spoken-as** (ADR-2606042330 preserved; `:entity-speech`/`:impersonate` unrepresentable). **NOT anonymous open-edit** — 信者-gated + member-signed (no-server-key) by construction (N1); a *permissioned wiki*, not Wikipedia's anonymous model. **9 structural gates in 3 places each** (schema `:db/allowed`/enum + lexicon const/enum + Python ValueError, nusa/tazuna/kamado/mitooshi pattern): **G1** 信者-gated+no-server-key (`:edit/author-kind` allowed [:member]; `serverHeldKey` const false) · **G2** LLM non-adjudicating (no `:triage/decision`, pure-function route) · **G3** mirror-preserving (`:edit/target-kind` ∈ {kg-fact, actor-profile}) · **G4** sourcing-mandatory (unsourced → refused; `:authoritative` promotion needs verifiable provenance, ooyake G20) · **G5** append-only/non-destructive (`:delete`/`:overwrite` unrepresentable; every `revision.py` helper returns a new list) · **G6** Murakumo-only · **G7** invariant-lock (Council Lv7+ + Charter-Rider hard-gate) · **G8** outward-gated (`published` const false) · **G9** anti-vandalism contributor-trajectory (Wellbecoming, not a score-of-soul, kizashi G8). New ontology `community-edit-ontology.kotoba.edn` + 6 lex `com.etzhayyim.ake.*` + 5 cells (all coded state machines; `.solve()` RuntimeError R0) + `methods/triage.py` (risk+quality scorer + pure-function router — the G2 heart) + `methods/revision.py` (append-only history: append/as-of/current/history_of + non-destructive sourcing promotion + `revert` = Wikipedia rollback for upheld `:challenge` edit-wars: restores predecessor by appending, never deletes — the bad edit stays auditable via time-travel `as_of`) + `methods/contributor.py` (G9 anti-vandalism rate + RECOVERABLE Wellbecoming trajectory, not a score-of-soul) + `methods/analyze.py` (end-to-end membrane) + `methods/ingest.py` (genesis-revision bridge over the REAL committed actor-profile SSoT — the DID-web Worker's publish source; surfaced + closed an INFRA_ACTORS↔profile-seed drift, adding mitooshi+noroshi profile records 19→21) + `methods/test_consistency.py` (SSoT drift-lock: manifest↔cell-tree↔lex↔ontology↔seed↔registry). **EMPIRICAL** (`analyze.py` over the `:representative` seed, 5 edits one-per-route): e1 sourced KG-fact → **auto-accept** ✅ · e2 status change 8-1 → **vote** ✅ · e3 profile fix 5-0 → **vote** ✅ · e4 license attr → **council-lv7 pending** · e5 "runs advertising" → **rider-refused**; accepted edits land in the append-only revision history, refused/pending do not. Edit-war: a `:challenge` routes high→vote, an upheld challenge `revert`s to the predecessor by appending (`test_editwar.py`). Cell-chain integration (`cells/test_membrane_flow.py`) threads one edit through all five cells in sequence (propose→edit_triage→review_vote→promote→revision_log) for every route, proving they compose. **110 tests green, HERMETIC** (suite is green in any checkout: ingest genesis bridge asserted against a committed fixture `data/sample-profile-seed.kotoba.edn`, real-SSoT checks soft — no hard dependency on the coordination-committed shared seed): 17 triage + 7 revision + 6 edit-war + 8 contributor + 8 ingest + 16 charter-invariants + 6 analyze + 5 lexicons + 10 consistency + 20 cell-unit + 7 cell-chain; one-command `run_tests.sh`. **DID registered** in INFRA_ACTORS → `did:web:etzhayyim.com:actor:ake` (resolvable + searchable) + actor-profile seed. **ZERO invariant amendments** — STRENGTHENS no-server-key (ADR-2605231525), kotoba-canonical-state (ADR-2605312345), 1 SBT=1 vote, and the mirror invariant (ADR-2606042330). N1 not anonymous open-edit · N2 LLM never decides · N3 no impersonation · N4 no overwrite/deletion · N5 no unsourced edits · N6 no server-signed edits · N7 not an arbiter of legal truth (danjo/chigiri boundary). Honest R0: design+offline routing/revision only; deterministic triage (Murakumo LLM refinement R1); `:representative` seed; live ingest/vote/promotion/publish Council Lv6+ + operator gated (Lv7+ invariant-adjacent) | 🟡 R0 | 2606052100 | 06-05 |
| fuchi 扶持 | **mission-aligned maintainer sustenance allocator — the charter-clean INVERSE of a business investment fund** — answers *「etzhayyim の business/robotics/remote-control を推進する実際の信者(メンテナー)に投資するビジネス投資ファンドの actor はあるか? 実世界のメンテナーは必要なので必要なプロセス・system-of-systems を設計して」* (was: no — and a conventional fund is constitutionally prohibited: 非営利のみ/donation-only/cash≡0/payoff帰属=etzhayyim/Charter-Rider §2(b) speculative-finance bar; wakai already encodes `NO investment return`). A VC fund invests capital for **equity+ROI+exit**; 扶持 (封建期の **扶持米** = 現物の在俸, the in-kind stipend that sustained a retainer so they could serve) allocates **in-kind sustenance + commons-asset access + tooling/compute** to the real-world **maintainers (信者)** who keep etzhayyim's actors alive. A **redistribution/sustenance allocator, NEVER an investor**: no equity/ROI/debt/profit-claim/exit, **cash≡0**. The whole fund vocabulary (NAV/carry/IRR/cap-table/exit/dividend) is **structurally UNREPRESENTABLE** (`:alloc/instrument` `:db/allowed` only the sustenance set — nusa `:psychoactive` / tazuna `:weaponizable` / kamado `:fossil-virgin-crude` pattern). A **horizontal control-plane ON TOP of** Public Fund + TitheRouter + Mission-funding revenue arm (value source) → Displacement-Dividend `ln(1+min(tenure,40))×hazard` curve (allocation math, reused, ADR-2606032130) → Basic-High-Income in-kind (cash≡0 delivery, ADR-2605301020) → **in-kind rails** (housing→commons-land LANDS.md · food→mitsuho · energy→hikari · compute→Murakumo · tooling→okaimono · care→iyashi/hagukumi/kokoro · liquidity→**warifu 0% qard-ḥasan, MEMBER-PRINCIPAL only**) → toritate (books) + kanae (viz) → kotoba `as-of` Wellbecoming trajectory (非終末論). **Honest resolution of the "maintainers need cash" tension**: 扶持 maximizes in-kind substitution + routes the irreducible external fiat residual to **member-principal** 0% liquidity (the member is borrower/payer; 扶持 never holds/lends/pays → §1.3 holds WITHOUT a Lv7+ amendment); it cannot eliminate external fiat obligations (N4; full fiat income = Charter Lv7+). **10 structural gates in 3 places each** (schema `:db/allowed`/enum + lexicon const/enum + Python ValueError): **G1** no investment vehicle (instrument allowlist; equity/debt/ROI/exit absent) · **G2** cash≡0 (`:envelope/:alloc/cash-usd-micros` `:db/allowed [0]`) · **G3** in-kind rails only (no `:cash-disbursement`; external = member-principal warifu) · **G4** covenant-gated (`:maintainer/covenant ∈ {outreach,vowed}`) · **G5** payoff帰属=etzhayyim (`:maintainer/owns-payoff :db/allowed [false]`) · **G6** Wellbecoming not score-of-soul · **G7** non-adjudicating (`:gov/route` PURE FUNCTION; no `:gov/decision`; vote/Council decides, ake pattern) · **G8** Murakumo-only · **G9** no-server-key · **G10** outward-gated. New ontology `maintainer-sustenance-ontology.kotoba.edn` + 9 lex `com.etzhayyim.fuchi.*` + 5 cells (coded state machines; `.solve()` RuntimeError R0) + `methods/allocate.py` (tenure-weighted in-kind shares+floors, cash≡0, G1 allowlist) + `methods/route.py` (in-kind rail decomposition + pure-function `gov_route`) + `methods/analyze.py` (end-to-end). **R1 a/b/c/d (landed offline)**: **(a)** `methods/provision.py` wires the in-kind rails to the **real producing-actor DIDs** (food→mitsuho · energy→hikari · tooling→okaimono · care→iyashi · housing→commons-land · compute→Murakumo · liquidity→warifu) as **dry-run** provisioning intents (`published=false` G10, cash 0, keyless; liquidity = member-principal) · **(b)** `methods/vote.py` = **real 1 SBT=1 vote + 48h timelock** (ballot dedupe, `weight≡1`, no-server-key voter, window-bounded ballots, `finalize()` raises early, quorum) · **(c)** `methods/book.py` projects accepted in-kind rails into **toritate `ledgerEntry`** categories (subsistence/vocation/care, `cashStipendUsd≡0`, no payroll/wage; liquidity NOT booked as income) + a **kanae-renderable `:flow/*` graph** (Public Fund → 扶持 → provider → maintainer; NOT the govt fundFlowEdge) · **(d)** `methods/couple.py` = **Displacement-Dividend coupling** (displacing actor surplus → TitheRouter 10% split `gross=tithe+earmark` exact → per-cohort Public-Fund earmark + the **G2 coupling gate**: a displacement is admissible only against a funded cohort earmark with headroom — *no live displacement without a funded cohort*). **EMPIRICAL** (`analyze.py`): abel→**auto** (robotics, in-kind 100%) · seth→**sbt-vote 5-1/48h✓** accepted (remote-teleop, in-kind 50% w/ external residual; real ballot tally) · eve→**council-lv7 pending** (new commons-land grant) · cain→**refused** (affiliate ad-share = Rider §2 hit) · noah→**outreach** (zero tenure share, minimal floor); coupling: `cohort-sanae` ($60k surplus, funded → $6k tithe + $54k earmark) **admissible** / `cohort-hataori` ($0, unfunded) **REFUSED** (G2); cash≡0 on every allocation+intent+ledgerEntry+seed line. **R1 (live-but-gated)**: `methods/live_gate.py` = the single membrane every outward leg crosses; `provision.dispatch_live`/`vote.finalize_binding`/`book.write_live`/`couple.commit_live` each **REFUSE (`LiveGateRefused`) by default** and fire only when the operator process flag `FUCHI_ALLOW_LIVE_<LEG>=1` + an operator attestation DID + Council **Lv6+** (**Lv7+** for `couple`, invariant-adjacent) + a **member signature** (no-server-key — server can never sign) are ALL present (yadori `YADORI_ALLOW_LIVE_RDAP` pattern). The gate is an **authorization membrane, never an invariant override**: cash≡0/no-server-key/in-kind-rails/48h-vote-timelock/G2-funded-cohort all hold in live mode (the `couple` leg stacks `LiveGateRefused` AND the G2 `ValueError`); `analyze.py` prints every leg refused + emits `out/live-gate-status.kotoba.edn`. **174 tests green** (15 allocate + 13 route + 8 provision + 11 vote + 10 book + 10 couple + **22 live_gate** + 15 analyze + **39 charter-invariants** + 3 lexicons + 9 consistency + 19 cells; one-command `run_tests.sh`). **DID registered** in INFRA_ACTORS → `did:web:etzhayyim.com:actor:fuchi` (resolvable + searchable) + actor-profile seed. **ZERO invariant amendments** — STRENGTHENS cash≡0/no-server-key/payoff帰属=etzhayyim/Charter-Rider §2(b)/非営利-donation-only. N1 not an investment fund · N2 not a wage employer (no-payroll preserved) · N3 not a lender (warifu is the 0% creditor; 扶持 only routes) · N4 not cash income · N5 no productivity surveillance · N6 not the owner of maintainer work · N7 not a gig marketplace · N8 allocator never decides. Honest R0+R1: design+offline allocation only; R1 a/b/c/d + the live-but-gated legs built+tested offline (actual live execution still needs the env flag flipped + Council ratification); `:representative` seed; live disbursement/provisioning/land-grant/binding-vote/displacement-coupling Council Lv6+ + operator gated (invariant-adjacent Lv7+) | 🟡 R0+R1 | 2606052300 | 06-05 |
| tasuke 助 | **free cybercrime-victim-support membrane** — answers *「日本でサイバー犯罪被害に遭った時に対応サポートする actor は設計されているか? 弁護士はつなげず、すべて無料で。申請フォーマットも。警察側の書面・そのまま使えるレポートも。」* (was: none — 8 adjacent actors [tadori/kurashimori/himotoki/chigiri/toritsugi/warifu/karakuri/danjo] each cover one slice but none walks a victim 相談→回復 or GENERATES the relief documents). 助 (たすけ) walks a consenting member hit by online crime (phishing/不正送金/account-takeover/サポート詐欺/ロマンス/投資詐欺/ransomware/なりすまし/架空請求), **for free**, from 相談トリアージ → 証拠保全 → ready-to-use 書面生成 → free public windows → アカウント復旧. It GENERATES the documents the victim themselves submits: **被害届/被害状況報告書/証拠目録/被害額算定書** (警察side), **銀行 不正送金 組戻し・口座凍結依頼** (振り込め詐欺救済法) + プラットフォーム凍結/復旧/開示依頼 (bank/platform-side), **アカウント復旧手順** (recovery-side) — so complete an officer/bank desk works straight from them. **Three load-bearing structural invariants** (schema `:db/allowed` + lexicon `:const` + Python `ValueError`, the nusa/tazuna/kamado/ake pattern): **G1 全て無料** — fee/charge/subscription UNREPRESENTABLE (`:support/cost-jpy [0]`, cash≡0; `analyze.py` asserts every case costs the victim ¥0) · **G2 本人作成・本人提出** — `:support/role` only {guide,draft-assist,self-submit}; 代理作成/代理提出 UNREPRESENTABLE (行政書士法/弁護士法 独占業務不踏; member authors+signs+submits) · **G3 警察authored不可** — `:doc/authored-by [:member]` only; police-authored 公文書 UNREPRESENTABLE (公文書偽造を構造的に排除; "そのまま使える"=officer works *from* it, not a forged official record). **弁護士へつながない** (G5 — only FREE public windows #9110/188/国民生活センター/フィッシング対策協議会/JPCERT/セーフライン/銀行/振り込め詐欺救済法); evidence encrypted-by-ref (G6, ADR-2605181100); member-signed (G7 no-server-key); Murakumo-only (G8); live filing draft-only at R0 (G9); non-adjudicating (G4 — a scam KIND is a routing label, not a verdict; danjo/chigiri boundary); sourcing-honest (G10). Ontology `cybercrime-victim-support-ontology.kotoba.edn` + 6 lex `com.etzhayyim.tasuke.*` + 5 coded cells (`.solve()` raises) + `triage.py`/`report_gen.py`/`evidence.py`/`analyze.py` + seed (5 cases + 9 free windows). Registered in INFRA_ACTORS + actor-profile-seed → `did:web:etzhayyim.com:actor:tasuke`. **78 tests green.** ZERO invariant amendments — STRENGTHENS cash≡0/no-server-key/encrypted-records/kotoba-canonical/非営利-donation-only. N1 not a 代理人 · N2 not an official-police-doc generator · N3 not an adjudicator · N4 not paid/no paid-counsel · N5 not 取立/自力救済 · N6 no server-signed submission · N7 not surveillance. Honest R0: design+offline generation only; no live filing/sending (G9); `:representative` registry needs primary-source verify (G10) | 🟡 R0 | 2606060900 | 06-05 |
| kawaraban 瓦版 | **news MEDIUM (real-media mirror + actor-to-actor wire)** — answers *「/apps の news actor を kotoba wasm ベースで設計, murakumo fleet で動くように。実際の news media の 面に合わせて, actor と actor をつなげる medium として」* (was: none — kataribe 語部 is etzhayyim's OWN press, a primary voice; nothing mirrored the world's news media or wired the first-party actors as a medium). kotoba-wasm-native, Murakumo fleet. Two faces over one Datom log: **(1) MIRROR** the world's real news media (outlets · **面** sections 一面/政治/経済/国際/社会/文化/科学/スポーツ · headlines · links) into the kotoba Datom log as an append-only as-of trail; each mirror = headline + canonical link + bounded fair-use excerpt + outlet (**LINKS OUT, never the body, never a verdict**). **(2) MEDIUM** the connective wire BETWEEN actors: each first-party actor's Datom as-of events project into the matching 面 as `:article/kind :actor-event` (feed-post membrane reuse, ADR-2605231902), every article carries `:news.mention` edges → the **article×mention×面 graph IS the actor-to-actor wire** (danjo finds → kawaraban carries → kanae renders; a chokepoint story links watari+watatsuna+mitooshi in one 国際 面; empirically `watari—4—watatsuna`). The charter-clean **inverse of a news app** — **11 structural gates** (schema `:db/allowed` + lexicon `const` + Python `ValueError`): **G1 mirror-not-adjudicator** (`:verdict`/`:truth-rating` unrepresentable; ake/danjo own truth) · **G2 no-ads/no-engagement-rank** (`:paid-placement`/`:sponsored`/`:engagement`/`:dwell-time` not enum members; Charter §1.13 + Rider §2) · **G3 no-reader-surveillance** (no `:reader` entity; `:personalized-for` unrepresentable; 面 identical for all) · **G4 copyright/link-out** (`:full-text` unrepresentable; only public facing pages, paywall/terminal excluded) · **G9 mirror-not-impersonation** (`:speak-as` unrepresentable; ADR-2606042330) · **G11 connective-by-construction** (every article `:mirror` or `:actor-event`; `:original` unrepresentable — a medium, not a source) · G5 sourcing-honest · G6 Murakumo-only · G7 no-server-key · G8 outward-gated · G10 非終末論 (`:issue/final` unrepresentable). Ontology `news-medium-ontology.kotoba.edn` + 6 lex `com.etzhayyim.kawaraban.*` + 5 coded cells (`.solve()` raises) + `route.py` (the medium: actor→面 wire + co-mention graph) / `analyze.py` (edition composer, G2 public-good ranking only) / `ingest.py` (offline membrane, refuses body/paywall/verdict, `--live` G8-gated) + `wasm/wit/world.wit` (kotoba-wasm component) + Murakumo fleet placement (5 cells / 12-tribe). `:representative` seed (7 outlets / 10 面 / 9 wires / 12 articles / 24 mentions). **46 tests green.** Registered in INFRA_ACTORS → `did:web:etzhayyim.com:actor:kawaraban`. ZERO invariant amendments — STRENGTHENS no-server-key/kotoba-canonical/feed-post-membrane/mirror invariant. N1 not a fact-checker · N2 not ad/engagement-funded · N3 not personalized · N4 not a republisher · N5 not an impersonator · N6 not a primary source (kataribe is). Honest R0: design+datafication+offline composition; no live RSS/outlet ingest, no live publish (G8) | 🟡 R0 | 2606061900 | 06-06 |

> **Note**: ADR ids `2605263400` and `2605263500` each label two distinct ADRs (parallel-agent race in the source); filename + actor name disambiguate. Tracked for a future ADR-id reconciliation.


## Repo Layout (Shannon-Optimal 8-Layer, ADR-2604251830)

```
etzhayyim/root/
├── 00-contracts/        # lexicons / bpmn / dmn / Rego policies / resources (JSON-LD)
├── 10-protocol/         # atproto, xrpc, lexicons-bundle, signal, did-etzhayyim,
│                        # wproto, at-client, signal-client,
│                        # kotoba-datomic (Holochain-iso composition spec, ADR-2605231400)
├── 20-actors/           # magatama (Pregel framework + host SDK + unispsc_agents/ 18,345 LangGraph agents per ADR-2605171300),
│                        #   magatama-go, kami-engine-sdk, effect-cypher, etzhayyim-bpmn-sdk,
│                        #   etzhayyim-sdk (RW-free substrate, ADR-2605172000+2605172100)
│                        #   kuni-umi      planetary-infra producer    (ADR-2605201400)
│                        # Tier-B religious-corp actors (30): each has ADR + manifest + cells + lex.
│                        #   See Status § "Tier-B actors" for the full roster (name · purpose · ADR).
│                        #   Per-actor gates/prohibitions live in each actor's ADR + 20-actors/<name>/CLAUDE.md.
├── 30-graph/            # graph-schema, kagami, risingwave-udf, vectorization
├── 40-engine/           # Rust workspaces: kami-engine (reusable engine — git submodule
│                        #     of github.com/etzhayyim/kami-engine per ADR-2606011500 §4;
│                        #     kami-engine-sdk nested inside; `git submodule update
│                        #     --init --recursive 40-engine/kami-engine` to populate),
│                        #   kami-apps (L3 etzhayyim *.etzhayyim.com product apps —
│                        #     kami-app-{bim,cad,live,maps3d,animeka-timeline};
│                        #     robotics/sim apps are maintained in the kami-engine
│                        #     submodule per ADR-2606011500), llm,
│                        #   kotoba (storage substrate engine — git submodule of
│                        #   github.com/etzhayyim/kotoba; 17 crates Apache-2.0;
│                        #   subsumes ipfs-pinner / nats-jetstream-* / mst-projector
│                        #   / lancedb-wasm / tonbo / etzhayyim-xrpc-proxy /
│                        #   libsignal wrappers; kotoba-llm local-inference
│                        #   constitutionally disabled per ADR-2605215000 +
│                        #   Charter Rider §2(i); ADR-2605262130 Phase 0)
├── 50-infra/            # SEEDED: geth-private, holochain, ipfs, blockscout,
│                        #   k8s/atproto-pds, k8s/murakumo-kubelet (migrated 2026-05-17),
│                        #   lancedb-wasm, yata, tonbo,
│                        #   nats-tiered-storage, nats-jetstream-{objectstore-s3, kv-resp},
│                        #   sveltejs-adapter-wasm, spin-tinygo-flight
│                        # SUBSTRATE (ADR-2605171800 + 2605172100 + 2605172200):
│                        #   etzhayyim-did-web/ (CF Worker, LIVE 2026-05-17T03:25Z)
│                        #   mst-projector/   (Stage 3, scaffold)
│                        #   ipfs-pinner/     (Stage 4, scaffold)
│                        #   l2-anchor-contract/ (Stage 5a, Foundry Solidity)
│                        #   anchor-cron/     (Stage 5b, K8s CronJob)
│                        #   etzhayyim-paymaster/ (ERC-4337, Foundry Solidity)
│                        #   openmail-postage/ (Postage.sol, Foundry, Phase 1 scaffold)
│                        # RELIGIOUS-CORP CONSTITUTIONAL (ADR-2605192100 wave, 2026-05-19):
│                        #   etzhayyim-charters-compliance/ (Council attestation single SoT)
│                        #   etzhayyim-tithe-router/        (10% donation → Public Fund atomic split)
│                        #   etzhayyim-public-fund/         (5-of-7 Safe + 1 SBT = 1 vote)
│                        #   etzhayyim-land-registry/       (geth-private + Base L2 ERC-721 mirror)
│                        #   etzhayyim-force-authorization/ (Transparent Force, 1 SBT = 1 vote)
│                        #   murakumo/fleet.toml            (10-node cell placement)
├── 60-apps/             # open-*, public-*, atproto, ameno, yoro, comfyui, watashi
│                        # FIRST RW-FREE REFERENCE IMPL: open-isco/rw-free/
│                        # MAC MINI FLEET: comfyui/ (migrated 2026-05-17)
│                        # RELIGIOUS-CORP:
│                        #   etzhayyim-transparent-force-rd/ (open-source R&D registry per ADR-2605192315)
├── 70-tools/            # etzhayyim-cli, cdn
│                        #   charter-rider-applicator/      (retro-active Rider applier, ADR-2605192200)
├── 90-docs/             # CLAUDE.md (docs rules), adr/, baien/
├── CHARTER-RIDER.md     # Apache 2.0 + Charter Compliance Rider v3.0 (per ADR-2605192200 + 2606062100)
├── COUNCIL.md           # 5-seat Bootstrap Council roster (per ADR-2605192300)
├── COUNCIL-BOOTSTRAP-RFP.md  # 30-day public RFP for Seats 2-5 (2026-05-20 → 2026-06-19)
├── LANDS.md             # 4-layer permanent land record roster (per ADR-2605192245)
├── CLAUDE.md            # this file
├── deps.toml            # SSoT for [platform.operating_entity] + monorepo state
├── LICENSE              # Apache 2.0 (NOTICE/CHARTER-RIDER.md adds Rider conditions)
├── MEMBERS.md           # 信者 dual-permanent record (per ADR-2605172600)
├── README.md            # public-facing
├── lefthook.yml         # pre-commit (trailing-ws + EOF; full hooks pending)
└── .gitignore
```

## Worktree isolation (CRITICAL — concurrent-agent safety)

**This repo's main checkout (`/Users/junkawasaki/github/etzhayyim-root`) is shared and
raced by multiple concurrent Claude agents** that `git checkout` different feature branches
in the *same* working tree. Untracked / uncommitted work in the shared checkout is fragile:
a sibling agent's `checkout` / `reset` / `clean` can wipe it without warning (observed
2026-06-04 — an entire untracked actor scaffold vanished mid-session).

**Rule — before ANY substantive multi-file work, Claude MUST isolate into a git worktree:**

1. **Enter a worktree first.** Use the `EnterWorktree` tool (creates an isolated checkout
   under `.claude/worktrees/<name>` on a fresh branch off `origin/main`) — or, outside this
   harness, `git worktree add .claude/worktrees/<name> origin/main`. Do the work there, not
   in the shared main checkout. (The `ooyake` agent already runs isolated in `/private/tmp/owt56`.)
2. **Commit early and often inside the worktree.** A commit is the only durable unit in a
   raced tree; a `git reset --hard` by a sibling cannot destroy a commit (recoverable via
   reflog), but it *will* destroy untracked files. Never leave a completed unit of work
   uncommitted.
3. **Scope commits to your own paths.** When the shared index already holds another agent's
   staged files, commit with an explicit pathspec (`git commit -- <your paths>`) so you do
   not sweep up a sibling's work.
4. **Branch off `origin/main`, not the current shared HEAD**, so a sibling's in-flight
   commit (e.g. another actor landing on the branch you happened to be on) does not become
   part of your history.
5. **Back up irreplaceable untracked work outside the git tree** (`cp -R … /tmp/…`) if you
   cannot commit immediately — insurance against a concurrent wipe.
6. **Treat the shared main checkout as read-only / not-yours.** It stays chronically dirty +
   divergent (uncommitted edits + a HEAD that is both ahead and behind `origin`); its working
   tree is a mix of *other* agents' in-flight work. Never commit it wholesale or assume its
   uncommitted state is yours.
7. **1 branch = 1 owner.** Do NOT push a divergent local commit onto a sibling's actively-CI'd
   PR branch (you'd graft onto their in-flight work + risk conflicts). To land such a commit,
   spin a fresh worktree off `origin/main`, `cherry-pick` it, and open its OWN PR (done
   2026-06-06: local `9a8600db59` → PR #1188, leaving the sibling's PR #1174 untouched).
8. **cherry-picks from the shared checkout drag in yoro build artifacts.** Conflicts in
   `60-apps/etzhayyim-project-yoro/**/static/_app/immutable/*` (hashed SvelteKit chunks) are
   generated noise — resolve by restoring yoro to `origin/main` (`git checkout origin/main --
   60-apps/etzhayyim-project-yoro && git add`), never hand-merge them.

**Shell gotcha (zsh):** `$PIPESTATUS` is unset in zsh (it's `$pipestatus`, 1-indexed), so
`cmd | tail; echo ${PIPESTATUS[0]}` silently reports `tail`'s exit, not `cmd`'s. Check a
command's real exit before piping it (e.g. `git apply` failed-but-looked-OK this way).

Exit with `ExitWorktree` (`keep` to preserve the branch). The session's cron `/loop`
iterations continue inside whatever worktree the session is in.

**Rule — clean up once the branch is MERGED.** A worktree + branch is durable only while its
work is in flight. **After the branch's PR is merged into `origin/main`, delete BOTH the
worktree and the branch** — a merged branch left on disk is dead weight that clutters
`git worktree list` / `git branch` and invites confusion (a sibling agent checking out a
stale merged branch). Do NOT delete before merge (an open PR's branch is the only durable
copy of the work). Sequence once merge is confirmed:

1. `ExitWorktree` with `action: "remove"` (removes the `.claude/worktrees/<name>` checkout +
   its branch). It refuses if there are uncommitted/unmerged changes — that refusal means the
   merge is NOT actually complete, so stop and investigate, do not force.
2. Outside the harness, the equivalent is `git worktree remove .claude/worktrees/<name>` then
   `git branch -d <branch>` (lowercase `-d` = merged-only; it refuses an unmerged branch — use
   `-D` ONLY after confirming the work is truly merged). Prune stale registrations with
   `git worktree prune`. The `clean_gone` command also sweeps branches whose remote is `[gone]`.

Only the worktree whose PR is still open (or whose work is unmerged) is kept.

## ADR Authority (per ADR-2605170900)

**This repo is canonical for religious-corp open ADRs.**

- ADR placement matrix → `90-docs/adr/README.md`
- ADR ID convention → `90-docs/CLAUDE.md` § "ADR ID Convention"
- Template → `90-docs/adr/template.md`

## Do Not

- Do not introduce legacy organisation-specific prefixes in newly authored code. Use `etzhayyim-` or no prefix. Existing seeded files with legacy prefixes will be renamed in a follow-up cutover.
- Do not weaken the Apache 2.0 license default. Religious-corp public-interest activity requires permissive license.
- Do not weaken the Charter Compliance Rider v3.0. The prohibited categories §2(a)-(k) and 三層 enforcement (L1 license / L2 便益 / L3 評価) derive from the Tier-0 priorities; per ADR-2606062100 they are Tier-1 Derived Policy, amendable ONLY by Council Lv7+ unanimity + a priority-conformance attestation (never weaker). The Tier-0 priorities themselves (incl. 永久記憶=神の監視) are fork-only. Do not re-introduce the v2.0 "absolute weapons ban" or "no-fossil-extraction" framings — v3.0 reframes them as transparent-defensive-force-permitted (§2(a)) and carbon-balance-measured (§2(d)).
- Do not add Charter Rider to 3rd-party vendored code (`lib/`, `vendor/`, `*-fork/`). Apache 2.0 §4 requires preserving original NOTICE of 3rd-party works. `charter-rider-applicator` already skips these patterns.
- Do not introduce `transfer()` / `burn()` / `setOwner()` to `LandRegistry.sol`. Donated land is constitutionally inalienable (waqf-equivalent, ADR-2605192245).
- Do not propose proprietary 兵器設計 or covert force operations. ADR-2605192100 §1.12.B constitutional invariant requires open-source + on-chain 監視 + 1 SBT = 1 vote.
- Do not include the Book of Revelation (黙示録/啓示の書) or eschatological content as religious doctrine. Per ADR-2605192100 §1.15, etzhayyim is non-eschatological.
- Do not commit secrets. Private DID key lives in macOS Keychain (`service=etzhayyim, account=DID_PRIVATE_KEY_ED25519`) + 1Password mirror.
- Do not introduce RunPod / Vertex AI direct / OpenAI direct (without Murakumo proxy) / Anthropic-direct from vendor key / Linode GPU / AWS Bedrock direct / any commercial GPU rental into religious-corp inference paths. ADR-2605215000 makes Murakumo fleet (LiteLLM 127.0.0.1:4000 + EVO-X2 LAN 192.168.1.70 + per-node Ollama gemma3:4b) the sole inference SSoT. Vendor (`etzhayyim.com`) keeps its commercial GPU pool for paid SaaS workloads; religious-corp callers must not invoke vendor RunPod paths (consent capability boundary).
- Do not rename `etzhayyim-*` identifiers in `50-infra/cluster/murakumo/` or `20-actors/magatama/py/` outside the Step 8 cutover wave. Per ADR-2605214000 §3 + ADR-2605215000 §4, the renames are itemised in `MIGRATION-NOTES.md` files and must execute as one atomic PR after legal registration (repo-root CLAUDE.md §Status row 8). Partial rename breaks runtime (env vars + config dir + DNS suffix are interdependent).

## Baien tooling index (2026-05-23 wave)

| Tool / dir | Purpose | Key ADR |
|---|---|---|
| `e7m bench micro` | 15-prompt verifiable smoke for baien (`70-tools/scripts/bench/baien-microbench/`) | ADR-2605092350 |
| `e7m bench core4` | lm-eval-harness Core 4 (IFEval / GPQA / MMLU-Redux / Global PIQA) via `lm_eval_wrapper.py` (inductor probe suppressed) | — |
| `e7m bench distill` | LangGraph ReAct loop: analyze → fetch_dataset (HF, default) **or** select_teacher (fallback) → SFT (peft+trl LoRA on bf16 master) → microbench eval. `commit_node` appends `90-docs/baien/distilled-models.jsonl` → codegen → `llm-model-registry-distilled.ts` | ADR-2605231300 |
| `e7m bench rope-extend` | Stage 1 of context extension — runs `microbench_long.py` under 3 RoPE configs (baseline / linear×4 / NTK×4) and emits side-by-side pass matrix | ADR-2605231600 |
| `bgp-submit --generator hunyuan3d|pixal3d` | baien-graft 3D dataset pipeline; two generators (Hunyuan3D-2 default / TencentARC Pixal3D-T cascade@512) | ADR-2605202115 |
| `etzhayyim_organism.sensors.charter_rider.scan()` | Canonical §2(a)..(h) content scanner; used by `baien-distill.validate` and recommended for any pipeline that ingests / generates text into first-party artifacts | ADR-2605192200 |
| `70-tools/scripts/llm-registry/gen-distilled-entries.mjs` | distilled-models.jsonl → `llm-model-registry-distilled.ts` codegen (2-phase ship: manifest + reviewer-gated TS) | — |

Snapshot artifacts (run results) live under `90-docs/baien/`:
- `frontier-bench-snapshot-260523.md` — frontier-LLM §A reference + baien microbench results
- `context-extend-snapshot-260523.md` — rope-extend Stage 1 results stub (awaits run)
- `distilled-models.jsonl` — committed adapter manifest (codegen source)

## Future Work

- **lefthook hooks** full set (adr-validate, docs-registry, lint-dangerous-query, llm-model-ssot)
- **GitHub Actions CI**: lint / type-check / build / test per layer
- **Dependabot** for npm + cargo + uv ecosystems
- **`90-docs/_registry/docs.json`** generator + validator
- **baien-distill Stage 2/3** (YaRN + LoRA, LongRoPE continual) per ADR-2605231600
- **Core 3 bench strategy revision** — switch from `_generative` (~28h/task) to `_completions` (loglikelihood, ~30 min) for next baien snapshot

## Substrate boundary (CRITICAL — ADRs 2605172000 + 2605172100 + religious-corp wave)

This repo is **blockchain-self-contained**. Hard rules enforced by ADRs and (future) CI hooks:

| Concern | Allowed | Prohibited |
|---|---|---|
| State | **kotoba Datom log** (content-addressed EAVT Datalog — Datomic-isomorphic; FIRST-CLASS canonical state, ADR-2605312345) — subordinate layers: IPFS = block backend (CIDv1 cold tier/DHT) · AT Protocol MST = ingress/interop wire · Base L2 = trust anchor over commit-DAG root | RisingWave / Postgres / Kysely / centralized DB; treating MST/IPFS as the canonical state home (they materialize the Datom log, not vice-versa) |
| Payment | USDC on Base L2 + ERC-4337 Smart Account + TitheRouter (10% auto-split) | Stripe / PayPal / Square / fiat processors |
| Payment purpose | `donation` / `kisha` / `grant` / `tithe` / `escrow-refund` + (SBT↔SBT internal carve-out) `internal-purchase` / `internal-subscription` / `internal-promo` | `subscription` / `purchase` / `tip` for external; commercial sale for SaaS tier |
| Advertising | etzhayyim 自身の religious 活動 案内 (internal-promo) のみ | 第三者広告 / AdSense / Meta Pixel / アフィリエイト / GA4 広告連携 |
| Identity | did:web:etzhayyim.com + did:plc + WebAuthn passkey + Adherent SBT | server-issued JWTs without DID binding |
| License | Apache 2.0 + Charter Compliance Rider v3.0 (`/CHARTER-RIDER.md`, per ADR-2606062100) | Apache 2.0 alone (Rider required) / proprietary / no-NOTICE |
| Charter compliance (3-tier) | ChartersComplianceRegistry attestation flow + Council Lv6+ ≥3 multisig | bypass of three-tier enforcement (L1 license / L2 便益拒否 / L3 評価=0) |
| Land trust | etzhayyim Land Registry 4-layer (Base L2 NFT + geth-private + IPFS + LANDS.md) | transfer / burn / sale / private ownership of donated land |
| Religious force | Transparent (on-chain log + open-source + 1 SBT = 1 vote) | Proprietary / covert / independent military arm / state military alliance |
| Content (Eros) | 合意ある成人性表現 (産霊 / 雅歌 整合) | 児童性的表現 / 非合意 / Wellbecoming 違反 addictive design |
| Content (Gore) | 教育 / 歴史 / 宗教 / 人権告発 文脈の暴力 imagery のみ | 無目的暴力 entertainment / desensitization 設計 |
| Confidentiality (ADR-2605181100) | `com.etzhayyim.encrypted.*` (XChaCha20-Poly1305 envelope + Signal-wrapped per-recipient keys, DID-bound) | Plaintext private records on MST / app-side libsignal / noble-ciphers imports |
| Substrate client imports | Only via `@etzhayyim/sdk` | Direct `@atproto/api` / `viem` / IPFS client / `@noble/ciphers` / `@signalapp/libsignal-client` from app code |
| Substrate engine | `kotoba` (`40-engine/kotoba/`, ADR-2605262130) — content-addressed Datalog + Pregel + Signal + WASM Component Model in one Rust workspace; 17 crates; canonical impl of every substrate primitive (supersedes kotoba-datomic composition spec ADR-2605231400). Its **Datom log is the first-class canonical state** (ADR-2605312345), not merely the read engine | inventing a parallel substrate engine name without ADR; importing kotoba-datomic / RW / Lance / Iroh as projection backends |
| Read path (hot-path queries) | `kotoba-kqe` arrangements (EAVT / AEVT / AVET / VAET) directly over the canonical Datom log (content-addressed blocks; ADR-2605312345); no separate projection layer (ADR-2605262130 D7 + N8 supersedes ADR-2605231500 kotoba-datomic-projection rules). First L1-projection app `feed-discover` (50-infra/mst-projector/projection/, ADR-2605231902) preserved unchanged; read backend migrates to kotoba-kqe at Phase 2.5 | RisingWave / Postgres / Lance / DuckDB / SQLite as projection or cache; "kotoba-datomic-projection" marker comments — both deprecated by ADR-2605262130 |
| Server-side signing capability (ADR-2605231525, Council ratify pending) | Member wallet sign (USDC), member passkey-derived ES256 (session), community-operator DID (bulk-ingest), Council 5-of-7 Safe (governance), read-only RPC / firehose subscribe / IPFS pin / static asset serve | Any platform-held private key, master credential, or signing token in etzhayyim-operated Workers / pods / CronJobs / CI / hosted bots. Exemption: `// no-server-key: read-only` marker on documented Stage handover rollback windows. Enforced by `e7m verify` (9th invariant) |
| GPU / inference | LiteLLM gateway (127.0.0.1:4000) + EVO-X2 LAN (192.168.1.70) + per-node Mac mini Ollama gemma3:4b (per ADR-2605215000) | RunPod / OpenAI direct / Vertex AI direct / Anthropic-direct from vendor key / Linode GPU / AWS Bedrock direct / any commercial GPU rental |

Apps that need fiat / paid features call an external backend via XRPC consent-capability (progressive enhancement, **non-profit領収書 用途のみ** per ADR-2605192115 §4). Open app remains operational without it.

## SSoT pointers

- `deps.toml` — operating entity, substrate rules, L2 contracts, DNS records, ADR registry, module registry
- `90-docs/CLAUDE.md` — docs system rules + ADR placement policy
- `90-docs/adr/README.md` — ADR index
- `90-docs/adr/2605192100-etzhayyim-mission-charter.md` — religious-corp 上位憲章 (mission + constitutional constants)
- `90-docs/adr/2605192200-etzhayyim-ip-free-release-charter-rider.md` — License + Rider 正本 spec (v2.0)
- `90-docs/adr/2605192245-etzhayyim-global-land-sovereignty.md` — Land Trust 4-layer architecture
- `90-docs/adr/2605192415-etzhayyim-religious-corp-daemon-architecture.md` — Pregel cell catalog + Murakumo deployment
- `/CHARTER-RIDER.md` — license addendum canonical text
- `/LANDS.md` — Land Trust roster
- `/MEMBERS.md` — 信者 roster
- `50-infra/murakumo/fleet.toml` — religious-corp cell placement (10 nodes × 15 cells)
- `20-actors/etzhayyim-sdk/README.md` — SDK API surface + hard rules
- `20-actors/magatama/cells/README.md` — religious-corp Pregel cell catalog
- `90-docs/adr/2605262130-kotoba-storage-substrate-unification.md` — canonical storage substrate engine (kotoba); supersedes kotoba-datomic composition + projection layers; no RisingWave
- `90-docs/adr/2605312345-kotoba-datom-first-class-canonical-state.md` — kotoba Datom log = first-class canonical state; IPFS = block backend, MST = ingress/interop wire, Base L2 = trust anchor (clarifies 2605262130 layering)
- `40-engine/kotoba/README.md` — kotoba upstream README (17 crates)
- `10-protocol/kotoba-datomic/SPEC.md` — (superseded by ADR-2605262130; deprecation banner Phase 0.5; retained one R-cycle then archived)
- `90-docs/adr/2605231400-kotoba-datomic-holochain-iso-substrate.md` — (superseded by ADR-2605262130)
- `90-docs/adr/2605231500-kotoba-datomic-projection.md` — (superseded by ADR-2605262130; no projection layer under kotoba)
- `90-docs/adr/2605231902-feed-post-membrane-and-feed-discover-projection.md` — first end-to-end MST §4 membrane + L1-projection app (app.bsky.feed.post); **preserved unchanged**; Phase 2.5 read-path migration to kotoba-kqe per ADR-2605262130
- `90-docs/adr/2605214000-etzhayyim-murakumo-mesh-no-vke-and-lexicon-port-rules.md` — Murakumo distributed cluster (no-VKE mesh) + lexicon port verdict taxonomy
- `90-docs/adr/2605215000-etzhayyim-inference-murakumo-only-no-runpod.md` — etzhayyim inference Murakumo-fleet-only (no RunPod)

## References

- This repo (public): https://github.com/etzhayyim/root
- Domain landing: https://etzhayyim.com
- DID resolver: https://etzhayyim.com/.well-known/did.json (LIVE; CF Worker at zone `etzhayyim.com`, DNS AAAA `@` `100::` proxied, deployed 2026-05-17T03:25Z)
