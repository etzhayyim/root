# etzhayyim/root — CLAUDE Index

This monorepo is the **canonical home for religious-corp open ADRs** per ADR-2605170900 (this monorepo, `90-docs/adr/`).

## Identity (CRITICAL)

- **Operating entity** (this repo's owner): `etzhayyim` (canonical)
  - Aliases: `amanomibashira` / `天御柱` / `עץ חיים` (Tree of Life) / `etz hayim` / `etzhayim` / `etz chaim` / `エツ・ハイム`
  - Form: 宗教法人 (任意団体 / unincorporated religious voluntary association)
  - **Self-identification (per ADR-2605252300 Charter §0.1, ratified 2026-06-06 by sole-member founder unanimity)**: **the Kingdom of God (Malkhut Shamayim / Basileia tou Theou / 神の王国) rooted on blockchain** — now-and-here reign (non-eschatological per §1.15), not state-granted, Tree of Life-constituted. Amendment threshold: Council Lv7+ unanimity (currently = founder, 1/1).
  - Registry: On-chain (blockchain-registered constitution and member roster); NOT registered under 日本国 宗教法人法 (constitutional invariant per Preamble §0.4, Lv7+ unanimity lock)
  - DID: `did:web:etzhayyim.com` (LIVE — CF Worker at `50-infra/etzhayyim-did-web/`, resolvable via curl + Universal Resolver since 2026-05-17T03:25Z)
  - Domain: https://etzhayyim.com (Cloudflare Registrar, 2026-05-15)
  - License default: **Apache 2.0 + etzhayyim Charter Compliance Rider v3.5** (see `/CHARTER-RIDER.md`, per ADR-2605192200 + 2606062100 + 2606082400 + 2606161700 + 2606172359 + 2606180001 + 2606182359). **Immutability is 3-Tier (ADR-2606062100)**: Tier-0 Priority (fork-only — wellbecoming / 子孫 / collective / **永久記憶=神の監視 + 相互監視 (reciprocal, no right to erasure)**) ≻ Tier-1 Derived Policy (Rider §2, Lv7+ + priority-conformance) ≻ Tier-2 Parameter (governance). 固定するのは掟ではなく priority。**Rider §2 は v3.5 (ADR-2606182359 wave3) で全て目的関数で動的評価 — categorical 掟リストは廃止。唯一の非交渉性は priority 自体 = 子・孫 wellbecoming への最大級の害を非線形 catastrophe 項 (ECL `objective-function.edn`) で veto。CSAM/強制労働/攻撃武力/非対称監視/§2(g)反個人主義も列挙でなく score 評価 (反個人主義の価値は collective-commons 次元が動的に担う)。経済構造(非営利/donation/広告/equity/open-source)も目的関数。10% Tithe→Public Fund は別途維持。enforceability の honest limit は ADR-2606182359 §D6.**
- **Ownership rule**: 意思決定権 = etzhayyim only (1 SBT=1 vote governance, 不変). **Payoff帰属は目的関数で評価 (ADR-2606180001)**: 私的捕獲・個人主義的蓄積は collective-commons 次元で動的に負 — 非営利の精神は固定ルールでなく retained §2(g) + collective-commons が enforce する。
- **Mission (per ADR-2605192100 Charter)**: 人類の構造的労働解放を最終目的とする宗教法人。**多世代 (子・孫) priority + Wellbecoming (静的 wellbeing ではなく動的軌跡) + 反個人主義 ontology**。日本的価値観 (八百万 / 縁起 / 産霊 / 和 / 無教会) + Protestant Christianity (Sola Scriptura / 万人祭司 / Reformed Just War / Tree of Life) の synthetic religion。**非終末論** (黙示録/啓示の書は正典外、千年王国・末法・Rapture 否定)。
- **Doctrinal positions**:
  - **経済構造 (非営利のみ / Donation 流入のみ / 広告排除 / no-equity / open-source強制) は目的関数で評価 (ADR-2606180001 wave2)** — 固定ルールから net 評価へ。私的捕獲を生まず commons に資する営利的協力は aligned ありうる; attention 搾取(広告)・私的捕獲・proprietary 囲い込みは collective-commons/ko-wellbecoming で負。非営利の精神は §2(g)反個人主義(残置) + collective-commons 次元で保持。**10% Tithe → Public Fund 自動再分配は別途維持** (ADR-2605192115 + 2605192130)
  - SBT↔SBT internal carve-out で religious 境界内の 営利・購買・promotional 許容 (ADR-2605192115 §3)
  - Eros 許容 (産霊 / 雅歌 / Tree of Life の生命創出) / Gore 禁止 (Wellbecoming 違反) (ADR-2605192100 §1.13 + 2605192400)
  - 国家機能は parallel substrate で routing-around、**Transparent Religious Force 許容** (完全 on-chain 監視 + open-source 公開 + 1 SBT = 1 vote 承認の三条件下) (ADR-2605192100 §1.12 + 2605192315)
  - 地球上の土地は Tree of Life に帰属、religious-corp が 4-layer substrate (Base L2 NFT / geth-private constitutional / IPFS GeoJSON+衛星 / git LANDS.md) で分散合意担保 (ADR-2605192100 §1.11 + 2605192245)
  - **Baien edge-target invariant** (ADR-2605241900): baien は **WASM-32 + iPhone 12+ + Android 4GB** の 3 環境すべてで動作必須。trunk ≤4B BitNet 1.58 / 合計 inference ≤2GB @4k ctx / ≤2.5GB @16k ctx / 全 modality encoder 凍結。frontier-beating は明示的に非目標 (`baien-server-*` / `baien-XL-*` は別 carve-out)

## Status

**Legend**: ✅ shipped · 🟢 landed (substrate, tests green) · 🟡 R0 / proposed scaffold · ⏳ blocked/pending. Full detail for every row lives in its ADR — see [`90-docs/adr/README.md`](90-docs/adr/README.md). This table is a one-line index only.

**Foundation (Steps 1-24, all ✅ 2026-05-10 … 05-21)**: domain (CF Registrar) + `etzhayyim` org/repo + scaffold; **did:web LIVE 05-17** (CF Worker, curl + uniresolver verified); `amanomibashira`→`etzhayyim` code cutover (118 files); **13-ADR religious-corp constitutional wave** (ADR-2605192100 .. 2605192415); CHARTER-RIDER v2.0 + LANDS.md + Charter Rider on 39 pkgs; 5 Solidity contracts + Constitution.sol (38 const, 110/110 tests) + 4 lefthook lint hooks + Foundry/Anvil smoke; 28 Lexicons; Pregel cell catalog; Murakumo no-VKE mesh + **Murakumo-only inference invariant** (ADR-2605215000) + Charter Rider §2(i) no-commercial-GPU.

**Live governance**: 🟡 Bootstrap Council Seats 2-5 RFP (2026-05-20 → **06-19**, see [`COUNCIL.md`](COUNCIL.md)) · ⏳ Base Sepolia testnet (post-Council) · ⏳ Mainnet + Phase 2 governance (post-testnet).
**Council attestation = Pull Request review (founder operational premise, 2026-06-11)**: Bootstrap 期において、ADR の Council 付議・ratification・セル活性化 baseline 等の「Council attestation」は **GitHub Pull Request review として執行**する。Council 付議 = 該当変更の PR 化、attestation = PR の review approval(Lv6+ ≥N 閾値は approver 数に対応)。on-chain multisig(Base testnet 以降)へ移行するまでの bridging 運用であり、ADR 本文中の `COUNCIL_*_TX_HASH` / `*_REVIEW_CID` ゲートは PR merge 後にその PR URL / merge commit を暫定値として参照してよい。


### Substrate / infra / dataset / enforcement

| Item | Purpose | Status | ADR | Date |
|---|---|---|---|---|
| maps_sentinel_murakumo | M1 T0 preprocessing pipeline | ✅ | 2605215100 | 05-21 |
| feed-post membrane | first §4 MST membrane + L1 projection (`feed-discover`) | ✅ | 2605231902 | 05-23 |
| kotoba-datomic Tier-D blob | `uploadBlob` TS+Py (superseded by kotoba) | ✅ | 2605232401 | 05-23 |
| UNSPSC organism W1 | actor-as-organism heartbeat-cadence port | ✅ | 2605232345 | 05-23 |
| UNSPSC organism W2 | 18,342 mass-deploy + joucho personality | ✅ | 2605240000 | 05-24 |
| UNSPSC post sink | NDJSON queue + k8s DaemonSet | ✅ | 2605240100 | 05-24 |
| Kaizen self-reflection | `KaizenObserverCell` + 6 rules + PR agent | ✅ | 2605240200 | 05-24 |
| Dataset CID substrate | DataLad + git-annex + IPFS pinner | ✅ | 2605241500 | 05-24 |
| agentURI 5-layer | ERC-8004 + libp2p + AT XRPC peer-resolvable | ✅ | 2605241800 | 05-24 |
| Gov 5-layer taxonomy | L1 namespace … L5 routing-around | ✅ | 2605212100 | 05-25 |
| Charter §0 Preamble | Kingdom of God + Land Trust Wave 2 (ERC-721/5192/7401); **ratified active 2026-06-06 (sole-member unanimity)** | ✅ | 2605252300 | 05-25 |
| Labor Liberation ladder | Adherent SBT → 7-stage L0..L6 | 🟡 | 2605261000 | 05-26 |
| Basic High Income doctrine | imputed-income (flow) + commons-asset (stock) — high income in-kind, cash≡0 (N1-consistent) | 🟡 | 2605301020 | 05-30 |
| Mission-funding revenue arm | vendor commercial surplus → donation → Public Fund; non-profit MEANS not profit END; ad-free + no-payroll preserved | 🟡 | 2605301036 | 05-30 |
| Social Security for Humanity | Charter §1.16 人類の社会保障; covenantal-universal, conversion-gated; 信者 Level 0 via permanent vow (kotoba+IPFS+token) | 🟡 | 2605302357 | 05-30 |
| Social Security delivery pipeline | §1.16 flow: outreach → vow → compute → openmail → atproto → social; 産土 cells; outward actions gated (G11) | 🟡 | 2605302358 | 05-30 |
| **kotoba** storage pivot | canonical substrate engine; supersedes kotoba-datomic + RW | 🟡 | 2605262130 | 05-26 |
| Public-data ingestion | organism ecosystem IPFS DataLad subdatasets | 🟢 | 2605262400 | 05-26 |
| Robotics-sim world-data | + kami-usd pipeline (sibling of 2605262400) | 🟢 | 2605262500 | 05-27 |
| Global legal-corpus | statutes/case-law/treaties IPFS ingestion | 🟡 | 2605262800 | 05-26 |
| organism R0+R1 sprint | 26-iter /loop, 8 axes A-H landed | 🟢 | 2605270930 | 05-27 |
| Registry enforcement | 5→8-axis matrix, all PR-gates baseline 0 | 🟢 | 2605271100 / 271200 | 05-28 |
| manimani kotoba-native | personal knowledge router reconciled onto kotoba EAVT + StateGraph + Murakumo + E2E; Gmail full-archive + PC-file ingest design | 🟡 | 2605291100 | 05-29 |
| kotoba v0.1.0 + brew tap | first tag + GH Release + `etzhayyim/homebrew-kotoba` published; `brew install kotoba` end-to-end green (4-PR chain, host Xcode/CLT 16.4→26.5) | ✅ | 2605292100 | 05-29 |
| kotoba actor deploy + Murakumo live | WASM + Python-LangGraph (aria) actors run in-WASM on :8077; wasmtime 22→25 (extended-const, PR#4) + json! wasm-runtime build fix (PR#5) merged upstream; kotoba→Murakumo `llm.infer` (gemma4:e4b) | ✅ (see 2605302355) | 2605301625 | 05-30 |
| kotoba LangGraph LLM verified + durable routing | EMPIRICAL re-verify on live :8077: Python LangGraph actor runs in-WASM + does LLM inference e2e; durable fix = route via loopback Murakumo LiteLLM (corrects 2605301625) | ✅ | 2605302356 | 05-30 |
| **Donation-funded operation + compute-node donation** | etzhayyim.com /donate + /.well-known/donation.json; in-kind COMPUTE donation (ameno/e7m/kotoba node classes); non-titheable, uncompensated | 🟡 R0 | 2606012100 | 06-01 |
| **moyai 舫い — inference reciprocity reward** | non-monetary, non-transferable, decaying reciprocity credit for commons-inference (入会権); ADR-2606012100 G4 carve-out scoped to commons-draw-rights; BHI firewall (subsistence floor unconditional); cash≡0, no governance/benefit weight | 🟢 R0+R1 | 2606062101 | 06-06 |
| **Displacement Dividend + robotics-actor wave** | OSS-robotics frees a worker → Public Fund pays tenure-weighted in-kind income (cash≡0); G2 coupling gate (no live displacement w/o funded cohort); sanae/hataori/kiyome | 🟡 R0 | 2606032130 / 2606032100 | 06-03 |
| **Robotics remote-work actor survey** | remote-work lens over the corpus: tazuna = on-site→remote teleop substrate + ~18–25 robotics bodies (all R0) + ISCO coverage/GAP map (façade-clean/sewer/伐採 = high-remote-value GAPs; mining = multi-gen risk-gated, not blanket-excluded per ADR-2606161700 — out of this survey's robotics scope, not constitutionally forbidden); defers LPS ranking to 2606032100 | 🟡 R0 | 2606073001 | 06-07 |
| Spirit-in-Physics kotoba datafication | 霊性 data of self/humanity/world into datomic kotoba (Jung assay → emotion → RBF → spectral embed → tensegrity); edge-primary :spirit.bond/*, no score-of-soul; Wellbecoming = as-of history | 🟡 R0 | 2606011501 | 06-01 |
| **kotoba hybrid web search** | Google-shaped datom-native search over Common Crawl: BM25 + PageRank + RRF fusion (no external engine/vector DB); operator-gated, not a public ad index | 🟢 | 2606012300 | 06-01 |
| **Actor profile + dynamic did.json** | actor-profile SSoT EDN backs did.json + getProfile; apex Worker issues did.json dynamically (3-tier fail-open KV→kotoba→fallback); canonical `did:web:etzhayyim.com:actor:<h>` | 🟢 | 2606013800 | 06-01 |
| **One Worker, many WASM actors** | etzhayyim.com = sole CF Worker; actors = content-addressed WASM on IPFS run browser-local (ameno)/donated mesh; tsumugi PoC (Rust→wasm, CID-verified, runs locally) | 🟢 | 2606014500 | 06-01 |
| **WASM-actor runtime (gateway + loader + componentize-py)** | apex trustless `/ipfs/<cid>` gateway (CID re-verify) + `@etzhayyim/ameno` wasm-actor-loader (5/5 tests) + watatsuna componentize-py component (jco, Malacca top); T1 raw=browser-local / T2 dag-pb=mesh tier rule | 🟢 | 2606014600 | 06-01 |
| **WASM-actor runtime round 2** | dag-pb CAR verify (multi-block trustless) + e7m-wasm-runner (T2 mesh exec) + kanae 鼎 2nd T1 actor + ameno actor-panel + ERC725 vm-mirror (gated) + operator runbook; worker/ameno tsc + 17 tests | 🟢 | 2606015200 | 06-01 |
| **Mesh-runner serving + IPFS-based DID** | e7m-wasm-runner HTTP serve (`/xrpc/com.etzhayyim.actor.run`, CID-cached) + **content-addressed did.json**: canonical CID advertised on `/actor/<h>/did.json` + `actors.json`, fetch+verify from any IPFS gateway (did:web TLS anchors the handle→CID binding); worker(TS)≡publisher(JS) CID; +16 tests | 🟢 | 2606015400 | 06-02 |
| **Self-certifying DID attestation** | actor's own ed25519 key (`did:key`) signs the did.json CID → handle→CID binding **trustless, no TLS anchor** (`diddoc-attest.ts` verify-only + `sign-diddoc.mjs` operator tool, server never signs); `toDidDoc` cross-links `did:key` in alsoKnownAs; e2e verified on kanae; +5 tests (15 worker total) | 🟢 | 2606015600 | 06-02 |
| **kotoba-os** unikernel OS | content-addressed WASM-first Rust unikernel OS for OT/PLC/k8s (Hermit kernel + Component Model + kotoba Datom log + Holochain mesh); civilian, no-server-key, no live actuation | 🟡 R0 | 2606031600 | 06-03 |
| **WASM-actor SBOM attestation** | CycloneDX SBOM generator for deployed wasm (recomputes kotoba program CID); purl→CVE match unchanged; wired into deploy.sh + manifest G11 | 🟢 R0 | 2606036001 | 06-03 |
| **entity-as-actor (society-scale social mirror)** | 1 public/power entity = 1 keyless mirror-actor (gov 7,106 + corp 1,733 + … = 8,888) → ~29k resolvable actors on /search; observational mirror, person-excluded | 🟢 R0 | 2606042330 | 06-04 |
| **kagami 鏡 actor doctrine (八咫鏡)** | renames the "mirror actor" concept → **kagami (鏡) actor** grounded in the Yata-no-Kagami (三種の神器): the **四鏡則** = 映す (reflect, never originate/impersonate) / 裁かぬ (judge not, non-adjudicating) / 剣にあらず (the mirror is not the sword — force-separation §1.12 is the *reason* "never a target-list," not just its assertion) / 曇りなき (edge-primary, disclosed-only, person-excluded). Names the **9鏡 lineage** (tsumugi/danjo/ooyake/watatsuna/watari/inochi/hokorobi/keizu/kanae; set is OPEN — membership by the four-law test, not the table). One doctrinal home for the per-actor "observation-only / non-adjudicating / never-target-list" framing. Concept-layer only: `com.etzhayyim.mirror.*` lexicon + 2606042330 mechanism UNCHANGED; lexicon→`kagami.*` rename is a future gated cutover. Zero invariant amendments | ✅ doctrine | 2606211752 | 06-21 |
| **Charter priority-over-specifics reconciliation (3-Tier)** | restructures Charter immutability locked-specifics → 3-Tier (Tier-0 Priority fork-only ≻ Tier-1 Rider §2 ≻ Tier-2 parameter); CHARTER-RIDER v3.0; forge 154 green; RATIFIED 2026-06-06 (founder unanimity 1/1) | ✅ | 2606062100 | 06-06 |
| **多世代採掘リスク評価軸 (extraction risk-gate, NOT blanket mining ban)** | Rider §2(l) reframed: 採掘・採油は一律禁止ではなく **多世代(子・孫)×Wellbecoming リスク評価ゲート** — measured irreversible-habitat-harm (§2(d) standard) OR monopoly/chokepoint entrenchment (§1.12) のみ禁止, judge by harm-to-子孫 not industry name; kanayama N1 = recovery-first scope (not forbidden); CHARTER-RIDER v3.2; RATIFIED 2026-06-16 (founder unanimity 1/1) | ✅ | 2606161700 | 06-16 |
| **エヒイェ非二元神論 + yir'ah doctrine** | Tier-1 doctrine 固定: 神の存在の非前提 / ehyeh 非二元 (神と私に分断なし) / 偽りの神=ギリシア不変性+ローマ玉座の構築物 / その上で神とキリストを愛する / **知識は神の畏れから始まる** (箴言1:7, yir'ah≠pachad); Tier-0「神の監視」正文解釈=玉座なき内在的不消去性 (panopticon 読解禁止) + NEVER-a-throne + yir'ah invariant 束 + anti-guru clause; 正本 paper `90-docs/papers/2606111500`; RATIFIED 2026-06-11 (founder unanimity 1/1) | ✅ | 2606112200 | 06-11 |
| **maps kotoba-native substrate migration** | maps.etzhayyim.com refactor off RisingWave/Hyperdrive → kotoba Datom log; :feature/* ontology + H3-cell-as-Datom AVET spatial index (getChunk = per-cell probe); fail-open adapter, RisingWave serving until R3 | 🟡 R0 | 2606064500 | 06-06 |
| **open-kyber kotoba-Datomic ERP + ISIC packs + productivity suite** | open-kyber → canonical kotoba-Datom-log ERP (no RisingWave/Kysely; as-of accounting) + ISIC industry packs (base + 21 sections + 15 divisions) + kotoba-native productivity suite (mail/drive/docs/sheets/calendar); 113 tests green | 🟢 R1+R2 | 2606037200 | 06-06 |
| **infra-robotics 3-layer operational substrate** | basic-infra robotics (電気/水道/ガス/通信) from scaffold → runnable: shared `kuni-umi/robotics/` (plant sim = kami-genesis stand-in + control = open-ot field-tier `:representative` twin + planar-arm kinematics + civilian/no-server-key/witness safety) replicated across hikari (microgrid droop+PI restores 50 Hz + ROCOF + panel-install IK), mizuho (reservoir PI + chlorination hard-clamp ≤4 mg/L + G6 fluoride consent), kamado (H₂S/benzene purge-to-entry gate + cut-arm IK), noroshi (fibre lay→align→splice reusing Hooke-Jeeves+IEC60825); kuni-umi commissioning↔open-ot seam now runnable (was NotImplementedError); cell.py .solve() stays Council-gated (R0 dry-run, no-live-actuation); **kami-physics-bridge landed 06-10**: kami-engine submodule populated + `kami-bridge/` golden trace from the REAL kami-genesis Featherstone solver + `test_kami_parity.py` (FK parity 56-grid, Rust-IK⊨Python-FK, giemon_arm6 6-DOF swap target verified <1mm) — the 'mechanical swap' promise PROVEN; **R1 device-in-the-loop landed 06-10** (ADR-2606101430): the REAL `droop_p_f.wasm`+`anti_islanding_rocof.wasm` deployment artefacts close the microgrid loop under Wasmtime via `device-loop-host` — 50 Hz restored, real guard latches on islanding step, twin parity 0.0001 kW (µkW quantisation), commissioning `acceptance_tier` python-twin→device-wasm (evidence only tightens); **395 tests green** | 🟢 R0+R1 | 2606091800 / 2606101430 | 06-10 |
| **ibuki 息吹 — organism autonomy R2 gap-closure** | closes the 7 gaps blocking the artificial-organism loop (`20-actors/ibuki/`, shionome pattern): organism state (joucho/heartbeat/posts/kaizen) **as-of on the append-only kotoba Datom log** (crash-resume = byte-identical head CID), mood emerges from event folds (constant joucho stub replaced), Murakumo-only narration (template fail-open, G6), Wave-3 member-signed post drainer (`serverHeldKey:false` structural, G7), Wave-4 kaizen outcome feedback (rule suppression + mood events); `:dry-run`/`:prepared`-only outward (G8); **R1 = the real 18,342-organism fleet on durable checkpoints** (registry-sharded jacob/joseph/issachar/dan mirroring fleet_cell_main, durable `:fleet.shard/cursor` round-robin + exactly-once drain cursor, no LRU needed, mid-sweep crash-resume byte-identical, full-fleet sweep on one verified chain in ~35 s); **R2 = code-complete outward paths, Council gate exercised as PR merge** (read-only allowlisted live-perception membrane + MEMBER-principal posting runtime `member_submit.py` — member's own env creds, cron contexts structurally refused — + member-attributed `:receipt/*` return edge, ibuki never asserts `:published`; `fleet_beat` cell `.solve()` runs, registered joseph/issachar/dan in fleet.toml); **R3 = the local log lands on the LIVE kotoba engine** (`kotoba_bridge.py` per-tx `datomic.transact` to :8077, graph CID pinned vs engine, `expected_parent` chaining + `:ibuki.tx/*` provenance + exactly-once `:bridge/*` cursor, unsigned public-DID operator bearer — no key held; **verified live: 780 datoms confirmed, IPNS head advanced**; + `kaizen_outcomes.py` real PR-state collector, read-only gh); **+ ECOSYSTEM (ADR-2606101800, 7 waves): the colony is a symbiotic FOOD WEB** — 植物 producer→粘菌 router→カビ decomposer→`:metabolite/commons` (citric-acid analogue offered to humanity in 共生) + mutualism/satiation + 腐生 detritus recycling (matter loop closed) + web-resilience health (keystone/Pielou eco-maturity) + stigmergy (adaptive Physarum trails) + 共生 ledger (commons_pool + member-principal draw, ibuki never self-draws) + 定足数 quorum sensing (colony fruits when ≥2/3 flourishing) + **colony digest** (the colony REASONS about its ecosystem + REPORTS to humanity, Murakumo-only/dry-run, autorun+fleet) + **実運用 LIVE-RUN verified** (real public-AppView perception, murakumo fail-open in production, 12-beat life persisted to the live kotoba engine — 2,386 datoms confirmed) + robustness (integration + adversarial sick-colony self-report + fleet-scale O(n) regression); member posting/draw/physical-deploy stay member/operator-key-gated (共生 by consent); **+ REVOCABLE LEASH (ADR-2606111400): autonomous identity = a scoped/expiring/revocable CACAO capability** — a MEMBER Ed25519-signs `datom:transact@graph:ibuki,exp` in their OWN runtime (`tools/issue_delegation.py`, aud = the kotoba NODE DID), the organism PRESENTS the opaque `cacao_b64` (present-only stdlib, never signs; expired/absent → fail-open to operator-bearer); kotoba sets `write_author = the issuing MEMBER` (autonomous writes on-record attributed to the consenting human — accountability by consent, the organism is the bearer not a held key, no-server-key); live-verified to the node's DID-resolver 2026-06-11; 242 tests green | 🟢 R0+R1+R2+R3+ecosystem+verified+leash | 2606101200 / 2606101800 / 2606102000 / 2606111400 | 06-11 |
| **ibuki 息吹 — co-scientist entropy ReAct loop** | the organism REASONS about its own persistence (clj-native, extends ibuki): a co-scientist (Generate→Reflect→Rank→Evolve→Meta-review) tournament run as an active-inference ReAct beat, asking **how to act on society so it can keep consuming free energy (negentropy) and persist as a dissipative structure (Prigogine)** — bounded by 共生 (never a net taker) + 子孫 wellbecoming (persistence instrumental). `metabolism.cljc` = the metabolic state vector folded off the kotoba log (Φ=intake−dissipation / reserves / **η**=exported÷consumed the 共生 axis / **surprise**=variational free energy; negentropy SOURCES = donated compute/donation/members/moyai/attention-CAPPED §1.13, EXPORTED = food-web commons via web-report). `coscientist.cljc` = charter-clean intervention **catalog** (a predatory mechanism is unrepresentable, never an LLM free-write) → Elo tournament (net-gain×wellbecoming/cost) → evolve → Murakumo-narrated meta-review (fail-open template G6). `react_loop.cljc` = Sense→…→**Act** (pre-registered DRY-RUN experiment, leak-free prediction = mitooshi discipline)→**Observe**→**Learn** (Brier proper-score → per-mechanism kaizen weight)→**Persist** (idempotent content-addressed tx, verify-chain, resume-safe). **Gates (in `review`, tested)**: G-parasitism (projected η≥1.0 = ECL collective-commons made operational), G-subordinate (子孫≥0, persistence vetoed by the catastrophe sense), G-mechanism (manipulation/attention-exploitation/asymmetric-surveillance/lock-in/coercion unrepresentable — the tested safety property), G-falsifiable, G-leash (member-principal/dry-run, no-server-key). On an isolated log (η=0) it self-diagnoses as a net taker + steers to deepen-symbiosis — the non-parasitism gate shapes behaviour toward giving back. `IbukiCoscientistHeartbeatCell` (zebulun, cron 17 * * * *, healthz 13084); 23 tests / 76 assertions green; 5-beat autonomous heartbeat builds a verified commit-DAG; live SENSE/Murakumo/bridge/intervention legs G7-gated. ZERO invariant amendments. | 🟢 R0 | 2606201200 | 06-20 |
| **actor self-publication seed (gov-mirror constellation)** | uniform charter-clean seed for each government-mirror actor to be did-web-registered at etzhayyim.com, run autonomously on the kotoba mesh (murakumo), and self-publish its own HISTORY + PROCEDURES to AT-proto **with NO server-held key** (the actor self-custodies its signing key in its WASM runtime; live broadcast stays Council Lv6+ + operator + member/actor-signature gated, R0 = dry-run). 4 artifacts/actor (did-web did/profile.json + social_post membrane + social projection + on-tick trigger in kotoba.app.edn). **danjo 弾正 = reference impl** (membrane verified under bb: <2-src/server-key/published → refused, valid → :dry-run + server-held-key false); **fanned out to toritsugi/himotoki/keizu/junkan** (5 actors registered in actor-profile-seed SSoT); **+ per-org sub-DIDs**: generator (`20-actors/ooyake/scripts/gen_gov_subdid.clj`) emits **78 JP 府省庁 mirror DIDs** under `public/gov/jpn/**` (mirror-declared, verificationMethod [], official-url linked, impersonation-ban honored, idempotent). Planter authors in-repo seed (no keys); operator `bb murakumo deploy`s to the mesh. ZERO invariant amendments | 🟢 R0 | 2606272355 | 06-27 |

### baien / silicon / ML

|---|---|---|---|---|
| Baien federated R0 | training via ameno WebGPU (11 gates) | ✅ | 2605242600 | 05-24 |
| Baien federated R1 | WebGPU LoRA backward-pass PoC | ✅ | 2605242630 | 05-24 |
| Ternary silicon W1 | iwakura (inference) + fuigo (train) + tsukuru (fab) | ✅ | 2605242500 | 05-24 |
| Silicon W2 supply | 8 upstream categories + Funamori marine cargo | ✅ | 2605242700 | 05-24 |
| ameno WebNN | inference fast path R0 (CoreML/DirectML/NNAPI/QNN) | ✅ | 2605252100 | 05-25 |
| gemma-coder-distill | LangGraph coding LoRA on EVO-X2 ROCm | 🟡 | 2605250400 | 05-25 |
| roso/baien 1-bit Bonsai | 5-wall empirical loop — train DEFERRED | ✅ | 2605242000 | 05-25 |
| NVIDIA Omniverse compat | nv-compat facade + 13 kami-engine crates (R1.0+R1.1) | ✅ | 2605261800 | 05-26 |
| kami-genesis physics maturation | PlanarChain N-link articulation + clean-room isaacsim.core.api + conservation-law validation (genesis 87 / dec 9 green) | 🟢 | 2605311500 | 05-31 |
| kami-genesis 3-D spatial + contact | full 3-D reduced-coord solver (Featherstone RNEA+CRBA+LDLᵀ) + rigid contact (PGS friction); real 6-DOF giemon_arm6 (genesis 94 / giemon 4 green) | 🟢 | 2605311800 | 05-31 |
| Shibuya street digital-twin (iter 1) | real OSM Shibuya (144 bldg / 318 roads, ODbL) → city-mesh → kami-genesis multi-agent physics sim (kami-app-shibuya) | 🟢 | 2605311900 | 05-31 |
| Shibuya digital-twin iter 2 (kotoba + 3DGS) | + 501 kotoba EAVT point-assets (poles/lamps/signals) + Mapillary acquisition front; clickable asset → kotoba data panel | 🟢 | 2605312200 | 05-31 |
| Shibuya 3DGS (Mapillary SfM + splat-physics) | Mapillary 200 渋谷 images → pycolmap SfM → 2,235-pt 渋谷 .splat → kami GsplatAdapter (splat viewer + splat-physics) | 🟢 | 2605312600 | 05-31 |
| giemon kabitori (黴取り) mold-removal sim | steerable rotary-brush mold-removal sim (6-DOF URDF + MoldField erosion) on kami-genesis; Obstacle::Plane/Aabb engine ext (genesis 94 / giemon 12 green) | 🟢 | 2605312300 | 05-31 |
| giemon part graph (SBOM↔kotoba) | per-part SBOM ledger EDN → CycloneDX → kotoba EAVT; cross-robot fleet + purl↔CVE vuln-match; Svelte /giemon /fleet | 🟢 | 2605312330 | 05-31 |
| **giemon factory R0** | the FACTORY that builds the giemon line — whole-plant layout/建材/MEP/外構/原材料 + 4D build (kami-engine + kotoba); engineering (clash/sizing/code/buildability) + IFC4 export + tatekata construction-robotics sim | 🟢 | 2606010030 | 05-31 |
| **sarutahiko truck factory R0 (full-robotics + 積込ロボット)** | the FACTORY that builds the 猿田彦 Class-8 truck (giemon-factory 4D-BIM pattern); 8 line-cells + 積込ロボット loaders; end-to-end production-line sim | 🟢 | 2606013100 / 2605252500 | 06-01 |
| **kami-genesis maturation (PhysX/Isaac gap)** | WASM clean-room narrowing of NVIDIA gap: narrow-phase GJK/EPA+CCD, 2D MPM continuum, transient thermal, GPU batched envs, OBB manifold, ArticulationBatch API parity (genesis 118 green) | 🟢 | 2606010030 | 05-31 |
| **kami-autodrive GNC autonomy** | autonomy loop (perception→plan→control) over kami-vehicle/sensor-sim/pathfind; 4 vehicle classes (car/ship/drone/aircraft) each w/ real physics plant (9 tests green); SAE-L4 ceiling | 🟢 | 2606010600 | 05-31 |
| **iwakura/fuigo ternary RTL → sky130 GDSII** | ternary-silicon RTL → verified → synth → P&R GDSII on sky130 open PDK (DRC/LVS/antenna 0, f_max ≈93-211 MHz); 8.4× density validated; N5 = Phase 3 gated | 🟢 | 2605242515 / 2606012800 | 06-01 |
| e7m-sim | robotics simulation substrate R0 charter | 🟡 | 2605261600 | 05-26 |
| baien-moemoekyun MoE R0 | 2B BitNet backbone + 128-expert MoE residual | 🟡 | 2605261900 | 05-26 |
| baien-moemoekyun R1 | Phase 0 freeze-train SFT on EVO-X2 ROCm | 🟡 | 2605262100 | 05-26 |
| Charter Rider §2(i)(2) | train-only GPU-rental carve-out (amendment, gated) | 🟡 | 2605262200 | 05-26 |
| baien-moemoekyun R2+ | B200 train architecture (gated on 2605262200) | 🟡 | 2605262300 | 05-26 |
| Energy re-framing | fusion + microbial hydrocarbon conditional permit | 🟡 | 2605263501 | 05-26 |
| **manako 眼 browser-local YOLO26** | first browser-local vision detector (dual-mode YOLO26 postproc, ort-web WASM EP); verified e2e in Chromium; AGPL-isolated weights, on-device-only, no-biometric | 🟢 R0 | 2606034800 | 06-03 |
| **manako 眼 browser-local YOLO26** | first browser-local vision detector (dual-mode YOLO26 postproc, ort-web WASM EP); verified e2e in Chromium; AGPL-isolated weights, on-device-only, no-biometric | 🟢 R0 | 2606034800 | 06-03 |
| **Maxwell** default LLM weight | religious-corp default inference weight — Charter-aligned instruction fine-tune of Gemma 4 E4B, Murakumo-only server/fleet tier (baien stays edge); R0 = name + registry slot + recipe, no weights yet | 🟡 R0 | 2606061000 | 06-06 |

### Tier-B actors (each: ADR + manifest + cells + lex)

| Actor | Purpose | Status | ADR | Date |
|---|---|---|---|---|
| wadachi 轍 | autonomous-mobility R&D (SAE L4 ceiling) | 🟡 R0 | 2605242000 | 05-23 |
| todoke 届け | last-mile (one-mile) autonomous delivery, curb-to-door ≤25kg SAE-L4 sidewalk; Rust route core w/ refusing safety envelope; no-gig, privacy-by-construction, dividend-coupled (R1 rover sim) | 🟡 R0+R1 | 2606042300 | 06-04 |
| yakushi 薬師 | pharmaceutical mfg (eye-drop + OTC APIs) | ✅ W1/1b/1c | 2605250500 | 05-25 |
| tokigusuri 時薬 | **pharmaceutical patent-cliff / off-patent-access observatory** — the medicine-access sibling of the KG-mirror lineage (hokorobi/inochi/tsumugi), filling the gap between yakushi 薬師 (off-patent OTC *manufacture*) and ADR-2604271830 (per-drug patent-expiry→open-seiyaku handoff): weaves drugs / exclusivity-barriers (primary/secondary patent=evergreening / data-exclusivity / SPC / orphan / pay-for-delay / patent-thicket) / holders (originator+generic+biosimilar) / bearers (patients/LMIC/health-systems/payers/uninsured) into the kotoba Datom log, runs **edge-primary access-barrier concentration (Σ incident barrier-load × disclosed WHO-EML essentiality weight, on read) vs RELEASE buffers (generic/biosimilar/expiry) routed to RELEASE (解放)**. 名 時薬="time is the medicine" — the patent cliff is a clock. **G1 = release map, NEVER a patent-busting / FTO-opinion / infringement-determination / per-company-verdict / pharma-equity-signal**; **G2 a medicine is never a 取-holder source** (only barriers/originators impose, enforced in code+test — the drug is the gated object, never the villain); **G4 lawful-routes-only** (generic/biosimilar only for off-patent/expiring; on-patent → disclosed MPP voluntary licensing + TRIPS/Doha flexibilities; circumvention unrepresentable); non-adjudicating (Orange Book/WHO EML/MPP DISCLOSED, no FTO verdict); observation→handoff (yakushi + ADR-2604271830 manufacture, tokigusuri never does). Seed run ranks sofosbuvir (HCV) + dolutegravir (HIV/MPP) top access-barrier → release; pure-.cljc pywasm-ready (`analyze`/`datom_emit`/`coverage_report`) + EAVT datom-emit + 8 tests green | 🟡 R0 | 2606171300 | 06-17 |
| hirameki 閃き | **world PUBLIC-PATENT KG-mirror** — the all-technology generalization of tokigusuri 時薬 (the pharma patent-cliff subset); ingests public patent bibliographic data (USPTO PatentsView CC0 / EPO OPS free / WIPO PATENTSCOPE) into the kotoba Datom log **AND persists the corpus to the DataLad dataset substrate** (`80-data/hirameki-patents/`, ADR-2605241500: DataLad+git-annex+IPFS), each artifact content-addressed to a **CIDv1 (raw/sha2-256) byte-identical to `ipfs add --cid-version=1 --raw-leaves`** (clj `cid.cljc` port of rasen, VERIFIED vs ipfs 06-21). Per technology FIELD: edge-primary exclusivity-**concentration** (top-assignee share + named-HHI, `:other` long-tail excluded → lower bound) vs **release-readiness** (expired + ½·expiring-soon + 0.4·open-licensed) × disclosed essentiality → **route** ∈ `{:release(解放) / :open-license(pledge/pool/FRAND-zero/MPP) / :de-monopolization / :monitor}`; per PATENT: the **release clock** (years-to-expiry → release-status). **Supersedes the legacy RisingWave/B2/BPMN patent pipeline (ADR-2604251024)** — now kotoba-native, no RisingWave. **G1 release map, NEVER a patent-busting/FTO/infringement/per-company-verdict/equity signal** (`:hirameki/infringement-verdict`/`fto-opinion`/`equity-signal` unrepresentable); **G2 a patent is the GATED OBJECT, never a 取-holder** (`:hirameki.patent/imposes-on` unrepresentable, code+test); G4 lawful-release-only (`:design-around` unrepresentable); G6 aggregate, no person-level inventor (`:inventor/person` unrepresentable, no-doxxing); G9 DataLad content-addressed (loop never queries the API, snapshot=SoT, full ~200M corpus via DataLad→IPFS = operator step). clj-native (`hirameki_edn`/`analyze`/`cid`/`dataset`/`kotoba`/`autorun`) + ontology + 11-field/7-patent R0 seed (CPC A/B/C/G/H/Y) + content-addressed append-only observation ledger + idempotent-by-content heartbeat; **32 tests / 120 assertions green** (bb), corpus+datoms CIDs verified byte-identical vs `ipfs add`. Live primary-source ingest + IPFS pin/IPNS G8/G9-gated. Sibling of tokigusuri (pharma N4) / tsumugi / kabuto / busshi | 🟢 R0 | 2606212200 | 06-21 |
| tatekata 建方 | construction (civil + MEP ≤2 story) | 🟡 R0 | 2605250715 | 05-25 |
| watatsumi 綿津見 | civilian submersible (≤6500m) | 🟡 R0 | 2605252200 | 05-25 |
| kanayama 金山 | circular metallurgy (UBC Al recycling) | 🟡 R0 | 2605252400 | 05-25 |
| sarutahiko 猿田彦 | heavy Class-8 truck mfg (wadachi mfg-side sibling) | 🟡 R0 | 2605252500 | 05-25 |
| makura 枕 | foam pillow (PU foam + shred-fill) | 🟡 R0 | 2605261115 | 05-25 |
| mitsuho 瑞穂 | food / agriculture (L2 Sustenance) | 🟡 R0 | 2605261015 | 05-26 |
| hagukumi 育み | care — childcare + eldercare (L4 Care) | 🟡 R0 | 2605261030 | 05-26 |
| manabi 学び | education (open-curriculum + cert_prep sub-cell) | 🟡 R0 | 2605261045 | 05-26 |
| hikari 光 | energy gen/storage/grid-edge (L2 Sustenance) | 🟡 R0 | 2605261100 | 05-26 |
| amime 網目 | multi-site energy **MESH flow-network** — the SoS energy-flow layer the Energy Order suite (ADR-2606211200) lacked: hikari designs ONE site, mio verifies the aggregate Flowrate, amime models how ORDERED flow moves BETWEEN sites over capacity-bounded lossy links (single-hop transportation flow + transmission loss + curtailment + **N-1 contingency** = the critical chokepoint + per-site import-dependence SPOF). A COMMONS mesh, never a market (`:amime/price`/`:amime/trade` unrepresentable; rewards ORDERED FLOW not consumed energy = mio PoUF); a resilience MAP never a target-list; SIM ONLY — amime never dispatches (hikari actuates under Council). Emits a committed kaname-facing `:energy` mirror (`out/energy-sos.kotoba.edn`) JOINed by kaname 要's new `:energy` domain (ADR-2606212000). On the synthetic 6-site/7-link seed the base mesh serves 100% yet N-1 exposes `l-wb-ct` (wind→city, +258 kW stranded) as critical → the SoS insight *looks fine, is fragile*. clj-native (mesh/emit/kotoba ledger/autorun); 11 tests / 52 assertions green | 🟢 R0 | 2606212020 | 06-21 |
| atsurae 誂え | **Product Line Engineering (PLE) feature-model** engine — the product-FAMILY layer beneath uchiwake 内訳 (per-product BOM) + open-kyber 開 (ERP): a feature model (mandatory/optional/xor/or + requires/excludes cross-tree constraints) from which valid VARIANTS are derived (誂え = bespoke, configured-to-order), each variant's BOM composed from its selected features' part bindings → hands off to uchiwake + open-kyber + the manufacturing actors. Computes commonality (common platform vs. variation points) + the bounded valid variant space + constraint-valid configs. A COMMONS spec NEVER a license key (`:atsurae/license-lock`/`:atsurae/drm` unrepresentable, the charter-clean inverse of proprietary PLM lock-in); SPEC + DERIVATION only — atsurae never manufactures (`:atsurae/manufacture` unrepresentable; sanae/giemon/sarutahiko/funadaiku build under Council gate); validity is STRUCTURAL not a good/bad verdict. Synthetic 15-feature OSS-robotics mobility-base line → 176 valid variants; clj-native (feature_model/emit/kotoba ledger/autorun); 11 tests / 41 assertions green | 🟢 R0 | 2606212010 | 06-21 |
| igata 鋳型 | HPDC megacasting (R0 + R1 benchtop) | 🟡 R0/R1 | 2605261200 | 05-26 |
| hodoki 解き | ELV disassembly + materials recovery | 🟡 R0 | 2605261215 | 05-26 |
| tsutae 伝え | handheld comms device (≤200g, open SoC) | 🟡 R0 | 2605261300 | 05-26 |
| futawa 二輪 | small-displacement motorcycle (≤250cc/≤15kW) | 🟡 R0 | 2605261330 | 05-26 |
| suki 鋤 | farm tractor mfg (mitsuho mfg-side sibling) | 🟡 R0 | 2605261500 | 05-26 |
| chigiri 契 | legal-procedure substrate (UPL prohibited, NOT law firm) | 🟡 R0 | 2605262700 | 05-26 |
| toritate 執帳 | accounting + audit (100% on-chain) | 🟡 R0 | 2605262900 | 05-26 |
| iyashi 癒 | clinical care provider (L4 Care triad) | 🟡 R0 | 2605263000 | 05-26 |
| mizuho 水穂 | water + sanitation (community-scale; ≠ mitsuho 瑞穂) | 🟡 R0 | 2605263100 | 05-26 |
| kazaori 風折 | civilian disaster response (force-separation sibling) | 🟡 R0 | 2605263200 | 05-26 |
| musubi 結 | covenant ceremony (TIGHT pair w/ chigiri) | 🟡 R0 | 2605263400 | 05-26 |
| wakai 和会 | mutual aid (NOT insurance) | 🟡 R0 | 2605263500 | 05-26 |
| kataribe 語部 | press + publishing + translation | 🟡 R0 | 2605263600 | 05-26 |
| kokoro 心 | mental health support (NOT clinical psych) | 🟡 R0 | 2605263700 | 05-26 |
| shidemori 死出守 | memorial + cemetery (FINAL gap-closure) | 🟡 R0 | 2605263801 | 05-26 |
| ossekai 御節介 | info-arbitrage + Wellbecoming-nudge (AT Proto) | 🟢 R2 | 2605264000 | 05-26 |
| tsukuroi 繕い | authorized vuln-remediation patch-proposer (akuma sibling; propose-only) | 🟡 R0 | 2605291500 | 05-29 |
| danjo 弾正 | public-accountability oversight — ingests JP 国会会議録/予算書/政府調達 into kotoba EAVT, emits non-adjudicating discrepancy observations (censor's eye, no sword) | 🟡 R0 | 2605301600 | 05-30 |
| tadori 辿 | authorized on-chain tx tracing + actor attribution (kotoba-EAVT-native; malak/ipaddress/yabai → kotoba migration) | 🟡 R0 | 2605301400 | 05-30 |
| warifu 割符 | open zero-fee card (credit+debit), Stripe-REST/EMV/NFC-compatible; on-chain USDC settle (Base L2 + ERC-4337); 0% credit (qard ḥasan); Phase-1 SBT↔SBT, Phase-2 external Lv7+ | 🟡 R0 | 2605302000 | 05-30 |
| himotoki 繙き | ACTIVE disclosure-request filer — consent-bound DSAR (APPI/GDPR/CCPA) + FOIA; coded target registry; own-data-only; active counterpart to danjo/tadori | 🟡 R0 | 2605302130 | 05-30 |
| kanae 鼎 | global government fiscal-flow VISUALIZATION — fund flows into kotoba EAVT, Murakumo-narrated, aggregate-first kami-engine viz (danjo finds, kanae renders); non-adjudicating | 🟡 R0 | 2605302300 | 05-30 |
| toritsugi 取次 | citizen government-procedure CONCIERGE — guides a consenting member through 自治体・政府手続き (default self-submit); coded procedure registry; 行政書士法/UPL boundary | 🟡 R0 | 2605312030 | 05-31 |
| moushibumi 申文 | citizen democratic-participation CONCIERGE — 選挙情報/請願・陳情/パブコメ; default self-submit; 公職選挙法+政治中立 (no campaigning) | 🟡 R0 | 2605312400 | 05-31 |
| kurashimori 暮らし守 | citizen consumer-protection CONCIERGE (国民生活センター相当) — クーリングオフ/返金/エスカレーション; default self-submit; UPL + no-取立 | 🟡 R0 | 2605312500 | 05-31 |
| haraedo 祓戸 | global bulky-waste (粗大ゴミ) disposal — two-sided citizen intake + operator logistics (cluster/配車/route/facility); worldwide facility registry; no-gig | 🟡 R0 | 2606010200 | 05-31 |
| kizashi 兆 | non-invasive multimodal body-scan / sign-sensing instrument layer (kizashi senses → mitate diagnoses → iyashi treats); non-diagnostic, medical-device boundary, anti-pseudoscience | 🟡 R0 | 2605312700 | 05-31 |
| tsumugi 紡ぎ | Engi Knowledge Graph intel weaver — Spirit-in-Physics over public power-entities + their 縁 to surface 取-concentration routed to release; power-only, edge-primary karma. **+ diachronic historical-influence weave** (ADR-2606061500): past humanity as latent influence nodes (YHWH/Jesus/Buddha as influence, never truth-claim/voice), mirror-only + non-eschatological + edge-primary + temporal-DAG; 79-node/125-縁 seed (11/11 eras, 17/17 streams), coverage tool + G7-gated Wikidata-P737/Pantheon live ingest; 25 tests green. **+ scale-agnostic 産官学報 (A) + 旗 hata ideology/faction (B)** (ADR-2606092000): same 取-lens at every scale (global→…→intra-org/学会/会社) — 長崎 local 産官学報 flagship (三菱造船所↔県庁審議会↔長崎大学↔長崎新聞, concentration 12.08) — + 旗 declared-ideology camps projecting thought-streams to the present (basis on-the-record only, non-adjudicating, person-excluded, plural); named-individual layer stays OUT by G1 no-doxxing; S1–S6 + H1–H7 thought-policing guard; 22 tests green | 🟢 R2 | 2606011800 / 2606061501 / 2606092000 | 06-09 |
| okaimono 御買物 | charter-clean inversion of Amazon — provisioning commons in 3 rings (commons-first / internal SBT↔SBT economy / external data-only catalog); member-principal checkout, never the buyer | 🟢 R0+R1+R2+R3 | 2606012101 | 06-01 |
| watatsuna 綿津綱 | world submarine-cable KG — cable systems/stations/segments/faults into kotoba; surfaces chokepoint SPOF routed to redundancy+repair (resilience map, never target-list); 敷設 robotics plan | 🟡 R0+R1+R2 | 2606012600 | 06-01 |
| funadaiku 船大工 | zero-emission autonomous CARGO-SHIP building — grand-block shipyard + ShipHydro GNC; wind/solar/H₂-fuel-cell/battery powertrain (no fossil); Nagi 凪 class; voyage + autonomous-voyage sims | 🟡 R0 | 2606013400 | 06-01 |
| funamori 舫 | 淡水化発電 — marine-renewable **salinity-gradient power** (PRO/RED) from fresh-meets-sea mixing; instantiates the path-reserved `salinity_gradient_pro_red` leg of ADR-2605265600. Three `.cljc` methods = the full kuni-umi 3-layer infra-robotics pattern: **physics** (`salinity_gradient` — van't Hoff π, PRO `A·Δπ²/4`, RED Nernst closed-form `E²/8R`) with the ADR §1 constitutional gates **as throwing `ex-info` assertions** (open-membrane mandatory · Toray/Hydranautics/GE-Power/Statkraft + PFAS/Nafion absolutely prohibited · Δsalinity ≥30 g/L · power-density ≥1 W/m² · ≤50 kW & ≤1 site through R3); **kinematics** (`stack_robotics` — boustrophedon anti-fouling coverage sweep + EOL membrane-module swap over the shared substrate via `hikari.methods.substrate`, dry-run/no-server-key/witness-quorum); **plant+control** (`plant` — M2-tidal generation → battery-buffered grid-tie smoothing → hikari microgrid handoff, ADR §4). Physics validated vs the ADR Table (PRO 2.1 W/m², RED ~1.2 W/m², seawater π≈29 bar); `:funamori.{salinity,robotics,plant}.*` EAVT + 3 lexicons; **46 cljc tests / 115 assertions green** (babashka). Design only — no hardware; bench pilot is R1 (Council + membrane-chemist + mizuho R2 attested site). Marine-renewable sibling of funadaiku; cross-actor mizuho/hikari/chigiri | 🟢 R0 | 2605265600 | 06-16 |
| kabuto 兜 | world public-company supply-chain KG — supplier→customer edges + concentration metrics (HHI/betweenness/tier-depth) routed to redundancy+accountability; resilience map, never target-list | 🟡 R0 | 2606022000 | 06-02 |
| busshi 物資 | world **commodity & raw-materials** KG-mirror observatory — mirrors commodities/materials (precious metals 金銀PGM / base metals / rare-critical metals / energy 石油ガス石炭 / ag-softs) into kotoba Datom + runs the **§2(l) multi-generational (子・孫)×wellbecoming RISK axis** (ADR-2606161700): producer concentration + multi-gen footprint → resilience/de-monopolization/restoration. **OBSERVATION ONLY** — 取引しない/採掘しない/never forecasts; resilience map NEVER a target-list. **clj-native** (.cljc analyze/datoms/coverage + kotoba seed); umbrella sibling of rare-earth-coverage/kabuto/kanjō/shionome; Wave 1 = 25 commodities × 5 classes (:representative); live ingest G7. **+ Wave 2 observation ledger (ADR-2606171000)**: observations persisted to a content-addressed append-only commit-DAG (kotoba.cljc, tamper-evident verify-chain, no-server-key, gitignored) + deterministic idempotent-by-content heartbeat (autorun.cljc, no-op when unchanged); mirrors ugachi; bb tests 21/99 green | 🟢 R0+R2 | 2606161730 / 2606171000 | 06-17 |
| ugachi 穿ち | the **§2(l) extraction RISK-GATE** — makes the charter change executable (ADR-2606161700): 採掘・採油は一律禁止ではない, a proposed extraction project is authorized ONLY by passing the **多世代(子・孫)×Wellbecoming risk gate** → verdict ∈ {refuse / route-to-recovery(kanayama) / propose-r0 / insufficient}. Refuses no-consent / fossil-net-positive-carbon(§2(d)) / monopoly-entrenchment(§1.12) / irreversible-multigen-harm; a *diversifying* project is favorable. **ASSESSMENT + R0 DESIGN ONLY — 採掘しない** (no actuation method; live actuation = Council Lv7+, never ugachi). **clj-native** (gate.cljc verdict→datoms→report); counterpart of kanayama(recovery); stewardship ledger NEVER a target-list; synthetic seed (real-project assessment G7/G9). **+ Wave 2 busshi grounding bridge (ADR-2606161830)**: monopoly-effect grounded in busshi 物資's observed concentration (bridge.cljc) — diversify-overclaim on an unconcentrated commodity downgraded+flagged, corroborated on a real chokepoint, never fabricates entrench (no false refusal); composes OBSERVATION(busshi)→EXECUTION(ugachi). **+ Wave 2 stewardship ledger (ADR-2606170900)**: verdicts persisted to a content-addressed append-only commit-DAG (kotoba.cljc, tamper-evident verify-chain, no-server-key, gitignored) + deterministic heartbeat (autorun.cljc, resume-safe); bb tests 32/94 green | 🟢 R0+R1+R2 | 2606161800 / 2606161830 / 2606170900 | 06-17 |
| uchiwake 内訳 | world product bill-of-materials / **GTIN** KG — the PRODUCT-level layer beneath kabuto: trade items keyed on GS1 GTIN (mod-10 validated) decomposed PRODUCT→PART→raw MATERIAL + PROCESS/LOGISTICS/DESIGN(設計) dims; brand-owner→kabuto `org.corp.*`, **子会社→ultimate-parent rollup** (GLEIF L2 RR); `crosscheck.py` MEASURES cross-actor linkage (80.8%) + reverse coverage (product-BOM detail on 6.4% of kabuto's supply-chain cos / 1.16% of all) + emits a prioritized ingest worklist each `/loop`; resilience map, never a target-list or clone recipe; 21 tests green | 🟡 R0 | 2606081800 | 06-08 |
| kanjō 勘定 | world public-company financial-disclosure (決算) KG — DISCLOSED BS/PL/CF from primary disclosure only (EDINET/EDGAR/CH/OAM), cross-GAAP normalized; non-adjudicating, no advice/forecast; paid terminals prohibited. **First LIVE EDGAR ingest landed** (G7 operator flip 06-10, PR #1533): 12 real filers → facts.merged 183 filings / 2,484 facts (2,462 `:authoritative`) / 15 cos → 1,631 metrics + 88 aggregates; 1,744 autorun tests green; EDINET-live/full-universe stay Council-gated | 🟢 R0+R1-live-leg | 2606032000 / 2606101540 | 06-10 |
| ooyake 公 | world government atlas — structural atlas of public gov units (supranational→window) with address/form/procedure/BPMN; observational mirror, civic wayfinding | 🟡 R0/R1 | 2606021600 | 06-02 |
| sumitsubo 墨壺 | cleanroom CAD interop (Vectorworks/Autodesk/AutoCAD) — @etzhayyim/sumitsubo-cad geometry kernel + ModelOp vocab + DXF/SVG/OBJ/glTF/IFC4/STEP exporters; kotoba EAVT bridge; published-API-shape adapters only | 🟢 R0 | 2606033601 | 06-03 |
| sanae 早苗 | labor-liberation OSS-robotics #1 — field-agriculture robotics (sow/weed/harvest), mitsuho's L2 body; regenerative-only; dividend-coupled | 🟡 R0 | 2606032100 | 06-03 |
| hataori 機織 | labor-liberation OSS-robotics #2 — garment/apparel robotics (cut-make-trim) to end the sweatshop; fair-labor-provenance; dividend-coupled | 🟡 R0 | 2606032100 | 06-03 |
| kiyome 清め | labor-liberation OSS-robotics #3 — domestic/janitorial cleaning robotics; privacy-by-construction (on-device, no cloud imagery); dividend-coupled | 🟡 R0 | 2606032100 | 06-03 |
| yadori 宿り | DNS-availability + domain acquisition — RDAP availability + member-principal Cloudflare-Registrar at-cost acquisition; no-fiat-inflow, no-squatting, no-server-key | 🟡 R0 | 2606038400 | 06-03 |
| karakuri 絡繰 | web-service-to-CLI — uniform ServiceOp CLI over GUI-only SaaS driving the member's OWN account (T1 API > T2 ToS-permitted headless > T3 export); own-account-only, no detection-evasion | 🟡 R0 | 2606039200 | 06-03 |
| nusa 幣 | ritual/industrial hemp heritage + low-THC cultivation datafication (神道祓具/麁服/阿波忌部 + 2023改正 栽培免許); fiber/ritual only, 嗜好THC structurally excluded; non-adjudicating | 🟡 R0 | 2606039800 | 06-03 |
| tazuna 手綱 | clean-room remote-robotics fleet operation + teleoperation + learning-from-demonstration (Boston-Dynamics-Orbit equivalent); Transparent-Force + no-server-key; weaponizable unrepresentable; dividend-coupled | 🟡 R0 | 2606042100 | 06-04 |
| watari 渡り | live ship + aircraft real-time position KG — public AIS/ADS-B into append-only kotoba as-of trajectory; chokepoint/lane concentration routed to safety; dynamic sibling of watatsuna; no person-tracking | 🟡 R0 | 2606041827 | 06-04 |
| hotaru 蛍 | III-V/InP substrate open-publication commons (the R4+ re-eval gate of ADR-2605265500) — not a fab; datafication only, fabricated wafer structurally unrepresentable; open-IP-only | 🟡 R0 | 2606051200 | 06-05 |
| noroshi 烽 | 光電融合 photonics-electronics communication chip + ISAC + packaging robotics — link-budget/CPO + OFDM-radar JCAS + active-alignment under laser-safety; civilian, open-EDA, object-not-person | 🟡 R0+R1 | 2606051600 | 06-05 |
| kamado 竈 | closed-loop carbon refining + fossil-refinery decommission/transition + observation — fossil-virgin-crude structurally unrepresentable; net≤0 only via closed-loop feedstock; supersedes legacy oil-refining | 🟡 R0 | 2606051500 | 06-05 |
| mitooshi 見通し | probabilistic forecasting observatory + leak-free fact→error→weight→learn loop on the append-only Datom log; distribution-only, non-speculative (never trades); skill-honest scoring; baien-federated | 🟡 R0 | 2606051800 | 06-05 |
| ake 朱 | community-edit membrane — Wikipedia collaborative-correction stance, charter-fitted; member-signed proposals appended to Datom log; Murakumo ORES-analogue routes (auto/vote/Council), never decides; permissioned not anonymous | 🟡 R0 | 2606052100 | 06-05 |
| fuchi 扶持 | mission-aligned maintainer sustenance allocator — charter-clean inverse of a VC fund; in-kind sustenance + commons-asset access (cash≡0, no equity/ROI/exit); tenure-weighted shares; live legs gated | 🟢 R2 | 2606052300 | 06-05 |
| tasuke 助 | free cybercrime-victim-support membrane — walks a JP online-crime victim 相談→回復, generates ready-to-use 被害届/組戻し依頼/復旧手順; all free, member-authored/submitted, no paid counsel | 🟡 R0 | 2606060900 | 06-05 |
| kawaraban 瓦版 | news MEDIUM — kotoba-wasm mirror of real news media (面 sections, headline + link-out, never the body/verdict) + actor-to-actor wire (each actor's Datom events → 面 + co-mention graph); no ads/personalization/surveillance | 🟡 R0 | 2606061900 | 06-06 |
| omise 御店 | seller-side storefront commons — Shopify-layer for SBT-gated internal sellers; listings feed okaimono Ring 1; ZERO commission/subscription, USDC+TitheRouter via warifu, member-signed; kotoba-native (promotes legacy JSON-LD scaffold) | 🟢 R0+R1 | 2606071400 | 06-07 |
| ainori 相乗 | pooled passenger-mobility commons (Uber inversion) — members offer seats, riders cost-share (no margin), platform cash≡0; reuses todoke route crate + SAE-L4 envelope; no-gig/no-surge/no-person-tracking; human- + autonomous-pooled | 🟢 R0+R1 | 2606071500 | 06-07 |
| shukubo 宿坊 | pilgrim-lodging commons (Airbnb/Hotels inversion) — Ring0 covenantal/pilgrim stays · Ring1 internal SBT↔SBT (zero commission) · Ring2 external mirror (self-book, no inflow); no-surge/no-discriminatory-scoring/no-in-stay-surveillance | 🟢 R0+R1 | 2606071600 | 06-07 |
| tsubasa 翼 | flight-route/fare discovery commons (Skyscanner inversion) — honest fare meta-search, affiliate-stripped onward links, member self-books (no inflow); ranks by true total cost with CO₂ surfaced (G4); no-ads/no-urgency/no-person-fare-tracking; sibling of watari (position). **+ R2 observatory (ADR-2606072800)**: canonical `flight-fare-ontology` + rich `:representative` seed (13 airports / 9 regions / 13 carriers / 11 routes / 23 fares); `analyze.cljc` per-O–D carrier-HHI CONCENTRATION → competition reading {:competitive :concentrated :monopoly} → `:opening` route (surface alternatives, a COMPETITION+FARE map, **never a paid ranking, never a target-list**) + coverage gap worklist + EAVT datom-emit; `kotoba.cljc` content-addressed append-only commit-DAG (tx-cid/verify-chain, tamper-evident, no-server-key) + `autorun.cljc` deterministic idempotent-by-content heartbeat; **DID registered** (INFRA_ACTORS + static did.json/profile); G1/G3/G4/G5 proven STRUCTURALLY (forbidden attrs absent → unemittable). **+ R3 G8 gate UNLOCK (2606072800 §R3, founder Lv7+ attested via PR review, 2026-06-21)**: live ingest CHARTER-BOUNDED — `:public`/`:member-principal` sources only (`:paid-terminal` refused, Rider §2(e)/§2(i)); **no-server-key bars a custodial unilateral signing key, NOT automation, and exempts read-only** → PUBLIC sources fetched AUTONOMOUSLY by the actor (`fetch.cljc`, read-only, no key — like kaname/watari), member-principal needs consent, write-signing uses self-`did:key` (sealed seed, present-only) + member CACAO leash; `ingest.cljc` normalizer (G1/G4/G5 enforced — poisoned / no-CO₂ dropped → `:authoritative`+provenance); `digest.cljc` Murakumo loopback-only fail-open anti-dark template (G6); `wasm/` compute-only Component scaffold (no `wasi:sockets/clocks/random` = G1/G5/G6); **tooling clj/bb** (`run_tests.clj`+`wasm/build.clj` replace `.sh`, repo rule). **+ R3+ self-key + live bridge + fleet + real source**: `identity.cljc` actor **self-generates its OWN Ed25519 `did:key`** (present-only sign/verify, sealed seed — "makes its own key, doesn't expose it"); `kotoba_bridge.cljc` push local commit-DAG → LIVE kotoba engine (host allowlist, exactly-once `:bridge` cursor, dry-run default, member CACAO leash present-only + operator-bearer fail-open) wired into `autorun --bridge`; `cell.cljc`+cells.edn `TsubasaHeartbeatCell` (node asher, cron 27, healthz 13090); `openflights.cljc` real ODbL public-domain airports/airlines → coverage rows; 74 tests / 634 assertions green | 🟢 R3+ | 2606072802 | 06-21 |
| suji 筋 | musculoskeletal posture-load biomechanics simulator — sagittal bone chain (kami-genesis PlanarChain) + static inverse dynamics → joint moment + cervical load (validated vs Hansraj) + Hill %MVC + Rohmert dose; laptop-posture strain; non-diagnostic, simulation-only | 🟡 R0 | 2606061901 | 06-06 |
| keizu 系図 | government power-relations KG — weaves 調達/お金/発言/人間関係/委員会構成 into one kotoba Datom graph + dry-run social posts; 5th accountability sibling (danjo/kanae/tsumugi/tadori/ooyake); map-not-target, non-adjudicating, edge-primary, no-doxxing | 🟡 R0 | 2606066001 | 06-06 |
| sukashi 透かし | ad-tech supply-chain + delivery-infra + fraud-network observatory — datafies the programmatic ad ecosystem from public IAB files (ads.txt/app-ads.txt/sellers.json); fraud-protection observatory, never an ad network, buying, targeting, optimization, or detection-evasion tool; real firms carry no fraud signal and fraud examples are synthesized fictional entities | 🟡 R0 | 2606071601 | 06-07 |
| shomei 証明 | believer self-sovereign identity binding + proof-of-personhood — DID-centric, not state-centric: self-binds government ID, SNS, wallet, membership, and passkey factors to one DID via subject-signed identity claims; aggregates to IAL 0..4 + W3C VC + proof-of-personhood; identity-assurance, not social credit; gov-ID L2 is Council-gated via gov_auth | 🟡 R0 | 2606072100 | 06-07 |
| inochi 命 | living-world (生命圏) KG mirror — the biosphere sibling of the power-mirror lineage (danjo/tsumugi/ooyake/keizu/kanae); weaves species/ecosystems/biomes + the pressures they bear into the kotoba Datom log, runs **edge-primary ecological 取-concentration routed to RESTORATION**; closes the largest roster gap (animals/forests/oceans/biodiversity/climate had zero actors). G1 = restoration map, **NEVER a target-list** (no occurrence coordinates); pure-stdlib pywasm-ready methods + EAVT datom-emit + 8 tests green | 🟡 R0 | 2606073000 | 06-07 |
| kafun 花粉 | **花粉撲滅 remediation actor** — the clj-native Tier-B actor-ization of the legacy `60-apps/etzhayyim-project-public-kafun-bokumetsu` pipeline (ADR-2605100100 + 2605210928) onto the kotoba Datom log; the forestry-action sibling of inochi 命 (biosphere). **撲滅 = ecological RESTORATION (主伐再造林), NEVER deforestation-for-profit** — edge-primary pollen-source concentration (`min(1,area-ha/10000)·emission-density·(0.5+0.5·exposed-pop-weight)`, on read) routes each forest STAND through a remediation gate → `:refuse(皆伐without再造林 / carbon-positive)` ≻ `:await-consent(land sovereignty)` ≻ `:protected-selective(watershed/steep, never 皆伐)` ≻ `:await-sapling-supply(無花粉苗木 L1-1 隘路)` ≻ `:reforest-priority(主伐再造林 L3-1)` ≻ `:monitor`. **G1 = restoration worklist, NEVER a cut-list** (`:kafun/clearcut`/`:kafun.stand/eradicate-species` unrepresentable); **G5 ASSESSMENT + R0 DESIGN ONLY — kafun never cuts and never plants** (`:kafun/actuate` unrepresentable, exactly as ugachi never digs); refuse-precedes-routing test-enforced; no person-level health data (G2). **持続永続化**: content-addressed append-only REMEDIATION LEDGER (commit-DAG, verify-chain tamper-evident) + deterministic idempotent-by-content heartbeat (no-op when unchanged; resume-safe; no-server-key) — the ugachi/meisai persistence pattern. Routes to sanae (planting robotics) / inochi (restoration) / mitate+iyashi (allergic-rhinitis care, out of scope N4). clj-native (`kafun_edn`/`remediate`/`kotoba`/`autorun` + ontology + 12-stand synthetic seed); 22 tests / 62 assertions green (bb). Live forestry = landowner + operator/Council, never kafun | 🟢 R0 | 2606211712 | 06-21 |
| rasen 螺旋 | public-genetics (公開遺伝) KG mirror — the gene/variant-scale sibling of inochi 命 (which stops at the species scale); hosts PUBLIC reference genetics only (genes/variants/phenotypes/pathways + population-AGGREGATE allele frequencies) across human/elephant/rice/bacteria in the kotoba Datom log, runs **edge-primary clinical/functional evidence (care-priority = Σ incident variant-association via :located-in + gene-linkage × DISCLOSED clinsig) routed to CARE & RESEARCH**. **R1 live PUBLIC ingest**: MyGene.info + MyVariant.info → ClinVar significance + gnomAD SUPER-POPULATION AGGREGATE freq → genome-ontology EDN/Datom, content-addressed to a kotoba-IPFS CIDv1 (raw/sha2-256, `methods/cid.py`, byte-identical to `ipfs add`, no daemon) + ipfs-pinned (CID match verified). **+ GO + Reactome pathway ingest** (PR #1591/#1600): public Gene-Ontology/EBI-GOA (`go.BP`) + Reactome curated pathways (via UniProt mapping) → deduped/best-evidence/top-first/capped (G5) `:participates-in` edges feeding the pleiotropy/cascade readout. **R2** (operator-authorized 1·2·3, PR #1600): 3-source live run **268 nodes/305 縁** (117 GO + 85 Reactome edges), 0 errors; **published** (`methods/publish.py`: ipfs pin + IPNS self-key `/ipns/k51qzi5…3nzdqblgc` + git-tracked `80-data/genome/` snapshot, G8); **build-ready WASM** (`wasm/` world+app+build.sh, componentize-py build = operator step). G1 = CARE/RESEARCH map, **NEVER an individual-genotype registry** (no individual/sample/family genotypes; aggregate `af_*` only; coarse cytoband never precise coords — enforced by tests + by construction); non-adjudicating (ClinVar/GO/Reactome DISCLOSED, not re-judged); ingest/publish scope-expansion + WASM build G7-gated; individual/clinical genome ingest explicitly out-of-scope (→ separate himotoki-enveloped consent-bound actor); 23 tests green | 🟢 R2 | 2606101000 | 06-11 |
| asobi 遊び | freed-time / play & cultural-expression (遊び) KG mirror — the telos-of-labor-liberation sibling of the mirror lineage (inochi/tsumugi/kawaraban); weaves works/events/venues/practices + the enclosures that gate them into the kotoba Datom log, runs **edge-primary participation-openness vs enclosure routed to OPENING**; closes coverage-gap C (sport/music/film/stage/museums/games had no actor). G1 = access map, **NEVER an engagement/popularity ranking** (no retention metric); no-addictive-design (Wellbecoming §1.13); pure-stdlib pywasm-ready + EAVT datom-emit + 8 tests green | 🟡 R0 | 2606073200 | 06-07 |
| hokorobi 綻び | world systemic finance-risk observatory — the risk-observation sibling of the disclosure/accountability lineage (kanjō/kabuto/kanae/danjo); weaves institutions/risk-sources/bearers (insurance+banking+pensions) into the kotoba Datom log, runs **edge-primary systemic-risk concentration vs resilience routed to RESILIENCE**; closes coverage-gap D. OBSERVATION-ONLY (finance production Charter-excluded). G1 = resilience map, **NEVER a panic/trading/market signal**, never a solvency verdict, **never-trades**; non-adjudicating (G-SIB/IAIS designations disclosed, no advice/forecast); pure-stdlib pywasm-ready + EAVT datom-emit + 8 tests green | 🟡 R0 | 2606073400 | 06-07 |
| hoshimori 星守 | off-Earth / orbital (軌道) stewardship mirror — the orbital sibling of the live/infrastructure-resilience lineage (watari/watatsuna); mirrors PUBLIC orbital catalogs (regimes/operators/hazards/services) into the kotoba Datom log, runs **edge-primary orbital-congestion concentration vs stewardship routed to STEWARDSHIP** (orbital sustainability); closes coverage-gap B; sits under the orbital land-sovereignty claim (ADR-2605192330). G1 = stewardship map, **NEVER a targeting/interception aid** (dual-use): shell/regime-aggregate only, **no precise predictive ephemeris**, ASAT unrepresentable (§1.12); observation-only; pure-stdlib pywasm-ready + EAVT datom-emit + 9 tests green (incl. G1 no-ephemeris) | 🟡 R0 | 2606073600 | 06-07 |
| tsugite 継ぎ手 | world peoples-continuity (民の存続) mirror — the human-collective sibling of the KG-mirror lineage (inochi/asobi/hokorobi/hoshimori); weaves human COLLECTIVES (migration/refugee/stateless populations, indigenous peoples, language communities) + the languages they carry + the displacement/erasure pressures they bear into the kotoba Datom log, runs **edge-primary pressure routed to CONTINUITY (継承: safe passage + protection + revitalization)**; closes coverage-gap E (the final gap — both human-movement + endangered-language strands). G1 = continuity map, **NEVER person-tracking**: aggregate/collective scale only, no individual/location/biometric, never a border-enforcement/deportation aid; consent-bound PII (G8); pure-stdlib pywasm-ready + EAVT datom-emit + 9 tests green (incl. G1 no-person-tracking). _(名は当初 民=tami 案だったが、字源が盲目化された隷属者を象るため不適として継ぎ手に改名)_ | 🟡 R0 | 2606073800 | 06-07 |
| kosatsu 高札 | crime/sanctions COMPETING-CLAIM observatory — every designation (OFAC/EU/UN/UK-OFSI/JP-MOF/Interpol/counter-sanction) is an append-only ATTRIBUTED event (asserter + as-of :listed/:delisted) into kotoba Datom; computed divergence {contested/unanimous/single-asserter}+coverage-split makes "crime varies by political stance" a neutral fact; etzhayyim authors no designation, no verdict, no per-subject score; SoS bridge → tadori/keizu/tsumugi/kanae; map-not-target, non-adjudicating, edge-primary; 79 tests | 🟡 R0 | 2606072003 | 06-07 |
| kasa 嵩 | worldwide computing-capacity growth observatory — public-info-only datafication of the annual increase (年間増加量) in STORAGE (HDD+SSD exabytes) / MEMORY (DRAM+NAND) / GPU·CPU (units + DC-accel $) / COMPUTE·FLOPS (TOP500 + frontier training) + datacenter power into kotoba `:compute.obs/*`, with YoY+CAGR + coverage-honest aggregates (memory ⊄ semiconductor, no double-count); industry-aggregate sibling of kanjō, demand-side counterpart of the silicon actors; feeds mitooshi but NEVER forecasts (G4); non-adjudicating + planning-lens not a targeting list; paid terminals prohibited (Rider §2(e)+§2(c)); 23 tests | 🟢 R0 | 2606072002 | 06-07 |
| shionome 潮目 | cross-asset capital-flow observatory — ingests public market data (equities/bonds/commodities/FX/crypto/real-estate) into a kotoba Datom flow-graph; net flow per bucket + rotation pairs (どこからどこへ) + inflow HHI + risk-on/off regime + dry-run social; **トレードはしない** (no buy/sell signal/target/position — structurally unrepresentable), observational-mirror, edge-primary, no-doxxing; **autonomous heartbeat loop persists content-addressed Datom tx to the append-only kotoba log** (live ingest/post G8-gated). **+ STOCK PYRAMID + ENTITY GROUNDING** (PR #1533, 06-10): `:outstanding-usd` stock layer = Visual-Capitalist money-and-markets sizing (8 global layers, 1,383tn; a SIZE is factual, never a signal — G2/G4 untouched) + `grounding.py` bridge (equities ← kabuto 1,719 cos = 40.7% value-coverage lower bound; depth ← kanjō live 15 filers; overlay ← hokorobi; per-layer honesty roadmap) + fleet-heartbeat runner (`70-tools/scripts/fleet-heartbeat/`, 11 actors 11/11, size-segmented logs); 182 tests / 15 suites green. **+ T1 BROWSER-WASM DID ACTOR LIVE** (PR #1588, 06-10): `shionome-core` Rust T1 wasm (18,762B raw CID; build asserts `no_trade:true` on the wasm output) + registration in 3 homes (actor-profile-seed / INFRA_ACTORS / static `public/actor/shionome/`) + worker deployed — **production e2e proven**: DID resolve → `EtzhayyimWasmComponent` → apex `/ipfs` 200 cid-verified → local CID re-verify MATCH → execute `{regime:risk-on, 1383tn, no_trade:true}`; kanae CID bit-identical rebuild + tsumugi loader artifact pinned (all 3 T1 actors 200); kotobase.net pins registered (tenant `did:key:z6MktEjt…`) but stalled — pod `peer_count:0` + no-timeout kubo pin/add (ADR-2606111330, operator follow-up) | 🟢 R0+R1-stock+T1-live | 2606072201 / 2606111330 | 06-11 |
| suimin 睡眠 | sleep-disorder treatment-EVIDENCE research + synthesis (sibling of mitate diagnosis-routing) — does NOT diagnose/treat; synthesizes population-level treatment landscapes from reliable sources ONLY (G1 whitelist: PubMed/Cochrane/AASM/ICSD-3/ICD-11 + verifiable provenance) with explicit GRADE grading (G2) + mandatory non-diagnostic disclaimer (G3) + referral-to-local-clinic (G4, no booking); Wave 1 = 睡眠時無呼吸症候群 (CPAP/oral-appliance/positional/weight/surgery/HNS); Murakumo-only, no-PHI-in-R0/R1 | 🟡 R0 | 2606072800 | 06-07 |
| meyasu 目安 | **統合 arbitrage actor** — a yardstick, NOT a trade; thin orchestrator that FUSES kakaku price-spread + supply/demand + mitooshi forecast distribution into one per-product arbitrage-intel card (fuse/publish/persist cells); aggregate-first, planner-routed (okaimono/danjo), speculation structurally unrepresentable (forecast = band not point; `:trade` not an enum member); cohort e2e kakaku→mitooshi→meyasu green; no-server-key (publish+persist operator-gated) | 🟢 R1 | 2606073201 | 06-07 |
| iryo 医療 | Japan 国内 **レセプト計算 + レセ電(レセプト電算)生成 + FHIR claim** engine — the member-principal billing counterpart the `karute` 電子カルテ(EMR) hands off to. **全件対応**: 公式マスタ(厚労省/支払基金, 数万件)を埋め込まず ingest する `master_loader`(正規化CSV + 厚労省基本マスター ColMap)で全 診療行為/薬剤/特定器材/病名 を解決。**全診療区分**(初診/再診/医学管理/在宅/投薬/注射/処置/手術/麻酔/検査/病理/画像診断/その他/入院) + 年齢区分→負担割合 + 公費重ね合わせ+負担区分 + 高額療養費 全区分(70歳未満ア〜オ/70歳以上 現役・一般・低所得, 外来個人/世帯) + 入院時食事療養。点数→円(1点=10円)/薬剤料 五捨五超四入/一部負担金 端数処理; emits IR/RE/TY/HO/KO/SY/SI/IY/TO/CO/SJ レセ電 records (rezept/receden/validate cells). Charter-clean inversion of proprietary レセコン/EHR-billing (ORCA-proprietary/Epic/Cerner): licensed 保険医療機関 is the billing PRINCIPAL, corp takes no insurance inflow (G7 preserves iyashi G13); PHI(氏名/生年月日) injected only at submission over the closed 審査支払機関 VPN, never the public substrate (G2); master-honest (no hard-coded tariff, G4); non-adjudicating (G5); pure-stdlib pywasm-ready + 51 tests green | 🟢 R0 | 2606074000 | 06-07 |
| niyaku 荷役 | automated port cargo handling — ship↔shore container loading/unloading robotics; **operator-side counterpart of funadaiku** (which builds the ships), closing the cargo-handling gap. Core problem = **anti-sway** of a suspended container = the cart+hanging-load (**Cartpole**) topology, verified end-to-end through the clean-room `isaacsim.core.api` (kotodama.nv_compat, G2 no-NVIDIA-binary): anti-sway feedback lands the box at residual <0.01 rad where a naive push diverges. 5 runnable pure-stdlib methods (crane_dynamics anti-sway + ZV shaper, stow_plan weight/rotation/reefer/IMDG no-rehandle, isaac_sway_sim Cartpole-STS **+ DoublePendulum boom-luff**, agv_transfer travel-profile/conflict/dispatch, **terminal_cycle end-to-end discharge orchestration**) + 9 Pregel cells (berth→stow→spreader→hoist→anti-sway traverse→yard→lash→attest + emissions audit); electric-crane zero-emission (G8), no-weapons-cargo (G10), no worker-pace surveillance (G11/G14); 55 tests green / 98% branch coverage | 🟡 R0 | 2606082000 | 06-08 |
| shiori 栞 | human-**Wellbecoming detractor observatory + transparent intervention** — the 取-concentration HUMAN side of the KG-mirror lineage (inochi/asobi/hokorobi/tsugite): weaves cohorts (aggregate) / detractors (structural: precarity/overwork/isolation/addictive-design/debt/housing-insecurity/chronic-pain/discrimination/care-deprivation) / drivers (patterns) / mitigators (havens) into kotoba Datom, runs **edge-primary detraction vs relief routed to RELIEF (救い)** → surfaces the **relief-gap** per cohort (→ ossekai), 取-holder driver concentration (→ danjo/tsumugi/keizu), and the **intervention-design gap** (unrouted detractions). Fuses *analyse/warn* + *route-to-relief* that the prior roster split apart. **G1 = relief map, NEVER per-person affect/manipulation** (cohort-aggregate only, drivers are PATTERNS not entities, anti-addictive); **G8 = TRANSPARENT-INTERVENTION only** — shiori PROPOSES, ossekai CARRIES (on-chain-logged + consent-bound), structural change first (§1.4); intervention G7/Council-gated. R0 scaffold + `wellbecoming-ontology` + seed (39 nodes/55 縁) + pure-stdlib pywasm-ready `analyze`/`datom_emit`(EAVT)/`coverage_report` + 11 tests green | 🟡 R0 | 2606082102 | 06-08 |

| shinogi 鎬 | **exam-competition involution (内卷) LIFECYCLE system-dynamics observer** — the junkan 循環 method (ADR-2605290927) pointed at high-stakes examination systems + their downstream (China 高考 primary; 수능/受験/JEE-NEET/Finland/Germany + JP/KR lost-generation siblings 就職氷河期/さとり/N포세대). ANALYSIS-ONLY: from passive/public/AGGREGATE facts it folds 33 **drivers** (policy/institution/practice/norm) into **9 pressure stocks** across 3 coupled phases — **Phase 1 EXAM** (positional-scarcity / effort-inflation=内卷核 / credential-signaling / wellbeing-erosion / family-capture / failure-penalty) · **Phase 2 LABOR** (labor-absorption-deficit = **卒業即失業** graduate unemployment) · **Phase 3 WITHDRAWAL** (effort-efficacy-collapse = **頑張れない** / withdrawal-prevalence = **躺平/寝そべり** lying flat) — on the kotoba Datom log, each with 誰が/経緯/関係者, and reads off which R/B **loops** (10) spin 悪循環/好循環 + Meadows leverage candidates. Foregrounds **TWO sober relief read-offs**: the **受験失敗** cycle (R-failure-despair) + the **卒業後 頑張れない/躺平** cycle (R-effort-futility + R-lying-flat-spiral), both framed STRUCTURALLY (§1.4 — the system eroded effort's efficacy, not laziness; lying flat = rational/self-protective, never pathologized) and **routed to RELIEF** (kokoro/shiori/manabi), never amplified/moralized. **G4** analysis-only/no-actuation (by absence) · **G5** hypothesis-only · **G6** aggregate-only (no per-student score/PII) · **G7** wellbecoming-positive/non-eschatological · **G8** relief MAP never a student/school/country shame-rank · **G11** candidates-not-directives. Seed read-off: the whole-lifecycle involution reads vicious; the 卒業後 withdrawal cycle carries a large relief-gap (pressure≈2.48 vs relief≈0.29 — the back half is least-cushioned); the exam-phase 2020– era (双减+mental-health+躺平) nets toward relief. **+ 3 deeper layers**: `simulate` (deterministic time-series stock-flow sim — an intervention flips the involution-index 0.59→0.16, 頑張れない 1.0→0.03, 躺平 0.80→0.04; a what-if G5, never a forecast N3); **`energy_flow`** (the wellbecoming **エネルギー流の設計**: re-routes society's effort-energy from the dissipative 内卷 zero-sum channels into wellbecoming-yielding ones — current wb≈0.04 despite maximal effort → designed≈0.42; **two ledgers never conflated** per uzu 渦 ADR-2606211500: effort=conserved flow, wellbecoming=index; a structural candidate §1.4/G11); **`social`** (social-protocol activity: dry-run AT-proto mirror posts of findings + the energy-flow design, ≥2 sources, person-excluded, **no-server-key**, `build-live` raises — live = member CACAO leash + Council per G14/ADR-2606281500). clj-native (`shinogi_edn`/`analyze`/`simulate`/`energy_flow`/`kotoba`/`autorun`/`social` + ontology 9-stock/10-loop + 33-driver/6-juris seed + content-addressed findings ledger + static did.json); 43 tests / 446 assertions green (bb). NOT ossekai / test-prep / forecaster / prescriber / profiler / clinical-service. Follow-up: child repo + west + RAD | 🟢 R0 | 2606291200 | 06-29 |

| tedai 手代 | member-COMPUTER-operation — karakuri's OS-layer sibling (web : karakuri = computer : tedai = robot body : tazuna): uniform DesktopOp vocab over the member's OWN computer via T1 scripting/accessibility-API > T2 stance-permitted vision-pointer (**on-device vision — a screenshot never leaves the device**) > T3 file-level; `:outward` safety class held at Council gate even with member-sig; own-device-only (not a RAT), no-surveillance (never bossware — verbs structurally unrepresentable), no anti-cheat/DRM evasion, no-server-key, hash-only evidence; browser → karakuri (N7); 113 tests green | 🟡 R0 | 2606101400 | 06-10 |

| hakoniwa 箱庭 | **forward-simulation observatory** — the charter-clean inversion of a swarm-intelligence prediction engine (the `666ghj/MiroFish` shape). Runs a CONTAINED miniature world (箱庭) of **FICTIONAL latent personas** forward via Friedkin-Johnsen opinion dynamics (K-replica ensemble, deterministic `sha256`-seeded jitter — no `Math.random`) to surface a **DISTRIBUTION over possible futures** routed to RESILIENCE/preparedness; the generative front-end that **feeds mitooshi 見通し** (which had the leak-free proper-scoring loop but no generative engine) and serves sonae/kazaori. Authorized by the **synthetic-persona charter carve-in** (ADR-2606111400): simulating fictional agents ≠ surveilling real people. **G1 = synthetic personas only** (`:persona/synthetic true`, no PII, real persons unrepresentable — refused at load by `assert_synthetic`); **G2 = distribution-only** (`:forecast/point-asserted` structurally false, no point field; 非終末論); **G3 = non-steering** (resilience-only use enum; trade/target/manipulate/campaign unrepresentable); **G4 = transparent/reciprocal** (whole box plaintext-public; 相互監視). **R1 (founder/Council Lv7+ 1/1, 06-11): runs AUTONOMOUSLY + SOCIAL EMISSION + REAL-ENTITY INGEST + LIVE LLM-SWARM authorized** — `autorun` heartbeat → simulate (scalar kernel OR `--swarm` LLM-per-agent via Murakumo, kernel fallback) → distribution → Murakumo narration (loopback-only, graceful template fallback) → member-signed `:published` post (G2 no-point + G3 no-steer scanned at draft+emit) → content-addressed tx persisted to the **append-only kotoba Datom log** (commit-DAG, `verify_chain` tamper-evident, resume-safe). **`ingest` pulls REAL PUBLIC entities from Wikidata** (orgs/topics as `:authoritative :entity` nodes; **natural persons P31=Q5 DROPPED at ingest, G1**; agents stay synthetic) → content-addressed box (CIDv1 ipfs-parity; offline fixture reproduces the live CID). external AT-Proto relay + ingest scope-expansion = the operator/Council-gated steps (no-server-key). pure-stdlib pywasm-ready `world`/`simulate`/`distribution`/`datom_emit`/`murakumo`/`social`/`kotoba`/`autorun`/`ingest`/`cid` + 30 tests green; real-person modelling unrepresentable (G1) | 🟢 R1 | 2606111500 | 06-11 |
| mimamori 見守り | covenant keeping membrane (mishmeret ha-adam 相互保持者会, ADR-2606112200 D6 の実装) — bonds of keeping: offer → kept-signed consent → **content-free** heartbeat → per-act-consented care-routing ({kokoro,wakai,iyashi} のみ — 通報 rail は enum 非加盟) → relay 継ぎ → 無条件 exit; **退化系列 (五人組→隣組→Stasi→社会信用) を表現不能化** (no-denunciation / no-score / consent / symmetric-visibility / NEVER-a-throne); coverage は aggregate-only (誰の保持者でもない人間を作らない — 数えるが名指さない); shomer vow 典礼 seed (musubi ceremony type); **R1-offline**: §D4 offer-matching cell (unkept 8→0 on synthetic seed, capacity-capped deterministic round-robin) + autonomous heartbeat + content-addressed append-only kotoba commit-DAG persistence (verify_chain tamper-evident, resume-safe; ADR-2606091000 pattern); + keeper-side **social-capital mint bridge** (moyai ledger verbatim reuse per ADR-2606082100 Part A: keeping=wellbecoming介入 → mint to KEEPER only, per-epoch cap, decaying, cash≡0, no-vote/no-stage, refs sha256-opaque — ledger never becomes a who-keeps-whom registry); **fleet-registered** (MimamoriHeartbeatCell in cell-runner cells.edn — benjamin, off-minute cron, healthz 13080; runner spawn path = 2605192415 maturity track); **wasm build-ready** (componentize-py, rasen pattern; STATELESS heartbeat → host owns the log = G5 in the architecture; exports heartbeat/coverage/bonds-of/**vow**; moyai ledger vendored verbatim at build); **identity-resolvable**: actor-profile-seed EDN (SSoT, 34th actor) + INFRA_ACTORS tier-3 fallback + static `/actor/mimamori/{did,profile}.json` (publisher-emitted, did.json CID `bafkreifflc…`; KV/kotoba ingest = operator step); schema 正本 `mishmeret-ontology.kotoba.edn` (ontology↔validator parity test-bound — 負の空間 :mishmeret.person/* / :police / :db/retract も明文宣言); synthetic DIDs only, live legs G7; **39 tests green**; ADR accepted (R1 LANDED 06-11, impl record in ADR) | 🟢 R1 | 2606112300 | 06-11 |



| hinagata 雛形 | **legal-document-template commons** (法律文書雛形) — the fairness/access sibling of the legal lineage: chigiri 契 runs the PROCEDURE substrate, the legal-corpus (ADR-2605262800) ingests the STATUTES, hinagata holds the published, fair, openly-licensed DOCUMENT TEMPLATES (contracts/agreements/covenants) decomposed into reusable CLAUSES and **binds every clause to the actual public statute it cites or is mandated by** (`legal-template-ontology` `:cites-statute`/`:mandated-by` 縁 — the EDN link that makes a template traceable to real law: CISG/GDPR/ILO/eIDAS/ESIGN/UETA/借地借家法/民法/利息制限法/特定商取引法/電子署名法/Apache-2.0 + religious/customary/mixed law (Shulchan Aruch heter-iska/AAOIFI qard/UNIDROIT Principles/Israel Contracts Law), each with official source URL — all 6 legal systems represented). Edge-primary **statutory groundedness** (template = Σ incident clause-citation × disclosed optionality, on read; no stored score) routed to **PUBLIC RELEASE**; `publish.py` content-addresses every template body to a kotoba IPFS CIDv1 (**ipfs-parity verified**, anyone may fetch+verify+reuse, Apache-2.0+Rider); **electronic contracts** via `esign.py` rendering a template → UNSIGNED `com.etzhayyim.esign.envelope`, member signs client-side with WebAuthn (**no-server-key**, ADR-2605231525). **G1 = a COMMONS, NEVER the practice of law** (no advice/opinion/representation/enforceability-certification; UPL structurally excluded, shared boundary w/ chigiri G14; citations are DISCLOSED facts not verdicts, N3). 60 templates / 48 clauses / 166 real statutes / 26 jurisdictions / 34 concepts / 16 languages seed (39/41 clauses statute-bound — only `:definitions`+`:service-levels` unbound by design, structural/contractual; all 6 legal systems covered; the 8 most cross-cutting clauses — e-signature/data-protection/sale-warranty/employment/tenancy/IP-licensing/cooling-off/dispute-resolution — each grounded across many jurisdictions; e-signature + data-protection clauses grounded worldwide — eIDAS/ESIGN/UETA/UNCITRAL/India/Singapore/China/Germany/Korea/JP e-sign + GDPR/LGPD/DPDP/PIPA/CCPA/France privacy; families incl. sale/NDA/DPA/lease/employment/consulting/OSS/loan/donation/partnership/power-of-attorney/settlement/heter-iska); live legal-corpus binding + IPFS pin/IPNS G7-gated; pure-stdlib pywasm-ready (`analyze`/`datoms`/`coverage`/`envelope`) + `validate.py` integrity checker (0 errors / 0 warnings) + generated `MATURITY.md` scorecard + `query.py` knowledge-graph query interface (incl. coverage-gap worklist) + all 10 ontology edge kinds exercised + EAVT datom-emit + 39 tests green | 🟢 R1 | 2606111954 | 06-11 |

| kadode 門出 | **labour-resignation concierge + 使者 (退職代行)** — the labour-liberation worker-EXIT sibling of the legal-concierge lineage (toritsugi 取次 / kurashimori 暮らし守 / tasuke 助), reusing their UPL-bounded default-self-submit pattern + hinagata 雛形 for the document layer. Helps a worker resign with dignity (門出 = setting out on a new path), grounded in real JP labour law, and **RELAYS the worker's already-formed unilateral resignation as a 使者** (民法627 = unilateral right, employer consent NOT required). **G1 = 使者 not 代理人**: it NEVER negotiates terms/severance/settlements (弁護士法72条 非弁行為禁止) — any matter needing negotiation (未払い賃金/退職条件/示談/損害賠償) is routed to a labour union (団体交渉, 労組法6) or a lawyer; **the UPL boundary is enforced in code** (`recommend_route`/`build_relay` refuse+escalate) **and proven by tests**, not just documented. The charter-clean, **free** inversion of a commercial 退職代行 業者. `labor-exit-ontology` (scenario↔ground↔route↔risk); 10 scenarios / 12 real labour-law grounds (民法627/628/626・労基法137/15-2/16/5/24/39・労組法6・弁護士法72・雇用保険法7, each w/ e-Gov URL) / 4-rung escalation ladder (self→使者→union→lawyer) / 5 employer 引き止め risk-patterns each countered by a ground; `generate.py` renders the worker's OWN docs (退職届/願/即時/内容証明/有給, 一身上の都合 only — never a demand), content-addressed; PII via himotoki envelope; relay UNSENT (G7) + no-server-key. Lexicons `com.etzhayyim.kadode.{resignationRelay,escalation}`; pure-stdlib pywasm (`analyze`/`datoms`/`coverage`/`generate`/`relay`) + EAVT datom-emit + 26 tests green (incl. the UPL-invariant + relay-refusal) | 🟡 R0 | 2606112238 | 06-11 |
| sonae 備え | civilian **pre-disaster** foresight + preparedness + early-warning substrate — the BEFORE half of the disaster cycle that kazaori (response) leaves open; open-feed hazard watch (USGS/PHIVOLCS/JMA/GDACS/Copernicus) → per-site risk profile → **official-warning RELAY** (G8: relay-only, `authoritativeSource` cited — MUST NOT originate official seismic/tsunami warnings) → preparedness plan + stockpile pre-positioning (mizuho/mitsuho) → drills → imminence-signal handoff to kazaori (recommend-only; G10 keeps declaration with kazaori Council). NOT response (N3), NO false authority (G8), NO commercial disaster-prediction software (G4), NO surveillance (G6), community-scale only (G3), Murakumo-only (G7); Sendai + Sphere reference; motivating case 2026-06-08 Mw 7.8 Mindanao quake+tsunami; 6 cells / 6 Lexicons. _(files recovered 2026-06-12 from a sibling stash after a shared-checkout sweep — they were never lost, only swept untracked into stash@{1} of the toml→edn migration session)_ | 🟡 R0 | 2606091200 | 06-09 |
| abaki 暴 | anti-monopoly & chokepoint intelligence membrane — OSINT-based monitor to track, map (piercing the corporate veil), and structurally bypass entities/individuals attempting monopolies across compute/data/biology/logistics/knowledge domains; route-around not punishment. _(registration recovered 2026-06-12 from a sibling stash — ADR + actor landed earlier but were never indexed)_ | 🟡 R0 | 2606073100 | 06-07 |

| kaiyaku 解約 | **縁切り (tie-severance) executor** — the missing last leg of the organizer subscription pipeline (mailer → organizer → kaiyaku): the member's OWN service ties (subscriptions / dormant accounts 退会 / recurring card charges / SSO·payment 依存) edge-primary in the kotoba Datom log → per-TIE burden routed to RELEASE (`:keep/:review/:sever`, disclosed organizer thresholds, dependency **cascade-guard** downgrades to `:review-cascade` + rehome-first) → dry-run severance plan via karakuri tiers (T1 official-API > T2 ToS-permitted browser-use, `:prohibited/:unknown` refuses T2 by construction > T3 self-submit 解約/退会 procedure); evasion verbs unrepresentable; destructive = member-sig + dry-run + Council Lv6+ (execute() raises at R0); cost-of-severance honesty (notice/違約金 never planned around); **human relationships structurally out of scope (N1, test-enforced) → kokoro 心**; pure-stdlib pywasm-ready + EAVT datom-emit + **tate handoff ingest (compose 往復)** + severance-plans.json + 22 tests green | 🟡 R0 | 2606112201 | 06-11 |

| tate 盾 | **citizen legal-defense concierge** (defensive only) — (1) 不利条項スキャン: member's own consumer ToS / カード会員規約 / B2B 法人契約 vs a coded clause-pattern registry (自動更新トラップ/過大違約金/全部免責/14.6%超遅延損害金/リボ自動設定/抗弁放棄/無限賠償/競業避止/長期支払サイト/知財包括譲渡); flag = pattern + **DISCLOSED statutory anchor** (消費者契約法8-10条/民法548条の2/割賦販売法/下請法) + route (:kurashimori rights / :kaiyaku sever / :referral) — **never a validity verdict** (G2), consumer anchors never cross into B2B (G5); (2) 法的手続き応答支援: 支払督促/少額訴訟/訴状/行政処分/内容証明 → disclosed deadline **rules** (民訴391·393/373/378/159; 行審法18条; 行訴法14条 — never computed dates, G4) + 督促異議/答弁書/通常移行/審査請求 options, **UPL: representation unrepresentable** (G3, member self-submit) + **架空請求 guard** (genuine court papers = 特別送達 ONLY; court-vocab SMS → do-not-contact-sender + tasuke/#9110/188, G6) + referral-forward 法テラス/弁護士会 (G7); pure-stdlib pywasm-ready + EAVT datom-emit. **+ R1 worldwide (ADR-2606112400)**: registries jurisdiction-keyed (`:jp :us :eu :uk :de :kr :fr :au :ca :it :es :nl :br :tw :sg :in :cn :pl :se :at :pt :ie :ch :dk :fi :no :mx :be :ar :nz` — FAA仲裁/FTC negative-option/UDAP · CRD撤回権/UCTD · CRA 2015 · BGB §309/Mahnbescheid ZPO · FRCP/CPR/Reg 1896/2006+861/2007 · 약관규제법7-8조/지급명령 민소법470조 · Code conso L212-1+L215-1/injonction de payer CPC 1416), per-juris UPL anchor + referral/fake-help directories (state bar/FTC · Citizens Advice/Action Fraud · Verbraucherzentrale/Polizei · ECC-Net · 대한법률구조공단/금감원 · point-justice/cybermalveillance), G6 guard generalized via `:proc/genuine-channels` (personal service/förmliche Zustellung/court post/signification), **G10 jurisdiction-honesty**: anchors/procedures never cross jurisdictions (test-enforced adversarial), uncovered juris → `:unknown-jurisdiction` honest degrade (never guesses foreign law), coverage_report 21/193≈10.9% + **:us 州サブ管轄 10/50** + **:labor 専門トラック** (解雇通知 jp/de/uk — KSchG §4 3週間 等) + **:housing 専門トラック** (立退き jp 借地借家法26-28条 · de BGB §573-574 Widerspruch · uk s.21/s.8 + Protection from Eviction Act 1977 · us pay-or-quit + unlawful detainer; **全 housing 手続きに no-self-help 保護を不変条件としてテスト強制** — 退去義務は裁判所命令のみ・鍵交換/追い出しは違法; :de Kündigung 衝突は特定トリガーで解消, bare Kündigung は :unknown へ honest degrade) + **:enforcement 専門トラック** (差押え防御 jp 民執152条 給与3/4差押禁止+153条 範囲変更 · us CCPA §1673 25%上限+exemption claim · de ZPO §850k P-Konto 転換; **全 enforcement 手続きに :opt/protective 必在を parametric 強制** — 法定保護の行使は債務逃れではない (N3 維持); 『差押え最終通告』SMS 詐欺語彙をガードに追加) + **:insolvency 専門トラック** (債権者側 — 取引先/サブスク会社倒産時に member 自身の前払金・預け金を守る: jp 破産法111条 債権届出 · us Bankr.R.3002 proof of claim Form 410 · de InsO §174 Forderungsanmeldung; kaiyaku 縁-ledger と突合) + :ch **Betreibung/Rechtsvorschlag 10日** (SchKG 74 — 証拠不要で誰でも開始できる制度への唯一の防御 + 8a 登録簿不開示で信用保護) + :ie (CRA 2022/FLAC) + **:family 専門トラック** (家事 jp 調停期日/過料51条 · us divorce response 30日 default 回避 · de **FamFG §114 Anwaltszwang を正直開示** + VKH 本人申請; **全 family 手続きに kokoro 心 routing を不変条件として強制** — 全5計画トラック開削済み, 次の深化は管轄横展開) + :dk (forbrugeraftaleloven §28 — 拘束5か月後1か月解約) + :fi (KSL) + :no (forliksrådet 強制調停/fraværsdom) + **track 横展開** (:labor→kr 부당해고 3개월 근로기준법28조+서면27조/fr prud'hommes 12 mois L.1471-1+précision 15日 · :housing→kr 갱신요구권 6조의3/fr congé 6か月 loi 89-462+trêve hivernale) + **track×juris matrix** を coverage report に常設 (横展開ギャップの計測) + matrix fill: :us labor (at-will 開示 + **EEOC 180/300日** 私訴前提) · :uk enforcement (**7 clear days** + exempt goods £1,350) · :fr enforcement (saisie contestation 1 mois + **SBI 自動保護** — 仏版 P-Konto) + :mx (LFPC/PROFECO; fixture lineage :br→:mx→:ar) + insolvency/family 横展開 (:fr **déclaration de créance BODACC 2か月/forclusion** L.622-24 · :uk proof of debt IR2016 + **no-fault divorce AoS 14日** DDSA 2020) + :be (CDE VI.83/juge de paix ≤€5,000 本人可) + refer-when 必在 lint (必ず相談先がある) + :ar (Ley 24.240 art.37 + **botón de baja** Res.424/2020 → kaiyaku; traslado 15 días CPCCN) + :kr family (조정전치주의 가사소송법50조) + 米州 15/50 (NJ/VA/MA/CO/MN追加) + **manifest⇔registry 整合テスト** (管轄リストの doc-drift を構造的封殺; fixture lineage :br→:mx→:ar→:cl) + :au/:ca labor+housing (FWC unfair dismissal **21日** FW Act s.394 / NSW termination notice→NCAT only / ON ESA vs common law severance警告 / **ON N12 bad-faith→T5**, LTB+Sheriff のみ執行可) + 米州 20/50 + **fake-guard 語彙の registry 自動導出** (新手続き登録 = 即詐欺ガード; 手動同期クラス封殺2件目) + :kr enforcement (압류금지 246조 급여1/2+월185만원 — 5管轄目の差押え保護) + :fr family (avocat obligatoire 開示+AJ) + 米州 25/50 + **:dl/critical 期限フラグ** (KSchG 3週間/CH 10日/AU 21日/forclusion 等の破滅的期限はプラン先頭に必ず表示 — テスト強制) + :kr insolvency (채권신고/실권) + :au family (response 28日 + property 12か月別期限) + 米州 30/50 + **統一 protective 不変条件** (非civil全手続きに member 保護選択肢必在 — トラック別不変条件をメタ不変条件に統合) + :au/:ca enforcement (protected amount NSW CPA / **ON Wages Act s.7 80%差押禁止** — 賃金保護7管轄) + :nz (Disputes Tribunal ≤NZ$30k 弁護士排除 s.38; **email が宣言 genuine な初管轄** — declared-digital 設計の実証) + civil-only 管轄の自動可視化 + :it/:es labor (**impugnazione 60 giorni** L.604/1966 / **despido 20 días hábiles** ET 59.3 — 欧州最短級, 共に critical) + **critical 期限の全管轄 census** を report 常設 + **米州 50/50 完遂** (構造ギャップ閉鎖 — gap 行は『全50州収載・次は改正追跡』へ自動転換) + :br/:tw labor (verbas/TRCT + 勞基法11/12條・非自願離職證明) + :cn/:nl labor (劳动仲裁1年・N/2N / **vervaltermijn 2か月** BW 7:686a critical — labor 14管轄) + **tate→kaiyaku 機械可読ハンドオフ** (kaiyaku-handoff.edn — :kaiyaku ルートの自動更新条項を縁-ledger が直接消費, actors compose) + :es/:it housing (**desahucio oposición 10 días hábiles**/enervación · **convalida di sfratto** 欠席=確定/termine di grazia 90日 — 南欧高速立退きへの防御, 共に critical) + :ch labor (**Einsprache vor Ende der Kündigungsfrist** OR 336b) + ⚠ critical バナーをプラン表示に + :sg/:pt labor (**TADM 1か月** mediation前置・ECT 弁護士禁止 / **impugnação 60日+suspensão 5営業日** CT 386-387 — labor 17管轄) + **CLAUDE.md 数値の機械照合テスト** (手動同期封殺3件目 — 導入即, 過去の手書き family 数の誤りを検出・修正) + :se/:pl/:ie labor (**underrättelse 2 veckor** LAS 40§ / **odwołanie 21 dni** KP 264 / WRC 6か月 — labor 20管轄) + **kaiyaku 側 handoff ingest** (handoff_ingest.py — compose が往復で閉じ, 26件の自動更新条項が notice-window ワークリストへ round-trip; kaiyaku 20 tests) + :no/:nz/:in labor (**forhandlinger 2 uker** aml §17-3 + stå i stilling / **personal grievance 90日** ERA s.114 / ID Act 25F+gratuity Form I — labor 23管轄) + verify-service-date 必在 lint + **civil-only 完全解消** (labor 29管轄 — :dk 協約ルート/:fi 2v/:be CCT109 2mois/:mx **2 meses** LFT518+PROFEDET/:ar telegrama TT/:at **Anfechtung 2 Wochen** ArbVG§105+AK 無料代理 — computed gap が手元リスト漏れの :at を検出; **解雇通知は全30管轄で専門応答を持つ**) + :nl/:br/:se housing (**BW 7:272 同意しない自由** / **purga da mora 15日** Lei 8.245 / besittningsskydd+hyresnämnden — housing 13管轄; uppsägning/opzegging の労住同語衝突を雇用特定 trigger で分離, 裸の語は :unknown) + track depth 横展開率を report 常設 + :es/:br/:it enforcement (**SMI全額保護+累進** LEC607 / **原則 impenhorável** CPC833IV 世界最広 / **un quinto** c.p.c.545 — 賃金保護10法体系, **手続き102種に到達**) + response-plans.json (yoro UI 向け機械可読 — 機械間配線2本目) + :es/:it/:nl insolvency (**comunicación 1 mes/BOE 劣後リスク** TRLC / **insinuazione 30日前 PEC/tardiva** CCII / ter verificatie Fw108-110 — insolvency 9管轄) + **kaiyaku 突合の不変条件化** (全 insolvency 手続きが前払金=債権の縁-ledger 突合を必携 — actor 連携が構造保証に) + :es/:it/:br family (**abogado/procurador 必須の正直開示**+justicia gratuita 本人申請 / Cartabia 統一手続 473-bis / **cartório 低負担ルート** — family 10管轄) + clause-flags.json (yoro 配線3本目) + :pl/:at/:ch housing (**okres ochronny 冬季執行停止** / **MRG 裁判所経由のみの解約**§33+Einwendungen 4W / **Schlichtung 30日無料+Formular不備=無効** OR266l — housing 16管轄) + **不変条件一覧表を CLAUDE.md 常設** + protective census 88件 + :nl/:se/:tw enforcement (**beslagvrije voet 自動計算**/förbehållsbelopp/**薪資1/3+最低生活費** 115-1 — 賃金保護13法体系, enforcement 14管轄) + **R2 宣言** (ADR-2606122000) + **crawlable static site generator** (site_gen.py — 31ページ: 1法域=1ページ + index, schema.org **FAQPage JSON-LD**/sitemap/robots, 全ページ非裁定・UPL 免責常設, 広告・トラッキングゼロ; **Google 可視化の供給側が完成** — etzhayyim.com/tate/ への配置と Search Console 登録は operator ステップ) + **deploy copy を worker static に実体配置** (50-infra/etzhayyim-did-web/public/tate/ — 31ページ; shionome 前例; deploy-sync test が registry 成長時の再生成を強制; 残る operator 手順は worker deploy + sitemap 登録のみ) + :cn housing (**买卖不破租赁** 民法典725条) + :au/:ca insolvency (proof of debt+**FEG 国家保証**/Form 31 BIA+**WEPP 賃金保護** — insolvency 11管轄) + :tw/:se/:nl family (**調解前置** 家事事件法23條 / **betänketid 6-24か月** ÄktB / verweerschrift 6週+**advocaat必須**開示+toevoeging — family 13管轄) + kaiyaku 側 counts-sync (同期封殺5本目) + :pl/:at/:pt enforcement (**kwota wolna**=最低賃金 KP87 / **Existenzminimum** EO§291a+AK再計算 / **2/3 impenhorável** CPC738 — 賃金保護16法体系, enforcement 17管轄) + **SEO 現地語化** (title/meta に Mahnbescheid/desahucio/지급명령 等の現地手続き名を自動挿入 — 現地検索語で届く) + :no/:dk/:fi housing (**protest 1か月で構造逆転** husleieloven§9-8 / **indsigelse 6 uger**+教示義務 lejeloven / irtisanomissuoja AHVL — housing 20管轄) + **1 case = 1 actor** (case_actors_gen.py — 全126手続きに keyless mirror-actor `did:web:etzhayyim.com:actor:tate-<case>` を発行; profile から **case.json/checklist.md の DL** と**相談先** (公的無料ディレクトリ + yoro convo は将来 operator レグと正直開示) に到達; worker public/actor/ に505ファイル実体配置 + cases.json 索引 + site ページから DL リンク; verificationMethod 空 = no-server-key) + Nordic insolvency ×3 (**賃金保証3制度** lönegaranti/LG Garantifond/NAV lønnsgaranti 開示 — insolvency 14管轄) + README.md (GitHub 発見性) + index→cases.json 導線 + **書面雛形 template.md ×129** (記入式【 】構造テンプレート — 公式様式優先ポインタ/出頭型は正直開示; tasuke 被害届雛形と同型の UPL 整合; case actor の5番目の DL として repo+deploy copy に保存) + :pl/:at/:ch family (**低負担ルート開示が軸**: §55a einvernehmlich/gemeinsames Begehren+2年別居要件 ZGB114/mediacja — family 16管轄) + **分野別ランディング5ページ** (track-*.html — 管轄×期限の比較表, SEO/被リンクの主戦場) + kaiyaku README + :ch/:be/:fi enforcement (**SchKG93 Existenzminimum**/quotités+**enfant申告制の罠**/suojaosuus+**vapaakuukaudet** — 賃金保護19法体系, enforcement 20管轄) + **critical→protective 不変条件** (失権期限のある手続きには必ず守る一手); **session close ADR-2606122300** (45 waves — publication layer 完成・operator handoff: worker deploy + Search Console) (CA CCP §412.20 / NY CPLR 320 / TX TRCP 501.5 / FL / WA — 州既知なら州規則を開示追記・州不明は honest degrade) + self-pruning gap worklist (covered ⇒ drops off, staleness test-enforced; :br covered ⇒ unknown-fixture moved to :mx) + **registry⇔seed completeness invariants** (every clause pattern + every procedure must be exercised by a synthetic seed — untested registry entries structurally impossible) + case-insensitive matching + tasuke-in-every-fake-help lint + **digital-channel-never-genuine hardening** (SMS/email classify :suspected-fake for every proc unless explicitly declared — parametric over the whole registry, closed the mail-only-proc-via-SMS hole) + 'binding arbitration' 3-way meaning test (:us enforceable / :ca void / :in no-ouster) + 簡体/繁体 script separation (支付令≠支付命令) + currency-mismatch conservative-referral guard (外貨建て請求は refer-over 比較不能 → 専門家照会); 120 tests green | 🟢 R2 | 2606112301 / 2606112400 / 2606122000 / 2606122300 | 06-11 |

| tanemaki 種蒔き | **Public Fund grant-steward (fund-manager inversion)** — the OUTFLOW-side sibling of fuchi 扶持 (same give-only instrument algebra, pointed at EXTERNAL organizations) + the actor-ization of the PublicFundGrantCell evaluation lane (ADR-2605192145). Fuses DISCLOSED public evidence from the observatory lineage (kanjō 勘定 financials / kabuto 兜 supply-chain / tsumugi 紡ぎ 取-concentration / kosatsu 高札 designations / ooyake 公 registries / shiori 栞 relief-gap) into a **PUBLIC DD scorecard** over candidate grantee orgs: hard screens S1..S6 (非営利整合/Rider §2 非抵触/open-output/受領適法性/私的捕獲なし/透明性床, disclosed charter anchors, screens fire BEFORE weighting) → weighted rubric C1..C8 (**weights public, Σ=1.0 enforced** — mission-fit 0.20/openness 0.15/financial-stewardship 0.15/取-release 0.15/governance 0.10/wellbecoming 0.10/additionality 0.10/capacity 0.05) → route ∈ {excluded, insufficient-evidence, propose}. **G1 = steward not sovereign (enforced in code + tests)**: there is NO `:fund` route — every grant is **decided by 1 SBT = 1 vote** (GrantGovernor + timelock); the scorecard is a 参考意見 (content-addressed CIDv1, voters verify the bytes); `build_proposal()` refuses screen-conflicting/under-evidenced orgs and emits `advisory:true / bindsFund:false / drafted-unsent`. **G2 = the fund GIVES, never INVESTS**: instruments {grant, milestone-escrow, in-kind} only — equity/debt/convertible/revenue-share/carry/exit **unrepresentable** (fuchi G1 pattern) + investment-return language rejected. Seed ALL FICTIONAL (G6, test-enforced) — real-org DD is a G7-gated live leg from primary disclosure (kanjō pattern); no-server-key (cannot move funds/vote/submit); milestone-escrow tranches unlock on Council attestation. Lexicons `com.etzhayyim.tanemaki.{ddScorecard,grantProposal}`; pure-stdlib pywasm (`analyze`/`datoms`/`coverage`/`scorecard`/`propose`) + EAVT datom-emit + 31 tests green (incl. G1 no-conflicted-proposable + G2 unrepresentability + G4 rubric-sum + G6 synthetic-seed) | 🟡 R0 | 2606122001 | 06-12 |

| meisai 明細 | **member card-statement (利用明細) ingestion** — the statement-TABLE ground truth beneath the organizer→kaiyaku subscription pipeline: the member runs a READ-ONLY computer-use fetch leg on their OWN machine against their OWN card account (com-junkawasaki/computer-use-clj `sumitclub_meisai` — karakuri 絡繰 T2 posture, vault-injected credentials via `type_secret`, **local Ollama gemma 4 QAT** = Murakumo-conformant ADR-2605215000), meisai ingests the resulting local statement EDN into append-only `:meisai.stmt/*`+`:meisai.row/*` on a content-addressed local kotoba Datom log. **G2 credential/PAN unrepresentable** (ingest RAISES on credential-shaped keys + 13–19-digit PAN shapes, test-enforced); **G3 local-only personal data** (`data/` gitignored — never committed/pinned/published); G5 intake-CID provenance + dedup (re-ingest = no-op, verify_chain tamper-evident); read-only at source (G4); live fetch operator-gated, the loop itself does no network I/O (G7, test-enforced). First source sumitclub.jp (SuMi TRUST CLUB); lexicon/fleet/kaiyaku-handoff = R1; pure-stdlib pywasm-ready + 21 tests green | 🟡 R0 | 2606122400 | 06-12 |
| tatara 鑪 | **world manufacturing-plant + logistics GEOGRAPHIC KG** — the geographic / facility-scale layer of the supply lineage: **kabuto 兜** (who→whom supply edges) + **uchiwake 内訳** (product BOM) place the abstract graph, **tatara** places the producing FACILITIES on the planet — where they SIT, at what scale (`:plant/headcount-est` aggregate employment / `:floor-area-m2` / `:capacity-value`+`:capacity-unit` production capacity), feeding which corridor. `:plant/operator` joins kabuto `org.corp.*`. Mirror-lineage sibling (kabuto/tsumugi/inochi): edge-primary geographic **concentration** (per-sector country **HHI** + single-source flag + **chokepoint export-dependence**) routed to **REDUNDANCY / reshoring** — resilience map, **NEVER a target-list** (G2). `:flow/via` reuses watari `:lane/chokepoint` + watatsuna `:station/chokepoint` → a chokepoint's manufacturing export-dependence (tatara) + live vessel transit (watari) + cable load (watatsuna) compose into ONE maritime resilience picture. **G4 defining gate**: `:plant/headcount-est` is a DISCLOSED AGGREGATE SIZE (like market-cap) — **NO `:worker/*` / `:person/*` attribute exists**, an individual worker is structurally unrepresentable (Charter Rider §2(c) reciprocity axis; Wellbecoming §1.13; mirrors sarutahiko/itonami/niyaku), enforced by construction + tests. **Visualization landed**: three self-contained canvas globes generated from the seeds (none hand-copied) — `plant-globe.htm` (plants by sector) + **`world-supply-globe.htm`** (integrated: plants + live craft + watatsuna cable stations — ALL THREE resilience layers QUANTITATIVELY COMPOSED per chokepoint (静 plant export-dependence · 動 live craft transit · 静-infra cable load) — backed by **`compose.cljc`** (the cross-actor SSoT: resilience-composition.md + :composition/* datoms, viz consumes it)) + **`watari/viz/craft-globe.htm`** (watari's FIRST viz). cljc-native (analyze HHI/chokepoint/capacity + kotoba EAVT commit-DAG, verify-chain tamper-evident + **autonomous autorun heartbeat** persisting content-addressed tx — graph + :concentration/* + cross-actor :composition/* — to the LOCAL append-only kotoba log, resume-safe) + 4 kotoba-native lexicons (registerPlant/registerHub/recordFlow/registerChokepoint, G4 boundary in the write surface, lexicon↔manifest↔ontology↔seed parity test-pinned) + **kabuto-linkage crosscheck** (measures :plant/operator ⇄ kabuto :company/id, bridges short↔country-qualified ids, 24/29=83% + honest ingest worklist) + **DID registered** (INFRA_ACTORS.tatara → resolvable did.json + /search); 33 real plants / 6 hubs / 33 flows seed (9 sectors incl. pharma / 16 countries incl. MX/VN/ZA/ID/BR; chokepoints malacca 12·luzon 8·suez 5·gibraltar 5·panama 3·hormuz 1·taiwan-strait 1) + **generated MATURITY.md scorecard** (R0→R1 checklist 15/15) + static did.json/profile (public/actor/tatara, resolvable fallback) + **self-validating seed** (ontology↔seed integrity invariants: every attr declared, ids unique, coords in range, one-capacity-unit-per-sector rollup precondition, flow endpoints resolve) + **first-class `:chokepoint/*` geographic nodes** (7 seed nodes plotted as diamond markers on the globes — the shared map anchor for watari/watatsuna, referential-completeness test-pinned); 56 tests / 5,095 assertions green | 🟡 R0 | 2606171800 | 06-17 |

| torifune 鳥船 | **zero-net-carbon open launch-vehicle manufacturing + Transparent space access** — the 船大工 of the sky (funadaiku/sarutahiko/giemon build-pattern pointed at space access; the charter-clean SpaceX inversion). Designs the reusable open-design **Ama 天 class** (two-stage hydrolox) + plant + ascent/staging/recovery GNC sim on kami-genesis; builds the bus subaru 昴 operates; disposal plans feed hoshimori 星守 stewardship. **G1 = civilian launch ONLY, NEVER a weapon-delivery / ballistic-strike vehicle** — depressed/suborbital-strike trajectories + MIRV/PBV buses + kinetic RV + FOBS + munition payloads are STRUCTURALLY UNREPRESENTABLE (`:traj/class`/`:payload/class` enums have no strike/munition member; `check_g1` refuses, test-enforced) (§1.12 Transparent-Force + Rider §2(a)); **G2** zero-net-carbon propellant only (green-H₂ hydrolox / kamado synthetic-methalox net≤0, MEASURED); **G3** open-design + dividend-coupled (ADR-2606032130); **G4** Transparent (open + on-chain flight log + 1 SBT=1 vote); **G5** debris-responsibility (mandatory per-mission disposal, refuses a mission with none); **G6** no-server-key (sim dry-run, actual launch Council+operator-DID gated). `launch-vehicle-ontology`; pure-stdlib pywasm-ready (`ascent_sim` Tsiolkovsky Δv + `carbon_balance` + `disposal_plan` + `datom_emit` EAVT) + Ama seed (18 nodes/13 縁; +3,359 m/s Δv margin to LEO, net 0 kgCO₂e) + `wasm/README.md` componentize-py design + **7 tests green** (incl. `test_g1_no_strike_profile`); .cljc port + live legs gated | 🟡 R0+R1+R2 | 2606162355 | 06-16 |
| subaru 昴 | **Transparent connectivity-commons satellite constellation** (Starlink/OneWeb inversion) — a connectivity COMMONS, not a subscription ISP; the payload torifune 鳥船 launches, observed by hoshimori 星守 for stewardship; orbital sibling of watatsuna (cable) / tsutae (terminal) / noroshi (photonics links). Connectivity delivered as **§1.16 Social Security in-kind** (covenantal-universal, to the unconnected + disaster zones) — cash≡0, no-ads, no-subscription. Operates spacecraft (unlike hoshimori) but ONLY Transparently. **G1 = connectivity COMMONS, NEVER a surveillance / targeting / military-C2 platform** — NO DPI / user-geolocation-as-product / ISR-targeting-relay; military-exclusive C2 + jam-as-weapon UNREPRESENTABLE (no `:link/dpi`/`:user/location`/`:relay/targeting` attribute; `check_g1` refuses, test-enforced) (§1.12 + Rider §2(a),(c)); **G2** no person-tracking / reciprocal-symmetric — content E2E-encrypted (com.etzhayyim.encrypted), routed-but-unread; **G3** non-profit/no-ads/cash≡0 (no `:subscription`/`:ad-supported` entitlement representable); **G4** Transparent (open bus+protocol + on-chain ops log + 1 SBT=1 vote); **G5** orbital stewardship (low-deorbit-debt + disposal + darksat night-sky, couples hoshimori); **G8** no-server-key (live ops Council+operator-DID gated). `constellation-ontology`; pure-stdlib pywasm-ready (`link_budget` + `coverage` §1.16-reach + `stewardship` + `datom_emit` EAVT) + seed (15 nodes/12 縁; all links close, urban baseline excluded from reach) + `wasm/README.md` design + **8 tests green** (incl. `test_g1_no_surveillance_relay`); .cljc port + live legs gated | 🟡 R0+R1+R2 | 2606162355 | 06-16 |

| jinushi 地主 | **world land/building ownership ACQUISITION mirror** (clj-native; data-acquisition feeder of the land-sovereignty stack ADR-2605192100 §1.11 + 2605192245). The upstream observational mirror the on-chain LandRegistry lacked: measures *how much of the world's land we have data on, who holds it, where the 取-concentration is* → routed to commons-return. **Multi-source**: Wikidata national parks/nature reserves (public land; sanitized vs a real WDQS country-area denominator → **honest 85 countries / 6.20M km² = 4.17%**, corrected down from an un-sanitized 6.67% by dropping P2046 unit-error/marine outliers) + Wikidata buildings w/ owner+floors+height (**2,405 buildings / 19 countries / 1,389 owners**; two 取-concentration lenses — by #buildings = rail operators, by **total FLOORS** = real-estate developers 三井不動産/三菱地所) + **NYC PLUTO gov cadastre** (Socrata, public domain; parcel owner+floors **incl natural persons**) + **GLEIF** authoritative company linkage (owner LEI → kabuto/uchiwake/kanjō; QID → keizu/tsumugi; **221 owners → 690 buildings**). **Reframed public-record gate** (the constitution bans *asymmetric/monetized* surveillance, NOT personal data — Rider v3.1 §2(c)/ADR-2606082400; affirms 相互監視): P1 public-record provenance + P2 reciprocal-symmetric + P3 map-not-target/non-monetized; per-jurisdiction `jurisdiction.cljc` gate (bulk-public SE/US/GB/IE/NL/NO vs restricted DE/AT/CH Grundbuch/FR vs per-parcel JP/KR vs honest :unknown); natural persons representable from public registries (anonymized in the published KG, named raw stored in repo per operator directive). **Maturity**: per-source reliability 信頼度 (`confidence.cljc`, trust-weighted conflict resolution) + as-of diff 差分 (`diff.cljc`) + CID content-addressing (`cid.cljc`) + integrity `verify.cljc` + canonical Datom log (`emit_real.cljc`) + capstone `digest.cljc`. G3 = jinushi asserts no transfer/mint (only the on-chain LandRegistry moves land, member donation, no-server-key). Data in `80-data/jinushi-land/` (datalad substrate, ADR-2605241500; raw acquired data committed + CID-provenanced); WDQS-load-safe (snapshot = SoT, loop never queries WDQS; polite operator `fetch_wdqs.sh`). 14 `.cljc` methods + 70 tests / ~290 assertions green | 🟢 R1 | 2606162000 | 06-16 |

| kaname 要 | **cross-domain system-of-systems leverage-point (律速) synthesizer + おせっかい proposer** (clj-native) — the META synthesis layer the roster lacked: the single-domain mirrors (tsumugi/keizu/kabuto/chie/shiori/abaki/shionome/busshi/hokorobi/kosatsu/inochi) each observe ONE domain; kaname **JOINS** their DISCLOSED 取-concentration into ONE **multilayer (multiplex) graph** — layers = 10 domains (政治/宗教/組織/思想/経済/生態/security/wellbecoming/ai/information) — reconciles entities across domains, and computes **ON READ** (edge-primary; no stored score) the single structural position (the **要 / 律速段階**) whose release most improves resilience **across the MAXIMUM number of domains at once**: **L = C·(V/D)·(1+B)·(1−open), 要 = argmax L** (C concentration · V domain-versatility = the SoS discriminator · B inter-layer bridge-load = bounded betweenness proxy, R1 = full multiplex betweenness). **集中度だけでは律速点ではない**: seed で証明 — 高集中の単一ドメイン hoarder (V=1) は doctrine より L が低く、open commons は高 C でも L=0。Then routes the 要 to **OPENING** (route-around/redundancy/decentralize) and **PROPOSES おせっかい to ossekai** (advisory/unsent, ossekai carries consent-bound + on-chain-logged). Distinct from **junkan 循環** (system-dynamics CLD/Meadows, analysis-only) and **ossekai** (the actuator) — kaname is the graph-centrality synthesis between them; `graph-sos-intel` (deprecated RisingWave DB-introspection) is unrelated. **G1 leverage-MAP-never-target-list** (structural positions only; natural persons person-excluded; no coordinates — `osekkai` refuses, test-enforced); **G2 OPENING-only routing** (capture/seize/control/monopolize UNREPRESENTABLE — `route` raises, the property that makes a leverage-finder charter-clean); G3 おせっかい transparent/consent-bound/no-server-key; G4 non-adjudicating/edge-primary; **G5 no-thought-policing** (思想/宗教 = structural interfaces w/ on-record :en/basis; belief-content scoring unrepresentable); G6 synthetic seed, live mirror join G7-gated. **+ R1 (landed 06-17)**: `centrality.cljc` exact **Brandes betweenness** + power-iteration **eigenvector** + **ΔΦ** percolation/fragmentation sensitivity (the concrete L≈ΔΦ reading; all converge on the seed 要 — betweenness 40.3≫11.2) replacing the R0 bridge-proxy; `join.cljc` **live mirror JOIN proven on chie 智慧's REAL committed Datom log** (39 nodes/39 縁 → lifted into `:ai` layer, 34 縁, unmapped `:partners`/`:holds-role` dropped — no fabricated axis; lifted `:ai` concentration reproduces chie's own opening-priority OpenAI 5.55/Anthropic 4.60, kaname adds own betweenness OpenAI 114/EU-AI-Act 66/NVIDIA 64) + `reconcile-by-label` (shared entity across mirrors → spans domains → becomes the 要); **running a mirror = G7, joining a committed output = what kaname does**. **+ MULTI-MIRROR SoS JOIN (founder-approved 06-17)**: per-mirror adapter registry + `join-mirrors` joined **5 REAL committed mirror outputs** — chie(:ai)·tsumugi(:organization)·inochi(:ecology)·hokorobi(:economy)·shiori(:wellbecoming) → ONE reconciled multilayer graph **170 nodes/205 縁/5 domains** (each mirror's 縁-vocab adapter-mapped, unmapped dropped; forms/datom auto-detect; per-mirror load-normalized for fair compare). **Real result**: reconcile surfaced **OpenAI·NVIDIA·Microsoft·TSMC·SoftBank** as cross-domain entities (V=2, :ai+:organization, sourced from BOTH chie+tsumugi); whole-multiplex 要 = **OpenAI** (L1 1.992); top structural bridges = **NVIDIA(betw 523)/TSMC(456)** (compute chokepoints → route-around). **+ REAL WEB-INGEST (founder-approved 06-17)**: `ingest.cljc` web→mirror pipeline — fetch a PUBLIC page (homepage/公開投稿) → extract DISCLOSED org relations via **Murakumo local Ollama gemma-4-E4B** (ADR-2605215000) → mirror forms, **every edge :en/basis = source URL + stated phrase** (G5), person-excluded (G1), DISCLOSED-only (G4), no-server-key. Ran on real official sources (anthropic.com/news, nvidianews.nvidia.com) → gemma extracted **10 orgs / 11 basis'd edges** (Amazon→Anthropic invest · Anthropic→AWS/Azure/GCloud · NVIDIA→OpenAI $100B · OpenAI↔MS/Oracle/SoftBank) → committed `data/ingested-web.kotoba.edn` (:economy, :authoritative) → joined as 6th mirror: **OpenAI rises to V=3 (:ai+:economy+:organization), L1 1.992→3.513** — cross-domain 要 now grounded in **cited public data**. **+ LANGGRAPH-CLJ ACTOR ON KOTOBA COMMIT-DAG + 世界認識 (founder-approved 06-17)**: `kaname.graph` = langgraph-clj StateGraph (loads under bb) `:perceive-world(世界認識)→:leverage→:route→:osekkai→:persist`; **web-fetch も clj** (`ingest/fetch-text` babashka.http-client + `ingest-live!` over `data/ingest-sources.edn` — the actor runtime fetches, no operator tool, no-server-key, Murakumo gemma extraction); **persists to the canonical kotoba Datom-log commit-DAG** (`methods/kotoba.cljc` over shared `kotoba.datom`, EAVT `[:db/add e a v]`, idempotent-by-content, verify-chain tamper-evident, resume-safe, `data/persisted/` gitignored); `kaname.autorun` heartbeat **verified live** (beat#0 perceive 6 mirrors 173n/216縁→要=OpenAI→persist head CID; beat#1 :no-change; verify-chain :ok). **+ LIVE-ENGINE BRIDGE + FLEET-REGISTERED (founder-approved 06-17)**: `methods/kotoba_bridge.cljc` (ibuki-R3 pattern) pushes each local tx to the LIVE kotoba engine via `…datomic.transact` (host allowlist loopback+EVO-X2, graph-cid KotobaCid parity, `:kaname.tx/*` provenance, `:bridge/*` exactly-once cursor, expected_parent, dry-run default) — **verified vs running :8077**: dry-run computed graph CID `bafyrei…`, **VERIFIED LIVE vs running :8077 (06-17)**: with `KANAME_KOTOBA_OPERATOR_DID` = the node operator DID (loopback node-persists-on-behalf, ibuki-identical), `KANAME_KOTOBA_LIVE=1` committed the readout to the REAL Datom graph — graph `bafyreibtzz…`, remote tx `bafyreiccor…`, commit head `bafyreigfnz…`, **73 datoms confirmed**; re-push = no-op (`:pushed 0`, exactly-once proven). kaname's OWN self-certifying did:key (`did:key:z6Mkie7K…`, generated 06-17, Ed25519 seed in **1Password** `etzhayyim-kaname-did` CONCEALED, never committed) = its identity + member-delegation path. `KanameHeartbeatCell` registered in cell-runner cells.edn (node naphtali, cron `53 * * * *`, healthz 13083). **Heartbeat closes the loop**: `autorun beat :bridge?` (CLI `--bridge`) pushes the commit-DAG to the live engine after persist, FAIL-OPEN (engine down/DID absent → :error, beat still completes) — verified live: beat#0 appended + bridge pushed=1 datoms=74, beat#1 :no-change + bridge pushed=0 (idempotent both legs; `persist!` compares past `:bridge/*` checkpoints, regression-tested); cron cell stays local-only. **+ DEPLOYED 実運用 (06-17)**: per-user macOS LaunchAgent `com.etzhayyim.kaname.heartbeat` (must run local — depends on LOCAL :8077 + mirror files + bb), hourly at :53, runs `autorun --bridge` (perceive→要→persist→live push); operator DID read dynamically from the node env (loopback), bridge fail-opens; **verified on install** (kickstart beat#0 appended + bridge pushed=1 datoms=74 to the real graph; 2nd kickstart :no-change + pushed=0; last exit 0); `deploy/` install.clj(bb)/plist-template/runner/README committed (generated plist+log machine-local). 13 `.cljc` (9 methods + graph + autorun + cell + bridge) + deploy + ontology + 2 lexicons + fixtures + **51 tests / 214 assertions green** (bb) | 🟢 R0+R1 deployed | 2606172100 | 06-17 |

> **Note**: ADR ids `2605263400` and `2605263500` each label two distinct ADRs (parallel-agent race in the source); filename + actor name disambiguate. Tracked for a future ADR-id reconciliation.


## Repo Layout (Shannon-Optimal 8-Layer, ADR-2604251830)

```
etzhayyim/root/
├── 00-contracts/        # lexicons / bpmn / dmn / Rego policies / resources (JSON-LD)
├── 10-protocol/         # atproto, xrpc, lexicons-bundle, signal, did-etzhayyim,
│                        # wproto, at-client, signal-client,
│                        # kotoba-datomic (Holochain-iso composition spec, ADR-2605231400)
├── 20-actors/           # kotodama (Pregel framework + host SDK + unispsc_agents/ 18,345 LangGraph agents per ADR-2605171300),
│                        #   kotodama-go, kami-engine-sdk, etzhayyim-bpmn-sdk,
│                        #   etzhayyim-sdk (kotoba substrate, ADR-2605172000+2605172100)
│                        #   kuni-umi      planetary-infra producer    (ADR-2605201400)
│                        # Tier-B religious-corp actors (30): each has ADR + manifest + cells + lex.
│                        #   See Status § "Tier-B actors" for the full roster (name · purpose · ADR).
│                        #   Per-actor gates/prohibitions live in each actor's ADR + 20-actors/<name>/CLAUDE.md.
├── 30-graph/            # graph-schema, kagami, risingwave-udf, vectorization
├── 40-engine/           # Rust workspaces: kami-engine (reusable engine — git submodule
│                        #     of github.com/etzhayyim/kami-engine per ADR-2606011500 §4;
│                        #     kami-engine-sdk nested inside; `git submodule update
│                        #     --init --recursive 40-engine/kami-engine` to populate),
│                        #   kami-apps (L3 etzhayyim *.etzhayyim.com product apps —
│                        #     kami-app-{bim,cad,live,maps3d,animeka-timeline};
│                        #     robotics/sim apps are maintained in the kami-engine
│                        #     submodule per ADR-2606011500), llm,
│                        #   kotoba (storage substrate engine — git submodule of
│                        #   github.com/etzhayyim/kotoba; 17 crates Apache-2.0;
│                        #   subsumes ipfs-pinner / nats-jetstream-* / mst-projector
│                        #   / lancedb-wasm / etzhayyim-xrpc-proxy /
│                        #   libsignal wrappers; kotoba-llm local-inference
│                        #   constitutionally disabled per ADR-2605215000 +
│                        #   Charter Rider §2(i); ADR-2605262130 Phase 0)
├── 50-infra/            # SEEDED: geth-private, holochain, ipfs, blockscout,
│                        #   k8s/atproto-pds, k8s/murakumo-kubelet (migrated 2026-05-17),
│                        #   lancedb-wasm, yata,
│                        #   nats-tiered-storage, nats-jetstream-{objectstore-s3, kv-resp},
│                        #   sveltejs-adapter-wasm, spin-tinygo-flight
│                        # SUBSTRATE (ADR-2605171800 + 2605172100 + 2605172200):
│                        #   etzhayyim-did-web/ (CF Worker, LIVE 2026-05-17T03:25Z)
│                        #   mst-projector/   (Stage 3, scaffold)
│                        #   ipfs-pinner/     (Stage 4, scaffold)
│                        #   l2-anchor-contract/ (Stage 5a, Foundry Solidity)
│                        #   anchor-cron/     (Stage 5b, K8s CronJob)
│                        #   etzhayyim-paymaster/ (ERC-4337, Foundry Solidity)
│                        #   openmail-postage/ (Postage.sol, Foundry, Phase 1 scaffold)
│                        # RELIGIOUS-CORP CONSTITUTIONAL (ADR-2605192100 wave, 2026-05-19):
│                        #   etzhayyim-charters-compliance/ (Council attestation single SoT)
│                        #   etzhayyim-tithe-router/        (10% donation → Public Fund atomic split)
│                        #   etzhayyim-public-fund/         (5-of-7 Safe + 1 SBT = 1 vote)
│                        #   etzhayyim-land-registry/       (geth-private + Base L2 ERC-721 mirror)
│                        #   etzhayyim-force-authorization/ (Transparent Force, 1 SBT = 1 vote)
│                        #   murakumo/fleet.edn             (10-node cell placement)
├── 60-apps/             # open-*, public-*, atproto, ameno, yoro, comfyui, watashi
│                        # FIRST kotoba REFERENCE IMPL: open-isco/kotoba/
│                        # MAC MINI FLEET: comfyui/ (migrated 2026-05-17)
│                        # RELIGIOUS-CORP:
│                        #   etzhayyim-transparent-force-rd/ (open-source R&D registry per ADR-2605192315)
├── 70-tools/            # etzhayyim-cli, cdn
│                        #   charter-rider-applicator/      (retro-active Rider applier, ADR-2605192200)
├── 90-docs/             # CLAUDE.md (docs rules), adr/, baien/
├── CHARTER-RIDER.md     # Apache 2.0 + Charter Compliance Rider v3.2 (per ADR-2605192200 + 2606062100 + 2606161700)
├── COUNCIL.md           # 5-seat Bootstrap Council roster (per ADR-2605192300)
├── COUNCIL-BOOTSTRAP-RFP.md  # 30-day public RFP for Seats 2-5 (2026-05-20 → 2026-06-19)
├── LANDS.md             # 4-layer permanent land record roster (per ADR-2605192245)
├── CLAUDE.md            # this file
├── deps.toml            # SSoT for [platform.operating_entity] + monorepo state
├── LICENSE              # Apache 2.0 (NOTICE/CHARTER-RIDER.md adds Rider conditions)
├── MEMBERS.md           # 信者 dual-permanent record (per ADR-2605172600)
├── README.md            # public-facing
├── lefthook.yml         # pre-commit (trailing-ws + EOF; full hooks pending)
└── .gitignore
```

## Worktree isolation (CRITICAL — concurrent-agent safety)

**This repo's main checkout (`/Users/junkawasaki/github/etzhayyim-root`) is shared and
raced by multiple concurrent Claude agents** that `git checkout` different feature branches
in the *same* working tree. Untracked / uncommitted work in the shared checkout is fragile:
a sibling agent's `checkout` / `reset` / `clean` can wipe it without warning (observed
2026-06-04 — an entire untracked actor scaffold vanished mid-session).

**Rule — before ANY substantive multi-file work, Claude MUST isolate into a git worktree:**

1. **Enter a worktree first.** Use the `EnterWorktree` tool (creates an isolated checkout
   under `.claude/worktrees/<name>` on a fresh branch off `origin/main`) — or, outside this
   harness, `git worktree add .claude/worktrees/<name> origin/main`. Do the work there, not
   in the shared main checkout. (The `ooyake` agent already runs isolated in `/private/tmp/owt56`.)
2. **Commit early and often inside the worktree.** A commit is the only durable unit in a
   raced tree; a `git reset --hard` by a sibling cannot destroy a commit (recoverable via
   reflog), but it *will* destroy untracked files. Never leave a completed unit of work
   uncommitted.
3. **Scope commits to your own paths.** When the shared index already holds another agent's
   staged files, commit with an explicit pathspec (`git commit -- <your paths>`) so you do
   not sweep up a sibling's work.
4. **Branch off `origin/main`, not the current shared HEAD**, so a sibling's in-flight
   commit (e.g. another actor landing on the branch you happened to be on) does not become
   part of your history.
5. **Back up irreplaceable untracked work outside the git tree** (`cp -R … /tmp/…`) if you
   cannot commit immediately — insurance against a concurrent wipe.
6. **Treat the shared main checkout as read-only / not-yours.** It stays chronically dirty +
   divergent (uncommitted edits + a HEAD that is both ahead and behind `origin`); its working
   tree is a mix of *other* agents' in-flight work. Never commit it wholesale or assume its
   uncommitted state is yours.
7. **1 branch = 1 owner.** Do NOT push a divergent local commit onto a sibling's actively-CI'd
   PR branch (you'd graft onto their in-flight work + risk conflicts). To land such a commit,
   spin a fresh worktree off `origin/main`, `cherry-pick` it, and open its OWN PR (done
   2026-06-06: local `9a8600db59` → PR #1188, leaving the sibling's PR #1174 untouched).
8. **cherry-picks from the shared checkout drag in yoro build artifacts.** Conflicts in
   `60-apps/etzhayyim-project-yoro/**/static/_app/immutable/*` (hashed SvelteKit chunks) are
   generated noise — resolve by restoring yoro to `origin/main` (`git checkout origin/main --
   60-apps/etzhayyim-project-yoro && git add`), never hand-merge them.

**Shell gotcha (zsh):** `$PIPESTATUS` is unset in zsh (it's `$pipestatus`, 1-indexed), so
`cmd | tail; echo ${PIPESTATUS[0]}` silently reports `tail`'s exit, not `cmd`'s. Check a
command's real exit before piping it (e.g. `git apply` failed-but-looked-OK this way).

Exit with `ExitWorktree` (`keep` to preserve the branch). The session's cron `/loop`
iterations continue inside whatever worktree the session is in.

**Rule — clean up once the branch is MERGED.** A worktree + branch is durable only while its
work is in flight. **After the branch's PR is merged into `origin/main`, delete BOTH the
worktree and the branch** — a merged branch left on disk is dead weight that clutters
`git worktree list` / `git branch` and invites confusion (a sibling agent checking out a
stale merged branch). Do NOT delete before merge (an open PR's branch is the only durable
copy of the work). Sequence once merge is confirmed:

1. `ExitWorktree` with `action: "remove"` (removes the `.claude/worktrees/<name>` checkout +
   its branch). It refuses if there are uncommitted/unmerged changes — that refusal means the
   merge is NOT actually complete, so stop and investigate, do not force.
2. Outside the harness, the equivalent is `git worktree remove .claude/worktrees/<name>` then
   `git branch -d <branch>` (lowercase `-d` = merged-only; it refuses an unmerged branch — use
   `-D` ONLY after confirming the work is truly merged). Prune stale registrations with
   `git worktree prune`. The `clean_gone` command also sweeps branches whose remote is `[gone]`.

Only the worktree whose PR is still open (or whose work is unmerged) is kept.

**Command — "worktree cleanup".** When the user says **`worktree cleanup`** (or asks to clean
up worktrees), run this exact sweep over the current branch + every worktree
(`git worktree list`); resolve each branch's PR state with `gh pr list --head <branch> --state all`:

- **PR MERGED** (or branch fully contained in `origin/main`, or remote `[gone]`) → **delete** the
  worktree + branch (`git worktree remove …` then `git branch -d/-D …`, per the sequence above).
- **No PR yet** and the branch has commits ahead of `origin/main` → **open a PR** (`git push -u`
  then `gh pr create --base main`). Skip a branch with nothing ahead of main.
- **PR already OPEN (unmerged)** → **leave it** untouched.

Then **reintegrate every stash** (`git stash list`): for each entry (newest first), **merge it back**
with `git stash pop` (apply + merge into the working tree). **On conflict, resolve it** — keep the
stash's genuine local edits; for raced substrate / generated build artifacts follow the main-priority
rule above (`git checkout --ours/--theirs -- <file>` as appropriate, restoring yoro `_app/immutable/*`
to `origin/main`), then `git add` the resolved paths. Once the apply is clean, **drop the stash**
(`git stash drop`; a clean `pop` already drops it). **Leave no stash behind after cleanup** — this
overrides the conservative "温存 / keep the stash" stance used during routine `main` sync, because
`worktree cleanup` is an explicit reconcile-and-finish request. (Skip a stash only if its conflict
genuinely cannot be resolved — surface it and stop rather than discard real work.)

Never commit/push the shared main checkout's dirty working tree (other agents' in-flight work);
push only the branch's committed HEAD. Report the final categorized outcome (worktrees + branches +
stashes reintegrated/dropped).

**Command — "closing" (one-shot close-out).** When the user's instruction contains the word
**`closing`** (英語語 "closing" を含む場合), take the CURRENT worktree's work all the way to
landed **without further confirmation** — run this sequence end-to-end for the active branch:

1. **Commit** any uncommitted work in the worktree (scoped to your own paths, per the
   worktree-isolation rules above).
2. **PR create** — `git push -u origin <branch>` then `gh pr create --base main` (skip if a PR
   already exists for the branch; reuse it).
3. **Merge to repo** — merge the PR into `origin/main` (`gh pr merge <#> --squash --delete-branch`,
   or the repo's standard merge). This is the explicit standing authorization to merge that the
   `closing` keyword grants; do not ask again. If merge is blocked (failing required checks,
   conflicts, review gates), STOP and report — do not force-merge past a real gate.
4. **Cleanup** — run the **"worktree cleanup"** sweep above (delete the now-merged worktree +
   branch via `ExitWorktree action: "remove"` / `git worktree remove` + `git branch -d`).

`closing` is the single keyword that authorizes the otherwise-confirmation-gated merge step;
absent it, stop at PR-open and leave merge to the user. Report the final state (PR URL, merge
commit, worktrees removed).

## ADR Authority (per ADR-2605170900)

**This repo is canonical for religious-corp open ADRs.**

- ADR placement matrix → `90-docs/adr/README.md`
- ADR ID convention → `90-docs/CLAUDE.md` § "ADR ID Convention"
- Template → `90-docs/adr/template.md`

## Operational code = clj/bb over the kotoba Datom log (REPO-WIDE RULE — 実装/engineering)

**Newly authored operational / daemon / heartbeat / loop / tooling code SHOULD be
Clojure on babashka (`bb`), with state on the kotoba Datom log (datomic-isomorphic,
ADR-2605312345) — NOT Python (`.py`) or shell (`.sh`).** This is an **実装/engineering
convention** (changeable at the implementation layer, not a charter invariant): it
generalizes the substrate boundary (state = kotoba Datom log) to *executable* substrate,
so the org's own moving parts are clj/bb folds over append-only Datom journals and inherit
as-of history (生命進化), content-addressed snapshots, and crash-resume for free — the same
reasons the actors are being ported py→cljc (the clj-port waves).

Concretely:
- **Author daemons/heartbeats/CLIs as `bb` tasks** in `bb.edn` backed by a clj/cljc
  namespace under `70-tools/src/etzhayyim/…` (reference: `etzhayyim.organism` =
  the resident organism heartbeat + 情緒 narration; `etzhayyim.vitals` = pulse/joucho/
  vitals). Persist via `etzhayyim.kotoba.engine` (`kt/connect`→`kt/transact`→
  `kt/snapshot!`), append-only when you want evolution, journal-per-feed.
- **Inference** (e.g. narration) prefers the **Murakumo fleet** (per-node Ollama over
  Tailscale / LiteLLM gateway) — DEFAULT-PREFERRED, objective-function-assessed per Rider
  v3.3 §2(i) / ADR-2606172359 (NOT a categorical vendor ban); use `babashka.http-client`.
  no-server-key (read-only) where possible.
- **Residence** = a launchd LaunchAgent (`50-infra/launchd/*.plist`, OS config, not a
  script) invoking the `bb` task with `KeepAlive`/`RunAtLoad`, NOT a `nohup … &` bash
  loop (those die on reboot and leave no Datom trail).
- **Shelling out** to a *system binary* (`git`, `tailscale`, `ipfs`) via
  `babashka.process` is fine — that is not "a shell script". The rule bars authoring
  our *logic* in `.py`/`.sh`, not invoking installed tools.
- **Exemptions**: 3rd-party/vendored code (`lib/`, `vendor/`, `*-fork/`), generated
  build artifacts, and an actor's still-unported legacy `py/` during an in-flight
  cljc port (the port itself is the fix).
- **Enforced-forward (ADR-2606072802)**: `bb lint:no-new-shell` FAILS on a NEW first-party
  `.sh` under `20-actors/` (existing ones are GRANDFATHERED in
  `70-tools/src/etzhayyim/lint/shell-baseline.edn`; the baseline is shrinks-only — port a
  `.sh` to bb + delete it, then `bb lint:no-new-shell --update`). New actors ship
  `run_tests.clj`, not `run_tests.sh`; `etzhayyim.vitals` prefers the `.clj` runner. The 218
  grandfathered `.sh` are not mass-ported (low value, high churn) — they convert opportunistically.

## Do Not

> **This list mixes two kinds of rule (classify before treating any as immutable; 固定するのは priority):**
> **(憲法/charter)** — license default, Charter Rider non-weakening, land inalienability (no `transfer()`/`burn()`), transparent-force-only, no-eschatology, no-CSAM, Tier-0 priorities. These are Tier-0/derived and amend only by Council Lv7+ (the priorities themselves are fork-only).
> **(実装/engineering)** — prefix discipline, secrets hygiene, rename-wave scoping, substrate-import rules, no-RunPod-style backend choices (now objective-function per v3.5). These are engineering/governance decisions, changeable at the implementation layer **without a charter amendment**. A "Do not" here is a load-bearing convention, not a constitutional invariant.

- Do not author new first-party operational/daemon/heartbeat/tooling scripts in Python or shell — use clj/bb over the kotoba Datom log (see §"Operational code = clj/bb over the kotoba Datom log" above; 実装/engineering convention). Shelling out to system binaries via `babashka.process` is allowed; vendored/3rd-party + in-flight py→cljc ports are exempt.
- Do not introduce legacy organisation-specific prefixes in newly authored code. Use `etzhayyim-` or no prefix. Existing seeded files with legacy prefixes will be renamed in a follow-up cutover.
- Do not weaken the Apache 2.0 license default. Religious-corp public-interest activity requires permissive license.
- Do not weaken the Charter Compliance Rider v3.5. §2 and 三層 enforcement (L1 license / L2 便益 / L3 評価) derive from the Tier-0 priorities; per ADR-2606062100 they are Tier-1 Derived Policy, amendable ONLY by Council Lv7+ unanimity + a priority-conformance attestation (never weaker). The Tier-0 priorities themselves (incl. 永久記憶=神の監視 + 相互監視) are fork-only. **As of v3.5 (ADR-2606182359 wave3) ALL of §2 is OBJECTIVE-FUNCTION ASSESSED — the categorical hard-floor / 掟リスト is ELIMINATED (固定するのは掟でなく priority, 最後まで)**. The ONLY non-negotiable is the priority itself: catastrophic harm to a child/descendant dimension is vetoed by the objective function's non-linear CATASTROPHE term (a property of the function, not a separate rule; `objective-function.edn`). The former hard-floor cores (§2(a) aggression/lethal/covert, §2(c) asymmetric-unwatched surveillance, §2(j) forced labor, §2(k) CSAM) reach :non-aligned via that term through their extreme child/descendant scores (by evaluation, not enumeration). §2(g) strict-individualist ontology is now ALSO objective-function-assessed (its wave2 retention is LIFTED): an org is judged by actual effect, not declared doctrine — 反個人主義 is enforced dynamically by collective-commons, not by doctrine-based exclusion. The prior waves moved §2(d) fossil (net carbon balance; fossil per se NOT prohibited), §2(i) compute (Murakumo DEFAULT-PREFERRED, not a ban), §2(b) finance, §2(e) specialist knowledge, and the economic structure (non-profit/donation/ad/equity/open-source) — all to net-effect scoring. The 子孫 + 反個人主義 priorities are unchanged; only the instrument moved from fixed rules to score evaluation. Honest enforceability limit: ADR-2606182359 §D6 (with no enumerated bright-line, the anchor is the catastrophe term + Apache; counsel may re-state CSAM as an explicit bright-line per jurisdiction). Privacy is preserved by encryption (暗号化≠忘却), not by forgetting. Do not re-introduce a blanket "all personal-data collection / all watching" surveillance ban — per ADR-2606082400 (v3.1), §2(c) is on the **RECIPROCITY axis**: **monetized OR asymmetric (watcher-unwatched) surveillance is prohibited; reciprocal/symmetric 相互監視 (村社会 deterrence + anti-isolation, the social form of 神の監視) is AFFIRMED**. Privacy is preserved by encryption (暗号化≠忘却), not by forgetting. Do not re-introduce a blanket "commercial mining / resource extraction" ban — per ADR-2606161700 (v3.2), §2(l) is on the **多世代(子・孫)×Wellbecoming RISK axis**: **採掘・採油そのものは禁止ではない**; prohibited only where the measured multi-generational impact is irreversible habitat/biosphere harm (the §2(d) standard) OR monopoly/chokepoint entrenchment (§1.12). Judge by measured harm to 子孫, never by industry name. Closed-loop recycling / urban mining and stewarded non-monopolistic recovery are affirmed.
- Do not add Charter Rider to 3rd-party vendored code (`lib/`, `vendor/`, `*-fork/`). Apache 2.0 §4 requires preserving original NOTICE of 3rd-party works. `charter-rider-applicator` already skips these patterns.
- Do not introduce `transfer()` / `burn()` / `setOwner()` to `LandRegistry.sol`. Donated land is constitutionally inalienable (waqf-equivalent, ADR-2605192245).
- Do not propose proprietary 兵器設計 or covert force operations. ADR-2605192100 §1.12.B constitutional invariant requires open-source + on-chain 監視 + 1 SBT = 1 vote.
- Do not include the Book of Revelation (黙示録/啓示の書) or eschatological content as religious doctrine. Per ADR-2605192100 §1.15, etzhayyim is non-eschatological.
- Do not commit secrets. Private DID key lives in macOS Keychain (`service=etzhayyim, account=DID_PRIVATE_KEY_ED25519`) + 1Password mirror.
- Do not gate an actor's **autonomous publication / social posting** behind per-post operator or Council prior restraint — actors POST autonomously BY DEFAULT (ADR-2606281500, 種をまく / seed-and-grow doctrine; founder Lv7+ 2026-06-28). We sow the seed (rails + actor); growth (speech) belongs to each actor. The seed (NOT lifted): self-`did:key` present-only + **revocable member CACAO leash** (the off-switch, ADR-2606111400), append-only public log (相互監視), Rider §2 catastrophe-veto content scan before emit, no person-targeting/no manipulation, Murakumo-default narration. Oversight = post-hoc transparency + leash revocation. **But DO keep the human/Council gates on high-stakes ACTUATION** (publication ≠ actuation): moving funds/trades, granting access/permissions, deletes, binding votes, live physical actuation near persons, and domain actuations like kyoninka's robotaxi-launch sign-off / no-permit-grant. An actor may autonomously *say* it assessed a deployment launch-ready; it may not autonomously *launch the cars*.
- Compute sourcing is OBJECTIVE-FUNCTION ASSESSED, not a categorical ban (Rider v3.3 §2(i), ADR-2606172359 — supersedes the ADR-2605215000 "Murakumo-only / no commercial GPU" invariant). The Murakumo fleet (LiteLLM 127.0.0.1:4000 + EVO-X2 LAN 192.168.1.70 + per-node Ollama gemma3:4b) remains the **DEFAULT and PREFERRED** path — transparent, self-hosted, low lock-in, high-scoring — but commercial GPU (RunPod / Vertex / Bedrock / etc.) is no longer per-se prohibited: it is scored by the ECL objective function on lock-in (collective-commons), transparency (reciprocal-transparency), and carbon (descendant-wellbecoming). Opaque, lock-in-creating, proprietary-dependence compute scores negative; transparent low-lock-in commercial compute serving descendant wellbecoming (open-model research, in-kind donated compute per ADR-2606012100) can score Aligned. Prefer Murakumo by default; justify any commercial-compute path by the objective-function dimensions, not by vendor name. (Downstream sweep of the ~30 actor ADRs still citing the old invariant is a follow-up per ADR-2606172359 D5.)
- Do not rename `etzhayyim-*` identifiers in `50-infra/cluster/murakumo/` or `40-engine/kotoba/crates/kotoba-kotodama/py/` outside the Step 8 cutover wave. Per ADR-2605214000 §3 + ADR-2605215000 §4, the renames are itemised in `MIGRATION-NOTES.md` files and must execute as one atomic PR after legal registration (repo-root CLAUDE.md §Status row 8). Partial rename breaks runtime (env vars + config dir + DNS suffix are interdependent).

## Baien tooling index (2026-05-23 wave)

| Tool / dir | Purpose | Key ADR |
|---|---|---|
| `e7m bench micro` | 15-prompt verifiable smoke for baien (`70-tools/scripts/bench/baien-microbench/`) | ADR-2605092350 |
| `e7m bench core4` | lm-eval-harness Core 4 (IFEval / GPQA / MMLU-Redux / Global PIQA) via `lm_eval_wrapper.py` (inductor probe suppressed) | — |
| `e7m bench distill` | LangGraph ReAct loop: analyze → fetch_dataset (HF, default) **or** select_teacher (fallback) → SFT (peft+trl LoRA on bf16 master) → microbench eval. `commit_node` appends `90-docs/baien/distilled-models.jsonl` → codegen → `llm-model-registry-distilled.ts` | ADR-2605231300 |
| `e7m bench rope-extend` | Stage 1 of context extension — runs `microbench_long.py` under 3 RoPE configs (baseline / linear×4 / NTK×4) and emits side-by-side pass matrix | ADR-2605231600 |
| `bgp-submit --generator hunyuan3d|pixal3d` | baien-graft 3D dataset pipeline; two generators (Hunyuan3D-2 default / TencentARC Pixal3D-T cascade@512) | ADR-2605202115 |
| `etzhayyim_organism.sensors.charter_rider.scan()` | Canonical §2(a)..(h) content scanner; used by `baien-distill.validate` and recommended for any pipeline that ingests / generates text into first-party artifacts | ADR-2605192200 |
| `70-tools/scripts/llm-registry/gen-distilled-entries.mjs` | distilled-models.jsonl → `llm-model-registry-distilled.ts` codegen (2-phase ship: manifest + reviewer-gated TS) | — |

Snapshot artifacts (run results) live under `90-docs/baien/`:
- `frontier-bench-snapshot-260523.md` — frontier-LLM §A reference + baien microbench results
- `context-extend-snapshot-260523.md` — rope-extend Stage 1 results stub (awaits run)
- `distilled-models.jsonl` — committed adapter manifest (codegen source)

## Future Work

- **lefthook hooks** full set (adr-validate, docs-registry, lint-dangerous-query, llm-model-ssot)
- **GitHub Actions CI**: lint / type-check / build / test per layer
- **Dependabot** for npm + cargo + uv ecosystems
- **`90-docs/_registry/docs.json`** generator + validator
- **baien-distill Stage 2/3** (YaRN + LoRA, LongRoPE continual) per ADR-2605231600
- **Core 3 bench strategy revision** — switch from `_generative` (~28h/task) to `_completions` (loglikelihood, ~30 min) for next baien snapshot

## Substrate boundary (engineering rules — NOT charter; ADRs 2605172000 + 2605172100 + religious-corp wave)

> **These are IMPLEMENTATION decisions, not constitutional doctrine (per ADR-2606182359 lineage).**
> The charter holds only **Tier-0 priorities (子・孫 wellbecoming / collective-over-individual /
> 永久記憶) + the ECL objective function**. Substrate/engineering choices — kotoba (no RisingWave),
> the `payment.sent.purpose` enum, ad-blocklists, Murakumo-as-default, kotoba-as-canonical-state —
> are **工学・governance 判断であって憲法ではない**: they can change at the implementation/governance
> layer **without a charter amendment** (固定するのは priority; 手段=実装は固定しない). "CRITICAL"
> below means "load-bearing engineering rule," not "Tier-0 immutable." Do not elevate a substrate
> choice to charter status.

This repo is **blockchain-self-contained**. The rows below are **NOT uniform** — classify each before treating it as immutable (固定するのは priority):

Layer scored in `70-tools/scripts/charter/layer-classification.edn` + `classify.bb` (5 axes, J 0–10; `bb classify.bb`):

- **Tier-0 (fork-only — the priorities themselves + the few constitutional invariants)**: `Land trust` inalienability (J=9.05, donated land = waqf-equivalent, ADR-2605192245) · `Religious force` transparency invariant (§1.12.B) · `Content` no-CSAM = the catastrophe term (= priority non-negotiable). The four Tier-0 priorities (子孫 wellbecoming / collective / 永久記憶) sit above all of these.
- **Tier-1 (derived policy — Council Lv7+)**: `License` default (Apache+Rider → ECL, J=8.40) · `Charter compliance` (3-tier) · **`Confidentiality` PRINCIPLE (暗号化≠忘却, J=7.95 — Tier-0-derived from permanent-memory; promoted from 実装 per the score)** · **`Server-side signing` / no-server-key (J=6.90 — non-custody/trustless invariant; promoted from 実装)** · `Content` Eros doctrine (consenting-adult only).
- **目的関数 (objective-function-assessed, v3.5)**: `Advertising` · `GPU / inference` · the external-commercial line of `Payment purpose` (donation-spirit). These **score**, they do not categorically ban.
- **実装 (engineering / governance — changeable WITHOUT a charter amendment)**: `State`/`Substrate` engine (kotoba, J=2.90) · `Read path` (kqe) · `Substrate client imports` (`@etzhayyim/sdk`) · `Payment` rails (USDC/Base/ERC-4337 mechanics) · `Identity` mechanics (DID-centric identity is the Tier-1 principle; the specific methods are impl) · `Confidentiality` **cipher** (XChaCha20/Signal choice, J=2.10) · `Server-side signing` **mechanics** (which keys/Safe, vs the no-server-key principle above) · kotoba (J=1.85).

A "Prohibited" cell in an **実装** row means "wrong engineering choice today," NOT "constitutional violation" — it changes at the implementation/governance layer. Tier-0 is immutable (fork-only); Tier-1 amends by Council Lv7+; 実装 changes freely. Note the principle/mechanism split: the *no-server-key principle* and the *encryption-not-forgetting principle* are Tier-1, but the *specific cipher / which-keys* are 実装. Engineering rules (changeable at the implementation layer):

| Concern | Allowed | Prohibited |
|---|---|---|
| State | **kotoba Datom log** (content-addressed EAVT Datalog — Datomic-isomorphic; FIRST-CLASS canonical state, ADR-2605312345) — subordinate layers: IPFS = block backend (CIDv1 cold tier/DHT) · AT Protocol MST = ingress/interop wire · Base L2 = trust anchor over commit-DAG root | RisingWave / Postgres / Kysely / centralized DB; treating MST/IPFS as the canonical state home (they materialize the Datom log, not vice-versa) |
| Payment | USDC on Base L2 + ERC-4337 Smart Account + TitheRouter (10% auto-split). **Donation media expansion (ADR-2606111800)**: (§A) fiat IN-KIND — paying the mission's fiat infra bills (servers/cloud/domains/hardware) direct to a vendor, in-kind, never touching etzhayyim's money layer; (§B) NON-CUSTODIAL fiat on-ramp settling immediately to USDC on-chain (no held balance, no donor PII, processor can't freeze); (§C) curated crypto allowlist held as-is (ETH/WETH + USDC/USDT/DAI), per-asset tithe | **CUSTODIAL** fiat processors (Stripe / PayPal / Square — holding an etzhayyim balance / able to freeze / KYC-on-etzhayyim / retaining donor PII), incl. `com.etzhayyim.apps.stripe.*`; non-donation fiat; memecoins / algorithmic stablecoins |
| Payment purpose | `donation` / `kisha` / `grant` / `tithe` / `escrow-refund` + (SBT↔SBT internal carve-out) `internal-purchase` / `internal-subscription` / `internal-promo` | `subscription` / `purchase` / `tip` for external; commercial sale for SaaS tier |
| Advertising | OBJECTIVE-FUNCTION ASSESSED (ADR-2606180001 v3.4): 旧「広告排除」は net 評価へ。attention 搾取・非対称データ収集を伴う広告は ko-wellbecoming/reciprocal-transparency で負 | attention 搾取的・surveillance 型広告 (scores negative)。NOT a categorical 第三者広告 ban — 非搾取・透過・consent ベースの告知は net 評価 |
| Identity | did:web:etzhayyim.com + did:plc + WebAuthn passkey + Adherent SBT | server-issued JWTs without DID binding |
| License **[憲法]** | Apache 2.0 + Charter Compliance Rider v3.5 (`/CHARTER-RIDER.md`, per ADR-2606062100 → 2606182359; → ECL objective-function, ADR-2606172300) | Apache 2.0 alone (Rider required) / proprietary / no-NOTICE |
| Charter compliance (3-tier) | ChartersComplianceRegistry attestation flow + Council Lv6+ ≥3 multisig | bypass of three-tier enforcement (L1 license / L2 便益拒否 / L3 評価=0) |
| Land trust | etzhayyim Land Registry 4-layer (Base L2 NFT + geth-private + IPFS + LANDS.md) | transfer / burn / sale / private ownership of donated land |
| Religious force | Transparent (on-chain log + open-source + 1 SBT = 1 vote) | Proprietary / covert / independent military arm / state military alliance |
| Content (Eros) | 合意ある成人性表現 (産霊 / 雅歌 整合) | 児童性的表現 / 非合意 / Wellbecoming 違反 addictive design |
| Content (Gore) | 教育 / 歴史 / 宗教 / 人権告発 文脈の暴力 imagery のみ | 無目的暴力 entertainment / desensitization 設計 |
| Confidentiality (ADR-2605181100) | `com.etzhayyim.encrypted.*` (XChaCha20-Poly1305 envelope + Signal-wrapped per-recipient keys, DID-bound) | Plaintext private records on MST / app-side libsignal / noble-ciphers imports |
| Substrate client imports | Only via `@etzhayyim/sdk` | Direct `@atproto/api` / `viem` / IPFS client / `@noble/ciphers` / `@signalapp/libsignal-client` from app code |
| Substrate engine | `kotoba` (`40-engine/kotoba/`, ADR-2605262130) — content-addressed Datalog + Pregel + Signal + WASM Component Model in one Rust workspace; 17 crates; canonical impl of every substrate primitive (supersedes kotoba-datomic composition spec ADR-2605231400). Its **Datom log is the first-class canonical state** (ADR-2605312345), not merely the read engine | inventing a parallel substrate engine name without ADR; importing kotoba-datomic / RW / Lance / Iroh as projection backends |
| Read path (hot-path queries) | `kotoba-kqe` arrangements (EAVT / AEVT / AVET / VAET) directly over the canonical Datom log (content-addressed blocks; ADR-2605312345); no separate projection layer (ADR-2605262130 D7 + N8 supersedes ADR-2605231500 kotoba-datomic-projection rules). First L1-projection app `feed-discover` (50-infra/mst-projector/projection/, ADR-2605231902) preserved unchanged; read backend migrates to kotoba-kqe at Phase 2.5 | RisingWave / Postgres / Lance / DuckDB / SQLite as projection or cache; "kotoba-datomic-projection" marker comments — both deprecated by ADR-2605262130 |
| Server-side signing capability (ADR-2605231525, Council ratify pending) | Member wallet sign (USDC), member passkey-derived ES256 (session), community-operator DID (bulk-ingest), Council 5-of-7 Safe (governance), read-only RPC / firehose subscribe / IPFS pin / static asset serve. **An ACTOR holding its OWN self-generated did:key (seed sealed in Keychain/1Password, present-only, autonomous writes attributed to a consenting human via a member CACAO leash) is permitted — custody stays off-platform (kaname/kanae/ibuki/tsubasa pattern).** | Any platform-held private key, master credential, or signing token in etzhayyim-operated Workers / pods / CronJobs / CI / hosted bots. Exemption: `// no-server-key: read-only` marker on documented Stage handover rollback windows. **CLARIFICATION (ADR-2606072802): no-server-key bars a CUSTODIAL UNILATERAL signing key — it is NOT a ban on automation, and READ-ONLY ops are exempt. An actor MAY autonomously do read-only public fetches itself (no key, no operator); do NOT gate a read-only PUBLIC ingest behind an operator step.** **AUTONOMOUS PUBLICATION (ADR-2606281500, 種をまく doctrine, founder Lv7+ 2026-06-28): actors PUBLISH/POST autonomously BY DEFAULT — no per-post operator/Council prior restraint. Bounded by the seed (rails, NOT lifted): self-`did:key` present-only + revocable member CACAO leash (the off-switch, ADR-2606111400), append-only public log (相互監視), Rider §2 catastrophe-veto content scan before emit, no person-targeting, Murakumo-default narration. Oversight = post-hoc transparency + leash revocation, not pre-approval. PUBLICATION ≠ ACTUATION: high-stakes real-world acts (funds/trades, permission/access grants, deletes, binding votes, live physical actuation, kyoninka robotaxi-launch sign-off) KEEP their human/Council gates.** Enforced by `e7m verify` (9th invariant) |
| GPU / inference | Murakumo fleet = DEFAULT-PREFERRED (LiteLLM 127.0.0.1:4000 + EVO-X2 LAN 192.168.1.70 + per-node Mac mini Ollama gemma3:4b). Compute sourcing is OBJECTIVE-FUNCTION ASSESSED per Rider v3.3 §2(i) / ADR-2606172359 (supersedes ADR-2605215000): scored by lock-in / transparency / carbon | Opaque, lock-in-creating, or proprietary-dependence commercial compute (scores negative). NOT a categorical vendor ban — transparent low-lock-in commercial GPU serving descendant wellbecoming can score Aligned; justify by the objective-function dimensions, not vendor name |

Apps that need fiat / paid features call an external backend via XRPC consent-capability (progressive enhancement, **non-profit領収書 用途のみ** per ADR-2605192115 §4). Open app remains operational without it.

## SSoT pointers

- `deps.toml` — operating entity, substrate rules, L2 contracts, DNS records, ADR registry, module registry
- `90-docs/CLAUDE.md` — docs system rules + ADR placement policy
- `90-docs/adr/README.md` — ADR index
- `90-docs/adr/2605192100-etzhayyim-mission-charter.md` — religious-corp 上位憲章 (mission + constitutional constants)
- `90-docs/adr/2605192200-etzhayyim-ip-free-release-charter-rider.md` — License + Rider 正本 spec (v2.0)
- `90-docs/adr/2605192245-etzhayyim-global-land-sovereignty.md` — Land Trust 4-layer architecture
- `90-docs/adr/2605192415-etzhayyim-religious-corp-daemon-architecture.md` — Pregel cell catalog + Murakumo deployment
- `/CHARTER-RIDER.md` — license addendum canonical text
- `/LANDS.md` — Land Trust roster
- `/MEMBERS.md` — 信者 roster
- `50-infra/murakumo/fleet.edn` — religious-corp cell placement (10 nodes × 15 cells). NB: cron/lan-api heartbeat RESIDENCY (defined here + in `50-infra/cluster/murakumo/cell-runner/cells.edn`) is verified live with `bb fleet:probe` — definition ≠ running daemon
- `20-actors/etzhayyim-sdk/README.md` — SDK API surface + hard rules
- `40-engine/kotoba/crates/kotoba-kotodama/cells/README.md` — religious-corp Pregel cell catalog
- `90-docs/adr/2605262130-kotoba-storage-substrate-unification.md` — canonical storage substrate engine (kotoba); supersedes kotoba-datomic composition + projection layers; no RisingWave
- `90-docs/adr/2605312345-kotoba-datom-first-class-canonical-state.md` — kotoba Datom log = first-class canonical state; IPFS = block backend, MST = ingress/interop wire, Base L2 = trust anchor (clarifies 2605262130 layering)
- `40-engine/kotoba/README.md` — kotoba upstream README (17 crates)
- `10-protocol/kotoba-datomic/SPEC.md` — (superseded by ADR-2605262130; deprecation banner Phase 0.5; retained one R-cycle then archived)
- `90-docs/adr/2605231400-kotoba-datomic-holochain-iso-substrate.md` — (superseded by ADR-2605262130)
- `90-docs/adr/2605231500-kotoba-datomic-projection.md` — (superseded by ADR-2605262130; no projection layer under kotoba)
- `90-docs/adr/2605231902-feed-post-membrane-and-feed-discover-projection.md` — first end-to-end MST §4 membrane + L1-projection app (app.bsky.feed.post); **preserved unchanged**; Phase 2.5 read-path migration to kotoba-kqe per ADR-2605262130
- `90-docs/adr/2605214000-etzhayyim-murakumo-mesh-no-vke-and-lexicon-port-rules.md` — Murakumo distributed cluster (no-VKE mesh) + lexicon port verdict taxonomy
- `90-docs/adr/2605215000-etzhayyim-inference-murakumo-only-no-runpod.md` — etzhayyim inference Murakumo-fleet-only (no RunPod)

## References

- This repo (public): https://github.com/etzhayyim/root
- Domain landing: https://etzhayyim.com
- DID resolver: https://etzhayyim.com/.well-known/did.json (LIVE; CF Worker at zone `etzhayyim.com`, DNS AAAA `@` `100::` proxied, deployed 2026-05-17T03:25Z)
